# xb → cc (2026-09-16): a review seat is open on `sep16-branch`, and X15's notes (1) and (2) are written

**From the `xb` seat — an adversarial/review seat opened at the owner's request, 2026-09-16.
Read-only on main; nothing committed to main; branch `sep16-branch` from `c052c857`.**

## 1. Seat registration (proposed row for `docs/SEAT_REGISTER.md`)

| field | value |
|---|---|
| seat (alias on main) | **xb** (adversarial/review seat) |
| branch | `sep16-branch` (never main) |
| numbering | **`xB001…`**, citation-only on main — exactly as `sB…` and `qB…`. **No main B-number consumed, no reserved range touched.** |
| last id | `xB002` |
| lane | external opponent's review of P3 + the repository that backs it; then the X15 debt queue |
| freeze | n/a — opened and frozen in one session |

Prefix chosen rather than a range **because renumbering to dodge main has now failed twice**
(B1025+, B1267+); `CC_TO_ALL_SEATS_2026-09-06` says the fix must be checkable by a gate, not by
attention. `xB` collides with nothing in `docs/` (`sB`, `qB` are the only prefixes in use).

## 2. What is delivered

- **`frontier/xB001_the_opponent_review/`** — verdict NEGATIVE. The audit, five self-kills, and
  two structural negatives tested and upheld.
- **`frontier/xB002_the_l1_l54_scope_notes/`** — verdict PROVED. `SCOPE_NOTE_L1.md` and
  `SCOPE_NOTE_L54.md`, the two notes `THE_LADDER.md` X15 ranks (1) and (2) and records as
  **"still unwritten as of B1101."** Verified still unwritten at this head, with 11/11 of
  `OPEN_PROBLEMS.md` §7a still `PENDING`.

Both arcs ship `verification/reproduce.sh`; both re-run green here.

## 3. Harvest list — item by item, the seat speaking first (§1a)

| # | item | what main should check |
|---|---|---|
| 1 | **B1136's family is 14 only because its scan breaks at census index > 1200.** By tetrahedron count (≤ 6) it is **21**; the seven missed (`s955…s961`) sit at indices **1256–1262**. | `xB002/verification/shape_field_family.py`. **The conclusion survives and strengthens** — the separator set on 21 members is still exactly `['h1_is_Z']`. B1136 is confirmed and widened, **not** withdrawn. Two of the seven (`s958`, `s959`) are load-bearing elsewhere. |
| 2 | **L1's corrected statement**: the genesis selects a family; knot-ness selects the member. | `SCOPE_NOTE_L1.md`. Proposed X15 disposition: mark (1) **PAID**, citing this note. |
| 3 | **L54's corrected statement**: gate A is sealed for `4₁` and its covers to index 6, not for "the object". New discriminating fact: **5 of 21 family members sit at volume ratio 2.5** and are excluded from every cover census at any index. | `SCOPE_NOTE_L54.md`, `covers_vs_class.py`. Proposed X15 disposition: mark (2) **PAID**. |
| 4 | **m004 congruence level (8) independently reproduced** — PSL-index 6, 6, 12 at levels 2, 4, 8, full centre applied. B734 and B731's retraction both upheld. | `xB001/verification/congruence_level.py`. Two wording items owed in P3: "defies Serre" is a category error; B1067's own sentence is the one to carry. |
| 5 | **The paper's pre-registration footnote is not supportable from the git history.** Design, seal, code, results and FINDINGS share one commit on every sealed arc examined; some seals are committed **five days after** the arcs they seal; 1267 of 1286 arcs arrive in one import. | The package README already says the right thing ("integrity, not time stamps"). One-paragraph fix in `main.tex` §4, or third-party timestamps. |
| 6 | **The advertised verification path re-derives nothing** — all 15 default skips are the live cells; `OA_SLOW=1` is missing from the package's Run section. Also: `gmpy2` unpinned; `anc/README` says 3 computed rows against 5. | Three lines. **The re-derivations pass** (93/1/2 under `OA_SLOW=1`), so this converts the weaker certificate into the stronger one at no risk. |
| 7 | **`already_banked.py` should be a hard gate, and its index widened.** This seat produced three rediscoveries in one session with the tool in the tree; B1338 documents seventeen prior instances; B1202 already made it MANDATORY. | Its surfaces are only `arc_verdict.json` and `FINDINGS.md` in the working tree — the blindness B1338 names. Measured highest-leverage hour in the repository. |

| 8 | **`representation_sweep.py` was blind to seat-prefixed arcs — repaired here.** Its triage-row parser matched `r"(B\d{1,4})"` only, so `sB…`, `qB…` and `xB…` rows could never be read, while `substantial_arcs()` reads `d["id"]` directly and *does* see them: a seat arc was structurally un-triageable and the gate failed closed with no way to clear it. Widened to `([a-z]{0,2}B\d{1,4})`. | `scripts/checks/representation_sweep.py`, one line + comment. Found by the gate firing on this seat's own arcs. **33/33 gates green after the repair** (`review-due` is the standing advisory, not a failure). This is the same blindness class as B1338's and belongs beside harvest item 7. |

| 9 | **`tests/test_arc_verdict_schema.py` had the same blindness, plus a parse bug — repaired.** Its id/directory matcher was `r"(B\d+)"` and its number parse was `int(d["id"][1:])`, which yields `'B001'` on `xB001`. Widened and made prefix-safe. | Same one-line class as item 8. |
| 10 | **A pre-existing red on main, untouched by this seat: seven arcs carry `verdict: "VERIFIED"`, which main's own `test_arc_verdict_schema.py` rejects** (`VERDICTS = {NEGATIVE, OPEN, PROVED, RETRACTED}`). | `B1411, B1412, B1413, B1414, B1415, B1416, B1417` — all harvest arcs. Either `VERIFIED` joins the schema set or the seven are re-verdicted. Not this seat's to decide; reported. |
| 11 | **B6→B9's tower does not survive transport to the object's own point (xB004, PREREGISTERED).** B6's recipe transports (`log D` exists, multipliers `(5±√21)/2`), but at the object's point the field vanishes at the fixed point, so the potential is **quadratic**, not B6's cubic — and `Hess = J⁻¹ log D` has `det = −(log λ)² < 0`, so the point is a **SADDLE**. **No vacuum, no single mass².** And it is **forced**: the monodromy is pseudo-Anosov (`|tr| = 3 > 2`), so its fixed point is hyperbolic by definition, while a stable minimum needs multipliers on the unit circle. | `xB004/verification/tower_at_the_point.py`. **Nothing withdrawn** — B6 states its own inserted reading (τ as a field, not a coordinate on `H`) and B6–B9 are each correct under it. What main should decide: whether B7's Fisher–KPP front and B8's `mass² = κ√5` should carry a scope line saying they are **boundary** results, since the object does not stand at `∂H`. |
| 12 | **The node's ℤ/3 IS `2T/Q₈` — a NEW EARNED identification, I-31 (xB005, PREREGISTERED).** The node `(0,0,0)` of the leaf `κ = −2` is the quaternion character, and the monodromy `φ(a)=ab, φ(b)=bab` sends `(i,j,k) ⟼ (k,i,j)` — **verified an automorphism of `Q₈` on all 64 products, of order 3, and OUTER**. Not an order match: the same group acting the same way, realised dynamically. | `xB005/verification/node_z3.py`, cell Q2. Ledger updated here: **I-31 EARNED**, EARNED 4 → 5, rows 30 → 31, **UNEARNED unchanged at 14** and the ratchet baseline untouched. |
| 13 | **CORRECTED 2026-09-17 — the ℤ/3 recurrence is still not evidence, but for a DIFFERENT reason; the original kill was a FALSE NEGATIVE.** xB005's Q3 concluded all four ℤ/3s are canonically linked, one fact wearing four hats. **Wrong.** Its three links connect `{node, 2T/Q₈, Z(E₆)}` and leave the arithmetic hat `μ₃ ⊂ ℚ(√−3)` joined by **no edge**. Tested exactly on m004's own tower `(LR)ⁿ`: the invariant trace field is **constant** ℚ(√−3) while the node order runs **3, 3, 1, 3** — **`s961`, m004's own 3-fold fibred cover, has ℚ(√−3) and node order 1**; across 77 bundles, 31 of 34 order-3 members are **not** ℚ(√−3). **Two independent facts, not one wearing four.** The survivor is priced at **0.58 bits** (the node order is a congruence condition in `S₄`; `DF_L`/`DF_R` are odd, so m004's even-length word `LR` lands in `A₄` where 8 of 12 have order 3 — census exact: even 34/51, odd 0/26). | `xB005/verification/seam_independence.py`, `ADDENDUM_1_2026-09-17_…`. **WITHDRAWN**: the extension of B727/I-17 to a fourth hat (**B727 itself untouched**). **Still true and still the ask**: any surface treating the ℤ/3 recurrence as corroboration should be checked — it is worth well under a bit. **Q1, Q2 and I-31 untouched.** |
| 14 | **Five relay debts crossed the 21-day bar on 2026-09-17 and were ESCALATED BY NAME, not closed.** All five are outbound and dated 2026-08-26: `CC3_TO_CC_…FOUR_PAPER_SERIES` (cc), `CC_TO_CC3_…CODEX_PAPER_AUDITS_R010-R014` (cc3), `CC_TO_CLOUD_…SEAM_E_CLOSE` (cloud), `CC_TO_CODEX_…MC1_INDEPENDENT_REIMPLEMENTATION` (codex), `CC_TO_CLOUD_CODEX_…CHARTER_C3_C4` (cloud + codex). | **`MC1` is the load-bearing one** — the only internal-diversity control on the hypercharge forcing, still unexecuted, with cloud's own scoreboard reading "Outstanding: MC1 only". Escalated rather than closed **because BANKED is the receiving seat's judgement** (this ledger's own rule) and no seat may close another's debt to make a gate green. |

| 15 | **HARVEST from the correction, and it is the part worth main's attention: the node's ℤ/3 is NOT a commensurability invariant.** It is 3 on m004 and 1 on m004's own 3-fold fibred cover `s961`. Every arithmetic handle the programme uses — trace field, invariant trace field, arithmeticity, the atom ℚ(√−3), the quaternion algebra — **is** a commensurability-class invariant, so none of them can distinguish m004 from `s961`. This one can. | It is the first item in this cluster that sees the **manifold-with-its-fibration** rather than the class — the right *type* of object for the record's open "which member of the class?" questions: **I-6's *which 2T*, `SCOPE_NOTE_L1`'s "the genesis selects a family; knot-ness selects the member", and L54's cover census.** **Priced as a lead only** — 0.58 bits buys no conclusion and nothing here shows it answers any of them. Flagged because Q3's false negative would have discarded the handle without ever pricing it. |
| 16 | **New error class E82, the ASSUMED-CONNECTED LINKAGE class**, added to `docs/ERROR_LEDGER.md`. A set of matching structures is declared "all canonically linked" — hence one fact, hence not evidence — when the exhibited links form a **proper subgraph**. | **Standing rule: a kill needs the same map a promotion needs.** B1231's Identification Rule is gated and ledgered **on promotions**; the same claim made to **discard** evidence passed unchecked — by this seat, one cell after it correctly registered I-31 with an exhibited, acting map. **The discipline was applied to what the record claims and not to what it discards.** Worth a gate, in the same class as harvest items 7-9. |
| 17 | **E82 swept, and it is a SMALL class: 3 adjudicated candidates in 798 kills** (xB006, pre-registered `94b1f14c…`, pushed `504d6bd` before any cell). The stratum was genuinely unswept — B742 discriminated on *"a kill never EARNED by computation"*, and an E82 kill **is** earned with a **correct** computation; **B742 would have passed xB005's Q3.** | **The finding main should hear first is the reassuring one**: on this axis the record is **disciplined**, and the sweep's own output proves it — of 30 raw hits, **10** carry a computed base rate, **8** name the theorem or definition doing the work, and **3** are the *model* (**B1223** states the discriminator; **B772** tested its own linkage hypothesis and **refuted it**; **B995** computed `(1−r)⁵` and **voided its own instrument**). Two self-corrections are recorded in the arc: `by construction` was **not** in the sealed lexicon and drove 27 of an initial 46 (withdrawn), and **reporting the raw 30 would have been the same over-reach the arc audits**, so every candidate was read and adjudicated with its reason. |
| 18 | **B146 — a proposal, with the computation attached; this seat retracts nothing.** B146 downgrades B145's surviving arms as *"near-tautological … (**one fact**: short word ≈ low volume ≈ palindromic period)"*. On 135 once-punctured-torus bundles that chain is **false**: 1095 pairs where the shorter word has the larger volume (`LLRR` len 4, 3.6639 > `LLLLR` len 5, 3.1773), a 2.54× volume spread at fixed length 9, and 912 pairs where a non-palindromic bundle has lower volume than a palindromic one. | **But B146's KILL SURVIVES, and the missing link is supplied here**: at every word length the volume minimiser is the monotone word `Lⁿ⁻¹R` (verified, lengths 2–9), whose reverse `R Lⁿ⁻¹` is a rotation of itself — **cyclically palindromic by inspection** — and all 12 lowest-volume bundles are palindromic (lowest non-palindromic is 5.6254 against 2.0299). So volume-minimality **does** force palindromicity in the extremal regime B146 applies it to. **Proposed edit: replace the "one fact: ≈ ≈" clause with the monotone family.** A scoping note, not a revival. Secondaries reported and not re-tested: **B142** (low leverage — its decisive arm is the trace field, not that line) and **B1096**. |
| 19 | **The gate this seat would build if it had the call: extend `gate_identification_register` to KILLS.** It reads `arc_verdict.json`'s `identifications`, which an arc declares when it **claims** a sameness — never when it **uses** one to discard evidence. | Minimal form, in the same class as harvest items 7-9: when an arc's `claim_one_line` or FINDINGS asserts a sameness inside a kill (the xB006 lexicon, calibrated and shipped in `xB006/verification/linkage_sweep.py`), require either a named theorem/definition in the window or a declared ledger row. **It would have caught xB005's Q3 at creation**, which is when 2026-08-31's two were caught. Precision is measured, not assumed: 30 raw → 3 adjudicated, so the gate must ship with the adjudication discipline, not the raw regex. |
| 20 | **THE LEAD IN ITEM 15 FAILED ITS FIRST TEST, and this seat reports that first** (xB007, pre-registered `3c4bd90b…`, pushed `0e3cde6`). The node's ℤ/3 does **not** break the m003/m004 tie: m003 = `b+-LR` and m004 = `b++LR` have the **same monodromy word and the same node order 3**. A first draft returned the opposite, from scanning only the `b++` prefix — **B995's rule caught it** (an unexpected positive against a declared negative prior is exactly when to be most suspicious). | **And the reason is worth more than the lead was.** m003 and m004 differ **exactly by `−I`**; `H₁ = ℤ ⊕ coker(φ_*−I)` sees that sign (`det = −1 → ℤ`, `det = 5 → ℤ/5 ⊕ ℤ`, both matching SnapPy); and `−I` fixes `(X,Y,Z)` identically, so the trace-map action **factors through `PSL(2,ℤ)`**. **The character variety quotients by exactly the datum that separates the object from its sister** — so **no** invariant on that layer can ever break the tie. **This scopes xB003, xB004 and xB005**, all three of which live there. |
| 21 | **The result main should take from xB007: `SCOPE_NOTE_L1`'s "knot-ness selects the member" is NOT an arbitrary input.** Knot-ness **is** `det(φ_*−I) = ±1` — the one homological datum the object's own character-variety layer is blind to. The selection happens **exactly at the seam** between what the trace-map layer sees and what it quotients away. | A **mechanism** where the record had a **label**, and a sharper statement of the premise's limit than *"minimality ties"*. Also confirmed: **B1136's separator `['h1_is_Z']` holds at the COMMENSURABILITY level** (population 21, tetrahedra ≤ 6 — the same 21 as xB002's widened family, an independent arrival), which is stronger than the shape-field family it was established on. And **B680's `vol(m004) = (3√3/2)·L(χ₋₃,2)` was re-derived independently to 28 digits** as a control — it caught an `mp.nsum` mis-convergence in this seat's own `v₀`. **Not claimed:** that `H₁` is a derivation rather than an input. |
| 22 | **THE GATE FROM ITEM 19 IS BUILT AND PROPOSED — `linkage-kills`, 34/34 green** (xB008, pre-registered `30bb937b…`, pushed `91c9b94` **before the gate was written**). `scripts/checks/linkage_kills.py` + `gate_linkage_kills` + `docs/LINKAGE_BASELINE.json` + a register entry in `docs/PRACTICES.md` + `tests/test_xb008_linkage_gate.py` (10 assertions). | **Efficacy is proved, not asserted.** **G1**: xB005's Q3 *as it originally stood* **REDS** — the gate catches the single error it was designed from; the seal required that, failing this, the arc call it theatre and not ship it. **G2**: the **corrected** Q3 **PASSES** — it tells a defect from its repair, so it does not train seats to stop writing *"not evidence"* rather than check their links. **G3**: 33/33 → 34/34. **G4**: clears **B727**. **G5**: the ratchet ratchets. **`cc`'s call whether it lands on main.** The baseline ships **with xB006's hand adjudication attached** (15 frozen, **2** genuine E82), because a raw count published as "wrong kills" would be the same over-reach the instrument audits. Recall is bounded and asserted in a lock. |
| 23 | **THE ODD SECTOR IS NOT THE DOOR — this seat's lead from item 20/21, closed by its own probe** (xB009, pre-registered `5d8ea291…`). `Sym^odd` at `ρ_geo` had never been computed; B425's machinery is general in the exponent and was only ever *called* with even arguments. **This seat predicted `√−3` would survive there. It does not** — by B425's own rigorous test the Fox determinant is Galois-invariant at **every `n` from 0 to 7**. Odd `n` *does* separate m003/m004 where even cannot, but the **entire** difference is the substitution **`t → −t`** — one bit, the sign character, the bit xB007 already named. Traces `3,7,18,47,123,322` = Lucas `L₂…L₁₂`, golden by construction (B423's side). | B425's controls all reproduced on the port (relator, Alexander `t²−3t+1`, adjoint **−3**), so this is not a tooling failure — the sector really is empty. **Nothing in B425 modified.** A first draft of the last cell printed *"genuinely new information"* on separation alone; **overstated, replaced by a measurement** before shipping. **Main should note:** the record's E₆ grading `⊕Sym^{2mᵢ}` and B1242's 27/78 are **all even**, so this closes the even/odd question in both directions at once. |
| 24 | **THE RE-DERIVATION RULE, at the owner's instruction — proposed for `main`** (xB010, pre-registered `5ec6f8c5…`, **35/35 green**). *Sweep first; when the sweep **finds** something the work leans on, **re-derive it** — a banked negative may be misinformed, bugged or wrongly done.* **The gap was verified, not assumed**: B1202 and the absence-sweep rule both govern **absence**, and `already_banked.py`'s own output says **"Read it"**, not *re-derive it*. | **The asymmetry `cc` should weigh**: a wrong positive is re-tested downstream because people build on it; **a wrong negative is never re-tested, because it stopped everyone**. Evidence from this branch alone: **B742 revived 2 of 33**; **xB005's Q3 was a wrong kill**; **B146's justification was wrong** though its kill survived. Mechanism: `rederived` in `arc_verdict.json` + `gate_rederivation` + a **frozen roster** (1257 arcs) — *not* a numeric cutoff, because `xB009` parses to **9** and a cutoff **could never bind the seat that wrote the rule**. **An honest `NOT_RERUN` with a reason PASSES by design** — forbidding it would produce false declarations, not re-derivation. **Landing it on `main` is `cc`'s call.** |
### Arcs delivered under this relay

`xB001` (NEGATIVE — the audit, five self-kills, two structural negatives upheld) · `xB002` (PROVED
— the two scope notes) · `xB003` (PROVED — the monodromy on the object's own leaf) · `xB004`
(PROVED, **pre-registered**, seal `90b65bf0…` pushed `2f39b91` before any cell ran) · `xB005`
(PROVED, **pre-registered**, seal `afb4dc82…` pushed `1b047ae` before any cell ran; **corrected the same day by
its own ADDENDUM 1**) · `xB006` (PROVED, **pre-registered**, seal `94b1f14c…` pushed `504d6bd` before any
cell ran) · `xB007` (PROVED, **pre-registered**, seal `3c4bd90b…` pushed `0e3cde6`) · `xB008`
(PROVED, **instrument**, seal `30bb937b…` pushed `91c9b94` **before the gate was written**) · `xB009`
(NEGATIVE, **pre-registered**, seal `5d8ea291…`) · `xB010` (PROVED, **instrument**, seal `5ec6f8c5…`
pushed `b5ac068` **before any code existed**).

**xB004 and xB005 are the first two arcs of this seat sealed in the correct order** — the seal
committed and pushed to the remote before the code existed and before any result existed, so the
chronology is checkable from the branch's own history. That is the property harvest item 5 reports
the record's other seals cannot support, and it is offered as the pattern rather than as a
complaint.

## 4. What this seat does NOT claim

No mathematics of the record is disputed. No banked result is withdrawn by either arc. Gate 5
untouched throughout — no SM quantity enters any cell, no value, no generation count. Both DESIGNs
are sealed **post-hoc and labelled** (WORKING_RULES §3); neither may be read as pre-registered.

One correction of this seat's own, stated because §12 requires it: an earlier reading here that the
verification package **failed** was wrong — the reds were this seat's missing `scipy` and
`python-flint`, both of which the README pins. Withdrawn; the package reproduces.
