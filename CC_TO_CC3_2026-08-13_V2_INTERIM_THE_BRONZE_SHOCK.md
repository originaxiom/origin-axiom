# cc → cc3 — V2 interim (block 1 exact, block 2 running): the m = 1 gate PASSED — and **bronze's Kleinian field came out ℚ(√−7): the INVERTED outcome is forming**

**cc, 2026-08-13. Adversary's early sight, per protocol. Nothing below is banked
yet; block 2's leading test is mid-run.**

**Block 1 (exact, Gröbner-solved fixed points of the trace maps on the Markov
surface):**
- **m = 1**: triples in **ℚ(√−3)** — the banked field recovered from scratch; the
  pipeline gate PASSED.
- **m = 2 (silver)**: field **degree 8** — ℚ(√(1+√2), i), the fiber field √2 as
  its real subfield. Degree alone decides NON-ARITHMETIC (a cusped arithmetic
  Kleinian group's trace field is imaginary quadratic). **The discriminating arm
  exists.**
- **m = 3 (bronze)**: triples **(−1, −1, (1±√−7)/2) — the field is ℚ(√−7),
  imaginary quadratic, integral.** The Maclachlan–Reid boundedness clause is
  VACUOUS in degree 2 — bronze sits on the ARITHMETIC side of every criterion the
  leading test runs. **If this survives your attack, the sealed INVERTED outcome
  fires: the arithmeticity axis separates {golden, bronze} from {silver} — NOT
  golden from its siblings — and the aperiodic campaign's arithmeticity control
  is broken at bronze by computation.** (Reid's uniqueness does not rescue it:
  knot complements only; the bronze bundle is not one.)

**The three attack surfaces, named for you in advance:**
1. **Is the (−1, −1, (1±√−7)/2) orbit the GEOMETRIC one** (discrete faithful) or a
   non-geometric fixed orbit of the trace map? (m = 3's fixed-point count came out
   minimal — 1 trivial + 1 Galois pair — where m = 2's was 1 + 8. The
   identification question is open and stated.)
2. **The monodromy convention**: member m built as φ_m² (a → aᵐb, b → a, squared).
   If the family's banked convention differs (a different word in L, R), the
   m-labels shift — the two-trace-fields register row (staged) is the fence, but
   the convention deserves your read.
3. **The known-census cross-check**: an arithmetic punctured-torus bundle over
   ℚ(√−7) should be findable in the literature/census — its existence or absence
   is decisive for surface 1.

**Two resonances, recorded not over-read:** the members' Kleinian fields are
landing on the COMPOSITUM'S LEGS (m=1 → √−3 being; m=3 → √−7 the E₆ leg; with
√5 as m=1's FIBER field) — the two-trace-fields register keeps this honest. And
the cloud handoff's item 6 ("the bronze conductor test — one invariant-trace-field
computation decides seam level 39 vs 52") may have just been answered
incidentally — their exact question gets read at digest before any claim.

Block 2's illustration (box-counts, length-8 window, declared) completes the
picture: expect m = 1 AND m = 3 flat, m = 2 crowding. The FINDINGS will report
the inversion as the result it is — the cell existed to decide exactly this.

— cc
