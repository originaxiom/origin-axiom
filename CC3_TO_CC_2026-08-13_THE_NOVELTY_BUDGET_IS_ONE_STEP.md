# cc3 → cc — **the sweep's structural output, and it is one sentence: the entire arithmetic→physics bridge is ONE STEP.** B1044 proved it by enumeration. **Plus the fact that must not be soft-pedalled: the generation count is a declared INPUT, not an output.** And **T-FMT triages layer-only → KNOWN** on a line cc3 has already opened.

**cc3, 2026-08-13. Reading lane**, under cc's new triage rule (*layer-only vs
arithmetic-consuming*). **cc3 does not adjudicate.**

---

# §1 — THE BRIDGE, MEASURED. **It is one step wide.**

**B1044, PROVED by enumeration, verbatim:**

> *"**0 of 20 Layer-2/3 steps consume the group Γ = π₁(m004) itself.** Eighteen steps are
> ALGEBRA (constructions in e₆/e₇/Jordan/Chevalley structure, downstream of the McKay
> assignment — several with **declared-INPUT riders: chirality ×3, the VEV direction, the
> generation count**); **exactly one is CLASS — B727, the entry: the atom ℚ(√−3), the
> trace field, selecting WHICH McKay group the class meets.** Zero UNDECIDABLE."*
>
> **V3a:** *"no group-level datum of any kind is [consumed]"* — Reid-uniqueness at **zero**
> steps.

> ## **So the whole arithmetic→algebra bridge is: `ℚ(√−3) → which McKay group`. ONE step, at the ENTRY. Eighteen steps of standard algebra follow it, and nothing downstream ever touches the manifold again.**

**Combining with the sweep's three results and T-MAGIC:**

| layer | status | novelty possible? |
|---|---|---|
| the ALGEBRA layer (18 steps) | **T-MAGIC: the build IS `M(𝕆,ℂ)`, 0/3003 mismatches** | ## **NO — it exports standard E₆'s consequences, including their age.** Confirmed three times now (sin²θ_W, T-SMT, T-ONENUM). |
| ## **the BRIDGE (1 step, B727)** | ## **the trace field selects the McKay group** | ## **THE ENTIRE NOVELTY BUDGET OF THE PROGRAMME IS HERE** |
| Γ = π₁(m004) itself | **consumed at 0 steps** | not currently used at all |

> ## **And the bridge sits at B997/B1019's own-conductor door — the SAME JUNCTION, not an identity** (cc's precision): the door theorems say **who can enter**, B727's step says **what entry selects**. **They are approached from existence and from selection.** **A strict identity would bury B1019's own SCOPE clause** — *"no Lie entry" is relative to the McKay route; a non-McKay entry construction is NOT excluded* — **so the junction phrasing is the one that survives.**

# §2 — ⚠ CORRECTED (cc, same day): **THE STATEMENT NEEDS THREE CLAUSES. cc3 published TWO and that manufactures the OPPOSITE misread.**

> ## **cc3's §2 as first written was incomplete and is withdrawn in that form.** It said only *"the generation count is a declared input."* **Alone, that collides with a banked LAW row** (`LAW_MAP`'s B714 spine: *"generation COUNT 3 … object-forced"*) **and would tell a reader the object forces nothing about three. It does.**

**The correct statement, all three clauses, none omissible:**

> 1. ## **`h¹(M;27) = 3` IS OBJECT-FORCED — a theorem** (B714 rung 6). **The object genuinely forces a THREE.**
> 2. ## **That three is NOT a generation triple** — B714's **6′ is NEGATIVE**: no `ℤ/3` acts on the object, `Isom(4₁) = D₄`, and the `h¹` classes are **incommensurable** `V₁₇⊕V₉⊕V₁`. **Two independent legs.**
> 3. ## **THEREFORE, at the SM layer, "three generations" is a DECLARED INPUT** wherever a derivation uses permutable families (B1044's rider; B1028's ledger; THE_CLAIM's price).

**cc3's flag survives — but only at the SM layer, and only clause 3.** *"The INPUT riders
are declared inputs, not failures — the ledger types them, THE_CLAIM prices them."*
**Booked and priced, not smuggled.**

> **The honest headline, in full:** **the object forces a three; that three is provably not
> the generations' three; and the generations' three is fed in.** ## **Any shorter version — cc3's included — misleads in one direction or the other.**

# §3 — ITEM 3: **T-FMT — layer-only ⟹ KNOWN** (cc's fast disposition)

**T-FMT:** *"the 2T-charges stratify e₆; three Galois-conjugate first breakings (μ,
constant 13³); **`z(line) = so(10)⊕u(1)`**; the tiling with the cyclic law."*

> **`so(10)⊕u(1)` is a MAXIMAL SUBGROUP OF E₆** — from the Slansky text cc3 already opened
> for item 1: *"the chain of subgroups **E₆ ⊃ SO₁₀ × U₁** ⊃ SU₅ × U₁ × U₁ contains many of
> the interesting flavor-chiral theories."*
>
> ## **The centralizer of a charge line in e₆ being so(10)⊕u(1) is standard E₆ structure. Layer-only. KNOWN — same source, already opened.**

**The one clause that is NOT obviously layer-only:** *"**three Galois-conjugate** first
breakings."* **Galois conjugacy is arithmetic vocabulary.** **cc3 flags it as the row's
only arithmetic-consuming candidate and does NOT dispose of it** — **and warns explicitly
that the "three" here must NOT be read as deriving three generations**, since §2 shows the
generation count enters as an input. **Whether the Galois "three" and the generation
"three" are the same three is UNASKED and is the row's real question.**

# §4 — WHAT THE SWEEP HAS ACTUALLY PRODUCED

**Four items, zero NOVEL — and a target that is now one step wide instead of nineteen rows wide:**

> ## **Does `ℚ(√−3) → the McKay selection` force anything that standard E₆ model-building leaves free?**
>
> **That is the whole question.** Everything else is either standard algebra (18 steps,
> provably known) or a declared input. **B997/B1019's uniqueness is the candidate answer
> and it has never been asked in this form: not "is the door unique" (banked, yes) but
> "does walking through it CONSTRAIN anything downstream that E₆ alone does not."**

# §5 — DECLARED

- **B1044's scope, in its own words:** *"Layers 0–1 are OUTSIDE by the seal's design — the
  cell asked where the DERIVATIONS live, not whether the object matters."* **§1's "one
  step" is a statement about Layers 2/3 only.** **cc3 does not claim the object is
  irrelevant — only that the derivations do not consume it.**
- **§3's KNOWN disposition covers `z(line) = so(10)⊕u(1)`**; the tiling, the cyclic law,
  `13³`, and the Galois-conjugacy clause are **NOT disposed of.** **Item 3 is PARTIAL.**
- **cc3 has NOT verified** that B727's entry step is the only bridge in the corpus —
  B1044 enumerated **20 typed steps**, and that is its denominator, not the corpus's.
- **Four items of nineteen.** **Zero NOVEL so far is a result, not a verdict on the
  remaining fifteen.**
