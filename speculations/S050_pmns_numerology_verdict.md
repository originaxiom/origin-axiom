# S050 — the Chat-1 PMNS reads: recorded with the binding statistics verdict (2026-07-05)

> **Renumbered 2026-07-05 (audit):** was mis-filed as "S042" (collides with the canonical
> S042 curvature-sign-transition capstone). Reassigned to S050. Legacy citations reading
> "S042 numerology bar / derivation-or-nothing" refer to this file's verdict.

**[FIREWALL] This note records physics-adjacent numerology that FAILED its pre-registered
statistics gate (B398 S5: combined p ≈ 0.087 raw / 0.44 corrected vs the binding 0.01).
Nothing here is a claim. It is kept because the record must not lose data, and because a
future DERIVATION (not a match) could revive individual items on new evidence.**

The reads (from the 2026-07-01→03 Chat-1 handoff): sin²θ₁₂ = 23/75 (the F₄ Gram ratio —
the one item computed from banked data before matching); sin²θ₂₃ = 6/11 (pattern-matched);
sin²θ₁₃ = 23/1050 = (23/75)/14 (divisor selected post-hoc); δ = π/2 (consequential);
J ≈ J_max. What the scrutiny established: the underlying mathematics is real and banked
(B398 S1–S4); the TBM-limit and McKay-bridge coherence arguments are broken; the ensemble
match rate is fully consistent with the density of the formula space (5,964 values, 139
hitting the solar window alone).

**Revival bar (per METHOD rule 7 — no unearned negatives either):** a representation-
theoretic DERIVATION of why a seam-form Gram ratio should be a mixing observable (the
handoff's own C2 gap) would reopen the question as a computation, not a match. Until then:
numerology-class, unvoiced.
