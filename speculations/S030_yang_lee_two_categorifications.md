# S030 — The two categorifications of `τ×τ=1+τ`: unitary Fibonacci vs non-unitary Yang–Lee

**Status: `TESTED-POSITIVE` (the Lee–Yang realization is now *computed*; B132/V121).** Firewalled; not a claim. The
*math* (the fusion algebra, the central charges, the arithmetic) is banked in B127; the **Galois conjugation realizing
Lee–Yang is now explicit** (below); the *physics bridge stays POSTULATED* (emergent non-equilibrium criticality, not
the SM). Nothing promotes to `../CLAIMS.md`; P1–P16 untouched.

> **Upgrade (B132/V121) — the σ₃ Lee–Yang realization is computed.** At level `k=3` (N=5), the SU(2)_k quantum
> dimension `d_τ = 2cos(π/5) = +φ` (the unitary Fibonacci point, `(G₂)₁`, c=+14/5). Its **Galois conjugate σ₃**
> (`q → q³`, i.e. `2cos(π/5) → 2cos(3π/5)`) gives `d_τ = −1/φ` — the **non-unitary Lee–Yang / M(2,5)** point
> (c=−22/5), verified in-sandbox. So the framework's native quantum theory at `k=3` *is* the Lee–Yang CFT, reached by
> the Galois conjugation `φ ↔ −1/φ` that maps the golden substitution's fusion rule to the M(2,5) minimal model. This
> is the strongest concrete physics bridge the arc has produced — and it is **emergent non-equilibrium physics**
> (Lee–Yang edge singularity, experimentally observed; Peng et al. 2015), firewalled from fundamental physics. The
> thread is no longer DORMANT: the bridge is computed, not merely noted.

> **Sharpening (B135/V124) — m=1-specific, modular-data level.** Two honest calibrations. **(1) m-specific, not a
> family.** Only the golden mean `λ₁=φ<2` is a quantum dimension (`2cos(π/(k+2))<2` forces m=1); for m≥2, `λ_m>2`, so
> there is **no metallic family of Lee–Yang CFTs** — the bridge is the *single* golden case. The metallic *family* is
> the family of distinct aperiodic **spectral** objects (`../knowledge/K010`, trace field ℚ(√(m²+4))), not a family of
> minimal models. **(2) Match level.** The m=1 match holds at **modular-data level** — fusion rule + quantum dimension
> + S-matrix Galois conjugate (Fibonacci → Yang–Lee, S⁴=I) + central charge (`c=−22/5`, `c_eff=2/5`) — which is
> *stronger* than the audit's "fusion-rule-only" caution, but it is **not** a full RCFT identification (no torus
> partition-function / character match; the framework's object is the hyperbolic, non-unitary complex-CS quantization,
> tied to M(2,5) via the quantum dimension/fusion, not a proven RCFT equality). So `TESTED-POSITIVE` stands, scoped to
> **modular-data level, m=1-specific, emergent**. (Standard modular data: audit R3 — Jeffrey 1992, Dong–Lin–Ng 2015,
> Lawrence–Zagier 1999.) See `../frontier/B135_lee_yang_m_specific/`.

## The fork (the fenced content)

The golden substitution's fusion algebra `τ×τ = 1+τ` (Perron eigenvalue `φ` = the quantum dimension of `τ`, B127 M-1)
has **two inequivalent categorifications**:

- **unitary Fibonacci** — `(G₂)₁`, central charge `c = +14/5` — the physical topological order (FQH `ν=12/5`,
  topological quantum computation, the Fibonacci anyon);
- **non-unitary Yang–Lee** — the minimal model `M(2,5)`, `c = −22/5` (verified, B127) — the **Lee–Yang edge**
  singularity of non-equilibrium critical phenomena.

The framework's native quantization is **non-unitary** (the metallic members are hyperbolic → non-unitary
complex-Chern–Simons TQFT; CS=0 → `c₋=0`, B127 K-D). So *if* it lands anywhere on this fork, it lands on the
**Yang–Lee** side — **not** the unitary Fibonacci order that the "figure-eight = Fibonacci anyons" reading invokes.

## Why it is DORMANT and not recommended (the thin thread T-1)

Even the Yang–Lee landing is **fusion-rule-only**. The framework supplies the fusion algebra (shared by Fibonacci
*and* Yang–Lee — the simplest rank-2 fusion category), and it **never** supplies the distinguishing data (the
braiding, the twist, the central charge), which is imported from modular-tensor-category theory as the
essentially-unique pentagon/hexagon solution. And the arithmetic doesn't even match: the manifold is `ℚ(ζ3)` while
both Fibonacci and Yang–Lee braiding live in `ℚ(ζ5)` (the arithmetic separation, B127 M-4). So there is **no computed
bridge** — only the weak fact that non-unitary TQFTs have *some* physical home (the Lee–Yang edge, real non-equilibrium
physics). **Record; do not pursue as a bridge without a forced braiding / central charge.**

## What would change the verdict

A computation that **forces** the braiding or the central charge from the framework (not the fusion rule alone) — e.g.
a metallic-specific reason the `R`-matrix / twist must take a particular value — would promote this from
fusion-rule-only to a genuine categorification claim. None exists. The `S027`-adjacent quantum-modularity door (the
`K₃`/Bloch grading) is the only place such forcing could plausibly come from, and it is MAGMA-heavy and untested.

**Reading the standing frame.** This is the most sophisticated form of the recurring pattern: the framework gives the
**fusion rule / quantum dimension** (cheap, shared) and never the **defining physical data** (braiding / twist /
central charge) — the same shape as the gauge-group route (`S029`), one level up. The honest, *emergent* home of the
object is aperiodic spectral theory (`../knowledge/K010`), not anyonic topological order.

Related: **B127**/V116 (the fusion algebra, the central charges, the arithmetic separation, K-C/K-D), `S029` (the
center-symmetry fork — same "fusion/symmetry cheap, defining-data absent" shape), `../knowledge/K010` (the proper
name), `../philosophy/P007`/`P008`. External: Ostrik (the Fibonacci categorification); Yang–Lee / `M(2,5)` (non-unitary
minimal model, Lee–Yang edge); the metallic-anyon kill (`PHYSICS_BRIDGE_MAP.md`: only m=1 categorifies).
