# S004 — W1/W2 (A↔B) asymmetry, structural

**Status: `SUPPORTED` (structure).** Firewalled; not a claim. *(The magnitude is the separate HELD item `S005`.)*

**Structural fact (cited).** The two Dehn-filling components are genuinely distinct and A↔B-conjugate: `W1` is
elliptic on `A`, `W2` on `B` (B71/B102); they satisfy transposed A-variety relations (`M³=L` on W1 = Falbel D2;
`M³L=1` on W2 = Falbel D3, exact, V44/V47). The defining substitution `a→ab, b→a` treats `a` and `b` differently.

**[LEAP] (kept separate).** Read the W1↔W2 pair as a particle–antiparticle pair, and the asymmetric treatment of
`a,b` as a built-in CP-like asymmetry of the foundational map.

**[HOOK].** The *structure* (that the pair exists and is asymmetric) is established. The matter/antimatter content
ties to `S012` (does `θ=−w₀` organize the `(+,−)` sectors). The asymmetric sector dimensions `(2,0),(2,2),(4,2)`
(B62) are the structural carrier.

**Negative beside the leap.** Computing the *magnitude* `|Z(W1)/Z(W2)|` and reading it as `n_B/n_γ ≈ 10⁻⁹` is
**value-matching → `S005` (HELD)**: there is no derivation that this ratio *is* the baryon asymmetry, and no banked
W1/W2 amplitude. S004 is the structural fact only.

Related: `S005` (the held magnitude), `S012` (θ=−w₀ → c), `PHYSICS_EXERCISE.md` T1.5.
