# S043 — topological Λ, the Kodama relation, and the DESI hook (with its 122-order negative)

**FIREWALLED SPECULATION. Tags: [MATH] verified, [HOOK] motivated, [LEAP] unestablished. Nothing crosses to
`CLAIMS.md`.** Written to address a standing omission (the program never engaged DESI) honestly — *neither*
dismissing it as "just physics" *nor* inflating a 122-order-off coincidence into "contact with observation." Math
anchor: B259 (the gravity brick), B250 (complex volume), K018 (scale-freeness).

## The one dimensionful-adjacent equation
[MATH] The Smolin/Kodama relation `k = 6π/(G·Λ)` ties the Chern–Simons level to the 4d Newton constant and the
cosmological constant. `G·Λ` is **dimensionless** in 4d (`[G]=1/M²`, `[Λ]=M²`). At the golden level `k=3`:
> **`G·Λ = 2π`.**

This is the single place the whole structure has a dimensionful combination meeting a pure number — and it meets it
*correctly*: a scale-free object fixes the **dimensionless** relation and leaves `G` and `Λ` separately to the
observer. That is exactly the program's relational-scale thesis (K018: the object produces no scale) made concrete.
**This much is worth keeping** as the cleanest dimensionful-adjacent statement the program has — *firewalled*.

## [HOOK] topological dark energy
If `Λ = 6π/(kG)` and `k` is an **integer that increments** (a count of topological transitions — the metallic
tower `m=1,2,3,…` / level steps), then `Λ` **decreases in discrete steps**: a "topological" dark energy that
weakens over time with **no fine-tuning and no continuous cancellation** — just an integer counter. DESI DR2 (2025,
BAO+CMB+SNe) reports evidence for **evolving** dark energy (`w₀wₐ` over ΛCDM) at **2.5–4.2σ** depending on the SNe
sample. The *qualitative* direction (dark energy weakening) rhymes with `k` increasing.

## [LEAP] / the honest negatives — why this is a hook, not a result
Verified in B259 (`gravity_brick.py` CHECK B), stated plainly:

1. **The 122-order magnitude gap (fatal as stated).** Golden `k=3 → G·Λ = 2π` is a **Planck-scale** `Λ`
   (`Λ ~ M_Pl²`). The observed dark energy has `G·Λ ~ 10⁻¹²³`, i.e. it needs **`k ~ 10¹²⁴`, not 3**. The golden
   value is the *cosmological-constant problem*, not its solution. So **`k=3` is emphatically NOT the DESI dark
   energy.** At `k ~ 10¹²⁴`, incrementing `k` by 1 changes `Λ` by `~10⁻¹²⁴` — far too small to see, and the golden
   structure (the whole point of the object) is irrelevant at that level.
2. **Step-function ≠ `w₀wₐ`.** A piecewise-constant `Λ(k)` is `w=−1` on each step; DESI fits a *smooth* `w(z)`. A
   stepwise Λ is a different functional form, not the thawing quintessence DESI prefers.
3. **No rate.** Nothing in the object says *when* `k` jumps or *how fast* — the cosmology (the `k(t)` law) is
   external input, not supplied.
4. **Sign / regime.** The figure-eight is hyperbolic (`Λ<0`, AdS-like, B259 STONE 1); the Kodama relation is usually
   quoted for `Λ>0` (de Sitter). The sign is not automatic.

## Speculation → calculation
| speculation | what would make it real (the calculation) |
|---|---|
| "topological Λ explains DESI" | a concrete `k(t)` law at `k~10¹²⁴` and its predicted `w(z)`, fit against DESI DR2 — and an account of why `k` is that large (decoupled from golden `k=3`). |
| "`G·Λ=2π` is contact with the c.c." | derive the *observed* small `Λ` (not the Planck value) from the object — i.e. solve the c.c. problem. Not done; not in sight. |
| "incrementing `k` = no fine-tuning" | show the step size and timing match `Ω_Λ(z)` without tuning. Untested. |

**Honest standing:** the *dimensionlessness* of `G·Λ` is the real, keepable [MATH] (and the program's scale-free
thesis). The DESI connection is a [HOOK] with a **122-order [LEAP]**; it is the program's closest *rhyme* with
observation but is **not** contact. Recorded so the motivation is preserved (not deleted) and the negatives are
stated up front — the firewall enabling the motivation, not suppressing it.

Related: [[S042_curvature_sign_transition]] (Λ-sign change), B259 (the wall map: #5), B250 (complex volume), K018
(scale-freeness).
