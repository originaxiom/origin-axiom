# S024 — the tower lives on the monodromy side of Hitchin, separated from the Fuchsian section by `det=−1`

**Status: `POSTULATED` (strengthened B150).** Firewalled; not a claim. Phase 2 of the physics-bridge sweep
(`PHYSICS_BRIDGE_MAP.md`). The *mathematics* of this reading is **proved** (B121); the *physics interpretation* below
is the firewalled leap. Nothing promotes to `../CLAIMS.md`; the physics chapter stays CLOSED.

> **Closing-arc update (B150/B151).** The monodromy-side reading is now **FORCED at the symmetry level**: the
> `SL(2,ℤ)` trace-map action on the Fricke character variety **is** the `N=2*` class-S S-duality mapping-class action,
> literature-confirmed (Allegretti–Shan 2411.17378; Cantat–Loray; GMN) — `B150/V139`. It is FORCED only as a *symmetry*
> identity; at physical magnitude it is RHYME, and the firewall holds (`B151/V140`) — the complex volume sources no
> scale. See `../frontier/B150_class_s_coincidence/`, `../docs/STRATEGIC_SYNTHESIS.md` §8/§8a.

**[MATH] (proved, B121).** The `(n²−1)`-dim trivial-point tower carries two inequivalent `SL(2)`-actions on the
adjoint `sl_n`:
- the **internal principal `sl(2)`** (Kostant `⊕Sym^{2i}`, even weights, `det=+1`) — which **is** the Fuchsian
  locus / the Hitchin section (B101: `V0` = the principal `V2` sector at `SL(3)`);
- the **external monodromy `GL(2,ℤ)`** (the tower `⊕Sym^d(M_m)`, mixed parity, `det=−1`) — the mapping class group
  acting on the character variety.
They agree only at `n=2`; for `n≥3` the gap is exactly the **`det=−1` parity** (the odd `Sym^d`), the same parity
the program singled out in B93/B94 (the catalog parity) and B118 (the inversion sign).

**[LEAP] (the firewalled reading).** In the Hitchin / non-abelian Hodge picture, the **Hitchin section** (Higgs
bundles with the principal/`det=+1` structure) and the **mapping-class-group monodromy** of the Hitchin system are
the two halves of the same story; geometric Langlands relates them. The reading: the **tower is the monodromy half**,
and the precise obstruction separating it from the Hitchin/Fuchsian half is the program's own `det=−1` parity. So
the trace-map tower is not "near" the Hitchin section by accident — it is its **monodromy partner**, offset by the
one structural feature (`det=−1`) the catalog is built on.

**[HOOK] (computable continuations, uncomputed).**
- Build the explicit `GL(2,ℤ)`-equivariant comparison map between the principal grading and the monodromy grading
  on the adjoint (the change-of-grading; B103's iso `P` is the n=3,4 ingredient) — is there an interpolating Higgs
  field / a one-parameter family from Fuchsian to monodromy?
- Place the `det=−1` parity inside the geometric-Langlands duality (does it correspond to the `SL(n) ↔ PGL(n)`
  Langlands flip, or to a twist by the center?).

**Negative beside the leap (the ceiling).** The citable chain is **Hitchin → Langlands → class-S / 4d N=2** (K006,
`archive/PHYSICS_RESONANCES.md`), and it stops at **N=4 SYM / class-S theories of the once-punctured torus** — not
the Standard Model, gravity, or the universe (those readings are DEAD; `../docs/atlas/FAILURE_ATLAS.md`). This is a
**structural** bridge (two `SL(2)`-actions and their `det=−1` offset), not a numeric one — it asks for **no value**,
so it is not numerology; but it also licenses **no physical prediction**. It is the architecture, not the furniture.

Related: **B121** (the proof), `B101` (the Hitchin/Fuchsian section), `K006` (the citable 3d-3d / Hitchin chain),
B93/B94/B118 (the `det=−1` parity), `PHYSICS_BRIDGE_MAP.md`.
