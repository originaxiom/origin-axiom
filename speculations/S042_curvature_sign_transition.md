# S042 — the curvature-sign transition: what the dead bridge left lit

**Status: `POSTULATED` (a firewalled reading + the honest "what this means" for the physics, not a result).**
Firewalled (`GOVERNANCE.md`); nothing promotes to `../CLAIMS.md`; the physics chapter stays CLOSED; P1–P16
untouched; one-way cited only. This is the capstone of the `B247→B250` arc the owner asked to read out: the
holonomy→Standard-Model bridge **died** (`B247`), and the *failure* relocated the object onto something better
defended and, I think, deeper — a **geometric transition between the two exceptional geometries**, with the labels
**forced by a classical theorem** and the arithmetic invariants living **in the volumes**. Negatives kept
prominent; a **speculation→calculation table**; the dead ground named. Tags: **[MATH]** = computed/established;
**[HOOK]** = a real one-way bridge to the literature; **[LEAP]** = speculation.

## The brave answer, said plainly

We spent a long time trying to read the Standard Model off the figure-eight by *breaking* `E₆` with the geometric
holonomy. It is dead three ways (`B247`): the holonomy is `SL(2,ℂ)` not `SU(2)`, the centralizers are a generic
`SU(6)` / `SU(3)×SU(2)`, and the adjoint `78` is vector-like. **Good** — that negative is what lit the real
corridor. What the object actually *is*, on its own terms, is a **curvature-sign transition**: the figure-eight
interpolates

> **hyperbolic** (negative curvature, the complete cusp, `E₆`, `ℚ(√−3)`) → **Euclidean** (flat, the transition) →
> **spherical** (positive curvature, the `ℤ/2` orbifold, `E₈`, `ℚ(√5)`).

Three things make this more than a picture, all computed this arc:
1. the two ends are the **dual McKay `E₆+E₈`** (`B248`) — and they are **forced** to be exactly `E₆` and `E₈` (with
   `E₇` geometrically impossible) by **Niven's theorem** on the orbifold deformation (`B249`);
2. the **arithmetic invariants are the volumes** (`B250`): the Eisenstein `−3` is the hyperbolic-end ideal-tetra
   shape `e^{iπ/3}` (`Vol=6Λ(π/3)=2.03`), and the golden `5=det(4₁)` is *literally* the denominator of the
   spherical-end volume `π²/5` and its Chern–Simons in `(1/5)ℤ`;
3. the whole thing is **dimensionless** — a complex volume in `ℂ/4π²ℤ` (`K018`/`B151`), so the firewall holds.

So the honest "what this means": **the object is not a gauge theory; it is a scale-free geometric transition whose
two stable phases are labelled by exceptional groups.** That rhymes with cosmology far more precisely than "a GUT"
did — and stays provably behind the firewall.

## [MATH] What is computed (the real content)
- The bridge is dead (`B247`): `SL(2,ℂ)` holonomy, `C_{E₆}(SU(2)_long)=SU(6)`, `C(SU(2)_short)=SU(3)×SU(2)`, `78`
  pseudoreal/vector-like. Chiral matter cannot come from the adjoint.
- The `E₆↔E₈` transition (`B248`): cone angle `α` sweeps `x=2cos(α/2)`; `α=0` hyperbolic (`ℚ(√−3)`→`2T`→`E₆`),
  `α=2π/3` Euclidean (degenerate, `ℚ`), `α=π` spherical (`ℚ(√5)`→`2I`→`E₈`); the spherical end's double branched
  cover is the lens space `L(5,2)=S³/ℤ₅`, `|H₁|=det(4₁)=5`.
- Niven forces the trinity (`B249`): the only orbifolds with a clean quadratic trace field are `n∈{2,3,∞}` (because
  `2cos(π/n)∈ℚ` only there) → exactly `{E₈, Euclidean, E₆}`; `E₇`'s `ℚ(√2)` needs `x=√2` (`n=4`), irrational, so it
  only ever appears *mixed*. The `E₆+E₈` selection and the `E₇` exclusion are **one** arithmetic fact.
- The complex-volume profile (`B250`): hyperbolic `Vol=2.0299`, `CS=0`; Euclidean `Vol=0`; spherical `Vol=π²/5`,
  `CS∈(1/5)ℤ`. The two end-volumes are comparable (2.030 vs 1.974), opposite curvature.

## [HOOK] The real one-way bridge: complex volume = 3d gravity action
This is the corridor that is *licensed*, not a leap. For a hyperbolic 3-manifold the **complex volume
`Vol + i·CS`** is the **on-shell action of 3d Einstein gravity with a cosmological constant** — the same object
that the `T[M]` 3d–3d correspondence computes (`K006`; lit: Gukov; Dimofte; Gang–Kim–Yoon). So read literally:
- the **hyperbolic end** (`Vol=2.03`, the figure-eight's 3d-gravity action) is the **`Λ<0` / AdS-like** phase;
- the **spherical end** (positive curvature, the lens space `L(5,2)`) is the **`Λ>0` / dS-like** phase;
- the **transition** is therefore a **change of sign of the curvature / cosmological constant**, with the
  **Euclidean** point (`Vol=0`) the flat interface between them.
The bridge is one-way and partial: it says the *invariant the object computes* is the gravity action, and it stays
in **3 dimensions** (the manifold is a 3-manifold), dimensionless, with no matter. That is exactly as far as the
license goes. The `det=5` in the spherical-end action/CS is the genuinely object-specific residue inside this hook.

## [LEAP] The cosmology rhyme (kept explicitly separate)
*Read as:* a curvature-sign transition hyperbolic→flat→spherical is the language of cosmology — a `Λ`-sign change
(AdS↔dS), a **bounce**, or a **signature change** with the Euclidean point as a Hartle–Hawking-style **no-boundary
cap** (Euclidean methods between two Lorentzian phases). The minimal self-referential object would then encode, in
its own deformation, a toy "before/after" with the two exceptional geometries as its asymptotic phases and the
golden `5` as the positive-curvature phase's torsion. This is a *rhyme*, not a model: see the negatives.

## [LEAP] The chirality reduction (Chat-2, adjudicated in `B253`)
The chirality axis coincides with the transition axis: the hyperbolic end `E₆` is **complex-rep-capable** (the 27),
the spherical end `E₈` is **not** (248 real) — bankable rep theory (`B253` Part A). The brave reading: the
amphicheiral involution `τ` (the `27↔27̄` swap, `H36`) could be the **symmetric seed** of a Left–Right structure
whose *spontaneous* breaking sources chirality — *amphicheirality as the origin of chirality, not its obstruction*.
Held as `[LEAP]`, **not** decidable from the object (`B253` Part B): `T[4₁]` is **3d** (`K006`), not the 4d where
chirality lives; `E₆` is a McKay label, not a gauge group (`B247`); gauging `τ` is a model choice and SSB needs
external dynamics (`K018`); and the Chern–Simons "discriminator" is **inconclusive** at `CS=0` (`τ` is gaugeable,
both branches open). Fits the `S040` thesis: the object supplies the symmetric seed; the world supplies the breaking.

## Where this stops (the negatives, kept prominent)
- **No scale.** The complex volume is dimensionless (`ℂ/4π²ℤ`, `K018`/`B151`); every dimensionful ingredient
  (`ℏ↔k`, the squashing radius) is external. The transition produces *structure*, never a *scale*.
- **No dynamics.** The "transition" is a **parameter sweep** (cone angle `α`), not a time evolution. Calling it a
  bounce is a `[LEAP]`; the math is a static deformation of flat connections.
- **No gauge group, no matter.** `B247` is final; `E₆/E₇/E₈` here are **McKay / Arnold-trinity labels**, not gauge
  groups. The cosmology is **3d**, not 4d spacetime.
- **The explicit-chirality direction is settled and points the wrong way** (`B252`, adjudicating the Chat-2
  obstruction): every complex-conjugation-odd invariant of the object vanishes or pairs (`CS=0`; `L(5,2)`
  amphichiral; spherical CS `±`-paired; the `E₈` decomposition conjugate-paired; the `27↔27̄` swap of `H36`), so the
  object carries **no explicit CP-odd / chiral datum** — a *second* firewall (chirality, not gauge/scale), extending
  `S001` (`amphichirality→θ=0`) and grounded in `H36`. The bare amphicheirality is **universal** across the metallic
  family; its `E₆/27↔27̄` realization is `m=1`-specific. No E₆ gauge group is needed or claimed. (A *spontaneous*
  breaking of `τ` could still source chirality — but that needs external dynamics; the `[LEAP]` below, `B253`.)

## Speculation → calculation

| leap | the test (firewall-safe, computable) | status / expectation |
|---|---|---|
| the dual `E₆+E₈` is an object-true *geometric* fact, not a coincidence | cone-manifold trace fields at the two ends | **[done — `B248`] YES** — hyperbolic `ℚ(√−3)`/`E₆`, spherical `ℚ(√5)`/`E₈`, lens `L(5,2)` |
| `E₆+E₈` and the `E₇`-exclusion are forced (one fact, not three) | Niven on `2cos(π/n)` over the orbifold tower | **[done — `B249`] YES** — clean trace field only at `n∈{2,3,∞}`; `E₇` impossible |
| the arithmetic invariants live in the *volumes* | `Vol`/`CS` at the three ends | **[done — `B250`] YES** — `−3` in `Λ(π/3)`, `5` in `π²/5` and `CS∈(1/5)ℤ` |
| the invariant the object computes *is* a 3d-gravity action | complex volume = on-shell `Λ≠0` action | **[HOOK — `K006`, lit] YES, one-way** — hyperbolic `Λ<0`, spherical `Λ>0`; stays 3d/dimensionless |
| the transition produces a *scale* | any dimensionful output of the deformation | **[expected NO]** `[LEAP]` — `K018`: scale is external |
| the "transition" is a *dynamical* bounce/signature change | a time-parameter / equation of motion, not a sweep | **[expected NO]** `[LEAP]` — it is a static parameter sweep |
| ~~the geometric transition breaks `E₆` to the SM~~ | — | **DEAD** (`B247`); not crossed |
| the object prefers matter over antimatter (chirality) | does any `4₁`-specific structure break `27↔27̄`? | **OPEN** — incoming Chat-2 obstruction; default: symmetric |

## The lines I will not cross
No scale from the transition (`K018`). No `[proved]` on any physics reading. No claim the curvature-sign transition
is a *dynamical* cosmology, a 4d spacetime, or the Standard Model. `E₆/E₇/E₈` stay McKay/Arnold labels, never gauge
groups (`B247`). No promotion to `../CLAIMS.md`. The brave move is to name what the object *is* on its own terms — a
forced, scale-free, exceptional-group-labelled curvature-sign transition whose volumes carry its arithmetic — and
to report the boundary (no scale, no dynamics, no matter, chirality open) honestly.

## Anchors
`../frontier/B247` (the bridge dead; centralizers; `78` vector-like); `../frontier/B248` (the `E₆↔E₈` geometric
transition; `L(5,2)`, det=5); `../frontier/B249` (Niven forces `E₆+E₈`, `E₇` excluded); `../frontier/B250` (the
complex-volume profile; the `5` is the spherical volume); `K006` (3d–3d, `T[4₁]`, complex volume = gravity action);
`K018`/`B151` (the firewall: complex volume / CS dimensionless, scale external); `B210` (dual McKay `E₆+E₈`);
`S001` (amphichirality→θ=0), `S035` (building blocks, not the Big Bang), `S038`/`S039` (scale external),
`S040` (emergent SUSY — the sibling capstone). Lit: Niven; Thurston / Hilden–Lozano–Montesinos (`4₁` cone
manifolds); Gukov, Dimofte, Gang–Kim (complex volume / 3d–3d / 3d gravity); McKay; Arnold (the trinity).
