# Tombstones — the DEAD speculations

First-class negatives, **permanent**. A `DEAD` speculation is never re-chased (root `GOVERNANCE.md` §5: `dead →
anything` is forbidden). Each epitaph links to the kill — the `../docs/atlas/FAILURE_ATLAS.md`, a `../CLAIMS.md`
D-row, or a `V`-ledger row. These are *here* so the bravery upstairs is honest: the room logs its deaths.

---

**S014 — the cosmological-constant value formula.** `Λ = Λ_Planck · φ⁻²ᴺ` (and the `k≈137` variant). **DEAD** by
**null hypothesis** — ~60% of random constants match as well — and by circularity (`N` is defined by the answer it
is meant to predict). Kill: `../CLAIMS.md` **D1–D3**; root `GOVERNANCE.md` §8. This is *the* archetype of the
`HELD`/numerology discipline; everything value-matching is held because of this death.

**S015 — tower eigenvalues = masses / operator dimensions.** The killed **spectral identification**. The tower is
**moduli-space monodromy** (`±φᵏ`, a single scale), not a fluctuation Hessian; a one-scale "spectrum" is not a mass
spectrum. Kill: **B107** (the dictionary correction). One of the two lines never crossed (`GOVERNANCE.md`).

**S016 — Goldman metric (1,1) Lorentzian.** Hoped the Goldman/Weil–Petersson form on the character variety was
Lorentzian. **DEAD** — the computation gives a definite Riemannian signature `(2,0)`, not `(1,1)`. Kill: B96/B101;
`../docs/atlas/FAILURE_ATLAS.md`.

**S017 — phase-space 3+1D at SL(3).** "The SL(3) character variety has 6 real dimensions → 3+1." **DEAD** — the
count is arithmetic; the metric that would make it spacetime is Riemannian, not Lorentzian. Kill: **B101**
split-form ladder; `../docs/atlas/FAILURE_ATLAS.md`.

**S018 — the spacetime tower up the ranks.** The hope that a 3+1 spacetime appears at SL(3) or higher where SL(2)
gives only 2+1 — Lorentzian signature "climbing the principal-SL(2) ladder." **DEAD by computation** (Lorentzian
only at `k=2`). Kill: **B101 R2**; `../docs/atlas/FAILURE_ATLAS.md` ("Spacetime Does Not Climb The Ranks", CLOSED).

**S019 — Fisher metric on CS level k.** A heuristic information-geometry reading on the Chern–Simons level. **DEAD**
— heuristic, not rigorous; no bounded test survived. Kill: the dead-sweep `V`-rows (see
`archive/PHYSICS_RESONANCES.md`).

**S021 — entanglement = holographic.** Hoped the Fibonacci critical chain's entanglement signalled holography.
**TESTED-NEGATIVE** — the entanglement is **logarithmic** (generic 1D-CFT criticality) and the emergent geometry is
**AdS-like but generic** (Fibonacci ≈ periodic): it does **not** bridge. Kill: **V37** (re-verification);
`../docs/atlas/FAILURE_ATLAS.md`. *(Listed among tombstones as a banked negative; strictly `TESTED-NEGATIVE`.)*

---

**Meta-kill — "one collision causes all walls."** The narrative that a single repeated `−1` is the common root
cause of the tower / degree=rank / eps-series walls. **WITHDRAWN** — `n=5` is a threshold where **three distinct**
`A_{n−1}` obstacles degenerate together (different eigenvalues, independent derivations, different onset). Kill:
**V91** (the three-obstacle correction). Not a speculation about physics, but a killed *framing*, banked here so it
is not silently re-adopted.
