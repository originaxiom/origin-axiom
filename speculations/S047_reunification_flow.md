# S047 — the reunification flow: symmetric UV → (filling) → broken IR (firewalled)

**FIREWALLED SPECULATION. Tags: `[MATH]` verified here · `[LEAP]` unestablished bridge · `[HOOK]` motivated rhyme.
Nothing crosses to `CLAIMS.md`; nothing is `HELD` without the two-part test.** The physics-facing reading of the
symmetry-broken sweep (2026-07-01, B336–B338), held behind the firewall while the math banks to `frontier/`. Extends
[[S046_value_at_the_seam]], [[two-ended-unification-and-wall-map]]; the math is assessed in the cited probes.

## The frame — structure ⊕ value, and the flow that bridges them
The sweep sharpened the wall into a tension and then found its bridge:
- **`[MATH]` structure ⊕ value (B337).** A *symmetric* configuration (one object, ℤ/3, shared `ℚ(√−3)`) forces the
  structure (E₆, `3/8`, cascade) but a *democratic* (degenerate) spectrum; *distinct* seeds order the spectrum but lose
  the shared arithmetic → no E₆. Same symmetry, opposite requirements. No static configuration has both.
- **`[MATH]` the value's home is doubly separated (B336).** The value would live in the imaginary seam `ℚ(√−15)`, invisible
  to the amphichiral (real) object — but the chiral *routes* to `√−15` are provably closed (monodromy `{t²−4}∌−15`;
  commensurables all `ℚ(√−3)≠ℚ(√−15)`), and `√−15` is arithmetically generic (B333). It is the abstract compositum, not a
  geometric quantity.
- **`[MATH]` the object contains a flow (B338).** Dehn filling `(1,n)` interpolates from the symmetric cusp (`CS=0`,
  amphichiral) to broken/chiral configs, with the chiral order parameter turning on as **`CS(1,n) ~ −1/(2n)`**. A real,
  object-internal flow whose order parameter is exactly the chirality the value needs.

## `[LEAP]` the reunification reading
The SM reconciles unification with hierarchy by **spontaneous breaking**: one symmetric structure at the GUT scale,
broken toward the IR. The object mirrors this *as a flow*:

> **symmetric object** (UV: E₆, amphichiral, `CS=0`) **— Dehn filling —→ broken/filled** (IR: chiral `CS~1/n`, orderable).

The cusp is the symmetric UV; filling is the flow that turns on chirality (the `1/n` order parameter); the filled IR is
where ordering can live. *If* this flow is the physical UV→IR running, the structure (E₆) is preserved at the top and the
values (ordering, CP) are the broken bottom — exactly the structure⊕value tension resolved dynamically rather than
statically. **Not shown**: that this geometric flow *is* the physical RG / Higgs breaking.

## `[HOOK]` where the value lives — the external choice
The flow **exists**, but its **parameter** (the filling slope `(1,n)`) is external: *which* breaking is realized = the
choice of vacuum = the "interaction with the nothing" (B286). So the object supplies the **moduli space of breakings**
(the track); physics supplies the **direction** (which vacuum). The value is not in the object *and* not in the flow's
existence — it is in the *selection* of the flow's endpoint. This is `P001`/`P011` (architecture-not-furniture) made
dynamical: the object is the space of possible breakings; the actual world is one selected point in it.

## Speculation → calculation table (new computable paths)
| # | reading | tag | concrete next calculation |
|---|---|---|---|
| 1 | *Does the `CS ~ −1/(2n)` law have a closed form / a `√−3` coefficient?* The `1/2` and the sub-leading terms are Neumann–Zagier data. | **[MATH]** | Fit `CS(1,n) = −1/(2n) + c₂/n² + …` and identify `c₂` (B290 found the *volume* `1/n²` coefficient `= 2π/|τ|`, `|τ|²=12`); check whether the CS sub-leading carries `√3`/`ℚ(√−3)`. |
| 2 | *Is there a filling that lands on an **ordered** three-generation config?* The flow's endpoints (B337 multiplicity) vs the fillings. | **[MATH]** | For fillings that are the metallic silver/bronze closings, compute their spectra; does any filling reproduce the ordered `{10.2:−4:−0.2}` structure — i.e. does a *single* flow endpoint carry both E₆-remnants and ordering? |
| 3 | *The two order parameters:* chirality `CS ~ 1/n` (B338) and the CP phase `±π/6` (B285). Same flow? | **[LEAP]** (piece **[MATH]**) | Track the commutator phase `κ = √3 e^{±iπ/6}` along the `(1,n)` flow; does the CP sign get selected as `CS` turns on (chirality → CP)? |
| 4 | *Is the filling flow a gradient/entropy flow?* (the σ-flow rhyme, S045). | **[HOOK]** | Compare `d(CS)/d(1/n)` and `d(vol)/d(1/n)` — is the flow monotone in a Perron-entropy / complex-volume potential? |

## The negatives (stated)
- **No value is forced.** The flow's parameter is external (B338); `√−15` is unreached and generic (B336/B333); the
  multiplicity ordering is caveated (overlap ≠ mass, B325). The reunification is a *frame*, not a value.
- The identification of the geometric filling flow with the physical RG / Higgs breaking is **[LEAP]** — the object gives
  the moduli of breakings, not the dynamics that selects one.
- `[HOOK]` #3/#4 are genuine open computations, not assertions.

## Firewall / HELD
Nothing here is `HELD(value-matching)` — no computed number is claimed to *be* a physical quantity. Every value-facing
sentence is `[LEAP]`/`[HOOK]`. Math cited one-way by `B`/`K`/`P` pointer; the mathematics never cites this note. Nothing
to `CLAIMS.md`.
