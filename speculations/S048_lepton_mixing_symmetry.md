# S048 — the lepton-mixing symmetry reading: *exact TBM* (θ₁₃=0), breaking external (firewalled)

**FIREWALLED SPECULATION. Tags: `[MATH]` verified (B342/B343) · `[LEAP]` unforced identification · `[HOOK]` motivated
observation. Nothing crosses to `CLAIMS.md`; nothing is `HELD` without the two-part test.** The physics-facing reading of
Chat-2's CP-redirect. The math backbone is B342/B343 (`frontier/`); this note holds the *interpretation*, and — because
this is the closest the program comes to touching measured data — the guardrails are stated **above** the reading.

> **CORRECTION (B343, 2026-07-01): the "object selects TM2" reading below is RETRACTED.** Verified: the neutrino residual
> symmetry is the object's own 2-torsion **Klein group** `ℤ₂×ℤ₂` (of the `(ℤ/4)²` torsion, B326), and the deck ℤ/3 acts
> on it **irreducibly** (`Φ₃` irreducible mod 2), 3-cycling the three `ℤ₂`'s and selecting **none**. So the object forces
> the **full Klein → exact TBM (θ₁₃=0)**, not TM1 and not TM2. The TM2 tension **dissolves** (the object never bet on
> TM2). And `θ₁₃=0` is itself excluded — so the object's symmetry is the `θ₁₃=0` *limit*; the TBM-**breaking** (θ₁₃'s size
> *and* direction) is **external**. The **same irreducibility** that makes the object mass-blind (B335) and split-blind
> (B327/B329) makes it **direction-blind** here — one arithmetic property. *(The `[LEAP]`/`[HOOK]`/TM2 text below is kept
> for the record of how the reading was corrected; read it through this banner.)*

## Guardrails (binding, read first)
1. **Symmetry, not magnitude.** A discrete-symmetry pattern (TBM/TM2) is *form* and may be discussed here; a fit of the
   *continuous* CKM/PMNS angles/δ would be **numerology** (the two-lines rule) and is forbidden. *(θ★/`D10` is a separate
   defunct project, not a constraint — this stands on the repo's own firewall.)*
2. **Currently disfavoured.** The object's ℤ/3 preserves the middle/magic column → it selects **TM2**. But **current data
   favours TM1** (`θ₁₂`: TM1 34.3° vs TM2 35.7° vs observed 33.4°; B342). So even the *symmetry* prediction is falsifiable
   and **in mild tension now**.
3. **B322 null.** This repo already null-tested the PMNS angles against the object's filling invariants → **chance**
   (`p≈0.5`).
4. **HELD not met.** There is **no derivation** that the object's geometric ℤ/3 *is* the physical lepton flavor symmetry;
   the null (B322) is chance. So nothing here is banked as *support* — it is "a symmetry in search of a meaning."

## `[MATH]` the backbone (B342)
The object's geometric ℤ/3 (the generation deck-phase, `{1,ω,ω²}`, B324/B335) is the **trimaximal (magic) symmetry**: the
ℤ/3-invariant column is `(1,1,1)/√3` = TBM's middle column, and ℤ/3-cyclic mixing is democratic (`|entry|²=1/3`). The
object's ℤ/2 (the amphichiral CP-sign `±π/6`, B285/B318) is the second residual symmetry. These are standard, verified
facts about the *math object* — no physics yet.

## `[LEAP]` the identification (unforced)
*If* the object's geometric `ℤ/3 × ℤ/2` **is** the lepton-flavor residual symmetry, then: the ℤ/3 preserves the magic
column → **TM2** (not TM1, not generic), giving the correlation `sin²θ₁₂ = 1/(3cos²θ₁₃)`. **Not shown**: that the object's
ℤ/3 is the flavor symmetry (it is a geometric deck-transformation of a knot complement; the map to flavor space is a
physical dictionary, not derived). And the selection is *disfavoured now* (guardrail 2).

**The δ-sign joint.** The object's CP phase *is* in `ℚ(√−3)` (κ = √3 e^{±iπ/6}, B285/B318) — a Galois orbit under
`√−3→−√−3`, so the `±` is **not forced** (symmetrizable, B330/K020). Identifying this `±` with the **PMNS Dirac phase δ**
requires the PMNS phase to live in the object's `ℚ(√−3)` — a physics-dictionary claim, **unweldable in-sandbox** →
**NEEDS-SPECIALIST**, not banked. (So the θ₂₃≈45° and δ-sign readings that Chat-2 chained are downstream of this unwelded
joint and are not claimed here.)

## `[HOOK]` the sector split (motivated observation)
The object's phase structure is *democratic* — it would show up where mixing is *large* and the mass hierarchy *weak*
(leptons), and be *washed out* where the hierarchy is *strong* (quarks: CKM near-diagonal). That the object's discrete
phase is "audible" in the lepton sector and "silent" in the quark sector is a suggestive rhyme with the data's own
structure. It is a `[HOOK]`, not a derivation.

## Speculation → calculation table
| # | reading | tag | concrete next calculation |
|---|---|---|---|
| 1 | Is the object's ℤ/3 really the magic column, or could it preserve TM1's first column? | **[MATH]** | Check which column the *specific* deck-ℤ/3 (not a generic cyclic ℤ/3) fixes; TM1 vs TM2 is decided by which eigenvector is ℤ/3-invariant. If it's the middle → TM2 (disfavoured); if reframable to the first → TM1. |
| 2 | Does the PMNS Dirac phase live in `ℚ(√−3)`? (the δ-sign weld) | **[LEAP]** | A physics-dictionary question — **NEEDS-SPECIALIST**; not in-sandbox. |
| 3 | The quark/lepton "audibility" split — quantify it | **[HOOK]** | Model a democratic phase perturbation on top of a hierarchical mass matrix; measure when the phase survives. |

## The negatives (stated)
- **No mixing angle or δ is derived or fitted** (that is numerology, forbidden). TM2 is a *symmetry pattern*, and it is
  **currently disfavoured vs TM1** (B342). The identification object-ℤ/3 = flavor-ℤ/3 is unforced; B322 says chance; the
  δ-joint is unwelded (NEEDS-SPECIALIST). Falsifier: TM1 vs TM2 (DUNE / Hyper-K).

## Firewall / HELD
Nothing here is `HELD(value-matching)` — no continuous quantity is claimed to *be* a physical value. Every value-facing
sentence is `[LEAP]`/`[HOOK]`. Math cited one-way by `B`/`K` pointer; the mathematics never cites this note. Nothing to
`CLAIMS.md`.
