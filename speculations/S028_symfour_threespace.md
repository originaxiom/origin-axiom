# S028 — the `Sym⁴(3-space) = sl(4)` reading (the algebra is proved; the "3+1" geometry is fenced)

**Status: `POSTULATED` (geometry only; the algebra is proved math, in B122).** Firewalled; not a claim. The
geometric/physical reading of `W` lives **entirely downstream of the open hinge** (whether the tower is
*functorially* `Sym^n(W)`, B122 — which currently has **no** functorial construction). Nothing promotes to
`../CLAIMS.md`; the physics chapter stays CLOSED. (Numbered S028 — **not** S024, which is the monodromy/Hitchin
entry.)

## The proved algebra (math tier — recorded in B122/K008, NOT here as structure)

`Sym⁴(3-dim) = 15 = dim sl(4)`, and `4` is the **unique** order where `Sym^a(3-space)` saturates `n²−1` (`Sym³=10`,
`Sym⁵=21`). With `W = V⊕1` (the **external monodromy fundamental**, B121/B122) this restates the n=4 fixed point of
the dimension identity (B117): *the 4th symmetric power of the 3-dim external fundamental saturates the SL(4)
adjoint.* This is **proved** and lives in B122 — it is cited here only as the object the reading attaches to.

## [LEAP] the geometric reading (fenced)

*If* `W = fiber ⊕ base` — the 2 homology directions of the once-punctured-torus fiber (`V = H₁`) plus 1 base/circle
direction of the mapping torus — then `dim W = 3` and `Sym⁴(W) = sl(4)` reads as: *a 3-dimensional object whose
4th-order invariants give the SL(4) adjoint.*

## Fences (mandatory — the firewall must not leak)

- **The algebra is proved; the geometric identification of `W` is NOT.** B121/B122 identify `W` **algebraically** as
  the external monodromy fundamental (`det=−1`); they do **not** establish that the `⊕1` is geometrically a "base
  circle," nor that `V` is `H₁` of a *spatial* fiber. That is an unverified reading.
- **"Spacetime" / "3+1" is stripped.** Reading the mapping-torus base as a *time* circle, and `3 = space, 1 = time`,
  imports a physical interpretation not earned — and "3+1 from a dimension count" is the **DEAD** thread (S017/S018:
  spacetime does not fall out of dimension counts; the metric is Riemannian, B96/B101). State this reading
  **conditionally and geometrically only**: "*if* `W` is the 3-manifold's tangent data (fiber ⊕ base), then the
  dimension identity is `Sym⁴(tangent) = adjoint`." No "spacetime," no "3+1," in the label or the claim.
- **Bound to the open hinge.** S028 acquires content **iff** the tower is *functorially* `Sym^n(W)` for a `W` with a
  canonical geometric origin (the B122 hinge — currently **open**, no functorial map; `W=Fricke` already KILLED by
  the weight/`det` check, §B121). If the hinge resolves to "characters/modules match but no functor," **S028
  collapses** together with the proof-route hope. Record it that way.

## [Fenced note — A2, the spin-2 / gravity overlay]

A separate reading names the two `Sym(V)`-bands "gravitational" (Layer 1 starts at `Sym²` = the spin-2 field) and
"matter/scalar" (Layer 2 at `Sym⁰`), with the band offset "the spin-2 gap." This is the **generic higher-spin
overlay already bounded** (`S010` / the `W_N` literature): `sl(N) ↔ W_N` holds for *every* `N`, singles out no rank,
and "`Sym²` = the physical graviton" needs the 3d-3d dictionary we have **not** established for this object. It is an
**unverified analogy requiring an unestablished 3d-3d dictionary** — recorded here, *underneath* the proved
two-band math (B117/B122/A1), never in `knowledge/`, never sharing a sentence with the algebra. Do not promote.

Related: **B122** (the proved `Sym^n(W)` identity + the open functorial hinge), **B121** (`W` = external
fundamental; the Fricke kill), B117 (the dimension identity), `S010` (the higher-spin bound), `S017`/`S018` (the
DEAD spacetime-from-dimension-count), `PHYSICS_BRIDGE_MAP.md`.
