# S062 — The singular monoid: Thue–Morse as the cancellation verb (firewalled speculation)

**From B493 (the TM endomorphism probe). The mathematics — T1–T7 — is banked and exact; THIS file is
the speculation that motivates it, tagged and firewalled. Nothing here reaches CLAIMS.md.**

Legend: **[MATH]** exact/banked · **[LEAP]** plausible, unproven · **[HOOK]** evocative framing.

## The exact core [MATH]
End(F₂) has two kinds of element on the Fricke stage: the **invertible units** (metallic maps, det ±1 —
the program's object; they preserve the κ-foliation and walk the Markov spine) and the **singular**
element **Thue–Morse** (det 0). TM's trace map T(x,y,z) = (z,z, xyz−x²−y²+2) **destroys the whole
κ-foliation except the reducible leaf κ=2** (κ′−2 = (κ−2)(x²+y²−xyz), exact), collapses to the diagonal
x=y, and acts as **exact angle-doubling** inside κ=2. The units carry log φ; TM carries log 2.

## The speculation [HOOK]
- **The unit = the object comparing itself with its shifted self.** Fibonacci σ: a→ab is "keep, then
  append the shift." Invertible (det −1): the comparison is *reversible* — the record survives its own
  reflection. This is the whole program: the object that records and endures.
- **Thue–Morse = the object comparing itself with its NEGATION.** τ: a→ab, b→ba, t(b)=swap(t(a)) — every
  step appends the *flipped* copy. It is the parity substitution: the ℤ/2 the program already tracks,
  turned into a generating rule. Singular (det 0): the mirror **destroys what it copies**. The dynamics
  is *perpetual self-cancellation* — and the multiplicative cocycle w′ = u²·w (T3) is that cancellation
  frustrated, never completing. [HOOK]
- **The two verbs.** If the units are the object's "memory" (record survives), TM is its "forgetting" /
  "measurement" — the non-invertible collapse. A world needs both: reversible unitary evolution AND
  irreversible collapse. The program has only ever computed the invertible half. TM is the first look at
  the other. [LEAP — the "unitary vs measurement" reading is a rhyme, not a theorem.]

## Why this might matter to the delta [LEAP, firewalled]
The delta (S060): the object lacks scale, multiplicity, dynamics. The singular monoid adds, at least
structurally:
- **A second scale.** log 2 (TM) alongside log φ (units) — two independent growth constants where the
  object had one. Two rates → the germ of a scale ratio. [HOOK — value-matching HELD from birth.]
- **Irreversibility / an arrow.** det 0 is a genuine loss of information (the collapse to x=y). The units
  are time-symmetric (det ±1); TM breaks it. If the object's "dynamics" was missing because it was purely
  reversible, TM is where an arrow could enter. [LEAP]
- **Mixed spectral type (the complementary lens).** Fibonacci = pure-point (order); TM = singular-
  continuous. Their interaction is a candidate for the mixed spectrum real matter has. [HOOK — see the
  planned symbolic-dynamics approach; lit-gate mandatory.]

## The honest negatives (stated, per firewall discipline)
- E1 (entropy = log 2) is NOT proven (conditional on Gromov/Dinh–Sibony + a noncompactness caveat).
- The "unitary vs measurement / duality-defect" dictionary (KW √2, Fibonacci W²=1+W) is a rhyme in
  speculations/ only, pending the emergence bar (Q2).
- No physics number is claimed; any "log 2 / log φ = a physical ratio" is HELD from birth (B398 gate).
- Whether the *interaction* (the mixed semigroup Q1) supplies a delta ingredient is UNRUN — a prereg'd
  open question, not a result.

## Speculation → calculation
| [MATH-HINT] | claim to test | computation | status |
|---|---|---|---|
| the mixed semigroup | do ⟨T_golden, T_TM⟩ words return κ=−2 points to a level set? | Q1: iterate exact ℚ-points of κ=−2 under mixed words | prereg'd, UNRUN |
| second scale | is log 2 : log φ structurally meaningful (not value-matched)? | the joint growth of the mixed semigroup | UNRUN |
| mixed spectrum | Fibonacci × TM = pure-point + singular-continuous together? | the diffraction of the product/combined system | UNRUN (lit-gate first) |
| arrow of time | does det 0 give a genuine irreversibility the units lack? | the non-invertibility structure of T on X(F₂) | [HOOK] |

Cross-refs: [[S060_the_delta_and_the_physiology]] (the delta), [[S061_the_bkl_reading]] (the dynamical
lens), P014 (the cancellation verb, one-way cite).
