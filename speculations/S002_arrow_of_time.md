# S002 — arrow of time = the trace-map flow direction

**Status: `SUPPORTED` (structure); D2 pending. — CORRECTED (B124/V113): at the tower-spectrum level there is NO
arrow (the spectrum is exactly forward/backward symmetric); the live reading is now *two-headed time*, not a one-way
arrow (see the Correction section + `../philosophy/P006`).** Firewalled; not a claim.

**Structural fact (cited).** The three trace-map fixed-point classes have distinct stability (B106): the **trivial**
rep is a repeller, the **geometric** a saddle, the **Dehn-filling** partially elliptic. The flow trivial→geometric
is **one-way**. Separately, Dickson parity (V22) makes monodromy-inversion `N→N⁻¹` twist the odd sectors by `(−1)`,
and the forced `det=−1` slice carries an orientation factor `(t+1)` the `det=+1` slice lacks (B92–B94).

**[LEAP] (kept separate).** Time's arrow = the **unstable direction of the scale-RG**; irreversibility = stretching
along the unstable manifold (positive Kolmogorov–Sinai entropy). The void (trivial rep) is the low-entropy past;
the flow runs away from it. The `(−1)ᵏ` parity asymmetry is the orientation.

**[HOOK] (bounded calc).** **D2**: iterate `T₁²` and map the unstable manifold of the trivial fixed point; ask
whether `log φ` (topological entropy = log spectral radius) is a **monotone** along forward orbits (an H-theorem).

**Negatives beside the leap.**
- The global "hyperbolicity / positive-entropy horseshoe" of the Fibonacci map is an **external** result about the
  *spectral* dynamics (Damanik–Gorodetski), which the repo (PC12 positioning) explicitly distinguishes from our
  trace-map Jacobian — cite it, do not bank it as our object's proven property.
- The "≈62 iterations = 60 e-folds" reading is **removed** — iteration-count ≠ e-folds (that is value-matching, T4
  / `S008`). The arrow's *direction* is Tier 1; any *number* of e-folds is held.

## Correction (B124/V113) — at the spectral level there is **no arrow**: two-headed time, not a one-way arrow

The linearized **tower** spectrum at the trivial fixed point is **reciprocal-closed** `(λ,1/λ)`, and time-reversal
acts as `λ↔1/λ` (generic symplectic, B124). The decisive recompute: **expanding-mode count `==` contracting-mode
count, exactly, every `n`, both `det` signs** — so the spectrum carries **no forward-vs-backward preference**. The
metallic-specific asymmetry is **handedness (chirality, P-like)**, not a time **direction (T-like)**: it breaks `P`
(the `det=−1` `char(−M^h)` sectors, B118/B121) and *preserves* `T` in the mode count. So the `[LEAP]` above
("arrow = the unstable direction") is **superseded at the tower level** by the **two-headed-time** reading
(`../philosophy/P006`, labeled): there is no arrow to derive, only a central reference point and two mirror
directions; reversibility — which we had read as the *obstacle* to an arrow — is recast as the **signature** of
two-directional time (Carroll–Chen / CPT-symmetric resonance). The fixed-point *flow* classes (B106: repeller /
saddle / elliptic) remain a distinct, coarser statement; the reading is firewalled (iteration ≠ physical time).

## [HOOK — DORMANT] the one metallic-specific thread: does the seed select the reference point?

Everything two-headed above is **generic** to symplectic maps (B124 §1) *except* one thing. In the standard
Carroll–Chen picture the central low-entropy "moment" is put in **by hand**; here the fixed point — where the two
mirror manifolds meet — is a **distinguished, seed-determined location**. The honest open question:

> Is the meeting point of the two directions (the fixed point) **selected by the seed** (an *entropic* reason), or is
> it just an external input as in the generic story (only a *spectral* reason — the void is where the abelianization
> `M` acts, B122's void-specificity)?

**Prior (stated, not pre-judged):** likely coincidence-of-structure — the fixed point is distinguished for *spectral*
reasons, not entropic ones, so I expect "external input, no seed-selection of a low-entropy boundary." But it is the
**single thread here specific to us** rather than to all symplectic maps, so it is logged `DORMANT` as the thing to
probe if this direction is pushed.

Related: `S008` (orbit count / e-folds), `PHYSICS_EXERCISE.md` T1.2, `../philosophy/P006` (the two-headed-time
reading), `../knowledge/K008` / **B124**/V113 (the reciprocal/time-reversal math + the det=−1 chirality residue).
