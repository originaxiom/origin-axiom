# S010 — n=4 boundary = `W₄` truncation

**Status: `DORMANT`.** Firewalled; not a claim.

**Structural fact (cited).** degree=rank holds for `n ≤ 4` and **fails at `n=5`** (the forced principal spectrum
`2cosθ = 3−n` reaches `−1`, `A²=I`, a root of unity; B95, proven). Separately, the `W_N` higher-spin
charge-conjugation grading and the Dickson `θ=−w₀` parity grading are the **same involution** (B74, structural) —
though the full Dickson spectrum is strictly richer than the `W_N` one.

**[LEAP] (kept separate, the most dangerous coincidence).** Read the `n=4` last-good-rank as carrying *dimensional*
meaning: `n=4 → 3+1`; the spin-4 / `W₄` truncation as the reason the tower "stops" at four.

**[HOOK] (literature, DORMANT — the honest yes/no).** Does the `n=4 / W₄` truncation have an **independent** reason
to stop at spin 4 (a real cutoff condition), or is it a coincidence? An **L5 literature** question. If it is a real
condition, the dimensionality pointer (`S020` region) has a lead; if not, it dies cleanly.

**[L5 RESULT — 2026-06-07, the yes/no answered].** The anchor is **real and citable** but does **NOT** single out
`n=4`. In 3d higher-spin gravity `hs[λ]` truncates to `sl(N)` at `λ=N` (spins `2,…,N`), so **`sl(4)` Chern–Simons
= spin-(2,3,4) gravity with the `W₄` asymptotic symmetry** (Campoleoni–Fredenhagen–Pfenninger–Theisen,
arXiv:1111.2834; hs[λ]→sl(N), arXiv:1304.0258). *But* the truncation at spin `N` is just "choose `𝔤=sl(N)`" — the
generic `sl(N)↔W_N` — **not** an independent condition forcing `N=4`. So the dimensionality reading gains a
**citation, not a forcing**: there is no spin-4 *cutoff* that distinguishes 4. See
`../frontier/B109_trace_map_dynamics/LITERATURE_L5.md`.

**Negative beside the leap.** This is **a pointer, not evidence** — the same number-matching that killed `ϕ^ϕ` if
treated as more. The naive "char-variety dimension → 3+1" count is already **DEAD** (`S017`); the W₄ anchor is real
but generic; the only *internal* `n=4` distinction is the degree=rank wall at `n=5` (B95), not the W₄ truncation.
S010 stays a held literature thread, now with the citation in hand.

Related: `S020` (3+1D WALLED), `S017`/`S018` (the dead dimension counts), `PHYSICS_EXERCISE.md` T2.3.
