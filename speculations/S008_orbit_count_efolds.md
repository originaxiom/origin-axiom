# S008 — trace-map orbit count (DORMANT) | "iterations = e-folds" (HELD)

**Status: `split` — the orbit-count is `DORMANT(D2)`; the "iterations = e-folds" identification is `HELD`.**
Firewalled; not a claim.

**The DORMANT half (fine, just not run).** Counting trace-map orbits / iterations to a given threshold is a
well-defined dynamical-systems computation on objects we have. It is **DORMANT pending D2** (the flow/basins probe),
not blocked — just queued.

**The HELD half (value-matching).** The identification **"iterations = e-folds"** — e.g. "≈62 iterations ≈ 60
e-folds" — is `HELD(value-matching)`: there is **no derivation** that an iteration count *is* an inflationary
e-fold count, and `62 ≈ 60` over an unconstrained count is exactly the failure mode the HELD rule exists to stop.
The iteration count is a structural quantity; the *e-fold reading* carries no licensed number.

**Rule.** Compute orbit counts (DORMANT→done after D2); **do not** bank any "iterations = e-folds" number as
support without a derivation that the count *is* an e-fold count AND a null test.

Related: `S002` (the arrow / D2 flow), `PHYSICS_EXERCISE.md` T1.2 / Tier 4.
