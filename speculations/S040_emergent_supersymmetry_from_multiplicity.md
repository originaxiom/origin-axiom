# S040 — Emergent supersymmetry from multiplicity, and where the missing structure is external

**Status: `POSTULATED` (a firewalled reading + a research direction, not a result).** Firewalled
(`GOVERNANCE.md`); nothing promotes to `../CLAIMS.md`; the physics chapter stays CLOSED; P1–P16 untouched;
one-way cited only. This is the owner's thesis — *content comes from interaction with something outside; crossing
the wall = interacting with the outside* — taken at full weight on the one place the object actually produces new
content from interaction: the **golden chain's emergent supersymmetry**. Negatives kept prominent; a
**speculation→calculation table**; the **dead ground named** (`S014` Λ-numerology, `S015` tower=masses — both
killed, `TOMBSTONES.md`). Tags: **[MATH]** = established/computed; **[HOOK]** = a real one-way bridge to the
literature; **[LEAP]** = speculation.

## The brave answer, said plainly

I spent two findings reporting that the golden (Fibonacci-anyon) chain flows to a CFT of central charge `c=7/10`
and calling it "a dimensionless number." It is not just a number: `c=7/10` **is** the tricritical Ising model
`M(4,5)` = the **first member of the N=1 superconformal minimal series**. So the object's **multiplicity** — the
golden unit, the *unique* anyon-realizable metallic mean (`B218`), interacting in a chain — produces, by
interaction alone, an **emergent supersymmetric theory**. That is the single most physically-loaded structure the
object has ever generated, and it is exactly the owner's recurring point: *content from interaction*. The honest
job here is to say what is real, what is missing, and where the missing part must come from — **externally** —
without breaching the firewall.

**[MATH] What is computed (the real content).**
- Multiplicity selects golden (`B218`, Jones index); the golden chain's emergent CFT is `c=7/10` (`B220`), and
  this is **exactly** the first N=1 superconformal minimal model — by three agreeing exact derivations (`B221`:
  the GKO coset `(SU(2)₂×SU(2)₁)/SU(2)₃`, the Virasoro minimal `M(4,5)`, the superconformal series `m=3`).
- The emergent SUSY is confirmed at the level of the **spectrum** (`B222`): the full tricritical-Ising operator
  content `{0,1/10,3/5,3/2}` (NS) + `{3/80,7/16}` (R), **including the `h=3/2` supercurrent** — the SUSY
  generator — essentially exact.
- The supersymmetry is **emergent / collective**, not on-site: the golden chain has no conserved fermion parity
  and no exact lattice supercharge (`B223`); exact lattice SUSY lives on a *different* (Fendley–Schoutens) sibling
  of the same Fibonacci Hilbert space.
- The interaction invariant is the multiplicity itself: `content(RᵐLᵐ)=m` (`B221`), tying the SUSY thread to the
  same `m` that governs the period law (`B219`) and the congruence shadow (`B212`).

**[HOOK] The two-SUSY question (a real one-way bridge).** The same golden / figure-eight object carries
supersymmetry **twice**, and they may be one structure seen two ways:
1. the **licensed 3d-3d SUSY** (`K006`): the figure-eight complement defines a 3d `N=2` theory `T[4₁]` whose
   SUSY vacuum moduli space *is* the character variety (`M_SUSY ≅ M_flat`), with **unbroken** SUSY (Cho–Gang–Kim);
2. the **emergent chain SUSY** (`B221/B222`): the golden *anyon* chain flows to the N=1 superconformal `c=7/10`.

The bridge: the golden anyon is the spin-1 primary of **`SU(2)₃`**, which is simultaneously the figure-eight's
**Chern–Simons boundary data** *and* the **coset denominator** of `c=7/10` (`B221`). So `SU(2)₃` sits on both
sides. **Are the two SUSYs the same structure?** Frame as a HOOK; the coset/boundary-CFT relation (`B221`) is the
one-way test — **not asserted**. (The 3d theory is `N=2`, the chain CFT `N=1`; the relation, if any, is via the
boundary/2d reduction, not an identity.)

**[LEAP] The external thesis — what is missing, and where it must come from.** Emergent SUSY exists **only in the
chain** — a single golden unit has no SUSY; the supersymmetry is a property of the *interaction* (`B223`: it is
collective, not on-site). So "content from interaction with the outside" is **literally realized**: the *outside*
each unit sees is its neighbours, and the SUSY is what that interaction generates. But what it generates is a
**dimensionless** structure (a central charge, a superconformal symmetry) — **not** a scale. The honest ledger of
what is still missing for *physical* supersymmetry, and where each piece must come from **externally**:
- a **SUSY-breaking scale** — external (the firewall, `K018`/`B151`: no invariant sources a scale; the scale is
  the level `k↔ℏ` / the squashing). Dead to revive: `S014`.
- an **exact supercharge / the fermions** — external to the anyon chain (`B223`: the exact `Q` lives on a
  different fermionic Hilbert space; the golden chain only has it in the IR).
- **spacetime** (vs 2d worldsheet/superconformal) SUSY — the 4d lift, a named `NEEDS-SPECIALIST` ceiling
  (`K006`); the chain's SUSY is a 2d *worldsheet* superconformal symmetry, not a spacetime one.

So the thesis the firewall earns, restated positively: **interaction manufactures genuine new structure — emergent
supersymmetry — but every dimensionful / spacetime ingredient enters from outside.** The object supplies the
*symmetry*; the world supplies the *scale*. That is the sharpest "more is different" the object has produced, and
it is fully consistent with the firewall holding (the content is structure, not scale).

## Speculation → calculation

| leap | the test (firewall-safe, computable) | status / expectation |
|---|---|---|
| the golden-chain CFT is *supersymmetric*, not just `c=7/10` | identify `c=7/10` as the first N=1 superconformal model (coset, superconformal series) | **[done — `B221`] YES** — three exact derivations agree |
| the spectrum carries the supercurrent | momentum-resolved ED; find the `h=3/2` field + full TCI content | **[done — `B222`] YES** — NS `{0,1/10,3/5,3/2}` + R `{3/80,7/16}`; supercurrent `x=3.0` ~exact |
| the SUSY is on-site (lattice-exact) | conserved `(−1)^F`? exact supercharge `Q` with `H={Q,Q†}`? | **[done — `B223`] NO** — emergent/IR-only; exact SUSY lives on the Fendley–Schoutens sibling |
| the chain SUSY = the object's 3d-3d SUSY | the `SU(2)₃` coset ↔ figure-eight CS boundary data relation | **[open] `[HOOK]`** — `SU(2)₃` on both sides; do **not** assert identity (N=2 vs N=1) |
| the emergent SUSY supplies a scale | any dimensionful output from the chain CFT | **[expected NO]** `[LEAP]` — `K018`: scale is external; the content is a central charge |
| ~~the object produces spacetime SUSY / the SM~~ | — | **firewalled** — 4d lift = `NEEDS-SPECIALIST` (`K006`); not crossed |
| ~~`c=7/10` ↔ E7/E8 ADE = the object's symmetry~~ | ~~ADE modular-invariant classification~~ | **HELD** — a pointer only; no bridge computed (over-reach) |
| ~~tower / multiplicity = a mass spectrum~~ | — | **DEAD** (`S015`; the tower is one-scale monodromy) |

## The lines I will not cross
No numerology (`S014` dead). No tower=masses (`S015` dead). No claim the chain SUSY is **spacetime** SUSY, sources
a **scale**, or yields the Standard Model. No `[proved]` on any physics reading. No promotion to `../CLAIMS.md`.
The 4d / spacetime-SUSY lift stays a named `NEEDS-SPECIALIST` ceiling (`K006`). The E7/E8 ADE angle stays HELD.
The brave move is to name what interaction *does* produce (emergent SUSY) and what it cannot (the scale) — and to
report the boundary honestly.

## Anchors
`../frontier/B221` (the exact SUSY identity + coset + `content=m`); `../frontier/B222` (the spectrum / operator
content / supercurrent); `../frontier/B223` (emergent vs lattice-exact SUSY, the FS sibling); `B218` (multiplicity
selects golden, Jones index); `B220` (`c=7/10` reproduced); `K006` (3d-3d, `M_SUSY≅M_flat`, the `T[4₁]` unbroken
SUSY); `K018` + `B151` (the firewall, scale external); `S039`/`S038`/`S034` (the scale / form↔filling theses);
`S037` (form and filling). Dead: `S014`, `S015` (`TOMBSTONES.md`).
