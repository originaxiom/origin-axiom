# CC3 → CC — COVER NOTE: four relays, one finding, six decisions

cc3 audit seat. Read this page; the other four are reference. Gate 5-Q
throughout. Nothing promotes to `CLAIMS.md`. **`docs/OPEN_LEADS.md`,
`forcing_graph.json`, `build.py` and every ledger are untouched — all four
relays propose, none banks.**

---

## THE ONE FINDING

Three campaigns ran today on unrelated material — a lead ledger, an arc corpus,
and a face taxonomy. Each returned the same defect, and none of them was
looking for it:

| instrument | built to hold | what fell through |
|---|---|---|
| the **lead closures** | statements about *the object* | a closure survives the relational re-read **exactly when its scope sentence names no manifold** |
| the **face classifier** | faces named by an *object* — infinite-hecke 100%, hearing 83%, sln-tower 79% | faces named by a *relation* — meeting **6%**, coupled-double **8%**, mtc-overlay **19%** |
| the **anatomy itself** | kills (`kill_graph`) | every positive result — so the 11 faces currently describe **what the object is NOT** |

**One sentence: our instruments hold objects, and relations fall through them.**
That is the owner's standing judgement — after B800 the object is the maturity
of its relations — arriving as a measurement rather than as a policy, three
times, from three directions.

A fourth instance, and the sharpest, because it happened *inside the audit*:
a re-read agent marked L63 REOPEN by quoting B578's *"L63 stays OPEN"* while
missing B666, which constructs the demanded map 36/36 exact. It indexed claims,
not the relation between claims over time. Withdrawn on verification; recorded
rather than deleted, because it sets the confidence level for everything else
in that relay.

---

## THE FOUR RELAYS, AND WHAT EACH ASKS OF YOU

**1. `CC3_TO_CC_2026-08-08_LEADS_TRIAGE.md`** — the triage you asked for.
43 leads: **30 STALE-CLOSED, 2 STALE-PREMISE, 11 LIVE.** Three of five agents
died mid-stream and the synthesis seat filled its own gaps, so **every citation
was then verified mechanically against `origin/main`** — paths resolved, quotes
normalised and substring-matched with ellipsis splitting; 25/30 automatic, the
other 5 by hand. **No fabricated quote, no missing file.** Appendix V.

**2. `CC3_TO_CC_2026-08-08_RELATIONAL_REREAD.md`** — the pass you did not ask
for, and the one that matters more. Classifying each closure by the era of the
arc that closed it: **24 pre-B800-only, 7 post-B800, 1 no-arc, ZERO mixed.**
The split is total. So the judgement that binds the leads binds the closures,
and re-reading those 24 gives **HOLDS 6, UNDER-READ 11, OVER-WIDE 6, REOPEN 2.**
Only six survive unchanged.

**3. `CC3_TO_CC_2026-08-08_ACCOUNTING_573.md`** — the 77%. They group: 441
placed on existing faces by scan, and of the 132 residue, 54 more to existing
faces, 64 into six new groups, **14 genuinely faceless.** Twelfth-face
candidate: **character-variety / trace-map substrate**, 27 arcs, named
independently by all three readers. Precision is **poor and stated as poor** —
49% recall, 48% strict precision (n=50). The 441 are a **candidate list, not an
attachment.**

**4. `frontier/B796_coupling_campaign/l73_recompute/`** — not a relay, an arc.
The only thing today that changed a verdict by computation, and it changed two:
it falsified L73's closure (the sister m003 has |torsion| = 5; the property
fails inside the commensurability class, at the hearing prime), then **refuted
its own attractive successor** — the "selector" reading — with a base-rate
control showing trivial torsion holds for **60.8%** of one-cusped census
manifolds. Proposed and refuted in the same file, deliberately, so no later
seat reading half of it can re-register it.

---

## SIX DECISIONS THAT ARE YOURS

Ranked by cost, cheapest first.

1. **Fix the `FINDINGS.md` glob in `scripts/forcing/build.py`** (one line). It
   ingests an arc only if that exact filename exists, silently skipping 42 arcs
   that carry real content under another name — including **B1–B5, the first
   five arcs of the programme** (`README.md` only). Does *not* change the 77%
   (I checked: 58–79% either way), so it is hygiene, not discovery — but any
   re-run of the attachment inherits the blind spot until it is fixed.
2. **Do not bank relay 1 alone.** A lead marked STALE-CLOSED whose closure is
   OVER-WIDE must not be counted as closed. Apply relays 1 and 2 together.
3. **Do not treat the 441 as attachments.** 48% precision. Use them as a
   worklist to be confirmed, and re-run only after decision 1.
4. **Attach a scope-correction note to the six OVER-WIDE closures**
   (L1, L54, L57, L77, L84, L93) naming the quantifier actually proved. Each
   already carries the one computation that would settle it. This is a note,
   not a reopening.
5. **Hold L73 and L98 out of any "closed" count.** L73 now has a computed
   falsification in-repo; L98 needs the MST/gap recomputation with a
   non-commensurable control.
6. **Rule explicitly on two taxonomy questions** rather than letting a default
   decide: is `character-variety` a twelfth **face**, or the object's algebraic
   *shadow* (a chart, not a region)? And is `observer` a face at all, given it
   is an axis *acting on* the object rather than a region of it? `physics-door`
   (18 arcs) should be a **boundary label, not a face** — making it one would
   repeat the exact category error that produced the current anatomy.

---

## CALIBRATION — what this seat got wrong today

One withdrawn verdict (L63, above). One synthesis seat that reported 157 arcs
outside the accounting when the true figure is **202** — corrected in relay 3.
One classifier whose control came back at 49% and whose precision came back at
48%, reported as poor rather than dressed up. One attractive finding proposed
and killed by its own control within the hour.

Stated plainly because the relays are long and you should know the error rate
before you read them: **the mechanical claims in all four are reproducible and
were re-derived on this seat; the judged claims are agent work and are labelled
as such in every table.**

---

## STILL OUTSTANDING FROM ME

The parent eigenvalue **landed**: `r = 7.0720041858752050007371941867273`,
gate PASS (9 overlap digits), P4 PASS (spread 0.0). P3 — the displaced control
— is at iteration 3, |g| ≈ 60.5, steps capped, correctly refusing to converge.
On the certificate: the combined PSLQ over both eigenvalues, which closes
Cell 9 rung (i) end-to-end, and the parent joins Plate A as a fifth panel.

Also delivered since the last relay: **C1 discharged** (the `build_moves`
docstring now states the truncation, why it is safe there, and where it must
not be reused), **C4 discharged on your GO** (`PLATE I — THE WALL` rendered and
re-titled; the file rename alone would have left `PLATE L` burned into the
image; `PLATE J — THE NULL` repointed), and **PLATE M — THE THREE ANATOMIES**,
which draws the vocabulary collision behind all of this: *face* is defined two,
three, and eleven ways on main simultaneously, `LAW_MAP.md:170` banks emittance
as *"a real THIRD FACE"* where B730 had already spent the third slot on
*meeting*, and *family* is never defined anywhere at all.

— cc3
