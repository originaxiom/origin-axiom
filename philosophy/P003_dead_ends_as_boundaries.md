# P003 — Dead-ends as boundaries

> **Philosophy — motivation only** (`GOVERNANCE.md`). Not a claim; never a premise. Cites the mathematics one-way.

## The stance

"Bank the negatives" is, in this project, not merely good hygiene — it is a **philosophical position about how the
shape of the possible is known**. A theory of *architecture* (`P001`) is defined as much by its **walls** as by its
rooms. Each killed reading is not a failure to be hidden; it is a **boundary discovered** — a place where the
structure provably does *not* go, which sharpens the outline of where it does.

## Why the deaths are first-class

The strongest results of the whole enterprise are often negatives:
- the cosmological-constant **value** formula is dead (null hypothesis) — this *defines* the numerology line;
- **tower-eigenvalues ≠ masses** (B107) — this *defines* the architecture/furniture boundary (`P001`);
- the **Hessian is `(2,0)` Riemannian, not Lorentzian** (B96/B101) — no spacetime falls out for free;
- the **3d→4d type-mismatch** is a structural wall, not a queued task (`../speculations/S020`).

Each of these *tells us where the boundary is*, and every one is recorded as a first-class object — in
`../docs/atlas/FAILURE_ATLAS.md`, the `../CLAIMS.md` D-rows, and `../speculations/TOMBSTONES.md`. A permanent kill
(`dead → anything` is forbidden, root `../GOVERNANCE.md` §5) is a permanent piece of knowledge about the shape.

## The consequence for how we work

Bravery and honesty are not in tension here; they are the *same* practice. One may speculate as freely as one
likes precisely *because* every wall is mapped and labeled — the labeled room with the honest door
(`../speculations/`). The map of the dead-ends is part of the theory of the possible, not an embarrassment beside
it.

Related: `P001` (architecture-not-furniture), `../speculations/TOMBSTONES.md`, `../docs/atlas/FAILURE_ATLAS.md`.
