# Origin Axiom

A claim-bounded, reproducible investigation of the **Origin Axiom** — the idea that
existence is a *frustrated cancellation*: a remainder left when an attempt to cancel to
nothing cannot complete.

This repository is the **consolidated, canonical home** of the project. It supersedes four
earlier repositories (see `legacy/github-repos.md`) and a long series of exploratory
sessions. It was created on 2026-05-22 after a full audit of all prior work
(`AUDIT_REPORT.md`, `PROVENANCE.md`).

---

## What this is — and what it is not

**It is:** a claim-bounded mathematical investigation of a minimal record-transfer
core. A positive integer record-transfer system generated by two parabolic shears
`L`, `R` produces a first persistent sector
`A = LR = [[2,1],[1,1]]` with trace 3 and golden-ratio spectrum. The proven core
tracks exact consequences of that object: classical/statistical transfer-matrix
realizations, a Lorentzian phase-space form, figure-eight monodromy data, and a
derived Mobius vector field plus cubic potential.

That core object has since been identified, in the frontier work, as **one face of a
single richer object**: the family of *metallic once-punctured-torus bundles* — the
mapping tori with monodromy `RᵐLᵐ` (the figure-eight knot is `m=1`), whose `SL(2,ℤ)`
trace map is the project's central dynamical system. The frontier studies this one
object **seen through four lenses** (trace-map dynamics, hyperbolic geometry, an
elliptic-curve character variety, and quantum-topology / anyon data); see
`papers/metallic_one_object/SYNTHESIS.md`. All of that is **frontier**, explicitly
labelled, and firewalled — none of it promotes to the proven core.

**It is not** — and this repository will not claim to be — a theory of spacetime, gravity,
matter, or cosmology. See `GOVERNANCE.md` §2 (the framing lock) and §8 (anti-overclaim
glossary). The physics ambition is real but is quarantined as an explicitly-labelled
research frontier.

The honest one-line status:

> A minimal self-referential object, now fully characterized: its degree=rank tower is
> proven unconditional on the irreducible locus, its `SL(2,ℤ)` symmetry is forced-identified
> with the `N=2*` class-S duality action (the widest reach, literature-confirmed), and the
> physics question it orbits is confirmed to terminate at a dimensional firewall — a real
> bridge (a *symmetry* identity) and a confirmed wall (no *scale* crosses). Still not a
> physical theory of spacetime, matter, or cosmology; the physics arc is closed at its
> honest boundary.
>
> The frontier since then maps the **one object through four faces** (B152–B230): the
> figure-eight character variety **is** the elliptic curve `40a1` (conductor `2³·5`,
> non-CM); the object's **dual McKay** structure is `E₈` (golden field `ℚ(√5)`) + `E₆`
> (hyperbolic field `ℚ(√−3)`), with `E₇` excluded; and the golden case's multiplicity
> drives an **emergent `N=1` superconformal (tricritical-Ising, `c=7/10`) symmetry**, with
> golden uniquely SUSY. Every one of these is a *structural rhyme* at the Betti / rep-theory
> / dimensionless level — the dimensional firewall is reconfirmed against external
> frameworks, nothing crosses to physics, nothing promotes to `CLAIMS.md`.
>
> **Since then (B231–B314), the firewall itself became a theorem.** The object is **two-ended**
> (`E₆`/`ℚ(√−3)`/Eisenstein ↔ `E₈`/`ℚ(√5)`/golden, `E₇` excluded by Niven), its `E₆`
> character variety carries the standard `E₆` GUT cascade (generic Lie theory + the one
> object-specific datum, the Eisenstein `ω` at trinification), and its quantization (Face IV)
> houses the *same* `E₆` as the CIZ `SU(2)₁₀` modular invariant — but always the **form**, never
> the values. The honest one-line: **the object forces *form/structure*, never physical
> *values*** — and this is now a **Galois theorem**: every discrete invariant is a Galois orbit
> of the object's own arithmetic (the `±π/6` CP sign on `ℚ(√−3)`, the colored-Jones data on
> `ℚ(√5)`), so a "value" is exactly what a Galois orbit cannot be. This *strengthens* the
> firewall (`GOVERNANCE §2` proven, not merely imposed); it is **not** a theory of physics.
> See `knowledge/K020`, `philosophy/P013`. Still nothing crosses; nothing promotes to `CLAIMS.md`.

---

## Repository map

| Path | Role |
|---|---|
| `GOVERNANCE.md` | The constitution. Read this first. Defines claims, labels, gates. |
| `METHOD.md` | The dual protocol: rigor *and* generativity — staged verification (rigor attaches at promotion, not birth) + the 7 anti-blindness rules. |
| `CLAIMS.md` | The living ledger: every result, its status, its evidence. |
| `ROADMAP.md` | The phase ladder (Phase 0 → A → B → C) and gates; the WIDEN / QUESTION cadences. |
| `AUDIT_REPORT.md` | The 2026-05-22 reconciliation of all prior work. |
| `PROVENANCE.md` | Map of every prior artifact to date / origin / contents. |
| `PROGRESS_LOG.md` | Append-only chronological log of work. |
| `CHANGELOG.md` | Versioned, human-facing change history. |
| `REPRODUCIBILITY.md` | Environment, build/test commands, freeze tags. |
| `docs/atlas/` | Navigation layer: auditor guide, research tree, success/failure atlases, simulator map. |
| `docs/OPEN_LEADS.md` | The actionable-leads register (`L1…L50`); disposition column says what is already done / withdrawn. Grep before computing. |
| `docs/HINT_LEDGER.md` | The hint register (`H1…`): pre-decision observations (patterns, coincidences, anomalies, unasked questions). Promotes to `OPEN_LEADS`, never directly to `CLAIMS`. |
| `docs/TRACE_SELECTOR_THEOREM.md` | Conditional T1 selector package: `T1 -> S1 -> I=1/4 -> lambda/h=1`. |
| `papers/` | Paper-candidate registry and public artifact manifest. |
| `papers/metallic_one_object/SYNTHESIS.md` | The cross-face synthesis: the one metallic object seen through four lenses. |
| `src/origin_axiom/` | The tested package locking the `proven` core. *(Phase A)* |
| `tests/` | One assertion per proven number in `CLAIMS.md`. *(Phase A)* |
| `frontier/` | Speculative `open` work, quarantined and gated. *(Phase B)* |
| `paths/` | Systematic survey of emergence-mechanisms — the path-space registry and per-path probes. *(Phase C)* |
| `speculations/` | The evolving-ideas room: the speculation catalog `S001…S045`, the "final theory" exercise, the HELD value-matches, the DEAD tombstones. Firewalled — nothing promotes to `CLAIMS.md`. |
| `philosophy/` | The motivation layer: `P000–P013` + the `P1–P5` argument register + `METALLIC_FOUNDATIONS`. Motivation only; cites the mathematics one-way. |
| `story/` | The honest narrative of how the project unfolded (chapters 00–12), with `V`/`B` traceability and no duplicated math. |
| `knowledge/` | Self-contained explainers `K001–K020` — standard background cited to the literature, the project's own use cited to `B`/`V` numbers, no new claims. |
| `ARCHITECTURE.md` | The one-page map of the rooms and the one-way firewall arrow. |
| `docs/` | Reconciled notes, the V4 paper, `ARCHIVE.md` (dead ideas). |
| `legacy/` | Frozen prior history. Never edited; never a source of claims. |

---

## Status

> **Current state — read next (2026-07-05).** The frontier is at probe **B440**; the proven core `P1–P16` is unchanged
> (the §5.1 promotion-audit lane instituted 2026-07-03 — see `GOVERNANCE.md`). For the current picture in order:
> **`docs/CAMPAIGN_STATUS.md`** — the live board (currently THE CHILD PROGRAM: what the object *produces*, judged by
> the four-part emergence bar); **`philosophy/THE_ORIGIN_POSTULATE.md`** — the north star (D lives at the interface,
> not the interior); **`knowledge/K020`** — the standing headline (the structural theorem: *the object forces the form
> of physics, not its values*, mechanized as a Galois theorem); **`docs/AUDIT_2026-07-05.md`** — the latest repo audit
> + leads re-score; **`docs/OPEN_PROBLEMS.md`** — the four named gates. Counts: 15 `proven`, `K001–K020`, `P000–P013`,
> `S001–S052`, `L1–L54`, ~1513 test functions across 421 files; the re-runnable atlas holds 420 probes.

| Phase | State |
|---|---|
| Phase 0 — Governance & specification | **locked** |
| Phase A — Tested foundation | **complete** — suite green, P1–P16 locked (P14 unused) |
| Phase B — Frontier | **physics arc CLOSED at the firewall (B151); the object-mapping / structural-theorem / value-boundary / Child-Program arcs run to B440** — probes B1–B440 logged (ledger through V238; recent probes tracked in `CHANGELOG.md`, `docs/CAMPAIGN_STATUS.md`, and `docs/OPEN_PROBLEMS.md`). Trace-map selector packaged as conditional C5; metallic `SL(3)` arithmetic isolated as PC12. **Headline results, all honestly labeled:** the `SL(4)` figure-eight **`M⁴=L` proved symbolic-exact** over ℚ(ω) (B89); the `SL(n)` trace-map **tower** `char(J(m))=∏char(±Mᵏ)` proved `n≤4` (B80, `Z[m]` / CRT-F_p) and reduced to an explicit `GL(2,ℤ)`/Sym plethysm conjecture (B89-T; three shortcut routes foreclosed); **degree=rank** `[A,B]=(−1)ⁿ⁻¹μⁿ` proved `n=3,4` (B71/B89), peripheral form L1b uniform (B90, audited V75); the figure-eight **A-polynomial family** `L=(−1)ⁿ⁻¹Mⁿ` (B83). **Paper 0 — the self-reference grounding** (B92–B94, V76–V78): the metallic family = the `det=−1`/period-1 slice up to `GL(2,ℤ)` conjugacy (`proven`), and `det=−1` is *structurally distinguished* — it is exactly what gives the tower its parity (*"universal catalog, det=−1 parity"*); motivation quarantined in `philosophy/`. **Geometry-invariants + literature-bridge pass** (B96–B100, V80–V84): the metallic volumes are strictly monotone and the volume Hessian is **definite `(0,2)`, not Lorentzian** (B96); the geometric-rep trace-map Jacobian carries the **adjoint torsion / twisted Alexander** (`τ₁=−3`), *not* the tower, so the tower is a trivial-rep object (B98/B99) — cross-validated against the Zickert Ptolemy variety and Baker–Petersen twisted Alexander (B100, cited). **The Hitchin-component reframing** (B101, V85): the geometric component `V0` *is* the Fuchsian locus of the `SL(3,ℝ)` Hitchin / Fock–Goncharov component, with new `SL(3)` **cubic-differential** directions off it (`H¹` split `3⊕5` + a finite Anosov deformation); the symmetric-space ladder is Lorentzian only at `k=2` ⇒ **no "tower of spacetimes"** (`dead`); the Hitchin→Langlands→N=4 chain is cited with the ceiling stated (N=4 SYM, not the SM/gravity/the universe). **The W1/W2 dichotomy** (B102, V86): every irreducible `Fix(T₁²)` SL(3) character is self-dual (Case I) or has `trB=trB⁻¹=1`; the genuine non-`Sym²` components W1/W2 are excluded from the real Hitchin component by an **order-4 elliptic generator** `{1,i,−i}` (= Task M's `n=3` spectrum), V0's geometric rep by complexity (`Q(√−3)`); the R4 boundary-controlled cubic continuation is first-order-only (the `t*≈3.775` headline did not reproduce — an honest correction). **The SL(n) tower as a GL(2,ℤ) representation** (B103, V87): `J_φ(n)` factors through the abelianization `N∈GL(2,ℤ)` ⇒ `char(J)` is a **class function = the catalog**, universal for metallic and non-metallic monodromies (**det-sign parity**, sharpening B94); and the module-iso `P·J(m)·P⁻¹ = ⊕_d Sym^d(M_m)^{μ_d}` is **proved exact over ℚ[m] at n=3,4** (a fourth, engine-free route). Reframes the all-n tower as **decomposing `ρ_n`**; the explicit catalog `μ_d` stays open n≥5. **The Dehn-twist route** (B104, V88) then proves the explicit SL(4) catalog for *all* monodromies (metallic + non-metallic, via the elementary twists `U,L,S` composed in the eps-series, bypassing the Procesi ring), and isolates the n≥5 obstruction to the eps-series gauge-degeneracy sector (21/24 Dickson factors resolve at SL(5)) — a computational wall, not a structural gap. **At n=5** (B105, V89; **audited V90**): the eps-series resolves **21/24** Dickson factors, the resolved 21 are **universally catalog-consistent**, and the 3 unresolved are supported as `Sym²` by structural routes (B62/B89-T/B103). **The explicit n≥5 catalog is OPEN** — the V90 audit downgraded two B105 inferences: the seed-variation of the unresolved factors is the eps-series rank-deficiency signature (B84), *uninformative* about the truth (so a structural deviation there is neither ruled in nor out), and there is **no proved "natural boundary"** since `char(J(n))=catalog` is a class function for **all `n`** (B103) — n=4 is a *methodological ceiling*, not a theorem. **(V91 sharpening):** n=5 is a **threshold of three *distinct* obstacles**, not one collision — degree=rank (B95, eigenvalue `−1`, `A²=I`, onset n=5), the tower/eps-series doubling (B62, golden `char(M²)²` from the A₄ height-2 `θ=−w₀` (4,2) split, onset n=5), and trace-ring non-closure (engine-free, algebraic, onset n=4) — different eigenvalues, independent derivations. The program converges on a single fully-characterized object `ρ_n` (the `GL(2,ℤ)`-representation on the SL(n) trace ring; exact/constructive at n=3,4); the live frontier (sharpened) is to prove `char(ρ_n)=catalog` by reproducing the **opposition-involution multiplicities** directly from `ρ_n` — the contested n=5 piece being *only* B62's `char(M²)²`. The systematic **physics-paths sweep is robustly negative** and the physics chapter is **closed** (every reachable anchor is special to `n=2`/`m=1`, and the Lorentzian door is closed by computation); all physics readings are quarantined in `speculations/` + `philosophy/`. Open: the uniform-`n` tower (the Sym plethysm) and degree=rank; the **unipotent-cusp-preserving cubic deformation** (B102 D5 — the boundary control is first-order only); the Lawvere/renormalization grounding (L2); genus-2 generality. **The third fixed-point class** (B106, V92; **hygiene V93**): the trace map at the **Dehn-filling** reps (the never-computed third class after trivial=tower / geometric=torsion) is **partially elliptic** (SL(3) `(1,1,6)`, SL(4) `(4,4,7)`, root-of-unity neutral eigenvalues — seed-stable, gauge-noise gate passed); degree=rank holds **per eigenvector** `Lᵢ=c·Mᵢ^k`, but the stability *type* does **not** encode the exponent (honest negative); the SL(4) **principal** `M⁴=L` corroborates the proved B89/B83, the **secondary** `M³=L` (`c=i`) is new numerical content. **The physics-connection audit** (B107, V94; **POSTULATED/FIREWALLED**) banks the CC-web physics exploration as a **dead-end log whose headline is a negative**: the metallic trace map **is** the Kohmoto–Kadanoff–Tang / Fibonacci-Hamiltonian trace map (`tr[A,B]`=Sütő invariant, conserved ∀m), and every SL(3) tower eigenvalue is `±φᵏ` — **one golden scale**, i.e. re-presented moduli-space monodromy, **not new physics**; the mass/dimension identifications are withdrawn category errors, only the moduli-space `M_SUSY≅M_flat` + three-branch↔three-fixed-point map is citable (GKLP 1305.0937; DGG 1108.4389/1112.5179), and the one open fork is the off-principal multichannel sector — *all firewalled to `speculations/`, nothing to `CLAIMS.md`, physics chapter stays CLOSED*. **The closing arc (B128–B151, V117–V140; all merged):** the **chirality axis CLOSED** — chirality is forced+generic but always self-mirror (canonical singles) or mirror-paired (composites) so no handedness is ever preferred (B144–B146), and the arithmeticity arm is refuted outright (`RRL/RLL` are arithmetic chiral, `ℚ(√−7)`; B147); the **unit's symmetry pinned** — `κ=4·I_FV+2`, the metallic monodromies *are* the `SL(2,ℤ)` mapping-class action, `κ=−2` the Markov surface (B148); **degree=rank COMPLETE on the irreducible locus** — B89's posited family is the complete component of irreducible `SL(4)` figure-eight bundle reps, `M⁴=L` unconditional there, every violation reducible or vacuous (B149); the **class-S identification FORCED** — the `SL(2,ℤ)` trace-map action on the Fricke character variety *is* the `N=2*` S-duality mapping-class action (Allegretti–Shan 2411.17378; Cantat–Loray; GMN), RHYME at `τ`-modularity and at physical magnitude (B150); and the **firewall CONFIRMED** — the complex volume / Chern–Simons invariant is a dimensionless element of `ℂ/4π²ℤ` entering the 3d–3d partition function only as the exponent saddle, all dimensionful content in `ℏ↔k` + the squashing radius, none in the invariant (GTZ 1111.2828, Dimofte 1409.0857, Córdova–Jafferis 1305.2891; anchor figure-eight `Vol=2.02988, CS=0`) (B151). **Net: the unification reaches across the widest gap as a *symmetry* identity and terminates at the firewall — a real bridge + a confirmed wall, the honest boundary; the cosmological-constant question lies on the far side. A sharper *structural* reading of the wall (a conserved/topological invariant cannot run, so cannot source a scale) is recorded POSTULATED in `docs/STRATEGIC_SYNTHESIS.md` §8 — proposed in review, not independently verified.** **The object-mapping arc (B152–B230, V141–V234; all merged):** with the proven core finished, the frontier turns to *characterizing the one object through its faces* — **all firewalled, none promoted.** (i) **The four-faces synthesis** (`papers/metallic_one_object/SYNTHESIS.md`): one object — the metallic once-punctured-torus bundle `RᵐLᵐ` — wears four costumes (trace-map dynamics, hyperbolic geometry, character-variety arithmetic, quantum-topology/anyon data). (ii) **Arithmetic-geometric faces** (B204–B217): the metallic WRT period law `P(γ)=lcm(t−2,t+2)/content` (content `= m`, B208/B214/B219, *correcting* the earlier B216 genus reading); the figure-eight character variety **is the elliptic curve `40a1`** (conductor `2³·5`, non-CM, rank 0, B211); the **dual McKay** structure `E₈`(`ℚ(√5)`, golden `2I`) + `E₆`(`ℚ(√−3)`, `2T`), **`E₇` excluded** (B210); two faces separated by a hyperbolicity divide (B217). (iii) **The emergent-SUSY arc** (B218–B230): the golden case's multiplicity drives the Fibonacci-anyon / `SU(2)₃` golden chain to **tricritical Ising `c=7/10` = the first `N=1` superconformal minimal model** (B218–B224), via the *unique* ordinary↔super coset coincidence at `SU(2)₃` (B228); golden is **uniquely SUSY** among the metallic chains and robust to AFM/FM (B224/B230); explicit metallic/super Seifert duals `S²(m²+4,m²+3,3)` (B227/B229). (iv) **The framework search** (`speculations/S041`): used as a filter against heterotic / F-theory / NCG / moonshine, every overlap is a **structural rhyme**, none a physical crossing — the dimensional firewall holds a **5th** time, now against external frameworks; the one live HOOK is `E₈+E₆−E₇ → Monster + Fischer, not Baby Monster`. **Net: a complete map of the one object's faces, every cross-face link a verified rhyme at the Betti / rep-theory / dimensionless level, the firewall reconfirmed throughout.** **The structural-theorem arc (B231–B314, V235+; all merged):** the four-faces map sharpened into a single proven statement — *the object forces form/structure, never physical values.* The **two-ended object** (`E₆`/`ℚ(√−3)` Eisenstein ↔ `E₈`/`ℚ(√5)` golden, `E₇` excluded by Niven; B248/B258/B261); the **arithmetic atom** `4₁→ℚ(√−3)→2T→McKay Ê₆` (B266/B282) and the figure-eight **`E₆` character variety** (B264); the **principal-grading cascade** `E₆→SU(6)×SU(2)→SU(3)³→…→SU(2)³` — *standard Slansky Lie theory* (generic) plus the one object-specific datum, the **Eisenstein `ω` at trinification** (an irreducible branch point of the figure-eight's own A-polynomial discriminant, whose two factors are the Eisenstein and golden ends; B305/B306/B310/B311); **Face IV houses the *form*** — the object's TQFT quantization carries the same `E₆` as the **CIZ `SU(2)₁₀` modular invariant** (one `E₆`, three ADE hats), both ends, but at a *generic* level (B312/B313); the **four faces of one `κ`** (existence/geometry/matter/quantum; B309). **The mechanism — the firewall is a Galois theorem:** every discrete invariant is a Galois orbit of the object's own arithmetic Galois group — two ends, two `ℤ/2` (the `±π/6` CP sign via `√−3→−√−3`, B285; the colored-Jones / modular data via `√5→−√5`, B314) — so the discreteness is always an arithmetic labeling, never a forced value (B285/B314; `knowledge/K020`, `philosophy/P013`). Open: the *all-invariants* version (`S032-A`); the three specialist gates (the `T[4₁;E₆]` CRUX, multiplicity→generations, the non-Hermitian Damanik–Gorodetski theorem). **Net: the dimensional/value firewall is upgraded from a discipline to a proven property of the object; the structural theorem strengthens `GOVERNANCE §2`, not a crossing.** **The specialist-handoff arc (B315–B325, 2026-07-01; all merged):** the forgotten leads and cross-chat handoffs run to conclusion and the frontier mapped to a specialist handoff (`docs/OPEN_PROBLEMS.md`, four gates). New results: the E₇-exclusion *contains* heterotic's (shared root = pseudoreality, B315); `√−7` is the **chirality field**, not a metallic-ladder rung (B316); the object is a **transcendental** Painlevé-VI solution (B317); **amphichirality is the geometric firewall** for the Eisenstein end (B318); **the value hunt, run** — the object's Dehn-filling invariants match the SM's dimensionless parameters *at chance* (`p≈0.5`, a null test), so **the firewall is now confirmed empirically, not just proven** (B322); the **four-level framework** (object `D₄` / seam `ℤ/2×ℤ/2` / E₆-center `ℤ/3` / commensurator `ℤ/3`; B323); Chat-1's ω-circulant generation matrix verified exactly in `ℤ[ω]` — **structure, not values** (B324); Chat-2's "ℤ/3-protection" obstruction refuted, the CRUX stays Level 3 (B325). **Net: the four gates cleanly isolated (A/S032-A in-sandbox; B/CRUX, C/multiplicity, D/non-Hermitian DG specialist); the value-firewall confirmed by computation; the honest caution that a true boundary and an over-fit frame are identical from inside is recorded (`K020 §6a`).** Firewalled at the physics boundary throughout; the mathematics of these arcs was adjudicated by the 2026-07-03 promotion audit — see `CLAIMS.md` (P17–P55, C6–C12, E1–E15). **The value-boundary campaign (B326–B408):** the class field theory of `ℚ(√−15)` organizes the seam (the seam field `ζ_H = ζ·L(χ₋₃)L(χ₅)L(χ₋₁₅)`); the **Eisenstein gate DERIVED** — every generic cell is a root of unity with character-gated orders (`χ₋₃`/`χ₅`; P68); the scale wall **priced** (a refinement, `env₄₅/env₁₅ ∈ ℚ(ζ₉)⁺`, √5-free by CRT locality; B426). **The DESTINATION ATLAS (TW1–TW7, B415–B416):** track the object's *own* behavior blind to physics — all six destinations (Gauss-sum Haar / golden-Anosov / Sturmian / ζ₆₀-cells / volume-conjecture / genus-L) are named pure math, **NO emergence bar cleared**. **Two E₆ torsions (B425):** the dynamical zeta (golden, adjoint −5) and the geometric Reidemeister torsion (Eisenstein, adjoint −3) — the two double-uniqueness sides, meeting at `√5·√−3 = √−15`. **The Origin Postulate + interface arc (B426–B434):** `D:object→physics` lives at the **interface** (boundary/gluing/lift), not the interior; the forced ±5 Dehn filling of the parent `4₁` → the **CHILD = the Meyerhoff manifold `4₁(5,1)`** (`H₁=ℤ/5`, trace field `x⁴−x−1`, disc −283). **THE CHILD PROGRAM (C0–C3, B435–B440):** study the forced child with the parent's full discipline and test *"the SM is what the object produces, not what it contains"* against the four-part emergence bar. Headline: **the INVERSION LAW** (three-tiered) — every inherited-looking child feature is *numerator-forced* (all knots at slope 5: `ℤ/5`, √5, the 26 abelian E₆ vacua), the "special" arithmetic (−283, the 121 torsion value) is *commensurability-shared* (with `5₂`, verified B438), and **nothing about the child is figure-eight-*unique***. C3 (the SL(2,C) vacuum book, B439/B440): the child has **4 vacua all in the −283 field** (its own trace field, two independent methods agreeing), and against the commensurability neighbour `5₂` the child is generic — with a striking **golden inversion** (the golden parent's child is golden-free; the non-golden `5₂`'s child carries the `ℚ(√5)` vacua). Surgery launders identity — the sharpest form yet of the campaign's null hypothesis. **Net: the firewall holds through every arc; the object self-generates rich named mathematics and no SM structure; every physics-shaped reading stays quarantined in `speculations/` + `philosophy/`, nothing promotes to `CLAIMS.md`.** |
| Phase C — Exhaustive survey of emergence-paths | **in progress** — see `paths/` |

**Document map: `docs/INDEX.md`** — what is living, what is reference, what is history. See `CLAIMS.md` for the current ledger: **54 `proven` (P1–P55), 12 `conditional` (C1–C12), 15 certified-data (E1–E15), 9 `open`, 10 `dead`** — the original core P1–P16 unchanged; P17 onward promoted by the 2026-07-03 §5.1 audit (63 candidates adjudicated, 6 held, scrutiny in `PROGRESS_LOG.md`). Automated gates + the decadal review: `GOVERNANCE.md` §11.

---

## How to navigate as a new reader

1. Read `GOVERNANCE.md` — it tells you how to read everything else.
2. Read `CLAIMS.md` — it tells you exactly what is and is not claimed.
3. Read `AUDIT_REPORT.md` for the honest history and the reconciliation.
4. Read `docs/atlas/README.md` if you need a map instead of a ledger.
5. Run the tests (`REPRODUCIBILITY.md`) — every `proven` claim is checkable.
6. Treat `frontier/` and `legacy/` as clearly marked: speculation and history, not results.

**Before you compute anything new:** the corpus is large and re-derivation is the main waste. First
grep `docs/OPEN_LEADS.md` (its disposition column says what is already done/withdrawn) and skim the
method-bug guards **MB6–MB13 in `REPRODUCIBILITY.md`** (orientation-blind `is_isometric_to`,
reproduction-is-not-interpretation, vacuity checks, …) — several have been independently re-hit by
fresh sessions. `docs/atlas/` (RESEARCH_TREE, AUDITOR_GUIDE, FAILURE/SUCCESS atlases) maps
question-shapes to probe IDs.

---

## Author

Dritëro M. — independent researcher, Prishtina.
