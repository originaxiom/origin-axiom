# Origin Axiom

A claim-bounded, reproducible investigation of the **Origin Axiom** — the idea that
existence is a *frustrated cancellation*: a remainder left when an attempt to cancel to
nothing cannot complete.

This repository is the **consolidated, canonical home** of the project. It supersedes four
earlier repositories (see `legacy/github-repos.md`) and a long series of exploratory
sessions. It was created on 2026-05-22 after a full audit of all prior work
(`AUDIT_REPORT.md`, `PROVENANCE.md`).

---

## What this is — and what it is not

**It is:** a claim-bounded mathematical investigation of a minimal record-transfer
core. A positive integer record-transfer system generated by two parabolic shears
`L`, `R` produces a first persistent sector
`A = LR = [[2,1],[1,1]]` with trace 3 and golden-ratio spectrum. The proven core
tracks exact consequences of that object: classical/statistical transfer-matrix
realizations, a Lorentzian phase-space form, figure-eight monodromy data, and a
derived Mobius vector field plus cubic potential.

**It is not** — and this repository will not claim to be — a theory of spacetime, gravity,
matter, or cosmology. See `GOVERNANCE.md` §2 (the framing lock) and §8 (anti-overclaim
glossary). The physics ambition is real but is quarantined as an explicitly-labelled
research frontier.

The honest one-line status:

> A candidate classical/statistical transfer-matrix framework with topological and
> Lorentzian mathematical structure, plus a conditional uniqueness theorem for its
> core — not yet a physical theory.

---

## Repository map

| Path | Role |
|---|---|
| `GOVERNANCE.md` | The constitution. Read this first. Defines claims, labels, gates. |
| `CLAIMS.md` | The living ledger: every result, its status, its evidence. |
| `ROADMAP.md` | The phase ladder (Phase 0 → A → B) and gates. |
| `AUDIT_REPORT.md` | The 2026-05-22 reconciliation of all prior work. |
| `PROVENANCE.md` | Map of every prior artifact to date / origin / contents. |
| `PROGRESS_LOG.md` | Append-only chronological log of work. |
| `CHANGELOG.md` | Versioned, human-facing change history. |
| `REPRODUCIBILITY.md` | Environment, build/test commands, freeze tags. |
| `docs/atlas/` | Navigation layer: auditor guide, research tree, success/failure atlases, simulator map. |
| `docs/TRACE_SELECTOR_THEOREM.md` | Conditional T1 selector package: `T1 -> S1 -> I=1/4 -> lambda/h=1`. |
| `papers/` | Paper-candidate registry and public artifact manifest. |
| `src/origin_axiom/` | The tested package locking the `proven` core. *(Phase A)* |
| `tests/` | One assertion per proven number in `CLAIMS.md`. *(Phase A)* |
| `frontier/` | Speculative `open` work, quarantined and gated. *(Phase B)* |
| `paths/` | Systematic survey of emergence-mechanisms — the path-space registry and per-path probes. *(Phase C)* |
| `speculations/` | The evolving-ideas room: the speculation catalog `S001…S028`, the "final theory" exercise, the HELD value-matches, the DEAD tombstones. Firewalled — nothing promotes to `CLAIMS.md`. |
| `philosophy/` | The motivation layer: `P000–P006` + the `P1–P5` argument register + `METALLIC_FOUNDATIONS`. Motivation only; cites the mathematics one-way. |
| `story/` | The honest narrative of how the project unfolded (chapters 00–09), with `V`/`B` traceability and no duplicated math. |
| `knowledge/` | Self-contained explainers `K001–K009` — standard background cited to the literature, the project's own use cited to `B`/`V` numbers, no new claims. |
| `ARCHITECTURE.md` | The one-page map of the rooms and the one-way firewall arrow. |
| `docs/` | Reconciled notes, the V4 paper, `ARCHIVE.md` (dead ideas). |
| `legacy/` | Frozen prior history. Never edited; never a source of claims. |

---

## Status

| Phase | State |
|---|---|
| Phase 0 — Governance & specification | **locked** |
| Phase A — Tested foundation | **complete** — suite green, P1–P16 locked (P14 unused) |
| Phase B — Frontier | **in progress** — probes B1–B124 logged (ledger V1–V113). Trace-map selector packaged as conditional C5; metallic `SL(3)` arithmetic isolated as PC12. **Headline results, all honestly labeled:** the `SL(4)` figure-eight **`M⁴=L` proved symbolic-exact** over ℚ(ω) (B89); the `SL(n)` trace-map **tower** `char(J(m))=∏char(±Mᵏ)` proved `n≤4` (B80, `Z[m]` / CRT-F_p) and reduced to an explicit `GL(2,ℤ)`/Sym plethysm conjecture (B89-T; three shortcut routes foreclosed); **degree=rank** `[A,B]=(−1)ⁿ⁻¹μⁿ` proved `n=3,4` (B71/B89), peripheral form L1b uniform (B90, audited V75); the figure-eight **A-polynomial family** `L=(−1)ⁿ⁻¹Mⁿ` (B83). **Paper 0 — the self-reference grounding** (B92–B94, V76–V78): the metallic family = the `det=−1`/period-1 slice up to `GL(2,ℤ)` conjugacy (`proven`), and `det=−1` is *structurally distinguished* — it is exactly what gives the tower its parity (*"universal catalog, det=−1 parity"*); motivation quarantined in `philosophy/`. **Geometry-invariants + literature-bridge pass** (B96–B100, V80–V84): the metallic volumes are strictly monotone and the volume Hessian is **definite `(0,2)`, not Lorentzian** (B96); the geometric-rep trace-map Jacobian carries the **adjoint torsion / twisted Alexander** (`τ₁=−3`), *not* the tower, so the tower is a trivial-rep object (B98/B99) — cross-validated against the Zickert Ptolemy variety and Baker–Petersen twisted Alexander (B100, cited). **The Hitchin-component reframing** (B101, V85): the geometric component `V0` *is* the Fuchsian locus of the `SL(3,ℝ)` Hitchin / Fock–Goncharov component, with new `SL(3)` **cubic-differential** directions off it (`H¹` split `3⊕5` + a finite Anosov deformation); the symmetric-space ladder is Lorentzian only at `k=2` ⇒ **no "tower of spacetimes"** (`dead`); the Hitchin→Langlands→N=4 chain is cited with the ceiling stated (N=4 SYM, not the SM/gravity/the universe). **The W1/W2 dichotomy** (B102, V86): every irreducible `Fix(T₁²)` SL(3) character is self-dual (Case I) or has `trB=trB⁻¹=1`; the genuine non-`Sym²` components W1/W2 are excluded from the real Hitchin component by an **order-4 elliptic generator** `{1,i,−i}` (= Task M's `n=3` spectrum), V0's geometric rep by complexity (`Q(√−3)`); the R4 boundary-controlled cubic continuation is first-order-only (the `t*≈3.775` headline did not reproduce — an honest correction). **The SL(n) tower as a GL(2,ℤ) representation** (B103, V87): `J_φ(n)` factors through the abelianization `N∈GL(2,ℤ)` ⇒ `char(J)` is a **class function = the catalog**, universal for metallic and non-metallic monodromies (**det-sign parity**, sharpening B94); and the module-iso `P·J(m)·P⁻¹ = ⊕_d Sym^d(M_m)^{μ_d}` is **proved exact over ℚ[m] at n=3,4** (a fourth, engine-free route). Reframes the all-n tower as **decomposing `ρ_n`**; the explicit catalog `μ_d` stays open n≥5. **The Dehn-twist route** (B104, V88) then proves the explicit SL(4) catalog for *all* monodromies (metallic + non-metallic, via the elementary twists `U,L,S` composed in the eps-series, bypassing the Procesi ring), and isolates the n≥5 obstruction to the eps-series gauge-degeneracy sector (21/24 Dickson factors resolve at SL(5)) — a computational wall, not a structural gap. **At n=5** (B105, V89; **audited V90**): the eps-series resolves **21/24** Dickson factors, the resolved 21 are **universally catalog-consistent**, and the 3 unresolved are supported as `Sym²` by structural routes (B62/B89-T/B103). **The explicit n≥5 catalog is OPEN** — the V90 audit downgraded two B105 inferences: the seed-variation of the unresolved factors is the eps-series rank-deficiency signature (B84), *uninformative* about the truth (so a structural deviation there is neither ruled in nor out), and there is **no proved "natural boundary"** since `char(J(n))=catalog` is a class function for **all `n`** (B103) — n=4 is a *methodological ceiling*, not a theorem. **(V91 sharpening):** n=5 is a **threshold of three *distinct* obstacles**, not one collision — degree=rank (B95, eigenvalue `−1`, `A²=I`, onset n=5), the tower/eps-series doubling (B62, golden `char(M²)²` from the A₄ height-2 `θ=−w₀` (4,2) split, onset n=5), and trace-ring non-closure (engine-free, algebraic, onset n=4) — different eigenvalues, independent derivations. The program converges on a single fully-characterized object `ρ_n` (the `GL(2,ℤ)`-representation on the SL(n) trace ring; exact/constructive at n=3,4); the live frontier (sharpened) is to prove `char(ρ_n)=catalog` by reproducing the **opposition-involution multiplicities** directly from `ρ_n` — the contested n=5 piece being *only* B62's `char(M²)²`. The systematic **physics-paths sweep is robustly negative** and the physics chapter is **closed** (every reachable anchor is special to `n=2`/`m=1`, and the Lorentzian door is closed by computation); all physics readings are quarantined in `speculations/` + `philosophy/`. Open: the uniform-`n` tower (the Sym plethysm) and degree=rank; the **unipotent-cusp-preserving cubic deformation** (B102 D5 — the boundary control is first-order only); the Lawvere/renormalization grounding (L2); genus-2 generality. **The third fixed-point class** (B106, V92; **hygiene V93**): the trace map at the **Dehn-filling** reps (the never-computed third class after trivial=tower / geometric=torsion) is **partially elliptic** (SL(3) `(1,1,6)`, SL(4) `(4,4,7)`, root-of-unity neutral eigenvalues — seed-stable, gauge-noise gate passed); degree=rank holds **per eigenvector** `Lᵢ=c·Mᵢ^k`, but the stability *type* does **not** encode the exponent (honest negative); the SL(4) **principal** `M⁴=L` corroborates the proved B89/B83, the **secondary** `M³=L` (`c=i`) is new numerical content. **The physics-connection audit** (B107, V94; **POSTULATED/FIREWALLED**) banks the CC-web physics exploration as a **dead-end log whose headline is a negative**: the metallic trace map **is** the Kohmoto–Kadanoff–Tang / Fibonacci-Hamiltonian trace map (`tr[A,B]`=Sütő invariant, conserved ∀m), and every SL(3) tower eigenvalue is `±φᵏ` — **one golden scale**, i.e. re-presented moduli-space monodromy, **not new physics**; the mass/dimension identifications are withdrawn category errors, only the moduli-space `M_SUSY≅M_flat` + three-branch↔three-fixed-point map is citable (GKLP 1305.0937; DGG 1108.4389/1112.5179), and the one open fork is the off-principal multichannel sector — *all firewalled to `speculations/`, nothing to `CLAIMS.md`, physics chapter stays CLOSED* |
| Phase C — Exhaustive survey of emergence-paths | **in progress** — see `paths/` |

See `CLAIMS.md` for the current ledger: 15 `proven`, 5 `conditional`, 9 `open`, 10 `dead`.

---

## How to navigate as a new reader

1. Read `GOVERNANCE.md` — it tells you how to read everything else.
2. Read `CLAIMS.md` — it tells you exactly what is and is not claimed.
3. Read `AUDIT_REPORT.md` for the honest history and the reconciliation.
4. Read `docs/atlas/README.md` if you need a map instead of a ledger.
5. Run the tests (`REPRODUCIBILITY.md`) — every `proven` claim is checkable.
6. Treat `frontier/` and `legacy/` as clearly marked: speculation and history, not results.

---

## Author

Dritëro M. — independent researcher, Prishtina.
