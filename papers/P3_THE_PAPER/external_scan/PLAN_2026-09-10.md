# THE PLAN — from the external seat, 2026-09-10

Written on being asked to take over, with the stated goal of a full ToE. It does not plan one, and
§0 says why before anything else. What follows is a plan for the rows that are actually advanceable,
with the closed rows named as closed.

---

## §0 — What cannot be planned, and why that is the record's own finding

A ToE plan for this object would contradict theorems this programme proved itself. Stating them once:

| row | why it does not close from this object |
|---|---|
| **rank reduction** | the cascade computes centralizers of torus elements; a centralizer contains a maximal torus, so **every measurement is rank-preserving** and no number of them reaches rank 4. Reaching the SM needs a breaking mechanism (VEV / Wilson line / orbifold) — GUT requirement #11, which the object does not supply. |
| **values (0 of 19)** | §6 is exhaustive at the declared heights: no object period is an SM ratio, no regulator enters an algebraic relation, and the hypercharge normalisation is **not derivable in principle** — homogeneity of the anomaly equations, true for any U(1) in any theory. |
| **`l` (the unit)** | external by design, permanent. Nobody derives the metre. |
| **`lambda`** | external **by theorem** — the object's algebra is tracial, its modular flow trivial, so it has no continuous clock to give. |
| **dynamics, gravity** | no derived action on main; a spin-2 *slot* with no propagator and no coupling. Model input in every candidate, this one included. |
| **7 of 11 GUT rows** | proton decay, doublet–triplet, fermion masses, neutrino mass, monopoles, baryogenesis, breaking sector — absent, and #11 structurally so. Plus the 12 exotics per generation, a named gap. |

A plan that promises to close these is the failure mode §2 of THE PAPER exists to prevent: arriving at
a target and calling the arrival evidence. **The honest headline is that this programme produced a
boundary result, and a boundary is worth more sharpened and refereed than widened and unread.**

---

## §1 — Tier 1: closeable now

### 1.1 Close the chirality row as a proved boundary — the highest-value single move

Row 2 of the ToE ledger is the only row currently graded **WALL** rather than absent. The evidence now
says it should close as a *theorem*, not stay open:

- 9 108 sectors in domain D, 98 census manifolds, three primes, Sym¹ and Sym² — **every index zero**
  (`external_scan/FINDINGS_EXTERNAL_INDEX_SCAN.md`);
- non-vanishing occurs **exactly** where reductivity fails, and reductive + parabolic cusp forces
  irreducibility, so **D is self-enforcing**;
- axiom 5 **provably** yields an amphichiral object (every orientation double cover is amphichiral;
  250/250) — the bit is *spent by the axiom*, not withheld by the arithmetic.

**The work:** prove, on domain D, `h^1(M;V) = h^1(M;V*)` — equivalent to `I = 0` by the record's own
identity `F = (a0 - a0*) + r1 - t0`. Duality alone gives only the sum `r1 + r1* = t1`; equality is the
content. **Deprioritise the 54 multi-cusped covers**: they vary the cover, and the evidence says the
cover does not control the answer.

**Payoff:** row 2 converts WALL → CLOSED-BY-THEOREM, L202 retires rather than extends, and §7 gains a
mechanism in place of a census.

### 1.2 Split the paper and submit Paper 1

`papers/P3_THE_PAPER/EXTERNAL_REVIEW_2026-09-09.md` §2 has the argument. Paper 1 (math.GT, ~20pp):
§7's topology plus Theorem 4.1 — novel, checkable, **independent of whether the E₆/SM story is right**,
and the easier arXiv endorsement. Blockers: the author block is still `[author]`; the chain table
claims 34 theorems against one `theorem` environment; the appendix should stop describing verification
as wholly internal now that an outside bench has reproduced the covers census, the 2804 split, the
1200-manifold parity, the base rate, the hypercharge cubic and the SL(2,Z/4) argument.

**Payoff:** refereed standing. This is the single largest credibility multiplier available, and it does
not depend on any open row.

### 1.3 Name the select-by-extension-over-m000 mechanism

Three instances: R037 (the 2T quotient class), R039 (the A4→2T lift), and §9's spin-lift row. The paper
uses it once and never names it. **The work:** is it one theorem? If yes, §9 loses input rows. If no,
decline it explicitly — codex's fence (`I-6 remains UNEARNED`) is the model.

---

## §2 — Tier 2: genuinely open, worth attacking

### 2.1 The `sigma` row via the conformal embedding

`c(su(3)_1) = 2`, `c((E6)_1) = 78/13 = 6`, so **`su(3)_1^3 subset (E6)_1` is a conformal embedding**
(verified). The σ row needs the boundary theory to be `(E6)_1`; §5's trinification frame is three
orthogonal A₂s. **The paper treats these as unrelated.** The anti-numerology clause is satisfied
because the frame is already the object's, not imported to fit. Does not by itself supply the graded
character or meet the six conditions.

### 2.2 The APS re-read of `Omega_3^SO = 0`

The record grades the 4d filling **impossible-by-theorem as canonical** because fillings are infinitely
non-unique. That conflates *selecting* a filling (impossible, and unnecessary) with obtaining a
*filling-independent invariant* (what APS is built to deliver: the bulk term's dependence is cancelled
by the eta-invariant boundary term). On that reading `Omega_3^SO = 0` is the **enabling hypothesis**.

**The competence is already in-house:** codex R040 proves its closed theorem with CGHN/**APS**
(`3 eta = 2 cs + tau mod 2`). `main.tex` mentions APS nowhere. Three checks first: the twisted
non-unitary E₆ setting, the cusped rather than compact case (Atiyah–Donnelly–Singer), and whether the
resulting index is `t0 - r1` or a different functional.

### 2.3 Finish the codex harvest

R038, R039, R040 are rowed OPEN in B1325 and not re-derived. R040 must be banked as **two separate
grades** at the seat's own request, and its census must **not** be promoted to a universal cusped
theorem.

---

## §3 — Standing discipline

- **The stale-claim gate (B1326) is now live.** Every future retraction should declare
  `retracted.stale_phrases`. The sweep says the class currently has exactly one member, already fixed.
- **Cite the prior art.** Asselmeyer-Maluga reaches the same doorway — fermions as hyperbolic knot
  complements, Mostow as the reason for the category, the figure-8 named as his arithmetic example,
  Maclachlan–Reid cited — and stops where this programme continues. §10 should say so; the disclosure
  strengthens the paper. Add the 3d-3d correspondence separately.
- **Two claims to keep retired.** The "three generations convergence" with his model is not one (his
  three is a claim; §7 declines three). And this bench's amphichirality result is the *weaker sibling*
  of codex R040's CS statement, which reached the same involution first and over a larger census.

---

## §4 — What success looks like

Not a ToE. A **refereed boundary**: a published topology paper whose results stand independent of the
physics; a chirality row closed by theorem rather than left open; a freedom ledger whose every row is
either closed, external-by-theorem, or permanent-by-design; and a prior-art section that names the one
programme that got closest.

That is reachable within the record's own theorems. A ToE is not, and the reason is in §0 — which the
programme discovered, and which is the most valuable thing it has.
