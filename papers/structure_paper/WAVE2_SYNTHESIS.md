# WAVE 2 — ARCHITECTURE: three plans, one independence verdict, and what they agree on

*Four agents, deliberately incompatible mandates. **Where they converge independently, that
is signal.** Where they split, the split is the decision.*

---

## 1. UNANIMOUS — three architects, arrived at separately

| decision | A | B | C |
|---|---|---|---|
| ## **Retire "nine independent selections"** | ✅ | ✅ | ✅ |
| **Cut the scattering determinant** | ✅ | ✅ | ✅ |
| **Cut / demote Prop 2880** | ✅ | ✅ | ✅ |
| ## **Promote B1019** — silver has *no door*, not "fails four of five tests" | ✅ | ✅ | ✅ |
| **Keep the negatives at full strength** | ✅ | ✅ | ✅ |
| **Cut or isolate ±-diagonality** | cut | cut | conditional |

**Three agents, three mandates, one conclusion on the count.** The analyst proved it, and the
architects reached it independently while building:

> **Collapse lemma (verified here).** With `χ_m(t) = t² − (m²+2)t + 1`:
> `χ_m(1) = −m²` (Selection I) · `χ_m(−1) = m²+4` (Selection II) · `tr φ_m = m²+2`
> (III, V, VI, VII). **Six of the nine test three values of one polynomial, and each reduces
> to `m² = 1`.**

And the paper hands a referee the weapon itself: it proves `det(φ_m−I) = −m²`, then two
subsections later defines the shadow modulus as `det(φ_m+I)` — under the banner *"the four
arguments share no lemma."* **That sentence is now withdrawn in the draft**, replaced by a
proved collapse lemma and the mechanism-vs-extension distinction. B224's own findings said
it years ago: *"characterized four independent ways, **all through the number 5**."*

---

## 2. THE BIGGEST TECHNICAL WIN — verified, and it fixes the acceptance test

Architect A found that the cascade rungs need not be certificates at all. A centralizer of a
subset of a Cartan is a **Levi subalgebra**, so `dim = 6 + |Φ(sub)|`, and the sub-diagram is
pinned by root count:

```
   dim  46  ->  40 roots  ->  D5        UNIQUE  -> FORCED
   dim  30  ->  24 roots  ->  D4        UNIQUE  -> FORCED
   dim  26  ->  20 roots  ->  A4        UNIQUE  -> FORCED
   dim  14  ->   8 roots  ->  A2+A1     UNIQUE  -> FORCED
   dim  18  ->  12 roots  ->  A3 or 2A2 AMBIGUOUS
   dim  12  ->   6 roots  ->  A2 or 3A1 AMBIGUOUS
```

> ## **The terminus `A₂⊕A₁` upgrades from CERTIFICATE to THEOREM.** The certificate now
> supplies only an integer (`dim = 14`); the classification does the forcing. **This is
> precisely the acceptance test's weak point, closed** — a referee who refuses machine
> computation gets a conditional *"if dim = 14 then the type is A₂⊕A₁"*, which is still a
> theorem they already believe.

A was also honest that 18 and 12 are **not** pinned — verified, and it should be stated.

**And it settles the `26` question.** `A₄` is a genuine `E₆` sub-diagram, so `su(5)⊕u(1)²` is
a legitimate Levi. "26 is not attained" is a census over computed directions, **not** a
theorem about `E₆` — the paper must say the second thing.

---

## 3. FOUR NEW DEFECTS, ALL VERIFIED HERE

| # | defect | verified |
|---|---|---|
| 1 | ## **The census does not add up.** The paper prints `26+6+5+1+1 = 39` typed links **and** 5 declared choices, from 43. `39+5 = 44`. | Recounted: `26 THEOREM · 6 IDENTITY · 5 NO-GO · 4 AXIOM · 1 COROLLARY · 1 CENSUS = 43` — **four** axioms. The draft amended 4→5 **without adding a link**, because the principal placement *is not a link* |
| 2 | ## **A missing declared choice: the puncture.** The ledger's own audit says *"the chain's price is exactly two FRAGILE forks: **F5 orientation and F6 the puncture**."* The paper prices F5 and never mentions F6 | The closed Sol sibling loses being, cusp and arithmeticity — **but keeps the hearing.** Same grade as the "honest scar" |
| 3 | **The forcedness gate certifies the wrong stretch.** It asserts no axiom in `C6..C17`, describing that as *"the stretch in which e₆ arrives."* **`e₆` first appears at `C24`**, and `C18 [AXIOM]` sits between | confirmed against the ledger |
| 4 | ## **B1019's "ADE cap of 120" is FALSE** — and all three architects wanted to promote B1019 | Binary dihedral `Q₄ₙ` has order `4n`, **unbounded**. Bronze must be excluded *structurally*: `PSL(2,13)` simple ⟹ `SL(2,13)` non-solvable ⟹ neither cyclic nor binary dihedral, and `2184 ∉ {24,48,120}` |

**Defect 4 is the campaign justifying itself again**: a false statement inside the arc every
architect independently wanted to promote. The arc's *code* is right; only its prose is loose.
A Wave-4 drafter copying that prose ships a falsehood into the negative control.

---

## 4. WHERE THEY SPLIT — the real decision

| | **A — survivability** | **B — the shape** | **C — the door, priced** |
|---|---|---|---|
| the paper is | **the cost classification** | **the recurring shape** (M1/M2/M3 schema) | **the entrance, priced** |
| length | ~19 pp | ~23 pp | ~32 pp |
| selections carried | 4 (I,II,III,IV) + IX repositioned | 6 by mechanism + 2 rows | 3 + the pair axis |
| §8 real form | **cut entirely** | cut | kept, conditional |
| stipulations | **6** | 4 | **6** |
| headline risk | low | medium | medium |

**A and C independently arrive at six stipulations** — and C names the sixth (the puncture)
that A had not. **That agreement, from opposite mandates, is the strongest signal in Wave 2.**

**One genuine disagreement worth your call:** the genre base rate (*one marginal
advance-prediction in twenty years*). B and C want it; **A cuts it**, arguing a "106-agent
panel" is not a citable instrument in a `57K10` paper and that naming the genre relocates the
paper into a worse reference class. **A's reasoning is better than the majority's.**

---

## 5. RECOMMENDATION

**The three are not incompatible.** A's cost classification *is* C's ledger; B's schema is a
framing that fits over either.

> ## **Take A's architecture. Take C's corrected ledger — six stipulations, including the
> puncture. Take A's Levi upgrade, which is the real mathematical gain. Take B's F5
> falsifier as one paragraph, because it makes the choice ledger and the shape claim the same
> statement seen twice.**

Concretely: **~20 pages**, ten sections, terminus `A₂⊕A₁` **forced by Levi classification
rather than certified**, six priced stipulations, four selections with the collapse displayed
rather than hidden, `B1019` as the negative control **with its false gloss repaired**, and the
negatives at full strength minus the one row whose artifact is missing.

**What this costs:** the real form, the `27`, the atoms, the signature `(15,12)`, `D₂`. All
past the terminus, all resting on a premise that is not proved.

**What it buys:** every load-bearing claim is a proof, a citation, or an exhaustive
classification — and the certificates supply integers to classifications rather than carrying
conclusions.

---

## 6. WAVE 3's DOCKET

1. Adjudicate the genre base rate (A vs B/C).
2. **Price the period-one choice** — C calls it *"the highest-value unrun cell in the whole
   campaign, and it is cheap."* If it runs, unpriced stipulations drop from two to one.
3. Repair the census arithmetic and the gate's certified stretch.
4. Repair `B1019`'s cap gloss **before** any drafter touches it.
5. Route back the four unlocated claims Architect B could not source.
