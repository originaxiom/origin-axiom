# WAVE 1 — THE CLAIM POOL (5 of 6 territories in)

*Preproduction output. **Nothing is designed or written from this until it is read.**
Five inventory agents have reported; the entrance territory is still running. Every lock
cited below was actually executed — roughly **1,000 test functions** across the five
reports, plus every gate.*

---

## 0. The headline: the paper undercounts its own spine

The paper claims **four** independent selections of the golden member. The corpus banks
at least **nine**, most with green locks, and five of them are absent from the paper
entirely.

| # | selection of `m=1` | status | lock | in paper? |
|---|---|---|---|---|
| I | `H₁(Mₘ) = ℤ⊕(ℤ/m)²` — only `m=1` is a knot complement | PROVED | `test_b251_metallic_specificity.py` ✅ | yes |
| II | `\|SL(2,ℤ/Nₘ)\|` exceptional only at `m=1` | PROVED (infinite family) | `test_b997_…py` ✅ | yes |
| III | `λₘ < 2` — the Jones index wall | PROVED | `test_b218_…py` ✅ | yes |
| IV | arithmeticity → `{1,2}` | COMPUTED | `test_b125_…py` ✅ | yes, with its limit |
| **V** | ## **the unique hyperbolic-volume minimum** among torsion-free positive-word bundles (`B197`) | **PROVED** | `test_b197_…py` ✅ | ## **NO** |
| **VI** | ## **the unique metallic mean whose anyon chain is superconformal** — `M(4,5)` the only unitary minimal model that is also `N=1` SUSY (`B224`) | **PROVED** | `test_b224_…py` ✅ | ## **NO** |
| **VII** | ## **`SU(2)₃` is the unique level where the ordinary and `N=1`-super coset constructions coincide** (`B228`) | **PROVED** | `test_b228_…py` ✅ | ## **NO** |
| **VIII** | ## **`\|Z_k\| = 1` at every non-vanishing level is `m=1`-unique** (`B132`; K016 calls it *"the strongest single selection criterion"*) | COMPUTED | `test_b132_…py` ✅ | ## **NO** |
| **IX** | ## **`tr[A_m,A_n] = 2 − (mn(n−m))²`; parabolic ⟺ `(m,n) = (1,2)`** — a *pairwise* selection (`B471`) | **PROVED** | `test_b471.py` ✅ | ## **NO** |

Plus **`B482`/T-TWOTEETH** — the twisted Markov spectrum below 3 is *exactly* `{√5, 2√2}`
— a second characterization of the `(1,2)` pair **from the paper's own genesis
language**, with no `tests/` lock.

> **This is the single most important Wave-1 finding, and it is the one that vindicates
> preproduction: the paper's spine is stronger than the paper knows, and I could not have
> learned that by editing the paper.**

---

## 1. What must be repaired before anything is written

| # | defect | evidence |
|---|---|---|
| 1 | ## **The scattering claim is SUSPENDED** | The corpus banks **two contradictory formulas** at arc-verdict authority — `Λ_K(s−1)/Λ_K(s)` (B739, C10) vs `Λ_K(s)/Λ_K(s+1)` (B737, LAW_MAP) — with no convention stated; neither lock covers the identity; and the programme's own audit records the result as **generic to every one-cusped `PSL(2,O₃)` quotient**, *"not m004-specific"* |
| 2 | ## **The ±-diagonality premise is not proved** | `B901` — the arc it is attributed to — contains **zero** occurrences of "diagonal"; `B907` calls it *"the ±-diagonal constraint — **the premise**"*. The gap is a **3-cycle** on the three enhancement lines |
| 3 | **C1/C2 have no lock** | `THEOREM_LEDGER` cites `test_b749_genesis_forks.py` for Morse–Hedlund and Hurwitz; the file defines only `f4,f5,f6,f7` and mentions neither. Defensible (both classical) but the citation is false |
| 4 | **The ladder is "includes", not "is"** | banked as a set of realized strata; the non-attainment of `26` is a census over coordinate subtori, not a theorem |
| 5 | **Crossing 5 has no surviving artifact** | `refresh_windows.log` absent; the `44°`/`2.21×`/eight-misses figures the paper prints have a red lock |
| 6 | **`B1066` — the exhaustion census — has ZERO locks** | the paper's most-quoted negative is the one with no executable check |
| 7 | **"Exhausted" is a bounded enumeration** | arity ≤ 3, fixed menus, kind-licensed; the arc keeps a **residual listener-map door** open. The load-bearing word is *licensed*, never *exhausted* alone |

---

## 2. Conflicts — reported, not resolved

- **B123 vs B125** — arithmeticity selects `{1}` or `{1,2}`? B125 overturns B123; no supersession marker on B123.
- **B996 vs B997** — is McKay access golden-specific? Reconciled *inside B997's text* (fixed modulus vs own conductor); a ledger reader sees a flat contradiction, and **B996 has no lock while B997 does**.
- **B92's own claim line** ends *"m is free, m=1 selected only by the systole metric"* — contradicted by the nine selections above.
- **B258 vs B125** — "trace field" in two senses; B258's `(non-arith)` gloss is wrong, its degree is not.
- **B218's arc line vs CLAIMS P43** — the "unique anyon-realizable" wording was **withdrawn**; the arc still carries it.
- **40a1 vs 15A8** — reconciled in a review log, in no ledger row.
- **Congruence level `(4)` vs `(8)`** — convention, documented, but the two arc headlines contradict verbatim.

---

## 3. Strong material absent from the paper, beyond the selections

`B864` hypercharge uniqueness (**proved**, the unique gaugeable abelian direction) ·
`B862` the `ℤ₆` global form · `B854`'s **transvectant reduction** collapsing abelianness
to one bracket · `B908`'s Leibniz-sign *mechanism* · `B877`'s weight-line lemma making
the cyclic law **a priori** · `B927` the genre base rate — *one marginal advance-prediction
in twenty years*, from a 106-agent panel · `B995` the **self-voided instrument** (the
seals binding *against* the programme) · `B1023` — **`∂S/∂σ = −Vol ≠ 0`: the object is
blind in `k` and SIGHTED in `σ`** · `B680` `Vol(4₁) = (3√3/2)·L(χ₋₃,2)` exactly ·
`B794` the mod-4 trace-norm law · `B211` the character curve ↔ elliptic `40a1` at 23
primes · `B781` m004 vs its sister m003 · `P12` the gluing equation factoring as
Eisenstein × golden.

---

## 4. Instruments repaired during Wave 1

- **The retraction gate was blind** to `.tex`, to `docs/views/`, and to cue-distance. Widened
  to 4,157 files; **found 8 live violations where it had reported 0**.
- **`test_b1034_l154.py` was FAILING** because of my own `TERMINOLOGY_POLICY.md`. Registered.
- **The forcedness census still certifies a sentence the paper has withdrawn.** Annotated.

---

## 5. What Wave 2 must decide

1. **Which of the nine selections the paper carries**, and in what order. Nine is not
   automatically better than four — independence must be checked, not assumed.
2. **Whether the chain or the selection theorem is the paper.** §§8–10 is four pages the
   paper itself calls standard.
3. **What happens to the suspended and premise-bearing results** — carried conditionally,
   demoted, or cut.
4. **The genre base rate and the self-voided instrument**: both strengthen §9 and both
   are absent.

**Nothing above is a decision. Wave 2 makes decisions; Wave 1 only says what exists.**
