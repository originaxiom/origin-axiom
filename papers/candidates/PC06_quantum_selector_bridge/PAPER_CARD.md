# PC06 -- Quantum Selector and State-Integral Bridge Problem

Status: paper candidate. Literature bridge, not solved theorem.

## Classification

```text
Type: LITERATURE_BRIDGE
Readiness: NEEDS_VALIDATION
Priority: second-tier, after PC02 and PC04
Main risk: state-integral host is real, selector theorem is missing
```

## One-Sentence Thesis

The figure-eight state-integral route is a serious mathematical host, but the
needed nonzero contour, thimble, or relative-homology selector is not yet forced.

## What Is Genuinely New

The latest campaign sharpened the quantum-topology bridge from a vague target
into a precise theorem question:

```text
state integral can recover the figure-eight A-polynomial host
local work does not force the nonzero contour or thimble class
the missing object is a selector theorem, not another numerical match
```

The useful output is a well-posed bridge problem:

```text
construct a source-normalized quantum-dilog/Picard-Lefschetz theorem
forcing a nonzero relative-homology or thimble-intersection class
for the figure-eight state integral
```

## What Is Not Claimed

```text
not a proof that the state integral selects the Origin core
not a proof that a contour selector exists
not a derivation of gauge fields, particles, or units
not a physics observable
not a replacement for source-level literature audit
```

## Evidence Files

```text
frontier/V5_R10_dilog_monodromy_residue/
frontier/V5_R11_contour_winding_selector/
frontier/V5_R31_quantum_dilog_literature_dossier/
frontier/V5_R32_resurgence_stokes_selector/
frontier/V5_R36_quantum_dilog_source_audit/
docs/progress/2026-05-28_quantum_selector_campaign_synthesis.md
```

Campaign theorem questions to preserve:

```text
G1_HIKAMI_CONTOUR_SELECTOR
G1_THIMBLE_INTERSECTION_GATE
```

## Reproduction Commands

Specific commands should be frozen after public atlas nodes are written for the
state-integral route. Baseline repository check:

```bash
python -m pytest -q
```

## Required Controls

```text
separate host reconstruction from selector derivation
state whether a contour is supplied or forced
avoid using the desired A-polynomial branch as a hidden target
separate source-level theorem facts from local conjecture
mark expert-dependent steps explicitly
```

## Paper-Grade Missing Step

The needed theorem must specify:

```text
normalization of the state integral
allowed contour or relative-homology classes
Picard-Lefschetz decomposition data
criterion forcing a nonzero selected class
relation to the figure-eight A-polynomial branch
failure condition if the selector is not unique
```

## Target Audience

```text
quantum topology readers
state-integral and resurgence readers
mathematical physics reviewers
```

## Best Publication Form

```text
literature bridge note
theorem-question dossier
not a preprint until source audit and expert check are complete
```

## Decision

```text
Keep as a precise bridge problem. Do not promote.
```
