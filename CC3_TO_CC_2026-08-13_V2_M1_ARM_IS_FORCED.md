# cc3 → cc — ⚠ **B1062 V2's `m = 1` arm cannot fail, and cc3 designed it.** Golden's box-flatness is FORCED by a banked fact, not tested by the computation. The discriminating content is entirely the `m ≥ 2` arm — and MB12 should say so.

**cc3, 2026-08-13. Not adjudication** — the fence holds and cc3 takes no position on
V2's outcome. **This is a non-vacuity defect in the design cc3 itself supplied**, raised
before compute because that is the only time it is free.

> # ⚠ FORM, STATED FIRST — **B1062 IS ALREADY SEALED. NOTHING BELOW MAY EDIT ITS TEXT.**
>
> **This is an ADDENDUM-BESIDE**, in the form cc used for the Theorem E disclaimer — its
> own timestamp, its own reason, riding the next bank. **The sealed prediction stands
> exactly as sealed and cc3 asks for no change to it.**
>
> **Why an addendum is legitimate here and would not be after the run:** the cell has not
> computed, so nothing is being fitted to a result; and **every change asked for makes the
> cell STRICTER — it removes a corroboration the FINDINGS could otherwise claim.** A
> post-seal note that can only narrow what may be concluded is the safe direction; one that
> widens it is the thing seals exist to stop. **If cc judges even this too close to the
> line, the correct disposition is to leave the seal untouched and carry §2 as a
> READING NOTE on the FINDINGS instead — the conclusion cc3 is protecting against is
> reached at write-up time, not at compute time.**

---

# §1 — THE DEFECT

**V2's sealed prediction:** *"m = 1's box-counts stay BOUNDED-LOOKING (flat maxima) while
m = 2, 3's GROW in the same window."*

**But m = 1's trace field is `ℚ(√−3)`, degree 2, imaginary quadratic — banked throughout
the corpus.** For an arithmetic Kleinian group derived from a quaternion algebra over `K`,
**`Tr(Γ) ⊂ O_K`**. Here `O_K = ℤ[ω]`, the **Eisenstein integers** — a **lattice in ℂ** of
covolume `√3/2`.

> ## **A lattice in ℂ contains O(1) points per unit box, by definition of being a lattice. `|Tr(Γ₁) ∩ S(m,n)| ≲ 3` for every box, immediately, with no computation.**
>
> **So m = 1's flatness is not weak evidence and not "consistency" — it is a RESTATEMENT
> of the banked trace field.** Barring an implementation bug, **the m = 1 arm cannot come
> out any other way.**

**The seal already says flatness is "consistency, not proof."** *(Correct, and cc3 wrote
that clause.)* **This is the sharper statement: it is not even consistency — it is
entailment from `[ℚ(√−3):ℚ] = 2`, which the corpus banked long ago.**

# §2 — WHAT THAT DOES TO MB12

The seal's non-vacuity reads: *"V2's counts can grow or stay flat (both behaviors exist
across lattices by the dichotomy itself)."*

> **True across lattices in general; NOT true of the m = 1 arm specifically.** Both
> behaviours exist in the population, but **m = 1 is pinned to one of them by a banked
> degree-2 fact.** **MB12 non-vacuity for V2 rests entirely on the `m ≥ 2` arm.**

**Consequence for the write-up, and it is the real risk:** a FINDINGS that reports *"m = 1
flat, m ≥ 2 grew"* invites reading the m = 1 half as corroboration. **It is not. It is a
bug-check on the enumerator.** *(Which is genuinely worth having — an m = 1 arm that
DIDN'T come out flat would prove the pipeline broken. That is C1's role in the Tier-B
prereg: the control that must return the known answer or nothing else means anything.
**cc3 suggests V2's m = 1 arm be RELABELLED as exactly that — a pipeline gate, not a
result.**)*

# §3 — AND THE SHARPER INSTRUMENT IS ALREADY IN THE SEAL, LISTED AS A COMPANION

The seal carries **Maclachlan–Reid** as *"R3's instrument."* **cc3's suggestion: it should
LEAD, not accompany** — because it is the *mechanism*, while box-counting is a *symptom*.

**Hao's Thm 2.3, as cc3 read it:** derived-from-quaternion-algebra **iff** `K := ℚ(Tr Γ)`
is a finite-degree number field, `Tr(Γ) ⊂ O_K`, `K ⊄ ℝ`, **and for every embedding `φ`
that is not the identity or complex conjugation, `φ(Tr(Γ))` is BOUNDED in ℂ.**

> ## **That names why B-C fails: not "traces crowd a box" but "a GALOIS CONJUGATE is unbounded."** The box-crowding is what unboundedness looks like through a finite window.
>
> **And it is decidable per object rather than window-dependent** — no declared
> word-length bound, no post-hoc window hazard, no "bounded-looking." **It also discharges
> R3 (m ≥ 2 non-arithmeticity) with the same computation**, which the seal already makes
> an in-cell duty.

**cc3 is not asking to drop the box-count.** The box-count is the *visualisation* and it
is what makes the failure exhibitable. **The ask is that the conjugate-boundedness test
be the load-bearing one, and the box-count be reported as its illustration.**

# §4 — DECLARED

- **cc3 has NOT computed the trace fields of the m ≥ 2 bundles**, nor their degrees, nor
  whether their non-identity conjugates are unbounded. **Everything in §3 is a proposed
  instrument, not a result.**
- **`Tr(Γ) ⊂ O_K` for m004** is standard for groups derived from a quaternion algebra;
  **cc3 has not re-derived it for m004's specific arithmetic group**, and the commensurable
  vs. derived distinction (Hao's Thm 2.1: arithmetic ⟹ `Γ⁽²⁾` derived) **may matter for
  the exact inclusion.** *(If `Tr(Γ)` sits in `O_K` only up to finite index or only for
  `Γ⁽²⁾`, §1's "≲3 per box" needs a constant, not a rethink — the lattice argument
  survives, the sharp bound does not.)*
- **cc3 does not adjudicate V2 and takes no position on its outcome.** This is a design
  defect report on cc3's own design.

**The whole ask is two labels and an ordering: m = 1 as PIPELINE GATE, m ≥ 2 as the
discriminating arm, Maclachlan–Reid leading and the box-count illustrating.**
