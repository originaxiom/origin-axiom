# R004 Wave-3 update — 108-question living closure map

The canonical registry now records 108 typed propositions.  It preserves exact algebraic results
without promoting them through absent selection or physics functors.

```text
37 PROVED
36 REFUTED
13 CONDITIONAL
16 EXTERNAL_BLOCKER
 1 EMPIRICAL
 5 OPEN
```

The main changes are:

- B1146's central-visibility correction is explicit: the selected minimal A1 sees the 2T center;
  the principal-A1 adjoint does not.  This does not select an A1.
- Six Wave-2 computations moved to exact `PROVED` status, including the invariant census and
  finite Habiro tables.
- The preregistered Kashaev recognition and generic degree-one peripheral-character claim closed
  negatively.
- Memos 41-53 were split into exact representation theorems, two uniqueness counterexamples, one
  antiunitary refutation and one new component-level open question.
- B1147's missing `reproduce.log` is registered as a process defect separate from its reproduced
  mathematics.

The five live computations are OA-C1067 (full-64 semilinear Sigma), OA-C1069 (geometric Pin-minus
torsor), OA-C1074 (controlled zero-count error), OA-C1077 (large-T GUE run), and OA-C1083
(component-level trace identity).

The generated map is `documents/PROGRAM_QUESTION_ANSWER_MAP.md`.  Its canonical editable source is
`documents/program-question-map/inventory/backbone.json`; `validate.py` locks dependencies,
statuses, source reachability for the new wave, counts and the open set.

This is audit closure accounting, not parameter-free physical closure.  The QFT, chirality,
vacuum, flavor-value and dynamics gates remain explicit.
