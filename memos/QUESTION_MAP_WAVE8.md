# R029A — Wave-8 upstream hostile reconciliation

## Verdict

Fresh main through B1218 and outside through memos 156–157 change the canonical
map in six places.  B1218 itself reproduces.  Gate C closes for the
commensurator mechanism, but outside's proposed next computation is stale:
B632 cell 2 already ran and proves an alternating rank-two operation on three
inequivalent local-system modes, not three generations.  Gate D remains open:
its exact full-trace identity survives, while its selected square-root sign and
finite-approximant fractal inference do not.

## Exact corrections

`certificates/r029_upstream_hostile_corrections.py` independently works over
`Q(w)/(w^2-w+1)` with stdlib rational arithmetic.

1. The B324 order-three labels are joined by exact invertible intertwiners on
   one carrier.  The character-variety orbit has size one.  Functorial
   principal composition produces one 27, not three copies.
2. In the full-trace convention,
   `I(E-lambda,E,2)=lambda^2+2`.  One kappa branch permits both signs of lambda;
   both conjugate branches give a four-element sign/Galois orbit.
3. The certificate records memo 156's externally checked exact source blob,
   levels and `[-6,6]^2` window, then independently reconstructs the recurrence.
   Its finite complex approximants have polynomial degrees
   21, 55 and 144, and exact dyadic `Q(w)` witnesses inside that window satisfy
   `|x_n|<2` at all three levels.  The sampled masks therefore have nonempty
   interior; the bounded finite lemniscates have exact planar box dimension
   two.  A pixel estimate below two is under-resolution and says nothing
   decisive about the limiting intersection.

## Current generation statement

- commensurator Z3: not multiplicity (OA-C1170, refuted);
- B632 `h1=3`: three inequivalent modes, not copies (OA-C1171, refuted as a
  generation reading);
- native chiral index `+3`: still an external geometry/QFT blocker (OA-C0009);
- conditional heterotic visible spectrum: remains a separate conditional
  compactification result (OA-C1006).

## Map state

The canonical inventory now contains 192 rows:

- 72 PROVED
- 58 REFUTED
- 15 CONDITIONAL
- 22 EXTERNAL_BLOCKER
- 2 EMPIRICAL
- 23 OPEN

The additional explicit rows are OA-C1170–OA-C1175.  Validation, deterministic
rendering, the exact certificate and its `python -O` run all pass.

## Scope

This wave does not claim a theorem about the limiting non-self-adjoint
Fibonacci spectrum, does not erase B632's genuine cohomology, and does not turn
the `{C,P}` discrete ledger into parameter-free physical closure.  It removes
three category errors: conjugacy is not multiplicity, cohomology dimension is
not a chiral-family index, and an under-resolved finite polynomial lemniscate is
not a computed fractal limit.
