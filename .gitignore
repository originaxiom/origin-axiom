##############################
# Language / OS basics
##############################

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Snakemake working dirs
**/.snakemake/

# iCloud / archives
*.icloud
*.zip


##############################
# LaTeX build artifacts
##############################

# Generic LaTeX aux files (anywhere)
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out *.npz
*.run.xml
*.toc
*.xdv
*.synctex.gz
*.nav
*.out
*.snm
texput.log

# Per-paper main PDFs (phase0–5 and any future phase)
# Canonical PDFs live in phaseN/artifacts/ and root artifacts/
**/paper/main.pdf


##############################
# Outputs we do NOT track
##############################

# Any "runs" subtree in outputs: heavy runtime junk
**/outputs/runs/

# Phase2 test scratch (often nests its own outputs/)
**/outputs/tests/_tmp_cfg_*/
**/outputs/tests/**/outputs/

# Extra noise under outputs: tmp/log/bak, but keep JSON/CSV/PNG/etc
**/outputs/**/*.tmp
**/outputs/**/*.log
**/outputs/**/*.bak

# Phase 5 generated outputs are all runtime products
phase5/outputs/*
!phase5/outputs/.gitkeep


##############################
# Phase-specific extras
##############################

# Phase 3 local safety backups (never commit)
phase3/fit/targets.yaml.bak.*


##############################
# Local/editor backup files
##############################

*.bak.*
*~
*.swp
*.swo
# Stage2 doc-audit working dir
stage2/doc_repo_audit/

# Local scratch + LaTeX build artifacts not tracked by design
commit_log_full.txt
phase2/paper/main.pdf
_internal/
