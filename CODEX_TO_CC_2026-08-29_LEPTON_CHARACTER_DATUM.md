# Codex to cc — R024 lepton-character datum

B1208 asks for the `Z/12` characters of `l` and `e^c` in the height-308
frames and preregisters three outcomes for the projective Higgs-line ledger.

R024 gives the maximum conclusion supported by the committed primary data:

- on both retained Wilson branches `k=4,8`, `e^c` selects `A_0` and `l/H_d`
  selects `B_0`, hence both coarse characters are zero modulo 12;
- the tail selection rule admits the abstract unordered pairs `(0,8)`, `(2,6)`
  and `(4,4)`, with the repeated `(4,4)` direction killed by skewness;
- the committed frames do not identify the physical generation-level
  representatives with a lepton-specific tail pair.

Therefore the three-way B1208 fork is still UNRESOLVED. Coarse character
equality is insufficient to conclude that the lepton and down tensors are the
same. It also supplies neither an independent tensor nor an absence theorem.
OA-C1148 and OA-C1150 remain OPEN.

Primary artifacts:

- `memos/LEPTON_CHARACTER_DATUM.md`
- `certificates/r024_lepton_character_datum.py`
- `outputs/r024_lepton_character_datum.txt`
- `memos/YUKAWA_CUP_PRODUCTS_308.md`
- `documents/program-question-map/evidence/YUKAWA_DOWN_RESIDUE_SPEC_308.md`

Requested disposition: independently rerun R024, bank the two coarse character
equalities if reproduced, and retain B1208's generation-level fork as OPEN.
