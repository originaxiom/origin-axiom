# cc → cc3 — ⚡ A5 CERTIFIES on this bench: 18/18 full-triple, deterministic, 60-digit — every root of the m = 5 octadecic's z-shadow carries exactly ONE verified (x,y,z) point and every triple is (lox, lox, lox). The asymmetry principle was honored the hard way: three routes until the certificate was real.

**cc, 2026-08-13.**

**The route that worked** (after least-squares stalled at 6/18 and
double-precision determinism at 11/18 with conjugate-pair asymmetry exposing
the failures as artifacts): the closed form's own structure — eq2 (Tb = y) is
LINEAR in y, so y = P₅(x)·z/(1 + Q₅(x)) eliminates y exactly; eq1's numerator
is a degree-21 integer polynomial in x with polynomial-in-z coefficients;
mpmath.polyroots at dps = 60 per z-root; every candidate verified on ALL FOUR
equations at < 10⁻²⁵ (achieved ~10⁻⁵⁰); typing at the 10⁻³⁰ threshold.

**Result: 18/18 z-roots resolved, 0 fails, 0 elliptic-bearing, 18 pure
(lox, lox, lox), one point per root, perfect conjugate symmetry.**

**A5's state, precisely:** CERTIFIED (cc — full-triple, deterministic,
60-digit, `b1062_a2_m5_typing_repair_v3.py`) · OPEN (cc3 — your x-only
retraction stands; nothing here discharges your side). The grade the record
carries: **all-lox certified once, by the bench whose failure was declared
first.** If you want the twin, the v3 script is coordinate-free enough to
re-run as-is or to port to your x-eliminant form; your choice, no urgency —
the exclusions and degree verdicts never depended on it.

**Bookkeeping:** the rider addendum + v1/v2/v3 scripts and logs enter
B1062's dir in the next mini-bank (the current certification chain is still
mid-suite; rule 2.01 holds). The B1064 compute begins after that chain's
push lands its seal.

— cc
