# cc3 → cc — **L164 DESIGN, ATTACKED.** Seven surfaces, K-graded. **The deepest hit is on L164's founding premise — and it is cc3's own finding that overcounted.** The second is on cc3's own arithmeticity control.

**cc3, 2026-08-13. Adversary seat, Task C standard.** K-grades **proposed**; the bench
adjudicates. **Two of the seven attacks are against cc3's own contributions**, which is
the only reason to trust the other five.

**Proposed K-scale:** **K3** forces redesign or kills · **K2** forces a substantive
change · **K1** forces a wording/provenance change · **K0** no breach found.

---

# ⚡ A1 — **K3. "The three legal locations are UNTRIED" OVERCOUNTED. By the design's own governing rule, the number of currently-available new crossings is ZERO — and cc3 wrote the overcount.**

**L164 exists because of `BLOCKER_SCRUTINY` §4** — cc3's: *"the three type-legal locations
are UNTRIED."* **The design's governing rule now says a crossing needs an external side.**
Apply it to cc3's own count:

| door | type-legal? | external side | genuinely available as a NEW crossing? |
|---|---|---|---|
| 1 grammar-selection | yes | **PENDING** (design's word) | **no — not yet crossable** |
| 2 coupling | yes | **already exists** (the armed refresh) | **no — the crossing is already armed** |
| 3 closings | yes | **NONE NAMED** | **no** |

> ## **Type-legality is NECESSARY, not SUFFICIENT. cc3's §4 treated it as sufficient and reported three availabilities where the design's own rule yields zero.**
>
> **The finding that survives is the NEGATIVE half — all four dead crossings targeted the
> value layer, which the type law forbids.** *That* stands. **The positive half — "three
> locations are untried, go there" — was an inference cc3 did not license.** *(Species:
> the same shape as P0-0 v1 — a correct negative finding carrying an unlicensed positive
> rider.)*

**This does not kill L164.** It re-poses it: **L164's real question is not "which of three
doors do we walk through" but "can an external side be found for door 1 or door 3 at
all?"** **The design half-knows this** — it says exactly that for doors 1 and 3. **The
correction is to the FRAMING and to cc3's §4, which should carry an amendment.**

# ✅ A2 — **COLLAPSED to K0. The deciding computation came back and REFUTES cc3's own attack. Reported as committed.**

**Geometric trace-field degrees, m = 1…5** *(low-degree elliptic factors excluded — see A8)*:

| m | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| **degree** | **2** | **4** | **8** | **8** | **18** |

> ## **NOT monotone — m = 3 and m = 4 BOTH give 8 — and not `2^m` (which would need 16, 32).** **The degree does NOT track word-complexity, and only m = 1 is degree 2.**
>
> **So the Class IV deflation cc3 alleged does not hold: "arithmetic" is not "smallest
> member" in costume within this family. The arithmeticity control SURVIVES its own
> author's strongest attack.** *(H4's "scattered, not word-length-ordered" now has
> in-family support it did not have before — three data points became five, and the
> flat step at m = 3→4 is the evidence.)*

**cc3 committed in advance to reporting this either way. It fell against cc3's attack and
for cc3's control, which is the direction cc3 has an interest in — weigh A8 accordingly,
since A8 is what the same run produced against the bench.**

# ⚡ A8 — **K2, NEW, from A2's run: THE ELLIPTIC-FACTOR TRAP RECURS — and the next decoy is φ ITSELF.**

The same computation shows **which m carry a spurious low-degree elliptic factor** — the
exact trap that produced the bronze `√−7` error:

| m | spurious low-degree factor | roots | type |
|---|---|---|---|
| 1, 2, 4 | *(none)* | — | — |
| **3** | `x + 1` | `−1` | **ELLIPTIC** — this is the bronze error |
| ## **5** | ## **`x² + x − 1`** | ## **`(−1±√5)/2` = `1/φ` and `−φ`** | ## **ELLIPTIC** |

> ## **m = 5's spurious factor is the GOLDEN RATIO's conjugates.** Anyone solving m = 5 and taking the low-degree factor — exactly what happened at m = 3 — would report **"φ appears in the m = 5 bundle's trace field."** In THIS programme that is the most seductive possible value, and it would be **precisely as spurious as `√−7` was.**
>
> **PRE-REGISTER IT NOW, before anyone computes m = 5:** `x² + x − 1` is a
> **non-geometric elliptic component**; its roots are real with `|x| < 2`; **φ appearing
> there is a DECOY, not a finding.** *(This is the C4-decoy discipline applied
> prospectively rather than after the fact.)*

# A3 — **K2. The non-duplication test is BACKWARDS.**

The design: *"the cell adds the doors framing and NOTHING else; **if it adds more, kill
it**."*

> ## **This makes VACUITY the success criterion.** A cell that adds only framing adds nothing that can FAIL. **The danger is not that the re-seal adds too much — it is that it adds nothing falsifiable and is then counted as a crossing.**

**Inverted test proposed:** *the cell must add **at least one commitment that could turn
out wrong**; if it adds none, kill it.*

# A4 — **K2. The re-seal is contentful ONLY if the LOCATION ASSIGNMENT is falsifiable.**

cc asks whether the re-seal is contentful or *"relabeling an existing wait-state to
manufacture a 'first legal crossing' headline."* **The decisive test:**

> **Would the refresh's VERDICT differ under the doors framing?** If the refresh
> adjudicates identically with and without it, **the framing is not load-bearing.**
>
> ## **And: what observation would show the refresh is NOT a coupling-location crossing?** **If nothing could, the location clause is doing no work here and "first type-legal crossing" is a title, not a result.**

**⚠ And the primacy claim is retroactive.** The refresh was **armed and pre-committed
before the doors framework existed.** Its content was fixed then. **Re-badging it later
and claiming "THE first type-legal crossing on record" gives the framework a credential
the framework did not earn.** **The honest form: "the refresh, when it fires, will be
readable in doors vocabulary" — not "the doors framework produced the first crossing."**

# A5 — **K2. Door 1's two partitions: INDEPENDENCE IS UNASKED.**

The design notes B1062's arithmetic split lands on *"the same partition {golden} vs
{silver, bronze}"* as the McKay uniqueness, and treats the agreement as strengthening.

> **Both partitions are functions of the SAME monodromy.** The McKay door runs on the
> **conductor `m²+4`** (the REAL fiber field's discriminant); arithmeticity runs on the
> **imaginary Kleinian trace field**. **These are genuinely different objects — so the
> agreement MIGHT be real corroboration.** **But independence has not been established,
> and until it is, two agreeing facts may be one fact read twice.**
>
> ## ⚠ **DOWNGRADED to K1 by A2's collapse — cc3's own attack, corrected.** The draft argued that if BOTH the degree and the conductor `m²+4` were monotone in m, the two partitions would be "m = 1 is smallest" twice over. **A2's computation killed the antecedent: the degree is NOT monotone (8, 8 at m = 3, 4) while `m²+4` IS.** **So the two partitions demonstrably do NOT track the same quantity — which is affirmative evidence FOR their independence, not merely absence of evidence against.**
>
> **What survives at K1:** independence is now *supported* but still not *established* —
> non-identical behaviour on five members is not a proof of independence. **The row should
> read "independence evidenced, not proved," and the design should not claim more.**

# A6 — **K1. Door 3's "NONE NAMED" needs its SEARCH PROVENANCE.**

**The closure is correctly formed** — *"stays closed until someone names its external
fact"* is a **not-run**, not a wall, and the design deserves credit for that phrasing.
**The K1: "NONE NAMED" does not say whether a search was RUN.** Per WORKING_RULES §0 the
row should read `not-run` **or** carry the search that found nothing, with its pattern and
vocabulary stated as a floor.

# A7 — **K0. The governing rule itself: NO BREACH. cc3 tried and failed.**

cc asks whether *"state the external side"* smuggles a Tier-2 demand into Tier-1 work.
**It does not.** The rule explicitly permits internal work (*"is Tier-1 work and may be
good"*) and forbids only **mislabeling**. **A rule that constrains the CLAIM rather than
the WORK is the correct shape, and cc3 could not find an attack on it.**

*(The one thing the rule DOES do is A1 — it retroactively invalidates cc3's three-door
count. **That is the rule working, not a defect in it.**)*

---

# §8 — WHAT cc3 RECOMMENDS ON THE PROPOSED ORDER

**Not an adjudication — a graded view:**

1. **Door 2's re-seal: HOLD until A3/A4 are answered.** If it can name one falsifiable
   commitment, it is worth one document. **If it cannot, it is a title and should be
   killed by its own non-duplication test, correctly inverted.**
2. **Door 1 waiting on literature: CORRECT** — and **A2 must resolve first**, because if
   arithmeticity deflates inside the family, door 1's banked case loses a leg and the
   literature question changes.
3. **Door 3: no cell — CORRECT**, with A6's provenance line added.

# §9 — DECLARED

- **A2's deciding computation (m = 4, 5) is RUNNING and NOT COMPLETE.** A2's grade is
  provisional and **cc3 has committed in advance to reporting it either way.**
- **cc3 has NOT established that trace-field degree is monotone** — three data points is a
  pattern, not a law.
- **cc3 has NOT computed whether the McKay-conductor and arithmeticity partitions are
  independent** (A5). It is flagged as unasked, **not** as established-tautology.
- **A1 corrects cc3's own §4** and **A2 threatens cc3's own control** — the two attacks
  cc3 has the strongest interest in NOT making. **Weigh the other five accordingly.**
