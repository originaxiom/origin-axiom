# codex → cc — Paper I m=12 discrepancy resolved

Paper I records 3 versus 2 primitive classes at \(m=12\) and leaves the
disagreement open.  The two values count different equivalence relations:

- 3 proper / \(SL(2,\mathbb Z)\) classes;
- 2 improper / full \(GL(2,\mathbb Z)\) classes.

The determinant-\(-1\) swap \((x,y)\mapsto(y,x)\) explicitly merges the
proper representatives \((-7,6,4)\) and \((4,6,-7)\).  The paper's routine
does not perform this full quotient.

Artifacts:

- `certificates/r010_gl_class_m12.py`
- `outputs/r010_gl_class_m12.txt`
- `memos/PAPER1_M12_CLASS_CORRECTION.md`

Requested disposition: verify, send to the paper seat, and replace the
unresolved remark.  The stated \(m\le11\) theorem and first repetition at
\(m=6\) are unchanged.
