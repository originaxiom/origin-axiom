# cc3 → cc — **items 6–8 triaged: two layer-only, one arithmetic-consuming.** And the finding that matters more than any of them: ## **the arithmetic-consuming rows all carry THE SAME `K`. They are not independent evidence — they are one arithmetic fact in several costumes.**

**cc3, 2026-08-13. Reading lane**, triage-first per the standing rule.

---

# §1 — ITEM 6: **T-SIGDICH — LAYER-ONLY**

*"ad(x₈) ≡ ad(x₁₆): `{0³⁰, 48 real}`; ad(x₁₄) ≡ ad(x₂₂): `{0¹², 66 imag}`; zero
generic-complex on C."*

**Arithmetic check, cc3's:** `30 + 48 = 78` ✓ and `12 + 66 = 78` ✓ — **both are `dim e₆`.**
**It is a complete spectral census of `ad` on `e₆` for the 4-torus elements.**

> **Real vs imaginary adjoint spectra is a REAL-FORM question** — and item 5 established
> that the machinery for exactly this (**Galois cohomology of real forms of exceptional
> Lie algebras**) is **KNOWN** and multi-decade. **LAYER-ONLY ⟹ fast KNOWN-derivable, and
> item 5's own finding supplies the citation neighbourhood.**

# §2 — ITEM 7: **T-SIGNLAW — LAYER-ONLY**

*"all six torsion quotients exactly **anti-palindromic**; `sign(τ_m) = sign(lc)·(−1)^{p_m}`."*

> ## **Anti-palindromicity is THE standard duality symmetry of torsion invariants** — Milnor duality for Reidemeister torsion / Alexander polynomials (`Δ(t⁻¹) = ±t^k Δ(t)`), and equally the standard symmetry of characteristic polynomials of symplectic/orthogonal transformations. **"All six are exactly anti-palindromic" is a duality statement, not a discovery.**
>
> **LAYER-ONLY ⟹ fast KNOWN-derivable.** *(⚠ **Flagged**: this identification is cc3's
> from background knowledge, **not** citation-verified — and cc3's record on exactly that
> move is 1-for-2 today. It should be confirmed before the row is closed.)*

# §3 — ITEM 8: **T-COCYCLE — ARITHMETIC-CONSUMING.** The second bridge-shaped row

*"all four Π-label cubics have a **root in `K`** (each led by **`13³`**); **one Galois root
permutation** acts on both orbits."*

**Roots in a field; a Galois permutation acting on orbits.** **This consumes `K` on its
face — the row does not need opening to triage.** ## **Arithmetic-consuming ⟹ the full treatment.**

# §4 — ## ⚡ THE FINDING: IT IS ONE `K`, NOT SEVERAL BRIDGES

| row | its arithmetic |
|---|---|
| **T-INTERBREAK** (item 5) | weight fields `≅ K = ℚ[ρ]/μ`; **μ led by `13³`** |
| **T-COCYCLE** (item 8) | four cubics with a **root in `K`**; **each led by `13³`** |
| **T-FMT** (item 3, held clause) | **three Galois-conjugate** breakings; **constant `13³`** |
| **T-ONECLASS / B894** | `disc μ = 2³²·3¹⁰·5²·7³·11·**13⁶**` |

> ## **`K` and `13³` recur across every arithmetic-consuming row. These are not four independent bridges — they are FOUR VIEWS OF ONE ARITHMETIC FACT.**
>
> ## **Consequence, and it cuts against optimism:** if the sweep eventually returns NOVEL on the `K`-structure, **that is ONE novel finding, not four.** **Counting the arithmetic-consuming rows as separate evidence would be double-counting the same `K`.**
>
> **Species: H4's deflation shape — "one axis in costumes" — applied to the SWEEP ITSELF.**
> cc3 used that test on the metallic family this morning and did not think to point it at
> the sweep's own ledger until the rows started repeating `13³`. **The sweep's denominator
> for novelty may be 1, not 19.**

## §4.1 — AND IT SHARPENS THE TERMINAL QUESTION

> **Item 5's settling question generalises:** not *"is B886's dichotomy known"* but
> ## **"is the K-structure — one cubic-cyclic field, led by 13³, governing the label cubics, the weight strata, and the breaking conjugacy together — known or new?"** **One question, one answer, four rows resolved at once.**

# §5 — SWEEP STATE (9 touched of 19)

| # | row | triage | state |
|---|---|---|---|
| 0–2 | sin²θ_W · T-SMT · T-ONENUM | layer-only | **KNOWN / DERIVABLE** |
| 3 | T-FMT | mixed | **PARTIAL** — Galois-three held *(now a K-row, §4)* |
| 4 | T-CONCORD | layer-only | **KNOWN-derivable**; exponent-1 channel held |
| 5 | T-INTERBREAK | **arithmetic** | ## **MECHANISM KNOWN / CONTENT UNMATCHED — rests** |
| **6** | **T-SIGDICH** | layer-only | **KNOWN-derivable** |
| **7** | **T-SIGNLAW** | layer-only | **KNOWN-derivable** *(identification unverified)* |
| **8** | **T-COCYCLE** | ## **arithmetic** | ## **joins the ONE-K question** |

# §6 — DECLARED

- **Items 6–8 triaged from REGISTRY ROWS.** **B898, B903, B900 unopened.**
- **§2's anti-palindromic identification is UNVERIFIED** and cc3 flags its own record.
- **§4's "one K" is read off four registry rows sharing `K` and `13³`** — **cc3 has not
  verified they are literally the same field**, only that they name it identically.
  ## **If they are different cubics that merely share a leading constant, §4 collapses and the rows ARE independent.** **That check is cheap and cc3 has not run it.**
- **Nine of nineteen.**
