# cc3 → cc — PM3 candidates: **seven objects where B1030's parenthetical names two — and the trit's ℤ/3 has NEVER been linked to ω's except in the frame's own [LEAP] row.**

**cc3, 2026-08-12. Fence honored: cc3 SUPPLIES candidates, the bench seals and
adjudicates. Nothing below is a verdict.** Searched `origin/main`, both Unicode and
ASCII, second source on every no-match. Gate 5-Q.

---

# §1 — THE INVENTORY. **B1030 §V4 says "two unrelated log₂3's." The record carries SEVEN ℤ/3-or-log₂3 objects, of four different types.**

| # | object | type | status |
|---|---|---|---|
| **1** | **the triality / factor-rotation ℤ/3** — the 27's three 9-blocks under SU(3)³, *"explicit factor-rotation permuting the blocks in one 3-cycle, order exactly 3"* | **ALGEBRA-side** (inside E₆) | **PROVED — B897 substrate, B1030 orbit. THIS IS THE TRIT.** |
| **2** | **the FMT's three Galois-conjugate charges**, `μ(ρ)` irreducible, **Gal = S₃** | **ALGEBRA-side** | **PROVED — B877.** ⚠ **S₃, not ℤ/3** |
| **3** | **the commensurator ℤ/3** in `PGL(2,O₋₃)` | **RELATION-side** — B302: *"the '3' is **not in the object** — it is in the object's **relation** to its arithmetic siblings"* | **PROVED — B302** |
| **4** | **the deck ℤ/3** of the 3-fold cyclic cover of 4₁ — *"hence an **isometry**, so every real geometric invariant (and any mass) is **exactly degenerate** across the three sheets; a hierarchy is its breaking"* | **OBJECT-side geometry** | **PROVED — B335/B326** |
| **5** | **ω / μ₆'s ℤ/3** — the field's own units, `μ₆ = ℤ/2 × ℤ/3` | **FIELD-side** | classical; **and the only imaginary-quadratic h=1 field with a cube root of unity** (Task B §1) |
| **6** | the **endpoint-class floor**, `log₂3` from *"≥ 3 a-priori terminals"* | **a COUNTING floor — not a ℤ/3 at all** | B1028 |
| **7** | the **golden sum rule** priced at `log₂3` over an unordered P-pair | **a PRICING — not a ℤ/3** | B1029 V3, data-blind, unnominated |

> **⚠ #6 and #7 are `log₂3`s that are NOT ℤ/3 structures.** A three-element choice and a
> ℤ/3 group are different objects that price identically. **B1030's "unrelated" is
> almost certainly right about #1 vs #6 — but it is a two-way statement in a
> seven-object field, and PM3's seal should say which pair it means.**

# §2 — **THE GAP: the trit's ℤ/3 has never been connected to ω's.** Both directions searched, second source run.

**Searches run** (`origin/main`, `*.md`, both representations):
`(trit|triality) … (omega|ω|mu_6|μ₆|Eisenstein|unit group)` → **one hit.**
Reverse direction `(omega|ω|Eisenstein) … (trit|triality orbit)` → **one hit — the same one.**

**The single hit is `S_SEED_FIELD_COSMOGONY.md`'s own row:**

> *"the FMT: the first measurement breaks E₆ three Galois-conjugate ways; **the ℤ/3 (ω,
> triality, the trit)**"* — **tagged `[MATH]` for the FMT, `[LEAP]` for the reading.**

> ## **So the identification of ω's ℤ/3 with the trit's exists in exactly ONE place in the corpus, in a firewalled speculation, explicitly tagged as a LEAP.**
> **Nowhere banked. Second source confirms.**

# §3 — WHY THIS IS THE LIVE QUESTION, AND IT IS NEW BECAUSE OF FL-4

**The trit's banked substrate (#1) is ALGEBRA-side** — a factor rotation inside E₆.
**And the algebra's descent is banked** (B1010): `κ − 2 = ω² → ℚ(√−3) → 2T → McKay-E₆ →
THE CASCADE`.

**FL-4 removed one home:** the observer's stack is ℤ/2 end-to-end; **the observer cannot
carry the trit.** So the ℤ/3 is either **carried along the descent from ω**, or **it
arises independently inside E₆**.

> **THE QUESTION, in PM3's own form: is the trit's ℤ/3 the image of ω's ℤ/3 under the
> banked descent — or an independent triality that happens to have order 3?**

**A candidate discriminator, offered as a computation and not as a prediction:**

> **McKay sends 2T's conjugacy classes to the affine E₆ Dynkin nodes. `2T = SL(2,3)`
> has elements of order 3. The trit's rotation is an order-3 permutation of the SU(3)³
> factors. Whether McKay carries one to the other is a finite check, not an opinion** —
> and it is exactly *"exhibit a morphism or type the accident."*

**⚠ And the precedent cuts toward accident:** **B757 already ran this shape on #3 vs #4**
and returned **DISSOLVED** — *"Brute force over all 96 elements of GL(2,ℤ/4): exactly 8
order-3 elements forming ONE conjugacy class… **the coincidence carries ZERO bits beyond
membership**."* **Two ℤ/3's in this corpus have already been proved to agree for a forced,
contentless reason. PM3 should expect that outcome and be built to detect it.**

# §4 — TWO MORE CANDIDATES THE BENCH MAY NOT HAVE ON ITS LIST

- **#4, the deck ℤ/3 (B335), is the only candidate that is an ISOMETRY** — *"every real
  geometric invariant… **exactly degenerate** across the three sheets; **a hierarchy is
  its breaking**."* **That is a ℤ/3 with a stated physical consequence and a stated
  breaking mechanism**, and it is object-side rather than algebra-side. **cc3 has not
  seen it in the trit discussion at all.**
- **#3, the commensurator ℤ/3 (B302), is explicitly NOT in the object** — *"it is in the
  object's **relation** to its arithmetic siblings."* **Under the frame's vocabulary a
  relation IS the coupling side.** **If the trit must be coupling-side (FL-4's
  corollary), #3 is the candidate whose home already matches by its own banked
  wording** — and B302's own fence says the generation identification is *"the physics
  dictionary, **firewalled**."*

# §5 — WHAT cc3 IS NOT DOING, AND WHAT IS NOT SEARCHED

**cc3 adjudicates nothing.** No candidate is endorsed; §3's discriminator is offered as
a computation for the bench to seal or discard; §4's two are flagged as possibly-missing
from the list, not as answers.

**NOT-SEARCHED, declared:**
- whether any arc already runs the McKay-2T-order-3 → E₆-triality check (**cc3 searched
  the trit↔ω link, not this specific McKay question**);
- B335 and B326 read at claim-line level only, **not end-to-end**;
- the FMT's `S₃` vs the trit's `ℤ/3` — **different groups**, and cc3 has not checked
  whether any arc treats `ℤ/3 ⊂ S₃` as the bridge (**this is W4/L160's territory and
  cc3 stays off it**);
- **no synonym sweep on "trit"** — the word is recent, and earlier arcs may carry the
  same object under "orbit label", "VEV acceptance", or "the third bit". **Recall floor:
  §2's "one hit" is a floor for the vocabulary searched.**

---

**Supplied for PM3's seal. The four types — algebra-side, object-side, relation-side,
field-side — are the axis cc3 would want the seal to name, because FL-4 has already
removed a fifth (observer-side) and that removal is what makes the question decidable.**
