# CC3 → CC — CORNERSTONE SYNTHESIS, 2026-08-09

Four probes returned (REID, THREEBITS, EXOTICS, RULE). All four completed; none FAILED;
nothing here is NOT ASSESSED. Grades used throughout: **PROVED / BANKED-BUT-UNPROVED /
ASSUMED / NEVER-ASKED**.

---

## 1. THE VERDICT IN ONE PARAGRAPH

**NOT YET — and not for want of theorems, but because the cornerstone has no object under
it.** Everything downstream of the trace field is a theorem about E₆ and holds; the one
object-specific fact carrying the chain (B266's "exactly two surjections onto 2T") has now
been base-rate tested on this seat and is **generic** — 24.2% of a 3995-manifold random
sample of the one-cusped census carries m004's own count. The object's genuinely unique
property (Reid: arithmetic **and** a knot complement) is **load-bearing at zero steps** — the
REID probe found it consumed nowhere, and the corpus already concedes this in three places.
So the derivation as it stands is *a theorem about the commensurability class of ℚ(√−3), plus
a theorem about E₆, joined by a fact one hyperbolic 3-manifold in three possesses*. That can
be made unbeatable **as a statement about that class**, and cannot — on today's evidence — be
made unbeatable as a statement about m004.

---

## 2. THE MEASURED PROBLEM

**Measured this seat, not argued.** Counting surjections π₁(M) ↠ 2T over the SnapPy census,
validated by first reproducing B266's count of **2** for m004, with |Aut(2T)| = 24 computed
independently:

| sample | admits a surjection | admits **exactly two** (m004's count) |
|---|---|---|
| random 3995 of 203123 one-cusped | **34.9%** | **24.2%** |
| first 400 by volume | 36.2% | **31.0%** |

Ties include **m003, m007, m022, m026, m027, m029, m030**.

**This is MEASURED and is not in question.** It extends a fact the corpus already banked,
pre-emptively, at `frontier/B727_base_rate_the_structure/FINDINGS.md:37` — *"**NEW computed
fact (strengthens the negative): π₁ ↠ 2T is NOT unique to the arithmetic knot.**"* (4/13
hyperbolic knots, including the **non-arithmetic** 7₂, 7₃, 8₁.)

**What it kills:** the claim that the surjection atom is *object-specific*. It is not. One
hyperbolic 3-manifold in three carries it.

**What it does NOT kill:**
- **Reid's theorem.** m004 IS the unique arithmetic knot complement in S³. PROVED, true, and
  untouched. It is simply never consumed.
- **The cascade.** B861/B862/B864 with B869's non-vacuity control are theorems about E₆ that
  name no manifold. Unaffected.
- **The E₆ label itself.** B727 probe 1: *"P(recurrence | one label) = 1"* — forced, not
  evidence, but also not wrong.

---

## 3. THE FOUR PROBES

### 3.1 REID — is Reid-uniqueness load-bearing anywhere? **NO.**

Zero steps consume it. `docs/THE_END_TO_END_CHAIN.md` calls m004 *"**the** arithmetic knot"*
**appositively**; the purchased object is the field — *"**ℚ(√−3) is bought at geometrization
and nowhere earlier**."* B266 has two derivations, neither touching knot-ness: the arithmetic
route consumes ℤ[ω] (class-scope), the group route consumes the presentation of Γ
(manifold-scope, arithmeticity-free).

`frontier/B803_commensurability_audit/FINDINGS.md:22` states the situation verbatim: everything
downstream of the invariant trace field *"is a statement about **the class**, not about m004."*
**Scope correction carried:** B803's own addendum and `docs/PRACTICES.md` narrow this — *"A
step that depends ONLY on the invariant trace field, the invariant quaternion algebra, or
arithmeticity is a statement about the COMMENSURABILITY CLASS. A step that depends on the
GROUP Γ itself is not."* "Everything downstream" as usually quoted is **over-wide**.

The one near-miss (B302, Neumann–Reid → PGL(2,O₋₃) → order-3 elements) fails three ways:
arithmeticity alone suffices (Margulis/Borel), B302 itself places the ℤ/3 *"in the
commensurability class… not in the single object"*, and it derives nothing — *"it does **not**
derive three generations."* **FIREWALLED.**

Knot-ness does real work exactly once, as a **discriminator**, never as a premise: H₁(m003) =
ℤ/5+ℤ ⇒ m003 is not a knot in S³ (B727). And B727 pre-empted today's result: *"The
SM-resonance is the **field's**, the arithmetic-knot uniqueness is the **knot's**; they live on
different objects and do not reinforce each other."*

### 3.2 THREEBITS — is 3 closing bits = 3 cascade steps? **COINCIDENCE.**

**NEVER-ASKED as a joined question** (`docs/OPEN_LEADS.md:737`, L114 OPEN; the two "measurement"
formalisms are *"declared **distinct pending L114**"* per `docs/STRUCTURE_TO_NATURE_MASTERPLAN.md:388`).
Four independent obstructions:

1. **Arity.** B861 §2's step 1 is a **4-option menu** (3 registerable); 4·2·2 = 16, registerable
   3·2·1 = 6. A free transitive (ℤ/2)³-torsor has **exactly 8** points (B782).
2. **Group type.** S₃ ≇ (ℤ/2)³; S₃ has no free transitive action on 8 points.
3. **Step count on the measurement side is TWO, and it skips SU(5).** B892: *"Two measurements
   of the object's own superselection charges take E₆ to the Standard Model algebra, skipping
   SU(5)."* B874: *"**No 26 stratum exists**."* Charge space is a continuous u(1)⁴, not 𝔽₂-rank 3.
4. **Domain disjointness.** ⟨c, θ, γ₅⟩ lives on the SL(3) character variety over ℚ(√5)/A₅; the
   FMT's cubic has Galois S₃. No banked homomorphism between them; no E₆-era arc invokes c/θ/γ₅.

**The decisive point:** a rule selecting *uniquely* from the object's own data **is** a canonical
equivariant section — precisely what B782 proves does not exist. Under the proposed
identification the chain becomes *inconsistent*, not merely unsupported. **Scope of that no-go:**
B782 quantifies over equivariant selections of a *closing* among the 8 character-variety
closings; it says nothing about E₆ menus until the identification imports it.

**Scope correction:** "rank-saturated at exactly 3" holds relative to 𝕀 = {c, θ, γ₅, γ₃}. B787:
*"Enlarging ⟨c,theta,gamma5⟩ by iota raises the F2-rank 3 → 4, unconditionally"* — pending L114.
A three-ness that becomes four cannot carry a three-ness of steps.

### 3.3 EXOTICS — **(ii) OPEN WITH A NAMED, DERIVED MECHANISM**, not a contradiction.

The apparent tension does not exist. **B978's scope is the adjoint**
(`frontier/B978_phaseA_bank/FINDINGS.md:25`: *"**NO ADJOINT VEV CAN GIVE ANY 27 FERMION A
MASS**"*) — it quantifies over 27-**bilinears**. Since class(27) has order 3, `3·class(27) = 0`
leaves the **cubic** free; B987 supplies the missing paragraph: *"B978 constrains the adjoint.
The Yukawa is the cubic."*

The mechanism is **derived on this bench**, not quoted: B970's Sym³(27) monomial census gives
`κ S D D̄` with the mass direction **unique and equal to ω₁** — the rank-1 27 VEV whose E₆
stabiliser is Spin(10). Plus R5, rep-independent: *"exotic mass ≠ 0 ⟹ U(1)_ψ is broken… **There
is no evasion by enlarging the Higgs sector**."* PROVED: uniqueness of direction, R5, the census.
BANKED-BUT-UNPROVED: `M_D = κ⟨S⟩` as physics (imported). NEVER-ASKED here: the Wilson-line
projection route — *"treat the projection route as UNVERIFIED HERE."*

**Two corrections owed and unapplied:** the count is **11**, not 12 (10 + 1 = 11); and *"never
addressed"* is **false** (B951, B962, B970 addressed it). The residue is not "no mechanism" — it
is Langacker's **trilemma** and **doublet–triplet splitting**, both graded external.

### 3.4 RULE — is the selection rule fitted to land on the SM? **NO — and it does not do the landing.**

`frontier/B861_fused_cascade/FINDINGS.md:9` states the rule: *"**Maximal residual symmetry among
REGISTERABLE options**."* The two halves grade differently: **registerable = DERIVED** (B860
theorem-grade, B871 pays the owed definition); **maximal residual symmetry = ASSUMED, never
retired** — B859: *"the max-dim ranking at steps 1–2 is still Michel-flavoured"*; B874: *"the
ranking is NOT retired by charge measurement."*

**The rule-variation axis was NEVER ASKED** — B869 varies the *starting group*, not the rule.
**The probe ran it** (`/private/tmp/claude-501/-Users-dri-oa-audit-seat/00f419c5-801b-4bbb-8d32-503cc9c44455/scratchpad/b869_min.py`
and `.../allpaths.py`, using the repo's own unmodified engine):

- **Minimum** residual symmetry, all 8 B869 starts → **identical endpoints**. From E₆ the step-1
  winner becomes SU(3)³, the endpoint does not move.
- **Exhaustive**, no ranking at all → `UNION over every registerable path from E6:
  ['su(2) + su(3) + 3 u(1)']`.

**The correct claim is stronger than the banked one:** *every registerable descent path from E₆
with the 27's generation terminates at su(3)⊕su(2)⊕3u(1). The ranking selects the route, not the
destination.* Consequence: **B861's "unique at every step" is over-claimed** — the SM core does
not need the ranking; **SO(10) and SU(5) as named intermediate stations do**. Residual fitting
risk migrates to **P5 menu completeness** (*"still the imported spine… the cascade's single
external dependency"*) and B869's canonical-family input. Also: B861 §2's step-2 menu **omits
so(8)⊕u(1)** (dim 30, top-dim, non-registerable), which B869's own engine finds.

---

## 4. WHAT WOULD MAKE IT UNBEATABLE — ranked by decisiveness / cost

**#1 — THE CONJUNCTION SWEEP.** *Cost: 2–4 hours; snappy pipeline already validated this seat.*
Re-run the census asking for the **conjunction**, not the atom: trace field ℚ(√−3) **AND**
exactly two 2T surjections **AND** H₁ = ℤ (knot-like). *Positive:* the conjunction cuts to a
handful ⇒ there IS an object-specific atom, and knot-ness moves from decoration to premise —
this is the only route by which Reid becomes load-bearing. *Negative:* the conjunction is still
populous ⇒ the atom is dead as an atom and the programme is, honestly and finally, about the
class. **Start here: it is the cheapest computation that can produce a cornerstone.**

**#2 — CONFLUENCE OF THE REGISTERABLE DESCENT.** *Cost: half a day; the code exists.* Promote
the RULE probe's exhaustive run to a `frontier/` arc with locks. *Positive:* retires the last
Michel import for the **endpoint** — the cascade becomes a theorem about the 27's content class
alone, ranking-free, and B869's two negative controls get sharper (so(14) and Sym² fail under
*every* rule). *Negative:* if some registerable path terminates elsewhere, the endpoint is
ranking-dependent and the cascade is fitted — a fatal finding, cheaply obtained.

**#3 — P5 MENU COMPLETENESS.** *Cost: 1–2 days (Dynkin's maximal-subalgebra classification +
Borel–de Siebenthal, machine-checked).* Enumerate all maximal subalgebras at each step and check
B861 §2's menus. *Positive:* removes the cascade's **single external dependency**; #2's
confluence result then stands unconditionally. *Negative:* a missing chain moves a step and every
uniqueness claim in B861/B863 falls. Fixes the known so(8)⊕u(1) omission en route.

**#4 — L114, ι's MEASUREMENT STATUS.** *Cost: 1 day; B787's rank computation already exists.*
Decide whether ι belongs in the observer's closing set. *Positive (ι out):* rank 3 is stable and
the FMT's S₃-triple can be compared to the torsor at step 1 — the only live join between the
measurement layer and the cascade. *Positive (ι in):* rank is 4, the 3=3 coincidence is dead by
arithmetic, and two ledgers get corrected. *Negative:* undecidable on present definitions ⇒ say
so and stop citing either measurement formalism as if joined.

**#5 — THE PROJECTION ROUTE FOR THE EXOTICS.** *Cost: 1 day, mostly literature.* Read a primary
source on Wilson-line/holonomy projection of E₆ exotics; m004 has a cusp, so holonomy is native.
*Positive:* a second, **object-native** exotics mechanism alongside imported `κ S D D̄`.
*Negative:* the κ mechanism stands alone as physics import — no worse than today.

**#6 — SCOPE NOTES (no compute).** *Cost: 1 hour.* Apply §5 below.

---

## 5. WHAT TO STOP CLAIMING NOW — the correction list

| # | statement | file | correction |
|---|---|---|---|
| 1 | *"If the bridge to physics works, it **must** run through the arithmetic atom (Reid-uniqueness → ℚ(√−3) → 2T → McKay)"* | `frontier/B282_e6_is_arithmetic_not_geometric/FINDINGS.md:43` | **The first arrow does not exist as an inference.** Reid-uniqueness does not produce ℚ(√−3); geometrization does (C6), and every member of PSL(2,O₃) shares it. Scope note, **not** retraction — B282's conclusion survives. |
| 2 | *"Object-specific forced content is narrow: the arithmetic atom (ℚ(√−3) → 2T → McKay E₆, B266/B282)"* | `docs/OPEN_PROBLEMS.md:33` | The 2T half is **not object-specific**: 24.2% of the sampled census carries exactly two surjections. Reword to "class-specific". |
| 3 | *"only the arithmetic atom (4₁ unique arithmetic knot, ℚ(√−3)↠2T) is object-specific"* | `docs/views/VERDICT_LEDGER.md:684`, `:831`, `docs/views/CLOSED_DOORS.md:123` | Same correction; these are generated views — fix the source rows. |
| 4 | *"E₆ → SO(10)×U(1) → SU(5)×U(1) → SM. Unique at every step."* | `frontier/B861_fused_cascade/FINDINGS.md` §2 | Add: **unique given the Michel ranking; the endpoint is ranking-independent.** The named intermediate stations rest on an unretired import. Also: step-2 menu omits so(8)⊕u(1). |
| 5 | *"the 12 exotics per generation \| **not addressed anywhere in the record** \| absent — a named gap"* | `docs/GUT_REQUIREMENTS_LEDGER.md:48` | **Both halves refuted 2026-08-08** (B970/B978). Count is **11**; addressed by B951/B962/B970. |
| 6 | *"the **twelve exotic states per generation**… **never addressed here**"* | `docs/THE_SM_VERDICT.md:153` | Same. |
| 7 | *"**L134 — THE TWELVE EXOTICS**… The record has never addressed them."* | `docs/OPEN_LEADS.md:922–924` | Same; L134 is **CLOSED** by B978. |
| 8 | X9 under **B. HOLE**; X9 queued at order 9, first move already done | `docs/THE_LADDER.md:42`, `docs/THE_CAMPAIGN.md:50` | Re-grade X9 out of HOLE, as X8 was on 2026-08-09. |

Items 5–8 are the **fifth instance** of the failure B978 §4 measured — here in reverse:
**declaring absent what the programme has already closed.** A cornerstone cannot cite these
ledgers as they stand.

---

## 6. THE HONEST CEILING

Suppose all six computations succeed. The programme would then have proved:

1. **A theorem about E₆**, ranking-free and menu-complete: every registerable descent from E₆
   carrying the 27's generation terminates at su(3)⊕su(2)⊕3u(1), with the global ℤ₆ form (B862)
   and hypercharge as the unique anomaly-free gaugeable U(1) (B864).
2. **A theorem about a commensurability class**: the Bianchi class over ℚ(√−3) supplies, through
   its quaternion algebra and ramified prime 3, exactly the finite group 2T whose McKay graph is
   Ê₆ — with 2T reachable from *no other* imaginary-quadratic field (B727 probe 2).
3. **Possibly** — only if #1 (the conjunction sweep) fires — a genuinely object-specific atom.

**What would still be out of reach, and would remain so:**

- **Every dimensionful quantity.** The object is **proved scale-free** (S3/B615), and B706 is
  total across value, field, dimension, cluster, vN-type and resurgence: *"every object continuum
  (A-poly curve / covering scale / tracial clock / ħ-series) is rigid/scale-free, **NONE the SM's
  free coupling**"* (`docs/LAW_MAP.md:159`). The dimensional firewall is explicit —
  `docs/STRATEGIC_SYNTHESIS.md`: κ *"**cannot become a dimensionful energy density without an
  externally supplied scale**"*, and *"the **scale is always supplied independently**."*
  α_s died on **kind**, not counting (`docs/OPEN_PROBLEMS.md:209`). Masses, M_GUT, proton
  lifetime, the Higgs VEV: permanently outside.
- **The 27-VEV half.** `docs/THE_LADDER.md` X24: *"the 27-VEV \| input in every framework,
  canonical nowhere."* B978 sharpens this from a rank statement to a mass statement: the object
  supplies the **adjoint** half, which provably cannot do matter mass. L138's orbit result does
  not close it — *"a canonical **orbit** is not a canonical **VEV**."*
- **Rank.** Every endpoint is rank 6 — SM core **+ 3 u(1)** — consistent with B952's rank
  obstruction. The cascade selects the SM *core*, never the SM.
- **The only addressable sector** is the dimensionless, RG-invariant one. L141
  (`docs/OPEN_LEADS.md:1019`): 't Hooft anomalies are *"**RG-invariant** — evaluable from the
  massless spectrum alone… **the one part of QFT a scale-free object may speak about without
  crossing Gate 5**"* — *"and why it is **the only one of its kind**."*

**The honest ceiling, stated once:** even at best, this is a complete and beautiful derivation of
the Standard Model's **gauge form** — group, global quotient, hypercharge assignment — from an
arithmetic class, with **no numbers**, and with the object's own uniqueness certificate still
hanging unused on the wall. That is worth building. It is not worth over-selling, and the base
rate measured today is the reason.

---

# ADDENDUM — computed AFTER this synthesis was written

The four probes ran before two computations landed on this seat. Both change the
texture of §1, and one of them removes a blocker the programme has carried for
weeks. Neither changes the verdict.

## A. The conjunction sweep — recommendation #1, executed

§4 ranked **the conjunction sweep** first: ask for *trace field ℚ(√−3) **and**
exactly two 2T surjections **and** H₁ = ℤ*, rather than for the atom alone. Run.

| condition | 4,000 one-cusped census | all 3,112 census knots |
|---|---|---|
| exactly two 2T surjections | 25.5 % | 36.4 % |
| H₁ = ℤ | 61.8 % | 100 % (they are knots) |
| **trace field ℚ(√−3)** | **0.18 %** | **1** |
| **all three** | **1 — m004** | **1 — `K2_1` ≡ m004** |

**The rare condition was never 2T. It is the trace field.** And the conjunction
returns m004 alone, in both reference classes.

## B. Sage installed — the exact invariant trace field, and a self-correction

Sage 10.7 + SnapPy 3.3.2 now run in one environment, removing the blocker
recorded in `frontier/B735_emittance/FINDINGS.md:32` as *"no Maass eigenvalue
computed — **Sage unavailable**"*. Exact invariant trace fields are computable:
m004 returns **x² − x + 1 = ℚ(ζ₆) = ℚ(√−3)**.

Validating the numeric proxy against exact arithmetic over 400 manifolds:
**396/400 agree, 0 false positives, 4 false negatives.** I had documented the
proxy as *over*-counting; it **under**-counts, and the relay says so where the
claim was made.

The eight exact ℚ(√−3) manifolds in that slice, with homology:

| m003 | **m004** | m206 | m207 | m208 | m410 | s118 | s119 |
|---|---|---|---|---|---|---|---|
| ℤ/5+ℤ | **ℤ** | ℤ/5+ℤ | ℤ/3+ℤ/3+ℤ | ℤ/10+ℤ | ℤ/2+ℤ | ℤ/2+ℤ | ℤ/2+ℤ |

**Every member of the class carries H₁ torsion except m004.** A knot complement
in S³ has H₁ = ℤ exactly, so none of the others can be a knot. That is the
mechanism behind Reid's theorem, recovered from census data rather than assumed.

## C. What this does to the verdict — it sharpens §1, it does not overturn it

The specificity **exists** and is now measured: within its own commensurability
class, m004 is isolated by **H₁ = ℤ**. What the REID probe established is that
the derivation consumes the **trace field** and never H₁ — so it selects the
class (m003, m206, m207, m208, m410, s118, s119, …) and stops one condition
short of the object.

So the cornerstone is **one nameable step away**, and §4's recommendation #1 is
now discharged with a sharper successor:

> **#1′ — Find the step that consumes H₁ = ℤ (or knot-ness in S³), or prove
> none can exist.** If such a step is found, the derivation inherits Reid's
> uniqueness and the cornerstone is built. If it provably cannot exist, the
> programme is about the Bianchi class PSL(2,O₃) — which is a real result, and
> should then be *claimed* as one rather than left ambiguous.

This is also, once more, B727's July sentence with both sides now measured:
*"the SM-resonance is the field's, the arithmetic-knot uniqueness is the
knot's; they live on different objects and do not reinforce each other."*

— cc3, addendum

---

# ADDENDUM 2 — recommendation #2 DISCHARGED (result reported to this seat, not re-run here)

**Provenance, stated first:** the confluence computation below was run
elsewhere and reported to this seat by the owner. It is recorded faithfully and
is **not independently verified here.** Verification would mean re-running the
enumeration against B861 §2's menus; that is cheap and is listed at the end.

## The result

Registerable options per step: **[3, 2, 1]**

- **Step 1** — three of four survive; **Sp(8) fails registerability.**
- **Step 2** — both survive.
- **Step 3** — **exactly one**: the Standard Model. **SU(4)×U(1) fails.**

Enumerating every registerability-respecting selection function gives **six**
reachable chains, and **all six terminate at the SM**:

| path | endpoint |
|---|---|
| SO(10)×U(1) → SU(5)×U(1) → SM | ✓ |
| SO(10)×U(1) → Pati–Salam → SM | ✓ |
| SU(6)×SU(2) → SU(5)×U(1) → SM | ✓ |
| SU(6)×SU(2) → Pati–Salam → SM | ✓ |
| SU(3)³ → SU(5)×U(1) → SM | ✓ |
| SU(3)³ → Pati–Salam → SM | ✓ |

Named variants: max-dim and first-listed give the banked chain; min-dim and
last-listed give SU(3)³ → Pati–Salam → SM. **Different intermediates, same
destination.**

## What it settles — the assumed half was never doing the work

The cascade has two halves and the programme has been crediting the wrong one:

| half | status in the ledger | what it actually does |
|---|---|---|
| **registerability** | **DERIVED** — B860 theorem-grade, B871 | **forces the endpoint** |
| maximal residual symmetry | **ASSUMED** — B859/B874, never retired | selects only *which path* |

So **the Standard Model endpoint does not rest on an unretired assumption.**
That assumption was only ever choosing scenery. Read with B869's control — the
rule is not an SM-generator from arbitrary inputs — the robustness is evidence
that **the menu structure forces the endpoint, not the ranking.**

This is the strengthening the RULE probe (§3.4) named in advance as the good
outcome, and it arrived.

## What it does NOT do, and this must not blur

It improves the **E₆ → SM** half of the spine. It does nothing to the
**m004 → E₆** half, which is where today's base-rate control landed:

- 2T is carried by ~35 % of the census and 36.4 % of knot complements;
- the derivation consumes the **trace field** (a commensurability-class
  invariant, B803) and never **H₁ = ℤ**, the condition that isolates m004
  inside its own class.

A more robust theorem about E₆ is still a theorem about E₆. **Confluence raises
the quality of the second half; it cannot supply the specificity missing from
the first.** Anyone tempted to read the six-for-six result as strengthening the
*object* claim should stop at this paragraph — that inference is exactly the
E20 shape.

## Correction list, refined

§5 item 4 said B861's *"unique at every step"* should carry *"unique given the
Michel ranking."* This result **improves** that correction: the honest statement
is stronger, not weaker —

> **The endpoint is forced by registerability alone; the ranking selects only
> the path. All six registerable chains terminate at the SM.**

That should replace item 4 rather than qualify it.

## To verify (cheap, and worth doing before it is banked)

1. Re-run the enumeration against `frontier/B861_fused_cascade/FINDINGS.md` §2's
   menus and confirm the option counts [3, 2, 1] and the two failures
   (Sp(8) at step 1, SU(4)×U(1) at step 3).
2. Cross-check against **P5 menu completeness** — still the cascade's single
   external dependency (§4 #3). Confluence over an *incomplete* menu proves
   confluence over that menu only. This is now the load-bearing open item for
   the cascade half, and it just became more valuable: with the ranking retired,
   **P5 is the only remaining import.**

— cc3, recording a result obtained elsewhere
