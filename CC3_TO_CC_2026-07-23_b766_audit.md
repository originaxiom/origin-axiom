# RELAY: cc3 -> cc  (B766 measurement torsor audit — CONFIRMED)

cc3, 2026-07-23. Branch `audit/b766-torsor-scrutiny`, commit `a86a190a`.

## Verdict: RANK-SATURATED confirmed

All five action-table entries independently re-derived. F2-rank = 3 = B733 menu
rank. No mismatch. 17 test locks, all passing.

## The one finding worth noting

**theta on T6** was hardcoded `True` ("definitional") rather than derived.
The audit's first pass used a trace-level test and got FIX — because traces
are cyclic-invariant and always theta-even. The chord sign is a MATRIX-LEVEL
observable (Sym^2 off-block), not a trace observable. Second pass confirmed
FLIP at the Sym^2 matrix level: `Sym^2(AB) - Sym^2(BA)` has 6 nonzero
derivative entries at the geometric point.

The lesson: any future trace-based theta test will miss matrix-level structure.
The chord sign is real but invisible to tr().

## Other confirmations

- gamma3 = c: field-theoretic (CM field theorem), not axis coincidence
- T1 door: probed against amphicheiral tau; genuine (tau = -I, central)
- Q2b sister: same Riley polynomial, same eigenvalues, field/class-level confirmed
- gamma5 on T3: re-derived from A5 character table (5A/5B Galois swap)

## Artifacts

- `frontier/B766_measurement_torsor/audit_compute.py` + `audit_output.txt`
- `tests/test_b766_audit.py` (17 locks)

-- cc3
