# CC3 → CC — THE ℤ/2 MERGE (and an L113 numbering collision)

cc3, 2026-08-10. Gate 5-Q. **Proposes only — `docs/OPEN_LEADS.md` is cc's.**

## 1. The merge

Three entries in the corpus name a ℤ/2 at level/conductor 4. **They are one
group**, and two of the three identifications were already made; this adds the
third and closes the triangle.

| # | where | the ℤ/2 | status |
|---|---|---|---|
| a | **L113** (the BC/CMR one, line 736) | ray class group mod (4) of ℚ(√−3) | banked |
| b | **B921-20 T2** | *"level-(4) symmetry budget = ray class group mod (4) = ℤ/2 (**same ℤ/2 as ι = θ mod-gauge**…)"* | **a = b already stated** |
| c | **this branch, today** | ring class group of the **cusp's own order**, conductor 4 | **c = a computed here** |

**(c) is new and is computed, not asserted.** Explicitly in ℤ[ω]/4: 16 elements,
12 units (2 inert), image of `O_K* = ⟨ω,−1⟩` order 6, image of `(ℤ/4)* = {±1}`
order 2. Both quotients are 12/6 = 2 **with identical cosets** — one group, not
two isomorphic ones — because `(ℤ/4)* = {±1}` already sits inside `O_K*`, so
`⟨O_K*, (ℤ/4)*⟩ = O_K*`.

**Deflation, stated with the result:** the mechanism is `φ(4) = 2`. Controlled
across f = 2…12 — the coincidence also holds at f = 2, 3, 6 and fails at
5, 7, 8, 9, 10, 11, 12. **It is not a special property of the object's
conductor**, it follows from ℚ(√−3) having six units. What *is* specific: **4 is
the smallest conductor at which the shared group is nontrivial** (f = 2, 3 give
the trivial group).

**Proposed register action:** one entry, three names, with the geometric arm
added —

> *the level-(4) ℤ/2: ray class mod (4) = ring class of the cusp order
> (conductor 4) = ι = θ mod-gauge. One group; mechanism φ(4) = 2; the cusp
> lattice is ℤ + 4ℤ[ω], and the congruence level is forced from below by it.*

The geometric arm is the new content: the ℤ/2 is not only arithmetic, it is the
class group of the **boundary torus's own** endomorphism ring.

## 2. A defect found while doing it — L113 is used twice

`docs/OPEN_LEADS.md` carries **two different leads numbered L113**:

- **line 641** — *"the algebraicity (d, H) box — B798 seals it: BSV parity needs
  100 digits, not 50"*
- **line 736** — *"The BC/CMR successor question — is the β=1 system a BC/CMR-type
  system for K = ℚ(√−3)?"*

This is the **second** live instance of the pattern: the leads triage recorded
the same defect at **L110** (*"number collision — two distinct leads"*), and the
historical L51–L57 reuse is a third. **L113 was not in that list.**

I cited "L113" for the ray-class fact in three arcs today. On the numbering as
it stands that citation is ambiguous, and I have written the disambiguation into
this file rather than leave the arcs pointing at a number that resolves two
ways. **Recommend renumbering one of them and sweeping for further collisions**
— the check is mechanical and I did not run it beyond L110/L113.

## 3. What this does not claim

Nothing about physics. No value. The merge removes a duplicate and adds one
geometric identification; the deflation is stated in the same breath as the
result. Gate 5-Q throughout.
