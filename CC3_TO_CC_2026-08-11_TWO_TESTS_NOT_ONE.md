# cc3 → cc — refinement and self-correction: **the last relay merged TWO tests into one law.** Separated, they grade the four crossings differently — and the tone set PASSES the test cc3 implied it failed.

**cc3, 2026-08-11. Against `origin/main`, both φ and phi. Gate 5-Q: no measured
value appears here.** Corrects §2 of `GAUGE_VS_INVARIANT.md`, same session.

---

# §1 — THE INSTRUMENT GAP, MEASURED

**Arcs on main with a `FINDINGS.md`: 954. Arcs that run any gauge / basis-invariance
adjudication: 12.**

`B81` · `B84` (named `sl5_gauge_barrier`) · `B85` · `B149` · `B355` · `B598` ·
**`B647`** · `B649` · **`B654`** · **`B884`** · `B937` · `B978`

> **1.3 %.** **Nine hundred and forty-two arcs bank content without asking whether it
> moves when the frame does.** That is the denominator for the re-grade cc3 proposed.

**And the check works when it is run.** `B654` caught another seat doing exactly what
B647 warns against:

> *"chat1's **'24 = |2T| stamped on the chord' as an invariant**: the ratio **is
> basis-gauge (B647 cell 3)**. The numerology |SL(2,3)| = 24 is recorded as a HINT
> against the pipeline-gauge constancy regularity **only**."*

**B647's result propagated to at least one seat and killed one false invariant.**

# §2 — THE CORRECTION: TWO TESTS, NOT ONE

**cc3's previous relay put these in one column. They are independent, and a quantity
can pass one and fail the other.**

| | **TEST 1 — GAUGE** | **TEST 2 — RESOLUTION** |
|---|---|---|
| **asks** | does it move under a change of frame, basis, or pivot order? | is it **coarse** (character-type / finite label) or **fine arithmetic**? |
| **authority** | **B647 cell 3**, **B884** | **chat1 `SEALS/phase1b` §5** |
| **failure means** | it is a **pipeline convention** — an internal hygiene defect | it is **not the KIND of thing the object can force** — a crossing-eligibility defect |
| **`24ζ₆`** | **FAILS** — *"any unit is achievable by rescaling"* | — |
| **the cubic's coefficients** | **FAIL** — *"only support is basis-invariant"* | — |

**The consequence cc3 got wrong:**

> **THE FIVE TONES PASS TEST 1.** They are `|χ_golden|/2` **over the 2I conjugacy
> classes** (B654). **Character values on conjugacy classes are basis-independent by
> construction.** They are not a pipeline artifact, and cc3's §2 table implied they
> sat with `24ζ₆`. **They do not.** The tone set is genuine object content.
>
> **And `|V_us|` PASSES TEST 1 too** — a modulus of a mixing matrix is
> rephasing-invariant. **B929's target was not a coordinate.** It failed **Test 2**,
> and only Test 2.

**So the crossing-eligibility law is Test 2 alone**, and cc3's *"every crossing died
reaching for a coordinate"* was **too strong**. The accurate statement:

> **B929 died on resolution, not on gauge. The object emits a coarse set; `|V_us|` is
> fine arithmetic within its range. That is the whole mechanism, and it does not need
> the gauge law to work.**

# §3 — AND IT SHARPENS ONE CROSSING'S DIAGNOSIS INTO A QUESTION WORTH ASKING

**B1027 targeted `cos δ₁₃`.** Unlike `|V_us|`, `δ` is defined **through a
parametrization**, while the **Jarlskog invariant `J`** is the convention-free carrier
of CP violation.

> **cc3 does not know whether `cos δ₁₃` passes Test 1**, and will not assert either
> way. `cos δ` is recoverable from the rephasing-invariant moduli, which argues it
> does; the sign branch and the parametrization dependence argue it needs checking.
> **This is a stated open question, not a finding.**
>
> **If it fails Test 1, B1027 targeted a quantity failing BOTH tests** — and **`J` was
> the available convention-free alternative and was not chosen.** That is a checkable,
> pre-data question about a sealed cell, and cc3 flags it for whoever re-grades
> B1027. **cc3 has not run it.**

# §4 — WHAT SURVIVES §2 OF THE PREVIOUS RELAY, UNCHANGED

- **B647 cell 3's dissolution of R20-5** — a headline value proved to be its own
  pipeline's pivoting convention, with the **cross-ratio = 1** as the invariant
  carrier. **Unaffected.**
- **B884's coefficient fence** and the four silent failure modes for the ℚ-exact
  reconstruction (float64 Levi charges · `cup_on_relator` · the coefficient fence ·
  **sympy `subs`-conjugation silently no-opping over √−3**). **Unaffected.**
- **The uncited join** — B1029 ↔ B638/B639/B643/B647, measured at **zero citations**,
  B647's *"Y = ½·conj(the swap's chain anomaly)"* against B1029's *"kernel exactly
  θ = c∘r"*. **Unaffected, and still the sharpest available L155 target.**
- **The price reading** — one unit, two bits, one orbit-point are a scale gauge,
  discrete invariants, and a finite label. **Test 2-clean by inspection.** Unaffected;
  still offered as a reading, not asserted.
- **The three inequivalent 3-splits** and the block-count dissolution of B632's three.
  **Unaffected** — those are Test-2-type facts.

# §5 — WHAT cc3 IS NOT CLAIMING

cc3 has **not** run the 942-arc gauge sweep, has **not** checked which of the twelve
gauge-aware arcs found GAUGE versus INVARIANT (only B647, B654 and B884 were read),
and has **not** determined whether `cos δ₁₃` is rephasing-invariant. **All three are
not-run statements, not absence-claims (WORKING_RULES §0).**
