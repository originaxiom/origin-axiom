# THE CLOSURE PROGRAM (DRAFT — merged design; launches on the owner's go after cc3's close-out)

*cc, 2026-07-23. The merged plan: cc's phase-gated design + the courier's handoff, received
under the standard gate. The courier's tiers are absorbed where they survive; the
adjudication notes below record what changed and why. Phase gates = owner checkpoints;
cc3 = the standing verification seat; every criterion sealed before its computation.*

## The four terminal states (the program's organizing principle — cc's design, kept)
Every open item ends CLOSED / WALLED (a no-go theorem for why it stays open) /
CONSTITUTIVELY-OPEN (openness proven, entered into the chain as boundary) / EXTERNAL
(handed off with a registered criterion). The courier's "completion = delivery" criteria
slot into Phase 5; the WALLS phase (absent from their plan) stays — it is the program's
signature move made systematic.

## Phases
- **Phase 0 — the Closure Census.** Blind multiagent sweep of the whole record (leads,
  carries, EXTENDED cells, doors, hints, upgrade-path columns); per-item terminal-state
  classification with sealed criteria. The courier's tier list = one input, diffed
  against the sweep.
- **Phase 1 — mechanical closures.** The residual quartet (WALL-7 53-point sweep; B685
  3-integrality; TOMB-L310 DAG; TOMB-L34 c_eff) + B500's wrap. [Courier 1.1/1.2 absorbed
  with corrections: the count is FOUR + B500, not seven — B767 already closed B489 and
  TOMB-L255; their 1.3 "let the stabilisations complete" is stale — B767 DELIVERED.]
- **Phase 2 — structural closures.** The T1-mover hunt (the outer-S₃ spec — expected
  wall); H-WORD-WELD derive-or-dismiss; the correspondence enumeration + permutation
  null (built by us); the D₅ = Spin(10) McKay check [courier T4]; Op-2 and the
  discrete-spectrum door if the owner opens them. [All absent from the courier's plan
  except the McKay item.]
- **Phase 3 — the correspondence closure.** cc3's folded verdicts; the θ-vacancy search
  [courier 2.1 — with the CONFLICT flagged: their candidate "ownership" IS the
  self/other boundary by their own Side-B citations (Gallagher: ownership = the c-side)
  — it cannot also occupy θ; their upādāna (grasping/releasing) is genuinely new and
  enters the gate]; the triadic-T1 verification vs the S₃ frame algebra [2.2]; F2's
  settlement [2.3]; the K.C. adjudication [2.4]; then P021-v3 sealed and matched, the
  owner adjudicating, VACANCIES KEPT AS VACANCIES if that is the truth [2.5, amended —
  their "Director fills" reverts to elicitation; the cross-test discipline stands].
- **Phase 4 — the walls.** The boundary theorems into the chain: the
  choice-incomputability argument as C22; A0's uncrossability; Q5's wall; whatever
  Phase 2 walls. [Absent from the courier's plan entirely.]
- **Phase 5 — the capstone.** PC26 (the chain paper) + the methodology paper [courier
  3.1/3.2, absorbed]; the specialist dossier + the five NEEDS-SPECIALIST leads [3.4/T4];
  **the JUNO timestamping AMENDED — see the pin note**; the final gold-standard pass
  (suite + slow tier + mutation sample + branch inventory); the seal tag.

## THE PIN NOTE (a governance refusal, banked)
The courier's 3.3 proposes timestamping BOTH 1/(2φ) and 1/(φ√5) for JUNO. **REFUSED:**
the co-signed one-number pin (B753, cc+cc3, in HINT_LEDGER) says one observable, one
registered number — 0.30902 stays; 1/(φ√5) is program-internal and does NOT register;
two live numbers for one measurement is the n=2-gap trap pointed at ourselves (Q6).
What Phase 5 MAY do: OpenTimestamp the EXISTING registration exactly as banked (the
hint, its wording, its low evidential grade) — a timestamp hardens provenance; it must
not widen the bet.

## Sequencing (standing)
cc3's close-out (the forks verification) lands FIRST → the owner's go → Phase 0.
B500 grinds into Phase 1 regardless. The owner checkpoints between every phase.
