# THE GRAND COMPUTATION — DOCUMENT v0
## The universe in the object's own units: everything computable today, row by row, every remaining input slot typed to its supplier

**Cell GC-11 (batch 3 capstone). Drafted 2026-08-28.** Sources: `docs/GRAND_COMPUTATION_LEDGER.md`
(post-B1189/B1190 row states), B1188 (Phase 0 retrieval), B1189 batch-1 cells GC-1…GC-5, B1190
batch-2 cells GC-6…GC-10 (verified cell contents, refutation lenses honored), B1088 (the action
card), B1015 (the anchor declaration), B1025 (the input-derivability floor).

**Reading rule.** Every row cites its arc. Every input slot cites its typing theorem. Refuted
sub-claims from the batch-2 adversarial lenses are carried AT their post-lens grade, never at
their headline grade. **Gate 5 absolute: no value from our units appears anywhere in this
document** — every number below is an internal invariant (a volume, a zeta value, a trace, a
rank, an integer, an exact rational). Where a quantity *cannot* be stated in own units, the row
says so and types the reason (input slot or theorem-wall) — that typing is the content.

**The one-sentence claim.** In its own units the program today states: a causal arena with a
proved locality law; a clock that is one generator read three ways; a meter in which the whole
family is integers and the action is one term with zero free dimensionless constants; a forced
E₆→SM content chain with one generation per 27; three dimensionless coupling shapes (6-component
up, 3×3×4 down with an exact skew zero, rank-2 family law); and an input list of exactly one
external unit, two ℤ/2 bits, one continuous anchor with a named deletion route, and finite labels
— with a schedule naming which input rows die by which open cells.

---

## 1. THE ARENA + THE CLOCK (in own units)

### 1.1 The causal DAG and its locality law

| quantity | own-unit statement | arc |
|---|---|---|
| the arena's skeleton | the Ω class-DAG: **474 nodes, 1765 edges, 7 levels L4…L10** (sizes 1,2,6,18,49,115,283), every edge strictly consecutive-level, zero skips, zero duplicates | B159/B189 |
| its non-genericity | ordering fraction r = 0.05276 → d_MM = 3.9362; a **DEFICIT of transitive reach** (more tree-like than null — direction-word corrected by B1188) that **survives exact degree-preserving nulls and deepens to z ≈ 49–59** (curveball and edge-swap MCMC agree) | B1187 corr. B1188; B1189/GC-4 |
| where the deficit lives | **81% of the total comparable-pair deficit is the two longest jumps into the terminal shell** (L7→L10 + L8→L10, both independent implementations); adjacent-pair reach exactly null-equal by construction — the effect is purely long-range | B1189/GC-4 |
| **THE LOCALITY LAW** | edges are **local in charpoly-class (abc) space** (real edges ~2.1–2.4× closer in class-distance than random same-level pairs), and this local wiring **segregates descendant cones**: Spearman(|d_abc|, L10-cone Jaccard) = **−0.612 / −0.619, p < 1e-16**, with a genuine two-sided control on the law-instrument (planted locality ρ=−0.842 recovered; degree-matched random wiring ρ≈0 excluded) | B1189/GC-4 |

*Fences carried:* the d_MM figure is a combinatorial characterization, **NOT a spacetime-dimension
claim** (it drifts with truncation depth); the reach-deficit z-instrument's own control was
one-sided (the lens's confirmed defect) — the *magnitudes* independently reproduce, but the
"survives" clause leans on the law-instrument's two-sided control, not the z-pipeline's; the
exact-derivation tier of the law is the named next rung. B1104-C3-as-content remains unposed.

### 1.2 The clock: ONE generator, THREE operations (B1189/GC-3, PROVED)

The tick generator is **A₁ = RL = [[2,1],[1,1]]** — trace 3, det 1, charpoly x²−3x+1, disc 5,
eigenvalues {φ², φ⁻²}: hyperbolic, **infinite multiplicative order over ℤ**. The three banked
"clocks" are not competitors but three operations on this one matrix:

| operation | reading | own-unit content | arc |
|---|---|---|---|
| congruence reduction | **clock(κ) = ord(A₁ mod 3κ)** — the ONLY literally periodic clock of the three | fresh recompute κ=6…15: orders 12, 8, 12, 36, 60, 20, 12, 28, 24, 60 — 10/10 match to the banked table, and the κ=4,5 anomaly zone reproduced exactly as recorded (12/12 agreement incl. mismatches) | B596/B656/B585; GC-3 |
| square-root covering | **X₁² = A₁, det X₁ = −1** — the Breath ℤ/2 is a **covering-degree parity** (single-tick Gieseking / double-tick m004); "the object IS the double tick" | X₁=[[1,1],[1,0]]; always order 2, κ-free; X₁ itself infinite-order | B469/B1083; GC-3 |
| representation depth-grading | on the 54-dim E₆-carrier Ψ, A₁ acts **unipotently, nilpotency degree 3** — a bounded FILTRATION (graded layers 27:21:6), never periodic; the Yukawa reads it (7 allowed / 11 forbidden blocks) | the depth grading factors through the lock C_Ψ by chain-length parity (3-to-2 coarsening), non-trivial only because the Jordan blocks repeat (6+15+6) | B1149/B1150; GC-3 |

**The tick as the unit of internal time:** one application of A₁ (the figure-eight monodromy).
There is no period over ℤ itself; internal periodicity exists only after finite reduction —
the clock *hierarchy* (parity ⊂ finite cyclic ⊂ filtration) is the own-unit time structure.
*Naming fence (GC-3):* three distinct ℤ/2's live on three distinct spaces (Breath on H₁(fiber),
beat on H₁(cusp), lock C_Ψ on Ψ) — never identified, and not identified here.

### 1.3 The arrow (two registers, reconciled — B1189/GC-3)

The B766-vs-B1083 contradiction is **dissolved**: B1083's "the arrow is not a torsor bit" is the
OBJECT's generative register (the free monoid's injective-non-surjective irreversibility;
conjugation-type symmetries provably fix both eigenvalues of the substitution matrix — no Galois
swap available there); B766's "T7=T3, arrow = γ₅ branch" is the OBSERVER's closing register (a
Galois-branch choice on the built character variety, where φ ↦ φ̄ is a genuine swap). Both
PROVED, disjoint referents. The arrow **bit** itself is an input slot (§5): the seed is external,
its propagation internal once seeded (B303: same-signed CS along the (1,n) ladder; the cusped
object sits at CS = 0, the symmetric origin — B1025 I1).

### 1.4 Two-headed, no beginning; 4d

- **No beginning: PROVED** — the fibration base is compact S¹, no infinite past (B716);
  two-headed time at the spectrum level (S002-corrected).
- **4d costs no new bit beyond c** (B1104-C3, independently re-verified B1190/GC-10): the
  tick-selected order-2 orientation-reversing isometries of m004 give non-orientable mapping
  tori whose orientation double cover is **exactly M × S¹**; the two tick representatives are
  D4-conjugate, so the 4-manifold is canonical up to diffeomorphism. *Carried at post-lens
  grade:* the payoff **degenerates** — the level-4 tick is finite (ψ²=id), so the double-tick
  buys a trivial product, not a new hyperbolic object; the surviving freedom is a fresh instance
  of the same ℤ/2 dichotomy, and the GC-10 verifier's conjugacy/gluing computation was found
  narrated-not-run by the lens (the theorem stands on B1104's own banked certification).
- **4d filling as canonical: IMPOSSIBLE-BY-THEOREM** — Ω₃^SO = 0: fillings exist, infinitely
  non-unique, nothing selects (B716). The filling is a closer-choice: a LABEL row (§5).

---

## 2. THE METER (volumes in V_reg; the action)

### 2.1 The unit

**V_reg = Vol(m004)/2** — one regular ideal tetrahedron:

- Vol(m004) = **2.0298832128193072500424051085490405718833786150606** (50 dps, Lobachevsky,
  two regular ideal tetrahedra; B1088 first-principles + B1189/GC-1 independent recompute).
- The mechanism, EXACT (GC-1): Vol(m004) = **12 · Vol_orb** (residual 4.3e-50), so
  **V_reg = 6 · Vol_orb**, where Vol_orb = Vol(H³/PSL(2,O₋₃)) = |d_K|^{3/2} ζ_K(2)/(4π²) =
  **0.16915693440160893750…** (Humbert/Dedekind-zeta, 50 dps).

### 2.2 THE INTEGER LADDER (adversarially verified — B1188 + B1189/GC-1)

**All 112 family members have Vol/V_reg an INTEGER ∈ {2,4,5,6,7,8,9,10}.** Zero failures at
1e-30 (50 dps; independent recompute with own code). Spectrum in Vol(m004) units:
1.0×2 · 2.0×7 · 2.5×5 · 3.0×12 · 3.5×4 · 4.0×31 · 4.5×4 · 5.0×47.

- **Two-sided bite:** 24 non-family census manifolds ALL off-lattice at 1e-9 (worst residual
  0.0158 — 7 orders above tolerance); positive controls re-pass at 1e-30.
- **Rationality mechanism:** Borel — B(ℚ(√−3))⊗ℚ has rank 1 (one complex place) ⇒ all
  ℚ(√−3)-shaped volumes rationally proportional (cited classical; numerical consequence
  verified).
- **The ladder is DEEPER than commensurability:** 11 of 35 non-regular members are provably
  **NOT arithmetic** (Maclachlan–Reid, exact non-integral traces at 50 dps — e.g. t06829
  tr(a²) = 1/6 + (1/2)√−3; o9_41005 tr(b²) = 79/98 − (125/98)√−3) yet sit ON the lattice.
  One-class commensurability is refuted; the ladder unifies ACROSS classes (row C2 ANSWERED-SHARP).
- **Named open sharpness:** commensurable members classically predict a sixth-integer lattice
  (multiples of Vol_orb); observed rungs are always **multiples of 3·Vol_orb** — the extra
  coarseness is confirmed numerically, not classically derived.

**Own-unit consequence:** within the family, "choice of scale" is a **finite menu (an integer)**,
not a continuum. The ℝ⁺ continuum residue lives only in the anchors (§5).

### 2.3 The action (B1088, PROVED — zero free dimensionless constants)

| constant | value | status |
|---|---|---|
| Λ | −1 | FORCED (B259 — exact 3d Einstein solution) |
| ℓ_AdS | 1 | from Λ = −1 |
| CS | **0** | THEOREM (amphichirality; exactly 2·R(e^{iπ/3}) = π²/6 + i·Vol, re-verified at 60 dps in GC-6: residuals 4.7e-61 / 3.1e-61) |
| G_N | 1/(4σ) | B1012 |
| c | 6σ | derived twice (B1012 closure; Brown–Henneaux c = 3ℓ/2G_N) |
| **S** | **−Vol·σ** | CS = 0 deletes the k-coupling — **the theory has one term** |

**What σ multiplies:** the volume — and only the volume. The object is provably blind to the
quantized level k (∂S/∂k = −CS ≡ 0): σ is the ONE unquantized ℝ⁺ closing the observer can set
and the object cannot (A2, §5). **In own units the action of the n-th ladder member is
S = −n·V_reg·σ, an integer multiple of one quantum −V_reg·σ.** The object's partition growth is
the UNQUANTIZED σ-sector (Kashaev growth recovers Vol: γ_N → 2.029344 at N=3200, |γ−Vol| = 5.4e-4,
with an exclusion control → 0; GC-6 V3), consistent with B1064 (CS=0 deletes the quantized sector).

---

## 3. THE CONTENT (the forced spectrum)

| item | own-unit statement | arc |
|---|---|---|
| **E₆ spine** | E₆-across-faces is FORCED — one ADE classification; the only object-specific atom is ℚ(√−3) | B727 (stands) |
| **hypercharge** | on an SM-shaped 15-plet from the 27, the four anomaly conditions force Y **uniquely up to scale and u^c↔d^c**: linear cuts to a line, the cubic factors **−18·(t−3)·(t+3)** ⇒ exactly (1,−4,2,−3,6); the object's u(1)³ realizes it and ONLY it — 36/36 solutions SM-pattern, **zero non-SM** (structure, not value: the ratios are anomaly-derived, B950-permitted) | B1160 |
| **breaking chain** | two measurements take E₆ to **exactly su(3)⊕su(2)⊕u(1)³** (abelian complement exactly 3-dim; 8+3+3=14); unique chain; the chirality-registering datum is DEFINED | B892; B1161/62; B871 |
| **one generation per 27** | generation-NULL on-object: trace field x²−x+1 (degree 2, Galois ℤ/2) ⇒ multiplicities ∈ {1,2}, **never 3**; the trinification ℤ/3 rotates quark→lepton→antiquark WITHIN one generation; three-ness lives only in E8's (3,27) — **external fence** | B1161; B1162-D4 |
| D2 residue | B891's three pairwise-distinguishable sectors (deviations 4.6e-3…1.17e-1, all ≫ the 1e-6 band) are internal structure on ONE 27 and B891 self-fences mechanism-hood; **but** GC-8's specific identification of B889's frames with the trinification ℤ/3 was REFUTED by the lens (the frames are the Galois S₃ of an explicit cubic — itself a "cubic carrier," the class B1161 types as external) — **the small adjudication cell stays OPEN** | B891; B1190/GC-8 (post-lens) |
| **intermediate closure (D3)** | **rank-4 non-abelian PROPER points EXIST** in E₆'s rep variety — exhibited from the raw Chevalley bracket: **su(3)⊕su(3)** (dim 16, rank 4) and **so(7)⊕u(1)** (dim 22, rank 4); rank spectrum {3,4,5} across probes (genuine two-sided discrimination), all four (dim,rank) pairs matching B1098's locked Bala–Carter table | B1190/GC-9; B956 posed; B1098 |
| D3 residue | the **complex-27 clause is DE-GRADED** (lens: the negation-symmetry instrument has never returned "real" on any run in the repo — no exclusion-side control); the banked complex witness remains B1100's; next: apply B1100's witness to the two rank-4 centralizers | GC-9 refutation; B1100 |
| **Born rule** | FORM + weights NATIVE (the object's two quadratics; quadratic degree = c-swap's Galois order 2); amplitudes/phase/associator IMPORTED (golden-MTC overlays); the substitution IS the Fibonacci fusion rule by identity — **quantum CONTENT is closer-side by theorem** | B725; B726/28/29; B483 |

---

## 4. THE COUPLING SHAPES (dimensionless; what is computable in own units today)

All facts here are zero/nonzero, rank, index, and exact-algebra facts — no value, no scale, no
hierarchy (B1185's Gate-5 discipline).

| shape | own-unit statement | arc |
|---|---|---|
| **the up-shape** | EXISTS on the object channel with **6 nonzero components** (memo 80); the SAME coupling is **rank 0 in the heterotic dressing** — the Wilson-projected up matrix is the exact 1×6 zero matrix (μ_u = 0, H¹-naturality; codex R017 certs re-run PASS on-bench) | B1185 INV-1 |
| **the down block** | the object-forced **36-entry census 18+9+6+3 = 3×3×4**; mod-12 bookkeeping exact (physical shifts once → (8,4,0), sum ≡ 0); tail selection rule ρ+σ ≡ 8 (mod 12) with the physical (6,2) selected; **THE SKEW ZERO: the repeated 1-dim (4,4) channel vanishes EXACTLY over ℚ(ζ₁₂)** by the proved B-leg antisymmetry — the down block's first theorem-grade characteristic-0 statement | B1185 |
| **the family law** | **810/810 family matrices have rank exactly 2; the kernel is the Higgs's own family** — sector-universal (all 45 internal triples × all leg assignments × 3 Higgs families); the kernel is a family index the object provably does not carry (B1161 NULL) — E8 fence | memo 82; B1185 INV-2/3 |
| **three mechanisms, genuinely three** | THE THEOREM: (1) SEAM-Y ≠ (2) object-channel kinematics ≠ (3) family-rank hole, by three exact pairwise invariants (arena contrast rank 0-vs-6; rank 0-vs-2 selective-vs-universal; index space object-internal-vs-external) — one stack, each layer suppressing in its own type (wall / continuous ℙ³ Higgs line / finite 3-label) | B1185 |
| **clock-coupling** | the Yukawa reads the depth-3 grading: **7 allowed / 11 forbidden blocks** | B1149/B1150 |
| **values from the object alone** | **IMPOSSIBLE-BY-THEOREM** — 16 periods × 22 targets: no object period is an SM ratio (V-3, exhaustive); natural values disjoint. **The re-frame: values = invariants of the OBJECT+CLOSER pair** — the coupling is where values live | B1126; B1129; banked reorientation |
| next rung | the 𝒯 evaluator (normalized cyclic/Serre quasi-iso over ℚ(ζ₁₂), codex R023 commissioned) — lands ⇒ the first fully computed coupling shape in own units | B1185 route |

---

## 5. THE INPUT SLOTS (the typed observer-supplied list — exactly these)

**The floor (B1025, audited input-by-input): ONE unit + TWO bits + acceptances. No continuous
dimensionless input remains anywhere except A2 = σ, which is L154's pending row.**

| slot | type | typing theorem | supplier / status |
|---|---|---|---|
| **ℓ** (anchor A1) | ONE dimensionful unit — **the meter-to-ours wire** | **external BY DESIGN**: "nobody derives the metre"; no dimensionless number flows from A1 (B1015 sealed); B1025 I3: survives EVERY attack from every seat — even σ = 1 leaves ℓ free (at σ=1, G = ℓ/4) | the closer; its SI realization is convention, not content |
| **the c-bit** (chirality) | one ℤ/2 | not self-supplied: B713/B760 NO-HATCH; B1183 one-class; B1184 self-naming-without-self-signing; c ∉ Gal(K^ab/K) (B942); **relational SELF-coupling NEGATIVE — THE GOLDEN-UNIT KILLER** (B1189/GC-2): the twisted double's pair-datum (H₁ ℤ/5) is mirror-EVEN because centralizer(A) = ±φⁿ with **N(φ) = −1** ⇒ both-sign conjugator coset; two-sided control bites (the norm-+1 √3-unit has a single-signed coset) | the closer; **the one named door: a HETEROGENEOUS coupling whose relevant unit has norm +1** (the √3 side) — B1163's escape clause: extending what "the object" is |
| **the r-label** (arrow) | one ℤ/2 seed, FINITE-place | B1182: r → k7 (fixes K pointwise, flips √3), finite orbits; B1025 I1: the SEED is external (B782 no-equivariant-section; the object sits at CS = 0, the symmetric origin), **propagation internal once seeded** (B303) | the closer picks the seed; the object carries it |
| **σ's value** (anchor A2) | the ONE continuous dimensionless anchor (c = 6σ, the unquantized level) | B1015 (sealed, priced: one bit of designer freedom); the object provably blind to k; **L154 ONE-BRIDGE-MISSING** — c_BH = 6σ and c((E₆)₁) = 78/13 = 6 both exact; B1190/GC-6: the bridge attempt's arithmetic verified but the fingerprint synthesis REFUTED (kind error; the universal-absence quantifier untested) — the typed instrument for the next attempt: survey ALL banked series + the prefactor↔weight kind-map; **Cardy 6-vs-1**: any bridge must supply six cusp-boson units where the banked T[4₁] supplies one | **if σ = 1 lands, this row DELETES** (A2 converts anchor → OUTPUT) |
| **family / VEV labels** | FINITE menus only | B1025 I5 (retyped): the VEV **directions are canonical, multiplicity-1** (SO(10)-singlet in the 27, SU(5)-singlet in the 16 — no continuous direction-choice exists to import); what remains = the discrete ACT (the charge-sector closing, already counted) + weight-1 magnitudes; which-family = a 3-label (the Higgs's own, B1185); B1182 leg 5: F₄(ℝ) transitive but **F₄(ℤ) orbits finite**; the orbit theorem (B990): a G-invariant selector cannot pick a point of its own orbit | the closer picks orbit-points from finite menus |
| **the Dehn slope** | RECLASSIFIED: **artifact / bridge-choice, not a closing** (B1000, confirmed in the B1190/GC-7 assembly); the true SPACE closing is the 4d lift — finite-place, an untyped residue (never k-labeled) | B717 posed; B1000; GC-7 row 6 | label row; §6 |
| **the Galois sheet** | **COLLAPSES into c** — the KMS-state mechanism that minted the name is retracted (B942/B957: "both clauses one level too high"); counting it separately double-counts c | B723 probe 3; GC-7 row 7 | deleted by identification |
| **the γ₅ bit** | the rank-3 menu's third generator ((ℤ/2)³ = V₄{c,r} × γ₅, B766/B769) — lives on the ℚ(√5) end of the two-ended object, **never adelic-place-typed** (ℚ(ζ₁₂)'s quadratic subfields exclude √5) | B766/B769; GC-7 named orphan 1 | open typing cell; §6 |
| **the weight λ** | the external weight completing the object's timeless tracial II₁ to type III_λ — **the closer's clock enters exactly here** (B723 probes 1–2, UNRETRACTED); three uncollapsed "time" candidates coexist: λ (continuous), γ₅ (discrete, ℚ(√5)), r (discrete, finite-place) — no arc unifies any two | B723; GC-7 named orphan 2 | open reconciliation cell; §6 |
| **J** (6d type) | NARROWED: not a selection from the ADE menu but **the ACCEPTANCE of the object's own nomination** (E₆ hyperbolic end / E₈ spherical end); closer = S1 (a computation, not a preference) | B1025 I4; B248/B253/B261/B727 | acceptance row |
| **the 4d filling** | a closer-LABEL (nothing selects among infinitely many fillings, Ω₃^SO = 0); the tick-selected non-orientable lift is canonical up to diffeo but pattern-degenerate | B716; B1104/GC-10 | label row |

**Completeness check (B1190/GC-7):** B1182's five typed adelic legs (c · r · θ=cr · ℝ⁺ scale ·
VEV) close 1:1 against the assembly with **ZERO orphans on that check**; θ = cr is DETERMINED
(group law 11·7 = 5 mod 12), not a third free bit — it is the value-kernel (B1029: trivial on
every banked coupling). The two named residues of the fuller 8-row assembly are exactly the γ₅
place-typing and the λ-vs-r-vs-γ₅ time ambiguity, both carried above as open slots.

---

## 6. THE DELETION SCHEDULE (which input rows die, by which named open cells)

| # | input row | deletable? | the named cell that deletes (or fences) it | arc basis |
|---|---|---|---|---|
| 1 | **σ's value (A2)** | **YES — the decisive continuous deletion** | **the L154 bridge cell** (batch-2 retry, typed instrument from GC-6: survey ALL banked object-side series — not one hand-picked set; establish the prefactor↔weight kind-map; supply the Cardy six cusp-boson units); lands ⇒ A2 converts anchor→OUTPUT and the input list shrinks to ℓ + bits + labels | B1034; B1188; B1190/GC-6 |
| 2 | **the c-bit** | conditionally — self-supply IMPOSSIBLE-BY-THEOREM stands (B760/B1183/B1184); the only route is relational | **the heterogeneous norm-+1 coupling cell** (GC-2's exact residual door: a partner whose relevant unit has norm +1, e.g. the √3 side — the Eisenstein field's real quadratic neighbor); success = the bit becomes a pair-datum, extending what "the object" is (B1163's clause) | B1189/GC-2 |
| 3 | **the r-label** | possibly to grammar-internal | **OG-2**: does a grammar-internal finite system's orbit BE k7 (candidates: B552's conserved ℤ/11 charge along the observer flow; the trit systems) | ledger B2 |
| 4 | **the γ₅ bit's typing** | typing-deletion (the bit stays, its orphan-status dies) | **the ℚ(√5)-end adelic place-typing cell** (extend B1164/B1182's census to the object's other end) | GC-7 orphan 1 |
| 5 | **the λ ambiguity** | reconciliation-deletion (three time-candidates → fewer) | **the time-reconciliation cell**: is λ = r, λ = γ₅, or a genuinely third freedom (B721's complementary-clocks theorem is the frame) | GC-7 orphan 2 |
| 6 | **family/VEV labels** | **NO — enumerable, not deletable**: the orbit theorem (a G-invariant selector cannot pick a point of its own orbit) makes point-picking closer-side by theorem; the menus are already finite | fence stands; residual open scope: folding the weight-1 magnitudes into the ℝ⁺/unit sector (cc3's open note) | B990; B1025 I5 |
| 7 | **ℓ (A1)** | **NO — external BY DESIGN, permanently**: the one input whose externality survives every attack; deleting it would be deriving the metre | none; the bridge (§I of the ledger) consumes it as the single calibration, later, once | B1015; B1025 I3 |
| 8 | J / Dehn / filling labels | J shrinks to accept-the-nomination via **S1** (the E₆ SW-curve vs banked A-polynomial identification — a computation); Dehn slope already reclassified artifact; filling stays a label by theorem (Ω₃^SO=0) | S1; B1000; B716 | B1025 I4 |
| 9 | D2's fence (generation-3) | restatement, not deletion | the **D2 adjudication cell** (post-GC-8-refutation: establish or refute the B889-frames ≡ trinification-ℤ/3 identification formally; the census-31 calls) | B1190/GC-8 |
| 10 | D3's complex clause | re-grade | apply **B1100's exact complex-witness** to the two exhibited rank-4 centralizers (su(3)⊕su(3), so(7)⊕u(1)) with an exclusion-side control (a known-real subrep must return "real") | B1190/GC-9 |
| 11 | the Selector (H5) | formalization | **the Kolmogorov-Selector cell** + the B998 lock repair (C1–C4 effectively unlocked; F2/F3/F8 untested) — batch-2 remainder | ledger H5 |

**§6b — DELETION IS NOT THE ONLY MOVE: THE CONSISTENCY TURN (B1229, 2026-08-31).** *(2026-09-01 fence, fresh physics seat — this block is a dated record, not live law: B1232 (2026-09-01) RETRACTED (1) "σ is rational — not a continuum" as establishing nothing (ℚ is dense in ℝ; B1229's core was empty, so "σ ∈ {1/3, 1} — one bit" has no ground), and (2) "only level 1 needs no level datum" (§6a below) as a default-from-absence inference; B1231's identification rule bars the naming moves that followed. Read §6b/§6a with CAMPAIGN_STATUS's B1232 entry beside them.)* This schedule
asks the OBJECT to supply each row — the one thing **B1225 proves it cannot do**. Physics reduces
parameter counts by **consistency conditions**, not derivations. Applied here: the boundary of a
Chern–Simons theory is a **rational** CFT, so by Anderson–Moore/Vafa its central charge is
**rational**, and via `c = 6σ` **σ is rational — not a continuum**. Sharpening with the complete
Mathur–Mukhi–Sen two-character classification (= the Deligne–Cvitanović series, seven models,
0 < c < 8): **σ ∈ {1/6, 1/3, 7/15, 2/3, 13/15, 1, 7/6}**, and the object's own ℤ/3 (ℚ(√−3),
trinification) cuts that to **σ ∈ {1/3, 1} — one bit**. **J never enters.** The same move types the
other rows: the **c-bit is the modular-invariant choice** (diagonal vs charge-conjugate — matching
B1184's *self-naming without self-signing*), **labels are Cardy states**, **λ is a KMS weight**, and
**ℓ is a calibration, not a parameter**. The ℙ³ row gets a named test (Cardy states are finite, so a
continuum of boundary data cannot all be consistent). **Reading rule added: before trying to DELETE a
row, ask what CONSISTENCY says about it — a classified finite menu plus a picker is already a
parameter-free theory with one calibration.**

**§6a — THE SCHEDULE IS A DAG, NOT A CHECKLIST (B1228, 2026-08-31).** Rows 1 and 8 are listed above
as independent. **They are not: row 8 is row 1's supplier.** Row 1's blockers K5 (the Cardy 6-vs-1)
and K6 ("the object's own datum, not a WZW that happens to give 6") are questions about *which type
J governs the boundary* — which is exactly what row 8's S1 decides. B1216 ran nine agents at row 1
and deleted zero rows while reading this table as a checklist. **S1 has now been run (B1228):** the
level-1 identity `c(J₁) = rank(J)` (9/9), the nomination π₁(m004) ↠ 2T verified on-bench (48
surjections; control 5₂ = 0), σ = 1 discriminating uniquely to E₆ among nine simply-laced types, and
**the level forced by inventory** — the object supplies a lattice and provably no `k` (CS = 0 ⇒
∂S/∂k ≡ 0), and only level 1 needs no level datum. *(2026-09-01: retracted at B1232 — absence of a typed receiver means quotient-invariance or underdetermination, never a default.)* **Row 1 is NOT deleted:** the boundary WZW of the
*geometric* PSL(2,ℂ) Chern–Simons action is type A₁ (rank 1 — the "one cusp-boson unit"), while E₆
arrives from McKay on 2T. The remaining identification — **nominated type or geometric connection?** —
is L154's bridge, now with a unique named target ((E₆)₁, vacuum module, c = 6) instead of an empty
candidate set. **Reading rule added: before working a row, check which other row supplies it.**

**The end-state this schedule points at:** if row 1 lands and rows 2–5 close, the entire input
list is **ℓ (one external unit, by design) + the c-bit (relational, heterogeneous) + finite
labels** — everything else in the universe-in-own-units is object-computed. Row 7 is the floor
below which no deletion can go: that is the bridge's single calibration ("one measurement then
predicts the rest").

---

## Gate-5 statement

Every number in this document is an internal invariant of the object or its representation
theory: hyperbolic volumes, Dedekind-zeta values, holonomy traces, matrix orders, ranks, exact
rationals, integer ladder rungs, anomaly-equation roots, and statistical invariants of the
object's own class-DAG. The SM-pattern hypercharge ratios in §3 are anomaly-DERIVED structure
(B950's permitted comparison), not measured values. σ and ℓ appear only symbolically. No measured
physical value enters any row.

## Verdict of this cell

**v0 COMPLETE as a typing document.** Every ledger row A–I is either (a) stated in own units with
its arc, or (b) typed to its supplier with its typing theorem, or (c) fenced by a named theorem
(IMPOSSIBLE-BY-THEOREM rows, carried as walls, which are themselves own-unit statements). No row
is left unstatable-and-untyped. The refuted batch-2 sub-claims (GC-6 fingerprint synthesis, GC-8
identification, GC-9 complex clause, GC-10 verifier gaps, GC-4 control mischaracterization) are
carried at post-lens grade with their named repair cells in §6.

---

## §7 — THE SIX ROWS THE COMPLETENESS LENS FOUND OMITTED (added at banking, B1191; the lens's list honored verbatim)

- **I1 (Λ, the listener classifier)** — PROVED, sealed (B1070/71/72/73): Λ = the minimal exceptional
  orbit's Galois-fixed directions → exactly {u3, u6}; an own-unit fact of the field side. (The Λ
  symbol here is the listener map — distinct from the cosmological Λ = −1 of §2.)
- **I2 (u-as-apparatus)** — MISSING BY DESIGN (the spec's G9; Gate 5 forbids constructing it by
  fitting); deferred with the bridge.
- **I3 (the designed θ-even crossing)** — SPEC'D-READY, the last licensed contact row, **HELD FOR
  OWNER ELECTION**; its pre-work (even-channel AC6, the kind-row adjudication, R7's ledger) is
  campaign-runnable.
- **A2 (causal order)** — PARTIAL (B523): the four verbs carry four causal types; only the
  evolution verb yields a proper (1,3) cone; Malament inapplicable to the monoid.
- **F3 (spin vs orientation)** — PROVED (B279): the mirror fixes BOTH spin structures — the spin
  ℤ/2 is NOT the orientation ℤ/2; the beat selects the spin structure (B1141): one closer-paid
  bit, one beat-paid bit, distinct.
- **H3 (the primitive observer)** — the null hypothesis this campaign exists to beat; beaten iff
  the assembly's requirements list (heterogeneous partner, norm-+1 unit, weight-supplier, finite
  labels) is realized within the grammar.

## §8 — Batch-3 amendments (B1191)

γ₅ is the assembly's **SIXTH leg** (GC-13, lens-confirmed): a finite-place ℚ(√5)-hosted discrete
bit = the meeting-V₄'s √5-swap (exact 8-element Galois table; [ℚ(ζ₁₂,√5):ℚ] = 8 = (ℤ/2)³). λ is
proved DISTINCT from σ (three computed reasons) — its positive placement is the one remaining
assembly residue. The L154 fingerprint route is DEAD honestly (GC-12: exactly one banked
q-series exists — B672's doublet — and it fails the kind-map's non-negativity at its first
nontrivial coefficient; everything else is not a q-series); the bridge requires a genuine
boundary character no banked artifact supplies. D3 CLOSES positively (GC-14: both rank-4
centralizers retain ALL-complex constituents under B1100's correct criterion). The Selector's
proved piece is a formal theorem with a proved finite-case correspondence to B1183; F2/F8 locks
were already banked (B1003 — finished-but-forgotten again), F3's lock drafted and green (GC-15).

## §9 — THE FLOOR AMENDMENT (B1193; cloud's priority-one catch, adopted)

Cloud's census addendum 5 caught an internal tension in this document: §5's floor ("no continuous
dimensionless input remains except σ") vs our own B1185 (the down-readout's **ℙ³ Higgs line —
purely continuous, C12-trivial on B₀**). Their adjudication stands: the ℙ³ line is NOT covered by
B1025-I5's multiplicity-one argument (it lives in a 4-dimensional block the argument never
reaches). **The floor is amended**: the input slots gain a row — *the down-readout's ℙ³ line: up
to three continuous parameters, PARTIAL-CONTESTED* — with the two-outcome adjudication cell named
(either the ℙ³ reduces, restoring the old floor, or the floor carries it permanently; both
bankable). The end-state list reads, honestly: **ℓ + the relational c-bit + finite labels + σ (one
bridge) + the λ-placement + the ℙ³ adjudication.**

## §10 — The five travelers from cloud's lane (harvest-queued, verify-don't-trust)

(1) the mirror-even hierarchy carrier (their memo 92 — load-bearing for the value arc); (2) the
Klein split (memo 93); (3) the Stage-2 closure (their sealed exhaustive comparison, owner-unsealed
— the coupling rows carry it once verified); (4) the degree ledger (memo 96 — the meter's third
telling); (5) the clock-coherence condition — the λ/r/γ₅ time-triangle's first refutation-capable
test. Plus the merge-born cell: **is cloud's branch ℤ/2 our r-label?** (both external, both ≠ c;
computable; their THE_TWO_CAMPAIGNS.md §V names it).

