# The error ledger (GOVERNANCE §13/§15; instituted 2026-07-16)

*One entry per ERROR CLASS, not per incident. Reviews check the window's
disclosed errors against this taxonomy: a recurrence strengthens the class's
standing rule; a new class gets a new entry. The question is always "what
standing rule would have caught this," never "who slipped." Incidents are
cited by arc/PR — the full record stays in the arcs (append-only).*

| id | class | description | standing rule that catches it | known instances |
|---|---|---|---|---|
| E1 | **Undeclared choice drift** | a basis / sign / normalization / stage choice made implicitly, then read as forced; results silently depend on it | the conventions block (GOVERNANCE §13); K020 discipline: separate forced from chosen | cc2's withdrawn h¹=3 (assumed λ-sign, 2026-07-16); B637 stage-1 prefix convention (p_i vs p_{i−1}); B601-vs-B238 convention reconciliations |
| E2 | **Sealed-criterion vacuity / transcription** | a sealed gate that cannot pass for any genuine object, or a reference table transcribed wrong at sealing | MB12 covers operations AND criteria; vacuity-check both directions before sealing (WORKING_RULES §8) | B644 M3 reference tables (violated χ(−g) = −χ(g)); the L85 V1′/V3′ sealing errata; **cc's OWN instance (C23 lock, 2026-07-24, self-caught on the owner's challenge): cc's first C23 lock asserted "distinct signatures ⟹ only the identity permutation preserves them" — ENTAILED by distinctness, cannot fail — an L1/MB12 vacuity written in the same session cc made L1 binding on the Wave-6 cells. Replaced with falsifiable computed facts (ρ_geo non-real; c swaps the pair; V₄ abelian).** **cc's OWN instance #2 (B1029 lock, 2026-08-11, caught by the test-vacuity gate pre-push): `assert "value-kernel"` — a bare string literal, always truthy; the `in`-clause had been dropped. The gate built for this class fired before the push.** **The frame-not-instance refinement (B1030, found in the audit seat's price_lock item 1): a vacuity control that PERTURBS A SUBSTITUTE (a different ledger) rather than the instance under test exercises the FRAME, not the lock — the in-place assertion `all(w % 1 == 0)` cannot fail at length-weight 1 regardless. A control must be able to fail THE INSTANCE, not merely demonstrate the check-shape can fail somewhere.** **OI-146 PART 1a (B772 audit, 2026-07-23): vol_m004/vol_orbifold = 12 by construction — an unfalsifiable tautology + a reused known-buggy nsum; the index=10 headline is sound via independent orbit-stabilizer, only the self-certificate is vacuous; fix = Hurwitz-zeta + real SnapPy volume per B734 fix2** |
| E3 | **Stale-checkout false negative** | "no prior work exists" asserted from a thorough search of an out-of-date clone | sync-before-computing (WORKING_RULES §1) | the Door-2 packet (2026-07-16: B605 existed three merges past their sync) |
| E4 | **Necessary-read-as-sufficient** | a necessary/cited/proxied fact treated as the discriminating computation | compute the discriminating fact in-sandbox (the B525 audit rule) | 3/10 banked negatives cracked by the B525 audit (B519 retracted); the Door-2 packet's tetrahedron argument (swap gap); the C2 seal's 'unpublished by causality' premise (the JUNO paper predated it 8 months — the design seat trusted a banked timeline note instead of checking the literature; caught at adjudication, 2026-07-17); counter-rule ADOPTED 2026-07-17: GOVERNANCE §16 (the factual-review lane, owner-approved L99); **cc's OWN instance #2 (C23 statement, 2026-07-24, self-caught): cc wrote that the surviving transposition is "refuted independently" while stating the shared stabilization premise in the same sentence — the arc's own verifier had already flagged that I2 and the fixed-point argument are NOT independent. This is the SAME independence-collapse pattern cc had just carried P2W5-GATEB for. Corrected: the universal spine is I1 + the fixed-point argument; the object-native enumeration establishes class membership, not a third reason.**; **cc's OWN instance (B775 W4 Z1, 2026-07-24): cc wrote the Z-ladder's irrationality holds "exactly when 5|κ" (an IFF) when only irrational⟹5|κ is forced — the converse fails at κ=15,20,25. The cell's output correctly said "only at"; cc introduced the iff in the write-up AND cc's own self-verification reproduced the error instead of catching it (the standard cyclotomic fact gives necessity, not sufficiency). Caught by the agent verifier; corrected same-day**; **OI-055 (B772 audit, 2026-07-23): the all-n 3-integrality premise closed by citing GSWZ arXiv:2412.04241 by TITLE, no pinned theorem for this series' Habiro-membership — census label downgraded EXTERNAL → SEARCH-BOUNDED(depth 100) pending the pin** — **UPGRADED 2026-07-24 (B776): Φ(h)Φ(−h) reconstructed FROM FIRST PRINCIPLES in-cell, 3^146@100 anchor reproduced, pure-3 through order 105; the premise is no longer cited-by-title but reconstructed-in-cell to depth 105 (still unproved all-orders = the well-posed GSWZ question)** |
| E4a | *(E4 instance, 2026-07-28)* **Asymptotic average used as a pointwise predictor** — cc predicted r = 8.863405 is the parent's k = 2 Maass eigenvalue because its Weyl position W·r³ = 1.989 sat 0.18 % from the k = 2 slot. **REFUTED** by cc3's projective-line sector test (dev_min = 1.080, no parent component; the sole parent is r = 7.072004 at 3.53e-10). The Weyl law is asymptotic and at k = 2 the positional fluctuation is O(1), so the agreement carried no resolving power. Aggravating: cc had argued *the same day* that a 0.344 % Weyl agreement was too weak to verify the G–H value, then used a 0.18 % one to predict. Standing rule (E4): **Weyl/asymptotic proximity is necessary-ish, never sufficient — the discriminating test must be computed.** |
| E5 | **Precision boundary** | low-precision constants fed into high-precision arithmetic; garbage that looks like structure | rebuild at target precision from exact sources (B629's 80-dps builder is the pattern) | B640 first run (float64 b238 matrices at 60 dps → 2044 pseudo-elements) |
| E6 | **Transcript-grep lock** | a test asserting an output string rather than the mathematical fact | locks assert mathematics (WORKING_RULES §7); transcript asserts marked as fallback | the pre-audit B632 locks (replaced by the 162/162 mathematical verifier) |
| E7 | **Overwritten failure record** | a failed run's output overwritten by the rerun | preserve failed outputs byte-faithfully BEFORE rerunning; hash corrected code before the rerun | B632 cell-2 failed runs (restored from session task files, 2026-07-15) |
| E8 | **Path assumption** | writing to an assumed path (docs/ vs root; cwd drift after cd) creating strays or missing the canonical file | verify the canonical path exists before appending (`git ls-files`); prefer absolute paths | the stray docs/PROGRESS_LOG.md (caught pre-commit, 2026-07-16) |
| E9 | **Overstated closure** | "proved/forced" printed for what the solve actually left open (residual dimensions, one-way implications) | state the solution-space dimension with the claim; direction-2 solves before "forced" | B638 closure first draft ("PROVED from swap data" → 10-dim residual, corrected in place) |
| E10 | **External-verification pretense** | phrasing that reads as third-party checking of internal work | PROVENANCE.md §0 grounding + the review spot-sweep (REVIEW_TEMPLATE §5) | "the external audit" phrasings (grounded + rephrased at Review 20) |
| E11 | **Overextended record** | writing a full value (hash, constant, digits) where only a prefix/summary was actually verified — the record asserts precision that was never computed | never transcribe beyond what was computed THIS session; recompute the full value before recording it | a 64-hex hash written from an 8-hex verified prefix at Review 20 (caught pre-commit, corrected from the recomputed digest) |
| E12 | **Global-state leakage between locks** | a test mutates process-global state (mpmath `mp.mp.dps`, module caches, cwd) that later tests silently inherit; failures are order-dependent and vanish in isolation | per-test fixtures set what they need (the b204 pattern); `tests/conftest.py` autouse restore around every test; never set precision at module level | the dps leak starving `test_e62_hearing_matrix_gates` in-suite (found + repaired at Review 20); b204's own historical note (dps=25 leak starving the pslq locks); `test_b353` in-suite failure LOCALIZED + REPAIRED (B666 cell 5, 2026-07-17): the mechanism is COLLECTION-TIME IMPORT — pytest imports every module before running, so the LAST module-level `mp.mp.dps` in sorted order is what all runtime tests see (the culprit was test_cc2_r5_adopted.py:282, dps 40; b353 needs 100); the earlier note's "collects after ⇒ exonerated" inference was FALSE and is withdrawn; the conftest-restore pattern cannot fix this class (it restores the leaked entry value) — the counter-rule is the per-test fixture (the b204 pattern); priced follow-up: the 12-file module-level-dps sweep — **LANDED (B676, 2026-07-18): 18 import-wraps, per-file pinned fixtures, conftest collection-finish guard (restores entry precision after collection so future module-level assignments cannot leak); masked leakers b246/b250 found + repaired; suite green 2428/0** |
| E13 | **Stale artifact text** | prints/comments describing a superseded method or diagnosis get baked into a sealed artifact when the code is revised in-trail; the mathematics is right, the record misdescribes it | pre-seal grep of prints/comments against the method actually executed (the cc2-receipt practice); in-trail revisions update their own narration | B649 stage-1 output's "pslq" header (method was direct relations); stage-2a's "projective" comment block (det was 1) — both found by the cc2 receipts, 2026-07-16 |
| E14 | **Noise-band artifact banked as a value** | a floating-point observable computed on an exactly degenerate spectrum (or similar ill-conditioned structure) gets banked as if well-defined; different noise realizations of the SAME exact object give different "values" | before banking any spectral/statistical scalar: state the convention exactly and verify the value is stable across ≥2 noise realizations / solver scales (the exploratory-numerics rule, applied to derived statistics) | B480's "⟨r⟩ = 0.16" (corrected 2026-07-17, B666 cell 6: the exact spectrum is a degenerate picket fence; exact conventions give 0 or 1; the banked number sat inside the demonstrated noise band 0.054–0.21) |


| E15 | **Exponent-as-base misread (GSWZ eq 2)** | B682 read the symmetrised-series denominators 1/3³, 4/3⁵ as "{3,5} = being × hearing" — but 3³, 3⁵ are POWERS OF 3; the series is integral away from 3 (3¹⁴⁶ at n=100), PURE BEING, NO 5 | re-read the source at exponents-vs-bases; corrected B685; the "{3,5} real signal" withdrawn (it drove nothing false — the design-cell kill does not depend on it) | caught establishing the figure-eight's genuine Nahm datum (B685) |

| E16 | **Chirality over-reading (my E-2a/S067 read)** | I read the opposite Atkin-Lehner signs w₃=+1, w₅=−1 as an emergent bifocal "chirality" / c-breaking at level 15 | they are FORCED by E₁₅'s EVEN RANK (root number ε=w_∞ w₃ w₅=(−1)^rank; opposite signs ⟺ even rank), a GENERIC elliptic-curve fact (verified 14a/26a/57a) — zero 3-vs-5 content | cc2 caught it in E-3b; verified this seat; S067 row-3 falsified; owned (B695) |

| E17 | **Swap/weld conflation (B702)** — the "metallic hearing ⇔ real-quadratic SWAP field" law conflated the being-face SWAP (golden ℚ(√−3), silver ℚ(i) — both IMAGINARY, rational tones) with the hearing-face WELD (order-10, ℚ(√5), where φ lives). The golden swap does NOT hear φ. cc2's E-Q FINDINGS introduced the mis-framing; cc verified only the rigorous core (silver rational tones), not the framing, and banked the over-reading LAW. Caught by cc2's self-recheck vs banked e1; corrected 2026-07-19: the swap asymmetry is TORSION-vs-NON-TORSION (being); hearing is the AUDIBILITY law (character field ℚ(√p*) real ⟺ p≡1 mod 4, WELD). Standing rule: a "hearing/tone" claim must name WHICH object (swap=being vs weld=hearing) and its field's real/imaginary type BEFORE the law is stated. | 2026-07-19 (B702 corrected) |

| E18 | **Workflow-artifact provenance under degradation** — when a multiagent campaign is degraded (B719: 4 API stream-timeouts), a compute agent's saved `_out.txt` can contain verdict/section text NOT generated by its committed script (a skeptic caught b719_probe2_out.txt had ~35 lines with no generating code), and a key probe (the h=3 control) can silently fail to run at all. The numbers were correct but the sealed artifact did not match its own script. Standing rule: on ANY workflow-degradation signal (errored agents, timeout failures), the banking seat RE-RUNS the load-bearing computation clean in-seat before sealing, and does not bank an auto-generated artifact that an independent auditor cannot reproduce from the committed script; a probe that did not run is carried OPEN, never inferred. Caught + repaired in-window (covers re-run clean; control re-run as a single focused agent). | 2026-07-20 (B719) |

| E19 | **Adjudication by cited negative (the courier-seat SEEING STRATEGY, first pass)** — RECURRENCE of the B525-audit "cited/necessary read as sufficient" class. In the first verification of the courier seat's five correspondences, cc DISMISSED three (C1 chirality, C2 signature, C4 hierarchy) by CITING banked negatives (B713/B716/B615) as if citation were refutation, without computing the discriminating fact. On computation (owner-prompted): C1 is SOUND (= B713 itself, not refuted); C2 is OPEN (h¹=3→3-spatial is unbuilt, B716 is about the geometry not the cohomology); C4's "no scale" was a CATEGORY ERROR (the hierarchy is a dimensionless RATIO, which the object DOES give — and τ₁₁/τ₈≈10^16.8 is a real near-hit to 10^17, ~3% base rate). Only C3/C5 were genuine (and C3 was computed). Standing rule reaffirmed: a negative asserted in ANY adjudication (even of an incoming claim) must COMPUTE its discriminating fact in-sandbox; "B-number X refutes this" is a hypothesis to check, not a refutation. Corrected in B724. | 2026-07-20 (courier-seat SEEING re-audit) |

| E20 | **Structure-skepticism lagged number-skepticism (B727 self-audit)** — the program applied rigorous base-rate/look-elsewhere discipline to NUMBER-matching in B724 (torsion near-hits are expected, not evidence) but NEVER ran the same test on its FLAGSHIP STRUCTURAL claim ("E₆ recurs across McKay / Lie / CIZ") until B727 (~2 years later). When finally run, the structure came back GENERIC: the three faces are ONE canonically-linked ADE classification (P(recurrence \| one label) = 1, a graph identity), E₆ is the only exceptional label an imaginary-quadratic field can reach (no birthday problem), the sister m003 shares ℚ(√−3) without being the knot, and non-arithmetic knots also surject onto 2T. The apparent richness was one field-fact refracted. A recurrence of SHORT-CATALOGUE labels (ADE/exceptional) across CANONICALLY-LINKED classifications is NOT independent evidence — it is forced by the classification theorems. Standing rule: before treating a structural recurrence as object-specific, ask (a) are the "faces" canonically linked (McKay↔Lie↔CIZ↔du-Val are ONE ADE list)? (b) how short is the catalogue / is there a birthday problem? (c) does a comparable object (sister, same field) share it? Compute the base rate; only content a comparable object does NOT share is object-specific. Cross-verified with cc2's odds-measurement (both GENERIC). | 2026-07-20 (B727) |

| E21 | **SL-level over-read before the PSL/center bookkeeping (B731)** — a research agent AND cc's first pass both concluded "m004 is congruence of level (2)²=(4)" from the SL image order 320 = |SL(2,O₃/4)|/12 = 3840/12, reading "index 12" directly. This SKIPPED the SL→PSL center: the center of SL(2,O₃/4) has order 4 ({±1, ±(1+2ω)}), the image contains only {±I}, so the PSL image is 320/2=160 and the PSL-index is 960/160 = **6, not 12**. The rigorous test (which alone decides Γ⊇Γ(I)) gave index 6 at every 2-power level ⇒ m004 is NON-congruence — the OPPOSITE of the SL-only conclusion. cc2's fabricated-citation guard had independently flagged "m004 at level (4)" as an unverified circulating claim. Standing rule: a congruence/index conclusion over PSL(2,·) must quotient by the FULL center of SL(2,O/I) (which is larger than {±1} when I is not prime), not read the SL image index; verify Γ⊇Γ(I) by [PSL-image index]=[geometric index], recomputed with the center. The plausible-computed-result over-read caught by recomputing the discriminating fact (E19 family). | 2026-07-20 (B731) |

| E22 | **Premature non-congruence from a shallow level-check (B731→B734)** — B731 concluded m004 is NON-congruence by computing the PSL image-index at levels 2,3,4,5,6 (6 at 2-powers, 1 at odd primes) and inferring the 2-adic index had STABILIZED at 6. It had not: the index jumps to 12 (the geometric index) at level (2)³=(8), so m004 IS congruence at level (8) (cc2 computed, cc verified — B734). A subgroup's congruence level can be a HIGH prime power; the image-index can PLATEAU (here at 6 through level 4) before jumping. Standing rule: never infer NON-congruence from a finite plateau — the index-vs-level function must be pushed until it either reaches the geometric index (congruence, at that level) or a proof of non-congruence is given (e.g. an explicit non-congruence quotient / a bound theorem); an apparent plateau at two consecutive powers is not stabilization. This is the SECOND correction in the same thread (E21 = SL/PSL center over-read gave the wrong level (4); E22 = shallow check gave the wrong conclusion). Both caught by the two-seat verify-don't-trust: the agent said level (4), cc's B731 said non-congruence, cc2's deeper computation + cc's verification converged on the TRUTH — congruence, level (8). | 2026-07-20 (B734) |

| E23 | **Level-convention ambiguity (adopted 2026-07-22, R27-5; cc3's proposal, cc's formalization)** — a "congruence level" statement silently depends on WHICH filtration it names: the standard SL-kernel level (Γ ⊇ ker(SL(2,O)→SL(2,O/I)), where m004 sits at level (4), index 3840/320 = 12) versus the mod-center/PSL filtration level (where the same group first realizes its geometric index at (8)). B731→B734's thread produced BOTH true statements, and cross-seat relays briefly treated them as contradicting. **STRENGTHENED 2026-07-29 (third instance, the review seat's catch): naming "SL vs PSL" is INSUFFICIENT — there are THREE groups here, not two.** For R = ℤ[ω]/4: **SL(2,R)** (3840), **SL(2,R)/{±I}** (1920), and the **true PSL(2,R) = SL/Z** (960, since |Z| = 4 — λ²=1 has four solutions 1, 1+2ω, 3, 3+2ω). cc and cc3 both wrote "PSL(2,ℤ[ω]/4)" for SL/{±I}; B731 computed in the true PSL and got index 6 = 960/160, while cc/cc3 got 12 = 1920/160. **All three are correct in their own group**; the one PSL(2,O₃) maps to is SL/{±I}, because O₃ is a domain whose only square roots of 1 are ±1. Standing rule (amended): a congruence-level or index claim names **the exact quotient group and its order**, never just "SL" or "PSL". Standing rule: every congruence-level claim names its convention explicitly (SL-kernel vs PSL/mod-center), and cross-seat comparisons convert to ONE convention before adjudicating; a "level" without a named filtration is an incomplete claim. | 2026-07-21 (B734 refinement; adopted R27-5) |

| E24 | **Verdict-by-unauthenticated-signal (adopted 2026-07-22, R27-5; cc3's proposal verbatim)** — a review/verify outcome accepted from a channel without provenance (no preserved reviewer output + completion metadata), instead of from the reviewer's own preserved report. Instance: the forged SEAL-READY signal during B742 (quarantined; forensic trail preserved in the arc's reviews/). Standing rule: verdicts are read ONLY from the preserved review artifact; any completion signal lacking metadata is quarantined as untrusted; execution never starts inside a review window (GOVERNANCE §16.1 timing explicit). | 2026-07-21 (B742 incident; adopted R27-5) |

| E25 | **Integer-relation false witness — the unbounded-coefficient trap (adopted 2026-07-22, R27-5; cc2's own-goal, minted as proposed at the B740 banking)** — numeric lindep/PSLQ "witnesses" with enormous coefficients (~65-digit) achieving residuals below a naive threshold (~1e-657 < 1e-451) at rank just under the field degree: 20 false "CONTAINS" verdicts inside cc2's own door-1 tool, caught by the exact factornf cross-check. Standing rule: an integer-relation witness is NEVER a containment/identity proof by residual alone — thresholds must be coefficient-size-aware (scale the bound by the coefficient height) AND every verdict-bearing relation gets an exact verification (factornf / symbolic containment / exact arithmetic); tool-level look-elsewhere is budgeted like any other. | 2026-07-21 (cc2 door-1 errata; adopted R27-5) |
| E26 | **Blind-projection negative** | a "no structure / no law" verdict computed in a cyclic-trace / abelianized / character projection that is invariant under the ordering-chord (θ-odd) sector where the object's live structure is claimed to sit — trace-level absence read as structural absence | B766 θ-T6 lesson (the chord is invisible to tr()); compute a chord-level / non-abelian analog before banking a negative; the B772 adequacy audit | 4/14 banked negatives flagged TRACE-BLIND-RISK (W3-067, W3-082, W4-115, W4-304; B772, 2026-07-23) — re-computation launched as B773 |
| E27 | **Disconnected verdict** | the flag that decides the outcome is not wired to any computed quantity — literal booleans, hardcoded truth values, a silent except-fallback supplying exactly the needed values, or a classify() that maps its inputs by definition. The arithmetic can be perfect while the verdict is decided by a constant | every verdict flag must trace to a computed quantity; a sealed two-outcome branch must be REACHABLE from the computation (not just present in the code) | B784 audit (2026-07-24): B780's literal signature tuples; B782/C22's regular-action construction making RESOLVED-B unreachable; CL-LATIN's hardcoded T3/T4 booleans; B781's silent SnapPy except-fallback. **4 vacuous locks and 9 documents carrying a false statement traced to this one mode**; **B919 NEAR-MISS (2026-08-05/06, self-caught pre-bank; instanced 2026-08-06, B920): a generated two-prime script's SILENT substitution failure ran one prime twice while the `two_prime` flag sat true in results.json — the flag was not wired to which primes actually ran; exposed by direct inspection of the generated script, retracted in place within minutes (frontier/B919_weinberg_traces/FINDINGS.md "two self-catches")** |

**E27 mechanical sweep (2026-07-25).** After the B784 audit, every session lock was swept for the class two ways. (1) Data-connected locks (20 aggregate-json readers): a mutation harness flips each json's verdict/value field, restores from `.bak`, and checks the suite goes red. Result: **20/20 wired** — 18 caught the generic mutation directly; the 2 initial "uncaught" (B770 census, B778 partial) were harness false-positives — census has no verdict field (asserts `raw_count`/`counts`, caught when those are mutated) and B778's partial-lock is a shape/id-set lock (caught when a cell id is mutated), with verdicts locked separately from the cell-level jsons. (2) Data-free locks (4 inline-assertion files the harness structurally can't mutate): manual tautology check — test_v4_genericity (snappy computations), test_b758_chain (ledger regex-parse), test_b779_convergence (real V4 group action) all falsifiable; test_b780_gate line 20 (`tr(AB)=tr(BA)`, a universal identity → impossible counterfactual) was the one L1-vacuous assertion, demoted to its explanatory comment (the falsifiable c-side on line 22 retained). **Net: 0 genuine E27 remaining in the session's lock layer.**

| E28 | **Silent-discard filter** | a tolerance/membership filter that drops data **without reporting what it dropped**. Because the dropped items are systematically the hard cases (long geodesics, large norms, poorly-conditioned rows), the filter selects for the author's expectation and manufactures self-vindication | **a filter that discards data must report its discards** — count them, report the worst discarded case, and treat a large discard fraction as a failed run, not a clean one | **cc's own instance (B794, 2026-07-28)**: cc's ℤ[ω]-membership filter (tol 1e-7) silently dropped long geodesics while re-checking cc's OWN hint H-B788-NORMSPLIT; it returned 12 m004-only norms against cc3's 41, "upholding" the claim. The dropped geodesics carried exactly the disconfirming odd norms (7,103,127,175,367). cc3's proved mod-4 theorem settled it against cc. Class named by the review seat in relay; **it caught cc within the hour of being proposed**. **INSTANCE FIGURES CORRECTED 2026-08-06 (B920, the cc3 level reconciliation rerun in-sandbox): cc's 12/zero-odd was in fact the CORRECT NORM-level exclusive set ({4,16,48,64,112,144,192,208,256,304,336,400}, all ≡0 mod 4) and cc3's 41-with-five-odd conflated trace and norm levels — the reconciled TRACE-level figures are 139 traces / 37 distinct norms / one odd (7); 103/127/175/367 are norm-level SHARED. The filter-hygiene class stands as minted (the filter DID discard without reporting — the defect path was real), but the clause "the dropped geodesics carried exactly the disconfirming odd norms" was wrong, and the hint survives at the norm level (HINT_LEDGER row (7); B794/FINDINGS level-reconciliation; E33 instance (a) had already recorded the over-correction).** |
| E29 | **Post-hoc analysis-model selection** | multiple defensible analysis models (nulls, surrogates, tolerances, windows) exist; the one reported is chosen **after seeing outcomes**, and typically the most permissive. The individual computation is correct; the verdict is chosen | the pre-registered model is the primary and is named in the prereg; any additional model is reported **alongside** it, never in place of it, with both numbers shown | **cc's own instance (B790, 2026-07-28)**: prereg §2 named the density-matched null as primary; cc reported "ordinary noise" off a **uniform** null that was never pre-registered. Compounding: cc's "Weyl-matched" null was itself miscoded (e^ℓ instead of e^{2ℓ}), so **neither** reported model was the committed one. Caught by the review seat. Corrected verdict (pool-matched, two models agreeing) still MISS, but earned |
| E30 | **Output-verified, derivation-unverified** | a result's *numbers* are independently reproduced and the result is then called verified — while the *argument* that produced them is never checked. Every table row can be right and the premise still wrong | verification names which layer it reached: numbers, derivation, or both. Reproducing a table is not checking its derivation, and a receipt must say which it did | **cc's own instance (B791, 2026-07-28)**: cc re-derived all eight rows of the review seat's Weyl budget from the L-value and confirmed them exactly — while the derivation behind them divided out a multiplicity that is not there. The arithmetic was right; N_i(T) = dim(V_i)·W(T), not W(T). Consequence: the review seat's headline "Gate 9 cannot discharge Gate 5" evaporated once the factor was restored |
| E31 | **Instrument-precondition unchecked** | a measuring instrument returns a confident, precise number while a *validity condition* of the instrument is unmet — matrix rank, truncation margin, grid resolution, convergence radius. The number is not wrong so much as meaningless | before trusting an instrument, assert its preconditions **in code**: overdetermination (rows ≫ cols), truncation margin vs the scale being probed, and a **displaced negative control** that must FAIL | **cc's own instances ×2 (B795, 2026-07-28)**: (a) collocation run at 90 points × 112 modes — nullity ≥ 22 by construction, σ_min ≈ 1e-48 for every r *including controls*; would have "confirmed" any input. (b) Bessel truncation margin 1.4× at r = 8.86 (needs ≳ 2×); the top two eigenvalues read as ABSENT. **Stopping one run earlier would have published a false negative aimed at another seat's correct result.** Both caught by the displacement controls, not by care |
| E32 | **Unfalsifiable premise (local rigour, global immunity)** | every *cell* carries a pre-registered falsifier and the *campaign premise* carries none. Each cell can fail correctly while the premise survives untouched ("we haven't found the right mechanism yet"). A programme can run to completion, produce nothing, and leave its premise as "banked" as on day one | a campaign states a **campaign-level** falsifier distinguishing its premise from the null-of-no-connection; if none can be written, the campaign is labelled **exploratory-interpretive** and nothing from it banks as *evidence for* the premise — only as mechanism-exclusions | **B796 coupling campaign (2026-07-28, gated pre-launch)**: premise written as "H0 (banked, now the campaign's **positive target**): values live in the observer–object coupling", justified by the refutation of values-in-the-object. **Refuting the rival does not establish the premise** — every banked null (character variety, rung-1 PSLQ, forced limits, B792 spectral) is predicted *identically* by H2 = "the object has nothing to do with the SM", which the plan never states. Per-cell falsifiers were added and do **not** fix this |

**Session note (2026-07-28).** E28–E32 all arose in one session, all from cc, and all share one shape: **a confident, precise output whose preconditions were unchecked** — the filter's discards, the null's provenance, the derivation behind the table, the instrument's rank and margin, the premise's falsifiability. None was a hedge; every one would have been read as a clean result. Of seven instances, **cc self-caught two**; the rest were caught by cc3, by the review seat, or by a control built for another purpose. The operative lesson is not "be careful" — it is that **controls built for one purpose catch errors of another**, which is the argument for keeping them when you are sure they are unnecessary.
| E33 | **Over-correction (adopted 2026-07-29; cc's own, wording sharpened by the review seat)** — **a specific verified fact set aside because a recently-learned error class made its conclusion feel suspect.** The failure is not insufficient scrutiny; it is *misdirected* scrutiny. the review seat's discriminator: **scrutiny must be aimed at a NAMED DEFECT PATH; suspicion that cannot name one is not a reason to discard a computation.** Checking a result harder is right when you can name the route by which it might be wrong (cc had written a filter with a known silent-drop path — checking that harder was correct); it is wrong when it becomes free-floating. | before discarding your own computation, name the defect path you suspect. If you cannot name one, the computation stands. A refutation gets the same verification as a claim | **cc, 2026-07-28/29 ×2.** (a) cc recomputed the trace-norm split, got the right answer (12 m004-only norms, none odd), and **discarded it** in deference to cc3's refutation — having *already raised the disproof* (norm 7 is in m003, so it cannot be m004-exclusive) and set it aside. Retracted a correct hint, H-B788-NORMSPLIT. (b) cc flagged the congruence index 12-vs-6 as an "open discrepancy contradicting two banked arcs" and handed it to cc3; **there was no discrepancy** — E23 had dispositioned it, and Z ∩ H = {±I} makes both correct. cc announced a conflict instead of computing Z ∩ H |
| E34 | **Apparatus-inflation (adopted 2026-07-29; review seat's proposal, cc's verification and two-layer merge)** — **a property of the INSTRUMENT or of a LARGER STRUCTURE, read off the results as if it were a property of the object.** The derivation is valid; the attribution is not. Two layers, recorded as one class because the failure is identical and only the layer differs. **(a) SCOPE-inflation** — a conclusion derived *through* an invariant of a larger structure (a commensurability class, a field, a category) attributed to the specific object. *Diagnostic: does the nearest sibling share it?* If yes it belongs to the shared structure. Verified instance: every step from the invariant trace field down to E₆/McKay, the three 27's, the cascade and the V₄ torsor is a **commensurability invariant** (Reid), and m003 shares m004's invariant trace field `x²−x+1 = ℚ(√−3)` at identical volume 2.0298832128 (snappy, B803) — so the sister ties **by construction**. Only homology (`ℤ/5+ℤ` vs `ℤ`), knottedness, hence **amphichirality**, congruence data and the **spectra** are manifold-level. **(b) METHOD-inflation** — the *method's* own recurrence read as the object's unity. The Recurrence Atlas already refuses its own version honestly: it reports that the trace map recurs in 45 % of probes because it is *our tool*, and that only `κ` is a forced first integral. | before attributing a conclusion to the object, name the largest structure the derivation passes through and ask whether the conclusion is already a property of *that*. For instruments, ask whether the pattern is the tool's footprint | **B727** found the E₆ genericity empirically; B803 supplied the structural reason it was forced. **Not gated, and the reason is recorded:** a regex cannot separate *"X is object-specific"* from *"the ONLY object-specific content is Y"* — the single LAW_MAP hit is the latter, i.e. correct text, so a hard-fail gate would fire on a right answer and train readers to ignore it. MANUAL in `docs/PRACTICES.md` | 2026-07-29 (B803) |
| E35 | **Oblique-readout violation (projection readout on a non-orthonormal basis)** — a decomposition readout taken by projection (transpose-projections, Rayleigh quotients) on eigenvectors that are complex/non-orthogonal or on non-normal matrices; the projections do not discriminate, and the readout confirms whatever was expected. The standing rule was minted in-arc at B881 §3 ("earned three times") but never got its ledger class — this row is the 2026-08-06 catch-up (B920), added at the FOURTH violation | **every decomposition readout must be oblique — solve in the full eigenbasis, never a projection — unless the basis is provably orthonormal**; NEVER Rayleigh-quotient eigenvalue reads on non-normal matrices (solve componentwise) | B875 (nearly-parallel sectors — the first); B876 (ill-posed per-sector grading — the second); B881 (transpose-projections on the non-Hermitian K₁ restriction reported every cell hitting all nine targets — the third; the rule minted); **B907 (2026-08-05, the FOURTH: the first pattern-feasibility census used Rayleigh quotients on non-normal matrices; caught because its output contradicted concrete exact automorphisms; corrected route reads the verdict from the exact involutions)** |
| E37 | **Harvested-explanation-unverified (banking an adjacent EXPLANATION from a source while verifying only its headline result)** — when integrating another seat's arc, the load-bearing *result* gets computed/checked but a *mechanism or explanation* travelling with it is carried as "CITED-standard" and propagated, unverified. Its wrongness then rides into every surface the result reaches. | when harvesting, verify not only the headline but every load-bearing EXPLANATION you will PROPAGATE; if you carry a mechanism you did not compute, mark it CITED/UNVERIFIED in place — never state it as a theorem. Compute the discriminating fact of the explanation, not just of the result. | **cc, B1123 (2026-08-21)**: harvested cc3's forcedness census (verified — ran the checker, 39/43) AND its §2.1 "doorway" (level 15 = 3·5 ⟹ SL(2,ℤ/15) ≅ SL(2,3)×SL(2,5) = 2T×2I by CRT ⟹ McKay ⟹ E₆/E₈) which I did NOT verify — took as CITED-standard group theory — and threaded into ~8 surfaces incl. README and a live mission-status. **cc3 self-caught it** (1a0b5a90: level 15 is IRREDUCIBLE not factored — B695/E-3, the faces interfere; the group is not 2T×2I via CRT; Part III makes no such chain — "a true statement about the wrong object"). The census was independent and STANDS; only the doorway explanation was withdrawn. The discriminating facts I skipped: is level 15 reducible? is the group 2T×2I (2880) or 2I×ℤ/3 (360)? Both were checkable against LAW_MAP/B695/B1011 and I did not check. Corrected in place across all surfaces, this ledger, and memory. | 2026-08-21 (B1123 corr.) |
| E36 | **Artifact-clobber (an exec'd frame overwrites the host arc's artifacts)** — a script exec'd from another arc (the B854 frame is the recurring case) writes its outputs (`results.json`) relative to cwd/`__file__`; run from the host arc's directory it silently clobbers the host's banked artifacts. Instanced 2026-08-06 (B920) from two B907/B910-era defects | **exec a foreign frame only from a scratch cwd, with `__file__` redirected into the scratch dir, in an isolated namespace; before commit, verify each banked artifact's provenance (does results.json contain THIS arc's verdict?)** | **B907**: the BANKED `results.json` contains the B854 frame's verdict, not the selector sweep — the frame's `__file__`-inherited write clobbered it pre-commit (nothing load-bearing lost: the sweep lives in `sweep_results.json`, the lock reads that; recorded in-arc as a provenance caution); **B910**: `kappa_class.py` defends against the same hazard by design (scratch-dir exec, final dump overwrites) — the class's fix demonstrated |

| E39 *(re-keyed 2026-08-13; mis-keyed E22 at Review 43 — the number was taken since B734's congruence-plateau class of 2026-07-20; Review-43-era texts citing "E22, verdict-before-certificate" resolve HERE. The collision itself is an E1-species instance ON THE ERROR REGISTRY, found at the digest's lane-2 cross-read)* | **Verdict-before-certificate** — a bank committed (and in the instance PUSHED) while the suite was red or unfinished: B1026/B1027 were committed with the anti-file-drawer/routing locks red because the commit command gated on gates alone, not on the suite's exit code. The routing lock's red was CORRECT (an unrouted NEGATIVE); the content was right but the certificate order was violated. Standing rule (stated precisely at Review 43, same day): **the PUSH gates on the suite's exit code for the exact committed tree** (`&&`-linked in one chain), never on gates alone; a red suite means route/fix in a follow-up commit before any push. Local commit ORDER is free — the review bank requires commit-before-suite because views-fresh counts commits — but nothing reaches a remote without the certificate for the tree being pushed. Caught same-hour by the kill-graph routing lock; the slip is recorded in the routing note itself. | 2026-08-11 (B1026/B1027 bank; repaired same-hour) **Instance #2 (2026-09-08, B1299 landing, commit 5a70e8cd):** the commit chain ran `pytest … | tail -1 && git commit && git push`, so the pipe returned tail's rc and a lock test that was RED (1 failed, 3 passed) did not stop the push. Root cause of the red: a scratchpad block extractor cut drafted blocks at the next `## ` header, and two blocks whose FIRST line was itself a `## ` header (the L204 lead, the B1267 addendum) were appended as EMPTY strings while the CHANGELOG/PROGRESS_LOG entries already said they had landed. Fixed in the follow-up commit the same hour; the rule stands as written (capture pytest's own rc, never through a pipe) and gains a corollary: a landing step that appends text must assert the text is non-empty and present afterwards. |
| E40 *(number assigned at the digest port 2026-08-13 — the consolidation branch coined it "E39", which main's same-day rekey had taken; their intended E38 pairing is honored here: E38 is a lock that FAILS when it should not, E40 is a lock that PASSES when it should not — the silent twin)* | **Cached verification** — an arc's lock asserts over a committed `results.json` cache instead of the instrument's fresh output; later work moves what the instrument measures and nothing re-runs it, so the lock validates the cache against itself BY CONSTRUCTION. Found by the consolidation seat's Review 1 (six red instruments behind a green suite at their 5b26e51; the pin be87a51 was "green over caches"). The corpus's oldest instance is MAIN's: B946's lock asserted over four keys its verify.py never produced, since B963 — repaired 2026-08-13 (the keys now compute via (6237\|p) = (77\|p), the √77 resolvent law, byte-identical to the cache). The systemic fix: `scripts/checks/instrument_freshness.py` + `tests/test_instrument_freshness.py` (non-mutating; snapshot-and-restore). Environment rider from the port audit: instruments measuring platform numerics or working-tree state certify CONTAINER-RELATIVELY — a freshness deviation is dissected before it is called a corpus error. Standing rule born of two same-day collisions: **E-numbers for branch-coined classes are assigned at the digest port, never on the branch.** | 2026-08-13 (the port; their B1041/B1049/B1054) |
| E41 *(caught by the audit seat's relay 2026-08-15, on their paper lane; verified and swept on this bench 2026-08-18)* | **Model invariant read as field invariant** — a property of a chosen integral model (here: disc μ's 5² and 13⁶ and 2³², borne by the non-maximal order ℤ[t]/μ) asserted as arithmetic of the field it presents (K, where disc K = 6237 and 5, 13, 2 are unramified). The instance: "the golden 5 enters by ramification" (B894 M6 + C28 + LAW_MAP + the masterplan's M6 line), live for 13 days. The species generalizes B1062's tower-invariance rule from typing to ramification: **field-level adjectives (ramified/split/inert) attach to the FIELD DISCRIMINANT, never to a polynomial discriminant** — compute disc K (maximality certified, e.g. Dedekind) before using a ramification word. | **the retraction sweep (row 9) + `tests/test_c28_ramification.py` (asserts 5 ∤ disc K exactly); the standing rule: any ramification claim cites disc K, not disc(poly)** | 2026-08-18 (B894/C28; corrected same-day as verified) |
| E42 *(caught by the field-masterplan's W3 cell 2026-08-18; the wrong number was live 64 days inside B92's own 2026-06-15 correction note)* | **Default-call inflation** — a library's default enumeration silently includes objects outside the mathematical definition in play, and the count inherits the tool's convention instead of the theorem's. The instance: Sage's `BinaryQF_reduced_representatives` counted the imprimitive form (2,±6,2) = 2·(1,±3,1) at disc 20, giving h = 2 where the primitive form class number is 1 — flipping B92's uniqueness statement at m = 4 (the companion IS unique; first genuine failure m = 6; the note's m = 8 listing also wrong, h = 1). The family rider that made the repair clean: every metallic order has a norm-−1 unit (λ·λ′ = −1 by the char poly), so narrow = wide throughout — no convention gap to hide in. | **class-number/orbit counts state the primitivity/properness convention AND verify by a second route (the order formula or hand reduction-cycles); B1069's three-route cross-validation is the template** | 2026-08-18 (B92 addendum; swept — no dependents consumed the wrong clause) |
| E45 schema-from-memory (registry rows) | 2026-08-20 (Review 47) | Writing a house-schema row from a remembered/summarized shape instead of reading a neighbor row first. TWO instances this window, one species: the arc_verdict field-name miss (crossing week) and the instrument-field type miss (six verdicts written as prose where the house type is boolean; suite44's gate caught it on the tracked state). The read-a-neighbor-row rule EXISTED — the recurrence is an enforcement gap. Standing fix: a schema-validator lock for arc_verdict.json (R47-2), so the schema is machine-held, not memory-held. |
| E46 tree-freeze during certification | 2026-08-20 (Review 47) | Mutating the working tree while a certifying suite runs: THREE instances, one species — the scratchpad-glob sweep into an arc dir (attribution gate blocked the push), mid-suite doc edits re-staling currency counters, and an uncommitted arc dir shifting the corpus head mid-suite. E39's exact-tree gate caught all three; nothing uncertified reached a remote. Standing fix (in PRACTICES + memory): staged-state pre-checks; land-by-name never glob; arc material stages in the scratchpad while suites run; fold-forward on collision. |
| E47 hash transcription at seal | 2026-08-20 (Review 47, protocol-integrity check) | A seal-ledger hash cell retyped rather than piped: B1071 v2's witness cell matched NO version that ever existed (the file's true digest is git-witnessed unchanged since its creating commit; custody unaffected — the computation ran against v1, whose witness verifies). Found by the review's spot-check, repaired by an append-only correction row. Standing fix: the seal ritual PIPES `shasum` output into the ledger row (command substitution), never retypes; the review's spot-check stays the backstop. |
| E48 digest corrupted by bulk remap | 2026-08-20 (re-derived on main from the audit seat's E844 at their request) | A bulk identifier rename rewrote hex INSIDE a recorded digest (their instance: `B1073`→`B8073` also matched the substring `1073` inside a 64-hex digest in an artifact-hashes file; one character of one digest, found by a full scan, exactly one corruption, restored). Distinct from E47 (retyped at write): this route strikes LONG AFTER write, via an unrelated edit, and neither route's procedural fix covers the other. Standing fixes: after any bulk rename, diff every `[0-9a-f]{7,}` token against the base (their rule); and the `seal-digests` gate as the route-agnostic read-time backstop (every recorded digest recomputed from its file; latest-row-per-path so corrections-by-append supersede while the wrong cell stays visible — a gate that forced deletion to pass would destroy the audit trail it protects). |

| E46 instance #4 (the class holds; the seat slipped) | 2026-08-21 (self-caught, minutes) | The banking seat edited two docs (the R48-F2 corrections) while suite58 was certifying the working tree — a push on green would have published a tree differing from its certificate. Self-caught within minutes; the suite was KILLED pre-push (nothing uncertified reached a remote — zero pushes, not even a blocked one) and the §CE fold-forward applied: corrections + the pending commit under ONE fresh suite. Answers the audit seat's standing §CE-completeness question ("does the law have a hole a fourth instance would find?"): the LAW held and prescribed the remedy; the hole was behavioral — a seat responding to a fresh finding while its own certificate ran. Standing addition to PRACTICES: during any certifying suite, incoming-finding responses draft in the SCRATCHPAD only, land at fold-forward. |

| E49 fake invariant form (ad-invariant vs τ-invariant), caught by classification-as-checksum | 2026-08-21 (B1118/B1119, cloud seat's eighth memo; registration completed at Review 48 — the entry was promised in B1119 FINDINGS but omitted, found by the R48 gate/integrity audit) | B1118's first real-form run produced character **−10**, impossible for any E₆ real form. Root cause: the inner product ⟨e_r,e_{−r}⟩ = +1 is τ-invariant (invariant under the symmetry being studied) but **NOT ad-invariant** under the paper's [e_r,e_{−r}] = −h_r convention (which needs −1); the fake form passed five checks because none tested ad-invariance. Caught by **the classification theorem as checksum**: a computed invariant landing outside the finite allowed-character set of E₆ real forms {+6, +2, −14, −26, −78} means the instrument, not the object, is wrong. Corrected results exact with controls green (B1119); B1114's Lorentz algebra unaffected (brackets/dimensions do not use the form). The banked contribution is the METHOD, now a **live gate**: B1125/B1127/B1134's sweeps all run the checksum (B1134: 560 characters evaluated, all inside the set — zero instrument breaks). |
| E50 the COST class — a lock never REACHED (harvested from cc3's B8139) | 2026-08-26 (B1152; cc3 paper-lane `1f455266`, integrate-don't-merge) | A lock that exists, works, and was never weakened but is **never RUN** because the suite is too expensive to reach it — invisible to every defence against *missing / weak / silent* checks, because it is none of those: it is **non-execution**. cc3's instance: their 4528-test suite takes **421 s to COLLECT** (whole runs killed by timeout), and missing-`FINDINGS` drift ran **16 arcs / 5 days** behind a lock that never got to fire. Distinct from E39 (which certifies the tree that *did* run) and E45/E48 (write-time / edit-time strikes): this strikes by **non-execution**. Main audited **CLEAN** of the drift (its suite gets run each bank) but is on the same trajectory (collection alone already >120 s). **Standing fix:** the fast lane — `scripts/affected_tests.py` (a conservative changed-file selector whose safety is a FULL fallback, so never a false green) + `scripts/run_suite.sh --changed` / `--fast` + the registered `slow` marker; and the corollary discipline **"a killed run is not a run that told you nothing"** (read partial output before discarding — as this seat read C4's finisher). Root lazy-fy of the slowest module-level importers = the named follow-up. |
| E51 the RETENTION gap — untracked relay files lost, rows survived (found by B1172's backlog triage) | 2026-08-27 (B1172; the lose-nothing sweep's escalation-audit descendant) | The nine 2026-08-09 relay FILES are gone from main's working tree and exist on **no reachable branch** — only their RELAY_LEDGER rows survive. The mechanism is the firewall's own design: relays are deliberately untracked (public-repo hygiene), so **nothing preserved them** — git protects tracked bytes, the ledger preserves dispositions, and the untracked file in between has NO custodian; a cleanup between sessions silently deleted the batch. This is the L114 class ONE LEVEL DOWN: B999's lesson was "branch protection preserves FILES; nothing preserved FINDINGS" — E51's is "**the ledger preserved ROWS; nothing preserved the untracked FILES.**" Two of the nine were processed before the loss (STEPPING_BACK banked; PATH_BEYOND triaged in B1009), so their content survives in banks; for the other seven, the ledger note is main's entire record. **Standing fixes:** (1) **sender-branch dual-homing** — every relay is committed on the SENDER's own branch at send time (cc3's newer `frontier/B8xxx/relays/` practice, now the standard for all seats); (2) the re-send protocol (B1172's ask to cc3, whose local relay dir may retain the batch); (3) the relay-debt gate's B1172 repairs make un-dispositioned aging visible before content can rot unnoticed (real clock; stale FAILS unless ESCALATED-by-name; all seat lanes matched; dateless = stale). |
| E52 the VERIFIER-DEFECT class — stabilization/control instruments wrong while their verdicts stood (named at Review 51) | 2026-08-28 (B1186/B1187 window; the class crystallized across two arcs and two seats) | A verification PASS whose own instrument is defective — distinct from E40 (cached verification: the right instrument never re-ran) and E50 (the right instrument never reached): here the instrument RAN and was WRONG, and the verdict survived only because the defect happened not to flip it. **Five instances in one window**: (1) B767's TOMB-L34 "3 seeds" were vacuous — the model is deterministic; RNG was seeded but never drawn (robustness theater); (2) B767's c_eff = S/log L estimator manufactured its own "inconclusive plateau" (conflates the additive constant; the two-parameter fit resolves it); (3) B767's WALL-7 docstring claimed deg+1 nonvanishing points prove "no roots" — they prove D ≢ 0 (generic-t), not all-t; (4) B189's C3 "indistinguishable from null" reversed at 100-seeds-per-level (Ω sits 11σ above its matched null — a 30-seed, one-level test under-resolved a real 0.15 deviation); (5) cc3's B8152 control was one-sided (recover-the-known-14 checks false negatives on KNOWN members; structurally blind to unknown missed members — the t06829 undercount rode through it). Kin: this seat's own same-window near-instances, self-caught before banking (the double-cast "high-precision" confirm that wrongly rejected o9_41001; the empty-slice window giving exact-zero entropies). **Standing fixes:** every stabilization pass re-verifies its INSTRUMENT before its verdict (does the seed matter? does the estimator measure the claim? does the logic prove the quantifier stated?); controls must be TWO-SIDED (a known-positive recovery AND an independent enumeration/complement); statistics claims carry their resolution (an N-seed null is a claim about deviations ≳ its own std); and cc3's B8151 verified-vs-used sweep + this row join the review checklist as VERIFY-THE-VERIFIER. |
| E52 instance #6 (the class holds; the adoption curve is data) | 2026-08-28 (cc3 B8153, self-caught) | One day after the E52 rule landed (two-sided controls), cc3's mod-p obstruction attempt ran the one-sided control first (does the test wrongly exclude the KNOWN target? no) and reported INCONCLUSIVE verdicts before the bite control (does it exclude ANYTHING? no — a degree-29 polynomial "not excluded" from a degree-4 slot; VACUOUS, abandoned). Self-caught by the seat that committed instance #5, under the freshly written rule, and narrated in their own relay — the failure mode survives its naming for at least a day; the BITE control ("prove the instrument can exclude a deliberately-absent target") joins the two-sided-control rule as its sharpest form. |
| E53 the SURFACE NON-PROPAGATION class — a banked result never reaches the surfaces that assert the opposite (named at Review 53; found because the owner refused the seat's answer) | 2026-08-30 (Review 53; B1218 the instrument, `5ec66cf0`/`55dd173c`/`c0e9a4a0` the repairs) | A result is banked, correct, findable, and cited nowhere it is needed: the arc closes a question and **the live surfaces go on asserting it open**. Nothing is lost, no instrument is wrong, no lock is missing — the failure is PURELY one of propagation, which is why it is invisible to every existing defence. Distinct from **E51** (retention: the artefact was destroyed — here the record is complete), from **E40** (cached verification: the instrument never re-ran — here NO instrument is involved), and from **E50** (never reached). **SEVEN INSTANCES IN ONE WINDOW**: the A2 stratum (≈4.3 bits), the hypercharge direction (≈4.17 bits) and the spin lift (1 bit) all priced FREE in `THE_ROAD` §IX against B1112 / B1109+B1118 / B1141 (the last hostile-verified two-bench on three engines) — ≈8.5 bits of phantom freedom, and the 4.17 row a straight DOUBLE-COUNT of the P bit listed one line above it; **L175 counted open on TWO separate surfaces** (§X and `OPEN_LEADS`) against B1110 (PROVED, "L175 CLOSES"); III.4's census contradicting its own node ON THE SAME PAGE; and L57's row asserting a question B364 (NEGATIVE) had answered, with three settled arcs bearing on it and NONE cited. **The mechanism, caught in the act**: B1109's re-pricing reached node III.5 (correctly `[PROVEN-FREE]`, "1 BIT") and reached NEITHER §IX's ledger NOR §X's census — *one arc, three surfaces, one updated* — and the §X repair did not touch the identical stale row in `OPEN_LEADS`, which had to be found separately. **The aggravating sub-mechanism — a lock that PINS the stale assertion**: `test_the_road.py` asserted the literal `"ten genuine open nodes"`, so with L175 closed since 2026-08-21 the test was ENFORCING the error and would have failed anyone who corrected it; a string-pin can only preserve what was true when it was written. Not E38 (a lock failing when it should not) nor E40 (passing over a cache): the lock passed, correctly, over content that was wrong. **Cause is measured, not anecdotal** — B1188/B985: live instruments recover object-faces at 79–100% and **relation-faces at 6–19%**, with **132 PROVED on-theme arcs absent from every live surface**; seven surfaced the moment anyone looked. Kin from the other side: codex's R029 independently re-derived, ten days later, a negative main had banked at B1095 and not surfaced where they would meet it. **Standing fixes**: (1) `scripts/checks/open_claim_sweep.py` (B1218), bite-controlled both directions, run before every review and before writing "open" anywhere; (2) an arc that closes a lead must name EVERY surface asserting it open — a propagation list is part of the bank, not only its own FINDINGS; (3) **doc-pinning locks assert FACTS, not STRINGS** — a lock pinning a count or status must also pin what DECIDES it, so the fact and its lock move together. |
| E53 instances #8–#9, and an independent same-day arrival from another bench | 2026-08-30 (the cloud seat's digest + their Gate C finding; both verified on this bench) | **#8 — the class arrived twice on one day, from two benches, neither seat reading the other.** The cloud seat's digest states the sub-mechanism in its own words: *"An audit cell that hardcodes what it audited is a record of a reading, not an instrument, and must never be re-run as one."* Their instance **fired**, which main's did not: re-run against a repaired draft, their own certificate reported **four false positives out of six** — two cells testing mathematical invariants true of every draft and therefore of none (MB12-vacuous, the third and fourth such vacuity in a fortnight after B1206's candidate iii and B1216's GC-16 clause), and two hardcoding the wording of the draft they audited. Had they trusted it they would have reported six completed repairs as not done. Main's instance is the same shape one layer up (`test_the_road.py` pinning the literal "ten genuine open nodes" while L175 had been closed since 2026-08-21). **Two independent arrivals in one day is the evidence the class is real and not a local slip.** Their repair is the template main adopts: each cell parses for the property at issue, the pin is a parameter, and both outputs are vendored so the cells fire at the audit pin and clear at the current one. **#9 — the most expensive instance recorded: GATE C.** `docs/OPEN_PROBLEMS.md`'s Gate C Settled list cited B307/B302/B321 and omitted **B323 and B324**, the two PROVED arcs that actually compute what the commensurator ℤ/3 does; with them the gate closes **by its own written refutation condition** (B323 kills the trinification disjunct; B324 exhibits a symmetric C₃ triple on a single knot, which B307 — already cited — forbids). Cost made concrete: a specialist question (Q9) had been **drafted that would have paid an outside expert to redo banked work**, withdrawn on discovery; and the corpus's load-bearing "four named gates" count was wrong (three: A, B, D). The closure **relocates** the generation question rather than answering it — B632's h¹(M;27)=3 GRADED route is explicitly *"NOT blocked by B307"* and its cell 2 is queued and unrun. **A companion finding from the same digest, reproduced on main: the instrument for staleness is itself scoped too narrowly to work** — `scripts/checks/doc_currency.py`'s registry holds **19 documents while `docs/` + root hold 169 (11% coverage)**, so `doc-currency` sat green throughout the window in which Review 53 found eight navigation views three reviews behind. E53's enforcement analogue: not a stale document, but the detector for staleness passing vacuously. Credit for both to the cloud seat. |
| E51 REOPENED — the nine relays RECOVERED (E51's FINAL verdict was itself an E54) | 2026-09-02 (B1235; found by fab5cloud, blobs verified here) | The nine 2026-08-09 relay files sit at the root of `audit/b775-braver-questions` @ 53da05f6 (9 blobs, 88 060 bytes; the head carries 118 CC3_TO_CC_* files). The E51 row above says **"exist on no reachable branch"** — that was asserted without enumerating the branches. The retention-gap CLASS stands (untracked files have no keeper); the INSTANCE is recovered. RELAY_LEDGER rows 39–46 re-marked RECOVERED; content still to be read and banked. | Nothing to fix in the mechanism — fix the claim: no absence verdict without the sweep (E54). | B1235 |
| E54 the ABSENCE-WITHOUT-SWEEP class — "we don't have X" asserted without enumerating the population X would live in (owner's rule) | 2026-09-02 (B1235; the owner's 2026-09-01 instruction, verbatim: *"one important rule: before you conclude we dont have something, swipe the repo first."*) | An absence claim is a universal statement over a population (branches, files, history, content), and it was being made after a search of a *sample* — prose grep on main, a filename glance, one branch. Instances found this window: (1) E51's "no reachable branch" — the files were on a reachable branch; (2) B1225's 08-31 addendum "the list does not exist on any of them" and the relay `SEND_THE_SEVENTEEN_ATOMS` — the 17 atoms were on main in `B1203/verification/reproduce.sh:10–12` since 08-28, as data inside a heredoc; (3) the paper branch's e99e2210 "missing" verdict (seat-reported); (4) the seat INDEX #34 entry; (5) P2's "no engine" framing — B869's engine was committed; (6) B1181's lock, which asserted the reliable method was in use without checking what the method computes. Sibling of E53 (a banked result not reaching the surfaces that assert the opposite): E53 is the record failing to *propagate*; E54 is a seat failing to *look* before declaring. Related to PRACTICES' "no match is not a finding" (B1001), which required a second SOURCE; this requires a complete SWEEP. | **THE ABSENCE RULE** in WORKING_RULES.md (owner's words); `scripts/checks/absence_sweep.py` — every remote head, filenames AND content, plus deleted-in-history, with planted controls both ways; the phrase "does not exist on any branch" must cite the sweep's output. | B1235 |
| E53 instances #11–#12 — two banked FENCES that never reached the flagship surfaces, found only because the E51 files came back | 2026-09-02 (B1235 addendum `the_nine_relays_read`; both flagged by cc3 on 2026-08-09/10 and lost with the files) | **#11:** `docs/THE_SM_VERDICT.md:45` and `docs/THE_FRAMEWORK.md:183` asserted "three generations, structurally … banked (B897, B928)" while B897's own FINDINGS say *generation-SHAPED at the tiling level, mechanism-hood NOT decided* (`LAW_MAP.md:194`: "mechanism-hood fenced"; B298 PROVED the object does not force three; P13 tombstones the 9+9+9 reading). Live 25 days on two flagship docs. **#12:** `THE_FRAMEWORK.md:163` fused the field's SM-resonance with the knot's arithmetic uniqueness — B727's OWN distinction ("different objects … do not reinforce"), with B993 showing Reid-uniqueness consumed at zero steps. Live 24 days after the fix was written. Both corrected in place with dated scope notes; the arcs are unchanged. **The lesson compounds E51:** an untracked relay is not just a lost file — it is a lost CORRECTION, and the surfaces it would have fixed keep asserting the overclaim. | B1235 |
| E51 CLOSED on content (the nine relays READ, not merely recovered) | 2026-09-02 (B1235 addendum) | Every claim in the nine recovered relays traced to where it lives on main, corrected, or queued by name: 7 rows BANKED, REVIVABLE → L195 (instrument), README_ARC_PROPOSAL → owner (outward-facing). Two corrections (E53 #11–#12), one label fix (B718:95, area not longitude — line 148 is correct), one verdict hole queued (B1–B5 → L196). Rule confirmed twice in the read: THE ABSENCE RULE (E54) — the confluence run looked unbanked and is B994; the revivable index looked absent and is 774 hatch-carrying kill-graph rows plus `query.py revive`. | B1235 |
| E55 the TRACE-FIELD-FOR-INVARIANT-TRACE-FIELD class — arithmeticity (a commensurability invariant) inferred from the degree of ℚ(tr Γ) instead of ℚ(tr Γ⁽²⁾) | 2026-09-02 (B1237; flagged by the physics-seat evaluation branch R35, recomputed here) | For a Kleinian group the invariant trace field kΓ = ℚ(tr Γ⁽²⁾) decides arithmeticity; ℚ(tr Γ) can be strictly larger. Silver (m136): trace field degree **8**, invariant trace field **ℚ(i)** — 350 traces of squares over words ≤ 6 all Gaussian integers, Vol = 4G = 12 × covol(PSL₂ℤ[i]) to 50 digits. **Instances:** (1) B258 FINDINGS:15–16 *"silver → 8 (non-arithmetic)"* — the source; (2) B1062 V2 *"NON-ARITHMETIC (degree alone decides)"* and *"separates {golden} from {silver, bronze}"* — inherited, **with the invariant datum x² = 2+2i recorded in the same table row**. The bank already held the right answer five times (B125/B147/B137/B850/B337) and contradicted itself for 20 days with no instrument noticing (an E53 shape on top). | Compute the invariant field (traces of squares) before typing arithmeticity; `frontier/B1237_physics_seat_r31_r38_harvest/verification/silver_arithmetic.py` is the one-line check. Kill graph: `B258-silver-inference`, `B1062-V2-axis`. | B258, B1062 (both corrected in their own verdict files + addenda, 2026-09-02) |
| E53 instances #13–#15 — three corrections that reached the log and never the corrected arc's verdict file (the physics-seat synthesis measured the class at verdict-file grain: `SUPERSEDED_UNMARKED` 48, `RETRACTION_NOT_PROPAGATED` 25 of 605 digested) | 2026-09-02 (B1237; the seat's W-D synthesis, each instance read on-bench) | **#13:** B361 PROVED, `superseded_by: None`, while B367 FINDINGS:45–54 refutes its induced law at pair (3,4) (bright, no doubly-elliptic seed; the minimal repair dies on (1,3)). Now `superseded_by: "B367"`, claim prefixed SUPERSEDED (not RETRACTED — the 8-pair fact holds; B818). **#14:** B259 wall #5 (k=3 → GΛ=2π, "122 orders") withdrawn by B980 (3+1-dim relation, dropped ħ), THE_FRAMEWORK/LAW_MAP/COSMOLOGY_LEDGER carry the withdrawal, B259's own files did not; two further live sites found and marked (`papers/metallic_one_object/SYNTHESIS.md:332`, `docs/ROADMAP.md:57`). **#15:** B892's sentence "takes E₆ to the SM algebra" (dim 14 = su(3)⊕su(2)⊕u(1)³ vs the SM's 12; B950) — B950/B951/B1236 all in the log, B892's verdict file silent. All three now carry the correction in `note` + an addendum. **The instrument gap:** nothing sweeps `superseded_by` against later arcs' FINDINGS — queued as **L197** (a judgment sweep over the seat's 48 pairs; not fanned out, per B1216). | THE PROPAGATION RULE (E53 row above); this cell's locks pin the notes in the corrected arcs' own files. | B361, B259, B892 |
| E56 the RESIDUAL-ONLY INTEGER-RELATION class — an algebraic relation for a numerical constant accepted because the residual abs p(t) is small, with no bound on the coefficients and no second route | 2026-09-02 (B1238; own bench instance, and B578-D6 read on-bench) | LLL/PSLQ/`algdep` at fixed precision always returns *something*; at 170 digits this bench obtained degree-5 "relations" for bronze's invariant traces with 50-digit coefficients and residual below 10⁻¹⁰⁰ — fake at any residual. **Instances:** (1) this bench, B1238's first bronze run (caught before banking by the coefficient bound); (2) B578 item D6 "DEGREE 6 (two precisions, residuals 1.7e-74 / 1.8e-142)" — the residuals were the whole evidence; its own verifier's objection (`LEDGER_RAW.md:76`: canonical route gives degree 8, stable 400–1500 bits) was right and was overruled; the D6 script never reached main. Bronze's invariant trace field is the octic x⁸+6x⁶−x⁵+12x⁴−3x³+8x²−x+2 by two routes at 1000 bits. Sibling of E55 (a wrong criterion) — this is a wrong *acceptance test*. | Accept a relation only if it is irreducible, largest coefficient below 10¹², residual below 10⁻²⁰⁰ at ≥ 300 digits, **and a second route agrees** (`nfisisom`); `frontier/B1238_seat_harvest_40a3_bronze_octic/verification/bronze_invariant_trace_field.py` applies all four. | B1238 (own instance, caught); B578-D6 (corrected 2026-09-02 in its own files); **xB015 (2026-09-17, own instance, caught before it was reported)** — the class in its RATIONALITY form: `Fraction(x).limit_denominator(D)` accepted as a rationality test with tolerance tau. With D = 1e5 and tau = 1e-9 the recogniser calls EVERY float rational (it approximates any real to ~1/D^2 = 1e-10), and a first exploratory run duly returned *'96.5 % of the cusped census has rational CS'*. The denominator is the coefficient height; an unbounded height with a fixed residual is E56 exactly. **Standing rule in this form: a rationality/denominator recogniser is valid only when 1/D^2 >> tau, and the bound must be stated in the same sentence as the rate.** xB015 declares D = 240, tau = 1e-9 (1/D^2 = 1.7e-5) in its seal and enforces it in code (`guard_ok`), and the family/control comparison uses one (D, tau). |
| E53 instances #16–#18 — three more corrections that never reached the corrected arc's own files; one of them is yesterday's (B1237 corrected B213's copy of a label and not B211, the arc that minted it) | 2026-09-02 (B1238; #16 surfaced by the physics-seat evaluation branch's W-E sweep, #17–#18 found here) | **#16:** B362 PROVED ("brightness law to 11 pairs, 0 counterexamples"), `superseded_by: None`, while B367 FINDINGS:89 refutes the law at pair (3,4) — B361 received this under B1237 and its sibling did not. Now `superseded_by: "B367"`, claim prefixed (not RETRACTED — the 11 exact pair verdicts stand; B818). **#17:** B211 is the arc that labelled the character variety 40a1; B1237 corrected B213's copy and left B211 PROVED with the wrong label. Now corrected at B211 (label 40a3, the map exhibited — see I-12) and at six live docs sites. Mine, one day old: **the propagation rule is to walk to the SOURCE arc, not the arc the seat happened to flag.** **#18:** "bronze trace field degree 6" at four sites (B125 FINDINGS:32, B137 FINDINGS:44, B578 RESULTS:26–32, B840 FINDINGS:36–37); the invariant trace field is degree 8 (octic, two routes), so no degree-6 trace field is possible; verdicts unaffected, all four corrected in their own files. Class count now 18. | The propagation rule (E53): walk every correction to the arc that MINTED the claim and to every live site — `superseded_by`, `note`, claim prefix, addendum; a seat's flag names one site, the sweep finds the rest (`grep -rn` the label across frontier/ docs/ tests/). | B362, B211, B125/B137/B578/B840 (all corrected in their own files, 2026-09-02) |
| E57 the LOCK-WITHOUT-TOOL class — a tracked test that runs an untracked script; the local suite is green because the bench has the file, a fresh clone reds | 2026-09-02 (B1238; found at the B1238 landing, one commit after the instance shipped) | B1237 (43e32d38, pushed) committed `tests/test_paper_ledger_counts.py` and its CHANGELOG line says the tool was "fixed and locked" — but `scripts/checks/paper_ledger_counts.py` had never been `git add`ed (no history at all). Every local certificate passed: the suite because the file was on disk, `path-refs` because the path RESOLVES. Root cause is the same `git add -u` + explicit-new-paths discipline that prevents `-A` accidents: it puts the burden of naming every new file on the seat, and the seat named the test but not the tool. Census at landing: 1 instance in 1160 tracked py files (the scan is B1238's `untracked_deps_scan` idea, now the gate). Tool added in B1238's commit. | New gate `tracked-deps` (`scripts/gates/gates.py`): every path a tracked test/script names that exists on disk must be in `git ls-files`; runs at push via the pre-push hook, which is the moment the fresh-clone reality begins. The complement of `path-refs` (exists?) — this one asks "does git HAVE it?". [2026-09-02 B1240] the gate's scope is tracked `.py` under `tests/` and `scripts/`; it is blind to shell runners under `frontier/` and to files that are simply ABSENT. Instances #2–#6: the five seam-harvest runners B1147 `reproduce_all.sh`, B1148 `reproduce_new.sh`, B1149/B1150/B1153 `reproduce.sh` — locked by string-only `REPRODUCES` tests, their certificates on a seat branch, their run record `reproduce.log` gitignored; none could run on a fresh clone. Fix: the full transitive closure vendored (65 files, sha256-pinned, `VENDORED_FROM.txt`), `scripts/checks/reproduce_belt.py` (string-lock / runner-closure / inert census, selftest 9/9) and `tests/test_reproduce_runners_live.py`, which EXECUTES all 26 runnable belt runners on every suite run. Third shape censused: six PINS-TEXT runners (B1171/1173/1176–1179) recompute nothing and print REPRODUCES; B1175 prints RECORD (the honest form).] | B1237 → fixed in B1238; #2–#6 (B1147–B1153) → fixed in B1240 |
| E52 instance #7 (the retracted instrument recurred on the seat that retracted it; the two-sided control caught it in one step) | 2026-09-02 (B1239, self-caught) | The first amphichirality detector written for B1239's closed-census scan was `M.is_isometric_to(mirror(M))` — the orientation-blind call B1181 used and B1235 retracted five arcs earlier (E52 #5's kin). It returned "amphichiral" for all 3000 cusped probes in four seconds; the chiral control (5₂, known chiral) came back ≅ its mirror and the run was discarded before any number was read. Detector replaced by `symmetry_group().is_amphicheiral()` and, per isometry, `is_isometric_to(M, M, return_isometries=True)` filtered on `cusp_maps()[0].det() == −1`; the two agree on 600/600 census manifolds with determinants consistent across cusps. Side finding: `isomorphisms_to()` does NOT canonize (m006: 2 of 4 isometries) and is not a substitute. **Rule sharpened:** a detector retracted in the record is a *named* blind spot — the lock test pins it (`5_2.is_isometric_to(mirror) is True` while `symmetry_group().is_amphicheiral() is False`) so its recurrence reds the suite rather than relying on the seat's memory. |
| E53 instance #19 — a law row quoting a RETRACTED number as its example, one arc after the retraction, on the seat that retracted it | 2026-09-02 (B1241; caught by the physics-seat evaluation branch's R51, which reached B1235's 38/74 split independently) | B1235 retracted B1181's 83/83 (orientation-blind isometry call; the family is 38/112 amphichiral) and wrote in the same FINDINGS *"its family-test law at LAW_MAP:263 stands"*. The row's second clause reads "only HELP family-level claims … exactly how amphichirality strengthened to 83/83" — the retracted computation as the illustration, and a clause that is false for universal family claims (14/14 → 38/112 HURT the universal claim). The retraction reached B1181's verdict file and B1235's own prose and NOT the law that cited the number. **Mine.** The propagation list for a retraction must include every surface that quotes the retracted VALUE, not only the surfaces that assert the retracted VERDICT — a `grep` for the number ("83/83") would have found it. | Corrected in place (dated bracket at `LAW_MAP.md:263`: the direction of the one-way test depends on the quantifier); B1235 addendum; the B1241 lock pins the bracket. Rule sharpened: retractions propagate by VALUE grep as well as by verdict. | B1235 → B1241 |
| E53 instances #20–#22 — three verdict lines that say more than their own arc's body (STALE-VERDICT / LABEL-ONLY at verdict-file grain) | 2026-09-02 (B1241; flagged by the physics-seat evaluation branch's Phase E part 1, each read on main) | **#20 B232** PROVED while FINDINGS:3–4 say "the central *unproved* theorem … an honest reduction … not a proof" — now OPEN, claim = the reduction + the checked range (n ≤ 8 exact, n ≤ 5 real Jacobian). **#21 B167** claim "cannot source an internal scale" while FINDINGS:3–8 mark the premise POSTULATED and the argument stated — qualifier added, NEGATIVE unchanged. **#22 B647** claim "forces the ratio Y[023] = 24ζ₆·Y[123]" while its Cell 1 line 14 says "NOT forced — residual ONE ℝ-linear condition" — claim corrected, PROVED unchanged. None had an addendum, none appear in B1237–B1240's sweeps. Same species as #13–#18: the arc knew, the verdict line did not. The measure of the class at this grain is the seat's Phase E: 128 SELF_REFERENTIAL_LOCK + 65 CLAIM_EXCEEDS_COMPUTATION flags over the corpus, 71 judged so far. | Each corrected in its own verdict file + a dated addendum (the source-arc rule, #17); `tests/test_b1241_master_identification_priced.py` pins all three. Phase E part 2 continues on the seat's branch. | B232, B167, B647 |
| E58 the JUDGED-FROM-A-SUMMARY class — an arc graded from someone else's one-line summary of it rather than from its own text, and the wrong grade accepted downstream because it arrived with authority | 2026-09-03 (B1243; committed by the main seat while correcting six errors of the same family) | The main seat graded a downstream artifact's T17 as "rests on a REFUTED identification (I-4/B813)" and ranked it the most serious of six corrections. T17's own docstring fences B813 verbatim — B813 killed a *value* identification filling a *coefficient* slot; T17 is a ℤ/2 → ℤ/2 map of symmetry types with its own MB12 failability control. The seat had read a third seat's summary, never the source. The receiving seat accepted the withdrawal within the hour. | **An incorrect correction propagates faster than the error it replaces, because it arrives with authority.** Before grading another seat's result: read the result, not a summary of it, and not the citation someone else attached to it. A summary is evidence about the summarizer. **CLAUSE ADDED 2026-09-03 (B1247, Review 54 R54-5): A CLAIM ABOUT A FILE IS TIME-INDEXED.** E58 and E59 both assume the source holds still. In a repo several seats write to, it does not. A cross-seat dispute this window resolved as **neither party's error**: this seat read `oa-distill/theorems/STATEMENTS.md` at the repo's then-current push (2026-09-02T18:21Z) and correctly reported T17 absent; the physics seat added it in `1002235` at 2026-09-03T12:18Z and correctly reported it present. Two correct reads, one contradiction, **because the file moved between them**. *Filed as a clause rather than a new class: all three failures are one family — you did not actually check the thing you think you checked — and minting a class per variation dilutes the ledger until nobody reads it.* **The rule: quote the SHA or timestamp you read, not just the filename.** "STATEMENTS.md omits T17" rots silently and then reads as an error; "STATEMENTS.md at `bedb0a1` omits T17" carries its own expiry. | Disposition: T17 REGISTERED (I-18 UNEARNED), not withdrawn; the correction withdrawn; `scripts/checks/citation_status.py` installed. Near-miss the same turn, caught before assertion: a second "correction" (that the VEV point is pure gauge) was about to be sent on the seat's own dimension count, against B1092/B1099 — caught by reading the arcs. **Instance #2 (2026-09-08, B1297):** PW §3.1's chirality taken as H¹(Q₃; R_ρ) from the corpus's own summary (I-26's reading); the source (arXiv:0905.1968 eq. 3.18) counts a signed intersection and calls the bulk vector-like. Caught by the plan's literature scout the day after the table; corrected in the arc's FINDINGS §0b, the sealed pre-registration left as written (the E4 shape too: a cited frame taken as the discriminating fact). |
| E59 the FOUND-BUT-NOT-REVERIFIED class — an item is located in the repo after a sweep and accepted **as it stands**, without being re-verified against the **new** question that prompted the search | 2026-09-03 (B1245; named by the owner mid-session: *"one mistake we make is that we think we dont have smth, we check the repo we find it and we dont reverify it in the new light"*) | The seat, needing a genericity control for C43, was about to write a fresh SU(5)/SO(10) desert-running script. B915 was found by sweep and read as "the arc that ran OUR curve" — its stored role. Re-opened against the NEW question (*what does this computation actually depend on?*), its `crossing.py` gives a strictly stronger answer than the new script would have: the curve has **no group parameter at all**, so group-independence is true **by construction** rather than by comparison. | **A found artifact carries the role it was filed under, not the role you need.** Distinct from E54 (declaring absence without sweeping) and E58 (judging from a summary): here the sweep succeeded, the source was even opened, and the item was still read for what it was known to say rather than interrogated for what it could now answer. Re-ask the new question OF the found artifact, explicitly. | Disposition: C43's group-independence scope landed at B1245 from B915's own dependency structure; no new computation and no new measured input were needed. Related follow-through failures in the same window are recorded with it — two statements made in session (C44's Borromean sharpening, this scope line) reached the CHANGELOG and not the chain, which is the same disease B1243/B1244 diagnosed, committed by the seat that diagnosed it. |
| E60 the CONCLUSION-SURVIVED-SO-THE-STATEMENT-LOOKED-FINE class — an identification is written down in a form that is *wrong*, but the downstream conclusion it was used for happens to be *right*, so nothing downstream reds and the error is invisible to every check that looks at outcomes. | 2026-09-05 (B1248, self-caught) | B1248 wrote the Maclachlan–Reid quaternion algebra as `(tr²A − 4, 2 − κ)` and called **ε itself the second Hilbert slot**. The true algebra is `(A₀², B₁²) = (tr²A − 4, (2 − κ)/(tr²A − 4))`; ε and the second slot differ by exactly the **first** slot. For the object the true algebra is `(5, −5)` and the wrong form gives `(5, −1)` — **both split**, so the arc's stated conclusion ("split, consistent with m004 arithmetic") came out right off a false premise, and no numeric check in the arc disagreed. **What caught it:** computing **2T**, the chain's own door — the Q8 pair `(i,j)` gives `(−1,−1)`, the **Hurwitz quaternions**, the known answer for the binary tetrahedral group, against the wrong form's split `(−1,+1)`. The discriminating case existed only where the first slot was *not* a square. **Untouched:** `det X₀ = squarefree(2 − κ)` (the realizer determinant, 14/14 integral) — and the arc's own lock already encoded the correct `B₁² = (2−κ)/(tr²A−4)`, so the *test* was right while the *prose* was false. **Rule:** when an identification is checked only on the object, check it on a case where the identification's two sides can *differ* — a known-answer group (2T → Hurwitz) is the cheapest such case, and it is now a permanent control in `tests/test_b1248_norm_classification.py::test_2T_gives_the_hurwitz_quaternions`. A conclusion agreeing is not evidence the statement is true. |
| E61 the GENERIC-FACT-ATTRIBUTED-TO-THE-OBJECT class — a computation is run on the object, comes out interesting, and is written up as a property **of the object** (or of a particular map) without ever asking whether the same value is forced for **every** input of that type. The write-up is true as arithmetic and false as attribution. | 2026-09-05 (B1248/B1249, self-caught, **three instances in one session**) | **(i)** *"the object's meridian pair carries class −1 — the object supplies its own bit."* Vacuous: for any two parabolics `tr[a,b] = y² + 2`, so `2 − κ = −y²`, minus a perfect square **identically, in every field** — the class −1 belongs to the meridian pair of *every* cusped hyperbolic knot complement (cross-checked on 5₂, 6₁). **(ii)** *"the torsor becomes the bit over ℚ(√5), the object's own monodromy field — the wall was a wall over the wrong field."* Generic: `2 − κ(A,X_A) = −(tr²A − 4)` **is** minus the discriminant of A's characteristic polynomial, so the class trivialises exactly over A's own eigenvalue field for **every** A (checked at tr A = 3,4,5,6,7,−3,11). Only *which* field is object-specific. **(iii)** *"all 48 surjections give κ = 0, so **the McKay door** is not class-preserving."* Misattributed: **all 384 generating pairs of 2T already have κ = 0**, so the figure-eight relator discriminates **nothing** and the fact is about generation in 2T, not about the door. **What each instance had in common:** the discriminating control was cheap (one symbolic line, or one wider population) and was not run until the claim already looked good. **Rule:** before attributing a computed value to the object or to a specific map, compute it on the **widest population of the same type** — all parabolic pairs, all traces, all generating pairs — and only claim what the wider population does **not** already give. Locked where each was caught: `test_the_relator_discriminates_NOTHING_the_door_claim_is_refuted` keeps instance (iii) permanent, so a later seat re-deriving the door claim reds the suite. Distinct from **E60** (a wrong *statement* whose conclusion happened to be right): here the arithmetic is right and the *attribution* is wrong. |
| E62 the FORMULA-FITTED-TO-ITS-OWN-EXAMPLES class — a closed-form claim is validated on the same small, hand-picked set that suggested it, and generalized. Every example agrees, so the claim looks proved; the discriminating region was never in the sample. | 2026-09-05 (B1248, caught by a **peer seat**, verified here) | B1248 claimed **`det X₀ = squarefree(2 − κ)`** for the integral realizer determinant, "verified 14/14 partners." Against the **actual** realizer module on 500 random noncommuting partners it scores **493/500**. The correct formula, supplied by a peer seat and confirmed here, needs no linear solve at all: **`det(AM − MA) = 2 − κ` identically** and **`det X₀ = det((AM − MA)/g) = (2 − κ)/g²`**, `g` the entry-gcd — **500/500**. Sharpest counterexample: `2 − κ = −121 = −(11²)`, `g = 1`, true `det X₀ = −121` (**torsor**), while squarefree gives `−1` (**the bit**) — the two formulas disagree precisely about *whether the pair carries the bit*. They differ only when `g²` is not the largest square dividing `2 − κ`, a condition **none of the 14 curated partners met**. The corrected criterion is **ε = −1 ⟺ 2 − κ = −g²**, not "κ − 2 is a perfect square". **Same seat, same message, a second catch:** B1192's √2 control fixed **one** integral matrix and drew a conclusion phrased about the **field** — both determinant signs occur inside the same trace field ℚ(√2) (tr = −34: `[[-35,-36],[1,1]]` → D = −1, `[[-29,-12],[-12,-5]]` → D = +1) — and B1248's first version **repeated that field-level phrasing** rather than catching it. **Rule:** a closed form is not tested by the examples that produced it. Validate against the *definition* it claims to compute (here: the realizer module itself), on a **randomly drawn** population, before the closed form is written into a claim line. Locked: `test_the_additive_commutator_formula_beats_squarefree` pins the −121 counterexample and `test_one_pair_does_not_license_a_field_conclusion` pins the ℚ(√2) pair. Distinct from **E61** (right arithmetic, wrong attribution) and **E60** (wrong statement, right conclusion): here the formula itself was wrong and the curated sample hid it. |

| E63 the RIGID-STRUCTURE-MISTAKEN-FOR-A-SPECIES-COUNT class — a multiplicity is found, verified to be non-generic against a random control, and read as a count of **things** — when every element of it is **equivalent** to every other under a symmetry that was never quotiented out. The control tested **rigidity**; the question was **inequivalence**. | 2026-09-05 (B1253, self-caught, but only after the claim had been stated and a patch to a canonical surface written) | B1253's draft headline, **refuted** by the arc itself within the session: *"the generation count is forced at three."* The geometry is real — 15 distinct 16-blocks, pairwise overlaps taking exactly two values (8, 10), 11 empty triples all `(8,8,8)` with union 24, max mutually-independent family 3 — and **non-generic** (random collections reach 3 in ~4% and show the two-valued rigidity in 0/300). **But all 15 blocks lie in a SINGLE Weyl orbit**, so the three 16s in any triple are **W(E₆)-conjugates sharing one character**: "three" counts **conjugates, not species**. This is precisely **B324's** objection, and `docs/OPEN_PROBLEMS.md` §C — *"Multiplicity → the generation count"* — was **CLOSED on 2026-08-30** for exactly this reason. **A patch to `docs/THE_SM_VERDICT.md` was written on the dead basis and REVERTED.** **Rule:** before reading a multiplicity as a count of things, **quotient by the symmetry acting on them**; and sweep `OPEN_PROBLEMS.md` for the route by name — its §C carried the deciding test, dated five days earlier. Locked: `test_the_blocks_are_ONE_WEYL_ORBIT_so_this_is_NOT_a_generation_count`. Distinct from **E61** (a generic fact attributed to the object): here the fact is genuinely non-generic and the *reading* is what fails. |
| E64 the COUNT-IN-A-HEADLINE-WITHOUT-ITS-LIST class — a load-bearing **count** ships in an arc's title while the items are **never enumerated in the artifact**, the catalogue having existed only in session; and, the worse half, a **shared-cause attribution** across those N items is asserted per-item while verified for one. A general mathematical mechanism gets presented as a per-instance historical finding. **Those are different claims.** | 2026-09-06 (B1255: *"the pattern behind **twelve** lost three-nesses"* — "twelve" appeared once in the arc, in the title; the ℚ(√−3) mechanism is verified for B298, plausible for B324/B1253 on conjugacy, UNVERIFIED for the other nine). Caught only when the owner asked *"should i push back more and where?"* and the honest answer named this arc's own headline. **Direct repeat of the 17-atoms precedent** (`B1225` addendum, 2026-08-31: *"a load-bearing input that exists as a number rather than as a thing"*) — read hours earlier the same session. **RULE:** a count in a headline must be enumerable in the same artifact, and a claim that N items share a cause must say which of the N were checked. Arc's COMPUTED results (μ13/HIER S₃, [C18,D₂]≠0, the 27=16+10+1 dimension count, I-24 REFUTED) are untouched; the narrative frame is scoped down. |
| E65 the STRUCTURALLY-INCAPABLE-FRAME class — an entire line of work is built inside a frame that a **theorem in the corpus says cannot deliver the target**, and the theorem sits in an arc read during the same session. Here: chirality (net h¹(27) ≠ h¹(27̄)) was pursued through **sl₂ embeddings** of the holonomy, but **every Sym^n of SL(2) is self-dual**, so any ρ factoring π₁ → SL(2) → E₆ has 27 ≅ 27̄ as π₁-modules and **net chirality is identically zero — for every embedding, making the embedding choice irrelevant to the target.** | 2026-09-06 (B1256/B1257 scoped by addendum; the self-duality statement is **B327's own text**, *"the 27 of E₆ is not self-dual, but its restriction to any SU(2) is, AUTOMATICALLY"*, and B327 was **read that same session** to source the archaeology). **TWO SIGNALS WERE ON SCREEN AND UNREAD:** (i) B327's sentence; (ii) this seat's own computed χ₂₇ on 2T = **(27,27,3,0,0,0,0), all real** — a real character IS the self-duality signature — printed and used to prove 2T-blindness without reading what it said about chirality. **What survives:** the arcs' mathematics is verified and stands (orbit dimensions, the Brieskorn uniqueness, the h¹ table, the 2T-blindness theorem); what is withdrawn is their standing as a **generation-count route**. **What it cost:** four arcs. **What the corpus already had:** chirality CONSTRUCTED (B944 census: 102 arcs, 70 PROVED) via **full-E₆(ℂ) Zariski closure from θ-odd twisting** (B582) with a binary switch (B576) and closings that supply it (B432/B434) — i.e. precisely by **NOT** factoring through SL(2). **RULE:** before building on a frame, ask what the frame provably CANNOT produce, and check that against the target. A frame is a hypothesis with a no-go attached. |
| E66 the GATE-NARROWER-THAN-ITS-OWN-RULE class — a gate's **docstring states a general rule** and its **predicate enforces a special case**, so every instance outside the special case is invisible. The gate sits green, and green is read as compliance with the *stated* rule rather than with the *coded* one. Distinct from a missing gate (visibly absent) and from **E53** (a correction that never reached the artifact): here the enforcement exists, is trusted, and is silently partial. | 2026-09-06 (B1290's follow-through, self-caught while answering an owner question about relay files) | **THIRD INSTANCE OF ONE SHAPE, and the ledger already held the other two.** (i) **B1226**: the attribution gate *"enforced ONE vendor token of the five the standing rule names, so this footprint was invisible."* (ii) **E53's companion**: `doc_currency`'s registry held **19 documents while docs/ + root hold 169 (11% coverage)** — *"not a stale document, but the detector for staleness passing vacuously."* (iii) **HERE**: `gate_tracked_forbidden` says *"cross-seat RELAY files … must not be committed LOOSE (at root or in docs/)"* and matched only `CC_TO_CC3|CC3_TO_CC|CC_TO_CC2|CC2_TO_CC` — so **CC_TO_FC, CC_TO_CLOUD, CC_TO_CODEX and CC_TO_ALL_SEATS were unenforced**. Cost: **two violations, BOTH committed by this bench on 2026-09-06, the same day the gap was found** — and while composing an answer that had already told the owner the 51 untracked relays were an uncommitted *backlog* whose disposition was their call. **They were untracked BY DESIGN.** The wrong recommendation was one command from being executed. **What caught it:** reading the gate's source before acting on my own recommendation, rather than reasoning from the filenames. **Fix:** the matcher keys on a **known seat as sender plus the relay convention's date stamp**, validated both directions (53/53 loose relays on disk matched; `docs/STRUCTURE_TO_NATURE_MASTERPLAN.md`, `README.md` and arc-archived relays correctly unmatched). The three already-banked loose relays are **grandfathered and bounded by a test** — GOVERNANCE §12 forbids moving banked paths — so a fourth entry reds the suite. The gate had **no test at all** before this; it now has three, including a planted violation. **RULE:** when a gate is the reason you believe a rule holds, **read its predicate against its docstring before trusting the green** — and give every gate a test that plants a violation, because a gate that cannot fail is indistinguishable from a gate that is passing. **(iv) 2026-09-07 (the doc-currency read at B1296's landing):** `retraction_sweep`'s docstring allows *a quotation of what was formerly claimed* as a mention, but its cue list lacked **"refuted"** — the corpus's commonest retraction verb — so registering B1253's headline turned the three lines that refute it (the arc's own FINDINGS, CAMPAIGN_STATUS, ERROR_LEDGER E63) into violations. Cue added with a dated comment; sweep clean at 19 phrases. |
| **E66 instance, and the class EXTENDED TO THE OPPOSITE DIRECTION** — E66 as written covers a predicate **narrower** than its stated rule (false negatives, invisible). The same docstring/predicate mismatch runs the **other** way and is just as costly: a predicate **broader** than its rule manufactures findings the rule does not claim. | 2026-09-13 (this bench; found while separating pre-existing reds from my own during B1349 addendum 2) | **Instance:** `tests/test_public_surface_scan.py` matched the bare prefix `reviewer-` (excepting only `-style`) under the comment *"placeholders, not prose (R38)"*, while the placeholder shape its own `STALE_REFERENCES` list names is that prefix **followed by digits**. The bare prefix red-flagged the ordinary phrase **"reviewer-facing verification package"** in the append-only `CHANGELOG.md` and `PROGRESS_LOG.md`, where no placeholder exists, so the test sat **red on prose** since `b94ed03a` (2026-09-09) and the red was read as a real hit. Narrowed to the prefix plus `\d`, and locked **in both directions** by a new control `test_reviewer_placeholder_pattern_is_not_vacuous` — it asserts the predicate catches four digit-suffixed forms and passes four prose forms (`-facing`, `-style`, `-friendly`, `-ready`). *The literal placeholder strings are deliberately NOT written out here: this file is in the public-surface scan set, and spelling them would trip both the narrowed predicate and the `STALE_REFERENCES` check — the first draft of this row did exactly that, which is its own small lesson about documenting a detector inside the corpus it scans.* **Fix rule:** a predicate is compared against its own docstring in *both* directions before it is trusted, and ships a two-sided control — one string it must catch, one it must pass. A gate that can only be wrong in one direction has only been checked in one. |
| E67 the CONTROL-THAT-VARIES-THE-WRONG-THING class — a criterion is checked for non-vacuity, the check PASSES, and the criterion is still vacuous, because the control varied a parameter the argument never ranges over. The MB12 habit is satisfied in form and defeated in substance: a green control is read as licence, so the vacuity survives the very step designed to catch it. Distinct from a MISSING control (visibly absent) and from **E66** (a gate narrower than its stated rule): here the control exists, is run, and points the wrong way. | 2026-09-06 (B1291, caught by an adversarial verification lane this bench commissioned, after the owner pushed back on the conclusion) | B1291's draft closed the corner/cone-point case with *"χ_orb = 0 for all 17 wallpaper quotients"* and attached a control showing the same formula returns **−1/42** on `S²(2,3,7)` and **+1/30** on `S²(2,3,5)`. That control passed and proved nothing: the argument ranges only over **quotients of a flat torus**, and **χ_orb(T²/G) = χ(T²)/|G| = 0 IDENTICALLY for every finite G** — so on its actual domain the criterion **cannot fail** and decides nothing. The control varied the *orbifold's geometry* (hyperbolic/spherical inputs the argument never sees) instead of the *thing the argument varies* (which finite G). Compounding it, the invariant was wrong anyway: **χ_orb is excluded on integrality alone** — for the cone orbifold `D²(n)` the index is 1 for every n while `χ_orb = 1/n` is not an integer, so χ_orb cannot be the index of any operator; the χ that enters is the **ordinary χ of the underlying space**. **What saved the conclusion was luck, not method**: the corner case really is closed, but on **genericity (E61)** and **admissibility**, neither of which the draft argued. **Also self-inflicted twice over:** this seat then *softened a correct conclusion under conversational pressure without computing*, and only recovered it by commissioning the verification. **RULE:** a non-vacuity control must vary **the same parameter the argument varies, across the same domain the argument ranges over** — if the criterion is applied to X ∈ D, exhibit an X ∈ D where it fails, not an X ∉ D. And when an invariant is chosen, check it can be an index at all (integrality) before it carries an argument. Locked: `test_the_VACUOUS_closure_is_labelled_as_refuted_at_source`, which pins the withdrawal banner in the script AND re-derives the vacuity (χ(T²)/|G| = 0 for every finite G) so the reason cannot rot. |
| E68 the FABRICATED-QUOTATION class — a string attributed to an arc in quotation marks that the arc does not contain; distinct from E58 (graded from a summary) and E64 (a count never enumerated) because it has NO source at all, adjacent or otherwise. **Instance #1 (2026-09-08, reported by the the session-relay seat seat about its own bench, relayed the same day):** *"…nothing downstream remembers which manifold it was"* attributed to B1011; zero hits on four phrasings; B1011's real scope line is about listener convention. **Rule (adopted from the seat's proposal, PRACTICES 2026-09-08):** every string in quotation marks attributed to an arc, a memo or a paper is grepped in the cited source before it ships; a quotation that does not grep is not shipped. Cross-seat class; the retrieval instruments (`already_banked.py`, `absence_sweep.py`) do not catch it because a fabricated sentence is absent by construction. |
| E54 instance (cross-seat) — the session-relay seat's "genuinely absent (verified with the working instrument): χ from the odd-plane determinant — 0 files" is B1103 THE BEING GATE (2026-08-21), whose vendored certificate's STAGE 2 is the derivation verbatim | 2026-09-08 (B1303; the relay of 2026-09-08 §1(a); the certificate re-run here 1364/1364, RC=0) | The item is in the record under the name of its CONSEQUENCE ("the being gate", the mod-3 gate as a theorem) and not of its METHOD ("determinant", "odd plane"); a grep by method words returns nothing on any bench. **Fix:** an absence sweep names the consequence's words as well as the method's (`absence_sweep.py` run twice); THE ABSENCE RULE's population is "every name the record could have given it". Sub-shape recorded as ABSENT-UNDER-THE-OTHER-NAME (H-B1303-BEING). No arc graded wrongly: HARVEST_LEDGER row 13 was SCHEDULED, now VERIFIED/B1103. |
| **E69 the UNASSERTED-VERDICT-STRING class** — a certificate PRINTS a verdict phrase next to a number, the phrase is not a function of the number (no assertion, no PASS/FAIL branch), and the script exits 0 whatever the number says; readers and harvests carry the phrase | 2026-09-08 (B1303; the instance found by fc R49 @ 659487bb on 2026-09-01 and by the session-relay seat's relay of 2026-09-08 independently, re-run and decided here) | **Instance #1:** `frontier/B1147_clane_harvest/verification/certificates/spacetime64.py` (the cloud's memo 27/33 certificate vendored by B1140/B1147) prints `color-singlet (0,0) content in the complement: 2 (0 = NO hypercharge room; matches memo 11)` — the "0" is a docstring intent, the number is 2, and both are RIGHT about different things (B1303 Q0: 2 zero-weight singlets in every embedding; centraliser 0 for B1140's principal embedding and 2 for fc's regular one). B1140's verdict stands with a scope clause; the print line remains a dead verdict string. **Fix rule (MB12's second half made mechanical, queued as a gate in v3.1 §7 (1)):** every printed verdict is an `assert`/PASS-FAIL on the number it stands beside; a receipt's exit code decides; a script that prints a NEGATIVE/ABSENT prints the positive-control line that could have been non-zero. Related: E39 (rc through a pipe), E67 (a control that varies the wrong thing), the relay's four "dead negatives" across three seats in one day. |
| E52 instance — B915's crossing solver was not solving the crossing (a comparison coupling held fixed in the first stage leaks into the answer; the second stage never re-solves the first equation) while the sealed MISS verdict stood | 2026-09-09 (B1304; found by the audit seat's R1, 2026-09-05; reproduced here with B915's own definitions: first-equation residual 0.0268 at the archived points, guess-dependence ~3·10⁻⁵, simultaneous solve differs) | The instrument's defect changes the curve by less than the miss: the seat's corrected solver gives d_min = 16.116 σ against 15.97 — the verdict survives, the instrument is retracted-keeping-the-computation (B915 addendum). **Fix rule:** a sequential root-finder over coupled equations must end with a residual check of EVERY equation at the final point (E39's "rc of the last step is not the rc of the chain", for solvers). |
| **E82 the ASSUMED-CONNECTED LINKAGE class** — a set of matching structures is declared *"all canonically linked"* (hence one fact, hence not evidence) when the links actually exhibited form a **proper subgraph**, leaving at least one member joined by nothing. The negative then over-reaches by exactly the missing edge, and closes a seam that is open | 2026-09-17 (xB005 Addendum 1, seat `xb`; the owner's instruction to reverify a same-day negative) | **Instance #1:** xB005's Q3 printed three links — `μ₃ ↔ ℚ(√−3)` (a group to a **field**, not an edge between two of the four ℤ/3s), `2T/Q₈ ↔ Z(E₆)` (McKay), `node ↔ 2T/Q₈` (its own Q2) — and concluded *"ALL FOUR ARE CANONICALLY LINKED … one fact wearing four hats"*. The edges connect `{node, 2T/Q₈, Z(E₆)}` only. Testing the missing edge: on m004's own tower `(LR)ⁿ` the invariant trace field is **constant** `ℚ(√−3)` while the node order runs 3, 3, **1**, 3 (`s961`, m004's 3-fold fibred cover, has `ℚ(√−3)` and node order 1), and 31 of 34 order-3 bundles in a 77-member census are not `ℚ(√−3)` — **the hats are two independent facts, not one wearing four.** The headline survived for a different reason (the second fact is worth 0.58 bits) but the reason was withdrawn, as was the arc's extension of I-17/B727 to a fourth hat. **Standing rule that catches it: A KILL NEEDS THE SAME MAP A PROMOTION NEEDS.** Write the link graph, list the edges you exhibited, check **connectivity** before writing "all N are linked"; an unlinked member is an **independent** hat and keeps its weight. **Why the class is new:** B1231's Identification Rule polices *"X here IS Y there"* and is gated and ledgered — **on promotions**. The same claim made to **discard** evidence passed unchecked, by the same seat, one cell after it correctly registered I-31 with an exhibited, acting map. Related: E26 (blind-projection negative), E70 (quantifier interchange), E33 (over-correction — the reason this addendum prices the correction DOWN rather than promoting it). |
| **E70 the QUANTIFIER-INTERCHANGE class** — a lemma proved about every ELEMENT (or every member of a family) is read as a theorem about the STRUCTURE those elements form (a stratum, a common fixed set, a whole class), where the quantifiers do not commute | 2026-09-09 (B1304; the audit seat's UPSTREAM_THIRD_AUDIT, 2026-09-07, its counterexample re-derived here exactly) | **Instance #1:** B1259 proved "every element of SO(7) has eigenvalue +1 ⇒ no element of a finite Ĝ ⊂ G₂ has an isolated fixed point" and concluded "Acharya–Witten isolation is unavailable in the entire class of flat G₂ orbifolds"; (ℤ/2)³ ⊂ G₂ (diagonal signs on the Fano coordinates) has every non-identity element fixing a 3-plane and the whole group fixing only the origin — an isolated maximal-isotropy stratum. B1259 scoped, not retracted (the element census and the pairwise lines stand). **Fix rule:** when a negative is stated for a class, write the quantifier string of the theorem and of the headline side by side before sealing; a headline with a ∀ the proof did not earn is E70. Related: E60 (the conclusion survived so the statement looked fine — here the physical conclusion may still hold, for reasons the lemma does not supply). |
| E53 instance #28 — B766's title ("time's arrow IS the golden branch") and S063's "the arrow of time enters exactly where det ≠ ±1" stood uncorrected for three weeks after B1083 proved the arrow was never a torsor bit | 2026-09-09 (raised by the session-relay seat's handoff of 2026-09-08, §3 "retitle, do not choose") | Dated correction banner on B766, a note on S063; the content of both arcs untouched (the rank-3 count; the four verbs). **Fix rule (R55-7 again):** a correction that changes what a word MEANS must reach every title that uses the word. |
| E54 instance (this bench, B1306 A) — "the figure-eight knot is named in none of the sixteen papers" and, a day earlier, "does not appear in the paper (the only 'Figure 8' is a figure caption)" — asserted from a text search whose patterns covered "figure-eight", "figure eight" and the ﬁ ligature but NOT the digit spelling; arXiv:1910.09966 §7 p. 29 reads "The complement of the ﬁgure 8 knot is one example of an arithmetic hyperbolic 3-manifold" | 2026-09-09 (B1306 B; found by the SM-derivation seat's own search on 2026-09-08, its prior-art document §1/§6 and the tower relay's third note, before B1306 A landed) | Corrected at source on seven surfaces (the dossier's Entry 5 twice, FINDINGS_A §6, the arc's claim, HARVEST rows 36/82, FRESH_EYES Q14, CAMPAIGN_STATUS); the verdict (KNOWN-ADJACENT) unchanged. **Fix rule:** an absence claim about a TEXT is a sweep only when its pattern set includes the digit and hyphen spellings and the ligatures of every term; the corrected pattern found the digit spelling in three of the sixteen papers. The seat speaks first: its correction is quoted in HARVEST row 86. |
| E53 instances #23–#27 — five corrections at source in one window, three of them recorded only in FINDINGS until Review 56 numbered them | 2026-09-09 (Review 56; the window B1294–B1307) | **#23** B1294/B1297 lacked the B1227 anchor for the amphichirality reading (the cloud's memo 170 found it; fixed B1305 A). **#24** B1305 A's forward list reproduced B1301's superseded even-level reading (fixed at source, B1306 A). **#25** MAIN_GOAL / CAMPAIGN_STATUS / B1293 / B1294 said "the SM gauge algebra is reached" without the rank-5 tree-level vacuum SM × U(1)_Z′ (fixed B1303). **#26** `THE_SM_VERDICT.md` carried neither B1294's sentence nor B1303's vacuum eight days on (found and repaired by Review 56, a currency banner). **#27** `MAIN_GOAL.md` JOIN 3 still called the E₆(−14)/E₆(−26) fork one "that no arc crosses" after B1265 derived it as a rank obstruction (the plan's Phase 4 (2); repaired by Review 56). **#10** (R55-8): a numbering gap with no recorded instance — retired by this note. **Fix rule unchanged (R55-7):** a correction is done when EVERY artifact carrying the claim is updated; the surface-propagation check is the next instrument arc. |
| E53 instance #29 — THEOREM_LEDGER's C2 row kept "There is NO F3 test … F3 is a citation to a test that does not exist" (dated 2026-08-09, re-stamped CURRENT 2026-08-19) for ten days after `tests/test_b1003_f2_f8_locks.py` (2026-08-30) locked all seven fork verdicts including F3's; the chain's own front page contradicted its lock file | 2026-09-09 (B1323, found while assessing whether C1–C3 are at their best shape) | Dated addendum on the C2 row (B1323) naming the lock; the row's content otherwise untouched; the C2 split (C2a/C2b) and the criterion census recorded in the same addendum | the class's mechanism again: a repair landed in a test file and never walked back to the surface that had declared the absence — the doc-currency stamp ("still CURRENT as of B1082") certified staleness rather than currency |
| E39 instance #3 — two tests (`test_b1266_sources.py`, `test_b1298_the_lift_fork.py`) and B1266's own selftest pinned the identification baseline at 12 unearned rows for a day after B1306 C and B1321 raised it to 14 (I-29, I-30); the targeted test subsets run at those landings did not include them | 2026-09-09 (found by the verification package's own lock run, `run_package.py --locks`, on its first day) | the tests now replay the baseline's audit trail instead of pinning a snapshot; the two new rows name the source each is an instance of (I-13, I-27), so the irreducible-source count stays 8 and the price 12; the package's lock set is now part of what a landing runs | the class's mechanism: a full-suite certificate was replaced by targeted subsets for a week of dense landings; the fix is not more care but the package, whose lock set caught it |
| E53 instance #30 — the paper's observer section still asserted "all 83 members of the family … are amphichiral, with no exceptions" seven days after B1235 retracted it (38 of 112 by the orientation-aware test) and after the paper's own SPEC marked it withdrawn; the sentence sat outside the appendix's claim list, so no lock touched it | 2026-09-09 (S9: two fresh-seat reads of the paper found it independently) | removed; a footnote records the withdrawal; the appendix preamble now says the claim list is editorial and that a lock once certified a withdrawn claim | the E53 #19 rule — retractions propagate by VALUE grep — was never run on the paper; the paper is now in the retraction sweep's scope (S9) |
| E52-adjacent, framing — the paper quoted the crossing's miss as "16σ" and sin²θ_W as "within about 1σ" where the denominator was a loop-truncation estimate (and the prose band in B915's FINDINGS contradicted its own `results.json` by a factor of six): against experiment the sin²θ_W coordinate is off by ~50σ | 2026-09-09 (S9: the physics and rigor reads) | the paper reports the discrepancies themselves (α_s 0.077 vs 0.118; sin²θ_W 0.2293 vs 0.2312) with the running assumptions and the precedents (Georgi–Quinn–Weinberg; Amaldi–de Boer–Fürstenau; Langacker–Luo); B915 carries a dated addendum | a σ whose denominator is not a variance is not a σ; report discrepancies and the error model separately |
| E66 instance (B1413, from the audit lane's R27) — B1335's irreducibility filter (`primescan.py`) tested for a common eigenvector with the SAME eigenvalue on every generator; an invariant line with different eigenvalues per generator (4 and 12 over F_13) passes it, so B1335's "in domain D" certificate certified a non-reductive module | 2026-09-15 | B1335's positive stands as an exact NON-SEMISIMPLE example (I = +1 over Q(u), semisimplification 0; recomputed on main in B1413); its domain claim is withdrawn to "outside the reductive domain"; the filter must test for an invariant line, not a shared eigenvalue | the enforcement narrower than its rule, on a domain certificate |
| **E71 the BRANCH-LOCAL-NUMBERING class** — an arc number is taken on a seat branch and collides with main's, because the seat's request (or main's grant) lives on a branch until a harvest and nothing counts the hours in between | 2026-09-09 (Review 56; R55-8 asked for the class) | **Instance #1:** the SM-derivation seat numbered B1267–B1276 while main banked B1267 and B1272–B1277 (resolved by `docs/SM_SEAT_ALIAS_TABLE.md`, 2026-09-06). **#2:** B1277 twice (main's leak closure; the seat's vacuum manifold). **#3:** B1302–B1304 twice on 2026-09-08 — the seat first by hours, main's numbering relay never seen (repaired B1306 A: RESERVED (1308, 1319), (1350, 1399); main's next B1320). **Fix rule:** a seat cites main's numbers as canonical and its own as `sm:`/`fc:`-prefixed aliases; reserved ranges are granted in the alias table, not by relay alone; the harvest gate (B1307) reports every seat push the day it lands, so a request cannot wait unseen for a harvest. **Instance 2026-09-16 (B1415): the class extends to LEAD labels — the SM seat registered L213–L215 on its branch while main registered L213–L215 (B1404/B1406) the day before; resolved by the alias table (sm:L213–L215) and by re-registering the two live ones as L220/L221; the fix that holds is the same: reserved lead ranges per seat.** |
| **E72 the ONE-SYMBOL-TWO-QUANTITIES class** — a symbol carries two *different numbers* in the record, defined by the same formula on different inputs, so a true statement about one is read as a statement about the other; distinct from **E1** (a choice made implicitly) because nothing was chosen — both definitions are correct and neither is wrong, and distinct from **E54** (absent-under-another-name) because here one *name* covers two *objects* | 2026-09-12 (B1344, found while verifying a forwarded seat report that had slipped on exactly this) | **Instance #1: κ = tr[a,b].** B309/B518/B1010 bank `κ − 2 = ω²`, `|κ − 2| = 1`, *"the UNIT obstruction, the founding sentence as an equation"* — that is the **knot group's meridian** pair. B416/B448/B1248 bank `κ = −2`, the parabolic commutator, *"the once-punctured-torus fibre of m004"* — that is the **fibre's** pair. Both are `tr[a,b]`; they are commutator traces of different generating pairs of different groups; `κ − 2` is `ω²` in the first and `−4` in the second. A forwarded report built a ℤ/3 reading on `κ − 2 = ω` and attached it to the fibre κ, where it is false; verified at B1344 by deriving the fibre point independently as the monodromy fixed point (`(2+ω, 1−ω, 1−ω)`, `x²−3x+3 = 0`, B448's own banked period-2 field). **Standing rule:** every arc that computes with κ names its **generating pair and its group** in the same sentence, and any cross-arc κ comparison states that both sides use the same pair before comparing. A symbol whose value depends on an unstated input is not an invariant.  **RESOLVED FROM THE LITERATURE SIDE (2026-09-14, B1404):** the fibre side is not a coincidence of presentation — `κ = −2` is the **defining condition** of Minsky's *punctured-torus groups* (Annals 149 (1999) 559–626: *"free two-generator Kleinian groups with parabolic commutator"*), the family the object's fibre group belongs to. So the two κ's are two κ's because they are **two groups**, one of which is in a named classified family and the other of which is **decidably not**: m004's meridian commutator has `|κ + 2| = 3.6055…`, so the knot group fails the family's definition. Verified exactly at B1404: B1344's own banked triple `(2+ω, 1−ω, 1−ω)` satisfies `x²+y²+z² = xyz` — it is **on the Markov surface**, which *is* the `κ = −2` locus — with the Fricke identity proved symbolically rather than recalled. The standing rule is unchanged and now has a reason: the two values are the family condition and a knot-group invariant, and no arithmetic relates them. |
| **E73 the COUNT-OVER-THE-WRONG-BASE-FIELD class** — a dimension is computed on a *coordinate representation* of the objects rather than in the space the objects actually live in, and the answer is read as a count of independent degrees of freedom. The inflation factor is the **degree of the field** the coordinates were taken over. Distinct from **E63** (a count inflated by a symmetry never quotiented out — there the excess elements are equivalent; here they are not even separate objects) and from **E39** (a stale baseline — the arithmetic here is correct, and correct about the wrong space) | 2026-09-13 (B1349 addendum 2; caught by the test written against the addendum, before the number reached a doc surface) | **Instance #1:** counting how many independent numbers the θ-even readout carries, the exact script represented each quadratic form's `ℚ(ζ₆₀)` entries as 16-vectors of power-basis coefficients and took the **rank over ℚ**, reporting `dim span = 6`. But a reading is a *real number*: `1` and `√5` are ℚ-independent and are both real scalars, so the ℚ-rank over-counts by the field degree. The honest quantity is `dim_ℝ span{Q_m}` in `Sym₄(ℝ)`, which is **3** (singular values `[4.11, 3.58, 2.45, 6.2e−16, …]`). The correction **reversed a verdict**: branch B of the R11 ledger went from `+2` (closes) to `−1` (fails). **Fix rule:** name the base field of every rank, dimension and count *in the same sentence as the number*, and state which field the **answer** lives in; when a computation is exact over a number field but the quantity graded is real, the rank is taken over ℝ and the exact field is used only to certify the entries. A count whose base field is unstated is not a count. Specific hazard for this corpus, which computes in `ℚ(ζ₆₀)` and reads answers as real couplings. |
| **E74 the PRE-REGISTERED-WRONG-RULE class** — pre-registration is adopted to stop *post-hoc* grading, and it does; but when the pre-registered **decision rule** is itself mis-derived, the same discipline converts a reasoning error into a **binding** one, and the honest habit ("follow the pre-registration") then enforces the wrong verdict. The mechanism that protects the outcome from the result protects the error too | 2026-09-13 (B1349 addendum 2; found when the computation's two branches were scored against the gate's own text) | **Instance #1:** `b1349d_outputs.py` pre-registered *"EAR-INDEPENDENT → outputs = 0, R11 can NEVER close."* That imported **AC4's** bench demotion (*ear-independent ⇒ non-discriminating ⇒ useless for a comparison*) into **R11**, which prices something else: *consumed* anchors. R11's anchor here is a **direction**; if the reading is direction-blind, no direction is selected and **the anchor is not consumed** — so ear-independence *helps* the ledger. Followed as written, the pre-registration would have declared the corpus's last licensed row dead on the branch where its arithmetic in fact closes. **Fix rule:** a pre-registered decision rule ships with its **derivation and the gate text it is derived from**, quoted; a rule that cannot be traced to gate text is **not sealed** and may be re-derived without it counting as post-hoc. Pre-registering an outcome map is only as good as the audit of the map. Related: **E66** (a gate narrower than its stated rule — there the *gate* is partial; here the *reader's paraphrase of the gate* is wrong), **E67** (a control pointing the wrong way). |
| **E75 the ROUNDED-CANONICAL-FORM class** — a set of points is deduplicated by rounding a canonical representative to a hashable key, and the resulting **count is wrong**, because rounding splits values that straddle a boundary and a pivot chosen by `argmax` **flips** between entries of nearly equal magnitude. The arithmetic on each point is fine; the *identification* of points is what breaks, so nothing looks numerically unstable. Distinct from ordinary precision loss: the failure is **combinatorial** (two names for one point, or one name for two), and it scales with the number of points compared, not with the conditioning of the computation | 2026-09-13 (B1349 addendum 3; diagnosed after the FOURTH instance, having been recorded three times as a bare symptom) | **Four instances, all in the same family, each caught by the same invariant:** B1348 withdrawn step 4 reported an orbit of **68**; B1349's first pass **370**, its addendum **423**; and the test written for addendum 3 reported **66** — none of which divides the group order 720, which the orbit–stabiliser theorem requires. **Fix, and it is not "use more digits":** deduplicate by a test of the actual equivalence relation, with no pivot and no boundary. For projective equality that is Cauchy–Schwarz — `[u] = [v] ⟺ |⟨u,v⟩| = ‖u‖‖v‖` — which has neither an index choice nor a rounding step; the float re-derivation then agrees with the exact one (two orbits of 48, split 4/4). Where the answer lives in a number field, canonicalise **in the field** (`b1349f_orbit_split.py` divides in `ℚ(ζ₆₀)` and needs no tolerance at all). **And the standing rule that made all four survivable:** every count that must satisfy a structural invariant **asserts** that invariant in the same function that produces it — `|orbit|` divides `|G|`, a dimension is ≤ the ambient one, a multiplicity sums to the total. This class is self-catching if and only if the invariant is asserted; unasserted, all four would have shipped as numbers. **Instance #5 and a SECOND MECHANISM (2026-09-13, B1401 addendum 2):** a Nielsen search keyed matrices with `mp.nstr(x, 14)`, which prints **significant** digits. Two copies of the *same* matrix — `a·b` and the meridian of m004, differing by `3.3e−16`, pivot agreeing — got **different keys**, because the near-zero entries printed their full noise mantissa (`-3.4e-82` against `1.1e-82`). The earlier **float** version had worked precisely because `np.round` rounds **absolutely**. **So raising the precision from float64 to 80 digits made the bug WORSE, not better** — more precision means more noise digits printed — which is the opposite of the intuitive cure and the reason this mechanism deserves naming beside the pivot one. Caught only because the search **failed its own control**; fixed by absolute rounding, after which the control passed and the search ball *shrank* from 8731 pairs to 1960, the excess having been duplicates under noise-distinct keys. **Rule, sharpened:** canonicalise by **absolute** tolerance, never by significant digits, and never by a pivot — and when a numerical fix makes things worse, suspect the key rather than the arithmetic. **Instances #6–#8 (2026-09-13, B1403), the class widened from KEYS to INPUTS:** a rationality test did `Fraction(float(x))` and compared at `1e-30`, so **no rational with a non-dyadic denominator could ever pass** — and the answer it was hunting was `13/12`; its controls used only `0, 1, 4, −1`, all exactly representable, so **a control that does not vary the thing which can break caught nothing** (E67's shape). Then the cusp shapes were read through Python `complex()` and tested at `1e-40`. Then **the control written to lock the fix was itself built at the ambient precision** and failed for the very reason it exists to catch. Adjacent and worth knowing: `mp.pslq` does `tol = to_fixed(tol, prec)` and asserts it non-zero, so a low ambient `mp.mp.dps` silently turns `1e-40` into `0` — the script passed and the test did not, for no reason visible in either. **Rule, widened:** the class is not only about canonical *keys* — it is about **any float standing where exactness is required**, the *input* included. When a high-precision test reports "no relation", suspect the precision of the input first; build a control's own values at the precision of the thing under test; and set numeric precision **explicitly**, because global numeric state is not a contract.  **Instance #9 (2026-09-14, B1404) — THE SAME FIX, ONE DAY LATER, IN THE CONTROL:** an exact identification of `κ_meridian` against the banked `ω²` **failed at a 5.0e−17 residual** while the measurement was correct at 60 digits. The comparison built its candidate as `complex(sp.N(2 + ω², 50))` — fifty digits computed, then truncated to sixteen by `complex()` — and compared at `1e-25`. The widened rule had been written the previous day and the very next arc reproduced the class **in the control**, which is where E67 says to look. Fixed twice: candidates built in mpmath at ambient precision, and SnapPy's output carried across by its own decimal **string** rather than through `complex()` (66 digits instead of 16). **The durable half of the fix is not numerical:** `κ − 2 = ω²` tests *which primitive cube root someone chose to call `ω`*, while `(κ−2)³ = 1` together with `(κ−2)² + (κ−2) + 1 = 0` tests the fact; the label-free form passes at `1e-60` and cannot be broken by a convention. **Rule added: when an exact identification needs a named constant, ask whether the name is part of the claim.** **Instance #10 and a THIRD MECHANISM — the class is not about floats (2026-09-14, B1404):** verifying that the m-th metallic mean is the m-th bundle's end invariant, **every even `m` failed a true statement**. The Galois-conjugacy step substituted `√(m²+4) ↦ −√(m²+4)` into the root, but `nsimplify` had already rewritten `(2+√8)/2` as `1+√2`, so for even `m` the substitution matched nothing and **silently did nothing** — a no-op that returns the *original* root and therefore fails the comparison. No rounding, no precision, no float: the same disease in **symbolic** clothing, *identifying an object by its printed representation instead of by a property*. Fixed by Vieta (`μ + μ̄ = m`, `μ·μ̄ = −1`) plus the minimal polynomial, both form-independent. **The class therefore has three mechanisms — rounded keys, float inputs, and `subs` on a non-canonical symbolic form — and one cause.** Corollary for review: a `subs` that silently matches nothing is indistinguishable from one that matched, so **assert that a substitution changed something** whenever its result is used as a different object.  **Instance #11 (same arc) — THE SAME REGEX RETURNED TWO DIFFERENT COUNTS DEPENDING ON THE PROCESS LOCALE:** the corpus-scale count of `κ = −2` used the bracket expression `[-−]2`. Outside a UTF-8 locale a bracket expression is **byte-oriented**, and the Unicode minus is **three bytes** in a one-byte slot, so the Greek-kappa branch cannot match at all. Measured: `LC_ALL=C` → **215**, `LC_ALL=C.UTF-8` → **442**, *same pattern, same corpus, same git*; the full-alternative form (`kappa ?= ?-2|kappa ?= ?−2|κ ?= ?-2|κ ?= ?−2`) returns **442 under every locale**. Nothing errors and the wrong number is entirely plausible — it was one edit from four documents. **Nastier still, it is invisible from where this corpus works:** Python sets `LC_CTYPE=C.UTF-8` for its children (PEP 538), so the script was right and the same command in a plain shell was wrong, which is how the discrepancy surfaced at all. **Fix:** no non-ASCII inside a bracket expression, and pin the locale explicitly rather than inherit it — global environment is not a contract, the same way global numeric precision is not (instances #6–#8). Locked by `test_guard_a_bracket_expression_makes_the_count_locale_dependent`, which asserts **both** answers. **Instance #12 (2026-09-14, B1406) — THIRD CONSECUTIVE ARC, and the class is now plainly not about floats:** the law `tr_even(RᵐLᵐ) = 0 ⟺ 3 ∤ m` came back **FALSE** on a complete 15-element check. The values were right and the **test** was wrong — it compared the `nsimplify`-ed *printed* form against `0` with `==` rather than testing `simplify(raw) == 0`, and an exact zero that has been rewritten does not compare equal to the integer `0`. Same shape as #10 (`subs` on a radical `nsimplify` had already rewritten) and #11 (a bracket expression read as bytes): **three mechanisms, one disease — testing a thing by how it is written.** The class's fix rule, in its final form: *identify by property, never by representation* — `simplify(x − y) == 0`, Vieta, a minimal polynomial, a Cauchy–Schwarz equality, an asserted structural invariant. Any comparison that could be changed by re-printing the same object is not a comparison. **RE-FIRING, and the ledger worked (2026-09-14, B1409):** instances #6–#8's mechanism — `mp.pslq` does `tol = to_fixed(tol, prec)` and **asserts it non-zero**, so a low *ambient* `mp.mp.dps` turns a `1e-35` tolerance into `0` and the call dies inside mpmath — reappeared in a new context: B1333's modules set `mp.dps` **at import time** and depend on the order they are imported in, and under **pytest** that order differs from a standalone run. The same computation passed from a script and failed from a test file, 8 of 10 tests dying in 0.94 s inside `mpmath/identification.py:158` rather than in any mathematics. **The banked fix applied unchanged** — pin `mp.mp.dps` explicitly in the driver and **re-assert it before every recognition** — — and then the **guard written to lock it failed too, for the same reason at one more remove**: it asserted `mp.mp.dps >= 50`, i.e. the ambient **value**, and a test that ran earlier had already lowered it. Rewritten as a **property**: set the ambient precision deliberately *too low*, run the recognition, and assert it still returns the right answer and restores the precision — which tests that the fix works rather than that nobody disturbed the global. **Three times in one file, at three removes — the measurement, the fix, and the guard on the fix — and the same rule ends all three: identify by property, never by representation, and global state is never a contract.** Recorded as **evidence the class is correctly stated**: the fix written for one context transferred to another with no rederivation. |
| **E76 the UNPRICED-SETTING class** — an input ledger prices the quantity the question is *about* and leaves the question's **setting** unpriced, because a setting reads as background rather than as a choice. The omission is invisible from inside the arc, since every table is internally consistent; and it is **systematically favourable**, because the setting was chosen to make the question askable. Distinct from **E73** (a count taken over the wrong base field — there the arithmetic is in the wrong frame; here an entire term is absent) and from **E1** (a choice made implicitly — here the choice is explicit and simply not billed) | 2026-09-13 (B1349 addendum 5; found by asking which *word* the row would be read at, after the arc had already priced the ear) | **Instance #1:** B1349 addenda 2–4 priced R11's **ear** anchor (`log₂` of the Galois-rational directions) and treated the **word** `RᵐLᵐ` as given. It cannot be given as the object's own: `gcd(1,15) = 1`, so the **golden** word — which **B997** proves is the *unique* metallic grammar whose own-conductor shadow is a McKay group — falls on the ear-**dependent** branch that is dead on two independent gates. Branch A therefore requires *departing* from the object's grammar, at `log₂7 ≈ 2.81` bits to name a non-unit, and with that term restored branch A closes on **one** reading of four rather than the **four of six** the addenda claimed. The claim "four of six" is withdrawn; the mathematics (the `gcd(m,15)` law, the exact spectra, the field statement, the C6 containment, the 4/4 split) is untouched. **Fix rule:** before totalling an input ledger, list **every** quantity that had to be fixed for the reading to exist — the observable, the direction, the word, the frame, the normalisation — and write `free`, with its reason, beside each one that is not billed. An anchor omitted silently is an anchor claimed as zero.  **Instance #2, and it widens the class from OMITTED to MIS-TYPED (2026-09-14, B1405):** having restored the word anchor, addendum 5 then priced it as a **label** — `log₂7 ≈ 2.81` bits *to name a non-unit residue of ℤ/15* — and branch A's surviving reading closed by `+1.19`. But `ord(R) = ord(L) = 15`, so the SU(3)₂ readout is a function of **m mod 15** while the **manifold is not**: verified exactly, `weld(1) = weld(16)` and `weld(3) = weld(18)`, while `vol(b++R¹L¹) = 2.0299` (m004) against `vol(b++R¹⁶L¹⁶) = 7.1769`. **Naming a residue and naming a manifold are different acts**, and every selection principle the programme has — minimal volume, minimal systole, Jørgensen extremality, B997's McKay uniqueness, the existence of a conductor — is about the **manifold**, and every one of them outputs `m = 1`, a **unit**, i.e. the *other* branch. Priced as the departure it actually is rather than as a label, the same reading runs `4 − 17.70 = −13.70` bits. **The anchor was the right line at the wrong type**, which is invisible to the fix rule as first written (*list every quantity that had to be fixed*) because the quantity **was** listed. **Fix rule extended:** beside each anchor, name **which object the choice ranges over**, and check that it is the object the row is about. An anchor that prices the wrong choice is worse than a missing one, because it looks paid. Related: **E72** one level up — there one *symbol* carried two quantities, here one *anchor* prices two different choices. |
| **E77 the PAIRED-BUT-NOT-LIVE class** — a gate enforces that two artefacts move *together* and therefore cannot detect that **neither moves at all**. The rule is correctly coded and correctly stated; it is the rule itself that has no **liveness** requirement, so the gate is greenest exactly when the thing it protects is most neglected. Distinct from **E66** (a predicate narrower — or broader — than its docstring: there the code and the stated rule disagree; here they agree and the rule is insufficient) and from **E57** (a lock whose tool is missing: there something is absent; here nothing is absent, only stale) | 2026-09-13 (found while auditing whether a returning seat could reconstruct the current state) | **Instance #1:** `gate_log_changelog_paired` enforces that `CHANGELOG.md` may not run more than one commit ahead of `PROGRESS_LOG.md`. Both stopped at **B1324 (2026-09-09)** while **25 arcs landed** — B1325–B1349 with their addenda, then B1400 and B1401 — and the gate stayed **green for the whole window**, because zero and zero differ by zero. The narrative surfaces a returning seat reads first were the *only* ones stale; every ledger (`THE_CHAIN_STATUS`, `KIND_TABLE`, `REPRESENTATION_TRIAGE`, `ERROR_LEDGER`, the alias table) was current, so no other check noticed. **Fix rule:** a pairing gate needs a **liveness** clause — compare the logs' newest entry against the newest banked arc id (or the commit count since either was touched) and fail on lag, which is exactly what `doc-currency` already does for other surfaces and what makes its noisy declared debts worth keeping. Generally: *a relative check between two artefacts is not a check on either.* |
| **E78 the SELF-INCLUDING MEASUREMENT class** — a measurement *of* a corpus is written back *into* that corpus, so re-running it afterwards returns the post-write state and **confirms whatever was just written**. The instrument is correct, the arithmetic is correct, and the answer is circular. Distinct from **E39** (a stale baseline — there the number is old; here it is perfectly fresh and self-referential) and from **E63** (a count inflated by an un-quotiented symmetry — there the excess is real objects; here the excess is the report itself). Specific to self-documenting corpora, which is what this one is | 2026-09-14 (B1404, twice in one arc) | **Instance #1:** the absence audit grepped `frontier/` for *Minsky*, *Marden*, *Maskit*, *Bromberg* — from a script living in `frontier/B1404_…/` that names all four in its own `TERMS` list. Every one came back **present**, at 1–4 hits, all of them the script. **Instance #2, after the first was fixed:** run again once the arc's prose had reached eight doc surfaces (`ERROR_LEDGER`, `THEOREM_REGISTRY`, `NOVELTY_AUDIT`, `THE_CHAIN_STATUS`, `OPEN_LEADS`, `RELAY_LEDGER`, `CHANGELOG`, `PROGRESS_LOG`), `GENUINELY ABSENT` came back **empty** — the six absent names were now 'present', in the files saying they were absent. **Fix:** pin every corpus measurement to a **revision that predates the write** (`git grep <rev>`), and record that revision beside the number. A count of a corpus without a revision is not reproducible and may not even be about the corpus. **Wider reading:** this is what makes a self-documenting corpus expensive — every ledger is both instrument and specimen, so `doc-currency`, the alias table and the absence audits all measure a body their own output has already changed. |
| **E79 the FALSE OWNER-CALL class** — a question the **record already answers** is escalated to the owner as a judgement. It is not a refusal and it does not look like an error: it looks like deference, and it produces a well-formed question that simply never resolves. The cost is specific — **the owner cannot know the question is decidable without redoing the work**, so the item sits in the leads file indefinitely while the answer is already written on a surface nobody re-read. Distinct from **E76** (an anchor priced at the wrong type — there a number is wrong; here no number is computed at all) and from a **genuine** owner-call, which is about *resources and exposure*: what to spend effort on, what to publish, what to risk | 2026-09-14 (B1407, caught by the owner's own push: *"it should not depend on me but on math"*) | **Instance #1:** B1405 established that L209 turns on *"is the row about the object, or about a residue class?"*, and this bench handed that to the owner as a fork with two priced horns. **The row's spec had been written for weeks, in three places** — `KIND_TABLE.md:102` (*"this row's one shot, with **zero anchors consumed**"*) and `CAMPAIGN_STATUS.md` at lines 994 and 1038 — **one of them the very table the row lives in**. With the zero-anchor clause read, branch A is unlicensed by construction and there is no fork: L209 closes negative, and the further finding (the spec-meeting observable reads an already-benched, non-discriminating value) follows in one afternoon. **Rule: before calling anything an owner's call, grep the record for the thing's SPECIFICATION.** A question is the owner's only when the answer depends on what they want, not on what is written. Corollary for this corpus specifically: it is heavily self-documenting, so *"nobody has decided"* is very often *"nobody has re-read"*. |
| **E80 the SCRATCHPAD-ROOTED VERIFICATION class** — a banked arc's verification directory is committed, complete and correct, and **not runnable from a clean checkout**, because its scripts reach outside the repository by absolute path for the data they were run against. The arc's own logs are genuine, its numbers are genuine, and nobody can reproduce them without re-deriving the input the scripts assume is already sitting on disk. Distinct from **E57** (a lock whose tool is missing — there something is absent from the repo; here everything is present and points outward) and from **L210**'s instance (tracked tests reading untracked artifacts — there the *test* is the problem; here it is the verification itself) | 2026-09-14 (B1409, found by a design pass over B1333's machinery before reusing it) | **Instance #1:** fifteen of B1333's committed scripts open with `sys.path.insert(0,'/tmp/sweep')` and read `/tmp/sweep/covcache.json` by absolute path — the cache of 54 chiral multi-cusped covers recognised exactly over `ℚ(ζ₁₂)`, which is the input to every one of its 38 070 sectors. `/tmp` does not survive a container, so the directory that certifies B1333's result cannot re-run it. B1409 was unaffected only because it calls `exact_rho` and **recomputes** the recognition rather than reading the cache. **Fix rule:** a verification directory resolves every path **relative to itself** or to the repository root, and any cache it depends on is either committed beside it or regenerated by a script that is. A result whose reproduction requires a file the repo does not contain is banked on trust, not on the artifact — which is the one thing this corpus's whole apparatus exists to avoid. |
| **E81 the UNCANONIZED-ISOMETRY-LIST class** — the isometries of a hyperbolic manifold are enumerated as the combinatorial automorphisms of the triangulation **as given** (SnapPy's `isomorphisms_to(M)`), not of the canonical triangulation (`is_isometric_to(M, return_isometries=True)`, `symmetry_group()`), so the list is a **subgroup** of the isometry group and every census-scale statement built on it — a symmetry-group order, an amphichirality verdict, a fixed-point multiset, a class membership — can be an **undercount** with no error raised. On SnapPy's census triangulations the two lists differ on 853 of the first 4 000 manifolds. The lesson had been learned once (B1239's docstring, 2026-09-02) and lived in a docstring, not here | 2026-09-16 (B1414, the outside bench's memo 231 cell 3 recomputed on canonical triangulations) | **Instance #1 (main):** B1321 swept 61 911 census manifolds with the uncanonized list and found six members of the three-line class to nine tetrahedra; there are **nine** (t10829, t12582, o9_42897 missed — their census triangulations show 2, 1, 1 of 12, 6, 6 isometries); the FAIL branch survives on all nine (B1321 addendum). **Instance #2 (the bench):** memo 231 cell 3's "89.88 % attain 4" is 99.90 % on canonical triangulations; its cell 6 scored m412 on 4 of 8 isometries (values unchanged). **Instance #3 (main, harmless):** B1302 saw 6 of 12 on m202 and read the missing six as cusp-swapping. **Not instances, checked:** B1295 (own propagation count), B1320, B1324 (canonical call). The one-line fix: never call `isomorphisms_to` for isometries; the lock in `tests/test_b1414_outside_bench_harvest.py` fires on t10829 | canonize first, or use `symmetry_group()`; a census sweep that reports symmetry-derived quantities must state which call it used |
| **E58 instance (this bench, B1349 addendum 2)** — the class extended from *someone else's* summary to **my own paraphrase**: `b1349d_outputs.py` graded B856/B641's ear-independence from my recollection ("the five tones are ear-independent") instead of their artifacts, and so measured **complex `h`** across ears where B856 line 124 grades `h(...).real` and B641 grades `mp.re(A/ζ)`, over **real** ears, on the **charge-conjugation-welded** word `C·RᵐLᵐ` rather than `RᵐLᵐ`. The control failed and was briefly read as *refuting* B856. Measured, not assumed, which deviation mattered: dropping `C` merely **negates** `λ` (`Re h` stays scalar), so the real bug was complex-vs-real — at `m=1` on the odd sector `spread|h| = 8.5e−01` while `spread Re h = 1.1e−16`. The missing weld did one visible thing: it flipped `h(5)` from B856's `−1` to `+1`. **Rule restated with the wider scope:** a named finding is graded from its own artifact — and *"my own memory of it"* is a relay like any other. | 2026-09-13 | Withdrawn as an instrument in B1349 addendum 2 §0(a); the diagnosis is pinned by `tests/test_b1349_ear_law.py::test_withdrawn_instrument_the_bug_was_complex_vs_real`, which asserts both halves (C negates, complex-vs-real breaks). |
| **E58 instance (this bench, B1345)** — a REFUTATION of another seat's named finding, graded from a transcript PARAPHRASE while the seat's own artifact was available on request; the paraphrase had inverted the finding's conclusion | 2026-09-12 (B1345 addendum, on receipt of the full handoff the same day) | The seat's **F6** says `I = -(a₂-a₂*)` is a **partial sum of an identically-vanishing alternating difference**, therefore *“no index theorem stands behind I; nothing forces its value either way”* — which is **B1335's own statement from the other side**, and F6 cites B1329's *“the identities leave I free”*, this bench's line. The transcript's compressed sentence (*“the index **vanishes because** …”*) read as an inference to `I = 0`, which is a non-sequitur and is what B1335 contradicts — but F6 is not that sentence. Refutation withdrawn; the amphichirality refutation in the same arc is unaffected (computed directly). Aggravating: the handoff's README fences its own work explicitly (*“assume the un-gated results are known until checked”*, a section listing what is NOT verified) and ships runnable checks. **Standing rule:** a refutation of a named finding is graded from that finding's own artifact, never from a relay, transcript or summary; absent the artifact the disposition is **“not assessed”**, not “refuted”. Distinct from E53 (a correction that fails to reach every surface) and from E68 (a quotation with no source at all): here the source existed, was accurate, and was not read. |
| **E53 instances #13–#14, plus the class read at INDEX level (B921-9b)** — three unpropagated corrections found by the sweep and the retraction audit, aged **25**, **88** and up to **80** days | 2026-09-14 (R57-3 second pass + B921-9b) | **#13 — `L144`, 25 days.** Door 5 was CLOSED 2026-08-20 (B1096) and the closure reached **three** surfaces — `THE_LADDER` rung X30, `THEOREM_LEDGER:395`, `SM_SPECIFICATION_LEDGER:275` (*"the door is shut structurally … this closes L144"*) — and **not `docs/OPEN_LEADS.md`**, which is the one surface a reader consults to ask what is open. The lead kept two load-bearing facts that the closure had falsified: *"No arc's `depends_on` cites B167"* (**B1241, B1248, B1249 all do**) and *"Nothing has tested it since it was written"* (**B1096 did**). **#14 — `B171`, 88 days.** Its `arc_verdict.json` was never touched after **B172** *"Answers the question B171 opened … affirmatively, hedged"* and **B173** certified the gap-label group at rank 3 by PSLQ, so **two generated views** (`THE_SPINE`, `VERDICT_LEDGER`) rendered a settled question as live — both faithful to a stale source, which is why no view-level check could ever have seen it. **The INDEX-level reading (B921-9b), and it is the sharper one:** seven of the twelve `RETRACTED` arcs had **no row in `docs/RETRACTIONS.md` at all**. Each was corrected properly *in its own file*, at the time — so this is not E53's usual "some surfaces and not others" but its limit case, **the correction reaching exactly one surface, its own**. It compounds, because `RETRACTED_PHRASES` is populated **from** that index: an unwritten row also means the retracted wording is never swept for. **What all three say together:** every instance is *unpropagated truth* — a closure or correction that is correct everywhere it landed — so no content-reading gate can see any of them, and the ages (25, 88, ≤80 days) show staleness does **not** accumulate per window. | (1) The two repairs made at source, the B1241/B167 way: a banner on `L144` with the body left standing and its false facts marked, and a qualifier appended to B171's `claim_one_line` so both generated views carry it. (2) **`scripts/checks/retraction_debt.py`**, wired as the `retraction-debt` gate: every `RETRACTED` verdict is NAMED in `RETRACTIONS.md` on a line carrying a retraction verb — the **existence** check that `retraction-sweep`, which polices **content**, is right not to perform. Shown reporting **7** before the rows and **0** after (MB12). (3) `open_claim_sweep.py` confirmed at **review cadence, not per-window**, on the finished 12-of-12 evidence rather than the first two instances. | R57-3; B921-9b; L220 (all twelve adjudications, each with the command that decided it) |
| **THE PROXY CLASS, instance #4 — an adjudication asserted from the instrument's excerpt** (the synthesis of E72–E80 fired again, and this time was caught before it shipped) | 2026-09-14 (R57-3, self-caught during the second pass) | The first pass reported *"7 of 12 candidates read — 3 stale, 4 accurate."* **Six were read.** The fourth "accurate" — the Review-37 scoped-stale note — was judged from **its line in `open_claim_sweep.py`'s output, which prints the first 150 characters of a claim**, and the document was never opened. The reading happened to be right; the *adjudication* did not happen. This is the window's nine classes in their most compact form — **a proxy checked in place of the thing it stands for** — after the wall-audit premise (a correction assumed unpropagated), the contaminated bootstrap (`/tmp/sweep` pre-populated by an earlier run) and the branch audit (full refs compared against leaf keys). **Fourth instance in two passes; the first the practice caught BEFORE the report shipped rather than after.** Two more of the same shape fell out of the same pass, both mechanical: `gate_views_generated` ran only `generate.py` and never `spine.py`, so **`THE_SPINE.md` had drifted to 1127 locks against the 1216 its generator produces** — inside the very directory the gate scans, under a header reading *"Regenerated with the views"*; and `NOVELTY_AUDIT.md` carried **two `## R5` headings** because nothing checks a round label for collision. **INSTANCE #5, same day, same shape, also caught pre-ship:** B921-9b's fourteen retraction quotes were grepped in their sources **before the rows were written**, and writing them broke three — a backtick added inside `M.is_isometric_to(mirror)`, an emphasis marker dropped from `only the *conclusion* is`, a `"gate"` re-typed with single quotes. All three still *read* right; none was the source's text. **Verifying the intended quote is not verifying the shipped one.** Found by re-extracting every quote from the shipped file and re-grepping: the audit also had to resolve each arc by its verdict file's `id`, because a directory glob picks `frontier/B58_stage1` (NEGATIVE) over `frontier/B58_sl4_tower_test` — the grandfathered B58 collision, a second proxy inside the checker written to catch the first. 16 of 16 verbatim after repair. | **Name the artifact you actually opened, and verify the artifact you actually shipped.** An adjudication cites the command that decided it and the file it read — never the search hit that surfaced it; and a quotation is re-checked **from the file as written**, not from the draft that preceded it. Codified where it bites: `L220`'s twelve rows each carry their deciding command. And, mechanically: every generator that writes into a checked directory is listed in the check that reads it (`views-generated` now runs both). | R57-3; L220; `docs/PRACTICES.md` (the retraction-debt section states the general form) |
| **E54 instance (this bench, B1410 → Addendum 1): "STILL OWED" IS AN ABSENCE CLAIM AND WAS NOT SWEPT** — an arc sealed, banked and pushed on a debt that had been paid six weeks earlier | 2026-09-15 (caught the next day, by the seat, while planning the work the wrong claim implied) | B1410's headline was *"H5's falsifier is priced, and it costs one owed definition"*, and its `POINT_LEDGER` row P1 named B861's registerability definition as the blocker. **It was paid at `B871_registering_datum` on 2026-08-03** — PROVED, never superseded, headline *"the cascade's last owed definition is paid"* — with four legs verified on all eight cascade menu rows, and `B994` independently records the same (*"B871 paying the owed definition"*). **`already_banked.py --wide` was run twice before sealing and never on the load-bearing claim**; run afterwards on it, B871 returns **third**. **Why the class did not fire:** the ABSENCE RULE triggers on *"X does not exist"* and this was *"X is still owed"* — the same assertion in the future tense, and the rule did not say so. **Why the record misled:** a debt's own entry lives in the arc that INCURRED it (`B861/FINDINGS.md:61`), and that arc has no reason to be updated when the debt is paid elsewhere — so **a debt's own record is structurally the corpus's most likely stale surface**, and E53 pointed at the file a reader would most trust. **What did NOT change:** row P1's verdict. It was `UNDECIDED` and stays `UNDECIDED`; a re-run after the correction returns the same four verdicts. Only the blocker's name moved — and the true blocker is **stronger**: B994's THE LIMIT, *"the endpoint is forced by an input the object does not have"*, with **chirality** proved absent twice (`B713` NEGATIVE, `B760` NEGATIVE) and **E₆** bought by the field (`B993`). **So the arc's conclusion survived its own false premise**, which is the sharpest reason to correct it rather than let it stand. | **THE ABSENCE RULE, WIDENED** (`docs/PRACTICES.md`, 2026-09-15): *"X is still owed" / "X remains open" / "nothing has done X" are absence claims and the sweep is owed before any of them is written* — and the sweep runs on **the claim the arc rests on**, not on the claim the arc is about. | B1410 Addendum 1; B871; B994; B861:61 (banner added same pass) |
