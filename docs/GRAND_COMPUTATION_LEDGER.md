# THE GRAND COMPUTATION LEDGER — the universe in its own meter: the missing-items register

**Created 2026-08-28 (B1188, owner-directed): "no superficial observer work — craft a plan and a
campaign and close this forever."** The observer stops being a terminal verdict and becomes a
computed part of the program. The target: **THE GRAND COMPUTATION — the universe expressed in the
object's OWN units**, with the bridge from its units to ours deferred as a separate, later, easier
task (one external calibration; "one measurement then predicts the rest," banked since B532/B535).

**The doctrine**: "observer-supplied" means *supplied by something whose construction is the
program's next theorem*. Phase 1 (the interface) was already done: WHAT must be supplied (B1168's
parity law), WHERE (B1182 leg-by-leg), AT WHAT COST (B1183's one ℤ/2 + one scale + finite labels),
and the first genesis theorem: **the observer cannot be the object itself** (B1183+B1184,
self-naming without self-signing). Phase 0 (this file's evidence base) was the six-lens full-repo
retrieval sweep (B1188) — its headline: **most of the observer theory already exists and was
off-surface** (the apparatus B723; the rank-exactly-3 measurement menu B766 saturating B733; the
emergence map B717; the Born-form theorem B725; the cut B1091; the freedom count B1164), and the
instruments were object-biased by measurement (B985: object-faces recovered 79–100%, relation-faces
6–19% — *"our instruments hold objects and relations fall through them"*).

**Row schema**: what the grand computation needs it for · **STATUS** ∈ {**PROVED** (arc) /
**PARTIAL** (gap named) / **MISSING** (cell spec'd) / **IMPOSSIBLE-BY-THEOREM** (the wall)} · the
close. Update rule: every bank touching a row updates it, same PR.

---

## A. THE ARENA (space, dimensionality, causal structure)

| item | status | the close |
|---|---|---|
| A1. dimension from the object | **THE LAW FOUND (B1189/GC-4)**: the DEFICIT of transitive reach survives degree-preserving nulls and DEEPENS to z≈49–59; localized (81% = the two longest jumps into the terminal shell); **the charpoly-LOCALITY law** — edges are local in class space, segregating descendant cones (ρ = −0.61, p<1e-16); combinatorial characterization, NOT a spacetime claim; B1104-C3-as-content still unposed | B1190/GC-10: the replication theorem verified (canonical non-orientable 4d object, double cover M×S¹ — 4d costs NO new bit beyond c) but the payoff degenerates (finite tick ⇒ trivial product); the locality law's exact-derivation tier |
| A2. causal order | **PARTIAL** — the four verbs carry four causal types; only the evolution verb yields a proper (1,3) cone (B523, buried); Malament inapplicable to the monoid | type: is the evolution-verb cone the object's causal order, relationally completable? |
| A3. 4d filling | **IMPOSSIBLE-BY-THEOREM as canonical** — Ω₃^SO=0: fillings exist, infinitely non-unique, nothing selects (B716) | stands; the filling is a closer-choice (a LABEL row) |

## B. THE CLOCK (internal time + the arrow)

| item | status | the close |
|---|---|---|
| B1. the tick | **ADJUDICATED (B1189/GC-3): ONE generator A₁ = RL, THREE operations** (representation depth-grading / square-root covering M²=RL / congruence reduction) — a typed hierarchy, no contest; B293 ≡ memo-51 JOINED as complementary (Poisson vs Jordan on the same (μ,λ); one named gap). Was: three candidates: (i) the meridian's nilpotency-3 depth grading — the only one doing physical work (the Yukawa reads it: 7 allowed / 11 forbidden blocks, B1149/B1150); (ii) the Breath ℤ/2 double-tick (det M=−1, M²=RL — "the object IS the double tick", B1083); (iii) the conductor-clock law clock(κ)=ord(A₁ mod 3κ) exact (B596/B656). Orphans: B448's period-2 cocycle heartbeat; B530's return-word pulse (beats in ratio √φ). **B293 ≡ B1149-memo-51 (the peripheral symplectic clock) discovered twice, 14 months apart, never joined** | GC-3: adjudicate the three + join B293/B1149 + type the order-2 beat vs order-∞ tick as two time-scales (B1147 memo 31) |
| B2. the arrow | **PARTIAL + a live contradiction**: typed finite-place (B1182: r→k7, finite orbits); B721: complementary clocks by theorem; B723 probe 2: the weight = the clock-supplier. **The B766-vs-B1083 contradiction RECONCILED (B1189/GC-3): compatible — two different invariants on two different objects (the OBSERVER menu's Galois choice vs the OBJECT torsor's monoid symmetry)** | GC-3 (the reconciliation is part of the clock cell); OG-2: does a grammar-internal finite system's orbit BE k7 (candidates: B552's conserved ℤ/11 charge along the observer flow; the trit systems) |
| B3. no beginning | **PROVED** — the fibration base is compact S¹, no infinite past (B716); two-headed time at the spectrum level (S002-corrected) | stands as structure |

## C. THE METER (the internal unit)

| item | status | the close |
|---|---|---|
| C1. the unit | **PROVED (B1188 + B1189/GC-1): THE DISCRETE LADDER, adversarially verified** — independent recompute zero-failures; 24-manifold wide bite; the mechanism EXACT: Vol(m004) = 12·Vol_orb (4.3e-50), V_reg = 6·Vol_orb; Borel rank-1 gives rationality; **AND the lattice is DEEPER than commensurability — 11 members provably NOT arithmetic (incl. t06829) yet on-lattice**; named open sharpness: only multiples of 3·Vol_orb occur. Was: THE DISCRETE LADDER — all 112 family members sit at exact half-integer multiples of Vol(m004) (none fail, 1e-30 at 50 dps); in **V_reg = Vol(m004)/2** (one regular ideal tetrahedron) every member is an INTEGER ∈ {2,4,5,6,7,8,9,10}; even the den-98-shape member lands on the lattice. Consistent with B762's early sample (2, 2.5, 3). Mechanism candidate: B(ℚ(√−3)) rank 1 (one complex place) ⇒ ℚ-multiples; the INTEGER lattice is the sharp observed structure | GC-1: adversarial verify (two-sided bite control: non-family manifolds must fail) + the Bloch-group mechanism + scope statement |
| C2. scale as relation | **ANSWERED-SHARP (B1189/GC-1)**: NOT one class — 11/35 non-regular members fail Maclachlan–Reid (exact non-integral traces); the ladder unifies ACROSS classes | the multiples-of-3 sharpness + the 24 unrefuted members' full trace-ring check (Sage tier) |
| C3. the ℝ⁺ leg's residue | **PROVED architecture**: S = −Vol·σ with ZERO free dimensionless constants (B1088); σ = the ONE priced ℝ⁺ closing; the ANCHOR DECLARATION (B1015, sealed): A1 = ℓ ("nobody derives the metre" — external BY DESIGN), A2 = c = 6σ; **B1025's floor: ONE unit + TWO bits + acceptances — no continuous dimensionless input remains** | stands; see C4 |
| C4. **THE BURIED PRIZE — L154 (σ = 1)** | **PARTIAL, one named gap**: c_BH = 6σ and c((E₆)₁) = 78/13 = 6 both exact; map-hunt NO-EXHIBIT; no-clash adjudicated (B1034) — **the missing piece is ONE bridge** (identify the stage CFT with the AdS₃ boundary CFT of the object's own quotient). If it lands, **A2 converts from anchor to OUTPUT and the input list shrinks to ℓ + bits** | **Batch-2 state (B1190/GC-6)**: the bridge attempt's arithmetic verified (both sides exact; the (E₆)₁ characters lens-rebuilt) but the fingerprint synthesis REFUTED (kind error) — L154 stays one-bridge-missing; the typed instrument for the next attempt: survey ALL banked series + establish the prefactor↔weight kind-map; the Cardy 6-vs-1 reading (six cusp-boson units needed, one supplied) = what any bridge must supply. + the G_N normalization check; B167's doors |

## D. THE CONTENT (spectrum, charges, generations)

| item | status | the close |
|---|---|---|
| D1. gauge/matter structure | **PROVED** — E₆ spine, hypercharge content, unique breaking chain, one generation per 27 (B1160/61/62); the measurement that registers it is DEFINED (B871's chirality-registering datum); two measurements take E₆ to exactly su(3)⊕su(2)⊕u(1)³ (B892) | stands |
| D2. generation count 3 | **PARTIAL** — NULL on-object; external (E8 fence). **Off-surface tension: B891 (sealed, passed): a SINGLE observer registers three pairwise-distinguishable matter sectors on the one 27** — either weakens the wall or needs its fence restated | B1190/GC-8: the no-tension reading (internal thirds, not generations) plausible but mechanism-unproven — the small cell stays open; census-31-calls |
| D3. the intermediate closure | **MISSING, precisely posed** (B956, buried): does the E₆ rep variety contain non-abelian PROPER points with commutant rank exactly 4 (the SM sits strictly between the two computed regimes)? Wilson/Hosotani mechanism-space unworked | **B1190/GC-9: rank-4 EXISTS** — su(3)⊕su(3) and so(7)⊕u(1) exhibited from the raw bracket; the complex-27 clause de-graded (instrument refuted) — apply B1100's complex-witness to the two centralizers |

## E. THE COUPLINGS (the dimensionless numbers)

| item | status | the close |
|---|---|---|
| E1. values from the object alone | **IMPOSSIBLE-BY-THEOREM** — V-3 (B1126): 16 periods × 22 targets, no object period is an SM ratio; B1129: natural values disjoint; ancestry B533 | **the re-frame: values = invariants of the OBJECT+CLOSER pair** — the coupling is where values live (banked reorientation; the crossing machinery below) |
| E2. Yukawa shapes | **PARTIAL** — three distinct mechanisms (B1185); 𝒯 evaluator commissioned (codex R023); THE SKEW ZERO exact | 𝒯 lands → first computed coupling shape in own units |
| E3. Born rule | **PROVED (form) / IMPOSSIBLE (content, as object)**: form + weights NATIVE (the object's two quadratics; quadratic degree = c-swap's Galois order 2, B725); amplitudes/phase/associator = imported golden-MTC overlays (B726/28/29); the substitution IS the Fibonacci fusion rule by identity (B483) | stands — quantum CONTENT is closer-side by theorem |

## F. THE SIGN (the one ℤ/2)

| item | status | the close |
|---|---|---|
| F1. self-supply | **IMPOSSIBLE-BY-THEOREM** (B760 NO-HATCH; B1183 one-class; B1163's escape clause names the only door: *"add a non-invariant structure the mirror breaks — which is precisely to extend what 'the object' is"*) | stands — forces the closer |
| F2. relational supply | **NEGATIVE for self-coupling (B1189/GC-2, the crown): THE GOLDEN-UNIT KILLER** — the twisted double carries a genuine pair-datum (H₁ ℤ/5) but it is mirror-EVEN: centralizer(A) = ±φⁿ with N(φ) = −1 ⇒ both-sign conjugators ⇒ mirror-self-equivalent; two-sided control bites (the norm-+1 √3-unit matrix has a single-signed coset). **The residual door, cell-named: a HETEROGENEOUS coupling whose relevant unit has norm +1** (e.g. the √3 side — the Eisenstein field's real quadratic neighbor). Was: seeds: the mirror-TWISTED double is NOT mirror-even (H₁: ℤ → ℤ⊕ℤ/5; the golden 5 lives entirely in the twist; Σ₂(4₁)=L(5,2)) (B591); the deck swap J is a genuine antilinear involution — c-as-swap mechanized (B638); the two ends ARE the exchange-even (disc −3) / exchange-odd (disc +5) sectors of a swap of two copies (B571 buried item); the wall-merge preserves exactly the orientation bit (B467). WALLS honored: naive mirror-double = plain double for the amphichiral object (B462); identical uncoupled copies sterile (B131/P016 — only the two-way coupled double mints); hearing-side is mirror-even (B1174) | GC-2: the relational-bit cell on the TWISTED double |
| F3. spin vs orientation | **PROVED, off-surface (B279)**: the mirror FIXES both spin structures — the spin ℤ/2 is NOT the orientation ℤ/2; and the beat SELECTS the spin structure (B1141) | fold into the F2 cell's frame (two distinct ℤ/2's, one closer-paid, one beat-paid) |

## G. THE LABELS (finite choices)

| item | status | the close |
|---|---|---|
| G1. the label vector | **PARTIAL** — branch (free orbit of 4, ℤ/2² with 11-dim moduli), which-family (the Higgs's own, B1185), Dehn slope, Galois sheet, torsor basepoint (B717's four closings); lift torsor computed (B870: 3 through ℤ/3, 6 through ℤ/6; m003's obstruction ℤ/5 — the first nonzero); the observer CANNOT carry the trit (B1040) | enumerate the full vector in one table (batch-2 cell); B1104's replication as 4d-label content |
| G2. measurement-of-measurement | **PROVED** — collapses once (15→3) then FIXED (B566-S4); self-application has only the trivial fixed point (H117) — the tower terminates | stands as structure |

## H. THE CLOSER ITSELF

| item | status | the close |
|---|---|---|
| H1. the assembly | **PARTIAL — the pieces exist, never joined**: apparatus (B723: object = timeless tracial II₁; external weight ⇒ type III_λ — the closer's clock enters exactly here); menu rank EXACTLY 3 = (c, θ, γ₅), saturated (B766/B733) with T6 = T4⊕θ and the T7=T3 tension (see B2); emergence map (B717: four incompletenesses ↔ four closings; "the observer is what breaks the c-swap by choosing a side"); the cut (B1091); freedom count 2 bits + 1 scale (B1164); T1's closing group (ℤ/2)³ abelian (B769, off-surface); ι-status banked but internally tensioned (B786 rank-3-unchanged vs B787 rank 3→4) | **Batch-2 state (B1190/GC-7): the count CLOSES** — B1182's five adelic legs match the assembly rows with ZERO orphans; B766's rank-3 = V₄{c,r} × γ₅. Residues: γ₅ never adelic-place-typed (the ℚ(√5) end); B723's weight λ a third time-candidate to reconcile; the realizer requirements typed (heterogeneous + norm-+1 unit + weight-supplier + labels) |
| H2. reflexive closure | **IMPOSSIBLE-BY-THEOREM for the sign** (B1184) | stands |
| H3. primitive observer | the null hypothesis; beaten iff H1 closes | — |
| H4. interface completeness | **PARTIAL** — cloud's C6 in flight; B1116's scope caveat (all four value/scale no-goes quantify over SINGLE-LEVEL invariants) must ride with the headline | C6 + carry B1116's caveat onto every surface that states the withholding |
| H5. the Kolmogorov Selector | **MISSING (no arc)** — but §3's piece is PROVED: the object fixes the PRIOR (G-invariant measures) where it cannot fix the POINT; the genesis-chain locks are miswired (B998: C1–C4 effectively unlocked — F2/F3/F8 untested) | the Selector formalization + the B998 lock repair (batch 2) |

## I. THE BRIDGE (its units → ours) — deferred, not lost

| item | status | the close |
|---|---|---|
| I1. Λ (the listener classifier) | **PROVED, sealed** — Λ = "the minimal exceptional orbit's Galois-fixed directions" → exactly {u3,u6}; three independent re-implementations; AC1–AC6 closed on the odd channel (B1070/71/72/73) | stands |
| I2. u-as-apparatus (G9) | **MISSING BY DESIGN** — the physical-apparatus layer is the spec's one unconstructed gap, firewalled (Gate 5 forbids fitting); the only builds on Λ are two calibration parameterizations, both NULL (B1128 meridian |U_e1|/|U_e2|=φ misses 5σ; B1132 sphere: freed dof carries no SM content) | deferred with the bridge |
| I3. **THE DESIGNED CROSSING, never run** | **SPEC'D-READY, off-queue**: the mirror (θ-even) row is THE LAST LICENSED CONTACT ROW (KIND_TABLE consumption ledger) — the designed shape Λ → u → u†T_m u (m=1,2,4) → one shot, ZERO anchors. Pre-work owed first: the even-channel AC6 run; the contact-quantity kind-row adjudication; R7's look-elsewhere ledger; the T_m non-commuting frame problem | **HELD FOR OWNER ELECTION** — firing it consumes the last licensed row (irreversible in the program's own accounting); everything BEFORE the shot (the pre-work) is campaign-runnable |
| I4. θ the value-kernel | **PROVED** (B1029: the frame-variant remainder of the value ring is ONE SIGN; B1182: θ→k5); B1037's join attempt DISTINCT (honest) | stands |

---

## The register repairs the sweep demands (GC-5, one cell)

L63 decided-but-open (the residue transports as **c** — B1174/B1182); L114 answered off-register
(B786+B787 on ι — with the rank-3-vs-4 tension to adjudicate); L120's foundation retracted
(B942/B957) — re-pose or close; B1168/B1166 verdict-vs-content drift (OPEN but PROVED-grade
own-verified); my L190 direction-word error (deficit, not excess); `representation_sweep`'s
500-char floor is structurally blind to one-sentence claims (B279, B552, B769, B786, B293) — the
drift-detector itself needs the E52 treatment; B834's retro-authored verdict; the B723-era
retraction leak audit (B1004) says LAW_MAP once inherited a retracted clause as an axiom — re-grep.

## The campaign's run-order

**Batch 1 (the close-loop workflow, now)**: GC-1 meter-verify · GC-2 relational bit · GC-3 clock
adjudication · GC-4 reach-deficit law · GC-5 register repairs. **Batch 2**: the L154-bridge · the
assembly cell (H1) · D3 · G1's full vector · the Selector + B998 locks · I3's pre-work. **Batch 3**:
THE GRAND COMPUTATION assembly — the universe written in rows A–H's own units; I3's shot if the
owner elects it; the bridge last.
