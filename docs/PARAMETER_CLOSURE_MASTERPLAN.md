# THE PARAMETER-CLOSURE MASTERPLAN — closing the floor into a parameter-free structure

**Owner-directed 2026-08-29.** The goal, stated so it can be checked: **every continuous
dimensionless input either derived or proved absent; every discrete input a finite menu; one
dimensionful unit, free by design.** No measured number appears in the target statement, and none may
appear in any cell's output (Gate 5 binds throughout).

## The floor as it stands

| slot | kind | state | closure means |
|---|---|---|---|
| **ℓ** | dimensionful unit | **external by design** | *nothing* — nobody derives the metre, and B1025 proves no dimensionless number flows from it. **Listing it as missing is a category error.** |
| **σ** | continuous dimensionless | one bridge missing | an **identification**, not a measurement: c((E₆)₁) = 78/13 = **6** and c_BH = 6σ, so *the stage CFT is (E₆)₁* **is** σ = 1, and the row deletes |
| **λ** | continuous dimensionless | genuinely external | collapse the three-way time ambiguity (λ / γ₅ / r), or find an **arithmetic quantization** of the Connes invariant |
| **the ℙ³** | ≤ 3 continuous | **one condition short** (3 → 2 → 1) | one more condition *linear in h*, from a **consistency** requirement — no symmetry can supply it |
| **the c-bit** | one ℤ/2 | relational + selector-free | show the **√3-partner is canonical** ⇒ ε forced ⇒ the bit stops being supplied |
| **r, family, VEV, filling** | finite labels | **already closed** | nothing — B990's orbit theorem forbids better. *Present them; do not pursue them.* |

## The four cells

**C1 — IS THE √3-PARTNER CANONICAL?** (c-bit) · **C2 — IS λ ARITHMETICALLY QUANTIZED?** ·
**C3 — IS THE LEPTON ROW AN INDEPENDENT CUT?** (ℙ³) · **C4 — WHAT EXACTLY MUST A BOUNDARY CHARACTER
SUPPLY?** (σ)

## Honest expectation, recorded before the run

**C3 and C1 can close.** C3 is a codimension count on the object's own tensor shape and B1215 opened
the route; C1 is a bounded question about the 112-family and the norm-+1 condition. **C2 and C4
cannot be closed by a workflow** — C4's remaining object is new mathematics (the record-side q-series
map) and C2's odds are low. What agents *can* do there is sharpen each to an exact, bounded, external
object with a pre-written acceptance test. **That is still the paper's requirement**: every slot
either closed, or reduced to a named object with a stated gate.

## The loop

Each cell **closes → is adversarially verified → is adjudicated** into an updated floor plus a
replan. The loop terminates when the floor's continuous rows are all *derived* or *proved absent /
externally bounded* — not when the agents run out of ideas. Verification packaging and guided
reproducibility are produced from the surviving cells, for the paper.

**Discipline carried in**: verify-don't-trust · MB12 (a criterion must be able to fail) · the
lexical-scope lesson (clause- or region-scope, never claim-scope) · no measured value, ever.
