# THE SESSION'S OPEN ITEMS — everything raised after the owner's two questions
*(2026-09-05. The owner's worry, in their words: "we might lose them undealt with." This file
exists so that worry is answered by a tracked artifact rather than by anyone's memory. Nothing
here is a claim; it is a disposition list. Landed with B1247/B1248.)*

## A. BUILT, REHEARSED, NOT YET LANDED

- [ ] **A1 — B1247**: `coverage_candidates.py` (7/7 tests, 30/30 gates in rehearsal). Closes
  **R54-1** (the length proxy is declared a gate threshold, not a measure of substantiality),
  **R54-2** (`CHAIN_COVERAGE.json` gains a `_criterion` naming its feeder), **R54-4** (the
  backlog is **9 arcs, not 154** — in-degree separates load-bearing from peripheral), and
  **R54-5** (E58 gains the *a claim about a file is time-indexed* clause).
- [ ] **A2 — B1248, THE QUESTION LEXICON**: the atlas's 18 motifs were authored 2026-07-01 and
  frozen (B806) while **~750 arcs banked since**. They index *the objects we study*, never *the
  questions we ask*. Seven question-motifs added — `arrow, monoid, measurement, closing, naming,
  choice, coupling` — re-mined and **tested**: every arc excavated by hand today is now retrieved
  by the question that wanted it (B497 → arrow/monoid/measurement; B766 → arrow/choice;
  B286/B287 → closing; B1184 → naming/choice). **This is the fix for the archaeology.**

## B. THE LEAD — the one I would actually hunt

- [ ] **B1 — B6_field_equation (OPEN, on ZERO surfaces, not in the chain, banked in week one).**
  `□τ + κ(τ² − τ − 1) = 0`, critical points **τ = φ (stable), τ = −1/φ (unstable)**; the
  canonical kinetic term `(½)(∂τ)²` is **declared a choice** by the arc itself.
  Three threads meet here: (i) B1157 lists a *kinetic term* among four missing objects — **we
  have one**; (ii) the potential is **τ² − τ − 1**, A7's Möbius fixed-point polynomial;
  (iii) A7's one bit selects φ over −1/φ, and **B6 says φ is the stable vacuum**.
  **The computable question:** is A7's bit *dynamically selected* rather than declared? That
  touches the chain's own census (4 axioms). **NOT a claim** — this is exactly the shape of
  unearned identification priced all day. Register it; do not assert it.
- [ ] **B2 — B1157's list as a specification, not a wall.** Of the four named absences:
  kinetic term → **B6 has one**; propagator → 2 arcs, both about lacking it; **Ward identity →
  genuinely ZERO (1 arc, B1157 itself)**; S-matrix → **23 arcs, UNASSESSED**. Assess the 23.

## C. THE DYNAMICS LEDGER — a conflation to correct

- [ ] **C1 — "the object lacks dynamics" is false as stated.** Six PROVED arcs: **B183** (a
  genuine **irreversible spectrum**, g_c = the minimum **Lyapunov exponent**, control exact ln 4),
  **B187** (exact diagonalization, thresholdless at all interaction strengths, with control),
  **B497** (four strata, exact κ-laws), **B496** (TM's exact κ-factorization), **B766** (time's
  direction = the golden branch, φ⁻²), **B530** (grammar golden at three levels; pure discrete
  spectrum certified). What is true is the **narrow** statement B1157 proved: *no parameter-free
  dynamical law at the archimedean ∞-place*. Correct the phrasing wherever the blanket form
  appears (S060's delta; any "no dynamics" gloss on Gate 5). Gate 5's own text — *no scale, no
  time, no dynamics* — means **no physical dynamical law**, and should say so.
- [ ] **C2 — the arrow "conflict" is a terminology collision, resolved.** **B766**: time's
  *direction* — a ℤ/2, the golden branch, inside the units (det ±1). **S063**: *irreversibility*
  — enters at det ≠ ±1, outside the units. **Both correct; different questions.** Same shape as
  the C22/B287 "closing" collision resolved at B1244. Land the scope line on both; neither may
  be quoted without its object. *(Currently exists only in conversation — the exact failure mode
  this list is for.)*

## D. THE DROPPED LANE

- [ ] **D1 — S062/S063/B496/B497 stopped at B749**; we are at B1247 (~500 arcs). The open
  question it named — *whether the mixed semigroup supplies a delta ingredient* — is **UNRUN and
  in no lead register** (0 hits in OPEN_LEADS and HINT_LEDGER). Register it as a lead.
- [ ] **D2 — B1157/B1156/B1115 cite the four-stratum monoid ZERO times.** A dynamics null that
  never opened the programme's own dynamics structure. Not a retraction — B1157's scope is
  narrow and correct — but the arcs should cross-cite.
- [ ] **D3 — B183/B187 buried.** An irreversible spectrum with a Lyapunov exponent, PROVED in
  the B18x era, absent from every dynamics discussion since.

## E. CLOSED — recorded so nobody re-digs

- [x] **E1 — the ethogram (copy/kill/hide/…)**: B456 froze 21 typed behaviours under a sha256
  with null catalogs; **B457 adjudicated NEGATIVE** — both pre-registered gates failed (6 hits vs
  null mean 4.0, p = 0.168; 0 of 3 structural predicates): *class-generic or whitelist-laundered*.
  Properly closed. Its absence from the crossings is therefore **not** a defect: class-generic
  behaviour has nothing object-specific to contribute.
- [x] **E2 — the four verbs are B497's four strata**: 1 (det ±1) **evolution**, 2 (|det|≥2)
  **renormalization**, 3 (det 0, TM) **decoherence**, 4 the fourth. The verb the owner could not
  recall is *evolution* — the only stratum the programme has ever computed.
- [x] **E3 — "can a→ab, b→a name it?"** B762/B1184: the object **can** name itself (unique in
  203,123) but the self-name is **letter-for-letter mirror-EVEN**, so the odd bit is
  **unutterable** in it. *Naming and choosing are proved complementary: the object says WHO it
  is, never WHICH WAY it is.*
- [x] **E4 — the RNA comparison**: survives as one line in B515, `[LEAP]/[HOOK]`, no detail
  banked. **Deliberately not reconstructed** — rebuilding a mechanism from its name is the error
  class the ledger already tracks.

## F. GOVERNANCE — raised, not yet acted on

- [ ] **F1 — "our own gates became arrogant" (owner).** Today's evidence: gates blocked the seat
  twice on *bookkeeping* (carry-ancestry keys, a review checkbox) while the atlas lexicon sat
  frozen for ~750 arcs, `representation_sweep` measured character counts, `relay_debt` reported
  45 open against a true 0, and C25 propagated a corrected sentence for a month. **The gates are
  precise about form and silent about substance.** The Gate 5 audit confirms it from the other
  side: zero over-fences in 46 items. Not too strict — confident about the wrong quantity.
- [ ] **F2 — name the retrieval channel that actually worked.** Four of five retrievals today
  came from **owner memory followed to source**, one from an instrument, **none from a sweep**
  (B1216: 9 agents, ~686k tokens, "ZERO ROWS DELETED, ONE REGRESSION"). Multi-agent sweeps
  produce material that is itself buried. The lexicon fix (A2) is the first mechanism aimed at
  this; whether it suffices is an open question for Review 55.
- [ ] **F3 — Review 54's hook** still prints "59 merges DUE" until the next anchor is recorded.
  Cosmetic; note so it is not mistaken for a missed review.

## G. CARRIED FROM REVIEW 54 (unchanged, listed so this file is complete)

- [ ] **R54-3** split `relay_debt`'s "open" into inbound/outbound (~20 min; actionable was 3, reported 45).
- [ ] **R53-5** λ's acceptance gate — the only ledger row with **no criterion at all**, carried since Review 50.
- [ ] **R53-1 / R53-2 / R53-4** live-lane harvest · cross-seat waits · the {C,P} promotion cell.
- [>] **R54-6** cc3's Papers I–IV — **parked by the owner**, venue/order/endorsement owner-gated.
- [ ] **The fc memo** — drafted and published, **unsent**: no channel to fc exists among the 26 peer sessions.

## H. ADDED AFTER THE CHECKLIST WAS FIRST WRITTEN (same session)

- [x] **H1 — A7 re-probed and the answer is NO.** Computed both orders: **LR's stable vacuum is
  φ, RL's is 1/φ**, identical curvature ±√5, product exactly 1. **Both orders have exactly one
  stable vacuum**, so stability does *not* break A7's tie. What remains is that the two vacua are
  **expanding vs contracting**; requiring the growing one selects LR — but that buys A7 with
  **two** declared inputs (B6's lift, which B6 itself calls a choice; and reading non-cancellation
  as "the vacuum grows"), replacing one. **A7 stays an axiom.** Control behaved: the class
  conjugate K has stable vacuum φ² > 1, so the criterion discriminates only *within* the A7 pair.
- [ ] **H2 — the A7 RE-PROBE THAT IS STILL LIVE.** A7's original wording (unchanged since
  `514c046e`, 2026-05-28 — the history holds no lost justification) says *"not eliminable by any
  **matrix-level** axiom."* That is scoped. **B1192 works at the integral GL₂(ℤ) level and proves
  the bit is a property of a PAIR** — trace-invisible 340/340, and a *homogeneous* pair carries
  none. If B1192 generalizes (its own scope: *"proved for the exhibited pairs; the general
  norm-classification is the refinement cell"*), then A7 is **provably not derivable from one
  object** — converting an axiom into a theorem *about* an axiom. **This is the re-probe target.**
- [ ] **H3 — THE OTHER-REFERENTIAL IS EXHIBITED, AND THE LANE IS DROPPED.** B1192's pair is
  concrete: **(A, M₁) = ([[2,1],[1,1]], [[2,3],[1,2]])**, disc 12, realizer **X₀ = [[2,−3],[1,−2]]**,
  det −1, single-signed; **ε(A,M₁) = −1** is a mirror-**odd** dimensionless ℤ/2 class of the pair,
  and X₀ induces the nontrivial Galois element on **both** spectral fields at once (√5→−√5 AND
  √3→−√3, exact). **It restricts to c.** *Why we could not find it:* it is **invisible to every
  trace word** (340/340) — no character-variety shadow — and the programme's whole instrument is
  the trace map. We were looking with the one tool structurally blind to it.
  **B1192/B1193 sit on ONE surface each and ZERO chain.**
- [ ] **H4 — the completion, and its honest limit.** Object alone: the self-name is mirror-EVEN,
  the sign unutterable (B1184). Pair: a mirror-ODD ℤ/2 — **the sign**. *The pair supplies exactly
  what the self-name cannot say.* **Limit:** ε is a **class, not a magnitude**. Crossing needs a
  dimensionless *number* from a pair. **That is the first crossing candidate that does not ask the
  object for something it provably cannot hold.**
- [ ] **H5 — CANDIDATE SYNTHESIS, not banked, needs attack at Review 55: "the object is complete
  about itself and empty about everything else."** Every boundary result has one form — the object
  supplies every *space* and never a *point* in it, **except points about itself** (B287's fiber
  slope returns its own A = LR; B294: selective for its own structure, catalogue for SM values).
  The programme's segments split on exactly this line: genesis → object → algebra (self-referential,
  39 forced links) vs values/crossings and the closings (other-referential, ten negatives and four
  axioms). **Consequence if it holds:** I-13 (the listener map u) is not one debt among nineteen —
  it *is* the boundary, and every other failure is an instance of it.
  **FALSIFIER (must be hunted before anyone leans on this):** find one banked case where the object
  supplies an other-referential point with no external input. C16's "NO POINT ×8" is the nearest
  existing statement and is a *census*, not this claim. **Not banked. Do not cite as a result.**

## I. THE A7 QUESTION, SHARPENED (added same session, after H2/H3)

**Asked:** if A7 is not derivable from one object, is it derivable from the pair (A, M)?
**Answered from B1192, at source: NOT YET — and the wall MOVED rather than fell.**

The bit's presence depends on the partner, and not simply on heterogeneity:

| pair | bit |
|---|---|
| (A, A) homogeneous | **none** — both det signs (recovers B1189's golden-unit kill) |
| (A, ℚ(√2)) norm **−1** | **absent** — single-signed det **+1**, mirror realized inside SL₂(ℤ) |
| (A, M₁ = [[2,3],[1,2]]) norm **+1** | **ε = −1**, restricts to **c**, realizer X₀ det −1 |
| (A, ℚ(√7)) norm **+1** | present but **IN TORSOR FORM** — *"no integral realizer; pairs GL₂(ℚ)-conjugate yet GL₂(ℤ)-inequivalent"* |

B1192's own adopted scope: *"the generic norm-+1-forces-ε=−1 mechanism does **NOT** hold as stated…
PROVED is the **exhibited pair's** ε = −1 … **the general norm-classification is the named
refinement**."*

- [x] **I1 — THE DECIDING COMPUTATION: the general norm-classification. CLOSED 2026-09-05 (B1248),
  and the dichotomy it was posed with was FALSE.** The item offered two outcomes — *a rule forces the
  partner* (A7 derived, 4 axioms → 3) or *the partners are a torsor* (A7 provably external, the H2
  upgrade). **Neither holds. There is a third branch, and it is the actual answer:** the partners are
  **classified by an exact arithmetic criterion**, `det X₀ = squarefree(2 − κ)` with `κ = tr[A,M]` the
  Fricke–Vogt invariant — so **ε = −1 exactly when κ − 2 is a perfect square**.
  - *Not forced*: the bit-carrying partners are an **infinite family** (κ = 2 + n²), not a point. So
    A7 is **not** derived and the axiom census stays at 4.
  - *Not a torsor*: the partners are **not** homogeneous — they carry **distinguishable invariants**
    (D = +1 no bit, D = −1 direct, |D| > 1 torsor form with det X₀ = D). So the H2 upgrade does not
    fire either.
  - *What is gained instead*: the object **sharply constrains which partners can carry c**, by a
    criterion rather than a survey. And the object's own value is now explained — m004's fibre has
    `κ = −2` (parabolic commutator = **the cusp**), below the wall `κ = 2`, so the object alone
    carries no bit **by mechanism**, not merely by the B1161/B1163/B1183/B1184 obstruction.
  - Also collapses **B1189's kill** into the same law: (A,A) has no bit because κ = 2 is Fricke
    reducibility, forced because A commutes with itself.
  - Registry `T-EPS-IS-KAPPA-MOD-SQUARES`; lock `tests/test_b1248_norm_classification.py`.
- [x] **I2 — the pattern confirmed one level up.** Trading "choose the order" for "choose the
  partner" is C22's shape again: **the object supplies the group, never the point** — now seen at
  three levels (the closing C22, the order A7, the partner B1192). Strengthens checklist **H5**;
  H5's falsifier is unchanged and still un-hunted.
- [x] **I3 — what IS new, and it is not small.** B1192's own consequence line: *"the closer's
  bit-supply has its **FIRST REALIZED INSTANCE** — a coupling within the program's own grammar
  **CAN** carry c; **H1-genesis is no longer hypothetical**."* Before B1192, "the observer supplies
  the bit" was a **label**. There is now an **exhibited coupling, inside the programme's own
  grammar, that supplies it.** That does not derive A7; it converts C18 from a posit into a posit
  **with a worked example**.
