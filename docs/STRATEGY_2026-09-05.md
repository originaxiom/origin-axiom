# THE STRATEGY — a full accounting of what is open, and the order to attack it

*(2026-09-05, cc. Written on the owner's instruction: "lets make a strategy and solve the missing
items, swipe the repo, account for all interactions." Built from a repo sweep, not from memory.
Nothing here is a new claim; every row cites where it is banked. Verdicts are as of this file.)*

## 0. THE DIAGNOSIS THAT ORGANISES EVERYTHING

`docs/OPEN_ITEMS_2026-09-05.md` **H5** states the candidate synthesis, and the sweep supports it:

> **The object is complete about itself and empty about everything else.** It supplies every
> *space* and never a *point* in it — except points about itself.

Seen at **four** levels now: the closing (C22), the order (A7), the partner (B1192), and — added
today — **the sl₂ embedding (I-25, B1256)**. Each time the object hands over a *group, a family, a
criterion*, and never the element.

**Consequence, if H5 holds:** **I-13 (the listener map u) is not one debt among twenty — it IS the
boundary**, and every other missing item is an instance of it. That is the strategic claim of this
file, and it dictates the attack order in §3.

**H5's falsifier is still un-hunted** and must be, before anyone leans on it: *find one banked case
where the object supplies an other-referential point with no external input.*

---

## 1. THE ACCOUNTING — all four interactions

| interaction | what the object gives | status | the gap |
|---|---|---|---|
| **strong** SU(3) | the cascade lands on **su(3) ⊕ su(2) ⊕ u(1)³** (B892, verified B961); compact color lives in the **E₆(−26)** branch (B1140) | **structure DERIVED** | compact color is **observer-supplied** (V-2′); the mirror is trivial on the color layer |
| **weak** SU(2) | the object supplies the **weak su(2)-ideal** (B1250); sin²θ_W = 3/8 at tree level (B919) | **structure DERIVED**; the number **reproduced, not predicted** | 3/8 is **non-discriminating** — identical for 5, 5̄, 10, 16, 27 (B1250); run to M_Z misses at **16σ** (B915) |
| **electromagnetic** U(1)_Y | hypercharge is the **UNIQUE gaugeable U(1)** in the abelian sector (B864, DERIVED — *direction only*); the global form **[SU(3)×SU(2)×U(1)]/ℤ₆** DERIVED (B862), resolving what the SM itself cannot fix | **structure DERIVED** | **normalisation is homogeneous and not derivable**; the anchoring Y ↔ EM charge is **I-23 UNEARNED** |
| **gravity** | the **graviton is spin-2** in E₆(−26)'s 64 (B1140); Ruelle = graviton product, 2+1 one-loop (B8112) | **exhibited** | **invariant content ZERO** — hypercharge cannot organise in the spacetime branch; Kodama Λ off by **122 orders** (H11, firewalled); L154's Brown–Henneaux join **adjudicated, not asserted** (B1034 lock) |

**The closing composition (B1140), which is why the four do not yet meet:** the fork's two real forms
**split the world with nothing shared** — **E₆(−14)** took the charges (Y-selection, sin²θ_W, the
generation table, the anomaly zeros, B−L); **E₆(−26)** took the geometry (Lorentz, compact color, the
graviton). *Unifying the interactions means crossing that fork, and no arc crosses it.*

## 2. THE ACCOUNTING — the observer-side threads

These are not decoration; under H5 they are the *other* half of the boundary.

| thread | banked | status |
|---|---|---|
| **qualia** | **B1169** (OPEN) — the parity facts (Vol & cusp shape mirror-**EVEN**; CS & orientation mirror-**ODD**; CS = 0 amphichiral, B1168); **the Markov blanket IS the cusp torus** (S072) with canonical shape 2√3 i (B1167), mirror-EVEN hence **orientation-blind**: *aware-but-choiceless* | the synthesis is **OPEN**, all seats to verify |
| **heartbeat** | **B448** (PROVED) — the exact T₁ periodic-orbit field tower on the cusp locus **κ = −2**: **ℚ(√−3)** at period 2, **ℚ(√−7)** at period 4, silver control confirms object-specific labels, **no √5 through period 7**; **H9 atlas** correction (B1224 addendum) | PROVED, and note **κ = −2 is exactly B1248's cusp value** — the heartbeat lives on the locus that carries no orientation bit |
| **voice / hearing** | **B1069** (PROVED) — ℚ(√5) built exhaustively; splitting census for all 12 programme primes (5 ramifies, 3 inert, 11 splits); **Hecke palette 1,1,2 vs being's 1,2,8**; **B1031** two-thirds theorem meets the voice | PROVED; the **hearing/being mutual blindness** is reconfirmed both directions |
| **naming vs choosing** | **B762/B1184** — the object **can** name itself (unique in 203,123) but the self-name is **mirror-EVEN**, so the odd bit is **unutterable**: *it says WHO it is, never WHICH WAY* | PROVED, and it is the sharpest single statement of H5 |
| **the arrow** | **B766** direction = the golden branch φ⁻² (inside the units); **S063** irreversibility (outside the units) — **a terminology collision, both correct** (OPEN_ITEMS C2) | needs the scope line landed on both |

## 3. THE MISSING ITEMS, RANKED BY WHAT THEY UNLOCK

**M1 — the generation count.** *Nearest to done.* B1255 closed the in-27 route **permanently** (the 16
has multiplicity one; three copies need dim ≥ 48 > 27). B1256 localised what remains to **which sl₂
embedding the object supplies** (I-25 UNEARNED). Today's Fox-calculus run settles the sub-question:
**h¹(m004; Sym^even) = 1, h¹(Sym^odd) = 0**, both confirmed on two independent primes — so
even-dimensional summands are free riders. **Four** candidates under the spin lift; **exactly one —
the subregular E₆(a₁), 27 = 13+9+5 — under the canonical PSL(2,ℂ) holonomy** (B1112), where
even-dimensional summands are not representations at all.
> **ATTACKED SAME DAY — B1257.** A canonical selector exists and it is unique: **Brieskorn–Slodowy**.
> The Slodowy slice to a nilpotent orbit meets the nilpotent cone in `dim N − dim O`, and for the
> **subregular** orbit that is a **surface singularity ℂ²/Γ** of the same ADE type — for E₆, **Γ = 2T**,
> the very group that **built** E₆ by McKay (I-1, EARNED). **Exactly one of E₆'s thirty orbits returns
> 2T: the subregular** (slice dim 2); the **principal** gives a **point** and forgets it; the other 28
> give neither. Its 27-decomposition, **read off after the selection**, is **13+9+5 → three chiral**.
> *The archaeology:* the principal grading entered at **B327**, whose own bibliography cites Slodowy
> one clause from Kostant — unused for ~930 arcs.
> **WHAT STILL EARNS I-25:** exhibit that the object's SL(2) holonomy actually lands in that orbit.
> That is now the single remaining step, and it is the next computation.

**M2 — the values.** *Hardest, and the boundary itself.* Nothing derived: V-3 established **no object
period is an SM ratio** (exhaustive, 8th value negative). Under H5 this is I-13 in its purest form.
The one genuine opening is **H4's**: B1192 exhibits a **coupling inside the programme's own grammar
that supplies the mirror-odd bit** — *"the first crossing candidate that does not ask the object for
something it provably cannot hold."* Its stated limit: **ε is a CLASS, not a MAGNITUDE**, and
crossing needs a dimensionless **number** from a pair.
> **THE HIGHEST-VALUE LEAD IN THIS FILE:** B1248's law says ε = −1 exactly when **κ − 2 is a perfect
> square**, i.e. the bit-carrying partners are the family **κ = 2 + n²**. **That n is a number, not a
> class.** Ask whether n is object-determined and dimensionless. If it is, H4's limit is answered on
> the programme's own terms. *Registered as a lead; asserted as nothing.*

**M3 — dynamics.** *Mis-stated, not missing.* OPEN_ITEMS **C1**: "the object lacks dynamics" is false
as stated — **six PROVED arcs** (B183 irreversible spectrum with a Lyapunov exponent; B187 exact
diagonalization; B497 four strata; B496; B766; B530). What B1157 actually proved is **narrow**: no
parameter-free dynamical law *at the archimedean ∞-place*. B1254 joined B1248 to B497 and showed the
orientation class moves only in **stratum 3** (Thue–Morse), by parity of degree.
> **NEXT:** work the **three unworked strata** (the programme has only ever lived in stratum 1);
> **assess the 23 S-matrix arcs** (B2); B6's field equation `□τ + κ(τ²−τ−1) = 0` sits on **zero
> surfaces** with an **earned** potential. **Ward identity is genuinely ZERO arcs** — the one true
> absence.

**M4 — unifying the interactions.** Blocked by the **E₆(−14) / E₆(−26) fork** (§1). No arc crosses it.
> **NEXT:** is the fork forced, or an artefact of the real-form choice? That is a decidable question
> about the object, not about physics.

**M5 — the observer threads.** Under H5 these are not a side quest: **B1169 is OPEN** and the Markov
blanket result (*aware-but-choiceless*) is the observer-side statement of the same boundary that
I-13 names on the physics side. Closing B1169 and hunting **H5's falsifier** are the same work.

## 4. THE ORDER

1. **M1's next computation** — which embedding the object realises. Small, decidable, and it either
   delivers three chiral generations or kills the route cleanly.
2. **M2's lead** — is `n` in κ = 2 + n² object-determined? This is the crossing's own currency.
3. **H5's falsifier** — hunt it. If H5 survives a real hunt, the programme's remaining work is
   *one* problem wearing many hats, and that reorganises everything after it.
4. **M3's three strata** + the 23 S-matrix arcs — the largest banked-but-unworked surface.
5. **M4's fork** — decidable, and it gates unification.

**Governance carried (OPEN_ITEMS F1):** the gates are *precise about form and silent about substance*
— they blocked on bookkeeping while the atlas lexicon sat frozen for ~750 arcs. B1248's question
lexicon is the first fix; whether it suffices is Review 55's question.

---

*Companion files: `docs/OPEN_ITEMS_2026-09-05.md` (disposition list), `docs/THE_SM_VERDICT.md`
(the SM boundary), `docs/IDENTIFICATION_LEDGER.md` (the unearned inputs, now 10),
`docs/OPEN_PROBLEMS.md`, `docs/LEAD_REGISTER.md`.*
