# THE CHAIN — STATUS OF RECORD

> **This is the chain's status of record. It supersedes its predecessors rather than joining them.**
>
> Four documents have answered the owner's question *"what exactly don't we have on the chain?"* —
> `THE_TOE_GAP.md` (KIND A/B/C), `THE_FULL_ACCOUNTING.md` (Registers 1/2/3), `THE_GRAND_TABLE.md`
> (DELIVERED/CONDITIONAL/…), and cloud memo 152 `THE_CHAIN_GAP.md` (the chain in order). **None of
> the four is on main.** Memo 152 was itself the fourth, written because the first three were in
> three taxonomies and none walked the chain in order. B1338 found a fifth being drafted.
>
> **The failure was never the analysis — it was that each consolidation was added, never adopted.**
> This one is adopted: it lives on main, it carries a currency date, and the four above are demoted
> to **sources**. A future consolidation should edit this file, not open a sixth.
>
> **Currency: row 14 refined 2026-09-12** (B1088/B1157 — "nothing derived anywhere" was too flat).
>
> **Adopted 2026-09-12** from cloud memo 152 (`e51afd6c:outside_bench/THE_CHAIN_GAP.md`, banked
> 2026-08-30), with the currency pass in §3 — **memo 152 is stale on its own headline**, and that is
> exactly why adoption means a pass and not a copy.
>
> Pre-flight: `python3 scripts/checks/already_banked.py --wide "<terms>"` (B1338) before writing
> MISSING / OPEN / "never run" anywhere in this file.

---

## 1. THE ONE-SENTENCE ANSWER (memo 152, adopted verbatim)

> **We are parameter-free from the minimal description to the SM's *structure*. We have *no*
> feature. And for the measured values that is not a gap — it is a proved theorem, because a value
> is exactly what a Galois orbit cannot be.**

## 2. THE CHAIN, IN ORDER (memo 152's table, adopted)

**✅ derived · ◐ derived but *generic* · 📖 classical/literature · 🔒 proved unobtainable · ❌ absent**

| # | link | | note |
|---|---|---|---|
| 1 | minimal description → **the object** `m004` | ✅ | population at the first steps is size one |
| 2 | object → arithmeticity, trace field `ℚ(√−3)` | 📖 | Reid: `m004` is the *unique* arithmetic knot complement |
| 3 | `ℚ(√−3)` → `2T` → `E₆` (McKay) | ◐ | ~1 manifold in 3 also arrives; arriving does not confirm the earlier steps |
| 4 | `E₆` → landing `su(3)⊕su(2)⊕u(1)³` | 📖 | Borel–de Siebenthal / Dynkin. Classical, not a discovery |
| 5 | the chain **stops** at the SM | ✅ | termination theorem, with a positive control |
| 6 | global form `[SU(3)×SU(2)×U(1)]/ℤ₆` | ✅ | **the strongest positive in the programme** — fixes what the SM itself does not |
| 7 | the arena: rank-3 abelian sector + SM-shaped 15-plet in the 27 | ✅ | the object's actual contribution |
| 8 | arena → content: 252 candidates → **2** | ◐ | exact, and arena-generic: *zero object tokens* in the computation |
| 9 | hypercharge **direction** | ✅ | forced |
| 9b | hypercharge **normalisation** | 🔒 | not derivable in principle — anomaly conditions are homogeneous |
| 10 | **three generations** | ❌ | see §3, where this row has moved |
| 11 | Yukawas, masses, mixings | ❌ | `ℙ(B₀)` cuts `3→2→1`; points need `0`. Exactly one condition short |
| 12 | every measured dimensionless value | 🔒 | nine value-crossing negatives; proved absent from the single object |
| 13 | scale / anything dimensionful | 🔒 | Mostow fixes shape, not size; nobody derives the metre |
| 14 | **dynamics — equation of motion, rate, action** | ❌ | the **form is imported**, the **constants are forced**, the **4d dynamics is absent**. B1088 (PROVED) assembles a *parameter-free action card* — `Λ = −1` FORCED (B259: the object is an exact 3d Einstein solution), `ℓ = 1` — but the action's form is classical 3d gravity, not derived. B1157 (NEGATIVE) closes the ∞-place "dynamical law" reading as **structural rhyme**. So: the object **solves** a theory whose constants it fixes; it does not **derive** the theory |
| 15 | the observer | ✅ | priced at exactly one bit, spent once at coordinatisation |

**Read the column.** Structure is ✅ or 📖 nearly all the way down; every ❌ and 🔒 sits at
*features*. **The chain does not thin out and stop — it completes as structure and hits a firewall.**
That is why it feels confusing: there is no ragged edge to point at.

## 3. CURRENCY PASS — what changed since memo 152

### 3a. Gate C is CLOSED, and memo 152's headline recommendation is stale

Memo 152's §4 recommends *"gates B, C, D enter the queue … **gate C is the three-generation
question**. If the programme wants a feature, that is the door."*

**`OPEN_PROBLEMS.md` §C records Gate C CLOSED on 2026-08-30 — the same day memo 152 was banked**,
by its own stated refutation condition: B323 (the E₆-level `ℤ/3` and the commensurator `ℤ/3` are
distinct, `3 ∤ 8`) and B324 (the three images are `g`-conjugates sharing one character), with
codex's Wave-8 strengthening reaching the same closure without routing through B307 at all — the
character-variety orbit has size one, so **the commensurator operation does not manufacture
multiplicity**.

The closure explicitly **relocates rather than answers** the generation question, and the record
then corrected its own successor twice in one day: `h¹(M;27) = 3` means **one class in each
inequivalent principal block `V(16)+V(8)+V(0)` — *not* three copies**, the phrase *"the actual live
generation computation"* is **WITHDRAWN**, and the physical generation/index question is tracked as
**codex `OA-C0009`**, not as a B632 cell.

> **So the live door for generations is not gate C.** Row 10 of §2 stands at ❌, and its address is
> `OA-C0009`.

### 3b. What this week adds to row 10 and row 14

| arc | bearing |
|---|---|
| **B1334** | `I = 0` is *proved* where a deformation exists, using **only dimension counting and inversion symmetry** — no geometry, no arithmetic, no chirality. The vanishing is not about the object being special |
| **B1336** | the surviving gauge group is `dim 0` for the geometric holonomy, abelian `u(1)⁴` for the finite `2T`. **No non-abelian group for cohomology classes to be multiplets of** on the flat-connection route; I-26 is made *harder* |
| **B1332/B1333** | isotropy is a one-cusp phenomenon; the multi-cusp index was derived and all 54 chiral multi-cusped covers evaluated (38070 sectors, zero) |
| **B1335** | the index identities and domain conditions **do not imply** `I = 0` — over `F_7`/`F_13` they hold while `I = ±1`. No proof can come from them alone |
| **B1337** | memo 79's *"chirality is character-invisible"* is an `SL2` fact (`tr A = tr A⁻¹`), true on every manifold — not a property of the object |
| **B1340** | **the joker, priced.** `C3` granted as a declared input (an *import*: the fixed-locus theorem gives `|det(A−I)| ∈ {0,4}`, and 3 never occurs). It does **not** complete the programme. It **costs** P9 — the record's Z′ is anomaly-free only on a **vector-like** spectrum, where the check is *vacuous* (all six coefficients vanish identically, so nothing could have failed). It **buys** one theorem: over a chiral spectrum the family part of any anomaly-free `U(1)′` is forced to `(0, t, −t)` — one generation neutral, the other two **opposite**, where P9 has them **equal** |
| **B1341** | **row 14 attempted, not reported.** The object's own discrete dynamics *does* have a least-action principle — provably only at the **double tick**. The half step reverses the invariant area form on every leaf, so no discrete Lagrangian generates it; the double step is generated by one. The obstruction is the **genesis bit itself**: `L·P` and the linearised half step share det −1 and trace 1. Scope kept: **not** a 4d field action — row 14's “4d dynamics is absent” stands. **The join, corrected same-day:** B448 had already verified that `T₁` is the *half*-monodromy and the figure-eight monodromy is `T₁²`, and had already named `κ = −2` as the Markov surface and `C = [[1,1],[1,0]]` (= `L·P`) as the det-−1 half — so **the action exists exactly at the monodromy and fails exactly at its formal square root**. Three independent reasons the physical step is the double one: orientation (genesis), geometry (B448), variational (B1341) |
| **B1345** | **the record has had the Jørgensen number since B309, under another name.** Jørgensen's inequality `|tr²A − 4| + |tr[A,B] − 2| ≥ 1` bounds every discrete group; at a **parabolic** pair the first term vanishes, so `J = |κ − 2|` — and B309/B518/B1010 bank *“|κ − 2| = 1, **the unit obstruction**”*. **J(m004) = 1 exactly** (verified on SnapPy's holonomy), and Callahan (2009) proves m004 is the **only orientable** hyperbolic 3-manifold that saturates it. So the programme's weakest link — *why this manifold* — becomes **variational** rather than descriptive. **Fence: it selects the MANIFOLD, not E₆**; everything downstream stays as subject to B727 as before. Two forwarded readings refuted: amphichirality ≠ saturation (six amphichiral knots at J > 1), and `χ = 0` is not why the index vanishes (B1335's `I = ±1` on m010) |
| **B1346** | **the chat1 handoff intaken — 34/34 reproduced, and one banked headline needs a clause.** F1 is confirmed on every order: **B1330's 952-sector scan ran on `v2873`, which is PROTECTED**, so its zero is *predicted* by B1297's theorem rather than evidence — the unprotected objects are `s958` and `t12833`. F5 confirmed and **generalised**: every metallic unit `λ_m` has **norm −1**, so `norm −1 ⇔ det −1 ⇔ the swap ⇔ the double tick is `X·swap(X)` ⇔ chirality is zero ⇔ **no action at the half step** (B1341)` — one fact in five languages, across the **whole family**, not the golden alone. And **Chat-2's “drilling m004 gives the silver bundle” is REFUTED**: the volume matches to 1e-13 but `drill(m004)` has **two** cusps and is the **Whitehead link**; `m136` has one |
| **B1347** | **the other three strata entered — and there is no arrow there either.** The multiplier `M` in `κ′−2 = (κ−2)·M` governs motion toward or away from `κ = 2` (“nothing”). Measured: strata 2, 2′, 3 give **both** collapse and escape from different starts — a **basin structure, not a direction**. Uniform only at the ends: stratum 1 never moves κ; stratum 4 lands on nothing in one step. **Exact:** on the object's own leaf the Thue–Morse multiplier is `−z²`, so one step crosses from below nothing to above it. **Leaving stratum 1 does not hand you an arrow**, just as leaving extremality does not hand you `χ ≠ 0` |
| **B1403** | **L208 fully closed: B1002's gcd cannot be compared to `gcd(m,15)` — their domains are disjoint.** A conductor needs an **abelian** cusp field. Verified independently at SnapPy high precision: the metallic cusp shape is **quadratic only at m = 1** (`τ²−2τ+13/12`, **ℚ(√−3)**) **and m = 2** (`τ²+1/4`, **ℚ(i)**) — both of B675's banked fields reproduced — and **not quadratic for m = 3..8**, the family leaving the quadratic world exactly at the bronze that B675 found non-abelian (`S₄`, *a DEAF object*). **Both conductor instances are UNITS of ℤ/15, hence both on the dead branch B**, while `gcd(m,15)` discriminates only on branch A. **Unaskable as posed, not unknown** — and structural: the family is abelian only at its two smallest members, which is *why* B675's law is a two-instance law. With B1402, L208 is answered in full | domain map; B675/B1002 untouched; bronze non-abelianness cited not re-derived; three more float-before-exact-test failures recorded, one inside the control meant to catch them (E75 widened from keys to INPUTS) |
| **B1404** | **the object's family has a NAME, a DEFINITION and a CLASSIFICATION THEOREM, and the record held every ingredient except the theorem.** The fibre group is a **punctured-torus group** — Minsky's family: *free, two-generator, discrete, **parabolic commutator***, i.e. `κ = −2`, the Markov surface. So the corpus's `κ = −2` (B416/B448/B1248; **442 lines across 187 files**, measured at `d08d1f98` before this arc wrote about it) is the family's **defining condition**, not a computed property, and **E72's collision has its reason**: two κ's because two groups. Minsky (Ann. 149 (1999) 559–626) classifies the family **by its end invariants**, and for m004's fibre group those are the fixed points of `RL` on `∂ℍ²` — exactly `φ` (attracting, derivative `1/φ⁴`) and `φ′` (repelling, `φ⁴`), Galois conjugates. **Two corrections, both load-bearing:** the classified object is the **fibre group**, not m004 — m004's knot group fails the definition decidably (`\|κ + 2\| = 3.6055…`, commutator not parabolic) — and the two *numbers* are a **marking**, not an invariant, since an `SL(2,ℤ)` change of basis moves the pair while fixing the manifold. What is marking-free is the **orbit** (every member **noble**, CF periodic part `[1]`), equivalently `RL`'s conjugacy class, equivalently the dilatation `φ²`. **The ladder, in the same coordinates:** `RᵐLᵐ = X_m²` so the fixed-point equation is `z² − mz − 1` with discriminant **`m²+4` = B666/B997's shadow modulus**, and the m-th end invariant is the m-th **metallic mean** (verified `m = 1..8`, CF purely periodic `[m]`) — **and every number in that sentence was already banked** (K002; B148/V137; T-GIES-FAM), the new content being only that `λ_m` *is* an end invariant. The arc drafted it as an extension and found it banked: **its own finding, committed while diagnosing it.** Absence audit corrected: Minsky, Marden, Maskit, Bromberg, ending laminations and **Epstein–Penner** are genuinely absent, while Guéritaud, Futer, Floyd–Hatcher, Lackenby and Jørgensen are all **present** — and two of the six absences were **invisible to a bare grep** (an Epstein *zeta function*, an *Eisenstein-end*) |
| **B1405** | **branch A's word anchor is not mis-SIZED, it is mis-TYPED — and the last licensed row's only surviving reading goes from `+1.19` bits to `−13.70`.** B1349 prices the word as a **label** (`log₂7 ≈ 2.81`, *name a non-unit residue*). But `ord(R) = ord(L) = 15`, so the SU(3)₂ readout is a function of **m mod 15** while the manifold is not — verified exactly, `weld(1) = weld(16)` and `weld(3) = weld(18)`, against `vol(b++R¹L¹) = 2.0299` (identified as m004) and `vol(b++R¹⁶L¹⁶) = 7.1769`. **Naming a residue and naming a manifold are different acts**, and **five** independent selection principles — minimal volume and minimal systole (both verified here, strictly increasing over the family), Jørgensen extremality (Callahan cited), B997's McKay uniqueness, the existence of a cusp-order conductor (B675/B1403) — **all output `m = 1`, a unit of ℤ/15, hence branch B**, the dead branch. Not one principle selects a branch-A word. And branch A's own residues `{0,3,5,6,9,10,12}` are the ones already disqualified: `0` is the **empty word** (`weld = C`, `λ = 1`, ear-independent trivially), `3` is B675's certified **DEAF** bronze, and `5,6,9,10,12` all have non-quadratic cusp fields. Re-priced as a departure from a five-fold selection rather than a label — floor `log₂|OrientableCuspedCensus| = log₂ 212641 = 17.70` bits — the surviving reading runs `4 − 17.70 = −13.70`. **The verdict is a fork and both horns fail the row's purpose**: about the object ⟹ the word is `m = 1` ⟹ branch B ⟹ dead; about a residue class ⟹ closes at `+1.19` but is not about m004. **Third independent ground for *do not spend it*.** Reported against itself: excluding the degenerate residue makes branch A **cheaper** (`+1.42`), not dearer. B1349's mathematics is untouched |
| **B1406** | **the two riddles answered, and they are one answer: the object is a BOUNDARY, and what feels missing is what it is the boundary of.** **The mirror's other half is the OTHER END.** `A_m = RᵐLᵐ` is **symmetric**, so `S A_m S⁻¹ = A_m⁻¹` exactly for every `m` with `S ∈ SL(2,ℤ)` — inverting the monodromy reverses the fibration and **swaps attracting and repelling**, which at `m = 1` is `φ ↦ φ′`, the **Galois involution** `√5 ↦ −√5`. One line of symmetry, so every metallic bundle is its own mirror *because `A_m` is symmetric*. **`T-UNIQ`'s transpose involution, B318's `√−3 ↦ −√−3` and `T-MIRROR`'s palindrome are ONE map** — and what a one-object mirror produces is not a second copy but a **grading**, which is exactly `C = S²`. **The bulk is the DEFORMATION SPACE, and the object is on its boundary:** `QF(T₁) = Teich × Teich`, `dim_ℂ = 2` (Bers, cited), and both end invariants are **irrational** (verified `m = 1..8`), so both Teichmüller coordinates have left the space. **The object holds 0 of 2 — and that is not a defect of the description, it is WHY the object is rigid.** **The parameter budget, written down for the first time:** the object supplies **zero** continuous parameters; `SL(n,ℂ)` at a `k`-cusped manifold supplies `k(n−1)` complex (Thurston for `n=2`; Menal-Ferrer–Porti cited, unverified); m004 has **1 cusp**; `k(n−1) ≈ 10` is what ~19 numbers would need. **AND THE EAR WAS NEVER FREE:** `C` is the grading, so the weld **traced** is the graded trace `str(m) = tr(C·RᵐLᵐ) = tr_even − tr_odd` — **no vector argument, no ear to anchor**. It is real, in `ℚ(√5)`, **mirror-invariant** (`str(m) = str(15−m)`), takes exactly **six** values all equal to `2cos(jπ/15)` for `j ∈ {0,5,6,9,10,15}`, splits the units by **2-torsion**, and **at the object's own word `str(1) = 1/φ` EXACTLY** — `0` ear anchor, `0` word anchor (B1405's five principles pay for it), 1 output. **And the feeling was right, measured:** `tr_even = 0` **exactly** unless `3 \| m` (B996's condition), so at `m = 1` the θ-**even** sector — the last licensed row's sector — has graded content **exactly zero**, and all of it is in the θ-**odd** sector. *Not a metaphor: a measured zero* (L216) |
| **B1407** | **L209 was never a choice — the row's SPEC settles it, and the row is dead on POWER.** B1405 left the fork *"object or residue class?"* to the owner; that was wrong, and the owner's push caught it. **The row's specification is written down in three places** — `KIND_TABLE.md:102` (*"this row's one shot, **with zero anchors consumed**"*) and `CAMPAIGN_STATUS.md` at 994 (*"coupling, zero anchors, one licensed row"*) and 1038. **Zero anchors is the spec, not an aspiration.** So **branch A is UNLICENSED** — its word anchor is non-zero by construction on *both* accountings (`2.81` bits at B1349, a five-fold-selection departure at B1405) — and **branch B is DEAD** on the two banked gates (the `2−2 = 0` tie with provably no third direction; the `ℚ(√2,√5)` kind exclusion). **And the observable that DOES meet the spec exists:** B1406's normalised graded trace `tr(C·RᵐLᵐ)/tr(C)` — `tr(C) = 2`, the grading's own index, so the normaliser is canonical — has **zero anchors**, range `[−1,1]`, field `ℚ(√5)`, and **all six values are members of B1011 C6's banked 15**, the same test branch A passed 4/4 and branch B failed 4/4. **But at the object's own word it reads `1/(2φ)` — exactly the value `KIND_TABLE` already records as the one B856 took to a bench and could not discriminate** (`|h|²` refuted on kind; `Re h` in a 1σ window holding ≥17 natural candidates). **THE ROW IS DEAD AND THE BINDING OBSTRUCTION IS POWER, NOT ACCOUNTING** — the fourth independent ground for *do not spend it*, and the one that explains the other three: the campaign spent three windows improving an accounting that was never binding, and the row could not have worked at any anchor price. **E79 added, this bench's own: the FALSE OWNER-CALL class** — a question the record already answers, escalated as a judgement, which stalls indefinitely because the owner cannot know it is decidable without redoing the work |
| **B1402** | **the three arithmetic cuts of the metallic family are ONE decomposition — and the mirror speaks exactly where McKay access dies.** B996 (mod-3 shadow full 2T iff `3∤m`, its stated list reproduced here), B997 (shadow modulus `m²+4` prime only at the golden), B1349 (`gcd(m,15)>1`). Since `15 = 3×5`, **the third decomposes into the first plus a 5-part**, and the parts carry different readouts: the **B996-degenerate** grammars (`3\|m`) carry the **nontrivial** values `1/2, −1/(2φ)`; `5\|m` is **silent**; `15\|m` trivial. Within the 3-part, golden iff `5 \| m²+4` — B997's prime on the value, not the grammar. **The uncomfortable reading:** B996 found E₆ access **generic** across two thirds of the family, and the mirror row speaks on the **complementary third** — **every grammar that reaches the McKay group is on the dead branch**, which sharpens B1349 addendum 5 (that had found only the golden word there) | relation among banked arcs; nothing reaches CLAIMS.md, F2 or Gate 5; half of L208 (B1002's other gcd) still open |
| **B1401** | **`|κ−2|` is a lower bound on `J`, and the bound is ATTAINED on a parabolic meridian in all five cases.** Five relayed values recomputed exactly (m004 `1`, m009 `√2`, m003 `4`, v2873 `3√3`, m202 `7`). The relay's two claims collapse to one: `J = inf(|tr²A−4| + |κ−2|)` over generating pairs, so pair-independence gives `J ≥ |κ−2|` as a **corollary**, equality iff a generator is parabolic. Nielsen-invariance verified (12 pairs, spread ≤ `5e−13`); **a first reading that κ varied is WITHDRAWN** — `(a²b,b)` is not a Nielsen move and κ is Fricke–Vogt conserved. **Every one has a parabolic meridian** reproducing κ, so `J = |κ−2|` attained *if that pair generates* — known for m004, named unproved for the other four, which would make them the **Jørgensen numbers themselves**. Explains a banked coincidence: **B309's unit obstruction IS `J(m004)` because the meridian is parabolic.** Note the default presentation pair does **not** attain `J` (4.606 vs 1) . **ADDENDUM 1 corrects this arc's own §4:** the generating step is now **closed for four of five** (m004, m009, v2873 by the meridian word `ab`; **m202 by its longitude `bba`**, since `b⁻²(b²a) = a`) — but **m202's meridian pairs provably do NOT generate** (a necessary `H₁` index test gives 3 and 2), so §4's “attained in all five” was unsupported for it as written and is withdrawn. **m003 is the one open case.** The two directions are now kept apart: **`J ≤ |κ−2|` is ESTABLISHED for four of five**; `J ≥ |κ−2|` stays conditional on κ being constant over *all* generating pairs. **`J(m004) = 1` alone needs no proviso** — Jørgensen's inequality gives `J ≥ 1` unconditionally, which is exactly why B309's `|κ−2| = 1` is a **unit** obstruction and why m004 is the case Callahan could settle | verification arc; the 3-generator relay values are withdrawn and cost nothing here, all five being 2-generator |
| **B1400** | **the six sweeps intaken: the headline reproduces to the digit and is an UNDERSTATEMENT.** A forwarded seat's six-sweep audit, every number recomputed here. Sweep 6's census-depth decline in the 2T-surjection rate reproduces **exactly** (34.25 %/4.183, 29.38 %/4.767, 21.12 %/5.650), as does B993's cornerstone control (m004: 48 raw surjections, /24 = **2**). The skip confound is absent (`skipped = 0`), but **stratifying by generator count puts the whole decline in the 2-generator stratum** (34.23 → 15.36 %, `z = +7.56`) with 3-generator groups **flat** (`z = +0.26`), and the growing 3-gen share props the aggregate up — **the headline understates it by better than 2×**. Slicing at depth is necessary, **not sufficient**: stratify by presentation complexity too. **B993 is NOT corrected** — right for its declared 400-manifold sample, and the bias runs against the programme's own skepticism. Sweeps 1/3/4 and sweep 2's located facts verify; Bianchi is miscounted (29 vs 115, immaterial); the absent-term list is right on `origin/main` while **8 of 16 are already present on this branch** (7 from B1343, waist size from B1345) — cross-seat visibility, E71/E53's class. Two near-misses of mine recorded: `-ilw` fails on hyphenated terms, and a Jørgensen diacritic that looked like an **E54** false absence until every hit proved **Andersen–Jørgensen**, a different person (**E72**), caught before it shipped | verification arc; nothing reaches CLAIMS.md, F2 or Gate 5 |
| **B1348 + B1349** | **the crossing door, opened and measured.** B1348 answered `LISTENER_MAP_SPEC` §1.2/§1.3 on the θ-odd plane — **existence POSITIVE** (Λ = the maximal-stabiliser directions = the 12-element icosahedral vertex orbit; AC4′ satisfied), **uniqueness NEGATIVE** (A₅ transitive), **freedom exactly 12**. B1349 then posed the sector that actually matters — the **θ-even mirror set, `KIND_TABLE`'s last unconsumed row** — and found **freedom 48** (`A₄ × A₅`, maximal `|Stab| = 15` on a rank-1 product state). **R11's arithmetic does not close**: one output against ≈5.58 bits of selection. **Recommendation: do not spend the last row.** I-13's price is now *known and unaffordable*, which is sharper than “unearned”. **ADDENDA 1–2 (same day, same ids — B1350–B1399 reserved, E71):** the exactification both arcs owed is **PAID** (rebuilt in `ℚ(ζ₆₀) = ℚ[z]/Φ₆₀`, every control passing with no float), **Galois cuts the freedom `48 → 8`** (rational directions are automatically Galois-fixed, so the anchor falls to `log′8 = 3` bits), and then the outputs are counted. **THE LAW: on the θ-even sector `Re h` is ear-independent exactly when `gcd(m,15) > 1`, ear-dependent exactly on the 8 units of ℤ/15** — exhaustive and exact over the complete period, since `ord(R) = ord(L) = 15`. **This splits the row: branch A (`gcd(m,15) > 1`) selects no direction, consumes no anchor, and yields 4 forced values `{−1/(2φ), 0, 1/2, 1}` — R11 CLOSES (`+4`, or `+1` conservatively); branch B (`gcd(m,15) = 1`) has only 2 ear-discriminating directions against 3 bits and FAILS (`−1`).** The arc's first positive instruction: **if the row is ever spent, spend it on a word with `gcd(m,15) > 1`.** B856 is reproduced from an instrument sharing no code with it (`h(5) = −1`, the golden `|h|²` pair) and **strengthened** — its numerical `2e-16` ear-independence becomes an exact identity for every `m = 1..15`. **The verdict stays NEGATIVE and the row still should not be spent:** R11 is input-accounting, not physics, and branch A's cheapest output `−1/(2φ)` has the modulus B856 already failed to cash (REFUTED ON KIND; ≥17 candidates in 1σ). **What gates the row now is kind-correctness, not arithmetic.** Three of my own errors recorded, none hidden: `b1349d` withdrawn as an instrument (**E58**, extended to a *self*-paraphrase — it compared complex `h` where B856 grades `Re h`), the pre-registered decision rule mis-derived (**E74**, new class — it imported AC4's bench demotion into R11 and would have declared the row dead on the branch that closes), and a count taken over the wrong base field (**E73**, new class — `dim` over ℚ of `ℚ(ζ₆₀)` coefficient vectors read as `6` independent readings; over ℝ it is `3`, which reversed branch B from `+2` to `−1`). **ADDENDUM 3:** the 4/4 split of the 8 rational directions between the two orbits of 48 — left *indicated, not established* by addendum 1 — is **established exactly** in `ℚ(ζ₆₀)`, both orbits size 48 with `|Stab| = 15`, `|orbit|` divides 720 asserted for all eight. Confined to one orbit the anchor is `log₂4 = 2` bits, so **branch B reads `2 − 2 = 0` and fails by a TIE** — sharper than `−1`, and unbreakable from inside the sector since `dim_ℝ span{Q_m : m` a unit`} = 2` exactly. A **fourth** float drift (an orbit of 66, not dividing 720) was caught here by the same orbit–stabiliser assert and its MECHANISM finally diagnosed: rounded canonical keys and `argmax` pivots both flip at boundaries (**E75**, new class; fixed by an index-free Cauchy–Schwarz projective test, after which the float route agrees with the exact one). **ADDENDUM 4 — the kind gate's first step:** branch A's four forced values are ALL elements of **B1011 C6's banked θ-even value set** (15 values, `{0, ±1/4, ±1/(4φ), ±1/2, ±1/(2φ), ±φ/4, ±φ/2, ±1}`), and **none of branch B's four ear-dependent extremes is** — structurally so, since each carries `1/√2` and generates `ℚ(√2,√5)` while the row's declared field is `ℚ(√5)`. **Branch B is therefore excluded on KIND as well, independently of its `2 − 2 = 0` tie — two gates, neither depending on the other.** Branch A earns **kind-ELIGIBLE, not kind-correct**. This is the split's third independent signature and the only one checkable against a law banked *before* the question: C6 agrees exactly on A and disagrees exactly on B. **ADDENDUM 5 WEAKENS THIS, and is written because the omission favoured the programme:** `m = 1` is the **golden** (trace 3) and **B997**'s *unique* McKay-shadow grammar, but `gcd(1,15) = 1` makes it a unit — **the object's own word is on branch B, dead on both gates**, silver with it. Addenda 2–4 priced the ear and treated the **word** as given, which it cannot be; naming a non-unit costs `log₂7 ≈ 2.81` bits, and with that anchor restored **branch A closes on ONE reading of four — the “four of six” count is WITHDRAWN** (**E76**, new class: the unpriced-setting class). The mathematics is untouched; the accounting was short a term. **The recommendation stands and is better founded: do not spend the row** — the only branch on which it is not short is not the object's grammar |
| **row 4/14, assembled** | the dynamics row is **four places with four answers and no arc holding the union**: **no** parameter-free law at the archimedean infinity-place (B1157, NEGATIVE and sound — but its *headline* reads broader than its argument, and both rows inherited that); a **derived potential** with an inserted kinetic term (B6, OPEN since week one); a **parameter-free variational structure at the monodromy** (B1341); and the four-stratum `End(F₂)` monoid for irreversibility (B497). The 4d action is still input, not derived. What is **false** is the reading these rows carried — that the object supplies *no* variational structure |
| **B1342** | **“is there a face we haven't seen?” — THREE OF FOUR.** The arithmetic faces are closed (B730: `ℚ(√−3)`, `ℚ(√5)`, `ℚ(√−15)`, a `V₄`) and the κ-faces are closed (B309/B312: existence, geometry, matter, quantum). But **B497's own last line reads “the program to date = stratum 1 of 4”**, and the reason is derived, not neglect: **κ is conserved in stratum 1 alone** — every other stratum moves between leaves — so every arc built on κ is structurally trapped there. Joined to B1341: the action lives in **half of one stratum of four**, and `S063` puts irreversibility at `det ≠ ±1`, so **the arrow and the action are in disjoint strata** |

Together these say the same thing from five directions: **the flat-`E₆`/cohomology sector of the
cusped object does not contain chiral matter, and the reasons have nothing to do with the object.**

**And B1340 answers the natural follow-up** — *what if we simply grant chirality?* — with: the chain
does not close, and the grant is not free. Its general lesson is worth carrying past the Z′: **on a
vector-like spectrum every consistency check that is odd in the charges is vacuous**, so any claim of
the form “it is anomaly-free / it is consistent” made on the 19 624 vacua must state whether it could
have failed.
The paper's SM algebra comes from the ninth closing `Y₉`, a closed manifold — nothing above touches
that, but the cusped object cannot supply it.

### 3c. Memo 152's correction on its own work, preserved

Gate D is the **non-Hermitian** case. The `B1085`/`B1095` Fibonacci-Hamiltonian numerics ran at real
coupling in the ordinary Damanik–Gorodetski regime; the verified numbers **do not transfer**, and
memo 152 records that this applies to five of its own memos (137, 143–147). Kept here so the next
reader does not reuse that code.

## 4. THE FOUR GATES — current state

| gate | question | tier | state |
|---|---|---|---|
| **A** | any invariant of a single seed that is trace-map-invariant, discretely multivalued, unsymmetrizable — a genuine forced choice? | in-sandbox | literal universal form **TOMBSTONED**; sharpened form live. Answer so far: no, class after class |
| **B** | does the 3d–3d correspondence at gauge type `E₆` realize the cascade as physical gauge dynamics? **THE CRUX** | SPECIALIST | tangent computed (`dim H¹ = 6 = rank E₆`); the Eisenstein-`ℤ/3`-selects-`SU(3)²` sub-path **closed negative** (B561/P12); the full exceptional state integral **needs a specialist** |
| **C** | does the intrinsic `ℤ/3` realize as three generations? | — | **CLOSED 2026-08-30** by its own refutation condition (§3a). Question relocated to `OA-C0009` |
| **D** | is there a Damanik–Gorodetski-type spectral theorem at the object's complex `κ = √3·e^{±iπ/6}` (non-self-adjoint cocycle)? | SPECIALIST | Hermitian `κ>2` is DG; the **non-Hermitian case is open** |
| *(hatch)* | Route A — the K-refined integral-orbit count | SPECIALIST | obstructions proved absent; coarse count closed positive; the counter is frontier mathematics |

## 5. THE JOIN DEFECT — memo 152's actual fix, still undone

Memo 152 measured the join between the registers and the specialist queue:

| | |
|---|---|
| gates the record says are where values could cross | **4** (A, B, C, D) + the hatch |
| typed SPECIALIST | **3** (B, C, D) + the hatch |
| present in `SPECIALIST_SEND_QUEUE.md` | **0** |

**Re-measured 2026-09-12 and still true.** The queue carries Q1–Q6 — SEAM-A's cusped extension,
`J₃(𝕆)` regulators, the B491 seam form, Cappell–Miller, B165 hyperbolicity, the closed-form `k`.
**Not one is a gate or the hatch.**

**Acted on in this adoption**, with gate C removed (closed) and the remainder drafted into the queue
as **Q7 (hatch / Route A), Q8 (gate B), Q9 (gate D)** — see `SPECIALIST_SEND_QUEUE.md`.
**Drafted, not sent**: memo 152's fence stands — *this bench drafts; the owner sends.*

## 6. FENCES

- **A gate opening is not a TOE.** Gate B or D opening gives one thing; `λ` would still have no
  acceptance criterion, `ℙ(B₀)` would still be one condition short, and **there would still be no
  equation of motion anywhere** (row 14).
- The four gates are the **record's** framing, adopted, not this bench's invention.
- Nothing here promotes to `CLAIMS.md`; the firewall of `GOVERNANCE.md` is untouched.

## 7. SOURCES (demoted from status of record)

| document | where | role now |
|---|---|---|
| cloud memo 152 `THE_CHAIN_GAP.md` | `e51afd6c:outside_bench/` | **primary source** for §1, §2, §4, §5 |
| `THE_TOE_GAP.md`, `THE_FULL_ACCOUNTING.md`, `THE_GRAND_TABLE.md` | outside bench | superseded taxonomies |
| `docs/OPEN_PROBLEMS.md` | main | the gates' full specifications and §C's closure |
