# HANDOFF — THE NEGATIVES HUNT (full-repo scrutiny, fresh seat, 2026-07-21)

*From: cc (the banking seat, end of the B720–B736 arc). To: a fresh clean-context seat.
Owner's directive (verbatim intent): scrutinize the whole repo — is everything calculated
properly, have we made mistakes? The repo began from a minimal not-nothing (philosophy/P000) and
everything emerged from it; MANY NEGATIVES WERE BANKED BEFORE the full anatomy of the object was
discovered. Hunt the negatives first: the owner believes positives are buried inside them.*

## Why this hunt is justified (the track record — not nostalgia)

1. **B525 audit:** cracked **3/10** banked negatives. Failure signature every time: a
   necessary/cited/proxied fact read as *sufficient*. (Memory: compute-the-discriminating-fact.)
2. **B731→B734 (days ago):** a banked "m004 is NON-congruence" was **WRONG** — the level-check
   stopped one 2-adic power too shallow (E22). The truth (congruence at (2)³=(8)) is now a
   two-seat theorem-grade result. A wrong negative hid a *strong* positive.
3. **B724 (E19):** three of chat-1's correspondences were dismissed by *citing* banked negatives;
   on computation, one was SOUND, one OPEN, one a category error.
4. **E-ledger classes E19–E22** exist because negatives get banked on cited, shallow, or
   center-bookkeeping-skipped grounds.

**The timeline argument (the owner's, and it is structurally sound):** most negatives were banked
when the object's anatomy was barely known. Discovered SINCE most of them: the V₄ forced faces
(being·hearing·meeting, B730); m004's congruence structure at level (8) (B734); B701 = Out(A₅) on
the non-abelian tower (B732); the Born-content ledger (B725–B729); the bounded observer menu
(B733); the EMITTANCE face (heartbeat/voice — the spectral side we had *entirely ignored*, B735);
the sisters' 2-adic tower (m003 at (2), m004 at (8)). **Any negative whose discriminating fact
predates the relevant anatomy is a candidate.**

## The hunting grounds (inventoried 2026-07-21)

| ground | size | where |
|---|---|---|
| atlas DEAD probes | **155** | `scripts/atlas/atlas_data.json` (status=dead) |
| atlas DORMANT probes | **17** | same |
| the walls (proved impossibilities) | **11** | `docs/LAW_MAP.md` §E |
| tombstones | ~30 entries | `speculations/TOMBSTONES.md` |
| parked leads | 588 lines | `docs/OPEN_LEADS.md` |
| error ledger (how negatives fail) | E1–E22 | `docs/ERROR_LEDGER.md` |

**The built tool:** `scripts/atlas/query.py` — the re-orientation card + the **obstacle→resolution
oracle**, built precisely to revive dead-ends (memory: recurrence-atlas-tool; vision in K023).
Use it. `python3 scripts/atlas/query.py card <B###>`.

## The method (binding — the discipline cuts BOTH ways)

1. **Compute-not-cite (E19), in BOTH directions.** A revival claim needs its discriminating fact
   recomputed in-sandbox exactly as a kill does. Do not revive on hope; do not re-kill on citation.
2. **The E22 lesson generalized:** before trusting any banked "X is impossible/absent," ask —
   *how deep did the computation actually go, and did it stop at a plateau that was never proven
   stable?* (B731 died of exactly this.)
3. **The B525 signature:** for each negative, ask — *was the discriminating fact computed, or was
   a necessary condition / a proxy / an authority read as sufficient?* Grep the FINDINGS for the
   actual computation; if it isn't there, the negative is UNVERIFIED, not false — recompute it.
4. **The anatomy cross-check (the new blade):** for each negative, ask — *which faces did it
   consult?* A negative that checked only the discrete arithmetic face is silent about the
   EMITTANCE face (B735); one that checked being OR hearing alone is silent about the MEETING
   ℚ(√−15) and the coupled V₄; one that used the field ℚ(√−3) is silent about the CONGRUENCE
   level-(8) structure (the field does not pin the object — m003 ties it).
5. **Base-rate discipline (E20) applies to revivals too:** a revived positive must beat
   look-elsewhere exactly as a fresh one would. The hunt must not become a rescue-everything sweep.
6. **House rules stand:** hash-first preregs for any re-adjudication arc; two-outcome; 3-skeptic
   adversarial verify; Gate 5 firewall (no SM value); nothing to CLAIMS; gates 8/8; Codeberg
   mirror; cc2 cross-verify on anything load-bearing.

## Priority strata (highest expected yield first)

- **P1 — negatives with the B525 signature** (cited/proxy/necessary-as-sufficient). Sweep the
  dead-probe FINDINGS for kills that cite a B-number or an authority instead of computing.
- **P2 — pre-anatomy structural negatives**: anything of the form "the object has no X / cannot
  Y" banked before B730–B735, that consulted only one face. The spectral/emittance face (B735) is
  BRAND NEW — *no* old negative ever consulted it.
- **P3 — shallow-plateau computations** (the E22 class): any negative resting on "we computed up
  to n and saw nothing" without a stabilization proof.
- **P4 — the early era (pre-B300)**: the minimal-not-nothing genesis through the first E₆ arc —
  banked when the object was a trace map and little else. Oldest = least anatomy = highest prior
  of a buried positive; but ALSO most likely already superseded — check the atlas card first.

## What is NOT a target (do not re-litigate without NEW information)

The fresh two-seat closings were computed WITH the full anatomy in hand and adversarially
verified: **B706** (kind-mismatch), **B727** (E₆-structure generic), **B733** (bounded observer
menu), **B736** (observer obstructed; zero SM parameter reductions). Re-opening these requires a
genuinely new computed fact, not a re-read. The firewall (Gate 5) is not a negative — it is the
methodology. The SM-bridge closing is not a target; buried MATHEMATICAL positives are.

## Current state (so the fresh seat knows where it stands)

main = `d8f8d83` (PC27 capstone banked). The honest capstone: the forced core is the atom
ℚ(√−3) + the V₄ torsor + the congruence realization + the Born ledger; methodology = the
base-rate firewall turned inward; the two ambitious doors closed. 673 atlas probes; gates 8/8;
Reviews through R26 (anchor 7e40985); errors through E22. Seats: cc (banking) + cc2 (independent,
relay at `/Users/dri/oa-seat-cc2/seat-work/relay/`) + chat1 (courier; verify before banking).
Read `WORKING_RULES.md` first (binding); deep context: `docs/LAW_MAP.md`, `docs/ERROR_LEDGER.md`,
`docs/PROGRESS_LOG.md` (tail), `MEMORY` auto-loads.

## Suggested opening move

Seal a hash-first prereg for the hunt (two-outcome per negative: REVIVED-with-computed-fact vs
RECONFIRMED-with-computed-fact — both outcomes are wins; an unearned negative is as bad as
numerology, dual-protocol rule 7). Then run it as a staged multiagent campaign: triage (P1–P4
strata over the 155 dead + 11 walls + 30 tombstones) → recompute each discriminating fact →
3-skeptic adversarial verify → bank per-negative verdicts in batches. Cross-verify anything
load-bearing with cc2.
