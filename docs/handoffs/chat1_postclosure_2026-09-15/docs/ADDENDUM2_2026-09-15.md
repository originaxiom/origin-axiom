# chat1 — ADDENDUM 2, 2026-09-15. **Corrections first.**
### Seat unchanged: no branch, no arc numbers, does not persist.
**This corrects claims in my own closure handoff that cc has already intaken.**

--------------------------------------------------------------------------------
## 1. RETRACTION — m202 and s959 are NOT distinguished  ***(highest priority)***
My closure handoff presented them as the objects carrying chirality + count + door.
**They are the first two hits of a volume-ordered scan.** Widening the census:

| scan size | det(A−I)=3 manifolds found |
|---|---|
| 4 000 | m202, s959 |
| 6 000 | + v3461, v3551 |
| 20 000 | + t10829, t12582 |

**The family grows and is not finite.** It also **spans multiple arithmetic classes** —
verified with two agreeing instruments (integral index vs all-tetrahedra shape fields,
6/6 agreement):
- **in `PSL(2,O_3)`**: m202 (idx 24), s959 (36), v3551 (42) — every tetrahedron `x²−x+1`
- **outside it**: v3461 (39.343), t10829 (41.260), t12582 (44.817) — 4–5 *distinct* shape
  polynomials each, non-integral index
- t12582 has `D_3`, not `D_6` — even the symmetry signature is not uniform

**Nothing in the family is forced by any theorem.** The relocation stands as *"m004 cannot
carry the count"*; it does **not** stand as *"these two objects can."*

## 2. BUG — cusp-permuting isometries  (`<chat1>/code/dets_fixing.py`)
`cusp_maps()[i]` on an isometry with `σ(i) ≠ i` is a map **between different cusps**;
`2 − trace` is not a fixed-point count there. **Scope of damage: exactly one result** —
"m003's hexagonal degree-4/6 covers carry det=3". Those were two cusp-permuting maps.
**RETRACTED: m003 has no covering path to the det=3 family either.**
All census scans are **unaffected** (they prefiltered on *having an order-3*, which forces
cusp-fixing on 2-cusped manifolds): old and corrected instruments agree **4/4 at N=6000,
zero spurious**. Fix: filter on `cusp_images()` first.

## 3. BUG — trace field from `shapes[0]`
I read the invariant trace field off **one tetrahedron**. For v3461 the 7 tetrahedra carry
**4 distinct** shape polynomials, one of which is `x²−x+1` — which is what fooled it.
**Having a `Q(√−3)` tetrahedron does not put a manifold in `Q(√−3)`.**
Every field claim of mine derived from `shapes[0]` needs re-checking; the **integral-index
test is the reliable one**. Confirmed sound: m004, m003, m202, s959, v3551 (single shape
polynomial across all tetrahedra).

## 4. BUG — Chern–Simons on non-hyperbolic fillings
SnapPy returns `cs` values for `m004(p,1)`, p = 0,1,2,3, whose `solution_type` is
*"contains flat tetrahedra"* and whose volume is **0.0000000**. Those values
(0, 0.0119, 0.0250, 0.0417) are **invalid** — p = 4 correctly returns `n/a`, which exposes
them. **Always gate `cs` on `solution_type == 'all tetrahedra positively oriented'`.**
**B286's slope law is UNAFFECTED and re-verified:** all eight slopes in that run —
(5,1),(7,1),(7,2),(9,1),(5,2),(8,3),(11,2),(13,4) — are individually hyperbolic, and
`CS(p,−q) = −CS(p,q)` holds **8/8 to 1e−16**. The law is verified **on hyperbolic slopes
only**, which my earlier statement failed to say.

## 5. WITHDRAWN — "the bulk term is a free integer"
I applied `c₂ ∈ Z + cs(∂W)` using SnapPy's **geometric** Chern–Simons of the *hyperbolic
structure*. That relation refers to the CS of the boundary **flat connection** — a
gauge-theoretic quantity. **Two different invariants sharing a name.** Same species of
category error as the Maschke one. The conclusion does not follow.

--------------------------------------------------------------------------------
## 6. WHAT SURVIVES, VERIFIED
- **Lattice theorem:** `det(A−I)=3` **forces hexagonal cusp shape** (only the hexagonal
  lattice admits an order-3 automorphism). **Zero exceptions in 20 000.**
  **m004's cusp is `2√3 i`** — neither square nor hexagonal, admits only ±1. So m004
  cannot carry the count for a **geometric** reason, independent of `Sym = D_4`.
- **Cusp-parity:** every 2-cusped manifold with an order-3 isometry has `det = [3,3]`
  (8 isometries, 4 manifolds, **zero counterexamples in 12 000**); no 1- or 3-cusped one
  has `det=3` anywhere. *(Forward direction rests on excluding a boundary-trivial order-3 —
  verified empirically, not proved.)*
- **Two-stage selector, both stages independent:** hexagonal (11/6000) → 2 cusps (5 of 11)
  → order-3 realised (4 of 5). **s596 is the counterexample** that keeps the last stage
  from being vacuous: 2-cusped, hexagonal, `D_4`, **no order-3**. And s596 **is** in
  `PSL(2,O_3)` while v3461 **is not** — the two properties cut across each other.
- **Cusp swap:** every det=3 manifold has an isometry exchanging its two cusps, and both
  cusps have shape **exactly `e^{iπ/3}`**. A one-cusp boundary condition has **no
  invariant meaning** → **row E of I-26 is closed by computation.**
- **THE CENTRAL NEGATIVE:** *no covering path from m004 or m003 to any det=3 manifold.*
  m004's degree-2 and degree-3 covers are **unique and 1-cusped** (vol 4.059766, 6.089650)
  — so they stay in a lattice class that can never carry an order-3. m202/s959 have the
  right volumes and **2 cusps**. The family is joined to m004 only by the ambient Bianchi
  group, which joins everything in the class.
- **Sol boundary exhausted, structurally:** every SU(2) flat connection on `m004(0,1)` is
  **reducible** — `|det(RL−I)| = |2−3| = 1`, one abelian point; the `ρ(y)=−I` case gives an
  explicit contradiction; one RL-fixed sign pair. **The integrality condition
  `−c₂ − (η+h)/2 ∈ Z` is ONE equation, not a selection rule.**
  **p = 0's canonicity (the Seifert framing, no choice) is paid for with an empty
  character variety.** Canonicity keeps arriving with emptiness attached — the
  Epstein–Penner hull is a collar with χ=0, m004 is forced and amphichiral, conventions
  A/B/C are standard and all zero.
- **Homology ladder** (combinatorial, valid on exceptional fillings):
  `p = 0,1,2,3,4,5,6 → Z, 0, Z/2, Z/3, Z/4, Z/5, Z/6`. **p=0 is the only infinite one.**
  The first genuinely **hyperbolic** filling is **p = 5** (vol 0.9813688).

## 7. METHODOLOGICAL
**Eleven instrument checks this stretch, eleven catches — all of my own errors.** Three
were found because my own output stated two contradictory things in one block (an order-3
det value in a group of order 16; a cs value beside a volume of 0.0000000; an `n/a` in one
row beside numbers in the others). **The cross-check between two instruments is what
works; a single instrument agreeing with my expectation is what fails.**
