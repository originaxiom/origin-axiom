# THE REPRESENTATION TRIAGE

*Purpose (L143, from B976): **the repo lost nothing; the summaries forgot.** Eleven banked
cascade arcs (B860–B873) were cited zero times on any synthesis surface — including **B864,
which derives hypercharge**, while a ledger row written five days later called hypercharge
"OPEN — the sharpest available target". Two gates already police the **content** of rows that
exist. Neither notices a row that was **never written**. This registry does.*

**How it works.** `scripts/checks/representation_sweep.py` (gate `representation-sweep`) lists
every **substantial** banked arc — non-instrument, PROVED or NEGATIVE, `claim_one_line` ≥ 500
characters — that is cited on **no** synthesis surface. Each must appear below with a
disposition. **An untriaged unrepresented arc is the defect.**

**Why claim length, not file size.** Calibrated on the block that was actually lost: a
6 KB **file-size** floor would have caught **1 of 11** (B864's FINDINGS is 3.7 KB — short and
dense). A **claim-length** floor of 500 catches **11 of 11**. A seat writes a long claim line
when there is a lot to say.

## Dispositions

- **PENDING** — a real object result owed a row on a synthesis surface. **This is a debt.**
- **PROCESS** — governance, verification bookkeeping or harvest; an *object* atlas is correct
  to omit it.
- **SURFACE** — the arc **is** a synthesis surface; self-reference is not absence.

| arc | disposition | note |
|---|---|---|
| `B855` | PENDING | the genericity controls were family members — m003 is commensurable with m004 (index 12 in PSL(2,O₋₃)). A real structural fact about the programme's own controls. |
| `B856` | PENDING | the hearing coupling obeys a **period-5 law** across the metallic family. A law, unrowed. |
| `B859` | PENDING | the incoming SM handoff scrutinised — contains the **OP theorem verified exactly**. The verification is bookkeeping; the theorem is not. |
| `B861` | PENDING | **the cascade's ONE principle** — maximal residual symmetry among *registerable* options. Core cascade; part of the block B976 found missing. |
| `B867` | PENDING | **the Sakharov gate** — all three baryogenesis preconditions, structural only. Substantial and entirely unrowed. |
| `B869` | PENDING | **G4 closed** — the false-positive control passes; the cascade rule is not an SM-generator regardless of input. |
| `B870` | PENDING | **G7 closed** — the central-extension lift obstruction, computed exactly. |
| `B872` | PENDING | **the coset leg** — 32 = 16 + 16̄ at every enhancement point, two independent legs. |
| `B874` | PENDING | **the measurement ladder** — the coordinate census of all 15 subtori of the 2T-torus, exact over ℚ. |
| `B875` | PENDING | **the triality-tiling theorem** verified on an independent build, three legs in one reproducer. |
| `B879` | PROCESS | the cc3 selection-cochain harvest — incoming-material bookkeeping, preserved verbatim. |
| `B881` | PENDING | descent stage 2 — the SM-graded coset commutation table, scoped before running. |
| `B935` | PENDING | **the composition hunt** — a NEGATIVE *with a mechanism* (rank-2 overlap ⟹ no three-term cascade by that route). Negatives with mechanisms are exactly what must not be lost. |
| `B938` | PENDING | **the sign of full flip** — no swap, a coset invariant; blind. |
| `B943` | PROCESS | the retroactive priority correction — governance. |
| `B948` | PROCESS | the relay + dead-clause sweep — governance. |
| `B968` | SURFACE | **is** `docs/THE_SM_VERDICT.md`. |

## The standing debt

**13 arcs marked PENDING.** Each is a banked object result with no representation on any
synthesis surface. They are not lost — they are locked, passing, and reproducible — but a
reader of LAW_MAP or the verdict would never learn they exist. **That is the debt B976
measured, now itemised.**

Marking an arc PENDING does **not** discharge it. It records that we know.

## Currency read 2026-08-13 (the register joins the doc-currency watch; head B1066)

The representation gate (b977's sweep) has held green through the window:
every substantial arc B1060–B1066 is cited on a required surface at bank
time (the gate blocked two pushes this window until citations existed —
working as designed). The 13-arc PENDING set stands unchanged; the pending
debt remains itemised, not hidden, and this read does not discharge it.

## Currency read 2026-08-21 (head B1127)

The B1067–B1127 window (the breakthrough packet, the reflection wave + the
forcedness census B1123, and value campaign B1124–B1127) added arcs that
are all surface-cited — each `creates_law` arc carries a THEOREM_REGISTRY
row and a CAMPAIGN_STATUS citation, and the representation sweep
(`scripts/checks/representation_sweep.py`) returns empty. The 13-arc
PENDING set is unchanged; this read does not discharge it. (The one
correction this window — B1123's forcedness doorway, error E37 — is
recorded in `docs/RETRACTIONS.md` and `docs/ERROR_LEDGER.md`, not here.)

## Currency read — 2026-08-28 (B1188, the grand-computation retrieval)

The six-lens sweep re-measured this surface's own blind spot: **the ≥500-char `claim_one_line`
floor structurally cannot see one-sentence claims** — the five sharpest off-surface
observer/closure arcs (B279 spin-vs-orientation, B552 the conserved ℤ/11 along the observer flow,
B769 the abelian (ℤ/2)³ closing group, B786 the ι torsor generator, B293 the peripheral symplectic
clock) are all invisible to it. The 13 PENDING rows below are unchanged across three currency
reads (08-13, 08-21, this one) — they ride into the campaign's GC-5 register-repair cell rather
than a fourth passive read. Detector fix owed: a short-claim lane (drop the floor for arcs whose
id appears in zero live surfaces, regardless of claim length).

## Currency read 2026-09-05 (B1248 / B1249)

Both arcs this window are **SURFACE** — represented on CHANGELOG, PROGRESS_LOG and CAMPAIGN_STATUS
in the same commit, with registry and ledger rows where owed:

| arc | disposition | where represented |
|---|---|---|
| **B1248** — ε is the Fricke invariant read mod squares | **SURFACE** | THEOREM_REGISTRY (`T-EPS-IS-KAPPA-MOD-SQUARES`), IDENTIFICATION_LEDGER (I-20, I-21), RETRACTIONS (the B1192 refutation + E60), ERROR_LEDGER (E60), CLAIM_CANDIDATES, OPEN_ITEMS (I1 closed) |
| **B1249** — the class does not pass the McKay door | **SURFACE** | CHANGELOG, PROGRESS_LOG, CAMPAIGN_STATUS; no registry row (declares `creates_law: false` — it is a census, not a law) |

The 13 PENDING rows above are unchanged and still ride into the register-repair cell; this read adds
no new PENDING. The short-claim-lane detector fix remains owed.
| B1328 | PROCESS | extension over the discarded parent m000 selects a central `Z/2` (fibre = the central involutions whose twist stays surjective; verified on 13 groups, 2T among them). Unifies codex R037, codex R039 and §9's spin-lift row into one mechanism, and types it as a **relation** (B1327) rather than an observer's choice. To surface once the seat that owns §9 rules on whether the arrow/chirality/swap triple double-counts. |
| B1330 | PROCESS | the best-case object for the chirality index — chiral, in the Q(sqrt-3) class, one-cusped, carrying Galois-unprotected order-3 twists: s958, v2873, t12833, t12835. v2873 returns zero across 952 in-domain sectors up to t0 = 4. Fifth independent regime at zero. To surface with the chirality row when it closes. |
| B1331 | PROCESS | the vanishing reduces to isotropy of L_V alone; L_V and L_V* are shown to be the SAME subspace (8 exact sectors on s958 over Q(zeta_12), t0 up to 3), so L_V is Lagrangian; and Galois is ruled out as the mechanism because s958 is chiral. To surface with the chirality row when the isotropy proof lands. |
| B1332 | PROCESS | the isotropy question of B1331 is well-posed only because the domain forces `psi` cusp-trivial (`V` itself is not self-dual: an invariant form needs `psi^2 = 1`), which upgrades the `t_0 = t_0*` that domain D already states into the strictly stronger `V|_T = V*|_T` as the SAME module — equal dimensions would not put `L_V` and `L_V*` in one space; isotropy then verified exactly over `Q(zeta_12)` on three CHIRAL targets (s958, t12833, t12835), 18 sectors each, zero exceptions — **but six per manifold are vacuous**, a 1-dimensional subspace being isotropic for free under an alternating form. Repaired by the form-free version: `L_V u L_V` vanishes in every component of `H^2(T;W (x) W)` carrying an `SL2`-invariant functional (non-vacuous exactly where the form test was empty). The blanket claim `L_V u L_V = 0` is **false** — `Sym^2 (+) Sym^4` leaves a 2-dimensional survivor, entirely off-diagonal and so invisible to any duality. To surface with the chirality row when the proof lands. |
| B1333 | PROCESS | **the blocker §14 of THE PAPER names is cleared**: the index on several boundary tori, derived from the same pair sequence. The formula is unchanged (`I = (a_0-a_0*) + t_0* - r_1`, in D `I = sum_i t_0^(i) - r_1`) — the two steps that could have failed, the exactness step and the annihilator property, both survive `c > 1`. What changes: `B^1(dM) = (+)_i B^1(T_i)` (a naive extension gets `r_1` wrong here), and isotropy becomes a condition on a **sum**, so it can vanish by cancellation between cusps — impossible with one cusp. Derived prediction, tested: a `psi`-nontrivial cusp is invisible (`t_0 = t_1 = 0`, peripheral holonomy being unipotent), so the index sees only its LIVE cusps and cancellation needs **two** of them. Verified on 3532 sectors with zero identity failures, and reproduces B1332's one-cusp numbers on `s958` sector for sector. Targets reproduced: 54 chiral multi-cusped covers to degree ten (matching the paper), 51 recognised exactly over `Q(zeta_12)`. **Evaluated (addendum): the trigger does not fire** — 38070 sectors at three primes over **all 54** covers (the three that first resisted PSLQ were a normalisation artifact, not a field obstruction: every trace lies in `Q(sqrt-3)`), zero identity failures, **1841 with two or more live cusps** (the regime impossible on one cusp), index zero in every one; the two 5-cusped covers redone **exactly** over `Q(zeta_12)` (486 sectors, 58 with ≥2 live cusps, zero non-zero) because `r_1` is a difference of ranks and a mod-`p` zero does not force an exact one. Not proved unfireable: one prime, lean germs, 3 covers unrecognised. To surface with the chirality row. |
| B1332-add | PROCESS | **the strong cusp-locality mechanism is refuted.** B1331 §3 named it the surviving candidate after ruling out Galois; it has a decidable form, since cusp-triviality makes the cusp matrices identical for every cusp-trivial `psi`, so all the `L_psi` sit in ONE space. If `L_V` were determined by the cusp data, every `L_psi` would coincide and the theorem would follow. They do **not**: on s958/t12833/t12835 across four germs, `L_psi != L_trivial` in 11 of 12 rows — while `L_psi = L_{psi^-1}` in **all 12**. So `L_V` moves with `psi` but is fixed by inversion; Galois and cusp-locality are both excluded and the mechanism is neither. A first version pooled the trivial character into the comparison and nearly recorded a false refutation of B1331's *finding* rather than its proposed mechanism. |
| B1333-add2 | PROCESS | **isotropy is a ONE-CUSP phenomenon — B1332's reduction does not generalise, and this bench's own arc is corrected.** B1332 said `I = 0` follows from isotropy alone and treated "is `L_V` isotropic?" as the whole theorem; B1331 said `L_V` and `L_V*` are the SAME subspace. Both hold at one cusp and fail at two: across 40 sectors on 14 chiral multi-cusped covers, exact over `Q(zeta_12)`, `L_psi` is isotropic in only **4**, `L_psi = L_{psi^-1}` in only **4** — and **`I = 0` in all 40**. The same code reproduces `ISOTROPIC: True` on one-cusped `s958`/`t12833` as a control, so this is not a pairing bug. So isotropy is **sufficient, not necessary**. Galois (B1331 §3), strong cusp-locality (B1332 addendum) and now isotropy are all excluded as the general mechanism: **the vanishing is more robust than every explanation proposed for it.** The surviving question is the unreduced one — why `dim L_V = dim L_V*` when the subspaces differ. |
| B1334 | PROCESS | **the record's first PROOF of `I = 0`** (narrow domain), using none of the three killed mechanisms. On the identity component `S^0` of the cusp-trivial character group: cusp-triviality holds `t_1` constant while `chi` moves; `r_1` is lower semicontinuous so maximal on a dense open `U`; `r_1(chi) + r_1(chi^-1) = t_1` is symmetric under inversion; `S^0` is an irreducible torus so `U` meets `iota(U)`, forcing the generic max to be `t_1/2` and then **every** value to be `t_1/2`. Only DIMENSIONS enter — which is why it survives chirality, non-isotropy and `L_V != L_V*`. Non-vacuous iff `dim S^0 > 0`: **2 of m004's 54 chiral multi-cusped covers** (`d10_4`, `d10_35`, 3-cusped, `b_1 = 4`), where the order-3 cusp-trivial characters are *exactly* the cube-root points of `S^0`, so every twist the programme uses is covered. Verified at all 36 points of `F_37^*`. **Does not reach the cases that matter most**: `dim S = 0` for the other 52 and for every one-cusped manifold (`m004`, `s958`, `t12833`, `t12835`), where `S` is finite and density has nothing to stand on. To surface with the chirality row. |
| B1334-add | PROCESS | **the map of what is left, and a no-go for the one-cusped case.** `dim S = b_1(M) - c`, so the deformation proof applies iff `b_1 > c` — a condition a one-cusped manifold *can* meet, but all 23 one-cusped covers of `m004` to degree ten have `b_1 = 1`. **No-go:** peripheral holonomy is unipotent, so `t_0 > 0` iff `chi` is cusp-trivial, and by T5 the index is identically zero off that locus — hence on a one-cusped manifold the whole content sits at finitely many **rigid** characters and no character deformation can ever reach it; deforming `rho` fails too (off-parabolic ⇒ `t_0 = 0` ⇒ T5 kills it trivially). **Map:** of 14 surveyed multi-cusped covers, **2 proved** (deformation), **1 reduced** (isotropy), **11 covered by neither**. The routes are independent — where deformation works, isotropy *fails* (0/2). So the one-cusped case, the one the programme needs, reduces entirely to B1332's form-free `L_V u L_V = 0`, now its **only** live route. |
| B1335 | PROCESS | **the vanishing is not formal, now exhibited — and the falsifier does NOT fire.** On the chiral one-cusped `m010`, honest `SL2(F_p)` reps twisted by cusp-trivial characters give `Sym^3` sectors **in domain D** with all four identities intact and `I = ±1` — verified by independent self-contained code (relators `+I`, so not the `-I` trap; `mu`,`lam` genuinely unipotent and commuting; no theorem T2–T5 applies). Not a one-prime accident: characters need `p = 1 mod 6`, and exactly the primes admitting them (7, 13) show it. **But it dies in characteristic zero**: `rho_geo(m010)` has `tr(mu) = -2`, so odd germs have `t_0 = 0` and vanish by T5, and with the valid even germs char-0 gives `I = 0` in all 10 sectors (singular-value gaps `1e45`–`1e50`). So the programme's vanishing is untouched. What IS established: **the identities plus the domain conditions do not imply `I = 0`**, so no proof can be assembled from them alone — closing off a class of attempts and explaining why formal manipulation of B1297's identities never worked. Debt: mod-`p` scans over *arbitrary* reps can show in-domain non-zeros absent in char 0. |
| B1332-add2 | PROCESS | **§1's reduction does not hold pointwise, and this bench's own arc is corrected again.** Isotropy of `L_V` *alone* gives only `dim L_V <= t_1/2`; equality needs `L_{V*}` isotropic too, which does not follow. B1335's `m010` sector over `F_13` is an explicit counterexample — `L_V = 0` is isotropic for all four of its invariant forms, `t_1/2 = 1`, and `I = 1` regardless, consistency restored by `L_{V*}` (dim 2 > `t_1/2`) failing isotropy with Gram `[[0,3],[3,0]]`, symmetric as the odd-`m` parity demands. **As a claim about the whole class the reduction is sound** (`V*` is in the class), so "prove isotropy for the class" stays the right one-cusped target, and no computed result changes. Method note: the `SL2`-invariant form on `Sym^m` is **not** invariant for the dual module; reusing it gave a Gram that was neither symmetric nor antisymmetric, which is how the error was caught. |
| B1332-add3 | PROCESS | **one-cusped isotropy broadened in characteristic zero, and it did not break.** Since B1334's addendum leaves isotropy the *only* live route for the one-cusped case, it was attacked: six further one-cusped manifolds (`m003 m006 m009 m035 m039 m040`), geometric holonomy, char 0 at 40 digits, germ **sums** — 106 sectors, **84 non-vacuous**, **128 genuine scalar equations**, all isotropic, zero inconclusive (18 are odd-parity, where `<x,x> = 0` is a real equation even at `dim L = 1`). Two errors caught en route: a first pass on single germs gave 48 sectors that were **all vacuous** and would have read as evidence (caught by this arc's own vacuity accounting); and 19 sectors read NOT isotropic — all mixed germ `Sym^2 (+) Sym^4` — an artifact of testing against every `pi_1(T)`-invariant form when the duality uses only the `SL2`-invariant ones, exactly the cross-term distinction §3 had drawn. Isotropy now rests on 128 equations across **nine** manifolds. Unproved, unbroken, still the only live route. |
