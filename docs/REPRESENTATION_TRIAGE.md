# THE REPRESENTATION TRIAGE

*Purpose (L143, from B976): **the repo lost nothing; the summaries forgot.** Eleven banked
cascade arcs (B860–B873) were cited zero times on any synthesis surface — including **B864,
which derives hypercharge**, while a ledger row written five days later called hypercharge
"OPEN — the sharpest available target". Two gates already police the **content** of rows that
exist. Neither notices a row that was **never written**. This registry does.*

**How it works.** `scripts/checks/representation_sweep.py` (gate `representation-sweep`) lists
every **substantial** banked arc — non-instrument, PROVED or NEGATIVE, `claim_one_line` ≥ 500
characters — that is cited on **no** synthesis surface. Each must appear below with a
disposition. **An untriaged unrepresented arc is the defect.**

**Why claim length, not file size.** Calibrated on the block that was actually lost: a
6 KB **file-size** floor would have caught **1 of 11** (B864's FINDINGS is 3.7 KB — short and
dense). A **claim-length** floor of 500 catches **11 of 11**. A seat writes a long claim line
when there is a lot to say.

## Dispositions

- **PENDING** — a real object result owed a row on a synthesis surface. **This is a debt.**
- **PROCESS** — governance, verification bookkeeping or harvest; an *object* atlas is correct
  to omit it.
- **SURFACE** — the arc **is** a synthesis surface; self-reference is not absence.

| arc | disposition | note |
|---|---|---|
| `B855` | PENDING | the genericity controls were family members — m003 is commensurable with m004 (index 12 in PSL(2,O₋₃)). A real structural fact about the programme's own controls. |
| `B856` | PENDING | the hearing coupling obeys a **period-5 law** across the metallic family. A law, unrowed. |
| `B859` | PENDING | the incoming SM handoff scrutinised — contains the **OP theorem verified exactly**. The verification is bookkeeping; the theorem is not. |
| `B861` | PENDING | **the cascade's ONE principle** — maximal residual symmetry among *registerable* options. Core cascade; part of the block B976 found missing. |
| `B867` | PENDING | **the Sakharov gate** — all three baryogenesis preconditions, structural only. Substantial and entirely unrowed. |
| `B869` | PENDING | **G4 closed** — the false-positive control passes; the cascade rule is not an SM-generator regardless of input. |
| `B870` | PENDING | **G7 closed** — the central-extension lift obstruction, computed exactly. |
| `B872` | PENDING | **the coset leg** — 32 = 16 + 16̄ at every enhancement point, two independent legs. |
| `B874` | PENDING | **the measurement ladder** — the coordinate census of all 15 subtori of the 2T-torus, exact over ℚ. |
| `B875` | PENDING | **the triality-tiling theorem** verified on an independent build, three legs in one reproducer. |
| `B879` | PROCESS | the cc3 selection-cochain harvest — incoming-material bookkeeping, preserved verbatim. |
| `B881` | PENDING | descent stage 2 — the SM-graded coset commutation table, scoped before running. |
| `B935` | PENDING | **the composition hunt** — a NEGATIVE *with a mechanism* (rank-2 overlap ⟹ no three-term cascade by that route). Negatives with mechanisms are exactly what must not be lost. |
| `B938` | PENDING | **the sign of full flip** — no swap, a coset invariant; blind. |
| `B943` | PROCESS | the retroactive priority correction — governance. |
| `B948` | PROCESS | the relay + dead-clause sweep — governance. |
| `B968` | SURFACE | **is** `docs/THE_SM_VERDICT.md`. |

## The standing debt

**13 arcs marked PENDING.** Each is a banked object result with no representation on any
synthesis surface. They are not lost — they are locked, passing, and reproducible — but a
reader of LAW_MAP or the verdict would never learn they exist. **That is the debt B976
measured, now itemised.**

Marking an arc PENDING does **not** discharge it. It records that we know.

## Currency read 2026-08-13 (the register joins the doc-currency watch; head B1066)

The representation gate (b977's sweep) has held green through the window:
every substantial arc B1060–B1066 is cited on a required surface at bank
time (the gate blocked two pushes this window until citations existed —
working as designed). The 13-arc PENDING set stands unchanged; the pending
debt remains itemised, not hidden, and this read does not discharge it.

## Currency read 2026-08-21 (head B1127)

The B1067–B1127 window (the breakthrough packet, the reflection wave + the
forcedness census B1123, and value campaign B1124–B1127) added arcs that
are all surface-cited — each `creates_law` arc carries a THEOREM_REGISTRY
row and a CAMPAIGN_STATUS citation, and the representation sweep
(`scripts/checks/representation_sweep.py`) returns empty. The 13-arc
PENDING set is unchanged; this read does not discharge it. (The one
correction this window — B1123's forcedness doorway, error E37 — is
recorded in `docs/RETRACTIONS.md` and `docs/ERROR_LEDGER.md`, not here.)

## Currency read — 2026-08-28 (B1188, the grand-computation retrieval)

The six-lens sweep re-measured this surface's own blind spot: **the ≥500-char `claim_one_line`
floor structurally cannot see one-sentence claims** — the five sharpest off-surface
observer/closure arcs (B279 spin-vs-orientation, B552 the conserved ℤ/11 along the observer flow,
B769 the abelian (ℤ/2)³ closing group, B786 the ι torsor generator, B293 the peripheral symplectic
clock) are all invisible to it. The 13 PENDING rows below are unchanged across three currency
reads (08-13, 08-21, this one) — they ride into the campaign's GC-5 register-repair cell rather
than a fourth passive read. Detector fix owed: a short-claim lane (drop the floor for arcs whose
id appears in zero live surfaces, regardless of claim length).

## Currency read 2026-09-05 (B1248 / B1249)

Both arcs this window are **SURFACE** — represented on CHANGELOG, PROGRESS_LOG and CAMPAIGN_STATUS
in the same commit, with registry and ledger rows where owed:

| arc | disposition | where represented |
|---|---|---|
| **B1248** — ε is the Fricke invariant read mod squares | **SURFACE** | THEOREM_REGISTRY (`T-EPS-IS-KAPPA-MOD-SQUARES`), IDENTIFICATION_LEDGER (I-20, I-21), RETRACTIONS (the B1192 refutation + E60), ERROR_LEDGER (E60), CLAIM_CANDIDATES, OPEN_ITEMS (I1 closed) |
| **B1249** — the class does not pass the McKay door | **SURFACE** | CHANGELOG, PROGRESS_LOG, CAMPAIGN_STATUS; no registry row (declares `creates_law: false` — it is a census, not a law) |

The 13 PENDING rows above are unchanged and still ride into the register-repair cell; this read adds
no new PENDING. The short-claim-lane detector fix remains owed.
| B1328 | PROCESS | extension over the discarded parent m000 selects a central `Z/2` (fibre = the central involutions whose twist stays surjective; verified on 13 groups, 2T among them). Unifies codex R037, codex R039 and §9's spin-lift row into one mechanism, and types it as a **relation** (B1327) rather than an observer's choice. To surface once the seat that owns §9 rules on whether the arrow/chirality/swap triple double-counts. |
| B1330 | PROCESS | the best-case object for the chirality index — chiral, in the Q(sqrt-3) class, one-cusped, carrying Galois-unprotected order-3 twists: s958, v2873, t12833, t12835. v2873 returns zero across 952 in-domain sectors up to t0 = 4. Fifth independent regime at zero. To surface with the chirality row when it closes. |
| B1331 | PROCESS | the vanishing reduces to isotropy of L_V alone; L_V and L_V* are shown to be the SAME subspace (8 exact sectors on s958 over Q(zeta_12), t0 up to 3), so L_V is Lagrangian; and Galois is ruled out as the mechanism because s958 is chiral. To surface with the chirality row when the isotropy proof lands. |
| B1332 | PROCESS | the isotropy question of B1331 is well-posed only because the domain forces `psi` cusp-trivial (`V` itself is not self-dual: an invariant form needs `psi^2 = 1`), which upgrades the `t_0 = t_0*` that domain D already states into the strictly stronger `V|_T = V*|_T` as the SAME module — equal dimensions would not put `L_V` and `L_V*` in one space; isotropy then verified exactly over `Q(zeta_12)` on three CHIRAL targets (s958, t12833, t12835), 18 sectors each, zero exceptions — **but six per manifold are vacuous**, a 1-dimensional subspace being isotropic for free under an alternating form. Repaired by the form-free version: `L_V u L_V` vanishes in every component of `H^2(T;W (x) W)` carrying an `SL2`-invariant functional (non-vacuous exactly where the form test was empty). The blanket claim `L_V u L_V = 0` is **false** — `Sym^2 (+) Sym^4` leaves a 2-dimensional survivor, entirely off-diagonal and so invisible to any duality. To surface with the chirality row when the proof lands. |
| B1333 | PROCESS | **the blocker §14 of THE PAPER names is cleared**: the index on several boundary tori, derived from the same pair sequence. The formula is unchanged (`I = (a_0-a_0*) + t_0* - r_1`, in D `I = sum_i t_0^(i) - r_1`) — the two steps that could have failed, the exactness step and the annihilator property, both survive `c > 1`. What changes: `B^1(dM) = (+)_i B^1(T_i)` (a naive extension gets `r_1` wrong here), and isotropy becomes a condition on a **sum**, so it can vanish by cancellation between cusps — impossible with one cusp. Derived prediction, tested: a `psi`-nontrivial cusp is invisible (`t_0 = t_1 = 0`, peripheral holonomy being unipotent), so the index sees only its LIVE cusps and cancellation needs **two** of them. Verified on 3532 sectors with zero identity failures, and reproduces B1332's one-cusp numbers on `s958` sector for sector. Targets reproduced: 54 chiral multi-cusped covers to degree ten (matching the paper), 51 recognised exactly over `Q(zeta_12)`. **Evaluated (addendum): the trigger does not fire** — 38070 sectors at three primes over **all 54** covers (the three that first resisted PSLQ were a normalisation artifact, not a field obstruction: every trace lies in `Q(sqrt-3)`), zero identity failures, **1841 with two or more live cusps** (the regime impossible on one cusp), index zero in every one; the two 5-cusped covers redone **exactly** over `Q(zeta_12)` (486 sectors, 58 with ≥2 live cusps, zero non-zero) because `r_1` is a difference of ranks and a mod-`p` zero does not force an exact one. Not proved unfireable: one prime, lean germs, 3 covers unrecognised. To surface with the chirality row. |
| B1332-add | PROCESS | **the strong cusp-locality mechanism is refuted.** B1331 §3 named it the surviving candidate after ruling out Galois; it has a decidable form, since cusp-triviality makes the cusp matrices identical for every cusp-trivial `psi`, so all the `L_psi` sit in ONE space. If `L_V` were determined by the cusp data, every `L_psi` would coincide and the theorem would follow. They do **not**: on s958/t12833/t12835 across four germs, `L_psi != L_trivial` in 11 of 12 rows — while `L_psi = L_{psi^-1}` in **all 12**. So `L_V` moves with `psi` but is fixed by inversion; Galois and cusp-locality are both excluded and the mechanism is neither. A first version pooled the trivial character into the comparison and nearly recorded a false refutation of B1331's *finding* rather than its proposed mechanism. |
| B1333-add2 | PROCESS | **isotropy is a ONE-CUSP phenomenon — B1332's reduction does not generalise, and this bench's own arc is corrected.** B1332 said `I = 0` follows from isotropy alone and treated "is `L_V` isotropic?" as the whole theorem; B1331 said `L_V` and `L_V*` are the SAME subspace. Both hold at one cusp and fail at two: across 40 sectors on 14 chiral multi-cusped covers, exact over `Q(zeta_12)`, `L_psi` is isotropic in only **4**, `L_psi = L_{psi^-1}` in only **4** — and **`I = 0` in all 40**. The same code reproduces `ISOTROPIC: True` on one-cusped `s958`/`t12833` as a control, so this is not a pairing bug. So isotropy is **sufficient, not necessary**. Galois (B1331 §3), strong cusp-locality (B1332 addendum) and now isotropy are all excluded as the general mechanism: **the vanishing is more robust than every explanation proposed for it.** The surviving question is the unreduced one — why `dim L_V = dim L_V*` when the subspaces differ. |
| B1334 | PROCESS | **the record's first PROOF of `I = 0`** (narrow domain), using none of the three killed mechanisms. On the identity component `S^0` of the cusp-trivial character group: cusp-triviality holds `t_1` constant while `chi` moves; `r_1` is lower semicontinuous so maximal on a dense open `U`; `r_1(chi) + r_1(chi^-1) = t_1` is symmetric under inversion; `S^0` is an irreducible torus so `U` meets `iota(U)`, forcing the generic max to be `t_1/2` and then **every** value to be `t_1/2`. Only DIMENSIONS enter — which is why it survives chirality, non-isotropy and `L_V != L_V*`. Non-vacuous iff `dim S^0 > 0`: **2 of m004's 54 chiral multi-cusped covers** (`d10_4`, `d10_35`, 3-cusped, `b_1 = 4`), where the order-3 cusp-trivial characters are *exactly* the cube-root points of `S^0`, so every twist the programme uses is covered. Verified at all 36 points of `F_37^*`. **Does not reach the cases that matter most**: `dim S = 0` for the other 52 and for every one-cusped manifold (`m004`, `s958`, `t12833`, `t12835`), where `S` is finite and density has nothing to stand on. To surface with the chirality row. |
| B1334-add | PROCESS | **the map of what is left, and a no-go for the one-cusped case.** `dim S = b_1(M) - c`, so the deformation proof applies iff `b_1 > c` — a condition a one-cusped manifold *can* meet, but all 23 one-cusped covers of `m004` to degree ten have `b_1 = 1`. **No-go:** peripheral holonomy is unipotent, so `t_0 > 0` iff `chi` is cusp-trivial, and by T5 the index is identically zero off that locus — hence on a one-cusped manifold the whole content sits at finitely many **rigid** characters and no character deformation can ever reach it; deforming `rho` fails too (off-parabolic ⇒ `t_0 = 0` ⇒ T5 kills it trivially). **Map:** of 14 surveyed multi-cusped covers, **2 proved** (deformation), **1 reduced** (isotropy), **11 covered by neither**. The routes are independent — where deformation works, isotropy *fails* (0/2). So the one-cusped case, the one the programme needs, reduces entirely to B1332's form-free `L_V u L_V = 0`, now its **only** live route. |
| B1335 | PROCESS | **the vanishing is not formal, now exhibited — and the falsifier does NOT fire.** On the chiral one-cusped `m010`, honest `SL2(F_p)` reps twisted by cusp-trivial characters give `Sym^3` sectors **in domain D** with all four identities intact and `I = ±1` — verified by independent self-contained code (relators `+I`, so not the `-I` trap; `mu`,`lam` genuinely unipotent and commuting; no theorem T2–T5 applies). Not a one-prime accident: characters need `p = 1 mod 6`, and exactly the primes admitting them (7, 13) show it. **But it dies in characteristic zero**: `rho_geo(m010)` has `tr(mu) = -2`, so odd germs have `t_0 = 0` and vanish by T5, and with the valid even germs char-0 gives `I = 0` in all 10 sectors (singular-value gaps `1e45`–`1e50`). So the programme's vanishing is untouched. What IS established: **the identities plus the domain conditions do not imply `I = 0`**, so no proof can be assembled from them alone — closing off a class of attempts and explaining why formal manipulation of B1297's identities never worked. Debt: mod-`p` scans over *arbitrary* reps can show in-domain non-zeros absent in char 0. |
| B1332-add2 | PROCESS | **§1's reduction does not hold pointwise, and this bench's own arc is corrected again.** Isotropy of `L_V` *alone* gives only `dim L_V <= t_1/2`; equality needs `L_{V*}` isotropic too, which does not follow. B1335's `m010` sector over `F_13` is an explicit counterexample — `L_V = 0` is isotropic for all four of its invariant forms, `t_1/2 = 1`, and `I = 1` regardless, consistency restored by `L_{V*}` (dim 2 > `t_1/2`) failing isotropy with Gram `[[0,3],[3,0]]`, symmetric as the odd-`m` parity demands. **As a claim about the whole class the reduction is sound** (`V*` is in the class), so "prove isotropy for the class" stays the right one-cusped target, and no computed result changes. Method note: the `SL2`-invariant form on `Sym^m` is **not** invariant for the dual module; reusing it gave a Gram that was neither symmetric nor antisymmetric, which is how the error was caught. |
| B1332-add3 | PROCESS | **one-cusped isotropy broadened in characteristic zero, and it did not break.** Since B1334's addendum leaves isotropy the *only* live route for the one-cusped case, it was attacked: six further one-cusped manifolds (`m003 m006 m009 m035 m039 m040`), geometric holonomy, char 0 at 40 digits, germ **sums** — 106 sectors, **84 non-vacuous**, **128 genuine scalar equations**, all isotropic, zero inconclusive (18 are odd-parity, where `<x,x> = 0` is a real equation even at `dim L = 1`). Two errors caught en route: a first pass on single germs gave 48 sectors that were **all vacuous** and would have read as evidence (caught by this arc's own vacuity accounting); and 19 sectors read NOT isotropic — all mixed germ `Sym^2 (+) Sym^4` — an artifact of testing against every `pi_1(T)`-invariant form when the duality uses only the `SL2`-invariant ones, exactly the cross-term distinction §3 had drawn. Isotropy now rests on 128 equations across **nine** manifolds. Unproved, unbroken, still the only live route. |
| B1336 | PROCESS | **I-26's "untouched" half paid: the surviving gauge group is abelian or nothing.** B854's `u(1)^4` is the centralizer of the **finite** 2T; the geometric holonomy is a lattice in `PSL2(C)`, hence Zariski-dense, so its centralizer is that of the whole `SL2` — computed exactly: **dim 0** (control: `Z(h)` = 6 = rank E6). **Theorem:** for any `H` with `2T <= H <= SL2`, `Z(H)` is a subalgebra of the abelian `Z(2T)`, hence abelian of dim ≤ 4, while the SM algebra is non-abelian of dim 12 — so **no flat connection with holonomy in the principal `SL2` containing 2T leaves the SM unbroken, whatever the holonomy**. I-26 is **not** earned and the ratchet does not move; the row should record the group as named and abelian-or-trivial. Scope: constrains the flat-connection route only (the paper's SM algebra comes from the closing `Y_9`), and the embedding is I-25, unearned. **Prior art is the SM seat's sm:B1269**, SCHEDULED in HARVEST_LEDGER row 125 since 2026-09-09 with no main text naming it — re-derived here independently, its fuller claim not verified. |
| B1337 | PROCESS | **the SCHEDULED sweep B1336 asked for: 156 of 277 are readable here.** The harvest ledger is 526 rows, **277 SCHEDULED** — over half is harvested work no main text names. 156 readable at their referenced commits (cloud 100, consolidation 30, codex 18, cc3-braver 5, audit 3); 121 not (physics 57, cc3 41, hostile-review 12, SM 9), on branches absent from this repo. List in `verification/readable_scheduled.json`. **Parsing trap recorded**: the commit ref sits *outside* the backticks, so a naive parser reports zero readable — the first pass here did exactly that and would have given the opposite recommendation. **First high-value row read, cloud memo 79**: its computation (`chi(w) = chi(w^-1)`, 484 words, zero exceptions) is right, its attribution to the figure-eight is **wrong** — in `SL2` every element is conjugate to its inverse, so this holds on *every* manifold (verified on four to 40 digits). The memo's own non-vacuity gate **refused**, which is the signature. **Third instance** of a pre-registered test that could not fail, across three seats. Corroborates B1334 from the character side. |
| B1338 | PROCESS | **the anti-rediscovery instrument is itself blind, and its two declared blind regions are stale.** The no-go consolidation this bench proposed had been written 13 days earlier (cloud memo 152, itself the *fourth* such document); the record already numbers this failure class (memo 158: *"BENCH ERROR #17 … the least excusable of the seventeen"*); B1202's `already_banked.py` is MANDATORY against exactly it — and **scans only `frontier/*` in the working tree**, so it cannot read `docs/` (I-26's "untouched", 277 SCHEDULED rows) nor the 156 artifacts readable only at other commits. Its declared blind regions are subject-matter controls; the **search-scope gap is undeclared**. Worse, both controls are **false**: memo 132 (*"the object cannot inflate"*) and memo 122 (*"no stabilizer"*) are run, certificated, GREEN. **Repair**: `--wide` added, default behaviour unchanged (B1202's `reproduce.sh` still REPRODUCES), gated by `tests/test_b1338_wide_already_banked.py`; the query that should have preceded the consolidation returns memo 152 top at 7/7. B1202's controls left for its owner rather than silently rewritten. |
| B1339 | PROCESS | **the chain status ADOPTED — the rediscovery loop closed by supersession, not a sixth document.** `docs/THE_CHAIN_STATUS.md` is now the status of record on main, adopted from cloud memo 152 with its 15-link chain table (absent ❌ vs proved-unobtainable 🔒), the four gates and the join defect; `THE_TOE_GAP`/`THE_FULL_ACCOUNTING`/`THE_GRAND_TABLE`/memo 152 demoted to **sources**. **The currency pass was needed**: memo 152 recommends gate C as the generation door, and gate C **CLOSED 2026-08-30 — the day memo 152 was banked** (B323/B324 + codex Wave-8: the commensurator does not manufacture multiplicity); the question is relocated to `OA-C0009`. Copying would have queued a specialist question about a gate closed 13 days ago. **Memo 152's own fix discharged**: it measured 4 gates + 1 hatch, 0 in the specialist queue — still true — so **Q7 (Route A), Q8 (Gate B, the CRUX), Q9 (Gate D)** are drafted in, **gate C deliberately absent**. DRAFTED, NOT SENT (both fences hold). Gated by `tests/test_b1339_chain_status_adopted.py`. |
| B1340 | PROCESS | **the joker priced: chirality costs the Z', and the record's anomaly check carries zero bits.** C3 granted as a DECLARED input (an import: the fixed-locus theorem gives `|det(A-I)| in {0,4}`, 3 never occurring). Two falsifiers this bench proposed are owned as VACUOUS (E6 is anomaly-safe; SM anomalies are linear in N). Rebuilding B1303's table from the E6 branching and reproducing its `-20250` as a positive control: **on the record's VECTOR-LIKE vacuum all six anomaly coefficients vanish IDENTICALLY in every parameter**, so "anomaly-free on the 19624 vacua" cannot fail and carries **zero bits**. **Theorem:** delete the mirrors and the family part of any anomaly-free `U(1)'` is forced to `(0, t, -t)` up to permutation. The record's `(-10, 5, 5)` is not of that shape, so P9's Z' is exactly what chirality forbids; P9 says the other two generations are EQUAL, chirality forces them OPPOSITE. The forwarded joker report is audited claim by claim. |
| B1341 | PROCESS | **the object's least-action principle exists only at the double tick.** Joins B21 (anti-Poisson half step, banked in the twenties) to B37 (feedback measured, then fenced at the awareness reading) — 4 corpus hits for "feedback" in 1300+ arcs, zero arcs joining "least action" to either. **Theorem:** a discrete Lagrangian's DEL map always preserves `omega = d1 d2 L da^db`, hence det +1; an orientation-reversing map of a surface preserves no area form; so an anti-symplectic map has NO discrete Lagrangian. It reaches the object: `T*omega = -omega` on EVERY leaf. **Two same-day addenda:** the Markov-surface and det-(-1) observations are re-credited to **B448**, which had them first (the instrument returned B448 top at 6/7 and this reader read the title, not the claim line — `already_banked` now prints claim lines for FINDINGS hits); and **B6's field equation joins it** — `tau^2-tau-1` IS `char(L.P)`, so B6's stationary points are the half step's eigenvalues and their product is `det(L.P) = -1`, the obstruction itself. **The action exists exactly at the monodromy and fails at its formal square root.** Scope: not a 4d field action; row 4 untouched. |
| B1342 | PROCESS | **"is there a face we haven't seen?" — three of four, and B497 named them in July.** The arithmetic faces are closed (B730, `V4`) and the kappa-faces are closed (B309/B312), but B497's classification ends **"the program to date = stratum 1 of 4"**. Recomputed from the substitutions, nothing copied: every trace map reproduced from its word, every kappa-law verified by **ideal membership**; multipliers `1`, `x^2 y^2`, **`x^2`** (computed here, B497 left it open), `x^2+y^2-xyz`, `0`. **Why the confinement is a theorem, not neglect: kappa is conserved in stratum 1 ALONE** — all four others move between leaves — so every arc built on kappa is structurally trapped there. Joined to B1341, the action occupies **half of one stratum of four**, and S063 puts irreversibility at `det != +-1`: **the arrow and the action are in disjoint strata**. Measured: period-doubling appears in 4 findings and **zero** claim lines across 1221 arcs. |
| B1343 | PROCESS | **deriving the other ADE faces is free and therefore evidentially empty.** Every other face is a standard functor applied to the same `2T`, so "can we derive them" answers yes trivially — B727's fence landing. **The blindness inheritance**, from B1258's "2T is too small: its eigenvalues are 12th roots of unity": any face that is a functor of 2T alone **inherits 2T's blindness** — it supplies a home, never a decision. So enrichment means consuming data the substitution gives BEYOND 2T. Run on the elliptic/Kodaira face: every Kodaira fibre monodromy is quasi-unipotent (`|trace| <= 2`), the object's `L.R` has **trace 3**, hence hyperbolic, hence conjugate to none — **the bridge is closed by the monodromy theorem**; no iterate escapes (the even Lucas numbers); the trace-1 near-miss with II* dies on determinant and points at E8 anyway. **One opening survives: Kronheimer's deformation parameter `zeta`.** Corrects a forwarded survey: the singularity face IS in the record, as Brieskorn-Slodowy (B1257/B1258). |
| B1344 | PROCESS | **verifying a forwarded seat report: one real result, one refutation, and a new error class.** CONFIRMED and genuinely new: **stratum 1 IS the chi = 0 stratum** — chi is multiplicative in fibrations and a mapping torus needs a homeomorphism, so F6's wall and B497's stratum 1 are the same boundary. REFUTED three independent ways: the claimed "object's geometric point" has `kappa = 2+w` not `-2`, is fixed by neither the monodromy nor its half step, and is not in the `Aut(F2)` orbit of either meridian point; **derived here**, the point is `(2+w, 1-w, 1-w)` with `x^2-3x+3 = 0`, B448's own banked period-2 field. **Root cause is our defect: ERROR_LEDGER E72**, the ONE-SYMBOL-TWO-QUANTITIES class — the record calls two different numbers `kappa = tr[a,b]` (the knot group's MERIDIAN pair, `|kappa-2| = 1`, and the FIBRE's, `kappa = -2`). Pending item 9 evaluated at the object: `1, 9, -3w^2, 3w, 0`, exact in `Z[w]`, no reading attached. |
| B1345 | PROCESS | **the Jorgensen number: the theorem is real and the record already had the number.** `J(m004) = 1.000000000000000` exactly, computed on SnapPy's holonomy, attained at `(ab, ba)`; Callahan (2009) cited, not re-proved. **The finding the report missed:** at a PARABOLIC pair `tr A = +-2` kills the first term, so `J = |kappa - 2|` — and B309/B518/B1010 bank *"`|kappa - 2| = 1`, **the unit obstruction**"*. **The record has carried the Jorgensen number as a headline law for hundreds of arcs under another name** — the E54 shape one level out, between record and literature. E72 is load-bearing: only the meridian branch saturates. **Two readings refuted:** amphichirality != saturation (six amphichiral knots at `J > 1`; chiral `5_2` sits BELOW amphichiral `6_3`; the report's sample was all-chiral, the E67 shape), and `chi = 0` is not why the index vanishes (B1335's `I = +-1` on m010). Fence, kept: **this selects the MANIFOLD, not E6.** |
| B1346 | PROCESS | **the chat1 handoff intaken: 34/34 reproduced, F1 corrects a banked arc, F5 generalises, Chat-2's nesting refuted.** The seat's own verification reproduces here (22 + 12 checks, controls included). **F1 confirmed on every order** — and its consequence is that **B1330's headline 952-sector scan ran on `v2873`, which is PROTECTED**, so its zero is predicted by B1297's own theorem; the unprotected objects are `s958` and `t12833`. **F5 confirmed and generalised beyond the handoff**: every metallic unit solves `x² − mx − 1`, so its norm is `−1` for every `m` — hence the forced self-mirroring at tick two belongs to the **whole metallic family**, and is the same fact as B1341's `det = −1` obstruction to a least-action principle. **Chat-2's §3a REFUTED**: `drill(m004)` matches `m136`'s volume to 1e-13 but has **two cusps** and is the **Whitehead link complement**; a punctured-torus bundle has one. The catching control was in chat1's own handoff (`NEG.2`, the s958/s961 volume trap). |
| B1347 | PROCESS | **the other three strata, entered: the κ-flow measured, the arrow NOT found, one exact identity.** B497 ended *“stratum 1 of 4”* in July and nobody iterated the other three. Since `κ′−2 = (κ−2)·M`, the orbit of distance-from-nothing is the multiplier product. **MAIN RESULT NEGATIVE: the multiplier does not supply an arrow** — strata 2, 2′, 3 each give both collapse and escape (34/66, 19/81, 18/82), a **basin structure**, refuting a reading this bench itself voiced before computing. Uniform only at the two ends: stratum 1 never moves κ, stratum 4 lands on `κ = 2` in one step. **EXACT:** on the object's leaf (`x²+y²+z² = xyz`, the Markov surface) the Thue–Morse multiplier is `M₃ = −z²`, so one step sends `κ−2 = −4 ↦ +4z²` — **crossing from below nothing to above it**, landing on nothing iff `z = 0`. One-step only: TM does not preserve the leaf. Control failed first and was right to (the classifier conflated point-escape with κ-escape); fences kept (S063 naming; the no-mapping-torus caveat). |
| B1348 | PROCESS | **the listener map POSED: existence YES, uniqueness NO, and the freedom is exactly 12.** `LISTENER_MAP_SPEC` §1.2/§1.3, neither previously attempted on this instrument. Rebuilt from Kac–Peterson: `|<R,L>| = 2880` reproduces B1011 C1 independently, the θ-grading is 2+4, the projective image on the odd plane is **A₅**, `ℂ²_odd` is **irreducible**, exceptional orbits exactly **[12,20,30]**. **EXISTENCE POSITIVE:** Λ = the maximal-stabiliser directions = the 12-element vertex orbit, and the **canonical weight directions are both vertices** (|Stab| = 5, cyclic). **AC4′ satisfied** with |Stab| as the discriminating quantity and the two-direction witness 5 vs 1. **UNIQUENESS NEGATIVE:** A₅ is transitive on the 12, so no group-stated rule separates them — B1040's SELECTION shape with a count. **I-13 UNEARNED, F2 untouched, no value comparison.** Owed: exact `ℚ(ζ₆₀)` re-derivation. Withdrawn before banking: an orbit-polynomial factorisation whose decomposition was numerically unstable. |
| B1349 | PROCESS | **the mirror sector posed: residual freedom 48, and R11 says DO NOT SPEND THE LAST ROW.** Three of `KIND_TABLE`'s four coupling rows are CONSUMED with decisive misses; the **mirror set (θ-even)** is the last, and the one-shot rule spends it whatever the outcome. B1348 posed the θ-**odd** plane, not the crossing target; this poses the right one. Projective image **720 = A₄ × A₅**, `ℂ⁴_even` **irreducible**, stabiliser spectrum 15/10/9/6/5/3/2 with every orbit size dividing 720, **maximal `|Stab| = 15 = 3×5`** on a **rank-1 product state** (tetrahedral vertex ⊗ icosahedral vertex) ⟹ **smallest distinguished orbit = 48**. **R11: one output against a 48-fold selection (≈5.58 bits) or a 48-element target set — vacuous by MB12 before the seal.** Recommendation banked as a result. Withdrawn first: an orbit enumeration whose sizes did not divide 720. `F2` untouched, I-13 still UNEARNED. |
| B1400 | PROCESS | **the six sweeps intaken: the headline reproduces to the digit and is an UNDERSTATEMENT.** A forwarded seat's six-sweep audit (`chat1_SWEEPS_2026-09-12`), every number recomputed here. Sweep 6's census-depth decline in the 2T-surjection rate reproduces **exactly** on all six numbers (34.25 %/4.183, 29.38 %/4.767, 21.12 %/5.650), and B993's cornerstone control with it (m004: 48 raw surjections, /24 = 2). The confound I looked for is absent — `skipped = 0`, so the instrument's >3-generator skip never fires — but **stratifying by generator count shows the decline lives ENTIRELY in the 2-generator stratum** (34.23 → 15.36 %, `z = +7.56`) while 3-generator groups are **flat** (34.55 → 32.71 %, `z = +0.26`), and the growing 3-gen share (6.9 → 33.2 %) props the aggregate up: **the headline understates the effect by better than a factor of two.** Their reading of B993 is correct including the direction — the bias runs against the programme's own skepticism, so nothing is inflated and **B993 is not corrected**. Sweeps 1, 3, 4 and 2's located facts all verify; one covered-term count is wrong (Bianchi 29 vs 115, immaterial); the 16-term absent list is right on `origin/main` while **8 of the 16 are already present on this branch** (7 from B1343, waist size from B1345) — a **cross-seat visibility** matter of the E71/E53 kind, since a seat cannot audit what has not landed. **Why PROCESS and not a claim:** this is verification of someone else's audit; nothing reaches `CLAIMS.md`, F2 or Gate 5, no value is read against measurement, and the one non-derivative piece — the stratification — refines another seat's finding rather than establishing a law (`creates_law` false, reviewed and dated). Two near-misses of my own are recorded in the arc: a `git grep -ilw` that returns 0 for hyphenated terms (their `-il` was right), and a Jørgensen diacritic that looked like an **E54** false absence until every hit proved to be **Andersen–Jørgensen**, a different person — **E72**, one name two people, caught before it shipped. |
| B1401 | PROCESS | **|κ−2| as a lower bound on J: all five relayed values exact, and the bound is ATTAINED on a parabolic meridian in every one.** Relayed values recomputed on the geometric holonomy: m004 `1`, m009 `√2`, m003 `4`, v2873 `3√3`, m202 `7` — all exact. **The two relayed claims are one claim:** `J = inf` over generating pairs of `(|tr²A−4| + |κ−2|)`, so pair-independence of `κ` gives `J ≥ |κ−2|` as a *corollary*, with equality iff a generator is **parabolic**. Invariance holds on the Nielsen class (12 pairs, spread ≤ `5e−13`); **a first reading that it VARIED is withdrawn** — the offending `(a²b, b)` is not a Nielsen move and `κ` is Fricke–Vogt conserved, so a differing `κ` proves a different class. **All five have a parabolic meridian** whose pair reproduces `κ`, so `J = |κ−2|` attained — *provided that pair generates*, known for m004 (Callahan) and named as unproved for the rest. Stronger than relayed: these are then the **Jørgensen numbers themselves**. Connects to the record: B309's unit obstruction **is** `J(m004)` *because the meridian is parabolic* — the `|tr²−4|` term drops. **Why PROCESS:** verification of a relay plus a corollary of a classical inequality; `creates_law` false with a dated review, nothing reaches `CLAIMS.md`, F2 or Gate 5, and no value is compared to measurement. **ADDENDUM 1 corrects §4 of this same arc:** the generating step is closed for **four of five**, **m202's meridian pairs provably do not generate** (necessary `H₁` test fails, index 3 and 2) and m202 is instead recovered by its **longitude** `bba`; m003 is the one open case; and only **`J(m004) = 1`** needs no proviso at all, sitting at the floor of Jørgensen's inequality. |
| B1402 | PROCESS | **the three cuts are one structure, and the mirror speaks exactly where McKay access dies.** L208 answered. B996's criterion recomputed here (mod-3 shadow orders `24,24,1,24,24,1,24` for m=1..7 — **B996's own list reproduced**): `⟨Rᵐ,Lᵐ⟩ ⊂ SL(2,ℤ/3)` is the full 2T iff `3 ∤ m`. Since `15 = 3×5`, **B1349's `gcd(m,15) > 1` DECOMPOSES into B996's cut plus a 5-part**, verified over the period with no exceptions — and the parts differ: `3\|m` only (the B996-**degenerate** grammars) carries the **nontrivial** forced values `1/2, −1/(2φ)`; `5\|m` only is **silent** (`λ=0`); `15\|m` is trivial (`λ=1`). Within the 3-part, golden iff `5 \| m²+4` — B997's prime acting on the *value* rather than the *grammar*. **The reading:** B996 found McKay access **generic** (`3∤m`, two thirds); the mirror row speaks on the **complementary third**, so **the row's arithmetic closes only where the E₆ access is gone** — sharpening B1349 addendum 5, which had found only the golden word on the dead branch. **Why PROCESS:** a relation among banked arcs by an arithmetic identity, `creates_law` false with a dated review; nothing reaches `CLAIMS.md`, F2 or Gate 5, and it is **not** a reason to spend the row — if anything the reverse. Half of L208 remains open (B1002's *other* gcd), named rather than guessed. |
| B1403 | PROCESS | **L208's second half closed: B1002's gcd and `gcd(m,15)` have DISJOINT DOMAINS.** A conductor is a Kronecker–Weber object and needs an **abelian** cusp field; B675 banks its law as **two instances** and reports the bronze as a **DEAF** object (`u = y²` an irreducible quartic with group **S₄**, so `[ℚ(τ):ℚ] = 8` embeds in no cyclotomic field). **Verified independently here** on `b++ RᵐLᵐ` at SnapPy high precision, with no LLL needed since the imaginary part forces `b` and the real part forces `c`: **m=1 quadratic** `τ²−2τ+13/12`, disc `−1/3` ⟹ **ℚ(√−3)**; **m=2 quadratic** `τ²+1/4` ⟹ **ℚ(i)** — *both of B675's banked fields reproduced from the cusp shapes* — and **m = 3..8 NOT quadratic**. **The answer:** B1002's gcd is defined only at `m = 1, 2`, both **units** of ℤ/15 and so both on **branch B**; `gcd(m,15)`'s content is on **branch A**, where the conductor fails at the very first member. **No `m` is informative for both — the comparison is not unknown but unaskable as posed**, and structurally so: the family is abelian only at its two smallest members, which is why B675's law could never be more than two instances. **Why PROCESS:** a domain map over banked laws, `creates_law` false with a dated review; B675 and B1002 untouched and correct; bronze non-abelianness **cited, not re-derived** (this arc proves only non-quadraticity for m ≥ 3); nothing reaches `CLAIMS.md`, F2 or Gate 5. Records **three more float-before-exact-test failures**, one of them inside the control written to catch the others (**E75** widened from keys to inputs). |
| xB001 | PROCESS | **the opponent's review: the package reproduces, the chronology does not, and five of the seat's own leads died.** External review seat (`xb`, `sep16-branch`), DESIGN sealed POST-HOC and labelled. Verified on a clean checkout in the exact pinned environment: seals **18/18**, locks **555 passed / 15 skipped / 0 failed**, and the fifteen gated cells under `OA_SLOW=1` **93 / 1 / 2** — the single red `gmpy2`, an undocumented dependency. An earlier reading of this seat that the suite *failed* was wrong (its own missing `scipy`/`python-flint`) and is **withdrawn**. The defect that stands: **the advertised `--locks` path skips all fifteen live re-derivations**, so the paper's own instructions certify without re-deriving. **The chronology claim does not survive** — 1267 of 1286 arcs in one import, design+seal+code+results sharing one commit date, some seals committed five days *after* the arcs they seal; the package README's "integrity, not time stamps" is the correct wording and `main.tex` §4's is not. **m004's congruence level (8) independently reproduced** (PSL-index 6, 6, 12 with the full centre; E21's `320 = 3840/12` reproduced), upholding B734 and B731's retraction. **B1157 and B151 tested per E19 and both HOLD** — no wrong kill found. Five leads dead: three false, two already banked (B1324/B1330/B1418; B136 = Goodman–Heard–Hodgson 2008). **Why PROCESS:** an audit; nothing reaches `CLAIMS.md`, F2 or Gate 5, no value is read against measurement, `creates_law` false. |
| xB002 | PROCESS | **the two owed scope notes, written — and a scope defect inside L1's own evidence arc, repaired.** `THE_LADDER.md` X15's notes **(1) L1** and **(2) L54**, recorded there as "still unwritten as of B1101" and verified still unwritten at this head (with 11/11 of `OPEN_PROBLEMS.md` §7a still `PENDING`), are delivered. **L1 corrected:** the genesis selects a **family**; **knot-ness selects the member** — extremality along the metallic diagonal selects a row, volume minimality **ties** (vol m003 = vol m004 exactly), arithmeticity is a commensurability-class invariant and **cannot discriminate between members at all**, and B1136's residue is one property, `H₁ = ℤ`. **L54 corrected:** gate A is sealed for `4₁` and its covers to index 6, not for "the object" in `COMPUTE_THE_PROGRAM`'s relational sense — newly computed, **5 of the 21 shape-field members sit at volume ratio 2.5** and are excluded from every cover census at any index. **NEW:** B1136's family is **14 only because its scan breaks at census index > 1200**; by tetrahedron count (≤ 6) it is **21**, the seven missed (`s955…s961`) sitting at indices 1256–1262, two of them (`s958`, `s959`) load-bearing elsewhere. **The conclusion survives and strengthens** — the separator set on 21 members is still exactly `['h1_is_Z']`; B1136 is confirmed and widened, **not** withdrawn. **Why PROCESS:** debt payment and synthesis; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB003 | PROCESS | **the monodromy on the object's own leaf: B1341's double tick IS the monodromy (proved), its fixed point sits on the ramified prime of ℚ(√−3), and the missing generating function has trace 5.** B1341 left the object's leaf explicitly open (`I = −1`, where its `I = 0` linearisation fails and its explicit `S` does not apply). **Proved here:** `T² = F_R ∘ F_L` identically, the two Dehn twists abelianising to `L` and `R` with `RL` of trace 3 — so the "double tick" is m004's monodromy, not an assumption; independently the literature presentation `φ(a)=ab, φ(b)=bab` induces exactly `F_L ∘ F_R`. **On the leaf `κ = −2`** (the Markov surface — **B1347's** identification, not this arc's) the monodromy has the node `(0,0,0)`, derivative eigenvalues the primitive cube roots of unity, and a conjugate pair of regular fixed points **every trace of which has norm 3 and equals `2+ω` or `2+ω̄`** — the **ramified prime above 3** of the object's own trace field. `D(monodromy)` there is **symplectic** (eigenvalue 1 transverse, pair `(5±√21)/2`, product 1), exhibiting B1341's theorem on the object's own leaf, and the generating function is `S = u²/2 − uU + 2U²`, **trace 5** against B1341's trace 3. **Three fences kept:** leading quadratic part only, not a global linearisation (resonances are unavoidable at a hyperbolic fixed point of an area-preserving map); `ℚ(√21)` **un-interpreted**, no reading offered; and this is trace-map dynamics, **not** the 4d field action TOE row 4 asks for. **Why PROCESS:** a dynamics-layer computation on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB004 | PROCESS | **B6's recipe at the object's point: it transports, and there is NO VACUUM there.** **PREREGISTRATION sealed (sha256 `90b65bf0…`) and PUSHED at `2f39b91` BEFORE any cell ran** — the order is checkable from the branch history, the property xB001 found the record's other seals cannot support. **C1 PASS**: the derivative on the leaf has multipliers `(5±√21)/2`, both positive real, so `log D` exists and the recipe transports. **C2 PASS**: at the object's point the field vanishes at the fixed point, so the potential is **quadratic** — where B6's, built from a Möbius (quadratic) field at a **boundary** point, is **cubic**; B7/B8/B9's cubic vertex is a `∂H` artefact. **C3, the finding, beyond the sealed prior**: `Hess = J⁻¹ log D` is symmetric with `det = −(log λ)² = −2.4549 < 0`, so the form is **indefinite** and the object's point is a **saddle**, against B8's `mass² = κ√5 = 1.9248 > 0` at a minimum — **there is no vacuum and no single mass² at the object's own point**. And it is **forced**: the monodromy is pseudo-Anosov (`|tr| = 3 > 2`) so its character-variety fixed point is hyperbolic by definition, while a stable minimum needs multipliers on the unit circle, i.e. an elliptic class — **B6's stability and the object's hyperbolicity are incompatible**, and B6 got stability only by standing at the boundary. **C4**: order-2 and order-3 terms both non-zero and `λ²·λ⁻¹ = λ`, so `u²v` is **resonant** — a vertex survives at next order, around a saddle. **Nothing withdrawn**: B6 states its own inserted reading and B6–B9 are correct under it. **Why PROCESS:** dynamics-layer computation on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB005 | PROCESS | **the node's ℤ/3 IS 2T/Q₈, realised dynamically — and that is exactly why the ℤ/3 recurrence is not evidence.** **PREREGISTRATION sealed (sha256 `afb4dc82…`) and PUSHED at `1b047ae` BEFORE any cell ran** — the order is checkable from the branch history. **Q1 PASS**: `F = F_L ∘ F_R` fixes the node `(0,0,0)` on the leaf `κ = −2`, with `D = [[0,0,1],[−1,0,0],[0,−1,0]]`, `D³ = I`, `D ≠ I` and spectrum `{1, ω, ω̄}` on the unit circle — an **elliptic finite-order** point, the exact complement of xB004's hyperbolic saddle at the *regular* fixed point (no tension: `dκ` vanishes at the node). **Q2 PASS — the identification, registered EARNED as I-31**: the node is the quaternion character `ρ(a)=i, ρ(b)=j, ρ(ab)=k` with `tr[i,j] = −2`, and the monodromy `φ(a)=ab, φ(b)=bab` sends `(i,j,k) ⟼ (k,i,j)` — a 3-cycle with no sign correction, **verified an automorphism of `Q₈` on all 64 products** and **verified OUTER**, hence the generator of `2T/Q₈`, the unique ℤ/3 in `Out(Q₈) ≅ S₃`. Not a group of the same order: **the same group acting the same way.** **Q3, the finding, and it OVERRULES this seat's own sealed prior** (which expected the node's ℤ/3 to be the one member *not* linked): `μ₃ ↔ ℚ(√−3)` is `ℚ(ζ₃) = ℚ(√−3)`, the **same object**; `2T/Q₈ ↔ Z(E₆)` is McKay (= I-1, EARNED), with `|Z(E₆)| = det(Cartan E₆) = 3` verified; `node ↔ 2T/Q₈` is Q2, **identical**. ~~All four are canonically linked~~ — **Q3 WAS A FALSE NEGATIVE, corrected the same day by this arc's own ADDENDUM 1 at the owner's instruction to reverify.** The three links connect `{node, 2T/Q₈, Z(E₆)}` and leave the **arithmetic** hat `μ₃ ⊂ ℚ(√−3)` joined by **no edge**; the "=> all four" is a non sequitur across that seam. **Q4a tests the missing edge exactly, on the object's own tower**: `(LR)ⁿ` is the `n`-fold cyclic cover of m004 along its fibration, so the invariant trace field is **constant** `ℚ(√−3)` (volumes 1×, 2×, 3×, 4× verified) while the node order runs **3, 3, 1, 3** — **`s961`, m004's own 3-fold fibred cover, has `ℚ(√−3)` and node order 1.** A quantity constant on a family is not the same fact as one that is not. **Q4b**, across 77 bundles: 31 of 34 order-3 members are **not** `ℚ(√−3)`, and 1 of 4 `ℚ(√−3)` members is **not** order 3 — dissociation in **both** directions. **So the hats are TWO INDEPENDENT FACTS, not one wearing four.** **Q4c prices the survivor DOWN**: the derivative image at the node is **`S₄`** (order 24, orders {1,2,3,4}, det 1 — the cube's rotation group), so the node order is a **congruence** condition; `DF_L`/`DF_R` are 4-cycles hence odd, so even-length words land in `A₄` where 8 of 12 elements have order 3 (census exact: even 34/51 = 0.6667, odd 0/26), and m004's `LR` is even — **0.58 bits**. **NET: Q3's HEADLINE survives, Q3's REASON does not** — right answer, wrong proof. **WITHDRAWN**: this arc's extension of B727/I-17 to a fourth hat; **B727 itself untouched**. **HARVEST**: the node's ℤ/3 is **not a commensurability invariant** (3 on m004, 1 on `s961`) while every arithmetic handle the programme uses **is** — the first item here that sees the manifold-with-its-fibration rather than the class, the right *type* for the record's open "which member of the class?" questions (I-6, `SCOPE_NOTE_L1`, L54); **priced as a lead only.** New error class **E82, the ASSUMED-CONNECTED LINKAGE class** — *a kill needs the same map a promotion needs*; B1231's rule is gated on promotions and nothing checked the same claim made to discard evidence. **Q1, Q2 and I-31 untouched.** The arithmetic untouched, and the closed negative **stands**: the arithmetic ℤ/3 = `2T/Q₈` is the **centre** of E₆, not the trinification grading — this arc never compared the node with the grading, and **I-14 stays UNEARNED**. **Why PROCESS:** character-variety and finite-group theory on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, no value, no generation count, `creates_law` false. |
| xB006 | PROCESS | **E82 is a class, but a small and shallow one: 3 candidates in 798 kills, and the primary one SURVIVES re-test with its missing link supplied.** **PREREGISTRATION sealed (sha256 `94b1f14c…`) and PUSHED at `504d6bd` BEFORE any cell ran.** **Why it exists:** E82 entered the error ledger the same day from **this seat's own** correction of xB005's Q3, and a seat that mints an error class from one self-inflicted error without checking whether it recurs has committed the same over-reach it is auditing. **The stratum was unswept**: B742's negatives hunt discriminated on *"a kill never EARNED by computation"*, but an E82 kill **is** earned and its computation is **correct** — **B742 would have passed xB005's Q3.** **S1 PASS**: the lexicon is calibrated **both ways** — it fires on a planted E82 kill and does **not** class **B727**, the record's own sound form, as a candidate. **S2**: 30 raw candidates. **Two self-corrections before any number was reported** — (1) a first draft added `by construction` while coding, which is **not in the sealed lexicon**, is descriptive prose (B425's *"golden BY CONSTRUCTION"*) and drove **27 of an initial 46** on its own: withdrawn; (2) **reporting 30 would have been the exact over-reach this arc audits**, so every candidate was read and adjudicated with its reason recorded and asserted to cover the sweep exactly — `BASE_RATE` **10** (rate computed), `LINK_NAMED` **8**, `PROSE` **5**, `MODEL` **3**, `NOT_A_KILL` **1**, **`E82` 3**. **S2b PASS, |C| = 3, inside the sealed prior's range 1–8.** **And the adjudication says something this seat did not expect to report: on this axis the record is DISCIPLINED, and its own output proves it** — ten kills carry a computed base rate, eight name the theorem doing the work, and three are the **model** (**B1223** states the discriminator itself; **B772** tested its own linkage hypothesis and **refuted it**; **B995** computed the dependence `(1−r)⁵` and **voided its own instrument**). **S3, the primary candidate re-tested:** B146's *"near-tautological … (**one fact**: short word ≈ low volume ≈ palindromic period)"* — on 135 once-punctured-torus bundles, *short word ≈ low volume* is **FALSE** (1095 inverted pairs; `LLRR` len 4 vol 3.6639 > `LLLLR` len 5 vol 3.1773), length barely carries volume information (2.54× spread at fixed length 9), and *low volume ≈ palindromic* is **FALSE** (912 inverted pairs). **The three are not one fact.** **But the other half, which the headline carries**: at the bottom the coupling is **structural** — all 12 lowest-volume bundles are palindromic, and **at every length the volume minimiser is the monotone word `Lⁿ⁻¹R`**, whose reverse `R Lⁿ⁻¹` is a rotation of itself, hence **cyclically palindromic by inspection**. **That is the map B146 never wrote**, and with it **B146's kill SURVIVES** — a scoping note, **not a revival**, exactly as the sealed S3 prior predicted. Secondaries reported, not re-tested: **B142** (low leverage — its decisive arm is the trace field being `ℚ(√−7)`/`ℚ(i)`, not this line) and **B1096**. **No arc re-verdicted, none retracted** — proposals go to `cc` with the computation attached. **Recall is NOT measured**, only calibrated: a kill phrased without a sealed construct is invisible, the deliberate B806 trade, and the true class may exceed 3. **Why PROCESS:** a corpus audit; nothing reaches `CLAIMS.md`, F2 or Gate 5, no value, no generation count, `creates_law` false. |
| xB007 | PROCESS | **the lead failed its first test, and failing it proved something better: the character variety is STRUCTURALLY BLIND to what separates the object from its sister.** **PREREGISTRATION sealed (sha256 `3c4bd90b…`) and PUSHED at `0e3cde6` BEFORE any cell ran**, with the inconvenient outcome required **in the headline**. xB005 Addendum 1 flagged the node's ℤ/3 as the right *type* for the record's *"which member of the class?"* questions, priced at 0.58 bits. **T4 returned B: the lead failed.** **T1 PASS** — `v₀ = vol(H³/PGL(2,O₃))` **derived, not hardcoded** (`L(χ₋₃,2)` by Hurwitz zeta, cross-checked to 30 digits); a first draft's `mp.nsum` **silently mis-converged** (0.7725 vs 0.78130), poisoning `v₀` so **every** member including m004 failed the filter — caught by re-deriving **B680's identity `vol(m004) = (3√3/2)·L(χ₋₃,2)`, which holds to 28 digits**. By Maclachlan–Reid the invariant trace field `ℚ(√−3)` **is** the commensurability class: population **21** (tetrahedra ≤ 6, reported), all volumes integer multiples of `v₀` — **the same 21 as xB002's widened B1136 family**, an independent arrival. **T2 PASS**: the class-invariant battery has **one distinct value each** across all 21 — zero separation power, so the diagnosis is about **type**, not weak tools. **T3**: **`H₁` is the only selector**, confirming **B1136's `['h1_is_Z']` at the commensurability level** — stronger than the family it was established on. **T4**: Cao–Meyerhoff's m003/m004 tie on volume, cusps, tetrahedra, |sym|, amphichirality, **word and node order**. **A first draft returned OUTCOME A here and it was wrong** — `bundle_words` scanned only `b++`, so m003 read `None ≠ 3`; **B995's rule applied** (an unexpected positive against a declared negative prior is when to be most suspicious), all four prefixes scanned, and **m003 = `b+-LR`, m004 = `b++LR` — same word, same node order 3**. **T6, beyond the seal**, because *"it cannot work, and here is why"* is worth more than *"it did not work"*: they differ **exactly by `−I`**; `H₁ = ℤ ⊕ coker(φ_*−I)` sees that sign (`det = −1 → ℤ`; `det = 5 → ℤ/5 ⊕ ℤ`, both matching SnapPy), so **knot-ness IS `det(φ_*−I) = ±1`, the homological shadow of the monodromy's sign**; and `−I` fixes `(X,Y,Z)` identically (verified), so the trace-map action **factors through `PSL(2,ℤ)`**. **The character variety quotients by exactly the datum that separates the object from its sister** — no invariant on that layer can ever break the tie. **Costs**: this **scopes xB003, xB004 and xB005**, all three of which live there. **Buys**: xB002's *"knot-ness selects the member"* is **not** an arbitrary input — it is the one homological datum that layer is blind to, so the selection happens **exactly at the seam**. A mechanism where the record had a label. **Not claimed**: that `H₁` is a derivation rather than an input. **Why PROCESS:** census computation on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB008 | PROCESS | **the identification discipline, extended from PROMOTIONS to KILLS: a gate that catches this seat's own error.** **PREREGISTRATION sealed (sha256 `30bb937b…`) and PUSHED at `91c9b94` BEFORE the gate was written.** **The hole:** B1231's Identification Rule is enforced on **promotions only** — `identification-register` reads a field an arc declares when it **claims** a sameness, never when it **uses** one to **discard** evidence. xB005's Q3 asserted four ℤ/3s were *"all canonically linked"* on links connecting three, and **nothing here caught it**; **nor could B742**, whose discriminator was *"a kill never EARNED by computation"* — an E82 kill **is** earned, with a **correct** computation. **The standing rule enforced: A KILL NEEDS THE SAME MAP A PROMOTION NEEDS.** **G1 PASS — the cell that mattered**: xB005's Q3 *as it originally stood* is classed `C`, so the gate **reds on the single error it was designed from**; the seal required that, failing this, the arc call it theatre and **not ship it**. **G2 PASS — the cell that makes G1 mean something**: the **corrected** Q3 is classed `R` and **passes**, so the gate tells a defect from its repair and does not train seats to stop writing *"not evidence"* rather than check their links. **G3 PASS**: **33/33 → 34/34**, 0 FAIL — and the suite's own `tracked-deps` and `practices-register` gates caught the new files and the unregistered gate first, which is the instrumentation working. **G4 PASS**: two-way selftest catches a planted E82 kill and clears **B727**, the record's own sound form. **G5 PASS**: the ratchet ratchets. **Completeness, never judgment** — a map, a **named theorem**, or a **computed base rate** (10 of xB006's 30, a sound and different argument that must never be flagged). **A ratchet, not a blocker**, for B1231's own reason. **The baseline is honest about itself**: 15 frozen entries each carrying **xB006's hand adjudication**, because publishing a raw count as *"wrong kills"* would be the same over-reach the instrument audits — of the 15, **2** are genuine E82 candidates, four have the link supplied, three are prose, one does no kill work, and **two are the MODELS** (B1223 states the discriminator; B772 refuted its own linkage hypothesis). **Recall bounded and stated**: a kill phrased without a lexicon construct is **invisible** — it reduces the class, it does not close it, and a lock keeps `by construction` **out** (it drove 27 of an initial 46 alone). **This seat proposes the gate on its own branch; landing it on `main` is `cc`'s call.** **Why PROCESS:** instrumentation; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB009 | PROCESS | **the odd sector is NOT the door: `√−3` cancels there too, and its whole content is one already-known bit.** **PREREGISTRATION sealed (`5d8ea291…`) and PUSHED at `ba60fac` BEFORE any cell ran**; short by the owner's instruction that a negative is not a deliverable, and by the seal's own commitment. **The gap was real**: xB007 proved the selection datum is the monodromy sign `−I`; `Sym^odd` sees it, `Sym^even` does not, and E₆'s powers {2,8,10,14,16,22} are **all even** (B1242: the 27 and 78 too) — so the entire computed record is even-sector and `Sym^odd` at `ρ_geo` had never been computed. **C0 PASS**: B425's own validations reproduce on this port. **O1 PASS**: `ρ_geo` is already `SL(2,ℂ)`-valued so `Sym^n` exists for all `n`; `H¹(M;ℤ/2) = ℤ/2` gives **exactly two lifts**. **O2, the headline, and THIS SEAT'S PRIOR WAS WRONG**: the Fox determinant is **Galois-invariant at every `n` from 0 to 7**, odd and even alike — **`√−3` cancels in the odd sector too, which carries NO NEW FIELD**. **O3**: odd does separate m003/m004 and even does not. **O4, which measures rather than celebrates it**: the **entire** difference is the substitution **`t → −t`** and nothing else (verified `n = 1,3,5`) — the sign character, **exactly one bit, and the same bit xB007 already named as knot-ness**. The traces `3, 7, 18, 47, 123, 322` are **Lucas `L₂…L₁₂`** — golden by construction, B423's dynamical side, not new. **Direction closed**, exactly as the seal named in advance. **One self-correction before shipping**: a first O4 draft printed *"genuinely new information"* on separation alone; overstated, replaced by a measurement. **Not claimed** (fenced in the seal): *odd = spin = 4d spinor = matter* is an I-10-class identification, not made, and now moot. Reuses **B425's machinery unchanged**. **Why PROCESS:** a closed lead; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB010 | PROCESS | **THE RE-DERIVATION RULE: a banked result is a HYPOTHESIS, not an answer — written, gated, and eaten by its own author.** **PREREGISTRATION sealed (`5ec6f8c5…`) and PUSHED at `b5ac068` BEFORE any code existed.** Owner instruction: *sweep first, and if what you planned already exists, **redo it anyway** — a negative may be misinformed, bugged or wrongly done.* **The gap was verified, not assumed**: B1202 and the absence-sweep rule govern **absence**, and `already_banked.py`'s own output says **"Read it"**, not *re-derive it* — nothing covered the case where the sweep **finds** something. **The asymmetry**: a wrong positive is re-tested downstream because people build on it; **a wrong negative is never re-tested, because it stopped everyone** — a bad kill is silent and permanent. **R1 PASS**: in `WORKING_RULES.md` and `docs/PRACTICES.md`, carrying **CITE ≠ RE-DERIVE**. **R2 PASS**: bound by a **frozen roster** (1257 arcs), not a numeric cutoff — demonstrated, since `xB009` parses to **9**, so any cutoff **would have exempted this seat's own arcs forever**; **xB010 is not on the roster**. **R3 PASS**: 34/34 → **35/35**. **R4 PASS, the cell that could go wrong by design**: an honest `NOT_RERUN` **with a reason passes** — a rule forbidding *"I did not re-run this"* produces **false declarations**, not re-derivation (the B1222 shape turned on ourselves); **silence** is what is impossible, not honesty. **R5 PASS, the author eats it first**: the statistic the rule argues from was **re-derived from B742's artifacts, not its prose** — `recompute/` holds exactly **32** targets and its table tallies **REVIVED = 2**; and **B146 is among them**, recomputed by B742 in July and re-tested by **xB006 today**, which found its kill sound but its **justification wrong** — two passes, two different defects. **And the honest limit is declared**: B742's *30 RECONFIRMED* is **not** independently confirmed here (parser tallies 8), so it is **SCOPED, not CONFIRMED** — not disputed, **unverified by this arc**, which is the distinction the rule exists to make visible. **Why PROCESS:** governance instrumentation; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB011 | PROCESS | **the owner's catch was right: B425's `√−3` cancellation is an AMPHICHIRALITY ARTEFACT, and xB009 tested the one object where the question is degenerate.** **PREREGISTRATION sealed (`642ccf4e…`) and PUSHED at `4b16b19` BEFORE any cell ran.** xB009 ran B425's Fox calculus on the figure-eight relator **alone** and compared two manifolds, yet claimed the **sector** — the **E70** shape. **Three defects of this seat's own, caught by F1's controls before any verdict**: the un-normalised Fox determinant is not an invariant (Wada normalisation added); the abelianisation is **not** the exponent sum (snappy's `aaabABBAb` forces `a↦0, b↦1`); and **snappy's `SL2C` lift sends the m004 relator to `−I`**, so `Sym^odd∘ρ` **is not a representation** and every odd-n number from it is **void** — repaired over `𝔽₂`. **B425 was right; the instrument was wrong.** **F1 PASS**: reproduces B425's exact values at n=0–3 up to the unit −1, **including odd n B425 never ran**, independently **confirming xB009's m004 result**. **F2**: 16 usable of 21 (5 excluded, `H₁` rank 2 — **reported, not dropped**). **F3, one way only**: **chiral ⟹ `√−3` survives, 8/8, no exception**; **the converse is FALSE** — m004 real at all n, m003/s955 at even only, and m206/m207/s957/s960/s961 **complex everywhere**. So the cancellation is an amphichirality artefact and **xB009's headline was false as a statement about the sector** — on chiral members `√−3` survives at **even n too**, so the odd/even axis was never operative: **chirality is**. **F4 PASS**: realness **stable under lift change**. **F5 PASS, and it closes the hope this arc raised**: m004 looked like a **second selector** (unique real-at-all-n, 1 of 16), but **`6₃` is also amphichiral and also real at every n** — realness tracks **amphichirality ∧ knot-ness**, both already known, so **xB007's `H₁`-only-selector finding STANDS**. A first draft of F3 printed *"chirality controls it"*; the table does not support it and the claim was **narrowed**. **Corrections landed**: B425 **SCOPED** (computations reproduced, not disputed), xB009 **CORRECTED** (m004 stands, sector claim does not) — banners on both. **Why PROCESS:** named mathematics on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB012 | PROCESS | **the object BELOW the axioms: A5 excludes something 24× smaller, unique, orientable, carrying the torsion the programme hunts — and the two cornerstone fields sit on opposite sides of that axiom.** **PREREGISTRATION sealed (`a8db856d…`) and PUSHED at `eb08a0c` BEFORE any cell ran.** **The owner required the foundations be read first, and it killed a claim this seat was about to make**: the manifold restriction is **not** smuggled — it is **axiom A5** (*"the first mixed closure is torsion-free"*), and the "hidden assumption" framing is **withdrawn before banking**. **V1 PASS**: the tower is **1 : 12 : 24** exactly — `H³/PGL(2,O₃)` (minimal cusped orientable **orbifold**, Meyerhoff, **unique**), m000 Gieseking (minimal cusped **manifold**, Adams, unique), m004 (minimal **orientable** manifold, **tied** with m003) — `v₀` derived from `L(χ₋₃,2)`, B680 holding to 25 digits, and **B197 re-derived**: `H₁(m003) = ℤ/5 ⊕ ℤ` vs `H₁(m004) = ℤ`, so **A5 is what breaks the tie**. **V2, the cell that decides the route — OUTCOME A**: `PGL(2,ℂ) ≅ PSL(2,ℂ)`, so the quotient is an **orientable** orbifold — **B1234's objection** (*"dropping orientability may BREAK THE TOOLS"*), which killed the Gieseking route, **does not apply here**, and no arc had tried this door. **V3 PASS**: finite orders in `PSL(2,O₃)` are **{2, 3}**, both exhibited — B302 re-derived. **V4 PASS, the structural result**: `O₃/(√−3) ≅ 𝔽₃` (ring map verified 2000/2000) and `SL(2,O₃) → SL(2,𝔽₃)` is **onto**, image order exactly 24 = |2T| — **the `ℚ(√−3) → 2T → McKay → E₆` spine is the Bianchi group's own reduction mod its ramified prime**, no knot, no cover, no choice; **B266's surjection is the RESTRICTION of it** to an index-24 subgroup. **V5, the bill and the actual finding**: the orbifold costs the entire knot layer (Alexander, A-polynomial, Jones/WRT, B425, xB009/xB011) and the entire fibration layer (`LR`, the trace map, xB003/xB004/xB005), **and A7's one bit — the origin of φ**. So **A5 buys the golden face and costs the torsion; dropping it buys the torsion and costs the golden face — `ℚ(√5)` and `ℚ(√−3)` sit on OPPOSITE SIDES OF ONE AXIOM**, and only one side has been walked. **Not claimed**: no physics; the orbifold's ℤ/3 is **not** identified with the trinification ℤ/3 (I-10/E82 class; xB005 priced that family at 0.58 bits); **base rate against generic Bianchi orbifolds owed and unmeasured**. **No axiom amended** — A5 is the record's. **Why PROCESS:** named mathematics on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
| xB013 | PROCESS | **the product of both faces supplies 3.39 bits and stops at a PAIR: the object of the two faces is m004 *and its sign-twin*, and the last bit is A5's.** **PREREGISTRATION sealed (`fd3568c3…`) and PUSHED at `063e19f` BEFORE any cell ran.** **X1, κ re-derived not cited**: at `ρ_geo`, `κ = tr[a,b] = 3/2 − (√3/2)i`, **`κ − 2 = ω²` exactly**, `|κ−2| = 1` (B309's unit obstruction), `κ = 2` ⟺ the cancellation completes — so **κ already IS the product of both faces** and this arc asks only whether it **selects**. Faces defined before computing: **Eisenstein** = invariant trace field `ℚ(√−3)` (xB007's 21-member class); **golden** = fibres as a o-p-t bundle with monodromy `|trace| = 3`. **The measurement**: Eisenstein alone **21** → fibres **6** → **golden 2** (`m003`, `m004`) → +A5 **1** (`m004`). **So the two faces cut 21 → 2: 3.39 bits**, and the golden face is **thin** (2/21, and 2/6 among those that fibre). **X4 → OUTCOME B, the sealed prior**: both survivors have identical volume `2.0298832128`, so the product returns a **PAIR** — and the reason is exact, `m003 = b+-LR` and `m004 = b++LR` share the **same word**, differing only by `−I`. **The residue is exactly ONE BIT and it is not new**: it is xB007's knot-ness `det(φ_*−I) = ±1`, **the bit xB007 proved the character variety cannot see**. **A correction this arc made to itself before shipping**: a first X5 draft said *"the intersection is a TOWER"* — **the table contradicts it**, since `(LR)ⁿ` for `n ≥ 2` has `|trace|` 7, 18 = Lucas `L₄, L₆`, **not 3**, so `m206`/`s961` are **not** golden; replaced by a measurement. **Established**: κ is the product of both faces; the product is a **strong** selector but **does not close**; **the object of the two faces is the PAIR `{m003, m004}`, not either alone** — the faces supply 3.39 bits and the **axiom supplies the last one**. **Not claimed**: no physics, no identification, no link graph (E82); base rates reported; the 6/21 fibred count is a **lower bound** (word length ≤ 9). **Why PROCESS:** named mathematics on banked structure; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. | **CORRECTED 2026-09-17 BY ITS OWN ADDENDUM 1 (owner-instructed re-test): THE PAIR DOES NOT HOLD AND THE BIT-COUNT IS WITHDRAWN.** Two defects, both this seat's. **(1) The faces were defined ASYMMETRICALLY** — Eisenstein as a **field**, golden as a **single trace** — and under field-vs-field **the whole tower is golden** (`L₂ₙ²−4 = 5F₂ₙ²`, verified n = 1…10); **X5's first draft said "a TOWER" and this seat overrode it — the first draft was right.** **(2) Both sets are INFINITE** — every `(LR)ⁿ` is a cyclic cover of m004 at volume `24n·v₀` (verified n = 1…6) — and the ratio **grows** with the cutoff (2.25 → 3.50 → 4.17), so *"21 → 2, 3.39 bits"* measures the **census window** and is **withdrawn**. **Corrected picture**: class (∞) → [both faces] → golden tower (∞) → [A6 minimality] → pair → [A5] → m004 — **the two faces supply NO finite selection; every finite step is an axiom.** **Answering the owner**: the two-element set is **not** sufficient and is not what the faces give; and **yes, the whole family is needed** — the intersection of the two faces **IS an infinite family**, the cyclic tower over m004, *what the faces actually determine* rather than a fallback. **Survives**: κ as the product of both faces, and that the intersection is not the whole class. **This strengthens the owner's thesis.**
| xB014 | PROCESS | **PATH A DIES: the ladder is real and generic, and m004 is rank 38 of 40 on the both-faces ratio.** **PREREGISTRATION sealed (`bc8a71ed…`) and PUSHED at `63fc1f1` BEFORE any cell ran**; short by the owner's rule and the seal's own commitment. **Z1 PASS — B207 SCOPED, not disputed**: its negative (*"no intrinsic exponential hierarchy"*) was **re-derived** and tested the **m-family** `RᵐLᵐ` — volumes **bounded** (2.0299 → 6.3914, ratio 3.1), `log λ_m` logarithmic — while this arc's **n-tower** `(LR)ⁿ` has volumes **linear/unbounded** and torsion **exponential**; B207 itself names the entropy/volume ratio as the entry point. **Z2 PASS**: the tower's torsion is **exactly** `L_n²` (odd) / `5F_n²` (even), rate → `log φ² = 0.96242365`, reproducing **Silver–Williams** (verified, `M(t²−3t+1) = φ²`) — **but that growth is GENERIC to every fibred knot**, so the ladder's existence is not news. **Z3**: the both-faces ratio `log λ / vol = 0.474127597`, **constant along the tower**, golden numerator over Eisenstein denominator. **Z4, decisive — the base rate kills it**: against Kojima–McShane's `1/(3π) = 0.1061`, over 40 once-punctured-torus bundles, **m004 ranks 38 of 40** (max `t00000` at 0.636080 vs m004's 0.474128). **Near the bottom, not extremal. PATH A DIES**, as the seal's weak prior expected. **One correction before any verdict**: a first Z1 draft asserted a magnitude (`tor₆ > 100·tor₂`), which failed at 320 < 500 — a badly chosen test, replaced by the **rate**. **Not claimed**: that a dimensionless ladder is a physical **scale** or that it crosses B1012's wall (that wall is **dimensionful**; a ladder is a dimensionless **index**). **Why PROCESS:** a closed path; nothing reaches `CLAIMS.md`, F2 or Gate 5, `creates_law` false. |
