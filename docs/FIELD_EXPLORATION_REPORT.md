# THE FIELD EXPLORATION REPORT — the masterplan's close (W6, 2026-08-18)

**The owner's question that opened it** ("did we explore the field until the end?") is
now answerable: the field is explored to its named edges — every gap that remains is
REGISTERED with an owner, a route, or an honest floor, and nothing is silently absent.
Synthesis authored on the banking seat; checked by two adversarial agents over the
banked state (their findings and dispositions in §4). `doc-currency` living document.

## 1. The field exploration, closed: what the six workflows + Track A returned

- **W1/B1067**: the ray-class asset double-rebuilt; the palette 1/2/8; ℚ(ζ₁₂) one-object;
  K's class group OPEN (953-place) — the one named field blocker, NOT in the crossing path.
- **W2/B1068**: 31 descent verdicts (17/10/4); the character-channel mirror (ω↔abelian,
  φ↔defining — one Lagrange fact); the splitting-rigidity discriminator H¹ = 0/1/0 at
  3/5/7 (p=5 uniquely non-rigid; prior-art owed); 15-row honest tail (critic unconverged).
- **W3/B1069**: the hearing biography — palette 1,1,2 vs 1,2,8 (unit rank discriminates);
  narrow (4)-ray class (ℤ/2)² non-cyclic; orders tower unified; the B92 catch (E42);
  the F11 on-ramp (B737's multiplier-ring language).
- **W4** (lead-update, banked): the 77-echo IS B882's conjecture; undressed datum → split ℚ³;
  KMRT §43 floor typed.
- **W5/L166**: the listener-map spec, licensed, AC4 repaired to AC4′.
- **Track A/B1070**: Λ derived — {u3,u6} = the unique Galois-fixed vertex pair; B641 a
  theorem; the discriminating channels closed-form; the mirror values u-independent.

## The ranking (one criterion: what feeds THE CROSSING CELL)

1. **THE SEALED LISTENER CELL** (B1070's promotion path): seal Λ's definition + the claim +
   the three theorem inputs; fresh re-derivation under seal. THE gate to the prereg.
2. **THE SILVER INSTRUMENT** (Track B1 = L149 + AC3 fused): the transplant target without
   which AC3 cannot run. Build = the own-channel analog stage.
3. **THE CONTACT-QUANTITY KIND ROW**: the crossing predicts Bloch pairings / even quadratic
   forms at Λ's output — a kind the table must admit BEFORE the seal (A4's ledger names the
   mirror row; B1070 proves the banked mirror VALUES are u-independent — the contact row is
   the FUNCTIONALS, not the value set; adjudicate + register in the prereg).
4. **S4-typing of B1000's five closings** (the register's other unstruck item).
5. Paper-grade, not crossing-path: the splitting-rigidity theorem (prior-art gate first);
   the character-channel mirror; the no-moduli theorem (B874 addendum); C28's one-line model.
6. Parallel/held: B882 via KMRT §43 (floor); F11 via the B737 on-ramp; K's 953-place;
   the SL(2,𝔽₄) bridge (j6); the 15-row descent tail.

## The crossing cell's shape (design facts, from the banked record)

- Channel: COUPLING (R10 declared); anchors: NONE (R11's open lane).
- The listener: Λ's output, sealed-derived (rank 1 above) — never chosen.
- Predictions: the u-dependent functionals at u3 (exact ℚ(ζ₆₀) numbers): Im-odd Bloch
  pairings (m = 1..5; axes exact) + even forms u†T_m u (m = 1, 2, 4).
- The comparison row: ONE licensed contact (the consumption ledger); new-seal-new-arc;
  R1–R11 + the twelve as rows all checkable; the location clause binds; MB12 on every
  criterion (AC4′'s two-direction witness is already exhibited by A2/A3).
- Failure mode stated in advance: a MISS under these requirements is the first crossing
  whose failure could not be blamed on interpolation, hemisphere, or normalisation
  (CROSSING_REQUIREMENTS §4's own sentence) — and the paper carries it either way.

## 2. THE REVISED SEQUENCING (the reader's two BLOCKING findings, adopted)

The draft's ranking stands EXCEPT where the adversarial reader broke it:

1. **The B1071 seal certifies THE DERIVATION, not a completed listener map.** The spec's
   own closing line binds: failing any AC = not a listener map. AC3 (sister transplant)
   cannot run before the silver instrument exists; AC6 (type conformance — menu-valued
   functionals only, no generic reals) is unattempted, and Track A's own scope fence
   excludes it. The seal's claim is scoped accordingly (the draft inherits this).
2. **R1 now has an owner.** The outside-B687's-atlas / outside-B743's-tower sweep of the
   crossing's predicted quantities (the Bloch pairings; the T_m forms at Λ's output) is a
   COMPUTATION with the check shown, and it runs before the prereg — R1's forcing
   failure (B1005) was exactly inadequately-searched ground.
3. **R6–R9 are prereg-authoring obligations, named**: R7 is substantive here (the fresh
   designer freedoms — which m ∈ {1,2,4}, Im vs Re, which member of the derived pair —
   get a look-elsewhere ledger before the seal).
4. **The character-channel mirror re-ranked crossing-adjacent** (χ's 1-dimensionality is
   what lets M_odd = χ·W factor cleanly — the reader's cross-check, now a LAW_MAP note).

**The path to the crossing, in order**: B1071 seal (the derivation) → the silver
instrument (L149 + AC3 fused) → AC6's type-run + R1's sweep + R7's ledger → THE
CROSSING PREREG (channel coupling; anchors none; the one licensed row; every R-row and
the twelve as rows checkable; the location clause) → seal → execute → the paper carries
the outcome either way.

## 3. What remains open, honestly (the register of edges)

- **The 15-row descent tail** (B1068's critic proposals, untested — BLIND rungs).
- **AC3/AC6** — the two unattempted acceptance criteria (owners: the silver build; the
  type-run).
- **B882's conjecture** (the 77-echo's true form; KMRT §43 floor) — its own lane.
- **F11** — the golden ledger's one GAP, with its first named on-ramp (B1069).
- **K's class group** (K = the charge field; the 953-place) — biography completeness.
- **L154's σ-identification** — the zero-anchor prize, strengthening not blocking.
- **The prior-art gates owed at promotion**: the splitting-rigidity discriminator (the
  classical p = 5 exception); the listener derivation (Klein's orbit structure classical;
  the identification is the content).

## 4. The W6 checks — findings and dispositions (2 agents, 13 findings, all dispatched)

**The adversarial reader**: 2 BLOCKING (R1 ownerless; the seal mis-scoped as "THE gate")
— both ADOPTED into §2. 3 AMEND (R6–R9 owners — adopted; the uncommitted B1070
amendment — landed this bank; the mirror's re-rank — adopted + LAW_MAP note). NOTES:
the twelve-faces wording (REPAIRED in the spec — an obligation, not a completed sweep);
the fourth-crossing ordinal (CLARIFIED: "the fourth SEALED crossing" is the register's
count — B915/B925/B929 + the designed cell; value-contact arcs are counted separately).

**The consolidation check**: 1 BLOCKING (the unlabeled K double-referent — RULED in
TERMINOLOGY: unqualified K = the charge field; instances labeled in CAMPAIGN_STATUS and
here). AMENDs: B1069's degree-word (REPAIRED: K is the cubic; its S₃ closure is degree
6); L166's stale entry (UPDATED with Track A's outcome). NOTES: the H¹(SL(2,5)) name
guard (defining-char vs char-0 — cross-referenced in the LAW_MAP row); the arc-ID
collision (already ruled — the reserved bands, the alias table); the owed LAW_MAP rows
(WRITTEN, three); "conductor" at four senses (TERMINOLOGY extended).

**Checks record**: wf_e37891cf-ba1 (2 agents, 0 errors); the findings JSON in the
session record; every BLOCKING and AMEND dispatched in this same bank.
