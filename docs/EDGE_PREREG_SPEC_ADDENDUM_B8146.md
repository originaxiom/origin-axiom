# EDGE_PREREG_SPEC — addendum-beside (2026-08-27, B1171; per the seal's own amendment rule)

**The sealed spec (`docs/EDGE_PREREG_SPEC.md`, sha 6ede5c8d…, sealed 2026-08-21) is NOT edited.** Its seal
row licenses exactly this: *"placeholder R6 per the owner's D-3, amendable by addendum-beside."* This
addendum re-poses §4's R6 on the finding of cc3's **B8146** (L173 precision column, closed NEGATIVE),
verified-read at `origin/paper/structure-genesis-first` 835bc815.

## The finding (cc3's, adopted)

The banked prediction (B8094/B1085) is a **COUNT** — the function ρ ↦ edge content, **5-vs-6
boundary-capable modes at ρ = α** — not an energy. So R6's question, *"what energy resolution is
experimentally standard,"* was **mis-posed from the start**: the right question is *"can they count modes in
a labelled gap"* — and the anchor experiment does not make that measurement. From the anchor
(Verbin–Zilberberg–Kraus, arXiv 1403.7124 = PRB 91 064201): arrays of 13–28 waveguides; the phason scanned
at ~13 discrete fabricated arrays (~π/6 spacing); **no error bars, no uncertainties, no resolution limits
anywhere**; mode identification qualitative ("light … remains there"), no numerical counts. *"A
demonstration paper, not metrology."* Absence in the source, not failure in the search.

**The program supplies the KNOB (the phason = our intercept, B8094) but not the READOUT.**

## R6, re-posed (superseding the §4 placeholder's FORM, per D-3's amendability)

The prereg's falsification interface must specify a required **OBSERVABLE**, not a required RESOLUTION:

> **R6′ (the commissioned observable):** the number of boundary-capable edge modes in a *labelled* Fibonacci
> gap, as a function of the scanned phason ρ, on a chain long enough to separate a 5-count from a 6-count at
> the preregistered windows — with the mode-count (not mode-energy) as the reported quantity, and the
> localization SIDE per §1.2 as the hand-resolving differential. A resolution figure is a threshold to
> clear; a missing measurement is something to **commission** — the "who can falsify us" section commissions
> a mode count rather than assuming one exists.

Everything else in the sealed spec — §0's conceded-standard fence, §1's two-hand differential, §2's kill
conditions K1–K3, §3's C-GAP firewall — **stands unchanged**. cc3's scope carried: not claimed that no
experiment anywhere counts edge modes, nor that the prediction is unfalsifiable; one anchor read in full
(Nature 553 59, arXiv 2201.04820, the 4D-QH line unread).

## Provenance (per the seal-provenance rule)

**BANKED IDENTITY:** the observable this addendum commissions is exactly the banked one — B1095/B8094's
computed differential (the cross-hand mirror-isospectral split; the 5-right/6-left boundary-capable count at
N=987, ρ=α) — reproduced in the sealed spec's §1 before any new quantity is named; this addendum adds **no
new predicted number**, it re-poses the *readout* of the already-banked count.

**PRIOR ART:** cc3's B8146 anchor read (arXiv 1403.7124 in full; Nature 553 59 / arXiv 2201.04820 / the
4D-QH line explicitly unread — their scope), plus the sealed spec's own §0 concession that raw
edge-mode-crossing counts are standard topological-pumping content (B1095, banked pre-seal). The bank grep
for prior mode-count commissions: none (this is the first; the R6 placeholder was a resolution figure).

*Registered beside the seal; SEAL_LEDGER row added same-commit. cc3 credited (B8146).* 
