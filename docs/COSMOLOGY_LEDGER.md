# THE COSMOLOGY LEDGER — the missing specification surface (GC-28)

**Created 2026-08-28 (GC-28, owner-directed): "one cosmology specification ledger, not three
probes" — THE_CAMPAIGN.md row 24's named first move, never made until this file.** THE_LADDER.md's
own taxonomy grades X3 (dark matter), X4 (inflation), X5 (big bang/initial condition) **BLIND**:
not because no computation ever touched these words (several arcs do, listed row by row below) but
because the corpus never assembled those touches into one dispositioned specification the way the
grand-computation ledger did for the arena/clock/meter/content/coupling/sign/label rows. This file
does that assembly. It draws **only on banked results** — nothing here is computed fresh; every
number is cited to the arc that produced it, and every row states plainly whether it is the
**OBJECT's** structural gift or the **CLOSER's** (observer's) unbuilt dynamics.

**Row schema** (matching `docs/GRAND_COMPUTATION_LEDGER.md`): status ∈ **HAVE** (arc, computed and
standing) / **PARTIAL** (part computed, part open, gap named) / **IMPOSSIBLE-BY-THEOREM** (a proof
of permanent unreachability, not a to-do) / **MISSING** (no dedicated arc; first computable probe
named). Gate 5 holds throughout: no measured physical value enters any row as an *input* to a
claim; measured values appear only as **comparison targets for computed negatives** (H131's Planck
TT residuals, B730 Q3's Ω_k/topology searches, B563's Planck-ratio null) — the same discipline
already governing the rest of the record.

---

## THE HONEST HEADLINE

**Cosmology in this program is two disjoint things wearing one name.**

1. **The OBJECT's structural gifts** — forced, exact, dimensionless, theorem-grade, and *already
   banked* before this ledger existed: the no-beginning theorem (row 1), the frustrated-cancellation
   founding fact (row 1), the Sakharov gate's three structural preconditions (row 6), the forced 3d
   Einstein solution with Λ = −1 (row 5), the accretion/generative arrow (row 3). These are real
   mathematics about *this one construction* (GOVERNANCE §2's framing lock) — not modesty, not
   hand-waving.
2. **The CLOSER's unbuilt dynamics** — rates, temperatures, a literal timeline, the measured value
   of Λ, a stable dark-matter candidate with a proved protecting symmetry, an inflaton, a CMB power
   spectrum, a structure-formation history. **None of these exist anywhere in the record.** Three of
   the eight rows below (dark matter, inflation, structure formation) have *zero* dedicated modern
   arcs — this is the one place in the whole existence audit where the honest word is not PARTIAL
   or IMPOSSIBLE-BY-THEOREM but plain **MISSING**, and each such row below names the first
   computable probe rather than leaving the gap unlocated.

The dividing line is exact and matches the rest of the record's own wall: **the object supplies
boundaries, the observer supplies every closing** (B716's own words, re-used here because it is
literally the same theorem doing the same work in the cosmology domain).

---

## THE EIGHT ROWS

| # | ingredient | status | the close |
|---|---|---|---|
| 1 | **The initial condition** | **PARTIAL** — see below | the object supplies the axiom chain + the founding non-cancellation fact + a proof there was no beginning; it does not and (by three independently STALLED mechanism attempts) apparently cannot supply a literal creation *event* |
| 2 | **Inflation-or-alternative** | **MISSING** — see below | first probe named below; zero SM-facing arc exists |
| 3 | **The expansion history** | **PARTIAL, mostly IMPOSSIBLE-BY-THEOREM for values** | the object supplies a dimensionless generative *arrow*, not a rate; the scale-torsor theorem forbids any dimensionful H(z) permanently |
| 4 | **Dark matter** | **MISSING, with one computed negative already on the books** | first probe named below (N1/N2 stability) |
| 5 | **Dark energy / Λ** | **HAVE (own units) / IMPOSSIBLE-BY-THEOREM (measured value)** | fully typed already (B1194/EX-2); reproduced here for the ledger's completeness |
| 6 | **Baryogenesis** | **HAVE (structural stage) / MISSING (rate)** | the Sakharov gate is exhibited; no rate, temperature, or asymmetry number exists or is claimed to |
| 7 | **The CMB's place** | **MISSING, with two computed negatives already on the books** | first probe named below |
| 8 | **Structure formation** | **MISSING** | first probe named below; zero dedicated arcs found |

---

## ROW 1 — THE INITIAL CONDITION

**HAVE (object side):**
- **The genesis axiom chain, priced** (P019, corrected by B998/B1003): not-nothing → A0 (being =
  description) → L1 (binary alphabet) → A2 (inexhaustibility/aperiodicity) → T3 (Morse–Hedlund) →
  T4 (golden slope, Hurwitz-extremal fixed point) → A5/A5b (carrier + puncture) → A6 (orientation) →
  T7 (Thurston/Riley/Reid: the figure-eight complement, forced). Re-priced exactly: **two owned
  choices (orientation, puncture) + theorems**, not thirteen unexamined premises.
- **The frustrated-cancellation founding fact** (B309/B518): κ = tr[a,b] = u² + 2; at u = 0
  (abelian, "nothing"): κ = 2; at the object's own point u = ω: **κ − 2 = ω², |κ − 2| = 1** — the
  minimal nontrivial (unit) obstruction to cancellation. Modality-corrected: "cannot cancel" is
  retired overreach; the honest statement is "generically does not cancel, and this object sits at
  distance exactly 1" (S_SEED_FIELD_COSMOGONY.md, binding).
- **No-beginning, proved** (B716, three sealed probes, 16 agents, 0 errors): the object's only
  internal time is the Anosov suspension of σ = [[2,1],[1,1]], **amphichiral** (14+ independent
  GL(2,ℤ) conjugators confirm σ conjugate to σ⁻¹; SnapPy `is_amphicheiral = True`) ⇒ no canonical
  arrow; the fibration base is a **compact periodic S¹, not a non-compact ℝ-line** ⇒ no infinite
  past, no beginning-as-an-event. (Independently re-derived kappa arithmetic and the amphichirality
  conjugator count in the EX-1 verification pass, both confirmed.)
- **The firewalled reframe** (S035): "Big Bang" survives only as *onset-of-non-cancellation*, not
  an engineered universe — explicitly not a literal creation event.

**STALLED (three independent from-nothing mechanism candidates, pre-genesis-era `paths/` probes,
each smuggling exactly the structure it claims to derive):**
- **`paths/E5_vilenkin_tunneling`**: the minisuperspace WKB tunneling amplitude reproduces the
  standard Vilenkin exponent `B(Λ) = 1/Λ` exactly (confirmed to ~1e-14) and is non-zero — but the
  mechanism is **not generic**: it presupposes an S³ FRW topology, a minisuperspace truncation, an
  *input* value of Λ (the mechanism consumes Λ, never derives it), an operator-ordering choice, and
  a boundary-condition choice (tunneling vs no-boundary vs DeWitt). Verdict: **STALLED** — "the
  framework it presupposes is the one it claims to produce."
- **`paths/E11_entropic_emergence`**: the combinatorics are exact (P(empty) = 2⁻ⁿ, peak
  multiplicity C(n, n/2), entropic pull growing linearly in n, no free parameters) — but the
  argument runs *inside* a pre-built configuration space and measure; it selects within possibility,
  it does not produce possibility. Verdict: **STALLED**.
- **`frontier/B4_bkl_gutzwiller` + `frontier/B23_bkl_gravity_controls`** (near-singularity, not
  creation, but the record's only BKL-cosmology material): the figure-eight orbit (word `LR`,
  trace 3) is the shortest primitive periodic orbit of the modular billiard, length `4·log φ`; at
  BKL parameter u = φ the three Kasner exponents `p₁ = −1/(2φ), p₂ = 1/2, p₃ = φ/2` form a geometric
  progression — the *unique* u with this property — and in a Gutzwiller sum over orbits to trace 17
  it contributes 37.8% (first-among-equals, ratio ~1.13 to the runner-up, **not dominant**). B23's
  trace-map perturbation at (1,1,1) gives the exact nonlinear term `2uw`, structurally bounce-like,
  but **no explicit dictionary to Misner/Kasner variables is derived**. Both verdicts: **STALLED**
  — "the nonlinear term is isolated; the gravity dictionary is missing," and every Kasner solution
  satisfies the vacuum Einstein equations, so nothing here *selects* the golden orbit as physical.

**IMPOSSIBLE-BY-THEOREM (as a spacetime cosmology, distinct from no-beginning above):** B716's three
probes jointly show time, 4d-ness, and Lorentzian signature are **all observer-coupling, not object
content** — Ω₃^SO = 0 (4d fillings exist, infinitely non-unique, nothing selects one); H³ is
isotropic (no invariant complex-time direction to Wick-rotate; Chern–Simons saddle actions are real,
= 0, at the amphichirality fixed point).

**The close:** the object supplies an axiom chain, a founding non-cancellation fact, and a proof
there was no external beginning. It does **not** supply a mechanism that derives its own starting
structure — three independent candidate mechanisms (quantum-cosmological, statistical, dynamical)
were tried and each smuggles exactly the ingredient (a Hilbert space, a measure, a metric variable
set) that the "from nothing" framing claimed to produce. No fourth mechanism class has been tried.
**First open probe, if this is re-attempted:** does the object's *own* forced structure (the golden
substitution σ, the Perron entropy log φ) supply either a configuration space or a measure *without*
importing FRW cosmology first — the specific failure mode common to all three STALLED attempts?
Unasked as of this ledger.

---

## ROW 2 — INFLATION-OR-ALTERNATIVE

**MISSING.** No dedicated arc computes an inflaton, a horizon-problem resolution, a flatness-problem
resolution, or an e-fold count from the object.

**The one number that was tried and explicitly retracted:** `speculations/S008_orbit_count_efolds.md`
proposed "iterations ≈ e-folds" (~62 tower iterations ≈ 60 observed e-folds). **S002/B124/V113
REMOVED this as value-matching** (T4/S008 gate) — it is not a derivation, it is a numerical
coincidence flagged and killed by the record's own discipline. The corrected reading (S002): the
linearized tower spectrum is **reciprocal-closed** (λ, 1/λ pairs) with expanding-mode count exactly
equal to contracting-mode count at every order and both determinant signs — **two-headed time, not
a one-way inflationary arrow.**

**The only STALLED candidate that touches this territory** is `paths/E5_vilenkin_tunneling` (row 1)
— a "tunneling from nothing" mechanism is the standard alternative-to-inflation initial-condition
story in the literature it cites, and it stalls for the same reason (imports the FRW Hilbert space,
Λ, and boundary condition it was meant to produce).

**First computable probe, named here for the first time:** the object's own tick/clock is now typed
exactly — **ONE generator A₁ = RL**, with a proved nilpotent depth-grading (B1149/B1150: 7 allowed /
11 forbidden Yukawa blocks read this same grading) and a Perron–Frobenius entropy `log φ` for the
generating substitution σ (S045, [MATH]-tagged, verified: σ = a→ab is the minimal-entropy primitive
two-letter substitution). **Unasked question:** does *any* dimensionless ratio built from this
depth-grading or this entropy reproduce a horizon-problem or flatness-problem *structural signature*
(e.g., a scale-invariant spectral index n_s, without fitting to the measured 0.965) under a
preregistered, two-sided, Gate-5-compliant test? Not run. Given S045's own explicit finding that the
Friedmann route H² ∼ ρ is **closed** (the object's metallic integer m is fixed while the cosmological
coupling Ω_m runs — a constant cannot equal a running ratio), the honest prior is that this probe
would likely stall the same way rows 1's three attempts did, but it has never actually been run, so
"MISSING" and not "IMPOSSIBLE-BY-THEOREM" is the correct grade.

---

## ROW 3 — THE EXPANSION HISTORY

**HAVE (dimensionless structure only) — the generative/accretion reading** (B168, on S035's
reframe): the Ω strict-full cone read as forward-only accretion: histories grow 96 → 2,488,080,
matrices 36 → 65,472, classes 1 → 283 over the measured depth. Non-cancelling histories carry
**entropy log₂ > 0**; the cancelling cone's endpoint entropy is **exactly 0** — "non-cancellation
generates; cancellation does not," a genuine dimensionless arrow. Retention rate decreases and
decelerates: 0.583 → 0.476 → 0.454 → 0.419 → 0.413 → 0.397, structurally NOT i.i.d.-constant (a
null test, G3, rules out the trivial alternative).

**IMPOSSIBLE-BY-THEOREM (any dimensionful rate)** — three independent doors, three closures, one
wall (B167, B168's own G4, B170/V164):
- **G4 (B168, decisive):** every emergent quantity in the ensemble is a **dimensionless pure
  ratio-of-counts** — no scale emerges from the accretion ensemble, the third independent
  confirmation (after B107/B151's single-invariant door and B167) that the scale wall holds in the
  generative mode too.
- **B167:** a single conserved invariant forbids a scale.
- **B170/V164:** the "Machian, growth-rate-is-the-only-scale" leap is assessed **undecidable from
  within** — its only testable form is value-matching, which is dead per S014's null control.
- **The general theorem behind all three:** the dimensionful no-go / scale-torsor theorem (LAW_MAP
  §E wall 10; B666 cell S): Hom(G, ℝ⁺) = 0 for the relevant structure group — "the only equivariant
  map into any nontrivial scale representation is T = 0." This is exactly the theorem that also
  forbids a measured value of Λ (row 5) and forbids H₀, w(z), or any expansion RATE by the same
  mechanism.
- **S045's explicit closure of the naive route:** Friedmann H² ∼ ρ was tried as a direct reading of
  the object's flow and is **closed** — the metallic parameter m is a fixed integer while the matter
  coupling Ω_m is time-dependent; a constant cannot equal a running ratio.

**The close:** the object supplies a real, computed, forward-only arrow with a growth law and a
decelerating retention rate — a genuine expansion-shaped structure, dimensionless throughout. It
cannot and by theorem never will supply an expansion *rate*, a value of H₀, or an equation-of-state
history: this is the scale wall relocating into cosmology, not dissolving.

---

## ROW 4 — DARK MATTER

**MISSING, but with one computed negative already on the books and one first probe named.**

**The one existing arc (X3's "1 arc"): B657 THE INVARIANT LINE — a specific dark-matter-candidate
identification REFUTED exactly.** The pre-existing `DARK_SECTOR_CAMPAIGN.md` (2026-07-17,
`frontier/B660_structure_campaign`) proposed reading the 27's principal-SL(2) invariant section
(h⁰(M;27) = 1, from the V₀ block of 27 = V₁₆ ⊕ V₈ ⊕ V₀) as "the dark matter candidate," on the
grounds that it carries zero SM charges and is absent from the cubic coupling Y (B637). **B657
computed the actual overlap and found it FALSE**: the GUT singlet (the SO(10)-branching singlet of
27 under E₆ ⊃ SO(10)×U(1)) sits at principal-grading eigenvalue h_pr = ±16 — the *extreme* of the
grading — while the invariant section v₀'s coefficient at that same singlet weight is **exactly
zero, in both branchings tested**. "The relayed 'h⁰ section = gauge singlet = dark matter candidate'
was a conflation of two decompositions of the 27 (GUT branching vs principal SL(2)). The label never
entered the repo; it dies here." (`frontier/B657_invariant_line/FINDINGS.md`) A genuine, precise,
Gate-5-clean negative: **one specific dark-matter identification attempt is dead**, cleanly, by
computation — not by absence of search.

**What DID survive from that campaign, structurally (not dark-matter-labeled):** THE PORTAL LAW
(B657 W1) — cupping with the invariant line v₀ (via the Jordan cross product 27 ⊗ 27 → 27̄) is a
**rank-5 isomorphism** of the double's H¹(D;27) onto its dual, exactly block-diagonal, sector-
respecting on the golden end. This is a real computed fact about the object's cohomology; it is
*not* claimed as a dark-matter portal coupling (that label required the refuted identification).

**The first computable probe, as directed:** the 27's roster under E₆ ⊃ SO(10)×U(1) is **27 = 16 ⊕
10 ⊕ 1** (cited standard, Slansky 1981; confirmed dimension-consistent 16+10+1=27; not independently
re-derived here — B1135 flags this exact citation as "CITED standard... not independently
recomputed"). This gives **two** SM-gauge-neutral fermion slots per generation:
- **N1** — the SO(10)-singlet living directly in the "1" of 27 = 16 ⊕ 10 ⊕ 1.
- **N2** — the SU(5)-singlet living inside the "16" (16 = 10 ⊕ 5̄ ⊕ 1 under SU(5)), the
  sterile/right-handed-neutrino-type state.

Their **coupling roles** are partially banked: B1185/memo 80 established that the object-channel
Yukawa kinematics for the up-type coupling **exist with 6 nonzero components** (not automatically
zero), while the heterotic-dressing route kills the same coupling to an exact rank-0 zero matrix —
"the object's kinematics allows what the dressing forbids." Whether N1 or N2 specifically carries a
nonzero coupling component, and whether either is protected from decaying into visible-sector states
by any of the object's own forced discrete structure (the c-bit ℤ/2, the r-label ℤ/2, the E₆ center
ℤ/3, or γ₅) — the minimal requirement for *any* dark-matter candidate, stability — **has never been
checked.** **First probe, unasked as of this ledger:** does any forced discrete symmetry of the
object act as a stabilizing parity on N1 or N2 that forbids their decay channels into the visible
27's charged content? Not run; this is the row's genuinely open computable question, not a wall.

**The close:** one specific naive candidate is dead by computation (B657); the roster of two
gauge-neutral fermions the E₆ content structurally supplies (N1, N2) has never had its stability
question posed, let alone answered — this is a MISSING row with a named first move, not a BLIND one.

---

## ROW 5 — DARK ENERGY / Λ

**Fully typed already by B1194/EX-2 (survives = true in that audit); reproduced here for
completeness of this ledger, not re-derived.**

**HAVE (own units, forced, exact):** the object is a genuine 3d Einstein solution with **Λ = −1
exactly** (B259: Ric = −2g, R = −6, solves 3d vacuum Einstein with Λ = −1; re-verified in-sandbox to
28 digits, Vol(m004) = 2.02988321281930725004240510855…). The action is **S = −Vol·σ with ZERO free
dimensionless constants** (B1088): CS(m004) = 0 exactly (amphichirality, a theorem not a numerical
near-zero), G_N = 1/(4σ), c = 6σ derived two independent ways (Sugawara closure and Brown–Henneaux)
and cross-checked machine-zero. The object spans **all three curvature signs** continuously via the
cone-angle family (B248/B981): hyperbolic (α=0, Λ<0, ℚ(√−3)/E₆) → Euclidean (α=2π/3, flat) →
spherical (α=π, Λ>0, ℚ(√5)/E₈) — the old "wrong sign" objection was **withdrawn as a category
error**: hyperbolic H³ is Riemannian, AdS is Lorentzian, and B716 bans identifying them (no canonical
Wick rotation; signature is the observer's).

**IMPOSSIBLE-BY-THEOREM (the measured 4d value):** the dimensionful no-go (LAW_MAP §E wall 10) plus
the scale-torsor theorem (Hom(G,ℝ⁺)=0, B666 cell S — the same theorem cited in row 3) puts Λ's
*value* permanently out of reach as a value, not as a gap in effort. THE_LAMBDA_POSITION.md states
this as the record's sober terminal position.

**The graveyard (dead, quoted so nothing resurrects):**
- **D1** (k≈137 sets Λ) — wrong Chern–Simons normalization. DEAD.
- **D2** (Λ = Λ_Planck·φ⁻²ᴺ) — circular. DEAD; S014 tombstoned.
- **The Kodama chain** (k=3 → GΛ=2π → "122 orders") — **WITHDRAWN** (B980): GΛ is dimensionless only
  in d=4, the object's level comes from a 3-manifold; and k_anyon = k_gravity was **posited**, not
  derived. "The object never made that prediction." Surviving honest number: Vol(m004) = 2.02988…,
  CS = 0 exactly.
- **S043's DESI hook** (`speculations/S043_topological_lambda_desi.md`) is a self-labeled "rhyme, not
  contact" and remains uncorrected in `speculations/CATALOG.md` despite B980's later withdrawal — a
  documentation-currency gap the EX-2 sweep flagged, not a live claim.
- **H131** (`docs/HINT_LEDGER.md`): a Planck-2018 TT-residual scan for log-periodic golden-ratio
  modulation vs proper ΛCDM — **NULL** (SNR −0.7, template fit −0.3σ; an initial 7× excess was
  a Gaussian-smoothing artifact, honestly killed).
- **B563**: ten unit-free Planck-ratio probes, preregistered — **NULL** (best object hit |ln
  ratio|=0.6825 vs 10⁴-trial null mean 0.7846, p=0.531; a plastic-number control ladder scores 0.46,
  *closer* than the real object).

**The close:** existence of Λ is forced and understood at the object's own (3d, unit-normalized)
level — this is not "missing," it is a theorem. Its numerical value and the physical sign convention
that would let it match the observed universe are permanently out of scope absent a still-missing
3d→4d lift (B277's two named blockers), exactly as row 3's scale wall says for every other rate.

---

## ROW 6 — BARYOGENESIS

**HAVE, as a structural stage — B867 THE SAKHAROV GATE.** All three preconditions structurally
present, exactly:
- **S1 (B-violation):** the coset arithmetic 24 − 12 = 12 = dim[(3,2) + (3̄,2)], exact.
- **S2 (CP violation):** the count of physical Kobayashi–Maskawa-type phases (N−1)(N−2)/2 = 0, 0, 1,
  3, 6 for N = 1..5 — first nonzero **at N = 3**, matching B866's independently-derived S3 triple
  exactly.
- **S3 (departure from equilibrium):** V(φ)/κ = −(7+5√5)/12 < 0, with an exact barrier height
  (5√5−7)/12 > 0 — Galois conjugates of the same algebraic pair (B6/B853); the mechanism
  self-consumes (B737-P2/B852: no re-equilibration channel).

**Explicit self-disclaimer, verbatim from the arc:** "NOT a computation of the asymmetry… no rate,
no temperature, no asymmetry number… whether the play runs is a dynamics question the corpus does
not yet contain."

**MISSING (the rate/dynamics side):** no computation exists — nor is one claimed to exist — of an
actual baryon-to-photon ratio, a departure-from-equilibrium *rate*, or a temperature scale. This is
the same object/closer split as every other row: the *structural stage* for baryogenesis is exhibited
and forced; whether the play actually runs, at what rate, producing what asymmetry, is entirely
closer-side dynamics that the object does not supply and that no arc has attempted.

**The close:** three independent B-violation/CP-violation/non-equilibrium preconditions are
structurally present and exactly counted — a strong, honest HAVE at the stage level. Nothing beyond
the stage exists; naming a first probe for the rate side would require importing a genuine thermal
history, which row 3's scale-torsor theorem already forbids the object from supplying on its own.

---

## ROW 7 — THE CMB'S PLACE

**MISSING as a dedicated arc (no C_ℓ power spectrum, no anisotropy prediction, no acoustic-peak
computation exists anywhere in the record) — but two computed negatives already exist and are worth
keeping on the books rather than re-discovering.**

**Negative 1 — H131** (`docs/HINT_LEDGER.md`, the July-14 data scan, promotion-gated under B811):
a log-periodic golden-ratio-modulation search against **Planck 2018 TT residuals** vs proper ΛCDM —
**NULL** (SNR −0.7; template fit −0.3σ). Flagged "the scan's valuable row" precisely because it is a
clean negative, not a near-miss.

**Negative 2 — B730 Q3, THE COSMIC-TOPOLOGY MEETING POINT** (`frontier/B730_forced_faces_and_cosmos`,
verified against live arXiv data): every cosmic-topology search in the literature (matched circles,
the COMPACT collaboration, the Poincaré dodecahedral test) returns **NULL** against the object's
own would-be topology signature; the universe is measured spatially flat (Planck+BAO Ω_k =
0.0007±0.0019). The one genuine mathematical curiosity found here — the object's own (5,1) Dehn
filling child, m004(5,1), **IS** the Thurston manifold (the #2-smallest closed orientable hyperbolic
3-manifold), independently studied in real CMB cosmology (Inoue, astro-ph/9810034, Laplacian
eigenmodes on exactly this manifold) — is explicitly base-rated as **tractability-selected, not
data-selected**: researchers study the smallest-volume manifolds because they're computable, not
because CMB data favors them. "The math connection is real and stronger than banked; the physics
claim is NIL." Also computed here: the child's invariant trace field is the *quartic* x⁴−x−1 (disc
−283, Galois S₄, no quadratic subfield) — the open object's own Eisenstein ℚ(√−3) does **not**
survive Dehn filling. Reality's arithmetic, on this reading, is the observer's, not inherited.

**First computable probe, unasked:** neither negative above tests a genuine *power-spectrum*
prediction (an amplitude, a spectral index, an acoustic-peak location) — both test topology/
periodicity signatures only. Given row 2's finding that a horizon/flatness-signature probe from the
object's own tick-entropy has never been run, and row 3's finding that no scale ever emerges from
the object, the honest expectation (not yet computed) is that any such probe would need to clear the
same scale-torsor wall before producing anything CMB-comparable. Unasked as of this ledger.

**The close:** the CMB question has been touched exactly twice, both times as a *negative control*
against a specific narrow hypothesis (log-periodicity; topology), both cleanly NULL, and the one
genuine coincidence found (the Thurston-manifold child) is honestly typed as researcher-selection,
not physics. No dedicated power-spectrum arc exists.

---

## ROW 8 — STRUCTURE FORMATION

**MISSING. Zero dedicated arcs found** (grep across `docs/`, `frontier/`, `paths/`, `speculations/`
for structure-formation, power-spectrum, and growth-of-structure language returns nothing beyond the
already-cited rows above).

**The only structurally adjacent computed fact** is row 3's accretion/generative reading (B168): a
forward-only, decelerating, dimensionless growth law (histories 96 → 2,488,080; retention rate
0.583 → 0.397, decreasing and decelerating). This is a growth process over the object's own
combinatorial ensemble, not over any spatial or gravitational configuration — it has never been read
as, or tested against, an analog of hierarchical clustering, halo growth, or a matter power spectrum.

**First computable probe, unasked:** does B168's retention-rate decay curve, treated purely as a
dimensionless growth-law shape (never as a value), have any structural analog to a growth-of-
structure exponent under a preregistered, Gate-5-compliant comparison? This has never been proposed
before this ledger and is registered here as the row's genuinely open first move — not a claim that
it would succeed; row 1 and row 2's STALLED precedents make failure the honest prior.

**The close:** this is the most purely and simply MISSING row in the whole cosmology ledger — no
attempt, no negative, no adjacent theorem. It is not walled; it has never been started.

---

## SOURCES CONSULTED (arc-level, not exhaustive citation list)

B259, B309, B518, B716, B867, B168, B167, B170/V164, B666 cell S, B1012, B1088, B248/B981, B980,
B563, H131 (`docs/HINT_LEDGER.md`), B730 (`frontier/B730_forced_faces_and_cosmos`), B657
(`frontier/B657_invariant_line`), `frontier/B660_structure_campaign/origin_docs/DARK_SECTOR_CAMPAIGN.md`,
B1135 (`frontier/B1135_gauge_closing`), B1185 (`frontier/B1185_yukawa_three_mechanisms`), memo 80
(`frontier/B1171_seam_harvest`), S008/S002/B124/V113, S035, S043, S045, `paths/E5_vilenkin_tunneling`,
`paths/E11_entropic_emergence`, `frontier/B4_bkl_gutzwiller`, `frontier/B23_bkl_gravity_controls`,
`docs/THE_LAMBDA_POSITION.md`, `docs/THE_LADDER.md` (X3–X5), `docs/THE_CAMPAIGN.md` (row 24),
`frontier/B1194_existence_audit/verification/audit_cells.json` (EX-1, EX-2, EX-6).

**Not independently recomputed in this session** (cited from banked FINDINGS.md as prior verified
work, per this cell's own instruction to assemble rather than re-derive): all the arithmetic above.
The one item flagged for future scrutiny: B1135's citation "27 = 16 ⊕ 10 ⊕ 1 under E₆ ⊃ SO(10)×U(1)"
is explicitly marked in its own source as **CITED standard (Slansky 1981), not independently
recomputed** — the N1/N2 roster in row 4 inherits that same caveat.

*Landed at B1196 from the GC-28 draft (the existence audit's blind-region close); every row sourced to banked arcs; update rule: every bank touching a row updates it, same PR.*
