# REBASE — 2026-07-16 (before any new campaign)

*The consolidated honest state after the 07-15 marathon: Branch 3 closed
and hardened, the audit seat's findings integrated (internal, read-only clone; PROVENANCE.md §0), the chord's first exact
invariants banked. Follows the 07-12 rebase's format: what stands, what
died, what is open, what gates what. Owner directive: rebase before any
campaign launch.*

## 1. What stands (proved or exactly computed, all class-level-gated)

- **The SM-values question is CLOSED at every tested level, on corrected
  inputs.** Branch 3 (271 sealed pairs) + the held-out controls (m136
  amplitudes, both objects' exterior torsion families) + the matrix
  layer (B631: the hearing matrix vs PMNS, p = 0.700,
  power-demonstrated instrument) + B615-R (seat 4: scheme/scale/
  uncertainty-corrected — the last p = 0.078 residual DISSOLVES,
  0.145–0.62 in every variant). One zero-cost forward row: JUNO will
  separate 1/(2φ) from 4/13 at sin²θ₁₂ within years; no seat acts.
- **The solo/chord dichotomy (the coupling thesis, cohomological
  form):** the solo complement carries NO cubic scalar (B632's
  obstruction: cd = 2 + Koszul); the solo's only cubic-family object is
  the alternating Ω: Λ²H¹(M;27) → H²(M;27*), surjective, rank 2,
  kernel 1 (audit-exhaustively verified, 162/162). **The mirror-coupled
  double carries a NONZERO alternating cubic 3-form** Y: Λ³H¹(D;27) → ℂ
  (B637, all class-level gates green) — exact in ℚ(√−3).
- **The chord's invariant anatomy (the weld family):** h¹(D;27) = 5
  generically, collapsing to 2 exactly at the full-E₆ chiral bends
  m = 4, 8 (⇒ no 3-form there — chirality and the cubic exclude each
  other); Y has a bend-independent core with the exact internal
  identity **Y[023] = 24·ζ₆·Y[123]** (the relator's Eisenstein unit),
  the equal pair Y[034] = Y[124] off m = 1, the zero law Y[01k] ≡ 0
  off m = 1, and exactly ONE dial-sensitive slot (Y[234]).
- **Three independent pipelines agree** on the dimension tables (this
  seat's amalgam-Fox; the audit's twisted M–V; cc2's route), and cc2's
  complementary invariant (the invariant-section-paired 5×5 form,
  antisymmetric, rank 4) is banked on the same manifold.
- **The five mirror manifolds are permanently named** (TERMINOLOGY.md;
  no invariant transfers without an explicit map): M_Sol(−A₁) (ℤ⊕ℤ/5,
  Sol — the conductor carrier), L(5,2), D_g(M) (±A₁ → homology sphere;
  the four B605 involution gluings all COMPATIBLE, all h¹ = 5),
  D_bent(M; m), and cc2's D_conjθ(M) (h¹ = 3, parity-clean; universal
  slope = the cusp modulus −2√−3).
- **The conductor chain, correctly stated:** Δ(−1) = det(A₁+I) = 5 =
  tr²−4 (exact identities); the reflection coset gates 5 | κ
  (B620/B621); **κ = 5 is the minimal bearing CHOICE** — the four
  upgrade obligations are L91.
- **L90 CLOSED** (in-repo): B575's Q ≡ 0 is untouched by the corrected
  cup chain (the per-term identity verified exactly, all six blocks).

## 2. What died or was corrected (and must stay dead)

- "Symmetric Yukawa on the double" — false as typed (alternating by
  Koszul). Sharper typing lesson: an ALTERNATING family tensor
  contracted with the SYMMETRIC E₆ cubic gives ZERO coupling for
  identical families — Y is not a Yukawa-type family tensor for
  identical generations even in principle; it is a flux/volume-type
  invariant across INEQUIVALENT modes.
- "The mirror-double has H₁ = ℤ⊕ℤ/5" — that is the Sol bundle, not any
  complement double (B591's own table).
- "The conductor selects κ = 5" — divisibility, not equality.
- "Three generation slots" → three inequivalent local-system modes;
  "forced vev" → the invariant section (h⁰ = 1 on the 3-form-carrying
  doubles; 0 at the chiral bends).
- The naive triality gate is DEAD by standard facts: the E₆ center acts
  on the 27 as the scalar ω (splits nothing); non-central order-3
  elements do not commute with the holonomy (Coxeter scaling) — no
  induced H¹ action. Chat-1's 3+2 proposal as posed does not run.
- The B631 hearing-matrix numbers stay structured-null; the eigenframe
  comparison stays tainted (contamination on record); Gate 5 stands:
  no recycling of any of these under new labels.
- Stage-1's 3-form table (pre-repair) stays quarantined-as-artifact;
  the sealed gates that caught it (and the cell-2 coboundary gate
  before it) are the day's methodological heroes: controls before
  values, always.

## 3. The open campaign menu (each behind its gate; NOTHING launched yet)

- **C1 — the swap decomposition (recommended first):** the deck
  involution of the double is the symmetry the chord actually OWNS.
  Gates: its H¹-action exists (coefficient intertwiner = the mirror
  conjugation) → the ±-eigenspace split of the five modes → the parity
  theorem for the observed reality/zero structure (the swap is
  orientation-reversing ⇒ Y(σ·,σ·,σ·) = −Y — should PROVE the core
  identities and re-derive the zero law as a selection rule; the m = 1
  exception = swap-breaking by the identity-exponent dial, checkable).
- **C2 — the cubic on D_conjθ(M) (recommended with C1):** h¹ = 3 there
  with no selection principle needed; Λ³ is 1-dim — ONE number in
  ℚ(√−3), VEV-independent, the cheapest well-posed new value in the
  program. Joint cell with cc2 (their manifold, this seat's cubic
  machinery; method-disjoint verification built in).
- **C3 — the dial law:** regress Y[234] (exact) against banked exact
  bend quantities (torsions, block data) — internal only.
- **C4 — the silver chord:** the universality control (does the silver
  object's double carry the same anatomy in its own field?). Heavier.
- **C5 — the comparison-map normalization** (torsion + Goldman;
  chat-1's proposal): after C1, since it needs canonical classes.
- **Standing gates:** L91's four obligations before any physics
  reading; Gate 5 + the full sealing checklist (MB12 + MB13-in-doc +
  pipeline controls + INPUT_COMPLETENESS_LEDGER) before any SM-facing
  comparison; PC26 v2-final absorbs this record (queued).

## 4. The discipline ledger (what this rebase certifies)

Hash-first held-out protocol: honored end-to-end all day. Sealed
controls: caught two wrong-number events pre-banking. Repair
adjudication: failed transcripts restored, post-hoc hashes labeled,
locks assert mathematics. Cross-seat verification: everything verified
on receipt, both directions. The one process debt paid publicly: the
B630 MB13 sweep ran late (recorded); every sealed design now embeds its
sweep results.
