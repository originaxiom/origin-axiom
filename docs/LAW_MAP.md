# THE LAW MAP — every law the object has produced, with its exact status

*Created 2026-07-16 at the owner's direction. One row per law: what it
says, its strength class, its witnesses, and what would upgrade it.
Strength classes: **THEOREM** (proved, machine-verified); **LAW**
(exact on every computed instance, mechanism open or partial);
**MECHANISM** (a law's explanation, possibly partial); **PLACEMENT**
(a structural identification awaiting derivation); **WALL** (a proved
impossibility). Companion documents: THEOREM_REGISTRY.md (formal
statements), REBASE_2026-07-16.md (the state), OPEN_LEADS.md (the
upgrades' lead rows).*

---

## A. The object's arithmetic (the solo laws)

| law | statement | status | witnesses | upgrade path |
|---|---|---|---|---|
| **The sign-law family theorem** | sign det′(I−Sym^{2m}A) = (−1)^m for EVERY hyperbolic A; closed form τ_m = (−1)^m(tr²−4)^m∏U_{j−1}(tr/2)² | **THEOREM** | B617; fig-8 Fibonacci face; m136 base 12 | — (done) |
| **The exterior sign law** | sign(Re τ_m^ext) = (−1)^m on both computed objects, all six exponents | **LAW** | B581, B627 (silver 6/6) | a proof in the B617 style; third object |
| **The conductor identities** | Δ(−1) = det(A+I) = tr²−4; det(A−I) = the unit; the two Gauss conductors = the two lifts' torsion homologies | **THEOREM** (identities) | B591, B588, B634-G1 | — |
| **The one-door theorem** | of the twelve Weyl-Weil conductors, only the reflections carry the non-square 4−t²; all others are field-blind squares | **THEOREM** | B620 | — |
| **THE HEARING LANDSCAPE THEOREM (SU(3)₂)** | for every metallic word R^{n−2}L: tr_odd = t₁^{n−2}X₁₁ + t₂^{n−2}X₂₂ on the 2-dim θ-odd block, T-phases EXACTLY e^{2πi·2/15}, e^{2πi·8/15} (h-arithmetic) ⇒ **\|tr_odd(n)\| = (2√3/D)·\|cos(π(4n−5)/10)\| ∈ {0, 1/φ, 1}** by the exact identities sin²72° = D²/12, sin²36° = D²/(12φ²); modulus period 5 (t₂/t₁ = primitive 5th root), palindromic; reality period 15 (real ⟺ n ≡ {0,3,5,6,9,10,12} mod 15). The golden word: minimum nonzero modulus, the unique UNIT-DETERMINANT word (= the unique prime conductor — the same criterion), the period's origin. NOT golden-unique: quiet+real recurs at n ≡ 3, 12 mod 15 (chat1's uniqueness claim refuted) | **THEOREM** (exact phases + exact trig identities + 38/38; **GENERALIZED (B665, cc2 + verified witnesses): THE SHADOW-CLASS LAW — \|tr_odd(W)\| = 2·\|tone(class of W mod 5)\| for ANY word; trace does NOT determine hearing (trace 7 splits φ vs 1/φ by class); all five doubled tones {0, 1/φ, 1, φ, 2} occur; the single-L closed form is the law's family section**; **STAGE-UNIVERSAL FORM (B666 cell 3): \|tr_odd(W)\| = \|χ_D(shadow(W))\| with D = dim θ-odd = the shadow-irrep dim — golden: \|χ₂\| of 2I mod 5, period 5; E₆ level 2: \|χ₃\| of PSL(2,7) mod 7, period 7, values {0,1,√2} (547-word corpus 6/6 classes); each stage hears only its own shadow (RRLL: loud at golden, DEAF at E₆); + THE GENERATING-FUNCTION THEOREM: a_n = (φχ₀+φ⁻²χ₂)/2 exact, additive Dirichlet form exact, the Euler-PRODUCT form REFUTED**) | B664 (chat1's computation, proved/corrected); B665 (cc2's general form + reciprocal corrections) | L107 (the cross-landscape words × stages; the E₆-level version; the silver at its own stage; the Dirichlet-character form); feeds L91 as supporting structure |
| **The κ-gating law** | base = 3^v·m gates the hearing-field window at m \| κ (5\|κ golden; the 3-part absorbed by disc(A₂)) | **LAW** (3 objects) | B618, B621, B624 | the general proof; more objects |
| **The bifocal-entanglement statement (the two ends' fields)** | golden: trace field = the BEING field alone (ℚ(√−3); the hearing ℚ(√5) disjoint — the unique banked metallic with arithmetically disjoint ends); silver: trace field ℚ(ζ₈) = ℚ(i)·ℚ(√2) = exactly the COMPOSITUM of its being and hearing quadratics. Sharpened by B662/D: the CHORD DATA descends to the being field k(Γ) on BOTH objects — the data separation is always clean; golden-special is only that the trace field adds nothing | **THEOREM-backed statement** (fields exact; the S-entry correction verified: 1/D ∉ ℚ(√5)) | B663 (cc2's clarification + this seat's checks); B247–B261/B210/B644 (the two-ended theorem); B662/D | L105 (2O/E7: does the silver hear the binary octahedral at its mod-8 shadow?); feeds L91 stage-selection (κ = 5 minimality) |
| **The Jacobian reality law** | J = μ+1/μ real ⟺ amphichiral word (the classical face of the pairing law) | **LAW** (8 words) | B626 | proof; discrete-branch IDs |

## B. The stage and the ear (the hearing laws)

| law | statement | status | witnesses | upgrade path |
|---|---|---|---|---|
| **The closure theorem** | amphichiral weld ⟹ conjugation-closed odd hearing spectrum at EVERY level (modular-data axioms + GHH anti-palindromicity) | **THEOREM** (one-way) | B613; 16/16 + 12/12 controls | the converse (empirical 7/7) |
| **The trace law / LAW-O** | trace(B_odd) = [5\|κ]/φ − [4\|κ]; stage-universal (Weil AND SU(3)ₖ) | **LAW** + **MECHANISM** | B601, B585, B587 (the Weyl-twisted Weil factorization) | the per-term Landsberg–Schaar proof (L82 W2) |
| **The three-layer separation** | spectrum = chirality; trace = field; matrix elements = listener coupling | **THEOREM-backed frame** | B611, B613, B620 | — (the program's spine) |
| **The hearing-group theorem** | the hearing factors through **2I × ℤ/3** at level 15 (Ng–Schauenburg), golden 2-dim character; tr ρ(RL) = −1/φ; the Anosov monodromy heard as the pentagon | **THEOREM** (class-table verified) | B640; a parallel web seat | — (verified) |
| **The twist-frame tone law** | Re(ζ⁻¹ūM_odd u), ζ² = det, is ear-independent on all 360; **five absolute tones {0, 1/(2φ), 1/2, φ/2, 1}**; 90/360 silent | **THEOREM** (proof sketch + 2e-60 on all elements) | B641; the web seat's Cayley–Klein mechanism | **MECHANIZED (B654): the tones ARE \|χ_golden\|/2 with exact multiplicities (census = class sizes ×3) — the pentagon voice is the character table; Plancherel = Schur ⟨χ,χ⟩/4** |
| **The Plancherel split** | mean-square universal tone = mean-square overtone = **1/4 each**, exactly, from the class equation | **THEOREM** (character arithmetic) | B641-G5 | — |
| **K020-in-the-ear (the Galois-ear law)** | the hearing group and tone set are stage-independent (form forced); WHICH golden character (−1/φ vs +φ) is Galois-chosen by the conjugate stage | **THEOREM** (sealed predictions 3/3) | B642; K020 | — |
| **The silent-weld prediction class** | tone-0 welds are decidable by table lookup; their amplitudes purely imaginary for every real ear | **SEALED PREDICTION CLASS** | B640/B641 witnesses 3/3 | standing falsifier |
| **The generic-silence law (the ladder's baseline)** | for gcd(κ, 2·3·5·7·11·19) = 1: Z(κ) = (1 − (κ\|5))/2 — the object's generic silence is the quadratic character of its own monodromy discriminant (5 = disc A₁); resonances (7²-class at 14, dyadic zero at 16, Φ₉ surplus at 19) ride ON the baseline. **THE COMPLETE VOICE (B663/A2, the resonant-phase law): Z(κ) = Σ_classes [certified jump] × [±1 per (class, profile)] × (κ|5)^[χ₅] — one character governs every phase at every rung; m_w(p) = 0 on all 24 class-prime pairs (101/101 cells; magnitude gates 101/101 vs the F1 certificates); dyadic/deep-3 remainder priced** | **CERTIFIED** (exact cyclotomic certificates, zero floats, both Legendre signs — Milgram shape, all γ_w ∈ {±1}; B651 P1/P2) | B646; B651 | — (certificate-grade; B659 sweep: the formula is a Dedekind–Kummer/reciprocity exercise once reduced to local monodromy behavior — the reduction itself is the banked content) |
| **The uniform jump law** | the per-class ladder jump = Π p^{min(v_p(κ),a_p)/2} over the fixed-point group's elementary divisors | **THEOREM** (325/325 exact integer certificates + the local proof, dyadic included — B651 F1) | B646; B651 | — |
| **The melody theorem (the ladder is periodic)** | Z(κ) = Z(κ + N₀), N₀ = 2⁶·3³·5⁴·7²·11²·19² — 6/6 sealed pairs at deviation exactly 0 across all behavior classes; the ladder's complete information content is ONE period of finite data (no accumulated-limit channel; Loop A closed) | **THEOREM-grade LAW** (sealed prediction-first; their prereg's decimal slip logged, formula sealed) | B651 (cc2 D2) | a proof from the certificate structure; **B654 Q-PERIOD: N₀ = 75·lcm{d²} EXACTLY, 75 = 3·(disc A₁)² — the twist and the discriminant carry the residue (HINT-grade read, exact identity banked)**; **B656/G2: the GENERIC subsequence has minimal period exactly 5 = disc(A₁) — the same period B204 PROVED at SU(2) (L24(c)'s first cross-stage point); **L100 CLOSED (B662/G, CERTIFIED 12/12 incl. minimality witnesses): the EXACT minimal period is P = 175560 = 2³·3·5·7·11·19 = the exponent of the total discriminant group (lcm of all Smith divisors of the 25 Weyl pencils); N₀/P = 13,167,000; zero aggregate cancellations — the independent confirmation of the resonant-phase law (B663/A2)** |
| **The PSL-factoring theorem (the kill rule's mechanism)** | the θ-odd block kills a realized central −I exactly at split primes and deep 3-powers (central-element form; 19 factor-rows exact, k = 2..8) | **THEOREM** | B651 (cc2 F2); B646 Q2 | — |
| **The stage-split clock law** | the clock = ord of A₁'s image through the block's NON-ABELIAN core (abelian character factors are provably A₁-blind); k ≥ 3: ord(A₁ mod ord(T_k)) with stage-typed central kills; the k ≤ 2 anomaly zone DISSOLVED (SU(3)₂: Fibonacci core, clock 10; E₆ k=2: PSL(2,7) core, clock 4, derived). **COMPLETION (B656/G4): B596's SU(3) table retro-derived — clock(κ) = ord(A₁ mod 3κ) EXACTLY, κ = 6..15 (10/10, independently verified); the "non-uniform multipliers" were conductor-vs-κ arithmetic; κ=4/5 are the law's own anomaly clauses (order destroyed / the half-period). B596 DATA → DERIVED; L84's multiplier clause discharged** | **LAW** + **MECHANISM** (prediction-first: 36 at level 5, 12 at SU(3)₁₃, 60 at E₆ k=8; **B663/A3: the deep-3 kill is a BLOCK CENTRAL CHARACTER — one sign per stage (E-type +1 / A-type −1), residue hypothesis falsified 73/73; first representation-level validation (SU(3)₆: B¹⁸ = −I exact, clock 36 reproduced; corroborated on this seat)**) | B646 (cc2 R2/N2/Q1/Q2); **B656 (cc2 G4 + independent verify)** | the k ≥ 3 split-kill = PSL-factoring (candidate); a uniform proof |
| **THE SIGN-HEARS-THE-DISCRIMINANT THEOREM** | for B_w = tI − w − w⁻¹ over a Weyl group on an even-rank lattice: det(w) = (−1)^{v_p(det B_w)} for EVERY w iff v_p(t²−4) is ODD; even v_p ⇒ exactly the sign-balanced half. The identity card's det(A±I) = f(∓1) is the exact quantity the Weyl-sign structure hears, at every prime | **THEOREM — UNCONDITIONAL (B666 cell 4: the pair products are literal squares (t−ζ−ζ̄)²; det B_w = (t−2)^{a₁}(t+2)^{a₂}Λ²; no ramified split exists; both directions + the exactly-half law abstract; hypothesis = any finite-order element of GL_n(ℤ), n even)** (battery: 207,384 + W(D4) fresh words + m = 3..50 symbolic) | B656 (cc2 G1 + independent verify); B654 (the identity card) | unifies the P1 jewel + the sector halves + the mirror B_id; odd-rank form |
| **The mirror generic law + the sector-carry law** | mirror channel (P = −3): Z₋₃(r) = ((r\|5) − 1)/2 = −Z₊₃(r) for r coprime to {2,3,5,7,17} (the mirror's generic voice is NEGATIVE — no det-1 class; B_id = −5I, det = det(A₁+I)⁶); ⇒ tr_even vanishes on doubly generic rungs: **the even sector carries a unit iff the mirror resonates** — retro-explains P4's killed inert⟺even correlate (its 3/3 hits mirror-resonant; its killer 23 mirror-generic) | **LAW** (certified r = 13, 29, 31, 37; 7/7 generic points; gate 13/13 vs the banked P4 column; the retro-explanation arithmetic re-checked on receipt) | B656 (cc2 G3) | more resonant rungs; the mirror's own jump law |
| **The splitting law (field import)** | the level's field import is typed by the prime's splitting: inert → the √-import; split → the cyclotomic subfield (no √); 3/3 inert + the κ=19 split rung | **LAW** (prediction-first at 19 and 20) | B646 (cc2 R1/Q1) | more rungs; the inert⟺even-sector correlate (3/3, NOTICED) |
| **The universal boundary slope** | every H¹ direction of the twisted double bends the cusp by the modulus −2√−3 itself | **LAW** | cc2 cell-3a (verified on receipt, B637 adjudication) | mechanism |

| **THE γ₅′ CORRESPONDENCE (the ear in the flavor-model catalog)** | the modular-flavor Γ₅′ ≅ SL(2,5) paradigm CHOOSES its group (10–13 catalog; geometric origin = their acknowledged gap), postulates level-5 structure, fits 8–10 parameters; the framework DERIVES the group from the monodromy (B640/B644), supplies the χ/χ̄ Galois pair as its one physical bit (GATE B), carries φ at the same structural locus, zero parameters after the bit. Verdict: CONSISTENT-NOT-SELECTIVE + the upstream claim (the framework supplies what the paradigm postulates) | **PLACEMENT → CANDIDATE FUNCTOR (B662/I, exact): the ear's hearing representation IS the Γ₅′-doublet 2̂′ — character equality on all 9 classes in ℚ(ζ₂₀) under the canonical SL(2,𝔽₅) identification; the Galois pair = their (2̂′, 2̂); the stage-twist value +φ = χ_2̂ (the twist selects their other doublet); H129's weight-5 FORCED (M_k(Γ(5)) ≅ Sym^{5k}(2̂); the doublet series' numerator exponents partition the E₈ exponents, denominators = 2I's degrees 12, 20; first weight-5 at n = 25 both — independently re-derived on the main seat)** (both 1/(2φ) lit-gates open, independent) | B660/S1 (cc2; this seat's B659/C12 sweep corroborates) | the specialist bar (R21-7); the upstream paper-shaped form if ever taken is mathematics, Gate 5 untouched |

## C. The chord (the coupled-object laws)

| law | statement | status | witnesses | upgrade path |
|---|---|---|---|---|
| **The subfield law (the chord's field of definition)** | the chord data — the σ*-matrix AND the full Y-tensor — is defined over the object's INVARIANT TRACE FIELD k(Γ) (fig-8: ℚ(√−3); silver: ℚ(i)), inside trace fields of degree 2 and 8 respectively. **RESTATED via the literature (B659 sweep): the "shape field" coincidence is NEUMANN–REID Thm 2.4 + 2.2 (shape field = k(Γ) for cusped manifolds; m136's k(Γ) = ℚ(i) verified) — the LAW's own content is that the CHORD DATA lands in k(Γ) rather than the full trace field** | **THEOREM (two banked objects; general mechanism identified)** — B662/D: Gal(L/ℚ(i)) = Klein four acts by sign-twisted conjugation (exact 1-dim intertwiners); the weld is a descent datum; the banked H¹ basis is itself a ℚ(i)-form (15/15 cocycle-level fixings) ⇒ Y, C Galois-fixed ⇒ ∈ k(Γ), FORCED; fig-8 degenerate (trace field = k(Γ)) | B638/B637; B649 3b-i/3b-ii; B654 Q-FIELD; B659 (the sweep resolution) | prove "chord data ∈ k(Γ)" from σ*-equivariance (one cell — the swap is Galois-compatible); third object = an NR-consequence check now |
| **The dimension grammar (3/5/1)** | h¹(M; 27) = 3, h¹(D_weld; 27) = 5, h⁰ = 1 on BOTH computed objects — the fig-8 (disc 5, ℚ(√−3)) and the silver m136 (disc 32, L = ℚ(s,i) deg 8, 3-generator presentation); no prediction was sealed (the silver's values were the discovery). **REDUCTION THEOREM (B656/G5): for ANY once-punctured-torus bundle + its double (χ = 0, PD via J, Mayer–Vietoris + half-lives-half-dies), the whole grammar is determined by TWO local inputs (i₁, i₂) = (dim V^holonomy, dim V^{peripheral ℤ²}); (1,3) ⇒ 3/5/1 — monodromy-independence localized to (i₁, i₂)**; **ONE-PER-BLOCK refinement (B657/W0b, exact, both objects): 27 = V17⊕V9⊕V1 holonomy-block-diagonal with per-block (h⁰,h¹) = (0,1)/(0,1)/(1,1) — each principal block carries exactly one H¹ class and the invariant-line class IS the Betti class** | **THEOREM (the metallic family)** — B662/A proved (i₁,i₂) = (1,3) metallic-uniform (centralizer-of-parabolic + single-Jordan-block lemmas; Zariski-density route rigorous; 30/30 exact on both objects), so the reduction forces 3/5/1 for EVERY member | B632/B637; **B649 stage 3a**; **B656 (cc2 G5)**; B657/W0b | L101 PROVEN (B662/A); **Q-BLOCK ANSWERED (B662/B): the adjoint 78 gives (i₁,i₂,h⁰,h¹) = (0,6,0,6) exact with one-per-block on all six adjoint blocks — the reduction confirmed on a THIRD module**; B659 sweep: the method = Menal-Ferrer–Porti + MV (the cross-object data is the law's content) |
| **THE PORTAL LAW (the invariant line couples to everything)** | P(u) = [v₀ × u] (the banked cubic's polarization; v₀ = the h⁰ invariant line, a 3-weight h_pr-NULL combination with coefficients (1,−1,1) — NOT the Spin(10) singlet, whose weight sits at h_pr = ±16 with v₀-coefficient exactly 0: the "gauge singlet" conflation REFUTED at W0a) is a **rank-5 isomorphism H¹(D;27) → H¹(D;27̄), kernel 0**, on BOTH objects (golden: exactly block-diagonal on the boundary-born/solo-inherited split, solo block corners (1,−2), middle −15/11; silver: rank-5 isomorphism; **L102 RESOLVED (B662/E): in the CANONICAL boundary-restriction decomposition the silver portal IS block-diagonal — P(B) = B* on both objects, golden control reproduces — the mismatch was basis choice**). The invariant line does not decouple; ZERO-PORTAL refuted | **LAW + MECHANISM (B663/A1: v₀ is an INVERTIBLE Jordan element — N(v₀,v₀,·) = −2·v₀ on its own support, N(v₀) = −6 ≠ 0, the Jordan unit direction; cross-multiplication by an invertible element is nondegenerate ⇒ the portal is an isomorphism BECAUSE the heart is invertible; independently recomputed from the banked cubic)** (two objects; sector-respecting FORCED-candidate — B662/E) | B657 (cc2 W0a/W1/W2a; W0a/W0b reproduced bit-identically on the main seat, W2a re-run end-to-end, W1's sealed matrix re-derived independently) | third object; L102 (canonical silver decomposition); mechanism for −15/11 |
| **The cubic dichotomy** | the solo carries NO cubic scalar (cd 2 + Koszul); the solo's only cubic-family invariant is the alternating Ω (rank 2, kernel 1); the CLOSED chord carries the nonzero alternating 3-form. **EXTENSION (B660/S4, restated BASIS-FREE by B662/H): the v₀-mediated trilinear is Koszul-ANTISYMMETRIC with rank 4 and a 1-dim kernel (the "solo block exactly zero" was basis-local — corrected); with D1's (m,m,0) and THE MASSEY WALL (B662/H: all off-diagonal solo triple products UNDEFINED — the cups are nonzero one level below — and the defined cubes zero with FULL indeterminacy), FOUR walls now say the hierarchy question is beyond the classical cochain triple-product level; the cup-class VALUES in H²(D;27̄) are the new live object (L106)** | **THEOREM** | B632 (+ audit 162/162), B637; **B660/S4 (verified at artifact level)** | — |
| **The chirality-exclusion law** | at the full-E₆ chiral bends the double's h¹ collapses 5→2: chirality and the cubic exclude each other on the same coupling | **LAW** (exact, 2 bends × 3 pipelines) | B637 parts 1–3 | mechanism |
| **THE LAW OF THE CHORD'S CORE** | Y[023] = **24ζ₆**·Y[123] on all NINE computed doubles, every gluing; the zero law Y[01k] ≡ 0 off the m=1 dial | **LAW (pipeline-gauge; the invariant carrier is the cross-ratio law)** + **MECHANISM (σ*-law mechanized via the anomaly characterization; ratio adjudicated as gauge — B647 cell 3)** | B637 stage 3; B638; B645 (the spectator extension); **B647 cell 2: THE ANOMALY CHARACTERIZATION — Y = ½·conj(the swap's chain-level anomaly), localized in the single side-2 (λ,μ) certificate (7/8 slots chain-exact; the dial slot's defects satisfy the forced combination)**; **B654 Q-AREA: the factor 2 is UNIVERSAL — silver defect = 2·conj(Y) exactly on both lawful triples (4·conj(Y) FALSE; the van Kampen area law refuted — fig-8 area 2, silver area 4, factor unchanged); the silver defect is s-free (ℚ(i) — the subfield law at the defect level)** | cell 3: the certificate's H₂ content → the π/6 phase (cell 1's reduction); L93 stays closed (B643) |
| **The unit cross-ratio law + the 13-dial** | the unique normalization-free invariant of the Y-tensor (Y[023]·Y[134])/(Y[034]·Y[123]) = **1 exactly** on every 024-silent double (6/6) — the 24ζ₆ ratio extends to the (3,4) spectator pair; on the three 024-lit doubles (m=1, φ(a)=a, φ(a)=A) every invariant deviates from 1 by an exact 13-divisible quantity (e.g. (3⁸+13√−3)/(3⁸+13)); v₁₃(cross-ratio − 1) ≥ 1 always | **LAW** (exact, 9/9; mechanism open) | B645; B637's tables re-parsed independently | mechanism; the meaning of 13 (split prime, N(1+2√−3)); more doubles |
| **The swap real structure** | the deck involution acts antilinearly (J² = +1); Y∘σ* = conj(Y); the phase geometry follows. PRECISION (2026-07-16, B649 3b-i): diagonal units are Hilbert-90 gauge-variant — the INVARIANT is the real structure + the matrix's FIELD OF DEFINITION = the object's imaginary quadratic field (fig-8: ℚ(√−3)/ζ₆-side; silver: ℚ(i)/ζ₄-side). REPRODUCED on the silver: J² = +1, σ*² = id, C·conj(C) = I exact | **THEOREM** (two objects) | B638; **B649 3b-i** | third object; why the imaginary quadratic subfield |
| **The typing wall (1′)** | an alternating family tensor ⊗ the symmetric E₆ cubic = zero coupling for identical families: no Yukawa-type family tensor at this level, comparison-independently | **WALL** | B632/B637 + the audit's Koszul point; PC26 wall 1′ | — |
| **The θ-fiber-pairing law** | θ fixes the principal holonomy pointwise ⇒ the conjθ-twist lives in the fiber identification (θ₂₇∘conj into the dual), not the representation | **THEOREM** (3 exact probes) | B639 | L92 realizes the twisted cubic |
| **The λ-rigidity** | the conjugate gluing admits ONLY the λ-inverting identification (the +λ intertwiner space has no invertible element) | **THEOREM** (exact solve) | B639 | — |

## D. The meta-laws (the program's spine)

| law | statement | status | witnesses | upgrade path |
|---|---|---|---|---|
| **The coupling thesis** | the solo object carries grammar, not values; content lives in the coupling — now in FIVE independent exact instances: the antiphase listener (B584), the displaced ear (B593), the three layers (B611/B613), the reality law (B626), **the cubic dichotomy (B632/B637)** | **THEOREM-backed frame** | the five instances | each new coupling invariant tests it |
| **The N = 1 mechanism candidate (geometric vs arithmetic automorphism)** | the trace field's conjugation is realized GEOMETRICALLY (the swap σ*, B638 — a constraint); the hearing field's Galois action has no geometric realization and survives as the one bit (B642/B652) — one imaginary field with a geometric conjugation + one real field with a free choice ⇒ N = 1 | **MECHANISM-CANDIDATE** (consistent with every banked fact; chat1's synthesis, adjudicated B654) | B638; B642; B652; B654 | a proof; the silver's version (ℚ(i) geometric conj ✓ — does its hearing field behave the same?) |
| **Form forced, value Galois-chosen (K020)** | the structure is object-forced; the VALUES are Galois orbits of the stage — now witnessed inside the ear (B642) and in the level ladder's field imports | **THEOREM** (K020) + growing witnesses | K020; B642; L74/L67 | — |
| **The two-ended theorem + dual McKay** | the object has an E₆/ℚ(√−3) end (hyperbolic/2T) and an E₈/ℚ(√5) end (monodromy/2I); the EAR realizes the 2I side | **THEOREM** (the ends) + **DERIVED** (the ear, 2026-07-16) | B247–B261; B210/L37; B640/B642; **B644** | — (L94 closed) |
| **The congruence-shadow theorem (the conductor is the ear's modulus)** | on ker(det), ρ_hear = χ_golden ∘ (reduction mod 5): the ear at the minimal bearing stage hears the Anosov monodromy through its congruence shadow AT THE CONDUCTOR; the pentagon = the cat map's order-10 class mod 5; the mirror twist = −I mod 5; the Galois ear = the other irrep of the same shadow | **THEOREM** (verification strength, elementwise 120 + kernel 0/560) | B644; B640/B642; B206/B210; **B646 (the disc(A₁)=5 triple shadow: the ear mod 5 + the Fibonacci core at κ=5 + the (κ\|5) generic silence — one discriminant, three organs; and κ=14 hears PSL(2,7))** | a conceptual proof (why the θ-odd plane carries the shadow); novelty NEEDS-SPECIALIST |
| **Measurement is passage through the finite** | the infinite Anosov dynamics is heard mod the conductor: what any ear couples to factors through a finite group | **PRINCIPLE** (two exact instances) | B640 (2I×ℤ/3 at level 15); B644 (the finite group IS the mod-κ shadow) | second stage family; general statement |
| **The stopping-rule record** | every SM-value comparison at every level: null or dissolved, on corrected inputs; one experiment-side forward row (JUNO) | **CLOSED NEGATIVE RECORD** | B615/B615-R, B616, B627/B628, B631 | reopens only via owner + L91's functor + the full checklist |
| **The stage-selection question** | κ = 5 is the MINIMAL BEARING CHOICE (5\|κ proved; equality is not) | **HYPOTHESIS** with 4 named obligations | B620/B621; B634 erratum; L91 | L91's four proofs |

## E. The walls (proved impossibilities — kept dead)

1. No SM value from the solo object (the hardened record; wall 1).
2. No Yukawa-type family tensor at the classical-cohomology level (the
   typing wall 1′).
3. No symmetric mass-shaped object on the solo complement (B632).
4. No G_SM sector alignment of the principal blocks (B604/B607/B608).
5. Chiral index 0 for every single-object construction; free
   involutions only (B565-T3, B605).
6. No C₃ trace field for any hyperbolic knot — three symmetric
   generations impossible in one object (B307).
7. The naive ℤ/3 triality does not act on H¹(D;27) (center = scalar;
   Coxeter scales the holonomy) — the 3+2 split as proposed is dead
   (the rebase).
8. THE TOTAL MIRROR WALL: the chord breaks EVERY orientation-reversing
   family of Isom(4₁) = D₄ — the two order-2 flips (B643) AND the two
   order-4 elements squaring to the half-longitude (B658; peripheral
   signatures ('', −1, +1) and ('aBAb', −1, +1)) — each family's only
   partial intertwiner on the double's 27 local system is supported on
   Sym⁰ (the invariant line) alone (d = (0,0,1), all four families,
   inner-freedom immune). The double's 27-cohomology symmetry is the
   deck swap σ* exactly. (The common retreat to v₀ = the portal's
   source is a registered HINT, not a law.)
9. THE EQUIVARIANCE WALL: no nonzero linear monodromy-equivariant map
   classical → stage exists (hyperbolic φ^{±2} vs the order-10
   congruence shadow; Sylvester T = 0 exact) — the correspondence is
   GROUP-functorial (mod-conductor ∘ golden character = B644) + typed
   (B650), never module-linear (B650 wave 2).
10. THE DIMENSIONFUL NO-GO (theorem-by-assembly, B660/S3): no
   observable carrying nontrivial mass dimension is typeable (R5's
   dimensionless requirement + GATE B's N = 1 discrete + B652's
   no-scale wall + the equivariance wall's finite-shadow mechanism) —
   masses, VEVs, Λ, cross-sections, running couplings are outside the
   framework's reach AS VALUES, permanently; the B615-era nulls are
   retro-explained as typing violations. The licensed observable class
   = exact dimensionless algebraic data. **UPGRADED TO A STANDALONE
   THEOREM (B666 cell S): the scale-torsor no-go — every output is
   stabilized by a finite/profinite structure; Hom(G, ℝ₊) = 0 for all
   such G (verified exactly on Gal(L/ℚ(i)), 2I, PSL(2,7), 2I×ℤ/3,
   SL(2,ℤ/15), W(E6)); the only equivariant map into any nontrivial
   scale representation is T = 0 — the B650-wall proof shape. Companion:
   THE SILENCE NOTE (cellS/SILENCE_NOTE.md) — for scale-rigid
   frameworks Λ enters gravity only as the Henneaux–Teitelboim /
   unimodular integration constant (citations live-verified); the
   physical hypothesis labeled and firewalled.**

---

*Maintenance rule: every new banked law adds a row in the same PR (the
same/next-PR ledger discipline); status upgrades edit in place with the
date; nothing is deleted — dead laws move to §E with their epitaph.*
