# The cc3-corpus audit (2026-07-23) — verdict: mathematics exemplary; lock architecture the program's best

Scope: cc3's three banked bodies (B749 genesis forks; B754 P2 stratum, 19 cells; B759–762
QP sequence) under the full instrument set the four-pass campaign built.

1. **Seal verification, 9/9.** Every sealed-class doc across the three arcs hash-verified
   and recorded elsewhere in the repo (the two FORENSIC_SEAL_RECORDs are cc gate docs, not
   seals). The two known seal-convention traps (algorithm-unnamed; self-referential) were
   already forensically resolved at banking.
2. **Mutation testing INVERTED the expectation.** Four verdict-bearing mutations to cc3's
   output artifacts fired ZERO locks — because cc3's locks contain zero artifact reads:
   they RECOMPUTE the mathematics from scratch (Riley polynomial, off-blocks, fiber
   dimensions, census facts re-derived in-test). This is compute-grade locking per the
   anti-transcript-grep rule (E6) — immune to artifact corruption BY CONSTRUCTION, and
   strictly stronger than the artifact-grep style this seat uses where the Sage-env split
   forces it. Their outputs are display; their locks are truth. ADOPTED as preferred
   practice: compute-grade locks wherever the env allows; artifact-grep only when the
   computation cannot run in the canonical suite env, and then full-line pinned.
3. **Vacuous-pattern scan: zero hits** across 27 compute files and 6 lock files.
4. **Transcription drift: none found** (samples verbatim: the 15/32 coupling fraction;
   the 203,123 census count).
5. **The process-slip catalog stands** (6 items this window: atlas-regen ×2, the AI
   trailer, seal conventions ×2, one seat path, relay files in-tree) — every one now has
   a standing counter-rule, and NONE ever touched a verdict.

Verdict: cc3's mathematics passes the deepest audit applied to any seat's corpus; the one
directional lesson flows FROM them TO this seat (lock architecture).
