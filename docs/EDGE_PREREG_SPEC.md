# THE EDGE PREREG — the mirror-isospectral split as a laboratory falsifier (L173; TO BE SEALED)

**Status at landing: SEALED (sha256 in `docs/SEAL_LEDGER.md` at the bank; amendments
only by dated addendum-beside, never edits). Owner rulings executed: D-2 (this lane
seals alone; the tone crossing is L178, its own gated cell, shared appendix), D-3
(placeholder R6 budget, amendable when the audit seat's anchor linewidths land).
Differential-first per L173's binding discipline: the first section states what
standard theory already forces, before any claim.**

**BANKED IDENTITY:** before any new (silver or experimental) number is read, the
pipeline must reproduce B1095's banked golden law inside itself — closed at
N = 987 and 2584, broken at exactly [0, 1] at N = 1597 and 4181 — the positive
control run and recorded in `frontier/B1106_edge_seal/b1106_gen_control.json`.
**PRIOR ART:** corpus grep at design time — B1085/B1095 (the differential's source
and its concessions), B724 (the defusal pattern this spec is built to survive),
B8094 (the realization catalogue), S034-N5 (the genericity discipline); external
literature named, not read, per Gate 5-Q: gap labeling (Bellissard school),
Damanik–Gorodetski, Sütő.

## §0 — WHAT STANDARD THEORY ALREADY FORCES (read this before the prediction)

For a Fibonacci-modulated chain (photonic, polaritonic, or cold-atom) with the
phason/intercept as the scan dial: **gap labeling** fixes the integrated density of
states in every spectral gap to values in (ℤ + ℤα) mod 1, and **bulk–boundary
correspondence** makes edge modes traverse those gaps as the phason scans, with
per-gap counts given by the gap labels. **Any observation of edge modes crossing
Fibonacci gaps under a phason scan — including the raw 5-vs-6 boundary-capable
counts at the windows below — is standard topological-pumping content, in kind**
(conceded in the banked record, B1095, before this prereg was written). A result of
that kind confirms textbook theory and says NOTHING about this program. **The
program's prediction is only what follows — the cross-hand structure that gap labels,
being hand-blind, cannot see.**

## §1 — THE PREDICTION (the differential; all content banked in B1095, exact)

Cut the bi-infinite golden Sturmian chain once; the two half-lines are the cut's two
hands. At **reversal-closed windows** (size N = an EVEN-index Fibonacci number; banked
verifications N = F₁₆ = 987 and F₁₈ = 2584):

1. **Exact two-hand isospectrality (forced).** The two hands' spectra agree exactly
   (the exchange conjugation J·H_R·J = H_L holds letter-for-letter at these windows;
   banked agreement 1.3×10⁻¹⁵ over 2584 levels). The energies are P-invariant.
2. **Complementary localization (free — the P-bit made measurable).** The shared
   boundary-capable energies localize on OPPOSITE hands, splitting as evenly as
   parity permits: an odd-sized shared family cannot split evenly (banked instance:
   eleven shared energies at N = 987 split 5-right / 6-left).
3. **The odd-index control (forced breakage).** At ODD-index windows (N = F₁₇ = 1597,
   F₁₉ = 4181) the exact isospectrality BREAKS at exactly the two cut-adjacent
   letters — the same apparatus must see the two-hand degeneracy fail there.

## §2 — KILL CONDITIONS (any one, at budget, kills the prediction; NEGATIVE banks)

- K1: the two hands' spectra differ beyond the R6 budget at an even-index window.
- K2: the localization split is absent, or even where parity forbids even, or
  sides-random across shared states (no P-equivariant structure).
- K3: the odd-index control FAILS to break the two-hand degeneracy (if odd windows
  look as closed as even ones, the "reversal-closed window" mechanism is wrong).

## §3 — CONTROLS (C-GEN RUN AND PASSED before this seal; C-GAP standing)

- **C-GEN (the genericity control): RUN, bite-controlled, PASSED — the closure law
  is golden-specific in this prereg's pinned convention.** The instrument first
  reproduced the banked golden law as its positive control (B1095's convention
  verbatim: slope 1/φ², cut phase ρ = α, left-outward = reversed(right)) — closed at
  N = 377, 987, 2584 (even Fibonacci index), broken at exactly the two cut-adjacent
  letters at N = 233, 610, 1597, 4181 (odd index): seven windows, three beyond the
  banked four. THEN the strict silver analog (slope 1/silver² = 3 − 2√2, same ρ = α
  convention) was tested at seven Pell windows (N = 29 … 5741): **it NEVER closes** —
  every window breaks, alternating cut-adjacent [0,1] and CENTER [N/2 − 1, N/2]
  breaks (the center-break structure recorded as observed). **Disclosure, so no one
  later mistakes it for a refutation:** a cousin silver parametrization (slope
  1 − 1/silver = 2 − √2) DOES mirror the golden even/odd alternation — therefore
  this prereg PINS the convention: the prediction and its control are stated for the
  1/metallic² slope family at ρ = α, and the experimental realization must implement
  that family. The tone lane's own genericity control is the audit seat's B8111
  (sealed prediction, honest failure, the 2-of-5 discriminating repair) — separate
  lane, separate control, both banked before either seal.
- **C-GAP (the gap-labeling differential, standing):** every claimed observable here
  must be checked NOT to be a function of gap labels alone. §1.1's two-hand identity
  and §1.2's localization SIDE are hand-resolved quantities; gap labels are
  hand-blind. Any observable that collapses to a gap-label function is standard
  content and may not be counted as confirmation.

## §4 — R6 PRECISION BUDGET (PLACEHOLDER per the owner's D-3; amendable by addendum)

Energy resolution ≤ 1/10 of the smallest level spacing within the shared
boundary-capable family at the chosen window (at N = 987: the eleven banked energies
of B1095's exact table set the spacing); localization readout must resolve WHICH hand
binds each shared state with side-assignment confidence ≥ 3σ. **To be instantiated
from the audit seat's anchor-linewidth lane when it lands; the placeholder is
declared, dated, and bounded — not silent.**

## §5 — APPARATUS (the shared appendix; also serves L178)

The banked catalogue (B8094): photonic waveguide arrays realizing the Fibonacci
quasicrystal (Verbin–Zilberberg–Kraus–Lahini–Silberberg, arXiv:1403.7124, PRB 91
064201); the off-diagonal Harper waveguide programme; polariton Fibonacci chains;
cold-atom Aubry–André–Harper with real-space edge detection. The scan dial in every
realization is the phason — the Sturmian intercept — which is exactly the coordinate
the object leaves free (B8091: slope forced, intercept free; B1085's ρ).

## §6 — RELATION TO L178 (the tone crossing; NOT part of this seal)

The scale-factorisation design's value crossing (the five tones as measured spectral
invariants, scale divided out) is a SEPARATE crossing sharing this apparatus and both
controls, registered as L178 and GATED on its own open question (are the tones
observables?). Nothing in this seal claims a value; nothing there is bound by this
seal. One envelope, two seals, per the sitting's mechanism test.
