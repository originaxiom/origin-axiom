<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **1094** |
| words of findings prose | **747,671** |
| test lock files referenced | **671** |
| arcs carrying an authored verdict | **1094** (100.0 %) |
| recorded closures | **769** (602 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 723 |
| NEGATIVE | 299 |
| OPEN | 62 |
| RETRACTED | 10 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 49 |
| `genericity` | 38 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 43 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B1200`** (646 words, 0 locks)  
ONE POLYNOMIAL, THREE FACES -- the saddle, the founding obstruction, and the boundary structure are the SAME Phi3 = u^2+u+1, and the map between them is c. THE CROSS-SEAT EVENT: cloud independently reproduced two of this bench's batch-5 results (the kappa-law governing the relational bit, B1195/GC-24; and its SELECTOR-FREEDOM, B1196/GC-27 -- epsilon constant on the pair's orbit) and returned a cross-link this bench did not name, Phi3(kappa-2) = 0, VERIFIED HERE EXACTLY rather than adopted. FACE 1, the saddle: B1195/GC-21 derived u^2+u+1 = 0 from the stationary phase of the object's own b=1 partition function, knowing nothing of kappa. FACE 2, the founding obstruction: kappa = tr[a,b] = u^2+2 at the Eisenstein point with |kappa-2| = 1 (the unit obstruction -- existence as frustrated cancellation, B309/B518/B285), whence kappa-2 = u^2 and Phi3(kappa-2) = 0 IDENTICALLY MOD Phi3 (exact polynomial remainder -- an identity of the construction, not a numerical coincidence). FACE 3, the boundary: cloud's memo-104 object (E6 as a free rank-3 Z[omega]-module) has Coxeter^4 giving M^2+M+I = 0, the same Phi3, rebuilt here with M^3 = I. THE SET IDENTITY: the saddle set IS {kappa-2, its conjugate} -- the same two numbers, not merely roots of the same polynomial. THE LINKING MAP, this bench's addition: the map carrying a saddle point to the obstruction is u -> u^2, which on the two primitive cube roots is exactly the SWAP -- the Galois generator c (B1174: c = mirror = chirality = Gal(K/Q)'s generator) -- so the faces are not merely governed by one polynomial, THE MAP BETWEEN THEM IS THE PROGRAM'S ONE INVOLUTION. WHAT THIS UNIFIES: the existence audit's list treated item 1 (SEAM-A's saddle) and item 4 (the bit's kappa-law) as separate; they are ONE INVARIANT -- the polynomial whose vanishing IS the founding frustration is the polynomial whose roots are where the partition function is stationary, and the boundary satisfies it too. THE OTHER HARVEST, GRADED: cloud's quine build (memo 107) landed on OUTCOME Q1 -- 'the self-report is complete except exactly one bit, and the bit is c' -- EXACTLY what B1184 predicted when the spec's criterion was anchored to it, with Q2 not firing so B1183 stands; their external-bit count and this bench's relational bit are (their words) 'the same statement at two levels' -- CITED, their certificate not re-run here, verification queued. Cloud's other new work and codex's R020/R021/R022 are fetched and QUEUED for digestion, unharvested, per the owner's 'when time'. FENCES: the identity is this bench's own symbolic re-derivation (cloud's memo is the prompt, not the evidence); FACE 3 is rebuilt only as the abstract Phi3-companion relation, their E6/Z[omega] construction remaining CITED/UNVERIFIED here; nothing weakens a negative -- kappa != 2 is still the obstruction, the marking is still not derived; the result is that these were never three facts. Gate 5 clean.  
`B1200_one_polynomial/FINDINGS.md`

**NEGATIVE — `B1174`** (802 words, 0 locks)  
THE Z/2-IDENTIFICATION CELL (R50-3; the register's Q1 = B1169's S1, double-discovered by the sweep and the meditation independently). THE HYPOTHESIS AS POSED -- the four program Z/2's (B942 chirality/Gal(K/Q), B957 value torsor, B1168 mirror bit, S068 genus/breath) are ONE involution -- is REFUTED. THE PROVED SUBSTANCE (the productive content, the B1157 shape): ONE SHARED INVOLUTION with four names -- c = the MIRROR (SnapPy: the reversed m004's holonomy traces are the complex conjugates on a,b,ab => on the trace field K=Q(sqrt-3) in C the mirror IS complex conjugation) = CHIRALITY (B942's quotient: c not in Gal(K^ab/K), c generates Gal(K/Q)) = the c-LEG OF BOTH V4's, located exactly by cyclotomic reduction mod Phi_12 (branch V4 = Gal(Q(zeta12)/Q): c = z->z^11 fixes sqrt3 + flips sqrt-3 = THE ORIENTATION LEG; z->z^7 FIXES K pointwise + flips sqrt3 = B1067's form-class swap = B1164's bit 2 -- the census grounded leg-by-leg; meeting V4 = Gal(Q(sqrt-3,sqrt5)/Q): c flips sqrt-3, fixes sqrt5). THE REFUTATIONS, each by an exact field action: B957's value torsor (cell 1) swaps sqrt5 -- but c is TRIVIAL on Q(sqrt5) (real) => the value swap is NOT c; THE MECHANISM = the parity law AT THE FIELD LEVEL: c acts nontrivially on a quadratic field IFF the field is IMAGINARY (the real/metallic hearing ladder is mirror-even -- no hearing-side Z/2 can be the orientation); S068 row 1's genus-Z/2 of Q(sqrt-15) must fix sqrt-15 hence flips BOTH sqrt-3 and sqrt5, while c flips only sqrt-3 and MOVES sqrt-15 => distinct legs of the same meeting V4, no canonical iso (the row's clean-negative fires WITH the constructive residue). ONE-LINE THEOREM: NOT ONE TORSOR -- ONE SHARED INVOLUTION (two V4's sharing exactly the c-leg; everything the observer's orientation touches is the c-leg; the value/genus/form-class bits are provably other legs). CONSEQUENCES ROUTED: B1169's S1 PARTIALLY PROMOTED (mirror=chirality=Gal proved; the remaining gap = the QP-4 obstruction-class comparison, named -- dated addendum in B1169); B1166's C4 CONSTRUCTIVELY RESOLVED ('the presentations share the c-leg' -- relayed to cloud for the C4 amendment + codex for the B1024 frame-leg typing: is theta=cr the k5/k7-type leg?); B1161's bypass-door label SHARPENED still SUPPORTED-CONJECTURAL (the W0 marking's c-component = the orientation bit, proved; the full identity awaits the QP-4 class); S068 row 1 closed. FENCE (honest): the first leg-2 implementation misfired (subs on a non-Symbol printed a wrong V4 table) -- caught by HAND-CHECKING k=11 against z^7=-z arithmetic BEFORE banking, re-implemented with exact reduction + load-bearing asserts (the MB12/vacuous-check class, self-caught in-cell). SnapPy check numeric 1e-9; the conjugate-rep fact standard; all Galois arithmetic exact. kill_graph routed (hatch: the QP-4-class comparison + the Q(sqrt-7) partial bridge). No firewall crossing; Gate 5 clean (Galois actions on named surds; no measured value).  
`B1174_z2_identification/FINDINGS.md`

**RETRACTED — `B964`** (743 words, 0 locks)  
TWO CORRECTIONS, prompted by the owner's doubt ('are we sure? i think were getting smth wrong') -- THEY WERE RIGHT. ERROR 1, a SCOPE error banked this morning in B962: 'the 27-VEV route provably stops one step short of the SM'. What is TRUE is that 27 -> 10+5+5bar+5bar+1+1 under SU(5) contains NO 24, so 27 VEVs ALONE cannot break SU(5) -> SM, and that branching stands. What was WRONG is the SCOPE: the 78 (ADJOINT) of E6 DOES contain a 24 (78 -> 45+16+16bar+1 under SO(10), and 45 -> 24+10+10bar+1 under SU(5)), so SU(5) -> SM is broken by an ADJOINT VEV, which E6 obviously has. The standard chain uses BOTH -- 78 VEVs for the rank-PRESERVING breaks (E6 -> SO(10)xU(1); SU(5) -> SM) and 27 VEVs for the rank-REDUCING one -- so 'stops one step short' is true ONLY for 27-ONLY breaking, the E6SSM's restriction, which exists because string constructions often supply only 27s; it is NOT a statement about the VEV route in general, and the E6SSM quotation leaned on was scoped to that setting and read as universal. ERROR 2, SUBSTANTIVE and repeatedly banked in B952, B959, B960 and echoed in B962: 'the object does not supply a VEV'. THIS IS FALSE. The unbroken group after an adjoint VEV <Phi> is the STABILIZER of <Phi> under the adjoint action -- that is, the CENTRALIZER of <Phi> -- SO 'ADJOINT VEV' AND 'MEASUREMENT / CENTRALIZER' ARE THE SAME OPERATION. The object DOES supply VEVs: the ADJOINT ones, and that IS the measurement cascade; B892's z(x1) and z(x1,y*) are the unbroken groups of adjoint VEVs, computed as centralizers. THE CORRECTED PICTURE: the object SUPPLIES the rank-PRESERVING (adjoint) VEVs -- this IS the measurement cascade -- and LACKS the rank-REDUCING (27) VEVs, with both needed. B952's rank obstruction is UNCHANGED and now has a physical name: adjoint VEVs preserve rank, which is exactly WHY the cascade preserves rank; the obstruction was never that the object 'has no VEV' but that it has ONLY THE RANK-PRESERVING HALF. THE REFRAMING: THE MEASUREMENT CASCADE IS AN ADJOINT HIGGS MECHANISM -- the programme has been doing GUT symmetry breaking all along without naming it that way, and what it lacks is precisely and only the rank-reducing 27-VEV half. That is sharper, physical, and makes the missing piece exactly one named object rather than a vague absence. WHAT SURVIVES OF B962: the 27 branching with no 24; the VEV DIRECTION being an input everywhere (Q2); the F4 = generic-VEV-stabilizer unification, now STRONGER since the generic 27 VEV and the adjoint VEV are both genuinely VEVs; OP^2 homogeneity including on triples, hence no canonical choice over R or C; the arithmetic exception (Kato-Yukie, cubic etale algebras), so L138 STANDS; and the certified Elkies-Gross null. WHAT IS WITHDRAWN: 'the 27-VEV route stops one step short' as a GENERAL statement, and 'the object does not supply a VEV' EVERYWHERE it appears. WHY IT WAS MISSABLE: both errors come from the same habit, TREATING 'VEV' AS MEANING '27 VEV' -- once the word is used loosely, 'the object supplies no VEV' sounds true and 'the 27 route stops short' sounds like a general no-go; the fix is terminological discipline, SAY WHICH REPRESENTATION THE VEV LIVES IN, EVERY TIME. Nothing in B959/B960's no-go is disturbed: that result is about CENTRALIZER constructions and is now visible as a statement about ADJOINT VEVS specifically, which is exactly what it always was.  
`B964_vev_correction/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

