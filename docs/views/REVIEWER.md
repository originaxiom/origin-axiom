<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **974** |
| words of findings prose | **672,259** |
| test lock files referenced | **626** |
| arcs carrying an authored verdict | **961** (98.7 %) |
| recorded closures | **749** (582 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 641 |
| NEGATIVE | 279 |
| OPEN | 31 |
| RETRACTED | 10 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 49 |
| `genericity` | 35 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 29 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B1054`** (1373 words, 1 locks)  
REVIEW 1 OF THE CONSOLIDATION SEAT -- the commissioned decadal review of this branch's own window (qB1024-qB1053, thirty arcs), run by the seat that produced it, with independence arriving one step later at main's digest per the commission's condition 3. THE FIVE FINDINGS, all measured with this window excluded from every denominator that measures it (E37): (1) ALL THIRTY ARCS CARRY verdict PROVED, against a corpus base rate measured over the other 930 with the strata separated (E20): instrument arcs 73/90 = 81.1%, plain arcs 538/841 = 63.97%; this window is 5 instrument (P = 0.35, NOT significant, withdrawn from the claim) plus 25 plain (P = 1.4e-5, the real finding, and the pooled 3e-6 first published here was a stratum-conflation) -- while EIGHTEEN of the thirty bodies carry retraction, refutation, decline or non-finding language and TWO declare an outright NON-FINDING; this is the window's own L166 instanced thirty times, by arcs banked AFTER L166 was written. THE CONTROL that makes it fair: the same thirty arcs carry an atlas status that DOES discriminate (banked 18, dead 9, dormant 1, open 2), so the judgement was made and recorded, just not in the field the negatives hunts select on -- a routing failure between two metadata fields, not an absence of judgement. (2) A HEADLINE FINDING OF THIS REVIEW WAS RETRACTED BY THE REVIEW ITSELF, and the retraction is the more useful result. The first form read: the consolidation-debt metric (published 245 -> 175) SELECTS ON verdict == PROVED, counts 175 and is blind to 191, showing 48% of its own subject -- framed as L166's defect committed by the window's own metric. THAT FRAMING DOES NOT SURVIVE CHECKING. The scope is DECLARED, in DEBT_LEDGER itself, in a comparison table added by qB1033 -- an arc of this same window: REPRESENTATION_TRIAGE covers PROVED u NEGATIVE, this ledger covers PROVED, and the two 'differ, and each is right for its own question'. The code agrees rather than merely the prose (representation_sweep.py:69 rejects any verdict outside PROVED/NEGATIVE), and that register is GATED -- representation-sweep fails the build on an untriaged arc -- so the 191 arcs called invisible are covered by a STRONGER mechanism than the ungated ledger they were measured against. The error was one of SCOPE, not arithmetic: the numbers were right and the sentence around them was wrong, which is exactly the defect species this window spent thirty arcs cataloguing, committed by the review that catalogued it. WHAT SURVIVES is a PARTITION REMAINDER an order of magnitude smaller: the two registers divide the corpus by verdict and NEITHER TAKES OPEN OR RETRACTED -- 41 arcs corpus-wide (OPEN 31, RETRACTED 10), of which 20 are uncited on the five curated surfaces and are not instruments (OPEN 16, RETRACTED 4); the four uncited RETRACTED are the least defensible cell. WHY IT HAPPENED, recorded because a taxonomy that only catalogues other people's instances is untested: I read the metric's CODE and the ledger's ROWS and did not read the section of that same file, twenty lines above the rows, which declares the scope -- triaging a document by the part that answered my question. (3) THE HANDOFF'S OWN CORRECTION TALLY DOES NOT SUM -- 12+6+4+1 = 23 against 24 numbered rows, and section 2.2's eleven rows carry no catch-mechanism column so the partition is not derivable from the tables it summarises; B1052 gated that section with n_corr >= 20, a lower bound standing in for an exact four-way claim, which is the text-versus-structure species inside the instrument built to prevent it. (4) TWENTY-NINE OF THIRTY ARCS ARE UNSEALED AND TWO DECLARE IT, against main's R43/R44 standard of declaring every unsealed arc in its header; the practice is defensible for consolidation arcs, the declaration is not optional. (5) TEMPLATE ITEM 1b's OWN INSTRUMENT UNDER-REPORTS: git branch -r --no-merged returned one ref on this container while git ls-remote --heads returns three, missing the relay audit seat's LIVE branch -- the command answers from the clone's cache, not from the remote. ADVANCEMENT, the window's better half: 27 LAW_MAP rows split 12 restorations / 4 re-verifications / 11 new, restorations largest, each re-verified before restoration per campaign step 5 -- a consolidation, not a discovery window -- but all 27 landed in section A (the object's arithmetic) and at least SEVEN state programme methodology, for which LAW_MAP has no section at all (section D's meta-laws are object/observer physics), a structural gap and an owner's call. THE LOOP: the last parseable action-items block on this branch is Review 37 and R38-R42 carry none, but main resumed at R43/R44 AFTER the fork, so the gap is BRANCH-LOCAL -- correcting the commission's reading that no prior review existed; the one inherited item is TOOLBOX.md, which R42 escalated at lag 638 as 'TWO reviews old' with 'it does not survive a third review undeclared', now at lag 684, and this review is the third and declares rather than discharges it because the document is main's and this branch never merges -- the routing is the defect, not the diligence. PROVENANCE, WITH A CORRECTION AGAINST THIS ARC'S OWN FIRST DRAFT: it reported '29 reproducers re-executed, 29 pass, 0 fail' and graded them RUN -- measured by EXIT CODE, and 28 of the 30 scripts return 0 whether they pass or fail, so that sweep was reading a harness that cannot fail. The honest instrument is scripts/checks/instrument_freshness.py, which reads each arc's own recorded verdict and separates STALE-GREEN / RED / KEY-LOSS / CRASH; it reports 28 instruments re-run, all green. B1025 carries no reproducer and declares it. PROMOTION SWEEP: zero promotions to CLAIMS.md, the firewall held; its two window citations are correction banners, not promotions. Sixteen action items registered with owners split OWNER / SEAT / MAIN-DIGEST; the twelve leads qL155-qL166 are registered and deliberately NOT decided, per the commission. NOT-REACHED, countable and never an implied rejection: the Codeberg mirror inventory, main's own B1025-B1044, the other 931 arcs, TOOLBOX.md's content, the twelve leads' dispositions. 72 mechanical checks; nothing to CLAIMS.md; Gate 5 untouched; no mathematics asserted or disturbed.  
`B1054_review_one/FINDINGS.md`

**NEGATIVE — `B1007`** (958 words, 0 locks)  
THE INSTRUMENT ALREADY EXISTED, AND THIS ARC'S FIRST DRAFT WAS WRONG ABOUT THE COST. Banked NEGATIVE: this arc set out to build an arbitrary-precision Maass solver because B1006 named MORE DIGITS as the value layer's bottleneck; it built one that DOES NOT WORK, claimed a cost overturn that is FALSE, and then found that a working one has been on main since B922. (1) THE HEADLINE IS A CORRECTION OF THIS ARC'S OWN FIRST DRAFT: a working, sealed, arb-based 25-digit solver is ALREADY IN THE REPOSITORY at frontier/B878_maass_upper_window/branch_cell9_rung1_v2.py, whose docstring reads 'conformant to sealed prereg v3 (169e9042)' -- THE SAME SEAL HASH B922 NAMES -- so it is the script behind the 58.1-hour run, and it was on main before this arc started. This seat wrote a new solver WITHOUT READING IT: the NINTH INSTANCE this session of grepping claims instead of reading code, B1006's cell D having duplicated B922 the same way five hours earlier. (2) THE COST CLAIM IS WITHDRAWN AND B798 STANDS IN FULL, INCLUDING ITS COST ESTIMATE. The first draft claimed B798's cost estimate was priced against arithmetic that is not what arb does. That is FALSE and B798's own text refutes it in the very sentence that makes the estimate: '4-5 orders of magnitude beyond the certified run, ON A DIFFERENT NUMERICAL STACK (arb/mpmath Bessel, mp linear algebra)'. B798 NAMED ARB, priced an arb instrument, and included arb's overhead as a factor of 10-100. And the 58.1-hour run DID use flint/arb: flint.ctx.prec = int((DIGITS+45)*3.33)+250, K_ir(r,x) = flint.acb(x).bessel_k(i*r), an flint.acb_mat LU solve, at n ~ 1300. WHERE THE ERROR WAS, PRECISELY: B798's cost model has TWO terms and this seat used only one -- modes scale ~LINEARLY WITH PRECISION (900 modes to 11250 at 100 digits) and the dense solve is CUBIC in modes (1953x at 100 digits), while this arc HELD THE MODE COUNT FIXED at 300-1000 and varied only dps. The per-operation timings measured here (1000 K_ir in 0.07/0.10/0.15 s and a 200x200 acb_mat solve in 0.34/0.48/0.64 s at 25/50/100 dps, i.e. arb overhead under 2x) are CORRECT AS MEASUREMENTS and DO NOT SUPPORT THE CONCLUSION DRAWN FROM THEM: precision is cheap PER OPERATION, but the cost is that higher precision DEMANDS MORE MODES and the solve is cubic in modes, so varying dps at fixed M measures the one term that was never the problem. NOTHING IN THIS ARC OVERTURNS B798's LAW OR ITS COST ESTIMATE. (3) THE SOLVER FAILED ITS OWN GATE (recover B922's r = 4.9000853730625213014795758 or claim nothing): M=40 gives g ~ 1e26, M=80 is smooth with no sign change. FIVE DIFFERENCES from the working solver, FOUR OF THEM FIXED IN ITS SOURCE WITH THE REASON IN A COMMENT: (a) NO COLUMN EQUILIBRATION -- the working code divides each column by |Y*K(2pi|mu|Y)| and states why, that the truncation-edge dynamic range (~1e-32 at the real run) collapses to O(1) 'which arb's certified LU requires at n >~ 1300', so THE 1e26 IS EXACTLY THIS AND THE FIX WAS ALREADY WRITTEN DOWN (B792's float64 scanner does the same, svd(V / cn[None,:]), and says so in its docstring); (b) NO RISEN-POINT FILTER -- the working code keeps a point only if mv and ts > Y*(1+1e-20), because a point that does not move gives f(z,Y) = f(z,Y), AN IDENTICALLY ZERO ROW, and this seat filtered nothing so the system was singular by construction; (c) MODES CHOSEN BY COUNT, NOT RADIUS -- the working code takes all mu with |mu| <= R_cut = (pi*r/2 + margin)/(2*pi*Y), the truncation set by where K_ir dies, whereas 'the first M by |mu|' is not the same set and has no convergence argument behind it; (d) Y = 0.28 against the sealed Y = 0.75; and (e) THE WRONG INDICATOR FOR THE JOB, the conceptual error -- the held-out-row residual g(r) is what the working code uses but ONLY for LOCAL REFINEMENT INSIDE AN ALREADY-BRACKETED ROOT (a damped bracketed secant), while DETECTION is done by SIGMA_MIN OF THE COLUMN-NORMALIZED V(r) in the float64 scanner, which DIPS at an eigenvalue; this seat used a REFINEMENT indicator to SEARCH, which is why scanning found no sign change -- g(r) IS NOT BUILT TO HAVE ONE. (4) WHAT SURVIVES: the exact Z[omega] geometry port (91 moves, tau = 2 sqrt3 i to +-4.8e-40, exact dual lattice, working pullback) is CORRECT AND REDUNDANT, since branch_cell9_rung1_v2.py already has _mpgen/_mpmoves/mp_reduce doing the same job in mpmath and already drives arb from it; and ONE genuinely new observation kept because it is not in the working source, that since 1/(2 sqrt3) is about 0.289, ordering Lam* by |mu| gives ONLY m in {-1,0,1} EVEN AT M=30 -- a consequence of choosing modes by count, and another reason not to. HONEST STANDING: THE PROGRAMME CAN REPRODUCE lambda_2 AND HAS BEEN ABLE TO SINCE B922, WITH THE CODE ON MAIN. What this arc contributes is a corrected cost picture (B798 WAS RIGHT), a precise diff against the working instrument for whoever touches it next, and one more datum on the failure mode that has now cost this session nine arcs: READ THE CODE BEFORE REBUILDING IT. No number produced by arb_maass.py enters any ledger, and the file is kept only as the record of the failed attempt. SCOPE: an INSTRUMENT arc asserting no new mathematics about the object; no SM quantity involved, Gate 5 untouched; it changes NO claim's standing and REMOVES a claim this seat made earlier today.  
`B1007_arb_maass/FINDINGS.md`

**RETRACTED — `B964`** (743 words, 0 locks)  
TWO CORRECTIONS, prompted by the owner's doubt ('are we sure? i think were getting smth wrong') -- THEY WERE RIGHT. ERROR 1, a SCOPE error banked this morning in B962: 'the 27-VEV route provably stops one step short of the SM'. What is TRUE is that 27 -> 10+5+5bar+5bar+1+1 under SU(5) contains NO 24, so 27 VEVs ALONE cannot break SU(5) -> SM, and that branching stands. What was WRONG is the SCOPE: the 78 (ADJOINT) of E6 DOES contain a 24 (78 -> 45+16+16bar+1 under SO(10), and 45 -> 24+10+10bar+1 under SU(5)), so SU(5) -> SM is broken by an ADJOINT VEV, which E6 obviously has. The standard chain uses BOTH -- 78 VEVs for the rank-PRESERVING breaks (E6 -> SO(10)xU(1); SU(5) -> SM) and 27 VEVs for the rank-REDUCING one -- so 'stops one step short' is true ONLY for 27-ONLY breaking, the E6SSM's restriction, which exists because string constructions often supply only 27s; it is NOT a statement about the VEV route in general, and the E6SSM quotation leaned on was scoped to that setting and read as universal. ERROR 2, SUBSTANTIVE and repeatedly banked in B952, B959, B960 and echoed in B962: 'the object does not supply a VEV'. THIS IS FALSE. The unbroken group after an adjoint VEV <Phi> is the STABILIZER of <Phi> under the adjoint action -- that is, the CENTRALIZER of <Phi> -- SO 'ADJOINT VEV' AND 'MEASUREMENT / CENTRALIZER' ARE THE SAME OPERATION. The object DOES supply VEVs: the ADJOINT ones, and that IS the measurement cascade; B892's z(x1) and z(x1,y*) are the unbroken groups of adjoint VEVs, computed as centralizers. THE CORRECTED PICTURE: the object SUPPLIES the rank-PRESERVING (adjoint) VEVs -- this IS the measurement cascade -- and LACKS the rank-REDUCING (27) VEVs, with both needed. B952's rank obstruction is UNCHANGED and now has a physical name: adjoint VEVs preserve rank, which is exactly WHY the cascade preserves rank; the obstruction was never that the object 'has no VEV' but that it has ONLY THE RANK-PRESERVING HALF. THE REFRAMING: THE MEASUREMENT CASCADE IS AN ADJOINT HIGGS MECHANISM -- the programme has been doing GUT symmetry breaking all along without naming it that way, and what it lacks is precisely and only the rank-reducing 27-VEV half. That is sharper, physical, and makes the missing piece exactly one named object rather than a vague absence. WHAT SURVIVES OF B962: the 27 branching with no 24; the VEV DIRECTION being an input everywhere (Q2); the F4 = generic-VEV-stabilizer unification, now STRONGER since the generic 27 VEV and the adjoint VEV are both genuinely VEVs; OP^2 homogeneity including on triples, hence no canonical choice over R or C; the arithmetic exception (Kato-Yukie, cubic etale algebras), so L138 STANDS; and the certified Elkies-Gross null. WHAT IS WITHDRAWN: 'the 27-VEV route stops one step short' as a GENERAL statement, and 'the object does not supply a VEV' EVERYWHERE it appears. WHY IT WAS MISSABLE: both errors come from the same habit, TREATING 'VEV' AS MEANING '27 VEV' -- once the word is used loosely, 'the object supplies no VEV' sounds true and 'the 27 route stops short' sounds like a general no-go; the fix is terminological discipline, SAY WHICH REPRESENTATION THE VEV LIVES IN, EVERY TIME. Nothing in B959/B960's no-go is disturbed: that result is about CENTRALIZER constructions and is now visible as a statement about ADJOINT VEVS specifically, which is exactly what it always was.  
`B964_vev_correction/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

