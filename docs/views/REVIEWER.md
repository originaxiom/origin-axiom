<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **880** |
| words of findings prose | **595,726** |
| test lock files referenced | **592** |
| arcs carrying an authored verdict | **867** (98.5 %) |
| recorded closures | **740** (573 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 558 |
| NEGATIVE | 270 |
| OPEN | 30 |
| RETRACTED | 9 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 48 |
| `genericity` | 34 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 24 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B956`** (869 words, 0 locks)  
L133 ANALYSED AND REFRAMED AS AN INTERMEDIATE-CLOSURE PROBLEM (repo half; the literature panel B955 runs in parallel). REPO SWEEP: Wilson line / Hosotani / holonomy-breaking appears in NO arc written before today -- every hit is a document written today or the July literature map -- and only 11 arcs mention a VEV at all, so rank-reduction mechanisms are essentially UNWORKED here, consistent with B949's finding that bridge_construction is the objective frontier at 5/14 with 8 dead. Good news (nothing to duplicate), caution (nothing to build on). THE REFRAMING, sharper than 'find a rank reducer': the object ALREADY HAS TWO REGIMES WITH COMPUTED COMMUTANTS AND THEY SIT ON OPPOSITE SIDES OF THE TARGET -- centralizing abelian torus data (B892's charges) gives su(3)+su(2)+u(1)^3 at RANK 6, TOO BIG; a Zariski-DENSE image (B582/B576) gives commutant = the centre Z/3 at RANK 0, TOO SMALL; and the Standard Model sits at RANK 4, STRICTLY BETWEEN. So L133 is an INTERMEDIATE-CLOSURE QUESTION: does the object's E6 representation variety contain points that are non-abelian but PROPER, whose commutant has rank 4 and retains complex reps? That names a GAP IN A KNOWN LADDER rather than a missing mechanism, which is far more tractable. THE TENSION, ONE LEVEL DOWN, AND IT IS THE SAME TENSION: B582's own proof says why it overshoots -- 'the Zariski closure H of the amalgam's image CONTAINS BOTH GLUED SL(2) IMAGES, so Lie(H) contains the generated algebra = e6, so H = E6(C)' -- i.e. THE CONSTRUCTION THAT BUYS CHIRALITY IS EXACTLY THE ONE THAT FORCES THE CLOSURE TO BE FULL, and a full closure has commutant of rank 0. So chirality and a rank-4 commutant pull in OPPOSITE DIRECTIONS AGAIN, one level below the theta-even/theta-odd trade-off B953 found: what supplies the matter destroys the rank, and what fixes the rank destroys the matter. THAT RECURRENCE IS THE MOST INTERESTING THING IN THIS ANALYSIS -- it is now visible at TWO INDEPENDENT LEVELS, which raises a question that must be asked BEFORE any computation: is this a coincidence of two constructions, or IS THERE A THEOREM BEHIND IT? If the latter, L133 is a NO-GO rather than a lead, and finding that out is worth more than finding a mechanism. CAUTION ON THE CANDIDATE, placed on the record BEFORE the panel returns: breaking by a flat connection gives the COMMUTANT OF THE HOLONOMY, so if the holonomy is ABELIAN (valued in a maximal torus) the commutant contains that torus and the breaking is RANK-PRESERVING -- EXACTLY THE SAME DEFECT AS MEASUREMENT. Only NON-ABELIAN holonomy can reduce rank; pi_1(m004) IS non-abelian so the mechanism is available in principle, but if the object's natural holonomies are abelian the candidate INHERITS THE VERY OBSTRUCTION IT WAS MEANT TO SOLVE. Pending B955 Q1/Q2, NOT settled here. WHAT THIS MAKES READY: the target is strictly between two already-computed regimes so the question is well-posed and bounded; the chirality-supplying construction provably overshoots and cannot be reused as-is (a WEAKER gluing is required); the proposed mechanism may inherit the obstruction; and the recurrence should be tested for a theorem before the mechanism is hunted. LIMITS: the Zariski-dense-implies-commutant-is-the-centre fact and E6's centre being Z/3 are standard and CITED not re-derived; nothing here is computed about the object beyond assembling banked results -- this is an ANALYSIS arc claiming no new mathematics; and whether an intermediate closure with the right commutant EXISTS AT ALL in E6 is not established here.  
`B956_l133_analysis/FINDINGS.md`

**NEGATIVE — `B959`** (926 words, 0 locks)  
L133 -- OUTCOME NO-GO (simply connected form) against a criterion sealed and pushed (4c3c4775) BEFORE any cell ran, with the prior disclosed as NO-GO and FOUND named as the convenient answer needing the higher bar; IT DID NOT CLEAR IT. BANKED-IDENTITY GATE PASSED: dim Z(su(3)_colour) in e6 = 16 reproduced on the same instrument. (2) INNER INVOLUTIONS CANNOT REDUCE RANK, form-independently: all 63 sign-gradings of the E6 root system were built and EVERY ONE FIXES THE FULL CARTAN, so every inner-involution fixed subalgebra has rank 6. (3) THE OUTER ROUTE REACHES RANK 4 AND KILLS THE 27 DOING IT: E6's Dynkin diagram has EXACTLY ONE nontrivial automorphism tau, of order 2, swapping node pairs (0,5) and (2,4) -- computed here from the Cartan matrix -- and those are the nodes carrying the 27 and the 27-bar, so TAU EXCHANGES THE TWO AND ON Fix(tau) THEY BECOME ISOMORPHIC: THE 27 RESTRICTS SELF-DUAL. Self-duality is inherited by restriction to ANY subalgebra, so the entire outer route -- F4, C4 = sp(8), and every subalgebra of either -- makes the 27 REAL. This closes both rank-4 outer targets AT ONCE with one argument, is form-independent, and generalises B953/B954 from theta alone to the whole outer class. (4) THE SURVIVING HATCH IS TOO SMALL: B955 left exactly one hatch, the non-abelian images pi_1(m004) actually has, and measured here they give max elementary-abelian 2-rank A4 = 2, D5 = 1, S5 = 2 and 3-rank A4 = 1, D5 = 0, S5 = 1 -- NONE EXCEEDS RANK 2. By Steinberg's theorem (centralizers of semisimple elements in a SIMPLY CONNECTED group are connected): for commuting involutions x,y, Z(x) is connected, x lies in its centre hence in every maximal torus of Z(x), and y semisimple in Z(x) lies in some maximal torus T, so <x,y> is TORAL, its centralizer contains a torus, and the rank is 6. So ALL THREE of the object's available images give rank-6 centralizers, closing the hatch on both the 2- and 3-primary sides. (5) THE ONE HATCH THIS DOES NOT CLOSE, STATED PLAINLY: the torality step ASSUMES THE SIMPLY CONNECTED FORM; it breaks at rank 3 because Z(x) need not be simply connected (which is why E8 has its famous non-toral 2^5), and in the ADJOINT form E6/Z3 a RANK-2 elementary abelian subgroup CAN be non-toral. So the no-go is proved for the simply connected form, and if the object's representations land in the adjoint form a non-toral rank-2 subgroup is NOT excluded by this argument -- registered as L136. Note sections 2 and 3 are form-INDEPENDENT, so even in the adjoint case the outer route and single inner involutions stay closed; only the rank-2 non-toral inner case survives. (6) WHAT IT MEANS, combined with the banked chain -- measurement preserves rank (B952), theta reduces rank but deletes chirality (B954), abelian holonomy preserves rank necessarily (B955), and now every remaining centralizer route either preserves rank or makes the 27 real: THE OBJECT CANNOT REACH CHIRAL MATTER AT RANK 4 BY ANY CENTRALIZER CONSTRUCTION. This upgrades B952's 'closed by absence' toward CLOSED BY PROOF, with the adjoint-form hatch the honest remaining gap. It does NOT say the object cannot reach the SM -- B955 established that E6 -> SU(5) via two 27 VEVs does exactly that and is standard -- WHAT IS EXCLUDED IS THE CLASS OF OPERATIONS THE OBJECT ACTUALLY HAS; a Higgs-type mechanism remains unexcluded and remains unsupplied. LIMITS: the torality step is CITED not re-derived (Steinberg) and is the load-bearing citation; section 5's hatch is real, so this is a no-go for the simply connected form only; the 27's self-duality on Fix(tau) rests on the computed diagram swap plus the standard duality fact, the same statement as banked B582; and nothing here refutes FMT/SMT, B582 or B955, all of which stand.  
`B959_nontoral_rank4/FINDINGS.md`

**RETRACTED — `B780`** (851 words, 0 locks)  
The c-versus-theta gate's verification and its 8-to-4 enumeration halving are retracted as vacuous: the signatures were hardcoded literals, not computed booleans.  
`B780_galois_reversal_gate/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

