<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **909** |
| words of findings prose | **614,092** |
| test lock files referenced | **592** |
| arcs carrying an authored verdict | **896** (98.6 %) |
| recorded closures | **744** (577 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 581 |
| NEGATIVE | 274 |
| OPEN | 31 |
| RETRACTED | 10 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 49 |
| `genericity` | 34 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 26 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B988`** (615 words, 0 locks)  
THE DECADAL REVIEW NOW CERTIFIES DOCUMENT CURRENCY, ROOM BY ROOM. The protocol had six steps -- suite green, gates green, atlas fresh, promotion-candidacy sweep, framing and stale-leads sweep, dated report -- and EVERY ONE CHECKS ARCS OR GATES while NONE checked whether the documents a reader forms their picture from still describe the programme, which is why ROADMAP_TOE.md stated the position as the kinematic/symmetry frame is forced arithmetic for a MONTH after B862/B863/B864 falsified it and THE_SM_VERDICT.md shipped omitting ELEVEN OF TWELVE cascade arcs, both surviving multiple reviews. STEP 7 ADDED IN FOUR PARTS, none optional. 7a MECHANICALLY FIRST: scripts/checks/doc_currency.py runs and ITS OUTPUT IS PASTED INTO THE REVIEW REPORT including every frozen opt-out and every DECLARED_DEBT WITH ITS AGE, and a debt older than two reviews is ESCALATED BY NAME -- a debt is not an exemption, the direct lesson of B982 where seven gate exemptions rested on an audit that never mentioned them. 7b BY READING, ROOM BY ROOM: seven rooms each with the one question the report must ANSWER rather than assume -- claims (superseded, retracted, or scoped wider than proved?), the chain (can a reader follow philosophy to aAbB to the object to faces/family/both rows to the algebra to the cascade to symmetry breaking and gauge groups WITHOUT A GAP?), the negatives (is every we-do-not-have-X still a claim with a citation?), method (does this describe how we ACTUALLY seal, verify and certify TODAY?), speculation and philosophy (is the firewall still one-way and does the motivation still match the mathematics?), interpretation and easy-read (can a new reader, human or agent, reconstruct the work from these alone?), and logs. 7c THE NAMED-CHAIN CHECK: every waypoint the owner named must be confirmed findable and current with the THIN ones RECORDED RATHER THAN HIDDEN, and today-s check is written into the protocol -- eleven well covered, MARKOV BLANKET at ZERO arcs and in NO document carrying a CONFLATION HAZARD since the corpus is saturated with Markov TRIPLES which is a different object, and FEEDBACK MECHANISM at 2 arcs, both now ladder rungs X31/X32. 7d THE STANDARD AND THE CLAUSE THAT MAKES IT REAL: whoever opens this repository, human reviewer or AI seat, can follow the complete chain of work and arrive at the current state WITHOUT BEING MISLED BY ANY DOCUMENT IN IT -- and A REVIEW THAT CANNOT ASSERT THAT NAMES WHAT BLOCKS IT, which is the operative sentence because it forbids a review from passing SILENTLY over a stale room, exactly how a wrong roadmap survived a month of reviews. SCOPE: governance not mathematics, proving nothing about the object; and it is a READING obligation -- 7a is mechanical but 7b-7d are judgement whose quality is the reviewer-s, so what the protocol guarantees is that the judgement is MADE AND RECORDED, not that it is correct. The currency measure behind 7a is deliberately crude (newest-citation lag): a document can be current while citing nothing recent, or stale while citing a fresh arc -- it is a prompt to read, not a proof of staleness.  
`B988_review_currency/FINDINGS.md`

**NEGATIVE — `B986`** (709 words, 0 locks)  
THE B500 DEPTH-5 REOPEN ATTEMPTED AND THE METHOD KILLED BY ITS OWN CONTROL -- L145a STAYS OPEN, the attempt failing on the INSTRUMENT not on the object. cc3's sweep surfaced B500 (revival_score 6, the highest in the kill graph, and in NO register), whose own text downgrades its kill to PROVISIONAL with 35 words UNCHECKED (26 timeouts + 9 never-reached, the D/M-heavy tail whose resultant eliminants blow up to degree ~3000-9280 beyond in-sandbox gp), and B525 specifies the reopen as 'F_p Groebner or longer timeout'. THE ROUTE PROPOSED: K = Q[t]/(t^4-t-1) is S4-quartic with NO intermediate fields so the child is present iff the eliminant h has a root in K; if p is INERT then O_K/p = F_{p^4} and a root in K reduces to a root in F_{p^4}, so contrapositively h mod p having NO root in F_{p^4} PROVES h has no root in K -- via gcd(h, x^{p^4}-x) by repeated squaring, cheap even at degree 9280, with the whole resultant chain run in GF(p) so the Q blow-up never happens. WHAT CAME BACK: 9 words computed with eliminant degrees 6450-7896 and a root in F_{p^4} at both inert primes 5 and 19; 26 words DEGENERATE, the resultant chain vanishing identically because they are exactly the words needing hunt_d5.py's SATURATION branch which this script does not implement; ZERO proved absent. THE CONTROL, RUN IMMEDIATELY: at p=5 degree 6500, 28 of 40 RANDOM polynomials have a root in F_{p^4}; at p=19 degree 6500, 29 of 40; at p=5 degree 74, 33 of 40. So about 70 percent of random polynomials PASS, and 'all 9 have a root' is EXACTLY THE BASE RATE, zero information. The LOGIC was correct and the POWER is nil: a degree-6500 polynomial has thousands of roots across small extensions so the informative outcome (NO root) essentially never occurs, and A CRITERION THAT CANNOT FAIL IS NOT A TEST -- MB12 applied to this seat's own instrument. THE GENERALISABLE REASON: any test of the form 'does h have a root of type X' is saturated at these degrees, and a discriminating test must use THE SPECIFIC FIELD K rather than merely its degree, i.e. an irreducible degree-4 factor over Q whose field is isomorphic to K, which is what B500's exact route computes and what the mod-p shortcut cannot reach. WHAT GENUINELY STANDS: (1) a REAL INSTRUMENT GAIN -- the eliminants that overflowed the PARI stack over Q are computable in SECONDS mod p, degrees 6450-7896 obtained where the exact route died, so the bottleneck B500 hit is NOT intrinsic and only the decisive test is; (2) the 26 degenerate words are DIAGNOSED NOT LOST, needing the saturation branch that hunt_d5.py already implements, and B525's 'F_p GROEBNER' is most plausibly exactly this -- Groebner OVER F_p to handle saturation -- rather than the root-existence test attempted here; (3) L145a stays OPEN with a sharper statement of what closing it requires, namely mod-p elimination WITH saturation for the 26 plus a FIELD-SPECIFIC rather than degree-specific decisive test for all 35. BANKED RATHER THAN DISCARDED because the attractive half is already written down and would otherwise be re-proposed: recording that the mod-p route computes the eliminants WITHOUT recording that its decisive test is vacuous would hand a later seat a method that looks like it works -- the same propose-and-refute-in-the-same-file pattern cc3 used hours earlier on its own selector reading.  
`B986_b500_stragglers/FINDINGS.md`

**RETRACTED — `B964`** (743 words, 0 locks)  
TWO CORRECTIONS, prompted by the owner's doubt ('are we sure? i think were getting smth wrong') -- THEY WERE RIGHT. ERROR 1, a SCOPE error banked this morning in B962: 'the 27-VEV route provably stops one step short of the SM'. What is TRUE is that 27 -> 10+5+5bar+5bar+1+1 under SU(5) contains NO 24, so 27 VEVs ALONE cannot break SU(5) -> SM, and that branching stands. What was WRONG is the SCOPE: the 78 (ADJOINT) of E6 DOES contain a 24 (78 -> 45+16+16bar+1 under SO(10), and 45 -> 24+10+10bar+1 under SU(5)), so SU(5) -> SM is broken by an ADJOINT VEV, which E6 obviously has. The standard chain uses BOTH -- 78 VEVs for the rank-PRESERVING breaks (E6 -> SO(10)xU(1); SU(5) -> SM) and 27 VEVs for the rank-REDUCING one -- so 'stops one step short' is true ONLY for 27-ONLY breaking, the E6SSM's restriction, which exists because string constructions often supply only 27s; it is NOT a statement about the VEV route in general, and the E6SSM quotation leaned on was scoped to that setting and read as universal. ERROR 2, SUBSTANTIVE and repeatedly banked in B952, B959, B960 and echoed in B962: 'the object does not supply a VEV'. THIS IS FALSE. The unbroken group after an adjoint VEV <Phi> is the STABILIZER of <Phi> under the adjoint action -- that is, the CENTRALIZER of <Phi> -- SO 'ADJOINT VEV' AND 'MEASUREMENT / CENTRALIZER' ARE THE SAME OPERATION. The object DOES supply VEVs: the ADJOINT ones, and that IS the measurement cascade; B892's z(x1) and z(x1,y*) are the unbroken groups of adjoint VEVs, computed as centralizers. THE CORRECTED PICTURE: the object SUPPLIES the rank-PRESERVING (adjoint) VEVs -- this IS the measurement cascade -- and LACKS the rank-REDUCING (27) VEVs, with both needed. B952's rank obstruction is UNCHANGED and now has a physical name: adjoint VEVs preserve rank, which is exactly WHY the cascade preserves rank; the obstruction was never that the object 'has no VEV' but that it has ONLY THE RANK-PRESERVING HALF. THE REFRAMING: THE MEASUREMENT CASCADE IS AN ADJOINT HIGGS MECHANISM -- the programme has been doing GUT symmetry breaking all along without naming it that way, and what it lacks is precisely and only the rank-reducing 27-VEV half. That is sharper, physical, and makes the missing piece exactly one named object rather than a vague absence. WHAT SURVIVES OF B962: the 27 branching with no 24; the VEV DIRECTION being an input everywhere (Q2); the F4 = generic-VEV-stabilizer unification, now STRONGER since the generic 27 VEV and the adjoint VEV are both genuinely VEVs; OP^2 homogeneity including on triples, hence no canonical choice over R or C; the arithmetic exception (Kato-Yukie, cubic etale algebras), so L138 STANDS; and the certified Elkies-Gross null. WHAT IS WITHDRAWN: 'the 27-VEV route stops one step short' as a GENERAL statement, and 'the object does not supply a VEV' EVERYWHERE it appears. WHY IT WAS MISSABLE: both errors come from the same habit, TREATING 'VEV' AS MEANING '27 VEV' -- once the word is used loosely, 'the object supplies no VEV' sounds true and 'the 27 route stops short' sounds like a general no-go; the fix is terminological discipline, SAY WHICH REPRESENTATION THE VEV LIVES IN, EVERY TIME. Nothing in B959/B960's no-go is disturbed: that result is about CENTRALIZER constructions and is now visible as a statement about ADJOINT VEVS specifically, which is exactly what it always was.  
`B964_vev_correction/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

