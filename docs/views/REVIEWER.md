<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **922** |
| words of findings prose | **622,152** |
| test lock files referenced | **593** |
| arcs carrying an authored verdict | **909** (98.6 %) |
| recorded closures | **744** (577 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 592 |
| NEGATIVE | 276 |
| OPEN | 31 |
| RETRACTED | 10 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 49 |
| `genericity` | 34 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 26 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B1002`** (631 words, 0 locks)  
B997-S OWED CHECK DISCHARGED: THE CONDUCTOR IDENTIFICATION IS RIGHT, AND CONDUCTOR NAMES TWO THINGS. B997 proved the golden is the UNIQUE metallic grammar whose own-conductor shadow group is a McKay group, conditional on conductor = m^2+4 which it flagged as INFERRED from two data points and not proved canonical. THE CHECK IS DISCHARGED AND THE ANSWER IS BOTH: the corpus already contains the definition in a banked LAW (B675) which uses CONDUCTOR for a DIFFERENT quantity than B666 does -- B675-s CUSP ORDER-S conductor is golden 4 and silver 2, while B666-s word-s own conductor (the shadow modulus) is golden 5 and silver 8. TWO DISTINCT QUANTITIES BOTH CALLED CONDUCTOR IN ADJACENT LAWS, a live terminology collision, and it is why the check looked open when the answer was banked. B997 used the SECOND, and m^2+4 reproduces it exactly (m=1 gives 5, m=2 gives 8), so B997-s identification is CORRECT AS THE SHADOW MODULUS and the flagged condition is discharged. AND B675 SUPPLIES THE MECHANISM B997 LACKED: the QUANTIZATION-INDEX LAW states the mod-shadow reduction is an ISOMORPHISM iff the conductor is COPRIME to the shadow and has a KERNEL LINE iff ramified -- so golden gcd(4,5)=1 gives ISO while silver gcd(2,8)=2 gives RAMIFIED. THEREFORE THE GOLDEN-S SHADOW IS THE McKAY GROUP BECAUSE BOTH INDEPENDENT CONDITIONS HOLD: (1) |SL(2,Z/5)| = 120 = |2I| with N in {3,4,5} the COMPLETE list, proved in B997 via |SL(2,Z/N)| >= (6/pi^2)N^3 > 120 for N >= 6; and (2) gcd(4,5) = 1 so the reduction is an ISOMORPHISM, B675-s law. The silver satisfies NEITHER: 8 is not a McKay order, and gcd(2,8) = 2 makes the reduction ramified, which is exactly why 2O appears ONE QUOTIENT DOWN rather than as the shadow itself. B666 stated the FACT, B675 stated the MECHANISM, B997 supplied the UNIQUENESS -- and none of the three cited the other two; they are now joined. CONSEQUENCES: B997-s condition (ii) is DISCHARGED, its conductor identification correct as the shadow modulus from a BANKED LAW rather than two matching values; B997 is STRENGTHENED not merely confirmed, its uniqueness now carrying a MECHANISM (the golden is the only metallic word where the shadow modulus is a McKay order AND the reduction is unramified); and a TERMINOLOGY COLLISION is registered, since conductor names both the cusp order-s conductor (B675) and the shadow modulus (B666), so any future arc using the word must say which -- the same class as the T1 collision cc3 found and the twelve things called the wall. SCOPE: still TIER 1, predicting none of the 19 numbers, and still the E8 end (2I) not E6, the E6 arrival remaining class-level by B993; what changed is that the uniqueness is no longer conditional on an inferred definition.  
`B1002_conductor_reconciled/FINDINGS.md`

**NEGATIVE — `B996`** (672 words, 0 locks)  
THE GRAMMAR-LEVEL CONTROL: ACCESS TO THE McKAY GROUP IS GENERIC ACROSS THE METALLIC FAMILY. P0: a census of GRAMMARS (metallic words R^m L^m and their mod-N shadows in SL(2,Z/N)), not of manifolds -- the population the owner identified as the right one. WHY THIS CONTROL AND NOT B995-S: the chain DERIVES m004 (C1 Morse-Hedlund, C2 self-selection, C6 Thurston/Riley) and never selected it from a census, so B993-s finding that a third of manifolds also surject onto 2T DOES NOT BREAK THE CHAIN -- what it does is narrower and sharper, REMOVING THE ENDPOINT-S POWER TO CONFIRM THE BEGINNING, since reaching E6 is not evidence C1-C5 were right if a third of entry points reach E6 too; THE CHAIN CAN BE SOUND AND THE SM STILL FAIL TO CORROBORATE IT. C2 is a THEOREM WITH A UNIQUE FIXED POINT so the population at C1-C2 is SIZE ONE, and the relaxation is the metallic family R^m L^m for m >= 2, exactly what-if-C2-did-not-hold. COMPUTED: |SL(2,Z/N)| for N=2..9 is 6, 24, 48, 120, 144, 336, 384, 648, so |SL(2,Z/3)| = 24 = |2T| and N=3 IS THE ONLY MODULUS WHERE SL(2,Z/N) IS THE McKAY GROUP. The metallic shadows mod 3, i.e. the subgroup generated by R^m and L^m: m=1 gives 24, m=2 gives 24, m=3 gives 1, m=4 gives 24, m=5 gives 24, m=6 gives 1, m=7 gives 24 -- so FIVE OF SEVEN METALLIC WORDS GIVE THE FULL 2T, and only m divisible by 3 degenerate, trivially since R^m and L^m are then the identity. VERDICT: access to the McKay group, and therefore to E6, is GENERIC across the metallic family, shared by EVERY metallic word with 3 not dividing m, i.e. TWO THIRDS of the grammars. THE GRAMMAR-LEVEL CONTROL RETURNS THE SAME SHAPE AS THE MANIFOLD-LEVEL ONE (B993): the arrival at E6 does not distinguish the golden grammar, and the hypothesis that only m=1 reaches the McKay group is FALSE. SCOPE, a real limit stated rather than buried: this tests ONE route, the mod-3 shadow, which is the only modulus at which 2T can appear as a FULL SL(2,Z/N); L105-s discriminator is a DIFFERENT statement -- that the golden-s OWN CONDUCTOR is prime so its shadow IS the McKay group while the silver-s is a prime power (8) so the McKay partner sits ONE QUOTIENT DOWN -- which is about WHICH MODULUS IS THE WORD-S OWN, and this cell did NOT pin down that notion, computing the shadow at a fixed modulus instead. SO L105 IS NOT REFUTED HERE; what is refuted is the stronger and simpler hypothesis that access to 2T is itself grammar-specific, and the finer question -- whether the golden is the unique metallic word whose own conductor is 3 -- REMAINS OPEN as the surviving form of L146. Consistency check: at N=8 the golden generates all 384 of SL(2,Z/8) while the silver generates only 32, so the two DO differ at that modulus, compatible with L105 and showing the two statements are about different things.  
`B996_grammar_control/FINDINGS.md`

**RETRACTED — `B964`** (743 words, 0 locks)  
TWO CORRECTIONS, prompted by the owner's doubt ('are we sure? i think were getting smth wrong') -- THEY WERE RIGHT. ERROR 1, a SCOPE error banked this morning in B962: 'the 27-VEV route provably stops one step short of the SM'. What is TRUE is that 27 -> 10+5+5bar+5bar+1+1 under SU(5) contains NO 24, so 27 VEVs ALONE cannot break SU(5) -> SM, and that branching stands. What was WRONG is the SCOPE: the 78 (ADJOINT) of E6 DOES contain a 24 (78 -> 45+16+16bar+1 under SO(10), and 45 -> 24+10+10bar+1 under SU(5)), so SU(5) -> SM is broken by an ADJOINT VEV, which E6 obviously has. The standard chain uses BOTH -- 78 VEVs for the rank-PRESERVING breaks (E6 -> SO(10)xU(1); SU(5) -> SM) and 27 VEVs for the rank-REDUCING one -- so 'stops one step short' is true ONLY for 27-ONLY breaking, the E6SSM's restriction, which exists because string constructions often supply only 27s; it is NOT a statement about the VEV route in general, and the E6SSM quotation leaned on was scoped to that setting and read as universal. ERROR 2, SUBSTANTIVE and repeatedly banked in B952, B959, B960 and echoed in B962: 'the object does not supply a VEV'. THIS IS FALSE. The unbroken group after an adjoint VEV <Phi> is the STABILIZER of <Phi> under the adjoint action -- that is, the CENTRALIZER of <Phi> -- SO 'ADJOINT VEV' AND 'MEASUREMENT / CENTRALIZER' ARE THE SAME OPERATION. The object DOES supply VEVs: the ADJOINT ones, and that IS the measurement cascade; B892's z(x1) and z(x1,y*) are the unbroken groups of adjoint VEVs, computed as centralizers. THE CORRECTED PICTURE: the object SUPPLIES the rank-PRESERVING (adjoint) VEVs -- this IS the measurement cascade -- and LACKS the rank-REDUCING (27) VEVs, with both needed. B952's rank obstruction is UNCHANGED and now has a physical name: adjoint VEVs preserve rank, which is exactly WHY the cascade preserves rank; the obstruction was never that the object 'has no VEV' but that it has ONLY THE RANK-PRESERVING HALF. THE REFRAMING: THE MEASUREMENT CASCADE IS AN ADJOINT HIGGS MECHANISM -- the programme has been doing GUT symmetry breaking all along without naming it that way, and what it lacks is precisely and only the rank-reducing 27-VEV half. That is sharper, physical, and makes the missing piece exactly one named object rather than a vague absence. WHAT SURVIVES OF B962: the 27 branching with no 24; the VEV DIRECTION being an input everywhere (Q2); the F4 = generic-VEV-stabilizer unification, now STRONGER since the generic 27 VEV and the adjoint VEV are both genuinely VEVs; OP^2 homogeneity including on triples, hence no canonical choice over R or C; the arithmetic exception (Kato-Yukie, cubic etale algebras), so L138 STANDS; and the certified Elkies-Gross null. WHAT IS WITHDRAWN: 'the 27-VEV route stops one step short' as a GENERAL statement, and 'the object does not supply a VEV' EVERYWHERE it appears. WHY IT WAS MISSABLE: both errors come from the same habit, TREATING 'VEV' AS MEANING '27 VEV' -- once the word is used loosely, 'the object supplies no VEV' sounds true and 'the 27 route stops short' sounds like a general no-go; the fix is terminological discipline, SAY WHICH REPRESENTATION THE VEV LIVES IN, EVERY TIME. Nothing in B959/B960's no-go is disturbed: that result is about CENTRALIZER constructions and is now visible as a statement about ADJOINT VEVS specifically, which is exactly what it always was.  
`B964_vev_correction/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

