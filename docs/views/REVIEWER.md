<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **882** |
| words of findings prose | **596,915** |
| test lock files referenced | **592** |
| arcs carrying an authored verdict | **869** (98.5 %) |
| recorded closures | **741** (574 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 559 |
| NEGATIVE | 271 |
| OPEN | 30 |
| RETRACTED | 9 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 48 |
| `genericity` | 34 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 25 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B961`** (631 words, 0 locks)  
L135 -- THE FRAME INSTRUMENT, BUILT ON THIS BENCH. B958 found the repo had NO independent construction of the frame or of M12: B909 verified section LVIII by RUNNING INCOMING MATERIAL rather than rebuilding, so every frame-arc claim had been checked against code this bench did not write. frame.py is the missing infrastructure, providing ad(v) as an exact 78x78 adjoint matrix, killing() as the EXACT Killing form tr(ad x . ad y), centralizer(S), killing_perp(S), derived(S), dim_of(S), cartan_basis(), a2_a1_levi(), and a self_test() that exercises every export against a BANKED number. THE SELF-TEST REPRODUCES FOUR BANKED NUMBERS: the Killing form is symmetric with rank 78 (nondegenerate, as e6 is semisimple); dim Z(su(3)_colour) = 16, matching B958; the A2+A1 Levi has dim 14, derived 11, centre 3, matching B892 and B951 EXACTLY; and the Killing-perp of the Cartan is 72 = 78 - 6. SO B892'S THREE NUMBERS ARE NOW DERIVABLE FROM SCRATCH ON THIS BENCH -- the first time, since before today they were only ever checked by running incoming code. THE SELF-TEST EARNED ITS KEEP IMMEDIATELY: the first run returned derived = 4 and centre = 10 instead of 11 and 3, caused by a real bug in this module -- sympy's rref()[1] returns the tuple of PIVOT COLUMNS and it was being used to index ROWS, silently producing a wrong-dimensional space with no error. The banked-number gate caught it and nothing else would have, since a 14-dimensional algebra with 'derived 4, centre 10' is not obviously absurd and, had derived() been used first on an unbanked question, the wrong answer would have looked like a finding; the fix is in place with the trap documented in the source. WHAT IT DELIBERATELY DOES NOT DO: guess the solo seat's frame, floor or M12 -- B958's stated reason for deferral stands, since reconstructing their definitions wrongly would produce a false verification OR a false refutation. WHAT REMAINS OWED: the presence side (solo sections LXXXIII-LXXXVI + XCII) is STILL NOT VERIFIED; the specific frame, floor and M12 need either solo's definitions stated precisely enough to rebuild, or an independent derivation of the orthogonal charge frame from banked structure, and either is a separate cell. LIMITS: this is an INSTRUMENT, not a result, and asserts no new mathematics about the object; the e6 structure constants come from B854's e6_centralizer.py, which this module wraps and verifies rather than re-deriving; and self_test() covers the exports against four banked numbers, not a proof of correctness for arbitrary input.  
`B961_frame_instrument/FINDINGS.md`

**NEGATIVE — `B960`** (558 words, 0 locks)  
L136 -- THE ADJOINT-FORM HATCH CLOSES ITSELF, discharging the single hatch B959 left open. NO PREREGISTRATION, DELIBERATELY and flagged as an OPEN-AND-SHUT READING (as B957 was): the answer is a one-line consequence of a standard fact and appeared the moment the question was posed, so sealing after seeing it would be worse than not sealing. THE QUESTION: B959 proved no centralizer construction reaches rank 4 with a complex 27, but its torality step used Steinberg's theorem and assumed the SIMPLY CONNECTED form, while in the ADJOINT form E6/Z3 a rank-2 elementary abelian subgroup CAN be non-toral and A4/S5 do supply rank 2 -- so which form does the object's E6 live in? THE ANSWER, computed: det(Cartan matrix of E6) = 3 so the centre of the simply connected form is Z/3; and the fundamental weights' root-lattice membership is omega_1 NO, omega_2 YES, omega_3 NO, omega_4 YES, omega_5 NO, omega_6 NO -- so the 27's highest weight is NOT in the root lattice, the centre Z/3 ACTS NONTRIVIALLY on the 27, and THE 27 IS A REPRESENTATION OF THE SIMPLY CONNECTED E6 ONLY: the adjoint form E6/Z3 HAS NO 27, its representations being exactly those of trivial central character (78, 650, ...). WHY THIS CLOSES THE HATCH RATHER THAN MERELY ANSWERING IT: the hatch was an escape route -- work in the adjoint form where non-toral rank-2 subgroups exist and rank 4 might be reachable -- but ESCAPING TO THE ADJOINT FORM COSTS YOU THE 27, THE VERY OBJECT WHOSE CHIRALITY WAS THE ENTIRE POINT. There is no configuration in which the escape helps: where the matter lives the form is simply connected, and where the form is adjoint there is no matter. So the simply connected form is FORCED by the presence of the 27, B959's torality step applies without qualification, and B959's NO-GO IS COMPLETE WITHIN ITS CLASS: no centralizer construction on this object -- measurement, holonomy, or finite image -- can produce chiral matter at the Standard Model's rank. WHAT IS STILL NOT CLAIMED: this does NOT say the object cannot reach the SM, since B955 established that E6 -> SU(5) via two 27 VEVs achieves rank 6->4 with the 27 still complex and that this is STANDARD -- what is excluded is the class of operations the object HAS, not the target itself, and a Higgs-type mechanism remains unexcluded and unsupplied; the class is specifically 'centralizer constructions', so anything outside it (a VEV, an orbifold projection, a quotient that is not a commutant) is untouched; and the standard facts used (det Cartan = 3, centre Z/3, adjoint reps = root-lattice weights) are CITED, with only the lattice-membership computation run here.  
`B960_l136_adjoint/FINDINGS.md`

**RETRACTED — `B780`** (851 words, 0 locks)  
The c-versus-theta gate's verification and its 8-to-4 enumeration halving are retracted as vacuous: the signatures were hardcoded literals, not computed booleans.  
`B780_galois_reversal_gate/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

