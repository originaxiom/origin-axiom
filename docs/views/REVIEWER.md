<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **1112** |
| words of findings prose | **762,231** |
| test lock files referenced | **672** |
| arcs carrying an authored verdict | **1112** (100.0 %) |
| recorded closures | **771** (604 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 731 |
| NEGATIVE | 301 |
| OPEN | 70 |
| RETRACTED | 10 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 49 |
| `genericity` | 38 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 45 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B1204`** (677 words, 0 locks)  
THE SHAPE OF A FORCING THEOREM, read off the record's own successful forcings. The owner asked whether prior symmetry BREAKINGS supply the insight/mechanism/principle for the value arm's missing forcing theorem; they do, but the lesson is not in the breakings -- it is in what actually FORCED in this record, and there are exactly TWO FAMILIES. (i) CLASSIFICATION + MEMBERSHIP -- a finite list plus a test for being on it: E6 forced because ADE/McKay is a finite list and 2T lands on it; the breaking chain unique because 2 of 27 candidates are SM-safe; the tone set a theorem of the McKay tensor law. The menu is FINITE before the test runs. (ii) CONSISTENCY/CANCELLATION -- a condition a candidate can VIOLATE: hypercharge (B1160), the program's ONE forcing that cut a CONTINUUM to points, whose anatomy is re-derived here and is the instructive part -- THE THREE LINEAR ANOMALY CONDITIONS CUT THE 5-DIMENSIONAL CHARGE SPACE ONLY TO A LINE, and IT WAS THE CUBIC [Y]^3 THAT CUT THE LINE TO POINTS (factoring -18(t-3)(t+3), t = +/-3, exactly the SM assignment up to scale and one relabel). BOTH FAMILIES ARE FAILABLE; NEITHER IS A SYMMETRY. WHY OUR TWO CANDIDATES FAILED, IN ONE SENTENCE: the candidates eliminated at B1203 -- c-equivariance (a cut of 0 from 11,720) and iteration of a->ab,b->a (kappa conserved at every rung) -- were both SYMMETRIES, and both were symmetries the admissible class ALREADY SATISFIES; that is MB12's rule biting on us, since a criterion that cannot FAIL cannot select. THE REFINEMENT B1203'S NARROWING NEEDED (its 'break a symmetry' was right in direction but too loose): A FORCING THEOREM IS A FAILABLE CONDITION, AND WHEN IT MUST CUT A CONTINUUM RATHER THAN CHOOSE FROM A FINITE LIST, ITS DECISIVE STEP IS NONLINEAR -- linear and symmetry conditions cut DIMENSION, only a nonlinear condition cuts to POINTS; this is the literal anatomy of the only continuum-to-points forcing the program has achieved. THE NAMED CANDIDATE HOME (PROPOSAL, labelled, not run here): the object's own cubic is the unique invariant in Sym^3(27) with nullspace dimension exactly 1 (B884), and it lives AT THE COUPLING -- precisely where the value re-frame (ledger row E1) says the values live; the proposed cell is whether a consistency condition on the object+closer coupling, CUBIC in the closer's data, exists that a candidate value-map can FAIL, with two bankable outcomes (it exists and cuts MENU-1 to a finite set, reopening the prediction arm with the condition fixed before looking; or it does not, confirming values-as-menu with its reason stated structurally rather than empirically). FENCES: the classification of the record's forcings is checkable and checked (the B1160 anatomy re-derived in verification/); the prescription is a PROPOSAL, not a theorem -- it says what shape to look for, not that the object has one; nothing weakens V-3. Gate 5 clean.  
`B1204_forcing_shape/FINDINGS.md`

**NEGATIVE — `B1205`** (756 words, 0 locks)  
THE CUBIC CELL RUN -- the cubic exists and is failable, but it cuts ONE dimension of THREE, and the missing ingredient is the LINEAR cuts, whose absence the record has already PROVED. WHAT RAN: B1204 prescribed a failable condition nonlinear in its decisive step, naming the object's cubic at the coupling; this cell ran it. THE CUBIC IS REAL: the down block is a 3x3x4 tensor (B1185), so Y_d(h) = sum_k h_k T[:,:,k] is LINEAR in the Higgs direction h in P^3 and det Y_d(h) IS A GENUINE CUBIC FORM ON THE P^3 -- degree 3, not identically zero, and FAILABLE (generic h has det != 0) -- exactly the shape asked for, at exactly the place row E1 says values live. THEN THE COUNTING KILLS IT: one polynomial equation cuts exactly one dimension, so {det Y_d = 0} is a cubic SURFACE in P^3 (dim 3 -> 2), NOT points; its SINGULAR LOCUS IS EMPTY (grad = 0 has no nontrivial solution; 5/5 random samples smooth, and classically a generic determinantal cubic surface IS smooth), so 'h is a singular point' selects nothing; and the rank<=1 locus has codimension (3-1)^2 = 4 inside dimension 3, EMPTY BY COUNT. THE DIMENSION LEDGER, why B1160's recipe does not transfer: B1160 started from a 5-dim charge space where THREE LINEAR anomaly conditions did FOUR dimensions of work, leaving a LINE, and only then did the cubic cut the line to POINTS -- the cubic did ONE dimension; here the P^3 has dimension 3 and NO linear conditions at all, so one cubic leaves a surface. AND THE ABSENCE IS PROVED, NOT SUSPECTED: B1195/GC-25 established that EVERY banked symmetry acts TRIVIALLY on B_0, which is precisely why the P^3 row was typed PERMANENT-AT-CURRENT-KNOWLEDGE -- so the two results LOCK TOGETHER. THE CONCLUSION INVERTS THE PRESCRIPTION: the missing ingredient for forcing the P^3 is NOT the nonlinear condition (it exists) but THE LINEAR CUTS. WHAT CHANGES: B1204 is amended not discarded -- 'failable, nonlinear in its decisive step' is right about the LAST step and silent about the FIRST; the full recipe is LINEAR CONDITIONS TO CUT DIMENSION, THEN ONE NONLINEAR CONDITION TO CUT TO POINTS, and the program has the second half and not the first. The requirement is now exactly stated: forcing the Higgs line needs THREE independent conditions LINEAR in h (or two plus the cubic), and since no symmetry provides them they must come from a CONSISTENCY requirement linear in the closer's data -- an anomaly-type constraint on the coupling, not a symmetry of it. And GC-25's permanence result is UPGRADED IN MEANING: not merely 'the floor carries three continuous parameters' but THE PROOF THAT A B1160-STYLE FORCING CANNOT REACH THEM. FENCES: the tensor entries are not banked (the T char-0 evaluator remains commissioned to codex, R023), so the computation is over the SHAPE (a generic 3x3x4 tensor), which suffices for a codimension count -- a single equation cuts one dimension whatever its coefficients -- but does NOT rule out the object's own T being non-generic in some other useful way (its determinant vanishing identically is worth checking when T lands, given the banked SKEW ZERO and the rank-2 family law); that check is named, not run. Smoothness is sampled (5/5) plus the classical fact, not proved for the object's T. Nothing weakens V-3. Gate 5 clean.  
`B1205_the_dimension_ledger/FINDINGS.md`

**RETRACTED — `B964`** (743 words, 0 locks)  
TWO CORRECTIONS, prompted by the owner's doubt ('are we sure? i think were getting smth wrong') -- THEY WERE RIGHT. ERROR 1, a SCOPE error banked this morning in B962: 'the 27-VEV route provably stops one step short of the SM'. What is TRUE is that 27 -> 10+5+5bar+5bar+1+1 under SU(5) contains NO 24, so 27 VEVs ALONE cannot break SU(5) -> SM, and that branching stands. What was WRONG is the SCOPE: the 78 (ADJOINT) of E6 DOES contain a 24 (78 -> 45+16+16bar+1 under SO(10), and 45 -> 24+10+10bar+1 under SU(5)), so SU(5) -> SM is broken by an ADJOINT VEV, which E6 obviously has. The standard chain uses BOTH -- 78 VEVs for the rank-PRESERVING breaks (E6 -> SO(10)xU(1); SU(5) -> SM) and 27 VEVs for the rank-REDUCING one -- so 'stops one step short' is true ONLY for 27-ONLY breaking, the E6SSM's restriction, which exists because string constructions often supply only 27s; it is NOT a statement about the VEV route in general, and the E6SSM quotation leaned on was scoped to that setting and read as universal. ERROR 2, SUBSTANTIVE and repeatedly banked in B952, B959, B960 and echoed in B962: 'the object does not supply a VEV'. THIS IS FALSE. The unbroken group after an adjoint VEV <Phi> is the STABILIZER of <Phi> under the adjoint action -- that is, the CENTRALIZER of <Phi> -- SO 'ADJOINT VEV' AND 'MEASUREMENT / CENTRALIZER' ARE THE SAME OPERATION. The object DOES supply VEVs: the ADJOINT ones, and that IS the measurement cascade; B892's z(x1) and z(x1,y*) are the unbroken groups of adjoint VEVs, computed as centralizers. THE CORRECTED PICTURE: the object SUPPLIES the rank-PRESERVING (adjoint) VEVs -- this IS the measurement cascade -- and LACKS the rank-REDUCING (27) VEVs, with both needed. B952's rank obstruction is UNCHANGED and now has a physical name: adjoint VEVs preserve rank, which is exactly WHY the cascade preserves rank; the obstruction was never that the object 'has no VEV' but that it has ONLY THE RANK-PRESERVING HALF. THE REFRAMING: THE MEASUREMENT CASCADE IS AN ADJOINT HIGGS MECHANISM -- the programme has been doing GUT symmetry breaking all along without naming it that way, and what it lacks is precisely and only the rank-reducing 27-VEV half. That is sharper, physical, and makes the missing piece exactly one named object rather than a vague absence. WHAT SURVIVES OF B962: the 27 branching with no 24; the VEV DIRECTION being an input everywhere (Q2); the F4 = generic-VEV-stabilizer unification, now STRONGER since the generic 27 VEV and the adjoint VEV are both genuinely VEVs; OP^2 homogeneity including on triples, hence no canonical choice over R or C; the arithmetic exception (Kato-Yukie, cubic etale algebras), so L138 STANDS; and the certified Elkies-Gross null. WHAT IS WITHDRAWN: 'the 27-VEV route stops one step short' as a GENERAL statement, and 'the object does not supply a VEV' EVERYWHERE it appears. WHY IT WAS MISSABLE: both errors come from the same habit, TREATING 'VEV' AS MEANING '27 VEV' -- once the word is used loosely, 'the object supplies no VEV' sounds true and 'the 27 route stops short' sounds like a general no-go; the fix is terminological discipline, SAY WHICH REPRESENTATION THE VEV LIVES IN, EVERY TIME. Nothing in B959/B960's no-go is disturbed: that result is about CENTRALIZER constructions and is now visible as a statement about ADJOINT VEVS specifically, which is exactly what it always was.  
`B964_vev_correction/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

