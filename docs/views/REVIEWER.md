<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **992** |
| words of findings prose | **671,684** |
| test lock files referenced | **602** |
| arcs carrying an authored verdict | **979** (98.7 %) |
| recorded closures | **756** (589 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 647 |
| NEGATIVE | 286 |
| OPEN | 33 |
| RETRACTED | 11 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 49 |
| `genericity` | 35 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 36 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**522 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B8090`** (592 words, 0 locks)  
COLD AUDIT FINDING 1: B1076's 'new exact character' is MIS-STATED ON EVERY SUMMARY SURFACE, while its internal record has it right. FINDINGS.md, arc_verdict.json and CHANGELOG.md all bank 'sign(lambda^2) is a NEW nontrivial character of the Klein group (negative on {I, chi_a}, positive on {chi_b, D2})'. From B1076's OWN recorded values -- lambda^2(I) = -1, chi_a = -(864/413)^2, chi_b = +(6912/3047)^2, D2 = +(2304/953)^2 -- the signs are indeed (-,-,+,+), and THAT FUNCTION IS NOT A CHARACTER: a character sends the identity to +1 always, since chi(e) = chi(e)^2, so sign(lambda^2)(I) = -1 fails multiplicativity, exhaustively on all 16 products. Two errors compounded: MIS-TYPED (called a character when it is -1 at the identity, hence not a homomorphism) and INVERTED (the genuine character is sign(lambda^2)/sign(lambda^2(I)), which is +1 on {I,chi_a} and -1 on {chi_b,D2} -- the OPPOSITE polarity to the bank). WHERE THE ERROR IS NOT: b1076_results.json states it CORRECTLY -- 'trivial on the subgroup <chi_a>={I,chi_a} and equal to -1 on the coset {chi_b,D2}' -- so the computation is right, the cell got it right, and the defect is entirely in the SUMMARY layer, having drifted on the way out and propagated to three surfaces. The arc's NEGATIVE verdict and every other banked result in it (CCC = 3!lambda coset-wide, the lambda family, the denom^4 law, the 77-vacuity kill) are UNAFFECTED. The fix is exact and free: state it as the nontrivial character of B^1 with kernel {I, chi_a}. EXPLICITLY NOT A BIAS FINDING: a polarity slip in a summary is direction-neutral and does not touch the NEGATIVE verdict; recorded as a defect in the record, not a thumb on the scale. SCOPE: a claim-level check on B1076's own values using only the Klein multiplication table; recomputes none of its algebra. The from-scratch recomputation of the two new gauges, which cc asked be taken deepest, is STILL OWED and is not what this rests on. Gate 5 untouched.  
`B8090_cold_audit_f1/FINDINGS.md`

**NEGATIVE — `B8089`** (718 words, 0 locks)  
DOOR 5 IS SHUT STRUCTURALLY: THE ANOMALY LAYER OVER THE OBJECT'S DERIVED CONTENT IS IDENTICALLY ZERO, SO IT CANNOT SUPPLY A RATIO AT ALL. Owner-elected run of L144, preregistered and SHA-256 sealed before the script existed. L144 held B167's door-map stale: B167 is self-labelled POSTULATED, orphaned (no arc cited it), predates the cascade campaign, and dismissed door 5 -- the ratio lane, which it called 'the only logically-open lane' -- because its survivor was 'not the SM's gauge content', a premise now false (B862 global form, B892 algebra, B876 matter). The licence is the scale theorem's OWN exemption clause, verified verbatim: dimensionless ratios, phases, mixing and counting are outside its hypothesis (w=0), and 't Hooft anomalies are RG-invariant hence scale-free by theorem. THE GAP: every banked anomaly computation runs over the IMPORTED chiral 15 (B864, B8070); B876 DERIVES the 16 with nu^c, and the layer over the derived content had never been computed. RESULT: over the 16 every gauge channel vanishes (U(1)^3, U(1)-grav, [SU(3)]^2U(1), [SU(2)]^2U(1), [SU(3)]^3, SU(2)-Witten) AND the global B-L cubic and gravitational anomalies vanish too, while over the imported 15 they are -1 and -1; the Z6 congruence 6Y = a*triality + b*duality mod 6 has the UNIQUE solution (4,3) by exhaustion. Non-vanishing invariants over the derived content: ZERO. THE FINDING: door 5 asked the anomaly layer for a ratio, but the layer of consistent chiral content is identically zero BY CONSTRUCTION, and a layer that vanishes identically cannot supply a ratio -- the ask is structurally unanswerable, not merely unanswered, the same shape as B990's structural gap. THE SHARPER POINT: L144 hoped a nontrivial anomaly would constrain dynamics ('forbids a trivial IR'), but nu^c -- the field the object DERIVES and which completes its matter -- is exactly what cancels the last non-vanishing invariant, the global B-L anomaly. The object's matter is TOO ANOMALY-FREE to say anything; completeness of the content and emptiness of the layer are the same fact. DISPOSITION: sealed OUTCOME B -- B167 STANDS and receives the first citation it has ever had, converting an orphaned POSTULATED taxonomy into a tested one. CONTROLS: bite control (dropping e^c returns -1, -1); second route re-derives all sixteen hypercharges from su(5) alone with no SM table load-bearing; nu^c isolation (16-vs-15 differ only in B-L). SCOPE: the RATIO lane only; the scale lane B413/B563 terminal and untouched; NO value compared to any measurement; finer spin-structure-dependent discrete anomalies registered as owed. Gate 5 untouched -- nothing enters CLAIMS.md.  
`B8089_anomaly_door5/FINDINGS.md`

**RETRACTED — `B8070`** (1056 words, 0 locks)  
THIS ARC'S OWN HEADLINE IS WITHDRAWN IN FULL: it claimed anomaly cancellation carries su(3)+su(2)+u(1)^3 (Lie rank 6) to su(3)+su(2)+u(1)_Y (Lie rank 4), escaping the GUT_REQUIREMENTS_LEDGER section D rank obstruction. Four defects, all verified on this bench. (1) The headline was never computed -- the source line prints the literal f-string {2+1+3} -> {2+1+1}; no Lie rank is calculated anywhere in the file. (2) The only quantity actually computed does not move: Killing rank is 11 with one u(1) and 11 with three, so the apparent (14,11) -> (12,11) descent is len(basis), a Python list length reading back the function's own argument. (3) The algebra was not the algebra -- the three 'u(1)' generators do not commute with su(3)+su(2) (max norm of the commutator 2.83, 12.73, 86.27) and the real span is 13, not the printed 14. (4) The controls tested adjacent quantities: C1 checked centralizer DIMENSION inside a rank-4 algebra rather than rank inside e6, and C4 tested an unrelated grid scan while the detector, which takes no charge assignment, returns (12,11) unconditionally. Independently, B971 (2026-08-08) had already closed this question NEGATIVE: on the object's own complete 27 every anomaly condition is identically zero, the solution space stays 3-dimensional, and the 3 -> 1 drop exists only over an imported chiral truncation that the object has no operation to perform. THE ONE THING THIS CELL CONTRIBUTES, kept as a scope note and NOT a retraction of B864: B864's 'hypercharge is the unique gaugeable U(1)' is uniqueness INSIDE the chosen 3-plane span(Y, chi, psi) -- the coefficient a drops out identically because A(Y) = 0 is the textbook fact, and over the full 5-dimensional charge space the cubic factors into THREE lines, -2.yL.(2yL+3yd).(4yL-3yd)/3, namely hypercharge, the u-d-swapped Minahan-Ramond-Warner solution, and the vector-like direction. B864 is correct as scoped and must not be quoted as 'anomaly cancellation selects hypercharge'. Error class: a headline printed rather than computed, wrapped in controls aimed at the claim's neighbourhood; a FALSE POSITIVE this time rather than a false negative. Gate 5 untouched.  
`B8070_anomaly_rank_descent/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

