<!-- GENERATED by scripts/views/generate.py -- DO NOT EDIT BY HAND.
     Source of truth: frontier/<arc>/arc_verdict.json + kill_graph.json.
     Hand-editing a generated view reintroduces exactly the drift it prevents. -->


# The programme, for a reviewer

A generated front door. Every figure below is derived from the repository's own metadata;
none is transcribed. Where coverage is partial, this page says so rather than rounding up.

## The object

The figure-eight knot complement **m004** -- the unique arithmetic knot complement, trace
field `Q(sqrt-3)`. The programme asks what such an object forces, and reports that it forces
**form** while refusing **values**. Most results are therefore negatives, and that is the
result, not the debt.

## What is in the record

| | |
|---|---|
| research arcs with findings | **1235** |
| words of findings prose | **908,612** |
| test lock files referenced | **715** |
| arcs carrying an authored verdict | **1235** (100.0 %) |
| recorded closures | **796** (629 classified, 167 routed-only) |

**Read `COVERAGE.md` before drawing conclusions from any other view.** The verdict ledger
projects only the authored fraction; the closed-door map projects only classified closures,
and those are a lower bound on the corpus's negatives, not a census.

## Verdicts, so far

| verdict | arcs |
|---|---|
| PROVED | 812 |
| NEGATIVE | 320 |
| OPEN | 92 |
| RETRACTED | 11 |

## How the doors were shut

Closures indexed by *mechanism*, not by arc number -- the form a reviewer can actually use.

| mechanism | doors |
|---|---|
| `other` | 359 |
| `unrouted-unclassified` | 167 |
| `kind-mismatch` | 51 |
| `genericity` | 38 |
| `no-landing-site` | 30 |
| `value-numerology` | 24 |
| `method-limit` | 13 |
| `incoming-claim-refuted` | 10 |
| *(all 66 mechanisms in `CLOSED_DOORS.md`)* | |

### The quality signal a reviewer should check first

**529 closures** were classified as having had their discriminating fact *not* computed in-sandbox -- a violation of the programme's own standing rule.
**All were resolved in `frontier/B799_uncomputed_doors/`**: 2 computed here in exact
arithmetic, 5 shown to rest on locks in other arcs (each lock re-run, not trusted), and
5 relabelled honestly as uncomputed with the reason stated.

## Worked exemplars

One of each disposition, so the ledger's vocabulary can be checked against real arcs.

**PROVED — `B1406`** (1757 words, 1 locks)  
THE TWO RIDDLES ANSWERED, AND THEY ARE ONE ANSWER: THE OBJECT IS A BOUNDARY, AND WHAT FEELS MISSING IS WHAT IT IS THE BOUNDARY OF. 26/26 exact checks. RIDDLE 1, THE MIRROR'S OTHER HALF IS THE OTHER END. A punctured-torus bundle has TWO ends, with invariants phi (attracting) and phi' (repelling), Galois conjugates (B1404). The mirror does not point at a second manifold, it EXCHANGES THE TWO ENDS OF THIS ONE: A_m = R^m L^m = [[1+m^2,m],[m,1]] is SYMMETRIC, and S = [[0,-1],[1,0]] in SL(2,Z) gives S A_m S^-1 = A_m^-1 EXACTLY for every m -- inverting the monodromy is reversing the fibration, which swaps attracting and repelling. Verified m = 1..8 that S sends mu_m to its conjugate and back; at m = 1 that is phi -> phi', the GALOIS INVOLUTION sqrt5 -> -sqrt5. AND THE PROOF IS ONE LINE OF SYMMETRY: for ANY symmetric M in SL(2), S [[a,b],[b,d]] S^-1 = [[d,-b],[-b,a]] = M^-1, so every metallic bundle is its own mirror BECAUSE A_m IS SYMMETRIC -- nothing golden about it. THE CORPUS HAS BEEN NAMING THIS INVOLUTION WITHOUT JOINING THE NAMES: T-UNIQ's 'the transpose/elliptic involution', B318's 'sqrt-3 -> -sqrt-3, the nontrivial Galois automorphism', T-MIRROR's palindromic word. They are ONE map: transpose => invert monodromy => swap ends => conjugate the invariant field. A mirror with only one object is a mirror between two ENDS, and what it produces is not a second copy but a GRADING -- which is exactly what C = S^2 is. RIDDLE 2, FOUR BULKS AND THE ONE THAT MATTERS. S^3 (fill the knot back, closed and rigid); the FILLING SPACE (discrete, Z^2 per cusp); the Z-cover (Minsky's object, also rigid); and the DEFORMATION SPACE, quasi-Fuchsian QF(T_1) = Teich x Teich, one factor per end, dim_C = 2 (Bers, cited). THE OBJECT IS NOT IN IT -- IT IS ON ITS BOUNDARY, AT A CORNER. Verified: both end invariants are IRRATIONAL for m = 1..8 (m^2+4 = k^2 forces m = 0), and a rational slope would be a parabolic, a geometrically finite end still holding its Teichmueller coordinate, while an irrational one is an ending lamination -- the coordinate has LEFT the space. Both of the object's have left. So the object is doubly degenerate, holding 0 of the bulk's 2 complex parameters, AND THAT IS NOT A DEFECT OF THE DESCRIPTION, IT IS WHY THE OBJECT IS RIGID. Rigidity and bulklessness are the same fact. THE PARAMETER BUDGET, the arithmetic nobody had written down. dim_C of the SL(2,C) character variety at the discrete faithful rep is k = number of cusps (Thurston, cited); dim_C = k(n-1) for SL(n,C) (Menal-Ferrer--Porti, CITED AND NOT VERIFIED HERE). The object itself: ZERO. m004 at SL(2): 1 complex. At SL(3): 2. QF(T_1): 2. A 5-cusped cover at SL(3), or a 10-cusped cover at SL(2): 10 complex = 20 real. m004 has exactly 1 cusp (verified); cusps grow in the tower, degree 4 already giving a 2-cusped cover. AS AN ACCOUNTING FACT AND NOT A PHYSICAL CLAIM: a description that must account for ~19 numbers needs k(n-1) ~ 10 -- many cusps, high rank, or both -- and a rigid point supplies zero, which no amount of arithmetic extracted from it changes. This counts degrees of freedom; no value is compared to any measurement. THE EAR WAS NEVER FREE -- THE BULK SUPPLIES A TRACE. C is the theta-grading (C^2 = I, C = S^2, +1 on a 4-dim even sector, -1 on a 2-dim odd one), so B856's weld TRACED is the graded trace str(m) = tr(C R^m L^m) = tr_even - tr_odd. A TRACE HAS NO VECTOR ARGUMENT: THERE IS NO EAR TO ANCHOR. And it is the quantity the mirror cannot move. Verified: REAL for every m; in Q(sqrt5); MIRROR-INVARIANT, str(m) = str(15-m) for all 15; str(0) = 2 = dim_even - dim_odd, the grading's own index; exactly SIX values over the period, {-2, -1, -1/phi, +1/phi, +1, +2}, all of them EXACTLY 2cos(j pi/15) with j in {0,5,6,9,10,15} -- arguments running over the divisors of the period rather than 15 itself; among units the split is by 2-TORSION, str = +1/phi exactly when m^2 = 1 mod 15; and AT THE OBJECT'S OWN WORD str(1) = 1/phi = phi - 1 EXACTLY. THE ANCHOR LEDGER: B1349's matrix element Re h(c) costs 2 bits of ear and 2.81 of word for 4 outputs; the graded trace costs 0 and 0 for 1 output, total +1. The word is free because m = 1 is what five independent principles select (B1405) -- and B1405's finding is what pays for it, since the principles all land on m = 1, which is exactly what killed branch A, because branch A needed a DIFFERENT word. The same fact that closed the licensed row opens this one. AND THE FEELING WAS RIGHT, MEASURED: tr_even(R^m L^m) = 0 EXACTLY unless 3 | m, and 3 | m is B996's degeneracy condition (B1402). So on TEN of the fifteen words the theta-even sector's graded trace vanishes identically -- the object's own m = 1 among them: tr_even(1) = 0, tr_odd(1) = -1/phi, str(1) = +1/phi. THE LAST LICENSED ROW LIVES ON THE EVEN SECTOR, AND AT THE OBJECT'S OWN WORD THAT SECTOR'S GRADED CONTENT IS EXACTLY ZERO, ALL OF IT SITTING IN THE ODD SECTOR -- the smaller half, the one B856 worked on and the campaign left. That is the precise version of 'we are computing part of something': not a metaphor, a measured zero. FOUR SENSES IN WHICH THE OBJECT IS HALF OF SOMETHING, three structural and one measured: a COMPLEMENT (bulk S^3, room only in the discrete filling space); a BOUNDARY POINT (0 of QF's 2 moduli, which IS its rigidity); an OPERATOR WITHOUT ITS STATE (matrix elements read where the bulk supplies a trace, which is why the ear could never be anchored); and ONE SECTOR OF A GRADED PAIR (the licensed one tracing to zero at the object's word). The honest reading of the gap: the missing structure is not a cleverer invariant of the object, because a rigid point has no parameters to give, by theorem. Room exists in exactly three places -- the TOWER, the RANK, and the FILLING SPACE -- and the programme has touched all three and budgeted none. E75 INSTANCE #12, THIRD ARC RUNNING: the law tr_even = 0 iff 3 does not divide m first came back FALSE, the values right and the TEST wrong -- it compared the nsimplify'd PRINTED form against 0 with == instead of testing simplify(raw) == 0. Identify by property, never by representation. The class is no longer about floats in any sense; it is about testing a thing by how it is written. FENCES: Bers, Minsky, Thurston's dimension count and Menal-Ferrer--Porti's SL(n) count are CITED, not derived, and the k(n-1) row is explicitly unverified. The '~19' is a count of degrees of freedom, not a comparison to a measured value. str(1) = 1/phi is an exact fact about SU(3)_2 modular data at the object's word; WHAT IT IS AN OBSERVABLE OF IS NOT CLAIMED. The closed form of m -> j is OBSERVED, not derived (L215). Nothing reaches CLAIMS.md, F2 or Gate 5; B1349, B1405, B675, B996 and B1402 are untouched.  
`B1406_the_mirror_and_the_bulk/FINDINGS.md`

**NEGATIVE — `B1349`** (7541 words, 0 locks)  
THE MIRROR SECTOR POSED: THE RESIDUAL FREEDOM IS 48, AND R11'S ARITHMETIC SAYS DO NOT SPEND THE LAST ROW. Of KIND_TABLE's four coupling rows, tones, probability and phases are all CONSUMED with decisive MISSes and the MIRROR SET (theta-even) is the last licensed row, delivered by B1011 C6 and never drawn on by any sealed comparison; the one-shot rule consumes CONTACT ROWS, so it is spent whatever the outcome. B1348 posed the theta-ODD plane, which is NOT the crossing target; this poses the right one. CONTROLS: the canonical rational basis from the weights (e(0,0), e(1,1), e(0,1)+e(1,0), e(0,2)+e(2,0)) satisfies C v = +v; <R,L> restricts exactly; the PROJECTIVE IMAGE IS 720 = 12 x 60 = A_4 x A_5, as forced since (-1,-1) and (-1,1) act as scalars on V_2 (x) V_2; C^4_even is IRREDUCIBLE; and a random direction has |Stab| = 1 with orbit 720. THE STABILISER SPECTRUM, every orbit size dividing 720 as orbit-stabiliser requires: |Stab| = 15, 10, 9, 6, 5, 3, 2 giving orbits 48, 72, 80, 120, 144, 240, 360. MAXIMAL |Stab| = 15, SO THE SMALLEST DISTINGUISHED ORBIT HAS 48 ELEMENTS -- four times the odd plane's 12. The structure is confirmed rather than assumed: 15 = 3 x 5 = (tetrahedral vertex stabiliser) x (icosahedral vertex stabiliser), and the maximal-stabiliser direction is RANK 1, singular values [1,0], a PRODUCT STATE v (x) w. METHOD CORRECTION recorded not hidden: a first pass orbit-decomposed by rounding projective coordinates and produced sizes that DO NOT DIVIDE 720 (68, 370, 423...), impossible by orbit-stabiliser; that enumeration is WITHDRAWN and the table is derived from stabiliser orders alone with |orbit| = 720/|Stab| -- the same float-drift failure as B1348's withdrawn step 4, caught by an invariant the answer must satisfy. R11'S ARITHMETIC, THE POINT OF THE ARC: on branch A (the row needs one direction) the anchor cost is a 48-FOLD DISCRETE SELECTION, log2 48 ~ 5.58 bits; on branch B (the row uses the whole orbit) there is no selection but the prediction becomes a 48-ELEMENT SET, a 48-fold look-elsewhere on the comparison side; and the mirror row is ONE row, so ON EITHER BRANCH OUTPUTS MINUS ANCHORS IS NEGATIVE AND R11 DECLARES THE CELL VACUOUS BEFORE THE SEAL. RECOMMENDATION, STATED AS A RESULT: DO NOT SPEND THE LAST ROW -- not now, and on this arithmetic not through this row at all, unless the mirror set's value set is shown to carry MORE THAN ONE independent output, in which case the outputs are recounted and R11 re-evaluated. Three of four rows are spent with decisive misses and the fourth is unrepeatable, so a gating computation that says NO is worth more than a fourth miss and costs less. WHAT WOULD CHANGE IT: count the mirror set's independent outputs (k > 5.6 bits closes the ledger); cut the 48 (the Galois question at the point level is OPEN, B1348's withdrawal having left it so); or add a new licensed row under R5 in advance, derived not fitted since F1 forbids fitting. FENCES: F2 untouched, no comparison performed, no value read, I-13 still UNEARNED, R4/R5/R10 left as banked. The exactification debt B1348 registered applies to both arcs. NUMBERING: this takes B1349, the last id before the reserved range B1350-B1399 (E71); the next new arc on this branch needs a range grant. ADDENDUM (same branch, NOT a new arc -- B1350-B1399 is reserved, so the work discharging B1349's own debt is banked here). (1) THE EXACTIFICATION OWED BY B1348 AND B1349 IS PAID: the instrument is rebuilt in exact Q(zeta_60) = Q[z]/Phi_60, every exponent landing in (1/15)Z so every entry is a Z-combination of zeta_60 powers (T = diag(z15^-2, z15^2, z15^8, z15^2, z15^7, z15^8); S = -i/(5 sqrt3) sum_w sgn(w) z15^{-3<w(l+rho),m+rho>} with i = zeta_60^15 and sqrt3 = zeta_60^5 + zeta_60^-5), and every control passes with NO FLOAT ANYWHERE: S Sdag = I, S = S^T, C = S^2 has entries in {0,1} and IS the charge conjugation (a,b) -> (b,a), and (ST)^3 = S^2. The integers both arcs' verdicts rest on are unchanged. (2) GALOIS DOES CUT, AND MEASURABLY: a direction with RATIONAL coordinates in the weight basis is fixed by every sigma in Gal(Q(zeta_60)/Q) automatically, so the question becomes how many distinguished directions are rational -- and that number, not the orbit size, is what R7 prices. theta-ODD: 12 -> 2 RATIONAL ([1:0] and [0:1], i.e. f1 and f2), cost log2 2 = 1 bit. theta-EVEN (the crossing target): 8 RATIONAL of the 96 maximal-stabiliser directions, cost log2 8 = 3 BITS, down from 5.58. The eight are the four canonical weight basis vectors plus four more with entries in {0, 1, -1/2}. STRUCTURE: the 96 are TWO orbits of 48, forced because A_4 has TWO classes of stabiliser-3 points (vertices and faces, four each), 2 x (4 x 12) = 96; the 4/4 split of the rationals between them is INDICATED, NOT ESTABLISHED. (3) R11 RECOMPUTED: anchor cost falls from 5.58 bits to 3 bits while outputs stay at 1, so outputs - anchors moves from -4.6 to -2. STILL SHORT, BUT THE RECOMMENDATION CHANGES SHAPE -- from 'not through this row at all' to 'ONE MORE OUTPUT THAN THREE AND IT CLOSES': if the mirror set's exact value set carries at least 4 independent numbers the ledger balances and the arc becomes worth its one shot. That is a concrete checkable follow-up where before there was none. (4) TWO ERRORS CAUGHT AND RECORDED: the rationality test first ACCEPTED 1/phi, because with tol 1e-8 and denominators to 10^4 the Fibonacci ratio 4181/6765 approximates it to ~1e-8 -- in an instrument whose value set is the golden nine that is the worst possible false positive; fixed to tol 1e-11 and denominators to 1000, WITH A CONTROL asserting the test rejects 1/phi and accepts -1/2, and the reported 14 became 8. And the orbit enumeration drifted a THIRD time, reporting sizes [48, 68] with 68 not dividing 720; the 48 and the two-orbit structure rest on orbit-stabiliser and on A_4's two stabiliser-3 classes, and the enumeration is used for no banked number. (5) THE RECOMMENDATION NOW: still do not spend the row yet, but the reason has moved from structural to arithmetic by a measurable margin, and the next step is no longer 'cut the 48' -- that is done, 48 -> 8 -- it is COUNT HOW MANY INDEPENDENT NUMBERS THE theta-EVEN MIRROR SET CARRIES. Four suffices. ADDENDUM 2 (same branch, same id -- B1350-B1399 reserved): THE OUTPUTS ARE COUNTED AND THE ROW SPLITS IN TWO. THE LAW: on the theta-EVEN sector Re h is EAR-INDEPENDENT exactly when gcd(m,15) > 1, ear-DEPENDENT exactly on the 8 UNITS of Z/15 (phi(15) = 8); the verification is COMPLETE not inductive, since ord(R) = ord(L) = 15 so m = 1..15 is the entire family, and there are ZERO violations. Reading: the listener's direction matters precisely when R^m L^m still generates the full cyclic group. THE CRITERION IS ALGEBRAIC NOT STATISTICAL -- for a real ear v = Bc, Re h = c^T Sym(Re A) c / (c^T G c) with A = B^T (C R^m L^m) B, so ear-independence is the exact identity Sym(Re A) = lambda G, decided in Q(zeta_60) with no sampling and no tolerance; this retires the float-drift class that bit B1348 step 4 and B1349 twice. B856 IS REPRODUCED FROM AN INSTRUMENT SHARING NO CODE WITH IT AND STRENGTHENED: h(5) = -1 exactly, |h|^2 in {1/(phi sqrt5), phi/sqrt5, 1} with the golden pair summing to 1, Re h(1) = 1/(2phi); B856 reports ear-independence at m=1 numerically (2e-16) and here it is an EXACT identity for every m = 1..15; h_odd(5) = -1 with C = -1 on odd means R^5 L^5|odd = +I, which IS B856's period-5 collapse, and m=15 on the even side gives lambda = 1 exactly since weld(15) = C = +1 there. EXACT EXTRA: tr(G^-1 Q_m) = 0 for ALL 8 units, so the ear-dependent readings are TRACELESS -- no mean, only spread. THE FIELD STRUCTURE: the four ear-independent values {-1/(2phi), 0, 1/2, 1} all lie in Q(sqrt5) inside Q(zeta_60), the field of the modular data itself, while EVERY ear-dependent eigenvalue is (1/(2 sqrt2)) x a unit of Z[phi] -- (1/(2sqrt2)){+-phi^2, +-1/phi} at m in {1,4,11,14} and (1/(2sqrt2)){+-sqrt5, +-1} at m in {2,7,8,13}, all eight PROVED from exact characteristic polynomials (radsimp(got-claim) = 0), never float-matched, because the b1349c trap had tol 1e-8 accept 4181/6765 as 1/phi. And sqrt2 IS NOT IN Q(zeta_60), proved twice: Phi_60 stays irreducible over Q(sqrt2) (factor degrees [16], compositum degree 32 != 16) and by Kronecker-Weber the conductor of Q(sqrt2) is 8, which does not divide 60; POSITIVE CONTROL, Phi_60 over Q(sqrt5) and over Q(-1/(2phi)) factors as [8,8] so those ARE inside. SELECTING AN EAR THEREFORE COSTS A FIELD EXTENSION THE MODULAR DATA DOES NOT CONTAIN -- addendum 1's Galois cut restated on the value side. THE COUNT, over R: dim_R span{Q_m} in Sym_4(R) is 3 over the whole period, 2 on the units, 1 on the non-units (all proportional to G); G lies in the span of all 15 but not of the units; either way there are 2 EAR-DISCRIMINATING DIRECTIONS. R11: BRANCH A (gcd(m,15) > 1, 7 words) consumes NO ear-anchor because no direction is selected, and yields 4 forced values, so outputs - anchors = +4, or +1 counting only -1/(2phi) (the lone value outside Q), or +1 even if ear-independence is ruled NOT to discharge the anchor -- IT CLOSES ON THREE OF FOUR READINGS, failing only if the conservative count and the undischarged anchor are combined. BRANCH B (gcd(m,15) = 1, 8 words) gives 2 outputs against 3 bits = -1 and DOES NOT CLOSE. THE FIRST POSITIVE INSTRUCTION THIS ARC HAS PRODUCED: if the row is ever spent, spend it on a word with gcd(m,15) > 1, where no ear is selected at all. THREE ERRORS RECORDED, NONE HIDDEN. (1) b1349d IS WITHDRAWN AS AN INSTRUMENT: it used R^m L^m unwelded and compared COMPLEX h across ears, where B856/B641 grade C R^m L^m and Re h over REAL ears. WHICH deviation mattered was MEASURED not assumed: dropping C only NEGATES lambda and Re h stays scalar, so the actual bug was complex-vs-real -- at m=1 on the odd sector spread|h| = 8.5e-01 while spread Re h = 1.1e-16. The missing C did one visible thing, flipping h(5) from -1 to +1. The control did its job: it refused to let the even number be read. E58 applied to my OWN paraphrase, not only to a relay. (2) THE PRE-REGISTRATION'S DECISION RULE WAS MIS-DERIVED: b1349d pre-registered 'ear-independent => outputs 0, R11 can NEVER close', importing AC4's bench demotion into R11, which prices CONSUMED anchors -- if the reading is direction-blind no direction is selected and the anchor is not consumed, so ear-independence HELPS the ledger. Backwards, and recorded rather than silently reversed. (3) A COUNTING ERROR OF MY OWN, caught by the test written against this addendum: a first pass reported dim span = 6 by taking the rank OVER Q of power-basis coefficient vectors, which measures Q-independence of field elements and inflates by the field degree; the honest quantity is dim_R span{Q_m} = 3, and the correction REVERSES branch B from +2 to -1. The 6 is withdrawn from both the findings and the script. A first draft also claimed the closure did not depend on the disputed anchor reading; that rested on the withdrawn 6 and is retracted. WHAT THIS DOES NOT LICENSE: R11 is an INPUT-ACCOUNTING gate, not a physics gate -- closing it says the cell is not vacuous under MB12 and says nothing about whether the row is right. B856 is the precedent ON THIS VERY QUANTITY: its |h|^2 reading was REFUTED ON KIND and the surviving Re h = 1/(2phi) sat in a 1-sigma window holding at least 17 natural candidates. Branch A's cheapest output is -1/(2phi), whose modulus is exactly the number the programme already failed to cash -- the honest reason not to read this as good news about physics. FENCES: F2 untouched, no comparison performed, no value read, nothing reaches CLAIMS.md, Gate 5 untouched, I-13 still UNEARNED (its price now known and affordable on one branch of two), verdict stays NEGATIVE. WHAT NOW GATES THE ROW IS KIND-CORRECTNESS, NOT ARITHMETIC. DUPLICATE CHECK RUN BEFORE CALLING THE LAW NEW (already_banked.py, two queries): no prior arc banks it; top hits are B1349 itself and B1011, the INPUT, already in depends_on. NEIGHBOURHOOD NOT DUPLICATION: B996 and B997 compute over the SAME words R^m L^m and also discriminate within that family by a number-theoretic condition on m, but neither mentions 15, gcd or coprimality (checked, not assumed) and their modulus is the SL(2,Z/N) shadow's conductor while this one is 15 = ord(R) = ord(L) in the SU(3)_2 modular data. Three arcs now cut the metallic family by arithmetic in m; whether the cuts are related is OPEN and not investigated here. ADDENDUM 3: THE 4/4 SPLIT IS ESTABLISHED AND BRANCH B TIES AT ZERO. Addendum 1 left the split of the 8 rational directions between the two orbits of 48 explicitly INDICATED NOT ESTABLISHED; it is now exact. Computed in Q(zeta_60) with projective canonicalisation by exact field inversion, so two directions compare equal iff they ARE equal, and with |orbit| divides 720 ASSERTED for all eight -- the invariant that caught three earlier float drifts (68, 370, 423). ORBIT 1 (size 48, |Stab| 15): e1, e2, (1,0,0,1), (0,1,1,0). ORBIT 2 (size 48, |Stab| 15): e3, e4, (1,0,0,-1/2), (0,1,-1/2,0). SPLIT = [4,4] over exactly two orbits. Control run first: R and L restrict to the theta-even sector EXACTLY (M6.Bev = Bev.M4, symbolic), so the 4x4 action is the 6x6 one. This also upgrades addendum 1's two-orbit structure from ARGUED (A_4's two stabiliser-3 classes) to VERIFIED by direct exact enumeration; scope, what is established is that the EIGHT RATIONALS occupy exactly two orbits of 48 four each, while the whole maximal-stabiliser set being 96 = 2 x 48 still rests on addendum 1's A_4 argument. THE CONSEQUENCE: confined to ONE orbit -- which it is whenever the orbit is GENERATED from a starting point rather than chosen -- the anchor is log2(4) = 2 BITS, not 3. So BRANCH B READS 2 - 2 = 0 AND FAILS BY A TIE, R11 requiring strictly > 0: a sharper negative than addendum 2's -1. One more ear-discriminating direction would close it AND THERE IS PROVABLY NO THIRD, since dim_R span{Q_m : m a unit} = 2 exactly. Branch B does not fail for want of effort; it fails by one dimension the sector does not contain. Branch A now closes on four of six readings instead of three of four, the two failures being the same combination as before (conservative output count TOGETHER WITH ruling that ear-independence does not discharge the anchor). Verdict stays NEGATIVE, I-13 stays UNEARNED, and addendum 2's closing judgement stands verbatim: WHAT GATES THE ROW NOW IS KIND-CORRECTNESS, NOT ARITHMETIC. ADDENDUM 4, THE KIND GATE'S FIRST STEP: BRANCH B IS EXCLUDED ON KIND TOO, INDEPENDENTLY OF ITS TIE. Addendum 2 said what gates the row now is kind-correctness; the first kind question needs no data at all -- does the reading land in the value set the corpus ALREADY BANKED for this sector? B1011 C6 banks the theta-even value set {0, +-1/4, +-1/(4phi), +-1/2, +-1/(2phi), +-phi/4, +-phi/2, +-1}, 15 values, carried by KIND_TABLE as the mirror set with declared kind amplitude-part, bounds [-1,1], field Q(sqrt5). NOT CLAIMED (E72): that C6's quantity and this one are identical -- C6 is a census over group elements of Re(zeta^-1 u-bar M u), these are the 15 metallic words with the weld. Only CONTAINMENT is tested, which is all the kind question needs, with a two-sided control that accepts the banked values and rejects near-misses at 0.30, 0.31 and 1/(2phi)+1e-9. RESULT: branch A's four forced values -1/(2phi), 0, 1/2, 1 are ALL FOUR ELEMENTS of the banked set; NONE of branch B's four ear-dependent extremes is (phi^2/(2sqrt2) = 0.9256 nearest banked 1, off 0.0744; 1/(phi.2sqrt2) = 0.2185 nearest 1/4, off 0.0315; sqrt5/(2sqrt2) = 0.7906 nearest phi/2 = 0.8090, off 0.0184; 1/(2sqrt2) = 0.3536 nearest 1/(2phi), off 0.0445). AND IT IS THE FIELD, NOT AN ACCIDENT: branch A lies in Q(sqrt5), the row's declared field, while every branch-B reading carries 1/sqrt2 and generates Q(sqrt2,sqrt5), and sqrt2 is not in Q(zeta_60) hence a fortiori not in Q(sqrt5) -- a branch-B reading CANNOT be an element of a set declared over Q(sqrt5), as field arithmetic and not as an approximation. SO TWO INDEPENDENT GATES NOW REJECT BRANCH B, neither depending on the other: the R11 arithmetic ties at 2 - 2 = 0, and the values are not of the row's kind. The near-misses make it sharp: 0.7906 against a banked 0.8090 is exactly the shape of a reading a fitter would call a near-hit and that is structurally not a member. BRANCH A EARNS KIND-ELIGIBLE, WHICH IS NOT KIND-CORRECT: right field, right bounds, an actual element of the banked set -- but whether the row is the right physical observable is the question B856 LOST on its own sector, and nothing here touches it; branch A's cheapest output is still -1/(2phi), whose modulus is the number that lost. A THIRD INDEPENDENT SIGNATURE OF THE SAME SPLIT (the second implying the third): ear-dependence; inside/outside Q(sqrt5) in Q(zeta_60); inside/outside C6's banked set. The programme's own banked law and this arc's computation AGREE EXACTLY ON BRANCH A AND DISAGREE EXACTLY ON BRANCH B, which is the strongest consistency check available here because C6 was banked before the question was asked. Verdict NEGATIVE, I-13 UNEARNED, F2 untouched, no comparison, no value read, Gate 5 untouched. THE SHAPE IS NOW COMPLETE: branch B dead on two independent gates; branch A arithmetically clear and kind-eligible with outputs matching a law banked before the question; what remains for branch A is the one thing this bench cannot settle by computation -- whether the mirror row is the right observable at all. AND AN OBJECTION TO ALL OF IT, RAISED AGAINST MYSELF AND CLOSED BY AN IDENTITY: B641 does not grade Re(A), it grades Re(A/zeta) with zeta = sqrt(det M|sector), and addenda 2-4 dropped the /zeta. Since |zeta| = 1, A/zeta = A conj(zeta), so the form actually graded is Sym(Re(conj(zeta) B)) and NOT Sym(Re B) -- a phase rotation, which changes a real part, so the gcd law, the field statement and the C6 containment could ALL have been artifacts of a missing normalisation. THEY ARE NOT, because zeta = 1 on every metallic word: det(R^m L^m) = det(T)^m det(S^-1 T^-1 S)^m = det(T)^m det(T)^-m = 1 for every m and on every invariant subspace since restrictions multiply the same way -- exactly in Q(zeta_60), det(T) = -z^10 and det(L) = z^10 - 1 with det(T).det(L) = 1 mod Phi_60 -- while det(C|even) = det(I_4) = 1 and det(C|odd) = det(-I_2) = +1, so det((C R^m L^m)|sector) = 1, verified EXACTLY for all 15 words on BOTH sectors, whence zeta = 1 and Sym(Re(conj(zeta) B)) = Sym(Re B). B641's twist is the IDENTITY on the metallic words, and it is worth saying exactly where det = 1 is automatic and where it is not, because a first pass asserted the wrong half: on the THETA-EVEN sector det(R|even) = det(L|even) = 1 INDIVIDUALLY, so zeta = 1 there for EVERY word in <R,L> and the even sector never needed the twist; on the THETA-ODD sector det(R|odd) = exp(-2 pi i/3) and det(L|odd) = exp(+2 pi i/3) are CONJUGATE PRIMITIVE CUBE ROOTS OF UNITY, and since det is a homomorphism its image on the odd sector is <omega> = Z/3, exactly three values, so det = 1 holds there ONLY for words with equally many R's and L's -- which R^m L^m is by construction. THAT is precisely why B641 needed the twist: ranging over all 360 group elements it meets all three odd-sector determinants, so its tone is only well defined after dividing by zeta. On the 15 metallic words the twist is vacuous on both sectors -- on the even one trivially, on the odd one because the word pairs each R with an L. So the quantity addenda 2-4 computed IS B641's quantity here. Cross-checked the other way as well: recomputing the entire table WITH the /zeta reproduces the identical ear-independent set {3,5,6,9,10,12,15}, identical lambdas and identical spectra, and the odd sector stays ear-independent for every m under both conventions. RECORDED rather than quietly checked because this is exactly the E72 shape -- one name, the tone, two quantities -- and it was a live route by which all four addenda could have been measuring the wrong thing; it is closed by PROVING the two quantities coincide on this domain, which is stronger than observing that the answers matched. ADDENDUM 5 WEAKENS ADDENDA 2-4's LEDGER, and is written because the omission favoured the programme. THE STRUCTURAL FACT, WHICH CUTS AGAINST US: the metallic words carry SL(2,Z) trace m^2+2, so m=1 is trace 3, THE GOLDEN -- and B997 proves the golden is the UNIQUE metallic grammar whose own-conductor shadow is a McKay group. But gcd(1,15) = 1, so the golden word is a UNIT of Z/15: ear-dependent, outside Q(sqrt5), outside C6's banked set -- IT IS ON BRANCH B, DEAD ON TWO GATES. So is silver (m=2); the first live word is bronze (m=3). THE MIRROR ROW'S ARITHMETIC CLOSES ONLY ON WORDS THAT ARE NOT THE OBJECT'S OWN, and the single branch-A value that leaves Q, -1/(2phi), occurs only at m in {6,9}. THE ANCHOR MY TABLES OMITTED: addenda 2-4 priced the EAR and silently treated the WORD as given, which is inconsistent, and the inconsistency runs in my favour -- the word cannot be given as m=1 because m=1 is dead, so using branch A REQUIRES DEPARTING FROM THE OBJECT'S GRAMMAR and that departure is an input, log2(7) = 2.81 bits to name a non-unit. Two coherent readings: (i) the row predicts the SET, B1011 C6's own framing, nothing selected and no anchor, which closes trivially but makes the prediction a 15-fold LOOK-ELSEWHERE on the comparison side -- precisely the objection addendum 1 raised against its own branch B, so consistency requires rejecting it here for the same reason; (ii) the row predicts ONE number at a specified word, and then the word is an anchor. Under (ii): 4 outputs - 2.81 = +1.19 closes; 4 - (2 + 2.81) = -0.81 fails; 1 - 2.81 = -1.81 fails; 1 - 4.81 = -3.81 fails. SO WITH THE WORD ANCHOR COUNTED BRANCH A CLOSES ON ONE READING OF FOUR, NOT FOUR OF SIX. THE 'FOUR OF SIX' COUNT IS WITHDRAWN. The mathematics of addenda 2-4 is untouched -- the gcd(m,15) law, the exact spectra, the field statement, the C6 containment and the 4/4 split all stand; what is withdrawn is the ACCOUNTING, which omitted an anchor. THIS STRENGTHENS THE RECOMMENDATION: branch B (the object's own word among them) is dead on two independent gates; branch A survives on one reading only and asks the programme to read its coupling off a grammar the object does not single out, against its own theorem that singles out the golden UNIQUELY; and branch A's cheapest output is still the modulus B856 failed to cash. DO NOT SPEND THE LAST LICENSED ROW -- not because the arithmetic is short by a hair but because the only branch on which it is not short is not the object's. Recorded as ERROR_LEDGER E76, the UNPRICED-SETTING class: a ledger prices the quantity the question is ABOUT and leaves the question's SETTING unpriced, invisible from inside the arc and systematically favourable because the setting was chosen to make the question askable. ADDENDUM 6 -- THE CUTS ARE RELATED: ONE PRIME, TWO ROLES. Addendum 2 left open whether the three arithmetic cuts of the metallic family are related; they are. NAMING FIRST, because B1002 records a LIVE TERMINOLOGY COLLISION -- 'conductor' is TWO quantities in adjacent laws: B675's CUSP-ORDER conductor (golden 4, silver 2) and the word's own SHADOW MODULUS (golden 5, silver 8, = m^2+4, B666 and B997's quantity, which B1002 confirms correct as the shadow modulus). EVERYTHING HERE USES THE SHADOW MODULUS, named before computing, the E72 discipline applied in advance. THE CORRESPONDENCE: on the ear-independent branch the forced value lambda LEAVES Q EXACTLY WHEN 5 | (m^2+4), with ZERO mismatches over the complete period -- m=6 (modulus 40) and m=9 (85) give -1/(2phi), while m=3 (13), 5 (29), 10 (104), 12 (148), 15 (229) give the rationals 1/2, 0, 0, 1/2, 1. Both sides depend only on m mod 5, hence on m mod 15, so the period-15 check is COMPLETE and not a sample; and the reason is elementary, m^2 = 1 mod 5 for m not divisible by 5, so 5 | m^2+4 iff m = +-1 mod 5, verified for m = 1..199. ONE PRIME, TWO ROLES: B997 uses the PRIMALITY of m^2+4 -- 5 is prime so the golden's shadow IS SL(2,Z/5) = 2I, E8's McKay group, and since |SL(2,Z/N)| is a McKay order for exactly N in {3,4,5} and m^2+4 = 5 only at m=1, the golden is UNIQUE; this arc uses the same prime's DIVISIBILITY, which decides whether the theta-even forced value is golden or rational. RELATED, AND NOT THE SAME CUT. B997'S UNIQUENESS IS UNTOUCHED, AND THE CHECK MATTERS: m=1 satisfies 5 | m^2+4 but gcd(1,15) = 1 puts it on the ear-DEPENDENT branch where the readings are traceless and there is no forced value at all, while the forced golden value occurs only at m = 6, 9 whose moduli 40 and 85 are divisible by 5 but NOT PRIME, so neither competes for B997's property. WHAT IT IS: an ENRICHMENT, not a new claim on the world -- two arcs asked independent questions and their answers are governed by the same prime in two different ways, exact over the whole period and resting on a one-line congruence, which is the shape of a structure found rather than fitted. WHAT IT IS NOT: a rescue of the row. Addendum 5 stands: if anything this SHARPENS its discomfort, since the forced golden value exists only at m = 6, 9, which are neither the golden word nor any named metallic grammar -- THE READOUT GOES GOLDEN EXACTLY WHERE THE GRAMMAR DOES NOT. STILL OPEN AND NOT GUESSED AT: B996's cut against either of these, and whether B1002's other gcd -- gcd(cusp-order conductor, shadow modulus), 1 for golden (isomorphism) and 2 for silver (ramified) -- relates to gcd(m,15).  
`B1349_the_mirror_sector_posed/FINDINGS.md`

**RETRACTED — `B1181`** (446 words, 0 locks)  
RETRACTED 2026-09-02 (B1235): THE FAMILY IS 38/112 AMPHICHIRAL, NOT 83/83 -- the method was orientation-blind. ORIGINAL CLAIM AS ASSERTED: THE AMPHICHIRALITY DEBT CLOSED (cc3 a0a349ef, harvested same-day as the B8147 retraction it completes). The one residue B1180 flagged -- family-wide amphichirality at the corrected >=83 family, UNCHECKED -- is now CHECKED BY cc3: 83 OF 83 AMPHICHIRAL, zero exceptions, zero undecided; SPOT-VERIFIED on this bench 5/5 by the reliable mirror-isometry method (m004, s955, o10_150700 the H1-killer, o10_150684 a cusp-shape carrier, t12840) -- deliberately NOT the isometry_signature route (the B1163-era vacuity trap). cc3's typing adopted: 'the claim was right in SUBSTANCE and wrong only in its COUNT -- the opposite failure mode from the separators, where the substance died with the count.' CONSEQUENCE: B1163's family-wide W0 obstruction upgrades verified-4-of-14 -> 83-OF-83; there is no sibling among 82 that supplies what m004 withholds -- the no-sibling-escape conclusion is far more robust than either seat had it (the closure line appended to B1163's B8147 addendum; B1180's FINDINGS written with the closure folded in). THE INSTRUMENT NOTE REGISTERED AS A sec-G METHOD-LAW ROW (LAW_MAP: THE ONE-WAY FAMILY TEST; method-laws live in sec-G, not the theorem registry, hence creates_law=false): enlarging an enumerated family can only HURT object-level claims (more members can share the property -- how both separators died) and only HELP family-level claims (more members exhibit it -- how amphichirality strengthened); 'the retraction and the confirmation are the same computation pointed in opposite directions'; corollary discipline: A FAMILY IS A CLAIM, NEVER A SETTING. Residue: the family's membership-criterion definition (all-regular vs trace-field, already relayed to cc3). Gate 5 clean.  
`B1181_amphichirality_closure/FINDINGS.md`

## How to falsify any of this

1. Every closure names an **escape hatch** -- the computation that would reopen it. Pick a
   door in `CLOSED_DOORS.md`, run its hatch; the door holds or it does not.
2. Every PROVED arc names its **test locks**. Run them.
3. The gates (`python3 scripts/gates/gates.py`) enforce the governance invariants, and each
   was verified by deliberate breakage -- a gate never observed to fail is not a gate.

You do not need to read the record to check the programme. That is the point of this page.

