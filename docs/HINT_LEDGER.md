# Hint Ledger

The **generative counterpart** to `../papers/VALIDATION_LEDGER.md`. It exists because the repo had a structural
gap: the only place to record a research idea was `OPEN_LEADS.md`, which admits **decisions to run** — so a hint
had no legal place to exist until it was already strong enough to be an actionable lead. Hints died young.

**Admission bar (deliberately low):** record *anything noticed* — a pattern, a coincidence, an anomaly, a cross-
finding rhyme, an unasked question. **Nothing here is a claim.** This is **not** a decision-to-run ledger.
Recording a hint is not asserting it (`../GOVERNANCE.md §6.2`, the staging principle). The **only judged step** is
**promotion to `OPEN_LEADS.md`** (a decision to run) or to a candidate claim (the verification gate). Governed by
`../METHOD.md`. Firewall (`../ARCHITECTURE.md`): hints may cite math (B/V/P); math never cites hints.

**Lifecycle** (resist adding states): `NOTICED → CHECKED → PROMOTED` | `→ DORMANT` (cheap check inconclusive —
parked, revivable) | `→ KILLED` (**only** via the full promotion gate → `../speculations/TOMBSTONES.md` with a
`residual-hint:` field). The heavy adversarial battery is **forbidden before promotion** (anti-blindness rule 2):
a cheap CHECK may only set a diagnostic flag (`generic?`/`vacuous?`/`non-reproducing?`) and route DORMANT — it may
**not** KILL.

`type ∈ {MATH, HOOK, ANOMALY, PATTERN, QUESTION}`.

## Live hints

| H# | hint | type | noticed | state | flags | disposition |
|---|---|---|---|---|---|---|
| H1 | `\|H₁\| = (2m²+7)²+2 ⇒ \|H₁\| ≡ 3 (mod 4)` for the metallic Seifert duals | PATTERN | 2026-06-27 | NOTICED | — | from B227; see H16 |
| H2 | the recurring **"5" web** — golden period 5; `n=m²+4=5`; `M(4,5)=SM(3,5)`; `40a1` conductor `2³·5`; the golden discriminant 5 shows up on every face | PATTERN | 2026-06-27 | NOTICED | — | see H17 |
| H3 | `content = m = congruence-modulus = gcd(a,b)` — one invariant, four names | MATH | 2026-06-27 | PROMOTED | — | banked B221 |
| H4 | the **hyperbolicity-split motif** — the object recurs on both sides of a hyperbolic/non-hyperbolic divide as *different* manifolds (B217 period/volume; B226 two-SUSY; B229 two-bulks) | PATTERN | 2026-06-27 | NOTICED | — | see H13 |
| H5 | the **determinant = SU(2) level** reading: ordinary `PS−QR=1` (SU(2)₁), super `PS−QR=2` (SU(2)₂) | MATH | 2026-06-27 | PROMOTED | — | banked B229 |
| H6 | the **silver FM `c=5/4`** coincidence: `c=c(SM(6))` but a different coset ⇒ "c-coincidence ≠ CFT identity" | ANOMALY | 2026-06-27 | PROMOTED | — | banked B230 (reinforces the coset criterion) |
| H7 | `E₈+E₆−E₇ → Monster+Fischer−BabyMonster` — the object's dual McKay selects two of McKay's three sporadic-seeding diagrams, excludes the Baby Monster | HOOK | 2026-06-27 | PROMOTED | — | → L46 / S041; see H14 |
| H8 | **heterotic** standard embedding `E₈ → SU(3)×E₆` **skips E₇** in the chiral/N=1 case — the *same exclusion pattern as B210* | HOOK | 2026-06-27 | NOTICED | chat1-verified-lit | see H14 |
| H9 | **level-rank duality** `SU(2)₃ ↔ SU(3)₂` at `q=e^{2πi/5}`, through `(G₂)₁` and `(F₄)₁`; the Fibonacci anyon `= (G₂)₁`. Not an iso (4 vs 6 irreps) — a modular-data duality | HOOK | 2026-06-27 | NOTICED | chat1-verified-lit | — |
| H10 | **S041 correction:** "NCG: no shared data" is wrong. Connes' `ℂ⊕ℍ⊕M₃(ℂ)` encodes primes `{2,3}`; ours `{2,3,5}`; overlap `{2,3}`, golden prime 5 absent — a *partial* rhyme | ANOMALY | 2026-06-27 | CHECKED | corrected | corrected S041 (an unearned-negative caught — rule 7) |
| H11 | the **Kodama state** = `SU(2)` Chern–Simons at `k ~ 6/(GΛ)`; at what Λ does `k=3`? | QUESTION | 2026-06-27 | **CHECKED** | **FIREWALL/HELD; computed** | **COMPUTED (k=6π/GΛ): k=3 ⇒ Λ≈2π/l_P²≈2.4×10⁷⁰ m⁻² (Planck scale, ≈6.28 Planck units); the observed Λ needs k≈6.5×10¹²² (no object structure).** Clean *quantitative* negative — the CC-problem's 122 orders = the gap between the object's natural level k=3 (UV) and k→∞ (IR). Firewall *relocated to k*, not crossed (K018). Run, not prejudged. → S041 |
| H12 | does `c=7/10` (our TCI) appear in **Conway super-moonshine** (the N=1 SCFT module via the Leech lattice)? | QUESTION | 2026-06-27 | **CHECKED** | **DEMOTED (B234)** | building-block: NOT established (2003.13700). The Fibonacci category appears in the Conway SCFT (2512.19640) **but the category is unique ⟹ generic ubiquity, NOT a 2nd moonshine hook** (B234/H26); filed under H17, below the object-specific McKay hook. S041 corrected. |

## QUESTION-pass additions (first cycle, 2026-06-27)

| H# | hint | type | noticed | state | flags | disposition |
|---|---|---|---|---|---|---|
| H13 | does the **hyperbolicity-split motif** (H4) have a single unifying statement across B217/B226/B229 — "golden lives on both sides of every hyperbolicity divide as distinct manifolds"? | QUESTION | 2026-06-27 | **CHECKED** | candidate-synthesis | **B233: YES, stated** — the monodromy class `γ∈SL(2,ℤ)` / `SU(2)₃` data is hyperbolicity-blind and is realized twice: a non-hyperbolic (Sol/Seifert) manifold (algebraic/quantum face) and a hyperbolic manifold (geometric/volume face), never one. Pattern from 3 witnesses; "every axis" not proven. |
| H14 | is the `E₈+E₆−E₇` exclusion (H7) the **same obstruction** as heterotic's E₇-skip (H8)? i.e. is our congruence-quotient obstruction for `2O` ↔ the chirality obstruction for `E₇` reps? | QUESTION | 2026-06-27 | NOTICED | — | the deepest framework-search question |
| H15 | do the **non-SUSY** metallic chains (`Z_{m²+2}` parafermions, the FM family) have their own special structure at the metallic levels `k=m²+2`? | QUESTION | 2026-06-27 | **CHECKED** | earned-negative | **B231: NO — the level `k=m²+2` confers no special chain structure for m≥2.** Built+validated the general `su(2)_k` chain ED (Ising/TCI reproduced); silver `k=6` is a generic `M(7,8)` chain, not SUSY. The metallic mean is a quantum dimension (`λ_m=2cos(π/(m²+4))`) **iff m=1**; B218(anyon-realizable)+B224(SUSY)+B231(qdim=mean) collapse to one boundary `λ_m<2 ⟺ m=1`. |
| H16 | is `\|H₁\| ≡ 3 (mod 4)` (H1) tied to `40a1`'s reduction / the character-variety arithmetic (the conductor `2³·5`)? | QUESTION | 2026-06-27 | NOTICED | — | arithmetic-geometry scout |
| H17 | **why** does `5 = m²+4` at `m=1` govern golden across *every* face (period, anyon level, SUSY coset, conductor) — one reason or a pile-up of coincidences? | QUESTION | 2026-06-27 | **CHECKED** | partial-unification | **B233: NOT a pile-up.** 8/8 faces cascade from one root (golden=m=1, field `ℚ(√5)`, disc 5); the *only* genuine coincidence is `min(m²+4)=5` is also the largest McKay prime (`SL(2,F₅)=2I=E₈`; `E₇` excluded). One cascade + one coincidence — verified exactly (`verify_five.py`). |
| H18 | **(standing physics-parallels prompt)** which of our verified results have known parallels in physics frameworks we haven't examined? (re-run every WIDEN) | QUESTION | 2026-06-27 | NOTICED | standing | spawns per-result sub-scouts; keeps L46/S041 live |

## Compute-cycle additions (2026-06-27)

| H# | hint | type | noticed | state | flags | disposition |
|---|---|---|---|---|---|---|
| H19 | ~~the Fibonacci fusion category in the Conway SCFT as a 2nd moonshine hook; match its F/R-symbols to the object's SU(2)₃~~ | HOOK | 2026-06-27 | **DORMANT (B234)** | **circular-test** | **KILLED-as-posed (B234/H26):** the Fibonacci category is unique ⟹ the F/R "match" is forced ⟹ the test cannot fail ⟹ no object-specific info. `residual-hint:` the *opposite* question (does Conway use `ℚ(√5)`/`40a1`/a chirality beyond the bare category?) is the only informative version; prior = no. Reframed under H17/H28. |

## Chat1/Chat2 handoff (2026-06-27) — registered NOTICED before judging

Two handoffs: **Chat-1** (the computed exploration survey — Paths 1/A/2/3 + 5 discoveries) and **Chat-2** (a critique
of Chat-1 + its own discovery). Every distinct item registered here first; verdicts attach at CHECK (`B234`).

**CHECKED 2026-06-27 via `B234`** (all recomputed, cross-checked): **VERIFIED** — H20, H22, H23, H25, H27, H28, H29
(+ the why-5 co-occurrence). **VERIFIED-and-corrects-our-own-work** — H26 (H19 is circular ⟹ **H12 demoted**,
S041 fixed). **CORRECTED** — H30 (chat2's "same wall" → E₇ is *overdetermined/triple*, distinct mechanisms, with
chat1). **agreed framing** — H31 (H11 wording softened). The strongest new result is **H27** (the trace-1 congruence law); the strongest *method* is **H28** (the
specificity filter, now standing in `S041`).

**Follow-up resolutions (`B235`, 2026-06-27):** **H24 CORRECTED** — F₄ retracted, footprint **3/5** (no
`SU(3)_k⊂(F₄)₁` conformal embedding: `c(SU(3)_k)` never `=26/5`). **H32 CLOSED** — `ℚ(√−7)` does **not** appear
(SnapPy: all `4₁` covers ≤deg 6 keep `ℚ(√−3)` — arithmetic; algebraically the trace-1 ladder closes at `{√5,√−3}`
by unimodularity `det∈{±1}`). **H34 RESOLVED** — the metallic family table m=1..6 (→ L49, the paper's §4, B235) **and** the `SU(3)₂` WRT
level-rank check (B238): `Z(4₁;SU(2)₃)=Z(4₁;SU(3)₂)=−1/φ` (figure-eight coincides via shared `κ=5`), but not a
general level-rank equality — figure-eight-specific.

**Follow-up resolutions (`B236`/`B237`, 2026-06-27, SnapPy+GAP):** **H21 CLOSED** — the coset-coincidence
mechanism verified (Lashkevich/GKO/Qiu refs; `M(4,5)≡SM_1` the same coset, unique; E₇-in-TCI bonus), clearing the
paper's originality gate. **(Lit-gate read done, NOVELTY_AUDIT R6):** the mechanism is **PARTIALLY-KNOWN** — the
uniqueness *fact* is standard (FQS 1985 / Qiu 1986); the two cosets appear separately; Lashkevich (read in full)
does *not* state the same-coset observation — so "modest organizing observation, not a theorem" is supported. Cite
FQS/Qiu directly (chat1's hep-th/0311129 was misattributed). **H33 RESOLVED** — silver carries **no `2O`** (GQuotients=0, field-only coincidence); and
chat1's "all metallic bundles carry 2T,2I" is **corrected** to manifold-specific (golden→2T, silver→S₄, bronze→2T+2I;
2O absent throughout).

| H# | hint | type | noticed | state | flags | disposition |
|---|---|---|---|---|---|---|
| H20 | **E₇ exclusion is OVERDETERMINED** — independent mechanisms: Diophantine (`48≠p(p²−1)`, no congruence quotient) + rep-theoretic (E₇ has **no complex reps**, `w₀=−1` ⟹ non-chiral) (chat1 Path 1) | MATH | 2026-06-27 | **CHECKED** | **corrected** | **Sage (`e7_fs_indicator.py`): E₇'s 56 is PSEUDOREAL (FS −1, symplectic), NOT "real (+1)" as chat1's draft stated** — trivial ∈ Λ²(56). Conclusion (non-chiral) unchanged. Diophantine part holds. |
| H21 | the coset-coincidence **mechanism** (ordinary GKO = super GKO coset at `SU(2)₃`) is **not stated as a proposition** in the lit; the uniqueness *fact* is Qiu 1986 (via Johnson–Clifford hep-th/0311129); Lässig et al. 1991 write the TCI coset (chat1 Path A) | HOOK | 2026-06-27 | **CHECKED** | **CLOSED (B236)** | refs found (chat1) + **verified by central charges (B236):** super `SM_k=(SU(2)_k×SU(2)_2)/SU(2)_{k+2}` (Lashkevich hep-th/9301093), ordinary M(m,m+1) (GKO); `M(4,5)`≡`SM_1` = the *same* coset `(SU(2)_1×SU(2)_2)/SU(2)_3` (unique over the sweep), c=7/10; uniqueness fact = Qiu 1986. **Bonus:** TCI = `(E₇)₁⊕(E₇)₁/(E₇)₂` (c=7/10) — E₇ in the TCI as a coset algebra (≠ the excluded McKay role). **Closes the H21 gate** on the paper's originality claim. |
| H22 | **character-field = trace-field correspondence:** `2T→ℚ(√−3)`=hyperbolic field, `2I→ℚ(√5)`=monodromy field, `2O→ℚ(√2)`=ABSENT ⟹ E₇ **triply** excluded (chat1 D1) | MATH | 2026-06-27 | NOTICED | — | B234 |
| H23 | the **silver bundle (m=2) carries E₇'s field** `ℚ(√2)` (disc 8) — "what golden excludes, silver includes"; the metallic *field ladder* `M_m=[[m,1],[1,0]]`, disc `m²+4` (chat1 D2) | PATTERN | 2026-06-27 | NOTICED | — | B234 |
| H24 | ~~exceptional-group footprint 4/5 (incl. F₄ via `SU(2)₃↔SU(3)₂→(F₄)₁`)~~ → **3/5** (chat1 D3, **retracted by chat1 + B235**) | HOOK | 2026-06-27 | **CHECKED** | **CORRECTED** | **F₄ RETRACTED (B235):** no integer `k` has `c(SU(3)_k)=c((F₄)₁)=26/5` ⟹ no `SU(3)_k⊂(F₄)₁` conformal embedding ⟹ the route is void. Footprint = **3/5**: G₂ (Fibonacci), E₆ (`ℚ(√−3)`), E₈ (`ℚ(√5)`); E₇ excluded. (The real `(G₂)₁×(F₄)₁⊂(E₈)₁` is generic CFT, not object-specific.) |
| H25 | **SUSY vs E₈ are distinct:** SUSY needs `n=5` exactly; the E₈-*field* needs `squarefree(n)=5` (m=1,4,11,…); `m=4` has the E₈ field but NOT SUSY (`M(19,20)` not super) (chat1 D4) | MATH | 2026-06-27 | NOTICED | — | B234 |
| H26 | **H19 is CIRCULAR** (chat2) — the Fibonacci fusion category is unique (one F-matrix, two braidings), so any Fibonacci system shares the F/R data ⟹ the "match" can't fail ⟹ no object-specific info; the Conway hook is **generic** (Fibonacci ubiquity), NOT a 2nd moonshine crossing; **demote H12 below McKay, file under H17** | ANOMALY | 2026-06-27 | NOTICED | corrects-own-work | B234 → deflate H12/H19, fix S041 |
| H27 | **the trace-field law** (chat2) — golden's two fields are one fact with a sign | MATH | 2026-06-27 | **CHECKED** | **RECONCILED → B239** | the fragile "trace-1, `disc=1−4det`" (the bundle monodromy `RL` is **trace 3**, not 1) is superseded by the referee-proof **unimodular** form (B239): `disc=t²−4det`; `√5` robust (RL trace 3 & M_1 trace 1 both →5); the **only** imaginary quadratic trace fields are `ℚ(i)` & `ℚ(√−3)` (the `disc=−4` floor); E₇'s `√2` needs **even** trace (silver); `ℚ(√−7)` below the floor. |
| H28 | the **specificity filter** (chat2's method) — sweep metallic `m`; classify framework overlaps as **golden-specific** (only m=1) / **object-specific** (only `{ℚ(√5),ℚ(√−3)}`) / **universal** (no m-dependence → demote). Turns the framework search from "collect rhymes" to "measure specificity" | MATH | 2026-06-27 | NOTICED | standing-method | B234 → S041 standing filter |
| H29 | the **SL(2,F_p)↔binary-polyhedral bound** (chat2, load-bearing) — only `p∈{3,5}` give exceptional McKay groups (orders `24,120`), `p=5` largest; re-derive the finite-SU(2)-subgroup bound independently before banking | MATH | 2026-06-27 | NOTICED | re-derive | B234 |
| H30 | **caution (chat2):** chat2's "trace-1 congruence = the SAME obstruction as the Diophantine `48≠p(p²−1)`" is an *interpretation* — verify provably-equivalent vs merely-coincident (chat2's own flag) | ANOMALY | 2026-06-27 | NOTICED | cross-check | B234 (my prior: DISTINCT → overdetermined, with chat1) |
| H31 | **caution (chat2):** H11's "122 orders = gap between `k=3` and `k→∞`" is near-tautological (any UV/IR ratio reappears the CC problem) — a clean HELD negative, not insight; don't sell as such | ANOMALY | 2026-06-27 | NOTICED | framing | adjust S041 H11 wording |
| H32 | **lead:** does `ℚ(√−7)` (the next trace-1 imaginary rung) appear in the object's data — a deeper cusp field, a covering, a Markov-level field? (chat2) | QUESTION | 2026-06-27 | NOTICED | needs-snappy | candidate OPEN_LEADS |
| H33 | **lead:** does the silver bundle (m=2) actually carry a `2O` quotient of its π₁, or is `ℚ(√2)` only a field coincidence? (chat1 lead 2 / chat2) | QUESTION | 2026-06-27 | **CHECKED** | **RESOLVED (B237)** | **NO 2O** (GAP GQuotients=0): `ℚ(√2)` is a field-only coincidence. Also **corrects chat1**: the binary-polyhedral structure is manifold-specific (golden→2T, silver→S₄ [not binary 2O], bronze→2T+2I), NOT "all carry 2T,2I"; **2O absent from all three**. |
| H34 | **lead:** `SU(3)₂` WRT of the figure-eight vs `SU(2)₃` (level-rank, B204); + assemble the full metallic table m=1..6 (model, SUSY, McKay, Seifert dual, \|H₁\|) (chat1 leads 1,3) | QUESTION | 2026-06-27 | NOTICED | needs-snappy/compute | candidate OPEN_LEADS |

## Physics-hunt handoff (2026-06-27, firewalled; verify-don't-trust adjudication of chat1 vs chat2)

A chat1 physics hunt (figure-eight → SM gauge groups / SUSY / generations) + a chat2 critique. All
**firewalled** (`speculations/`-tier; nothing to `CLAIMS.md`); the verdicts below are my independent
re-computation.

| H# | hint | type | noticed | state | flags | disposition |
|---|---|---|---|---|---|---|
| H35 | **golden integrality of the colored Jones** — `[N]·J_N(4₁;e^{2πi/5})={1,−2,−2,1}` (`φ` cancels `1/φ`; mechanism `sin(π/5)sin(3π/5)=√5/4`) | MATH | 2026-06-27 | **CHECKED + CLOSED** | **VERIFIED → B240** | re-computed; **then closed by an amphicheiral-knot sweep** (validated `U_q(sl₂)` R-matrix colored Jones): two-tier — *amphichirality ⇒ real, `ℤ[φ]`* (class property), but *pure `ℤ` is figure-eight-specific* (only `4₁` among `6₃,8₉,8₁₂,8₁₇,8₁₈,10₁₇`; even `8₁₈=(σ₁σ₂⁻¹)⁴`, same braid family, gives `−8+7φ`). Galois-rigorous. `Z=−χ(Slavich)` HELD. Firewall-clean. |
| H36 | **amphicheirality = the E₆ outer automorphism** — conjugation on `2T`'s irreps = the finite-E₆ Dynkin automorphism | MATH | 2026-06-27 | **CHECKED** | **VERIFIED (math), NOT a physics bridge** | **adjudicates the chat1/chat2 hinge:** I computed the McKay quiver of `2T` (= affine `Ẽ₆`), removed the fixed trivial/affine node, and confirmed conjugation **is** a nontrivial graph automorphism of the finite `E₆` diagram. So chat1 is **substantively right** (standard McKay/Galois), chat2 right only that the *script asserted* it. But it is a clean rep-theory fact, **not** a derivation of physics. |
| H37 | the SM-emergence chain (`E₆` embedding → `SU(3)×SU(2)×U(1)`; twisted class-S → N=1; `A₄/V₄→3` generations) | HOOK | 2026-06-27 | **CHECKED** | **FIREWALLED / DEFLATED** | chat2's deflations **confirmed**: "SUSY-breaking CLOSED" and "3 generations forced" do **not** hold — `z²=ω₃` is shared by *all* regular-ideal-tetrahedra manifolds (not object-specific), the generation count is `\|χ\|/2=1` not 3 (the `χ=6` step is reverse-engineered), and the 4d lift is uncomputed. The `(E₆)₁⊃SU(3)₂×(G₂)₁` central charges are exact (rep-theory), but the *gauge-group identification* is a rhyme. **Scale: provably external** (Buckingham-Pi — chat2, sound). HELD; nothing crosses. |
| H38 | does the Gang–Yonekura twist-knot `SU(3)` (the §6 horizon lead) equal our level-rank `SU(3)₂` (B238)? | QUESTION | 2026-06-27 | **CHECKED** | **RESOLVED (B241): DISTINCT** | **No** — GY [arXiv:1803.04009] is a global *flavor* symmetry of `T[K]`, **universal** across all hyperbolic twist knots (from `A₁`); ours is a CS *gauge* level-rank dual, **figure-eight-specific** (golden, `κ=5`). Different type *and* specificity; within GY's twist family only `4₁` (the unique amphicheiral one) carries the golden structure (computed, B240 tool). Verified the *negative* too (this session's lesson). A clean distinction — further deflates H37 (even the two SU(3)'s don't unify). Firewall-clean. |
| H39 | the **three-SU(3) trichotomy**: SL(3,ℂ) char variety (B71) ‖ level-rank `SU(3)₂` (B238) ‖ GY flavor `SU(3)` (B241) — distinct? related? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B242): three distinct objects + the new edge** | three SU(3)'s on three faces (classical geometry / quantum / SCFT); distinct in type & specificity. **New computed edge:** the level-rank duality `SU(2)₃↔SU(3)₂` acts on the fundamental knot invariant (`=HOMFLY(a=qᴺ,z=q−q⁻¹)`) as **complex conjugation** at the golden `κ=5` root (`a=qᴺ↦q^{5−N}=−q⁻ᴺ`), so it's **exact iff amphicheiral** — `4₁→−2/φ`; chiral knots → conjugate pairs. **Unifies amphichirality (B240/B241) with level-rank (B238).** (1)↔(2)=quantization; (3) separate. → PAPER §4.5. Firewall-clean. |
| H40 | does the level-rank=conjugation mechanism (B242) generalize across levels / to a "metallic ladder" of values? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B243): mechanism UNIVERSAL; ladder REFUTED** | the conjugation mechanism is **level-independent** (`q^κ=−1` ⟹ `a²↦conj(a²)` for every `SU(2)_k↔SU(k)_2`) — exact-iff-amphicheiral at all `k` (verified `k=2..10`); golden `−2/φ` is the `k=3` case, NOT special to the mechanism. **Verify-don't-trust caught a seductive over-claim:** the figure-eight value `V(k)` is golden(`k=3,√5`)+silver(`k=6,√2`) but the "metallic ladder" is **refuted** — `V(k)` is quadratic only at `φ(κ)≤`-small levels `κ∈{5,8,10,12}`; **bronze** (`κ=13,m=3`) is degree 6, not in `ℚ(√13)`. A 2-case coincidence, not a pattern. → PAPER §4.5. Firewall-clean. |
| H41 | the trichotomy's (1)↔(2) edge: does the quantum `SU(3)₂` see the 3 components of the SL(3,ℂ) char variety (B71)? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B244): no at level 2; it's quantization** | the `SU(3)₂` fundamental invariant is a **single number** (`−2/φ`) — does not resolve the 3 components; they're the classical SL(3,ℂ) **saddles** the WRT quantizes, resolved only in the `k→∞` asymptotics. **Computed (SnapPy Ptolemy, N=3):** the 3 components carry distinct complex volumes — geometric `V0` has `Vol = 4·Vol(4₁) = 8.1195` (the `Sym²` `(n³−n)/6` factor, confirming B71's `V0=Sym²` via the volume), `W1/W2` have `Vol=0`. Fundamental rep doesn't see volume (`→1` as `k→∞`). → PAPER §4.5. Firewall-clean. |
| H42 | does the level-rank=conjugation (B242/B243) extend to higher colors, where the transpose is genuine? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B245): exact transpose duality at all colors** | the higher-color level-rank `SU(2)_k` color-N (sym `[N−1]`) ⟷ `SU(k)₂` transpose (antisym `[1^{N−1}]`) holds **exactly** for `4₁` at every `N,k≥N−1` (reduced colored HOMFLY, no phase; verified `N≤5`). Mechanism: duality = `(a=q²↦q^k=−q⁻²) ∘ (transpose q↦q⁻¹)`; the fundamental is self-transpose ⟹ collapses to B242/B243's pure conjugation — *why* "=conjugation" is fundamental-specific. Tools validated: IMMM arXiv:1203.5978 symmetric (eq 4: `p=1`=fundamental HOMFLY, `A=q²`=B240 `J_{p+1}`) + antisymmetric (eq 8) ⟹ reduced transpose symmetry `H^Λ(A,q)=H^S(A,1/q)` confirmed independently. → PAPER §4.5. Firewall-clean. |
| H43 | does the large-color quantum SU(3) invariant of `4₁` grow at the SL(3) geometric rate `4·Vol=8.1195` (a "SL(3) volume conjecture")? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B246): NO — symmetric VC gives the complement volume, group-independently** | **verify-don't-trust overturned the naive expectation.** The symmetric large-color VC `(2π/p)log|H_{[p]}(A=qᴺ)|` extrapolates to `Vol(4₁)=2.0299` for **both** SU(2) (`→2.0298`) and SU(3) (`→2.0297`), **not** `8.1195` — because `A=qᴺ→1` as `q→1`, washing out the group. The SL(3) geometric `8.1195` is **principal-rep** (`Sym²`) classical data (B244), not a large-color limit. The fixed-`A` ('t Hooft) generalized volume is a **continuous** `A`-curve (0,5.09,8.70,13.79 at `A=1,1.5,2,3`; Aganagic–Vafa Q-deformed A-poly), not singling out `8.1195`. Three limits kept apart: large-level=CS phase (B244) / large-color=complement vol / fixed-A=continuous. → PAPER §4.5. Firewall-clean. |
| H44 | (chat1/chat2 V1 handoff) does the figure-eight holonomy break `E₆` to the SM gauge group via `C_{E₆}(SU(2))`? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B247): bridge FAILS; both desks' centralizer corrected** | **FIREWALLED.** Sage branchings: `78→(8,1)+(8,7)+(1,14)` (resolves chat1's 64/78). Corrected centralizers: `C_{E₆}(SU(2)_long, idx1)=SU(6)` (dim 35, `E₆⊃A1×A5`); `C_{E₆}(SU(2)_short, idx3)=SU(3)×SU(2)` (dim 11, **no U(1)**) — **neither is the `SU(3)×SU(2)×U(1)²` dim-13** both chats claimed (chat2's rank argument fails). SU(6) ⊃ SM but as a generic GUT. **The object-specific bridge fails (chat2 right, decisive + sharpened):** the geometric holonomy is irreducibly `SL(2,ℂ)` (complex traces, `ℚ(√−3)` → E₆) — confirmed via chat2's matrices AND SnapPy; the `SU(2)` reps that could break E₆ are a *different* char-variety component carrying `ℚ(√5)` (golden, `tr(ab)=φ`, → E₈). The connection that selects E₆ is provably not the one that could break it; no canonical identification ⟹ breaking VEV is a choice, not geometry. → PAPER §6 (firewall table). Nothing to CLAIMS. **Verify-don't-trust catches on chat2's computations (judgment sound, several specifics wrong):** (i) chat2's Test-1 *matrices* used `u=e^{iπ/3}` — a root of `u²−u+1`, NOT the figure-eight Riley poly `u²+u+1` (`e^{±2iπ/3}`); not a valid rep (conclusion unaffected). (ii) chat2's "Riley `u=1/φ`" at `x=0` is loose — actual `u=−(5∓√5)/2`, `tr(ab)=φ`/`−1/φ` (`|−1/φ|=0.618`); golden right, value wrong. (iii) chat2's `dim-13` centralizer wrong (it's SU(6)). chat2 RIGHT on: the commuting inclusion, rank-5 (long), the GUT hypercharge (`27→16+10+1`, A.1), and the decisive deflation. |
| H45 | (push from B247) is the dual McKay `E₆+E₈` (B210) an object-true geometric statement? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B248): yes — it is the hyperbolic↔spherical geometric transition** | **FIREWALLED (McKay/Arnold-trinity, not gauge groups).** The figure-eight cone-manifold (meridian trace `x=2cos(α/2)`) sweeps: `α=0` (x=2) **hyperbolic** complete cusp, `u=e^{±2iπ/3}`, `ℚ(√−3)→2T→E₆`; `α=2π/3` (x=1) **Euclidean** transition, `u=−2` (ℚ); `α=π` (x=0) **spherical** `ℤ/2` orbifold, `u∈ℚ(√5)`, `tr(ab)=φ`, `SU(2)` holonomy, `ℚ(√5)→2I→E₈`. The spherical end's double branched cover is the lens space `L(5,2)`, `|H₁|=det(4₁)=5` — so the golden "5" of the E₈ end *is* the knot determinant. Refines B210: both fields arise as flat connections/geometries on `4₁` (two ends of one deformation), not geometry+monodromy. → PAPER §3.2. Nothing to CLAIMS. |
| H46 | (push from B248) sweeping the full orbifold tower — which McKay fields appear, and is E₇ really absent? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B249): Niven's theorem forces exactly E₆+E₈; E₇ geometrically impossible** | **FIREWALLED.** At the `ℤ/n` orbifold the trace field is `ℚ(2cos(π/n), √((5−x²)(1−x²)))`, clean-quadratic iff `2cos(π/n)∈ℚ` — by **Niven** only `n∈{1,2,3,∞}`: `E₆` (hyperbolic, `ℚ(√−3)`), Euclidean (`n=3`, ℚ), `E₈` (spherical `ℤ/2`, `ℚ(√5)`, cover `L(5,2)`, det=5). E₇'s `ℚ(√2)` needs `x=√2` (`n=4`), irrational ⟹ only the **mixed** `ℚ(√2,√−3)`, never clean. **So dual McKay E₆+E₈ AND the E₇-exclusion are ONE arithmetic fact** (Niven on the cone-deformation) — a 4th, geometric, unifying mechanism for §5.2. Object-specific: only `m=1` is a knot complement (has the orbifold/det structure). Physical read (firewalled): a curvature-sign transition hyperbolic→Euclidean→spherical = a structural rhyme, no scale/dynamics. Novelty APPEARS-NOVEL. → PAPER §5.2. Nothing to CLAIMS. |
| H47 | (push from B249) quantify the transition — what are the volume + Chern-Simons along E₆→Euclidean→E₈? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B250): the arithmetic invariants ARE the end-volumes** | **FIREWALLED.** Complex-volume profile: **hyperbolic E₆** `Vol=6Λ(π/3)=2.0299` (the `−3` tetra `e^{iπ/3}`), `CS=0`; **Euclidean** `Vol=0` (collapse); **spherical E₈** `Vol=π²/5=1.9739` (the `5`=det(4₁), via `L(5,2)`), `CS∈(1/5)ℤ` (`{0,2/5,3/5}`). So `−3` lives in the hyperbolic-end tetra shape and `5` is *literally* the spherical-end volume/CS denominator — one geometric quantity carries both. End-volumes comparable (2.030 vs 1.974), opposite curvature. Quantitative form of the §3.3 "5 cascade" + §5.1 dimensionless-CS firewall. Hyperbolic anchor cross-checked vs SnapPy. → PAPER §3.3/§5.1. Nothing to CLAIMS. |
| H48 | (push from B249/B250) is the transition really golden-specific — does any m≥2 metallic bundle host it? | QUESTION | 2026-06-28 | **CHECKED** | **RESOLVED (B251): golden-specific by a homology obstruction** | **FIREWALLED.** `Mₘ`=mapping torus of `RᵐLᵐ`, monodromy `[[1+m²,m],[m,1]]` (trace `m²+2`); `H₁(Mₘ)=ℤ⊕(ℤ/m)²` (= B126/S029) equals `ℤ` **iff m=1**. A knot complement in `S³` has `H₁=ℤ`, so only the figure-eight is one ⟹ only it has the meridian/determinant/lens-space-double-cover the transition needs; m≥2 keep only `ℚ(√(m²+4))` in the trace field. The m=1 "5" stack collapses: discriminant `m²+4` = `det(4₁)` = 2-bridge numerator (`4₁=b(5,2)`) = `|H₁(L(5,2))|` = 5 (SnapPy-confirmed `m004=4_1`). Sharpest §4.1 tier (fails ∀m≥2 by obstruction, not near-miss). → PAPER §4.1. Nothing to CLAIMS. |
| H49 | (Chat-2 handoff) is chirality (27↔27̄ preference) the located firewall — does any 4₁-specific structure break the matter–antimatter symmetry? | QUESTION | 2026-06-28 | **CHECKED** | **ADJUDICATED (B252): chat-2 right on all 4 steps; obstruction real and unbroken** | **FIREWALLED.** Verified (Sage): 27 complex, 78 real, E₈→E₆×SU(3) pairs 27/27̄; Step 3 (`amphicheirality = E₆ outer aut = 27↔27̄ swap`) **IS banked = H36** (re-confirmed Sage) — chat-2's citation correct. **Could not break it**: every conjugation-odd invariant is symmetric (`CS=0`; `L(5,2)` amphichiral `2²≡−1 mod5`; spherical CS `±`-paired; E₈ pairing). So the object IS 27↔27̄ (matter–antimatter) symmetric — a located **2nd firewall** (chirality, not gauge/scale), extending S001 (θ=0), grounded in H36. **Precision:** amphicheirality is topologically *universal* across metallic m (S001/B92/B211-L32), but its E₆/27↔27̄ realization is m=1-specific (only m=1→ℚ(√−3)→2T→E₆). **Self-caught:** I nearly mis-recorded Step 3 as a mis-citation (reflexive deflation); H36 caught it — verify-don't-trust on one's own "no". → S042 chirality resolved. Nothing to CLAIMS. |
| H50 | (Chat-2 handoff, follow-up) does chirality REDUCE to gauged-vs-SSB of the amphicheiral τ, and is it decidable (cheaply, via CS)? | QUESTION | 2026-06-28 | **CHECKED** | **ADJUDICATED (B253): Part A banked; the reduction is firewalled, not object-decidable** | **FIREWALLED.** **Part A (bankable, Sage):** complex-rep capability is E₆ yes (27), E₇/E₈ no (56 pseudoreal / 248 real); `|Out|` 2/1/1. The "chirality axis" coincides with the B248 transition axis (E₆ capable→E₈ not); E₇ also Niven-excluded — consistent. **Correction to B252 (adopted):** "cannot source asymmetry"→"no *explicit* CP-odd datum; SSB of τ would source it but needs external dynamics" (fits S040). **Part B (the reduction) firewalled:** not decidable from the object — (1) `T[4₁]` is 3d (K006), not 4d where chirality lives; (2) E₆ is a McKay label not a gauge group (B247); (3) gauging is a model choice, SSB needs dynamics (K018). CS "discriminator" **inconclusive at CS=0** (τ gaugeable but both branches open; decisive only if CS≠0). Branch b ("amphicheirality as origin of chirality") = legitimate `[LEAP]` in S042, not banked. Net: firewall sharper, not weaker. → S042, B252 refined. Nothing to CLAIMS. |
| H51 | (Chat-1 handoff) does the "uniqueness chain" (4₁→ℚ(√−3)→2T→E₆→SU(3)×SU(2)×U(1)) + chain merger verify? | QUESTION | 2026-06-28 | **CHECKED** | **BANKED (B254): the arithmetic+CFT spine verifies; quantum & arithmetic chains merge** | **FIREWALLED.** Verified (exact `c`): conformal tower `(E₆)₁ ⊃ SU(3)₂×(G₂)₁ ⊃ SU(3)₂×SU(2)₃×U(1)` closes at `c=6`; `(G₂)₁`=Fibonacci `⊃ SU(2)₃×U(1)`. **Merger (new):** the `SU(2)₃` from FLW universality (B204 quantum chain) = the `SU(2)₃` of the E₆ embedding (arithmetic chain) — the two faces converge at `(G₂)₁`=Fibonacci. **Corrected:** handoff's McKay fields wrong (`E₇↔2O`=ℚ(√2), `E₈↔2I`=ℚ(√5); spine ℚ(√−3)→2T→E₆ right). **F₄ Z₂-grading** (Sage): `27=1+26`, `78=26+52` — rep theory banked; Yukawa-mass reading firewalled. **Settled/firewalled:** V1 refuted (B247), amphicheirality=H36, "GUT boundary conditions/SU(2)_R/one SM gen" = dead bridge. → uniqueness-chain spine banked. Nothing to CLAIMS. |
| H52 | (Chat-1 handoff) the "dimensional filter" — is d=3 the unique simplex giving an exceptional with a complex fundamental? | QUESTION | 2026-06-28 | **CHECKED** | **BANKED (B255): yes — the tetrahedron, uniquely** | **FIREWALLED.** `d`-simplex rotation group `A_{d+1}` is an `SO(3)` subgroup only for `d∈{2,3,4}` (cyclic/`A₄`/`A₅`); McKay → `d=2` A-type (classical), `d=3` `2T→E₆`, `d=4` `2I→E₈`. Only E₆ has a complex fundamental (B253), so **the tetrahedron (d=3) is the unique simplex selecting an exceptional with a complex fundamental** — same `d=3` as the ideal-tetra hyperbolic end (B250). **Corrected** handoff's `d=2` muddle (ℤ₆→SU(6) not SU(3); classical either way). Physics ("3 spatial dims / chiral matter") firewalled. → companion to B253. Nothing to CLAIMS. |
| H53 | (deep-dive hunt) where is E₇? — does the metallic family realize the full Arnold trinity? | NOTICED→CHECKED | 2026-06-28 | **CHECKED** | **BANKED (B256): E₇ is the SILVER bundle's discriminant field** | **FIREWALLED.** Monodromy disc `t²−4 = m²(m²+4)` ⟹ discriminant field `ℚ(√(m²+4))`: m=1→`ℚ(√5)`→2I→**E₈**, m=2→`ℚ(√2)`→2O→**E₇**. With the figure-eight trace field `ℚ(√−3)`→2T→**E₆**, all three Arnold groups appear. **Reconciles** B237 (no 2O π₁-quotient — but golden's E₈ isn't a π₁-quotient either; both are discriminant/homological McKay, same level), B249 (Niven excludes E₇ from the orbifold trinity), B251 (silver not a knot). ⟹ **E₇ is arithmetically present (silver disc) but geometrically homeless** — the unique Arnold group with no cone-manifold/cover realization. Answers "where is E₇" (H14): relocated to silver, not subtracted. Open: any non-discriminant realization of silver E₇? the Euclidean (ℤ/3) transition point. Nothing to CLAIMS. |
| H54 | (deep-dive hunt) what is at the Euclidean transition point (n=3, ℤ/3 orbifold) — the missing middle of B248→B256? | QUESTION | 2026-06-28 | **CHECKED** | **BANKED (B257): the order-3 Eisenstein discriminant branch point** | **FIREWALLED.** Four facts at `x=1`: (1) it's the **discriminant/branch point** of the character variety — hyperbolic (`x>1`, complex `u`-pair) and spherical (`x<1`, real `u`) branches collide at `u=−2` (double root); the transition IS the disc locus. (2) The **meridian is the order-3 rotation** (`ζ₆`, `ζ₆²=ω` the Eisenstein cube root) — the ℤ/3 cone. (3) **Complex volume = 0** (Vol & CS both vanish; the zero/origin of the B250 profile). (4) Cusp shape `2√−3` ∈ ℚ(√−3) (rectangular, **not** hexagonal — corrected guess); last Eisenstein point before golden ℚ(√5) at n=2. **"The three"** = n=3 = order-3 meridian = ω = the geometric cone order, **NOT three generations** (firewalls the recurring speculation cleanly). Completes the 3-anchor set E₆/Euclidean/E₈. Nothing to CLAIMS. |
| H55 | (deep-dive hunt: S027 quantum + H27) why do ℚ(√−3) & ℚ(√5) co-appear, and does the quantum face see the two ends? | QUESTION | 2026-06-28 | **CHECKED** | **BANKED (B258): H27 resolved + the quantum mirror** | **FIREWALLED.** **H27:** the two fields are different invariants — discriminant field `ℚ(√(m²+4))` is quadratic for ALL m (the 2×2 monodromy eigenvalue field); the hyperbolic **trace field is quadratic ONLY at m=1** (`ℚ(√−3)`; SnapPy: silver/bronze degree 8, non-arithmetic). They co-appear iff the object is metallic AND arithmetic = the figure-eight (unique). Neither forces the other; co-appearance = the intersection, realized as the transition's two ends. **Quantum mirror:** Kashaev `⟨4₁⟩_N` (`q=e^{2πi/N}`, N→∞) → `Vol=2.0299` (hyperbolic/E₆/ℚ(√−3) end, log-corrected check); golden-root WRT (B240) → `ℚ(√5)` (spherical/E₈ end). **Three faces (geometry/arithmetic/quantum) unify on the two ends.** The SM is the walled 4th face (B247) — the honest "unify quantum & SM" horizon IS this wall (a theorem, B247/B252/B253, not a gap). Nothing to CLAIMS. |

## Tombstoned hints
*(none yet — killed hints route here with a `residual-hint:` field, per anti-blindness rule 4.)*
