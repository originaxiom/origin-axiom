# THE CONSISTENCY CAMPAIGN

**Opened 2026-08-31 (B1229's method flip). Owner: *"craft a campaign and do it properly."***
**Gate 5 absolute throughout. Every cell is TWO-OUTCOME and both outcomes are bankable.**

## The premise (B1229)

`GRAND_COMPUTATION_v0` §6 is a **deletion schedule**: kill an input row by *deriving* it from the
object — the one thing **B1225 proves impossible**. B1216 spent nine agents on it for zero
deletions. The flip: **ask consistency, not the object.** Consistency *classifies the menu* even
where the object cannot *pick*. A classified finite menu plus a picker is a parameter-free theory
with one calibration — the map's own goal sentence.

## Stop rules (declared before any cell runs)

1. **No cell may claim a row DELETED.** Cells re-TYPE rows (continuum → finite → bit) or name the
   decisive retrieval. A deletion claim requires the owner's election.
2. **Base-field discipline (C-1's own lesson, binding on every later cell):** every
   continuous-parameter count states the field it is taken over.
3. **A cell that cannot fail does not run** (MB12). Each cell below declares its bite.
4. **`already_banked` before every novelty claim** (B1202) — the rule this session broke once.
5. **Cited ≠ verified.** A cert re-run is not a re-derivation; say which one happened.

## The cells

| # | cell | two outcomes | status |
|---|---|---|---|
| **C-1** | **The base-field audit.** Sweep every continuous-parameter count in the input ledger; re-count each over the object's own field. Generalises the ℙ³ tell: projectivising a d-dim ℚ(ζ₁₂)-space over ℚ manufactures 4d−1 parameters where the object sees d−1. | **OVER-COUNTED** (≥1 row shrinks) / **CLEAN** (every count was already field-correct — the ℙ³ is isolated) | RUN FIRST |
| **C-2** | **The ℤ/12 character assignment** (codex holds it; B1216's R1). Decides identity-vs-scalar on B₀. | **A: identity** → ℙ³ is real, floor carries 3 continuous params / **B: scalar** → B₀ is 1-dim over ℚ(ζ₁₂), ℙ = a point, **zero** | RELAY |
| **C-3** | **λ as a KMS weight.** Is the modular (Tomita–Takesaki) flow periodic for a rational boundary? Periodic ⇒ λ is a finite label. | **PERIODIC** (λ joins the finite menus) / **NOT** (λ stays a genuine continuum; the floor carries it) | QUEUED |
| **C-4** | **The r-label via the T-matrix.** T has finite order (12 at E₆). Is the arrow a branch choice of finite order? | **FINITE-PLACE CONFIRMED** (matches B1182's typing from an independent direction) / **UNRELATED** | QUEUED |
| **C-5** | **The σ menu, robustness.** Drop the two-character/ℓ=0 restriction: does the menu stay discrete? Rationality (Anderson–Moore/Vafa) says yes; compute how much it grows. | **STAYS SMALL** / **GROWS UNUSABLY** (then only the rationality core survives, which is still not a continuum) | QUEUED |
| **C-6** | **The ledger amendment.** Rewrite §5's input slots with the consistency typing and the field of each count. | (bookkeeping; runs last) | LAST |

## What the campaign is NOT

It does not claim σ = 1 (B1228's retraction stands), does not derive J (a free label), and does not
touch B1225. It re-types inputs; it does not conjure them.
