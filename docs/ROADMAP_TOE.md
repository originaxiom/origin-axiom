# THE ROADMAP — what the program has, what remains, and what "a road to a ToE" honestly means here

**2026-07-08. Written at the owner's request after the residue arc closed. Firewalled: this
document states the program's position; it claims nothing physics-shaped. The reflective
reading lives in the S-room (Atlas card 26); this file is the sober map.**

## I. Where the program stands

The object is **described** — completely, exactly, and with its own generating mechanism
exhibited: one self-generating substitution, whose statics (seam form, selection rules,
subfield lattice, dark locus, continents), dynamics (the 8-4-3 → (4,4,3,4) scalar law,
the l-sweep, the spectral fields), relations (gluings, walls, children), thermodynamics
(escape rate, gap labels), and residue (the orientation character and its breath) are all
now derived, each reducible to named classical mechanisms with the object contributing a
handful of Fricke/Galois inputs — most centrally **tr(A₁A₂) = 15 = the level itself**.
The program's central theorem remains its own hardest constraint: **the Inversion Law** —
what the object forces is its arithmetic, and everything built on that arithmetic is
shared by the class that has it. Fifteen-plus physics-shaped hypotheses died against it;
the one invariant that survived every register is one bit, and that bit is derivable
everywhere it appears.

The machine works: three seats, preregistration, falsifiers, exact arbiters, the shuttle
rule ("name the operator" — four collisions caught in one arc), burden-inversion, and a
ledger that corrected two banked numbers and its own drafting seats without external
input. This is the program's second product, and it is not the lesser one.

## II. The immediate deliverable: the mathematics papers (Chat-1's accounting, endorsed with corrections)

Content verified and banked for three papers; each writing pass re-verifies at promotion
time per the dual protocol:

1. **The Seam Form** (the 44 values, spectral structure, exchange identity, the
   cornerstone, the Klein-four selection rules and their subfield-lattice mechanism, the
   dark locus, Par as the source of all selection rules, det(Par) = −1).
   Corrections to carry: the selection-rule banks are B459/B468 (not B465); tier tables
   are pair-specific (B459 addendum).
2. **The Inversion Law** (child program C0–C6, the three laundering tiers, the wall as
   orientation-reversing merge, the collision census incl. the triple, the transfer
   function and what the wall forgets). Corrections: the collision bank is B467 (not the
   ethogram); the oriented statement is 5₂(5,1) = 4₁(−5,1); "torsion 121" is the
   parent-side abelian-book resultant, not the child's H₁.
3. **The Selection Rules and the Dynamics** (the 8-4-3 stated in its B465 form — the
   order multiset of an order-4-mod-scalar operator with invariant decomposition
   4⊕4⊕3⊕4, NOT an octet; the Fricke derivation chain; the heartbeat as the σ-orbit;
   the breath laws BR1/BR2; the σ-parity mechanism for the emptiness (B469-P2b)).

4. **The Markov Uniqueness** (tr[A_m,A_n] = 2 − (mn(n−m))²; Cohn's stage; the parabolic
   uniqueness of (1,2); the breathability locus; the breath hierarchy root ⟹ mirror ⟹
   balanced ⟹ frozen residue with its exact witnesses). **This paper LEADS** — the
   crispest self-contained result the program owns — pending the lit-gate ("we could not
   locate this identity," never "nobody wrote it down").

Plus the standing flagship paper. These are mathematics papers; they say what the object
IS. That is the honest deliverable available today. **The six-measurements scorecard is
retired to the S-room (S056) with its null numbers attached, by its author, after
three-seat convergence — the theorems don't need it.**

## III. The open registers (the map of where a non-laundered channel could still live)

Every register probed so far returned class-shared or empty. What has NOT been probed to
exhaustion, in order of tractability:

| register | state | blocker/cost |
|---|---|---|
| BR3 + Phase 2b (the breath fields; the parity column on the full ledger) | queued, in-repo machinery | sessions |
| H112 (the family trace law's surviving core) | hint recorded, naive form refuted | a theorem attempt |
| R2 Borromean SL(3) + RZ (Relation campaign close) | sage sweep running | ladder in progress |
| D3: the INTERACTING/many-body register | prereg'd, power-gated | the one register with no laundering theorem (no interacting gap-labeling exists); Macé lit-gate |
| the monodromy-group cell | blocked | Chat-1's loop definitions (standing debt) |
| multi-cusp / large-N assembly | NEEDS-SPECIALIST | genuinely new mathematics |
| **the L50/DGG dictionary at E₆** (T[M₃, E₆]) | NEEDS-SPECIALIST | research-frontier QFT; nobody has built it |

## IV. What "a road to a full ToE" honestly means after the Inversion Law

Three scenarios, exhaustive as far as the program can see:

- **(a) A non-laundered observable exists** in one of the unprobed registers (the live
  candidates are exactly the table above — chiefly the interacting register and the
  genuine dictionary). The program's method is built to find it if it is there, and to
  kill it honestly if it is not. This is the only scenario in which the object itself
  carries SM content, and the burden-inversion rule prices it correctly: it must produce
  a value no class-sibling shares, out-of-sample.
- **(b) The class is the object.** Everything forced is class-shared — then physics, if
  it lives here at all, picks the CLASS, not the member, and the honest ToE-shaped claim
  shrinks to: *the kinematic/symmetry frame is forced arithmetic; scale, chirality choice,
  and dynamics are not in it* — which is precisely the banked five-wall/two-ended
  boundary theorem. This is already a result. It is not the ToE hoped for; it is a true
  statement about where such a theory cannot come from.
- **(c) The deliverable is the boundary itself**: publish the mathematics (II), state the
  boundary theorem with its receipts (15+ kills, each with a mechanism), and hand the two
  genuinely open doors (the E₆ dictionary; the interacting register) to the communities
  equipped for them, as precisely-posed problems. A program that maps where the answer
  is NOT, exactly and reproducibly, has done science.

The program's stance, per the standing rules: pursue (a) through the queued registers at
full discipline, hold (b) as the running null, and build (c) continuously so that the
deliverable exists at every moment the program might stop.

## V. Sequencing

1. Close the Relation campaign (R2 → RZ) and the Breath Campaign's Phase 2b + BR3.
2. Write Papers 1–3 (promotion-time re-verification passes).
3. D3 (interacting register) — the highest-value unprobed cell.
4. H112 theorem attempt; the lens probes (#212); Chat-1's loop definitions when delivered.
5. The specialist asks, packaged: T[M₃, E₆]; multi-cusp assembly; the certified orbit
   enumeration (B451's boundary).
