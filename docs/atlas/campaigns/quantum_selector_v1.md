# Quantum Selector Campaign v1

Status: public-safe synthesis. No claims. This page summarizes the completed
campaign at a reviewable level and intentionally excludes raw run logs,
scoreboards, source downloads, and local path inventories.

## Run Facts

```text
campaign: quantum_selector_v1
cycles completed: 232
unique mechanisms evaluated: 50
source-derived questions: 80
verdicts: 6 survives, 28 stalled, 8 killed, 8 needs-expertise
```

Lane counts:

```text
boundary_category: 2
gauge_dictionary: 2
general: 35
negative_controls: 2
observable_gate: 3
pentagon_identity: 1
state_integral_selector: 5
```

## Main Result

The campaign did not find a physics bridge. It sharpened the mathematical
survivor:

```text
cancellation only becomes generative when inverse acts are distinguishable
distinguishability introduces order
order permits noncommutativity
noncommutativity can leave residue
the L/R residue is the strongest surviving Origin-side object
```

This supports the same direction as the conditional uniqueness theorem:

```text
A1-A7 -> A = LR, up to order -> P1-P16
```

The live object is therefore:

```text
ordered noncommutative cancellation residue
```

The campaign repeatedly failed to turn that residue into physics without
inserting a dictionary, functor, unit scale, source law, or observable channel.

## Survivors

The six survivors were:

```text
COV2_LR_COMMUTATOR_TRACE3_PHI
COV2_FREE_GROUP_COMMUTATOR_RESIDUE
COV2_HOLONOMY_LOOP_RESIDUE
CANCEL_NONCOMMUTATIVE_RESIDUE_SEARCH
CANCEL_DISTINGUISHABILITY_ORDER_GATE
GUT_SO10_16_PACKET
```

Interpretation:

```text
the first five are Origin-side residue results
the sixth is an external host-level matter-compression target
```

The first five all express the same pattern in different languages:

```text
abelian content cancels to zero
ordered nonabelian word remains nontrivial
closed label loop can carry nonidentity holonomy
minimal L/R shears retain trace 3 and phi-spectrum data
```

These are candidate inputs for PC04, not physics claims.

## Killed Routes

The campaign killed eight routes:

```text
exact zero cannot leave zero under source-free deterministic maps
source-free polynomial zero-birth fails
commutative inverse cancellation leaves no residue
dimensionless closure cannot generate dimensionful units
figure-eight constants do not derive physical constants without units or a measurement map
active A-polynomial convention kills the (1,1)-singularity claim
theta-star / CKM route leaks direct measured inputs
zero remains absorbing without source, noise, clock, or boundary data
```

The strong lesson:

```text
literal 0 -> 1 is dead in the tested source-free deterministic classes
commutative cancellation is dead as a generator
dimensionless trace/phi/topology cannot become physics without scale/source data
```

## Stalled Routes

The main stalled classes were:

```text
state integral recovers a host curve but not a contour selector
boundary counts do not select a representation category
finite cusp symmetries do not construct gauge connections
trace 3 does not select three matter families
discriminant 5 does not construct D5 roots
binary word count 16 does not construct a Spin(10) spinor action
finite spectral triples are hosts only after their data are supplied
```

Meaning:

```text
interesting host exists
selector, dictionary, action, unit scale, or observable is missing
```

## State-Integral Outcome

The campaign did not dismiss the state-integral route. It made the missing
theorem precise.

What works:

```text
state-integral saddle structure can recover the figure-eight A-polynomial host
```

What does not work locally:

```text
the nonzero contour, thimble, or relative-homology class is not forced
```

Needed theorem:

```text
a source-normalized quantum-dilog / Picard-Lefschetz theorem forcing a nonzero
relative-homology or thimble-intersection class for the figure-eight state
integral
```

This belongs under PC06 as a bridge problem, not under the proven core.

## Physics-Side Hosts

The campaign again found strong external hosts:

```text
SO(10) 16 as matter-compression host
finite spectral triples and spectral action as SM+GR host
```

But every Origin-derived route stalled:

```text
trace 3 is not a matter-copy functor
discriminant 5 is not a D5 root datum
binary word count 16 is not a Spin(10) spinor action
finite spectral-triple data are selected only after supplied
```

These hosts define possible bridge targets. They are not derived from the
Origin core.

## Impact On Paper Candidates

```text
PC02 strengthened indirectly: the top survivor repeats the ordered L/R core.
PC04 strengthened directly: the residue evidence cluster is now clear.
PC06 sharpened: the state-integral problem is a selector theorem question.
PC01 strengthened: the obstruction pattern repeats across another campaign.
```

## Next Use

Do not treat this campaign as a reason to run broader searches immediately. Its
best use is to stabilize public artifacts:

```text
1. finish PC02 external-review packet
2. build PC04 noncommutative-residue atlas node
3. build PC06 state-integral selector-gap atlas node
4. add failure nodes for zero no-go, commutative cancellation, units, and observable gaps
5. only then decide whether a narrower source audit is justified
```

## Final Verdict

```text
SURVIVES as a map of candidate residue structures and bridge obstructions.
Does not produce an observable.
Does not change CLAIMS.md.
```
