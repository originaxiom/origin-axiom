# THE CLOSURE CAMPAIGN — registration + pre-registrations (2026-07-09)

**Owner-approved (2026-07-09): "launch a campaign to tackle each of them and fully close the program
properly."** Scope: the four gates (`docs/OPEN_PROBLEMS.md` A–D), the two APPEARS-NOVEL candidates
(B491 R1/R2), and the standing tower prize (ρ_n / μ_d). Governed by `GOVERNANCE.md`; staged rigor per
`METHOD.md`; every probe below is **pre-registered here before compute** (§6.2) with its outcome enum;
kills require residual-hints; tombstones are not re-chased; foreign controls are mandatory (the B438
rule). Firewall unchanged: nothing here promotes physics; mathematics promotes only through the §5 gates.

**The honest closure criterion (K020 §6a applied to ourselves).** A true boundary and an over-fit frame
are indistinguishable from inside; therefore this campaign *cannot* certify novelty or finality by
internal computation. Its terminal deliverable is: everything computable computed to its labeled end,
the repo green and internally consistent, the Origin-Postulate refutation clause formally adjudicated
(owner present), and the **external-contact package regenerated from corrected sources**. The send
remains owner-gated; "fully closed" without the external read is not claimable, and this document says
so in advance.

---

## Phase 0 — hygiene (this session; no science)
Gates green (atlas re-mined 468; attribution reword; review anchor restored); locks B477–B492 added;
staleness fixes (CLOSURE promotions sentence, INDEX counts, frontier-README B92, knowledge INDEX
K021–K024, board dedupe); the B106/B108 **witness pin** (see F9 note in the B106 probe docstring);
`SPECIALIST_NOTE_R1` + B479 field correction (ℚ(√41) → degree-4 over ℚ(√5)) propagated.

## Phase 1 — the derivability duels (assume-derivable, try to derive)

**CL-1a (R2 duel) — the seam √−15 vanishing vs CRT/Galois descent.**
*Question:* does a CRT/Galois-descent argument (through the exact tensor factorization C = C₃·C₅, P66,
and the character-gated orders, P68) derive the one-node-absent subfield-lattice vanishing of the seam's
√−15 channel (B459/P1)?
*Outcome enum (committed):* **DERIVED** (a proof + the mandatory out-of-sample check: predict the channel
content of a never-computed pair before computing it — the B390 idiom; R2's novelty then *closes* as a
corollary and the P1 paper is rescoped) · **OBSTRUCTED** (a proof that the descent route cannot produce
the vanishing, extending B491's signed-permutation obstruction; novelty *sharpens*) · **PARTIAL** (named
residue → DORMANT hint, not a kill).
*Controls:* reproduce the banked bright/dark classification 12/12 before any new derivation step is trusted.

**CL-1b (R1 duel) — the held-breath field law vs Cantat's fixed-curve method.**
*Question:* does Cantat's (2009) fixed-curve technique, applied to the finite-order σ_m at κ = −2, yield
the divisor-indexed field law ℚ(τ_d, √(τ_d²(τ_d²−8))) (B479, as corrected 2026-07-09) as an easy corollary?
*Outcome enum:* **COROLLARY** (worked derivation; R1's novelty closes to a remark, the P3 paper rescopes to
the divisor-indexing observation) · **NOT-BY-THAT-ROUTE** (the obstruction named — e.g. the fixed *curve*
degenerates at the elliptic points and the method sees only the quartic's ℚ(τ_d)-rational part) ·
**PARTIAL** (→ DORMANT).
*Control:* first reproduce Cantat's own ℚ(√17) for the pseudo-Anosov [[2,1],[1,1]] by the same pipeline;
the duel is invalid if the control fails.

## Phase 2 — Gate A: the class-by-class sealing sweep

One probe per remaining invariant class; per-probe outcome enum: **SEALED** (the class is a Galois orbit
of the object's arithmetic — the B330 uniform strategy) · **COUNTEREXAMPLE** (a genuine forced choice —
this refutes S032-A and is an immediate owner-escalation event) · **TOOL-BLOCKED** (named tool, parked
for no-tool, not handed off as "prove it").
Classes, in order: (2a) nonabelian adjoint/Ptolemy torsion across all character-variety components
(Fox/Wada route, the B425 method); (2b) Chern–Simons/η beyond the banked CS=0; (2c) irregular covers
index 7–8 (compute budget capped; horizon extension, not a theorem); (2d) SL(3) gluing-variety
invariants; (2e) extended-Bloch/K₃^ind beyond the object's own class. If all computable classes seal:
Gate A's honest terminal is "**sealed at every computable horizon; the universal statement remains
open**" — C-guardrail language, not victory-by-exhaustion.

## Phase 3 — Gates B/C/D: in-sandbox reductions (CLOSED is not on these enums)

**CL-3B (Gate B sub-pieces):** the σ-stability register (H103/H104): bounded enumeration hunting a
chiral (non-σ-stable) 2T↪E₆ embedding; third-order {4,8} integrability (extend B352's cup-product to the
Massey bracket); one attempt at the geometric θ-identification. Enum per piece: **REDUCED** (gate
statement sharpened + fresh data) · **NEGATIVE** (escape closed, firewall confirmed) · **TOOL-BLOCKED**.
**CL-3C (Gate C):** compute the commensurator ℤ/3 action explicitly on the three 27-copies (SnapPy +
PGL(2,𝒪₋₃) index-12 structure) and test it against the *written* refutation condition. Enum: **OPENS**
(three symmetric copies of the 27 — owner escalation) · **CLOSES** (trinification factors / non-symmetric
— the wrong 3, banked) · **BLOCKED** (the orbifold computation names its missing tool).
**CL-3D (Gate D):** the non-self-adjoint numerics package at κ = √3·e^{±iπ/6}: pseudospectra of the
non-Hermitian cocycle, escape/non-escaping-set structure, dimension estimates — the conjecture-shaped
data a spectral theorist needs. Enum: **DATA-BANKED** (+ a precisely posed conjecture) · **SURPRISE**
(structure contradicting the DG picture → H1 discipline, owner present before voicing).

## Phase 4 — the tower time-box

One attempt at μ_d for all n via ρ_n (the B103 fourth route + the contested B62 char(M²)² piece),
**hard time-box** (one session). Enum: **PROVED** · **SHARPER-REDUCTION** · **UNCHANGED** (banked
without shame; the prize has survived four routes already).

## Phase 5 — the close (owner present)

(i) The Origin-Postulate refutation-clause adjudication: by the postulate's own written criterion, has
the interface search cleared any four-part-bar entry, or has every physics-producing entry required
fresh fitted input? A formal verdict, logged — either way. (ii) THE CLOSURE document (successor to
`docs/CLOSURE_2026-07-05.md`) with the campaign's full outcome table. (iii) The external-contact
package: the four gate briefs + the two duels' terminal statements, regenerated from corrected sources,
consistency-checked against the ledgers (the F4 rule: no known-corrected error leaves the house). Send:
owner-gated, explicitly out of campaign scope.

---
*Registered on the live board (`docs/CAMPAIGN_STATUS.md`). B-numbers are assigned at run time from the
next free ID; this document is the pre-registration of record for each probe above.*
