# Repo audit — 2026-07-05 (the owner's two-goal audit)

*Goal 1: find what is stale / incomplete / redundant / erroneous. Goal 2: find uncomputed
leads now unblockable by data we already have, plus hints for the paths that emerge from
negatives. Four inputs reconciled here: three in-repo auditors (hygiene / correctness / leads)
and Chat-1's independent audit. Everything below is banked; the firewall is untouched (no
physics to CLAIMS).*

---

## 0. The one correctness catch that mattered (→ B438)

**B436's "arithmetically special child" shipped with only the SLOPE control and skipped its own
pre-registered FOREIGN control** — the exact omission that had already sunk B437's "golden
return." Running the missing control (B438):

| feature | 4₁(5,1) child | 5₂(5,1) foreign | 6₁ / m003 / m007 | trefoil |
|---|---|---|---|---|
| trace field | x⁴−x−1, **−283** | x⁴−x−1, **−283** ✅ shared | different fields | (not hyperbolic) |
| total abelian torsion | **121** | **121** ✅ shared | — | 1 |

**Nothing about the child is figure-eight-UNIQUE.** {4₁, 5₂} form a commensurability class that
shares *both* the exit field and the torsion value; the value differs only for a genuinely
different parent (trefoil → 1). The **Inversion Law is now three-tiered**:

1. **numerator-forced** — every knot at slope 5: H₁ = ℤ/5, the √5 character field, the 26
   abelian E₆ vacua.
2. **commensurability-shared** — 4₁ ≈ 5₂ (not generic): the −283 field, the 121 value.
3. **figure-eight-UNIQUE** — **none found.**

The forced birth records the parent's *commensurability class*, not its individual identity.
**Break criterion raised:** a genuine inheritance break must distinguish 4₁ from **5₂** (its
neighbor), not merely from the trefoil or a generic knot — so **C3's interior→exit control set
now includes 5₂**, not just the trefoil. Corrections applied to B436 (FINDINGS reading) and
B437 (the Lucas VALUE is Δ_K-specific, not "family-generic"; only the FORMULA is generic).

---

## 1. Hygiene fixed in this pass

- **B437** — title/status de-"GOLDEN RETURN"-ed to the retraction; the "control dichotomy" line
  corrected (formula generic, value knot-specific-but-class-shared); the arithmeticity no longer
  billed as "the genuinely special figure-eight-specific fact."
- **B435** — the ℤ/5 birthmark demoted to numerator-forced; the count pinned at **26** (25
  nontrivial + trivial); founding.py print label reconciled.
- **B436** — title/status carry the B438 refinement; the foreign-control omission is recorded.
- **S-number collisions resolved** — three files had grabbed already-assigned capstone numbers.
  Renumbered to the next free slots and re-indexed in the CATALOG (canonical capstones keep
  their numbers):
  - `S042_pmns_numerology_verdict` → **S050** (S042 = curvature-sign transition stays).
  - `S043_energy_package_verdicts` → **S051** (S043 = topological-Λ/DESI stays).
  - `S044_the_structure_reframe` → **S052** (S044 = Alexander–Kodama stays).
  Ambiguous citations disambiguated (the "numerology bar / derivation-or-nothing" → S050; the
  "structure reframe (S044/B414)" → S052).
- **e₃ guard** — `frontier/B399_wall_scale/triple_cubic.json` was an UNCONVERGED mid-run CRT
  snapshot (its denominator collides with a listed prime — the classic under-determined-CRT
  artifact). Stamped `_status: UNSTABLE`; the collection sentinel (task #200) is still running.
  The LEAD_REGISTER "now?=YES" for B399 correctly means *computable now*, not *done* — left as is.
- **Board** — CAMPAIGN_STATUS now names THE CHILD PROGRAM (was still on the completed DESTINATION
  ATLAS), lists what is actually RUNNING (m=3 symbolic, e₃ sentinel), and carries B429–B438 in
  LAST BANKED; the stale "[PR pending]" dropped from B425. PROGRESS_LOG extended B429→B438.

## 1b. Hygiene noted, NOT yet fixed (scoped follow-ups)

- **CATALOG** does not yet backfill index rows for S045–S049 (live files). Low priority.
- **OPEN_PROBLEMS.md Gate B** predates B426–B438 (no explicit "Gate B" heading found on the
  quick pass — needs a read-through, not a blind edit). Queued.
- Chat-1's reading notes (kept as leads, not defects): **L43** "the child is the 3d-gravity
  side" (a framing to test at C4/C5, not a correction); **H103/H104** σ-stability as a candidate
  interface-break locus (a C3 hypothesis); **wake S026** (the state integral — a possible
  Meyerhoff partition-function computation once C3's flat connections are in hand).

---

## 2. Leads now unblockable by data we already hold (Goal 2)

Ranked by "settles something × cheap × unblocked-right-now." The unifying fact: **Sage 10.9 +
snappy 3.3.2 are in-env**, which retroactively unblocks several "needs-Sage" verdicts, and the
**B425 CRT/Fox engine** + the **B433 3d-3d calibration** turn three register leads into
compute-now jobs.

| lead | what a result settles | why unblocked now |
|---|---|---|
| **Cluster A — metallic A-polynomial (m=2,3)** | the child/parent A-poly family; B433 already did m=1 by CRT | the m=3 symbolic cross-resultant is *already running*; extend the CRT route to m=2 (silver) |
| **L54 — gate-A adjoint/Ptolemy torsion** | deepens the value-firewall's one in-sandbox gate | B425 already demonstrated the exact Fox engine on this class |
| **B399 e₃** | fires the B403/B405/B406 sentinel battery | sentinel running; needs convergence, not new tooling |
| **L34 — silver/bronze Weil ζ / aₚ / conductor** | the character variety's arithmetic | point-counting over F_p is a small script on banked A-polys |
| **the "needs-Sage" trivials (B96, B140, B153/154, L11)** | small verdicts left open for lack of Sage | Sage is in-env; re-run |

## 3. The path that emerges from the negatives (the strategic read)

Every child result so far confirms the **Inversion Law**: the object launders its own identity
through surgery. This is not a dead end — it *sharpens the target*. The three-tier hierarchy
says a genuine physics-relevant "break" cannot come from any invariant that a commensurability
neighbor shares. That eliminates whole classes of candidate signals in advance and points the
campaign at exactly two channels that are still knot-*and*-class-discriminating:

- **C3 — the SL(2) vacuum book** (next). The irreducible flat connections at M⁵L=1 are a
  discrete spectrum whose *field content per vacuum* is not obviously a commensurability
  invariant. This is the first place tier-3 uniqueness could appear. Run 5₂ as the tight
  control (not just the trefoil).
- **C5 — WRT via surgery** (the second decisive channel). The quantum invariant τ_k(child) is
  built from the parent's *colored Jones*, which is knot-specific in a way the trace field is
  not. "What language does the child speak at 5|k — golden, Eisenstein, −283, or new?" is the
  sharpest tier-3 question the campaign can pose.

If both channels also come back commensurability-shared, the honest terminal statement is
strong and publishable: **surgery launders identity — the object's forced child inherits its
parent's commensurability class and nothing finer, so no figure-eight-specific structure
survives the birth.** That is the Child Atlas verdict (Paper-3 seed) if the bar is not cleared.

---

*Provenance: B438 (foreign_control.py → foreign_control.json; test_b438, 3/3). Board:
CAMPAIGN_STATUS.md, PROGRESS_LOG.md. Renames via git mv; CATALOG index note updated.
Firewall intact — nothing here promotes to CLAIMS.*
