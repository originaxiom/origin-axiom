# THE ROLES — a measured accounting

**Date:** 2026-09-12 · **Seat:** cc · **Status:** a measurement, not a claim. Nothing here promotes
to `CLAIMS.md`.

The owner asked whether phenomena foundational to the programme — *feedback mechanisms, the universe
as a closed Markov blanket, least action, the substitution rule itself* — **"got lost or cut at some
point"**, and what roles **the shadow, the other faces, the child, the product, and the feedback of
all interactions** actually play; and whether **"something like qualia math"** is needed for
completion.

This answers by measurement. Each row was swept with `already_banked`, then the hits were read to
their claim lines — **not their titles**, which is the failure mode B1341's addendum records.

## The finding, in one line

**Nothing was cut.** Every named role is either *answered and unjoined*, *answered and fenced*, or
*never asked*. Not one was deleted. The programme's loss mechanism is **not deletion; it is
non-citation** — the same mechanism B1010 measured on the κ-unification, B1338 on the
anti-rediscovery instrument, B1339 on the chain status, and B1341 on itself — **twice**, the second time on this very document, whose “the product has no referent” row was wrong within hours.

## The table

| role | corpus presence | where it actually lives | state |
|---|---|---|---|
| **least action** | 135 hits, **0** arcs joining it to the object (and **B6**, an Euler–Lagrange equation, OPEN since week one, on zero surfaces — B1247's finding) | **B1341** (today): `T₁` is generated by no discrete Lagrangian, `T₁²` is; and B448 had already shown `T₁²` **is** the figure-eight monodromy | **was never asked**, now answered. The fact that settles it (B21, anti-Poisson) was banked in the twenties |
| **feedback** | **4 hits in 1300+ arcs** | **B20/B37** — defined operationally, *measured* (the trace map has feedback **and** an invariant), then **quarantined at the awareness reading only**. Campaign row **X32** | **answered, fenced, and its mechanism unused for 1300 arcs**. See the correction below |
| **the substitution rule** | alive and load-bearing | `THE_FRAMEWORK`, `THE_LADDER`; and its abelianisation `L·P` is the det = −1 that B1341 shows obstructs the action | **never lost** — it is the spine |
| **Markov blanket** | 21 hits, 3 arcs | **B1169** (OPEN): *"the Markov blanket IS the cusp torus (S072), canonical shape 2√3 i (B1167), mirror-EVEN hence orientation-blind — aware-but-choiceless"*. Campaign row **X31** | **answered, OPEN, unverified by other seats** |
| **qualia** | **4 hits** | **B1169** (OPEN) — the parity facts: Vol & cusp shape mirror-EVEN, CS & orientation mirror-ODD | **one OPEN synthesis, all seats to verify.** Not refuted, not earned |
| **the other faces** | load-bearing | **B258**: one object, three faces — geometry, arithmetic, quantum. The quantum face has been asked **one** question (chirality, B1305: "2 or nothing"). A **fourth face** is registered at CITED grade from cc3 (`B1200/ADDENDUM_cc3_B8154_fourth_face.md`) | **the quantum face is the least-worked of the three** |
| **the child** | | **B443** (NEGATIVE): *"the child program cleared no emergence bar: every floor is numerator-forced or shared with 5₂, and nothing figure-eight-unique was found"* | **asked and answered NEGATIVE.** Not a hole |
| **the shadow** | 29 hits, **0** settled arcs | **two unrelated technical senses**: the *shadow class* `(1,0)` of conjugation in the anchor rule (`CROSSING_REQUIREMENTS` R11), and *"the smallness of Λ is the shadow of the trivial vacuum's degeneracy"* (`CRYSTALLIZATION`, whose value formula is **dead**) | **not one role — a word doing two jobs.** No arc owns it |
| **the product** | 74 hits, **0** settled arcs | **CORRECTED same day (B1341 second addendum): it HAS a referent.** *"The product of creation is inversion"* (`λ·σ(λ) = −1`) is `det(L·P) = φ·(−1/φ) = −1` — the product of **B6**'s two stationary points, the determinant of **B448**'s half-monodromy, and the obstruction **B1341** proves forbids a discrete Lagrangian | **located.** The phrase was never empty — it was **unindexed**, which is this table's own finding applied to this table |

## Two corrections the measurement forces

**1. X32's premise is now doubtful.** Campaign row 22 reads: *"the object is **timeless** with
**trivial modular flow**, so feedback needs a carrier that is not dynamics — say what it is before
probing."* **B1341 supplies a carrier that IS a dynamics**: the figure-eight monodromy `T₁²`, acting
on the character variety, with a conserved quantity (Fricke–Vogt) and a least-action principle. The
object is timeless in the *modular-flow* sense; it is **not** without dynamics. X32 should be
re-posed against the monodromy rather than against a search for a non-dynamical carrier.

**2. The Markov conflation hazard is real and is hereby honoured.** X31 warns: *"the corpus is full
of Markov **triples**, a different object."* It is right, and B1341 walked straight into the
adjacency. To state it flatly:

> **The Markov *surface* (B448, B1341) and the Markov *blanket* (B1169) are unrelated mathematics
> that share a surname.** The surface is Markov's Diophantine equation `a²+b²+c² = 3abc`, the phase
> space of the trace map. The blanket is the statistical separation of an inside from an outside,
> identified in B1169 with the cusp torus. **No result about one is evidence for the other**, and
> any future arc that links them must do so by an exhibited map, not by the word.

## The invoice question

The owner asked whether **"a complete accounted ledger/invoice of everything that happened"** is a
price the programme must pay at the closing. The record's own answer, assembled:

* the **inputs** are counted (`INPUT_COMPLETENESS_LEDGER`, `docs/PRICED_DOORS.md`), and the count is
  a **lower bound** — B1231 says so explicitly, because **I-13** (the master identification) has been
  performed implicitly since the first physics-flavoured cell;
* the **identifications** are counted (`IDENTIFICATION_LEDGER`: UNEARNED 9, REFUTED 4, EARNED 4);
* the **errors** are counted (`ERROR_LEDGER`);
* what is **not** counted anywhere is **what each closed arc emitted that no later arc consumed** —
  which is exactly the quantity every loss above is an instance of.

So the honest answer is: the invoice exists for inputs, identifications and errors, and **does not
exist for unconsumed outputs**. That missing column is the programme's measured loss mechanism, and
it is the one ledger the record has never built.

## Do we need "qualia math"?

Not to finish the physics. The rows that block the main goal are values (0 of 19), chirality (a
tested wall), and a 4d action (still absent). None of them is an observer-side question, and B1169
is OPEN rather than blocking.

But the record does not get to call the crossing finished without it, and it says so in its own
currency: **I-13**, the listener map `u` — *"a structural analogue ≡ the physical quantity of that
name, on the observer's side"* — is **UNEARNED** and is the register's own name for the crossing
cell. Whatever "qualia math" would be, the record has already priced its slot and left it unpaid.
