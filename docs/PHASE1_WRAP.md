# PHASE 1 WRAP — the mechanical closure lane (Closure Program)

*2026-07-23. Five compute waves (B771 W1–W5) + the adequacy audit (B772) + the
chord re-computation (B773), all under sealed preregs, every verdict adversarially
verified before banking. This is the Phase-1 deliverable.*

## The numbers
- **44 distinct Phase-1 targets touched → 38 net-banked, 5 genuinely open.**
- Positive closures (identifications, theorems, fixes): the majority.
- Negatives/refutations/bounded: held, and one OVERTURNED (see B773).
- Every banked cell has a re-runnable compute.py + an independent verify pass;
  25+ compute-grade locks across the wave suite.

## The headline results
1. **e₃ = cos(2π/9)/864 exactly** — the level-1215 triple IS the ζ₂₇ Chebyshev
   trisection rung (three independent routes). The census's top lead, closed positive.
2. **B685's kill formalized all-n** conditional on exactly the GSWZ Habiro-ring
   theorem (obstruction computed, not asserted); depth u⁵→u⁷ + verified prediction.
3. **The D4 ceiling constant identified**: √(5+2√3−2√2−√6), deg-8 minpoly, 120 digits.
4. **The level-27 prediction refuted exactly** — resolvent = 3·ζ_{3^k}⁻¹, norm frozen 9.
5. **L39 promoted to theorem** (all-t Gauss-sum); **H103 refuted** (chiral 2T exists);
   **Meyerhoff ±5 = oriented mirror pair**; **the metallic genus sequence (3,1,41)** —
   the genus GROWS at m=3.
6. **A banked negative OVERTURNED** (B773): W4-304's "level-45 identically zero" was
   an even/odd trace CANCELLATION, not absence — the θ-odd sector carries tr_odd=1/4.

## The methodological result (B772 → B773)
The owner's "are we computing properly?" produced a real finding: 6/14 banked
negatives compute in the trace/abelianized projection that is BLIND (B766 θ-T6) to
the θ-odd/chord sector where the object's live structure sits. B773 proved this
costs us — W4-304 was a projection artifact. The verify layer held symmetrically
(a false chord-positive, W3-082c, caught with equal force). New error class E26
(blind-projection negative). **This justifies a chord-level pass over the OTHER
banked trace-level negatives — the Phase-1 → Phase-2 open question.**

## The 5 genuinely-open Phase-1 residuals
- **W5-139**: genus=41 verified — a dead verdict-branch to strip (cosmetic).
- **W5-100**: D₀ convergence verified — a decorative check to strip (cosmetic).
- **W3-067c / W4-115c** (chord-carries): a full-trace-field cert / a fabricated
  string — both wall-hardenings pending a clean pass.
- **W3-082c** (chord-carry): NEEDS-SPECIALIST (Kim's arithmetic-CS bridge uncomputed).

## Event-driven / EXTERNAL (documented, not blocking)
- **R29-3 (B500 stragglers)**: the child-hunt stragglers hit the IN-SANDBOX wall
  (35 of 150 timeout/never-reached; PARI stack + 3600s limits; the 1 resolved =
  DFDMM child ABSENT at eliminant deg 74). EXTERNAL/specialist — a compute wall, not
  a math wall. Raw logs kept uncommitted for human review (exploratory-numerics rule).
- **R29-6 mutation test** fires at Review 30; the hygiene arc (gate/seal residuals)
  and the sentinels remain queued.

## What Phase 1 did NOT touch (Phases 2–5)
The structural substance — the T1-mover hunt, the aAbB derivation door, H-WORD-WELD,
the enumeration + permutation null, D₅=Spin(10) — plus the correspondence (Phase 3),
the walls (Phase 4), and the capstone (Phase 5). Phase 1 was mechanical closure only.
