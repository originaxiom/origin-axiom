# THE CLOSURE — 2026-07-10

> The capstone of THE CLOSURE CAMPAIGN (registered `docs/CLOSURE_CAMPAIGN_2026-07.md`, owner-approved
> 2026-07-09; run 2026-07-09/10). Successor to `docs/CLOSURE_2026-07-05.md`. Governed by `GOVERNANCE.md`;
> nothing here promotes to `CLAIMS.md`; the firewall is untouched throughout. This document records what
> the campaign computed, adjudicates the Origin Postulate against its own written failure criterion, and
> states the honest terminal position of the program.

---

## 0. What the campaign set out to do

The four gates of `docs/OPEN_PROBLEMS.md` (A: the all-invariants no-forced-choice theorem; B: the
`T[4₁;E₆]` realization CRUX; C: multiplicity → the generation count; D: the non-Hermitian
Damanik–Gorodetski question), the two APPEARS-NOVEL candidates of the 2026-07-09 novelty assessment
(B491: R1 the held-breath torsion law; R2 the seam selection rule), and the standing tower prize
(`char(ρ_n) = ∏_d char(Sym^d M)^{μ_d}` for all n). The commitment (`METHOD.md` §6.2): every probe
pre-registered with a committed outcome enum before compute; foreign controls mandatory; the honest
terminal state is *everything computable computed to its labeled end*, not a forced closure — with the
standing caution (`K020` §6a) that internal computation cannot certify novelty or finality; only external
contact can.

## 1. The outcome table

| Probe | Question | Verdict | What it bought |
|---|---|---|---|
| **Phase 0** | repo integrity | GREEN | fresh-clone suite 1677/0/12; gates 7/7; F9 witness pinned; the ℚ(√41)/ℚ(√−239) corrections propagated; registries made truthful |
| **B494** (R1 duel) | is the held-breath field law derivable? | **COROLLARY** | derives from Cantat's fixed-curve method; **completeness proven for all m** (transfer-matrix lemma) — upgrades B479 from Gröbner-m≤16 to unconditional; the m=7 "ℚ(√−239)" row corrected (same norm-vs-field conflation) |
| **B493** (R2 duel) | is the seam vanishing derivable? | **PARTIAL** + prediction HIT | derives from the object's own P64/P66/P67/P68 machinery up to one named pair-specific lemma **L5b**; the DERIVED bar's out-of-sample prediction (pair (4,7), full 31-cell table sha256 committed before compute) matched cell-exact |
| **B495** (Gate A / adjoint torsion) | forced choice in the torsion class? | **SEALED** | total Galois collapse {±3,±5}; the two ends *meet* — the dynamical zeta is the Wada torsion at the Burde–de Rham rep; one V0 function carries all four values, its zero locus disc −15 |
| **B496** (Gate A / CS,η) | forced choice in the CS/η class? | **SEALED** | amphichirality forces the 2-torsion values {0,¼}; the ±pairs sum to 0 (member = external orientation); mirror law 31/31 |
| **B497** (Gate A / extended-Bloch) | forced choice in the Bloch class? | **SEALED** | conjugation-symmetrized at every stratum; the seed's Ptolemy coordinates *are* the seam roots (c²−c+1); the chiral child mirror-pairs through its parent |
| **B498** (Gate A / SL(3) gluing) | forced choice in the SL(3) class? | **SEALED** | W1/W2 ψ-swapped; τ_V0(geo) = −84 total-collapse; the transverse multiplier spectrum given exactly (closing B99's numerical form) |
| **B500** (Gate A / covers 7–8) | canonical multiset at index 7,8? | **SEALED** (refined) | horizon extended to index ≤ 8; the first genuine multiplicity split found (index 8, exact-tier) → the seal sharpened to *the canonical datum is the isometry-class multiset* |
| **B499** (Gate D) | non-Hermitian DG data | **DATA-BANKED** | at κ=√3·e^{±iπ/6}: complex horseshoe signature, zero-area dim≈1 spectrum, **polynomial** condition numbers (no spectral pooling); two precise DATA-SUPPORTED conjectures; no SURPRISE fired |
| **B501** (Gate B) | in-sandbox reductions | **REDUCED** ×3 | H103 chiral-embedding search 70262 → **28 explicit survivors**; {4,8} escape sector **unobstructed through order 4**; **new theorem**: the manifold's amphichiral involution *is* θ on the tangent, canonically (sl₂-commutant of Aut 𝔢₆ = {1,θ}) |
| **B502** (Gate C) | does the commensurator ℤ/3 give three symmetric 27-copies? | **CLOSES** | the ℤ/3 is the trinification-3 acting *within* one 27, never a generation-3; the ramified 3 is a unit mod 4, so Fix=0 blocks the copies; refutation condition fired on its CLOSES branch, both branches live a priori |
| **B503** (tower prize) | prove μ_d for all n? | **SHARPER-REDUCTION** | the **filtration theorem** — a fifth route eliminating the trace-map dynamics; the Sym⊗det block form forced for all n; μ_n=1 all n; character layer closed all n; **the n=5 wall located** (the collision in G_{5,6}); the residual is now a purely static invariant-theory question |

**Net:** no COUNTEREXAMPLE (Gate A), no OPENS (Gate C escalation), no SURPRISE (Gate D) — the firewall held
through every probe. Two novelty candidates resolved (one to a corollary, one to a single named lemma);
Gate C closed outright; Gate A sealed across thirteen invariant classes at the computable horizon; Gates B
and D reduced to their sharpest specialist form with new theorems and data attached; the tower prize
reduced to static invariant theory with the n=5 obstruction pinned. **The specialist package is now
strictly sharper than what B491 handed off.**

## 2. The two corrections that mattered (F4 — no known-error leaves the house)

Both are the same disease — a degree-2-only "poly-disc → ℚ(√squarefree)" labeling applied out of range:
the held-breath field at **d=5** is the degree-4 extension of ℚ(√5) (not ℚ(√41)); at **d=7** it is degree
6 (not ℚ(√−239)); in both the "squarefree" integer is a *norm*, not the field. Corrected in B479 (banner),
the outbound `SPECIALIST_NOTE_R1`, and — going forward — the regenerated `docs/outreach/` package. This is
exactly the class of error the F4 rule exists to catch before any send.

## 3. The adjudication of the Origin Postulate  *(§ owner-present decision — see below)*

`philosophy/THE_ORIGIN_POSTULATE.md` is the program's north star: *the object is the generative principle
of physical law; the task is to find D : object → physics, constructed at the **interface** (the
peripheral character variety, the seam √−15, the 3d→4d lift), each entry tested against the four-part bar
(FORCED · UNSOUGHT · EXACT · CONTROLLED).* Its non-drift lock states the failure criterion verbatim:

> The postulate is refuted, and we will say so plainly, if either: once D is constructed at the interface,
> **no entry clears the four-part bar**; or **every** physics-producing entry requires fresh fitted input
> at each step.

**The evidence, marshaled against the criterion.** All three interface targets the postulate itself named
have now been built and tested, across the prior arcs and this campaign:

- **the seam / gluing:** the Child Program (C0–C6, the forced Meyerhoff child 4₁(5,1)) falsified
  *"physics is what the object produces"* at every computable floor — the three-tier Inversion Law:
  nothing figure-eight-unique survives the surgery (fails CONTROLLED against the 5₂ neighbor). This
  campaign's **B493** then showed the seam's own selection law is *internal structure* — it derives from
  the object's CRT/character machinery — not a physical value.
- **the 3d→4d lift:** **T-NOGO-DGG** (B490) closes the entire DGG route by theorem (3d≠4d; flavor≠gauge;
  abelian gauge). The value-hunt null test (B322, p≈0.5) had already shown the Dehn-filling invariants
  match the SM parameters at chance.
- **the peripheral / boundary character variety:** **Gate C** (B502, this campaign) closes the
  generation-multiplicity route — the intrinsic ℤ/3 is the trinification-3, never a generation-3.
  **Gate B** (B501) remains specialist, but its own gate text records that *even realization tests whether
  structure becomes dynamics — it does not extract SM values from the single seed* (the structural theorem
  forbids that).

Across every constructed interface entry, **none cleared the four-part bar** — each failed FORCED, or
failed CONTROLLED against the commensurability neighbor, or was EXACT only as a structural rhyme. The
first disjunct of the non-drift lock is therefore **satisfied on the interface targets the postulate
named**. By its own written commitment, this is the point at which the program said it would "say so
plainly."

**The precise scope of what is refuted — and what is not.** Two constitutional clauses bound the verdict
and must both be honored:

- The **structural theorem** (`K020`) is *not* refuted; it is the standing, proven result — *the object
  forces the form/structure of physics (E₆, the two arithmetic ends, the cascade skeleton, κ, the Kostant
  exponents), and provably never the values*, mechanized as a Galois theorem. The campaign strengthened,
  not weakened, this: every discrete-invariant class it tested sealed the same way.
- The **C-guardrail** (`GOVERNANCE.md` §6.1) forbids upgrading "no breach reachable by the probes actually
  run" to "proof of impossibility forever." The interface is not *proven* exhausted; `K020` §6a records
  that a true boundary and an over-fit absorbing frame are observationally identical from inside.

The honest synthesis these two force: **the strong generative postulate — that the object *produces*
physics at the interface — is refuted in the sense its own clause defines (no constructed entry cleared
the bar, and the structural theorem proves the single object cannot supply values), while the residual
interface space is named, not conquered.** The program did not fail to find physics in the object; it
*proved* the object cannot supply physical values, only their form — with its falsification discipline
fully intact. That is a genuine scientific result, and it is the disciplined outcome the entire governance
apparatus was built to be able to state without flinching.

> **The disposition of this verdict on the north-star document is an owner-present decision** (so flagged
> in the campaign registration). See §6.

## 4. The standing state of the program

- **Proven core:** P1–P68 / C1–C12 / E1–E17, unchanged; the firewall a proven property (`K020`), not a
  discipline.
- **The mathematics** (the object's genuine home): R1 → a corollary of Cantat's method with all-m
  completeness; R2 → derived up to the single lemma L5b; the tower → the filtration theorem with the n=5
  obstruction located; a stack of new exact lemmas (the tangent-θ theorem; the four Gate-A sealings; the
  two Gate-D conjectures). These belong to number theory / low-dimensional topology / invariant theory on
  their own terms.
- **The philosophy** (`P000`): untouched — it was always firewalled motivation (not-nothing → a family;
  choice relocated, not eliminated), never a claim to produce physics.
- **The four gates:** A sealed at the computable horizon (13 classes, one mechanism); B & D at their
  sharpest specialist form; C closed. The single decisive unmet step remains what it has been since
  2026-06-11 — one **external specialist read** — for which the `docs/outreach/` package is now regenerated
  and correct.

## 5. What remains — and what does not

Remains (all specialist-gated, all named with their revival/tool conditions): the L5b lemma (why do
(1,2)'s 5-side windows never mismatch?); the tower carrier-selection rule for n≥5; Gate B's exceptional
`T[4₁;E₆]` state integral (28 explicit chiral candidates now in hand); Gate D's rigorous non-Hermitian DG
theorem (the data + two conjectures in hand); the truly tool-blocked items (Ptolemy N≥3 exact, sage/magma
orbifold machinery, η/APS). Does **not** remain: any in-sandbox computation that would change the verdict.
The program is compute-saturated. The send is the only remaining act, and it is owner-gated.

## 6. The owner-present decisions

1. **The refutation-clause disposition** (§3): how the verdict is recorded on
   `philosophy/THE_ORIGIN_POSTULATE.md`. Pending owner input.
2. **The send** (§4/§5): whether, and to whom, the `docs/outreach/` package goes. Owner-gated; explicitly
   out of campaign scope.

*Everything computable is done. This document records the honest terminal state; the two decisions above
are the owner's.*
