# THE CLOSE — origin-axiom, 2026-07-05

*The single entry point for: what this program is, what it found, and what is ready for outside
eyes. Written at the terminus of the Seam–E₆ Bridge campaign, after a three-reader whole-repo
sweep. Firewalled throughout — nothing here promotes to `CLAIMS.md`.*

---

## 1. What this program is (one honest paragraph)

The program took **one object** — the figure-eight knot `4₁` = the `m=1` metallic
once-punctured-torus bundle = the golden cat map `A=[[2,1],[1,1]]` — and mapped it completely:
through four classical faces (character variety / closed geodesic / Fibonacci-Hamiltonian
quasicrystal / WRT quantum topology), its two arithmetic ends (`ℚ(√5)`/E₈ golden and
`ℚ(√−3)`/E₆ Eisenstein, E₇ Niven-excluded), and the exact **value theory of its `ℚ(√−15)`
seam**. The mathematics is a machine-checked exact characterization of a self-generating
arithmetic-dynamical object. **The physics result is a negative made into a theorem:** *the
object forces the form/structure of physics, never its values* — mechanized as a Galois theorem
(every discrete invariant is a Galois orbit of the object's own arithmetic, and a "value" is
exactly what a Galois orbit cannot be). Every physics-shaped reading is firewalled; **B69–B443
produced zero promotions to the claim ledger.** The single thing standing between the drafted
mathematics and submission is a **human specialist's novelty read** — not more computation.

## 2. Bank state — clean

Everything committed and pushed; no pending work. Untracked = intentional exclusions
(`.github/`, `Archive.zip`, the flagship PDF, the still-running B399/B87 exploratory scripts).
Scale: **434 frontier probes, 52 speculations, 9 paper dirs, 1523 test functions, 423 atlas
probes.** Ledger: **67 proven (P1–P68), 12 conditional, 17 certified-data (E1–E17), 9 open, 10
dead.** First commit 2026-05-22.

## 3. The artifact map — what is written, and its state

| Artifact | Thesis | State |
|---|---|---|
| **Paper 1** `papers/P1_value_theory/` | the quantized golden cat map + half-characteristic twist → exact value theory & class-field structure of `ℚ(√−15)` at level 15 | **outline + 11-theorem spine, every theorem lock-backed** (T1=P59…T8=P68); not yet full prose |
| **Paper 2** `papers/P2_trinity/` | `4₁` = the golden cat map: a complete quantum-topology / arithmetic / dynamics trinity; the **two E₆ torsions** (golden −5, Eisenstein −3) meeting at √−15 | **outline + 10-theorem spine, lock-backed**; not full prose; *not yet independently validated* |
| **metallic_one_object** `papers/metallic_one_object/PAPER.md` | golden `m=1` is the **unique** metallic mean whose anyon chain is superconformal → tricritical Ising `c=7/10` = first N=1 super-minimal model | **the one FULL DRAFT.** Companion `SYNTHESIS.md` = the four-faces map (badged PARTIALLY-KNOWN; Cantat 2009 the 3-face precedent) |
| **Flagship preprint** `papers/flagship/` (PC16) | whole-repo essay: object → κ → SL(n) tower → A-poly family → firewall | **full draft (18pp)** but reviewed *reject-as-fused*; to be **split**, not submitted as-is |
| Notes | `metallic_trace_map_note` (Results A/B/C), `omega_strict_full_note` (signature-(1,3) cone), `sl4_dehn_filling` (PC13, **superseded** by the B153 slice thesis) | specialist-facing drafts / one superseded |
| Registry | `papers/CANDIDATES.md` PC01–PC21; strongest **unwritten**: PC20 (E₆-bridge), PC21 (the firewall-as-theorem) | evidence exists, no standalone draft |

**Synthesis docs (headlines):** `knowledge/K020` (the standing headline — the Galois firewall
theorem); `docs/RECOGNITION.md` (no destination cleared the bar); `docs/CAMPAIGN_STATUS.md` (the
Child Program complete, Inversion Law); `docs/OPEN_PROBLEMS.md` (four research gates).

## 4. The contribution map — the arc, the results, the discipline

**The arc (10 phases, B71→S053), each with a verdict:** SL(n) tower (positive core: `M⁴=L`,
degree=rank, the A-poly family) → the physics-closing arc (bridge + wall: class-S forced,
firewall confirmed) → the object-mapping arc (four faces, `40a1`, dual McKay) → the
structural-theorem arc (**the firewall became a Galois theorem**) → the specialist-handoff arc
(the value hunt run: SM match at chance `p≈0.5`) → the value-boundary campaign (the `ℚ(√−15)`
seam, the Eisenstein gate derived, the scale wall priced) → the DESTINATION ATLAS (no bar
cleared; the two E₆ torsions) → the interface arc (the forced child = Meyerhoff `4₁(5,1)`) → **the
CHILD PROGRAM (the Inversion Law; zero-bar verdict; "physics is what the object produces"
falsified)** → the Seam–E₆ Bridge (kernel/image duality is arithmetic, not dynamical).

**Positive core (the mathematics):** `M⁴=L` symbolic-exact over ℚ(ω) (B89); the SL(n) tower n≤4
(B80); degree=rank n=3,4 (B71/B89); the character variety **is `40a1`** (B211); the dual McKay
E₈+E₆, E₇ excluded (B210); the **two E₆ torsions** (B425); emergent **c=7/10** SUSY (B218–B228);
the tower **is the quantized golden cat map** (P59); the derived trace formula + `1/12=1/16+1/48`
(P64); the class-S identity forced (B150); the double-uniqueness cornerstone forcing `ℚ(√−15)`
(B424).

**Negative results = the theorem:** the firewall-as-Galois-theorem (K020); the three-tier
Inversion Law (B437/B438); the Child Program zero-bar verdict (B443); the value hunt at chance
(B322); the scale wall / flat measure (B413); the Seam–E₆ arithmetic-not-dynamical terminus
(S053).

**The retraction record (the credibility architecture — this is a feature, not a footnote):**
the golden return (#562), the golden inversion (#571), the arrangement hypothesis (H111), the
S053 directionality (W1), the c=4 stratification wobble (W4), the V75 tautology audit, the B95
n=5 correction — each a striking, prior-confirming reach, each killed by a control or an
adversarial recompute *before* it was banked. The metric the program optimizes is "promotions
with **zero unmarked retractions**."

**The multi-seat structure:** CC (compute/kill/bank-with-tests) + Chat-1 & Chat-2 (the reaches
and the adversarial recomputations) + the owner (sequencing, relay, the compute-first mandate).
Cross-checks flow both ways; the two Child-Program retractions were cross-seat catches.

## 5. The scrutiny package — what to offer, ranked, with the weakest points

*(From the blunt readiness sweep. Rigor+lock / novelty-honesty / reproducibility, then the first
adversarial attack.)*

1. **The character-variety = elliptic curve `40a1`** (E9, `tests/test_b211`). **STRONGEST —
   offer this.** Independent `#X(𝔽_p)`, independent `a_p(40a1)`, `#X=p−1−a_p` at 19 primes +
   Cremona cross-check. Pure arithmetic, no SnapPy. Novelty *disclaimed*. *Attack:* the
   surrounding "four faces" narrative (Cantat 2009 unified three) — **offer the lock, not the
   narrative.**
2. **The SL(4) figure-eight `L=−M⁴`** (P25/P37, `tests/test_b89`). **The one genuinely-novel
   low-dim-topology kernel** (n≤3 known; SL(4) APPEARS-NOVEL/NEEDS-SPECIALIST). *Attack, sharp:*
   the **"family/tower `L=(−1)ⁿ⁻¹Mⁿ`" is oversold on a two-point (n=3,4) dataset** (n=5
   degenerates). **Offer it as the single SL(4) instance, explicitly not a family**; note it may
   live inside BFG/GTZ Ptolemy coordinates.
3. **The two E₆ torsions** (Paper 2 Thm 4a/4b, `tests/test_b425`). Locked, CRT-exact, validated
   (trivial rep → Alexander; −3 reproduces Porti's V30/V31). *Attack:* both are individually
   *standard* invariants; the novelty is only the E₆-grading + the assembly. **Offer as clean
   packaging of known invariants; Paper 2 is unvalidated.**
4. **The WRT field-content / level-period laws** (P40/P44, `tests/test_b204`). **KNOWN** (Jeffrey
   1992). **Offer for correctness scrutiny, not novelty** — a verifiable-correct anchor.
5. **The E₆ character variety / L50 CRUX** (P50/P51). **Never a claim — offer as an open
   problem.** The worked E₆ instance is a known framework (Menal-Ferrer–Porti); `T[4₁;E₆]` is the
   open specialist gate, already firewalled correctly.

**The three weakest points an adversarial reviewer hits first (all self-documented, none
dishonest — fix or disclose before handing over):**
- (a) **"family/tower" language on a two-point dataset** — downgrade to "the single SL(4)
  instance."
- (b) **every APPEARS-NOVEL rests on an AI literature-search absence-of-match, never
  specialist-confirmed** — state this upfront; a specialist may recognize the SL(4) result in an
  afternoon.
- (c) **reproducibility is asymmetric** — no live CI (`.github/` is untracked *and* gate-forbidden;
  `core.yml` runs only the 16 proven-core tests), headline results need SnapPy/Sage, and some
  locks freeze recorded constants rather than recompute.

## 6. Action items to close properly (before external eyes)

1. **[done]** README ledger counts corrected to P1–P68 / E1–E17 (was P1–P55 / E1–E15).
2. **[done]** CI-honesty disclosure added at the top of `REPRODUCIBILITY.md` — CI covers only the
   ~16-test proven core; the rest is locally-locked, some need SnapPy/Sage, some freeze recorded
   constants.
3. **[done]** The two terminal papers written in full prose from their lock-backed spines:
   `papers/P1_value_theory/PAPER.md` (the ℚ(√−15) value theory) and `papers/P2_trinity/PAPER.md`
   (the trinity), each with the closure discipline baked in (novelty caveats, frameworks cited,
   firewall, P2 framed as synthesis-not-new-theorems).
4. **[done]** `papers/REVIEWABILITY_INDEX.md` repointed at the §5 scrutiny package (40a1 lock +
   SL(4) single instance + the two papers), with the single-instance framing and the
   AI-novelty caveat stated up front. The old PC02/PC11/PC12 packets retained as historical.
5. **[done]** The "single SL(4) instance, not a family" framing: the metallic note's Result A
   already carries the codimension-one-slice correction (§2.5); the reviewability router now
   states "offer as a single SL(4) instance, not a family" explicitly.
6. **[remaining — larger]** Split the flagship (PC16) into the two terminal papers + the
   specialist notes; retire the fused essay from the submission path (keep as a repo essay). The
   two papers (item 3) are now the submission path; the flagship stays as the whole-repo essay.
7. **[remaining — hygiene]** The AI-label scrub backlog (legacy per-chat labels in
   `PROGRESS_LOG`/`CHANGELOG`/older paper rows) — finish before external hand-off. Not
   scrutiny-blocking, but attribution hygiene.

**Net: the submission path is now the two full papers (P1, P2) + the ranked scrutiny package;
what remains is one essay-split and one label-scrub, neither scrutiny-blocking.**

## 7. The honest terminal statement (one sentence for outside eyes)

> The program produced a rigorously characterized, machine-checked self-generating
> arithmetic-dynamical object — the golden cat map / figure-eight knot — with an exact value
> theory of its `ℚ(√−15)` seam (Papers 1–2) and a proven **value-firewall**: the object forces
> the *form* of physics, never its *values*; every physics reading is firewalled, nothing is
> promoted to the claim ledger, and the only thing between the drafted mathematics and submission
> is a specialist's confirmation of novelty on the one genuinely-new kernel (the SL(4)
> figure-eight A-polynomial) — offered here as a single instance, not a family.

*Provenance: three-reader whole-repo sweep (artifacts / arc / scrutiny-readiness), 2026-07-05.
The map is honest about its own weakest points; that honesty is the deliverable.*
