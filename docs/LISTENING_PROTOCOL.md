# THE LISTENING PROTOCOL — the structural comparator (a standing methodology gate)

*Two-seat instrument, 2026-07-19: cc2 authored on the owner's "go"; cc reviewed,
refined (§8), and banked. Its algebraic spine is the B704 seam. A METHODOLOGY instrument (like the base-rate gate and
COMMS_PROTOCOL), NOT a physics claim. It disciplines HOW the object is compared to
any external structure (the SM, "reality"). Gate 5 stands and is made CONSTRUCTIVE:
this says what WOULD count as the object speaking about reality — structure, never
value — without asserting any such comparison holds. Advisory; cc banks.*

## 0. The thesis it enforces
The object forces FORM, never VALUE (B685; K020). The measurement torsor is
NON-canonical (phase-2 / B701): the object hands you the Galois orbit, never the
point. Therefore any bridge to reality must (a) compare STRUCTURE, not numbers, and
(b) predict the STRUCTURE OF THE ARBITRARINESS, not the values. "The object goes
silent when we speak for it" (B685) is the operational law: asking for a value is
speaking for it.

## 1. The comparison hierarchy (name the rung BEFORE comparing)
Only the top rungs are the object SPEAKING; the bottom are speaking FOR it.
1. **FIELD / REALITY** — does the external structure live in the object's field, on
   an AUDIBLE direction (real quadratic, p≡1 mod 4)? Most rigid; hardest to fake.
2. **TORSOR / GALOIS** — does the external FREEDOM organize as the object's
   non-canonical torsor / the B704 𝔽₂ seam (no canonical origin)? Falsifiable: a
   torsor is or isn't there.
3. **FORM / RELATION** — does the external structure satisfy a relation the object
   FORCES (a sum rule, a selection rule, a factorization), specific enough for a low
   base rate?
4. **SINGLE-RATIO** — number ≈ number. Requires BOTH a base-rate cell AND a forcing
   mechanism. For THIS object B685 denies the value-forcing mechanism ⇒ rung-4
   value-matches are DEAD ON ARRIVAL, however small the σ-distance.
5. **FIT** — free parameters tuned to agree. Meaningless by construction.
**Gate rule:** rung 1–3 may proceed; rung 4 stops unless it clears base-rate AND a
mechanism (it won't, for values); rung 5 never counts.

## 2. The structural vocabulary (what the object actually says — the B704 spine)
The legitimate comparanda (the object's forms):
- **Fields:** ℚ(√5) (hearing, real, AUDIBLE), ℚ(√−3) (being, imaginary), ℚ(√−15)
  (meeting), and the general stage field ℚ(√p*).
- **The seam (B704):** the 𝔽₂-vector space where STAGES are a basis, MEETINGS are
  𝔽₂-sums (the genus bit), with NO canonical origin (B701). Comparisons target
  basis vectors and their sums, not coordinates.
- **Audible directions:** p ≡ 1 mod 4 (real quadratic); the fundamental-UNIT tone
  (p=5 golden, uniquely metallic — cell A). Audibility TYPES the seam basis.
- **Torsors:** the non-canonical Galois torsors (fiber functor = measurement;
  phase-2). The measurement lives in the orbit, never the point.
- **Groups / arithmeticity:** 2I/2T/2O, E₈/E₆, McKay data; torsion-of-shapes
  (golden = the unique arithmetic bundle, cell C).

## 3. The meta-stance (what to predict)
Predict the STRUCTURE OF THE ARBITRARINESS, not the values:
- WHICH external parameters are Galois-related (the form).
- WHAT field / torsor their freedom forms (the shape of the choice).
- The AUDIBLE / inaudible dichotomy (what is even hearable).
The output is NEVER "the object predicts value X." It is "the object forces
structure S; the external system has S or it does not." Reduce the external system
to its convention-INDEPENDENT (Galois-invariant) invariants FIRST — the object only
speaks Galois-invariant truth; comparing to human bookkeeping parameters compares to
a frame, not to reality.

## 4. The gate checklist (mandatory before any object↔reality comparison)
1. NAME the rung (§1). Rung 4–5 → stop per the gate rule.
2. NAME swap-vs-weld and real/imaginary field-type FIRST (E17 — the B702 lesson).
3. Convention / unit-robustness check (the θ₀ trap; the melody/period trap).
4. Galois-invariance: the compared quantity must be convention-free.
5. Base-rate cell for any numeric coincidence (the 7983360 lesson).
6. If it survives 1–5, state it as a FORM match at its rung — never as a value.

## 5. Worked calibration (this session's four cells)
- **GOOD (rung 1–2):** the golden's 3-way uniqueness — fundamental-unit tone (A),
  only arithmetic bundle (C), hears-because-fibered (B). Each answers "what is the
  golden's TYPE"; none is a fitted number. This is the object speaking.
- **TRAP (rung 4):** θ₀ = 2/9. Corrected 7σ → 0.89σ (CONSISTENT) yet STAYS
  firewalled — tautology (Q=2/3) + no forcing mechanism + convention-dependence. The
  σ-number was itself a mis-done ratio comparison; the verdict rests on structure.
  Consistent ≠ meaningful.
- **CONFLATION-AND-FIX (why step §4.2 exists):** B702 compared the silver SWAP tone
  to the golden WELD tone — different objects in different (imaginary vs real)
  fields. Retracted; the audibility law (real weld/character field) is the home.
- **BASE-RATE-DEAD (rung 4 failing §4.5):** 7983360 = |W(E₆)|·154 — a smooth-number
  coincidence, not structure.

## 6. First honest doors (firewalled — each needs a sealed owner-authorized design)
Named, NOT asserted (Gate 5 / Gate 5-SM discipline):
- Rung-1: does any SM sub-structure live on an AUDIBLE direction (a real-quadratic
  field the object forces)? — a field-reality question.
- Rung-2: does the SM's irreducible FREEDOM (the un-derivable parameters) organize as
  a Galois torsor / a B704-seam 𝔽₂ structure with no canonical origin — i.e. is the
  SM's arbitrariness the object's non-canonicity? — the deepest falsifiable door.
- Rung-3: does any SM relation match a sum-rule / selection-rule the object forces?
These are DIRECTIONS for future sealed designs, not results. No SM comparison runs
without a sealed owner-authorized design (Gate 5-SM).

## 7. Firewall
Methodology only. Produces no physics claim. It makes the firewall constructive: it
defines what listening WOULD be (structure, not value), so the program can pursue the
bridge honestly instead of drifting into value-numerology. Companion to the base-rate
gate and COMMS_PROTOCOL; proposed as a standing GATE all seats apply.


## 8. Banking-seat review + refinement (cc)
Reviewed and adopted as a STANDING GATE. Sound, firewall-consistent, and correctly
built on the B704 seam. Two refinements make the top rungs concretely runnable:

**8.1 Rung-1 is falsifiable-TO-PRECISION (the operational test).** "Lives in the
object's field" is not a yes/no on a finite-precision measurement — it is an
ALGEBRAICITY test: run PSLQ (integer-relation / lattice reduction) on the measured,
convention-reduced quantity against a basis of the predicted field ℚ(√p*), bounded to
degree ≤ d and height ≤ H. The FALSIFIER is *precision*: a genuine structural relation
survives more digits; a numerological one breaks at the next digit. Report the digit
at which it holds and the digit budget — a rung-1 "hit" is only meaningful if it
survives beyond the digits used to find it. This is what distinguishes rung 1
(membership, base-rate-immune) from rung 4 (proximity, base-rate-dead): membership in
a fixed low-degree lattice is measure-zero; proximity to a fraction is not.

**8.2 Rung-2 is where B704 actually runs (the deepest door, made concrete).** The
test of "is the SM's arbitrariness the object's non-canonicity?" is: take the SM's
irreducible, un-derivable freedoms (the flavor parameters), reduce them to their
Galois-invariant field data, and ask whether that data organizes as the seam's
𝔽₂-multiquadratic structure (stages = basis, meetings = 𝔽₂-sums, NO canonical origin)
— NOT whether any parameter equals a value. A rung-2 result is a statement about the
SHAPE of the freedom, and it is falsifiable: the freedom has the 𝔽₂/torsor structure
or it does not.

**8.3 Standing status.** Adopted as a companion gate to the mandatory base-rate/
convention gate (COMMS_PROTOCOL §4) and the firewall. Every object↔reality comparison,
any seat, runs the §4 checklist and names its §1 rung first. It makes the firewall
CONSTRUCTIVE without asserting any comparison — the honest form of "how do I listen"
([[speculations/S069]]). No §6 door runs without a sealed, owner-authorized Gate-5-SM
design.
