# THE THEOREM REGISTRY — every theorem and law, mapped for the novelty relaunch

**Created 2026-07-08 (the owner's directive: document everything before the literature
campaign relaunches). Schema per entry: statement / bank / reproducer / lit-status /
search terms for the novelty sweep. Lit-status values: KNOWN (cited, never claimed) ·
DERIVABLE (classical mechanics, banked as exhibit) · NEEDS-LIT (novelty unresolved —
the relaunch targets) · APPEARS-NOVEL (survived a prior sweep). Standing rule: every
future bank that creates a theorem or law adds its line here IN THE SAME PR.**

## Tier 1 — the paper-leading theorems (NEEDS-LIT, highest priority for the relaunch)

| # | statement | bank | reproducer | lit-status / search terms |
|---|---|---|---|---|
| T-UNIQ | tr[A_m,A_n] = 2 − (mn(n−m))²; parabolic ⟺ (m,n) = (1,2). **MECHANISM (P4 panel, verified): for ANY symmetric pair in SL(2), tr[A,B] = 2 − (M₁₂−M₂₁)² with M = AB (since BA = (AB)ᵀ) — the square is the transpose/elliptic involution; the family content is M₁₂−M₂₁ = mn(n−m). Two-line proof; significance weight moves to the Cohn identification** | B471 (+panel) | `chain_verify.py` | **GATED (P4 rounds 1–2)**: PARTIALLY-KNOWN — mechanism classical (Sarnak reciprocal geodesics; Gehring–Martin δ=0 slice; Goldman/Fricke); (1,2) instance VERBATIM in Reutenauer 2009/2019 (the Markoff morphism); books anchor to fixed trace −2, no two-parameter family. OURS: coordinate lemma, metallic parametrization, uniqueness scan (vs Schmutz Schaller 2022, Nielsen) |
| T-MIRROR | **CORRECTED (P4 panel)**: the palindromic-alphabet argument is TWO-BLOCK only (counterexample A₁A₂A₃ = RLRRLLRRRLLL); chain rungs word-mirror verified ≤ 8 (standard-word lemma = the open proof route); word criterion sufficient-not-necessary (the (ℤ/12)² pair); the word-mirror ⟹ amphichiral bridge lemma must be stated | B470 + correction | `hierarchy_verify.py` | **GATED**: PARTIALLY-KNOWN — full criterion proved for closed Sol bundles (Tian–Wang–Wang arXiv:2406.13241, transplants); rev·swap ⟺ conj-to-inverse = Baake–Roberts 1997; symmetry group = GL(2,ℤ)-normalizer (Floyd–Hatcher 1982). OURS: punctured word-form, witnesses, tables |
| T-GIES-FAM | X_m = [[m,1],[1,0]], X_m² = A_m, det −1 ∀m: every metallic bundle orientation-double-covers a non-orientable bundle (m=1: Gieseking; SnapPy gate m000 ✓) | B469 BR2 | `br1_br2.py` | **NEEDS-LIT**: "non-orientable punctured torus bundle orientation double cover", Gieseking family literature |
| T-COLLIDE | 4₁(5,1) ≅ −5₂(5,1) = m003(−2,3); in-window census: 3 collision children incl. the TRIPLE 4₁(1,2) = 5₂(−1,1) = 6₁(1,1) (ℤHS); merge orientation classified | B467 (+census) | `f3_wall.py`, `census.py` | **NEEDS-LIT**: Brakes 1980, Livingston, "knots with common Dehn surgery", "cosmetic surgery pairs twist knots", Whitehead-link symmetry |
| T-BB | the root criterion (tr − 2 = t² AND t \| B − I) — **CORRECTED SCOPE (P4 panel, verified): breathable TRACES = metallic traces t²+2; A_m = the PRINCIPAL breather; family = locus ⟺ h⁺(m²+4) = 1 (counterexamples: A₁³; [[19,30],[12,19]] non-principal at trace 38, ℤ[√10] h = 2)**; chain composites rootless 2–200 certified, beyond = conjecture | B469/B470/B471 + correction | `hierarchy_verify.py` | **GATED**: root criterion EXACTLY Northshield 2010 eq. (7) (cite, not claim); classification = Latimer–MacDuffee verbatim; Sarnak negative-Pell form; Yokoi/Biró for which t. OURS: assembly + h⁺-equivalence (nowhere stated) + principal-breather framing |
| T-BREATH-TORSION | **CORRECTED (re-panel round 2, 2026-07-09, CC-verified in Sage):** σ_m fixes on κ=−2 the order-d torsion characters for divisors d ≥ 3 of m (⊇ PROVEN via a^d=I ⟹ σ_m=swap ⟹ fixed=symmetric locus; the "EXACTLY"/⊆ direction is NOT proven — downgrade to ⊇). Held-breath FIELD: **d=3 ⟹ ℚ(√−7)** (minpoly z²−z+2, genuinely quadratic — τ_3=−1 rational) STANDS; **d=5 is NOT ℚ(√41)** — the order-5 held-breath character has minpoly z⁴−3z³+7z²−4z+4, a **DEGREE-4** field (field disc 5²·41, quadratic subfield **ℚ(√5)**, NO real roots). The "41" was the squarefree part of the poly discriminant 16400=2⁴·5²·41, mislabeled as the field. The "closed-form field Δ_d=τ_d²(τ_d²−8)" gives a clean quadratic field ONLY when τ_d ∈ ℚ; for irrational τ_d (d=5) the character field is degree 4. m∈{1,2,4} breathless stands. | B479 (+ CORRECTION in SCRUTINY_P1P3_round2.md) | `held_breath_mechanism.py` | **NEEDS-LIT + CORRECTED**: closest prior art Cantat 2009 (fixed-curve → field); the d=5 degree-4 field over ℚ(√5) is the honest object |
| T-TWOTEETH | the twisted (det −1) Markov spectrum ∩ (0,3) = **exactly {√5, 2√2}** = {μ(X₁), μ(X₂)} (golden, silver); (2√2, 3) a maximal gap. Via Markov's theorem + axis-form correspondence (disc v²+4) + negative-Pell unit-squeeze (v²−D_m t²=−4 solvable only m=1,2). **A SECOND independent characterization of the (1,2) critical pair** (spectral/Pell, unrelated to the commutator). seat-2 A2, CC-verified (neg-Pell filter spot-checked to m=5741; symbolic B3/A6) | B482 (seat-2 A2) | `verify_seat2.py` | **W0 GATE DONE: PARTIALLY-KNOWN, thin residual** — a one-corollary repackaging of **Andersen–Duke 2019 (Markov spectra for modular billiards) + Markov**. CAVEAT: 2√2 realized by BOTH det(−1) disc-8 AND non-det(−1) disc-32 forms → proof must be about antipalindromic-CF square-roots, not disc v²+4 (seat-2's filter has this gap). CITED, not claimed; at most a one-line remark in P4 |
| T-COMM-UNIFIED | tr[A,B] = 2 − gap²/(det A·det B) for symmetric A,B of ANY determinant (gap = M₁₂−M₂₁, M = AB): unifies P4's Lemma 2.2 (det +1 bodies A_m, gap = mn(n−m)) with the det −1 letters (X_m, gap = m−n) as ONE transpose mechanism. Generalizes T-UNIQ | B482 (seat-2 B3) | `verify_seat2.py` (symbolic, exact 0) | GATED with T-UNIQ (Sarnak/Gehring–Martin/Goldman): the det-general form is the same classical mechanism; the two-parity unification is the framing |

## Tier 2 — the structural theorems (proofs banked; lit-status = classical-anchored)

| # | statement | bank | reproducer | lit-status |
|---|---|---|---|---|
| T-COHN | ⟨A₁,A₂⟩ = ⟨g₁,g₂⟩ = the commutator subgroup of PSL(2,ℤ) (Nielsen identities; balanced words ∈ kernel) | B471 | `chain_verify.py` | KNOWN core (Cohn 1955); the metallic reading NEEDS-LIT |
| T-CHAIN | s_{n+1} = s_ns_{n−1} conserves the Markov cubic; /3 walks the spine 1,2,5,13,194,…; every renormalized pair parabolic; mod-60 state period 20 = ord(W₁) | B471 | `chain_verify.py` | Fricke/Markov classical; the body-tower framing NEEDS-LIT (Zagier gate for constants) |
| T-NORM | N(λ_m) = −1 = det(X_m) (companion matrix); frozen through the φ-power degeneracy (m = 1,4,11,29 = φ^{1,3,5,7}); impossible in imaginary quadratic fields | B469 BR-N | `br_n_norm.py` | DERIVABLE (classical units); the three-register identification is the program's |
| T-HIER | root ⟹ mirror ⟹ balanced ⟹ frozen residue; all strictnesses witnessed exactly; composition: balance always, mirror on palindromic alphabets, root never beyond letters | B470 (PR #625) | `hierarchy_verify.py` | the frame is the program's; components DERIVABLE |
| T-843 | M(1) = Par·W₁·W₂ has spec = ζ₆₀⁸·μ₄, mults (4,4,3,4), M⁴ = ζ₆₀³²·I; the whole l-sweep = SL(2,ℤ/15) class data; sole object input tr(A₁A₂) = 15 | B465 | `exact_engine.py` | DERIVABLE (Weil/Egorov + Gauss sums); Weil-rep character theory cite |
| T-SIGMA | σ exchanges the Dehn-filling pair W₁ ↔ W₂ (= the ℚ(√−7)/D(3,3,4) pair, B444); fixed locus = the p=q line ∋ all-ones triple point; the geometric rep's σ-orbit = the B448 heartbeat pair (3±√−3)/2 | B466 | `sigma_action.py` | mechanism = Gieseking deck action; NEEDS-LIT thin |
| T-LIFT | conj(W(m,1)) = Par·WR₁₄^m·Par·D(m,14): σ's operator lift = the c ↦ −c twist, Par-dressed; two-world, not similarity (+j obstruction T₋ⱼ−Tⱼ = cmj) | B469 Ph2 | `sigma_lift_check.py` | program's own (Weil-rep conjugation standard-shaped) |
| T-P2B | real-form conjugation-stability + parity oddness ⟹ Vol(Vᵢ) = 0 exactly, CS(Vᵢ) ∈ {0,½}: the emptiness is a σ-parity selection rule | B469 Ph2 | FINDINGS §P2b | DERIVABLE from banked anchors (Falbel real-form) |
| T-2REG | det(Par@N) = sign(σ on (ℤ/N)²) = (−1)^{(N−1)/2}: the two-register breath law (levels 15/45/75/225) | B469 BR1 | `br1_br2.py` | DERIVABLE (Jacobi) |
| T-PQB | det(Par·W(w)) = −ω^{#L−#R}; letter tower: Pisano-8 rhythm; body chain: frozen −1, order 60 | B470 RF3 | `rf3_quantum.py` | DERIVABLE (banked det decomposition) |
| T-NOGO-DGG | for any once-punctured-torus bundle M (metallic RᵐLᵐ or cyclic cover), M ↦ T[M] via 3d–3d never gives the SM: (1) T[M] is 3d, SM is 4d; (2) every nonabelian symmetry is flavor not gauge (Gang–Yonekura); (3) the gauge group is abelian U(1)^{N−c} (DGG). Subsumes kills K9–K12. | B490 | per-kill B-nodes; B488 | KNOWN lemmas (DGG 2011; Gang–Yonekura 1803.04009); the subsumption is the program's |
| T-θTANGENT | **(wording corrected 2026-07-14, B570-C1 verifier catch)** the manifold's amphichiral involution acts on the tangent ANTILINEARLY, as conj∘θ (its ℂ-linear part is θ); the ℂ-LINEAR θ itself is realized by the HYPERELLIPTIC involution (B353, gauge-certified 7.1e-102). The sl₂-commutant of Aut(𝔢₆) is {1,θ}, θ = the E₆→F₄ folding (Out E₆ = ℤ/2). The original wording ("amphichiral IS θ") conflated the two involutions: θ is ℂ-linear, the amphichiral map is antilinear (B347 `_AMPHI`, conjugate=True) — B353 and B521 are compatible once linear/antilinear is stated. Corollary (B570-QA): d(σ∘φ⁻¹) = θ at the geometric point — Galois conjugation equals the fold, modulo the object's own mirror, exactly at the tangent. | B521 (audit B501) + B353 + B570-C1/QA | `test_b521.py::test_gateB_theta_is_out_e6`, `test_b570_qa_trichotomy.py`, `test_b570_c1.py::test_amphichiral_and_hyperelliptic_automorphisms_are_different_maps`, `test_b353_geometric_theta_identification.py` | DERIVABLE — Out(E₆)=ℤ/2 classical; the geometric identifications are the program's |
| T-FILTRATION | on 𝔪/𝔪² at x₀, the trace map T_φ preserves the degree filtration (Luna slice + Procesi FFT + BCH); gr_d = indecomposable degree-d invariants of two traceless matrices, acted on by N via GL(2) ⟹ **char(ρ_n) has the Sym⊗det block form for ALL n**, dynamics eliminated; only static multiplicities remain; n=5 wall = the doubled Sym²=char(M²)² isotypic space | B522 (audit B503) | `test_b522.py`, `verify_filtration.py` | SHARPER-REDUCTION (probe-writeup strength; standard ingredients Luna/Procesi/BCH); first arm + character layer PROVED all n; NEEDS-LIT for the catalog-grade proof |
| T-L1-IWIP | the Level-1 automorphism φ:a→abccd,b→acd,c→abcd,d→ac ∈ Aut(F₄) is **iwip** (train_track/Bestvina–Handel, validated); λ(φ)=β=3.6762 ≠ λ(φ⁻¹)=3.0523 (verified independently) ⟹ **not geometric** (Handel–Mosher) ⟹ **F₄⋊φℤ word-hyperbolic** (Brinkmann) and **NOT a 3-manifold group** (Stallings) | B524 | `test_b524.py`, `iwip_certificate.py`, `verify_dilatation.py` | KNOWN framework (Bestvina–Handel/Brinkmann/Stallings); the specific certificate is the program's. Level-1 group-theory SETTLED |

## Tier 3 — the banked laws with controls (pre-registry campaign results)

| # | law | bank | lit-status |
|---|---|---|---|
| L-INV | the Inversion Law (3 tiers, 7 floors; children class-generic; golden absent in 4₁'s child) | B435–B443 | program's own; frame paper 2 |
| L-SEAM | the seam form (44 values ℚ(√5,√−3); spectral σ₁=σ₂=1/24; cornerstone ℚ(√−15); exchange identity) | B358–B367 | program's own; paper 1 |
| L-KLEIN | 5 vanishing patterns = the subfield lattice; tier tables are pair/address-class data | B459 (+B468) | equivariance-mechanism DERIVABLE; counts = data |
| L-COND | seam level = conductor of compositum(geometry, dynamics); Alexander = monodromy charpoly (fibered) | B449 | classical-anchored; the seam framing program's own |
| L-CENT | C_𝔢₆(principal sl2) = 0 exact; long-root control dim 35 | B463 | Kostant (cited) |
| L-TOWER | N·Var = S₅(m)·C₃ via character-twisted CRT bases | B446 | APPARENTLY-UNWRITTEN (prior lit-gate) |
| L-ESC | γ(λ=3) = 0.445(6) three-method; B186's 0.51 = early-window bias (corrected); grammar ≠ full 2-shift | B451 | Bowen–Ruelle classical; the correction is the program's record |
| L-2STREAM | level-2 arithmetic: trace fields ESCALATE per rung (4/8/12; 14; >32) while scale fields stay Markov-quadratic | B470 RF2 | **NEEDS-LIT**: "trace fields punctured torus bundles word length", Guéritaud–Futer |

| T-KQ | the quantum commutator table; **THE CLOSURE THEOREM [W₁²,W₂³] = I** (CRT centrality); Q₈/SL(2,5) images; κ_q(1,1) = −1 | B472 | `kq_verify.py` | **GATED (2 rounds)**: iff = corollary of published halves (⟸ KR Cor. 6/Kelmer; ⟹ Appleby 2005 odd-dim injectivity — DOWNGRADE accepted); table/Q₈×SL(2,5) assembly/closure address/divisor lattice = ours scoped; magnitude law = Howe (verified 25/25) |
| T-MASTER | **the master theorem**: κ_q = ε(jl)·χ₅ (two characters: Q₈-parity × mod-5 closure), and BOTH κ_q and the seam tier factor through the divisor pair (gcd(x,20), gcd(y,12)) — the entire selection architecture = two functions on the 36-cell divisor lattice of the clock orders; B474's laws = finite cell checks | B474 | `cross_table.py` + master table, locks | the co-factorization is the program's own; divisor-lattice selection rules NEEDS-LIT (Paper 1/4 spine) |

## Constants awaiting identification (the relaunch's inverse-symbolic targets)

vol/letter of the letter tower **c = 0.934102018057787980264187790656** (28 digits via
the additivity extrapolation; PSLQ NEGATIVE vs golden-sector Lobachevsky basis singly+pairs;
candidate closed form = the Bloch-Wigner hull average over the Farey shapes — OPEN; the
companion ADDITIVITY LAW: tower volume defect < 1e-27 by n=13, doubly-exponential decay —
NEEDS-LIT vs Brock/Guéritaud) · torsion temperature
**0.6295727/syllable** (= 1.2591398/letter; λ relation λ_CC = λ_Chat2^φ) · λ_chain
**1.57705744122666946… per R/L letter** (25 digits; PSLQ excludes integer relations of degree ≤ 8 at coefficient height ≤ 10⁴ — a HEIGHT-BOUNDED exclusion, not a non-algebraicity certificate — P4-panel phrasing correction;
Zagier gate). **CONSTANTS DEFLATION (2026-07-08, seat-2's kill of its own conjecture): the
torsion temperature IS λ_chain in a different Fibonacci normalization (the φ/2 relation is
bookkeeping) — the ledger holds exactly TWO genuine constants: λ_chain and c.** · the B451 resonance spectrum — NOW COMPUTED at certified truncation N = 8: leading 0.4415
(= the escape rate ✓), **second resonance REAL NEGATIVE, rate 0.70(8)**, third a complex
pair near 0.89; certified primitive table {2,4,5,6,7,8 → 2,1,2,3,4,5} with all periodic
points proven real and simple; the algebra recovered one orbit numerics missed (|Λ| = 915).

## Literature anchors received (seat-1's sweep, 2026-07-08)

Aigner 2013 · Reutenauer 2018 · Cusick–Flahive 1989 (the three books; the Cohn stage and
the Markov-trace/commutator-subgroup facts are fully classical — T-COHN's core is KNOWN).
Guéritaud (Annals 2006, punctured-torus-bundle volumes via the Farey triangulation; the
tower's volume law lives inside it). **Pandey–Wong: the Bonahon–Wong–Yang volume
conjecture is PROVED for the LR (figure-eight) once-punctured-torus bundle** — directly
adjacent to RF3's quantum tower; the Pisano residue rhythm should be read against the
BWY asymptotics (registry pointer for RF3's continuation). New tower observation logged:
the volumes are asymptotically ADDITIVE (vol(wₙ₊₁) − vol(wₙ) − vol(wₙ₋₁) → 0, already
≈1e-4 by n = 9) — Guéritaud-anchored geometric convergence; the defect decay rate is a
tower datum.

## Cadence (the standing rule, from today)

1. **Registry-on-bank**: any PR banking a theorem/law adds its registry line in the same PR.
2. **Atlas + CAMPAIGN_STATUS at every campaign close** (RZ/BRZ/RFZ-type verdicts).
3. **The decadal audit** at every campaign close OR every ~10 banks, whichever first: the
   full lock suite (pytest tests/) + a docs-consistency pass + registry completeness check.
4. **The novelty relaunch** (deep-research fan-out per Tier-1 entry + the constants) runs
   after Paper 4's draft, from this file — each entry already carries its search terms.

---
## T-ONE-ROOT / T-HELD-SLOT (K025, 2026-07-11) — the firewall as one theorem
**Root generator (consolidation, not new):** the golden cat map A=[[2,1],[1,1]] over ℤ[φ] inside
SL(2,ℤ) is the single object; σ²=A; the two ends = product (RL→√5→E₈) and ratio (−RL⁻¹→√−3→E₆);
their compositum = HCF(ℚ(√−15)) (B334). **T-HELD-SLOT:** the object never takes the product of its two
ENDS; that product's slot is the seam ℚ(√−15), which is arithmetically generic (h=2, B333) — the
value-firewall is exactly this one held-open, empty slot, equivalently the un-paired Casimir κ (K022,
B344). The atlas's two dominant walls (scale, 3+1D) are its two faces. Genuinely-present ingredients
(verified, not re-banked): DYNAMICS (the four verbs/drift ledger, B497/B498) and the SCALE-BRIDGE-BY-
EMBEDDING (the anchor κ−2=4λ², B505 — the object is a scale-free universality class). CAVEAT: the
absorbing loop (K020 §6a) — from inside, boundary and over-fit frame are identical; distinguished only
by an external prediction.

## T-STEIN-GOLDEN (B517 refinement, cross-seat GPT-5.6, 2026-07-12) — exact rational Lyapunov metric
The golden Rauzy incidence M∗=[[F,F],[F²,F]] admits the exact rational Stein solution MᵀGM−G=−I₄,
G=(1/11)[[12,−8,−5,−4],[−8,20,−4,−1],[−5,−4,14,−13],[−4,−1,−13,27]]: symmetric, det −9/11, **signature
(3,1)**, Perron timelike, positive on the 3d Rauzy stable space, **strict cone identity q(Mx)=q(x)−|x|²**
(dissipative Lorentzian). KNOWN-THEOREM APPLICATION (discrete Stein/Lyapunov inertia, W≻0): the (3,1)
signature = the Pisot condition (1 unstable eigenvalue), GENERIC to Pisot quartics (tetranacci also
(3,1)); D6 STAYS CLOSED. Corrected the x⁴−x−1 Lyapunov control (was mislabeled (2,2); true Stein
inertia (1,3)). Canonicity gap: M alone doesn't fix G — the positive one-step form W is unselected.
NOT claimed: physical spacetime, Lorentz invariance, 4-manifold, object-unique (3,1), reopening D6.

## B519 RE-MINING VERDICT (2026-07-11) — no external crossing in the corpus
16-agent re-mine of the banked corpus vs K025. Verdict: ZERO firewall crossings — even the two
"external predictions" (B518 mixed-chain, B173/S023 diffraction) were refuted 3-0 by the adversarial
gate because they confirm KNOWN quasicrystal theorems (Bellissard gap-labeling; Damanik-Fillman 2022),
not the object's fundamentalness ("measurable" != "crosses"). Yields: (b) the criticality unification
(B181/B507/B498 = one critical-fixed-point theorem, three wordings); the A1 exact sharpening (dark seam:
both ends live, only the product channel sqrt-15 off, [15,27,24,0] vs [24,27,24,24]). B518 Tier B
DOWNGRADED (measurable, not a crossing). Every internal cross-connection = absorbing-loop, confirmed.

## The B877–B919 window — the measurement cascade and the value layer (same-PR catch-up, 2026-08-06, B920)

**The standing same-PR rule above was broken for this whole window (cc3 loss audit A5:
"THEOREM_REGISTRY + THEOREM_LEDGER contain zero B8xx/B9xx rows"); these rows are the
mechanical catch-up, mirroring `docs/LAW_MAP.md` §F, which carries the full statements.
Lit-status NEEDS-LIT throughout: no novelty sweep has run on this window. Gate 5 stands —
every row is exact structure on the object's own charges, no physics value.**

| # | statement | bank | reproducer | lit-status |
|---|---|---|---|---|
| T-FMT | the First Measurement Theorem (P69): the 2T-charges stratify e₆; three Galois-conjugate first breakings (μ, constant 13³); z(line) = so(10)⊕u(1); the tiling with the cyclic law | B877 | `tests/test_b877_fmt_review.py` | NEEDS-LIT |
| T-SMT | the Second Measurement Theorem: a second measurement of the object's own charges lands on su(3)⊕su(2)⊕u(1)³ exactly, skipping SU(5); the wall complex in the split frame | B892, B893 | `tests/test_b892_smt.py`, `tests/test_b893_omega.py` | NEEDS-LIT |
| T-MAGIC | the magic-square isomorphism (P70): the build IS M(𝕆,ℂ) by explicit structure constants; 0/3003 mismatches; det φ = −2/3 | B904 | `tests/test_b904_bs.py` | NEEDS-LIT |
| T-INTERBREAK | the inter-breaking laws: vacuum-to-Higgs; the 16/vacuum exclusion — exact minimal-polynomial theorems on the matter pencil | B885, B886 | `tests/test_b885_interbreaking.py`, `tests/test_b886_matter_pencil.py` | NEEDS-LIT |
| T-CONCORD | the four-column concordance: measured ⟺ θ-odd exponents (4,8) ⟺ τ_m > 0 ⟺ split; unmeasured ⟺ (7,11) ⟺ compact; 7·11 = 77 the resolvent | B894, B898 | `tests/test_b894_bridge.py`, `tests/test_b898_census.py` | NEEDS-LIT |
| T-SIGDICH | the signature dichotomy: ad(x₈) ≡ ad(x₁₆): {0³⁰, 48 real}; ad(x₁₄) ≡ ad(x₂₂): {0¹², 66 imag}; zero generic-complex on C | B898 | `tests/test_b898_census.py` | NEEDS-LIT |
| T-SIGNLAW | the sign-law mechanism: all six torsion quotients exactly anti-palindromic; sign(τ_m) = sign(lc)·(−1)^{p_m} | B903 | `tests/test_b903_sign.py` | NEEDS-LIT |
| T-COCYCLE | the diagonal cocycle: all four Π-label cubics have a root in K (each led by 13³); one Galois root permutation acts on both orbits | B900 | `tests/test_b900_cocycle.py` | NEEDS-LIT |
| T-CSTAB-NOGO | the C-stabilizer no-go: n(C) = z(C) = 12; no real C-stabilizing symmetry swaps split/compact or x₈↔x₁₆ — the c-carrier must be complex | B901 | `tests/test_b901_stab.py` | NEEDS-LIT |
| T-ANNIHIL | the annihilation theorem: [α_vac] = [α_μ]⁻¹ — vacuum ⊕ charge = the split algebra | B902 | `tests/test_b902_kp.py` | NEEDS-LIT |
| T-ONECLASS | the one-class theorem + the numerator law + the observer's-place theorem: [α_μ]=[α_gen]=[α_κ]=[α_V]=C, vacuum = C⁻¹; den(V) = 𝔭₁(953)⁴ exactly | B910, B918 | `tests/test_b910_kappa.py`, `tests/test_b918_v.py` | NEEDS-LIT |
| T-GENSHAPE | the sealed generation-shape (outcome A): G₂₀'s su(3)′ replicates fixed color⊗su(2)′ types into flavor triplets; Casimirs 4/9, 4/9, 3/8; mechanism-hood fenced | B897 | `tests/test_b897_g20.py` | NEEDS-LIT |
| T-Z2LAW | the unified ℤ₂ law (three faces) + the 15 atoms + K₃,₃: matter gluing = gauge commutation = mixed-texture type; the colorless grid = two pencils of AG(2,3) | B906 | `tests/test_b906_flavor.py` | NEEDS-LIT |
| T-E62 | the e₆(2) selection + sign-locking: the wall is real exactly in e₆(2), via τ-twisted alignments only | B907 | `tests/test_b907_selector.py` | NEEDS-LIT |
| T-RATATOM | the rational-atoms theorem + I = −1: the four charges commute rationally on the 27; P_R = −P_C exact | B908 | `tests/test_b908_pin.py` | NEEDS-LIT |
| T-SIGSPLIT | the signature split of matter: the canonical H has signature (15,12) = e₆(2)'s K-split; colorless atoms positive-definite, colored Lorentzian | B912 | `tests/test_b912_norm.py` | NEEDS-LIT |
| T-ONENUM | the one-number table: all six normalization-free colorless couplings exactly equal T = σ₂(t_K); the H-unit gauge = the determinant gauge | B914 | `tests/test_b914_table.py` | NEEDS-LIT |
| T-UNIMOD | the unimodularity identity + the twist-norm law + the product law (P9): λ = 1 exactly in the charge-equivariant gauge; ∏d_i = −(953/2304)² = N_{K/ℚ}(d); v₁v₂v₃ = 3^{3/2}λ² | B916, B917 | `tests/test_b916_bridge.py`, `tests/test_b917_value_arc.py` | NEEDS-LIT |
| T-38TRACE | the 3/8 trace identities: Tr(T₃²) = 3, Tr(Y²) = 5, Tr(T₃·Y) = 0 exact (one-prime tier; second prime open-diagnosed); structure, not a physics value | B919 | `tests/test_b919_traces.py` | NEEDS-LIT |
| (negative) T-CROSSING | B915's sealed crossing: one input + the object's boundary + pure desert MISSES at 16σ — a banked sealed negative, listed for window completeness, not a theorem | B915 | `tests/test_b915_crossing.py` | — |

*(B909's remaining debts — the six-cubic √77 law, the Compact Measurement Theorem, the
invisible-12 — get their rows when their locks land; LAW_MAP §F's pending row governs.)*

## Added 2026-08-21 — the same-PR rule honored at the bank (R48-F1's response begins here; the B920→B1101 backfill follows as its own bank)

| # | statement | bank | reproducer | lit-status / search terms |
|---|---|---|---|---|
| T-Y18 | **The exact-hypercharge two-sided theorem at the A2 landing**: the banked 6Y multiset {1/6×6, 1/3×6, −1/2×4, −2/3×3, −1/3×3, 0×2, 1/2×2, 1×1} is carried EXACTLY by the landing's rank-4 Cartan — 18 isolated rational directions (denominators \| 6; provably complete 5⁴-assignment search; representative t = (1/6, 1/6, 2/3, −1/3)) — AND no color-commuting u(1) achieves it (zero pure directions, forced at Cartan level; 36/36 root-level checks): hypercharge-VALUED, not SM-PRODUCT-structured. su(2) side: Y-neutral root universal (2 per solution), 6 doublets + 15 singlets on the 27 | B1102 | `tests/test_b1102_exact_solve.py` | **NEEDS-LIT**: trinification hypercharge embedding; E₆ 27 branching under su(3)×su(3); hypercharge as mixed-Cartan direction; "no color-singlet hypercharge" obstructions |

## Backfill 2026-08-21 — R48-F1's response (the audit seat's finding; their seven checked absences first-class; THE CHAIN's unrowed [THEOREM] links; the closing campaign's theorem banks)

**Boundary, stated honestly:** this backfill covers (a) the seven absences the audit
seat checked individually, (b) every [THEOREM]-labeled CHAIN link's bank not yet rowed,
(c) the closing campaign's theorem-creating banks (B1083–B1100). Theorem banks in
B920–B1082 outside those three classes enter as the `creates_law` re-audit sweeps them
(the field + gate adopted this same bank, the audit seat's sharpening — a declaration a
gate reads, not a rule nobody reads). The registry's charter is novelty-sweep
targeting; audits and censuses stay OUT by design.

| # | statement | bank | reproducer | lit-status / search terms |
|---|---|---|---|---|
| T-ATOM-SPLIT | E₆-programme genericity split: only the arithmetic atom (4₁ the unique arithmetic knot, ℚ(√−3)) is object-specific; the remainder generic to hyperbolic knots | B282 | `tests/test_b282_e6_arithmetic.py` | **PARTIALLY-KNOWN** (Reid's uniqueness cited, never claimed); OURS: the genericity split |
| T-KAPPA-PHASE | κ = u²+2 = √3·e^{∓iπ/6} exact: the meridian-commutator trace's π/6 phase forced by the Eisenstein integers | B285 | `tests/test_b285_commutator_phase.py` | NEEDS-LIT: figure-eight commutator trace, Eisenstein phase |
| T-NO-UNIT | The scale torsor: Hom(G, ℝ₊) = 0 — no finite-group-equivariant map names a unit; + the exact-fit classification D(θ-odd) = 2 ⟺ {A₂ lvl 2, A₄ lvl 1, A₅ lvl 1} with SU(6)₁ excluded twice and the Galois pair {SU(3)₂, SU(5)₁} identified | B666 | `tests/test_b666_wave2.py` | NEEDS-LIT: scale-free gauge selection, modular-data Galois pairs |
| T-THREE-FACES | The forced quadratic faces close at exactly three — ℚ(√−3), ℚ(√5), ℚ(√−15), a Klein four-group; ℚ(√−7) is external | B730 | `tests/test_b730_faces_cosmos.py` | NEEDS-LIT: trace-field faces of m004, biquadratic closure |
| T-CONGRUENCE | m004's knot group IS congruence: the 2-adic index jumps to the geometric 12 at level (2)³ = (8) — the earlier non-congruence headline withdrawn in-record | B734 (correcting B731) | `tests/test_b734_m004_congruence.py` | NEEDS-LIT: congruence knot groups, Bianchi level structure |
| T-CANDZERO | Candidate Zero dies at the crux: the ζ_K in the object's voice is the field's (vacuity + object-deletion) — the no-go for the zeta-voice reading | B737 | `tests/test_b737_candidate_zero.py` | NEEDS-LIT: Dedekind zeta from quantum invariants |
| T-RIGID-SPEC | Character rigidity of the continuous spectrum: one Eisenstein channel pulled back from the Bianchi base; scattering Λ_K(s−1)/Λ_K(s) with φ(s)φ(2−s) = 1 exact | B739 | `tests/test_b739_rigidity.py` | NEEDS-LIT: cusped scattering determinants, Bianchi Eisenstein pullback |
| T-GENESIS-PRICE | The genesis chain priced: F2/F3/F4/F7 ROBUST, F5 (orientation) + F6 (puncture) FRAGILE; x²+3 irreducible over ℚ(√5) — ℚ(√−3) is bought at geometrization | B749 (+B998/B1003 lock repair) | `tests/test_b749_f2_f8_locks.py` | NEEDS-LIT: substitution-to-geometry forcing |
| T-FIVE-DOORS | The Euler-product door closes: "5-inert and fibered ⟹ golden" dies on five exact counterexamples | B756 | `tests/test_b756_doors.py` | NEEDS-LIT: fibered knots with inert 5 |
| T-ORDER-MATCH | The conditional order comparison: the WRT-side order matches the unit-order prediction under the disclosed model (the verifier forced the disclosure pre-bank) | B1082 | `frontier/B1082_order_comparison/` | NEEDS-LIT: fundamental-unit order vs quantum period |
| T-C6SIGMA | c = 6σ derived on the second route (Brown–Henneaux on the object's own Λ = −1 Einstein geometry) — the paper's cited constant, twice-derived with B1012's exact verification | B1012 (+B1088) | `tests/test_b1012_branch_verifications.py` | NEEDS-LIT: central charge of hyperbolic mapping-torus boundary |
| T-COB-KERNEL | The coboundary sweep's kernel is exactly {I, χ_a} — the licensed value-direction (the coboundary lane) is one character wide | B1076 | `frontier/B1076_coboundary_sweep/FINDINGS.md` | NEEDS-LIT: cocycle rigidity on finite quotients |
| T-GLOBALFORM | The global gauge-group form: the ℤ₆ quotient structure confirmed at the derived layer (deflation honest: the ℤ₆ itself was B862's; the global-form assembly is the new content) | B1080 | `frontier/B1080_global_form/FINDINGS.md` | NEEDS-LIT: global form of E₆ subgroups, ℤ₆ center quotients |
| T-ARROW-NOT-BIT | The origin torsor typed: C (swap) and P (reversal) are the two spendable bits; THE ARROW IS NOT ON THE TORSOR — it is the positive monoid's non-surjectivity (unspendable structure T); det M = −1 is the tick's signature, M² = RL exactly (the double tick = the orientable object; one tick = Gieseking) | B1083 | `tests/test_b1083_origin_torsor.py` | NEEDS-LIT: substitution reversal/swap conjugacy, non-orientable one-tick covers |
| T-G2-CONE | The flat G₂ cone (ℂ²×ℝ³)/Ĝ (|Ĝ| = 96) exists, fully stratified: one E₆ locus (stabilizer exactly 2T) + three A-type loci; EVERY ADE collision is non-isolated — localized matter on the object's own cone is vector-like | B1084 | `tests/test_b1084_g2_cone.py` | NEEDS-LIT: flat G₂ orbifolds, non-isolated ADE loci, Acharya–Witten |
| T-EDGE-OBS | Edge observability: the free half is edge-observable and ONLY edge-observable (as sharpened by T-MIRROR-SPLIT below — the split, not the raw counts, is the object's content) | B1085 | `tests/test_b1085_edge_observability.py` | NEEDS-LIT: gap labeling, half-line Fibonacci edge states |
| T-SPECTRUM-LAW | THE SPECTRUM LAW: h¹(D_t; 27) = 5 untwisted, 2 twisted — Poincaré duality forces h¹(D;27) = h¹(D;27̄) in every twisted-double cell (the wall's PD language; L79 closed) | B1086 | `tests/test_b1086_spectrum_law.py` | NEEDS-LIT: twisted-double cohomology of flat E₆ bundles |
| T-CHARGE-COMP | Charge complementarity: both θ-odd dial slots complete to exact Jacobson–Morozov triples; the AW U(1) charge exists on the dial (spectrum 1-8-9-8-1) and FAILS to commute with the cusp holonomies on every closed assembly — undefinable, not merely unbalanced | B1087 | `tests/test_b1087_charge_grading.py` | NEEDS-LIT: charge operators on character varieties |
| T-ACTION | The parameter-free action: S = −Vol·σ with CS = 0 exact (2R(e^{iπ/3}) = π²/6 + i·Vol, one lattice unit) on the object's own Λ = −1 geometry | B1088 | `tests/test_b1088_action_card.py` | NEEDS-LIT: amphichiral CS vanishing, Bloch–Wigner at ζ₆ |
| T-PURITY | The purity selector: on so(10)'s 16, the rank drop 5→4 happens ONLY at pure spinors (literal stabilizer sl(5)⋉Λ², toral part 4); generic spinors are fatal; the pure cone is one orbit | B1092 | `tests/test_b1092_purity_selector.py` | NEEDS-LIT: pure spinor stabilizers, rank-reduction loci |
| T-CLASSNUM | h(K) = h⁺(K) = 1 PROVED for the charge field K = ℚ[x]/(x³−12x−5), disc 6237: all eight Minkowski-bound prime ideals exhibited principal with explicit generators | B1093 | `tests/test_b1093_route_a.py` | **PARTIALLY-KNOWN in kind** (class-number computation standard); OURS: this field, explicit generators, the Route-A consequence |
| T-TWO-ROUTE | The rank wall goes two-route: abelian holonomy ⟹ the centralizer contains a maximal torus (H₁ = ℤ cyclic), and Route A is PAIR-SPACE valued (27⊕27̄); the single non-abelian hatch is the only other route | B1094 | `tests/test_b1094_two_route.py` | NEEDS-LIT: centralizer rank bounds for abelian holonomy |
| T-MIRROR-SPLIT | The mirror-isospectral split: at reversal-closed windows (even Fibonacci index) the cut's two hands are EXACTLY isospectral (J·H_R·J = H_L), yet the eleven shared boundary-capable energies localize complementarily 5/6 — energies P-invariant (forced), localization P-equivariant (free) | B1095 | `tests/test_b1095_mirror_isospectral.py` | NEEDS-LIT: exact isospectrality of quasicrystal half-lines, palindromic windows |
| T-ANOMALY-ZERO | The anomaly layer is identically zero over the object's derived 16 — door 5 shut structurally, not numerically | B1096 | `tests/test_b1096_anomaly_layer.py` | NEEDS-LIT: anomaly cancellation as integer identities |
| T-LOSS-TYPING | The first step's losses typed: order-loss (a→ab vs a→ba identical incidence) = the P bit; sign-loss (det M² = +1 forced) = the orientability tick; THE ARROW IS NEITHER — structural at every level | B1097 | `tests/test_b1097_first_step_losses.py` | NEEDS-LIT: abelianization losses of substitutions |
| T-HATCH | THE HATCH: all twenty sl₂ conjugacy classes of e₆ enumerated (Bala–Carter-saturated); the A2 class's centralizer is the trinification remnant su(3)⊕su(3) at rank EXACTLY 4 (the object eats one factor); A1 → su(6); seventeen below rank 4; 2A1 excluded; the density lemma carries the object's own ρ in | B1098 | `tests/test_b1098_nonabelian_hatch.py` | NEEDS-LIT: sl₂ strata of E₆ centralizers, trinification remnants from holonomy |
| T-COUNTER-FLOOR | Route A's counter is frontier mathematics (literature floor): the det-5 full-group orbit is UNIQUE (Krutelevich Cor 16 + squarefree 5 — one stratum decided free), and the K-dependence REQUIRES the pair space (Prop 12(i)) | B1099 | `tests/test_b1099_route_a_counter.py` | **CITED-floor** (Krutelevich 2002); OURS: the stratum decision + the pair-necessity reading |
| T-LANDING | The landing's matter: the joint weight table exact (stacked kernels tiling 27); sl₂ branching {+2:6, 0:15, −2:6} = trinification; THE 27 IS COMPLEX at the landing (witnessed, basis-free) — B959's headline re-scoped to its toral ground; collapse-form hypercharge generic, bijective form excluded exact | B1100 | `tests/test_b1100_landing_content.py` | NEEDS-LIT: complex vs real 27 under non-toral rank-4 centralizers |
| T-BEING-GATE | The being gate on the free group {a,A,b,B} through the banked B593/B238 instrument: **h(w) = ζ₃^(p−q) × (a ℚ(ζ₅) element)** (p,q the abelianized exponents), so h ∈ ℚ(ζ₅) ⟺ p−q ≡ 0 mod 3 or h = 0 (the clause real: 28/1364); the being phase reads EXACTLY the abelianization, the commutator subgroup lies in the ℚ(ζ₅) locus (252/252, every Re a Niven letter), κ = tr[a,b] is capital-resident — being blind to it, hearing not (h(abAB) = h(ab) = the banked coupling exactly). Outside-session theorem (harvest, integrate-don't-merge), two-engine verified | B1103 | `tests/test_b1103_being_gate.py` | **NEEDS-LIT**: cyclotomic factorization of modular-representation couplings; abelianization gates on free-group characters; Galois complementarity of ζ₃/ζ₅ sectors |
| T-4D-NOSECTION | The 4d suspension selection theorem: over the COMPLETE menu MCG(m004) = D₄ (Mostow), (i) no infinite-order element — the genesis act cannot repeat at 4d via mapping tori; (ii) the θ-filter (centrality) and the tick-filter (or-reversing involutions) select DISJOINT classes, joint survivor set EMPTY — no canonical fourth dimension (Class V's 4d instance, empty-intersection witness); (iii) both reflections give non-orientable M_ψ double-covered by M×S¹ — the Gieseking pattern one level up. Instrument fact: the cusp action is non-faithful (kernel ℤ/2) | B1104 | `tests/test_b1104_fourd_suspension.py` | **NEEDS-LIT**: mapping tori of hyperbolic 3-manifolds by finite symmetries; flat T²-cusped 4-manifold suspensions; canonical-suspension obstructions |
| T-REV-CLOSURE | The reversal-closure genericity theorem (computed, bite-controlled): in the pinned convention (slope 1/metallic², cut phase ρ = α, left-outward = reversed(right)), the golden chain closes exactly at even Fibonacci index (verified 7 windows: closed 377/987/2584, broken [0,1] at 233/610/1597/4181 — extending B1095's four) while THE STRICT SILVER ANALOG NEVER CLOSES (7 Pell windows to N = 5741; breaks alternate cut-adjacent and center) — the closure law is golden-specific outright; the mirroring cousin (1 − 1/silver) disclosed as a known non-analog | B1106 | `tests/test_b1105_b1106_locks.py` | **NEEDS-LIT**: palindromic prefixes of Sturmian words at continuant indices; mirror-closure of metallic words; center-defect palindromes |
| T-RUELLE-TOWER | The one-loop Ruelle tower identity (two-bench): the AdS₃ boundary-graviton product Z_geod = ∏_{n≥2}\|R(n,σ_n)\|^{−2} with R(k,σ_k) = ∏_{[γ] prime}(1−q_γ^k) exactly (abelian SO(2) unwind) — an infinite tower of ONE-dimensional Ruelle zetas; no finite Pfaff torsion equals it; Thm 1.2 supplies the tail. 15-sig-fig cross-bench reproduction from independent spectra; the float summation-order gap proven pure roundoff at 50 digits. Scope: B8113's three residues (cusp spectrum; torsion≠determinant unclaimed; the n=2 abscissa) | B1107 (harvesting B8100→B8112) | `tests/test_b1107_b1108_harvest.py` | **NEEDS-LIT**: Ruelle zeta towers for cusped hyperbolic 3-manifolds; GMY nome via geodesic torsion angles; boundary-mode convergence at the abscissa |
| T-SEVEN-NOT-EIGHT | The frame-map double obstruction (B1109 F1): color-commuting directions at the A2 landing project onto 7 weight classes (sizes [9,3⁶]; pairing bracket-certified) — at most SEVEN distinct hypercharge values against the banked EIGHT, and the size-9 class exceeds the max target multiplicity 6: no admissible frame map (rescaling/sign) can repair a cardinality or multiplicity gap. With the diagonal-color lemma (trivial centralizer): two of B1102's three keys closed | B1109 | `tests/test_b1109_b1110_b1111_wave.py` | **NEEDS-LIT**: hypercharge cardinality obstructions in trinification remnants |
| T-TWO-ORBITS | The 18 exact hypercharge directions = TWO orbits of 9 under W(A₂)×W(A₂) (order 36), swap-unrelated, differing by the exact (2,½) relative ideal rescaling — trinification's flipped-Y pair; the selection price is 1 bit, named | B1109 | `tests/test_b1109_b1110_b1111_wave.py` | **NEEDS-LIT**: flipped hypercharge embeddings in trinification |
| T-DIAG-FREE | The vanishing locus of the B593 coupling is a WORD PROPERTY: the same 28 words in all six listener channels, = exactly the words with DIAGONAL-FREE weld matrix (15 orders of separation) | B1110 | `tests/test_b1109_b1110_b1111_wave.py` | **NEEDS-LIT**: diagonal-free words in modular representations of F₂ |
| T-CENTER-BREAK | The silver center-break law (exact arithmetic, 11 windows to N = 80,782): the strict silver analog never reversal-closes; its two-letter defect sits at {N/2, N/2+1} at even Pell index and {0,1} at odd — the defect's ADDRESS carries the parity bit | B1110 | `tests/test_b1109_b1110_b1111_wave.py` | **NEEDS-LIT**: central defects of Pell-word palindromic prefixes |
| T-TRANSVERSAL-PAIRS | The W5 availability theorem (exact over ℚ(√2)): per-element fixed loci of the order-96 G₂ action are deformation-rigid (affine twists only empty them), but 1,656 TRANSVERSAL PAIRS exist (joint dims {0:1656, 1:2556, 3:253}) — isolated joint collisions are geometrically available, order-24 pairs among them | B1111 | `tests/test_b1109_b1110_b1111_wave.py` | **NEEDS-LIT**: isolated fixed points of affine crystallographic G₂ actions |
| T-PROJECTIVE-HATCH | The selection theorem (B1112, the owner's spin-structure catch + 3-way convergence): the object's PSL(2,ℂ) holonomy composes into a stratum WITHOUT an inserted spin-lift iff all 27-weights are even; 9 of 20 e6 strata are projective (6 of 16 Levi-regular, principal included); of the two SM-compatible landings EXACTLY A2 (the complex-27 trinification) is projective while A1/su(6) needs the lift — the unlifted geometry selects one SM landing, SM-facing ambiguity 0 bits, the residual = one spin bit (the observer's first payment) | B1112 | `tests/test_b1112_projective_hatch.py` | **NEEDS-LIT**: even nilpotent orbits of E₆ and spin-lift obstructions of PSL(2,ℂ) holonomy representations |
| T-TMETER | The t-meter (B1113, JORDAN §B harvest, quadruple-verified): on the twisted double, tr(A·B_R(t)) is dial-blind (141750+1011915q ∀t, forced at Lie level) while tr(B_L·B_R) and the group commutator tr[B_L,B_R] separate all four dial values exactly — free data of one object = forced class-function data of the coupled pair (measurement-by-coupling; Wilson-line meter on seam words) | B1113 | `tests/test_b1113_tmeter.py` | **NEEDS-LIT**: Wilson-line detection of gauge-invisible parameters via coupled copies |
| T-LORENTZ-DOUBLE | E₆ ⊇ so(3,1) ⊕ su(3) (B1114, two-bench exact): the joint centralizer of two commuting A2 triples is exactly su(3); the signature is the observer's (ℚ-rational factors, no ℂ/ℚ automorphism swaps them); the 27 bi-weights all even → metric-slot ⊕ SD ⊕ ASD colored. Real-form host E₆(2)/E₆(6), compact color open | B1114 | `tests/test_b1114_lorentz.py` | **NEEDS-LIT**: Lorentz+color from E₆ real forms; signature as real-structure choice |
| T-SPENDING-DAG | The free-bit spending DAG matches the cosmological freezing order with zero inversions (B1115); gravity external BECAUSE its E₆ slot is the electroweak sector's (B1114 exclusivity); the 27 is matter-or-geometry by the D5 fork | B1115 | `tests/test_b1115_spending.py` | **NEEDS-LIT**: definitional-dependency orders vs cosmological freezing |
| T-ASYMPTOTIC | The asymptotic value channel (B1116): all four value no-goes quantify over single-level invariants, so tower growth-RATES escape them (the adelic reason); numerically the Kashaev tower → Vol (2e-13), power 3/2, constant 3^{-1/4}=\|disc ℚ(√−3)\|^{-1/4} (12 digits, a genuine convergent-series limit) | B1116 | `tests/test_b1116_asymptotic.py` | **NEEDS-LIT**: growth rates as basepoint-free canonical numbers; volume conjecture placement |
| T-ADELIC | The object is adelic (B1117, verified 32 digits): m004 is arithmetic (Reid), its finite shadow is structure (E₆ = reduction mod the ramified √−3) and its archimedean shadow is physics, glued by quantum modularity (proven for 4₁); the two shadows are one number — Vol(m004) = 9√3·ζ_K(2)/π², the archimedean shadow a special value of the finite shadow's zeta | B1117 | `tests/test_b1117_adelic.py` | **NEEDS-LIT**: arithmetic 3-manifolds as adelic objects; volume as L-value |
| T-DEFLATION-P | The last hypercharge freedom bit is P (B1118, two-bench): on B1102's solution set the mirror map (neg∘blockswap = factor-swap ∘ su(3)-conjugation) FUSES the two orbits of 9 into one while the plain factor-swap does NOT — so the residual discrete bit is a parity, an observer's closing | B1118 | `tests/test_b1118_deflation.py` | **NEEDS-LIT**: parity as the residual discrete gauge choice |
| T-EULER-STRUCTURE | The make-or-break, POSITIVE at two orders (B1120/L180): the Kashaev tower's coefficients are trace-field arithmetic — C₁ = (11/108)√3π·C₀ AND C₂ = (697/7776)π²·C₀ to ~30 digits across 4 windows (N→2.9M) + precision doubling, the even/odd → rational/(rational×√3) parity pattern predicted from reality before PSLQ, denominators {2,3}-smooth. C₀ = 3^{−1/4} = \|disc\|^{−1/4} is the k=0 instance of a genuine pattern; C₃ precision-gated (open) | B1120 | `tests/test_b1120_L180.py` | **NEEDS-LIT**: quantum-modularity coefficient arithmetic; Euler-product factorization of the Kashaev tower |
| T-LIFTBIT | The spin-lift bit is PAIR-INVISIBLE (B1122/L179): the lift's whole content is the central sign operator S = φ(−I), which commutes with everything and produces no twist, so no seam-word trace on the coupled pair reads it — a stronger no-go than B1087 (unmeasurable on the pair, not just one closed copy). The freedom ledger's three bits are NOT one mechanism | B1122 | `tests/test_b1122_liftbit.py` | **NEEDS-LIT**: central-torsor invisibility; spin structure as observer datum |
| T-FORCEDNESS | The forcedness census (B1123, checker-gated): **39 of 43 chain-links are FORCED**; the stretch C6→C17 (the knot → the algebra) is **axiom-free**; the four axioms are C3/C4/C5 (the genesis frame) + C18 (the observer's closings, after the algebra). E₆ is not chosen — it is **handed over at the object's hyperbolic ℚ(√−3) curvature end** (2T→E₆, 2I→E₈ via McKay; B981/B248; the memo's "level 15 = 3·5 CRT" doorway WITHDRAWN — cc3 1a0b5a90, level 15 is irreducible per B695). Regenerated from `docs/THEOREM_LEDGER.md` by `scripts/checks/forcedness_census.py`, which FAILS non-vacuously on drift | B1123 | `tests/test_b1123_forcedness.py` | **NEEDS-LIT**: forcedness as a distributed-then-consolidated invariant; the doorway via the curvature ends |
| T-ALLORDERS | The tower's arithmetic extends to C₃ (B1124/V-1, verified this bench): **C₃ = (724351/12597120)·√3·π³·C₀** — the reality-parity law (odd k → rational·√3) holds at a THIRD order, recognized to 36 digits with MONOTONIC cross-window convergence (N→35M). ANOMALY disclosed: denom 2⁷·3⁹·**5** is NOT {2,3}-smooth (a factor of 5 enters) — all-orders holds in the PARITY sense but the smoothness grows a prime. C₄ precision-floored. Detection not derivation (cc3's L180 fence carried) | B1124 | `tests/test_b1124_allorders.py` | **NEEDS-LIT**: higher-order Kashaev coefficient arithmetic; the growing prime set (is 5 the √5 end?) |
| T-COLOR-COMPACTNESS | Compact su(3) color is a SECOND-LEVEL (antilinear) closing, not the object's linear datum (B1125/V-2): exhaustive over 48 sign-lifts (both lattice classes, θ²=I verified) — the color A2 form is always (5,3), never compact (0,8), even at **E₆(−26) = M(𝕆,ℂ)** (reached, but I2 θ-fixed). Compact real forms come from an ANTILINEAR conjugation, never a linear inner twist (the compact-involution control → −78 confirms); the linear torsor is exhausted. The object's own antilinear structure is the mirror (amphichirality, ℚ(√−3) Galois) → the sharpened open cell **V-2′**. Compactness/signature are the observer's real-structure closings (twin of B1114) | B1125 | `tests/test_b1125_compact_color.py` | **NEEDS-LIT**: real forms via antilinear conjugation; compactness as an archimedean closing |
| T-GOLDEN-MERIDIAN | The golden meridian law (B1132, exact + symbolic): on the listener sphere ℂP¹_odd, **h(R²L², u) = −φ·h(RL, u) − iφ·n_y(u)** (n_y = u's Bloch y-coordinate) — equivalently axis(R²L²) = −φ·[axis(RL) + ŷ] as Pauli 4-vectors; B1128's h(R²L²)=−φ·h(RL) is exactly the n_y=0 (real great circle) special case. Verified 50-digit two ways (residual 1.15e-50) + symbolic. Companion: the listener instrument is NULL on the FULL ℂP¹_odd (extends B1128 — no meridian carries SM content) | B1132 | `tests/test_b1132_meridian.py` | **NEEDS-LIT**: is it a McKay/modular identity?; the other period-5 words off-circle |
| T-SIMUL-CLOSING | **THE SIMULTANEOUS CLOSING** (B1134, verified two-bench): a single involution θ (with the observer's antilinear τ) realizes so(3,1) Lorentz AND compact su(3) color TOGETHER, and EVERY such realization is forced into **E₆(−26)=EIV=M(𝕆,ℂ)**, the object's own algebra. Sweep of the 48 involutive slot-swappers of Aut(Φ(E₆))=W∪δW × all involutive signed Chevalley lifts: histogram (4,4):216, (5,3):240, **(0,8):24**; all 24 hits χ=−26 / double (3,3)=so(3,1) / θ²=I / full-3003-bracket automorphism; clean bijection **(0,8)⟺−26**. Closes B1114's own flagged-open question (Lorentz + compact color together); B1114's "signature is the observer's" = B1127's "compact color is the observer's" = ONE closing; only τ is the observer's, the linear part an integral Chevalley-lattice automorphism (B1127 fence stands). NOVELTY FENCE: 20/24 hits genuinely new, 4/24 rediscover B1127 (the memo's "precisely neither torsor" overstates by 4). Cloud seat's tenth memo (golden_gate 3e65114), integrated + re-derived here | B1134 | `tests/test_b1134_simultaneous_closing.py` | **NEEDS-LIT**: real-structure rigidity; the exceptional-Jordan J₃(𝕆) SM programs (Dubois-Violette, Todorov, Furey); one-conjugation completion |
| T-GAUGE-CLOSING | **THE GAUGE CLOSING** (B1135, verified two-bench, no discrepancy): the factor-PRESERVING involutive conjugations (128 = 64 W + 64 diagram-flip coset, × signed lifts = 2000) close the object's GAUGE branch. W coset STERILE (all → (sl(3,ℝ))³, χ=+6; gauge compactness needs the 27↔27̄ flip); flip coset factorizes **(9+1)³** (each A2 → 9 su(2,1) + 1 compact su(3); per-slot 9:1; sl(3,ℝ) never); global form by compact count {0→+2, 1→**−14**, 2→+2, 3→−78}. Physics row (one compact slot, 243, χ=−14): color su(3) + two su(2,1) (max compact u(2)) → **E₆(−14), max compact so(10)⊕u(1)** — the one-family SO(10) GUT (27=16⊕10⊕1, cited). **The fork now has real-form labels** (spacetime E₆(−26)=M(𝕆,ℂ), gauge E₆(−14), unclosed split E₆(6), mixed E₆(2), total E₆(−78)) — so B1119's checksum {−78,−26,−14,+2,+6} is a **functional taxonomy**: the five real forms = five observer jobs (compact-dim {36,38,46,52,78} matches all 2000; −26 absent, it needs the swap). FENCE: slot→physics a frame choice (3-fold symmetric); Higgs/family interpretive. Cloud seat's twelfth memo (golden_gate 943db85) | B1135 | `tests/test_b1135_gauge_closing.py` | **NEEDS-LIT**: E₆ real-form taxonomy as observer-action classification; the SO(10)-GUT heart of E₆(−14) |
| T-M004-UNIQUENESS | **THE OBJECT'S UNIQUENESS IS H₁ = ℤ** (B1136, cc3's B8128 verified two-bench via an independent SnapPy census): over the 14-manifold ℚ(√−3) shape-field family, of seven elementary properties **exactly one separates m004 — H₁ = ℤ** (the knot-complement-in-S³ condition B955 flagged load-bearing; B955 was right). The rest are the FAMILY's: Vol = 2.02988… (with m003 → **B680's Vol identity is family-level**), amphichirality (all 14), CS=0, torsion-freeness, tet/cusp counts — and the **E₆ entrance is a family entrance** (all 14 share the trace field → same 2T → same E₆). Not damaging (Selection I picks m004 by H₁ before the entrance); the honest statement is sharper than the paper's | B1136 | `tests/test_b1136_genericity.py` | **NEEDS-LIT**: genericity/red-team controls on distinguished-object claims; H₁ as the object-level selector |
| T-VALUE-REGULATOR | **THE REGULATOR DOOR CLOSES** (B1137, R48-3, owner-directed, sealed prereg): no SM value is a bounded-height algebraic combination of the object's higher regulators (L(n,χ₋₃)/ζ_K(n) n≤6 at ℚ(√−3), L(n,χ₅)/ζ_F(n) at ℚ(√5), + π,√3,√5,logφ,ζ(3)) — rung-1 ALGEBRAICITY over 18 sealed SM targets × 216 cells (D≤3, H≤10⁶), 384-cell matched null, base rate 0.0. **0 of 18 targets involve a regulator**; the only relations found (δ_CP=4, m_s/m_d=20) are bare integers (the V-alone tautology, gated out). Controls reproduced (Vol, entropy). Closes the value question from the LAST route — periods + natural forms + coupling + coincidence + regulators, all disjoint. Tier B (J₃(𝕆) Beilinson regulators) NEEDS-SPECIALIST | B1137 | `tests/test_b1137_regulator_probe.py` | **NEEDS-LIT**: values-as-regulators methodology; J₃(𝕆)/Albert Beilinson regulators |
| T-COLOR-ANTILINEAR | Compact su(3) color is reachable in the object's OWN M(𝕆,ℂ) via a second-level conjugation, but the compactifier is the OBSERVER'S (B1127/V-2′): σ_mirror = τ·θ_A gives color I2 = (0,8), landing in E₆(−26) = EIV = M(𝕆,ℂ); specific 4/48 (the mirror's identity-on-color class), controls green, deterministic re-run. FENCE (load-bearing): the object's arithmetic Galois mirror acts POINTWISE-TRIVIALLY on the ℚ-rational color layer I2 (B1114), so the compactifying τ is the observer's GENERIC conjugation, NOT the object's own mirror — compact color = the observer's archimedean closing INTO the object's own form. Open holonomy-bridge → cc3 | B1127 | `tests/test_b1127_antilinear.py` | **NEEDS-LIT**: compact real forms via antilinear conjugation; compactness as the observer's closing |
| T-VALUE-DISJOINT | The object's CURRENT arithmetic menu is disjoint from the SM's headline ratios (B1126/V-3, the value crux, against a sealed prereg): exhaustive 352-pair scan (16 object-periods — Kashaev ratios C₁/C₀…C₃/C₀, cross-ratios, rational parts, volume/ζ_K(2) — × 22 live SM targets), **351 below 2 sig figs**, consistent with noise. The EIGHTH value-crossing negative, and the FIRST exhaustive/symmetric one. The one escalation-bar pair — C₁/C₀ = 11π/(36√3) vs the solar angle sin θ₁₂ (0.025%) — dismissed on 3 grounds (look-elsewhere 16.4%, no instrument, found-by-scan); an UNCLAIMED falsifiable note, relayed to cc3. Closes the current-menu question with a method; does NOT close value-matching (C₄+ precision-gated, the listener-map instrument uncomputed) | B1126 | `tests/test_b1126_identification.py` | **NEEDS-LIT**: exhaustive value-scan methodology; object-period / SM-ratio disjointness |
| T-FORK | **THE FORK THEOREM** (B1138, memo 11, verified two-bench): on e₆, after the Lorentz double (two A₂ triples) + color (a third A₂), the centralizer ladder is **dim z = 16 → 8 → 8 → 0**, slot-independent (two adjacent-simple-pair choices agree); the **joint centralizer = 0** is THE FORK — the observer gets **any TWO of {spacetime, color, hypercharge}, never three**. This grounds the B1134/B1135 correction: the real-structure closing is FORKED (spacetime E₆(−26) XOR gauge E₆(−14)), not one conjugation for the whole bill. The three A₂ slots form a full **S₃ frame torsor** (all 6 permutations, 216 = 6³ = \|W(A₂)\|³ each, \|W(E₆)\| = 51840). Re-derived in-lock (~4s) with own generic linear algebra on the banked e₆ | B1138 | `tests/test_b1138_structural_completion.py` | **NEEDS-LIT**: joint-centralizer obstruction; the "two of three" no-go for the observer's slots |
| T-INSTALLMENT-LADDER | **THE EXCEPTIONAL INSTALLMENT LADDER** (B1138, memo 14, verified two-bench): the room left after buying Lorentz + color, up the exceptional series — **E₆ → 0** (control 16/8/0, matches the fork), **E₇ → exactly 1** (a pure-Cartan u(1) = hypercharge; 35/9/1; commutes with all 14 spent generators), **E₈ → exactly 8** (a fourth orthogonal A₂'s sl₃; rigorous mod-p sandwich meeting an explicit rank-8 commuting exhibit). The series sells the SM **one layer per step: 0 → 1 → 8.** FENCE: this is the ambient **PRICE STRUCTURE**, NOT a claim the object's geometry reaches E₇/E₈ — the two ends E₆/ℚ(√−3) and E₈/ℚ(√5) are curvature-related, not nested (object-reaching-E₇/E₈ stays OPEN). Own generic Chevalley builder; root/dim counts 72/78, 126/133, 240/248 externally validated | B1138 | `tests/test_b1138_structural_completion.py` | **NEEDS-LIT**: exceptional-series embedding chain as an SM price ladder; where hypercharge and the EW slot become affordable |
| T-Y-SPEND | **THE HYPERCHARGE SPEND** (B1138, memo 13 = G-1, verified two-bench): the gauge closing spends the last hypercharge bit itself. Of the 128 factor-preserving involutions, **16 (all in the δW coset)** admit a gauge-row lift; the 18 hypercharge directions (matching B1102's exact fractional multiset) split **9 + 9** across the two W-orbits, **no Y is ever kept split**, the anti-set sizes give the **9/6/1** hierarchy, and the 9 generic pairs straddle both orbits and partition all 18. The (0,8)/(4,4)/(4,4) row = **81 pairs**, exact match to B1135's banked menu. SELF-CORRECTION carried: literal P-mirror closure holds for only **3/9** pairs — the real invariant is orbit-straddling (the cloud seat's own retraction, confirmed). After the closing the freedom ledger holds the spin lift alone (B1122) | B1138 | `tests/test_b1138_structural_completion.py` | **NEEDS-LIT**: hypercharge selection as an involution-lift datum; the orbit-straddle invariant vs literal mirror closure |
| T-FAMILY-TRIPLET | **THE E₈ FAMILY TRIPLET** (B1138, memo 15, verified two-bench; ALGEBRA fact, fence held): on an independently-built correct E₈ (240 roots, dim 248), each of 4 orthogonal A₂ slots has E₆ complement and **248 = (8,1)⊕(1,78)⊕(3,27)⊕(3̄,27̄)** — the **27 entering three times**, indexed by the family A₂; E₆'s 27 = 16⊕10⊕1 with multiplicities **[1,10,16]** matching B883's independently-derived (E₇-cubic) values. FENCE (held explicitly): this is the **CITED-standard algebra fact** (E₈ ⊃ E₆×SU(3); Slansky/Bars–Günaydin) — it says nothing about whether the OBJECT reaches/forces E₈, which stays **EXHIBITS-NOT-FORCES** (B1033: object-forcing BLOCKS; the ends curvature-related, not nested). "27 enters 3×" is NOT "3 generations forced." Cert note relayed (cloud E₈ Cartan matrix asymmetric; rebuilt; result unaffected) | B1138 | `tests/test_b1138_structural_completion.py` | **NEEDS-LIT**: E₈⊃E₆×SU(3) as a generation-index structure; the gap between the algebra fact and object-forcing |
| T-SYMMETRY-POINT-TABLE | **THE SYMMETRY-POINT TABLE** (B1139, memos 23/24, verified two-bench; the value campaign's Lane A, honestly typed): the closing assignment's 15-row (color, T₃L, T₃R, Y, Q, B−L) table is **one SM generation** (QUANTIZED) — byte-identical to the cloud cert, 27 states, Q multiset {0:5, ±1:2, ±⅓:6, ±⅔:3} (18 colored + 9 singlets; Q_L doublet at Y=⅙, vector-like exotic D-quark at Y=−⅓, the 9-singlet trinification bidoublet grid), pinned non-circularly to the standard embedding by B1102's own 6Y multiset. And **sin²θ_W = 3/8** (Tr T₃L²=3, Tr Q²=8, closing-independent 72/72) — TYPED AS A **REPRODUCTION**, not a fresh emission: the direction (the hypercharge line → the ratio) is FORCED, but the normalization (the "/6") is exactly what **B991 proved is a convention** (anomaly-homogeneous q→λq). The canonical GUT value the object is consistent with, not one it emits | B1139 | `tests/test_b1139_symmetry_point_table.py` | **NEEDS-LIT**: trinification 27 as one generation; sin²θ_W = 3/8 as a forced-direction-with-convention value |
| T-ANOMALY-REALIZED | **ANOMALY-FREEDOM IS THE REALIZED E₆ THEOREM; NAIVE B−L IS UNPHYSICAL** (B1139, memo 24, verified two-bench): all **8 anomaly traces vanish exactly** (Tr Y, Tr Y³, Tr(T₃L²Y), Tr Q, Tr Q³, Tr(B−L), Tr(B−L)³, Tr(T₃R²Y)) — but e₆'s invariant degrees are **2, 5, 6, 8, 9, 12** (no degree-3 invariant, verified in the vendored module), so Tr(X)³ = 0 on the 27 for **any** Cartan element (spot-checked on a random non-physical one). The vanishing is the **general E₆ anomaly-freedom theorem realized on the closing's charges** — a consistency check (as memo 24 frames it), NOT a fresh forcing. Honest NEGATIVE beside it: the naive **B−L = 2(Y−T₃R)** is unphysical (members 5/3, −4/3, ±2); the physical B−L is a different Cartan = the open cell **SP-1** (memo 25). B−L as a quantity not denied — the naive relation is | B1139 | `tests/test_b1139_symmetry_point_table.py` | **NEEDS-LIT**: exceptional-group anomaly-freedom (no cubic Casimir) as automatic; the correct B−L Cartan in the trinification frame |
| T-64-ORGANIZED | **THE 64 ORGANIZED — the spacetime branch holds a graviton, not hypercharge** (B1140, memo 27, the finale, verified two-bench, slot-independent; verdict NEGATIVE): the E₆(−26) spacetime branch's 64 = 78−14 (Killing-complement of the fork's own su(2)×su(2)×su(3)) decomposes as **(spin-2,0;1) ⊕ (0,spin-2;1) ⊕ (1,1;3) ⊕ (1,1;3̄) = 5+5+27+27** — a color-singlet graviton (two spin-2) + 54 colored bi-vectors — with **INVARIANT CONTENT ZERO** (only 2 of 8 (0,0)-Lorentz states are color-singlets, the graviton middles). So **no u(1)/hypercharge can organize there**: the firewalled value hypothesis (B1134's relay — the 64 as the value target) closes as the campaign's **tenth honest value-negative, the first by STRUCTURE not scan** — the rep-level restatement of the fork z=0 (B1138), same 14-dim subalgebra (cc3's 2nd grounding: 14 = B8078 rung → 64 forced). THE CLOSING COMPOSITION: the fork's two forms share nothing — E₆(−14) the charges, E₆(−26) the geometry (incl. the graviton). SCOPE: the antilinear-θ complex-spin-2 gluing NOT checked (real-structure data absent; not confirmed/refuted). Routed to the kill-graph | B1140 | `tests/test_b1140_the_64_organized.py` | **NEEDS-LIT**: E₆(−26)/M(𝕆,ℂ) coset as spin-2 + colored; graviton-in-the-exceptional-Jordan programs; the value-organization no-go |
| T-SPIN-PAYMENT | **THE SPIN PAYMENT — the object's beat assigns the last free discrete bit** (B1141, memo 28, hostile-verified two-bench, three engines): the freedom ledger's last free bit (the spin lift = the two SL(2,ℂ) lifts of m004's holonomy, H¹(M;ℤ/2)=ℤ/2) is **assigned, not free** — the Gieseking beat (the object's own ℤ/2) closes over **exactly one** spin structure. Three load-bearing claims confirmed by own code: the beat's **intertwiner space is exactly 1-dimensional** (8×4 system, exact rank 3 / nullspace 1 + 50-digit SVD); **N(λ)=\|λ\|²≥0** so the twisted lift (needs \|λ\|²=−1) is impossible (positivity automatic — the ℚ(√−3) norm is positive-definite); **χ is beat-invariant** (length parity preserved, proved in general). Explains B1122 (a topological consistency condition, not a dynamical observable — why no coupling reads it). Two non-load-bearing errata relayed (ω minpoly x²+x+1 not x²−x+1; norm X²−XY+Y² not X²+XY+Y² — both positive-definite). NOT fermionic fields — kinematics only; SP-2 (is it the fermion-capable structure?) is the live hinge. NEEDS-CERT: the w̃-cohomological packaging + the Galois∘Fibonacci naming (beat-trilogy 16/17/18) | B1141 | `tests/test_b1141_spin_payment.py` | **NEEDS-LIT**: spin-structure selection by an antiholomorphic symmetry; the beat as Galois∘Fibonacci; chirality from which spin structure survives parity-reversal |
| T-LANE-C | **LANE C — the cusp spectrum is ζ_K = ζ·L(χ₋₃), GUE-generic; a placement, not a value** (B1142, verified two-bench, own mpmath/scipy; verdict NEGATIVE for value/graviton): density before statistics (only density can falsify) — the Laplace Weyl N(130)≈75,316 vs **108** observed → 697× too sparse → **not** the Laplace/geodesic spectrum. The 108 are the zeros of the **complete Dedekind ζ_K = ζ·L(χ₋₃)** (exact count **43+65=108**), what the object's scattering determinant φ=Λ_K(s−1)/Λ_K(s) (B739) is built from → the **cusp/scattering side**. GUE-distributed (KS D=0.1176, Poisson rejected) but **GENERIC** (Montgomery/Katz–Sarnak — a universality class, not object-specificity; discriminates nothing). **Graviton bridge DEAD** (cc3 B8129/B8130): Ruelle Euler product purely geodesic, L(χ₋₃) only via the cusped functional equation — graviton (geodesic) and Lane C (cusp) meet nowhere; the graviton is two faces, the finale independent. Routed to the kill-graph | B1142 | `tests/test_b1142_lane_c_cusp_spectrum.py` | **NEEDS-LIT**: scattering-resonance statistics of arithmetic cusped 3-manifolds; GUE genericity for Dedekind-zeta zeros |
| T-BL-FOURTH | **SP-1 CLOSES — B−L is a genuine fourth Cartan direction** (B1143, memo 25, verified two-bench on B1139's banked 27): solving color + the 10 forced quantized targets gives rank 5, nullity 1 → the family **c = [0, c₁, 0, −⅓, 1, 0]** (β_L auto-redundant); Tr(B−L)=Tr(B−L)³=0 for the whole family; **span{Y,T₃R} and span{Y,T₃R,T₃L} both UNSOLVABLE** (witnesses w·B−L = 3/2, 3) → B−L independent of the existing charges. The closing's Cartan content is complete (Y, T₃L, T₃R, B−L). **Confirmed two-bench with cloud memo 25** (reconciled in B1144): a naive vector, evaluated in B1139's frame, reads as Y − T₃L (in span{Y,T₃L}, non-uniform), but memo 25's own certificate (`sp1_bl.py`) builds B−L in the closing's coordinates where it is physical on all 27 and independent (golden_gate primary source) — a *cross-frame* difference, not a cloud error; both benches agree on the fourth direction. SP-3: 72 assignments → one table (invariant); anomaly batteries 30/30 + 6/6 vanish; "36/36" FLAGGED. SP-4 FLAGGED (three-A₂ frame, not four) | B1143 | `tests/test_b1143_bl_fourth_cartan.py` | **NEEDS-LIT**: the B−L Cartan in the trinification/E₆ frame; exceptional anomaly-freedom counting |
| T-SP2-SEAT | **SP-2 — the beat closes EXACTLY on the fermion-capable stratum's 27 over the selected lift; the last discrete spin-lift bit is assigned** (B1145, cloud memo 29, sealed prereg `c384dd3e` derived blind, verified three independent ways): the odd A1/su(6) stratum's **27 is minuscule** → restricts to the root-sl₂ as **6·(fundamental 2) ⊕ 15·(singlet)**; the beat **W=exp(q·e)** is upstream of the embedding, so **Ω=exp(ρ₂₇(q·e))∘gal** closes functorially. Exact over ℚ(√−3): relator=+I, C=ρ₂₇(−I)≠I with C²=I central, **Ω²=A27**, **Ω·A27·Ω⁻¹=A27**, **Ω·B27·Ω⁻¹=ρ₂₇(B⁻¹ABA⁻¹B)**. The exact algebra is the theorem. **Codex-sharpened fence:** the A₁ is su(2) *internal* to E₆ (centralizer su(6)), not the 4d Lorentz group; **no Pin structure, no Dirac index, no 4d chirality, three generations NOT proved** — "the generation's seat closes" is the program's *thesis* reading, not a theorem | B1145 | `tests/test_b1145_sp2_fermion_seat.py` | the internal→spacetime bridge (E₆(−26), B1140) is the unpaid verb |
| T-ONE-CLASS-OBSERVER | **THE ONE-CLASS THEOREM — the object's two independent no-self-closure obstructions are ONE ℤ/2 class** (B1183, PROVED): B760's QP-4 chord-sign obstruction and B1163's orientation obstruction are the same ℤ/2-torsor class under the **one global involution c**, whose restrictions are simultaneously the Galois generator on ℚ(√−3) (ω ↦ ω²), σ₄ on ℚ(ζ₅) (fixing √5), and trivial on ℝ. The proof is an **equivariance of torsors**: the map *orientation choice ↦ sign(Im) at the chosen embedding* is c-equivariant, and an equivariant map of torsors under one group is an isomorphism; nontriviality has the same source on both sides (c is an automorphism OF the object — amphichirality on the geometry side, the inseparable σ₄-orbit on the chord side), so **one basepoint choice trivializes both at once**. Consequences: B1169's S1 fully promoted (the four "cannot self-close" probes are ONE bit, proved); B1161's bypass-door label upgraded to PROVED-AS-DECOMPOSED. **Fence**: B760's SU(2) realization, its 15/32 coupling and its ±√3 evaluation are cited, not re-run; the theorem identifies the obstruction CLASSES, not the closing acts | B1183 | `tests/test_b1183_one_class_theorem.py` | **NEEDS-LIT**: ℤ/2-torsor obstructions to canonical orientation on amphichiral hyperbolic 3-manifolds; chord-sign sectors under Galois |
| T-QUINE-SPLIT | **SELF-NAMING WITHOUT SELF-SIGNING** (B1184, PROVED, split verdict with a proof on each side): the object **names itself completely** in mirror-even letters — its own description is emitted from its own alphabet — and **provably cannot sign itself**: the description is exactly one bit short of closing, and that bit is c. The two halves are independent results, not one hedged claim. **Fence**: the phenomenal reading is not asserted; the statement is about the record's own description-closure | B1184 | `tests/test_b1184_quine_synthesis.py` | **NEEDS-LIT**: self-reference/quine constructions with a residual automorphism; Kleene-style closure short by a torsor bit |
| T-RELATIONAL-BIT | **THE RELATIONAL BIT EXISTS** (B1192, PROVED): for the heterogeneous pair (the object, its √3-partner) the mirror is GL₂(ℤ)-realizable **only with det −1**, so **ε = −1 is a mirror-odd, dimensionless ℤ/2 class OF THE PAIR** — belonging to neither relatum — which **restricts to c**. Both Galois actions computed; **trace-invisible 340/340**; two-sided controls; 43/43 exact. The observer's bit therefore has a realized instance *inside the grammar*: a coupling can carry it. **Fence**: lens-scoped to the exhibited pairs; the general classification is the refinement cell (answered at B1195 by κ) | B1192 | `tests/test_b1192_close_loop_batch4.py` | **NEEDS-LIT**: det −1 realizability of mirror involutions over real quadratic orders; pair-level ℤ/2 invariants |
| T-SELECTOR-FREE | **THE OBSERVER'S BIT IS SELECTOR-FREE, AND THE SELECTOR SPLITS INTO THREE REGIMES** (B1196, PROVED): ε(A,M) = −1 is invariant under **simultaneous GL₂(ℤ)-conjugation** (8/8 conjugators, both determinant signs, rebuilt from scratch) — the bit **arrives as invariant pair-data with no selection act**. Beside it, the Selector resolves into an exact three-regime dichotomy (finite-transitive / compact uniquely-ergodic / non-compact non-normalizable), **a theorem in each regime**, with σ and λ sitting on the non-normalizable side — which is *why* they are anchors. B1183 is confirmed as the regime-one instance | B1196 | `tests/test_b1196_batch5b.py` | **NEEDS-LIT**: unique ergodicity as a selection principle; invariant pair-data without a choice function |
| T-PHI3-THREE-FACES | **ONE POLYNOMIAL, THREE FACES** (B1200, PROVED): the saddle equation of the object's own partition function, the founding obstruction κ − 2, and the boundary's complex structure all satisfy **Φ₃ = u² + u + 1**; the **saddle set IS {κ − 2, its conjugate}**; and the map between the faces is **u ↦ u² = the swap = c**. Two of the existence audit's eight items are therefore **one invariant seen twice**. Cross-seat: cloud reproduced the κ-law and selector-freedom; this bench verified the Φ₃ cross-link exactly and contributed the linking-map identification. **Independently re-reached from the holonomy** by cc3 (B8154, verified at B1208), giving Φ₃ a second derivation route | B1200 | `tests/test_b1200_one_polynomial.py` | **NEEDS-LIT**: Φ₃ as a common invariant of quantum-dilogarithm saddles and Fricke obstructions |
| T-V4-TORSOR-IDENT | **C4′ — THE FRAME AND BRANCH V₄s ARE ONE NAMED-ACTION TORSOR** (B1182, PROVED): the √−3-internal pair — the frame V₄ = ⟨c, r⟩ and the branch V₄ = Gal(ℚ(ζ₁₂)/ℚ) — are identified by the **unique label-preserving isomorphism** (c, r, θ) ↦ (k₁₁, k₇, k₅). The forcing is exact: c ↦ k₁₁ by B1174; **r ↦ k₇ by trace-reversal invariance** (tr(w) = tr(w^rev) proved on generic 2×2 matrices), so r fixes K pointwise and k₇ is the unique nontrivial branch leg doing so; θ ↦ k₅ by the group law. **An honest self-correction is part of the result**: B1175 guessed θ would be the K-fixer; the computation says r is. Free corollary — **the arrow is finite-place** (r's leg k₇ = the mod-4 form-class swap, finite orbits), so orientation stays the sole archimedean bit | B1182 | `tests/test_b1182_c4prime_resolved.py` | **NEEDS-LIT**: named-action torsor identifications; trace-reversal (anti-automorphism) invariance on character varieties |
| T-THREE-MECHANISMS | **THE THREE-MECHANISMS THEOREM — the record's three Yukawa-suppression mechanisms are GENUINELY THREE** (B1185, PROVED; answers L186): none is identifiable with another, by three **exact pairwise invariants**. **INV-1 ARENA**: the same up-coupling is rank 0 in the heterotic dressing (codex R017's naturality factorization re-run PASS on this bench — the exact 1×6 zero) while 6-nonzero on the object channel (memo 80, byte-identical at B1171) — one mechanism cannot both kill and allow the same coupling, so SEAM-Y ≠ object-kinematics and the difference is located in the **arena**. **INV-2 RANK + SELECTIVITY**: rank 0 and sector-asymmetric vs rank 2 and sector-universal. **INV-3 INDEX SPACE**: the family index is external (degree-2 trace field, multiplicities never 3) while the object channel is roster-internal with zero kills. The "one mechanism, three costumes" reading is **falsified**. Synthesis (fenced, organizing): three mechanisms = three layers of the stack — imported dressing / object channel / E₈ possibility fence | B1185 | `tests/test_b1185_three_mechanisms.py` | **NEEDS-LIT**: distinguishing suppression mechanisms by arena vs rank vs index space |
| T-DARK-ANNIHILATION | an exact **iff** law: dark pairs annihilate termwise rather than by cancellation, and s-darkness holds **iff the 5-side never donates √5 to an imaginary product** (**B393**) | B393 | `tests/test_b393_mechanism.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-ESCALATOR-RUNG1 | the escalator's coupling rule (C,D) = (M, M²) is **FORCED at rung 1 and a CHOICE above it** (B517's intertwining forces C = p(M)), with the explicit 8-letter carrier σ₈ locked. The forced/free boundary located at a specific rung (**B557**) | B557 | `tests/test_b557_carriers.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-E6-RECURRENCE-GENERIC | **THE GENERICITY THEOREM, and the programme's most consequential negative**: *"E₆ across three faces"* is forced by **one ADE classification**, so the recurrence is **generic, not evidence** — the faces are canonically linked (McKay ↔ Lie ↔ CIZ ↔ du Val are one list), E₆ is the only exceptional label an imaginary-quadratic field can reach, and the sister m003 shares ℚ(√−3) without being the knot. **Only the arithmetic atom π₁(m004) ↠ 2T survives** — and B993 later measures even that at ~1 in 3 of the census. Any claim of object-specificity must clear this arc first (**B727**) | B727 | `tests/test_b727_baserate.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-INTERBREAKING-LAWS | **TWO STRUCTURAL LAWS** on the 27, uniform across all three Galois pairs (zeros at 1e-21 to 1e-26): each frame's 27-singlet lies **entirely inside every other frame's 10-block**, exactly, because the singlet is a Π-weight vector (**B885**) | B885 | `tests/test_b885_interbreaking.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-MATTER-PENCIL | **THE MATTER PENCIL, exact**: P(x,λ) = det(xI − ρ(x₈) − λρ(x₁₆)) on the 27 factors over ℚ as F₁¹·F₂⁸, both cubic in x — **the 27 carries exactly SIX joint Π-weights** in two size-3 Galois orbits (multiplicities 1 and 8, the 8 being the octonion dimension as an exact weight multiplicity) (**B886**) | B886 | `tests/test_b886_matter_pencil.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-ONE-CLASS-COMPACT | **THE ONE-CLASS THEOREM** (compact pencil): the cubic κ splits [1,2] over K with the exact root certified by polynomial remainder, and its Kummer element lands in one class — the ancestor of the observer-side one-class theorem (B1183) in the value layer's own coordinates (**B910**) | B910 | `tests/test_b910_kappa.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-ONE-CLASS-VALUE | **TWO THEOREMS**: the one-class theorem **extends to the value layer** — the hierarchy element V (exact minimal cubic, irreducible, disc kernel {7,11}, [1,2] over K with a root certificate matching the solo derivation digit-for-digit) satisfies [α_V] = C = [α_μ] (**B918**) | B918 | `tests/test_b918_v.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-RANK-OBSTRUCTION | **THE RANK OBSTRUCTION (theorem-grade)**, stated beside the eleven rows any grand unified theory must satisfy — including that **non-SUSY SM coupling unification FAILS** and that proton-decay limits **exclude minimal SU(5)**. The requirements ledger is what any positive claim of ours is scored against (**B952**) | B952 | `tests/test_b952_rank.py` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-Y-NORMALISATION-NOGO | **THE HYPERCHARGE NORMALISATION IS NOT DERIVABLE IN PRINCIPLE — a THEOREM ABOUT THE EQUATIONS**, not a limitation of the object: it concerns the anomaly-cancellation equations **as algebraic forms**, and holds for **any U(1) charge assignment in any theory**. The paper's hypercharge claim is therefore *direction only*, by theorem (**B991**) | B991 | `NO DEDICATED LOCK (covered incidentally by tests/test_b1028_freedom_ledger.py)` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-MCKAY-ACCESS-GENERIC | **ACCESS TO THE McKAY GROUP IS GENERIC ACROSS THE METALLIC FAMILY** — a census of *grammars* (metallic words RᵐLᵐ and their mod-N shadows in SL(2,ℤ/N)), which is the right population because the chain **derives** m004 rather than selecting it from a manifold census. A companion negative to B727 (**B996**) | B996 | `NO DEDICATED LOCK (covered incidentally by tests/test_b1009_verification.py)` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-GOLDEN-CONDUCTOR-UNIQUE | **THE GOLDEN IS THE UNIQUE METALLIC GRAMMAR WHOSE OWN-CONDUCTOR SHADOW IS A McKAY GROUP** — proved over an **infinite family** at Tier 1 at the E₈ end, which is why it escapes the design flaws that voided B995 and B996. **The specificity that survives B727's genericity, stated positively** (**B997**) | B997 | `NO DEDICATED LOCK (covered incidentally by tests/test_b1028_freedom_ledger.py)` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-LISTENER-DERIVED | **THE LISTENER PAIR IS DERIVED, NOT CONVENTIONAL**: on CP¹_odd the projective action of the instrument is the full 60-element icosahedral group, Aut(2T × 2I) = S₄ × S₅ exactly, with no shear — three compute agents and three adversarial verifiers, every attack failed (**B1070**) | B1070 | `NO LOCK` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-COMPOSITION-GATE | **THE COMPOSITION GATE, closed to a two-line law**: on the θ-odd plane h(w) = χ(w)·W(w)₁₁ with χ the μ₃ homomorphism and W's entries never leaving ℚ(ζ₅) — convergent-independent with Track A (**B1073**) | B1073 | `NO LOCK` | re-audited into the registry at B1214; see the arc for scope and fences |
| T-CS-TORSION | **Amphichirality forces the Chern--Simons invariant to be 2-torsion, not zero.** For an amphichiral cusped hyperbolic 3-manifold CS = -CS, so 2*CS = 0; in the mod-1/2 normalisation the admissible values are exactly {0, 1/4}. Verified 6/6 on the amphichiral one-cusped census (m004, m136, m206 at 0; m003, m135, m207 at 1/4), against 1/394 = 0.25% of non-amphichiral manifolds having CS = 0. **Sharpens B303**, whose 'CS = 0 forced by amphichirality' is the special case that m004 happens to occupy; the reason given there is incomplete. Also a second separator of the sister pair: m004 at 0, m003 at 1/4. | B1224 | PROVED |
| T-NO-CANONICAL-SELECTOR | **No symmetry readable off the object can select within its own canonical class.** With G = Stab_Aut(D)|_T read off D (GC-15/B1191), and the candidate set built from object-canonical atoms (B1168: canonical ⟺ β-even ∧ dimensionless), G fixes every candidate — so the action is TRIVIAL, not transitive, every subset is invariant, and no selection is possible. Generalises B1203's *exactly 0 of 11,720* cut from c to every such G: invariance cannot distinguish among invariants. Consequence: MENU-1's 'forcing theorem that selects one map before looking' is **impossible in the form sought**, and any selector must use β-odd or dimensionful data — the reader's column. | B1225 | PROVED |
| T-BETA-ODD-IS-BIT-VALUED | **The object's β-odd channel is bit-valued, so value-questions fired at it are type-mismatched at the source.** Two parts. (i) *The equivalence break*: B1012's chain `blind-to-k ⟺ CS = 0 ⟺ amphichiral` fails at the second link in **both** directions — m003/m135/m207 are amphichiral with CS = ¼, and m208 is chiral with CS = 0. The identity `∂S/∂k = −CS` stands, so the correct statement is **blind-to-k ⟺ the complex volume Vol + i·CS is real**; the scale wall holds for m004 but is a *contingent* datum (which element of the symmetry-given ℤ/2 the object occupies), not a symmetry theorem. (ii) *The typing*: by B1168 (canonical ⟺ β-even ∧ dimensionless) the 28 SM parameters split 6/3/16/3, and the β-odd box — θ_QCD, δ_CKM, δ_PMNS — is the only one with an object-side output, namely a ℤ/2 (T-CS-TORSION): **one bit**. All 3 probes ever fired into that box (B1027, B1137, B813) demanded a continuous value; 0 asked the bit, so their negatives measure the mismatch, not the object. No CP phase derived — Gate 5 intact; the forward question is registered as **L192**, not banked. Lock `tests/test_b1226_beta_odd_box.py`. | B1226 | PROVED |
