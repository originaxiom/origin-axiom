# ORIENTATION — the whole programme on one page, for a seat that has never seen it

**Written 2026-08-11 by the CONSOLIDATION REFRESH.** `WORKING_RULES.md` §0 currently sends every
new seat to **five** documents before it may compute (`COMPUTE_THE_PROGRAM`, `THE_LADDER`,
`THE_FRAMEWORK`, `THE_CAMPAIGN`, plus the rules themselves). This is the one door. It does not
replace them — it is what you read *first*, and every grade below cites the arc that set it, so
it can be checked by grep and decays visibly like any other surface.

**Currency rule, the same one the ladder binds itself by:** a statement here moves only with a
cited arc. If you find one stale, fix it *with a citation*.

---

## 1. THE OBJECT, in four lines

`L = [[1,1],[0,1]]`, `R = [[1,0],[1,1]]` — the two primitive shears. Their first mixed product

> **`A = LR = [[2,1],[1,1]]`**, trace 3, spectrum `φ^±2`

is the monodromy of the **figure-eight knot complement `m004`** — the unique arithmetic knot
complement, trace field `ℚ(√−3)`, minimal volume, unique hyperbolic structure by Mostow.

**The founding sentence** — *existence is a frustrated cancellation* — is one number:
**`κ = tr[a,b]`**, with **`κ = 2` ⟺ the cancellation completes ⟺ nothing** (B309/B518).

## 2. WHAT FORCES IT, AND WHAT THAT COSTS — the honest axiom bill

| | |
|---|---|
| **A1–A6** | two records · invertible over ℤ · orientation-preserving · one primitive move each · first *mixed* torsion-free closure · minimality. **144 → 1** (`UNIQUENESS_THEOREM.md`) |
| **A7** | the residual bit: A1–A6 cannot distinguish `LR` from `RL`. **B979: A7 is exactly where φ enters** — the Möbius fixed-point polynomial is a *based*, not a class, invariant |
| **the exchange axiom** | **`P` (the record swap) is NOT forced by A1–A6** — B16: *"that axiom is plausible, but it is still an axiom."* It is what makes the half-step `F = LP`, `F² = A` exist, hence the **trace map** — which the atlas measures at **45 % of all probes**. **B1026** |
| **five typed external data** | two 𝔽₂ bits (time's arrow; chirality = τ), one scale, one Lie type, one rank-closing VEV direction. **The fifth is unsourced** (B1017) |

**Read the third row carefully.** `THE_CLAIM.md`'s counted list is *"A1–A6 + A7 + five typed
external data"*. The exchange axiom is not in it. **It does not disturb the derivation theorem**
— that chain never passes through the half-step — but the whole trace-map substrate rests on it.

## 3. THE LAYERS — what the object turns into

| layer | content | key arcs |
|---|---|---|
| **1 — two ends** | not one-ended: hyperbolic `ℚ(√−3)`/2T/**E₆** (α=0) → Euclidean (α=2π/3) → spherical `ℚ(√5)`/2I/**E₈** (α=π). E₇ excluded by Niven | B248, B250, B249, B253 |
| **2 — the algebra** | 2T → E₆ by McKay; the build **is** `M(𝕆,ℂ)`, the magic-square entry, by explicit isomorphism (0/3003 mismatches) | B882, B904 (P70) |
| **3 — the cascade** | *maximal residual symmetry among registerable options* → `su(3)⊕su(2)⊕u(1)³`; **the global ℤ₆ form DERIVED** (a choice the SM cannot make about itself); **hypercharge direction DERIVED**; termination theorem | B892, **B862**, **B864**, B863 |
| **4 — the value layer** | `I = −1` exact; `λ = 2304/953`; `sin²θ_W = 3/8` **REPRODUCED, not predicted** | B908, B931, B919 |
| **5 — the boundary** | after B1013's re-sort, **four of five classic "walls" are boundary or specification. The value layer is the one real, tested WALL** | B1013 |
| **6 — the machine** | prereg with committed hashes, 26 gates, the atlas, an error ledger that has corrected its own drafting seats | — |

## 4. WHERE IT ACTUALLY STANDS — the only scoreboard that counts

`WHAT_WOULD_COUNT.md` is the programme's sole whole-programme seal, and it is unambiguous:

- **Tier 0 (the object is forced)** — **DONE**, price exact.
- **Tier 1 (structure without values)** — **SUBSTANTIALLY DONE.** ℤ₆ and hypercharge are
  **DERIVED** — full credit by that document's own bar.
- **Tier 2 (ONE sealed dimensionless ratio, out of sample)** — **NOT DONE, and since B1005 with
  NO LIVE CANDIDATE.** Three sealed crossings, three negatives (B915 16σ · B925 · B929).
- **Tiers 3–4** — not done, not close.

> **Tier 2 is the tier that decides whether this is physics at all, and everything above Tier 1
> is supported by zero results.** That is not discouraging; it is the first time the distance has
> been measurable.

**"Parameter-free" means zero free *dimensionless* parameters** — the target is countable: **19**.
**One dimensionful anchor is everyone's** and taking it is the correct end state, not a failure.

## 5. THE FIVE TRAPS THAT HAVE ACTUALLY COST ARCS

1. **"We don't have X" is a hypothesis, never a conclusion** (`WORKING_RULES` §0). It failed
   **nine times in one session** on 2026-08-09. Grep the **code**, not claim lines.
2. **A claim line says what an arc *concluded*; the source says what the repo *can do*.** B1007
   rebuilt a Maass solver while a working sealed one sat on main. **And it cuts both ways** —
   B787's claim line *overstates* its body.
3. **Name your quantifier before computing** (P0). *A closure survives the relational re-read
   exactly when its scope sentence names no manifold.* Six of 25 pre-B800 closures failed this.
4. **Undeclared convention drift (E1) is the most recurrent error class, and it is still live.**
   Three collisions found in this pass alone: **`θ`** names three distinct objects; **B62 = 2 ×
   P33** (full height-±h space vs positive roots); **B939's shadow-map prose is transposed**
   against its own code. All three were found by *diffing two surfaces*, not by reading either.
5. **A criterion that cannot fail is not a test.** Run the vacuity control *before* reading the
   result. B986 died on exactly this — ~70 % of random polynomials passed it.

## 6. WHAT THE INSTRUMENTS ACTUALLY COVER — measured, not named

Every instrument that has been measured covers substantially less than its name implies, and the
programme published the number each time. **The unmeasured ones are where to look next.**

| instrument | measured coverage |
|---|---|
| arcs carried by a **curated** consolidation (`LAW_MAP` · `THE_FRAMEWORK` · `THEOREM_LEDGER` · `CLAIMS` · `THE_LADDER`) | **37.9 %** — 579 of 933 absent, **353 of them PROVED** (`consolidation/DEBT_LEDGER.md`) |
| arcs carried by **some** navigational surface | **100 %** — the *generated* views are complete by construction (`GOVERNANCE` §12). **Reachable ≠ distilled**, and only the second number is a gap |
| `LAW_MAP` test locks | **~4 %** of rows; the decided posture is *unenforced index with traceable provenance* |
| `kill_graph` | **≈66 %** of negatives (95 % CI 56–80) |
| the atlas | **0.629** top-3 concept coverage in the current era vs 0.995 in its own |
| `chain-locks` | checks the cited test **file exists**, never that it tests the link (B1025) |
| the test suite | **3,744 pass, 0 errors**; 4 failures, **none mathematics** (B1025) |

## 7. THE SHORTEST HONEST SUMMARY

> A four-letter combination principle, formalised as six motivated axioms, forces the
> figure-eight knot complement **up to one bit** — and that bit is where φ enters. The object so
> forced carries a two-ended geometry whose ends are E₆ and E₈ by McKay, an algebra that is the
> magic-square entry `M(𝕆,ℂ)`, and a measurement cascade that lands on the Standard Model's
> algebra, **derives the global form the SM cannot fix about itself**, derives hypercharge, and
> **halts there by theorem**. What it does not supply — rank reduction, matter mass from the
> adjoint, scale, time, chirality, the 27-VEV — is in each case a **theorem with a named
> mechanism** rather than an absence. **It has never produced one sealed dimensionless number,
> and it says so.**

**All verification is INTERNAL** — the owner plus AI seats cross-checking each other. Nothing
here is peer-reviewed. *"Verified" always means recomputed by a second internal pipeline.*

---

**Anchors:** `GOVERNANCE.md` (constitution) · `METHOD.md` (its generative twin) ·
`WORKING_RULES.md` (binding, every session) · `THE_FRAMEWORK.md` (what we have) ·
`THE_LADDER.md` (what we lack) · `WHAT_WOULD_COUNT.md` (the bar) ·
`COMPUTE_THE_PROGRAM.md` (the pre-compute protocol) · `ERROR_LEDGER.md` (E1–E36, read before
repeating one) · `consolidation/DEBT_LEDGER.md` (what no *curated* consolidation carries).
