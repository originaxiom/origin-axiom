# The Recognition campaign — compress, then identify the generating object

*(Registered 2026-07-03 after the eight-kill day. The read: we are computing OUTPUTS of a theory
we have not identified. EXPAND mode is suspended; this campaign runs RECOGNITION: the full
constraint list below, then pre-registered identification sweeps against the classical zoo.
K020's counterweight re-read before registration: an absorbing loop is avoided by kill
conditions on every sweep — "no match in the declared zoo" is a bankable outcome. Firewalled.)*

## Phase 0 — THE CONSTRAINT DOCUMENT (what any candidate theory MUST reproduce, all exact)

**Habitat.** Level-N Weil representation (odd N), theta lift (half-characteristic; the FORCED
polarization — C6), Par-twisted pair observable `tr(Par·P_a·Q_b)`, seeds = mapping tori of RᵐLᵐ.

**C-1 (fields).** Singles — including two-block words and fiber covers — are √−3/√−15-free
(real-quadratic); at N=45 the singles collapse to four cells of exactly 1/4. Pairs at N=15 land
in H = ℚ(√5,√−3) with √−15 content (44/49 cells); at N=45 in the 12-dim compositum
ℚ(ζ₉)⁺·H (144/144 cells imaginary). The home GROWS with the level.
**C-2 (the flagship).** t(0,4) = −1/48 − (1/80)√5 − (1/48)√−3 + (1/48)√−15 exactly.
**C-3 (sum rules).** Every row and column of every pair table sums to exactly 0.
**C-4 (parity structure).** Coxeter-oddness s(a, 12−b) = −s(a,b); column 0 forced silent; the
value stratification of (1,2) = the prime-3 parity split (rigid rows {0,4,6,14} at ±1/48 =
the trivial-at-3 tower; row 16 exactly silent with NO forcing law — three involutions killed).
**C-5 (the exact tables).** Value sets: (1,2) ±{1/48,1/60,1/80,1/120,1/160,1/240,1/480} rank 4;
(2,3) ±{1/144,1/288}; (2,4) ±{1/120,1/240,1/480}; (3,4) ±{1/48,1/96}; (1,3) = (1,4) = 0 exact.
Aggregates Σs² = 43/7200, 1/2304, 3/3200, 1/192, 0, 0.
**C-6 (non-locality).** The values do NOT factor through (m mod 3, m mod 5, CRT labels) —
the three-factor product class is refuted with a 0/200 null; the support law dies at
(3,4)-bright vs (1,3)-dark (identical covering types). Finer than spectra.
**C-7 (functoriality).** Gluing kills (all six two-block words seam-null); covers relabel
exactly (t_cover(a,b) = t_base(7a mod 20, b)); word rotation acts by the √5-Galois involution.
**C-8 (the decode).** J·Par = ζ₆⁻¹·X·Z with J = Ŝ² monomial (j → 1−j): the observable is a
parity-signed one-step Weyl hopping amplitude. The κ-word D̂WR̂D̂⁻¹WR̂⁻¹ has trace exactly 1.
**C-9 (the minimal sector — the sharpest constraint set).** ord(W₁@N) = 4N/3 at every tested
level. A unique invariant two-state helicity sector (tr Ŝ = 0, dihedral relation GLOBAL) exists
iff m = N/15 is a prime power (m = 1, 3, 5, 9 ✓; m = 15 ✗ — a LOCAL object failing to glue),
with exponent/phase/trace: (6, 108°, 1−φ), (6, 36°, φ), (25, 90°, 0), (54, 108°, 1−φ) —
phase fractions {3/10, 1/10, 1/4, 3/10}. The sector seam-couples to itself at ±1/48.
**C-10 (denominators).** All values 2^a·3^b·5^c; the set {48,96,120,144,240,288,480} = 24·k
(exception 160) — the 24 of η/framing sits in the data.
**C-11 (the E₆ germ).** Six tangent directions integrable through order 3; universal-τ = −2√3·i
STRICTLY first-order; the depth-2 τ-defect is θ-GRADED (F₄-targets saturate, escape ~3× down).
**C-12 (context).** The two-ended E₆+E₈ structure with E₇ Niven-excluded; disc (5)·(−3).

## Phase 1 — the recognition sweeps (each pre-registered; kills are bankable)

- **R1 — the Dedekind/Rademacher/multiplier zoo** (FIRST; pre-registration below).
- **R2 — the local-global reformulation**: the two prime towers separately + the explicit
  gluing-obstruction cocycle at N = 225 (WHY the local sectors fail to align — computable).
- **R3 — the reciprocity identification**: Deloup–Turaev / abelian-TQFT linking forms /
  modular data against C-1…C-8.
- **R4 — the Kashaev bridge** (= PD3.1).

## R1 pre-registration (committed before computation)

**The declared zoo** (exact, computed for the seed matrices A_m = RᵐLᵐ and the relevant
level-N data): the classical Dedekind sum s(d, c); the Rademacher function
Φ(γ) = (a+d)/c − 12·sign(c)·s(d, |c|); the η-multiplier exponent; the θ-multiplier exponent;
the level-N Gauss/Milgram phase; Dedekind sums at the tested levels' cusp widths.
**The targets**: the four sector phase fractions {3/10, 1/10, 1/4, 3/10} at m = 1, 3, 5, 9
(as functions of the level) and the rigid value 1/48.
**Match criterion (declared)**: exact rational equality, allowing ONE global affine map
q ↦ αq + β with α, β ∈ ℚ fixed across ALL data points simultaneously.
**KILL**: no zoo member (with one affine map) reproduces the phase quadruple; separately, none
reproduces 1/48. Either kill banks; a hit escalates to the derive-don't-fit test (Phase 2:
re-derive the full C-5 tables from the identified theory).


## R1 — first pass banked (2026-07-03): the seed-indexed classics MISS

Computed exactly: Rademacher Φ(A_m) = 3 for ALL m ∈ {1,3,5,9} — **constant across the metallic
family** (a new small exact datum; a constant cannot generate the varying phases). Dedekind
s(1,m) = 0, 1/18, 1/5, 14/27 — no single affine map carries either zoo member to the phase
quadruple {3/10, 1/10, 1/4, 3/10}. **Both seed-indexed classics are killed under the declared
criterion.** Recorded subtlety: the sector phases are indexed by the LEVEL N = 15m (the golden
seed quantized at growing level), not by the seed matrix — the level-indexed zoo (the Weil/Maslov
index of the A₁-action on the level-N theta space; η-quotient multipliers; level-scaled Dedekind
sums) is the registered continuation, computable directly from the banked engine. 1/48 remains
untested against the zoo (queued with the level-indexed pass).
