# THE FRAMEWORK — the whole thing, put together

**Assembled 2026-08-08 on the mature corpus (152 post-B800 arcs, 125 PROVED).**
Supersedes `ROADMAP_TOE.md` (2026-07-08), which was written **before** the cascade
campaign and still describes the programme's position as *"the kinematic/symmetry frame is
forced arithmetic"*. That has not been the position since B860–B980.

**Motivation, stated plainly, per the programme's own policy** (*the firewall is for
sobriety, not for silence*): the thesis is that **reality emerges from the four-letter
combination principle aAbB**. This document assembles the mathematics that bears on it.
Claim language stays calibrated — **DERIVED**, **REPRODUCED**, **STRUCTURAL**, **ABSENT**
— so that when something fires you can tell it apart from something that merely sounds
like it. Gate 5 holds: no measured value enters `CLAIMS.md`.

---

## LAYER 0 — THE FOUR LETTERS ARE THE AXIOMS

The aAbB principle is not a metaphor here. It is **A1–A4** of the uniqueness theorem:

| axiom | content | the letters |
|---|---|---|
| **A1** | the state space is **ℤ²** — a *pair* of record-counts (not one, not three) | **two records: a, b** |
| **A2** | updates are invertible over ℤ (`GL(2,ℤ)`) | **each letter has its inverse: A, B** |
| **A3** | orientation-preserving (`det=+1`, `SL(2,ℤ)`) | the orientation the object later *breaks* |
| **A4** | one primitive update per record — exactly the two shears `L`, `R` | **the four letters, and only four** |
| **A5** | the first *mixed* closure is torsion-free | forces the word to be **mixed** — aAbB, not aA |
| **A6** | minimality (first hyperbolic class) | picks the **smallest** such object |

> **A1–A6 force the persistent sector `A = LR = [[2,1],[1,1]]`, and from `A` the entire
> proven core P1–P16 follows as exact consequences.**

**A7 is the whole residue.** A1–A6 are symmetric under swapping the two records, so they
fix the object only up to the order `LR` vs `RL`. That is **one bit** — *"the smallest
piece of inserted structure in the whole construction"*.

**B979 (2026-08-08) settled what that bit costs and buys.** It is **load-bearing, not
cosmetic**: `LR` and `RL` are conjugate (so any *class* invariant is blind to the
difference), but the Möbius fixed-point polynomial is a **based** invariant and it differs
— τ²−τ−1 (roots **φ**, −1/φ) versus τ²+τ−1. And:

> ### **A7 isolates, to a single binary choice, where the specific number φ enters.**

**So the four-letter principle forces the object up to exactly one bit, and that bit is
the origin of φ.** That is the programme's sharpest statement about aAbB, and it is a
theorem, not an aspiration.

**The honest boundary of Layer 0:** A1–A6 are *"motivated but not laws of nature"* by the
theorem's own words. Deriving the substrate from nothing is **mapped dead** (B949). The
result is **conditional uniqueness**, which is a real and unusual thing to own.

---

## LAYER 1 — THE OBJECT, AND IT HAS TWO ENDS

`A = LR` is the monodromy of the **figure-eight knot complement m004** — the minimal
hyperbolic 3-manifold, unique complete metric by **Mostow rigidity**, so its geometry is
*canonical*, not chosen.

**It is not one-ended. B248 proves a cone-angle transition through all three curvature signs:**

| cone angle α | geometry | trace field | McKay |
|---|---|---|---|
| **0** | **hyperbolic** (complete cusp) | **ℚ(√−3)** | 2T → **E₆** |
| **2π/3** | **Euclidean — flat, the transition** | ℚ (degenerate) | — |
| **π** | **spherical** (ℤ/2 orbifold, L(5,2)) | **ℚ(√5)** | 2I → **E₈** |

**One AJ recursion carries both ends** (B261). **E₆ is the only chirality-capable end**
(B253). Niven's theorem forces clean quadratics *only* at the two ends, making E₆+E₈ and
the E₇ exclusion **one arithmetic fact** (B249).

**Exact invariants.** Vol = 6·Λ(π/3) = **2.0298832128193072500424051081…**;
**CS = 0 exactly at the hyperbolic end** (forced by amphichirality), **CS = π²/5 at the
spherical end** (B250). The Mostow metric **solves 3d vacuum Einstein exactly, Λ = −1**
(B259, verified).

**The family is part of the object.** Sister **m003** — commensurable, index 12 in
PSL(2,𝒪₋₃), same trace field (B803, B855). The **child**, the **metallic family**, the
**two chiralities**: the object breaks **c** (conjugation/orientation/time) but is
**θ-symmetric** — amphichiral. That amphichirality is not decoration; it is what forces
CS = 0, and therefore what makes the object scale-free.

---

## LAYER 2 — THE ALGEBRA

**2T ⊂ SU(2) → E₆ by McKay.** And the build has a classical name: it is **M(𝕆,ℂ)**, the
Freudenthal–Vinberg magic-square entry, constructed explicitly over ℚ (B882, B904).

- the **27** built on the Chevalley frame via the e₇ 3-grading (B883)
- the **invariant cubic** and the SM-graded Yukawa-support table (B884)
- the **triality tiling** theorem (B875); **32 = 16 + 16̄** at every enhancement point (B872)
- the charge plane (x₈, x₁₆) carries **exactly three distinguished lines** (B866)
- the **matter pencil** P(x,λ) factors over ℚ (B886); three cubics, **one quadratic
  resolvent** (B888)
- **E₆-across-faces is forced** (B727) — the atom being ℚ(√−3) as the unique arithmetic knot

---

## LAYER 3 — THE MEASUREMENT CASCADE (the interface to physics)

**One principle** (B861): *maximal residual symmetry among **registerable** options*,
where registerable means the 27's generation structure survives.

| result | status |
|---|---|
| first measurement theorem (FMT) | B877 |
| second measurement → **su(3) ⊕ su(2) ⊕ u(1)³** | B892, independently rebuilt B961 |
| **the global ℤ₆ form: [SU(3)×SU(2)×U(1)]/ℤ₆** | **DERIVED (B862)** — resolves an ambiguity **the SM itself cannot fix** |
| **hypercharge = the unique gaugeable U(1)** | **DERIVED (B864)** — ψ, χ anomalous over chiral matter; b = c = 0 forced. *Direction only; normalisation is homogeneous and not derivable* |
| **the TERMINATION THEOREM** — the cascade halts because the SM is the **terminal registerable algebra** | B863 |
| three generations, structurally; D₂ carries the hierarchy | B897, B928 |
| menu completeness P5; padding lemma; gates G4–G7 | B873, B865, B868–B871 |
| the Sakharov preconditions, structurally | B867 |

**And the naming that matters: the cascade IS an adjoint Higgs mechanism** (B964) — the
unbroken group after an adjoint VEV *is* the centralizer of that element. The programme
was doing GUT symmetry breaking all along without calling it that.

---

## LAYER 4 — THE VALUE LAYER

Exact arithmetic, no fitting: **I = −1** as the Leibniz sign (B908); **λ = 2304/953**
forced, with 2304² = 2¹⁶·3⁴ **derived** (B931); the **one-class theorem** and its
extension to the value layer (B910, B918); the twist-norm law (B946).

**sin²θ_W = 3/8** at tree/GUT level (B919) — **REPRODUCED, not predicted**; it is a known
GUT relation, and the run to M_Z **missed at 16σ** (B915).

**Three sealed crossings, three negatives:** B915 (16σ), B925 (outcome B by the chain's own
algebra, before data voted), B929 (right shape, index 1.84; wrong magnitude 5–9×). And the
deeper reason (B936): **the value layer is frame-relative** — no value appears in any
invariant of the Hermitian pair.

---

## LAYER 5 — THE BOUNDARY, WITH MECHANISMS RATHER THAN DISAPPOINTMENTS

This is the layer that changed most in the last month. Each line is a *theorem with a named
cause*, not an absence.

**Rank.** E₆ has rank 6; the cascade's landing site has rank 6; the SM has rank 4.
**Centralizers of semisimple elements contain a maximal torus, so every measurement is
rank-preserving** (B952). Closed on all routes: 63 inner sign-gradings (B959), the outer
route (τ makes the 27 self-dual on Fix(τ) → the 27 goes real), abelian holonomy (H₁ = ℤ ⟹
cyclic image, B955), finite images (B959), and the adjoint-form escape is self-defeating —
ω₁ ∉ the root lattice, so the adjoint form **has no 27** (B960).

**One cause behind it: τ does double duty** (B963). τ ≠ id **is equivalent to** the 27 being
complex, **and** τ is the only rank-reducing involution. Spend it on rank and you land in
Fix(τ) where 27 ≅ 27̄. *(Not an uncertainty principle — no conjugate pair, no bound, reality
type is discrete. The analogy was tested and rejected.)*

**Matter mass.** **78 ∉ 27 ⊗ 27**, so **no adjoint VEV can give any 27 fermion a mass**
(B978/V5). The adjoint half does gauge structure and provably cannot do matter mass.

**Scale — and this now has a mechanism.** Gukov's complex-CS action splits as
**k·I_CS + iσ·I_grav**, with **k ∈ ℤ quantized** and **σ not quantized**, where
**G_N = 1/(4σ)**; both are *external couplings*, not manifold invariants. The object has
**CS = 0 exactly**. So **the integer-quantized term vanishes identically and only the
unquantized gravitational term survives.** The object cannot fix a scale because its own
**amphichirality deletes the quantized half of its action.** That upgrades B413/B811's
scale-free negatives from observation to explanation. *(Normalisation check owed before
this is a claim rather than a lead.)*

**Time, 4d, signature.** All the observer's: **no canonical arrow, no canonical 4d filling,
no canonical Wick rotation** (B716). The object supplies four incompletenesses — space,
time, charge, value — and the observer supplies every closing (B717). Thermal time **fails
on identity**: the object's clock is tracial type II₁ with **trivial modular flow** (B721).

**The 3d→4d lift EXISTS** — B277 proves a **canonical class-S lift** (fiber Σ₁,₁ → 4d N=2*
SU(2), monodromy **φ = RL**, *A7's bit again*). It fails to be a chiral 4d SM for two named
reasons: it is **N=2 (non-chiral)** and needs an external N=2→N=1 datum, and **the 6d type
is still a free input**.

**The 27-VEV.** Needed for rank reduction with the 27 kept complex. It is an **input in
every framework** and **canonical nowhere** — 𝕆P² = F₄/Spin(9) is homogeneous (B962). The
one live opening: Kato–Yukie classify rational orbits of *pairs of 27s* by **cubic étale
algebras**, and **L138 FIRED** (B969) — the object's canonical pair returns **K** exactly,
3/3. *Scope: an orbit is not a point.*

**Withdrawn today (B980):** the `k=3 → GΛ=2π → 122 orders` chain. Two stacked conflations —
a 3d level fed into Smolin's *3+1-dimensional* formula, and the **anyon** level (B218/B224)
read as a gravitational one, where Smolin's k **is** Λ rewritten, so equating them *posits*
the answer. **The 122-order shortfall was never the object's.**

---

## LAYER 6 — THE MACHINE (the second product, and not the lesser one)

Preregistration with committed hashes before compute; 19+ governance gates run in seconds;
the recurrence atlas; three seats under **integrate-don't-merge**; a verdict for every arc;
an error ledger that has corrected banked numbers **and its own drafting seats** without
external input.

Three gates were added on 2026-08-08 alone, each aimed at a failure that had actually
occurred: `lawmap-scope` (compression drops scope), `retraction-sweep` (retracting a claim
doesn't retract its instances), `representation-sweep` (arcs that never got a row).

**Why this matters to the thesis:** L138 fired *sealed*. `GΛ = 2π` sat for weeks looking
exactly like a result and was circular. **The machine is what lets you tell those apart** —
without it, a ToE claim and a coincidence are indistinguishable from the inside.

---

## WHAT REMAINS — ranked, and each one named

1. **L138's scope** — a canonical *orbit* is not a canonical VEV. Closing that gap is the
   single most valuable open item; it is the only live route to a distinguished VEV.
2. **The two ends' asymmetry in the quantized sector** — CS = 0 at E₆, CS = π²/5 at E₈. The
   quantized term survives at one end and dies at the other. Computable, two-ended, unrun.
3. **L134** — the twelve exotic states per generation (27 = 16+10+1). Any E₆ model must make
   them heavy. Never addressed here.
4. **L141** — the anomaly layer is scale-free **by theorem** ('t Hooft anomalies are
   RG-invariant), which makes it the one part of QFT a scale-free object may speak about.
5. **L144** — B167's five-door map is POSTULATED, orphaned, and pre-dates the cascade; its
   door-5 dismissal rests on a premise B862/B864/B892 overturned.
6. **The N=2→N=1 datum** and **the 6d type** — B277's two named blockers on the 4d lift.
7. **Chirality without an inserted closing**; strong CP; the hierarchy; ν-mass.
8. **13 arcs** still owed LAW_MAP rows; **43 leads** open or partial, **35 of them pre-B800**
   and needing re-triage against the mature object.

---

## THE ONE-PARAGRAPH STATEMENT

The four-letter combination principle, formalised as six motivated axioms, **forces** the
figure-eight knot complement up to a single binary choice — and that choice is exactly where
φ enters. The object so forced carries a two-ended geometry whose ends are E₆ and E₈ by
McKay, an algebra that is the magic-square entry M(𝕆,ℂ), and a measurement cascade that
lands on the Standard Model's algebra, **derives the global ℤ₆ form the SM cannot fix**,
**derives hypercharge from anomaly consistency**, and **halts there by a termination
theorem**. What it does not supply — rank reduction, matter mass from the adjoint, scale,
time, chirality, the 27-VEV — is in each case now a **theorem with a named mechanism**
rather than an absence, and in the scale case the mechanism is the object's own
amphichirality. The remaining distance is enumerated above, and every item on it is a
computation someone can run.
