# THE FOUR FORCES — by their names, with what the object supplies and what it does not

*(Owner, 2026-09-06: "why don't we identify four fundamental forces of our program to easier
communicate… why don't we call them their names?" This file exists so we can. **Naming a slot is not
claiming the physics** — the firewall (`PRACTICES.md`: no SM values to `CLAIMS.md`; physics readings
wait on L91) governs what may be **claimed**; it never required us to be vague about what we are
**talking about**. Every row cites where it is banked and states its own fence.)*

---

## STRONG — SU(3) colour

| | |
|---|---|
| **the object supplies** | the **su(3) factor** of the measurement cascade: the second measurement lands on **su(3) ⊕ su(2) ⊕ u(1)³** (B892, verified independently B961) |
| **fenced** | that algebra is **dim 14**; the SM's is **12** — **two extra abelian factors**, so the chain is **two steps from the SM, not zero** (`SM_SPECIFICATION_LEDGER` D1) |
| **compact colour** | lives in the **E₆(−26)** branch (B1140), which **B1265** shows is **outer** and unreachable from the object's own D₂ |
| **not supplied** | α_s, or any coupling value |

## WEAK — SU(2)

| | |
|---|---|
| **the object supplies** | the **weak su(2) ideal** — the object supplies *that there is one* (B1250) |
| **fenced** | **sin²θ_W = 3/8** is a **known GUT relation reproduced, not predicted**, and is **non-discriminating**: the ratio is 3/5 for **5, 5̄, 10, 16 and 27 alike** (B1250). The run to M_Z **misses at 16σ** (B915) |
| **not supplied** | the weak coupling, the W/Z masses, the Weinberg angle as a *measured* quantity |

## ELECTROMAGNETIC — U(1)

| | |
|---|---|
| **the object supplies** | **hypercharge is the UNIQUE gaugeable U(1)** in the chain's abelian sector — ψ and χ are anomalous over chiral matter, so b = c = 0 is **forced** (B864). And the **global form** [SU(3)×SU(2)×U(1)]/**ℤ₆** is **derived** (B862) — **an ambiguity the SM itself cannot fix** (Tong 1705.01853 leaves Γ ∈ {1,ℤ₂,ℤ₃,ℤ₆}) |
| **fenced** | B862 is **conditional** in its own words (*"conditional on exactly what the cascade is conditional on"*); **direction only — the normalisation is not derivable**; and the anchoring Y ↔ electric charge is **I-23, UNEARNED** |
| **not supplied** | α, the electron charge, any value |

## GRAVITY

| | |
|---|---|
| **the object supplies** | a **spin-2** in E₆(−26)'s 64: the 64 decomposes under su(2)×su(2)×su(3) as a colour-singlet **graviton** (two spin-2) + 54 coloured bi-vectors (B1140); and the Ruelle = graviton product / the 2+1 one-loop (B8112) |
| **fenced** | **INVARIANT CONTENT ZERO** — hypercharge cannot organise in the spacetime branch. The Kodama Λ is **122 orders** from observation (H11, firewalled). L154's Brown–Henneaux join is **adjudicated, not asserted** (B1034 lock) |
| **not supplied** | G, Λ, any dynamics — **the Ward identity has genuinely ZERO arcs** |

---

## WHY THE FOUR DO NOT MEET — named, and now explained

**B1140:** the fork's two real forms **split the world with nothing shared** — **E₆(−14)** took the
charges (hypercharge selection, sin²θ_W, the generation table, the anomaly zeros, B−L); **E₆(−26)**
took the geometry (**Lorentz, compact colour, the graviton**).

**B1265 says WHY, and it is a rank obstruction, not an accident:**

- **D₂ — the object's own twist — is an involution with signature 32 − 46 = −14**, so it **derives
  E₆(−14)**: the charge branch is the object's own.
- D₂ is **inner** (conjugation by a torus element), and inner involutions reach only **equal-rank**
  real forms. **E₆(−26) has 𝔨 = f₄ of rank 4 — OUTER.** **No torus element of E₆ reaches it.**

> **So the strong/weak/EM side and the gravity side are in different real forms, and the object's own
> twist can only ever land in one of them.**

**What would cross it:** only an **outer** involution — E₆'s **diagram automorphism**, i.e. **θ**.
**And the corpus cuts against it twice:** θ is **trivial on the character variety**, and **B576** says
**θ-odd is precisely what DESTROYS F₄-stability** — it moves *away* from the E₆(−26) side. So
*"θ crosses the fork"* is **contradicted until shown otherwise**, not a natural next step.

## THE HONEST ONE-LINE SUMMARY PER FORCE

| force | structure | values |
|---|---|---|
| **strong** | **derived**, two steps out (dim 14 vs 12) | **none** |
| **weak** | **derived** (the ideal exists) | **none**; 3/8 is reproduced and non-discriminating |
| **electromagnetic** | **derived** (unique gaugeable U(1); ℤ₆ form, conditional) | **none**; normalisation not derivable |
| **gravity** | **exhibited** (spin-2) | **none**; invariant content zero |

**All four are structurally present and none supplies a number.** That is the same sentence as
`THE_SM_VERDICT.md`'s, said in force-names so it can be talked about.

---

*Governing fences: the firewall (`PRACTICES.md`), Gate 5, and `docs/IDENTIFICATION_LEDGER.md` — where
**I-23** (Y ↔ electromagnetic charge) and **I-10/I-11** (the internal A₁ ↔ 4d Lorentz spin, i.e. the
**fork**) are the unearned rows standing behind the EM and gravity entries.*
