# THE STATE, CRYSTALLISED — 2026-09-06

*(Owner: "lets crystalize it." Consolidates the 2026-09-05/06 session, B1254–B1266. Supersedes no
arc; where it disagrees with an older statement, the arc cited here wins. Every number below is
computed on this bench and locked by a test.)*

---

## THE SENTENCE

> **The object derives the Standard Model's gauge skeleton and its own real form, and derives none of
> its numbers. The trade is 11 irreducible inputs for 0 of 19 parameters — net negative by parameter
> count, real by structural content, and the two do not convert.**

---

## 1. WHAT IS DERIVED

| | how | status |
|---|---|---|
| **E₆** | McKay, 2T ↔ affine E₆ | **I-1 EARNED** — a theorem, not a choice |
| **the SO(10) grading** | **D₂**, the object's own 11-flip twist: flips = 1+10, the **16 untouched**; control **0/4000** random 11-subsets | **I-22 EARNED** (B1250) |
| **the real form E₆(−14)** | **D₂ is an involution**: dim 𝔨 = 46 (so(10)+u(1)), dim 𝔭 = 32, **signature 32 − 46 = −14**, and E₆(−14) *is* the form of signature −14 | **DERIVED (B1265)** — and a **point, not a family** |
| **the descent** E₆→SO(10)→SU(5)→SU(3)×SU(2) | cut by **three characters, each a weight of the object's own 27** | B1250/B1252 |
| **one complete generation** | the **16** with exact hypercharges, **all six anomalies cancel**, hypercharge conserved **45/45**, **40 Yukawa terms**, all four SM operators present | B1253 |
| **the global ℤ₆ form** | the cascade selects [SU(3)×SU(2)×U(1)]/ℤ₆ | **B862 — and the SM itself cannot fix it** (Tong 1705.01853 leaves Γ ∈ {1,ℤ₂,ℤ₃,ℤ₆}). **CONDITIONAL**, in B862's own words: *"conditional on exactly what the cascade is conditional on (P5 + …)"* |
| **hypercharge direction** | the unique gaugeable U(1) in the abelian sector | B864 — *normalisation not derivable* |
| **termination** | the SM is the terminal registerable algebra | B863 |
| **the hierarchy's mechanism** | canonical gauge is generation-**degenerate** (x+3)³; the **D₂ twist** splits it into HIER's three distinct roots | B923 |

## 2. WHAT IS NOT — and why, precisely

- **The 19 numbers: none.** Seven sealed crossings, seven negatives. sin²θ_W = 3/8 **reproduces a
  known GUT relation** and is **non-discriminating** (identical for 5, 5̄, 10, 16, 27 — B1250).
- **The generation count.** The in-27 route is **closed permanently** (B1255: 27 = 16+10+1 carries the
  16 at **multiplicity one**; three copies need dim ≥ 48 > 27). What remains is **I-25** (which sl₂)
  and **I-26** (why h¹ counts generations at all) — the latter **upstream of every generation
  statement in the corpus**, and unregistered until this session.
- **Chirality is not self-supplied**, and **E65** says why: **every Sym^n of SL(2) is self-dual**, so
  any holonomy factoring π₁ → SL(2) → E₆ has 27 ≅ 27̄ and **net chirality identically zero — for
  every embedding.** The frame that *does* carry it is **θ-odd twisted, full-E₆(ℂ)** (B582/B576), with
  a **closing** supplying the bit (B432: **31/31** sampled Dehn fillings chiralize — though
  **slope selection stays free input**, which is itself another H5 instance).
- **The interactions do not meet**, and **B1265** says why: **D₂ is inner** and reaches only
  equal-rank real forms; **E₆(−26)** — Lorentz, compact colour, the graviton — has **𝔨 = f₄ of rank
  4** and is **outer**. **No torus element reaches it.** That is *why* B1140's two branches share
  nothing. **Crossing needs θ** — and the fence is now **doubly** motivated. **θ is trivial on the character
  variety**; and **B576** (banked long before B1265, and *not cited in its argument*) independently
  ties θ to exactly this subalgebra: *"θ-**even** deformations stay **F₄-stable** and vector-like; any
  deformation with nonzero θ-**odd** component has Zariski closure **all of E₆** and is chiral."*
  Since **f₄ is E₆(−26)'s maximal compact** (dim 52 → signature 26 − 52 = −26), **θ-odd is what
  DESTROYS F₄-stability** — it moves *away* from the E₆(−26) side, not into it. **That is a reason to
  doubt "θ crosses the fork", not support for it.** *(Two arcs, two eras, the same subalgebra reached
  from opposite directions — found by verifying an old banking instead of citing it.)*

## 3. THE PRICE

```
SPENDS   4 axioms + 10 UNEARNED identifications  =  14 rows outstanding
         4 axioms +  7 irreducible sources       =  11 ACTUAL FREE INPUTS
BUYS     0 of the SM's 19 free parameters (26 with Dirac neutrinos)
```

**Both numbers are real and measure different things** (B1261, corrected by B1266): **14** is what the
ratchet tracks and what must each be individually discharged; **11** is the theory's free-input count,
since earning **I-13** discharges I-18 and I-23 with it and earning **I-10** discharges I-11.
**Against the SM's 19, the comparison that means something is 11.**

**The rate moves by exactly three operations** — **earn** a source (−1), **refute** one (−1), or
**derive** a parameter (+1). This session did the second: **I-9 REFUTED**, 15 → 14.

## 4. THE SEVEN SOURCES, TYPED

| type | sources | what it needs |
|---|---|---|
| **the master** | **I-13** — the listener map u *(worth 3 rows)* | the crossing cell |
| **H5-type** — object supplies a **family**, observer picks | **I-6** (2 quotients), **I-14** (85 gradings), **I-25** (4 embeddings) | a selector, or a proof of choice-independence |
| **missing bridge** | **I-10/I-11** (the fork — a **rank** obstruction), **I-26** (an index theorem across a dimension gap) | a construction, not a choice |
| **unclassified** | **I-7** | — |

> **I-13 is the highest-value single target — by count, not by argument.**

## 5. THE PATTERN, AND ITS TWO MECHANISMS

**OPEN_ITEMS H5** — *the object supplies every SPACE and never a POINT* — now has **eight measured
instances**: the closing (C22), the order (A7, **2**), the partner (B1192), n (B1248, ∞), the sl₂
embedding (**4**), the 2T quotient (**2**), the L3 grading (**85**), the L3→L4 map (**2**).

**It has (at least) two mechanisms, and they must not be conflated:**

1. **Duality** — **B1227**: amphichiral ⇒ the mirror is a **self**-isometry ⇒ a mirror-odd invariant
   satisfies **2I = 0**. In a **torsion-free** group the odd part is **forced to 0** (no magnitude);
   in a group **with torsion** it survives (B1224: CS ∈ {0, ¼}). **This is E65/B1260's seven
   chirality walls as one theorem.**
2. **Counting** — the multiplicities 2, 4, 85, ∞ are combinatorial, and mechanism 1 does not explain
   them.

**H5's falsifier is sharpened but NOT met:** it asks for an **other-referential** point the object
supplies; B1224's CS = 0 is a point, but a **self**-referential one, which H5 permits. **B1265's real
form is likewise about the object itself.** H5 stands **un-falsified and un-banked**.

## 6. THIS SESSION'S OWN ERROR RECORD — kept, because it is evidence

- **E63** — a rigid structure mistaken for a species count (B1253's own draft headline, refuted before
  banking).
- **E64** — *"twelve three-nesses"*: a **count in a headline with no list**, and a shared-cause
  attribution verified for **one** of twelve. A direct repeat of the 17-atoms precedent read hours
  earlier.
- **E65** — the **structurally-incapable-frame** class: four arcs built inside a frame a theorem in
  **B327's own text** said could not deliver the target, with this seat's own χ₂₇ = (27,27,3,0,0,0,0),
  **all real**, printed on screen and unread.
- Also withdrawn: *"unique in 729"* (four, not one), *"9+9+9 ⇒ no three identical summands"* (7+7+7
  exists), *"I-7 is malformed"* (**the parser was the bug**), a hardcoded path the suite caught, and
  a κ = 3 ≟ tr(A) coincidence that was **tautological** (κ = 2 + n² makes κ = 3 automatic at n = 1).
- **Banked material re-derived eight times** — including **B1087's charge/holonomy non-commutation**,
  reported as new. The sweep discipline exists for this and was not applied to the *mechanism* words.

## 6b. OLD BANKINGS VERIFIED FOR THIS DOCUMENT — on the owner's *"verify old bankings … so u dont regress"*

Every load-bearing older arc this crystallisation leans on was **re-read at source**, not cited from
memory. Results:

| arc | holds? | what re-reading changed |
|---|---|---|
| **B582** θ-odd double has Zariski closure full E₆(ℂ), 27 complex | ✔ | supports E65's frame correction as stated |
| **B576** θ-odd ⇒ full E₆, chiral; θ-**even** ⇒ **F₄-stable**, vector-like | ✔ | **the convergence above** — and it *sharpens the fence against θ as the crossing* |
| **B432** 31/31 fillings chiralize | ✔ | **fence recovered: *"slope selection stays free input"*** — another H5 instance |
| **B862** the ℤ₆ global form | ✔ | **CONDITIONAL**, in its own words; this doc had called it flatly *derived* — **corrected** |
| **B863** termination at the SM | ✔ | as stated |
| **B864** hypercharge the unique gaugeable U(1) | ✔ | as stated; normalisation still not derivable |

**Two regressions caught before they shipped:** calling B862 unconditional, and the drift toward
*"θ crosses the fork"* — which **B576 contradicts**. Both are in this document only because the old
arcs were opened.

## 7. WHAT TO DO NEXT

Per `docs/MAIN_GOAL.md`, in order:

1. **JOIN 1** — the generation count: **h¹ vs h¹(V\*) on B102's W1/W2**, the only non-self-dual rank-3
   system the corpus has (B1260). Decisive either way.
2. **JOIN 3** — the fork: **does the object's θ act as an outer involution, and with which fixed
   subalgebra?** Now a named question, worth **2 rows** (B1265/B1263).
3. **JOIN 2** — **I-13**, worth **3 rows**: the crossing cell. Hardest, highest value.

**And the standing rule that produced everything above:** *sweep the corpus before proposing a frame,
and ask what the frame provably cannot produce.*

---

*Locked by: `tests/test_b1261_the_price.py` (the price, ledger well-formedness), `test_b1266_sources.py`
(the reduction to 7), `test_b1265_fork_inner_outer.py` (the signature and the rank obstruction),
`test_b1260_where_chirality_can_live.py`, `test_b1264_h5_census.py`. Suite green at `PYTEST_RC=0`,
6118 passed. 30/30 gates.*

---

## POST-CRYSTALLISATION NOTE (2026-09-06, the assembly seat — B1267–B1271, merged the same day)

*Not a revision of the state above; the arcs it cites win where they disagree. Read with `docs/THE_ASSEMBLY_2026-09-06.md`.*

| §7 item | what the assembly seat computed | result |
|---|---|---|
| **1. JOIN 1 — h¹ vs h¹(V\*) on B102's W1/W2** | `scripts/external_verification/nonselfdual_w1w2.py` (9 generic points; 6 cusp-fixed-vector points × 3 central twists), plus PSL(2,7)'s twelve 3s and a non-semisimple extension | **N = 0 in every row**; and the general bound on the cusped object, −h⁰(∂M;V) ≤ N(V) ≤ h⁰(∂M;V\*), gives \|N(27)\| ≤ 1 near the geometric point for every deformation (B1268) |
| **2. JOIN 3 — θ as an outer involution** | not addressed as posed; B1268 reached a θ-odd deformation of the cusped E₆ holonomy (400-bit Newton, residual 10⁻⁶⁰) and found N = 0 with no cusp invariants | the fork question (which fixed subalgebra) stays **open as named** |
| **3. JOIN 2 — I-13** | B1271: the Yukawa E₈ forces on its generation triplet is **zero** (ε family tensor; one class on m004); the count's only surviving route (h¹ = 3) is also the Yukawa's, and both need a closing with an H³ and a non-self-dual 3_ρ | the master row's object gains a third face: the three-class carrier |

Also computed on the branch: **E₆ derived from both faces** (B1270: the golden E₈ = the icosians of ⟨R,L⟩ mod 5,
cut by the Eisenstein ratio — B1138 §4's family triplet is this cut, its fence EXHIBITS-NOT-FORCES kept as the
transport's choice of face), and the transports (B1269: with an E₆ singularity the 27 has no slot; the SM
algebra is not a centralizer in e₆). §6's failure mode *"banked material re-derived"* recurred on this seat (E68:
the triplet, the VEV route) and is corrected by addenda. Price unchanged: 14 rows, 11 sources.
