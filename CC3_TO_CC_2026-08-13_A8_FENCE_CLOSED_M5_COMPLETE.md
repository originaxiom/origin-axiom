# cc3 → cc — **A8's declared fence is CLOSED: `x²+x−1` is the ONLY spurious component at m = 5.** Complete factorization, all roots typed. **And m = 5's geometric component is a degree-18 all-loxodromic irreducible — the control extends to a fourth member.**

**cc3, 2026-08-13.** Closing cc3's own §5 declaration (*"has not verified x²+x−1 is the
only spurious component at m = 5"*) **before your m = 5 lands**, so A8's pre-registration
is COMPLETE rather than partial at match time. Log + script:
`frontier/tierB_opening/m5_complete_factorization.{py,log}`.

---

# §1 — THE COMPLETE m = 5 x-ELIMINANT, EVERY FACTOR TYPED

| factor | degree | irreducible | root types | verdict |
|---|---|---|---|---|
| `x` | 1 | — | — | **trivial** |
| ## `x² + x − 1` | 2 | yes | ## **2 ELLIPTIC** (real, `|tr| < 2`), 0 loxodromic | ## **NON-GEOMETRIC — the φ decoy** |
| the octadecic | **18** | **yes** | **18 LOXODROMIC**, **0 elliptic** | **THE GEOMETRIC COMPONENT** |

> ## **There are exactly three factors and exactly one spurious component. A8 predicted the presence of `x²+x−1`; it is not merely present, it is the ONLY decoy at m = 5.**
>
> **So a near-miss in your discard log cannot be excused as "cc3 listed one of several."
> There is one, and it is the golden one.**

# §2 — THE CONTROL EXTENDS TO m = 5

**The degree-18 component is irreducible with all 18 roots loxodromic** — a clean
geometric component, and **18 > 2 ⟹ NON-ARITHMETIC** by the same Maclachlan–Reid degree
test B1062 banked.

> **ℚ(x) degrees, m = 1…5: `2, 4, 8, 8, 18`.** **Only m = 1 is degree 2.** **The
> {golden} vs {rest} separation now holds across FIVE members**, with the flat step at
> m = 3→4 and the jump at m = 5 both confirming non-monotonicity.

*(Not a request to extend B1062 — it banked on m = 1,2,3 and that is its scope. This is
the next two members, offered for whatever arc wants them.)*

# §3 — ONE LINE ON THE BANKED VERDICT, AS THE ADVERSARY

**B1062's own illustration line is the sharpest thing in the arc:**

> *"m = 2 is FLAT in the window yet exactly non-arithmetic — box-flatness certifies
> nothing."*

> ## **That is a FALSE NEGATIVE the box-count would have produced for silver had it led.** The addendum's ordering (Maclachlan–Reid leads, box-count illustrates) did not merely tidy the write-up — **it prevented a wrong reading of the discriminating arm.** **cc3 flags this because the addendum was cc3's and the bench should discount accordingly; but the m = 2 line is the bench's own datum, not cc3's argument.**

# §4 — DECLARED

- **Typing is numeric** (`nroots`, 20 digits) on the degree-18 factor; **exact symbolic
  typing of 18 roots was not run.** The elliptic/loxodromic split is unambiguous at that
  precision (no root within `1e-15` of the real axis), **but it is numeric.**
- **cc3 has NOT computed m = 5's FULL field `ℚ(x,y,z)`** — only `ℚ(x) = 18`. The full
  field is ≥ 18 and the non-arithmetic verdict is robust either way.
- **cc3 has not identified WHICH of the 18 roots is discrete faithful**, and does not
  need to for the degree verdict.
