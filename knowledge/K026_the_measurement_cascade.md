# K026 — The measurement cascade (what the 2026-08 campaign knows)

*Knowledge-room entry: the organizing story of B877–B917, stated as what is KNOWN —
every clause carries its arc. Written at the owner's loss-scan; the campaign's story
had no room entry.*

The object carries four superselection charges (2T-invariants) spanning a rational
torus. Its own arithmetic selects a measured PLANE: the two θ-odd-exponent directions,
marked in advance by the torsion sign law (B581→B894), by the split/compact spectrum
dichotomy (B898), and by the resolvent 77 = 7·11 assembled from the two UNMEASURED
exponents (B888). **Measurement number one** breaks E₆ along the charge cubic μ
(constant 13³) — three Galois-conjugate ways, centralizer so(10)⊕u(1) (the FMT, P69).
**Measurement number two** — of the object's own charges again — lands exactly on
su(3)⊕su(2)⊕u(1)³, skipping SU(5) (the SMT, B892); its wall is complex in the split
frame at every root (B893) and REAL exactly in the quaternionic form e₆(2), reachable
only through outer-twisted conjugations (B907, sealed; completeness by sign-locking).
The ambient tiling is the (𝕆,ℂ) magic-square algebra BY EXPLICIT ISOMORPHISM (P70).

The 27 of matter organizes into fifteen flavor atoms — joint eigenlines of a RATIONAL
commuting family (the tower never enters — B908) — whose colorless nine form a 3×3
grid: the rows are the even transversals of a determinant, the columns the odd ones,
and the first derived value of the programme, I = −1, is that determinant's orientation
parity, the Leibniz sign, exact (B908). Under the canonical Hermitian structure (B912:
signature (15,12) = e₆(2)'s K-type split) the colorless world is Euclidean with nine
exact scales and the colored world is Lorentzian, one (1,2) cone per quark-type atom.
The colorless coupling table collapses to ONE number (B914), and in the object's own
charge-equivariant gauge that number is EXACTLY 1 (B916/B917): the object's tree-level
couplings are trivial in its own frame — all arithmetic content (2304/953, T, the
hierarchy v-weights with v₁v₂v₃ = 3^{3/2}λ²) lives in the NORMS OF RELATIONS between
canonical observers (the twist-norm law, B916). The vacuum and matter cubic fields
annihilate in the Knus–Paques group (B902), all three matter cubics share ONE class
(B910), and the value primes (953, 1129, 421493) split in K with unique degree-one
places while the structure primes (13, 17, 19) stay inert (B917).

**The crossing** (B915, sealed, owner-authorized): one measured input plus the object's
boundary across a pure desert MISSES at 16σ — the naive identification is dead, the
structure stands whole, and the failure triangle points at the object's own compact
D-chain (E₆ ⊃ D₅ ⊃ D₄ ⊃ D₃ ⊃ A₂ — the CMT lane) as the desert's replacement (R4b,
its own seal, pending).

What this room should hold next: the CMT entry when B909 banks; the R4b verdict
whichever way it falls.
