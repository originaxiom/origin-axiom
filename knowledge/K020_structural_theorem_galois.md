# K020 — The structural theorem: the object forces form, not values — and it is a Galois theorem

> **Explainer** (`GOVERNANCE.md`). Self-contained; standard material cited to the literature, the project's own use
> cited by `B`/`V` number (no re-proof). Nothing here promotes to `../CLAIMS.md`; never a premise in a proof. The
> textbook-layer consolidation of the **B238–B314 era** (the two-ended object, the E₆ arc, the cascade, the seam, the
> structural theorem and its Galois mechanism) — the era `K001–K019` predate. Firewalled: the physics readings live in
> `../speculations/`; this doc states only the mathematics and its honest tiering.

## The headline

Across every face examined in this era, one verdict recurs: **the object forces *form/structure* — never physical
*values*.** It is selective for its own structure (E₆, the cascade, `κ`, both arithmetic ends, the exponents) and a
flat catalogue for the actual Standard-Model values (couplings, the level, the scale). This is the **structural
theorem**, and it now has a mechanism: **every discrete invariant of the object is a Galois orbit of the object's own
arithmetic Galois group**, so the discreteness is always a *labeling*, never a forced value. *The value-free monad is a
Galois theorem.*

## 1. The two-ended object (B248/B249/B258/B261)

The figure-eight is a **two-ended** arithmetic object. One canonical structure sits at each of two arithmetic poles,
forced apart by Niven's theorem (a rational angle with rational cosine is `0,±1,±½`):

- **Eisenstein end** — `ℚ(√−3)`, the hyperbolic trace field, the cube root `ω`, the group **E₆**.
- **Golden end** — `ℚ(√5)`, the Fibonacci/quasicrystal field, the golden ratio `φ`, the group **E₈**.
- **E₇ is excluded** (its field `ℚ(√2)` fails the disc≡1 mod 4 / Niven gate; B256: E₇ is the *silver* m=2 group, not the
  figure-eight's). The two ends are two regimes of one AJ recursion (B261).

This double arithmetic is the program's deepest *object-specific* fact: the figure-eight is the unique minimal object
carrying **both** ends, and most later results reduce to it.

## 2. The arithmetic atom and the E₆ character variety (B266/B282/B264–B270)

The Eisenstein end is an **atom**: `4₁ → ℚ(√−3) → ramified prime 3 → SL(2,𝔽₃)=2T (binary tetrahedral) → McKay affine
Ê₆`. The figure-eight is *the* arithmetic `ℚ(√−3)` knot complement (Reid). Its **E₆ character variety** exists (the
principal `sl₂ → e₆`, deformations graded by the Kostant exponents `{1,4,5,7,8,11}`, B264/B274/B275).

## 3. The cascade — generic E₆ + the one object connection (B305/B306/B310/B311)

The principal grading of E₆ gives a forced Borel–de Siebenthal cascade of centralizers of `exp((2πi/N)·h)`:
`E₆ → SU(6)×SU(2) → SU(3)³ (trinification) → SU(3)²×SU(2) → SU(3)×SU(2)²×U(1)² (⊃ left-right ⊃ SM) → SU(2)³`. **This
chain is standard (Slansky 1981) — generic E₆, no figure-eight input.** The *object-specific* content is one step: the
**trinification grading eigenvalue is `ω = e^{2πi/3} ∈ ℚ(√−3)`** (B305), and the trinification point is an *irreducible
branch point* of the figure-eight's own A-polynomial discriminant `(x−1)²(x+1)²(x²−3x+1)(x²+x+1)`, `x=M²` — whose two
non-trivial factors are exactly the **Eisenstein** (`x²+x+1`) and **golden** (`x²−3x+1`) ends in one discriminant
(B311). The "πi/3-spaced deformation realization" of the *whole* chain on the curve is **refuted** (B310): the cascade
values `2πi/N` are not equally spaced, and the N=2 step is a reducible point. *Cascade = standard chain + the banked ω;
its physical realization is the `T[4₁;E₆]` CRUX (specialist).*

## 4. Face IV houses the *form* (B312/B313)

The object's TQFT quantization (Face IV — WRT, the quantized character variety, Fibonacci anyons) houses the *same* E₆
as the content: it is the **CIZ exceptional modular invariant of SU(2)₁₀** (Cappelli–Itzykson–Zuber), sharing Coxeter
number 12 and exponents `{1,4,5,7,8,11}` with the Lie-group E₆ (the cascade) and the McKay `2T` atom (its classical
limit). **One E₆, three ADE hats** (McKay subgroup / Lie group / modular invariant). Face IV houses *both ends*
(E₆@k=10 Eisenstein, E₈@k=28 golden — two of the three CIZ exceptionals), and the trace field is compatible via the
triality (`ℚ(√−3) ⊂ ℚ(ζ₁₂)`, since `3 | 12 = h(E₆)`). But the level `k=10` is **generic** to SU(2)₁₀, not
object-specifically selected — so **Face IV houses the form, not the values**: the structural theorem at the quantum
level. The (G₂)₁ = Fibonacci category is a *common substructure of both ends* (B313: SU(2)₃ even-spin ⊃ G₂ ⊂ E₆;
central charges `9/5 → 14/5 → 6` exact) — real, but generic (`E₆ ⊃ G₂×SU(3)` is Slansky); "matter = anyons" is refuted
(`27 → (7,3)+(1,6)`, not `27 → 7`).

## 5. The four faces of one κ (B309)

`κ = tr[a,b] = u²+2`, the Fricke–Vogt non-cancellation invariant (`../philosophy/P008`), is the single object wearing
four faces: **existence** (`κ ≠ 2`, P008), **geometry** (the seam, B286), **matter** (`arg(κ−2) = ∓π/6` = the CP phase,
B285; the cascade), **quantum** (Face IV). `κ−2 = ω²`, `|κ−2| = 1` (the unit obstruction). At `u=0`, `κ=2` (the P008
abelian wall); at `u=ω`, `κ = √3·e^{∓iπ/6}` (the CP phase) — one κ, both readings.

## 6. The mechanism: the structural theorem is a Galois theorem (B285 + B314)

A *forced choice* (`K013`/B130) is an invariant that is (1) trace-map-invariant, (2) discretely multivalued, (3)
**unsymmetrizable**. B130 sealed the **trace ring**: `κ` is continuous on the fixed locus — not discrete, no choice. The
one place a forced choice could hide is the **discrete** quantum invariants (Face IV, root-of-unity-valued). **B314
seals them too, and the same way as B285 sealed the classical end** — by a Galois action:

| end | field | the discrete invariant | the symmetrizing Galois `ℤ/2` |
|---|---|---|---|
| classical (Eisenstein) | `ℚ(√−3)` | `κ = √3·e^{±iπ/6}` — the CP sign | `√−3 → −√−3` (**B285**) |
| quantum (golden) | `ℚ(√5)` | the WRT / colored-Jones / modular data | `√5 → −√5`, `φ → −1/φ` (**B314**) |

Concretely (B314): the figure-eight colored Jones at the k=3 root is `J_N(4₁; ζ₅) = {1, 1−√5, 1−√5, 1}`, all in
`ℚ(√5)`; the two primitive-root orbits give `{1−√5, 1+√5}` — a golden Galois orbit; the SU(2)₃ quantum dimensions are
`{1, φ, φ, 1}`. So the discrete Face IV values are a Galois orbit: choosing among them is choosing an arithmetic
labeling (which `√5`), **not** forcing a physical value → condition (3) fails → no forced choice. The `±` in the CP
phase and the `1∓√5` in the colored Jones are *the same object*. **The object's two arithmetic ends each carry a Galois
`ℤ/2`, and each symmetrizes its end's discrete invariants** — that is the algebraic cause of "form, not values."

## 7. What is open

The Galois mechanism seals the **two load-bearing invariant classes** — classical (trace ring, B130) and quantum
(Face IV, B314). The residual is **S032-A** (`../speculations/S032`, `OPEN_LEADS` L7): the *fully general* statement
that **no invariant whatsoever** (incl. arbitrary cohomology / torsion) is discretely-multivalued-and-unsymmetric for a
single seed. B314 makes it newly attackable (a single mechanism is the candidate proof strategy); if proven, single-seed
member-contingency is **irreducible** — the firewall's deepest form. Choice enters only with **heterogeneity** (two
distinct seeds, `K014`/B131). And the figure-eight is the **most-selected member of a forced family**, not forced
outright (`K009`: systole + the non-metric expansion threshold + arithmeticity; `../philosophy/P000`).

**Anchors:** B248/B249/B258/B261 (two-ended object), B266/B282 (the atom), B264–B270/B274/B275 (E₆ character variety),
B285 (CP phase = Galois), B286–B294 (the seam), B305/B306/B310/B311 (the cascade), B309 (four faces of κ), B312 (Face IV
houses the form), B313 (the Fibonacci bridge), **B314** (Galois seals Face IV); `K009`/`K013`/`K014` (selection,
no-forced-choice, heterogeneity), `../philosophy/P008`/`P009`/`P012`, `../speculations/S032`. External: Slansky (1981,
the E₆ chain); Cappelli–Itzykson–Zuber (1987, SU(2)_k modular invariants); the McKay correspondence; Niven (rational
angles); Coste–Gannon (the Galois action on modular data).
