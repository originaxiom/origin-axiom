# K010 — The object named: the metallic-mean Schrödinger cocycle and its Fricke–Vogt invariant

> **Explainer** (`GOVERNANCE.md`). Self-contained; standard material cited to the literature, the project's own use
> cited by `B`/`V` number (no re-proof). Nothing here promotes to `../CLAIMS.md`; never a premise in a proof. This is
> the **proper name** of the central object — the genuine address it has in mathematics and in the physics of
> aperiodic order.

## The object has a name, a literature, and a community

The project arrived — from a "why is there something" principle — at a surface, an invariant, and a dynamics that are
all classical and named. Stated plainly: **the central object is a Schrödinger cocycle over the metallic-mean
substitution subshift, analyzed by its trace map; `κ` is its Fricke–Vogt invariant.** Each piece:

- **The trace map.** The map on `(x,y,z) = (tr A, tr B, tr AB)` is the **Kohmoto–Kadanoff–Tang (KKT) map** (1983),
  introduced precisely to analyze the Fibonacci quasicrystal. The "trace-map tower" (`K003`) is KKT renormalization.
- **`κ = tr[A,B]`**, the boundary/commutator trace, is the **Fricke–Vogt invariant** (Vogt 1889; Fricke; rediscovered
  by physicists as the conserved quantity of the trace map). It is the unique polynomial conserved by the KKT
  dynamics; in normalized form `I = κ − 2` (B127).
- **The surface** it lives on — the relative `SL(2,ℂ)` character variety of the once-punctured torus at fixed
  boundary trace — is a **Markov surface** `x²+y²+z²−xyz = const` (`K001`); the mapping-class-group action on it is
  classical (Fricke, Goldman).
- **The operator** is the **metallic-mean Schrödinger operator** — the **Fibonacci Hamiltonian** for `m=1`, the
  **silver-mean chain** for `m=2`, the **metallic-mean** (precious-mean) quasicrystals for general `m` (`K007`,
  `S023`). These are studied objects.
- **The spectrum** is a **zero-measure Cantor set / singular-continuous** spectrum — **Sütő's theorem** (1987) for
  Fibonacci, extended by **Damanik–Gorodetski**, who proved the deeper fact: the KKT map restricted to the surface is
  **uniformly hyperbolic — a Smale horseshoe** — and the spectrum is the set of energies whose trace-map orbit stays
  bounded (the stable lamination of the horseshoe).

## The dictionary (exact, and computed)

The project's "non-cancellation" principle translates exactly (B127):

| the project's language | character variety | spectral theory of aperiodic order |
|---|---|---|
| **cancellation**, `κ=2` | reducible / abelian / commuting locus | `I=0` ⟹ coupling off ⟹ **periodic**, AC spectrum `[−2,2]` |
| **non-cancellation**, `κ>2` | irreducible representations | `I>0` ⟹ **quasicrystal**, KKT map **hyperbolic (horseshoe)**, **Cantor spectrum** |

So **non-cancellation is the positivity of the Fricke–Vogt invariant, which is the hyperbolicity of the
renormalization trace map, which is the Cantor spectrum of an aperiodic Schrödinger operator.** `I=κ−2` vanishes
precisely on the commuting locus (B127, verified); the void Jacobian is hyperbolic with spectrum `{φ²,−1,φ⁻²}` (B124).
`κ=2` is the integrable/periodic boundary; `κ>2` is where renormalization becomes chaotic and the spectrum fractures
into dust that can never reassemble into a periodic band. That dust *is* the `κ`-object — the thing that cannot
collapse to nothing — named.

## What is the project's, and what is the field's

The **connection** is not the project's — the field is the spectral theory of metallic-mean / substitution
Schrödinger operators via trace-map renormalization (Kohmoto, Sütő, Bellissard, Damanik, Gorodetski), and the metallic
generalization (silver, bronze, …) is studied. What is the project's is the **recognition and the framing**: it
arrived at this surface, this invariant, and this dynamics independently, and can now see that "non-cancellation" *is*
the Fricke–Vogt positivity that opens the Cantor spectrum — aperiodic spectral theory done in the character-variety
dialect.

## The boundary (the firewall, in proper terms)

This is **mathematical physics / condensed matter** — the physics of *aperiodic matter*, real and observed
(quasicrystals, their fractal electronic spectra, anomalous transport). It is **not** fundamental physics:
`κ = 2 + λ²` is the coupling of a tight-binding chain, not a vacuum or a constant of nature; the spectrum is the
allowed energies of *one particle in a fixed quasicrystal*, not a particle spectrum (no fields, no second
quantization, no species/mass ratios). And the framework is **achiral and phase-trivial** (CS=0, B127), so it carries
no connection/holonomy content — **not a gauge theory, by direct computation** (`P007`, B127). The object describes
**order within existence**; it does not derive the *contents* of existence. The fundamental firewall is unchanged.

This is the strongest honest "this is physics" the arc has produced — and it is **emergent physics, named correctly**.
*(The quantum layer adds a second emergent identity: at level `k=3` the σ₃ Galois conjugate realizes the **Lee–Yang
edge** `M(2,5)` (`d_τ=−1/φ`) — computed in B132, `S030` (TESTED-POSITIVE), `K015` — also emergent non-equilibrium
physics, firewalled from fundamental.)*

## The geometric bundles' trace fields (B140 — a record, distinct from the `φ`-fixed S031 object)

The orientable metallic bundles `RᵐLᵐ` (the `φ²`-geometric object; `M_m = [[m,1],[1,0]]`, `M_m² = RᵐLᵐ`, `K011`) have
**invariant trace fields** that are **imaginary quadratic only for m=1,2**: **m=1 → ℚ(√−3)**, **m=2 → ℚ(i)** (B125,
B129/`K012`), **m≥3 → higher-degree** (B140/V129; a structural fact, not a compute limit). This is almost certainly
*why* m=1,2 are the family's two **arithmetic** members (arithmeticity ⟺ imaginary-quadratic invariant trace field
for these). Note this is the **`φ²`-geometric bundle** trace field — a **different object** from the **rational**
`φ`-fixed content of the trace-map automorphism (`S031`, B140); the two must not be conflated.

**Anchors:** B107/`K007` (the KKT identification), B124 (the void Jacobian / horseshoe-anchoring hyperbolicity), B127
(the Fricke–Vogt dictionary + CS=0 + the arithmetic separation), `K001` (the Markov surface), `S023` (the distinct
metallic quasicrystals), `../philosophy/P008` (the reading); **B148/V137 (`κ = 4·I_FV + 2` re-derived exactly; the
metallic monodromies are the `SL(2,ℤ)` MCG action), B151/V140 (the firewall confirmed — `κ`/the complex volume is
dimensionless, `∈ ℂ/4π²ℤ`, and sources no physical scale)**. External: Kohmoto–Kadanoff–Tang (1983); Sütő (1987);
Bellissard (gap labeling); Damanik–Gorodetski (the Fibonacci Hamiltonian / horseshoe); Vogt (1889), Fricke, Goldman
(trace coordinates).
