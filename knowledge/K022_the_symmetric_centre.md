# K022 — The object is the symmetric centre; it forces the *form* of the deviation space

**Textbook layer (`knowledge/`): a self-contained explainer, firewalled, citing its `B`-provenance. Introduces no new
result — it names the reframe that the B343–B346 arc produced.** Nothing here promotes to `CLAIMS.md`; the physics
dictionary stays postulated.

## 0. The reframe in one sentence

The object is **not** value-*blind* — it is the maximally-symmetric **centre** of physics, and it forces the **form of the
deviation space** around that centre (its symplectic metric, its ℤ/3-graded texture, its cross-structure links), while the
world sets the deviation **magnitudes**. This deepens the structural theorem (K020) from *"form, not values"* to *"the
object is the centre, and forces the geometry of the perturbations around it."*

## 1. The object is the symmetric centre

Across every sector examined, the object sits at the **fully symmetric fixed point**:
- **democratic masses** — the light generations are exactly degenerate (B335, by isometry of the deck ℤ/3);
- **exact TBM** — lepton mixing is `θ₁₃ = 0`, the symmetric point, *not* TM1 or TM2 (B343);
- the **CP phase `±π/6` is extremal** at the amphichiral point (B340);
- the **two arithmetic ends are balanced** (B332/B334).

And **one property explains every silence:** the deck ℤ/3 acts **irreducibly** (`Φ₃` irreducible mod 2 and mod 4, B326).
Irreducibility = *no invariant sub-object* = maximal symmetry, so the object is **mass-blind, split-blind, and
direction-blind** — it refuses to distinguish any asymmetric quantity (B343). The firewall is not imposed; it is the
representation theory of the Eisenstein `ω`.

So the object is the **symmetric origin**; physics is the **deviation** from it.

## 2. The object forces the *form* of the deviation space (B344–B346)

The tangent *spectrum* at the centre is the metallic tower `char(M^k)` (B65) and splits by E₆-exponent into escape-`{4,8}`
vs trapped-`{5,7,11}` (B265). The **new** content (this arc) is the deviation-space **geometry** — all **forced,
dimensionless relations** (forms, in the sense of `sin²θ_W=3/8`/the period law), never magnitudes:

| structure | statement | probe |
|---|---|---|
| **symplectic metric** | `det(dφ)=1` + κ-invariance ⇒ deviations come in a **reciprocal pair `(λ, 1/λ)`** (symplectically conjugate; Goldman); κ is the **un-paired Casimir = the scale/cusp door (external)** | **B344** |
| **ℤ/3 texture** | the deviation modes carry ℤ/3 charges `{0,1,2}`; a forced **charge-conservation selection rule** ⇒ the anti-diagonal (= ω-circulant/democratic) texture | **B345** |
| **cross-structure map** | **symplectic conjugation = ℤ/3 charge conjugation** (the two coincide at the centre); the **E₆-exponent grading is independent** — a third, orthogonal structure | **B346** |

The picture: the deviation space has a **symplectic metric** whose conjugation *is* the **ℤ/3 charge conjugation**, plus an
**independent E₆-exponent grading**. The one direction the object cannot pair — κ, the Casimir — is exactly the **external
scale**. The near-orthogonality of the structures is *why the object can be the symmetric centre of many independent
physical sectors without correlating them.*

## 3. What is forced, and what is not

**Forced (form):** the deviation-space *geometry* — the reciprocal/symplectic pairing (B344), the ℤ/3-charge texture
(B345), the symplectic↔ℤ/3 link (B346). These are dimensionless relations/textures, bankable form.

**Not forced (external):** the deviation *magnitudes* — the size of `θ₁₃`, the mass ratios, the CP-phase value, the scale.
The un-paired κ-direction is the scale door, and it is the world's. And the "prize" — a *second data-facing falsifier*
alongside TBM — **did not materialise** (B346): even the cross-structure relations are *forms*, not measured predictions.

So the honest state is the sharpest version of the whole program: **the object is the symmetric centre and forces the form
of the perturbation space around it; the world supplies the perturbation's magnitude and direction.** Symmetry — including
the *symmetry of the deviations* — is the object's; magnitude is the world's.

**Provenance.** Centre: B335, B343, B340, B332/B334. Irreducibility: B326, B343. Deviation form: **B344** (symplectic),
**B345** (ℤ/3 texture), **B346** (cross-structure). Tangent spectrum (prior): B65, B265, B264. Metric: B293 (Goldman).
Structural theorem: K020. Firewall precedent (relation = form): B304 (`sin²θ_W=3/8`), B204/B219 (period law).
