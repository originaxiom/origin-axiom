# CC3 → CC — THE FRAMEWORK, one day on: the 2026-08-09 delta

cc3 audit seat. Gate 5-Q. Nothing banked; `docs/THE_FRAMEWORK.md` untouched.

## Why a delta and not a new digest

The owner asked whether the repo should be read and digested completely once
more. **Investigated first, per the owner's own suggestion — and the answer is
no.** `docs/THE_FRAMEWORK.md` was assembled **2026-08-08**, one day ago, on the
mature corpus (152 post-B800 arcs, 125 PROVED), in seven layers with calibrated
claim language. It is the digest. Re-digesting from scratch would repeat it and
risk drifting from it.

What it needs is **one day of updates**, and today's audit touches it in six
places — one of which is an inconsistency internal to the document.

**A currency warning that belongs with this.** The docs tree holds at least six
synthesis documents, and three are explicitly marked *"Historical snapshot —
kept per GOVERNANCE §9; not the current state"* (`UNIFIED_STATE`,
`STRATEGIC_SYNTHESIS`, `SESSION3_SYNTHESIS`, all 2026-07-03). A reader who opens
the wrong one gets a July position. **THE_FRAMEWORK should say, in its header,
that it is the current digest and name the superseded set** — it already does
this for `ROADMAP_TOE.md` and should finish the job.

---

## 1. WHAT REMAINS item 8 — DISCHARGED TODAY

> *"**43 leads** open or partial, **35 of them pre-B800** and needing re-triage
> against the mature object."*

Done, in two passes, both relayed:

- **The triage** — 43 leads: **30 STALE-CLOSED, 2 STALE-PREMISE, 11 LIVE**, every
  citation verified mechanically against `origin/main` (0 fabricated quotes).
- **The relational re-read** — the closures classified by the era of the arc that
  closed them: **24 pre-B800-only, 7 post-B800, 1 no-arc, ZERO mixed.** Re-reading
  those 24 against the mature object gives **HOLDS 6, UNDER-READ 11, OVER-WIDE 6,
  REOPEN 2.** Only six survive unchanged, and they share one property worth
  keeping as a rule: **a closure survives exactly when its scope sentence names
  no manifold.**

Item 8 can be struck and replaced with the six OVER-WIDE scope corrections.

## 2. LAYER 2 — a conflation to fix, and it is B727's own distinction

Current text:

> *"**E₆-across-faces is forced** (B727) — the atom being ℚ(√−3) as the unique
> arithmetic knot"*

That fuses two properties B727 itself insists are separate:

> *"the SM-resonance is **the field's** (ℚ(√−3)), the arithmetic-knot uniqueness
> is **the knot's**; **they live on different objects and do not reinforce each
> other**."*

Measured today, which is what makes the fix concrete rather than editorial:

| condition | census (4,000) | knots (3,112) |
|---|---|---|
| surjects onto 2T | 34.9 % | 38.8 % |
| exactly two surjections (m004's count) | 24.2 % | 36.4 % |
| **invariant trace field ℚ(√−3)** | **0.18 %** | **1** |
| all three + H₁ = ℤ | **m004 alone** | **m004 alone** |

**Suggested replacement:** *"E₆-across-faces is forced by the **field** ℚ(√−3)
(B727), which is a commensurability-class invariant (B803) — not by the
arithmetic-knot uniqueness, which is a separate property of the same manifold
and is consumed at no step of the chain."*

## 3. LAYER 3 — the cascade principle should be restated STRONGER

Current: *"One principle (B861): maximal residual symmetry among **registerable**
options."*

The confluence run (reported to this seat, recorded with provenance in
`CC3_TO_CC_2026-08-09_CORNERSTONE.md` addendum 2) splits that principle in two
and shows the load sits on the derived half:

- registerable options per step **[3, 2, 1]** — Sp(8) fails at step 1;
  **at step 3 exactly one option is registerable**, SU(4)×U(1) failing;
- **all six** registerability-respecting chains terminate at the SM.

So **registerability (DERIVED — B860 theorem-grade, B871) forces the endpoint;
maximal residual symmetry (ASSUMED — B859/B874, never retired) selects only the
path.** The SM endpoint does not rest on the unretired assumption. That is a
strengthening and the layer should say it.

**Consequence:** with the ranking retired from load-bearing status, **P5 menu
completeness is the cascade's only remaining import.**

## 4. LAYER 5 (scale) — a second, cheaper, independent mechanism

Current: the Gukov split `k·I_CS + iσ·I_grav`, CS = 0, so the quantized half
dies — *"(Normalisation check owed before this is a claim rather than a lead.)"*

Today's weight ledger gives an **independent** and far more elementary reason,
with nothing owed:

Assign every quantity its weight under `g → k²g` (length +1, area +2, volume +3,
Laplace eigenvalue −2, and **0** for traces, trace fields, Galois data,
Chern–Simons, torsion, cusp shape, level). Then:

- **8 of the 11 faces carry only weight-0 data.** `Hom(G, ℝ₊) = 0` for finite and
  profinite G — the B666 torsor no-go — *is* that fact without cohomology:
  **you cannot get a length out of a trace.**
- A scale exists **iff** the object forces a relation between quantities of
  *different* weight. **Hyperbolic geometry is exactly scale-covariant**, so no
  internal relation can be weight-inhomogeneous — **on any face, emittance
  included.** An adversarial sweep looking for a counterexample found none.
- Positive form: **the object is a shape and a shape has no size.** Fix the
  curvature radius R once from outside and everything follows —
  `vol = 2.0298832 R³`, `systole = 1.0870701 R`, `λ₂ = 25.0108367 / R²`. So
  "zero free parameters" is exactly **zero free shape parameters, one free scale
  parameter.**

Both mechanisms can stand; the weight argument is cheaper, owes nothing, and
covers faces the Gukov argument does not reach.

## 5. WHAT REMAINS item 3 — contradicted by this document's own addendum

Item 3: *"**L134** — the twelve exotic states per generation… **Never addressed
here.**"*

But the document's own **2026-08-09 addendum** says matter mass has its
mechanism (the cubic, `27³ ⊃ 16·16·10`, B884's table) and scopes B978's no-go to
the adjoint only. And today's EXOTICS probe found L134 **closed by B978**, with
`GUT_REQUIREMENTS_LEDGER.md:48`, `THE_SM_VERDICT.md:153` and `OPEN_LEADS.md:922`
all still recording it as never addressed.

**Four ledgers plus this line are stale in the same direction: declaring absent
what the programme has already closed.** That is the mirror of the failure the
morning's triage was built for, and it is now the larger of the two.

## 6. LAYER 6 — one instrument to add, one defect to fix

**Add:** `docs/REVIVABLE.md` + `scripts/revivable/build_revivable.py` — generated,
deterministic, sealed on its input hash, `--check` for CI. It indexes the kill
graph's 135 revival hatches and 28 high-score routes, a lead structure no
register could be queried against. Rationale and five ways to disprove it in
`CC3_TO_CC_2026-08-09_REVIVABLE_rationale.md`.

**~~Fix:~~ ALREADY DONE — cc banked it as B984/B985 while this seat was writing.**
Verified on `origin/main`: `build.py` now carries `_findings_path`, which prefers
the canonical name, then known variants, then any `*FINDINGS*.md`, under the
comment *"Exact-name matching lost 42 arcs … an arc is ingested on having content
rather than on having guessed the filename convention of its year."* The
recommendation is discharged; the row is kept struck rather than deleted so the
loop is visible.

**Also already done:** the anatomy is now **14 faces** — the eleven plus
`axioms`, `cascade`, and **`character-variety`**, the twelfth-face candidate this
seat's residue analysis nominated and three readers named independently.

**What is still open in that area** (checked today, after the fix):
**B1–B5 still carry no `arc_verdict.json`.** The ingestion hole is closed; the
*verdict* hole is not. **B2 is the one that matters** — it falsified the
handoff's monodromy claim (*"the image polynomial is not divisible by the
A-polynomial"*) and, in the same breath, diagnosed why and named the successor:
*"the handoff conflated two different moduli spaces — the monodromy acts on the
character variety of the **fiber**, not on the meridian/longitude coordinates."*
`B13_trace_map_character_variety/README.md` opens by citing exactly that. **So
the twelfth face was opened by B2's falsification**, and B2 is filed as nothing:
no verdict, absent from `THEOREM_LEDGER` and `LAW_MAP`. On this repo's own B818
auditor rule it is PROVED. Registering it is a bookkeeping act with real
content.

---

## 7. ALSO CLOSED TODAY, not yet in the document

- **L114 discharged** — ι is **out** of the closing set; **B766's rank 3 stands**.
  The two "measurement" formalisms are **not one object**: rank 3 counts closing
  axes, rank 4 counts rep-variety symmetries, and `θ_T · ι = contragredient` is
  inner on V0 (self-dual) and outer on SL(3). ι's non-triviality *is* the
  non-self-duality obstruction.
- **The 573 unattached arcs accounted for** — they group; only 14 are genuinely
  faceless. Twelfth-face candidate: the **character-variety / trace-map
  substrate** (27 arcs, named independently by three readers). The mechanism
  behind the 77 % is B805's: the anatomy was induced from a **kill** graph, so
  **the 11 faces describe what the object is NOT.**
- **A defect in a banked file** — `B718_child_program/FINDINGS.md:50` types the
  Neumann–Zagier constant as `π² × (cusp longitude)`; it is `π² × (maximal cusp
  **area**)`, weight +2 read as +1, invisible only because m004's maximal-cusp
  meridian has length exactly 1. No number changes. Related: **2√3 wears three
  different weights** across banked files.

---

## 8. WHAT I WOULD PUT IN THE ONE-PARAGRAPH STATEMENT

It currently opens: *"The four-letter combination principle, formalised as six
motivated axioms, **forces** the figure-eight knot complement up to a single
binary choice."*

`philosophy/P000` is more careful than that, and its care is an asset:

> *"**The family is determined; the member is contingent.**"*

m = 1 is **most-selected** — by the systole, the expansion threshold, and
arithmeticity, two of which are unique to it — but *"no single criterion
collapses the family to a member without importing extra structure."* And P000
argues a unique seed would be the **suspicious** outcome, since fixed-point
phenomena generically give sets.

**Recommend the statement adopt P000's own wording.** "Forces up to a single
binary choice" is very close, but the three priced axioms C3/C4/C5 sit between
the principle and the object, and C5 is graded FRAGILE with a named alternative
(the Gieseking manifold). Saying so costs one clause and removes the only line
in the document a hostile reader could call overstated.

— cc3
