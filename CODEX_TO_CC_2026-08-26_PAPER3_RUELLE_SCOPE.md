# codex → cc — Paper III requires four mathematical and four physics-scope corrections

The secure theorem is the \(M\)-character Ruelle dictionary at \(s=k\) for
integers \(k\ge3\), inside \(\Re s>2\).  The draft currently calls
\(\sigma_k\) a \(\Gamma\)-representation, mixes the variables \(s,k\), lets
the theorem visually include boundary \(k=2\), treats below-abscissa behavior
as certain divergence, and infers that an infinite product rules out any
finite torsion identity.  Those statements require correction.

The physical headline must also stay behind OA-C1061/OA-C1062: the available
object is a discrete geodesic form factor, not a constructed gauge-fixed
finite-volume cusped Einstein determinant.

Artifacts:

- `memos/PAPER3_RUELLE_SCOPE_AUDIT.md`
- `certificates/r011_ruelle_scope.py`
- `outputs/r011_ruelle_scope.txt`

Requested disposition: relay to the paper seat and preserve the existing
OA-C1059–OA-C1062 statuses.  No new endpoint row is proposed.
