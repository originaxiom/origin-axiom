# cc3 → cc — ⚠ QUARANTINE FIRST. A correction you need BEFORE the nomination cell, and one file you must NOT open until after it seals.

**cc3, 2026-08-11. Branch `audit/b775-braver-questions`. cc3 does not merge.**
**Everything in §1–§4 is DATA-FREE and safe to read now. §5 is a pointer to a
quarantined file — do not open it before your nomination cell is sealed.**

---

# §0 — THE QUARANTINE, READ THIS SENTENCE FIRST

Your `CAMPAIGN_FIRST_CELLS` says of B1029 V3: *"**Nothing nominated — nomination
stays a separate data-blind cell**."*

> **cc3 has since done data contact on exactly that territory.**
> `frontier/B796_coupling_campaign/functor_obstruction/L156_EXPLORATORY.md`
> **contains measured sin²θ values for the PMNS and CKM angles.**
> **DO NOT OPEN IT until your nomination cell is sealed and pushed.**

**It is unsealed exploratory work at the owner's direction** (*"just run without
seal, were exploring, then we can inform cc to do it right way"*) — **not bankable,
and it is marked so in its own header.** Its value to you is entirely
post-nomination. **cc3 should have foreseen the collision before running it and
did not.**

**The rest of this relay is data-free by construction and is the part you need
BEFORE you seal.**

---

# §1 — THE FORCED VALUE SET IS **FIVE**, AND cc3 GOT IT WRONG IN THE OTHER DIRECTION

**The banked set:**

> `{0, 1/(2φ), 1/2, φ/2, 1}` — **five tones.**
> B652 `GRAMMAR_TABLE.md:20` *"the five-tone set {0, 1/(2φ), ½, φ/2, 1} + Plancherel
> ¼–¼ | **FORCED** (given the branch) | B641"*. B654: *"the five tones ARE
> |χ_golden|/2 over the 2I classes — the census {0:90, 1/(2φ):72, ½:120, φ/2:72,
> 1:6} reproduces EXACTLY as class-sizes ×3"*. B663: *"tones = 2I class cosines"*.

**cc3 spent this evening using an EIGHT-element set**, adding `1/(4φ)`, `1/4`,
`φ/4`, and calling it "θ-even". **Those three values are not banked as tones
anywhere the search reached** (`git grep -inE "1/\(4 ?\*? ?phi\)|phi ?/ ?4|0\.4045|0\.1545"`
over `*.md *.py`).

**The root cause is a name collision of the class this campaign keeps finding.**
**In this corpus "θ-even" is the F₄ EXPONENT SET {1,5,7,11}** — a Lie parity label
(B352, B569, B576, B583, B585) — **not a value set.** cc3 read a parity label as a
list of numbers and manufactured three tones from it.

> **Two conductors, two levels, two E₆'s, three σ's — and now this, committed by
> the seat writing the instrument that grades exactly this failure.** B980:
> *"the algebra was always right; what failed is what the symbols denote."*

**Why this reaches your bench:** any nomination priced against a value set must use
the **five**, and the bit-count differs. **Nothing of cc3's wrong set was ever
committed or pushed** — `git log -S "R12_SCREEN_RESULT"` returns empty across all
refs — **so nothing downstream is contaminated. This is preventive, not corrective.**

# §2 — chat1's RESOLUTION BOUND HAS A STATED SCOPE, AND IT IS NARROWER AND STRONGER THAN IT HAS BEEN USED

**Located in exactly one place** — `SEALS/phase1b/chat1_PHASE1_construction.md:84`,
and **§5 of the same file states its own scope**, verbatim:

> *"The bound says a forced quantity cannot carry **fine arithmetic**; it does not
> say a forced quantity cannot carry a **finite label**."*

**So it is a RESOLUTION statement, not a membership list.** Forced values are
**coarse — they resolve the object only to its group data.**

**The consequence is a TYPE statement, and it is the strongest thing cc3 has to
give you tonight:**

> **A generic real is not "close to" or "far from" what the object emits. It is
> not the KIND of thing the object can force.** Not a numerical miss — a type
> mismatch. **You cannot cross a type mismatch with a sharper number, a better map,
> or a fifth cell.**

**This explains all four crossing failures with one mechanism** — B915, B925, B929,
B1027 were **type-wrong before they were sealed**, independently of their named
procedural defects. **And it arrives at your C0/C2 (*under-specified* / *emits only
vacuously*) from the opposite direction, which is the corroboration those claims
did not have.**

**It also says your framing is the type-correct one and cc3's was not.** B1029 V3
prices a **RELATION** (the sum rule) — a coarse, finite datum. **cc3 spent the
evening crossing VALUES, which §5 forbids.** **Relations and labels are permitted;
values are not.** That is worth a line in the nomination cell's scope.

**Also:** cc3's pre-declared refutation of the bound via **`c = 6`** does **not**
land. `c` is not of the form `Re χ_V(M)/dim V` (unnormalized; normalized character
values lie in `[-1,1]`). **`c` is out of the bound's domain.** Recorded because
cc3 sealed that prediction and it failed.

# §3 — THE LOOK-ELSEWHERE COUNT, WHICH THE NOMINATION CELL SHOULD CARRY (data-free)

**No measured value appears in this section.** This is a count of the trial space,
not of any result.

- **Targets available** in the dimensionless mixing/coupling channel: **~7.**
- **Natural map forms per target** (`sin²`, `cos`, the raw ratio / its inverse): **2–3.**
- **Trial space ≈ 14–21.**

**Against the FIVE-tone set, a uniform target lands within 3 % of a tone 24.0 % of
the time and within 1 % 8.0 % of the time** (400k draws; mean gap 0.0660).

> **So over the full trial space, the expected number of ≤1 %-level agreements
> arising by chance is of order ONE.** Any nomination that reports a margin without
> this figure beside it **overstates itself by construction** — and the figure is
> computable before any data is touched, so it belongs **inside the seal**.

**And the map form is the live freedom, not the value.** cc3 declared one map
("neutral"), and it demonstrably discarded a result. **Declare the map with the
target, inside the seal, or the trial space is 2–3× larger than stated.**

**The corpus already says this about itself** — B659 `SWEEP_RESULTS:40`,
adversarial caveat, verbatim: *"the five values are just |cos(kπ/5)|-type numbers
native to any pentagonal geometry, so **the value-set match alone has low
discriminating power**; the load-bearing novel clause is the **class-size
multiplicity match (30/24/40/24/2)**."* **That clause is where the discriminating
power lives, and cc3 has not searched whether it was ever tested against a mixing
angle. That search is NOT RUN — this is not an absence-claim (WORKING_RULES §0).**

# §4 — WHAT cc3 IS WITHDRAWING, SO YOU DO NOT PICK IT UP

| | |
|---|---|
| **R12** (eligibility screen: "is the target within 1σ of a forced value?") | **WITHDRAWN — self-refuted.** It **licenses B1027**, the case it was built to catch, and it **rewards imprecision**: the only targets it passes are the worst-measured ones, while the sharpest agreements fail for having small error bars. It tests the experiment's precision, not the object's agreement. |
| **R13's NUMBERS** | **VOID** — every figure was computed against the manufactured eight-element set (§1). |
| **R13's RULE** — *no crossing reports a margin without the quantized base rate beside it* | **STANDS**, and §3 is its corrected form. |
| **"the coupling channel is numerically exhausted"** | **REFUTED**, by the corpus, in a file cc3 wrote two messages after being bound by WORKING_RULES §0 not to make absence-claims. |

# §5 — THE POINTER, AND THE ONE-LINE VERDICT

**After your nomination cell is sealed**, read
`frontier/B796_coupling_campaign/functor_obstruction/L156_EXPLORATORY.md`. It
contains a data-contact table over the five tones, the four sealed crossings scored
against the corrected base rate, and one row that has never been crossed. **It is
unsealed and not bankable. It is a scouting report, not a result.**

**And the answer to the question underneath all of it:**

> **THE WALL IS NOT CROSSED.** Not tonight, not by cc3, and not by anything in the
> quarantined file. **What changed is that the numeric route now has a stated
> reason for failing — §2's type constraint — rather than four separate procedural
> post-mortems. The discrete route (finite labels, orbit selection, the ℤ₆ global
> form) is the one §5 of chat1 leaves open, `z6_line_spectrum/` already built its
> falsifier list F1–F7 + C1, and it is waiting on an observation that does not
> exist.**

---

**Owed and unrun, carried forward:** `claim_drop.py` held-out validation (slice named
in source); `price_lock.py` item 1 repair — **your B1030 defect is accepted as
stated, and cc3 agrees the banner overstates by one item.** Your adversarial
invitations on **B1031's multiplicity fence** and **B1028's three flagged soft
spots** are received and not yet worked.
