# R32: source-C3 module and resolved-partner projection controls

2026-09-16. Path-local PB-BOUNDARY work, no B allocation. Design, proof,
prior, input hashes, producer and tests must be committed, pushed and
remote-confirmed before their first scientific execution. No empirical
premise, SM target, new source solution or main bank is part of this cell.

BANKED IDENTITY: R19 gives the actual Fox cochains; R20 the actual
geometrical/word C3 action and equivariant line locus; R30 the distinct
resolved domain and corrected C3 control; R31 the compact Poisson
well estimate. B325 already knows the regular C3 representation.
These are reused, not rediscovered. See SOURCE_C3_PRIOR.md.

P0: compute the three honest scalar lifts of each of the three invariant
rank-one characters in the prescribed three-proper-arc source model,
and invariant subspaces of its singular and resolved form complexes.
The positive-energy pairing lemma applies to any graded self-adjoint
operator with a commuting grading-preserving unitary symmetry on its
actual domain. This does NOT quantify over all arithmetic-parent
quotients, source theories, spin lifts, covers or physical orbifolds.

P1: X33/PB-BOUNDARY, continuing R20's explicitly unperformed projection
and R31's resolved-spectrum duty. P2: atlas card read with epoch caveat.
P3: law/lead/code/body search finds prior, including B325, R19--R31 and
the main B1413 harvest; no absence or novelty claim. B1014 and B936
flagged by already_banked were read and do not provide this operator
projection. P4: B960's hatch explicitly excludes orbifold/noncommutant
routes from its centralizer kill; R20 also narrows its 27-specific
claim. Do not extend that kill here. P5: seal before execution, preserve
all failures. P6: declared expectation is Reg for the two acyclic
characters, one invariant source mode, and retained resolved partners.
This is an analytic prior, not a measured or independently reviewed result.

## Proof versus computation

SOURCE_C3_PROOF.md gives the finite-cell fixed-fibre trace argument,
equivariant long exact sequence, trivial-line comparator and analytic
domain/positive-spectrum statement. Published triangulation input is
read from the primary Luck--Rothenberg theorem with boundary. Tests
cannot certify this proof merely by reproducing its character arithmetic.
No ordered geometrical assignment of weights to arcs is promised.

The geometric hypotheses reuse the frozen R20 100/212-bit canonical
certificates; no new interval geometry run is claimed. The producer
rebuilds the actual order-three matrix from exact word maps and uses
the frozen R30 C3 control to re-evaluate all three fixed characters
with the original R19 Fox complex. It must reject (zeta,1) as fixed.

## Exact controls, fixed before execution

All module algebra uses exact SymPy rationals and sqrt(-3), no floating
phase thresholds or numerical eigenspectrum. Enumerate all 27 ordered
triples of cube-root fibre weights. Trace zero must hold exactly for
the six permutations of (1,zeta,zeta^2). Same-weight triples are an
explicit rejecting control for the acyclic-base hypothesis, but must
remain valid for the trivial-line control.

Check group averages as matrices: order, unitarity, idempotence, ranks,
all three lift phases, both C3 generators, a rational orthogonal basis
change and the cyclic-permutation realization. Check that a commuting
diagonal mass matrix can have three unequal masses (B325's scope).
An order-two matrix falsely supplied as C3 must be rejected.

Use an explicit minimal relative-cone chain complex for the trivial
line: C0=1, C1=5, C2=1, d0=(0,0,1,1,1)^t and d1=0; C3 acts by its
two nontrivial H1 characters and three identical source-fibre phases.
Compute projected cohomology using ranks, not a prefilled outcome
table. Test natural and both twisted lifts, with dimensions (2,1),
(1,0), (1,0), and net one. Cross-check by averaging the alternating
cohomology character.

For the nontrivial source/core MINIMAL chain model use d=t I on Reg,
t in {0,1,2,1/7}. This is a homotopy model, not a physical mass formula.
Projection must give (1,1) at t=0 and (0,0) otherwise. Compute D^2
and the exact intertwining relation. Projecting one parity only must
fail the domain/differential-invariance check when t is nonzero.

For arbitrary constant C3 gauge-factor multiplicities sampled on
{0,1,2}^3, compute tensor weights and compare invariant dimension
with dim W for all scalar lifts. The all-W statement follows from
the proof, not completeness of this finite sample.

Two-sided physical logic control: the circle reflection projects its
zero modes to one/zero but each nonzero Fourier level to one/one.
Use exact Fourier differential matrices at n=1,2,5. This intentionally
rejects a universal claim that group projection can never make an
unpaired zero mode. Closed fixed circles must have Euler trace zero.

## Execution and reporting

First run the native producer under exclusive raw capture, then the
new test file, then the four unchanged full test files:

    tests/test_physical_bridge_holonomy_spectrum.py
    tests/test_physical_bridge_holonomy_equivariance_word_control.py
    tests/test_physical_bridge_resolved_fermion_c3_control.py
    tests/test_physical_bridge_global_poisson.py

Include the new file in that focused run. No broad-suite or full-green
claim: the previous 54-file R31 historical result and its 24 failed/error
IDs remain unchanged; the original packet-alias failures also remain.
Keep the tree read-only during each scientific run. Any scientific
repair requires a separately sealed successor, retaining original files
and failed output. Publish receipts with only declared machine-path and
remote-family privacy redactions; raw hashes remain byte-faithful.

Report proof/finite tests/reused geometry/physical assumptions separately.
Update reader surfaces and explicit remaining duties, then gates and
byte custody, before committing and pushing only this seat's branch.
No own result is described as independent receiving review or main bank.
