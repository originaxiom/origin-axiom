# R16 post-failure instrument controls — before their execution

2026-09-07. Original seal bec4aa92, original source/design/tests unchanged.
The first producer stops at the sign-symbolic exponential limit and emits
no JSON. Original tests finish: 12 pass, 4 fail in 29.62 seconds. Full
receipt CHARGED_DOMAIN_FAILURE.txt; environment-prefix-only redaction of
the explicitly ignored local raw receipt. No scientific failure is hidden.

BANKED IDENTITY: R14/R15's prescribed source field and finite cohomology,
and the original R16 metric/differential and stated testing thresholds.
PRIOR ART: the first R16 source and tests are the evidence being repaired,
not overwritten. No external scientific premise or interpretation changes.

P0: exactly the same local charged de Rham family and global formal
extreme degrees as CHARGED_DOMAIN_DESIGN.md. P6 prior: four instrument
failures should resolve by elementary identities and numerical scaling;
if they do not, preserve the next failure rather than relax expectations.

1. The original simplify routine leaves a hyperbolic identity unevaluated:
   sinh(2r)*(coth r-tanh r)-2. Add an exponential rational normal form as
   a zero test ONLY; retain the original nonzero expression otherwise.
   Test all eight equations plus a deliberately wrong Higgs coefficient
   and the known nonzero 1+sinh(r)^2. Do not declare every residual zero.
2. SymPy's unconstrained power integral returns unevaluated Piecewise/
   Integral branches. Verify the explicit primitive -r^(-2L)/(2L) by
   differentiation, and evaluate its endpoints. The integral remains
   the integral of the original graph energy. Also check the minimizing
   cutoff's normalization and its energy primitive independently.
3. Original quadrature uses absolute tolerance 1e-13 even on norms much
   smaller than that; its worst relative discrepancy is 1.9601911e-9,
   above the sealed 5e-12 bar. Scale the log-coordinate integrand by the
   larger leading endpoint exponent and set epsabs=0, epsrel=1e-12.
   Do not use the analytic integral as the numerical answer or loosen
   the 5e-12/1e-3 test thresholds. Same 168 charges/forms/cutoffs.
4. For real q and positive z the exact real log density is
   -2*q*(U+C*z^2*log z+c*z^2)-3*log z. Verify its exponential equals
   the original positive norm density, then take its elementary limit.
   Both signs and q=0 remain in scope; no sign is picked to make it run.

The separate control module imports the immutable source and replaces
only these four instrument operations in MEMORY. Its own tests directly
assert the mathematics and re-invoke the original failed assertions on
that repaired module. The original test file still reproduces its four
failures when invoked normally; it is not silently rebound repository-
wide. Test the angular/threshold results again as unchanged controls.
Hash and commit this design, control source and tests before execution.
The full physical goal and complete H1/Fredholm obligations stay active.
