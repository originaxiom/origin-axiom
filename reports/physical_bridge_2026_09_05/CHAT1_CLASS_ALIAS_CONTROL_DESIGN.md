# Separate observed-failure control: the published tessellation identifier

2026-09-15. Seal and push this design, wrapper and new tests before
execution. Original seal 5b6391c368a7695fef4b86a7c7bf8bfee7f8a309 and
all its source/tests remain unchanged. This is not a changed hypothesis.

The first native exact run stopped at construction of the final negative
control: the installed general Manifold constructor does not recognize
the paper's old name otet06_0000. All eight original new tests errored
at their shared fixture for the same reason. No printed exact-index
or matrix-group result survived that aborted aggregation, and no such
result is claimed from that run. The independent Sage canonical run
passed and is not rerun or changed by this correction.

Use the published triangulation isomorphism signature instead of an
unresolved alias. Fominykh et al., author PDF dated October 6, 2015,
Table 2, page 7 gives otet06_0000#0 as gLLPQccdfeefqjsqqjj. The same
paper's Remark 5.5 names it as a negative two-coloring control. Those
precise source passages have been read, not a search-name guess.

The wrapper loads the unchanged original producer after checking its
SHA256 and only redirects that one constructor input to the published
signature. All mathematical computations, fixed sample names, index
expectations, negative controls and predicates remain the original's.
The output retains both the historical name and constructor signature.
No fallback to an arbitrary manifold, smaller sample or unverified
volume ratio is allowed. New tests repeat all original mathematical
assertions against the resolved fixture and explicitly lock the alias.

Preserve the first failure logs and these follow-on logs separately.
Run native plus the new test file with exclusive external captures and
a read-only checkout. No full-suite green or initial-run success is
claimed. The manuscript's physics still needs a representation-valued
operator/index and source/end action, not just this arithmetic check.
