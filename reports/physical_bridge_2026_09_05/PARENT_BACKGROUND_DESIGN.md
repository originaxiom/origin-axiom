# R39: complete parent equations, not a central projection

2026-09-20. Pre-execution design; no shared B allocation.
This file is frozen with code, tests, proof, prior and inputs in the
path-local R39 entry of docs/SEAL_LEDGER.md before first execution.
The later execution receipt, not this design, records actual outcomes.

Reconciliation disposition: B574/B1420 already supply the SL2 pairing
and twist escape; B274 supplies the principal E6 smooth germ. Do not
repeat them as missing results. The bounded duty here is their actual
parent-action, positive-metric and full-operator join. Existing B1418/
B1427 nonsplit positives survive this geometric control and are not
claimed physically admissible by their algebraic indices. See
[the reuse map](CORPUS_RECONCILIATION_2026_09_20.md).

P0: this computes the complete D5-singlet SL4 sector of R38's
candidate E8 parent, a restricted charged-source ansatz, and the
specified geometric Sym3(SL2) background on oriented hyperbolic
three-manifolds with a chosen holonomy lift. Neither outcome decides
all E8 backgrounds, all sources, either arithmetic row, or a TOE.
The local construction is generic to curvature minus one, not a
distinction of one member or the golden arithmetic class.

P1--P4: the approved common-model origin gate, PB-BOUNDARY/PB-ACTION,
R29's explicitly omitted full-parent couplings, R35's noncommuting
hatch and R38's charged adjoint roster. Read the prior note. B574
already explains SL2 self-duality; no novelty is claimed for that
obstruction. The positive and its full gauge/matter costs must be
reported together. No empirical inputs or observed constants occur.

P6: the authored calculation predicts a nonzero traceless family
moment map when a nonzero rank-one/rank-two source Gram matrix is
mistaken for a central source. It ALSO predicts that the full
geometric noncommuting connection solves all parent residuals,
with finite Higgs norm on finite-volume quotients. Its full algebra
should have 55, not merely 45, invariant generators, and its paired
coefficient bundles should have an explicit unitary self-duality.
These are disclosed expectations before execution, not discoveries
against a negative prior. Any failed identity stops its claimed join.

## Conventions and fixed two-sided tests

1. Use R38's actual D5 in the first five E8 coordinates and its
   U=(0,0,0,0,0,-2,2,2). The four SU4 weights in the last three
   coordinates are (-1,1,1)/2, (-1,-1,-1)/2, (1,-1,1)/2,
   (1,1,-1)/2. Their differences must give ALL twelve D3 roots;
   U becomes diag(3,-1,-1,-1). Compare every joint E8 weight in
   248=(45,1)+(1,15)+(10,6)+(16,bar4)+(bar16,4), not dimensions alone.
2. Hermitian Higgs/gauge convention: C=phi+iW, phi=(C+Cdag)/2,
   W=(C-Cdag)/(2i). Curvature is dC+C wedge C; the real residual
   is I=div(C+Cdag)+g^ij[C_i,C_jdag]. Keep the metric divergence.
   A general traceless 4-by-4 matrix includes central h, traceless
   3-by-3 B, independent charged row alpha and column beta. Verify
   ALL four blocks of its curvature by independent matrix expansion.
3. In the restricted h-real, B=beta=0 ansatz verify the entire
   matrix moment map. Its noncentral lower block is minus the
   traceless Gram matrix G_ab=sum_i conjugate(alpha_ia) alpha_ib.
   Rank-one and rank-two examples must fail a central-only criterion;
   an isotropic full-rank Gram matrix must pass that algebraic test.
   This latter test is pointwise, not a background solution. After
   the F01 reception, also check the rank-(1,3) invariant-projector
   pairing against both Id4 and the actual U generator. A scalar
   endomorphism is not the smaller subgroup's central generator.
   Also verify I=-2 d_A^*phi in the common Hermitian convention.
   This is a source-type/sign control, not a constructed compensating J.
4. In upper half-space g=z^-2(dx^2+dy^2+dz^2), use the normalized
   four-dimensional sl2 representation H=diag(3,1,-1,-3),
   E superdiagonal (sqrt(3),2,sqrt(3)), F=Edag. Test
   C=(E/z, iE/z, H/(2z)). Recover the 2-by-2 precursor independently
   from q^-1 dq, q=((sqrt(z),(x+iy)/sqrt(z)),(0,1/sqrt(z))).
   Check all curvature, real moment-map and Hermiticity components.
   Omitting commutators, using the flat metric, or dropping B must
   fail. The B-dropped example must retain zero CENTRAL moment map
   while failing the full equations: the precise false-positive control.
5. Check the invariant norm of phi and the cusp tail integral, including
   the expected factor 60 from trace in the four to trace in the E8
   adjoint. Verify that factor on the whole symbolic Cartan, not a
   fitted single value. Global
   descent and full-action stationarity are authored arguments, not
   a numerical solution of the quotient or a theorem invoked beyond
   its boundary hypotheses. No compactness-only Corlette shortcut.
6. Compute common invariant kernels on 4, exterior-square 4 and
   traceless endomorphisms of 4. Include the ten extra E8 generators
   from the exterior-square singlet. Identify the invariant algebra
   by its FULL D5 weight/root system and Cartan matrix, not 55 alone.
7. Exhibit the antisymmetric unitary J intertwining 4 with its dual,
   for E,F,H, all background coefficients and the positive metric.
   Its complete invariance kernel must be one-dimensional; adjoining
   U must kill that kernel (an algebra control, not a chiral solution).
   Show the original charge-three singlet and charge-one triplet do
   not form separately closed sectors of the full positive operator:
   apply d_C^*d_C to x e0 and retain its complementary component.

Seal the design, proof, prior, inputs, native code and tests; commit,
push and server-confirm before first import/execution. Run native,
new tests, then R38's exact twelve-file population plus the new file.
Retain raw exits and any failure. Corrected science requires a new
named seal; do not weaken a criterion after seeing its result.

## What either outcome can and cannot decide

A full solution would earn a conditional classical parent background,
not R29's localized tubes, three generations, a chiral phase or an
E8-selection law. A self-duality comparison is restricted to domains
transported by J (including matched minimal/maximal L2 closures);
it does not exclude different defects, boundary sectors, additional
holonomy or non-SL2 deformations. Keep the sourced path intact.
The next duty is a specified deformation/source matching problem
with the full operator, not another free profile scan or a raw index.
