# Next mode join: working hypotheses, not a result

September 23, 2026. Own branch audit/physical-bridge-2026-09-05.
UNSEALED / UNEXECUTED. No new science producer, numeric calculation,
certified estimate, all-parameter finite-volume claim or physical mode
identification is supplied by this note. The completed checkpoint is
[the R43 reception](PROJECTIVE_MODE_RECEPTION.md).
The prior is cautiously positive for retaining paired cohomology modes,
not for deriving net chirality. Seal the actual proof/control design,
inputs and tests before their first execution.

## 1. Why this is a bounded next step

R42 supplies canonical harmonic-flat data on a finite-volume proper
convex quotient. F11 supplies exact exceptional H1 classes and an L2
comparison in a different metric. R43 receives the all-positive
proper-convexity theorem but does not discharge the marked-quotient,
volume or norm/domain joins. The target is the actual type-one cusp's
complete positive norms, not a new census of algebraic ranks.

The two analytic questions must stay separate: does a bounded tail
contraction give closed-range cohomology with a positive end gap; and
does its norm tend to zero, as needed for the stronger confinement
argument used in F11? A surviving continuous positive spectrum would
not itself eliminate normalizable zero modes.

## 2. Proposed model-domain comparison

The following calculations are proposed controls to verify, not
certificates. In the affine chart of Ballas-Long's peripheral model,
write coordinates (x0,x1,y), x1>0, and

    r = x0 + log(x1) - y^2/2,
    Omega = {x1>0, r>0}.

The displayed projective peripheral flow has the proposed form

    (x0,x1,y) -> (x0+s*y+s^2/2-t, exp(t)*x1, y+s).

It preserves r formally; the linear lift has to be normalized to
determinant one before comparing the ACTUAL coefficient metrics.
Do not mistake these local model coordinates for an already proved
global developing-domain formula.

At the point (R,1,0), try the projective rescaling

    X0=x0/R, X1=x1, Y=y/sqrt(R).

The transformed inequality is

    X0 - Y^2/2 + log(X1)/R > 0,  X1>0.

The candidate pointed limit is the projectivization of the product
of a positive ray with a three-dimensional Lorentz cone. The tempting
metric comparison is a hyperbolic two-dimensional cusp factor and a
nonshrinking longitudinal factor. It is NOT the full three-dimensional
hyperbolic metric of F11.

Required proof, not a finite-sample substitute:

- Identify the actual global developing quotient, its marking, cusp
  type and finite compact core for the exceptional parameters, including
  the hypotheses of the continuation/generalized-cusp theorems.
- Establish a valid sandwich or pointed convergence for the ACTUAL
  domain, not only for the homogeneous comparator. The existence of an
  invariant convex domain by itself is not that estimate.
- Apply the precise all-jet affine-sphere continuity theorem with its
  normalization and a compact set containing the comparison point.
  Track the canonical positive metric on the real linear bundle too,
  not just the base Riemannian metric.
- Verify the proposed base scales: radial 1/R, meridional 1/sqrt(R),
  longitudinal order one. A volume density of order R^(-3/2) would
  make the model end integrable, but must not be asserted for the
  quotient until its comparison and fundamental domain are established.
- Handle all relevant form norms, duals and unitary central twists.
  Unitary twisting may preserve local norms while changing descent;
  write the actual map instead of identifying the bundles by rank.

## 3. Proposed operator test

In an appropriate normalized peripheral frame the semisimple
longitude generator should remain off the imaginary axis for q!=1,
while its commuting nilpotent part becomes controlled after the
rescaling. Check this with the literal matrices and scalar characters,
not just their eigenvalues; nonnormal condition numbers matter.

If the inverse of the longitude operator and the longitudinal vector
length are uniformly bounded in the canonical norms, try to construct
the bounded full-complex contraction K. Verify the radial commutator,
complete closed domains, cutoff and compact-core gluing. This could
give the L2/relative/ordinary comparison without compact resolvent.

If the required bounds fail, retain that failure and derive the actual
end operator; do not infer universal vanishing or chiral asymmetry.
If the comparison succeeds, it would put the ALREADY KNOWN paired
exceptional classes in the SAME finite-energy background. It would
not select q or the scalar character, give a chiral four-dimensional
spectrum, produce the full parent/gauge sector, or complete physics.

## 4. Controls and stopping rule for the later seal

Positive controls: the product-cone affine sphere, a unitary scalar
twist and the literal nontrivial q longitude.
Discriminating controls: q=1 (the inverse argument must fail), an
artificial zero semisimple weight, and an incorrect base rescaling.
Explicitly compare bounded versus decaying homotopy norms; do not
encode compact resolvent as the only possible successful outcome.

Keep R40/R41's distinct nonsplit source/end route registered. Complete
this same-background comparison before another geometric analogy or
parameter scan. If the result is paired, preserve its matter-mode
positive and direct the chirality mechanism back to the actual
source/action/interaction duties. Nothing here merges those models.
