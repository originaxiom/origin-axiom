# Path-local R16 — the charged domain at the prescribed logarithmic sources

2026-09-07, before first execution. This continues R14/R15, not the smooth
principal-local-system problem. No B allocation and no empirical target.

BANKED IDENTITY: reuse R14's periodic line-source end and R15's global
commuting field, finite pair and compact-E6 adjoint grading. Their source,
test and seal bytes remain unchanged. R16 means only this report path.

PRIOR ART: logarithmic Higgs defects and relative deformed de Rham
cohomology are established mechanisms, not discoveries of this round.
Pantev--Wijnholt, https://arxiv.org/pdf/0905.1968, equations (3.20),
(3.36)--(3.40), checked 2026-09-07. For singular first-order domains see
Hartmann--Lesch--Vertman, https://arxiv.org/pdf/1709.01636, section 4:
their abstract edge expression and spectral gap are useful checks, but
their compact stratified-space theorem is NOT a theorem about this
complete cusp/line corner. In particular their (4.8) uses the stronger
closed interval [-1,1]; we derive the normal first-order threshold here
directly, without borrowing their D-squared domain assertion.

## Grounding, scope and searches

P0: charged twisted de Rham forms in a trivial angular gauge bundle
near prescribed proper geodesic line defects in curvature-minus-one
hyperbolic three-manifolds, and the degree-zero/top-degree solutions on
R15's globally extended positive-source backgrounds. The claim quantifies
over this cut ansatz, not every face, bundle, sibling or physical theory.

P1/P4: continue X33's boundary hatch and PB-BOUNDARY, not reopen a
closed-double count under the same hypotheses. WORKING_RULES, the
compute protocol, ladder/campaign, current framework and CC banking
receipt were consulted. The owner's continuing chirality task and R15's
explicit next obligation explain this ordering. No global B number is
taken; SM's allocation and main's incoming work are preserved.

P2: atlas chirality card returns the known 1161-arc, epoch-limited
inventory; its null is not a novelty certificate. P3: already_banked
query `logarithmic Witten indicial domain` returns 16 hits, zero settled
arcs matching two of four terms. The all-head `absence_sweep.py indicial`
enumerates eleven local/remote heads, zero current/content/path hits and
zero matching deleted paths. This is an absence only for that word, NOT
for weighted de Rham theory under every synonym. Earlier broad Witten,
logarithmic-domain and normalizability searches returned PRESENT.
R15's full design/report and producing source were read; no available
global charged-kernel certificate is silently substituted by its scalar
resolvent or its finite cohomology.

All nine remote branch heads and tags were fetched and live-SHA verified.
Current pins: main c78003cdc2068f72355c57eedd380fe9d93ad524; SM
d1a91c7aee7bf9aa8b3cfa3c01ddfa0d5fd622aa; physics
659487bbd93c7990c4686a8b86985b6b66efedc4. New main B1295's complete
body was read: its complex Fourier coefficient agrees in convention
with R13's real sine coefficient, but its producers have NOT been rerun
here. Its finite grids do not constitute an interval proof on all heights.
SM B1282's faces addendum and producer were read; its Alexander-module
route is different from the chosen compact-E6 source parent. SM B1283's
incoming vacuum claim is not a recertified global F-flat-variety audit.
None of these incoming files is merged or used as a new computed premise.

P6 prior: expect a nonempty strong-source region in which the line's
normal operator needs no extra self-adjoint trace choice, and a weak
region with genuine trace freedom. Expect cusp decay alone to be an
insufficient normalizability test. Neither outcome decides H1 on the
complete singular geometry. Report the actual threshold, not a number
chosen to retain net three.

## Declared operator and conventions

Coordinates (r,theta,t), orientation dr wedge dtheta wedge dt. Put
A=sinh(r), B=cosh(r), metric diag(1,A^2,B^2), volume AB dr dtheta dt.
The dimensionless effective coupling is a=q*beta, including any Higgs
amplitude/kinetic convention. An E6 root grade +/-1 does not set beta.
H=a log(tanh r), H'=a/(AB), d_H=d+dH wedge, delta_H=delta+i_grad(H),
D_H=d_H+delta_H. These are differential forms in the specified twisted
reduction, NOT an assumed physical spinor Dirac operator on M alone.

All eight coordinate form components are tested. Theta and t independent
joint d_H/delta_H zero branches are proposed by elementary integration:
tangential forms have coefficient exp(-H); a component J containing dr
has coefficient exp(H)*product_{j in J} g_jj/(AB). Derive their actual
orthonormal radial powers and norm measure before counting anything.
Smooth subtracted regular parts change no leading power on a COMPACT
piece of line. Fixed longitudinal momentum is lower order there; this
does not prove a uniform estimate where a line enters a cusp.

For nonzero angular Fourier modes exp(i m theta), m integer, derive the
full four-component normal matrix by exterior derivative and metric Hodge
star, on (1,dr,r dtheta,dr wedge r dtheta), with coefficient r^lambda.
Compute the second copy wedged with dt too. Normalize L2(r dr) to L2(dr)
before stating a first-order spectral window. No angular cutoff can prove
an all-m statement: use the generic characteristic polynomial and the
integer inequality m^2>=1 for m!=0. Nontrivial angular holonomy would
shift m, and is explicitly outside W=0, not ruled out.

Minimal domain is the graph closure of compact support away from r=0;
maximal means distributional D_H is L2 on the punctured neighborhood.
Keep D_min distinct from the Hilbert-complex operator d_min+delta_max.
Likewise d_max+delta_min is a different complex extension. Compare the
paired scalar/radial traces with the Green boundary form. On the flat
normal model the least radial-cutoff energy of a r^lambda branch is
1/integral_epsilon^R r^(-2 lambda-1) dr. Derive this capacity from
Cauchy--Schwarz and its minimizing derivative; include logarithmic
critical cases. These statements concern the LINE end, not arbitrary
conditions at an artificial outer radius.

At a cusp use R14/R15's actual F=U(w)+(pi Q/A)z^2 log z+c z^2+v,
where v is bounded in the high tail and U has beta_i log|w-p_i|
singularities. For the exact scalar e^(-qF), norm measure is z^-3 dw dz.
Separate local spatial integrability from height integrability. The dual
top form is e^(qF) dvol. With all endpoint densities positive, test both
signs of q, q=0, threshold equality and unequal densities. A square-
integrable formal zero solution belongs to the maximal distributional
domain; that does not automatically put it in the physical extension.

## Two-sided tests, before execution

1. Hodge star squared, d_H squared, and all eight exact hyperbolic joint
   zeros. An intentionally flat-volume codifferential must reject the
   proposed hyperbolic radial solution at a specified nonzero charge.
2. Derive every orthonormal exponent; integrability requires exponent
   >-1 with r dr. Check a in {-2,-1,-1/2,0,1/2,1,2}, including all
   degree counts and charge/Hodge duality. A dr-only measure deliberately
   disagrees on an integrable r^(-3/4) component.
3. Derive the generic angular matrix and its polynomial from the operator,
   then independently assert its entries, both parity blocks, the dt
   copy and normalized symmetric tangential matrix. Removing m or the
   half-density shift must change its predicted roots/critical window.
4. Compute the nonzero scalar/radial Green pairing and both cutoff graph
   behaviors. L2 is NOT sufficient when both traces survive. Radial
   forms are in d_min by radial cutoff even where not in D_min; scalar
   forms analogously lie in delta_min. Do not label these the same domain.
5. Compare analytic power integrals against independent log-coordinate
   quadrature and the exact hyperbolic norms. R=0.01, epsilon=1e-4,
   1e-6,1e-8; relative hyperbolic/normal difference <1e-3. Flat integral
   relative quadrature error <5e-12. Exact threshold classification,
   not a finite numerical cutoff, decides divergence.
6. Derive scalar and top-form equations on a general smooth F, the cusp
   exponential and z^-3 factors, and the complete L2 conditions for the
   stated positive-source ansatz. Reject mixed/nonpositive source inputs
   to that classifier instead of applying an unproved sign extension.

Source, tests and this design are hash-sealed and committed before first
execution, including literal provenance markers checked in the staged
design. Preserve first failures; any repair is a new sealed file. No
editing while a scientific or certifying run is live. Local verification
is not independent/main banking acceptance; H1 convergence, Fredholmness
at joint cusp/line ends, source selection and the complete TOE stay live.
