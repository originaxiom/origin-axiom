# R4 orientation control — declared after the first result, before this control

2026-09-05. The positive R4 minimum must not become a false selection claim.
R4 explicitly disclaimed uniqueness; test a concrete competing minimum of
the **same** potential. No new empirical input or changed potential enters.

Keep phi_1=S and phi_2=N. In the already chosen SU5 Cartan set
`A0=H3+4 H4+16 H5+64 H6`, and normalize A0 by the positive factor
`sqrt(Tr Y^2 / Tr A0^2)`. This fixes the same adjoint norm as R4, not a new
scale or a target chosen to fit data. The prior is that all twenty SU5 roots
have nonzero A0 charge and its compact common stabilizer has dimension four.

Compute that stabilizer by the actual action on the two vectors and the
adjoint, and separately count the SU5 root charges. Verify all potential
squares still vanish and that A0 is not gauge-equivalent to Y (unequal
stabilizer dimensions suffice). A=0 is a control that fails the fixed-norm
potential. Outcomes: competing minimum exhibited / named competitor fails.
Do not call failure of one competitor a uniqueness proof.

This tests a property of the declared R4 action. It does not exclude radiative
selection, other allowed operators, a different action or a source-derived
selection rule. It does not identify this conditional model with the full
relational origin program. The code and design are sealed before execution.
