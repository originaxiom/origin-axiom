# R44: the same-background cusp join -- pre-execution design

2026-09-25. Path-local work; no shared B/I allocation. Author: originaxiom.
All new scientific files are to be committed, pushed and server-confirmed
before their first execution. This design is not an execution report.

## Scope and honest prior

This computes over the positive real Ballas--Long defining SL4 family,
its actual peripheral subgroup, unitary scalar twists and duals, on
the canonical affine-sphere base and coefficient norms. It is NOT a
class-wide, all-filling, all-coefficient or full-parent spectrum census.

The cautiously positive prior is that the already received exceptional
paired H1 classes survive on this same finite-energy background. A
bounded, nondecaying tail homotopy is a success, not a failed compact-
resolvent test. Net chirality is not predicted by equal dual dimensions.

The proof is authored mathematics using cited existence, continuity,
Margulis and compact-core theorems. Finite symbolic tests cannot certify
its global geometry, topology or operator-domain arguments. No external
proof acceptance, interval solve or normalized wavefunction is promised.

## Identification and convention register

- Literal generators and longitude word are the unchanged R42 producer's;
  q=2t>0, k=log(q), beta=6/(q-q^-1), q!=1. No projective scalar is
  silently used as the coefficient holonomy.
- Ordered basis is (p0,p1,p2,p3) constructed from the longitude projector
  and meridian logarithm in the proof. Cone orientation can change the
  sign of the second coordinate; volume uses its absolute determinant.
- Affine coordinates are (x0,z,y) with homogeneous fourth coordinate 1.
  Peripheral coordinates (x,t) each have period 1; radial R increases
  toward the end. The frame convention is D=d+B, hence positive Bx,Bt.
- The actual base is h_B, not the fixed hyperbolic benchmark. Coefficient
  norm is the R42 affine-sphere H; scalar twists are unitary line tensors,
  duals use the dual norm. Counts are cohomology/Hodge counts, not particles.
- Literature's type-one group is identified by explicit matrices and
  coordinates, not by its label. No shared identification row is changed.

## Two-sided finite controls (MB12)

1. Reconstruct the peripheral basis from the literal matrices; verify
   both conjugacies symbolically, including beta, determinant, nilpotency
   and the actual off-unit eigenvalues. Samples at rational q and 1/q
   supplement rather than replace the rational identities.
2. Verify the model radial function under BOTH commuting transformations.
   Reverse its logarithm sign: the longitude control must fail.
3. Verify the determinant-one rescaling, meridian/longitude conjugations,
   the radial connection and all three curvature components. Omitting
   the radial connection must fail for the nonconstant nilpotent part.
4. Derive the product-cone metric from log(-u), with exponents 3/8,1/4;
   check the Monge--Ampere normalization, positive principal minors and
   nonshrinking longitudinal vector at the basepoint. A different radial
   exponent must fail the same Monge--Ampere equation.
5. Check the Fourier inverse algebra for arbitrary real frequency and
   nonzero real k, including the dual. At k=0 and zero frequency it must
   be singular. Replacing a semisimple weight by zero must also fail.
6. Verify the full radial commutator and exterior contraction identity,
   not only the torus differential. Check the radial volume exponent
   -3/2 and contrast the nonzero longitudinal limit with the old
   fixed-hyperbolic shrinking longitude. This is NOT a spectral census.

All first outputs and failures are retained. Corrections require new
files and a separate seal, never edits to executed scientific files.
Native run, new isolated pytest file, then proportionate regression of
the unchanged R42 background/control tests. Old failures stay visible.

## Outcome grammar

- SAME-BACKGROUND PAIRED: algebra controls pass and authored global/end/
  domain proof is complete on its stated inputs. Receive F11's exact
  exceptional ranks without running another rank search.
- CONDITIONAL JOIN: finite controls pass but an analytic/topological
  hypothesis remains unpaid. State that exact hypothesis; do not turn
  it into either a physical positive or a universal negative.
- CONTROL FAILURE: preserve the residual and scope; inspect the failed
  calculation before changing any narrative. No automatic route kill.

No outcome selects q, a central character, a physical parent action,
four-dimensional chirality, coupling values, gravity or a full TOE.
R40/R41's distinct nonsplit/source route remains registered.
