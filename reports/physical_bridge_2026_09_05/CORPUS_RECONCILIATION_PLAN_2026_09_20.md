# Owner-requested prior-work reconciliation, before the next physical probe

This is a retrieval and reading task, not new scientific evidence or a
new B-number. Starting audit head: f59d0d6ee74fc0f961b85fcb505fce85437ba112.
The three PARENT_BACKGROUND draft documents are UNEXECUTED and UNSEALED.
They must not count as prior independent discoveries or completed results.

## Population and limits

Fetch every advertised GitHub head and tags, retaining older tracking refs.
Record the server-advertised heads separately from retained local refs.
Search every reachable unique blob from heads, remote refs and tags, plus
the two historical tips retained by earlier retrieval instruments. Include
current filenames, names occurring in history, deleted contents, code,
shell-embedded data and nonignored untracked files. Keep working drafts
separate from committed evidence. Binary bytes are searched, but compressed
PDFs/archives are NOT decoded by that search. No semantic absence, exhaustive
paper reading or coverage of unavailable/unreachable history is licensed.

The existing absence_sweep.py remains a required companion, not the sole
history instrument: its deleted-history lane checks PATHS, not deleted file
CONTENTS. Its normal CLI display also truncates examples. The supplemental
reader adapts the already-used global_poisson_history_scan.rb traversal;
old sealed scripts remain unchanged. Instrument controls must recover a
deleted-content-only witness, binary data, Unicode, filename-only and
untracked positives, reject a fresh nonce, retain both fixture heads, and
fail closed on a Git error. These controls test retrieval, not physics.

## Reading questions

1. What is already proved about SL2 self-duality, nonabelian/nonsemisimple
   holonomy and the actual escape conditions? Read bodies AND producers.
2. Do existing PW/Hitchin/T-brane, harmonic-metric or gauge-action results
   already supply the proposed E8/SU4 background, moment-map analysis or
   common physical field/action/domain? Distinguish local and global claims.
3. Which chirality/domain, source-action and mirror-interaction positives
   can actually compose, and which cannot under the stated hypotheses?
4. Do other seats' geometry, gravity, threshold or normalization results
   change a load-bearing premise of the common-model-first roadmap?

Every selected item receives one of: REUSE EXISTING RESULT, RELATED BUT
DISTINCT DUTY, UNVERIFIED INCOMING CLAIM, or WITHDRAW DUPLICATE PROPOSAL.
Record branch/commit/path provenance. Read any claimed retraction's own
scope and the kill graph's hatch; neither prose verdict nor regex count
is a mathematical verification. No scientific rerun is hidden in this
retrieval pass. Any later rerun must identify its unchanged producer,
record output/exit and separate conditional math from physical meaning.

## Deliverable

A reproducible compressed retrieval receipt, the instrument controls,
and a compact producer-level reuse map. Amend the next-probe plan only
after the reading; leave the source/action/domain and physical-selection
questions visible. Commit and push only on this audit branch, with no
shared B allocation, main merge, silent gate waiver or TOE completion.
