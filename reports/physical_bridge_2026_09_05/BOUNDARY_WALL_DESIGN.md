# R25: realize the reference as a wall, then test its price

2026-09-09. Pre-execution design; path-local, no B allocation.

BANKED IDENTITY: R23's actual two-flavour charged mass symbol and
positive equal-mass reference; R24's whole rounded-boundary eigenline
flux; R19's constant gauge zero mode. Their distinct hypotheses remain.

PRIOR ART: B1085's edge analogy, the routed older corpus bodies, and
the original R23/R24 producers; see BOUNDARY_WALL_PRIOR.md. Standard
domain-wall localization and spin-surface index theory are mathematical
inputs, not discoveries or automatic source-derived physical sectors.

## Scope, choices and two outcomes

P0: the free charged two-flavour Dirac theory on a product collar of
each compact, rounded, nonzero-mass source boundary in R24's positive
proper-arc class. The mass direction and its induced eigenline are the
actual ones. The interpolation to a scalar reference mass is an ADDED
ansatz. Neither an arbitrary physical defect nor every completion is
quantified over. Include both reference signs, reversed total flux,
zero-flux controls, all scalar mass-sign pairs, and different genera.

P1--P4: follow PB-BOUNDARY/X33's registered quantum-end duty. No new
phenomenological fit, selected k=3, main merge or source replacement.
P6: expect a realizable free interface with opposite boundary index,
not a mirror-free completion. If the actual Clifford sign, normal
solution, bundle projection, characteristic polynomial or coupling
identity fails, retain the failure and narrow that conclusion. No
negative on other defect, massive-U1, nonabelian or interacting routes.

All source/test bytes are sealed before import or execution. Failed
first runs and original tests remain immutable. Follow-on corrections
require separate source, design and receipt. Do not turn a structural
symbolic comparison failure into a mathematical no-go.

## A. Faithful chirality and the normal equation

Use four Euclidean gamma matrices (sigma1 tensor 1, sigma2 tensor 1,
sigma3 tensor sigma1, sigma3 tensor sigma2). Set chi4=-product(gamma4),
with + denoting R23's odd/left convention. Seven-dimensional matrices
are gamma_mu tensor 1 and chi4 tensor sigma_i. On right chirality the
internal symbol is -sigma_i, exactly R23's mapped even-form symbol.
Outward normal is the third internal direction. The oriented boundary
has the four external directions followed by internal directions 1,2.
Compute chi6=i product(gamma_tangent)=Gamma_normal=chi4 tensor sigma3.

On a fixed compact boundary flatten |m| without changing P+/P-. Let
r increase outward, M,ell>0, and choose

    Mwall = M P+ + M tanh(r/ell) P-.

It interpolates M n.sigma to +M identity. Compute its trace: the
scalar identity component is not in the original traceless Higgs mass.
No supersymmetric/reality-compatible UV origin of that new mass is
assumed. The negative reference control instead crosses P+ from +M
to -M, keeping the P- mass -M throughout.

Solve (Gamma_normal partial_r+Mwall)psi=0 channel by channel.
For the crossing channel derive cosh(r/ell)^(-M ell), the chirality,
normalization via t=tanh(r/ell), both asymptotic decay tests and exact
integer-exponent integration controls. Constant-sign channels have no
two-sided normalizable solution. Doubling for the conjugate charged
representative is forbidden by R23's actual reality map.

An infinite product normal line is a localization model, NOT a new
finite-volume geometry. A finite collar approximates it when its
width times the bulk gap is large. No uniform fixed-M limit along
the singular exhaustion is asserted.

## B. Induced bundle, index and anomaly

Projection gives P- nabla_R P- with R24's actual spin connection, not
an arbitrarily flat auxiliary connection. Recompute it with a normalized
local eigenvector and a curved connection; compare its curvature to
the projector formula. Check K-=-K+ and a nonzero off-diagonal derivative:
projection is the low-energy adiabatic operator, not an exact finite-M
block diagonalization when DP is nonzero. On each fixed smooth boundary
the tangential scale may be separated from M; no singular global
heat-kernel/discrete-spectrum theorem is imported.

For compact spin surface Sigma, let S^2=canonical, deg S=g-1.
The twisted spin Dirac index is chi(S tensor L-)=deg L-=-k, per
flat scalar gauge component, or multiplied by its representation rank.
Derive this with Riemann--Roch; omitting S is a rejecting control.
On the connected genus-four, k=3 case, the degree-zero holomorphic
bundle may or may not be trivial: index does not fix its individual
kernel dimensions. Do not identify it with an Alexander character
without an actual map. Spherical O(d-1) controls must give index d.
For a disconnected surface sum the component indices; genus is not
needed for the total. Reversing the reference exchanges eigenline and
six-dimensional chirality; check the same net four-dimensional -k.

Compute the six-dimensional Weyl anomaly from the actual H weights,
not from copied coefficients: +[Ahat ch(R) ch(L-)]8=-R23 B8. Retain
all eigenline powers in general external families. In the product
zero-mode background only one internal K factor survives integration,
giving -k I6. Reversed reference must also give -k I6. Compare the
bulk/local/end descent with R23, including internally constant gauge
parameters. This matches the perturbative anomaly polynomial only;
no full boundary effective action, torsion phase or global anomaly
certificate is asserted. Bulk-edge mixed correlators are not computed.

## C. Localization is not charge decoupling

For the SAME minimally coupled gauge field with normalized constant
profile u0=1/sqrt(V), canonical fermion normalization gives g7 u0 T
on every normalized mode. This statement assumes finite positive V;
an infinite-volume product collar would instead remove that gauge
zero mode and change the question. It is not a compactification proof.

Compute a translated exponential profile with unit norm: its overlap
with a fixed core profile tends to zero while its coupling to the
constant gauge profile does not. Repeat in a half-cusp with measure
exp(-2s) ds and the compensating exp(s) fermion factor, including the
finite lower endpoint normalization and local escape probability.
This is a controlled example, NOT the actual R15 wall eigenfunction.
Include a localized gauge-profile contrast and the volume-divergence
contrast: the conclusion only applies to the unchanged constant mode.

Distinguish the isolated interface's net index from a complete finite
system: core/wall mixing can pair opposite modes and remove them,
which does not leave an unpaired chiral spectrum by itself. Interacting
boundary sectors, gauge breaking and source-derived inflow remain live.

## Custody and handoff

Save complete first native JSON, focused mathematical tests, an
explicitly enumerated broad regression, and reporting gates. The tree
is read-only while suites run. Update all current readers, scoped law
rows and next duties. Commit and push this audit branch regularly;
full main banking and independent verification are not asserted.
