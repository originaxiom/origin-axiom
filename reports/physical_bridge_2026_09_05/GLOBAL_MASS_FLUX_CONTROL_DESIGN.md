# R24 follow-on: exact connection equality, not expression-tree equality

2026-09-09. Source-informed after the first R24 run; before control
execution. Original d304e2b0 source and tests remain unchanged.

BANKED IDENTITY: R23's explicit U and natural form action; R24's
original mapped matrix. Its first native run completed, but reported
connection_matches=false. The first focused run preserves that one
new failing lock and six original R20 failures: 110 passed, 7 failed.

PRIOR ART: the original R24 design and its recorded matrices; the
algebraic-versus-structural distinction in symbolic matrix equality.
No new scientific sign, convention, domain or target is introduced.

P0: the same finite-dimensional bundle map for arbitrary real
connection one-form coefficients. Hypothesis: the original comparison
has an expanded left side and an unexpanded right side; their exact
entrywise difference vanishes. This hypothesis has not yet been run.
P6 expects a representation-normalization defect, not a failed spin
map. A nonzero residual is a scientific failure and will be retained.

Calculate the polynomial residual on ALL entries. Independently
construct the induced even-exterior connection by replacing each
coframe factor using A v=a cross v, rather than calling the original
creation/annihilation connection construction. Check its three basis
generators separately; by linearity this covers all one-form values.
Conjugate by the same original U, without fitting a new unitary.

Require agreement with Gamma tensor 1 + 1 tensor Gamma. The omitted
second factor, overall reversed sign and swapped connection components
must each leave a NONZERO polynomial residual. Verify the resulting
spin action on a completely general mass vector as well. Raw syntactic
inequality is preserved as a diagnostic, never mistaken for proof.

Seal this design, control source and new tests before importing them.
Record first native output and focused/expanded runs with the original
failing test still present. No original assertion is edited, silently
xfail-marked, or reported green. This control does not repair the old
R20 test interface, prove a global determinant or provide independent
banking review. The global physical duties of the original R24 remain.
