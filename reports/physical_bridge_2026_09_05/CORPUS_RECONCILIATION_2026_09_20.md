# Reuse the mathematics; test the physical join

2026-09-20. Owner-requested all-head/history reconciliation before
R39. Starting scientific head f59d0d6e; retrieval instrument/plan
checkpoint 12fe9ac7. This is prior-result recovery and selected
unchanged-producer verification, not new R39 science or a full TOE.
No shared B-number, merge or independent main-bank acceptance.

## Retrieval boundary

All seven advertised GitHub heads and tags were fetched. No external
seat advanced relative to the preceding R35 intake; five retained
tracking refs are not five new active seats. The supplemental reader
searches every reachable unique blob, including deleted CONTENTS,
and separately records current head trees, historical filenames and
the nonignored worktree. Its eight synthetic controls pass; the
existing absence instrument's five controls also pass.

At the cc0484ea partial checkpoint the historical run was in progress.
It subsequently FAILED during gzip export: Ruby attempted to transcode
compressed bytes through a text-mode file. The 3,585.98-second run has
no usable receipt; its empty output and failed capture are retained.
No completed-coverage or absence claim follows. A separate v2 reader
uses binary export, checks roundtrip/exclusive creation, and tests its
faster line finder against the old method on 76 fixtures. Its first
control invocation also failed on Ruby 2.6's absent filter_map; the
map/compact correction passes all 12 controls. No old reader is changed.
Working drafts and these outgoing reconciliation notes are NOT prior
independent discoveries. Ref trees are sampled at scan start; worktree
bytes are sampled at its end. The v2 rerun has not yet completed.

The older absence instrument's deleted lane checks filenames, not
deleted contents, and its CLI abbreviates the examples. The
already-banked tool searches frontier findings/verdicts rather than
the whole paper or every branch. Its single-term query cannot meet
its minimum two-term settled threshold; a longer query can miss a
settled result by dilution. Neither zero settled hits nor an atlas
card from epoch 1161 is evidence of absence.

This is literal/regex retrieval, not semantic proof of novelty or
proof that all physics work has been inspected. Compressed documents
are not decoded by the byte search. Ignored files, unavailable remote
history and unreachable objects are outside the declared population.
Primary-source reading is separately graded in the smoothness note.

## Producer-level reuse map

Pinned main below means 987c0c8f; outside means 13d2c5b6; review means
cf12bd6e; September16 means 2795e46c. Full pins, paths and digests
are in the custody receipt. Old local bodies are pinned to f59d0d6e.

| Prior | Checked content and scope | Disposition for this work |
|---|---|---|
| B574; main B1420 | SL2 algebraic representations are self-dual; scalar twists can break that pairing. The exact m010 example gives indices 0,+1,0 for the specified trivial/order-six/order-two twists. | REUSE. Do not present untwisted Sym3 pairing as a discovery. A positive-metric full-operator/domain map is a distinct duty. |
| B265/B274; later B576/B578 and L53 | Density/closure, finite-order checks and all-orders smoothness were repeatedly separated and recombined. L53's residual conflicts with the older all-orders record. | REUSE the principal smoothness result with the explicit sourced argument in the companion note; do not announce another missing-integrability wall. Broader density claims are not newly certified here. |
| Main B1418 | Nonsplit coefficient systems give genuine algebraic index asymmetry. The unchanged s958 producer reproduces +1 and semisimplification zero on three presentations, two conjugate characters each. | REUSE the positive. It lies outside the reductive domain D used by the older conjecture; a physical harmonic metric/domain is still owed. |
| Main B1427 | Its own Sym3 t12835 witness has magnitude two, outside the doublet's at-most-one count. Frozen exponents reproduce I=-2 exactly over Q(zeta12), with semisimplification I=0. | REUSE. Withdraw any proposed all-module at-most-one interpretation. Do not rerun an unfinished cross-seat census as if it were missing. |
| B1320--B1322 | Cusp determinants, fixed-locus Euler characteristics and a physical PW index are different quantities. Four high-precision E6 points with zero counts are not a theorem over all germs. | RELATED BUT DISTINCT. Preserve the computed zeros and the live non-SL2/nonsemisimple hatches; physical identification remains unearned. |
| B576/B582; B959/B1094 kill-graph entries | The first pair preserves complex-representation positives; the latter pair has explicit global-form/abelian-holonomy hypotheses and open hatches. | Do not turn their scoped findings into a universal parent/PW chirality kill. Old full closure producers were read selectively, not all rerun. |
| Main B1430/B1431 | The embedding correction and the class-index discriminator were already received in R35. A 3d supersymmetric index is not a 4d chiral matter count. | REUSE the reception boundary, not a new branch update or a physical chirality derivation. |
| Outside REQUIRE_AND_TEST memo 231 and THE_PAPER_ALREADY_HAD_IT memo 236 | Producers and the paper contain results not visible in a verdict-only query. Review F1 says SL2 self-duality is absent; the paper on BOTH its review pin and main contains it. | Correct the documentary absence, not the mathematics. No blanket endorsement or rejection of the review's other claims. |
| September16 xB029--xB031 | The acyclic threshold shortcut has narrower hypotheses than all hyperbolic threshold calculations. The generation bounds have finite coefficient populations; xB031 is explicitly in progress. | Retain the usable torsion machinery and unfinished duty. No physical constants, completed census or universal negative inferred. |

The exact index is I=n(V)-n(V*) with n=a1-r1, the kernel dimension
of degree-one restriction. The shortcut
I=t0-r1 is not interchangeable when the invariant dimensions
differ. In the reproduced t12835 case the tuples (a0,a1,t0,r1) are
(0,3,1,2) and (1,3,1,0), respectively. The old result survives its
weak stored-data lock: the new reuse check actually evaluates the
frozen witness through the unchanged B1418 exact engine.

## What was independently executed here

The three producer files were extracted byte-for-byte from pinned
main, without fixing or changing their algorithms. m010 and s958
were first rerun as written, then rerun with explicit assertions on
the resulting values because printed PASS alone need not set a
failure exit. The t12835 first witness was evaluated with its frozen
cyclotomic field and character exponents; the m010 control ran in
that same invocation. All four captured invocations exit zero.

This is independent execution of existing code, NOT an independent
implementation, an entire census, or a new physical vacuum. The
B1427 scripts contain literal placeholder paths and missing imports;
their stored-JSON tests are not a substitute for this computation.
Those portability/lock defects do not retract the mathematics.

The source audit restores the existing principal E6 smooth germ:
[smoothness, hypotheses and analytic limitation](SMOOTHNESS_RECONCILIATION_2026_09_20.md).
Its six deformation parameters must not be counted as six normalizable
particle modes. No quantum spectrum, coupling fit or gravity result
has been added by this reconciliation.

## Consequence for the approved strategy

1. Retain R19's conditional singular three/zero kernel and R29--R31's
   different resolved action/domain and light pairs. The smoothness
   theorem and nonsplit index witnesses neither close nor kill them.
2. Narrow R39 to the genuine join: full parent flatness and moment
   map, global positive metric, whole unbroken algebra and complete
   operator/domain. Reuse known SL2 pairing as a control. A central
   projection or a weight count is insufficient.
3. Treat any geometric Sym3 solution as a benchmark, not the desired
   chiral endpoint. A following non-SL2 deformation or source/end
   matching must pass the same physical gates. The existing t12835
   Sym3 twist is a concrete coefficient input to assess, not an
   already admissible parent background.
4. Only then compute the whole light-mode interaction and anomaly
   completion. A permitted quartic is not selective mirror removal;
   an algebraic index is not a four-dimensional spectrum.

The three PARENT_BACKGROUND files remain UNSEALED, UNEXECUTED drafts.
R38 remains this lane's latest original scientific probe. No source
choice, E8 selection, three physical families, Standard-Model values,
gravitational dynamics or completed TOE follows from this checkpoint.
The same previously recorded test/governance debts are not waived.

## Checkpoint custody and remaining retrieval

The [partial custody receipt](CORPUS_RECONCILIATION_PARTIAL_CUSTODY_2026_09_20.json)
checks 15 completed capture/exit records, 42 selected repository blobs,
three unchanged extracted producer files and four primary PDF/text
pairs. Full command/output copies, with only machine paths redacted,
are in CORPUS_RECONCILIATION_PARTIAL_RUNS_2026_09_20.json.gz. Original
local byte digests are retained separately from public-copy digests.
This receipt intentionally excludes the STILL RUNNING historical
scan; it is not mislabeled a completed all-history receipt.

An additional local audit/fork-2026-09-20 ref was observed during the
original read window, then pointing to the same 12fe9ac7 checkpoint.
On the later read it advanced independently to be669308, with a new
F01 nonsplit-admissibility result. Its unchanged producer and seven
tests now reproduce; the complete cutoff argument was read directly.
The [separate reception](FORK_F01_RECEPTION_2026_09_20.md) preserves
both its scoped obstruction and positive local cusp, without treating
the smaller subgroup's central source as a scalar in the full parent.
That fork was not created or edited by this work.
Only audit/physical-bridge-2026-09-05 is used for our commits/pushes.
The scan's start-ref snapshot and later worktree snapshot are not
claimed simultaneous; reporting drafts are excluded from prior evidence.

R38's unchanged custody check passes. First reporting gates: 26 PASS,
the exact same four FAIL details as the preceding R38 checkpoint;
review counter 188. No full-suite run or independent receiving-seat
review is claimed. Follow the final checkpoint checks separately.

Navigation/download failures were not read as scientific negatives:
a guessed paper-version URL returned 404 before the actual version
was obtained; publisher requests failed; one inventory diagnostic
first mishandled pathless commit records and was corrected; a locale-
sensitive external hash command failed before Ruby hashing was used.
The producer math was not modified to repair any of those failures.
An index-formula typo in this outgoing draft was caught against the
producer before commit: n is a1-r1, as the published text now states.
