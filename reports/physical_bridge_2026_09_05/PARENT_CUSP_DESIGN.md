# R45: the actual parent roster at the canonical cusp

September 25, 2026. Author: originaxiom. Pre-execution design.
Commit, push and server-confirm every scientific path before execution.

P0: this computes over R44's positive real projective family q>0,
q!=1, its four determinant-one scalar twists, and its specified
SU4-in-E8 parent. It is not a class-wide physical vacuum classification.
The whole parent roster is used, not only the paired spinor coefficients.

P6: expect the connected unbroken gauge ALGEBRA to be so(10), with
normalizable gauge zero modes in the supplied classical parent action.
Expect nonzero longitudinal weights to inherit R44's end contraction,
but zero-weight sectors NOT to inherit that contraction. A nonzero
peripheral cohomology is not a failed background or a chirality no-go.

Inputs: R39's actual E8 branching and Lie maps, R42's harmonic-flat
background, R44's global cusp/norm/domain argument. Ballas--Long's
published density theorem is an external input to the all-q statement,
not a theorem certified by the finite samples here. Central twists
are exactly chi in {1,-1,i,-i}; an arbitrary unitary scalar would not
be an SL4 coefficient and is not silently admitted to this parent.

Conventions: D=diag(1,-3,1,1), N=E02+E23, P=N^2, J=(5,-3,1,-3)/8,
R44 frame F, connection d+B. Gauge factor first in the branching.
The adjoint basis is ordered off-diagonal units then Eii-E33, i=0,1,2.
Peripheral cohomology is complex cohomology, NOT global particle counts.
Gauge zero modes are the compact-real kernel of the positive quadratic
form ||d_A s||^2+||[Psi,s]||^2 on the natural complete-space closure.

MB12 controls, all fixed before execution:

1. Reuse the complete R39 weight map and its wrong-bar control. Check
   every longitudinal weight against the entire 248-root roster.
   Derive all representation matrices, not dimension-only matching.
2. Check full sl4 invariant kernels on 4, dual4, exterior-square4,
   End0(4), and exact common holonomy kernels at q=2 and 1/2.
   The old Sym3 wedge singlet must be recovered as a DIFFERENT-input
   positive control. Compare meridian nilpotency with that Sym3 input.
3. Carry the full radial flat connection into every coefficient.
   On each nonzero longitudinal-weight summand check both sides of
   the finite nilpotent Fourier inverse. Zero weights must be refused.
4. On the adjoint zero-weight summand check the actual two-variable
   Koszul complex and its exponential/holonomy version. Derive the
   ranks, not only Euler characteristic. Verify the explicit common
   invariants and the singular longitude operator.
5. Verify actual radially transported cusp-parallel sections and their
   norm power counts; compare an integrable longitude one-form and
   nonintegrable meridian one-form for the trivial coefficient. These
   are local end controls, not a global H1 census.
6. Keep trace ratio, all five parent summands and finite-volume cutoff
   domain assumptions explicit. No generic scalar twist in SL4,
   universal compact resolvent, full neutral H1 or net chirality claim.

Execute native producer, dedicated tests, then the fixed R39/R44/R45
three-file regression. This population does not include R42's known
Ricci-comparison failure; it is not a repair of that failure or a
full-suite certificate. Retain every first output and nonzero exit.
Corrections require separately sealed files, not edits to executed science.

Outcomes: PARENT/END SPLIT if the exact controls support the authored
argument; CONDITIONAL if a global analytic input remains unpaid beyond
the already declared inputs; CONTROL FAILURE if any expected algebra
fails. No outcome derives the parent, four-dimensional spacetime,
quantum consistency, parameter selection, interactions or a TOE.
