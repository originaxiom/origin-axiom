# R36 pre-execution design: charged normalized overlaps with their end term

2026-09-19. Own starting pin febedec014e45cb384627fdb6bf1e830e001113d.
Path-local PB-BOUNDARY / PB-ACTION, no shared B allocation. Commit,
push and verify the remote seal before the first scientific execution.
The already committed working derivation is disclosed prior, not a
blind forecast or a scientific run. No empirical target enters.

P0: quantify over the explicitly ADDED R30/R33 compact positive-norm
coupling model, its scalar/one-form positive eigenpairs and smooth
charged scalar coefficients, plus the two named flat comparators and
the nonzero-field Robin control. Not all source completions, all
members, the complete cusp, all fermion form degrees, or quantum phases.
P1: PB-BOUNDARY/PB-ACTION's source-derived coupling duty remains open.
R19's singular kernel, R31's pairs and R34's allowed quartic are retained.
P2: the atlas card was read; its generic output is not an absence claim.
P3: all 13 settled-threshold bodies from the four-term query are now
read, along with B1036/B1039 and selected relevant producer bodies.
The prior record specifies exactly which incoming conclusions are NOT
used. Papers and claim lines were also searched; no novelty claim.
P4: the B960 and B1086 hatches do not rule out this added H-representation
coupling with a source/end domain. Their scope is not broadened here.
P5: seal this design, proof, prior, input inventory, producer and tests.
P6: expect the candidate identity with its boundary term to hold, and
both comparators to confirm their already derived results. Expect NO
physical mirror-gap or profile-selection result from this experiment.
These are exact consistency/discrimination tests, not predictions of
a previously unseen physical number or an empirical fit.

## Required checks and bite

- Off-shell covariant product and Green identities for arbitrary
  functions, nonconstant volume density, arbitrary connection and
  both charge pairs. Full variable gauge covariance, not just phases
  constant in space. Omitting the drift must fail.
- Direct normalized scalar and fermion integrals on the interval,
  exact eigen-equations and true boundary data. Removing the end
  term must fail even though fermion boundary data hold.
- Large-t ratio AND absolute overlap limits AND scalar gradient cost.
  The constant-profile limit reproduces R25/R30's normalization prior.
- Nonzero Witten field and Robin, not undeformed Neumann, control.
- Direct complex Fourier matrix integrals on the nontrivial flat
  line, positive and negative momentum, invariant singular-value
  polynomial and basis-change control; square holonomy nontrivial.
- Complex bilinear null at positive Hermitian norm as a tensor-type
  discriminator. Exact zero eigenvalues rejected before division.
- Operand exactness checked BEFORE subtraction; a changed rational
  expression and matching floats must not silently pass.

No finite control proves the general manifold/domain argument; that
is the authored proof with stated hypotheses and independent review
still owed. No R31 trial is passed off as an exact eigenfunction.
No prescribed comparator scalar is passed off as a stationary source.

## Execution and custody

First run the native producer under exclusive raw capture, then the
whole new test file. Inspect both outputs before the focused run.
If a science assertion fails, retain source/tests/output unchanged;
any correction needs a separately sealed successor, never an in-place
edit of the failed test or a changed tolerance. A focused run may
proceed with an observed failure only if it is explicitly disclosed.

The focused population is the unchanged nine-file R34 final population
plus tests/test_physical_bridge_yukawa_overlap.py, frozen in the input
inventory. Its four previously failed IDs must remain visible.
The old halted R33 control remains outside that population; do not
claim full-suite green or independent banking from this selected run.
The worktree stays read-only for the entire execution envelope.

Capture stdout, exit status and hash; preserve minimally redacted
public copies and the exact file population. Afterward update the
reader surfaces, artifact inventory and governance receipts, then
commit and push this branch only. The full physical-theory goal is
not complete on either outcome of these overlap tests.
