# R40: preserve the actual nonsplit coefficient in a candidate parent

2026-09-20. Pre-execution, path-local R40; no shared B allocation.
Seal this design, proof, prior, input inventory, producer and test file;
commit, push and confirm the remote pin before first import or execution.
No observed constants or empirical predicates enter this design.

P0: this computes the marked R27 m010 coefficient and all sectors of ONE
specified A4 x A4 embedding in the supplied E8 candidate parent. It also
exhausts the scalar determinant repairs of that coefficient. It does not
classify the arithmetic class, all parent embeddings, physical metrics,
source completions, or possible theories of everything.

P1--P4: continue COMMON_MODEL_AUDIT_2026_09_20.md section 8's origin gate,
not a new coefficient census. Reuse the R27 exact Fox/restriction engine
and R38 complete E8 roots. Keep R19/R29's sourced construction and the
F01--F04 conditional analytic restrictions. The freshly read F05 geometric
Wilson/gap result is a different coefficient, not a nonsplit exclusion.
Prior reading, retrieval limits and credits are recorded in the prior.

P6: expect the rank-four witness to fail literal SL4 membership, but to
embed faithfully as an unchanged summand of rank five. Expect index +1
to survive there, and scalar determinant repairs to have zero index.
The exterior-square and singlet indices and formal anomaly sums are NOT
known before this execution. Either an anomaly-free or anomalous formal
roster is bankable. Neither outcome is itself a physical spectrum.

## Fixed conventions and controls

1. Work exactly over Q(zeta_12), u=zeta_12^2, i=zeta_12^3. Use R27's
   marked presentation aabaBaaBab, mu=AbAA, lambda=babA, left-to-right
   multiplication and uppercase inverse. Preserve its two generators
   and the explicit simultaneous triangularization, not just eigenvalues.
   V=chi Sym^3(rho), chi(a)=u, chi(b)=-1. Verify det(V)=chi^4 is nontrivial.
2. Scalar repairs delta Sym^3(rho) with delta^4=1 exhaust exactly eight
   cases: delta(a)=+/-1, delta(b)=1,i,-1,-i. Compute their full paired
   restriction data. Torus-acyclic and cusp-trivial cases are distinguished.
   Semisimplification is a different representation, used only as a control.
3. W=V direct-sum L, L=(det V)^-1. Verify SL5 membership, every relator,
   all marked peripheral maps, retention of the original nonsplit block,
   and the failure of the wrong determinant line. Faithfulness is the
   explicit GL4 map X -> diag(X,det(X)^-1), not a dimension coincidence.
4. In R38's eight-coordinate E8 roots take gauge A4 simple roots
   e1-e2,e2-e3,e3-e4,e4-e5; structure A4 simple roots
   e7-e8,e6-e7,e7+e8,-(e1+...+e8)/2. Check Cartans, orthogonality,
   root-lattice index, and ALL 248 joint weights including Cartans.
   In these conventions expect (24,1)+(1,24)+(10,5)+(bar10,bar5)
   +(5,bar10)+(bar5,10). Wrong conjugations must fail the weight test.
   Exhaust all 25 center pairs; check kernel (zeta,zeta^-2), order five,
   and trivial intersection with either separate factor.
5. Compute paired ordinary/interior cohomology for 1, V, L, W,
   exterior-square V, V tensor L, exterior-square W, V tensor L^-1,
   End0(V), End0(W), plus their duals in the same algorithm. Preserve
   H0,H1,H2, torus H0/H1, restriction rank and interior H1 separately.
   Check direct-sum identities and an explicit SL2 Clebsch--Gordan map
   exterior-square Sym3 = Sym4 + 1 on the ACTUAL two generators.
6. Check symbolic SU5 quadratic/cubic trace normalizations. Report the
   SU5^3, SU5^2 U1, gravity^2 U1 and U1^3 anomaly expressions IF the
   algebraic indices were left-Weyl multiplicities. U1 generator is
   diag(1,1,1,1,-4). Do not assert that this centralizer is the complete
   physical gauge group or that interior cohomology counts L2 modes.
   A 10+bar5 control cancels SU5^3; 10+5 does not. Both must be detected.
7. Exhibit the positive-metric map H4 -> diag(H4,(det H4)^-1) and full
   connection/current map X -> diag(X,-tr X). Verify the trace formula
   and E8/defining-five factor on the whole symbolic Cartan. This
   transports a specified sourced equation; it does not supply a source
   action, a harmonic metric, or its physical domain by itself.

Run native, new tests, then the prior thirteen-file R39 population plus
R27's reductivity tests and this new file. Never erase a failed first run
or change a sealed expectation retrospectively. Any correction is a new
named, sealed artifact. Keep the four known prior test failures visible.
Local research preservation is not full-suite/main-bank certification.

## Physical decision and next duty

The positive outcome earns an actual coefficient-to-parent map with its
whole accompanying algebraic roster, not chiral particles. A formal
anomaly would diagnose this particular interior-index dictionary, not
kill sourced/end/inflow alternatives. A zero anomaly would not establish
normalizability or a vacuum. Next construct a compatible global positive
background and source/end action, then derive its physical operator and
whole light spectrum. No replacement of the physical goal by group theory.
