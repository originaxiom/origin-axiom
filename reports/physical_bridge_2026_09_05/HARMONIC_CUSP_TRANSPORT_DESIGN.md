# R13 post-result transport controls — sealed before these controls execute

The original seal is 9c646408. Its six fits passed the declared numerical
criterion. The first terminal capture truncated 28,226 output tokens at
24,000; the RECEIVED truncated text is preserved, not called a complete
first transcript. The unchanged source was rerun with a larger capture
limit; HARMONIC_CUSP_RUN.txt and harmonic_cusp_results.json retain that
complete repeat (4.789488520997111 seconds, versus first run 5.813778405979974).
Twenty R12/R13 locks pass. These are NOT full-suite green or TOE certificates.

This supplement is motivated AFTER seeing the fit, not retroactive
preregistration. The earlier held-out equations still use the same
height-ascent mechanism. Test the field directly under the four literal
Riley generators and inverses, with no pullback in the verification.
This is a same-author computational control, not independent receiving-seat
verification and not a rigorous bound on the infinite Fourier tail.

P0: the cutoff-10, height-.65, seed-1304 numerical harmonic candidate
in the ORIGINAL declared unit-meridian-period asymptotic class.
P6: direct generator transport and 1-form pullback should pass. If they
fail, retain the old output but withdraw its interpretation as an
adequately tested global harmonic candidate pending a separately sealed
repair. Do not change the old source, criteria, coefficient or cutoff.

1. Read the committed machine output, not refit to new targets. For
   each a,b,A,B generate 3,000 points using independent seeds 2310--2313,
   horizontal box [-2,2] x [-2,2], heights [.6,.9]. Retain points for
   which BOTH the original and generator image heights are >= .55.
   This cut avoids testing a cutoff-10 series outside its resolved range;
   report retained counts (at least 25 per generator required). Verify
   max |f(gP)-f(P)-ab(g)| < 2e-9 on ALL retained points. This is not
   a universal automorphy or global truncation certificate.
2. Compute df analytically from the actual Bessel/trigonometric series.
   On 16 retained points per generator compare (Dg)^T df(gP) with df(P).
   Dg is an independent centered finite difference at steps 2e-6 and 1e-6.
   Require max component residual < 2e-6 at both steps. Compare analytic
   df with direct finite differences of f at three independent points.
3. A deliberately corrupted leading coefficient (multiply by 1.01) must
   violate direct generator transport (>1e-4 on the same points); simply
   satisfying the local BPS equation must not pass as global automorphy.
4. At z=1,2 compute the Fourier-series meridian period by periodic
   trapezoidal quadrature with 128 nodes, above twice the bandwidth.
   Require |period-1| < 1e-11. Also compute the separate tangential and
   normal RMS components; do not discard the tangent field because the
   radial zero set was the old index probe.

No new physical input is selected, no masses are predicted and no
chirality count is inferred from these transport checks. They strengthen
the computed geometric Higgs INPUT that the physical operator must use.
