# R11 — the founding ratio's actual family action

STATUS 2026-09-06: sealed design before this instrument's first execution.

BANKED IDENTITY: B1138 already exhibits the E8 branching; R4--R10 are
conditional physical-model calculations, not a source-derived TOE. This
cell addresses PB-ACTION's explicit-map requirement, not another fit of
that model. Existing successes, sealed files and failures are untouched.

PRIOR ART: fetched main 87a2eb3d (B1275), SM-derivation seat 945e091d
(its renumbered B1270/B1271), physics seat 5788e1fd (R63). Source bodies,
code, tests and corrective addenda read, including the generation-index
correction. The regex `subregular|icosian|family.rotation|source.to.action`
is PRESENT on all ten enumerated heads, zero matching deleted PATHS
(not a search of deleted content). Already-banked `icosian family rotation`
returns 128 hits, two settled two-term matches: B1011 and B949, both read.
B1011 is the hearing representation's McKay tensor, not this root-space
action. B949 is an atlas/axiom audit. The atlas card, identification audit,
law/lead/kill-graph surfaces and grounding documents were consulted. The
local ledger's 24-row count predates newer upstream rows; no merge or
whole-upstream certification is implied. No universal absence claim.

P0: over the explicit icosian E8 lattice and all its twenty order-three
units, compare quaternion left multiplication with a family-A2 Weyl
rotation. This is conditional on this two-face construction, its mod-5
map and metric. It is not a theorem over every programme relation or a
choice of physical compactification. No empirical parameters enter.

## Question, conventions and two-sided outcomes

Use exact rational coordinates `(x0,y0,...,x3,y3)` for quaternion entries
`xj+yj*sqrt(5)`. The root-normalized metric is twice the `x+y` functional
on the quaternion norm. Generate the 120 units explicitly and the 240
roots as units plus `(phi-1)*units`. Check full multiplication closure,
norms, root reflections, a genuine integer lattice basis and determinant;
never truncate rational coordinates during integerization. A scale-two
integerization is a deliberate rejected control if denominators require
four. An order-four unit is the non-A2 complement control.

Construct an isomorphism from the *marked* R,L generators of SL(2,F5) to
unit icosians by exhaustive generator-pair search. Verify all 14,400
products, not only a count or the generator orders. Map `g=-R L^-1` through
that actual isomorphism. The coordinate isomorphism is a convention: test
all twenty possible order-three images, inverses, and conjugation by all
120 units, rather than naming the first coordinates canonical.

For the plane A=span_Q{1,g}, let E=A-perp and Lg(x)=g*x. Compare:

* Lg, the incoming source seat's literal action;
* Wg=s_1 s_g, with s_a(x)=x-B(x,a)*a for norm-two roots;
* Ug=Lg Wg^-1, a possible compensating E6 action.

Record exact characteristic polynomials, fixed dimensions on the real
root span (NOT invariant dimensions of the full Lie algebra), root and
six-class permutations. If Lg=Wg, bank that equality. If only their class
permutations agree, bank the distinction and compute whether a canonical
factorization survives: Wg must fix every E6 root, Ug must fix A pointwise,
they must commute, and Ug must have an explicitly verified E6 reflection
word. A failed factorization remains a failure, not a universal no-go.

Independently reconstruct Wg as I+(Lg-I)P_A with the metric projector P_A.
This makes uniqueness precise: prescribed action on A and identity on its
orthogonal complement. Check covariance under quaternion conjugations and
g -> g^-1. These tests distinguish coordinate freedom from a different
map. Compute every E6/A2 mixed root sum to justify the commuting Lie
subalgebras; exhibit an order-three SU(3) Weyl representative. A root
permutation alone does not fix all lift phases. No full E8 adjoint lift
of Lg, gauge-boson spectrum or holonomy is inferred from its fixed Cartan.

Controls must reject: a corrupted root set, non-integral coordinates,
an invalid marked generator assignment, and Lg mislabelled as an E6-fixing
family action. The genuine Wg must pass the same family-action predicate.
Compute E6 Cartan type and the six 27-weight classes from definitions;
do not count a printed label as proof.

## Physical interpretation and limits

In the twisted 7d SYM construction, matter multiplicities come from the
appropriate coefficient-bundle zero modes, not the fiber dimension:
Braun et al., arXiv:1812.06072, equations (2.40)--(2.49), checked in the
primary paper. Boundary/normalizability/background assumptions remain
necessary on a cusp. Pantev--Wijnholt arXiv:0905.1968 section 3.8 locates
couplings in zero-mode products/overlaps and emphasizes kinetic
normalization. Neither paper identifies this repo's lattice with its
required physical geometry. Their statements are not executed hypotheses
of the finite root computation. Wilson's E8 notes section 3.6 provide
independent icosian context; all algebra claimed here is recomputed.

Even a successful Wg supplies an algebraic map, not three chiral physical
families, a preferred compactification, the R7 action, observed flavor,
four-dimensional gravity or a TOE. Conversely Lg != Wg must not kill the
branching or the derived family component. B1274's selector and R63's
SL2 comparison are read leads, not certified by this cell.

Seal design/source/tests and their SHA256 hashes with a local commit before
execution. Run quiescent; serialize fully before exclusive output open;
refuse overwrite; retain first failures. New repairs require new files
and a new seal. Register each surviving sub-lemma in the law map and the
physical continuation in PB-ACTION. No remote writes.
