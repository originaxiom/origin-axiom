# R25 final all-head intake: preserve the capabilities and their grade

2026-09-09, after the scientific runs. Refetching all branches/tags
advances outside from 8b32fb4a to
7ce09cba807012b876409e94a2d4ba177adaee8b. Other remote heads are
unchanged: main b94ed03a, SM 1703c0d8, physics 659487bb. No merge.
The immutable pre-execution sweep remains pinned to its original tree.

Five new first-parent commits and their changed paths were inspected:
59eeb97f, 51f8b002, c1aeb755, ccf30e6f, 7ce09cba. The complete new
memo-193 and memo-188 addenda and both changed instrument functions
were read. Their results are RECEIVED unless explicitly checked below.

## A useful independent check, and a new tool capability

Memo 193 addendum 1, THE_GUARD_THAT_CAUSED_THE_DAMAGE.md, examines
our 833b939b tree. It reports 7 failed/21 passed, diagnoses exactly
our retained R24 structural matrix comparison and the six R20 calls
stopping at a missing combinatorial bridge. It independently obtains
zero for the R24 matrix residual and acknowledges our already passing
separate exact control. This agrees with our own existing computations;
it is NOT independent assessment of R24's total-flux proof or R25.

It also reports that Q.is_isometric_to(K, return_isometries=True)
returns the interface consumed by our iso_data(), while
Q.isomorphisms_to(K) is empty. A fallback makes its copy of the
original ten R20 tests pass. That modified run is not reproduced here
or inserted into the original sealed source. Its numerical geometric
bridge is a registered CAPABILITY to test, not a new interval certificate.

The actual R20 source was reread: iso_data consumes cusp_images and
cusp_maps; geometry conjugates the canonical action by that bridge.
Our existing word-control producer was also reread: exact presentation
automorphisms already saturate the certified canonical upper count and
give the full original H1 action. The new fallback should be checked
against those exact action sets and both cusp equations, and its own
geometric certification boundary stated, before being used as another
control. It does not fill an absent character-locus theorem: that
theorem is already established by our separate word/canonical route.

## Other changes are received at their stated scope

- The B1137 commit adds 352 null-grid lines and reports a byte-identical
  final report after recomputing both grids. That long computation was
  not rerun here; this is not a new certificate from our bench.
- instrument_freshness now handles a non-dictionary JSON top level
  before dictionary-field access. The changed source was read; its
  complete sweep was not rerun or copied into our branch.
- Memo 188 withdraws a stale-checkout claim of a silent progress log,
  alongside its earlier withdrawn depends_on claim. We directly checked
  main's September-dated headings, including September 9, against the
  older fba45fc2 tree. This supports rejecting the alleged ten-day
  silence. No exact daily-entry census, first-commit-date census or
  remaining throughput statistic is independently certified here.
  The changed throughput source now prints its tree and computes its
  dated conclusion from the extracted log instead of frozen prose.

The receipts are not silently merged, the older failed evidence is
not rewritten, and the new numerical fallback does not change R25's
own science or certification status. Its follow-up is registered in
OPEN_LEADS; this intake is not a transmitted relay or a B allocation.
