# Partial-filling receive audit — before execution, 2026-09-07

## Owner clarification BEFORE seal/execution — operative scope

The owner withdraws the loop counts and degree-seven rational values as
load-bearing evidence. The earlier draft below is retained as an
UNEXECUTED disposition, not the test being run. Its loop is renamed
legacy_unexecuted_grid and is NOT called. The incoming source is only
preserved; no 18/22 reproduction or B432 rerun is executed in this round.

The operative input is the supplied degree-five cover signature
`kLLLPLQkcefegijjiijiieldllxtxa_aBbBabBbbacb`, cusp zero filled at (2,1).
Prove cover provenance by an explicit combinatorial isomorphism from a
degree-five m004 cover, not equal volume. Compute the numerical witness,
then certify hyperbolicity on the partial presentation and the permanently
filled triangulation. The latter has two entirely complete cusps, making
SnapPy's verified complex-volume algorithm applicable.

SageMath 10.7/SnapPy 3.3.2 are available locally. The added interval
producer imports sage.all before snappy; at 100 and 160 bits it checks
the witness, its orientation reverse, its unfilled cover, m004 and 5_2.
Normalize the certified imaginary complex-volume interval by interval
2*pi^2. The verified algorithm has normalized period 1/4 (ordinary CS
has period 1/2). Excluding all multiples of 1/4 therefore excludes both
mirror-compatible classes, which is the required result. Use only exact
rationals and interval comparisons in the decision, not decimal floats.
Check positive certified distance (expected >0.09), opposite CS and
equal volume on reversal, precision compatibility/shrinking interval,
and synthetic zero/quarter/1-over-24 controls. 'Interval meets zero'
does not prove an exact zero; the controls are not oversold.

P0 and P6 are now the named-witness question: expect certification to
preserve the positive existence claim; no grid-count or rational-value
claim. No non-geometric declaration follows from a failed triangulation.
Nonzero CS changes a geometric saddle's k dependence, NOT automatically
a quantum boundary sector or a physical chiral spectrum. Likewise zero
on-shell CS does not delete the CS functional for other connections.
The scoped model/physical-selection caveats of the original draft stand.

## Original draft, dispositioned above before any scientific execution

The owner asks to verify the outside-bench partial-cover-filling proposal.
This temporarily takes priority over executing R16's now-sealed instrument
controls; that ongoing singular-source path is preserved, not abandoned.

BANKED IDENTITY: B432's initialized-CS filling mechanism, B1064's named
route (a), and B1227's distinction between a vanishing invariant and
amphichirality. Original B432 source and its transcript-only tests are read.
PRIOR ART: receive outside-bench commit 7670a010, certificate
outside_bench/certificates/partial_filling_joint.py (46 lines, copied
unchanged as partial_filling_incoming.py), its full output and memo 170.
No first-in-history claim follows from four zero phrase counts. The
all-head phrase sweep finds an unrelated 'partially filled' matrix entry
in B738 and the new outside-bench files. Main's exact four phrases do
not locate a prior topological construction. This is lexical evidence,
not an exhaustive search of every implementation of partial surgery.

P0: reproduce the finite degree-4-through-8 cover/slope instrument, then
check its selected witnesses, especially degree seven, as cusped
hyperbolic geometry. No claim about all covers, all fillings, physical
fermion chirality, a rational boundary CFT or a complete TOE.
P6 prior: cusp-preserving nonzero-CS examples are plausible; the count
18/22 is an instrument sample, and exact 1/24 needs more than a decimal.
Keep the possibility of a non-geometric or non-exhaustive sample visible.

The exact incoming producer stops at its FIRST usable slope on each cover:
(1,0),(1,1),(2,1),(3,1),(1,2), cusp index zero. Its filter rejects text
containing 'degenerate' or 'not', but allows flat/negative tetrahedra;
exceptions and unknown CS are skipped. Thus 22 is not an exhaustive
denominator and its rows must be assigned stable cover/basis identifiers.
The literal run will be preserved without changing this algorithm.

Independent receipt: enumerate the same covers, record all five attempted
slopes with errors and solution types, and retain the same first-accepted
selection. Initialize CS on each unfilled cover before changing it and
check its CS modulo 1/2. Record orientation-preserving decorated isosigs
with cusp ordering/peripheral curves, cover degree and volume. These
identify the actual (cover,cusp,slope) inputs rather than a floating label.
Classify reflection obstruction by 2*CS != 0 modulo 1/2; CS != 0 alone
is insufficient, since CS=1/4 is a two-torsion control.

For each selected case: high-precision recomputation; permanently fill
only cusp zero and simplify; retain actual remaining cusps and solution
type; record a symmetry-group amphichirality check if available. Do not
discard a negative tetrahedron triangulation as proof of non-hyperbolicity.
For the degree-seven +/-1/24 candidates additionally reconstruct the
oriented decorated input, compare the filling again, reverse orientation,
and identify the resulting cusped manifold in the census if possible.
Decimal agreement at 1e-30 is high-precision evidence, not an exact
rational CS proof or a rigorous interval hyperbolicity certificate.

B432 control: reproduce p=0..7,q=1..7 primitive slopes with the SAME
initialized cusped CS and volume>0.3 filter, but use the correct CS
period and report solution types rather than assuming the filter is a
hyperbolicity proof. No old JSON is overwritten. Known m004 and 5_2
check zero/nonzero CS and opposite symmetry behavior.

Primary documentation checked 2026-09-07:
https://snappy.computop.org/manifold.html (CS divided by 2*pi^2;
complex volume modulo i*pi^2; covers; partial dehn_fill; the remaining
cusps count; decorated oriented signatures and high precision).
https://snappy.computop.org/verify.html (Sage/interval verification and
the extra modulo-2-torsion limitation). No high-precision float is called
interval-verified. Neumann, https://msp.org/gt/2004/8-1/gt-v8-n1-p10-p.pdf,
places the invariant in its modular value group, not an integer level.

Two-sided controls: reproduce or explicitly fail the claimed 18/22;
recover at least one geometric cusp-preserving witness; test zero and
quarter-period mirror-compatible controls versus +/-1/24; retain both
charge/orientation signs, all skipped attempts and uncertain geometry;
compare initialized B432's count to 31. A selected geometric saddle with
CS nonzero changes its on-shell k dependence but is NOT automatically
a derivation of a quantized boundary sector, rational E6 level one,
fixed sigma, or physical chiral fermions. Cover/cusp/slope selection and
the post-filling arithmetic/gauge/family transport must still be earned.

Hash this design, incoming source, independent source and tests, and
commit before first execution. No B allocation, main edit, source merge,
remote message or publication acceptance is implied by this local audit.
