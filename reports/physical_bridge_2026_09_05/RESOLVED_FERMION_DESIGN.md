# R30: the fermion domain through finite-width cores

Pre-execution design, 2026-09-15. Path-local continuation of R29;
no B allocation, empirical target, or complete-theory claim.

BANKED IDENTITY: R19's actual Fox complex computes the relative source
groups; R23 identifies its odd/left and even/right mass operator; R26
preserves the index on its FIXED singular domain. R29 resolves bosonic
cores but explicitly does not transfer that domain. The core-gluing
mapping cone below reuses R19, not a newly discovered cohomology engine.

PRIOR ART: [intake](RESOLVED_FERMION_PRIOR.md), R18/R19/R23/R25/R26/R29.
The analytic candidate is [the argument](RESOLVED_FERMION_PROOF.md).

## P0--P6

P0: test the declared quadratic H fermion extension on fixed compact,
connected, smooth three-dimensional truncations, with resolved proper
source arcs, an extending flat unitary coefficient, exact bounded
mass one-form, and absolute outer data. A separate light-mode argument
requires the explicitly stated uniform logarithmic well bounds. This
is not all defects, the full parent action, or the complete cusp limit.
The exact m202 character complex is one application, not a manifold-free
physical selection theorem. Cochain Euclidean spectra are algebraic
controls, not discretized PDE masses.

P1--P4: this continues PB-BOUNDARY/X33 at its R29 next duty; the owner's
common-theory task is the reason for this ordering. The ladder, campaign,
working rules, banking protocol, current framework/lead entries, actual
prior producers and relevant kill-graph hatches were read. The all-head,
all-reachable-blob retrieval and its limits are in the intake. Absolute
boundary data and Witten conjugacy are standard mathematics, not novelty.

P6: expect zero NET compact index for this smooth extending class, and
light opposite-chirality partners in its strong shrinking wells. A
failed algebraic/domain/normalization check withholds the affected claim;
it does not kill the original singular construction. No expectation is
an executed result.

## Model and conventions fixed before execution

Use R29's H subgroup and charged representative R=16_(1), with the
R23 charged-pair reality convention: its conjugate is not another
independent determinant. Fermion left profiles are odd forms; right
profiles are even. Keep positive ordinary bulk L2 kinetic norm and
the minimally coupled internal operator

    d_q = d_A + q dF_epsilon wedge,
    D_q = d_q,max + d_q,max*.

At the actual compact boundary choose the absolute realization
iota_n psi=0. Across artificial core interfaces match the whole form,
not just its tangential trace. The adjoint and D_q squared boundary
conditions are derived in the proof. Metric, kinetic coefficient,
source profiles, widths, fields and this outer condition are inputs.
No supersymmetry or absent additional Yukawa term is inferred.

The fermion action is quadratic with zero classical fermion background.
Its gauge/h variations have bilinear currents, not fixed external
sources. It therefore leaves R29's zero-fermion stationary bosonic
solution intact in THIS added model, but supplies no quantum vacuum.

Seal this design, proof, producer and tests, commit and push before
their first scientific import or execution. Preserve every first run.
No old science, test assertion or tolerance is edited.

## A. Operator and domain controls

Use the original exterior matrices from R23 and the differential engine
from R18. Check Hermiticity, oddness, exact weighted conjugacy in all
eight form components and the nonzero wrong-sign control. Conjugacy
is not an unweighted isometry or isospectrality.

For each coordinate normal check the absolute half-dimensional
projector is isotropic for the Green matrix and that its annihilator
has exactly the same dimension. Show the full-transmission graph
cancels the two opposite normal Green forms; matching only tangential
data must fail. Check the scalar Robin condition and local variational
current matrices, including a nonzero rejecting field.

## B. Actual relative-to-resolved complex

From R19 take v=(x-1,y-1)^t and its independently Fox-derived row f.
For k contractible components and unitary transports r form

    d0(t) = [[v,0],[r,t I_k]],  d1=[f,0].

At t=0 this is the relative complex plus k even core states; t!=0
contracts those k pairs back to the base complex. Exhibit the exact
chain homotopy, not only its Euler number. The algebraic t is NOT a
physical width, tunneling coefficient or mass.

Controls: k=0,1,2,3,4; trivial character, (-1,1), (i,1), the C3 pair,
and both exact diagonal Alexander roots (-3 +/- i sqrt(7))/4.
Check t=0,1,1/2, phase transports (1,i,-1) at k=3, all-degree
Laplacian ranks, and a rank-deficient attachment. Use two independent
methods: matrix ranks/contraction and R19 base/relative groups.
For generic k=3, positive spectra at t=.1,.3,1 must agree in the
even and odd blocks to absolute 2e-9; do not infer geometry from them.

## C. Light-mode variational mechanism

Derive F_epsilon by integrating R29's actual hyperbolic radial h,
including its continuous core join. Its scalar local zero solution
is exp(-qF), not exp(+qF). Derive the transverse tail norm primitive
with t=tanh(r): integral t^(1-2a)/(1-t^2)^2 dt, a=q beta.
Check exact primitives at a=1,2, and independent quadrature at
epsilon=.01,.003, R=.2 with relative tolerance 3e-10.

The all-width statement is the proof's min-max argument, not these
samples. State its uniform log-bound hypotheses, the whole-proper-arc
cutoff needed to avoid axial leakage, and the constants' dependence
on the fixed truncation. Threshold and sign controls include
a=0,1/2,1,2 and opposite q. A logarithmic flat comparator must reject
a missing transverse measure. An axial-cutoff comparator must retain
its nonvanishing energy/norm ratio. Do not claim exactly k light
levels, eigenfunction convergence, or interchange cusp/width limits.

## D. Normalized currents and disposition

For a retained unbroken constant D5 gauge zero mode, check normalized
fermion overlap equals g7/sqrt(Vol) independently of its concentration.
This is the R25 normalization law applied to this extension, not a new
selection theorem. Do not apply it to R29's nonconstant massive-U1
profile. Record left/right perturbative anomaly cancellation of each
paired R mode, not a complete seven-dimensional determinant.

Run native checks, new tests, focused R18/R19/R23/R26/R29 tests, then
exactly R29's 51-file broad population plus this new test. Read-only
tree during science. Compare all failure/error IDs to R29. Run reporting
gates without waivers. Independent proof review, full repository green,
main banking, complete ends, anomaly completion and TOE remain separate.
