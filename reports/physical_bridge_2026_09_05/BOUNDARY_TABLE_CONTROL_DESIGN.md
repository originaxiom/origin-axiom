# Boundary-table control: preserve the core and the index type

2026-09-16. Named receiving control, not a new B number or R32 theorem.
This re-executes frozen R19/R24/R30 assertions. It does not claim a new
source solution, rederive every analysis theorem, or begin a full suite.

## P0 and the incoming question

Scope: compact torus-boundary cores with k disjoint proper prescribed
source arcs, the existing strong singular complex and the separately
declared resolved absolute complex. Not the whole commensurability class,
all orbifolds, all source actions or a physical impossibility statement.

Pinned intake: main 3857877dfcc2ac2d2c8f3d31a90114aeb606004c, especially
docs/handoffs/CHAT1_I26_DECISION_TABLE_2026-09-15_VERDICT.md and its adjacent
chat1_i26_decision_table_2026-09-15/I26_DECISION_TABLE.md and
code/dplus_decision_table.py. All three were read completely. B1413's
complete findings and corrected consolidation relay were also read.

The received A--F table is elementary Euler arithmetic for specified
subsets of the UNDRILLED Q. Its identification of row D with R24/R30,
and its assertion that only privileging one cusp yields three, are
different claims from those arithmetic values.

## Prior, already in the record

Own base before this intake: 745e10b8545f80b0f593aa744e16dad2781074c7.
R15 section 2, R19 sections 2--4, R24 sections 3--5 and R30 sections 2--4
already distinguish Q from its source exterior C. R24 even tests the
incorrect unchanged-core substitution explicitly. R30's C3-label
correction is retained; no old (zeta,1) sample is called C3 fixed.
B1413 itself records the correct Euler values in its R24 paragraph.

Fresh all-remote/tag fetch completed without merging or changing seats.
The already-banked query "relative Euler source cusp drilling annuli"
returned 219 hits, none above its settled threshold. This is not an
absence certificate: the exact prior result is present in R24 and the
incoming main harvest. Atlas card is only general context. The
all-head regex presence sweep is retrieval, not theorem checking.
No semantic absence, novelty or unread-history coverage claim is made.

## Analytic accounting to check, not a fitted alternative

Let Q=C union N, with N the k solid arc tubes (each D2 x I),
intersection T the k lateral annuli, and E the punctured exterior tori.
Collar thickening supplies the excisive decomposition. Then

    chi(Q)=0, chi(N)=k, chi(T)=0,
    chi(C)=chi(Q)-chi(N)+chi(T)=-k,
    chi(E)=-2k,
    chi(C,T)=-k, chi(C,E)=+k.

All endpoints on ALL cusps are counted; endpoint discs are disjoint.
The independent cubical product controls are not hyperbolic manifold
models. Euler additivity gives the general formula; actual m202 Fox
cochains give the stated flat-character relative counts in R19.
Signs: N_16-N_bar16=-chi(C,T)=chi(C,E), not chi(C,T) itself.
For k=3 the singular generic sector is three/zero with both cusps.
Keeping Q but choosing its six-punctured boundary gives a different
pair chi(Q,E)=+6, not the sourced exterior pair.

R30's split model adds three even core cochains to three odd relative
ones. Its total dimension six has net graded index zero. At nonzero
attachment the nontrivial source-C3 base is acyclic. At finite width
R31 instead bounds at least three positive light Dirac pairs, not
exactly six zero states or a +6 chiral index. These are distinct types.

For the specified positive-endpoint source class, b_i=pi Q_i/A_i>0 on
each active cusp and exp(-2s) partial_s F=2b_i s+b_i+2c_i+o(1).
Finite through-flux constants change the safe cap height, not its
eventual sign. This does NOT force a cusp-exchange-symmetric vacuum,
but the sourced Euler result does not require that symmetry or choose
one privileged cusp. Zero endpoint charge, new end conditions or
additional defects change hypotheses and stay outside this control.

The APS/L2 claims in row C are not proved by assigning the empty set
to a boundary variable. Nor does flat-bundle Chern--Weil data alone
give every sourced/cusped physical index. These comparisons require
their actual operator and domain; the present rerun is not an APS
index calculation.

## Honest prior and falsifiers

Expected: existing controls recover the above distinctions. A failure
is recorded and inspected before promoting this reception correction.
The original files and assertions will not be edited to pass this run.
Two-sided checks already include k=0, charge reversal, wrong unchanged
bulk Euler, nontrivial exceptional/trivial holonomy, singular attachment,
and actual C3-fixed versus merely cube-root holonomy. No target SM
quantity or empirical fit enters.

The exact selection is in BOUNDARY_TABLE_CONTROL_SELECTION.json.
It is a narrow rerun, deliberately not a full-suite certificate; known
unrelated first-run errors remain. Code/tests remain at the pinned own
base, with the selection and this design committed and pushed before
execution. The checkout is read-only while tests run.

The symmetry-projection question is deferred, not closed: this intake
must first prevent a false identification of the models being projected.
Physical source/end selection, anomaly completion and mirror dynamics
remain the current task after this audit.
