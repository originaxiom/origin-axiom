# R37: finite tube scalar, interface and whole-mode control

2026-09-20. Path-local PB-ACTION / PB-BOUNDARY. Starting pin a770e53c.
Seal and push before scientific execution. The committed working
note already contains the candidate answers; no blind forecast.

P0: the specified ADDED compact H-source EFT and a separate flat
T2 times interval comparator. No class-wide no-go, actual hyperbolic
eigenfunctions, empirical target, physical phase or complete cusp limit.
P1: follows the source-action/normalized-coupling duty and the approved
[one-model audit](COMMON_MODEL_AUDIT_2026_09_20.md), not a new field scan.
P2/P3/P4: prior and frozen inputs below; no absence/novelty conclusion.
P5: this design, proof, prior, input inventory, producer and tests are
immutable after the first run. A correction requires a separate seal.
P6: positive prior for all identities and controls in the working note.
Expect a divergent selected-pair ratio but a finite full-space norm
ratio. That is a control against overclaim, not an expectation of a
physical mirror no-go. The interaction measure remains a model input.

Required discriminators:

1. Differentiate the full charge density and scalar potential, in ten
   complex vector components, with respect to S, Q and div h. Include
   both charged gauge currents. Check the S=0 stationary point and
   its real Hessian; missing source feedback must fail off shell.
2. Verify the weighted covariant Green identity and natural boundary
   flux with arbitrary weight/volume functions. Dropping the weight
   derivative must fail. Derive the constant-mode normalization and
   canonical mass/quartic/locking coefficients. Positive, zero and
   negative scalar-mass controls must be distinguished.
3. Directly integrate all 16 entries of each overlap matrix on the
   separate flat comparator. Check full-domain norms, exact eigen-
   equations and outer data. Prove completeness only at eigenvalue
   two; retain lower and other modes as physical duties, not discard.
4. Integrate all interface faces and retain 2P=2M+flux. Omitting the
   interface must fail. Test the off-shell weighted coefficient form
   for arbitrary F and connection, not just the flat example.
5. Check invariant singular polynomials under a genuinely mixing
   complex unitary basis change. Compare selected-entry and full-space
   limits, and weighted versus unweighted interaction densities while
   keeping the scalar kinetic normalization fixed.
6. Exact operands only; float cancellation cannot certify an identity.

Native run first, inspect; entire new test file next, inspect; then the
unchanged R36 ten-file population plus the new file. Preserve the four
known failed IDs. Tree read-only throughout checks. Exclusive raw stdout
and exit capture; publish copies with environment paths redacted only.
Run governance and custody before committing reports. This is local
research preservation, not full-green independent main banking.

On success, the next duty is the parent/source-field dictionary and
actual full low-energy operators, NOT another arbitrary scalar profile.
On failure, preserve the failed mathematical instrument and diagnose
its scope before using it for a physical negative. No outcome here
completes chirality, anomaly matching, quantum gravity or the TOE.
