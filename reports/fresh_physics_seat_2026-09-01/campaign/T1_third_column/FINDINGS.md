# T1 — THE THIRD COLUMN / CONNECTING-BLOCK ANNIHILATION TEST

Outside evaluation seat, campaign cell T1, 2026-09-01. Independent second route to codex's
in-flight computation (B1232's fenced fork). **Gate 5 absolute** — no measured SM value appears
anywhere in this cell; every numeric coupling below is synthetic or symbolic.

## VERDICT: **BLOCKED** — missing datum typed exactly; three deliverables banked on the way

The decisive question — *does the actual down/lepton coupling annihilate the actual connecting
block C?* — **cannot be computed from committed repo data**, and this cell certifies that with
receipts (s4). What CAN be computed from committed data, and was:

1. **The (3,4,1) sequence itself is reconstructed** (s1) — B1232 fenced it as codex's unverified
   in-flight computation; it is in fact derivable from the committed character ledger alone.
2. **The exact decision criterion is proved over Q(zeta_12)** (s2): P^3-invisibility <=> vanishing
   of **27 typed entries** — when codex's numbers land, the verdict is a mechanical substitution.
3. **The spread instrument is replicated and validated with bite** (s3): annihilating branch
   spread **exactly 0.0**, generic bite control **nonzero** (4.834 on B1232's own construction,
   byte-matching their banked 4.83; 15.76 max-entry on the real 3x3x4 shape). **Not DEGRADED.**

## 1. The sequence — what C, V, T are, from which committed objects (task step 1)

Sources: `frontier/B1212_two_replies/memos/YUKAWA_CUP_PRODUCTS_308.md` [M1],
`frontier/B1212_two_replies/documents/program-question-map/evidence/YUKAWA_DOWN_RESIDUE_SPEC_308.md` [M2],
`frontier/B1232_codex_r031_verified_and_three_columns/codex_certs_rerun.txt` [R031A transcript].

The down block lives in `mu_d: A (x) B (x) B -> H^3(O_Y)` with **B = H^1(Y, Lambda^2 V_308) =
38-dim = (33-dim connecting quotient) + (5-dim Serre-dual tail coker)** [M1]. Committed ledger:
connecting raw C12 multiplicities **(2,4,3,3,2,3,2,3,2,3,3,3)** (sum 33); the C^18 -> C^21 map
has kernel chi_10+chi_11 => rank 16, coker 5, raw coker labels (0,4,6,8,10), Serre-dual tail
labels **(0,2,4,6,8)** [M1].

**The identification (s1, all arithmetic verified exactly):**

| object | identified with | dim | source |
|---|---|---|---|
| **V** | **B_0** = the raw-chi_2 (= physical-chi_0) block of B — the space the P^3 projectivizes | **4** | conn chi_2-multiplicity 3 + tail label 2 once; matches R031A "B = 3 Reg + chi_0 + chi_11", K-rank 4 |
| **C** (sub) | **B_2,conn** — the 3 connecting-quotient directions of raw label chi_2 (33-column indices 6,7,8 per [M2]) | **3** | conn vector entry m[2] = 3 |
| **T** (quotient) | **<bhat_2>** — the raw-label-2 Serre-dual tail slot | **1** | dual tails (0,2,4,6,8) contain 2 once |

So **0 -> C -> V -> T -> 0 with dims (3,4,1)** exists with these committed identities — the part
B1232 fenced as "codex's exact computation, still running" is, for the *sequence*, already in
the committed record. What remains codex's is the *coupling values*, not the sequence.

**Conventions (E23 discipline):** chi_r(g) = zeta_12^r on [M1]'s marked C12 generator; physical
= raw twisted by the Lambda^2 V determinant linearisation **chi_{-2} applied once** ([M1];
chi_{-3} is carried by Delta_G in the trace pairing, not applied to B); Serre duality inverts
phase (r -> -r mod 12); connecting quotient = sub, tail = quotient (the long-exact-sequence
order in [M1]); splittings parameterized s_t(1) = bhat_2 + sum_k t_k c_k, t in K^3 (matches
B1232's `verify_quotient_lemma.py`). **Each convention has a run failing-alternative control in
s1 [6]**: the chi_{-3} twist fails the R031A match; skipping the Serre phase gives
dim(raw-chi_2) = 3 (no (3,4,1)); double twist fails.

**A prose discrepancy, flagged for the banking seat:** per the committed census
(36 = 18 conn/conn + 9 tail6/conn + 6 conn/tail2 + 3 tail6/tail2, [M2]), *annihilation of C* =
vanishing of the **27** entries whose Higgs leg (B_2) is connecting (3 Q-families x 3
dc-families x 3 connecting directions); the surviving observable is the **9-entry** tail family
matrix. B1232's phrase "that nine-entry tail/connecting block vanishes => the P^3 is a
splitting artifact" reads as if the *annihilation condition* were 9 entries. On the committed
census it is 27 (down channel); "nine" is either the per-direction 3x3 family matrix, or
codex's lepton-specific count not committed here. Typed, not resolved.

## 2. Does the actual coupling annihilate C? (task step 2) — **BLOCKED, datum typed**

**MISSING DATUM (exact type):** the values of the 27 Higgs-connecting entries T[i,j,conn_k] of
the selected (A_7, B_6, B_2) block — over Q(zeta_12), or even over GF(1009). Equivalently: the
normalized evaluator **T_cal = (Delta_G, Tr_{Y,Omega}, S)** ([M2]'s "single next artifact",
commissioned to codex R023). **Where looked (s4, programmatic):** every committed `frontier/`,
`docs/`, `tests/`, `papers/` file; the load-target `certify_yukawa_down_tail_cech_308.sage`
(5 files *mention* it, **the file is committed nowhere** — B1185's E51 single-homing debt
confirmed still open); codex's seat `/Users/dri/oa-audit-seat/aud1t/codex-r023` (**absent on
this Linux bench** — a macOS path on the owner's machine); and the two spec documents' own
proof boundaries, verbatim: *"No numerical or exact 1x18 down-Yukawa row is present in the
committed certificates"* and *"no nonzero down entry or rank is yet [claimed]"*. The "lepton
connecting block at three good primes" appears only as B1232's prose fence — no transcript in
this tree.

**Committed structure CANNOT decide it (a small proved negative, s1/s2):** the connecting and
tail directions of B_2 carry the **same raw character chi_2**, and C12 acts trivially on all of
B_0 (R031A: generator exponents (0,0,0,0)); the selection rule rho+sigma = 8 (mod 12) is
satisfied by the whole 36-block. So **no banked character/selection rule distinguishes C from a
complement** — the annihilation fork is invisible to every committed symmetry and genuinely
requires the chain-level evaluator. (Consistent with, and sharpening, GC-25/B1195.) The one
committed exact entry-level zero — **THE SKEW ZERO** on the repeated (4,4) pure-tail channel —
lives in the tail sector and says nothing about C.

**The up channel (where defined): ANNIHILATES, doubly and vacuously.** The up coupling's Higgs
slot is H_u = C_0, **1-dimensional** ([M1] Wilson table) — it never touches B_0, so no
splitting choice enters its observable at all; and independently **mu_u = 0 exactly** (rank 0,
the committed naturality theorem, re-run PASS twice on the owner's bench per B1185/B1232
transcripts — cited, not re-run here: the cert files live on codex's absent seat). The live
fork is down/lepton only.

## 3. The spread test (task step 3) — instrument replicated, **bite confirmed**

`s3_spread_replication.py`, 2000 random splittings per case:

| case | construction | spread |
|---|---|---|
| B1232 byte re-run, annihilating (their seed 20260901) | (3,4,1), scalar observable | **0.000e+00** |
| B1232 byte re-run, generic | same | **4.834e+00** (their banked 4.83, matched) |
| **A: annihilates C** (real 3x3x4 shape, tail-only) | 9 family observables | **0.000e+00** all entries |
| **B: generic (MANDATORY BITE CONTROL)** | all 36 entries N(0,1) | **9.315** at (0,0); **15.76** max — NONZERO => not DEGRADED |
| C: single conn entry T[1,2,c1]=1 | minimal obstruction | 6.136 at entry (1,2), **0.0** at (0,0) — localized |

And **exactly** (s2, sympy over Q(zeta_12), reduction mod z^4 - z^2 + 1): the identity
Y[i,j](t) - Y[i,j](0) = sum_k t_k T[i,j,conn_k] holds symbolically, so spread == 0 <=> the 27
entries vanish <=> Y factors uniquely through T (Ybar[i,j] = T[i,j,tail]); a genuine
annihilating Q(zeta_12) instance gives **identically zero** deviation over 25 exact rational
splittings, and a single conn entry set to zeta_12 gives deviation zeta_12 != 0. **The
criterion fails in both directions, exactly and numerically.** Case C additionally shows an
OBSTRUCTED verdict would name the offending entries individually.

## 4. What unblocks this cell

Any one of: (i) codex commits the Q(zeta_12) 1x18 connecting row (or full 36-block) — s2+s3
substitute and the verdict is immediate; (ii) codex commits even the GF(1009) connecting-row
values — a nonzero pivot certifies OBSTRUCTED at char 0 (a zero row mod one prime would NOT
certify ANNIHILATES — one prime can lie in that direction; the spec's own caution); (iii) the
lepton three-good-primes transcript lands in-tree. The E51 dual-homing debt
(`certify_yukawa_down_tail_cech_308.sage` uncommitted) is re-confirmed open and is the same
blocker wearing its provenance costume.

## Files

`s1_reconstruct_sequence.py` / `s1_out.txt` — the (3,4,1) reconstruction + convention controls.
`s2_annihilation_theorem.py` / `s2_out.txt` — exact equivalence over Q(zeta_12), both-way bite.
`s3_spread_replication.py` / `s3_out.txt` — 2000-splitting replication + mandatory generic bite.
`s4_missing_datum_probe.py` / `s4_out.txt` — the BLOCKED certification with receipts.
`t1_verdict.json` — machine-readable summary.

## Fences

Nothing outside this cell directory was modified. mu_u = 0 and the R031A exponents are **cited
from committed transcripts** (their runnable certs live on codex's seat, absent here) — the
only two cited-not-rerun facts in this cell; both are dual-homed in B1185/B1232 transcripts.
The s3 numerics use synthetic couplings by construction and are labeled as such — no branch
claims to be the actual coupling. Gate 5: no measured value entered; no held comparison was
designed, none was needed (the observable here is splitting-dependence, an internal fact).
