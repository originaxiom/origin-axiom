# G2 — THE T1 UNBLOCK ATTEMPT: underdetermination proved, the fork shown independent, the spec written

Campaign cell G2, 2026-09-01, owner-elected follow-on to T1 ("T1 green. act"). **Gate 5
absolute** — no measured SM value appears anywhere in this cell; all numerics are synthetic,
symbolic, or exact bookkeeping over K = Q(zeta_12). Conventions (E23): identical to T1/s1
(chi_r = zeta_12^r on [M1]'s marked generator; physical = raw twisted by chi_{-2} once;
Serre inverts phase; conn = SUB, tail = QUOTIENT; splittings s_t(1) = bhat_2 + sum t_k c_k).

## VERDICT: **UNDERDETERMINED-PROVED** (+ the freedom group + the codimension analysis + the spec)

and, checked carefully because it was the cell's biggest possible result: **FORCED-NONZERO is
NOT available** — no committed constraint resolves B1232's fork in either direction. That is
now a theorem with machine-checked witnesses, not a suspicion.

## 1. The computation attempt (task 1) — five routes, five typed terminations (g1)

Can the 27 values T[i,j,conn_k] of the (A_7,B_6,B_2) block be COMPUTED from committed data by
implementing R023's commissioned evaluator T_cal = (Delta_G, Tr_{Y,Omega}, S) on the banked
frames? **No — and each route ends at a typed absence, not a hard computation:**

| route | what was tried | receipt |
|---|---|---|
| A | direct retrieval, wider patterns than T1/s4 (assigned rows, Tr/Delta_G code, tail vectors) | 4 raw hits, each adjudicated IN THE OUTPUT a false positive (B807's "11 x 18 = 198" face/motif count; B1139's hypercharge anomaly traces Tr_Y; two prose mentions of the *unbuilt* evaluator). The probe fired and was inspected — it has teeth. |
| B | **git history + all four branches** (new vs T1: the working tree is not the record) | none of the six evaluator artifacts (`certify_yukawa_down_tail_cech_308.sage` etc.) was EVER committed on any branch — E51 is not a deletion; the files never entered this record |
| C | instantiate the committed formula [M2] input-by-input | all **10** value-determining inputs absent (Phi's 44 coordinates, f, frames, splittings, section bases, the 672×33 matrix, D, S, Delta_G, the trace); the 5 committed supply classes are character/index/rank bookkeeping — value-inert |
| D | rebuild the geometry from committed selection data | the ambient is partially specified (12 rays, Pic_Q via arXiv:1112.1097) but "candidate key = 308" has no committed enumeration algorithm, ordering, or coefficients — a name, not a construction |
| E | squeeze the spec's committed tail-row coordinate strings | they are coordinates in the basis of an UNCOMMITTED presentation (D and the e_i dictionary absent) — value-inert alone; closed constructively in g2 [4] |

## 2. Underdetermination PROVED, at two layers (task 2) — g2

**THEOREM A (two-model independence).** Two tensors are exhibited over Q(zeta_12), both
satisfying EVERY committed constraint on the down block — the selection rule rho+sigma ≡ 8,
B-leg antisymmetry, and hence the committed SKEW (4,4) ZERO (which the checker DERIVES, as its
planted known case): **T_ann** (all 27 connecting entries 0; spread exactly 0; P^3 invisible)
and **T_obs** (one connecting entry = zeta_12; spread nonzero, exhibited exactly). **The
committed record is consistent with both branches of B1232's fork — the annihilation question
is formally independent of it.** This is strictly stronger than T1's BLOCKED: not "the datum
is missing" but "the record PROVABLY cannot decide," by the standard two-model method.

**THEOREM B (the freedom group).** Even granting codex's frames up to everything the committed
record fixes (the character decomposition and the conn-sub/tail-quotient filtrations, nothing
else), the 36 block values are determined only up to

> **G = ( GL(A_7) × P(B_6) × P(B_2) × K^×_trace ) / (scalar torus)**, effective dim **27**
> (raw 9+7+13+1 = 30, minus the 3 redundant scalar-difference directions — verified to
> annihilate EVERY block exactly; generic orbit dim = 27, computed as the exact rank of the
> 30×36 linearized action over Q).

Under G: the 27 **values are gauge-variant** (a random G-element moves 9 of them on T_obs; the
tail values move on T_ann), while the **property "all 27 vanish" is G-invariant** (structural:
the parabolics preserve conn; checked exactly on 25 random group elements). **The boundary is
sharp (bite):** the non-parabolic element g[3,0] = 1 (tail→conn leak) makes 9 connecting
entries of the transformed T_ann nonzero — outside G the property is unstable, so the
committed filtration is load-bearing and G is exactly the filtration-preserving group. The
fork QUESTION is therefore well-posed on the committed record; its ANSWER is not in it.

(Also note: the spread over splittings IS the gauge orbit of the observable under the
unipotent radical of P(B_2) — T1/s3's instrument and Theorem B are the same fact twice.)

## 3. The conditional analysis (task 3) — g3

- **Allowed space = the FULL K^36 block**, shown structurally: every one of the 12 (j,k)
  channels has label sum 6+2 = 8 (K1 saturated, 0 equations); antisymmetry pairs the block
  only with its disjoint mirror (0 equations); the skew zero lives outside the block (0
  equations). The committed constraints' support is disjoint from the block.
- **codim(annihilates) = 27** (Jacobian rank 27, exact), dim 9 = exactly the surviving 3×3
  tail observable Ybar. Instrument controls: {M1=0} → codim 9, {det M0=0} at a rank-2 point
  → codim 1, both recovered.
- **THE FORCING CENSUS — the fork check, done carefully:** ten committed candidate
  constraints enumerated with sources (selection rule; skew zero; "no texture zero"; mu_u=0;
  the rank-16/33+5 presentation facts; KS rank 10; the lambda-term row; B1205's cubic; the
  Hoppe/local-freeness gates; the E8 rank-2 family law). Each is a permission, an
  off-block/off-map fact, a shape-generic statement, or fenced non-importable — and the
  single witness T_ann defeats them all at once: **nothing committed forces a nonzero
  connecting entry.** B1232's fork does NOT resolve to OBSTRUCTED by constraints alone (nor
  to ANNIHILATES — T_obs). The cell's biggest-possible-result door is closed honestly.
- **The conditional E8 layer** (labeled, fenced by B1185 INV-3 — the family index is not
  object-internal, so this is bookkeeping about an import, not a claim about the object):
  the weak law det Y(t) ≡ 0 (20 cubic coefficient equations) cuts K^36 to a variety that is
  smooth of **dim 26 (codim 10)** at its common-left-kernel points (exact Jacobian rank 10),
  and **both fork branches survive inside it** (witness W1: annihilating, tail rank 2 —
  satisfies even the strong rank-exactly-2 form; witness W2: obstructed, common-kernel,
  generically rank 2). Annihilation inside the variety = {conn = 0, det(tail) = 0}: dim 8,
  **codim 18** in the component. **Derived falsifiable cross-check, free of charge:** the
  imported law + annihilation ⇒ det(Ybar) = 0 — if codex's values land annihilating with a
  nonsingular tail observable, the import is refuted for this block. The strong form forces
  the OBSERVABLE nonzero, never the 27.

## 4. The commissioning spec (task 4)

`COMMISSIONING_SPEC.md` — hand to codex verbatim. Input set I1–I6 (defining data; chosen
bases; Delta_G; the normalized trace; the tail realization incl. D and the e_i dictionary;
the two-tier GF(1009)/Q(zeta_12) values), each item annotated with the exact freedom of
Theorem B it kills; acceptance gates G1–G6 including the planted SKEW-ZERO known case and a
generic bite control (failable both ways); the minimal fork-deciding subset
(I1+I2+I5+I6a for OBSTRUCTED; +I6b for ANNIHILATES); and the 27-vs-9 count note posed to
codex as a confirm-or-correct.

## MANDATORY BITE (cell-level)

Every instrument in this cell decides a planted case: the g1 retrieval probe fired on 4 hits
and each adjudication is printed; the g2 checker catches three planted violations by name and
derives the committed skew zero as its known case; the two models are distinguished exactly
(spread 0 vs zeta_12/3-type deviations); the g2 group-boundary bite (non-parabolic leak)
breaks the invariant as it must; the g3 codimension instrument recovers two planted known
codimensions before reporting the new ones. Nothing here is unfalsifiable dressing.

## Files

`g1_route_attempts.py` / `g1_out.txt` / `g1_routes.json` — the computation attempt, 5 routes.
`g2_underdetermination_theorem.py` / `g2_out.txt` / `g2_theorems.json` — Theorems A and B.
`g3_conditional_locus.py` / `g3_out.txt` / `g3_locus.json` — the conditional analysis.
`COMMISSIONING_SPEC.md` — task 4. `g_verdict.json` — machine-readable summary.

## Fences

Nothing outside this cell directory was modified. The two models of Theorem A are SYNTHETIC by
construction and labeled so — neither claims to be the actual coupling; the theorem is about
what the committed record admits, and needs nothing more. The E8-law layer is a labeled
conditional on an import that B1185 INV-3 fences as non-importable; nothing in this cell
asserts the law holds for the object. mu_u = 0 and the R031A facts are cited from committed
transcripts exactly as in T1 (their runnable certs live on codex's absent seat). Gate 5: no
measured value entered; no held comparison was designed or needed. The 27-vs-9 discrepancy
stays TYPED (T1's flag), now routed to codex as a spec question rather than resolved here.
