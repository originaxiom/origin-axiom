# B384 (D4 / PD3.1) — PRE-REGISTRATION: the Kashaev bridge, computed side

**Committed before computation. Scope: the COMPUTABLE half of D4 — put our object and the
figure-eight quantum invariants on one table and test the sharpest computable coincidences.
Identification-with-citations is flagged NEEDS-SPECIALIST for novelty throughout; kills bank.**

## What is already banked that this touches

The tower IS the quantized golden cat map at level N (P59: ord(W₁@N) = π(N)/2, Pisano); the
figure-eight monodromy [[2,1],[1,1]] is THE cat map of our m=1 object (B67: the trace-map fixed
locus reproduces the figure-eight A-polynomial); the Kashaev invariant of 4₁ is the canonical
hyperbolic-volume object on the SAME monodromy. The bridge question: does our level-N theta
data see the Kashaev/quantum-dilogarithm side?

## T1 — the Kashaev invariant smalls (exact integer target)

⟨4₁⟩_N = Σ_{k=0}^{N−1} |(q)_k|² with q = e^{2πi/N}, (q)_k = Π_{j≤k}(1−q^j) — compute EXACTLY
(cyclotomic integer arithmetic, then the real value to 50 digits) for N = 5, 15, 45, 135
(our tower levels) and neighbors N = 7, 9, 25, 27, 81. Banked facts to test against:
  (a) the N-scaling log⟨4₁⟩_N /N → Vol(4₁)/2π (volume conjecture — KNOWN, sanity gate only);
  (b) REGISTERED BET: the ℚ(√5)-content — does ⟨4₁⟩_N at our levels (N ≡ 0 mod 5) carry the
      golden unit (e.g. ⟨4₁⟩_5 ∈ ℤ[φ]-norm form, factor λ₁ = φ² or φ√5)? KILL: no φ-structure.
  (c) REGISTERED BET: ord-matching — is the 5-adic/3-adic valuation of ⟨4₁⟩_N at N = 15,45,135
      tied to the Pisano ladder (π(N)/2 = 20, 60, 180)? KILL: no relation visible on 3 levels.

## T2 — the trace-side companion (our object at the same levels)

The banked slot data gives tr-level constants at N=15 (±1/48 etc.). Compute the ANALOGOUS
Par-graded traces of the pure cat-map word tr(Par·W₁^j) DFT'd (a-side only, the single-seed
line) at levels 15 and 45 in the exact engine — the object that would sit opposite ⟨4₁⟩_N on
the bridge (both are weighted traces over the same monodromy family). REGISTERED BET: a
rational-linear match between the level-45 single-seed sector constants and the level-15 ones
(the tower transports the constants). KILL: no relation. (This doubles as the level-45
companion-constant step of the escalation protocol — computed WITHOUT any physics wording.)

## T3 — the modular-data neighborhood (PD1.2, data-only step)

Extract our S/T data at level 15: the seam sector's 2×2 block of (Ŝ, T̂) in the canonical model
vs the theta model (exact). Compare against the U(1)_k/Weil-TQFT family shape: S = (1/√k)·
[[1,1],[1,−1]]-type vs our block. Output: the exact block + the family it matches or a NO-MATCH.
Novelty of any match: NEEDS-SPECIALIST.

## Outcomes

Each of (b), (c), T2, T3 is two-outcome and banks. (a) is a sanity gate (known theorem-adjacent;
failure = my code is wrong, not a discovery). No physics claims; firewall absolute.
