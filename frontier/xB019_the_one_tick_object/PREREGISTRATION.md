# xB019 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell of this arc's
`verification/` runs.**

## P0

Hyperbolic 3-manifolds, orientation, Chern–Simons, and finite quotients. Named mathematics. No
value, no generation count, no physics reading, nothing to `CLAIMS.md`. Gate 5 absolute.

## The question — B1234's OWN declared live question, unanswered for 16 days

xB018 named `GL(2,ℤ)` as the axis it held fixed. **The sweep found that axis already has an arc on
it, and a question left open on purpose.** B1234:

> *"**Does not establish:** that dropping A6 makes values derivable. A non-orientable object may not
> support the machinery at all — **Chern–Simons, the complex volume and the SL(2,ℂ) representation
> theory all use orientation** — so dropping A6 may **break the tools** rather than open a door.
> **That is now the live, sharp question.**"*

**This arc answers it, per tool, and then asks what xB018's machinery adds: of the two bits, which
one exists before the squaring?**

## THE CONFIGURATION AXES — declared first (xB017 Addendum 4's rule)

| axis | values | this arc |
|---|---|---|
| **orientability** | orientable `m004` / non-orientable `m000` (Gieseking) | **varied — this is the arc** |
| **the tick** | `M` (det −1) / `M² = RL` (det +1) | **varied** |
| **the tool** | volume · `H₁` · `π₁` · SL(2,ℂ) holonomy · `CS` · complex volume · symmetry group · surjections onto 2T | **all eight audited separately** |
| **the bit** | A5(c) (knot-ness) / A7 (the LR–RL order) | **both, via xB018's characterisation** |
| ambient group | `GL(2,ℤ)`/`SL(2,ℤ)`; `PGL(2,ℂ)`/`PSL(2,ℂ)` | **named, and the ℂ-side is the mechanism** |
| the census | `m000` only, not the wider non-orientable census | **held FIXED — named in the verdict** |

## The sweep, done first (xB010's rule)

* **B1234** — the arc that owns this axis. Its cells: orientation double covers are **40 of 40
  amphichiral** against a **3.0 %** base rate; **m000's orientation cover IS m004** (volume ratio
  exactly 2); and **the arithmetic does not need the squaring** — surjections `π₁ ↠ 2T` are **48 for
  Gieseking and 48 for m004**, and Gieseking is in the same `ℚ(√−3)` class. Its finding: **the eight
  walls are properties of a CHOICE made at link 6, not of "the minimal something."** Its unclaimed
  clause, carried here verbatim: **surjecting onto 2T is generic (~⅓), so the equal counts are not
  evidence of distinction.**
* **B1083** — `M` has `det = −1`, `M² = RL` exactly; the one-tick mapping torus is Gieseking;
  **"on the one-tick object chirality cannot be POSED (non-orientable); the second tick buys
  orientability and pays amphichirality."**
* **xB018 (Path C)** — the two bits characterised by their action on `CS`: **A7 negates, A5(c)
  translates by ¼**; A7 is visible only to a **based** invariant, A5(c) only in the **k-coupling**.
* **xB012** — the tower `1 : 12 : 24` = `H³/PGL(2,O₃)` : m000 : m004.
* **Verified before sealing, and it killed a guess of mine:** `LR` and `RL` **are** conjugate **in
  `SL(2,ℤ)`** — conjugator `(−5,−8,−3,−5)`, `det = 1` — so B979's "same conjugacy class" is an SL
  statement and correct. **My guess that A7 was "GL-conjugacy only" is dead before it was written.**
  And the Breath pulse is exhibited: `M = [[1,1],[1,0]]`, `det = −1`, `M² = RL` — **the Fibonacci
  matrix, whose eigenvalue is φ itself.**

## The cells, with predictions and kill conditions

**G1 — B1234's cells 2 and 3, re-derived.** `m000`'s orientation cover isometric to `m004`, volume
ratio 2; and the surjection counts `π₁ ↠ 2T` for both. *Prediction:* cover confirmed; **48 = 48**.
*Kill:* any disagreement stops the arc until resolved.

**G2 — THE TOOL AUDIT, the cell that answers B1234.** For each of the eight tools, does it exist on
`m000`? *Prediction:* **volume, `H₁`, `π₁`, the symmetry group and the 2T count survive; `CS` and the
complex volume BREAK — and break by DEFINITION, not by difficulty.** *Kill:* if `CS` returns a value
on a non-orientable manifold, the premise of B1234's question is wrong.

**G3 — which of the two bits exists at tick one? (the blind cell)** xB018 characterised A5(c) as
knot-ness (`H₁` torsion-free) and A7 by its action on `CS`. *Prediction, and I am genuinely unsure of
the first half:* **A5(c)'s datum is `H₁`, which survives** — so the bit can be posed at tick one;
**A7's datum is `CS`, which does not exist** — so it cannot, matching B1083's *"chirality cannot be
posed."* **What I cannot call is whether `H₁(m000)` is torsion-free**, i.e. whether the A5 bit at
tick one is already *on*. *Kill:* if `H₁(m000)` has torsion, A5(c) is posable but set the other way,
and the arc must say so.

**G4 — the answer to B1234's question, stated per tool and with its consequence.** *Prediction:*
**dropping A6 breaks exactly the tools that CONSTITUTE the walls** — so *"drop A6 to escape the
walls"* is **ill-posed rather than merely hard**: there is no `CS` on the one-tick object that could
be non-zero. *Reporting rule:* this is a **negative for the escape route and a clarification of the
question**, not a new wall, and must be said that way.

**G5 — what the squaring buys, itemised**, separating what it **does** buy from what B1234 showed it
does **not**.

**G6 — the verdict**, naming every axis held fixed.

## What this arc will NOT claim

Not that Gieseking is distinguished (B1234's own unclaimed clause: 2T surjection is generic ~⅓) ·
not that dropping A6 makes any value derivable · not that the tools' breaking is evidence *for* A6 ·
no identification (E82/I-10) · no physics reading · nothing to `CLAIMS.md`.

## The seat's prior, declared

G1 reproduces · G2: `CS` and complex volume break, the rest survive · **G3: A7 cannot be posed;
A5(c) can — and whether it is already ON is the thing I cannot call** · G4: the escape route is
**ill-posed**, which is a sharper outcome than "hard" · **G6: B1234's live question gets a definite
per-tool answer, and the honest headline is that the walls cannot be escaped by dropping A6 because
the walls' own invariants do not survive the drop.**
