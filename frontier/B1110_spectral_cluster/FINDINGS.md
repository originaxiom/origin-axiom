# B1110 — THE SPECTRAL CLUSTER (F5/F6/F9): the vanishing locus is the word's own; silver's defect carries the parity bit in its address; the naive tone channel never existed

**Status: banked (frontier). Verdict PROVED (F5's census + mechanism at
float-grade with the B1103-exact anchor; F6's law at 11-window strength with an
exact formula; F9's two-line impossibility exact). The wave's second arc. Gate 5
untouched. Lock `tests/test_b1109_b1110_b1111_wave.py`.**

## F5 — L175 CLOSES: THE h = 0 LOCUS IS A WORD PROPERTY, and the mechanism is DIAGONAL-FREEDOM

Across SIX channels spanning every listener sector of the B593 instrument (the
two θ-odd dials, the two even-symmetric combinations, the two fixed basis
states), the vanishing word-sets over all 1364 free-group words to length 5 are
**IDENTICAL — the same 28 words in every channel** (the odd-u₃ set agreeing with
B1103's exact count). The outside session's conjecture ("if the vanishing locus
is a property of the word, that is a fourth theorem in the family") lands on its
strong branch. **The mechanism, found and checked:** the 28 are exactly the
words whose weld matrix is **DIAGONAL-FREE** — every diagonal entry vanishes
(max |diag| = 1.2×10⁻¹⁵ across all 28), along with the paired symmetric
off-diagonals — which forces every single-index and every ± pair channel to
zero simultaneously, while RANDOM test vectors do NOT vanish (the matrices are
far from zero; the locus is the diagonal's, not the matrix's). *(Cross-checked
at the owner's demand: the smallest NONZERO |h| across all 1364 words is 0.53 —
a separation ratio of 5x10^9 above the census threshold, no borderline cases —
and the mechanism separates by fifteen orders: every non-vanishing word has
max|diag| >= 0.30 while the 28 sit at 1.2x10^-15.)* Typed:
float-grade census with exact-anchor agreement; the exact certification of
diagonal-freedom rides the lock's slow gate.

## F6 — VIII.3 CLOSES: THE CENTER-BREAK LAW (silver's own conservation)

The strict silver analog (slope 1/silver², ρ = α) never reversal-closes — and
its failure obeys an exact law, verified at ELEVEN Pell windows to N = 80,782:
**always exactly TWO mismatched letters; at EVEN Pell index they sit at exactly
{N/2, N/2+1} (the center); at ODD index at {0, 1} (the cut).** Golden alternates
closed/broken-at-cut; silver alternates broken-at-cut/broken-at-center — the
reversal-defect is conserved at two letters and **its ADDRESS carries the
parity bit**. OBSERVED at 11-window strength with the formula stated — and
RE-VERIFIED IN EXACT ARITHMETIC (Fraction convergents, error ~1e-91) at all
eleven windows including N = 80,782, dismissing the float-floor hazard; the
combinatorial proof (Pell-word central-defect structure) is a named minor open.

## F9 — L178's GATE HARDENS: the naive channel is arithmetically impossible

Differential-first, stated plainly: the substrate dictionary (B518) reads
κ − 2 = 4λ² with REAL coupling λ, so the physical Fibonacci-operator sheet has
κ real and ≥ 2. The object's own bridge value is κ − 2 = ω² — **complex, off
the real-substrate sheet entirely** (the required coupling λ = ω/2 ∉ ℝ). So
"set the substrate's coupling to the object's κ and read the tones off the
spectrum" — the naive identification — **never existed as a channel**. The
tones-as-observables gate stands, HARDENED with a computed reason; the
surviving channels are typed: (i) the root-of-unity/WRT door (the audit seat's
B8116 second door — root-of-unity data crossing to archimedean invariants);
(ii) tones as non-positional invariants (ratios/holonomies rather than
spectrum positions). L178 remains GATED; the gate now has structure.

## Road dispositions at this bank

- L175 → CLOSED (this arc; the diagonal-freedom theorem-shaped mechanism).
- VIII.3 → CLOSED-OBSERVED (the law + formula; proof = named minor).
- V.3/VIII.2 (L178's gate) → hardened, channels typed; still GATED (correct).
