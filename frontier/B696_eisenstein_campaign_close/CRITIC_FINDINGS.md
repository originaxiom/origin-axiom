# LOOP E-4 — THE COMPLETENESS CRITIC (adversarial exhaustion check,
# BEING/Eisenstein face). Read-only pass over e1_landscape, e2_level15,
# loop_E2/{ea_3adic,eb_jordan_heart}, loop_E3/{e3a_Ldepth,e3b_chirality},
# all LOOP_*_CLOSE.md, EISENSTEIN_ATLAS.md, and the cited frontier record
# (B637, B657, B671, B681/B683, B685, B689-B695, S067). No repo writes.

## METHOD

For each claim in the ATLAS, I asked: (a) is the underlying banked DATA
the *complete* banked data on the subject, or only a subset the campaign
happened to use; (b) does a banked SCRIPT/MATRIX exist that a cell could
run/read directly to answer a neighboring question the campaign didn't
ask. Two items below (#7, #8) survived this test — both fit the exact
adversarial categories the prereg named ("silver/bronze cubic core
ratios if banked data exists"; "the sum-rule's rank-2 structure at other
words") and neither appears in the ATLAS's own OPEN/DEFERRED list.

## THE CLASSIFIED LIST

1. **Cubic core ratio across golden's own bend family (9 doubles: none,
   m=1/5/7/11, four D_phi involutions).** ANSWERED (E1 + B637 stage 3):
   24*zeta_6 on 6 "024-silent" doubles, +exact multiple of 13 on 3
   "024-lit" doubles. DEAD-OR-ANSWERED.
2. **Cubic core ratio golden vs silver.** ANSWERED: field obstruction
   (zeta_6 not in Q(i)) proven, not merely observed. DEAD-OR-ANSWERED.
3. **Cubic core ratio at bronze/trace-5-12.** Checked: grep across the
   whole repo (frontier/, docs/, papers/) for bronze/trace-5 cubic or
   swap data returns NOTHING — the entire 27-dim E6 local system has
   never been built for any metallic-family member past silver (B69's
   bronze/m=5,6 work is a DIFFERENT construction: the SL(2) trace-relation
   curve / cusp-torsion law, not the E6-27 cohomology). Building it is a
   from-scratch amalgam-Fox-calculus construction (B637-scale effort).
   PRICED-SPECIALIST (correctly named in the ATLAS's OPEN/DEFERRED list).
4. **Swap eigenvalues, period-3 test (golden, silver).** ANSWERED:
   FALSIFIED (golden period-6, silver no finite period; object-chosen
   trace-subfield law). DEAD-OR-ANSWERED.
5. **Swap eigenvalues at golden's OTHER 8 doubles (bent/involution).**
   The banked GOLDEN_SWAP_DIAG (B638, "G2") is stated as a single
   diagonal, sourced from the conjugation-weld construction generic to
   the h1=5 family, not re-derived per bend in this campaign. Lower
   priority than #7/#8 below (the swap law is plausibly a structural
   fact of the weld common to all h1=5 doubles, per B638's own framing,
   not obviously bend-sensitive the way the cubic's Y[234] slot is) --
   flagged but NOT elevated to a blocking item; would need reading
   B638's own scope note to settle, PRICED-leaning, not asserted either
   way here.
6. **Phase catalogue (twelfth roots, golden-specific; silver generic).**
   ANSWERED (E1 task 2). DEAD-OR-ANSWERED.
7. **The forced-Koide/sum-rule test's rank-2 structure, GOLDEN, at
   the OTHER 8 doubles (bent m=1/5/7/11, involutions a/A/b/B).**
   COMPUTE-CLOSABLE-NOW (moderate effort). The E1 Koide test used
   scalars from "B671/B673 d1_results.json fixed_basis_coords" ONLY --
   d1_pairing.py's own header states this is "the SAME unbent/'none'-bend
   row" as part2b_stage2_fixed_output.txt. But B637's stage-3 campaign
   already computed the FULL cubic Y-tensor on all 9 doubles (the cubic
   ratio law Y[023]=24*zeta_6*Y[123] verified 9/9), meaning the
   underlying H^1 cohomology / cocycle data for all 9 doubles was
   already constructed via banked, already-written scripts
   (b637_d4_gluings.py, b637_stage3.py, b637_threeform.py). Whether the
   FINER vector-pairing object (D1 -- the actual source of the
   Koide-test scalars [c_23],[c_24],[c_34]) shows the SAME rank-2
   "2-tied+1-free" structure with the SAME coefficient
   -(7983360/13)*omega on the other 8 doubles, or whether it is
   bend-DEPENDENT (mirroring E1's "field-locked landscape" finding for
   the cubic itself), is UNANSWERED. This is exactly the being-face
   landscape question E1 was chartered to ask ("the cubic's core ratio
   and how it VARIES") applied to the sum-rule object specifically, and
   it isn't in the ATLAS's OPEN/DEFERRED list. Requires composing
   already-banked machinery (no new mathematics, no external
   literature/data) -- a real cell, not a trivial lookup, but
   compute-closable from what's banked.
8. **The rank-2 sum-rule analog for SILVER.** COMPUTE-CLOSABLE-NOW, LOW
   EFFORT. frontier/B673_loop4_integration/packet/loop4/d3_silvercup/
   d3_results.json (cell "D3", FORM-MATCH verdict banked 2026-07-17)
   ALREADY contains the full 25-pair silver vector-cup table, including
   the "23","24","34" obstruction-coordinate vectors, in the SAME
   fixed-basis-coordinate format d1_pairing.py used for golden. Nobody
   has computed rank(span{[c_23],[c_24],[c_34]}_silver) or its tying
   coefficient. This is PURE LINEAR ALGEBRA on already-serialized JSON
   vectors -- no new derivation, no new run, no external input. It
   directly tests whether golden's rank-2 "generic-kill" pattern (E1
   task 3) is a golden-only accident or a second data point for the
   "field-locked landscape" thesis the campaign already advances for
   the cubic (E1 finding 1b). Caveat (does not block the compute,
   only the geometric READING): B673's own D3 note says the silver
   boundary/solo split is UNRESOLVED (w2a), so indices "2,3,4" in
   silver's basis are not YET known to be the geometric analogs of
   golden's solo classes -- but the rank computation itself needs no
   such identification (B657's W2a "FORM-MATCH" tests already proceed
   this way, without canonical alignment). Not mentioned anywhere in
   the ATLAS.
9. **Jordan heart v0 -- rank-3 invertible, sharp-fixed, N(v0)=-6, F4
   1+26 split, portal rank-5 isomorphism -- at golden AND silver.**
   ANSWERED (E-2b/eb_jordan.py): identical numeric values both fields,
   independently re-verified this seat's read of eb_jordan.py (computes
   N, sharp-fixed check, F4 split, AND independently recomputes the
   portal's rank/kernel from its own stored matrix). DEAD-OR-ANSWERED.
10. **v0 at bronze+.** Same as #3 -- no 27-dim construction exists for
    bronze at all. PRICED-SPECIALIST.
11. **zeta_3 structure / totient argument (phi(3)=2 prime forces a
    minimal self-conjugate doublet, forbidding golden's richness).**
    ANSWERED (E-2b, B691, two-route gated). DEAD-OR-ANSWERED.
12. **3-adic law for the being Habiro object -- exact defect pattern.**
    Checked ea_3adic.py directly: it DOES use every available banked
    coefficient, including GSWZ's "COMMENTED-OUT draft note in
    text131.tex" for n=6..10 (verified in-script, lines 199-203) on top
    of the n=0..5 published values and the n=100 denominator. No
    further banked terms exist to extract (n=11..99 would require
    reimplementing GSWZ's unpublished 600-term PARI pipeline). The
    exact defect pattern is genuinely PRICED-SPECIALIST, correctly
    named as such in both E-2a's FINDINGS and the ATLAS.
13. **The level-15 handshake -- genuine coupling (E2), inert-5 Hecke
    test (E-3a), ramified-3 companion a_(P3)=-1 (resolved same-day in
    B694, despite B693 flagging it as a "follow-on"), L(E_15,s)
    interference + c-symmetry (E-3b), K3/K2 refinement (being FIELD's
    L-value vs being PRIME's Hecke datum, both true/different
    questions).** All ANSWERED, two-route/two-seat gated throughout.
    DEAD-OR-ANSWERED.
14. **v0's full F4 STABILIZER SUBGROUP (group-theoretic, beyond the
    linear 1+26 eigenspace split already computed).** Would need
    Lie-group/GAP-Magma-style machinery not present in the banked
    linear-algebra scripts. PRICED-SPECIALIST, not compute-closable
    from banked data.
15. **Cross-bend behavior of the being FIELD's own L-value
    (L(chi_-3,2)=Vol) vs the K3/K2 split, at the OTHER 8 doubles.**
    Speculative extension of E-3b's K3/K2 refinement; no banked
    per-double L-function machinery exists to test this from data
    alone. PRICED-SPECIALIST (not elevated; more a research question
    than a banked-data closure).

## VERDICT: NOT-EXHAUSTED

Two COMPUTE-CLOSABLE-NOW being-face items block a clean EXHAUSTED call,
both explicitly of the adversarial shape the prereg named and neither
named in EISENSTEIN_ATLAS.md's own OPEN/DEFERRED section:

- **#8 (silver sum-rule-analog rank check)** -- lowest effort: pure
  linear algebra on already-banked vectors in
  frontier/B673_loop4_integration/packet/loop4/d3_silvercup/d3_results.json
  (repo, read-only). No new derivation.
- **#7 (golden sum-rule at the other 8 bend/involution doubles)** --
  moderate effort: composes already-written, already-run machinery
  (B637's stage-3/d4-gluings cohomology construction + the D1
  vector-pairing method) that the campaign has not yet pointed at
  anything but the single unbent double.

Everything else the campaign covered (cubic landscape within golden,
golden-vs-silver field obstruction, swap non-periodicity, phase
catalogue, the Jordan heart's universality, the totient root, the
3-adic partial mirror at its data ceiling, and the full level-15
handshake through K3/K2) is either genuinely closed or correctly priced
(bronze/trace-5+ construction, the exact 3-adic defect, the F4
stabilizer). The residue is real but small and named exactly: run #7
and #8, then the being-face exhaustion call can be revisited.
