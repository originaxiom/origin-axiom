# B451 — PREREGISTRATION: D4 / Relation R5 — the trace-map transfer-operator Ruelle resonances

**Committed before computation. Firewalled. The reserved thermo-campaign slot (S054 row 4),
double-closing Chat-1's path 5 (the mapping-class action IS the trace map on the character
variety — the identification, one line, auditable).**

## The question (blind)
The resonance spectrum of the trace-map transfer operator on the B186-certified horseshoe,
beyond the banked leading eigenvalue (the escape rate). The strongest genuine-surprise
candidate of the Relation campaign (no laundering theorem covers transfer-operator spectra).

## Gates (binding, per the red team)
- **The leading eigenvalue must reproduce the banked escape rate γ = 0.51 at κ > 2 (B186,
  ground-truth-validated) before any subleading resonance is read.**
- The κ-scan bounded to the certified hyperbolic region κ > 2 (κ = 2 is the non-hyperbolic
  calibrator — cycle expansions undefined there, stated).
- The symbolic dynamics is an INPUT (B186 certified hyperbolicity via escape rate, not a Markov
  partition): the orbit enumeration = periodic points of the trace map by degree (the B448
  recursion machinery), with the grammar completeness checked against the escape-rate gate.

## Method
Cycle expansion of 1/ζ(z) = ∏(1 − z^{n_p}/|Λ_p|) over primitive orbits to length N (precommit
N ≤ 12); resonances from the zeros; convergence monitored by shadowing-term decay. m-scan
(golden/silver) + κ-scan (κ ∈ {2.5, 3, 4}) per the thermo standard.

## Bins
Leading = γ ✓ then: subleading spectrum whitelist-expressible (λ-powers etc.) → LAUNDERS /
a resonance structure not reducible within a session → burden-inversion → escalate / the exact
resonance table → NEW-MATH-eligible (no covering computation known — lit-gate: cycle-expansion
literature for Fibonacci/KKT trace maps, Cvitanović-school).
