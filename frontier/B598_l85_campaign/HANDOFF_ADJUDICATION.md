# The L85 handoff (chat-1, 2026-07-14 late) — adjudication

**Adopted as the campaign's literature frame, with the corrections below
binding. Per the standing rule, every claim was checked before use; the
provenance note applies (all verification internal).**

## ADOPTED (verified or verifiable-as-scoped)

1. **The working ansatz: "the map IS the state integral at a shifted saddle."**
   Consistent with the banked hearing law's structure (O(ε) parity-forbidden,
   ε² = the odd quadratic form) and with P0's confirmed ε = 0 case
   (S₀ = Vol/2π at 1e-41; the 1-loop 3^{−1/4} at 8e-12). Adopted as the P2/P3
   frame; the Dimofte–Gukov and Charles–Marché citations enter the campaign
   bibliography as [CITED].
2. **The S₁-level Sym-block factorization** (S₁^{E₆} = Σ_m −½ log τ_m):
   plausible via block-multiplicativity of the adjoint torsion (B126/B581
   adjacency); REGISTERED as a P3 gate to verify, exactly as chat-1 flags.
   The full-integral factorization is a stronger claim — P3 tests only what
   it needs (the S₁ level) first.
3. **The route P1→P2→P3**: matches the campaign structure already banked
   (B598); the "6×2 cusp matrix" framing of P1 is adopted.

## CORRECTED (with the in-sandbox discriminating facts)

1. **The number-field claim is FALSE as stated.** Computed (pari nfisincl):
   √−3 ∉ ℚ(ζ₂₀) and √−15 ∉ ℚ(ζ₂₀); √5 ∈ ℚ(ζ₂₀) ✓. ℚ(ζ₂₀) is NOT the
   compositum of ℚ(√−3) and ℚ(√5) (that compositum needs conductor divisible
   by 3 — it sits in ℚ(ζ₆₀)). Moreover the banked amplitude lives in the
   SMALLER field ℚ(ζ₅) (B594's adjudication: (√5−1)/4 + (ζ₅−ζ₅⁻¹)/(2√5)).
   **The correct statement is cleaner than the numerology: the hearing
   amplitude's arithmetic is purely golden (ℚ(ζ₅)) — the stage's field; the
   object's trace field ℚ(√−3) does not enter it.** The "20 = 4×5 encodes
   the θ-odd exponent and the golden prime" reading dies with the claim.
2. **The S₁ normalization**: the handoff's "3^{−1/4} = e^{S₁/2}" fudges a
   factor; the banked form is the width bridge T² = |τ₁| = 3 with the 1-loop
   prefactor |τ₁|^{−1/4} (B598-P0). The θ-odd extension T_m² = |τ_m| is a
   REGISTERED P3 check, not yet a fact.
3. **"Third unhearability" numbering** (the handoff calls R3-M's kill "a
   third unhearability"): the bank counts four (vacuum, fillings, bare
   states, the closed-contraction/mirror kill). Cosmetic; recorded to avoid
   ledger drift.

## The anisotropy remark (adopted as a flagged observation)

If T_m² = |τ_m| extends to the θ-odd blocks (P3 check), the width ratio
T₄/T₈ ≈ 6×10⁻⁸ makes the semiclassical state extremely anisotropic across
the two chiral directions — recorded as chat-1's [HOOK], testable only after
P3's normalization check.

## Addendum — the role-separation law (chat-1's corrected reading, generalized)

Chat-1's post-correction reframing is adopted in the generalized form the bank
supports: **structure from the object's field, values from the stage's field.**
ℚ(√−3) organizes WHERE to listen (the dial, the θ-parity architecture — B575's
exact model is ℚ(√−3)-arithmetic, as P1's cusp table now exhibits explicitly);
the amplitude VALUES belong to the listening stage's cyclotomic arithmetic
(ℚ(ζ₅) on the golden stage; ζ₁₄-arithmetic at E₆₂ — B589). The two fields do
not mix in the amplitude; this matches B586's banked "the number is the
stage's, the pattern is the object's."
