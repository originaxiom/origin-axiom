# STEP 8 — PREREG AMENDMENT 2 (V3 criterion (iii); sealed after V3's
output was committed, justified ONLY by facts banked BEFORE the seal)

**2026-07-15. V3's output is committed as-sealed (#962: FAIL on clause
(iii)). This amendment strikes the magnitude clause of (iii) as PROVABLY
UNSATISFIABLE AT SEALING TIME, on banked facts alone — no information
from the V3 run is used in this justification:**

B594-H3 (banked 2026-07-14, lock `tests/test_b594_e6_hearing.py`)
established the order-4 consistency (UᵀρU)⁴ = I on the E₆₂ odd 3-space;
C = −1 there, so B = Uᵀ(Cρ)U = −UᵀρU satisfies B⁴ = I. Every eigenvalue
of a finite-order operator is a root of unity: |λ| = 1 for ALL
eigenvalues. The sealed clause "|μ| ≠ |λ| (width separation)" was
therefore impossible for ANY outcome of the computation — the same MB12
error class as V1's vacuity (there a trivially-true operation, here a
trivially-false criterion), caught one cell later. The sealing error is
recorded.

The INTENT of (iii) — "the third channel separated by the width labels" —
is served by what remains: the splitting is canonical by CONJUGATION TYPE
(the non-real pair vs the real eigenvalue), which is exactly the
separation the D1 correction demanded ("a rank-2 image or a quotient,
determined by the construction, not assumed" — B597 correction 2). The
magnitude clause was excess sealed by error.

## V3′ (amended criterion)

V3′ PASS requires, from the ALREADY-COMMITTED output (#962; no rerun):
  (i)   the characteristic polynomial real (spectrum conjugation-closed);
  (ii)  exactly one non-real conjugate pair + one real eigenvalue;
  (iii′) the real eigenvalue μ ≠ 0.
All three are genuine discriminators (each could have failed: complex
σ₂/det; three real or three non-real roots; μ = 0 rank collapse).

## The amended verdict, read from #962

(i) TRUE (σ₂ = +1, det = −1, both real); (ii) TRUE (spectrum exactly
{−1, +i, −i}); (iii′) TRUE (μ = −1). **V3′ = PASS.**

No other change. The outcome statement follows the locked A/B/D table
with cells V1′ (PASS), V2 (FAIL — genuine, decisive, no cell defect),
V3′ (PASS).
