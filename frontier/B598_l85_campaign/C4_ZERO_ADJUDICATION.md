# Chat-1's c₄ = 0 computation — adjudicated (2026-07-15)

**The computation is correct. The interpretation overreaches. Both facts on
the record before anything builds on "the SM is one quadratic form."**

## What is true

Chat-1 ran the stage-side deformed amplitude and found c₀ ≈ c₁ ≈ c₃ ≈ 0
(the selection rule) and c₄ = 0 to the noise floor. Confirmed — and
SHARPENED: c₄ = 0 is not a numerical discovery but a **construction-level
identity**. The stage pairing with a LINEAR displacement,
A(ε) = (ψ − εu)ᵀW(ψ + εu), is a polynomial of degree exactly 2 in ε
(verified symbolically: every coefficient beyond ε² is identically zero).
Nothing in this model can produce an ε⁴ term; "c₆ = 0 too" is the same
tautology. The "norm-ratio prediction c₄/c₂ ≈ −0.165" describes the
NORMALIZED amplitude A/‖ψ_ε‖² — a different observable, whose ε⁴ tail is
generated by the denominator; the unnormalized pairing was never going to
have one.

## What the banked record already says against "purely quadratic, no tower"

On the ALGEBRAIC face the tower is REAL and exact: B599-ALG's own output has
Im orders **[2, 3, 4, 5, 6, 8]** at m = 4 (and [2, 3, 4] / [2, 4] at m = 8)
— nonzero t³, t⁴, ... coefficients, computed exactly. The difference is the
deformation model: the algebraic side twists by exp(t·v_m) (nonlinear in t —
the honest character-variety motion), the stage model displaces linearly.
The stage model is the QUADRATIC TRUNCATION of the exponential motion, not
evidence that higher orders vanish in nature.

## The synthesis (what survives, and it is worth keeping)

Chat-1's structural instinct survives in a corrected form: on the algebraic
face, ALL t-coefficients of a given word/slot are generated by the SINGLE
element v_m through exponentiation — the tower is determined by (v_m, word),
not a source of independent parameters. So "the chirality information lives
in one object per direction" is right; "the ε⁴ terms are exactly zero" is
wrong (they are exactly DETERMINED). The campaign consequence stands as P2
concluded: the matching happens at the ε²/t² level, where both faces are
defined and exact; the higher algebraic orders are the exponential unrolling
of the same generator, available as consistency checks (a good P3 bonus
gate: the t³/t² ratio is fixed by v_m — checkable).

## Action

No bank of "purely quadratic" as a law. P3's matching proceeds at order 2
with the tower as a consistency resource. Chat-1's shift of focus to the
INTERNAL structure of M_odd (eigenvalues, phases, stage/level dependence) is
endorsed — that is where the campaign was already headed.
