# B1030 — FINDINGS: the price-lock verification (the trit adopted, with its exact cost)

**Seat:** cc · **Date:** 2026-08-11 · Verification arc (incoming-results protocol: cc3's
§7 price-lock, `price_lock.py` in the audit clone). No outcome-prior to protect — the arc's
content IS the recomputation. Compute: `b1030_verify.py` (this tree; independent routes, not
a rerun). Lineage: B1025 (the retype), B936/B782 (the bits), B897 (the banked 9-blocks).

## V1 — the two bits: CONFIRMED by formula

H¹(⟨τ⟩, 𝔽₂⁶) computed by the closed permutation-module formula (Z¹ = 2^(f+p), B¹ = 2^p,
H¹ = 2^f with f = 2 fixed nodes, p = 2 swapped pairs) = **(ℤ/2)² — agreeing with cc3's
brute force** and with banked B936. The carriers are the two τ-fixed nodes, distinguishable
by valence (3 vs 1): the labelling is canonical. B782's rank-3-minus-A7 arithmetic cited,
not recomputed (its arc is banked).

## V2 — the trit: CONFIRMED, and it is banked computation, not textbook data

The trinification blocks' singlet-bijection (each 9-block a singlet under exactly one
SU(3), the three blocks hitting the three factors bijectively) and the triality orbit
(explicit factor-rotation permuting the blocks in one 3-cycle, order exactly 3) — both
verified by direct computation. **Strengthening cc3 did not cite: the three 9-blocks are
themselves a banked, sealed-before-compute computation (B897, two primes, identical
cross-prime signature)** — the trit's substrate is the record's own, not an import.
**The VEV acceptance therefore quantifies: one ℤ/3 label = log₂3 = 1.585 bits.**

## V3 — item 1 (one unit): a defect found, the claim unharmed

The in-place assertion `all(w % 1 == 0)` **cannot fail at length-weight 1** — the vacuity
control substitutes a *different* ledger (length = 2), so the control exercises the frame,
not the instance. Item 1 is a **type declaration** (the ledger's 5/6 split, correct and
useful) plus an unfailable consistency clause — not a theorem. MB12 in its subtlest form,
on the auditor's own instrument; reported back on the relay channel. The one-unit CLAIM
itself stands (it needs only the ledger's content, which matches the banked weight ledger).

## V4 — the adoption arithmetic, stated before anyone asks

Adopting the trit moves THE_CLAIM's floor to **one unit + two bits + one trit + one
acceptance (J)** — discrete input bits 3 + log₂3 = 4.585. B1028's conservative output
floor is 2 + log₂3 + 1 = **the same 4.585** (coincidence of floors: the endpoint class
and the trit orbit are unrelated log₂3's). Consequences, in the open:

- **The ledger's actual verdict is untouched**: retroactive designer freedom stays 0.000
  bits — inputs are hypotheses, not retroactive freedom (rule C-E).
- **B1028's "even-conflated +1.585" sentence does not survive adoption**: the conflated
  comparison lands at exactly 0.000 at the floor. That sentence was true of the
  pre-adoption typing and stays in B1028 as a record of its moment; it is not carried
  forward.
- Post-adoption, the compression case rests where it always actually rested: the
  conflation is a category error, the floor is a deliberate under-count, and every
  uncounted credit (the one-line anomaly cone, the STRUCTURAL triple, the 11-cell
  pattern) sits on the output side.

## Verdict: PROVED (verification affirmative; one defect reported; the trit adopted)

THE_CLAIM's hypothesis line updates to the quantified floor with this arc as the cite.
Gate 5 untouched; no measured quantity anywhere in this arc.
