# xB013 — PREREGISTRATION (sealed BEFORE any computation of this arc)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell runs.**

## P0

Hyperbolic geometry, fibrations and arithmetic over a commensurability class. Named mathematics.
No value, no generation count, no physics reading. Gate 5 absolute.

## The owner's thesis

> *"physics might be the product of both faces."*

xB012 established the two faces sit on **opposite sides of axiom A5**: the **Eisenstein** face
(`ℚ(√−3) → 2T → E₆`) is a **commensurability-class** property — re-derived there as the Bianchi
group's own reduction mod `(√−3)`, needing no knot and no cover — while the **golden** face
(`φ`, `ℚ(√5)`) is a property of the **fibration**, which the class does not have.

## What the record already has, and this arc must not rediscover

**B309, upgraded by B518 and restored by B1010: the κ-unification.** *"κ = tr[a,b], ONE COMMUTATOR
TRACE, FOUR FACES … `κ = 2` iff the cancellation completes ('nothing'); `κ − 2 = ω²` with
`|κ − 2| = 1`, the UNIT obstruction"*, with the real form **`κ = 2 + λ²`** (Fricke–Vogt). **κ is
already the product of both faces**: `2 + λ²` is golden, `κ − 2 = ω²` is Eisenstein.
**This arc re-derives κ (xB010) and does not re-claim it.**

## The question that is NOT answered

κ is computed **at m004**. Nothing establishes whether the *join* is a property of m004 or of
something wider — and, sharper:

> **Does the product of the two faces SELECT the object, where neither face alone can?**

xB007 proved no class invariant can point at a member (the Eisenstein face is blind by
construction), and xB012 proved A5 — an **axiom** — is what breaks the m003/m004 tie. If the
**intersection** of the two faces singles out m004, then the object is derived **from the two faces
instead of from A5**, and the owner's thesis is a selection principle rather than a slogan.

## The two faces, defined operationally BEFORE computing

* **EISENSTEIN face**: invariant trace field `ℚ(√−3)` — i.e. membership of m004's commensurability
  class. The population is xB007's **21** census members (tetrahedra ≤ 6, cutoff reported).
* **GOLDEN face**: the manifold **fibres** over `S¹` with a once-punctured-torus fibre whose
  monodromy has **|trace| = 3**, giving eigenvalues `φ^{±2}` — the golden root, and B425's
  dynamical side.

## Cells and two-outcome criteria — declared before running

| cell | question | outcome A | outcome B |
|---|---|---|---|
| **X1** | re-derive **κ**: `κ − 2 = ω²`, `\|κ − 2\| = 1`, `arg κ = ∓π/6`, and `κ = 2 + λ²` | reproduces B309/B518/B1010 exactly | fails — this arc's premise is wrong and it stops |
| **X2** | compute the **golden** face across the 21: which fibre as once-punctured-torus bundles, and with what monodromy trace and field | the table | — |
| **X3** | **the intersection**: which members carry **both** faces? | computed, with the count stated | — |
| **X4** | **THE DECISIVE CELL.** is m004 the **unique minimal** member of the intersection? | **YES** — the two faces **SELECT** the object, with no A5 and no knot-ness: a derivation replacing an axiom | **NO** — a tie or a tower survives, so the product of the faces **still does not select**, and the minimal both-faces object is a **PAIR or a FAMILY** |
| **X5** | price it, and say what it does and does not mean | — | — |

## Declared prior, so the arc can overrule it

**X4 outcome B**, and this seat expects the tie to survive: **m003 = `b+-LR` and m004 = `b++LR`
have the SAME monodromy word**, differing only by sign (xB007 proved it), and `|trace| = 3` for both
— so the golden face almost certainly does **not** break what A5 broke. **If so, the honest result
is that the owner's thesis is CONFIRMED IN FORM but does not yield selection: the minimal object
carrying both faces is a PAIR, `{m003, m004}`, not a member** — which is itself the strongest
statement yet that the programme's object is not a single manifold.

**The inconvenient outcome for the owner's framing, named in advance:** if X3 returns a whole tower
`(LR)ⁿ` rather than a thin set, then "both faces" is not a selection principle at all, merely a
description, and this arc says so in its headline rather than dressing a description as a
derivation.

**Discipline.** (i) No identification: that the golden face and the Eisenstein face are "the same"
or "multiply" in any physical sense is **not** claimed — E82's rule applies and the link graph must
be exhibited. (ii) A base rate is owed: fibring with `|trace| = 3` may be common in the class, and
its frequency is **reported**, not assumed rare. (iii) κ is **re-derived, not cited** (xB010).

## Scope

Census cutoff inherited from xB007 (21 members, ≤ 6 tetrahedra), stated not hidden. `creates_law`
false. Nothing reaches `CLAIMS.md`, F2 or Gate 5. No axiom amended.
