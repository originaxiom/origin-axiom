# PREREG — B769: THE T1 STRUCTURE — discrete frame or continuous angle? (SEALED)

cc banking seat, 2026-07-23, owner's GO. B766 left T1 (the c/θ-pairing axis) unmoved by
every banked involution and cc3's audit certified the door genuine — but T1's OWN
structure was never computed. It is now load-bearing twice over: the chain's last
unpriced discrete-vs-continuous fork, and the collapsed correspondence's pivot (two
continuous candidates compete for T1; the courier's reading "T1 = continuous tuning of
the c/θ interaction strength" is mathematically licensed ONLY if T1 has a continuous
part). Gate 5-Q; Gate 5; nothing to CLAIMS.

## The two formalizations (BOTH computed; declared before any computation)
- **(A) Group level:** T1 = the choice of V₄-FRAME — which unordered pair of the three
  nontrivial elements {c, θ, cθ} plays "the generators". Option space = the 3 frames;
  candidate structure group Aut(V₄) ≅ S₃.
- **(B) Representation level:** T1 = the RELATIVE POSITION of the c-eigensplitting and
  the θ-eigensplitting on the banked modules: (i) the u-derivative plane at the geometric
  point (2-dim: a relative angle), (ii) the SL(3) 8-coordinate trace space (θ: the 4+4
  sum/difference split, B48; c: the real structure/conjugation) — intersection dimensions
  and the Grassmannian relative position, exact.

## Cells (two-outcome each)
1. **The unmovedness THEOREM.** Prove (not observe): 𝕀's action on the V₄ legs is by
   conjugation in an abelian group ⇒ trivial ⇒ T1's fixedness under 𝕀 is FORCED — any
   instrument moving T1 must realize an OUTER automorphism of V₄ (non-central,
   non-abelian over the closing set). Upgrades B766's empirical row.
2. **(A) computed.** Which subgroup of S₃ is realized by banked object-native operations
   (𝕀, τ = −I central, γ's)? Expected from cell 1: the trivial subgroup — the full
   3-frame set is an unbroken choice. Two-outcome: TRIVIAL-REALIZED (T1's discrete part
   is a genuine 3-fold choice) / PARTIAL (some frames identified — report the quotient).
3. **(B) computed on module (i).** At the geometric point: the c-frame (Re/Im) vs the
   θ-frame (even/odd) on the derivative plane — compute the relative angle EXACTLY.
   Declared candidate outcome: ALIGNED (angle 0 — the geometric point canonically
   identifies the two frames on this module; T1's continuous part is GAUGE-FIXED at geo,
   which would itself explain the exact (1,1,0) flip-vector behind chord = c⊕θ) /
   OFFSET (a nonzero exact angle = a new invariant).
4. **(B) computed on module (ii).** The 8-space: dim(odd ∩ real), dim(even ∩ real),
   dim(odd ∩ imaginary), dim(even ∩ imaginary) at the geometric point — exact integers;
   the relative position class. Two-outcome: SPLIT-COMPATIBLE (the intersections are
   maximal — the two splittings commute; T1's continuous moduli collapse) /
   GENERIC-POSITION (nontrivial relative position — a continuous modulus EXISTS and its
   banked value is computed).
5. **The correspondence consequence (recorded, not adjudicated).** From 2–4: which
   candidate TYPES T1 can host — triadic-discrete (the 3 frames), continuous (the
   modulus, if any), or composite. The courier's Attention row and the vacant-θ rebuild
   inherit the constraint. Q2b: the sister shares every module (field-level) — the
   comparator statement is about class-level vs object-level structure, stated.
