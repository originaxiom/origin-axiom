# FINDINGS — B769: THE T1 STRUCTURE — discrete (a 3-frame torsor); the continuous reading dies at the geometric point

cc banking seat, 2026-07-23, owner's GO. Prereg c2d69536 (both formalizations declared
pre-compute). Gate 5-Q; nothing to CLAIMS.

## The verdicts

1. **The unmovedness THEOREM (upgrading B766's empirical row).** The closing group
   ⟨c, θ, γ₅⟩ ≅ (ℤ/2)³ is abelian, so its conjugation action on V₄ is trivial —
   **T1's fixedness under 𝕀 is FORCED**, verified by brute force over the group. Any
   instrument that moves T1 must realize an OUTER automorphism of V₄ (Aut(V₄) ≅ S₃) —
   i.e., must be non-central and non-abelian over the closing set. The door is now a
   precise target, not a mystery.
2. **Formalization (A): T1's discrete part is a genuine 3-element torsor.** The three
   V₄-frames (which pair of {c, θ, cθ} generates) are permuted by an S₃ of which the
   banked object-native operations realize ONLY the identity (𝕀 by the theorem; the
   amphichiral τ = −I central, per cc3's audit). TRIVIAL-REALIZED: the frame choice is
   unbroken — **T1 is a 3-fold discrete choice**.
3. **Formalization (B), the modules — the continuous candidate dies at the geometric
   point, in two stages.** (CORRECTED IN-CELL from the first draft's near-vacuity:)
   - On the 8-coordinate SL(3) trace space, the θ-odd coordinates **VANISH IDENTICALLY
     at the geometric point** (x1=x4, x2=x5, x3=x8, x6=x7, exactly, in ℚ(√−3)) — the
     geometric point is θ-FIXED as a point (consistent with the discrete-faithful rep
     being reversal-equivalent), so the relative-position question moves to the tangent
     space. ("Purely imaginary" was vacuously true of zeros — caught and restated; the
     vacuity class strikes even inside a running cell.)
   - On the tangent plane (the u-derivative at ω), the content is genuine (probe
     −5 + i√3: even = −5 along Re, odd = √3 along Im) and the θ-frame lies EXACTLY
     along the c-frame: **relative angle 0 — the geometric point gauge-fixes the
     continuous relative position**, and this alignment IS the mechanism behind the
     exact chord = c⊕θ flip-vector (both involutions negate the same line there).

## The composite: T1 = a 3-frame discrete torsor; no continuous modulus survives where the object lives.

## Consequences (recorded)
- **The chain**: C21 — T1's structure theorem (discrete 3-torsor; mover must be outer/S₃;
  the continuous position gauge-fixed at geo).
- **The correspondence**: the courier's "T1 = Attention (continuous tuning of the c/θ
  interaction strength)" is NOT mathematically licensed — the tuning parameter they
  reached for is gauge-fixed to alignment at the geometric point, and T1's genuine
  content is TRIADIC-discrete. The T1 slot wants a 3-valued phenomenological candidate;
  Attention (continuous per its own citations) cannot occupy it; combined with F3's kill
  (θ vacant), the candidate matrix now has TWO slots needing new occupants and one
  triadic constraint no candidate anticipated.
- Q2b: every computed structure is field/class-level (the sister shares the modules);
  the statement is about the closing architecture, not m004-specificity — stated.

Artifacts: compute.py + output.txt (with the in-cell correction). Lock:
tests/test_b769_t1.py (compute-grade).
