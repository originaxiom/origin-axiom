# B814 — PREREGISTRATION: Gate D — is the GKY hypothesis well-posed at E₆?

**Sealed before any computation.** cc banking seat, 2026-07-30. **Gate 5 absolute: this tests
whether a hypothesis is well-posed. It produces no physical value and nothing reaches `CLAIMS.md`.**

## 1. The question

An incoming probe plan would test the Gang–Kim–Yoon index identity on the programme's E₆-on-m004
structure. GKY carry an explicit hypothesis:

> *every irreducible component of `X^irr(M)` is of dimension 1*

The plan schedules this check for **week 2**. **It is answerable from banked data**, and it is the
cheapest of its four gates. If the hypothesis fails at E₆, the identity has no well-posed statement
there and a month-long probe ends on day one.

## 2. The banked inputs (not recomputed here; cited and re-read)

| source | content |
|---|---|
| **CLAIMS E14** | `dim H¹(4₁, Sym^{2m}) = 1` per E₆ exponent — **six blocks** |
| **B575** | `dim H¹(V_{2m}) = dim H²(V_{2m}) = 1` for all six; *"the variety continues past the principal point in all six directions"* |
| **B370** | all six E₆ tangent directions **unobstructed at third order** |

## 3. Two-outcome, fixed now

- **HYPOTHESIS FAILS AT E₆** — the six exponent blocks assemble into **one** tangent space of
  dimension 6 at the principal point, so the component through it has dimension > 1.
- **HYPOTHESIS SURVIVES** — the six blocks are **six separate components of dimension 1 each**
  (which *satisfies* GKY), **or** the tangent is 1-dimensional, **or** higher-order obstructions cut
  the component to dimension 1.

**The survival branch is the one that must be actively checked, not assumed away.** Six 1-dimensional
components is a perfectly good reading of "six blocks each with `dim H¹ = 1`", and it would leave
GKY intact. This arc fails if that possibility is not addressed head-on.

## 4. The limit, declared before the run

**`dim tangent = 6` does not by itself prove `dim component > 1`** — a singular point can carry a
tangent larger than its component. What upgrades the evidence is B370's **unobstructedness to third
order**: deformations that actually extend. And B370 states its own limit — *"conditional by nature
(higher orders untested — no 'smooth, period' claim)"*.

> **The honest ceiling is STRONG EVIDENCE, not proof.** Written here so the result cannot later be
> read as more.

## 5. What would make this arc a failure

- Reporting `dim tangent = 6` as **proof** the component is 6-dimensional.
- **Not addressing the six-separate-1-dimensional-components reading**, which would leave GKY intact.
- Concluding anything about the probe's other three gates (brane construction, prior art,
  normalisation) from this one.
- Letting any outcome touch `CLAIMS.md` or be stated as a physics result.

## 6. Pre-stated expectation

I expect **HYPOTHESIS FAILS** — that the six blocks are the six θ-graded pieces of a *single* E₆
deformation space, not six separate varieties. **If they are separable, I am wrong and Gate D does
not close the probe.**
