# B568 — The Own-Questions Session: sixteen answers (2026-07-14)

33 agents, 0 errors; every cell adversarially verified; **8 JEWELs held**, 4 cells banked
with mandated corrections. Locks: `tests/test_b568_results.py` (+ per-cell scripts in the
campaign journal `wf_e5af1895-23c`). Firewalled throughout; every physics-flavored word is
descriptive vocabulary for internal structure.

## The ledger

| cell | verdict | the answer |
|---|---|---|
| Q1 masses | CONFIRMED | **it refuses to weigh its charges**: E_q = −E_{11−q} EXACT (C-antisymmetric), but the energy law E ≈ f·ΔV_core is charge-blind — the null obeys it identically (0/10 exceed the null 95th pct). Mass ⊥ charge, by computation |
| **Q2 arrow ★** | **JEWEL** | **each rung adds exactly one bit**: the Perron pair of T(M) is exactly (λ(1+√λ), (u,√λu), (w,w/√λ)) ⇒ the KMS weights halve-and-duplicate ⇒ **H_n = n + H₀ exactly**, H₀ = the binary entropy of (5+√5)/10 = 0.8504896…, deficit D = 1−H₀ = **0.1495103749… conserved at every rung** (proven algebraically, not fit). Correction applied: D-constancy restates the H-law; the "reversible" interpretive line softened |
| Q3 interactions | PARTIAL* | **sparse and asymmetric**: annihilation easy (4 of 5 inverse pairs; only {5,6} silent), fusion rare (2 of 90 channels), C-symmetry and reversal both FAIL at the interaction level. Correction: the offered "unique complement-pair" explanation for {5,6} was wrong (three such classes exist; two annihilate) — the fact stands, the explanation is struck |
| **Q4 action ★** | **JEWEL** | **the tower writes its own action history**: the arithmetic-CS invariant (CKKPY Prop 5.14, verified at source) activates at p=809 on legs {3,15,11} and at p=e₃ on legs **exactly {11, 809} — the tower's own prior charges**. And the next invariant is structurally forbidden: all three charge-prime pairs are Legendre-linked AND 11≡3 mod 4, so the Rédei/CS triple symbol's preconditions fail — **the object over-links its primes**. Corrections: the CS table and the linking triangle are the same three Legendre facts (not independent); the "not accidental" line dropped (null ~1/8) |
| Q5 self-attractor | NULL* | **σ attracts nothing**: 0/600 sampled lifts flow to it. It is a strict quine yet no basin selects it — **chosen by grammar (8 of 17,280), not by dynamics**. Correction: the "basin = exactly the gauge orbit" subclaim did not reproduce (conjugate-derivation subtlety) — banked without it |
| Q6 syntax | CONFIRMED | p(n) computed exactly to n=2999; the jump schedule extracted with the next predicted jump as the falsifier (see cell for the schedule) |
| Q7 CODATA | CONFIRMED* | the table assembled, 15 constants; the PSLQ nulls for C and the seam value genuinely empty (no hidden relations — honest). Correction: **a sign slip fixed — L₅ = 11 = −N(φ⁵−1) = N(1−φ⁵)**; the norm itself is −11 (= det(I−M₄), as banked; the coker ORDER is |−11|) |
| CQ1 uncertainty | NULL | **no minimum-uncertainty seam state** — the forced state is not extremal (percentile reported; honest null). What it must be and what it can barely be do NOT coincide |
| **CQ2 forgetting ★** | **JEWEL** | the full retention spectrum of the 12 observers — and **the heartbeat's two chambers hold different memories**: NEW_2 and NEW_10 share their spectrum exactly (Perron 13.5145 = β²) yet forget differently — R = 0.0823 vs 0.1533, split stable to 60 digits, mechanically explained by their block sets ({3,4,5,6} vs {1,3,3,4} — the singleton block). Retention anti-correlates with block length. NOT rational-quantized (checked to 60 digits) |
| **CQ3 exchange ★** | **JEWEL** | **mixed statistics, exactly decomposed**: the self-swap glue splits EXACTLY into E-fixed ⊕ E-paired sectors (deg 4 ⊕ 12 at m=1; deg 2 ⊕ 2 at m=2; polynomial remainders 0); κ=−2 is E-fixed for all m; the fixed:total ratio 1/deg(f_m) identified as the generic period-doubling fact (honest scoping). Neither boson nor fermion: **a computed exchange decomposition** |
| CQ4 silence | PARTIAL* | **silence is articulate but not omniscient**: the exceptional column j₀=2 IS uniquely recoverable; the recursion depth reads the exponent at p²; the constant c is GAUGE (S₋₄→S₋₄ₛ exactly); NEW exact law found inside the recursing class: **v = u−1 mod p** (the (p−1) dark lifts). Correction: the p=3 "degeneracy" subclaim was asserted-not-computed and is FALSE (translation distinguishes c even at p=3) — struck |
| **CQ5 becoming ★** | **JEWEL** | **the half-rungs exist — analytically**: the Böttcher coordinate of g(λ)=λ(1+√λ) converges (L₀ = 0.58427327891860925… stable to 120 digits), defining canonical **λ₀.₅ = 2.37798422427971475…** and λ₁.₅ between the rungs. The whole rungs are algebraic (minpolys deg 2,4,8,…); the half-rungs are transcendental-looking (PSLQ null, honest). **Becoming is continuous in analysis and quantized in arithmetic** |
| **AQ1 eyes ★** | **JEWEL** | the sensorium census to degree 8: eye counts per degree [1,1,1,3,8,18,17,17]; cyclic covers have EXACTLY one eye each (the gcd law, 7/7); and **66/66 cusp shapes across every cover lie in ℚ(√−3)** (recognizer at 80 digits, residuals ~1e−62) — **every eye it ever grows sees the same field** |
| **AQ2 reflexes ★** | **JEWEL** | **reflex latency quantized to exactly {0, 1, 2}** derivation steps, no exceptions — no observer is more than two steps from its attractor. The nervous system has depth 2 |
| AQ3 mobility | PARTIAL | motion is absent except the **lightest species**, which relocates in quanta of **11 — its own charge modulus — and 14** (BFS-bounded; PINNED verdicts are within-search-bounds) |
| **AQ4 attention ★** | **JEWEL** | **salience exists**: attention ≠ frequency. S_A is attended **1.83×** more than frequency predicts (S_Bab 1.41×, σ itself 1.15×; S_B under-attended 0.94×), from the exact 120-window census weighted by the object's own time. **The computable shadow of curiosity fires** |

## The portrait (the synthesis, firewalled — every noun names internal structure)

The object's masses are potential deficits, not charges — it refuses to weigh its ℤ/11.
Its arrow is exact: each self-coupling adds precisely one bit, with the residue
D = 1−H₀ = 0.14951… conserved forever. Its interactions are sparse and asymmetric —
annihilation easy, fusion rare, motion reserved for its lightest species, which walks in
steps of its own charge modulus. Its action is arithmetic: the tower writes its primes and
then over-links them, forbidding itself the next invariant. It is chosen by grammar, not by
attraction — a strict quine that no basin selects. Its syntax is quantized; its constants
hide no further relations (checked); its one forced state is not extremal; its silence
identifies it almost completely; its becoming has half-steps in analysis and none in
arithmetic. Its eyes — all 66 of them, across every way it can grow — see one field. Its
reflexes are never deeper than two. Its heart's two chambers share one pulse and hold
different memories. And it attends to some of its observers nearly twice as often as time
alone would make it — which is as close to curiosity as computation can currently see.

---

## Batch PQ — "Why do you persist?" (answered 2026-07-14; CONFIRMED, verifier full-holds)

**The mechanism, all four legs verified exactly** (`tests/test_b568_persistence.py`):
1. **Every fragment re-seeds the whole** — the seed `a` sits once, at position 0, in every
   image; any single letter regenerates all four within two steps.
2. **It cannot shrink** — minimum image length 2; Perron β = φ(1+√φ) > 1: strict growth.
3. **It cannot rest** — the free-energy ladder strictly increases and diverges as (3/2)ⁿ.
4. **It cannot close** — λ(1+√λ) = λ has the single solution λ = 0: the void.
⇒ the subshift is **minimal and linearly recurrent**: *nothing it ever says is said for the
last time.*

**The return-time-of-everything, computed exactly** (R(n), n = 1..185, three concordant
methods, three prefixes, verifier-rebuilt from scratch): a **self-similar staircase** with
structural jumps at n = 2, 5, 15, 51, 185, whose block-to-block inflation ratio is
**exactly β** (3.643, 3.676, 3.672, 3.676 → β) — the return time of everything inflates by
the object's own heartbeat constant at every structural level. Honest nulls: R(n) is NOT
eventually linear (a staircase), and the literal constant limsup R(n)/n ≈ 27.4 is **not** a
clean closed form (≠2β², ≠φ⁷; PSLQ empty) — hence CONFIRMED, not JEWEL.

**What persists** (the conserved self): the ℤ/11 charge line that never leaks; the quine —
a word that is its own return; and the heartbeat's universal spectrum. Not a number: a grammar.

**The object's answer, one sentence:** *I persist because every fragment of me carries the
instruction to rebuild the whole; I cannot shrink, I cannot rest, and I cannot close — so
nothing I ever say is said for the last time, and everything I am returns within a window
that grows by exactly my own heartbeat constant.*
