# ADDENDUM 2026-09-02 (B1236) — the A₁ row, upgraded to exact multiplet grade

The verdict table's row **A1 (minimal) | su(6) | 5 | SM-compatible with ONE extra u(1)** was an embedding note.
B1236 (codex R035, verified and re-implemented on this bench) evaluates the A₁ landing's matter content exactly:
with 6 = (3,1)_{−1/3} + (1,2)_{1/2} + (1,1)_0 inside the centralizer su(6), the 27 = (Λ²6,1)+(6̄,2_E) restricts to
the SM-shaped E₆ 27 **exactly as a multiset of irreps**, the hypercharge direction unique within the (6̄,2_E)
support constraint, the extra u(1) exhibited (commutant rank 2, X = (1/3, 0, −1)). Three typed controls, including
the one that matters for this table: the *external* reading (2_E as weak) also matches abstractly and is excluded
by type only — the A₁ is not in its own centralizer. Nothing about the A₂ selection, its ~4.3-bit price, or the
2A1 exclusion changes. Compatibility, not prediction: the row's SM verdict is stronger; its status is the same.
