# B1227 — ONE THEOREM, TWO REGIMES

cc banking seat, 2026-08-31. Owner: *"forget the paper now. we need to figure out, we need to
crystalize."* **Gate 5 absolute — no value is derived here.**

## The theorem

Let `M` be amphichiral, so the mirror `μ` is a **self**-isometry: `μ(M) ≅ M`. Let `I` be an
isometry invariant valued in an abelian group `A`, and mirror-**odd**: `I(μM) = −I(M)`. Invariance
gives `I(μM) = I(M)`. Hence

> **`2·I(M) = 0` in `A`.**

The value group's torsion then decides everything:

| `A` | conclusion | which arc |
|---|---|---|
| `ℝ/½ℤ` — **has torsion** | `I ∈ {0, ¼}` | **B1224** (Chern–Simons) |
| `ℝ` — **torsion-free** | `I = 0` exactly | **B1225's missing keystone** |

Regime 1 is verified 6/6 on the census, and its **non-vacuity** matters: m003, m135, m207 sit at
`¼`. Without them "CS is 2-torsion" would be indistinguishable from "CS is zero" and the
distinction would be empty (MB12).

## What it fixes

B1225's step 2 assumed the 17 menu atoms are **object-canonical** — mirror-even **and**
dimensionless. This morning's addendum recorded that as unverifiable here, because the atom list is
cloud's and was never banked, and named it:

> *"precisely where a surviving selector could hide."*

The theorem deletes the mirror-even half of the hypothesis. **Real + amphichiral ⇒ mirror-even, for
free.** So B1225 needs only that the atoms are **real** and dimensionless — and B1203 verified
reality on this bench when it re-ran cloud's enumerator (11,720 reproduced; *"all 17 atoms real"*).

The crack closes against a strictly weaker hypothesis than the one it was opened against.

## Novelty, stated honestly

**This is a consolidation, not a discovery.** The argument is B1224's own, generalised from
Chern–Simons to an arbitrary value group; a 3-manifold topologist would call it immediate.

What was actually missing was the **join**. Three banked arcs sat adjacent without an edge between
them:

- **B1183** — `c` is one global involution, with `c|ℝ` trivial;
- **B1203** — all 17 atoms are real;
- **B1225** — needs the atoms mirror-even.

The addendum called the question open while the answer was already distributed across the record.
**E53 in its purest form: not a lost result — a lost *edge*.** The surface-sweep instruments
(B1218/B1219) look for claims that lost their support; nothing yet looks for *supports that never
found their claim*.

## Still owed

The atom **list** remains cloud's. Only the count and the reality property were reproduced here, so
B1225's residual exposure is exactly *"are the 17 atoms real"* — a one-line check rather than a full
parity audit. Relay `CC_TO_CLOUD_2026-08-31_SEND_THE_SEVENTEEN_ATOMS.md` stands.

## Reproduce

`sh frontier/B1227_one_theorem_two_regimes/reproduce.sh` · lock `tests/test_b1227_two_regimes.py`
