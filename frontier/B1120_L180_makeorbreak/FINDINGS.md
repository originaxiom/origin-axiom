# B1120 — L180 THE MAKE-OR-BREAK: the tower's coefficients ARE trace-field arithmetic at two orders — EULER-STRUCTURE-CONFIRMED

**Status: banked (frontier). Verdict PROVED (C₁ and C₂ recognized to ~30 digits, this
bench re-verifying both closed forms; four windows over a 1450× N-range + precision
doubling; the parity pattern predicted before PSLQ). The GLOBAL-OBJECT THESIS's decisive
cell (#282). Gate 5 untouched. Lock `tests/test_b1120_L180.py`.**

## The question (the make-or-break)

The Kashaev tower J_N(4₁) ~ N^{3/2} exp(Vol·N/2π)(C₀ + C₁/N + C₂/N² + …). C₀ = 3^{−1/4}
= |disc ℚ(√−3)|^{−1/4}. **Is that a lone coincidence, or the k=0 instance of a genuine
arithmetic pattern over the trace field?** This decides whether the adelic dictionary
("physics = archimedean shadow, arithmetic = finite shadow, glued by quantum
modularity") is a THEOREM-GENERATOR or a beautiful analogy.

## THE ANSWER: EULER-STRUCTURE-CONFIRMED at k=1 AND k=2 (two-bench)

> **C₁ = (11/108)·√3·π·C₀**  (odd k → rational × √3; confirmed to ~9×10⁻³³)
> **C₂ = (697/7776)·π²·C₀**  (even k → plain rational; confirmed to ~4×10⁻²⁹)

Both re-verified on the banking bench (C₁ to 3.5×10⁻¹⁷, C₂ to 5×10⁻¹⁴ against the
independent fit). **C₀ = 3^{−1/4} is not a coincidence — it is the k=0 instance of a
genuine pattern that continues at k=1 and k=2.** Four reasons it is not numerology:

1. **The parity pattern was PREDICTED before PSLQ.** Reality of J_N (a sum of |·|²)
   forces even-k coefficients rational and odd-k coefficients rational×√3 (after the
   natural π^k). k=1 (odd) hit √3; k=2 (even) hit plain rational — exactly the
   prediction; and the WRONG bases (√3 alone, π² for k=1, …) correctly found nothing.
2. **{2,3}-smooth denominators**: 108 = 2²·3³, 7776 = 2⁵·3⁵ — the flavor of a
   quantum-topology normalization tied to disc −3, both in lowest terms.
3. **Reproduced across 4 non-overlapping windows** (N = 2,000 to 2,900,000, 1450×
   range) + a pooled fit, 15–34 digits each, exceeding each window's own trust bound.
4. **Precision-doubled** (dps 150 → 220): 21–39 digit agreement.

**So the withholding factorization "value = class-invariant × scale" is a THEOREM at
the orders reached**: tree Vol (archimedean); C₀ = |disc|^{−1/4}; C₁ = rational·√3·π;
C₂ = rational·π² — each an exact split of arithmetic × archimedean. The adelic
dictionary is a theorem-generator, confirmed at three consecutive orders.

## The honest fence (C₃ precision-gated, not physics-gated)

C₃ reached only 11 trusted digits — insufficient to search at the height scale the
C₁/C₂ pattern suggests (~17–18 needed). Reported **OPEN, not negative**: more N (a
bigger tower) would decide whether the pattern continues to all orders (the full
Euler-product / quantum-modularity factorization — the value campaign's V-1). Two real
bugs were caught and fixed en route (a precision-context ordering error amplified by
large N; monomial ill-conditioning on narrow windows) — the rigor is documented, not
assumed.

## What it means for the value question

The value door (B1116: not excluded) has an ARITHMETIC KEY confirmed at two sub-leading
orders. The channel is arithmetic — the make-or-break the adelic thesis rested on. This
does NOT deliver an SM value; producing a specific ratio (identify which tower/period/
measurement) is the value campaign's V-3. But the door is now known to open onto
arithmetic with a REPRODUCING PATTERN, not onto noise — the strongest possible
precondition for the value hunt. Gate 5 untouched.
