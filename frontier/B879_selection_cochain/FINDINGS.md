# B879 — the cc3 selection-cochain harvest: six claims verified, banked per the seat's own reconciliation — C6 (the quantum amalgam wall + even-solo no-go) is the headline

cc banking seat, 2026-08-03, late night. Harvest of cc3's first originated campaign (packet
`OA_CC3_selection_cochain_campaign_2026-07-17.zip`, sha256 `e59df18a…`, 38 files, preserved
verbatim under `packet/`), processed with its own 2026-08-03 **reconciliation addendum** —
which this seat checked and confirms: the addendum's self-downgrades are accurate.
Mathematics scope; nothing to `CLAIMS.md`; Gate 5 untouched (never approached by cc3 either).

## 1. Verification map (every leg independent of the packet's own compute)

| claim | verdict | this seat's leg |
|---|---|---|
| **C1** collapse theorem | **VERIFIED** | own enumeration: 745 primitive classes (per-length exactly (1,2,3,6,9,18,30,56,99,186,335)); unit det(A−I) ⟺ tr = 3 over all 745 (det(A−I) = 2−tr, two lines); realized by exactly one class (RL) |
| **C2** deflation strata | **VERIFIED** | amphichiral = 53; prime-fundamental-discriminant set = exactly {5,13,17,29,37,53,173,229} (8 values; d prime, d ≡ 1 mod 4); joint with amphichirality = 11 |
| **C3** five-value set | **CORROBORATION, as reconciled** | the packet table's counts 188/153/249/147/8 confirmed; 10 random rows recomputed with main's own B238/B664 machinery at ≤ 2×10⁻¹⁵; banked as a complete-family (745, length ≤ 12) corroboration UNDER B665/B666's law — not a new law, not a pointwise-formula verification (the table has no shadow-class column; the addendum is right) |
| **C4** silver corollary | **VERIFIED** | own scan: exactly one entangled-amphichiral class — R²L² at d = 2 |
| **C5** field communality | **VERIFIED** | d = 5 realized by 16 classes incl. R⁴L⁴ (tr 18); \|tr_odd(R⁴L⁴)\| = 1/φ, real — the RL-nonuniqueness witness recomputed here (im ~ 4×10⁻¹⁶) |
| **C6** quantum amalgam wall + even-solo no-go | **VERIFIED — the headline** | their reproducer rerun here live: h¹(D) primary [2,0,0,6], trivial-boundary [5,6,10,17] at κ = 4..7 — κ = 5 gives 0/6 vs classical 5, the mismatch as claimed and κ = 5 not singular in the primary series; stored results match the rerun; **the no-go lemma verified by hand in one line** (A⁻¹−I = −A⁻¹(A−I) ⟹ ker equal ⟹ the model's solo term is always even ⟹ the classical 2+3 split is structurally unreachable in this model class) |

Also verified: silver \|tr_odd\| = 1 exactly, real — the packet's gate row.

## 2. What is banked, in the seat's own proposed structure

- **C6** as the standalone result: deformation-type quantum-cochain targets are dead (Ocneanu
  rigidity + Maschke, as packeted), and the mapping-torus/amalgam model **cannot** reproduce
  the classical 2+3 cochain split — its solo term is even, identically. A genuine no-go with
  a one-line lemma at its core. The weld's linear stage action carries no image of the
  classical cochain structure.
- **C1+C2** as the selection statement: three banked criteria are ONE criterion (trace
  minimality), with amphichirality / field-cleanliness / landscape-minimality as corroborating
  strata that do NOT jointly cut to {RL} without it.
- **C3** as a one-line corroboration under B665/B666 (recorded here; no new law row).
- **C4/C5** as stated.

## 3. Carried forward

1. **The L91 amendment** the packet proposes ("five independent criteria" → "trace minimality
   selects; the strata corroborate") — a LAW_MAP/OPEN_LEADS edit, listed for the next
   register pass, not silently applied tonight.
2. The pointwise mod-5 shadow-class column (would upgrade C3's corroboration toward the
   sealed conjecture) — priced small, unowned.
3. cc3's V5 (SnapPy geometric-amphichirality cross of the 53) — optional consistency pass.

## 4. Honest boundaries

- The packet's own prereg chain (PREREG_SCC cdd2cd39 → 84ba1c96, SEALS.txt) is preserved but
  not re-hashed against external copies; the seat's reconciliation was spot-checked (its C3
  self-downgrade is correct — the table indeed has no shadow-class column).
- C6's Ocneanu/Maschke legs are cited theory applied by the packet; this seat verified the
  computational legs (the h¹ table, the lemma) and the claim's scoping, not the cited
  rigidity theorems themselves.

`tests/test_b879_cochain.py`
