# W2a — BANKED: THE AMALGAM WALL (cc3 + sonnet agent, 2026-07-17; prereg
# addendum 84ba1c96; all controls exact; exact cyclotomic arithmetic)
Controls: bare-stage h0 = h1 = 0 at kappa 5 reproduced; W unitary exactly
at all four levels; eigenvalue orders 20/10 exact (Q(zeta_60) embedding).
RESULT: the Mayer-Vietoris/amalgam quantum cochain does NOT reproduce the
classical h^1(D) = 5 at kappa = 5: h1 = 0 (T-boundary convention) or 6
(trivial-boundary), neither tuned, neither 5. SEALED VERDICT: MISMATCH —
the wall banks. STRONGER (the no-go lemma): h1(M') = h1(M) identically
(ker(A-I) = ker(A^-1-I)), so the model's solo term is ALWAYS EVEN — the
classical 2+3 split (odd solo = 3) is unreachable by ANY boundary
convention in this model class, at any level. The wall is structural, not
numerical. Stage probe: kappa = 5 not singular among {4,5,6,7}.
CONSEQUENCE: the weld's linear action on the stage cannot see the
classical cochain structure at all (bare AND glued). Next candidates per
DESIGN_DA.md, now priced: tube-algebra Hochschild with weld-twisted
coefficients (needs F-symbols); the Eichler-Shimura/Gamma(5) route (L108).
Artifacts: w2a_amalgam.py, w2a_results.json, w2a_run.log. Repo untouched.
