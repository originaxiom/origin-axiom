==========================================================================
THE CORRECTION
==========================================================================
  L^2(Gamma_41\H^3) = L^2(Gamma_p\H^3, E_rho),  E_rho = E1 + E5 + E6 of RANKS 1, 5, 6.
  Weyl on a rank-m flat bundle counts m*W(T) eigenvalues. Those are generically SIMPLE:
  E_i has IRREDUCIBLE holonomy, so nothing forces a dim(V_i)-fold degeneracy. (A
  dim(V_i)-fold degeneracy would need a finite group acting on m004 itself -- but the
  degree-12 cover is NON-REGULAR, image order 1920 with point stabiliser 160, so there is
  no such deck action.)

      => sector i contributes  dim(V_i) * W(T)  DISTINCT eigenvalues to m004.

  Chat-1 wrote N_i(T) ~ W(T) "the same in every sector", obtained by dividing the
  with-multiplicity count dim(V_i)*W(T) by an assumed multiplicity dim(V_i). That
  multiplicity is not there.

  total = sum_i dim(V_i) W = 12 W = 0.03427836163  = Vol(m004)/(6 pi^2)? True

==========================================================================
CONSISTENCY ANCHOR (why the error hid)
==========================================================================
  V1 has dim 1, so BOTH readings give W(T) there -- and V1 is exactly the inherited
  parent spectrum, the one sector we can check against a known count. The readings
  only diverge at V5 and V6, which is where no independent count was available...

==========================================================================
THE DISCRIMINATOR: the bank's OWN screen retention
==========================================================================
  GATE9 outputs: screen retained V5 = 25, V6 = 24, against a hand-set cap of 24.

    sector    dim*W(12) [corrected]     W(12) [Chat-1]   observed
        V5                     24.7                4.9         25
        V6                     29.6                4.9         24

  V5: corrected predicts 24.7, observed 25 -- essentially exact.
  V6: corrected predicts 29.6, observed 24 == THE CAP. It was TRUNCATED, not complete.
  Chat-1's reading predicts 4.9 for both, i.e. a 5x over-retention. The data says the
  screen was ON budget, and that the cap is what broke the run.

==========================================================================
CHAT-1'S 'LIVE DEFECT' EVAPORATES
==========================================================================
  GATE5 requires 10 distinct per sector. Solve dim(V_i) W T^3 = 10:
    V5: T = 8.8797
    V6: T = 8.3561
  GATE9's sealed interval reaches 12 > both. => GATE 9 CAN DISCHARGE GATE 5.
  No widening to 15.5, no 2.03x cost, no re-preregistration needed.

  THE REAL BUG is the hand-set cap: V6 needs ~30 and the cap is 24, so V6 is
  truncated before confirmation ever runs; V5 trips the guard at 25 > 24.
  Replace the constant with a budget-derived cap (a stated multiple of dim(V_i)*W).

==========================================================================
RESTATED COMPLETENESS CRITERION
==========================================================================
  For sector s and confirmed interval [a,b]:
      mu_s = dim(V_s) * W * (b^3 - a^3),   z = (n_s - mu_s)/sqrt(mu_s)
  PASS |z| <= 2; FAIL-LOW => skipping; FAIL-HIGH => spurious survivors.
  Evaluated on CONFIRMED counts only, declared before confirmation runs.

    r <=       V1       V5       V6   m004 total
     6.0     0.62     3.09     3.70         7.40
     7.5     1.21     6.03     7.23        14.46
    10.0     2.86    14.28    17.14        34.28
    12.0     4.94    24.68    29.62        59.23

==========================================================================
APPLIED TO cc3's B792 SCAN
==========================================================================
  cc3 confirmed 4 two-height-stable dips below r = 5.9129
  corrected expectation (all sectors) = 7.086   z = -1.16
  |z| <= 2 => PASS, but on the SKIPPING side. Under Chat-1's counting the
  expectation would be 1.77 and z = 1.67
  -- the opposite side. So the factor decides which way cc3's scan is off.

==========================================================================
results_correction.json written
==========================================================================
