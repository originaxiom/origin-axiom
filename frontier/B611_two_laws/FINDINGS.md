# B611 — the two-law test: both registered predictions FAILED, and the failures are the findings

**Status: banked (frontier). Nothing to `CLAIMS.md`; no SM quantities.
Predictions registered in the script docstring before running; both
verdicts honored as sealed. Lock `tests/test_b611_two_laws.py` (fast,
~30 s). Third/fourth objects via the banked B134 amphichirality theorem:
RRLL (blocks (2,2), cyclic palindrome ⇒ AMPHICHIRAL, the silver metallic
bundle) and RRRL (blocks (3,1) ⇒ chiral).**

## P1 (amphichirality ⟺ conjugation-closure): FAIL as sealed — on a degenerate edge

RL and RRLL (both amphichiral): closed at ALL 15 scanned levels. RRL
(chiral): closed at NONE. RRRL (chiral): closed ONLY at κ = 4 — where
dim_odd = 1, and a single eigenvalue is "conjugation-closed" iff it is
real, which is no mirror pairing at all. The sealed prediction said
"never" and therefore FAILS; the four-object pattern above the
degenerate dimension is perfect. REFINED CANDIDATE (new, unsealed, for a
future prereg with a fifth object): amphichiral ⟺ closed at every level
with dim_odd ≥ 2; chiral ⟺ closed at no such level.

## P2 (the golden-exclusivity of unit-circle departure): FAIL — the candidate dissolves

NO object departs the unit circle at ANY level: **B_odd is unitary BY
CONSTRUCTION** ([C, W] = 0 and W unitary ⇒ the odd compression is W
restricted to an invariant subspace). B609's exploratory
"finite-order-off-golden" note conflated the LISTENER'S MATRIX ELEMENT
h₃ (|h₃| < 1 because u₃ is not an eigenvector) with the SPECTRUM. The
unit-modulus census was vacuous as a discriminator — an MB12 miss inside
an exploratory label (never sealed as a verdict cell; B609's row is
hereby superseded).

## What replaces it (verified exactly where cheap)

- **The golden eigenvalues are e^{±2πi/5} — fifth roots of unity**
  (locked: 2cosθ = 1/φ with a conjugate pair on the unit circle forces
  θ = 2π/5). With κ=7's 8th roots and κ=13's 28th-root angles: the
  hearing operator is a CLOCK at every level computed; the object's
  golden content lives in the LISTENER'S matrix elements (h₃, the
  hearing-law amplitudes) and the TRACE law — the coupling, not the
  operator's invariants. The observer-coupling thesis lands exactly
  here: the stage's spectrum is universal clockwork; what is golden is
  what a golden-basis listener extracts from it.
- The amphichirality signature lives in the spectrum's PAIRING (P1's
  surviving pattern), the field signature in the TRACE (B601, B610) —
  three separable layers now on record: pairing = chirality of the
  object; trace = its field; matrix elements = the listener's coupling.

## Supersession note

B609 FINDINGS' exploratory item 1 ("finite-order off golden / leaves the
unit circle at golden multiples") is SUPERSEDED by this arc's diagnosis;
its Phase-2 sealed values are untouched.
