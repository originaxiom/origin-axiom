# B127 — chirality, Fibonacci, arithmetic, and the object's proper name

Resolves the "threads 3 & 4 + Fibonacci" investigation (verify-don't-trust, in-sandbox). **The physics-bridge claim
returns a clean, multiply-confirmed negative** (the firewall `P007` confirmed from chirality + arithmetic); the
strongest survivor is the object's **proper name**.

`probe.py` verifies:
- **M-1** Fibonacci/Yang–Lee fusion algebra = the golden substitution matrix (`λ²−λ−1`, Perron `φ`).
- **M-2** the metallic bundles are the **CS=0 achiral locus** (SnapPy-guarded; census mix discriminates).
- **M-3** `expansion ⊥ unitary topological order`; **`λ_m<2` only for m=1** → only golden can be a quantum dimension.
- **M-4** the **arithmetic trichotomy**: fusion `ℚ(√(m²+4))` (real) vs manifold imaginary-quadratic vs braiding `ℚ(ζ5)`
  — disjoint (`ℚ(ζ3)∩ℚ(ζ5)=ℚ`).
- **the Fricke–Vogt dictionary** (`knowledge/K010`): `I=κ−2` vanishes on the commuting locus; `κ=2` periodic AC,
  `κ>2` Cantor (Damanik–Gorodetski horseshoe).
- central charges (Fibonacci `+14/5` vs Yang–Lee `−22/5`); the null scale test (K-E).
- the three BMR arithmetic classes named `{RL→ℚ(√−3), RRLL→ℚ(i), RRL→ℚ(√−7)}` (√−7 non-metallic).

```
python frontier/B127_chirality_arithmetic_naming/probe.py
python -m pytest tests/test_b127_chirality_arithmetic_naming.py -q
```

The CS=0 / BMR SnapPy recomputations **skip** when SnapPy is absent (the records + the pure-sympy facts always run).

**Tier.** MATH / number-theory + emergent (aperiodic-order) physics, firewalled from fundamental. The four kills
(K-A..K-E) are tombstoned; the naming is `knowledge/K010` + `philosophy/P008`; the thin Yang–Lee thread is
`speculations/S030` (DORMANT). Nothing to `CLAIMS.md`; P1–P16 and the wall untouched. See `FINDINGS.md`; ledger
**V116**.
