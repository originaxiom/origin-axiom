# xB018 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell of this arc's
`verification/` runs.**

## P0

Mapping-class combinatorics, once-punctured-torus bundles, Chern–Simons invariants. Named
mathematics. No value, no generation count, no physics reading, nothing to `CLAIMS.md`. Gate 5
absolute.

## The path

**Path C, owner-approved:** *are the A5 bit (m003 vs m004) and the A7 bit (LR vs RL) the same ℤ/2?*

## THE CONFIGURATION AXES — declared first, under the rule xB017 Addendum 4 earned

**xB017 produced four corrections in one day and three were unvaried axes, not wrong arithmetic.**
So this arc declares its axes before its cells, and its verdict will name every value left fixed.

| axis | values | this arc |
|---|---|---|
| **which A5** | (a) *"the word must be mixed"* (the table row in `THE_FRAMEWORK`); (b) *"the group is torsion-free"* = the manifold restriction (xB012's reading); (c) *"H₁ is torsion-free"* = knot-ness, the m003/m004 bit | **all three named; only (c) is a ℤ/2, and the arc says so** |
| **which A7** | (i) letter-swap `C` (L↔R); (ii) reversal `P` (read backwards); (iii) the abstract "order of the two records" | **all three varied** |
| **same-ness criterion** | equal as maps · same subgroup · same action on a named invariant · conjugate | **all four stated and tested separately** |
| **where the bits act** | the word · the bundle `b±±W` · the manifold · the invariants (`H₁`, `CS`, the based fixed-point polynomial, the trace field) | **all varied** |
| **the object** | the minimal word `LR` alone · longer words · the whole `b±±W` family | **all three** |
| ambient group | `SL(2,ℤ)` for the monodromy; `PSL`/`PGL` for the manifold | monodromy in `SL(2,ℤ)`; **held fixed, and named** |

## The sweep, done first (xB010's rule)

Read, not cited:

* **`THE_FRAMEWORK` Layer 0.** **A5's table row says *"the first mixed closure is torsion-free —
  forces the word to be MIXED, aAbB not aA."*** That is **not a ℤ/2** — it is a constraint. The
  m003/m004 reading is **xB012's**, and xB012 also called A5 *"the manifold restriction."*
  **The record uses "A5" for at least two distinct things, and Path C's question presupposes the
  third.** This arc names all three rather than silently picking one.
  **A7:** *"A1–A6 are symmetric under swapping the two records, so they fix the object only up to
  the order `LR` vs `RL`. That is one bit."*
* **B979 — A7 is load-bearing and NOT discharge-able.** **`LR` and `RL` ARE conjugate**, via the
  record-swap `P = [[0,1],[1,0]]`, so **every class invariant is blind to A7**. The **based**
  Möbius fixed-point polynomial is not: `τ²−τ−1` (roots **φ**, −1/φ) versus `τ²+τ−1`. And its
  convention note is itself the content: *"which order carries the golden polynomial flips [with
  naming] — that dependence IS the inserted bit."*
* **B1083 — the torsor is `K₄`**, typed as **two spendable bits — `C` (swap, chirality) and `P`
  (reversal, parity) — plus one unspendable structure** (positivity; the arrow was never a torsor
  bit). Also: `M² = RL` exactly, the one-tick object is Gieseking (non-orientable), **and
  orientability and amphichirality arrive in the same purchase at tick two.**
* **xB007** — knot-ness `det(φ_*−I) = ±1` is the one datum the character variety is blind to.
* **xB015 K6** — `CS(b+-W) − CS(b++W) = ¼` exactly, 494/494 words: **the A5(c) bit is the `−I` bit,
  and it is visible in the k-coupling.**
* **B128 M-C** — **the R↔L swap negates `CS` exactly** (block-reversal = mirror).
* **xB013** — m003 `= b+-LR` and m004 `= b++LR` **share the same word**, differing only by `−I`.

## The cells, with predictions and kill conditions

**C1 — the three A5s, separated.** State each reading and test which is a ℤ/2 at all.
*Prediction:* (a) and (b) are **constraints, not bits** — (b) holds for **both** m003 and m004, so
it cannot distinguish them. Only **(c)** is a ℤ/2. *Kill:* if (b) separates the sisters, xB012's
reading is the bit and this arc is posing the wrong question.

**C2 — does A7 change the object?** Compare `b++LR` with `b++RL`: volume, `H₁`, `CS`, isometry
type. *Prediction:* **identical — the same manifold** (B979: conjugate monodromies).

**C3 — does A5(c) change the object?** Compare `b++LR` with `b+-LR`. *Prediction:* **different
manifolds**, `H₁ = ℤ` versus `ℤ/5 ⊕ ℤ`.

**C4 — the decisive comparison.** If C2 and C3 hold as predicted then **A7 fixes the homeomorphism
type and A5(c) does not**, so **they are not the same ℤ/2** under the "same action" criterion —
and the arc must then say under which of the four criteria, if any, they *could* still coincide.
*Kill:* if A7 changes the manifold, or A5(c) does not, the whole comparison is rebuilt.

**C5 — the action on invariants, both bits, three invariants.** `CS`, `H₁`, and B979's **based**
fixed-point polynomial. *Predictions:* A7 **negates `CS`** (B128 M-C) and so acts **trivially on
m004**, whose `CS = 0`; A5(c) **shifts `CS` by ¼** (xB015 K6) and so acts **non-trivially** there.
A5(c) does not touch the word, so it leaves the **based polynomial fixed** — while A7 is the only
thing that moves it. *This is the sharpest form of the answer if it holds.*

**C6 — `C` versus `P` on the minimal word (the cell I cannot call).** B1083 types the torsor as
`K₄ = ⟨C, P⟩`. On `LR`, letter-swap gives `RL` **and** reversal gives `RL`. **Do the two torsor bits
act identically on the minimal object, and differ only on longer words?** If so, **the `K₄`
collapses to a single ℤ/2 exactly at the object A6 selects**, which would explain why A7 is *"one
bit"* rather than two — and would be a genuine structural finding rather than a restatement.
*Declared prior:* I expect them to agree on `LR` and to differ on some longer word, but **I have not
checked, and the length at which they first differ is not something I can predict.**

**C7 — the group generated.** Acting on the family `{b±±W}`, compute `⟨A5(c), A7⟩`: is it `ℤ/2`
(same bit), `ℤ/2 × ℤ/2` (independent), or larger? *Prediction:* independent.

**C8 — the verdict**, naming every axis held fixed.

## What this arc will NOT claim

Not that either bit is a physical chirality or parity · not that the K₄'s collapse (if C6 finds one)
is a selection principle · no identification (E82/I-10) · no physics reading · nothing to
`CLAIMS.md`.

## The seat's prior, declared

C1: only (c) is a bit · C2: A7 fixes the manifold · C3: A5(c) changes it · **C4: NOT the same ℤ/2** ·
C5: the sharp form — **A7 negates `CS` and dies on m004's zero; A5(c) shifts by ¼ and survives** ·
**C6 I cannot call** · C7: independent · **C8: the two bits are different, and the interesting part
is not that they differ but WHERE each one is visible** — A7 only to a *based* invariant, A5(c) only
to the k-coupling. **Two bits, each invisible to the layer the other lives on.**
