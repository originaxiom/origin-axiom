# B457 — Ethogram EZ: the gates on the frozen catalog — H0a EARNED; the campaign closes

**Status: banked (frontier). Firewalled. The freeze verified (sha256 match); the two typed
gates run exactly as preregistered (B452, dry-run-validated before any observation). Verdict:
NO H1. H0a earned — and the ethogram banks as the object's behavioral map, the program's first
artifact organized by what the object DOES.**

## The gates

- **EZ-1 (word channel, licensed n=1001):** the closure lemma is correctly NON-specific
  (p₁ = 1.0 — every 2-generator system closes; the environment's property). The two "fires"
  (the 2-cycle values; the dynamics-disc (3,5)) are **label-specificity**: the mechanisms are
  generic (B448's controls), the object's numeric labels are its own — exactly the
  "object-specific value, generic mechanism" pattern adjudicated throughout. Per the frozen
  prereg, EZ-1 alone cannot make H1.
- **EZ-1 (knot/class channels, adjudication-only):** the shared/object-only table matches the
  source-probe adjudications (the identical-child and Lucas-launder signatures shared with 5₂ —
  it IS the same child; the component-cs-zero object-only — laundered via amphichirality + the
  CR structure, B458).
- **EZ-2 (numeric, the B398 machinery, binding p<0.01):** 29 numbers entered; **6 hits vs null
  mean 3.99 (p99 = 8): p = 0.168 — DOES NOT FIRE.** No correspondence with the frozen
  20-observable registry beyond chance — the B322 verdict, reconfirmed at the behavioral level.
- **Structural predicates (joint-only): 0 of 3 satisfied.** No object-specific multiplicity-3,
  no {8,3,1}, no definite CP (E5 proved the components carry exactly zero).

## The campaign verdict (three preregistered bins)

**H0a, EARNED** — at every gate, by demonstration: the behavior catalog is class-generic or
whitelist-laundered throughout. The Ethogram's deliverables stand regardless of the negative:
- **The behavioral map** (B456, frozen): 21 behaviors — the organism reproduces exactly once,
  without heredity, and its offspring cannot even be assigned a parent (the identical child);
  its word-growth can never leave its birth arithmetic (by lemma); it beats instead of choosing
  (four exact pulses); it has no homeostasis because it has no dissipation (the card retired by
  computation); its response is flat to third order and its finite response is priced beyond
  budget (the quantified engine boundary); its components carry exactly nothing (E5); its
  selection rules are the subfield lattice of its own field (B459).
- **The dictionary with its retirements** (S055): the full ethological frame worked as designed —
  the mappings that failed, failed honestly and taught disanalogies (no heredity ⇒ no evolution;
  no dissipation ⇒ no homeostasis; no death — evolution ends only by exhaustion of novelty,
  which the closure lemma proved for the word channel).
- **The handoffs' three tests, all closed**: Test 1 (component content) = zeros for known
  reasons; Test 2 (Hessian) = the banked zero + the priced boundary; Test 3 (CP on V₁/V₂) =
  zero, with the control showing nonzero is generic. The escape hatches remain set aside, as
  both seats agreed.

## Reproduce
```
python3 gates.py     # verifies the freeze hash, runs both gates, prints the verdict
pytest ../../tests/test_b457.py
```
