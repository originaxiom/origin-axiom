# Registered WHILE the B377 full census runs (2026-07-03, late): the derived prediction

From the four local censuses (levels 3, 5, 9, 25 — computed this session, F_p exact):
doublets exist ONLY at the first powers (3: ord-4 doublet at 90°; 5: doublet at 36/108° by
σ-class); prime squares 9 and 25 host lines but NO doublets. Under the (line)⊗(doublet)
mechanism this DERIVES the 225-death: no doublet on either side ⇒ no sector in any pairing.
**PREDICTION for the in-flight full-multiplicity census: NO invariant 2-dim module at 225**
(the death is real and derived — branch (a)-refined of the relayed §2, not a census artifact).
KILL: any 2-dim hit refutes the (line)⊗(doublet) exhaustiveness at mixed levels.
