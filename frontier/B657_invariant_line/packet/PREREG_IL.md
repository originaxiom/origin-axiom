# THE INVARIANT-LINE CAMPAIGN — sealed prereg (cc2, 2026-07-17). House name:
# the INVARIANT-LINE sector (no physics label; Gate 5 absolute; K020 discipline).

W0a (the conflation, decided): reconstruct v0 = the holonomy-invariant line of the
27 (nullspace of {A27-I, B27-I}; HARD GATE dim = 1 = banked h0). Compute v0's
weight-basis support; the Spin(10)xU(1) branching of the 27's weights (16(1) +
10(-2) + 1(4) by U(1) charge; identify the charge-4 singlet weight); THE DECISIVE
NUMBER = the h_pr-eigenvalue of the GUT-singlet weight (v0 is h_pr-null, so a
nonzero eigenvalue REFUTES chat-1's identification outright); the exact overlap
<v0, w_singlet>. Sealed outcomes: CONFLATION-REFUTED / IDENTIFIED / PARTIAL.
W0b (chat-1's unbanked one-per-block assertion): holonomy c principal SL(2) =>
27 = V17 + V9 + V1 as local systems; Fox h0/h1 PER BLOCK on the solo M. GATES:
sums reproduce banked (h0, h1) = (1, 3); the V1 block gives exactly (1, 1) (the
trivial system: h1 = b1 = 1). Outcomes: ONE-PER-BLOCK / the actual distribution.
W1 (THE PORTAL, golden): P(u) = [v0 x u(.)] with x = the Jordan cross product
27 (x) 27 -> 27-bar (polarization of the banked cubic norm: <x X y, z> = N(x,y,z);
convention pinned to cell3b_stage2's trilinear evaluator). Deliverables: the exact
matrix of P: H1(D; 27) -> H1(D; 27-bar) in class bases, its rank, and the
boundary/solo split of the image (B637's 3+2 table). Controls (sealed, all three
must pass): coboundary invariance in both slots; trivial-coefficient control;
exactness in Q(sqrt-3). Sealed outcomes: ZERO-PORTAL / RANK-r (components stated
exactly, equal-vs-asymmetric noted factually) / BOUNDARY-ONLY.
W2a (silver control): same pipeline on B649's banked exact 27-letters; K020 test =
FORM matches, VALUES differ; any FORM mismatch banked honestly as knot-specific.
W2b (repair note to chat-1): Targets B/C category errors (B650 wall citation) +
the banked stage-side sector laws as the true analogue + naming discipline.
Adaptive protocol: redirect note at each wave's end; tactical freedom in-wave.
