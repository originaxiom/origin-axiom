# W1 ADDENDUM (sealed before the portal run; folds in wave-0 outcomes)
W0a: CONFLATION-REFUTED — findings language = the invariant line (3-weight
h_pr-null combination), never "dark"/GUT-singlet. W0b: ONE-PER-BLOCK — the
five double classes inherit a block/provenance structure; the portal matrix is
reported with a per-source-class breakdown, and the boundary/solo split is
attempted from B637's table (else deferred to main-seat post-analysis, stated).
The portal P(u) = [v0 x u(.)] runs exactly as pre-sealed (prereg 9d8aa8ff):
Jordan cross product to 27-bar; the exact 5x5 class matrix; rank; the three
controls (coboundary, trivial-coefficient, exact field) are hard gates.
