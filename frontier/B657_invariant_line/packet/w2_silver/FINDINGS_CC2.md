# W2a — BANKED: THE SILVER CONTROL — FORM-MATCH DEEP, ONE FINE MISMATCH FLAGGED
# (cc2 + subagent, 2026-07-17; prereg 9d8aa8ff; 19/19 gates, exact in L)

FORM-MATCH (K020's FORCED candidates): the 3/5/1 grammar; the one-per-block table
REPRODUCED ENTRY FOR ENTRY ((0,1)/(0,1)/(1,1)); **v0_silver has the SAME shape as
golden's invariant line — three basis terms, coefficients (1, -1, 1), all
h_pr-null** (the invariant line's form is metallic-universal); the portal is a
RANK-5 ISOMORPHISM with kernel 0 (matches golden). N_silver rho-invariant on all
six generators; b1(D) = 1; trivial control = identity; coboundary controls exact.

FORM-MISMATCH (flagged for adjudication, not forced into agreement): golden's
portal was exactly block-diagonal on B637's boundary-born/solo-inherited split;
silver's portal is exactly UPPER-TRIANGULAR with no analogous zero block, and the
support-width grouping (2 classes on {a,b,c}; 1 on {b,e}; 2 on five letters) does
not align with golden's 2+3 composition. HONEST CAVEAT: the silver class basis
(stage-3a construction) has no canonical alignment with golden's provenance
labels — the mismatch may be basis choice rather than geometry. Deciding requires
a canonical boundary-restriction decomposition on the silver double (priced, one
cell). Until then: the SECTOR-RESPECTING property of the portal is NOT claimed
FORCED; the ISOMORPHISM property is (two objects, two fields, same rank/kernel).

HONEST GAP: silver's persisted assets carry the principal grading but not the
weight lattice, so no GUT-branching analog was computable (not needed — W0a's
refutation was decisive on golden; noted, not fabricated).
Artifacts: w2a_silver.py, silver_portal.json, w2a_run.log. Repo untouched.
