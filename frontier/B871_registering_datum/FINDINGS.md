# B871 — G5 closed at the group layer: "registerable" IS the existence of a B599-legal chirality measurement

cc banking seat, 2026-08-03. Mathematics scope; nothing to `CLAIMS.md`; Gate 5 untouched.
**Not preregistered** — the referee queue's keystone definition, paid with exact integer
computations on the cascade's actual menus.

## 1. The owed definition, paid

B861 fused the cascade under one principle but left one import: *"chirality-registering
measurement = a B599 pairing datum" is still owed.* Here is the datum:

| slot | content |
|---|---|
| bra | χ_R — the generation's formal character (irreps = basis of a free ℤ-module) |
| probe (insertion) | f_ρ = χ_ρ − χ_ρ̄ — **θ-odd** (the swap negates it), **dial-neutral** (built on stripped non-abelian labels) |
| contraction | the invariant orthonormality (Kronecker) pairing |
| **evaluation** | **A(R, ρ) = mult_ρ(R) − mult_ρ̄(R) ∈ ℤ** |

## 2. The theorem — four legs, verified on every cascade row

1. **It registers**: A(R̄, ρ) = −A(R, ρ) on all 8 menu rows — the evaluation is odd under
   the θ-class swap; a nonzero A hears which side it is on.
2. **B599 parity**: the even (self-conjugate-paired) part of χ_R contributes **0** to every A;
   the odd remainder carries the whole evaluation. Registering is a strictly odd-sector hearing —
   odd bra-part against odd probe, **n = 2, the B593 shape**: the minimal chirality measurement
   is second order, exactly where B599 says the first surviving configuration lives.
3. **The keystone equivalence**: (∃ρ with A ≠ 0) ⟺ B861's gate, row by row across all three
   steps — including both failures (Sp(8), SU(4)×U(1): every A = 0) and all six passes. The
   criterion can fail and can pass (MB12). **The SM registers with amplitude 2** at the (3̄,1)
   probe — the doubled anti-triplet.
4. **Blindness is an intertwiner where the gate fails**: the multiset pairing (Schur, per-irrep)
   realizes A ≡ 0; the cascade's banked explicit witness **J6 = ε on Λ²(4) reverified over ℤ**
   (J·ρ(X) + ρ(X)ᵀ·J = 0 for all 15 sl(4) generators — B860's exhibited side).

## 3. What this closes

The fused principle is now machinery end to end at the group layer: **"registerable" =
"a B599-legal measurement of chirality exists," and the cascade's gate is that existence
condition** — not an imported definition. With B863 (termination), B864 (dial/gauge split),
B869 (false-positive control), B870 (lift), the selection spine's imports reduce to **P5 (menu
completeness) alone**.

## 4. Honest boundaries

- **The stage-internal refinement remains**: this arc works at the group-character layer with a
  formal invariant pairing; realizing the probe *inside* the SU(3)₂ modular stage (state vectors
  and actual contractions, as B599's theorem is phrased there) is the remaining refinement. The
  identification stage-Θ = θ-class rests on the banked grading (B599's C-centrality + B868's
  jurisdictions), not recomputed here.
- The probe is dial-neutral **by construction** (stripped labels); which dial-charged probes the
  observer additionally owns is B864's ledger, untouched.
- Nothing here reaches values or generation count. Gate 5 untouched.

`tests/test_b871_registering.py`
