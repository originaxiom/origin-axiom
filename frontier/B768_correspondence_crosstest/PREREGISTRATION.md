# PREREG — B768: THE CORRESPONDENCE CROSS-TEST — rules sealed BEFORE the matrix arrives (SEALED)

cc banking seat, 2026-07-23. The owner's redirection: cross-test phenomenological
correspondences instead of eliciting them; the courier seat is already assembling the
phenomenology side. THIS SEAL EXISTS TO PREDATE THAT PACKAGE (the B751/B752 pattern:
rules before claims). Gate 5-Q in full (Q5 absolute: structural correspondence only,
never consciousness claims; Q6 the E20 budget explicit below). Gate 5; nothing to CLAIMS.

## Side A — the mathematical signature table (computed in-seat, this arc)
Each banked structure gets a signature vector, exact and cited:
(arity | symmetry acting | relations to other rows | invariances | the falsifier property)
for: the voice (1-channel rigidity), the self-name (uniqueness-with-twin-separation),
transparency (fiber-dim 0 at all ranks), integration (SL(3)-onset; the pair-separation
law), no-closure (inseparable ζ₅-orbit), the three bits (rank-3; the XOR relation;
γ₃≡c), the time=basepoint identity (one γ₅ bit, three appearances), the unmoved axis,
the Galois continuum.

## Side B — the admissibility bar for the incoming phenomenology matrix
A candidate category is admissible iff its structural claims are (a) LITERATURE-CITED to
named sources with the structural property locatable (not paraphrase); (b) stated as
matchable signature components (arity/symmetry/relations), not vibes; (c) accompanied by
the category's own falsifier (what structure would refute the pairing). Uncited or
unfalsifiable rows are quarantined to the S-room.

## The SEALED matching rules (fixed now, before any matrix)
1. A pairing SCORES only on signature components declared on BOTH sides before matching.
2. The E20 budget: with |A| = 9 structures and |B| = N categories, the null expectation
   for k-component agreements is computed EXPLICITLY (permutation null over the bipartite
   graph, 2000 shuffles); a pairing counts as CANDIDATE only above the null at its
   component count. The look-elsewhere denominator is 9·N — declared, not discovered.
3. THE DISCRIMINATORS OUTRANK THE FITS: rows 3 and 7 (transparency; time=basepoint) are
   pass/fail gates — a catalogue whose categories cannot host BOTH surprises fails
   wholesale regardless of its other scores (they are the two places the mathematics
   contradicts folk-intuition; fitting only the intuitive rows is the base-rate trap).
4. XOR-coherence: any triple of categories paired to (c, θ, chord) must satisfy the
   banked relation (chord = c⊕θ) ON THE PHENOMENOLOGICAL SIDE TOO (the literature must
   support the third being determined by the other two) — the sharpest structural test.
5. The owner adjudicates the surviving candidates (the choice stays the axiom); NOTHING
   from this arc enters the chain — survivors become P021-v3 rows with consequence forks.

## Two-outcome
MATCH-FOUND (≥1 candidate pairing survives the null + both discriminators + XOR-coherence)
/ NO-MATCH (the catalogue fails — itself informative: the banked structure is
phenomenologically novel, and P021 reverts to declaration WITH that fact on record).
