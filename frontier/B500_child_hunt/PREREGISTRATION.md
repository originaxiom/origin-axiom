# B500 — THE CHILD HUNT: search the word-space for x⁴−x−1 (prereg, committed before compute)
**Target (fixed now):** the child field ℚ[x]/(x⁴−x−1), d_K=−283, S₄. **Match = FIELD ISOMORPHISM**
(same maximal order; disc match alone is only a lead). Search: all words depth 4–5 rich in all three
verbs (W3 data: wildness has required F+M+D), via resultant-chain eliminants + Jacobian saturation +
exact factorization + polgalois; every S₄ quartic factor → compute d_K (round_two) → compare to −283;
isomorphism test only on disc-matches. Newton-cloud genuineness per factor; κ≠2 required (irreducible).
**Airlock (armed in advance):** ANY disc −283 or field-isomorphism hit = B398 event — STOP, convene all
seats, owner present; no interpretation before the statistics gate (LMFDB S₄-quartic base rate at
|d_K|≤ census max, thresholds per B499 W2). **Kill:** depth-5 exhaustion without a hit → "the child is
not a short word" (banked negative). Forced-and-unsought is satisfied by THIS prereg (target named
before any search). Firewalled.
