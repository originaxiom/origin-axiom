# B1028 — FINDINGS: the global freedom ledger

**Seat:** cc · **Date:** 2026-08-11 · **Prereg sealed e13d09a5 BEFORE compute** (in
`ARTIFACT_HASHES.txt`, seal pushed at 82e2ee7b). Campaign: THE WHY CAMPAIGN, Lane I-1.
Compute: `b1028_walk.py` — a **self-checking desk audit**: every 0-priced link opens the
cited arc's own `arc_verdict.json` and asserts its discriminating phrase before the cite is
usable (compute-the-discriminating-fact, applied to the record itself). Conservative
counting rules C-A…C-E declared at the top of the script, before any number.

## The question, answered

*How many bits of designer freedom did the entire chain consume, link by link, and how many
bits of structure does it emit against declared ambient classes?*

## §1 — the links: retroactive freedom = 0.000 bits, with every zero cited

Eleven arrows, none skipped: A1–A6→object (B14 uniqueness, the leftover sign = A7, an
*input*); object-selection within its own family (B997 — the **infinite** metallic family
scanned as a control, unique hit; FLAG: the family declaration is the residual soft spot,
priced 0 because no live alternative family was ever on the table); object→κ→ℚ(√−3)
(B1010 lineage); ℚ(√−3)→2T (B266 — the ramified prime selects; **no door criterion was
chosen**: B1019, the siblings have no door at all); 2T→E₆ (McKay bijection); carrier = 27
(**conditional on the chirality input**, which is declared and allowed to work: Sym²(27)
has no invariant, Sym³ exactly one — B884; 27↔27̄ identified by the object's own
θ-symmetry; frame "inherited not chosen"); the cascade rule (B994); termination (B863;
FLAG: the P5 branching menu is a mathematical *import*, stated not hidden); the generation
reading (B897); faces (none enter the structural chain — face choices exist only in the
value-layer crossing lane, whose spent bits live in **its** ledger, a separate book).

**The seal's two named risks both RESOLVE on the banked record:**

1. **The cascade rule** — the assumed half ("maximal residual symmetry") **does not do the
   landing**; registerability, the *derived* half, does — and all six registerable
   selection functions land at the SM (B994). Path freedom log₂6 = 2.585 bits exists and
   affects no output.
2. **The generation link** — the cell was **sealed before compute** (prereg e293f095,
   criterion verbatim), so its retroactive freedom is 0 *by construction*; its grade debt
   is paid on the output side instead (rule C-A: its output is excluded until the Lane II
   upgrade).

## §2 — the inputs (hypotheses — published, typed, separate from freedom)

A1–A6 (motivated-not-laws; prices audited B1003: five robust, two fragile) · A7 = 1 bit ·
arrow = 1 bit · chirality = 1 bit · one unit · two acceptances (J = accept-the-nomination;
the VEV act — the one-trit retype at log₂3 is **relayed and not yet verified by this seat:
recorded pending, not adopted**). Total discrete input bits: **3**.

## §3 — the outputs, each against its ambient class declared in the row

| output | ambient class (declared) | bits |
|---|---|---|
| global form ℤ₆ | the 4 forms the SM admits, Γ∈{1,ℤ2,ℤ3,ℤ6} | **2.000** |
| endpoint algebra | ≥3 distinct a-priori terminals of the menu tree (floor, C-D) | **1.585** |
| mass-in-cubic, doublet-in-10 | cubic-vs-bare (coarse, C-C) | **1.000** |
| hypercharge direction | **continuum pin** — the anomaly cone is exactly one line (B864·B991) | noted, not counted (C-B) |
| generation triple | STRUCTURAL — excluded as a debt (C-A) | 0 |
| CS=0; no dimensionful | controls, not SM-facing bits | 0 |

**Conservative bits-out = 4.585 — a deliberate FLOOR.**

## §4 — VERDICT: COMPRESSION (the seal's first outcome)

- **Retroactive designer freedom: 0.000 bits** — every link banked-forced with its forcing
  arc cited and phrase-checked; the soft spots are flags in rows, not prices.
- **Conservative output floor: 4.585 bits**, excluding the continuum pin, the STRUCTURAL
  triple, and the 11-cell Yukawa pattern.
- **The both-ways honest comparison:** even if a skeptic charges the 3 *declared* discrete
  input bits against the output floor — a conflation of hypothesis with freedom, but grant
  it — the balance is 4.585 − 3 = **1.585 bits > 0** (the MB12 non-vacuity shape:
  outputs − consumed inputs > 0), with the continuum pin and every excluded structure all
  on the credit side.
- **What the coincidence hypothesis must now explain for free:** ≥ 4.585 bits of declared-
  class structure, plus a one-line anomaly cone, plus the sealed-before-compute generation
  signature — while the ledger shows zero retroactive bits available to buy them with.
- Declared prior (COMPRESSION, risk at two links): **HELD** — and both named risks
  resolved *against* the risk, by arcs already banked before this ledger ran.

**The stop rule does not fire** (that clause was THE ADDRESS's). Lanes II–III proceed.
The margin is stated as what it is: a small, clean, conservative floor — the campaign's
claim "cannot be coincidence" is now a *number with declared classes*, not a feeling.

Gate 5 untouched. No SM value appears anywhere in this arc.
