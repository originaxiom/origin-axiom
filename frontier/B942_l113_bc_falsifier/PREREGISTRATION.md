# B942 PREREGISTRATION — L113: THE BC/CMR FALSIFIER (sealed before compute)

**Date sealed:** 2026-08-07 · **Seat:** cc (banking) · **Register:** MASTERPLAN v2
Phase III item 3, *"scheduled EARLY, deliberately: the sharpest thing that could kill
the observer construction runs before anything is built higher on it."*
**Lane:** MATHEMATICS. Gate 5 untouched; nothing to CLAIMS.

## The question (B851 §3, verbatim — the successor question that arc isolated)

> **Is the programme's β=1 system actually a BC/CMR-type system for K = ℚ(√−3)?**

B849's verdict against the chirality-as-Galois-label reading rested on two steps.
B851 confirmed step one (the CMR symmetry group is I_K/K\* ≅ Gal(K^ab/K), which fixes
K pointwise; the string "complex conjugation" occurs zero times in the CMR paper).
**Step two — that the programme's own system IS that system — was never demonstrated,
and is the single load-bearing assumption.** It is an in-repo question about our own
arc, not a literature question.

## THE KILL CONDITION (MASTERPLAN v2, locked — may not be moved, only executed)

> **L113 YES** → the observer construction's foundation fails; Phase III restructures
> around the wreckage, honestly.

This preregistration exists so that the seat cannot soften that on contact.

## The cells (each two-outcome, each computed in-sandbox, compute-not-cite)

**Cell 1 — the identification, from our own text.** Does B723 name the CMR system and
its symmetry group, or merely gesture at a thermal analogy? Decided by quotation from
`frontier/B723_build_the_observer/FINDINGS.md` alone. **YES** = B723 names the
Bost–Connes/CMR system over ℚ(√−3) *and* names Gal(K^ab/K) as the label group.
**NO** = the system is unnamed/other, and L113 resolves NO on different grounds.

**Cell 2 — the group-membership fact, COMPUTED not cited.** Is complex conjugation an
element of Gal(K^ab/K) for K = ℚ(√−3)? The discriminating computation: exhibit an
explicit element of K fixed by every σ ∈ Gal(K^ab/K) and moved by conjugation.
Prediction (disclosed): **NO, c ∉ Gal(K^ab/K)** — but it must be computed, not asserted.

**Cell 3 — the escape hatches, enumerated and each killed or left open.**
 (a) **Object-level route**: does m004's own Bianchi arithmetic carry the system instead
     (which would change the group)? B736 is banked NEGATIVE here; re-verified, not cited.
 (b) **Quotient route**: is there a *canonical* ℤ/2 quotient of Gal(K^ab/K) that could
     serve as the sheet label? Computed via quadratic extensions of K.
 (c) **Anti-automorphism route**: could conjugation act on the KMS states from outside
     the symmetry group (as an automorphism of the ambient tower rather than an element
     of the torsor group)? If YES this is a *repair*, not a rescue, and must be stated
     as such.
 (d) **Archimedean route**: does K have a real place whose ℤ/2 could be the sheet?

**Cell 4 — the level audit.** If chirality and values are BOTH assigned to the
Gal(K^ab/K) label, is that consistent? A free and transitive action makes the label set
≅ Gal(K^ab/K), a large profinite group; chirality is a ℤ/2 (B713). Compute whether one
label can carry both, and if not, name which level each belongs to.

## The two outcomes (fixed now)

- **OUTCOME YES** — the programme's β=1 system IS BC/CMR-type for K = ℚ(√−3). Then
  B849's verdict stands **unconditionally**, the chirality-as-Galois-label identification
  is refuted **at the level of group membership**, and **the kill condition executes**:
  the observer construction's chirality clause fails and must be retracted or repaired in
  the open. Any surviving clause (e.g. the values/torsor clause) must be stated
  separately and its survival argued, not assumed.
- **OUTCOME NO** — it is not that system. Then the level mismatch does not apply, and
  **B849 must be amended to say so** — its verdict becomes conditional on a system it
  cannot name.

**There is no third outcome.** "Unclear" is not available: if the computation cannot
decide, that is reported as an instrument failure and the lead stays OPEN with the
reason recorded — it does **not** default to the programme's favour.

## The disclosed prior

**OUTCOME YES, high.** B723's text appears (on a reading made before this seal) to name
both the CMR system and Gal(K^ab/K) explicitly, and B851 already verified the group
fact in the literature direction. The honest expectation is therefore that this arc
**executes a kill condition against the programme's own construction.** The prior is
disclosed so that a YES cannot later be presented as a surprise, and so that a NO — which
would be the *convenient* answer — is visibly the one that had to overcome a stated prior.

Scoreboard priced: eleven sealed cells before this one; five object-overrules, four
disciplined wins, one split, one gate held shut (B940).

## Files (after sealing)

`b942_cells.py` → `results.json`; `FINDINGS.md` verbatim against these criteria;
locks in `tests/test_b942_l113.py` (seal-integrity first, then one lock per cell,
then a lock that the kill condition's execution is recorded if YES).
