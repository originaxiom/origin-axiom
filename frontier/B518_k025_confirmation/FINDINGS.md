# B518 — Confirming K025 by computation: the three tiers (A universality, B the prediction, C the derivation)
**Computed 2026-07-11. Confirms K025's central reading (scale-free universality class + the anchor),
produces the one falsifiable external distinguisher (the mixed-chain law), and upgrades the root claim
from dictionary to derivation. Honest per the absorbing-loop caveat: A and C confirm STRUCTURE; only B
can ever confirm a physics CROSSING. Lock `tests/test_b518.py`.**

## Tier A — the scale-free universality class (CONFIRMED at the trace-map level)
κ−2 is a coupling-SQUARED distance from the classical locus in every 2-letter Fibonacci-substituted
transfer system: Schrödinger onsite (κ−2 = 4λ², B505), hopping (t_A/t_B), spin/Ising (K_A,K_B) — the
**same trace map T=(z,x,xz−y) preserves κ across all substrates** (substitution-universal), while the
physical MEANING of the coupling (hence the SCALE) is substrate-specific. That is a scale-free
universality class: one object (the trace map + κ), many realizations, scale set by the substrate.
K025's central claim confirmed. (Caveat: confirms the class, cannot distinguish "fundamental" from
"good effective description" — the absorbing loop.)

## Tier B — the falsifiable, lab-buildable prediction (the external distinguisher)
**The mixed-chain label law, sharpened to a binary claim that avoids the dense-module over-fit trap:**
| chain | dyadic ½-gap (TM signature) | golden 0.618-gap (Fib signature) |
|---|---|---|
| pure Fibonacci | **absent** | present |
| pure Thue–Morse | present | **absent** |
| **MIXED (Fib+TM directive)** | **present** | **present** |

A mixed-history aperiodic chain carries BOTH signatures, each absent in one pure chain. Presence/absence
of these two specific gaps is measurable on a spectrometer and **cannot be over-fit**. A lab builds three
photonic/polariton chains (Fib, TM, mixed) and checks the 2×2 table. **This is the one computation whose
confirmation would CROSS, not relocate, the firewall** — the external prediction K025 requires. (Rigorous
version needs the N-scaling protocol per B505; this is the clean testable core. The dense-module fit for
arbitrary labels is NOT claimed — only the two discriminating gaps.)

## Tier C — root dictionary → derivation (CONFIRMED, arithmetic backbone)
From the two letters R=[[1,1],[0,1]], L=[[1,0],[1,1]] of SL(2,ℤ), the four faces are now EXPLICIT
invariants of the one root, not "is a face of":
- **σ² = A** (the Fibonacci substitution's square IS the golden cat map [[2,1],[1,1]]);
- **PRODUCT** RL = A, trace 3, char x²−3x+1, disc 5 → ℚ(√5) → E₈ (dynamics end);
- **RATIO** −RL⁻¹, trace −1, char x²+x+1, disc −3 → ℚ(√−3) → E₆ (geometry end);
- **SEAM** = ℚ(√5)·ℚ(√−3) = ℚ(√−15), HCF = ℚ(√5,√−3) (B334, cited).
K025's root claim upgraded from dictionary to derivation for the arithmetic backbone.

## Verdict + what remains
A + C confirm K025's structure (real, unified, scale-free — publishable). B is the falsifiable core, and
by the absorbing-loop caveat it is the ONLY option that can confirm a physics crossing — it goes to a
specialist/lab. Open: B's N-scaling rigor; A2 (the full RG basin-of-attraction); and the write-up (P5 +
the mixed-chain experimental proposal). Firewalled; no value claimed; the "3d/space" reading stays P015/P016.
