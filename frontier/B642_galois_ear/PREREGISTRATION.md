# B642 — THE GALOIS-CONJUGATE EAR (prereg; the prediction SEALED before running)

**Chat-2's §VI prediction, sealed verbatim BEFORE the conjugate-stage
computation: at the Galois-conjugate stage (the √5 ↦ −√5 twist of
SU(3)₂'s modular data; the Yang–Lee-side root), the SAME construction
yields:**

- **P1:** the image group is again 2I × ℤ/3 (order 360, the same class
  equation);
- **P2:** the five absolute twist-frame tones with φ ↔ 1/φ swapped:
  {0, φ/2, 1/2, 1/(2φ), 1} — i.e., the SET is the same, the
  φ-members exchange multiplicities (72 at ±1/(2φ) ↔ 72 at ±φ/2
  stays symmetric — the sharp form: the tone of each CLASS swaps
  φ ↔ 1/φ);
- **P3 (the headline):** tr ρ(RL) = +φ (instead of −1/φ).

**Falsifiers (locked):** any group other than 2I × ℤ/3; any trace
other than +φ; a tone set differing from the Galois image.

**The reading if confirmed (firewalled to placement):** K020's
mechanism (form forced, value Galois-chosen) appearing in the ear —
which golden character is heard depends on which conjugate stage the
listener stands on; the two-ended (E₆-end, E₈-shadow) pairing gains
its second witness.

Implementation: the banked 80-dps builder with the Galois multiplier
k = 7 on all root-of-unity exponents (k ≡ 1 mod 3 fixes the twist ω;
k ≡ 2 mod 5 flips √5). MB12: P1–P3 each can fail independently.
