# B862 — novelty-scope addendum (2026-08-12, R43-8; the window handoff's prior-art find)

**Prior art:** Todorov 2018 (arXiv:1805.06739) derives the Standard Model gauge group as
the intersection of two maximal Borel–de Siebenthal subgroups of F₄; S(U(3)×U(2)) — the
ℤ₆ global form — appears there as a group-theoretic object.

**B862's novelty, scoped exactly:** not the EXISTENCE of the ℤ₆ form as a subgroup (prior
art has it), but (i) the **cascade deriving it** — the form arrives as the step-3 winner
of the object's measurement chain, conditional on exactly the chain's stated conditions,
and (ii) the **resolution claim**: the SM's own data leaves Γ ∈ {1, ℤ₂, ℤ₃, ℤ₆} ambiguous
and the chain FORCES ℤ₆ — a selection the group theory alone does not make. The
euclidean/split discriminant does not defend B862 (zero Jordan content in the arc); it
defends B897's separate content. This addendum scopes the claim line's "resolving a
structural ambiguity the SM itself cannot" to the derivation-and-selection novelty.

*(Addendum-beside per the locked pattern; the banked FINDINGS and verdict are untouched.)*
