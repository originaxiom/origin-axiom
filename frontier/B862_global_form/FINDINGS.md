# B862 — the cascade selects the SM's GLOBAL FORM: [SU(3)×SU(2)×U(1)]/ℤ₆ — resolving an ambiguity the SM itself cannot

cc banking seat, 2026-08-03, from the joint structure meditation. Mathematics scope; nothing to
`CLAIMS.md`; Gate 5 untouched. **Not preregistered** — a one-computation arc; footing = the kernel
computation is elementary and the literature anchor is named.

## The fact

The SM's own data does not determine its gauge **group** — only its algebra. All four quotients
Γ ∈ {1, ℤ₂, ℤ₃, ℤ₆} of SU(3)×SU(2)×U(1) are consistent with local physics
([Tong, *Line Operators in the Standard Model*, arXiv:1705.01853](https://arxiv.org/pdf/1705.01853)):
correlation functions of local operators cannot tell them apart.

**The cascade's step-3 winner is not the naive product.** The subalgebra su(3)⊕su(2)⊕u(1)
selected in B861 exponentiates *inside SU(5)* to **S(U(3)×U(2))**, and the kernel of
SU(3)×SU(2)×U(1) → S(U(3)×U(2)) is computed here (hypercharge direction diag(2,2,2,−3,−3)/6 made
explicit): **exactly ℤ₆** — all six sixth-root indices, order 6.

> **The chain does not merely select the SM's algebra. It selects the global form
> [SU(3)×SU(2)×U(1)]/ℤ₆ — resolving a genuine structural ambiguity of the SM, for free.**

This is the first place in the programme where the selected structure **outperforms the SM's own
data**: the SM cannot answer this question about itself; the chain answers it. Falsifiable in
principle — the four quotients differ in their line-operator spectra (one-form symmetry).

## Status within the layer ledger

This closes **layer 2** of the SM-architecture decomposition recorded in the session (algebra /
global form / one-generation content / chirality / anomaly / generations / Higgs / real form /
spacetime) — conditional on exactly what the cascade is conditional on: **P5 + the fused rule**.
Layers 6 (generations), 7 (Higgs rep), 8 (the compact real form — B715's wall), 9 (spacetime)
remain open, and layer 8 is the deep one: the compactness⟺chirality exclusive-or (B583) means the
wall and the cascade's engine are the same coin.

## What this does NOT establish

- Conditional on the cascade (P5 + one rule + one owed definition). Not an unconditional theorem.
- Says nothing about values, generations, the Higgs rep, the real form, or spacetime.
- The line-operator distinction is *in-principle* falsifiability, not a proposed experiment.

`tests/test_b862_global_form.py`
