# B1245 — the follow-through: three things said, none landed

**2026-09-03 · cc (banking) · Gate 5 untouched.**

## Why this arc exists

In one session the seat built a gate for claims that reach the log and never the surface (B1243),
audited the corpus for the same disease (B1244) — and then said three things and landed none of them:

| said | where it went | where it belonged |
|---|---|---|
| C44's Borromean sharpening | CHANGELOG ×15 | the chain: **0** |
| C43's group-independence | conversation only | the chain: **0** |
| "I'll log it as E59" | nowhere | ERROR_LEDGER: **0** |

Recording that the diagnostician committed the disease is the point of this arc, not an aside.

## 1. C43's scope, earned on-bench

**The 16σ desert miss is group-independent by construction.** B915's own `crossing.py` settles it
with no new run. `curve_point(MU)` takes **one physical argument** — the unification scale. Its
inputs are the **SM** betas (41/10, −19/6, −7), the SM two-loop matrix, the **5/3** GUT
normalisation (shared by SU(5) ⊂ SO(10) ⊂ E₆ under the standard embedding), and "the three meet."

An AST pass over the **executable** code — docstrings and comments stripped — finds **zero**
group-specific tokens. The only "E₆" is docstring prose; the only "27" is the coefficient 27/10.

> The control is **not** "run SU(5) and SO(10) and compare." It is that **there is no group
> parameter to vary.**

So C43's kill is a fact about the **desert configuration**, not about the object or about E₆ —
which is what a downstream seat proposed from the literature, now **earned from our own sealed
computation's dependency structure** instead (B1244's prereg E-4 forbids the literature route).
No new measured input: B915's seal already covers α_em(M_Z).

## 2. C44's Borromean sharpening

Read the ladder: z(T1) = 16, z(T1+colour) = 8, z(T1,T2) = 8, **z(T1,T2+colour) = 0**. Every *pair*
has room; **only the triple vanishes.** The fork is irreducibly three-way — Borromean, not pairwise.

That is exactly what separates it from an **uncertainty relation**, which is pairwise and conjugate:
one pair, one bound, a continuous trade-off bought with a scale. Here there is no trade-off (zero is
zero, not "a little of all three") and **no scale for one to be measured in** (Gate 5). The
resonance with Heisenberg is a resonance of non-commutativity *in general*, and by the **B1223
template a matching shape is not a connection**. The addition therefore **refuses** the
identification rather than making it, and points at the real comparison: **B1087's charge
complementarity**, where [H, ρ(μ)] ≠ 0 and [H, ρ(λ)] ≠ 0 — charge and holonomy admit no common
eigenbasis, the same content as [x,p] ≠ 0.

## 3. E59 — the found-but-not-reverified class

Named by the owner mid-session: *"we think we dont have smth, we check the repo we find it and we
dont reverify it in the new light."*

The instance is this arc's own §1. Needing a genericity control, the seat was about to write a fresh
SU(5)/SO(10) script. B915 was found by sweep and read as *"the arc that ran our curve"* — its filed
role. Re-opened against the **new** question — *what does this computation actually depend on?* — it
gave a **strictly stronger** answer than the new script would have.

**A found artifact carries the role it was filed under, not the role you now need.** Distinct from
E54 (declaring absence without sweeping) and E58 (judging from a summary): here the sweep succeeded,
the source was opened, and the item was still read for what it was known to say.
