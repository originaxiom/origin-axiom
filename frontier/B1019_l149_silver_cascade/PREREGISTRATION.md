# B1019 PREREGISTRATION — L149: the silver cascade (sealed before compute)

## The question
Does a non-golden metallic grammar, entered THROUGH ITS OWN CONDUCTOR, cascade to the SM's
structural skeleton under the SAME rule (B861: maximal residual symmetry among registerable
options, the generation as the uniform object)? This is the endpoint-discrimination control that
B1009's withdrawal demands and the withdrawn sentence's precise replacement hangs on.

## The entry map is COMPUTED, not chosen (C1) — with the false start recorded
A first design sketch assumed silver → 2O → E₇ (L105's conjecture read naively). TWO BANKED ARCS
KILL THAT ENTRY: B237 (π₁(silver) has NO 2O quotient — GAP GQuotients; ℚ(√2) is a field-only
coincidence) and B996's check (⟨R²L²⟩ mod 8 has order 32 — a 2-group; 2O has order 48 and is not
nilpotent). So C1 computes, exactly:
  (a) the silver's own-shadow group S = ⟨R²L², (R²L²)^conj?⟩ … precisely: the subgroup of
      SL(2,ℤ/8) generated by R² and L² (order 32 per B996's banked check — recompute + iso type);
  (b) its McKay classification (binary subgroup ↔ ADE): a generalized-quaternion/binary-dihedral
      type gives a D-SERIES diagram — the Lie entry is then the corresponding D-group;
  (c) the generation carrier: the entry group's minuscule rep(s) (D-series: vector 2n, spinors
      2^{n−1}); ALL candidates enumerated, none picked post-hoc.
  Same map for bronze (m=3, conductor 13): ⟨R³L³⟩ ≤ SL(2,ℤ/13), order + type + entry (control 2).

## The cells
- C1 the entry map (above), exact, for m = 2 and m = 3, citing B675/B997/B1002/B237/B996.
- C2 the chirality gate at entry: does the computed carrier admit a chiral (complex) generation?
  Computed from the entry group's rep theory (D_n spinors complex iff n odd — the parity is part
  of the computation, not assumed). If NO chiral carrier exists: the silver cascade is VECTOR-LIKE
  AT STEP 0 — recorded as DIVERGES.
- C3 if C2 passes: run B861's walk verbatim (same rule, same registerability test, same
  uniform-object discipline) on the entry's branching lattice. Endpoint recorded.
- C4 the generic-door control: silver via the mod-3 shadow enters E₆ with the 27 — identical walk
  to the golden's by construction (B996 genericity); recorded to state the refinement precisely.
- C5 the verdict grammar, sealed:
  * DIVERGES (C2 fails, or C3 lands off-SM) ⟹ THE ENDPOINT DISCRIMINATES AT THE WORD'S OWN
    CONDUCTOR: the golden's SM-landing is not shared by its family through their own doors, and
    B1009's withdrawn sentence is replaced by: "matching the SM confirms the axioms exactly
    insofar as the entry is the word's own — the confirming power lives at the own-conductor
    door, where the golden is unique (B997)."
  * CONVERGES (C3 lands on the SM skeleton through the silver's own door) ⟹ the withdrawn
    sentence becomes TRUE and is banked as such: the endpoint carries no confirming power even
    at the own door; validation rests entirely on grammar-level uniqueness.

## Prior (honest)
NO CONFIDENT PRIOR — the first sketch's confident prior (E₇, dies at step 0) was refuted by the
record before sealing, which is precisely why this cell is worth running. Weak lean: DIVERGES
(a 2-group entry is far from E₆'s rep theory), stated as a lean, not a prior.

## Scope
Structure only; no SM value; zero anchors; the walk's rule is B861's verbatim (any rule variation
would be a different cell — B994 governs rule-robustness on the golden side only).
**Date sealed:** 2026-08-10 · **Seat:** cc · Gate 5-Q; zero anchors; structure only.
