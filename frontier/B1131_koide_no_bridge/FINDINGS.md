# B1131 — P-KOIDE: the det φ = 2/3 ↔ Koide 2/3 coincidence IS a coincidence — NO-BRIDGE

**Status: banked (frontier). Verdict PROVED (a NEGATIVE: NO-BRIDGE, against a sealed prereg;
det φ = −2/3 re-confirmed EXACT this bench; four pre-committed bridge routes all failed;
coincidence base-rate computed). Value-probing wave, the follow-on that CLOSES B1129's one
flagged candidate. Owner-directed. Gate 5 untouched. Lock `tests/test_b1131_koide.py`.**

## The question (owner-directed; B1129's one gap)

B1129 flagged |det φ| = 2/3 (B904's E₆-isomorphism determinant, from the 27/matter rep)
landing exactly on Koide's charged-lepton 2/3, dismissed only for lack of an INSTRUMENT.
This cell built the instrument — a construction bridging det φ (the object) to the Koide
geometry — or proved it cannot be built. Highest numerology risk in the campaign; the bar
was the **45° geometry, not the number 2/3**.

## THE ANSWER: NO-BRIDGE (the instrument was built and FAILED)

> The two 2/3's are a **coincidence**. det φ = −2/3 is a **basis-change determinant on
> E₆'s rank-6 CARTAN** — no 3-vector, no inner product with (1,1,1), no cosine, no angle;
> categorically the wrong KIND of object to encode Koide's mass-angle geometry.

- **det φ = −2/3 re-confirmed EXACT** on this bench (the full 78×78 φ from B904's
  `stage4c_phi.pkl`, exact rationals). New structural finding (the agent's, carried): φ is
  block-diagonal — the 72-dim root-space block is a signed permutation (det +1, no content),
  the entire −2/3 lives in a 6-dim block comparing two bases of the **Cartan** (the object's
  geometric torus ↔ the simple coroots). *(Fence: the det = −2/3 is confirmed here; the
  root/Cartan block split is the agent's decomposition — plausible but not corner-contiguous
  in the default Chevalley ordering, so not independently re-derived here. It is NOT
  load-bearing: the negative stands on the failed routes + base rate below.)*
- **Koide side verified**: Q = 0.66666 (2/3 to 5 sf), α = 44.9997° (45° to 4 sf),
  Q = 1/(3cos²α) to 1e-41.
- **Four pre-committed bridge routes, all failed**: (a) the trit / three 9-blocks (the only
  natural 3-vector) gives α = 0°, Q = 1/3 — the ANTIPODE of Koide's 45° point, not toward
  it; (b) det φ = 1/(3cos²α) read backward is bare arithmetic with zero discriminating power
  (a 3-sided die's P(>1) = 2/3 "solves" it identically) — disqualified by the seal; (c) the
  rank-3 Jordan element v₀ gives α = 70.53° (arccos 1/3), not 45°; (c′) φ's own spectrum has
  no 45° angle (nearest singular value to 1/√2 = 0.630, gap 0.077).
- **Coincidence base rate**: ~8.3% of reduced fractions with denominator ≤ 30 are
  {2,3}-smooth like 2/3 (~1 in 12). E₆'s arithmetic is pervasively {2,3}-smooth and Koide's
  is a 3-fold democratic construction — both mechanically favor small denominators, no
  causal link required.

## What it closes

B1129's dismissal is now **sharpened from "no instrument yet" to "the instrument was built
and failed"** — the one gap in the value hunt's exhaustive negative is closed. The object's
2/3 (a Cartan basis-change determinant) and Koide's 2/3 (a lepton mass-angle) are the SAME
NUMBER for INDEPENDENT reasons. The value question is now closed from every route tested:
periods (V-3), natural forms (B1129), the coupling (B1128), and the single most suggestive
coincidence (this cell). Gate 5 held (lepton masses only on the Koide-target side).
