# B917 — the value arc (handoff 7) verified: the suite reproduces; P9 closed at receipt; THE CANONICAL-GAUGE IDENTITY λ = 1; the bridge question isolated

**Date:** 2026-08-05 · **Seat:** cc (banking, verifying solo handoff 7 §LIX–LXVIII)
· **Method:** integrate-don't-merge; their suite rerun here; instant checks at
receipt; the λ tension driven to a single isolated question (B916, in flight).

## Instant checks (receipt-hour, exact)

- **P9's product law: TRUE** — 27·2304⁴ = 760840571584512 as pure integers;
  hence v₁v₂v₃ = 3^{3/2}λ² given their §LXV ingredients. Their factorizations
  (2304 = 2⁸3², 13824 = 6·2304, 953/1129/421493 prime) all check.
- **The biography's split pattern**: μ mod p = [1,2] at 953, 1129, 421493 ✓;
  17, 19 inert ✓; the naive 13 pattern [1,1,1] hits exactly the index-
  distortion (13¹²) their §LXVIII flagged — their careful route required, and
  their p456 rerun here reproduces the [1,2]/inert table including
  disc(hierarchy-cubic) = 2⁶⁴3²⁴5⁶7³11·73²·214189² (the new index primes).

## The suite (rerun on this bench)

- **tauP**: the τ-intertwiner P — weight matching TRUE, ratio-solve 27/27 with
  zero conflicts, exact referee PASS, symmetric, all ±1 ✓ (exact tier).
- **norms/fulltab**: the six lll invariants all equal, λ = 2.41762854144805…
  (45 digits) — their democracy reproduces end-to-end here.
- **belt100**: 953λ − 2304 and 953·CCC − 13824 residuals at this rerun's
  precision floor; CCC = 3!·λ confirmed; the hierarchy v² values reproduce.
- **perm_test (P8/§LXVII)**: the machinery test shows exactly the predicted
  slot transposition under root permutation — the generation-equivariance
  theorem's end-to-end check reproduces.
- **vhunt/p42/p456**: reproduce, including their honest negatives (the
  scale-contaminated probes' retirement is visible in the outputs).

## THE NEW IDENTITY (this seat's contribution, exact): λ = 1 IN THE CANONICAL GAUGE

Recomputing THEIR invariant λ = |c|/√(|q_i q_j q_k|) inside B914's exact
environment (same vectors for c and q; the B883-primitive ±1 cubic; the
integer H₊): **λ = 1 exactly, all six couplings** — the colorless couplings
ARE the geometric means of the raw H-norms. The object's tree-level colorless
sector is UNIMODULAR in its own gauge.

## The isolated question (B916, dispatched)

Their λ = 2304/953 vs this λ = 1: since λ is per-atom-rescaling invariant
within a realization, and the cubic content (±1) and H (±1 integer) match
naively, the factor must live in the 27 ↔ 27̄ realization bridge —
"primitive-in-its-own-basis" is basis-dependent. B916 computes the bridge
intertwiner exactly and either confirms 2304/953 as the bridge constant
(both seats right; cross-seat canonical content = the RATIOS: CCC/λ = 3!,
the hierarchy ratios, the equalities) or localizes a real error. Until B916
lands, λ's ABSOLUTE value is flagged convention-pending in both ledgers.

## Tier statement (their warning honored)

§LIX–LX exact-tier: VERIFIED EXACT here. §LXI–LXV numeric-certified:
REPRODUCED at this bench's precision; the exactification pass (V-L2) remains
the registered floor — B914's exact machinery is the template and already
covers the six lll couplings and the nine scales; extending it to CCC and V
is the remaining work.

## Files

Their suite outputs in the session scratchpad per relay discipline;
`tests/test_b917_value_arc.py` locks the instant checks and the λ = 1 identity.
