# B510 — ADDENDUM: the covering picture had the roles inverted — the character variety is the cover, 40a1 the base

*Addendum written 2026-09-02 under **B1238** (the seat harvest, third ring). Every number below recomputed on this bench — `frontier/B1238_seat_harvest_40a3_bronze_octic/verification/`. The arc's verdict file carries the same correction in its `note`.*

This arc's theorem: "40a3 = Jac(the branch double cover d² = (c²−1)(c²−5) of 40a1), 2-isogenous to 40a1; the programme's two independently-found elliptic curves are the character variety and the Jacobian of its own branch cover." The computation is right; the identification of *which* curve is the character variety came from B211 and was wrong. The double cover d² = (c²−1)(c²−5) **is** the character variety Φ = 0 (4Φ = (2z−x²−1)² − (x²−1)(x²−5), c = x the meridian trace). So the corrected sentence is shorter: **the two curves are one** — X^{na} ≅ 40a3 over ℚ — and 40a1 is X^{na}'s quotient by σ(x,z) = (−x, x²+1−z), the sign twist by H¹(m004;ℤ/2) composed with the swap of Φ's two roots ((X,Y) = (x², x(2z−x²−1)) lands on Y² = X(X−1)(X−5); `verification/b211_phi_jacobian.py`). The three branch points {0,1,5} of 40a1 are the images of x² ∈ {0, 1, 5}: the σ-quotient identifies x = ±1 (the two rational points (±1,1) of X^{na}) and x = ±√5.

The "unification sentence" is earned at a *lower* level than covering geometry — as an identity of plane curves — and enters the identification register as I-12 (EARNED). The 283-airlock is untouched. Verdict stands with this correction; the lock `tests/test_b509.py` (j's distinct, both curves in class 40) is unaffected.
