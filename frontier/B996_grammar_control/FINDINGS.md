# B996 — the grammar-level control: access to the McKay group is GENERIC across the metallic family

**Date:** 2026-08-09 · **Seat:** cc (banking) · **Lane:** MATHEMATICS. Gate 5 untouched.
**P0 — the quantifier:** computes over **the metallic words RᵐLᵐ and their mod-N shadows in
SL(2,ℤ/N)**. Not a manifold, not a census of manifolds — **a census of grammars**, which is the
population the owner identified as the right one.

---

## Why this control and not B995's

**B995 asked the wrong population.** The chain **derives** m004 — C1 (Morse–Hedlund), C2
(self-selection), C6 (Thurston/Riley) — it never selected it from a census. So *"a third of manifolds
also surject onto 2T"* (B993) **does not break the chain**. What it does is narrower and sharper:

> **It removes the endpoint's power to confirm the beginning.** Reaching E₆ is not evidence that
> C1–C5 were right, if a third of entry points reach E₆ too. **The chain can be sound and the SM
> still fail to corroborate it.**

The control that *does* speak to the chain as built runs **over grammars**. And C2 is a **theorem
with a unique fixed point**, so the population at C1–C2 is **size one** — the relaxation is the
**metallic family RᵐLᵐ, m ≥ 2**: exactly *"what if C2's self-selection did not hold."*

## Computed

**First, where the McKay group can appear at all.** |SL(2,ℤ/N)| for N = 2…9: **6, 24, 48, 120, 144,
336, 384, 648**.

> **|SL(2,ℤ/3)| = 24 = |2T| — and N = 3 is the ONLY modulus where SL(2,ℤ/N) is the McKay group.**

**Then the metallic shadows mod 3** — the subgroup of SL(2,ℤ/3) generated by Rᵐ and Lᵐ:

| m | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| \|shadow mod 3\| | **24** | **24** | 1 | **24** | **24** | 1 | **24** |
| is 2T | **yes** | **yes** | no | **yes** | **yes** | no | **yes** |

> ### **Five of seven metallic words give the full 2T. Only m ≡ 0 (mod 3) degenerate — trivially, since Rᵐ, Lᵐ ≡ I.**

## Verdict

**Access to the McKay group — and therefore to E₆ — is GENERIC across the metallic family.** It is
shared by **every** metallic word with **3 ∤ m**, i.e. **two thirds** of the grammars.

> **The grammar-level control returns the same shape as the manifold-level one (B993): the arrival
> at E₆ does not distinguish the golden grammar.** The hypothesis that only m = 1 reaches the McKay
> group is **FALSE**.

## Scope — and it is a real limit, stated rather than buried

This tests **one route**: the mod-3 shadow, which is the only modulus at which 2T can appear as a
*full* SL(2,ℤ/N). **L105's discriminator is a different statement** — that the golden's *own
conductor* is prime so its shadow **is** the McKay group, while the silver's is a prime power (8) so
the McKay partner sits **one quotient down**. That is about **which modulus is the word's own**, and
this cell **did not pin down that notion** — it computed the shadow at a fixed modulus instead.

**So L105 is not refuted here.** What is refuted is the stronger and simpler hypothesis that access
to 2T is itself grammar-specific. **The finer question — whether the golden is the unique metallic
word whose own conductor is 3 — remains open and is the surviving form of L146.**

*(Consistency check: at N = 8, m = 1 generates all 384 of SL(2,ℤ/8) while m = 2 generates only 32 —
so the golden and silver do differ at that modulus, which is compatible with L105 and shows the two
statements are about different things.)*

---

**Verdict: NEGATIVE for the simple hypothesis.** The E₆ arrival is generic across grammars, exactly
as it is generic across manifolds. **The endpoint cannot confirm the beginning at either level.** The
surviving question is L105's own-conductor formulation, which needs the notion defined before it can
be tested.
