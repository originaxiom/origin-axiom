# B8089 — PREREGISTRATION: the anomaly layer over the object's DERIVED content (L144, door 5)

**Sealed 2026-08-19, BEFORE any computation.** Seal digest in `SEAL.txt` (SHA-256 of this file,
algorithm named there, computed over the file's bytes — not self-referential).

**Owner's word:** *"go"* — L144 elected after the audit showed Gate 5 is not what blocks the ratio
lane. **The scale theorem's own exemption clause is the licence:** *"Dimensionless ratios, phases,
mixing parameters, counting: untouched by this theorem (weight w = 0 is outside its hypothesis)"*
(`frontier/B666_leads_campaign/cellS/PROOF_NOTE.md:192`, verified verbatim in-sandbox today).

---

## 0. THE STANDING CONSTRAINT, restated before anything else

**This is the RATIO lane, not the scale lane.** B413 (tower-measure flat, exact) and B563
(preregistered Planck retry, p≈0.53, plastic control lands *closer*) are **terminal** and are **not
re-opened here**. Nothing in this arc computes, seeks, or reports a dimensionful quantity.

**Gate 5:** nothing from this arc enters `CLAIMS.md`. The SM appears as a **comparison target and as
imported field content**, never as a produced value.

---

## 1. THE BASELINE — what is already banked, declared now so "new" is testable later

| banked | what it says | scope limit |
|---|---|---|
| **B864** | the anomaly cubic; hypercharge unique gaugeable U(1) **inside `span(Y,χ,ψ)`** | computed over the **IMPORTED chiral 15**; `a` drops out identically so it says nothing about `Y` itself; over the full 5-space the cubic factors into **three** lines (B8070 sharpening) |
| **B862** | the global form is **ℤ₆**, not the naive product | a statement about the *group*, not about anomalies |
| **B892** | the cascade lands `su(3)⊕su(2)⊕u(1)³` | algebra, not matter |
| **B876** | the 27 gives **16+10+1** — one generation **with ν^c**, plus conjugate | matter content, derived |
| Part VI | `sin²θ_W = 3/8` at unification | a trace-normalisation ratio, **already banked** |

**The gap this arc addresses, stated as a fact about the baseline:** every anomaly computation in
the bank runs over the **imported chiral 15**. The object **derives the 16**. *The anomaly layer over
the object's own derived content has never been computed.*

---

## 2. THE COMPUTATION (fixed now; no adaptation after seeing output)

Over the derived content — one generation in the **16** of `so(10)` (i.e. `Q, u^c, d^c, L, e^c, ν^c`)
with algebra `su(3)⊕su(2)⊕u(1)` and global form **ℤ₆** — compute **exactly, over ℚ**:

1. **Every gauge anomaly coefficient**: `[SU(3)]³`, `[SU(3)]²U(1)`, `[SU(2)]²U(1)`, `U(1)³`,
   `U(1)`–grav, `[SU(2)]²`-Witten (mod 2).
2. **The same over the 15** (drop `ν^c`) — the paired control that isolates what `ν^c` does.
3. **`B−L`**: its cubic and gravitational anomalies over the 16 and over the 15.
4. **The ℤ₆ global form**: whether the ℤ₆ acts freely on the derived matter, i.e. whether the
   matter is consistent with the *quotient* group and not merely the cover.
5. **Every ratio** of non-vanishing invariants produced above, in lowest terms.

**Instruments:** exact rational arithmetic; charges assembled from the `so(10)` weight structure,
not typed in from a table where avoidable; a second, independently-coded charge assignment as a
cross-check.

---

## 3. THE TWO OUTCOMES — sealed, and neither is a null

**OUTCOME A — DOOR 5 OPENS.** The anomaly layer yields at least one quantity that is
simultaneously: **(i)** RG-invariant, **(ii)** dimensionless, **(iii)** quantized (rational, from
counting/representation data), **(iv)** carrying genuine **gauge content**, and **(v)** *not* on the
baseline list of §1. ⟹ **B167's door-map needs a row rewritten**, and door 5's dismissal
(*"mathematics… not the SM's gauge content"*) is falsified on its own terms.

**OUTCOME B — DOOR 5 STAYS SHUT.** Every invariant produced is one of: **(i)** already on the
baseline list, **(ii)** identically zero by cancellation and therefore carrying no ratio, or
**(iii)** a pure normalisation convention with no gauge content. ⟹ **B167 stands, and receives the
first citation it has ever had** — an orphaned POSTULATED taxonomy finally tested.

**Both outcomes are banked with equal prominence.** B is not a failure; it converts an untested
postulate into a tested one.

---

## 4. WHAT WILL NOT BE ALLOWED TO COUNT — written down while it costs nothing

- **No value-matching.** Not one number will be compared against a measured constant. Every prior
  null in this lane (B533, B549, B558, B615, B687, B724, B743, B322) was a *matching* search;
  B724's look-elsewhere theorem guarantees a ×3 near-hit for any target. **A match is not evidence
  here and will not be reported as one.**
- **Re-deriving a banked fact is OUTCOME B, not A.** Recovering `sin²θ_W = 3/8`, or that the SM
  anomalies cancel, is a **control passing**, not a discovery. The textbook cancellation is
  *expected*; if it fails, the instrument is wrong, not the Standard Model.
- **`ν^c` making `B−L` anomaly-free is expected and is a CONTROL, not a result.** It is textbook.
  What would be new is only if the object's *derivation* of `ν^c` (B876) converts that into a
  quantity absent from §1 — and that must be argued explicitly, not asserted by juxtaposition.
- **No claim that a nontrivial discrete anomaly implies anything about the real world.** It would
  constrain the IR of *this algebra with this matter*, nothing more.

---

## 5. FALSIFIERS AND CONTROLS (fixed before compute)

- **Instrument control:** the `[SU(3)]³` anomaly must vanish for the vector-like colour content and
  `[SU(2)]²U(1)`, `U(1)³`, `U(1)`-grav must vanish over the full 16. **If any fails, the arc is
  instrument-broken and reports nothing else.**
- **Bite control:** the same machinery run on a deliberately *anomalous* content (drop `e^c`) must
  return **non-zero**. A cancellation check that cannot detect an anomaly is not a check.
- **Second-route control:** charges re-derived from `so(10)` weights independently of the first
  assignment must agree on every coefficient.
- **The `ν^c` isolation:** 16-vs-15 must differ **only** where `ν^c` can contribute; a difference
  anywhere else means the two runs are not the same computation.

**Prediction recorded before compute (so the arc can be wrong):** I expect **OUTCOME B**. The
textbook anomalies will cancel, `B−L` will be anomaly-free over the 16, and I expect the ℤ₆
consistency to come out *trivially satisfied* — because the SM matter is exactly what ℤ₆ was defined
to quotient by. **If that is what happens, B167 stands and I will say so plainly.**

---

**Author:** cc3, audit seat. Band B8000+ per cc's 2026-08-19 ruling. No merge from this seat.
