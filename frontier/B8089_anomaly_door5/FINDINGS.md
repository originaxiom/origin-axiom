# B8089 — door 5 is shut STRUCTURALLY: the anomaly layer over the derived content is identically zero

**Date:** 2026-08-19 · **Seat:** cc3 (audit) · **Verdict: NEGATIVE** for door 5, **PROVED** for the
structural statement. Preregistered and sealed (`SEAL.txt`, SHA-256) **before** `anomaly_door5.py`
existed. Owner-elected: *"go"*. Gate 5 untouched — **no value is compared to any measurement.**

## Why this ran

L144 held that `B167_conserved_no_scale_lemma`'s door-map was stale. B167 is **self-labelled
POSTULATED**, **orphaned** (no arc's `depends_on` cited it), and predates the cascade campaign. Its
door 5 — the *ratio* lane — it called *"the only logically-open lane"* and then dismissed because
its survivor was *"mathematics… not the SM's gauge content."* **That premise is now false**: B862
derives the global form, B892 the algebra, B876 the matter.

And the licence is not a loophole. The scale theorem states its own exemption verbatim
(`B666/cellS/PROOF_NOTE.md:192`): *"Dimensionless ratios, phases, mixing parameters, counting:
untouched by this theorem (weight w = 0 is outside its hypothesis)."* 't Hooft anomalies are
**RG-invariant**, hence **scale-free by theorem** — the one part of QFT a scale-free object may
speak about without crossing Gate 5 at all.

## The gap that made it a new computation

**Every anomaly computation in the bank runs over the IMPORTED chiral 15** (B864, with B8070's
sharpening). **B876 derives the 16** — one generation *with* `ν^c`. The anomaly layer over the
object's **own derived content** had never been computed.

## Result

| channel | over the derived **16** | over the imported **15** |
|---|---|---|
| `U(1)³`, `U(1)`-grav, `[SU(3)]²U(1)`, `[SU(2)]²U(1)`, `[SU(3)]³`, SU(2)-Witten | **all 0** | all 0 |
| **`(B−L)³`** | **0** | **−1** |
| **`(B−L)`-grav** | **0** | **−1** |

- **ℤ₆ global form:** the derived matter is consistent with the *quotient*, and the congruence
  `6Y ≡ a·triality + b·duality (mod 6)` has a **unique** solution mod 6, `(a,b) = (4,3)` — found by
  exhaustion, not quoted.
- **Non-vanishing invariants over the derived content: ZERO.**

## THE FINDING, and it is better than a null

**Door 5 asked the anomaly layer for a ratio. The anomaly layer of consistent chiral content is
identically zero by construction — and a layer that vanishes identically cannot supply a ratio.**
The ask is **structurally unanswerable by this layer**, not merely unanswered. This is the same
shape as B990: *the gap is structural, not evidential*, so no sharpening of the same instrument can
close it.

**And the sharper irony, which is the real content.** L144's hope was that *"a nontrivial anomaly
forbids a trivial IR"* — that the layer would **constrain dynamics**. But `ν^c` — the very field
the object *derives* (B876) and which makes its matter content complete — is **exactly what cancels
the last non-vanishing invariant**, the global `B−L` anomaly. Over the imported 15 there is
something to match (`−1`, `−1`); over the derived 16 there is **nothing**. **The object's matter is
too anomaly-free to say anything.** Completeness of the content and emptiness of the layer are the
same fact.

## Disposition of L144, per the sealed two-outcome

**OUTCOME B. B167 stands — and receives the first citation it has ever had.** An orphaned
POSTULATED taxonomy is now a **tested** one, on the door it itself called the only open lane. That
is a real upgrade to the record, and it is what the seal promised B would deliver.

## CONTROLS

- **Instrument:** all six gauge channels vanish over the 16 — the textbook fact, reproduced.
- **Bite:** dropping `e^c` returns `U(1)³ = −1`, `U(1)`-grav `= −1`. *A cancellation check that
  cannot detect an anomaly is not a check.*
- **Second route:** all sixteen hypercharges **re-derived from `su(5)` alone** — `Y` a traceless
  generator of the **5**, `10 = Λ²(5)` with `Y_{ij} = Y_i + Y_j`, the singlet at 0 — reproducing
  route 1's multiset exactly. **No SM table is load-bearing.**
- **`ν^c` isolation:** 16-vs-15 differ **only** in `B−L`, in no gauge channel.

## SCOPE

`su(3)⊕su(2)⊕u(1)` with the derived 16 and global form ℤ₆. **The RATIO lane only — the scale lane
(B413, B563) is terminal and untouched.** No value compared to any measurement; **every prior null
in this lane was a *matching* search, and this is not one.** Says nothing about the manifold.
Finer **discrete** anomalies (spin-structure-dependent ℤ₆ subtleties) need a framework beyond this
arc and are **registered as owed, not asserted**.
