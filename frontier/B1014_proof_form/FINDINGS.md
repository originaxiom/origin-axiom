# B1014 — the proof-form: the derivation theorem closed, the anchor doctrine licensed, the claim stated

**Date:** 2026-08-10 · **Seat:** cc · **Lane:** governance/synthesis (owner-approved twice:
"i aprove", "confirmed"). Gate 5-Q; no computation about the object; no SM value anywhere.

**Verdict: PROVED** (an assembly whose every load-bearing line is a banked arc, executing two
owner decisions).

---

## The two decisions this arc executes

**1. "We can close proof-as-derivation too."** Done — `docs/THE_CLAIM.md` §1 states THE DERIVATION
THEOREM as one theorem: hypotheses = the counted input list (A1–A6 + A7's bit; two 𝔽₂ bits, one
scale, one Lie type), conclusion = the SM structural skeleton as a graded table
(COMPUTED / DERIVED / THEOREM / STRUCTURAL, arc by arc), with the controls stated proudly
(waypoint genericity B993/B996; grammar uniqueness B997/B1002; endpoint discrimination honestly
UNCOMPUTED, L149; six-chains-one-endpoint B994). **A STRUCTURAL grade is a named debt inside a
closed proof, not a hole in an open one** — the only STRUCTURAL arrows are the generation
structure (B897/B928); everything else is theorem-grade or an explicit input.

**2. The anchor doctrine.** The owner's bridge principle — *"anchoring one or two elements is
allowed and doesn't compromise parameter-free, because the observer's perspective has to be
bridged to the framework's nature as structure"* — is not a relaxation; **it is licensed by the
framework's own theorems**: B782 (no equivariant section computes the closing choice), B936
(values frame-relative in the pair), B1012 (∂S/∂k = −CS ≡ 0). *A theory whose values live in the
observer–object coupling, and which then supplied its own coupling frame, would be inconsistent.*
Codified as **`CROSSING_REQUIREMENTS` R11** with three teeth: anchor identity sealed in advance;
outputs − dimensionless anchors > 0 (MB12 vacuity applied to anchoring); the anchor *choice*
priced. **"Parameter-free" retains its exact banked meaning: zero FREE dimensionless parameters.**
Worked precedent: B915's single declared input.

## The artifacts

- **`docs/THE_CLAIM.md`** — one page: §1 the Derivation Theorem (closed) · §2 the Anchor Doctrine
  (bridge-by-theorem) · §3 the Prediction Channel (the one real wall; the coupling layer's closed
  form, B1011; gated by the ten requirements + R11, with L149/L150 as the gating computations).
  Ends with the one-sentence claim. README carries the pointer.
- **`CROSSING_REQUIREMENTS` R11** + preparation step 0 (declare the anchor set before L150/L149 —
  the frame question and the anchor choice interact).
- **`OPEN_LEADS` L151** — the anchor budget as a structural prediction: the input typing (two
  discrete bits + one Lie type + one scale) *derives* the R11 allowance; if the minimal working
  anchor set matches it, the accounting is a consistency theorem nobody designed. Two-outcome.

## What this deliberately does not do

It does not weaken any gate (Gate 5 untouched; the kind gate still fronts every SM contact); it
does not touch the value wall's null results; it does not claim Tier 2 — §3 says the wall is real
and names the road, not a crossing. And the claim page's controls section keeps the deflationary
results **in the claim itself**, because they are what make the surviving statement credible.

---

**Verdict: PROVED. The derivation is stated as one closed theorem; the anchors are licensed by the
no-section theorem; the claim fits on a page and every sentence has an arc.**
