# B392 (W5) — the wall probes: two banked, one running

**Status: P-DYNAMICS and P-CHIRALITY banked; P-SCALE running (watcher armed).
Pre-registration committed first. Firewalled — interpretation lives in the speculations
note, not here.**

## P-DYNAMICS: NO-MATCH (as registered)

Our sector twist exponents {3/10, 7/10} vs the tricritical-Ising T-spectrum
{h − 7/240 mod 1} = {1/120, 17/240, 49/120, 113/240, 137/240, 233/240}: **no member
matches** (exact rationals, p_dynamics.json). With the banked triangular-S no-match
(PR #485), the TIM identification of the sector is dead at BOTH S and T levels — the
c = 7/10 kinship (tr = golden) does NOT extend to the modular data. Wall 3 priced: the
dynamics identification needs a different theory family (or none).

## P-CHIRALITY: NO-ALIGNMENT (the exponent test)

B347's θ-grading (the hyperelliptic involution = the E₆→F₄ fold) has anti-pair = the
E₆∖F₄ exponent lines {4, 8}; the seam sector's exponent sets are {6,14} (mod 20, W₁-side)
and {2,10} (mod 12, W₂-side). No exponent-level alignment exists; the two 2-dimensional
anti-sectors (θ-anti escape pair; τ₃-anti seam constants) are STRUCTURAL ANALOGS ONLY.
Wall 2 priced: connecting them requires the same missing bridge the specialist register's
E₆ row (L50/CRUX) already names — no new sandbox computation can close it.

## P-SCALE: the constant is FROZEN; the support walks twisted (half-hit, half-kill)

Level-135 m=1 singles (2 primes, unanimous): nonzero cells at a ∈ {29, 74, 119, 164} =
**a ≡ 29 (mod 45)**, four cells at spacing ord/4 — every value EXACTLY 1/4. The registered
value-prediction HIT (1/4 frozen at 15, 45, 135 — **no running in this channel**; Wall 1
priced: the tower's seed constant generates no scale). The registered support-prediction
(a ≡ 1 mod 15) KILLED: the support residue moves 1 mod 5 → 1 mod 15 → 29 mod 45 (5-part
flips +1 → −1, 9-part 1 → 2) — the single-seed support walks the tower by a twisted
congruence, banked as new exact data (p_scale_135.json).
