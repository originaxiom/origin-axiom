# B392 (W5) — PRE-REGISTRATION: the wall-probe reconnaissance (three probes, firewalled)

**Committed before computation. One computable two-outcome probe per wall; physics wording
lives ONLY in the firewalled interpretation notes; everything below is level-model
arithmetic. Time-box: 2 sessions.**

## P-SCALE (Wall 1: does anything RUN with the level?)

The banked cross-level facts: the m=1 single-seed constant is EXACTLY 1/4 at levels 15 and
45 (identity transport, PR #484); the sector phase OSCILLATES (36°/108° by 3-adic parity);
the seam/sector decouple above 15 (W2). The probe: **the third rung — compute the m=1
singles at level 135** (fp engine; 180 Par-traces + DFT, cheap). REGISTERED PREDICTION:
value exactly 1/4 on the cells a ≡ 1 (mod 15), zero elsewhere — i.e. the constant is
FROZEN (no running in this channel). KILL: any deviation (a running constant would be the
opposite pricing of Wall 1).

## P-CHIRALITY (Wall 2: does the E₆ grading see the seam?)

From B347: the figure-eight's E₆ character-variety tangent carries two ℤ/2 gradings, with
θ = the E₆→F4 split. The probe: compute θ's action on the B347 tangent basis and test
whether its fixed/anti decomposition ALIGNS with the τ₃ (√−3-flip) split of the banked
seam constants (the two sector constants are τ₃-conjugate faces). Two-outcome:
alignment (an exact correspondence statement) / no-alignment (the gradings are disjoint
structures — banked as the wall's pricing). Machinery: B347's banked tangent data.

## P-DYNAMICS (Wall 3: is the sector twist diagonal TIM's?)

Our sector twist diagonal is (e^{2πi·3/10}, e^{2πi·7/10}) (banked, PR #485). The
tricritical-Ising model (c = 7/10) has T = diag e^{2πi(h − c/24)} over its six primaries
h ∈ {0, 1/10, 3/5, 3/2, 7/16, 3/80}. The probe: exact rational comparison of our exponents
{3/10, 7/10} against TIM's {h − 7/240 mod 1}. REGISTERED EXPECTATION: NO-MATCH (the naive
identification dies; together with the banked triangular-S no-match this prices the
identification wall precisely: the golden weights are NOT the TIM twist spectrum, despite
the c = 7/10 kinship). A match would be the surprise outcome. NEEDS-SPECIALIST flag on any
interpretive step beyond the arithmetic either way.

Locks from output JSONs; board + notification at wave completion. Firewall absolute.
