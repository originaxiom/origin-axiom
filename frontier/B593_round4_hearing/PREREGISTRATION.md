# B593 — ROUND 4: making the sign summable (prereg)

**Date: 2026-07-14. Registered BEFORE the computation. Firewall: nothing to
CLAIMS.md; no SM quantities. Owner mandate: "whatever you recommend — brave,
open-minded; the governance protects us."**

## The forced question (from B592/B592-OPEN)

The twist is heard in the open matrix as the sign of the θ-odd block and in no
closed contraction (C-symmetric states sum it to zero). Round 4: what listener
can SUM the sign? Two routes, both computable now.

## R4-A — THE SECOND-ORDER HEARING LAW (the dial-deformed listener)

Deform the listener's state along the dial: ψ_ε = ψ + εu, u in the θ-odd
plane (the stage model of B575's honest θ-odd moduli; ε real). The mirror
flips the odd part: mirror(ψ_ε) carries ψ − εu. CLAIM (derived from the
banked structure, to be verified exactly):
> A_ε(g) = A₀(g) ∓ ε²·(u† M_open(g) u) + O(ε⁴-terms in |u|),
with NO O(ε) term (parity conservation) and the twisted/untwisted difference
> A_ε^twisted − A_ε^untwisted = −2ε²·(u† M_odd(g) u)
— **the closed chiral amplitude exists and equals ε² times the open odd
block, sign flipped by the twist.** Cells: derive symbolically in the 6-dim
stage model; verify numerically for u = u₃, u₆, mixtures; welds I, RL, RRLL;
gates: ε = 0 reduces to the banked B592-OPEN values; the O(ε)-vanishing is
exact; consistency with the quadrature theorem's quadratic shape noted.
Outcomes: LAW HOLDS (bank the first closed chiral amplitude, at order ε²) /
LAW FAILS (diagnose; the parity-conservation proof would have to be wrong).

## R4-B — THE THIRD ENTITY (the non-invertible listener)

The state C-symmetry proof used invertibility. The first non-invertible knot
is 8₁₇. Cell: compute J₃(8₁₇) and J₃(reverse(8₁₇)) at the SU(3)₂ root with
the validated R-matrix pipeline (braid word obtained programmatically;
closure identity gated by |V(−1)| = det(8₁₇) = 37 through the N = 2 layer,
plus spherogram identification if available; the reverse = the reversed
braid word, standard). Outcomes (registered): DIFFERENT → a natural
non-C-symmetric state exists (would also be a quantum detection of
non-invertibility — extraordinary; verify at ≥2 roots and treat with maximum
suspicion per the governance) / EQUAL → banked null, consistent with the
folklore that RT invariants do not detect inversion; the C-symmetry wall
extends beyond invertibility at this color/root, and the third entity must
be sought elsewhere (links, cables, or the dial route of R4-A which needs no
third entity).

## Controls / discipline

R4-A: exact-reduction gate at ε = 0; two independent odd directions; the
symbolic O(ε) coefficient printed and asserted zero. R4-B: pipeline
re-validated on 4₁ (banked) before 8₁₇; the braid-closure identity gated by
the determinant through the N=2 layer; both word conventions (reversed,
index-flipped) computed to separate reverse from same-knot Markov moves.
MB13: B592/B592-OPEN (the sign and the blocks), B575 (the moduli are honest),
X2R (the quadratic shape), B584 (third unhearability — what R4-B probes past),
the R-matrix pipeline (B592-C4, validated). No prior second-order hearing or
non-invertible-listener computation exists in the bank.
