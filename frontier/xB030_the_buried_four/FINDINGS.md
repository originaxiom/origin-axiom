# xB030 — THE FOUR BURIED RESULTS, DUG UP: one number under five names, a cap that holds on 506 manifolds built to break it, a tuning whose shape this seat got wrong, and the place where three generations could still come from

**Seat `xb`, `sep16-branch`, 2026-09-18. PREREGISTRATION sealed at `3340135e` before any cell existed.**
**Owner's instruction: *"deal with all burried ones."***

---

## 0. THE WITHDRAWAL THE SEAL ALREADY CARRIES

This seat told the owner that `H¹(Q; ω) = 0` is *"a new selection rule on the Wilson line that the
record's closing has never applied."* **Withdrawn in the preregistration, before any cell ran, and
repeated here.** The record does not fail to impose `h¹ = 0` — **it deliberately works where
`h¹ = 1`**, because that is where the chirality index can be non-zero at all (sm:B1303's 2×2
criterion, harvested and independently verified at B1306; xB027's `h¹(χ²) = 1` at every locus).
**The proposed rule was the negation of the record's own mechanism, asserted from memory without
checking.** What is actually there is better, and is §2 below.

---

## U1 — ONE NUMBER, FIVE NAMES (E82 satisfied: every edge exhibited *before* the sentence)

| edge | status |
|---|---|
| **E1** `α = (3+√5)/2 = φ²` | **EXHIBITED** (symbolic) |
| **E2** `Δ_{4₁}(t) = t² − 3t + 1` has roots `φ^{±2}` | **EXHIBITED** (symbolic) |
| **E3** xB029's **measured** tower growth constant `= log α` | **EXHIBITED** — re-read from `g2_mssm.json`, not recalled: `0.9624236501` vs `log α = 0.9624236501` |
| **E4** B1418's **golden locus** is `λ² = φ²` | **EXHIBITED** — from B1418's own `DESIGN.md`: *"m004 at the golden locus: χ(μ) = λ with λ² = φ² (K = ℚ(√5), primitive element φ) … H¹(π; K_{λ²}) ≠ 0 is verified, not assumed"* |
| **E5** that locus **is** a root of the Alexander polynomial | **EXHIBITED** — `Δ(φ²) = 0` exactly |

**Graph connected on all five.**

> **`α = φ²` is simultaneously: the Alexander root of the figure-eight; B1418's golden locus, where
> `H¹ ≠ 0` and the chirality index can fire at all; and the exponential rate at which the tower's
> torsion homology grows (xB029). Three arcs named the same number three ways and none of them saw
> the other two.**

**FENCE:** this is a **renaming, not a discovery**, and it is an **identification**, so E82/I-10
apply — **nothing is promoted and nothing enters `IDENTIFICATION_LEDGER` as a new fact.** What it
buys is that a *torsion-growth* statement and a *chirality-locus* statement are statements about one
algebraic number.

---

## U2 — THE CAP: `h¹ ≤ 1` HOLDS ON 506 MANIFOLDS CHOSEN TO BREAK IT

**The method is exact, not statistical.** For **prime** `d`, `H₁(Q_d; ℚ)` is a `ℚ[ℤ/d]`-module
`≅ ℚ ⊕ ℚ(ζ_d)^a`, so `b₁ = a(d−1)` and **`h¹ = b₁/(d−1)` exactly**, the common value on the Galois
orbit.

**v1 passed and was WEAK, and said so:** of 146 members only **4** had any character with `h¹ = 1`;
the 140 census manifolds contributed **nothing informative**. A population that cannot exhibit
`h¹ = 1` cannot distinguish `h¹ ≤ 1` from `h¹ ≤ 3`.

**v2 targeted the population that can** — a prime-degree cyclic cover of a `ℚHS³` can only have
`b₁ > 0` when the `p`-rank of `H₁` is ≥ 2:

| | |
|---|---|
| closed census manifolds scanned | **11 031** |
| `ℚHS³` candidates with `p`-rank ≥ 2 at some prime `p ≤ 13` | **1 979** |
| **INFORMATIVE** (some character with `h¹ = 1`) | **506** |
| **with `h¹ ≥ 2`** | **0** |
| distribution of `max a` | **{0: 1473, 1: 506}** |
| **divisibility control** — `b₁ ≡ 0 (mod d−1)` at every prime `d` | **0 violations across the whole scan** |

Plus the object's own closed tower: `n=5` (`d=11`, `a=1`), `n=6` (`d=2,5`, `a=1`), `n=7` (`d=29`,
`a=1`), and `a=0` at `n=3,4,8`.

> **`h¹ ∈ {0,1}`. Never 2. On 506 manifolds selected precisely because they were the ones that
> could have broken it.**
>
> **The sealed kill condition was the outcome the programme WANTS, and it did not fire.**

### AND THE PART THAT IS NOT A DEAD END — where three could still come from

`|I| ≤ h¹` holds for the **`m = 1`** module. **It does not hold for higher symmetric powers**, and
**the record already has the counterexample**: B1418's own table gives **`|I| = 2` on `t12835`**, at
four loci of orders `(3,12,1), (3,12,2), (3,4,1), (3,4,2)`, **all at `m = 3`** — `V = Sym^m(ρ_χ) ⊗ ψ`
with `ρ_χ` reducible non-split.

**So the cap is MECHANISM-SPECIFIC, and that is the useful half:**

- the **abelian / `m = 1`** route is capped at **one** generation — which is exactly what xB026 and
  xB027 measured on both towers, and this arc now shows the cap is a property of **506 manifolds of
  the class**, not of the two towers;
- the **higher-`Sym` reducible-non-split** route is **not capped by it**, and is the **only** place
  in the record where the index has ever exceeded 1;
- and **B1418 left 3 325 modules on `t12835` and 1 542 on `t12833` NOT RUN** — *"a fourth state,
  never folded into zero"*, in its words — **all at later loci and larger `m`, which is precisely
  where the only known `|I| = 2` lives.**

> **THE APPLIED CONCLUSION, marked as a reading and not a claim:** *if three generations are reachable
> from this object's class at all, they cannot come from `h¹`, and the single most informative unrun
> computation in the record is `t12835`'s 3 325 remaining modules at larger `m`.* That is registered
> already as L222(iii) — **what this arc adds is the reason it should be first rather than third.**

---

## U3 — THE TUNING IS DIOPHANTINE, BUT THIS SEAT'S SEALED SHAPE FOR IT IS REFUTED

**v1 was defective on two counts, both recorded:**
1. It included `λ` with `Gλ ≡ 0 (mod k)`, where `4sin² = 0` **exactly** — the flat bundle is
   **trivial** there, it is not a symmetry-breaking Wilson line, and FW's eq. (3.1) is **undefined**
   (it is the zero-mode case). The *"best `P_eff` = 669"* figures it printed are
   **floating-point artefacts of `log`(rounding error)**, not physics.
2. Its sealed prediction — **density `O(1/k)`, "a one-in-`k` coincidence"** — is **REFUTED by its
   own numbers**, which went `1, 1, 5, 13` as `k` grew.

**v2, degenerate residue excluded, `gcd(G,k) = 1`:**

| `k` | `λ` reaching `P_eff ≥ 84` | density | derived `2θ(k)` | best `P_eff` (at `‖Gλ‖ = 1`) |
|---|---|---|---|---|
| 99 | **0** / 98 | 0 | 0.0060 | 59.743 |
| 199 | **0** / 198 | 0 | 0.0062 | 74.403 |
| 317 | 2 / 316 | 0.0063 | 0.0064 | **84.180** |
| 999 | 6 / 998 | 0.0060 | 0.0067 | 108.284 |
| 4001 | 28 / 4000 | 0.0070 | 0.0072 | 137.423 |

**What is true:** the condition is on **`‖Gλ/k‖`, the distance to an integer** — a near-integer
condition, as predicted — the **best `λ` sits at `‖Gλ‖ = 1` in 7 of 7** and matches the closed form
`(1+2M)log k − M log 4π²` to `< 4 × 10⁻³` *(the cell's own pass-flag used a `10⁻⁶` tolerance and
reported `False`; **that is a mis-set flag, not a finding** — the agreement is in the table)*, and
**`k ≥ ~317` is needed at all: at the source's own `k = 99`, nothing reaches 84.**

**What is false:** the density is **≈ 0.6 % and slowly GROWING**, matching the derived `2θ(k)`, **not
`O(1/k)`.** The tuning is a **1-in-160 condition, not a 1-in-`k` one.**

**FENCE:** this is the **source's own worked example** `Q = S³/Z_k`. The source says the general case
is unknown. **It is not a statement about the landscape.**

---

## U4 — WHERE THE LABOUR DIVIDES, AND WHY THE RECORD'S NEGATIVES LOOK WORSE THAN THEY ARE

**Two different places to get chirality from, by quotation:**

- **The literature.** Acharya et al.: *"the non-Abelian gauge fields are localized along
  three-dimensional submanifolds … whereas **chiral fermions are supported at points at which there
  is a conical singularity**."* Friedmann–Witten then **require** the three-cycle to contribute
  nothing: *"in expanding around the trivial `SU(5)` connection on `Q`, **there are no zero modes**…
  Such zero modes would lead to massless chiral superfields in the adjoint"* — guaranteed by **finite
  `π₁`**. **Chirality from the singular points; the 3-manifold must be cohomologically silent.**
- **The record.** Chirality from `I = t₀ − r₁`, which is **built from `h¹` of the three-manifold**
  and is **zero unless `h¹ ≠ 0`**.

> **These are opposite demands on the same quantity.** The literature's hypothesis exists to kill
> what the record's mechanism needs. **That is why finite `π₁` sits in Friedmann–Witten and why
> hyperbolic manifolds are outside it** (xB029 Addendum 1) — and it is the structural reason the two
> programmes cannot simply be composed.

**And the calibration, restated at its correct width:** Acharya et al. **assume** the GUT group, the
Wilson-line breaking, the MSSM chiral spectrum — hence the count of three — and the Standard Model
Yukawas. **The record's negatives are measurements of the half the literature assumes.** *That is a
calibration and not a licence: an assumption shared with the literature is still an assumption, and
nothing here moves Gate 5.*

---

## THE VERDICT

**U1** one number, five names, all five edges exhibited — **a renaming, ledgered as nothing**.
**U2** `h¹ ≤ 1` on **506 informative manifolds, 0 exceptions, 0 divisibility violations** — the
abelian route is capped at one generation as a property of the class; **and the cap does not bind the
higher-`Sym` route, where the record's only `|I| = 2` already sits, with 3 325 modules unrun.**
**U3** Diophantine in shape, **but the sealed density law is refuted by the cell's own numbers**, and
v1 was defective at the degenerate residue.
**U4** documentary: the two mechanisms are opposite demands on `h¹`.

**Prior scored: U1 correct · U2 correct (and this seat predicted against the programme's interest and
was not rescued) · U3 half right, the quantitative half REFUTED · U4 correct.**

**Gate 5 absolute. No value. No generation count claimed. Nothing to `CLAIMS.md`.**
