# xB030 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-18. Sealed, hashed and pushed before any cell runs.**

## P0

The owner's instruction: *"deal with all burried ones"* — after *"lets not do the most repeated
mistake of burying the important results and not applying them to the deeper questions we ask,
especially about physics."* **Four results were produced in xB029 and filed as footnotes, fences or
side-remarks. This arc digs each one up, tests it, and applies it — or retracts it.**

**Gate 5 absolute. No value. Nothing to `CLAIMS.md`.**

## 0. A CORRECTION THIS ARC OWES BEFORE IT STARTS

In the reply that named the buried four, this seat told the owner that
`H¹(Q; ω) = 0` is **"a new selection rule on the Wilson line that the record's closing has never
applied."** **That is WRONG and is withdrawn here, before any cell runs.** Reconnaissance on the
record shows the opposite: **the closing's chirality index needs `h¹ ≠ 0`.** sm:B1303's 2×2
criterion *(harvested, and VERIFIED by an independent route at B1306)* computes exactly the
characters with `h¹(Y_n; ψ) = 1` — the SM seat calls them **the tower's supports** (3, 0, 20, 27, 56,
0, 147 at `Y₃…Y₉`) — and xB027 found `h¹(χ²) = 1` at **every** locus of both towers, *"which is
precisely what bounds `|I| ≤ 1`."* **The record does not fail to impose `h¹ = 0`; it deliberately
works where `h¹ = 1`.** The rule was not missing. **This seat proposed a selection rule that is the
negation of the record's own mechanism, from memory, without checking.**

**What is actually there is better, and is this arc's centre.**

## THE CONFIGURATION AXES — declared first

| axis | values | this arc |
|---|---|---|
| **which buried item** | U1 identification · U2 the cap · U3 the tuning's shape · U4 the labour split | **all four** |
| **U2's population** | the object's tower · the closed fillings of it · the full closed census | **all three** |
| **U2's degrees** | prime only (where `h¹` is EXACT) · all | **prime for the sharp claim; composite reported separately** |
| **what may be claimed** | mathematics · physics | **mathematics only; every physics sentence is a reading and is marked** |

## THE CELLS

**U1 — THE IDENTIFICATION: are the growth constant, the Alexander root and the "golden locus" one
number?** `α = (3+√5)/2`; is `α = φ²`? `Δ_{4₁}(t) = t² − 3t + 1` — are its roots `φ^{±2}`? The
reducible (abelian) locus of a knot group's character variety sits at meridian eigenvalue `m` with
`Δ(m²) = 0`, so `m = φ^{±1}` — is **B1418's "golden locus" on `m004`** that locus?
*Prediction:* **all four coincide**: `log α = 2 log φ = 0.9624236501…` is xB029's measured tower
growth constant, `log` of the Alexander root, and the locus B1418 names golden.
***E82 BINDING — THE LINK GRAPH IS WRITTEN BEFORE THE CLAIM.*** Each edge must be **exhibited**, and
**connectivity checked**, before any sentence of the form "these are one fact". **An edge this seat
cannot exhibit makes its endpoint an INDEPENDENT hat and it keeps its weight.** The edge most at risk
is *"the record's B1418 golden locus IS `m = φ`"* — that is a claim about **another arc's object** and
must be **read from B1418**, not assumed.
*Fence:* if it holds it is a **renaming**, not a discovery — and an identification, so **E82/I-10
applies and nothing is promoted without the ledger.**

**U2 — THE CAP, AND THE KILL CONDITION IS THE OUTCOME THE PROGRAMME WANTS.** The record's generation
count is bounded by `h¹`. xB029's F3 measured `b₁` of cyclic covers of closed hyperbolic `ℚHS³` and
found the nonzero ones **saturated**: `n=5`, degree 11, `b₁ = 10 = 11−1`; `n=7`, degree 29,
`b₁ = 28 = 29−1`. **For PRIME `d` this determines `h¹` exactly**, because `H₁(Q_d; ℚ)` is a
`ℚ[ℤ/d]`-module `≅ ℚ ⊕ ℚ(ζ_d)^a`, so `b₁ = a(d−1)` and **`h¹ = b₁/(d−1)`, the common value on the
Galois orbit.**
> **THE QUESTION, PUT AS PHYSICS:** the closing gets chirality from `h¹`, and `|I| ≤ h¹`. **Three
> generations on this mechanism REQUIRES `h¹ ≥ 3` somewhere.** Is `h¹ ≤ 1` a law of this class, or an
> accident of the manifolds looked at so far?
*Prediction, sealed:* **`h¹ ≤ 1` throughout** — `b₁ = a(d−1)` with `a ∈ {0,1}` on every prime-degree
cyclic cover of every closed hyperbolic `ℚHS³` tested.
***KILL CONDITION, BINDING, AND IT IS THE RESULT THE OWNER WANTS:*** **any prime `d` with
`b₁ = a(d−1)`, `a ≥ 2`** means `h¹ ≥ 2`, the cap is **false**, `|I| ≥ 2` becomes reachable, and
**"three generations is not derivable from this object" loses its structural reason.** **This seat is
predicting against the programme's interest and says so now.**
*Built-in consistency control:* for prime `d`, `b₁` **must** be divisible by `d−1`. **Any `b₁` that is
not is a computation error, not a finding**, and the cell must halt on it rather than report it.
*Vacuity control:* the population must contain manifolds with `b₁ > 0` covers at all — an all-zero
column measures nothing.

**U3 — THE COSMOLOGICAL-CONSTANT TUNING IS DIOPHANTINE, QUANTIFIED.** xB029 found 92.3 % of the
source's `P_eff` comes from `4sin²(Gπλ/k)` being near-minimal at `Gλ ≡ 1 (mod k)`. **Measure the
shape of that requirement** across the lens family: for each `k`, over all admissible `λ`, what
fraction reaches `P_eff ≥ 84`, and what is the exact scaling when `Gλ ≡ ±1`?
*Prediction:* the achieving set is governed by `‖Gλ/k‖` (distance to the nearest integer) alone;
`P_eff ≈ (1+2M)log k − M log(4π²)` at `Gλ ≡ ±1`; and the **fraction of `λ` reaching 84 is `O(1/k)`**
— *a one-in-`k` coincidence, not a continuous scan.*
*Fence:* this describes **the source's own worked example**, not the landscape. It is a statement
about `S³/Z_k`, and the source says the general case is unknown.

**U4 — WHERE THE LABOUR DIVIDES** — documentary, by quotation from both sources, with the two
chirality mechanisms named and contrasted. **No prediction; it stands on its quotes.**

## WHAT THIS ARC WILL NOT CLAIM

Not that `h¹ ≤ 1` is proved (a finite search is not a theorem) · not that U1's coincidence is a
discovery rather than a renaming · not that U3 says anything about G₂ manifolds that are not lens
spaces · **no crossing of Gate 5** · nothing to `CLAIMS.md`.

## THE SEAT'S PRIOR

U1 holds, **and the at-risk edge is the one to B1418** · **U2 holds, which is the WORSE outcome for
the programme, and this seat would rather be refuted here than confirmed** · U3 holds · **U4's
honest content is that the literature takes chirality from CONICAL SINGULARITIES and demands `h¹ = 0`
on the three-cycle, while the record takes chirality from `h¹` on the three-cycle — two different
mechanisms, and the record's caps generations at one.**
