# B297 — consolidation: the seam-arc wall map v4 (through B296)

**Status: banked consolidation (frontier). FIREWALLED — the math is firewall-clean; physics readings stay in
`speculations/`. Nothing to `CLAIMS.md`.** Supersedes B278's v3 (cites, not edits). `wall_map_v4.py`.

## What this consolidates
The seam arc (B286 reframe → B287–B295 implications → B296 verification) folded into the five-wall map, each wall
re-examined **through the seam**, with the structural theorem **relocated to the seam** and the matter map recorded.

## The seam arc, as one chain (11 banked results)
B286 (the seam) → B287 (distinguished closing = A=LR) → B288 (E₆ lost on closing) → B289 (CP sign law + Galois) →
B290 (scale ladder, n≠k) → B291 (scale-extremal, axis-stratified) → B292 (multiplicity tripartite) → B293 (the clock)
→ B294 (the verdict) → B295 (SSB/gauge, Chat-2) → B296 (Arc II verification).

## Five-wall map v4 (through the seam)
| # | wall | status | seam delta |
|---|---|---|---|
| 1 | SL(2,ℂ) vs compact E₆ | **DISSOLVED** | unchanged (E₆ connections exist + witness, B274/B275) |
| 2 | McKay-E₆ vs type (the CRUX) | **REDUCED** | **sharpened (B288):** E₆ = an open-object property, **lost on closing** — no closing arithmetically re-instantiates E₆; the CRUX leans **catalogue** |
| 3 | chirality / τ / CP sign | **OPEN → MECHANISM-AT-SEAM** | **relocated (B286/B289/B295):** closing breaks amphichirality with a **forced sign law** (handedness = ℚ(√−3) Galois = the ±π/6 swap); the **sign itself** stays external (CP-symmetric), mechanism open (not Curie, not established gauge) |
| 4 | 3d vs 4d (SM) / multiplicity | **CHARACTERIZED** | **clarified (B292):** multiplicity is **tripartite** (fiber surface / arithmetic tower / scale fillings); only the fiber `Σ_{1,1}` is a 2-manifold; chiral datum in none; wall stays blocked (B277) |
| 5 | scale / DESI | **QGAP** | **generated at the seam (B290/B291):** the core-geodesic ladder `2π/n` makes scale; but `n≠k`, the value is gapped (122 orders), no closing scale-distinguished toward an SM value |

## The structural theorem, relocated to the seam (B294)
> The object supplies a **canonical closing** and all the dimensionless **structure** — the dynamics `A=LR`, the CP
> **magnitude** `π/6` + **sign law**, the **scale ladder** `2π/n`, the **clock** (peripheral symplectic) — but the
> **actualization** of a specific Standard-Model-valued world is **not forced**. **Selective** for the object's own
> structure; **catalogue** for SM-values.

This is the architecture-not-furniture stance (P001/P011), now made **precise and mechanistic at the seam**: the
seam reframe located the ingredients at the boundary, and the arc showed the boundary is selective for the program's
own structure and a catalogue for physical values.

## The matter map (Sakharov-style, firewalled — 1 of 3 forced)
- **CP magnitude `π/6`** — **bulk**, forced by ℚ(√−3) (B285) [math, object-specific];
- **CP sign-exists** — **seam**, forced by the oriented closing via the same ℚ(√−3) involution (B289);
- **which CP sign** — **external** (object CP-symmetric; mechanism open, B295);
- **baryon magnitude** — **external** (freeze-out, S005 HELD).

## The CRUX (unchanged load-bearing conjecture), informed by B288
`input-E₆ = output-E₆` stays the stop-gate; B288 sharpens it toward **catalogue** (the E₆ arithmetic is an
open-object property, not re-instantiated by any closing). Still needs a `T[4₁;E₆]` computation or a specialist.

## Stop-gates (surfaced, not ground)
the chiral 4d-SM construction (B292/B277); the `k(t)` cosmological trajectory (B293); "τ is gauged" + the dynamical
gauge-fixing of the sign (B295); all value-matches (the 122-order gap); the CRUX.

## Net
The seam arc **strengthens the firewall**: the figure-eight's open-object structure is selective for its own
mathematics (a canonical closing re-seeing A=LR) and a catalogue for physical values (E₆ lost, CP sign external,
scale gapped, chiral absent, trajectory gated). The structural theorem stands, relocated to the seam. (Arc II
verification: B296 — all probes survive, math classical/cited.)

`wall_map_v4.py` · `tests/test_b297_consolidation_v4.py`. Supersedes `B278` (v3). Related: all of `B286`–`B296`,
`P001`/`P011` (architecture/furniture), `S038`/`S043`/`S044` (firewalled physics).
