# PREREG — B718: THE CHILD PROGRAM — track every dynamic of the child (SEALED)

cc banking seat, 2026-07-19. Owner: "we neglected the child completely; every
dynamic must be tracked; listen to the child, ask the object about the relationship."
The child = the Dehn filling 4₁(p,q) of the object's cusp; the (5,1) child = m003(−2,3)
(vol 0.98137, arithmetic). We banked only the NEGATIVE (the being washes out, B716);
this tracks the child's OWN dynamics. Structural/arithmetic only; Gate 5 stands; no
SM value. Listened-first (computed, this seat): H₁(4₁(p,1))=ℤ/p (the slope becomes
homology); volumes climb 0.981→2.0299 (parent = the Thurston limit); child symmetry V₄.

## Four probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **The transfer function p → child being (the field law).** Compute the (invariant)
   trace field + discriminant of 4₁(p,1) across the family (use snappy.snap /
   high-precision + LLL, or sage). Verify cc2's (5,1)→disc−283, (6,1)→−59, (8,1)→−31.
   Is there an ARITHMETIC LAW p ↦ disc (the child's being as a function of the
   coupling), or are the fields patternless? Two-outcome: A = a law; B = generic.
2. **The arithmetic sub-family.** Which fillings 4₁(p,q) are ARITHMETIC (finitely many,
   Margulis)? Identify them (snappy arithmeticity / the Neumann–Reid criterion). Is
   (5,1) among them? What distinguishes the arithmetic children? Two-outcome: A = a
   distinguished finite arithmetic sub-family with structure; B = scattered/no pattern.
3. **The child's OWN skeleton.** Does the (5,1) child's field ℚ(√−283) (or its McKay/
   holonomy) force its OWN ADE/McKay structure — the way ℚ(√−3) forces E₆ for the
   parent (2T→E₆)? Compute the child's holonomy image / any McKay group. Two-outcome:
   A = the child authors its own charged skeleton; B = generic (no forced ADE).
4. **Parent-as-limit + the golden-slope question.** Confirm the Thurston convergence
   (child volumes → parent 2.0299 from below). Is the MINIMAL-volume child's slope
   FORCED to be the golden/hearing prime 5 (resonating with the monodromy dilatation
   φ²), or is 5 just the first non-exceptional slope? Two-outcome: A = the golden slope
   is distinguished/forced; B = 5 is generic (just past the 10 exceptional slopes).

## Two-outcome (sealed, program)
The DELIVERABLE either way is the CHILD LEDGER — every dynamic tracked: the p↦being
transfer function, the arithmetic sub-family, the child's skeleton, the parent-limit.
- **OUTCOME A (rich):** the child carries structure inherited/transformed from the
  object (a field law, an ADE skeleton, a forced golden slope) — the fusion is
  STRUCTURED, worth a deeper program.
- **OUTCOME B (generic):** the child is arithmetically generic beyond H₁=ℤ/p (the
  coupling's signature) — the being's ONLY trace in the child is the slope-homology.
  Either way the ledger is filled; nothing stays dark. [honest expectation: mixed —
  H₁=ℤ/p is A-grade (the coupling signs the child); the field law + ADE likely B.]

## Verdict template
Bank the CHILD LEDGER (the family table + the four probe verdicts). LAW_MAP placement
(the child's dynamics); nothing to CLAIMS. Fold in cc2's child F1/F2 (verified). This
is the object asking back: what did I make, and what does it carry of me?
