# PREREGISTRATION DRAFT — B1075: THE MODULI CROSSING (coupling → |V_ij|, the kind table's own admissible pairing, never before contacted)

## SCOPE
The fourth sealed crossing in CROSSING_REQUIREMENTS' sense. Channel: COUPLING (R10
declared). Anchors: NONE (R11's open lane — nothing consumed). The comparison happens
ONCE, after this seal, against then-current data (the one-shot rule; B1063's refresh
precedent governs any future refresh, pre-committed here: none).

## THE PREDICTION (field-side, sealed before any data contact)
At the DERIVED listener (B1071's sealed pair — a theorem, not a convention), the tone
magnitudes of the weld family are EXACTLY
  |tone|(m=1) = |tone|(m=4) = 1/(2φ) ; |tone|(m=2) = |tone|(m=3) = 1/2 ; |tone|(m=5) = 1
— the 2+2+1 structure carried by the m ↔ 5−m conjugation (B1073's gate; the sealed
C3). The candidate value set for moduli contact is V = {1/(2φ), 1/2} — the m=5 value 1
is EXCLUDED IN ADVANCE as a modulus candidate by unitarity (a 3×3 unitary row with a
unit modulus has no mixing; mixing exists is Tier-0 knowledge, not a data peek), and
its exclusion is stated here so it cannot be quietly dropped at comparison time.

## THE CLAIM (set-level, zero assignment freedom taken)
C: at least one PMNS modulus |U_ij| equals a member of V within the precision budget
(R6, below). NO tone-to-(i,j) assignment is made — none is banked, so none is chosen;
the full look-elsewhere over 9 moduli × 2 values = 18 comparisons is priced (R7).

## BANKED IDENTITY: before any new number is read, the execution pipeline reproduces
## inside itself, from the banked instrument (B1011's exact model + B1071's sealed
## derivation), the five tone values ±1/(2φ), ±1/2, +1 and the 2+2+1 conjugation
## structure — exact match required, else the run aborts unsealed.
## PRIOR ART: the design-time record — the R1 disclosure section below IS the sweep
## (B743's catalogue, B856's kind-refuted category-wrong pairing, B1066's consumed
## pairings), run with the check shown (the R1 appendix); the tone values' internal
## provenance: B641 → the web seat's composition gate → B1071's seal (the credit
## chain banked at B1073).

## R1 (the searched-ground disclosure, the check shown in full in the R1 appendix)
The Re-channel values are catalogued in B743's sealed tower artifact (3/5, untested by
its executed sweep); tone-vs-sin²θ₁₂ was tested at B856 (+0.16σ) and REFUTED ON KIND —
the kind-correct moduli pairing has never been contacted; B1066 consumed the
tones↔sin²θ (R-B) and probability↔sin²θ (R-A) pairings; THIS pairing (tones↔moduli) is
a new pose under a new seal, exactly the form B1066's own language licenses. The
SECOND-SHOT status of the tones row is priced (R7), not hidden.

## R5 (kind): amplitude-part magnitude ↔ amplitude modulus — the kind table's K2 row
("CKM/PMNS moduli |V_ij| — admissible: |tone|-type"). PMNS chosen over CKM: the
coupling channel's banked contact history is leptonic (B856/B1066 precedent); CKM is
NOT tested here and remains unconsumed — stated so the choice is one declared bit (R7).

## R6 (precision budget): computed at seal time from the target class's public digit
count (moduli known to ~2-3 significant digits with δ-correlations): the budget line
states what |Δ| counts as a hit at N ≈ 1.43·d·log₁₀(H) with the 18-comparison
correction; if the corrected power cannot exclude at 2 digits, THE PREREG DOES NOT
SEAL (the register's own line: a prereg that cannot check every row does not seal).

## R7 (the freedom ledger, in bits): the 18-comparison look-elsewhere (log₂18 ≈ 4.17
bits); PMNS-over-CKM (1 bit); the twisted-vs-raw weld convention (0 bits — |·| kills
the sign); the listener member (0 bits — |tone| is pair-symmetric); the m=5 exclusion
(0 bits — Tier-0 unitarity, declared above). TOTAL ≈ 5.17 bits, entering the
significance arithmetic explicitly.

## R8 (the declared prior): MISS EXPECTED, and the design is informative either way
(B925's discipline): a priced hit at 2+ digits would be the programme's first
kind-correct value contact; a miss retires the tones row's last admissible pairing and
completes the coupling channel's consumption — a real result either way. HIT-SHAPE
(within budget but under power) is graded BELOW success in advance (R9); designer
contamination acknowledged: the +0.16σ B856 history is known to the designer.

## R2/R3/R4/R10/R11: interpolation not assumed (the tones are RG-insensitive exact
algebraic numbers; the taxonomy row is cited at seal); the identification routes
through the observer/coupling layer (R3 ✓ by construction); normalisation discharged
(B1012, R4); channel declared COUPLING, no mixing with the pair channel (R10); anchors
none (R11). The location clause binds; the twelve faces are filled as rows in the
sealed appendix. The B1038 Jarlskog rider does not bite (no phase content used).

## OUTCOME GRAMMAR: HIT (≥1 of 18 within budget at power, look-elsewhere-corrected
significance stated) / MISS (none within budget — the tones row completes its
consumption; the coupling channel's value story closes) / UNPOWERED (the R6 line
failed — recorded, unsealed, no comparison made). The paper carries the outcome
verbatim in all three cases.

## Gate 5: no measured number appears in this file. The comparison values enter ONCE,
at execution, from the then-current global fit, version-pinned (the B1063/B1066
data-currency lesson: the newest release, checked at execution day, not remembered).
