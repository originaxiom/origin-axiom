# B1112 — THE PROJECTIVE HATCH: the object's own unlifted geometry selects the trinification landing (SM-facing selection ambiguity = ZERO bits)

**Status: banked (frontier). Verdict PROVED. Harvest arc — the criterion is the
OWNER's catch (the spin-structure bit B1098 priced over without naming), independently
computed by an outside bench (PROJECTIVE_HATCH.md, 16 Levi-regular strata) AND completed
here over all 20 strata through B1098's own constructors. THREE-WAY convergence. Gate 5
untouched. Lock `tests/test_b1112_projective_hatch.py`.**

## The overlooked bit (the owner's observation, verbatim in spirit)

B1098 composes the object's holonomy ρ: π₁(M) → SL(2,ℂ) with sl₂-embeddings and prices
the stratum at 1-of-20 ≈ 4.3 bits, "no selection mechanism claimed." But the object's
CANONICAL holonomy is **PSL(2,ℂ)** — the geometric representation. The SL(2,ℂ) lift is
a CHOICE: H¹(M; ℤ/2) = ℤ/2 (H₁ = ℤ), two lifts differing by the sign character — a
**spin-structure bit the object does not supply**. A composed holonomy φ∘ρ is
lift-independent ⟺ φ kills −I ⟺ **all 27-weights of the class's sl₂ element H are
even**. Call such a stratum **PROJECTIVE**: defined by the object's own geometry with
no inserted choice. (E₆'s center is ℤ/3, no 2-torsion, so this even-orbit dichotomy
is the classical one — the criterion is a textbook invariant aimed at the right question.)

## The computation (all 20 strata, this bench)

Through B1098's own strata constructor (the 16 Levi-regular JM elements over the 31
simple-root subsets + the 4 distinguished-non-regular from its random search), each
class's exact 27-spectrum of H read off the representation. Parity verdict per class:

**9 of 20 strata are PROJECTIVE (EVEN):** dim_c ∈ {0, 0, 0, 1, 2, 4, 8, 14, 16}.
Among the 16 Levi-regular: exactly **6** — reproducing the outside bench's independent
count digit-for-digit. The bound "6–10" they could only estimate is now **pinned at 9**.

## THE SELECTION THEOREM (SM-facing, complete over all 20 strata)

Of B1098's two SM-compatible landings:

| landing | centralizer | 27-parity | projective? |
|---|---|---|---|
| **A2** (the hatch) | su(3)⊕su(3), rank 4 | **EVEN** {−2:6, 0:15, 2:6} | **YES** |
| **A1** | su(6), rank 5 | **ODD** (weight +1, mult 6) | no — needs the lift |

> **Exactly one SM-compatible landing is projective: the trinification landing (A2).**
> The su(6) route requires the spin bit; the excluded 2A1 row would too. **The
> object's own unlifted geometric holonomy reaches exactly one SM-compatible landing,
> and it is the one whose 27 is COMPLEX (B1100).**

The A2 EVEN parity is **exact, not float**: its spectrum IS B1100's banked branching
multiset {−2:6, 0:15, 2:6}, which carries its own independent lock — so the theorem's
load-bearing fact rides existing certification. And the **principal embedding** (the
E₆ entrance itself) is EVEN: the programme has been implicitly projective on its
entire main line all along.

## THE REPRICING (the freedom ledger's most physical row)

The hatch's freedom-ledger row SPLITS:
- class-choice restricted to the **projective menu**: log₂ 9 ≈ 3.17 bits (the object's
  own reachable menu, not 20);
- **SM-facing choice within it: UNIQUE → 0 bits of SM-facing ambiguity.**

The full 4.3-bit price applies only if one first buys the lift bit the object does not
offer. **A1's SM product structure (F2's MATCH-UP-TO-FRAME result, same day) costs
exactly one spin bit** — a ℤ/2 closing-type choice. This is the second instance of the
deflation pattern (first: Wilson-menu orbit homogeneity — priced choices dissolving
under a forgotten symmetry): here the forgotten quotient is −I.

## The located bit (interpretive register, labeled — but earned)

The lift bit IS a spin structure — the fermionic door. The object, being geometry, is a
PSL(2)-creature: bosonic, projective, lift-free on its main line. The strata that need
the lift are exactly the ones it never selects. **The spin bit is the first thing the
observer pays that the object cannot** — a located referent for B994's honest limit
(chirality/registration is external), the torsor no-sections, and the tick's ℤ/2.

## Method / fences

Parity read from float eigenvalues of the exact rational rho27(H) rounded to integers
(integer sl₂-spectrum, separation > 1 — a proof at this margin); the SM-relevant A2
row is exact (= B1100's banked multiset). The 4 distinguished-non-regular classes are
below SM rank (B1098's table) and cannot change the SM-facing conclusion. Live
re-run needs the session certificate (env `B1098_CERT_PATH`); the lock verifies the
stored parity facts + the A2/B1100 cross-check (independently locked).
