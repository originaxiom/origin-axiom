# B396 (Closure II / M4) — PRE-REGISTRATION: the all-pairs trace-formula gate

**Committed before computation. Target: the complete-table derivation (D3(c)) — verify the
P64 factorization C[j,l] = ζ₆⁻¹·tr(W_{m₁}ʲW_{m₂}ˡJ⁻¹)·ζ₁₅^{Q_γ′(1,1)} on its domain
(det(γ′−I) invertible mod 15) for ALL SIX banked pairs, not just (1,2). γ(W_m) =
[[1+m², m],[m, 1]]; J = ζ₆⁻¹XZ·Par (P57).**

REGISTERED EXPECTATION: 100% domain match for every pair (the formula is universal — its
derivation used no (1,2)-specific input). KILL: any pair with domain mismatches — that
would localize a genuine anomaly of the twist at specific seeds (bank the mismatch table;
a major event). Either way, the banked value dataset becomes fully formula-derived (with
the boundary cells enumerated per pair).

Machinery: B382's assemble_constant.py pattern generalized; exact engine; detached with a
PID-watcher. Locks from the output JSON. Time-box: 1 session. Firewalled.
