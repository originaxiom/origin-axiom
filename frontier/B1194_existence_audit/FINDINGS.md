# B1194 — THE EXISTENCE AUDIT

**Verdict**: `PROVED` · **instrument**: false · **creates_law**: false
**Banked**: 2026-08-28 (`bb018041`) · **This document authored**: 2026-08-29 (R52-4 discharge, B1207)

> **Provenance of this document.** The arc banked its verdict, its results file and its
> verification artifacts, but no findings document -- the gap B817's writer-safety gate caught in
> the first full OA_SLOW run (R52-4, 2026-08-29). What follows is authored **from this arc's own
> banked record**: `arc_verdict.json` is primary, with `b1194_results.json` and `verification/` beside it.
> Nothing is supplied from memory and no computation is re-narrated that the record does not
> carry -- the exact mirror of B1176's thirteen retro `arc_verdict.json` files, where FINDINGS was
> the primary and the verdict the missing half. Section 1 is the banked claim, segmented at its
> own enumeration; section 2 lists the artifacts that certify it.


## 1. The finding, as banked

THE EXISTENCE AUDIT (owner-directed: no 'missing' without exhausting the repo; six lenses + twelve adversarial checks, archived verbatim): THE HAVE list is twelve rows deep (arena law, one-generator clock, the integer meter with exact Borel mechanism, the zero-free-constant action, the forced content, the opened rank wall, the three coupling mechanisms, parameter-free 2+1 gravity with the omega saddle, the adelic unification number, the six-leg observer assembly closing zero-orphans, the ten-negative value wall AS a theorem, the priced input floor). THE PROVABLY-CANNOT list is eight walls each naming where the ingredient lives instead (values -> the coupling; scale -> the sigma anchor; chirality -> the cut, proved four independent ways; the bit's self-supply -> relational; the 4d filling -> a label; three-ness -> E8-external; orbit-point picking -> the closer; the metre -> external by design). THE GENUINELY-MISSING list is EIGHT items, each with a named open cell, ranked by leverage:

**(1)** SEAM-A's CS=0 stationary-phase door;

**(2)** the lambda placement (both seats' top residue);

**(3)** L154's boundary character (deletes the last continuous input);

**(4)** the relational-bit norm-classification;

**(5)** the P^3 adjudication;

**(6)** D2's sector adjudication;

**(7)** the Selector formalization + lock repair;

**(8)** THE COSMOLOGY LEDGER -- dark matter, inflation, the initial condition are the one genuinely blind region (zero dedicated modern arcs, grep-confirmed; the named first move 'one cosmology specification ledger' never made) -- while the big bang question itself is answered structurally (the genesis chain priced at one deep axiom + two engineering choices; no-beginning proved: compact base, no infinite past; the bang re-framed as the onset of non-cancellation, kappa-2 = omega^2 the unit obstruction; the Sakharov gate structurally present with the honest no-rate disclaimer) and the object's OWN cosmological constant is PROVED (Lambda = -1 in own units) with the measured Lambda a value-wall question. Lens flags carried: EX-6's survives=false and four classifier-unavailable notes recorded -- the synthesis is banked at ASSEMBLED grade with its lens verdicts archived beside it. Gate 5 clean.

## 2. The certifying record

- `arc_verdict.json`
- `b1194_results.json`
- `verification/audit_cells.json`

## 3. Status at authoring

The verdict field is authoritative and unchanged: `PROVED`. This document does not re-adjudicate it; it makes the arc readable without opening its JSON, which is the whole function the missing file was performing badly.
