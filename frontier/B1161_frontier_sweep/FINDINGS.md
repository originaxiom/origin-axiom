# B1161 — The frontier sweep (compute-all-we-can): the computable frontier mapped and exhausted; the bypass door IS SEAM-A

**Status: banked (frontier). Verdict OPEN** — a **compute-all-we-can capstone**: the owner's "compute all we
can, then review" directive, executed as a 6-cell frontier sweep (WF, 7 agents), each cell typed and the
load-bearing ones **re-verified on this bench** (`verification/reproduce.sh` → `REPRODUCES`;
`verification/frontier_map.txt` = the full map). The headline is a **unification**, not a crossing: the two
open structural frontiers — the bypass door (forcing C's branch selection) and SEAM-A (the heterotic axiom)
— are the **same obstruction**, the missing archimedean marking. No firewall crossing; Gate 5 clean across
all six cells.

## The deep synthesis — the bypass door IS SEAM-A

The crown cell tried to **force C's P1/P2 branch selection object-intrinsically** (which would derive the
MSSM spectrum without the walled heterotic axiom). It is **NOT-FORCED, and the residual is PROVED
IRREDUCIBLE** by a theorem: **V₄ = Gal(H/ℚ)** (H the ring class field, disc 144, B1067) acts **freely and
transitively** on the 4 surviving primary branches — all stabilizers trivial — so **every Galois-invariant
object quantity is constant across them**; no object-intrinsic invariant can pick a branch, and each carries
an unselected 11-dim moduli family. But the free-orbit theorem forbids only *Galois-invariant* selectors —
an object-supplied **archimedean marking** (a canonical H↪ℂ, the actual holonomy root) *would* break the
orbit and single out a branch. **That marking is exactly SEAM-A's absent W₀ / marked-H³ basis.** So the
crown's irreducibility reads precisely *"irreducible while SEAM-A stays walled"* — **the bypass door and
SEAM-A are one obstruction seen from two sides.** The program's remaining structural blocker is a *single*
missing object — the ∞-place archimedean marking — not two independent walls.

## The six cells (own-verified where marked; `frontier_map.txt`)

| cell | outcome | result |
|---|---|---|
| **P1/P2-force** | **NOT-FORCED** | bypass door shut; free-orbit theorem (proved-irreducible). Scoped positives: C4×C3 is the *unique coprime CRT split*; the odd-leg set {4,8} is K-rational — link C's decomposition + leg-set gain object footing, but branch selection + moduli stay observer-paid. |
| **L132-B892-frame** | **CONVERGES** | hypercharge forcing now **dual-homed** — B892's color-A₂ centralizer = A₂+A₂; the weak A₂'s 3 sl₂'s give 36/36 SM, zero non-SM, same as B1160's trinification frame (backbone own-verified). |
| **SEAM-A-seal** | **NEEDS-SPECIALIST** | the proposed PSLQ non-relation sealer is **vacuous** (own-verified: PSLQ is live — recovers B682 as [−2,3] — but PSLQ(rational,Vol)=None for the claim *and* controls, just restating Vol∉ℚ). Cannot seal MISMATCH; SEAM-A stays the FLOOR with the exact Arakelov/W₀ bar. |
| **down-Yukawa** | **BENCHABLE / WITHHELD** | unlike the up-Yukawa (μ_u=0, *absent* — SEAM-Y wall), the down coupling is **object-forced and structurally PRESENT** as a full 3×3×4 tensor (own-verified selection arithmetic: Q/dᶜ/Hd/Hu = 3/3/4/1, C12 imposes no texture zero); only the Higgs-line projection Y_d=T[:,:,h_d] is observer-supplied (the same ℙ³ branch class as P1/P2). The firewall thesis one level deeper. |
| **generation-index** | **NULL** | own-verified: trace field x²−x+1 (disc −3, degree 2, Galois ℤ/2) ⇒ multiplicities ∈{1,2}, **never 3**; χ(4₁ complement)=0. The object's honest content is **ONE generation**; the "3" is external (a cubic carrier / chosen cover / E8), never object-paid on this object. |

## The exhausted-frontier boundary (the "we computed all we can" line)

- **WALLS (imported/absent, not benchable):** link A heterotic (OA-C1002); SEAM-Y up-Yukawa (μ_u=0);
  branch selection (free-orbit theorem + moduli).
- **NEEDS-SPECIALIST (sharp bar):** SEAM-A — the Arakelov/W₀ bar (the PSLQ route now proved vacuous).
- **GENUINELY OPEN, not benchable on this object:** generation number 3 (NULL here; only external routes).
- **BENCHABLE-but-unbuilt:** the down-Yukawa char-0 3×3×4 tensor (its readout itself observer-gated).

Everything reducible to object group theory / toric cohomology / in-sandbox numerics is **done**. What
remains is two proved irreducibilities (branch selection, generation NULL), two walls (heterotic,
up-Yukawa), one NEEDS-SPECIALIST with a sharp bar (SEAM-A), and one buildable-but-unbuilt artifact
(down-Yukawa evaluator).

## What is bank-grade vs cited

- **BANK-GRADE (own-verified this run):** the SEAM-A PSLQ-vacuity + liveness (B682 recovered);
  generation-index NULL (degree-2 Galois ℤ/2); the L132-B892 backbone (A₂ centralizer = A₂+A₂); the
  coprime-CRT-split + K-rational-odd-leg decomposition positives; the free-orbit theorem (own-run
  `crown_p1p2.py` + adversarially verified); the down-Yukawa selection arithmetic (3×3×4).
- **CITED (single-homed / on report, provenance debt, not leaned on for verdicts):** the full 3003-bracket
  e₆/27 build and cloud's trinification 36-count (backbone re-derived, execution on report); the 11-dim
  moduli count + arXiv:1112.1097 Table-3; the down-Yukawa char-0 tensor entries (only GF(1009) tail reps
  off-branch); the Galois structure of H (cited to B1067 disc-144).

## Fences

No firewall crossing. Gate 5 clean — only integer/rational group theory, anomaly identities, Galois action
on branch labels, multiplicity counts, and irrationality facts; the SM hypercharge/spectrum ratios are
anomaly-**derived** structure (B950), no measured value asserted as derived.

## Routes

- **SEAM-A / L182:** the bypass door and SEAM-A unified — the one obstruction is the archimedean W₀. The
  SEAM-A specialist bar (Arakelov/W₀) is now the single highest-leverage open item; the PSLQ sealer is
  retired as vacuous.
- **Hypercharge (B1160):** dual-homed (two frames) — closer to a two-bench fact (cloud's cert still cited).
- **down-Yukawa:** a buildable char-0 evaluator cell (the 3×3×4 tensor), readout observer-gated.
- **Feeds the review** (next): the exhausted-frontier boundary is the review's anchor.
