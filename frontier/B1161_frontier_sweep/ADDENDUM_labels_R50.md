# B1161 addendum — two label corrections (Review 50, 2026-08-27; from the fresh-eyes cold audit, applied by append)

**1. "THE BYPASS DOOR IS SEAM-A" — labeled CONJECTURAL-SUPPORTED, not theorem.** The committed headline
reads as an identity. The honest grading: the **free-orbit theorem** (V₄ acts freely ⇒ no Galois-invariant
selector) and the **marking-breaks-the-orbit** implication are THEOREMS (own-run + adversarially verified);
that the bypass door's missing selector and SEAM-A's missing W₀ are **ONE object** is a **conjectural
identification** — both point at "the missing archimedean marking," and the subsequent work *supports* it
strongly (B1163: W₀ = the orientation, one obstruction three views; B1168: the mirror-parity law puts both
on the same odd side) — but "point at the same absence" has not been upgraded to "are the same cohomology
class." The identification is carried as SUPPORTED-CONJECTURAL; the ℤ/2-identification cell (the register's
Q1) is exactly the computation that would settle it.

**2. The down-Yukawa "PRESENT" — softened.** The committed cell reads "the down coupling is
object-forced+PRESENT as a 3×3×4 tensor." Precisely: the coupling is present **as a representation-theoretic
object** (the tensor exists and is object-forced); its **char-0 exact evaluation is unbuilt** (the
BENCHABLE-but-unbuilt evaluator, the same cell's own boundary note). "Present" = existence at the
representation level, not a computed table. No conclusion changes; the word is bounded.
