# B1161 addendum 2 — the bypass-door label UPGRADED: from SUPPORTED-CONJECTURAL to PROVED-AS-DECOMPOSED (2026-08-27, B1182+B1183)

The R50-era label ("the bypass door IS SEAM-A" = supported-conjectural) is superseded by a **proved
decomposition**. The bypass door's obstruction = the full branch-V₄ marking = **two bits, typed adelically**:
- **the archimedean component (the c-leg) = SEAM-A's W₀ = the orientation** — and by B1183's ONE-CLASS
  THEOREM this is the same ℤ/2 class as the QP-4 chord-sign obstruction: **proved one class**;
- **the finite component (the k7-leg) = the arrow/form-class label** (B1182) — arithmetic, finite orbits,
  escape-(1) available: **separately payable, NOT SEAM-A's**.

So the honest final statement: *the bypass door and SEAM-A share their archimedean component exactly (one
proved class); the bypass door additionally needs the finite k7 label, which is cheap and separate.* The
conjectural identity is replaced by this sharper proved structure.
