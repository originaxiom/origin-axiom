# B414 (Structural test 1) — PRE-REGISTRATION: is the object's forced ℤ/3 the generation space?

**Committed before computation. The first pre-registered STRUCTURE-to-STRUCTURE test
(S044 reframe): not a number match — a forced structural-count identification. Firewalled;
a structural correspondence banks as [MATH]+[HOOK], NEVER as a physics claim.**

## The registered question

Is the object's ℤ/3 EXACT and FORCED (no parameter freedom) and UNIQUE (one ℤ/3, not many
competing multiplicities)? A generation space must be exactly 3-fold, forced, and singled
out. Census the exact multiplicity structures across the banked object:
- the (2,3) Mercedes ℤ/3 (P61 stabilizer / the three equal-norm columns);
- the ζ₉⁺ orbit (the 405 singles, the 1215 triple — order exactly 3);
- the E₆ exponents' 3+3 split (2m mod 6);
- the existence-law doublets (order-2, NOT 3 — the control: if 2-fold structures are
  equally forced, the "3 = generations" reading is not privileged).

REGISTERED TWO-OUTCOME:
- **PASS (structural sighting):** the 3-fold structure is exact, forced, and PRIVILEGED
  (the object's fundamental multiplicity is 3, not 2 or arbitrary) — bank the forced ℤ/3
  as the object's generation-count analog [MATH fact + HOOK]; proceed to structural test 2
  (the selection-rule lattice vs anomaly/hypercharge structure).
- **FAIL (no privilege):** 2-fold and 3-fold structures are equally forced, or the ℤ/3 is
  parameter-dependent ⇒ the generation reading is not privileged; bank the honest negative
  (the object has no distinguished 3, so "3 = generations" is wishful).

VACUITY GUARD: the doublet control makes FAIL genuinely possible — the object DOES have
exact ℤ/2 structures (the class group!), so "3 is privileged" is a real, falsifiable claim.

## Machinery
Banked exact data (B380/B401/B394 + the triple); a census + a forced-ness check (does the
structure survive parameter variation / is it symmetry-forced). Exact; locks. NO promotion.
