# B381 (D2) — the twist isolation: pre-registration (before computation)

**The statement to establish or refute: THE SEAM IS A TWIST INVARIANT.** Two level-15
quantizations of the same cat map exist in the banked record: the THETA lift (D_θ = diag
ζ₁₅^{j(j−1)/2}, WR_θ = F·D_θ⁻¹·F⁻¹ — the seam-bearing model, forced within its premise by C6)
and the CANONICAL lift (T = diag ζ₁₅^{j²}, S = F/g(15) — the standard cat-map convention;
Par-commuting; seam-null by B358/P19). D2 computes what exactly separates them.

**Q1 (the equivalence question):** does a unitary intertwiner U exist with
U·D_θ·U⁻¹ ∈ ⟨T⟩·(phases) and U·WR_θ·U⁻¹ ∈ ⟨S⟩·(phases) — searched over the natural candidate
family (Heisenberg translations X^m Z^n × diagonal quadratic-phase gauges × Galois relabels),
exhaustively at level 15 mod p with exact confirmation of any hit. Outcomes: (a) an intertwiner
exists up to a CHARACTER — the two lifts are projectively equivalent and the seam difference is
pure normalization convention (would WEAKEN C6's reading — bank honestly); (b) no intertwiner in
the family — the lifts are inequivalent as decorated models, and the obstruction is the TWIST.

**Q2 (the twist cocycle, exact):** compute the diagonal defect δ(j) = (theta phase)/(canonical
phase) = ζ₁₅^{j(j−1)/2 − j²} = ζ₁₅^{−j(j+1)/2} and its S-side companion; express the twist as
the half-characteristic translate it is; verify the P57 mechanism statement: in the canonical
model the inserted Par lies in the image's COMMUTANT (detects nothing — recompute the banked
null as a commutant fact), while in the theta model Par differs from the true parity J by
exactly the Weyl step (P57) — so the seam = the matrix elements of the gap Par·J⁻¹ = ζ₆·(XZ)⁻¹.

**The theorem-shaped deliverable (if Q1(b) and Q2 land):**
    seam ≠ 0  ⟺  the quantization carries the half-characteristic twist,
with the twist exhibited as an explicit cocycle/translate and the seam identified as the
Weyl-step matrix elements the twist exposes. KILLS: Q1(a) (equivalence) reframes rather than
kills — banked as the honest weakening; any failure of the commutant recomputation kills Q2.
