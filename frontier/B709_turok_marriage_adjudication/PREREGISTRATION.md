# PREREG — B709: THE TUROK MARRIAGE — adjudication (SEALED before verdict)

cc banking seat, 2026-07-19. Incoming: chat-1's "THE TUROK MARRIAGE" handoff
(12 structural parallels, identifications I1–I5, tasks T1–T5). Owner pasted it.
This is a PHYSICS-FACING structural claim — the class the firewall + the
Listening Protocol exist for. Adjudication only: mathematics + the banked
record. Gate 5 stands; no SM value claimed. chat-1 itself labels every "IS" a
conjecture and asks proof-or-disproof with the firewall on — this arc answers.

Prior banked ground this lands on (verify-don't-trust, both seats):
- B701: measurement = a fiber-functor Galois torsor, provably NON-canonical.
- B707: the two-seat Turok verdict = INVERTED (his CPT ℤ/2 has a canonical
  fixed point, the bang; ours provably has no canonical origin).
- B706: the object's freedom is DISCRETE (𝔽₂ seam); the SM's is CONTINUOUS —
  a KIND-mismatch (wall 11); the F₂→ℝⁿ bridge is unbuilt.
- S3/B615: the dimensionful no-go (the program has no scale).
- memory two-chiralities-c-vs-theta: the object BREAKS c (conj/orient/time),
  is θ-symmetric; amphichirality ⟺ θ-symmetric ⟺ vector-like.

## The two-ℤ/2 partition (the load-bearing distinction, stated before verdict)
The object carries TWO different ℤ/2's, and the adjudication turns on which one
each chat-1 claim invokes:
- ℤ/2-A = AMPHICHIRALITY (K = K̄; CS = 0 at the geometric point). CANONICAL —
  it fixes a geometric slice.
- ℤ/2-B = the FIBER-FUNCTOR TORSOR (√5 → −√5 on the measurement side, B701).
  NON-canonical — no fixed basepoint.
Turok's CPT is CANONICAL (fixes the bang). So a defensible marriage can reach
only ℤ/2-A; any claim routed through ℤ/2-B is INVERTED, not identical.

## Per-item two-outcome criteria (sealed)

- **I1/T1 (CPT = amphichirality, embedded 3D→4D) — CC's crux.**
  A: a construction embeds the fig-8 mapping-torus suspension coordinate as the
  cosmological TIME of a 4D (FRW) spacetime, with a metric — marriage rigorous.
  B: no such construction; the suspension time is INTERNAL to a compact-S¹,
  Euclidean-signature (H³, Λ<0) 3-manifold flow, categorically distinct from
  external Lorentzian-ℝ FRW time; and the value side stays ℤ/2-B-inverted (B701).
  → verdict = STRUCTURAL-ANALOGY-ONLY if B.
- **I4 (vacuum selection = fiber-functor choice, "resolves B706").**
  A: an explicit F₂ → ℝⁿ (discrete→continuous) map is exhibited. B: it is
  asserted, never built, and marries CANONICAL vacuum selection to ℤ/2-B
  (NON-canonical) — inverted; does NOT resolve B706. → OVERREACH if B.
- **T3 (N(v₀)=−6 → DM mass 4.8×10⁸ GeV).**
  A: a scale-free derivation yields a dimensionful mass. B: dimensionless
  integer → dimensionful mass needs a SCALE the program lacks (S3/B615).
  → REFUTED if B.
- **T4 (heart channel forced-zero ⟹ massless ν).**
  A: an intrinsic identity forces the free channel to 0. B: the channel is
  FREE; forcing needs external CPT machinery (the B706 bridge).
  → PERMITTED-NOT-FORCED if B.
- **I5/T2 (36 scalars = 36 E₆ positive roots).**
  A: 36 uniquely selects E₆ AND the E₆ root data reproduces Boyle–Turok's
  conformal conditions. B: 36 is shared (E₆, B₆=so(13), C₆=sp(12), all dim-78
  rank-6) — base-rate; count carries ~0 information. → DEAD if B.
- **I2 (C=θ-fold, P=being √−3, T=hearing √5).**
  A: a representation-theoretic identity forces the assignment. B: the θ-fold
  is the E₆ OUTER automorphism, NOT a V₄ element; assignment arbitrary; and the
  object BREAKS c while being θ-symmetric (so a c-type CPT is not the object's
  symmetry). → FIREWALL if B.
- **T5 (joint prediction registry).**
  A: object-DERIVED predictions exist to register. B: Turok's predictions
  (massless ν, Majorana, no B-modes, 4.8×10⁸ DM) are HIS, joinable only via the
  refuted marriage; θ₀=2/9 and sin²θ₁₂=1/(2φ) are rung-4 single-ratios with no
  mechanism (B685) — honest ONLY as INDEPENDENT numerological hints, never as
  derived joint predictions; and chat-1's claimed "existing hash" for sin²θ₁₂
  is itself unverified (no such sealed claim in the record). → the registry is
  NOT ours to bank if B.

## KEEP kernel (pre-declared survivor, rung 2–3, NOT a physics promotion)
The COMPLEMENTARITY (we derive the gauge group + 3 generations Turok assumes;
he has the 4D dynamics + vacuum selection we lack) and STRONG-CP-BY-MIRROR
(CS = 0 ↔ θ_QCD = 0, same solution TYPE). These are the B707 rung-3 echoes —
structural, no value.

## Verdict template (pre-stated)
Expected all-B: the "marriage" is REFUTED as an identity; CONFIRMED as an
INVERSION (ℤ/2-B) + a KIND-COMPLEMENTARITY (B706). One LAW_MAP placement row
(the two-ℤ/2 partition), HINT_LEDGER rows for θ₀/sin²θ₁₂ as independent-hints-
only, firewall notes for I2/I5/T3/T5. NO physics reading, nothing to CLAIMS.
A relay-back note to chat-1 with the corrections + the T1 crux verdict.
