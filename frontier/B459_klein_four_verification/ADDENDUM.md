# B459 ADDENDUM — Chat-2's group-action computation: two corrections, the within-level control, and the pre-registered reading for the incoming verdict

**Added 2026-07-07, on the owner's "mark everything" instruction, BEFORE Chat-2's control
verdict arrives. Firewalled.**

## Chat-2's new computation (the same exact data, re-read)

Chat-2 re-read the B459-verified table as group theory: 120 nothing-forbidden, 70 whole-group,
20+20 "the two nontrivial cosets," and **10 points with forbidden-set {√5,√−3,√−15} — "not a
subgroup, not a coset"** — read as a distinguished exceptional locus, with the verdict "210/240
obey the group law; the strongest exact affirmative structure the program has."

## Correction 1 — there is no exceptional locus (the dictionary was wrong)

In the **Galois-correspondence dictionary** (the banked B459 mechanism), the pattern
{√5,√−3,√−15}-forbidden is simply **"the value is rational" — the ℚ-row of the subfield
lattice** of ℚ(√5,√−3), as regular as every other row. One uniform law covers **240/240**:
*every point's value lies in a subfield.* The "87.5% pass / 12.5% break" split is an artifact of
forcing the subfield lattice through a coset lens (a subgroup/coset must relate to the identity;
subfields don't care).

## Correction 2 — the coset reading misses its own third coset

With channels as Klein-four elements (e=1, a=√5, b=√−3, ab=√−15), the cosets of the three
order-2 subgroups are {b,ab}, {a,ab}, and **{a,b}**. The first two occur (20+20). **The third —
{√5,√−3}-forbidden ⟺ "the value is purely ℚ(√−15)-valued" — NEVER occurs** (banked, B459).
Chat-2's frame flags a false exception and misses the true absence, which is the table's real
asymmetry (Law 2's content).

## The already-banked evidence bearing on "fig-8-specific vs level-15-generic"

- **The breakdown is already known to be ADDRESS-DEPENDENT within level 15**: B402's QR classes
  have dark = 120, NQR classes dark = 80 — so "the 120/70/20/20/10 table" is one address-class's
  table, not "the figure-eight's." Any specificity claim must survive the address scan first.
- **The honest cross-object control is fixed by the banked conductor law (B449)**: another
  bundle's seam lives at ITS conductor — silver at level 8, where ℚ(ζ₈) is again biquadratic
  (Klein four; subfields ℚ(i), ℚ(√2), ℚ(√−2)). "A different knot at level 15" mixes frames
  (15 is the fig-8's conductor).
- **The lattice SHAPE is equivariance-forced** (any Galois-equivariant family respects the
  subfield lattice) — the shape was never capable of being a fingerprint; only the COUNTS and
  the absent-pattern choice are data.

## The within-level control — RUN (exact ℚ(ζ₆₀); `pair_control.py`): OUTCOME B, decisively

| pair | tier table |
|---|---|
| (1,2) | 120 generic / 70 zero / 20 ℚ(√5) / 20 ℚ(√−3) / 10 ℚ — the "selection rule" |
| (1,3) | **NON-LATTICE**: 120 points with ONLY √−15 dead (s ≡ 0 identically — the banked dark pair), 75 zero, 30 ℚ(√5), 15 ℚ |
| (2,3) | 160 generic / 62 ℚ(√5) / 15 zero / 3 ℚ — no ℚ(√−3) row at all |
| (2,4) | 160 / 32 / 20 / 18 / 10 |
| (3,4) | 144 / 54 / 23 / 19 — no ℚ(√−3) row |

**Every pair has its own table.** The "120/70/20/20/10 selection rule" is the (1,2)-PAIR's
arithmetic, not the figure-eight's. And the (1,3) pair breaks even the lattice SHAPE (a
single-vanishing pattern, impossible in (1,2)'s table) — which corrects THIS record's own
overreach: B459's "one uniform law, 240/240" was itself (1,2)-specific. The refined honest law:
**per pair, values lie in the subfield lattice of the pair's VALUE-SPAN, and the span varies by
pair** (full biquadratic for (1,2); the s=0 hyperplane for (1,3); √−3-free spans for (2,3)/(3,4)).
The standing pattern, again: generic mechanism (equivariance), pair-specific labels (the tables).

## PRE-REGISTERED reading of Chat-2's incoming control verdict (fixed now)

- **Outcome A — same lattice shape, same counts** on the control: fully construction-generic;
  the strongest launder; the "affirmative lead" closes.
- **Outcome B — same lattice shape, different counts** (the expected outcome): the standing
  pattern — generic mechanism, object/address-specific labels. The counts then face the
  burden-inversion rule: derivable from the gating/CRT laws (B431's lines + the CRT cells) →
  LAUNDERS; underivable within a session → escalate per B398.
- **Outcome C — a different lattice shape** (non-subfield patterns on the control): would
  contradict the equivariance mechanism → treat as an anomaly; adversarial recompute of the
  control's construction FIRST (conventions are the usual culprit — the B355 lesson).
- In every outcome: ℤ/2×ℤ/2 ≅ "CP structure" remains a structural predicate that cannot fire
  alone (B452's frozen rule); "you cannot accidentally reproduce a coset structure" remains
  inverted (equivariance reproduces it automatically).
