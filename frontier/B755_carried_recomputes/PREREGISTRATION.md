# PREREG — B755: the five carried recomputes (the B741 sweep's NOT-LOCATED facts) (SEALED)

cc banking seat, 2026-07-22. The R27-carried block: five banked negatives whose
discriminating facts were NOT-LOCATED by the fact_computed sweep (frontier/
B738_pathfinder_compiler/FACT_COMPUTED_SWEEP.md — sealed input). One cell per id;
two-outcome each; E19 both directions (a recompute can CONFIRM the kill's basis or
EXPOSE it). Gate 5 throughout; B332's cell touches SM ratios ONLY as a null-test
adjudication (B655/B751 precedent; the stopping rule stands).

## Cells
1. **B140 — the genus-2 mirror CS-flip witness.** Build a chiral genus-2 surface bundle
   (snappy.twister, available in-env — it was MISSING at bank time), hyperbolic, and its
   mirror; verify vol(M) = vol(M̄) and CS(M) = −CS(M̄) (mod the standard normalization).
   Chirality check: M not isometric to M̄. Two-outcome: WITNESSED (the cited
   orientation-reversal theorem gains its first in-repo genus≥2 instance) / ANOMALY
   (report exactly; the citation is standard mathematics so anomaly ⇒ my construction).
2. **B332 — the 1/4-numerology null, ratio set DECLARED HERE** (the sweep's condition:
   the chat-1 set is not in-repo, so the set must be preregistered): targets = the B752
   sealed 25-value PDG dimensionless table (cited in-script), restricted to values in
   (0.1, 0.6) — the range where 1/4-suppression readings lived. Candidates: 1/4 vs
   controls 1/5, 1/e, 1/φ, 1/π, plus 40 seeded (seed 11) log-uniform randoms in
   [0.15, 0.45]. Windows 2% and 5%. Two-outcome: 1/4 NOT DISTINGUISHED (the kill's null
   basis is now computed in-repo) / DISTINGUISHED (the kill's basis was wrong — flag,
   E20-gate any reading).
3. **B685 — the GSWZ symmetrized-series 3-denominator fact.** The banked extraction is
   INTERNALLY INCONSISTENT as recorded (inline coefficients show denominators 5; the
   conclusion asserts pure 3-powers) — so the cell MUST re-read the source: fetch
   arXiv:2412.04241 (§1/eq (2)), implement the series from the PAPER's definition,
   expand Φ(h)Φ(−h) in (q−1) with exact rationals, factor denominators. Validation
   gates: reproduce the paper's OWN printed low-order coefficients AND the banked
   valuation claim (3¹⁴⁶ at order 100) if reachable; report the largest verified order.
   Two-outcome: PURE-3 CONFIRMED (the terminal theorem's Habiro leg gains its in-repo
   computation; the vacuous lock test_gswz_powers_of_three gets replaced by a real one)
   / NOT-PURE-3 (the leg REOPENS; B685's terminal theorem gets a correction header —
   state the consequences before computing, per E19). If the source is unreachable
   in-sandbox: BLOCKED-ON-SOURCE, uncounted, honestly reported.
4. **B720 — the computable halves of the threefold NO-MATCH.** (i) ℚ(ζ₃) vs ℚ(i)
   disjointness (exact: the compositum degree / intersection = ℚ); (ii) the Markov
   quiver is finite-MUTATION but not finite-TYPE (enumerate its mutation class — expected
   size 1 up to iso — and verify it is not an ADE orientation); (iii) flat-connection
   local-DOF count: dim H¹ = 0 generically for closed-manifold flat connections vs the
   2d phase-space statement (a bounded derivation, stated as such). The two literature
   classifications (CM mixed-Tate over ℤ[i]; ABHY finite-type) stay CITED and are
   FLAGGED as the irreducible cited residue — the cell computes the object-side halves
   only. Two-outcome per half.
5. **S019 — formulation + honest disposal.** (a) Repair the tombstone's BROKEN kill
   citation (speculations/archive/PHYSICS_RESONANCES.md contains no Fisher content) →
   repoint to FAILURE_ATLAS C4 + the E21/V6 proxy with proxy status stated. (b) The
   minimal formulation, posed and closed: ANY 1-parameter statistical family has a 1×1
   Fisher form ≥ 0; a Lorentzian reading requires ≥2 dimensions with indefinite
   signature — so "no Lorentzian emergence from Fisher-on-k" holds BY DIMENSION for any
   scalar-k family; computed instance via E21's banked probe machinery (Fisher(D(I)) =
   16·g_WP > 0). Two-outcome: the kill upgrades from heuristic to dimension-theorem /
   the formulation finds an obstruction (report).

## Verdict template
Each cell lands its own verdict; the arc's headline = the count LOCATED-BY-RECOMPUTE
(target 5/5 minus any BLOCKED). Consequences applied in-arc: lock replacements (B685's
vacuous lock), tombstone repair (S019), FINDINGS annotations on the originals ONLY where
a fact flips (E19). Nothing to CLAIMS.
