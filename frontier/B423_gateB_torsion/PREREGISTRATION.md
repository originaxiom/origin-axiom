# B423 (GATE B — the computation, not the argument) — PRE-REGISTRATION

**Committed before computation. The owner's decisive point: every wall test was a SYMMETRY
argument (does the structure ALLOW a frame — always no). This is the COMPUTATION (does the
structure PRODUCE one). The precedent: the seam's 1/48 came from computation, not symmetry.
Gate B (the E₆ torsion with the Fox matrix) is the one thing argued-about but never RUN.
Firewalled; name the output as math; judge by the emergence bar.**

## The computation (blind)

The E₆ adjoint Reidemeister torsion of the figure-eight at its holonomy. E₆'s adjoint (78)
decomposes as ⊕_i Sym^{2m_i} over the E₆ exponents {1,4,5,7,8,11} (B347: H¹ = 6, one per
exponent). The figure-eight is fibered with monodromy A = [[2,1],[1,1]] (trace 3, eigenvalues
φ², φ⁻²). The twisted torsion in Sym^{2m}(A) at the parabolic/geometric point:
    τ_m = det(I − Sym^{2m}(A)) = ∏_{k=0}^{2m} (1 − φ^{4(m−k)}),
and the E₆ adjoint torsion is τ_{E₆} = ∏_{i} τ_{m_i} over the six exponents.

## THE REGISTERED READ (blind, before computing)

Compute τ_{m_i} for each exponent and τ_{E₆}; then FACTOR the result and characterize:
- **GOLDEN/FLAT (the prior):** τ_{E₆} is a pure power-of-φ / Fibonacci-Lucas quantity (only
  primes {5} and Lucas/Fibonacci integers) ⇒ the computation CONFIRMS the symmetry verdict;
  the object is golden through-and-through; the papers are the right move.
- **STRUCTURE (the owner's bet):** τ_{E₆} carries integers/primes NOT reducible to φ/{5}
  (e.g. new primes, an unexpected factorization) ⇒ the computation produces structure the
  symmetry analysis could not see ⇒ the wall was a COMPUTATION wall ⇒ run the bar check
  (does the structure match an SM invariant) → if it clears, the escalation.

## The emergence bar (four-part, campaign-standard)
Any forced invariant of τ_{E₆} (its prime content, its factorization pattern, the per-exponent
"two-track" split) that is (i) forced (ii) unsought (iii) an EXACT SM structure (iv) survives
a control (the SAME torsion for a NON-figure-eight monodromy, e.g. the silver A=[[2,1],[1,1]]²
or trace-4 word) — clears the bar. Anything short = named negative.

## Machinery
Exact ℤ[φ] arithmetic (φ² = φ+1); Sym^{2m}(A) eigenvalues = φ^{4(m−k)}; det(I−·) exact.
Locks from JSON. This is the LEADING (torsion) term of Gate B; the full oscillatory state
integral's Hessian is the specialist residual, but the torsion is the computable core.
