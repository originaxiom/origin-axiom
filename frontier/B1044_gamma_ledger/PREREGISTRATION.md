# B1044 PREREGISTRATION — THE Γ-LEDGER: what, specifically, is m004 still doing? (the audit seat's Challenge 3, settled by computation)

*cc banking seat, 2026-08-12. Sealed before compute; hash to docs/SEAL_LEDGER.md,
committed and pushed to both remotes before any cell runs. Gate 5 throughout. Relay
checked before this seal (latest: THE_FRAMEWORK_CHALLENGED — accepted, applied; this
cell is its Challenge 3's settling computation). The PM campaign's fourth cell.*

## The question (B803's open address, unfilled since B993)

**Name every step in the framework's Layers 2–3 (the algebra build and the
measurement cascade) that depends on the GROUP Γ = π₁(m004) ITSELF — as opposed to
the trace field, the quaternion algebra, or arithmeticity (class-level data), or to
generic inputs.** B803's own addendum defines the test: *"a step depending only on
the trace field, quaternion algebra, or arithmeticity is a CLASS statement — but a
step depending on the GROUP Γ itself is not."* If the Γ-column is EMPTY, the
framework's subject is the class (and the LEVEL LEDGER's restatement — a theorem
about a CLASS, entered by a uniquely-doored GRAMMAR — is complete). If NON-EMPTY,
each Γ-step is marked as load-bearing object-specificity — the most important lines
in the framework.

## The method (the B1040 per-step ledger form, applied to the derivation stack)

The inventory is FIXED as THE_FRAMEWORK's Layers 2–3 step list as restructured
2026-08-12 (the file at this seal's commit): Layer 2 — the 27 on the Chevalley
frame (B883); the invariant cubic + Yukawa support (B884); the triality tiling
(B875); 32 = 16+16̄ (B872); the three charge lines (B866); the matter pencil over ℚ
(B886); the quadratic resolvent (B888); E₆-across-faces (B727); M(𝕆,ℂ) (B904).
Layer 3 — the registerability principle (B861); the FMT (B877); the second
measurement (B892/B961); the ℤ₆ global form (B862); hypercharge (B864); the
termination theorem (B863); generations/D₂ (B897/B928); menu completeness/padding/
gates (B873/B865/B868–B871); Sakharov (B867); the adjoint-Higgs naming (B964).

Per step, from its arc's FINDINGS as written (evidence quoted per row, the B1040
discipline): the step's inputs typed into exactly one of —

- **Γ** — consumes the group itself (a presentation, a specific representation of
  THIS group beyond its trace field, holonomy data no class-sibling shares);
- **CLASS** — consumes the trace field ℚ(√−3) / the quaternion algebra /
  arithmeticity / commensurability-class data (B803's list);
- **ALGEBRA** — consumes only 2T/E₆/McKay/Jordan structure (downstream of the
  class's McKay assignment; no manifold data at all);
- **GENERIC/INPUT** — consumes a declared external input (chirality, the VEV
  direction, J) or generic Lie theory;
- **UNDECIDABLE** — the banked text does not determine the consumption; the
  blocking sentence quoted (a permitted outcome; not-searched is never absent).

## The cells

- **V1 — the ledger** (every step, one row, quoted evidence).
- **V2 — the Γ-column verdict**: EMPTY (the class-restatement completes; B803's
  address closes; every "THE OBJECT" heading reads through the LEVEL LEDGER) /
  NON-EMPTY (each Γ-step named and marked; the framework gains its most-important-
  line markers). Counts reported as counts; mixed rows reported as the ledger.
- **V3 — the cross-checks**: (a) B993's zero-steps claim ("Reid-uniqueness is
  consumed at zero steps") re-verified against the ledger's Γ-column; (b) the
  entry-point fence — κ = tr[a,b] and the monodromy ARE Γ-data but live in Layers
  0–1 (the grammar/object layers), OUTSIDE this inventory: the cell types Layers
  2–3 only, and states plainly that the question is where the DERIVATIONS live,
  not whether the object exists.

## Prior: DECLARED, weak — EMPTY

B993/B994/B996/B1040/B1042 all relocated steps off the manifold; B803's address has
stood open through all of them without a Γ-step being named. Lean: the column is
empty. Declared weak (the inventory has never been swept at this granularity — the
lean could be an artifact of nobody looking). Both outcomes are informative; the
NON-EMPTY branch is the more consequential and the prior does not protect it.

## MB12 / non-vacuity

The typing can land in every column: Layer 0–1 steps WOULD type Γ (the fence names
two — κ, the monodromy — proving the Γ-type is inhabitable); B884's build types
ALGEBRA; B862's derivation consumes the cascade's class-level data; chirality types
INPUT. Each column can be hit and each can be missed; the verdict turns on the
Layer-2–3 rows only.

## Hazards pre-registered

Window-read-as-whole (every quoted sentence to its final period); the two-referent
registers (σ, π/6, "measurement" ×2 — Layer 3's steps use "measurement" in the
CASCADE sense, the register's second referent; typed at first use); the B701
citation form; φ AND phi; distinct commit subjects; sum(..., K0) n/a (no K-valued
computation — this is a structured-reading cell); verdict vocabulary {PROVED,
NEGATIVE, OPEN, RETRACTED, PARTIAL}.
