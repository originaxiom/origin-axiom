# B1044 — THE Γ-LEDGER: the Γ-column is EMPTY — B803's address closes; the framework's subject in Layers 2–3 is the CLASS entered through the ALGEBRA, and the object's whole job is to EXIST and to ENTER

*cc banking seat, 2026-08-12. Sealed 0d8776d2, pushed both remotes before compute.
A structured-reading cell (the B1040 form): 20 steps, each typed from its arc's own
construction-naming text, quoted per row in `b1044_ledger.md`. Gate 5. The declared
weak prior (EMPTY) was NOT protected: the ledger method reads bodies, not verdicts
— per the digest protocol's rule 4, adopted mid-window.*

## The verdict of the ledger

**V2: the Γ-column is EMPTY — 0 of 20 Layer-2/3 steps consume the group
Γ = π₁(m004) itself.** Eighteen steps are ALGEBRA (constructions in
e₆/e₇/Jordan/Chevalley structure, downstream of the McKay assignment — several
with declared-INPUT riders: chirality ×3, the VEV direction, the generation
count); **exactly one is CLASS — B727, the entry**: the atom ℚ(√−3), the trace
field, selecting WHICH McKay group the class meets. Zero UNDECIDABLE: every arc
names its own construction basis.

**V3a: B993's "Reid-uniqueness is consumed at zero steps" is CONFIRMED by
enumeration** — not only Reid-uniqueness: no group-level datum of any kind is
consumed after the door.

**V3b: the entry fence, stated plainly.** Γ-data is REAL and load-bearing — in
Layers 0–1: κ = tr[a,b] (the bridge equation, the founding number), the monodromy
A = LR (the object's build), the grammar's one-bit residue (A7, where φ enters).
**The object's contribution is concentrated ENTIRELY at existence and entry: it is
forced by the grammar (Layer 0), it carries the trace field to the door (Layer 1 →
B727), and everything after the door is the class's and the algebra's.** This cell
types WHERE the derivations live; it does not diminish that the object is what
walks through.

## What closes and what it means

- **B803's open address CLOSES EMPTY.** The framework's LEVEL LEDGER restatement is
  now complete rather than conjectural: *a theorem about a CLASS, entered by a
  uniquely-doored GRAMMAR, with the object as the class's minimal representative* —
  and the Γ-ledger is its proof-by-enumeration for Layers 2–3. The audit seat's
  Challenge 3 ("what, specifically, is m004 still doing?") has its computed answer:
  **existing, entering, and nothing else after the door — which is exactly what the
  door-structure (B997/B1019), the template/selection split (B1040), and the trit's
  field-descent (B1042) independently found from three other directions.** Four
  results, one shape, none of them planned to agree.
- **The framework edit this licenses** (rides the bank): the LEVEL LEDGER's open
  address paragraph updates from "unfilled" to "CLOSED EMPTY (B1044)"; the
  "THE OBJECT" headings stand — with the ledger as their reading key.
- **The consequence for the crossing programme**: any future crossing design that
  routes through "more object data" in Layers 2–3 is type-wrong from the start —
  the object has no further data there to give. Crossings live at the door
  (selection events), in the coupling (the ℤ/3 channel), or at the closings (the
  declared inputs). This is the fourth independent derivation of the same
  targeting rule.

## Scope fences

- The inventory is THE_FRAMEWORK's Layers 2–3 as restructured 2026-08-12 (at the
  seal's commit). Layers 0–1 are OUTSIDE by the seal's design — the cell asked
  where the DERIVATIONS live, not whether the object matters.
- "Measurement" throughout = the CASCADE sense (the register's second referent),
  typed at first use per the seal's hazard row.
- The INPUT riders are declared inputs (B1028's ledger), not failures — the ledger
  types them, THE_CLAIM prices them.

## Verdict

**PROVED** (the ledger: 20 rows, 0 Γ, 1 CLASS at the entry, 18 ALGEBRA, 0
undecidable; B993 confirmed by enumeration; the address closed). Locks:
`tests/test_b1044_gamma.py`.
