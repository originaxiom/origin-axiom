# B1043 — ADDENDUM (2026-08-13): the invariant line IDENTIFIED — it is the forced VEV, and it always was

*cc banking seat, next-day. Meditation 2's PM-II-1 (the identification cell) DISSOLVED
before sealing: the answer is banked by construction.*

## The identification

B632 cell2 constructs the forced VEV as `fix = nullspace((A27 − 1) ⊕ (B27 − 1))` with
`assert len(fix) == 1` and `v0 = fix[0]` — **exactly the joint π₁-fixed space of the
27**. So the "new fact h⁰(M; 27) = 1" this arc's entry gate surfaced IS B632's
assertion, and **the identification is definitional: the object's unique invariant
direction = the cascade's forced VEV**, with principal-sl₂ invariance verified in the
same construction (e_pr, f_pr, h_pr all annihilate v0).

## What is genuinely new (and it is a statement, not a computation)

The FINDINGS-level sentence, never before written: **the one input-DIRECTION the
claim prices (the rank-closing VEV) enters the algebra along the object's only fixed
vector.** The object supplies exactly one invariant direction; the cascade's input
uses exactly it. The door-structure, at the level of a single vector.

## The species note (recorded for the digest's lanes)

A fact banked as a code assertion (`assert len(fix) == 1`, B632, 2026-07) was
invisible to FINDINGS-level retrieval for six weeks and was "surfaced as new" by
B1043's entry gate — the cite-don't-rediscover species operating INSIDE the corpus,
caught by Meditation 2's own instrument within an hour of ranking the question.
Consequence for the record channel: **load-bearing assertions in compute scripts
deserve FINDINGS-level sentences at bank time** — a candidate PRACTICES row for the
digest window. R45-3's lead-candidate (the identification) is RESOLVED-BY-RETRIEVAL;
its LAW_MAP row (with B1043's superlinear law) now carries the identification.
