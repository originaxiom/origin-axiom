# B1043 — PM2: THE TRIPLE ASSEMBLY — h¹ = 10 = 7 + 3: the forbid HOLDS and creation is SUPERLINEAR (the cycle contributes one extra class), the decoy certifies the pass, and the arity pairing names its true home

*cc banking seat, 2026-08-12. Sealed 575ad81b, pushed both remotes before compute.
B632's machinery reused whole (the B1039 exec pattern); exact K arithmetic;
sum(..., K0) throughout. Gate 5. The F3 ADJUDICATION is firewalled to the cosmogony
amendment; the mathematics below banks as mathematics.*

## The results

- **W0 (the entry gate): PASS** — the double, recomputed in the graph-of-groups
  formalism, reproduces the banked h¹ = 5 with anatomy 2 + 3. **And the gate earned
  its keep before passing: the first run HALTED** on a wrong shortcut in the
  script (connecting = h⁰(T) assumed h⁰(M;27) = 0). The computation says
  **h⁰(M; 27) = 1 — the 27 carries an invariant line under the solo group** (a fact
  nobody had occasion to bank; recorded here, identification out of scope). With
  the sealed coker formula computed properly (the actual MV difference maps), the
  double gives 3 − 1 = 2 connecting ✓.
- **W1: h¹(triple; 27) = 10** (Fox calculus on the sealed presentation; ρ(t) = 1 as
  declared; h⁰(triple) = 1).
- **W2 (the anatomy): connecting = 9 − 2 = 7, bulk = 3.** The bulk is the solo
  diagonal (restrictions agree cyclically), same as the double's. **The cycle's
  fingerprint, typed: the triangle's vertex-difference pattern has rank 2 (kernel =
  the diagonal), so the cyclic closure contributes exactly h⁰(M) = 1 EXTRA
  connecting class beyond linear seam-scaling.** Per-seam yield: 7/3 > 2.
- **W3 (the forbid + the decoy):** **F3 PASSES — 7 ≥ 6 — and not at the boundary:
  STRICTLY above.** The decoy D (≥ 12, deliberately unbanked) **FAILS** — so per
  the sealed discrimination reading, the comparison DISCRIMINATES and F3's pass is
  INFORMATIVE. **R44-12 (the decoy-forbid control, the audit seat's declared debt)
  is DISCHARGED at the first forbid-attacking pass, as owed.**
- **W4 (the arity question): HALT-VOID, with the door named.** The assembly's
  presentation complex has no 3-cells — the genuine three-slot cup H¹×H¹×H¹ → H³
  has NO TARGET here. The algebraic triple carries the arity-matched CLASSES (ten
  of them) but not the arity-matched PAIRING dimension; the 2-slot saturated form
  is exactly what the arity observation excludes as the test. **The named door: an
  H³-bearing three-body assembly — the closed mirror-double OF the 3-fold cyclic
  cover** (a genuine 3-manifold containing three commuting copies of the object) —
  is the arity test's true home. A future cell; the 3-fold cover, struck from F3's
  referent by X5 for seam-count reasons, re-enters through the front door as the
  arity test's substrate.

## The two in-compute discoveries (each a fact, neither sealed for)

1. **The solo invariant line: h⁰(M; 27) = 1.** The full 27, restricted to the
   object's own group, fixes exactly one direction. Which direction — and whether
   it is B884's VEV line v₀ — is a one-cell identification question, registered as
   a lead candidate, not asserted.
2. **Superlinear creation with an exact mechanism.** The excess over linear
   (7 vs 3 × 2) is exactly the graph's b₁ times the invariant line: **closing the
   loop creates one more class than the seams' linear count — and the class it
   creates is the invariant direction circulating the cycle.** The frame's forbid
   demanded "not less than linear"; the object answered "more than linear, by
   exactly the loop."

## Prereg self-critique (recorded, MB12 honesty)

The seal's W4 pre-registered outcomes NON-EMPTY / EMPTY and a halting coboundary
control, but did not pre-register VOID-BY-DIMENSION as a named outcome. The halt is
within the sealed halting discipline (the control halts before a vacuous read), but
the outcome-space omission is a drafting lesson: **a pairing cell's prereg must
state the target's dimension exists before listing outcomes about its content.**
Species: the MB12-vacuity family, caught in-arc.

## Verdict

**PROVED** (h¹ = 10 = 7 + 3; the gate, the formula, the fingerprint, the
discrimination — all computed). The F3 adjudication note lands in the cosmogony
amendment same-batch. Locks: `tests/test_b1043_triple.py`.
