# B1043 PREREGISTRATION — PM2: THE TRIPLE ASSEMBLY (L157-4 under the pinned forbid; the first arity-matched test of the wall; the R44-12 decoy-forbid control discharged in this seal)

*cc banking seat, 2026-08-12. Sealed before compute; hash to docs/SEAL_LEDGER.md,
committed and pushed to both remotes before any cell runs. Gate 5 throughout. Relay
checked before this seal (no file newer than the trit candidates; PM3's JOINED
relayed). The PM campaign's main compute (gate "after FL-4" discharged by B1040;
the F3 pins sealed 2026-08-12 in the cosmogony amendment BEFORE this arc existed).*

## The assembly, defined honestly

The F3 amendment pinned "the cyclic three-copy weld M₁–M₂–M₃–M₁ with three gluing
tori." **Topological honesty, stated at seal: a manifold-level cyclic weld of three
one-cusped pieces is impossible** (a cycle needs each vertex glued along two
distinct tori; m004 has exactly one cusp). The pin's referent is therefore **the
ALGEBRAIC triple: the triangle graph-of-groups** — three vertex copies of
π₁(m004) = ⟨aᵢ, bᵢ | REL(aᵢ, bᵢ)⟩, three peripheral ℤ² edge groups (the three
"gluing tori"), identity gluing convention throughout (the convention B1036 CAUGHT
and corrected — cited, reused). This is the same level at which the banked double
was computed (B1036's double-Fox ran on the amalgam's presentation). Presentation
(path amalgam + one HNN letter closing the cycle):

```
⟨ a, b₁, b₂, b₃, t |  REL(a,b₁), REL(a,b₂), REL(a,b₃),
                       L(a,b₁)·L(a,b₂)⁻¹,  L(a,b₂)·L(a,b₃)⁻¹,
                       t·a·t⁻¹·a⁻¹,  t·L(a,b₃)·t⁻¹·L(a,b₁)⁻¹ ⟩
```

with REL the banked relator, L = LONG the banked longitude (B637), meridians
identified (a₁ = a₂ = a₃ = a), and **ρ(t) = identity** (the identity-gluing
convention extended to the stable letter — the minimal choice, declared here; any
result's dependence on this choice is in-scope for the write-up's fences).

## The cells

- **W0 — THE ENTRY GATE (halting):** recompute the DOUBLE (the path-of-two amalgam,
  no cycle) in this exact formalism and machinery. It must reproduce the banked
  h¹(dbl; 27) = 5 with the 2-seam-born/3-glued anatomy (B1036/B1039's gate). Any
  other number HALTS the arc (machinery fault, not a result).
- **W1 — h¹(triple; 27)** via Fox calculus on the presentation above (primary
  route), exact arithmetic, B632's banked machinery reused whole (the B1039 exec
  pattern; the namespace-drift hazard pre-registered: inline missing helpers, pull
  nested namespaces).
- **W2 — the anatomy:** the connecting/bulk split per the graph-of-groups exact
  sequence (the cycle's own H¹ contribution accounted explicitly — the triangle
  graph has b₁ = 1, and its contribution is typed in the write-up, not silently
  folded into "seam-born").
- **W3 — THE FORBID (F3, pinned 2026-08-12 pre-arc) + THE DECOY (R44-12,
  discharged here):**
  - **F3 as sealed:** the triple's total connecting-map contribution must be
    **≥ 6** (the double's 2 per seam × 3 seams). FORBIDDEN: any reading selected
    after the number exists — the pin stands as written in the cosmogony amendment.
  - **DECOY D, pre-committed in this seal (deliberately unbanked):** "the triple's
    total connecting contribution is ≥ 12 (4 per seam)." D has NO banked basis —
    it is the same shape as F3 with a fabricated threshold. **The sealed
    discrimination reading:** F3-pass + D-fail ⇒ the machinery discriminates and
    F3's outcome is informative. BOTH pass ⇒ suspect: a vacuity investigation opens
    before any F3 credit is claimed. Both fail ⇒ F3 genuinely fails and the frame
    loses its monotone-creation verb (the amendment's own consequence). **This
    discharges the audit seat's declared decoy-forbid debt at the first
    forbid-attacking pass, as owed.**
  - The F3 ADJUDICATION is FIREWALLED to the cosmogony file (an amendment note at
    bank); the h¹/anatomy mathematics banks as mathematics regardless.
- **W4 — THE ARITY QUESTION (the prize cell; NO PRIOR, both arguments stated as
  promised in B1039's FINDINGS):** the symmetric V-valued pairing at THREE slots —
  the full cubic C3 evaluated over the triple's classes with three bodies (the
  functional read, B632's machinery; coboundary control first, halting). B1039's
  EMPTY was a 2-body verdict; Slansky's typing says the symmetric d_μνλ has no
  canonical 2-body evaluation and **the triple is the first assembly whose arity
  matches the invariant's**. Outcomes: **NON-EMPTY** (the wall BREAKS at matched
  arity — the mass-crossing's structural prerequisite EXISTS; the sector-complete
  statement gains its scope clause "at arity 2") / **EMPTY** (the wall EXTENDS to
  arity 3 — the arity hypothesis dies; the negative names whatever door it names,
  per the pattern). FOR non-empty: the arity match (typed, classical). AGAINST: the
  wall's three-sector streak. **No lean declared.**

## Priors

W1/W2: none declared (the double's 5 = 3+2 is the only calibration; the cycle's
extra topology makes any extrapolation a guess — stated, not leaned on). W3: the
pin is a FORBID, not a prediction. W4: NO PRIOR, both arguments above.

## MB12 / non-vacuity

W0 can fail (a wrong number halts). W3's F3 can pass AND fail (the connecting
contribution is a computed dimension; nothing forces ≥ 6 — the decoy D exists
precisely to certify the non-vacuity of the comparison). W4 can go both ways
(B1039's EMPTY shows the machinery returns empty when empty; B1036's 5 > 3 shows
it returns creation when present).

## Hazards pre-registered

sum(..., K0) for every K-valued reduction; the namespace drift (B1039's two
KeyErrors — the fix pattern cited); exact arithmetic only; functional-read only
(never raw cup values); the identity-gluing convention's scope stated in fences;
ρ(t) = 1 declared above; window-read-as-whole; φ AND phi; the B701 citation form;
distinct commit subjects; verdict vocabulary {PROVED, NEGATIVE, OPEN, RETRACTED,
PARTIAL}.
