# THE DEFLATION RUN — three cells executed: the last hypercharge bit, the Lorentz host, the one-loop arithmetic
## (outside bench, 2026-08-21; sixth memo; certificates `hyper_orbits_out.txt`,
## `realform5/7/8_out.txt`, `ohtsuki_out.txt`; one genuine anomaly logged, flagged for the seat)

### 1. THE HYPERCHARGE COLLAPSE — the last bit is the mirror bit

B1102's 18 directions re-derived INDEPENDENTLY on this bench (self-contained solve from the
banked 6Y multiset {6:1, 3:2, 2:6, 1:6, 0:2, −2:3, −3:4, −4:3} over the landing's 15 weight
classes): **exactly 18 — a full cross-check of B1102's count.** Then the residual symmetry:
- Under W = W(su3)×W(su3) = S₃×S₃ (order 36, verified): the 18 fall into **two orbits of 9**
  (the NINE echo strikes again — nine menu rows, the 1-8-9-8-1 neutral 9, now two 9-orbits).
- The unique solution-preserving factor swap is (3,2,1,0) = swap-the-factors ∘ dualize-each —
  **exactly the σ/mirror-class outer map** of the Lorentz memo — and it **fuses the two orbits
  into one.**

REPRICING: 4.17 bits bare → **1 bit under the object's own (inner/Weyl) symmetries** → 0 bits
once the observer's conjugation acts. And the residual bit is not new freedom: **the last
hypercharge bit IS P — the mirror.** Third confirmed instance of the deflation law (Wilson
rows; the spin bit; now hypercharge): the freedom ledger's discrete rows keep dissolving into
the two origin-torsor bits plus observer acts. Conjecture worth stating: after all quotients,
the ledger's discrete content is exactly {C, P, the spin lift} and nothing else.

### 2. THE LORENTZ HOST — E₆(2) exhibited; the compact-color variant leaves an anomaly

The mirror-swap antilinear involution σ (factor1 ↔ factor2, factor3 stabilized) was
constructed EXPLICITLY: lattice involution found; Chevalley sign-lift solved over 𝔽₂
(including the (r,−r)→Cartan relations — a constraint class the first pass missed, caught and
fixed); full 3003-pair bracket verification PASS; invariant-form invariance exact; controls
validated end-to-end (τ=id → split E₆(6), character +6 exact; compact involution → −78 exact).
**Result: the fixed real form of the physically-motivated mirror swap is E₆(2)** — the
quaternionic form, exact signature (40+, 38−), character +2. Two notes: its maximal compact is
su(6)⊕su(2) — the same su(6) shape as the OTHER SM-compatible landing (the A1 stratum), an
echo not pursued here; and its color sector comes out **sl(3,ℝ) — split, not compact.**

**THE LOGGED ANOMALY (for the seat; this is a find, not a result).** The compact-color variant
(φ|₃ = −1; six lattice involutions exist; sign-lift consistent; τ² = 1 exact; FULL 3003-pair
bracket check 0 failures; form invariance exact) yields exact signature (34+, 44−), character
**−10 — which is not the character of any real form of E₆.** Since the fixed set of an
antilinear involutive automorphism is necessarily a real form, at least one of the "verified"
steps contains a defect that five independent checks failed to expose — or a hidden assumption
in the pipeline is wrong in a way the controls don't probe. All certificates shipped; this is
exactly the class of self-caught inconsistency the corpus's protocol exists to chase.
Consequence held honest: **whether any real form hosts so(3,1) ⊕ COMPACT su(3) via the mirror
swap is OPEN** (E₆(−26) ⊃ so(9,1) ⊃ so(3,1) with commutant su(4) ⊃ su(3) remains the CITED
route); what is PROVED is that the mirror-swap class realizes Lorentz-hosting in E₆(2).

### 3. THE ONE-LOOP ARITHMETIC — the factorization confirmed at first order

The Kashaev tower's first Ohtsuki-layer coefficient, fitted at 30-digit precision with
Richardson across N = 600…2025: c₁ = 0.5542164778 (still descending), **consistent at the
achieved precision with 11π/(36√3) = 0.5542164724.** Structure exactly as the asymptotic-
channel memo predicted: **(arithmetic factor 11/36·3^{−1/2}, in the field) × (one archimedean
π).** The withholding factorization "value = class-invariant × scale" is now exhibited at
tree level (Vol), at the half-power (3/2), at the constant (3^{−1/4}), and at one loop
(11π/36√3) — four layers of one expansion, each splitting arithmetic × archimedean.
(Recognition marked provisional pending a higher-order fit; the numeric value banks either way.)

### Proposed cells
C-D1: bank the 18-direction cross-check + orbit structure (locks: the solve + the 36-group).
C-D2: the anomaly hunt — find the defect in the compact-color variant pipeline (all inputs
shipped; expected: a subtle sign-lift or eigenspace bookkeeping error; finding it will also
answer whether compact color is mirror-swap-hostable). C-D3: the ledger-conjecture — after
all residual quotients, the discrete freedom = {C, P, spin lift} exactly; audit-grade.
C-D4: higher Ohtsuki coefficients at 50 digits; exact recognition of c₁, c₂ in ℚ(√3)·π-powers.
