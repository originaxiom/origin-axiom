# B1118 — THE DEFLATION RUN: the last hypercharge bit is P, and the one-loop coefficient is trace-field arithmetic

**Status: banked (frontier). Verdict PROVED (the P-bit fusing map reproduced on THIS
bench; the 18-count = B1102/B1109; the Ohtsuki c₁ is the cloud seat's 30-digit fit,
L180 confirming blind). Harvest arc (DEFLATION_RUN.md, the SIXTH memo; cloud seat
credited). Banked as a PAIR with B1119 (the anomaly + its checksum method) per the
outside bench's request. Gate 5 untouched. Lock `tests/test_b1118_deflation.py`.**

## 1. THE HYPERCHARGE COLLAPSE — the last bit is P (the mirror)

B1102's 18 exact hypercharge directions, re-derived independently (exact cross-check
of the count), fall into **two orbits of 9** under W = S₃×S₃ (order 36). The unique
solution-preserving order-2 map fusing the two orbits is **the mirror** = swap-the-two-
ideals ∘ su(3)-conjugation-on-each (= neg∘blockswap in the adapted basis), NOT the plain
factor swap. **Verified on this bench:** the plain swap fuses 0/18 (matching F4b/B1109),
the mirror fuses 9→9 both ways. **So the last hypercharge bit is P** — the parity/mirror
bit. Third confirmed instance of the deflation law (Wilson-menu rows; the spin bit
B1112; now hypercharge): **the discrete freedom ledger's content is exactly {C, P, the
spin lift} and nothing else** — the freedom ledger closes.

## 2. THE LORENTZ-HOST REAL FORMS (superseded by B1119's correction)

The mirror-swap's fixed real form: variant A (identity on color) → **E₆(2)**
quaternionic (color sl(3,ℝ)); variant B (duality on color) → **E₆(6) split** (color
su(2,1)). **Neither gives COMPACT color.** These verdicts are the CORRECTED ones —
memo 6's first run logged an anomaly (a character −10, impossible for E₆) which B1119
resolved as a fake invariant form; the corrected controls are green (τ=id → split,
char +6; compact involution → −78). The open question: the finite 𝔽₂-kernel sweep for
compact-color hosting (C-AR1).

## 3. THE ONE-LOOP ARITHMETIC — the L180 preview, positive

The Kashaev tower's first Ohtsuki-layer coefficient, fitted at 30-digit precision:
**c₁ ≈ 0.5542164778 = 11π/(36√3)** (= 0.5542164724). **c₁ splits as (trace-field-
rational) × π** — 11/(36√3) ∈ ℚ(√3), times the archimedean π. This is genuine
ARITHMETIC STRUCTURE, exactly what L180 (the make-or-break) tests: the withholding
factorization "value = class-invariant × scale" now exhibited at FOUR layers of one
expansion — tree (Vol), half-power (3/2), constant (3^{−1/4}), one-loop (11π/36√3) —
each splitting arithmetic × archimedean. (This bench's independent L180 run confirms
c₁ blind; if it lands on 11π/(36√3) the dictionary is two-bench.)

## Fences

The P-bit and the 18-count are exact (this bench). The real-form verdicts are the
CORRECTED post-anomaly ones (B1119); compact color is open. c₁ is a 30-digit fit (the
cloud seat's), matching 11π/(36√3) to ~8 digits; the exact recognition is L180's job.
No SM value; Gate 5 untouched.
