# B1189 — THE CLOSE-LOOP, BATCH 1: five ledger cells computed and adversarially verified (GC-1…GC-5)

**Status: banked (frontier). Verdict PROVED** (the sitting; cells carry their own verdicts below).
The grand-computation campaign's first close batch — the multiagent loop the owner directed
(tiered per the cost rule; 15 agents, each cell + two adversarial lenses).
`verification/reproduce.sh` → `REPRODUCES` (the GC-2 algebra re-run exactly in-house; committed
cell artifacts asserted). Gate 5 clean throughout.

## GC-1 — THE METER (PARTIAL, survives): the ladder verified; the one-class framing REFUTED; the lattice is DEEPER than commensurability

The 112-member ladder **independently re-verified** (own code, zero failures at 1e-30; wide bite
control: 24 non-family manifolds all off-lattice, worst residual 7 orders above tolerance). **The
mechanism, exact**: Vol(m004) = **12 × Vol(H³/PSL(2,O₋₃))** (residual 4.3e-50, via the
Humbert/ζ_K formula) — so V_reg = 6·Vol_orb. Borel's rank-1 Bloch group (classical, cited) forces
RATIONAL ratios. **But the one-class assumption dies**: **11 of the 35 non-regular members are NOT
arithmetic** (Maclachlan–Reid; explicit non-integral traces at 50 dps — e.g. t06829 tr(a²) =
1/6 + (1/2)√−3; o9_41005 tr(b²) = 79/98 − (125/98)√−3; each cross-validated against the relator to
~1e-63): o10_143600, o10_143602, o9_41000/04/05/06/08, t06828, **t06829**, t11365, v2875. **So the
family spans multiple commensurability classes yet every member sits on ONE integer lattice — the
lattice is deeper than commensurability.** Named open sharpness: commensurable members should a
priori populate the Vol_orb-integer (sixth-integer) lattice; only multiples of 3·Vol_orb occur —
un-derived. Caveat: the 24 consistent members are unrefuted-not-proven arithmetic (5-word sample;
full trace-ring needs Sage machinery).

## GC-2 — THE SIGN (NEGATIVE, survives): the relational bit dies by the golden unit's norm

The crown question answered with an exact mechanism. The twisted double DOES carry a genuine
**pair-datum** (H₁ = ℤ⊕ℤ/5 vs the plain double's ℤ; det(A±I) = −1/+5, SNF exact) — a datum of the
RELATION invisible to either copy. **But it is mirror-EVEN**: the centralizer of A in GL₂(ℤ) is
±Bⁿ with **B = A − I the Fibonacci matrix = φ itself (B² = B + I), det B = N(φ) = −1** — so the
conjugator coset A→A⁻¹ = J·{±Bⁿ} carries **both determinant signs** (J det +1; K = JB det −1,
both verified conjugating A to A⁻¹), making the twisted double **mirror-self-equivalent**. Under
B1168's acceptance law (mirror-odd ∧ dimensionless) the relational class cannot carry c. **The
two-sided control bites**: M = [[2,3],[1,2]] (unit 2+√3, norm +1 — a²−3c² = −1 impossible mod 3)
has a single-signed coset — the instrument can exclude a sign, so the golden both-signs result is
informative. **The theorem-shape: the object's own norm-(−1) fundamental unit is what forbids the
self-coupling from orienting it.** The F2 door narrows to HETEROGENEOUS couplings (B131's
"heterogeneity mints" + this cell's norm-criterion: a partner whose relevant unit has norm +1).

## GC-3 — THE CLOCK (PROVED, survives): one generator, three operations; both tensions dissolved

The three tick candidates are **not competing clocks but three operations on ONE generator
A₁ = RL** (trace 3, det 1, disc 5): the meridian depth grading = a REPRESENTATION-side operation;
the Breath ℤ/2 = the SQUARE-ROOT/covering operation (M² = RL); the conductor clock = CONGRUENCE
REDUCTION (ord(A₁ mod 3κ) — table re-verified). **B766-T7=T3 vs B1083 reconciled as compatible**:
they type two different invariants on two different objects (a Galois field-automorphism choice in
the OBSERVER's menu vs a monoid-conjugation symmetry of the OBJECT's torsor) — both stand.
**B293 ≡? memo-51 answered**: same (μ,λ) cusp data, NOT the same structure (a Poisson bracket on
the character variety vs a Jordan decomposition on the carrier) — joined as complementary; one
named computable gap (does the Goldman Casimir control the Jordan spectrum?).

## GC-4 — THE ARENA (PROVED; a lens boolean-slip noted): the reach-deficit LAW found

The 11σ deficit **survives degree-preserving nulls and DEEPENS to z ≈ 49–59**; **localized: 81%
of the deficit is the two longest jumps into the terminal shell (L7→L10 + L8→L10)**; **the law
named and tested: the DAG's edges are LOCAL in charpoly-class (abc) space, segregating descendant
cones** (Spearman(|Δabc|, L10-cone-Jaccard) = −0.612…−0.619, p < 1e-16). The correctness lens
CONFIRMED all magnitudes by two from-scratch re-implementations (own transitive closure, an
independent Curveball null) while flagging the cell's own "bite control" label as defective
(draw-#0-vs-rest is not a true bite) — the flag is carried; the lens's own boolean was mis-set
(refuted=true with confirming text) — an E52-flavored slip on the verify side, recorded. Scope
caveat carried: this characterizes the DAG's combinatorics; d_MM is not a spacetime dimension.

## GC-5 — THE REGISTER (PROVED, survives): all five repairs verified, rows drafted

L63 **CLOSED as c** (B1174/B1182 verified at source; θ is the k5/value-kernel leg — a different
leg). L114 **stays open, adjudicated**: B786 (the OBJECT's self-dual torsor: rank unconditionally
3, ι≡θ there) and B787 (the AMBIENT SL(3) variety: ι a genuine 4th involution) answer different
questions about different spaces — the "rank 3 vs 4" tension is a mis-set frame, and the real
question (is ι a MEASUREMENT operation) is B787's own open item, correctly cc3-assigned. L120
re-posed (the CMR tie-in is dead by B942/B957; the II₁/III interface tests stay live).
B1168/B1166 typed (verdict OPEN reserves C6-completeness / the C4-half respectively — reservation
notes drafted, verdicts unchanged). B279 and B769 spot-verified exactly (their lock tests pass:
6/6). The full paste-ready row texts are archived in `verification/batch1_cells.json`; the L63 and
L114 rows land in OPEN_LEADS in this bank; the rest follow in the next register pass.

## Fences

One verify agent (the GC-2 instrument lens) was security-flagged for a broad process-kill during
its run; its actions were reviewed — no live workloads were harmed (all background runs had
completed) and its verdict is independent of the flagged action; noted per protocol. GC-4's lens
boolean mis-set is recorded above (the substance was confirmation). The GC-1 prompt itself carried
the wrong one-class assumption — the cell refuted its own framing, which is the loop working. All
cells wrote only to the scratchpad; every landing here re-asserted from committed artifacts. No
firewall crossing; no measured value.
