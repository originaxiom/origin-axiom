# B610 (slice 1) — the m136 weld scan: three universality splits

**Status: banked (frontier). The Listening Campaign Phase 4 (K4.2 +
K4.3/K4.4 rows), first slice — the stage side only (the weld word is the
only object-dependence; the classical side over m136's field is the
registered heavy follow-on). Object per adjudication 5: the R²L bundle =
m136 (chat-1's "5₂" corrected). Structure-scan tier; MB12 facts in the
script docstring. Lock `tests/test_b610_m136_weld.py` (fast, ~15 s).**

## The scan (κ = 4..16, 20, 24; fig-8 RL controls rerun alongside)

1. **Chat-1's claim CONFIRMED (K4.2):** the m136 odd hearing trace is
   COMPLEX at most levels, with exact-looking values: **ω = −1/2+i√3/2
   at κ=5**; ζ₆ at κ = 6, 10, 15; ζ̄₆ at 7; −i at κ = 4, 16;
   **(1−√3)·e^{iπ/3} at κ = 12 and 24** — the object's eigenvalue field
   ℚ(√3) enters through ζ₆-phases ("rotating through ω, ω̄", as relayed;
   forms best-effort at 1e-9).
2. **The conjugation-closure law is OBJECT-SPECIFIC (K4.3):** the m136
   odd spectrum is conjugation-closed at NO scanned level, while the
   fig-8's is at EVERY level (B601). Since the closure came from the
   mirror pairing and the fig-8 is amphichiral while m136 (R²L) is
   chiral, the mechanism candidate is sharp: **conjugation-closed
   hearing spectrum ⟺ amphichirality of the object** [EXPLORATORY —
   needs a third object and a proof; registered].
3. **No magnitude-chirality window for m136 (K4.4):** the m136 spectrum
   is UNIT-MODULUS at every scanned level — including the naive field
   conductor candidates (κ = 8, 12) and their multiples (24). The
   fig-8's departure from the unit circle at its golden multiples
   (B609's exploratory row) is therefore doubly special: m136 shows
   field content in the TRACE PHASE but never in the magnitude, at
   least to κ = 24. The "silver dictionary at the conductor" hypothesis
   finds no window here; chat-1's conductor framing needs revision
   (their "silver conductor = 7" matches nothing computed — the ℚ(√3)
   content appears at the disc-12 multiples).

## Universality table rows (computed; interpretation is chat-1's lane)

| Row | fig-8 (RL) | m136 (R²L) |
|---|---|---|
| odd-trace reality | real, the two-indicator law (B601) | complex, ζ₆-phased ℚ(√3) values |
| spectrum conj-closed | every level | no level |
| unit-modulus | leaves the circle exactly at golden multiples | never (κ ≤ 24) |
| the unit tone κ=4 | −1 (real, deaf) | −i (complex) |
