# G4 — BANKED: B596'S MULTIPLIER TABLE RETRO-DERIVED, 12/12 (cc2, 2026-07-17;
# prereg 5848c32d; g4_run.log)

B596 banked (2026-07-16 era) the SU(3)-family clocks as "Pisano-anchored with
non-uniform multipliers {1/2, 1, 3/2, 2}... not a uniform function of kappa" and
sharpened L84: any functor must explain them. VERDICT: RETRO-DERIVED — every row
kappa = 6..15 is EXACTLY clock = ord(A1 mod 3*kappa): the multipliers are the
arithmetic of the CONDUCTOR (ord T = 3*kappa) versus the naive modulus kappa —
nothing else. The two anomalous rows are the clock law's anomaly-zone clauses:
kappa = 4 (k=1): "the antisymmetrization can destroy order" = the dim-1 block's
character blindness (clock 1); kappa = 5 (k=2): the half-period = the Fibonacci
core carrying clock 10 with its abelian twist blind (Q2's theorem).

CONSEQUENCE FOR B650/L84: the demand "explain the clock multipliers" is now
discharged BY the identified functor itself — mod-conductor reduction at
ord(T) = 3*kappa produces every multiplier; the clock law + the functor close
B596 jointly. The 2026 "kappa-dependent corrections that lengthen or destroy"
were the conductor and the anomaly zone all along.
=> proposal to cc: B596's table row upgrades DATA -> DERIVED; the L84 sharpening
clause retires; pairs with B650's W2-G2 as the order-level completion of the
trace/det-level dictionary.
