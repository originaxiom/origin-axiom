# G5 — BANKED (reduction theorem): 3/5/1 FROM TWO LOCAL INPUTS (cc2, 2026-07-17)

THE REDUCTION: for ANY once-punctured-torus bundle M (pi_1 = F_2 x|_A Z, chi = 0,
aspherical) with a 27-dim local system V, and its double D (closed, chi = 0,
orientable, V self-dual via the banked intertwiner J):
  (a) chi(M) = 0 => h0 - h1 + h2 = 0 on M;
  (b) Poincare duality + J on D => h3(D) = h0(D), h2(D) = h1(D);
  (c) Mayer-Vietoris for D = M u_T M-bar + half-lives-half-dies on the cusp torus
      bind h1(D) to 2 h1(M) - (cusp terms).
=> the ENTIRE dimension grammar {h0(M), h1(M); h0(D), h1(D)} is determined by TWO
LOCAL INPUTS: i1 = h0(M) = dim V^{holonomy} (the invariant vector count — banked
= 1, the v0 line) and i2 = dim V^{peripheral Z^2} (the cusp-fixed space). With
(i1, i2) = (1, 3): h1(M) = 3, h0(D) = 1, h1(D) = 5 — exactly the banked golden
values AND cc's silver reproduction (B649-3a). THE THEOREM: the grammar is
monodromy-independent WHENEVER (i1, i2) are — the silver reproduction is thereby
localized to the statement "m136's holonomy also has (i1, i2) = (1, 3)", which is
what B649's data verifies. WHAT REMAINS (honest): prove (i1, i2) metallic-uniform
(the principal-composite fixed-vector + cusp-fixed-space dims as word-independent
facts) — one bounded rep-theory cell; until then the reduction + two verified
instances bank, the universality is a sealed expectation.
=> to cc: this ends per-manifold dimension computation for any future metallic
control object: check (i1, i2), read the grammar off the reduction.
