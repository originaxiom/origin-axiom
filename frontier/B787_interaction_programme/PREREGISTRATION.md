# B787 — PREREGISTRATION (sealed before compute)

Gate 5 + Gate 5-Q binding. Base-rate honesty is the first law. Sealed hash in
ARTIFACT_HASHES.txt. No result banks HIT without (a) clearing its base-rate control and
(b) surviving an adversarial refutation pass.

## Base-rate control (applies to every NUMERIC door)

The look-elsewhere budget: the "programme quantity" anchor set is large. For any numeric
door, before matching, enumerate the candidate targets {JUNO 0.30902, φ, 1/φ, √3, √5, 1/(φ√5),
2/3, cardinal angles, small rationals} and the tolerance window; compute the EXPECTED number
of chance hits (targets × candidates × window). A match counts as a HIT only if it is (i)
EXACT (algebraic identity, not a ~% coincidence) or (ii) a residual far below the chance
expectation with a stated mechanism. A ~0.3–1% coincidence at base-rate density ≈ 3.6 (error
48's lesson) is a MISS, recorded as such.

## Per-door two-outcome criteria

- **Phase 1 — ι-identification.** OUTCOME A: ι equals a specific element of (ℤ/2)³ (c, θ, γ₅,
  or a product), proved by evaluating all four involutions on the SL(3) geometric rep and
  matching the action; OUTCOME B: ι is a genuinely independent 4th generator (rank-4 on the
  non-self-dual variety), consistent with B786. Either is a real result. Locked against B766.

- **D1 Fox calculus.** A: the Fox matrices of σ vs σ_mirror at an SL(3) rep are related by a
  computed group-ring intertwiner that is NOT reproducible at the trace level (a genuine
  θ-bridge). B: the relation collapses to a trace/gauge artifact (θ still invisible). 
- **D2 R-matrix.** A: a Born probability from R on the pointer-object τ×τ pair equals JUNO
  (0.30902) EXACTLY or clears base-rate, OR yields a new exact invariant. B: all Born numbers
  are {0.276, known MTC values, base-rate} — physics-silent.
- **D3 15A8 @ Fib.** A: a_{F_n} has a proven closed form / linear recursion / periodicity
  beyond what Hecke multiplicativity forces generically. B: a_{F_n} is generic (no extra law).
- **D4 E₆(78)/V₄.** A: the torsion spectrum's c/θ-parity assignment is FORCED (each exponent's
  eigenspace determined by the McKay V₄ action, not chosen). B: the assignment is base-rate /
  the diagram automorphism is neither c nor θ.
- **D5 state integral.** A: Z(u) at a programme u yields a transcendental with identifiable
  structure (a period, an L-value, a regulator) the algebraic specialisation misses. B: the
  values are numerically unremarkable / algebraic / base-rate.
- **D6 Habiro @ Fib.** A: c_{F_n} has a growth law / recursion / r-stream connection. B:
  c_{F_n} is generic among the c_n.

## Phase 3 (relabeling) — conditional trigger

Runs ONLY if Phase 1 shows ι ≠ θ affects a banked label. For each of B759 (√3 coupling norm),
B769 (T1 triadic), Wave-5 (listener clock): recompute whether the object in play is reversal
(θ) or inversion (ι); relabel only where the computation says inversion. A relabel is a
computed verdict, never a text edit alone.

## Completion (Part 9, items 1–3; the campaign's scope)

Complete when: all 6 doors tried+recorded (hit/miss), the ι-identification settled, the
ι-relabeling propagated (if triggered). Items 4–8 (GSWZ, P021 seal, PC26, JUNO timestamp,
specialist) are out of scope — recorded, owner/specialist-gated.

## Sealed criteria hash

To be recorded in ARTIFACT_HASHES.txt at seal time (shasum -a 256 of this file).
