# B42 -- Findings

> Logged observation, not a claim.

## Result

For the tangent return:

```text
mu = 4I + 2
lambda_+ = (mu + sqrt(mu^2-4))/2
torsion proxy = mu - 2 = 4I
discriminant = mu^2 - 4 = 16I(I+1)
```

On `I>0`, these natural quantities are monotone or have boundary extrema. None
has an intrinsic stationary point at `I=1/4`.

## Verdict

**`STALLED`**

Variational criteria do not derive S1 unless one builds `I=1/4` into the
functional.
