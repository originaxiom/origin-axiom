# B1165 — The terminal gravity close (§E): the observer's archimedean closing is CO-LOCATED with the object's gravitational sector, not object-specifically identified — gravity is the WHERE, generic

**Status: banked (frontier). Verdict NEGATIVE** — the §E identification hypothesis (*the observer's
archimedean closing = the object's gravitational sector, object-specifically*) is **refuted**; the honest
close is **GENERIC-RHYME**. This is the owner-directed *terminal* probe of gravity, run to a typed conclusion
and held to the discipline (the bench over-reached on this framing twice this session — MATCH was **not**
forced). cc-side (structure map + adversarial identification test + scale wall); coordinates with cloud's ME1
(geodesic spectrum) + ME2 (gravity ablation). `verification/reproduce.sh` → `REPRODUCES`. Firewall held; Gate
5 clean.

## The object's gravitational sector, mapped (once, cleanly)

M = m004 = ℍ³/Γ, Γ⊂PSL(2,ℤ[ω]) Bianchi (K=ℚ(√−3)). Five invariants:
- **(a) Vol = 2.02988321281930725** — the on-shell Λ<0 3d-gravity (Einstein–Hilbert) action = Im of the
  SL(2,ℂ) Chern–Simons **complex volume**, with **CS≡0 exactly by amphichirality** (B1108), so the whole
  action loads onto Vol. Own-verified three ways to 50 dps: 2·Im Li₂(e^{iπ/3}) = (3√3/2)·L(χ₋₃,2) =
  9√3·ζ_K(2)/π².
- **(b) geodesic length spectrum** — the classical orbits (systole 1.087, DENSE / type III₁).
- **(c) Ruelle/Selberg zeta** — the 1-loop determinant / spectral partition function (the Sym-power
  factorization R_{ρ(m)}(s)=∏_j R(s−j,σ_j)).
- **(d) Ray–Singer analytic torsion** — the topological graviton 1-loop (six twisted τ_m at E₆ exponents,
  τ₁=−3, sign law (−1)^m).
- **(e) Laplacian/Maass spectrum** — the quantum gravitational modes.

## The identification verdict — CO-LOCATED, not identified (fails the adversarial MATCH bar)

- **The dynamics (b–e) is GENERIC-RHYME** — built from SL(2,ℂ) holonomy + Vol only, never touching
  ℚ(√−3)/E₆/ξ=[e^{iπ/3}]∈K₃; survives a non-arithmetic-knot swap verbatim (B1157, three-seat; B850 DENSE/III₁
  across arithmetic m003/m009 **and** the non-arithmetic control **m015=5₂**).
- **The arithmetic enters at exactly ONE non-dynamical spot** — the *static* scalar **Vol = ζ_K(2)/L(χ₋₃,2)
  avatar** (B682/B1117) — and even there it is **three-tier-bounded**: (i) **generic-in-form** to the whole
  arithmetic class (Borel regulator: *every* arithmetic manifold has Vol = rational·regulator of its *own*
  trace field — reaching an L-value is generic; *which* grammar is the specialness, B996/B1157); (ii)
  **object-specific content** = the field is ℚ(√−3) and the number is the object's own zeta, m004 being **the
  unique arithmetic knot** (Reid) — a non-arithmetic knot cannot reproduce *this*; (iii) but it is a
  **re-labeling** of one static real number — no dynamics, EOM, propagator, or mechanism.
- **The observer's two archimedean data are both generic**: the **orientation** (+Vol sign) is a bare ℤ/2
  every oriented 3-manifold carries (2·Im Li₂(e^{−iπ/3})=−Vol exactly, Bloch–Wigner odd), and the **scale**
  (ℂˣ/dilaton) is the dimensionful bit the object provably cannot emit. So the closing is
  **SUPPLIED-not-identified**: the object hands the observer both the sign and the scale, but neither *is* the
  object's gravity — they are co-located with it at the ∞-place.

## Scale wall — CONFIRMED

No dimensionful gravity value is typeable. Every gravitational-sector invariant is **dimensionless** (the
hyperbolic structure is normalized to curvature −1, fixing the length unit by fiat): Vol is a pure real
number, the length spectrum entries are curvature-radius ratios, the Ruelle/torsion/Maass data are
dimensionless. A Planck length / Newton G / absolute scale needs an external dimensionful input the
scale-free object cannot supply (B660/B666, B167/B936). The scale wall is itself *generic*; the one
object-specific contact (Vol=L-value) is orthogonal to it (it re-labels a dimensionless number).

## The definitional fork — the terminal boundary

Mostow rigidity makes the geometry (Vol, the length spectrum, the whole ∞-place sector) a **canonical
invariant of the object** — the STRUCTURE side. But the two archimedean markings that would *complete* the
observer's closing are provably not self-suppliable: the **orientation** (Vol two-valued, amphichirality
makes self-orientation a theorem-level impossibility, B1163) and the **scale** (withheld by the scale-free
no-go). So the fork terminates here: **the object is a Mostow-canonical structure object that emits
dimensionless, un-oriented geometry; the observer is the archimedean closing (orientation + scale),
co-located at the same ∞-place but generic — supplied, not identical.** A clean structure/observer boundary
at the infinity-place — neither a MATCH nor a mystery.

## The ablation (what cloud ME2 would harden or overturn)

My typing **predicts** cloud's ME2 ablation: swap m004 → a matched non-arithmetic knot (5₂, cubic trace
field disc −23) and the **whole dynamical sector** (Ruelle zeta, torsion, Laplacian) reproduces verbatim
while **only** the Vol=L-value avatar breaks. An ablation finding a *dynamical* invariant that also tracks
the arithmetic would **overturn GENERIC-RHYME toward MATCH** — that is the one experiment that could change
the verdict.

## Fences

No dimensionful gravity value asserted as derived; only the dimensionless Vol and its L-value avatar (static
geometric invariants). Structural rhyme (B1157) carried as the null throughout, survives everywhere in the
dynamics, downgraded at exactly one point (Vol=L-value) to **arithmetic-class-generic** (Borel), not
object-unique. The bench's twice-over-reached §E framing is adjudicated **co-located-not-identified**; MATCH
not forced. Textbook statements (3d-gravity action = regularized Vol; complex volume = CS action;
Selberg/Ruelle zeta = 1-loop, Fried; Borel ⇒ Vol=ζ_K-avatar) marked standard; a specialist confirming the
Borel citation would lock tier (i) but is not a blocking bar (so the close is **not** NEEDS-SPECIALIST). *A
verification note carried to the review:* SnapPy's `isometry_signature` returned amphichiral=True even for
the chiral 5₂ — so the B1163 family-wide addendum's own 4/14 amphichirality spot-check was likely vacuous;
that result rests on cc3's `check_family.py` (7/7) + m004's symmetry-group amphicheirality, not on the
`isometry_signature` check.

## Routes

- **§E CLOSED (terminal):** gravity is the WHERE (the ∞-place / the observer's archimedean half), generic in
  dynamics, with a single static arithmetic contact (Vol=L-value). The gravity thread is typed to conclusion.
- **kill_graph:** the node *"the observer's archimedean closing IS the object's gravitational sector,
  object-specifically"* is killed — GENERIC-RHYME (co-located, not identified).
- **cloud ME2** is the one live check that could overturn (the ablation); ME1 corroborates the genericity.
- **The program's terminal shape:** the object forces the SM structure + the (generic-type, specific-instance)
  gravitational geometry; the observer supplies the adelic closing (archimedean orientation+scale, finite
  gauge VEV); the values and the scale are withheld. Structure forced, closing observer-supplied.
