# xB025 — THE REFEREE RELAY, VERIFIED AT SOURCE: the arithmetic holds, two inferences overreach

**Date:** 2026-09-18 · **Seat:** `xb` · **Branch:** `sep16-branch` · **Verdict: VERIFIED-DIFFERS**
(banked as **PROVED** per the W1 vocabulary — the arc establishes what it checked)

**This is a relay verification, not a sealed prediction.** `PREREGISTRATION.md` declares in full that
the checks were run as the relay was read. No kill condition is claimed.

---

## 1. THE RELAY'S ARITHMETIC — CHECKED AGAINST THE SOURCE BRANCH, NOT ITS SUMMARY

The SM-derivation lane (`<remote>/standard-model-derivation-0qt6ao` — house convention, vendor prefix dropped) fetched at **`23532539`**. `B1374` and `B1375` read
directly from `FETCH_HEAD`.

| relayed | source says | verdict |
|---|---|---|
| Y₄ = t12839: **12 800** on 64 loci | *"t12839 carries 12 800 on 64 loci"* | **exact** |
| Y₅ = o10_150696: **800** on 200 loci | *"Y_5 = o10_150696 800 on 200 loci (241 loci, 4 400 firing)"* | **exact** |
| Y₆: **67 200** on 576 loci | *"Y_6 67 200 on 576 loci (639 loci, 10 816 firing)"* | **exact** |
| **80 800**, exactly one net generation, never two, never three | *"on every one of the 80 800 backgrounds the count is exactly one net generation … never two, never three"* | **exact** |
| Y₂ = m206 none, Y₃ = s961 nothing fires | *"Y_2 = m206 (9 loci, 16 firing modules) and Y_3 = s961 (31 loci, nothing fires)"* | **exact** |
| signature `(0,1,1,0)` vs `(0,2,1,2)` | same, verbatim | **exact** |

**Independent cross-check from this branch.** `B1413`'s exact ℚ(u) recomputation of the m010 witness
gives `a₀=0, a₁=1, t₀=1, r₁=0` for V and `a₁=2, r₁=2` for V*. **That is the relayed signature to the
digit** — so *"the instrument I verified is the instrument this runs on"* is correct.

**Three things the relay understated, all from the source:**

1. **`B1374` and `B1375` both self-declare `"instrument": true`.** By their own grading these are
   instrument arcs.
2. **The non-semisimple fence is forced by a theorem, and B1374 knows it.** `B1413` R27(a) reads
   Menal-Ferrer–Porti **Theorem 0.1** at source (arXiv 1001.2242v2) and concludes the one-cusped
   index is a **theorem-zero on the whole geometric finite-twist class**; a nonzero index therefore
   *requires* leaving it. `B1374`'s own claim line says it fires *"on reducible non-split modules"*
   and that *"R27's m010 witness [was] reproduced first"*. **`B1413` and `B1374`/`B1375` are the same
   fact from two sides, not in tension** — B1413 proves nonzero needs extension data, B1374/B1375
   compute what happens exactly there.
3. **The right-handed neutrino is not uniform across levels.** `B1374`: `ν^c = 0` on t12839,
   anomaly-free. `B1375` §5: on Y₆, **57 600 of 67 200 carry no net singlet and 9 600 carry one with
   the generation's own sign (all six counts equal), 0 with the opposite.** More
   Standard-Model-shaped than relayed — and it means "exactly one net generation" is uniform in five
   slots, not six. **`Y₇` is still running.**

## 2. B1366's INGREDIENT — VERIFIED EXACTLY

The relay's strongest new claim is that the SM sits in E₆ **uniquely up to conjugacy**, resting on
*"120 A₂'s, all one orbit"*, and it hedges (*"if the orbit claim holds"*). The hedge can be lifted
for the ingredient.

`verification/e6_a2_orbit.py`, exact integer arithmetic in the simple-root basis, no Lie-theory
library — Cartan matrix, reflections, breadth-first orbit walk:

- **K1, positive control:** root counts **A₃ 12, D₄ 24, E₆ 72, E₇ 126, E₈ 240**, every root of
  squared length 2.
- **K2, the control that matters:** on the **disconnected** system A₁ ⊕ A₂ the walk returns
  **two** orbits, sizes `[1, 3]` — the Weyl group cannot mix components, so it must.
  **A "single orbit" verdict from code that could only ever return one would be worthless.**
- **K3:** E₆ has **72 roots**, **120 A₂ subsystems**, and the Weyl group acts with **a single orbit
  of size 120**. **CONFIRMED.**

## 3. WHERE THE RELAY OVERREACHES — TWO INFERENCES, BOTH ROUTED AS DEBTS

**K4 — the ingredient is not the conclusion.** Three gaps, none closed here:

- **Root subsystems ≠ subalgebras.** K3 counts A₂'s *generated by roots*. Dynkin separates regular
  from special (S-)subalgebras; a non-regular A₂ is not in this count and the orbit walk cannot see
  it.
- **A₂ is colour, not the Standard Model.** SU(3)×SU(2)×U(1) needs the SU(2) and both U(1)s placed —
  and main's **B1415** shows that is its own computation: of the 12 order-4 elements of
  `exp(u(1)_Y + u(1)_γ)`, **exactly 4** leave only the SM roots of su(6).
- **Conjugacy in which group?** W(E₆), the adjoint group, or `Aut(E₆)` — which contains the order-2
  diagram automorphism, precisely the one relating **27** and **27̄**.

**And a verified statement on main the relay should reconcile with.** `B1415`: *"Kac's order-3 inner
classes of E₆ are six with the seat's fixed types and **none is the Standard Model**."* That may be
consistent — inner automorphism classes and subalgebra embeddings are different objects — but a
claim of a *unique* SM embedding must **exhibit** the reconciliation.

**The second overreach is about this seat's own arc.** The relay writes that the stabiliser is
*"the two the paper calls 'withheld' and 'relational'"*. **xB021 carries `identifications: []`. It
made no such identification.** The structural half — `|⟨A5,A6,A7⟩| = 4`, orbit `{0,¼}`, stabiliser
exactly `{A6,A7}`, `4 = 2×2` — is verified and correct. **The bridge to the paper's vocabulary is
the unverified half**, and it is load-bearing for the relay's framing.

**Both are routed to `docs/IDENTIFICATION_LEDGER.md` as I-32 and I-33, status UNEARNED** — which by
B1231's rule is *"an open debt, not a defect"*.

## 4. ONE READING OF THE RELAY THAT IS RIGHT, AND TIGHTER THAN IT KNOWS

*"the orbit {0, ¼} is exactly {CS(m004), CS(m003)}"* — **correct.** xB023 W0 re-derived both today:
`cs(m004)` class **zero**, `cs(m003)` class **quarter**. And xB023 supplies the **mechanism** the
relay treats as a closed loop: **m004 sits at 0 because it is the orientation double cover of the
Gieseking manifold**, with **78 of 78** such covers at class zero against a **27.2 %** base rate
across the full 203 123-manifold one-cusped census.

**Also owed to the relay:** xB021 is **superseded in detail** by work banked since. xB022 found the
three arithmetic "misses" are **explained** — they turn on invariants with a **trivial orbit**, a
third species that *"move to the orbit"* cannot reach even in principle. *"9 of 12 with 3 named
misses"* understates the current state.

## 5. CREDIT, AND WHAT THIS ARC DOES NOT DO

**The relay's best control is one it barely mentions:** an independent single-prime run over 661
reproducing the record's **four spurious loci**. Agreement *including on artefacts* is stronger
evidence than agreement on clean numbers.

**This arc does not** ratify the unique-embedding conclusion, ratify the withheld/relational
identification, or assess the paper — **this seat has not read the paper** and says so rather than
inheriting a verdict about it.

**Gate 5 absolute. Nothing to `CLAIMS.md`.**
