# THE LEVEL-LADDER CAMPAIGN — does the chiral theater read the splitting of its own conductor?

**Seat:** cc2 (read-only seat; deliverable = cross-seat packet for banking-seat adjudication;
B-number to be assigned at banking — provisionally `CC2-LL`).
**Opened:** 2026-07-15. **Prereg tier:** campaign. **Sources:** the jewel veins **L73 + L74 + L77**
(owner directive, 2026-07-14) + side cell **L76**; hint gate **H133** ("Gate: level 4, or a proof").
**Firewall:** pure stage-side arithmetic (affine E₆ modular data, mapping-class action). Nothing
SM-facing runs anywhere in this campaign; no physics reading; "law" language stays frontier/
hint-grade until banked. Provenance: all verification internal (owner + AI seats), per the
Review-18 pre-committed scope.

**Parallel-safety declaration (checked against the live board 2026-07-15):** no overlap with the
running L85 campaign (B598 P2 = the quantization arrow / Weil pairing — classical↔quantum map),
chat-2's pending B599 algebraic-face artifacts (the θ-parity theorem), PC26 drafting, or the two
detached sentinels (m=3 genus, e₃). This campaign lives stage-side **across levels** — a lane no
seat currently occupies. MB13 sweep done: level 4 appears nowhere in the corpus except as H133's
own gate; the L-register rows L73/L74/L77/L76 are OPEN with no runs recorded.

---

## The unified question

The θ-odd (chiral-difference) block of the E₆-stage mapping-class representation carries, per
level, a specific arithmetic — and three registered observations say that arithmetic tracks the
factorization of the stage's own conductor **κ = k + h∨ = k + 12** read through the object's
trace field ℚ(√−3):

| level k | κ = k+12 | odd primes of κ, ℚ(√−3)-character | banked odd-block content | banked clock (odd-block order) |
|---|---|---|---|---|
| 1 | 13 | 13 split | degenerate (odd dim 1, action trivial, Z=+1) — B569 | 1 |
| 2 | 14 = 2·7 | 7 **split** | the **ℤ/7 sine kernel** S_odd = −i·(2/√7)[sin(2πst/7)] (B570-C3, B572); order-4 rotation, eigenvalues {+1,+i,−i}; Z=+1 | 4 = ord(A₁ mod 3) |
| 3 | 15 = 3·5 | 5 **inert** | **ℚ(√5) imported**: the 8 odd magnitudes obey one integer octic = (w²−10w+20)×(sextic → two Galois-conjugate cubics over ℚ(√5)) (B578-D7); Z=+1 | 60 = ord(A₁ mod 30) |
| **4** | **16 = 2⁴** | **none** (2 inert; 3 ramified is ubiquitous) | **← this campaign** | **← this campaign** |

Level 4 is the decisive dyadic point: the first rung of the ladder whose conductor has **no odd
prime**. Every clause of the candidate law makes a falsifiable prediction there, and H133's own
registered gate is exactly "level 4, or a proof."

**The question sheet:**

- **Q1 (L73 / H133):** Is Z₄ = Tr ρ₄(A₁) = +1? And: write + lock the one-page abelian-invisibility
  proof (det(A₁−I) = −1 ⇒ unique fixed point on every torsion lattice ⇒ Z ≡ +1 in every abelian
  theater), the vein's anchor, so the nonabelian question sits on a proved floor.
- **Q2 (L74):** What is the level-4 odd block's arithmetic? Under the splitting law: **{2,3}-smooth
  only** (weak form), with the inert-prime clause predicting a **ℚ(√2) import** (strong form —
  the silver field at the inert 2, rhyming with ℚ(√5) at the inert 5).
- **Q3 (L77):** What is ord(ρ₄(A₁)|odd)? Is it Pisano-anchored — ord(A₁ mod N) = π(N)/gcd(π(N),2)
  for some modulus N(4) — extending {level 2 ↦ N=3, level 3 ↦ N=30}? And is the mechanism the
  odd block's own T-content (readout iv below)?
- **Q4 (L76, independent side cell):** Is there a law relating the cover-torsion tower
  tₙ = |Res(Δ₄₁, tⁿ−1)| = |det(A₁ⁿ−I)| (= 1, 5, 16, 45, 121, 320, 841, … — P8's torsion orders,
  C4's cover orders) to the escalator charge tower eₙ = det(I−Mₙ) (B556)? Towers to n = 12, exact;
  the 11-loci question (11 | tₙ ⟺ n ≡ 0 mod 5 vs banked 11 | eₙ ⟺ n ≡ 1 mod 3) decided.
- **Q5 (L75, optional, hint-harvest only):** do E₈-end invariants appear quantitatively in the
  level-3 blocks beyond the golden octic? Output = NOTICED-grade `H#` candidates only; no claims.

---

## The banked constraint sheet (what any answer must be consistent with)

1. **The level-1/2/3 values** exactly as banked: B569 (level 1: odd dim 1, trivial, Z=+1;
   consistent-data rebuild after the inconsistent-handoff kill), B570-C3 (level 2: odd dim 3,
   ρ(A₁)|odd non-scalar order 4, eigenvalues {+1,+i,−i}, Z=+1, all 729 Verlinde integers), B572
   (S_odd(E₆,₂) = −i·(2/√7)[sin(2πst/7)], exact, S-block only, rephasing gauge noted), B578-D7
   (level 3: 20 primaries, odd dim 8, order 60, Z=+1, the integer octic with ℚ(√5) content),
   B586 (tr_even = 0 at E₆₂ — the whole amplitude θ-odd).
2. **The θ-parity stage-face rule** (B599, banked): Θ-invariant contractions of Θ-homogeneous
   factors vanish unless the θ-odd factor count is even — *zeros forced, values stage-specific*.
   Cited as context; this campaign computes values the rule does not force.
3. **The clock theorem** (B570, THE_SELECTION_RULES clause D): ord(A₁ mod N) = π(N)/gcd(π(N),2)
   (Fibonacci–Pisano). Level-2/3 clocks anchor at N = 3 and N = 30 respectively.
4. **det(A₁−I) = −1** (exact; L73's anchor) and the abelian floor: AP2's 243-theater Z=+1
   Gauss–Milgram theorem + the B580 Round-1 sharpening (Deloup + rank-2 filling span: abelian
   theaters hear no knot at all).
5. **The B569 lesson (hard gate):** the monodromy word's spelling changes projective phases.
   ρ(A₁) must be built via the **two-word lock** — T²ST and TST⁻¹S⁻¹ agree to tolerance — and the
   full modular battery (S symmetric+unitary, S² = the θ-flip conjugation permutation,
   (ST)³ = S², S⁴ = I, Verlinde integers ≥ 0, q-dim cross-check) passes **before** any readout.
6. **The dimension ledger** (derived here from the comark enumeration, to be re-verified in-code):
   P⁺_k(E₆) = 3, 9, 20, 42 and odd dims 1, 3, 8, 17 at k = 1, 2, 3, 4 (self-conjugate counts
   2, 3, 4, 8). A mismatch at k ≤ 3 with banked counts = STOP.
7. **The a-priori cyclotomic envelope.** All S/T entries at level k live in ℚ(ζ_{6κ}) (inner
   products in (1/3)ℤ; h-denominators | 6κ). "Minted arithmetic" is defined **relative to this
   envelope**: content at odd primes p | κ (level 2: the 7-kernel; level 3: the 5-import) is the
   law's subject; content at primes ∤ 6κ is impossible; the law's level-4 claim is about which
   part of the {2,3}-envelope is *saturated* and whether any quadratic import appears.

---

## The cells

- **P0 — GATES (no readouts before all pass).**
  (a) Environment: venv, numpy/sympy/mpmath; record versions in the packet.
  (b) Run the in-repo level-2 script verbatim (`frontier/B570_allowed_plays/c3_e6_level2_monodromy.py`)
  — every gate green, banked numbers reproduced.
  (c) Re-derive level 3 with this campaign's own generic-k engine (independent build): 20
  primaries, odd dim 8, order 60, Z₃ = +1, the octic reproduced **exactly** (integer
  coefficients matched to B578-D7's banked polynomial).
  (d) The generic-k primary enumerator validated against the ledger row 6 counts.
  (e) The exact pipeline validated against the float pipeline at k = 2, 3 (S entries as exact
  ℤ[ζ_{6κ}] integer-accumulation Weyl sums vs the float einsum — agreement < 1e−9).
  **Any P0 failure = hard stop; adjudicate conventions before proceeding (the B569 lesson).**

- **P1 — THE PREDICTION SHEET (frozen + hashed BEFORE P2 runs; see §Predictions below).**

- **P2 — THE LEVEL-4 COMPUTATION (blind).** Build κ=16 modular data with both pipelines; gate
  battery; then bank to `outputs/` *before* comparison: dim(odd) [expect 17 per row 6];
  Z₄ exact; ρ₄(A₁)|odd: order by exact matrix powers, eigenvalues + minimal polynomials over ℚ,
  the magnitude polynomial(s) of the S_odd entries (the D7-analogue readout), S_odd structure
  (kernel form if any), and readout (iv): the odd block's T-phase conductors. Numbers banked
  verbatim, then — and only then — compared clause-by-clause to P1.

- **P3 — THE PARITY CONTROL + THE VERDICT TABLE.** Same readouts on the θ-even block (does the
  even sector read κ's splitting too? if yes, the law is stage-arithmetic, not chirality-specific
  — an honest scope correction, not a kill). Assemble the k = 1..4 law table; issue the A/B/D
  verdict per the locked outcome table; register follow-ons (the level-5 κ=17-inert decisive
  prediction — registered, NOT run here).

- **P4 — L76, THE TWO TOWERS (independent cell, exact, cheap).** tₙ and eₙ side by side to
  n = 12 (both exact integers; eₙ per B556's tower recursion). Preregistered tests: (i) the
  11-loci: 11 | tₙ ⟺ n ≡ 0 mod 5 (Pisano π(11)=10 ⇒ expected) vs banked 11 | eₙ ⟺ n ≡ 1 mod 3
  — do the loci ever intersect (n ≡ 10 mod 15)? (ii) gcd(tₙ, eₙ) for all n ≤ 12; (iii) the norm
  comparison: tₙ = |N_{ℚ(√5)}(φ²ⁿ−1)| vs banked eₙ = N_{ℚ(√5)}(gₙ(φ)) — any visible relation
  between the generators (data readout, no fit). Every outcome banks as data + hint.

- **P5 — L75 hint-harvest (optional, time-permitting).** NOTICED-grade rows for the hint ledger
  only.

- **P-proof — THE L73 ONE-PAGER (independent of P2).** The abelian fixed-point proof written
  (det(A₁−I) = −1 ⇒ A₁−I ∈ GL₂(ℤ/N) for every N ⇒ unique fixed point 0 on (ℤ/N)² ⇒ every
  abelian theater's mapping-torus invariant collapses to the vacuum term, Z = +1; Gauss–Milgram
  normalization per AP2's framework, Deloup cited) + a numeric lock sweeping all nondegenerate
  (A, q) with |A| ≤ 128 plus the 243 banked theaters.

## Predictions (P1 — frozen before P2; the falsifiers)

Under the splitting law (L74) + Pisano anchoring (L77) + H133:

- **PRED-1 (H133):** Z₄ = +1 exactly.
  *Falsifier F1:* Z₄ ≠ +1 → H133 dies at its own gate; bank the kill + the residual hint
  (which value, which field it lands in — mandatory residual-hint field).
- **PRED-2 (the weak splitting clause):** the level-4 odd block's arithmetic is **{2,3}-smooth**:
  every minimal polynomial of ρ₄(A₁)|odd eigenvalues and of the S_odd magnitude set has splitting
  field of conductor dividing 96 = 2⁵·3 (equivalently: no prime p ∉ {2,3} divides any such
  conductor/discriminant beyond the envelope).
  *Falsifier F2:* any odd prime p ≥ 5 in any conductor → the splitting law as stated is REFUTED
  (an odd-prime modulus minted at a conductor with no odd prime).
- **PRED-3 (the strong inert clause, riskier — the two clauses may separate, which is itself a
  finding):** a **ℚ(√2) import** appears in the odd-block magnitude arithmetic (the inert-prime
  quadratic at p = 2, the analogue of level 3's ℚ(√5) at inert 5). Declared subordinate to
  PRED-2: PRED-3 failing while PRED-2 holds = outcome B (the law's inert clause is
  odd-prime-only), not a full kill.
- **PRED-4 (the clock):** ord(ρ₄(A₁)|odd) is finite, even, and equals ord(A₁ mod N) =
  π(N)/gcd(π(N),2) for at least one N ≤ 200; the modulus-map fit k ↦ N(k) over three points
  {(2,3), (3,30), (4,N(4))} is reported as data (no fit is preregistered as *the* map — two
  points cannot pin a law; level 4 constrains).
  *Falsifier F3:* the order matches no Pisano form with N ≤ 200 (or the matrix has infinite
  order/defective structure) → the Pisano-anchoring reading dies at level 4.
- **PRED-5 (mechanism candidate, readout not prediction):** the clock equals the multiplicative
  order of the S-conjugated odd T-content — computed and reported either way.
- **MB12 vacuity guard:** dim(odd, k=4) = 17 expected; if 0 or the block is scalar, the cells
  are vacuous → stop and report, no verdicts.

## The outcome table (locked)

- **A (the law strengthens):** PRED-1, -2, -4 hold (PRED-3 either way). The splitting law
  graduates vein → stated frontier LAW with four rungs; the level-5 (κ = 17, **inert prime**)
  prediction — a fresh real-quadratic import at a *prime* conductor — is registered as the
  law's next decisive gate. Nothing promotes; L73/L74/L77 rows updated with the data.
- **B (partial):** some clauses hold, others fail → the law is re-scoped honestly (which clause
  survives at which strength); residual hints registered per failure; the surviving clauses'
  level-5 predictions registered.
- **D (the kill):** PRED-1 or PRED-2 fails → the vein's law dies at its first real test; the
  exact counterexample banked; mandatory residual-hint rows (METHOD rule 4); the L-register
  rows updated to TESTED-NEGATIVE-at-level-4 with the honest scope (levels ≤ 3 data stand).

Either way the deliverable banks: level 4 is new exact data the corpus does not have, and
every readout is an integer/algebraic certificate, not a float.

## Machinery inventory

- **EXISTS (in-repo, reused as pattern):** the W(E₆) BFS (51,840, exact integer matrices +
  parity); the Kac–Peterson build + gate battery + two-word monodromy lock
  (`c3_e6_level2_monodromy.py`); the level-3 reproducer (`tests/test_b578_level3.py`); the θ
  diagram flip; Verlinde + q-dim cross-checks; B556's charge-tower recursion (P4); P8's
  torsion identity (P4).
- **TO BUILD (this campaign):** the generic-k primary enumerator (comarks 1,2,2,3,2,1); the
  **exact** S-pipeline (integer exponent accumulation in ℤ[ζ_{6κ}], reduction mod Φ_{6κ}) with
  float cross-check; the generic odd/even splitter + block extractor; the min-poly /conductor
  battery (sympy `minimal_polynomial` + discriminant factorization + mpmath 50-digit
  cross-check); exact matrix-order computation; the P4 tower scripts; the P-proof one-pager +
  lock.
- **Cost:** level 4 = 42 primaries → 903 S-pairs × 51,840 Weyl terms ≈ 47M integer ops
  (vectorized; minutes). Full campaign ≪ one seat-day.

## Discipline

Prereg = this file, sha256-hashed at freeze (recorded in the packet README) before P2 runs.
P2 numbers banked to `outputs/` verbatim before comparison. MB8 null-glance and MB12 vacuity
per cell; gates hard-stop; every negative carries `residual-hint:`. Nothing here touches
CLAIMS.md; register updates are *proposed* in the packet and applied only by the banking seat.
The packet ships with a verify-don't-trust checklist (every number recomputable from the
scripts alone; no state carried from this seat's session).
