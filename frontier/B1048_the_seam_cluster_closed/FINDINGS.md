# B1048 — the seam cluster CLOSED, and the scale wall's real proof was sitting uncurated

**Status: banked (frontier). The other nine rows of the cluster B1047 opened. Consolidation only —
every statement here is a banked arc's, re-verified before restoring. Firewalled; nothing to
`CLAIMS.md`; Gate 5 untouched; zero anchors.**

---

## The headline is not the seam

> ### The survivor sitting beside the corpus's worst trap is a **THEOREM**, and **no curated surface carried it**.

**B408** — registered at B1046 as the worst case in the corpus — opens

> *"**THE SEAM DOES NOT CONTRACT — the one scale lever stands** … Ratio ≈ 1.2170 > 1"* — a phrase now retracted and registered (`RETRACTED_PHRASES` row 9)

over an `arc_verdict` of **NEGATIVE**, and kills itself 27 lines down (*"the seam **CONTRACTS** —
persistence was an artifact … max over embeddings **is biased by embedding count**"*).

**B426 is the arc that upgrades that correction from a diagnosis to a theorem.**

**Stated precisely, because the loose version is false.** B1046's registry **does** name B426 — its
B408 row reads *"B426 upgrades the correction to a theorem — survivor and trap sit side by side"*.
**What was missing is not the name but the LAW.** Measured with this arc's rows excluded: **B426
appears on none of the five curated surfaces.** A reader of the consolidations met the trap and
nothing that answered it.

### The theorem, re-derived here from scratch

`B408`'s seam-envelope ratio has the exact closed form

    env₄₅/env₁₅ = (3α² + 4α − 1)/10,    α = 2cos(2π/9)

with **minimal polynomial `1000x³ − 1500x² + 360x − 19`** — a cyclic cubic in **`ℚ(ζ₉)⁺`**,
**`√5`-free because 5 does not divide the field discriminant `3⁴`** (the structural half: the
5-local factor is level-inert 15 → 45, so the golden factor cancels and the ratio is purely
3-local). Therefore the three *"real embeddings"* whose max produced 1.2170 are **the three Galois
conjugates of one number**, `{1.21702, 0.20787, 0.07510}`, and the orbit's averages are exact:

| | |
|---|---|
| arithmetic mean | **exactly `1/2`** |
| RMS | **exactly `√51/10` ≈ 0.71414** |
| geometric mean | **exactly `(19/1000)^⅓` ≈ 0.26684** |

**The 1.2170 "growth" exists only under `max`, which selects a conjugate.** Every number above was
recomputed from the polynomial's coefficients — **no floating point in any exact claim** — and
`α`'s own minimal polynomial `t³ − 3t + 1` was re-derived first.

### ⚠ And B426's slogan is over-broad. The correction travels with the restoration.

B426 writes *"**Every Galois-invariant functional of the orbit is < 1**"* — retracted here, `RETRACTED_PHRASES` row 10. It is not:

- the elementary symmetric **`e₁ = 3/2 > 1`**;
- the sixth power mean **`M₆ = 1.01340 > 1`** (while `M₅ = 0.97699 < 1`).

**The exact boundary, computed here: the power-mean family contracts for every `p` below
`p* = 5.5932…`, and exceeds 1 only as it degenerates toward `max`** — which is *precisely* the
embedding bias B408's own correction named. **The wall is not weakened by this. It is given its
boundary**, and the defensible statement is *"every genuine **average** of the orbit contracts"*,
not *"every functional"*.

**A scale-lever claim is the most firewall-sensitive object in the programme** — it is what
`WHAT_WOULD_COUNT` grades Tier 2 on — which is exactly why the qualifier had to be found before the
row was written, not after.

---

## The other two restorations

### THE SEAM FIELD IS FORCED, AND ITS LEVEL IS A CONDUCTOR (B449 · B427)

**The requested falsification could not run, and that IS the answer.** `Alexander(5₂) = 2t²−3t+2`
and `Alexander(6₁) = 2t²−5t+2` have **leading coefficient 2**, so neither knot is fibered, there is
no monodromy, and the *"disc(trace) × disc(Alexander)"* formula's second factor **names nothing**
for them. *"Level 161" is not a failed prediction; it is a non-sequitur.*

**For a fibered knot the Alexander polynomial IS the homological monodromy's characteristic
polynomial** — verified exactly: `Alexander(4₁) = t² − 3t + 1 = charpoly([[2,1],[1,1]])`, disc **5**.
So the formula is really **`disc(GEOMETRY) × disc(DYNAMICS)`**, `ℚ(√−15)` is **restored to forced
status**, and **level 15 is the conductor of `ℚ(√−3, √5)`**.

**In family:** silver's dynamics is `ℚ(√2)` (charpoly `t² − 6t + 1`, disc 32, conductor 8) and its
geometry `ℚ(i)` (conductor 4) → compositum **`ℚ(ζ₈)`, conductor 8**, with **B448's independent
witness `z⁴ + 16`**, run the previous day for an unrelated purpose. **Bronze predicts seam level
∈ {39, 52}** — *registered and not run*, and it is one cheap invariant-trace-field computation.

**And the exchange is Galois (B427).** Swapping the two slots acts by **`σ₁₇`** — the units `k` with
`4k ≡ 8 mod 60` are exactly `{17, 47}` — which **fixes `i`, negates `√5` and `√−3` individually,
and therefore FIXES `√−15`**. Verified to 40 digits. **The seam's physical channel is
exchange-symmetric**; the asymmetry lives entirely in the side channels. B427 also **corrected the
handoff it adjudicated**: `tr(Q³A) = tr(QA)` is false on the actual matrices (`C = ζ₁₅`,
`C′ = ζ₁₅²`).

### THE SEAM IS AN ADDRESS PROPERTY (B363 · B402 · B478) — three rows, one statement

- **NECESSITY (B363).** A `Par`-commuting lift makes every `tr(Par·P·Q)` real, and a real element
  of `H = ℚ(√5, √−3)` has no seam component — re-derived: `Im(x + y√5 + z√−3 + s√−15) =
  √3·(z + √5·s)`, which for rational coordinates vanishes **iff `z = s = 0`**. **But necessity is
  not sufficiency:** all **225** one-sided Heisenberg twists are dark, and so is the one-slot theta
  lift in either order. **The seam is TWO-SIDED.**
- **INTENSITY (B402).** Across the 15 D-side twist addresses the s-cell count is a function of
  **`gcd(address, 15)` alone** — `f = {1: 44, 3: 32, 5: 36, 15: 0}` — and **the untwisted canonical
  point is the UNIQUE dark address of the whole space**. *Classicality is the exception, not the
  seam*, and the "spontaneous choice" question dissolves: nothing is chosen. **Re-run end-to-end
  (5m16s, pure `Fraction`); the artifact reproduced byte-identical.**
- **THE SHIFT (B478).** `Par·D(m,c)·Par = D(m,c)·Z^{cm}` — proved here symbolically in `j` **and**
  checked over every `m, c ∈ 1..15` and all 15 diagonal entries. **Trace-invisible but
  address-shifting** by `cm` units: the metallic letter moves the stage.

**One scope rider that must travel: B363's 225-twist scan is NUMERICAL tier** — double precision,
by its own status line. Only the theta×theta bright side is exact. **The row says so**, and the
upgrade is a choice rather than a wall, since the same `Fraction` engine serves the rest of the
cluster.

---

## The three declines, each on the arc's own words

- **B459 — its own ADDENDUM withdraws the headline, twice.** *"The selection structure is the
  **QR-class's at level 15, not the object's**"* (the address axis, an independent control) and
  *"every pair has its own table … which **corrects THIS record's own overreach** — B459's 'one
  uniform law, 240/240' was itself (1,2)-specific"* (the seed-pair axis). **An `E34`
  apparatus-inflation shape, caught by the arc itself.** *Its `LAW_SIBLINGS` row, written one arc
  ago, said SAME-LAW — OWED; reading the addendum resolves it to DECLINE. That is the registry
  working as intended: a triage is a placeholder for a body read, not a substitute for one.*
- **B431 — the claim-line gap, fifth instance in this cluster.** Its claim line names **one**
  gating line (`y ≡ 0 mod 3`); the body has **two** (`x ≡ 0 mod 10` as well), plus two value-level
  corrections. And B459's controls make the table the **(1,2) pair's**, not the object's.
- **B474 — its own words: the correspondence *"is nominal"* and *"demands a mechanism"*,** with the
  derivation *"the next wave"*. What stands is the **seventh float-kill**, and a float-kill is
  process, not a law.

---

## The cluster, closed — with its arithmetic stated because the first draft got it wrong

**14 rows = 8 RESTORED** (B393 · B410 at B1047; B426 · B449 · B427 · B363 · B402 · B478 here)
**+ 3 DECLINED — RETRACTION** (B359 · B361 · B362, B1047) **+ 3 DECLINED** (B459 · B431 · B474,
here). *The first version of that line read "5 restored, 6 declined", which double-counts B1047's
rows; the arc's own check now enforces `8 + 3 + 3 = 14` over a disjoint union.*

**Campaign step 6 is satisfied for the whole cluster** — *"a band is DONE when its rows are
restored or explicitly declined with a reason"* — for the first cluster in the B300–B499 band.
Measured with **B1048 and later excluded** (E37).

## E38's third instance — and the sweep the first two repairs never did

**B1048's six restorations broke `B1033/verify.py`'s `len(ledger_set) > 200`** at 194. That is
`E38` (progress-eroded threshold) again — **and the sharpest instance, because B1042 had already
repaired a `> 200` absolute count IN THE SAME FILE and left its sibling standing.** *Naming an
error class does not sweep for it* — this refresh's own recurring finding, arriving one level down.

**So this time the file was grepped rather than waited on**, and the sweep found another:
`tests/test_consolidation_coverage.py` had a docstring promising *"bounded rather than pinned
exactly, so ordinary consolidation work does not break the lock"* — **one line above an absolute
count** that ordinary consolidation work walks straight through. Both are now **shape bounds**
(a ≥10× ratio; a share band), with raw upper guards kept where the other direction would be a real
regression. **`ERROR_LEDGER` now says the repair is not complete until the file is swept.**

**Provenance.** `verify.py` (76 checks) · `tests/test_b1048_seam_closed.py` (11 locks) ·
bodies read: B363 B402 B408 B426 B427 B431 B449 B459 (+ ADDENDUM) B474 B478 ·
`DEBT_LEDGER` §B300–B499 · three new `LAW_MAP` rows ·
regenerated: `B402/q2_landscape.py` (byte-identical).
