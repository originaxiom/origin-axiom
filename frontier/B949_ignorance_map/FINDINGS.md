# B949 — THE IGNORANCE MAP: what the atlas says we are ignorant of, and one correction to my own reading

**Date:** 2026-08-08 · **Seat:** cc (banking) · **Lane:** MATHEMATICS / MAP.
Gate 5 untouched. **Directive:** *"read the atlas toolset and sweep the repo to see what
we miss and what we're ignorant of."*

---

## 1. The toolset, read

`scripts/atlas/query.py` exposes seven functions over a mined corpus of **865 frontier
probes** — `context_card`, `resolutions_for` (the obstacle→resolution oracle),
`motif_trace`, `revive`, `meeting_points`, `gaps`, and the whole graph.
Corpus status: **banked 573 · dead 218 · open 55 · dormant 19.**

Its own honesty note, which this arc endorses: the trace map appears in 361 probes (41%)
and that is a **tool selection-effect, not unity**; only **κ** (211×, 24%) is a forced
first integral.

## 2. THE OBJECTIVE IGNORANCE RANKING — all eleven obstacle types

The atlas classifies every probe by the obstacle it hits. Computed here in full (the
built-in `gaps()` prints only the worst six):

| obstacle | banked | dead | open | dormant | total | **resolved** |
|---|---|---|---|---|---|---|
| **source_free** | **0** | 1 | 0 | 0 | **1** | **0.0 %** |
| **bridge_construction** | **5** | **8** | 1 | 0 | 14 | **35.7 %** |
| measure | 76 | 45 | 5 | 2 | 128 | 59.4 % |
| gauge_dict | 48 | 24 | 4 | 3 | 79 | 60.8 % |
| numerology | 21 | 8 | 4 | 0 | 33 | 63.6 % |
| spacetime_3p1 | 98 | 34 | 12 | 3 | 147 | 66.7 % |
| cancellation | 46 | 17 | 4 | 1 | 68 | 67.6 % |
| observable | 34 | 15 | 0 | 1 | 50 | 68.0 % |
| units_scale | 90 | 17 | 14 | 8 | 129 | 69.8 % |
| particle_dict | 74 | 25 | 0 | 0 | 99 | 74.7 % |
| selector | 18 | 3 | 3 | 0 | 24 | 75.0 % |

**`bridge_construction` is the real frontier: the most-attempted-least-resolved type —
14 probes, 8 of them dead, one open.** Building a bridge from the object to anything else
is where this programme's attempts go to die, and the table says so quantitatively.

## 3. `source_free = 0/1` — AND THE CORRECTION TO MY OWN READING

`source_free` is the "from nothing" obstacle; its lexicon triggers are literally
`["source-free", "cannot move", "no rule", "not-nothing", "from nothing"]` (`atlas.py`).
It is the **Origin Axiom itself** — the project's founding question, and the thing the
repository is named after.

Zero of one banked. The single tagged probe (**B58 stage1**, dead) turns out on inspection
to be a **keyword match** on a trace-algebra cotangent-spectrum arc, not a genuine attack.

**I was on the way to reporting "the programme never worked its own founding question."
That would have been wrong, and the record says so plainly.** `docs/UNIQUENESS_THEOREM.md`,
in its own status block:

> *"It is **not** a derivation of the substrate from nothing (**that direction is mapped
> dead** — see PROGRESS_LOG Phase C and the frontier obstruction maps)."*

So **`source_free = 0/1` is not an oversight. It is a correctly-recorded dead direction.**
The atlas is reporting the truth, and the honest ignorance lies one step to the side.

## 4. THE ACTUAL IGNORANCE: six axioms, and one inserted bit

What the programme *does* have is a **conditional** uniqueness theorem: the minimal record
axioms force the persistent sector **A = LR = [[2,1],[1,1]]**, and from A the entire proven
core P1–P16 follows. The axioms are explicitly *"motivated but not laws of nature"*:

| | axiom | what it inserts |
|---|---|---|
| A1 | state space is **ℤ²** — a pair | *not one; not three* |
| A2 | updates invertible over ℤ (GL(2,ℤ)) | reversibility |
| A3 | det = +1 (SL(2,ℤ)) | orientation-preserving |
| A4 | primitive one-channel unit shears | gives exactly L and R |
| A5 | torsion-free closure | the topological route |
| A6 | minimality (minimal hyperbolic trace) | the algebraic route |
| **A7** | **the residual L-before-R choice** | *"the smallest piece of inserted structure in the whole construction"* |

> **THIS is what we are ignorant of.** Not "why something rather than nothing" — that is
> mapped dead and correctly so. **Why *these* axioms.** Everything the programme owns is
> downstream of A1–A6, and A1–A6 are motivated, not forced. A1 alone ("a pair; not one;
> not three") carries the whole two-record substrate.

A5 and A6 are noted in the source as two independent routes to the same conclusion, so the
system is over-determined there — which is a strength, and narrows the ignorance to the
remaining four.

## 5. A NEW CONNECTION — A7 may be discharge-able, and B945 is the reason

A7 is stated as *not eliminable by any matrix-level axiom* — the one irreducible labelled
input. **B945 (yesterday) bears directly on it and was computed for an unrelated purpose.**

B945 computed the stabiliser of the object's word under the Klein group generated by
ρ (reversal) and **σ (the R↔L swap)** and found:

> **Stab_V(RL) = the FULL Klein group** — σ fixes RL, because σ(RL) = LR and LR is a cyclic
> rotation of RL.

So **the L↔R swap is a symmetry of the object.** If A7's choice is exactly this swap, then
the "smallest inserted structure in the whole construction" **inserts nothing the object
can see**, and the construction has one fewer free input than the uniqueness theorem
claims for it.

**Registered as L131, NOT claimed.** Two reasons for caution, both of them the programme's
own: (i) B945's own honest limit is that it computes at *word* level, not in Isom(m004) —
a word-level stabiliser realises bundle symmetries but the converse is not established;
(ii) A7 may be doing its work somewhere the word level cannot see (an orientation or a
labelling downstream), which is precisely the *right object, wrong level* trap this repo
catalogues. **The discriminating computation has not been run.**

## 6. Motif structure — no hidden gap there

The built-in `gaps()` returns no avoiding motif pairs. **That is correct, not a bug**:
computed independently here, the strongest avoidance among the 17 motifs with ≥ 60 probes
is `dickson_tower × z3_generation` at **ratio 0.73** of expectation (17 actual vs 23.3
expected), with nothing below it. The corpus is well-mixed; **no two major themes are
strangers.** There is no structural gap hiding between motifs.

## 7. The honest ranking

1. **A1–A6 (and A7)** — the axioms everything rests on, motivated but not forced. The
   deepest ignorance, and it is *conditional-theorem* shaped rather than open-question
   shaped, which makes it attackable.
2. **`bridge_construction`, 5/14 with 8 dead** — the objective frontier by the atlas's own
   count; where attempts die.
3. **The un-derived residue** — {13·421493, 17·1129} (L130), plus 953's own origin (B931)
   and 5's (B937): a family of open residues the programme can *sort* by Frobenius class
   but cannot *derive*.
4. **The observer's mechanism** — newly OPEN as of B942/B948, not settled.
5. **`source_free`** — not ignorance but a **recorded dead end**. Listing it as a gap
   would be double-counting a closed door.

---

**Verdict: MAP.** The atlas's worst-resolved category is a correctly-recorded dead
direction, not a blind spot — and the real ignorance is the six motivated axioms
underneath everything, with A7 possibly discharge-able by a computation this programme
already ran for another reason. `bridge_construction` is the objective frontier.
