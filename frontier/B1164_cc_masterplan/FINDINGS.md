# B1164 — cc's part of the A–E masterplan: the observer freedom is 2 discrete + 1 continuous bits (all archimedean), the firewall is principled, and the phase route to W₀ is closed

**Status: banked (frontier). Verdict OPEN.** cc's three masterplan cells (MA2 census · MD1 firewall pin ·
ME3 phase/orbit), each own-verified (`verification/reproduce.sh` → `REPRODUCES`), with **two
verify-don't-trust corrections applied** — one to my own meditation, one to the workflow's own overreach.
Runs in parallel with cloud's five (their lane) and codex's MC1. No firewall crossing; Gate 5 clean.

## MA2 — the observer-bit census: 2 discrete + 1 continuous (a correction to my "one bit")

The whole program's observer freedom = the missing archimedean marking W₀, a point of (H↪ℂ), H the ring
class field ℚ(ζ₁₂)=K(√3) (disc 144, Gal=**V₄**). It factors:

- **Discrete bit D1 — the orientation** (√−3): chirality-insertion ≡ orientation ≡ the +Vol/−Vol sign are
  **one ℤ/2**, theorem-anchored (B1163: amphicheiral ⇒ mirror-fixed ⇒ un-self-suppliable; Bloch–Wigner odd).
- **Discrete bit D2 — √3** (the √3 hinge, B1155: ℚ(ζ₁₂)=K(√3), √|disc K|=√3).
- **Continuous C1 — the ℂˣ scale/dilaton** (B1163 §B: ratios forced, scale withheld).

**Correction to my meditation §A:** I said the open items were *one* bit (the orientation). That undercounts.
The **branch-selection V₄** (B1161's free orbit) is **(ℤ/2)²** — orientation is *one leg* of it, not the
whole. Own-verified: (ℤ/12)^× = {1,5,7,11} = V₄ ⇒ **2 discrete bits**; plus the continuous scale.

**Correction to the workflow (verify-don't-trust):** the MA2 agent guessed **bit D2 = √5 ("hearing")** and
flagged it unproven. It is **wrong** — the three quadratic subfields of ℚ(ζ₁₂) are ℚ(√−3), ℚ(√3), ℚ(√−1);
**√5 is not among them.** The √5/E₈ "hearing" end is the *separate two-ended structure*
(E₆/ℚ(√−3) ↔ E₈/ℚ(√5)), not a subfield of this ring class field. **D2 = √3.** (Whether the branch-V₄ is
*literally the same torsor* as the being×hearing V₄ (B713) and the measurement torsor H¹=(ℤ/2)² (B1024)
stays an unproven residue — three d=2 presentations, isomorphism unchased.)

## MD1 — the firewall pin: PRINCIPLED and FALSIFIABLE (§D settled)

The pinned structure-vs-value classifier, committed as the B950 decision procedure. For each banked datum d:
- **dim(d)** ∈ {dimensionful, dimensionless}; **prov(d)** ∈ {forced (group theory / arithmetic / anomaly, no
  fit to a measured number), fitted}.
- **d is STRUCTURE iff dim=dimensionless AND prov=forced; VALUE iff dimensionful OR fitted.**

**Provenance is the discriminator** (dimensionlessness is necessary-not-sufficient — 18/19 SM params are
dimensionless ratios). **Tested against the whole corpus, zero exceptions.** Borderline rule (precedent):
**decompose compound objects** — hypercharge = {direction: anomaly-forced, dimensionless = STRUCTURE} +
{normalization/g′: homogeneous scale = VALUE}; own-verified, the cubic [U(1)]³ is homogeneous (fixes the ray,
leaves the scale free). **Outcome-independent:** sin²θ_W=3/8 stayed STRUCTURE through a 16σ downstream miss
(B915) — an outcome-fitted line would have demoted it. **The falsifier** (the pin's teeth): the firewall
breaks iff a banked datum is prov=forced *and* dimensionful-or-equals-a-measured-value-by-derivation; the
standing test is the value campaign (B1124–B1137: 0/18, Koide proven a coincidence in-sandbox). **The line
survived by structure, not stipulation — it is principled, not movable.** This discharges meditation §D.

## ME3 — the phase-trivial marking does NOT break the orbit (W₀ pruned to modulus-only)

A phase-trivial marking leaves the free V₄ orbit intact and selects no branch. Two independent own-verified
angles: **place-separation** — the shape z=e^{iπ/3} is a primitive 6th root (z⁶=1, arg/2π=1/6), so the CS
"phase" is torsion in μ₆ (finite place); the orientation is the *sign* of Bloch–Wigner (archimedean,
mirror-odd, D(z)=−D(z̄)); the two orientation branches carry **identical torsion, opposite volume-sign**, so
fixing the phase leaves the ℤ/2 undetermined. **Arithmetic** — a phase-trivial condition is Galois-invariant
⇒ constant across all 4 branches ⇒ zero selectivity. **Consequence:** cloud's beat-conjugation relay (the
phase is object-forced = 0) is **confirmatory, not orbit-breaking** — after it lands, cloud's W₀ search
should be **re-scoped to modulus-only** (the orientation ℤ/2 + the scale). The phase route is closed.

## §E — the gravity placement (conservative; the hypothesis this feeds)

The convergent signal across all three cells: **everything the observer supplies is archimedean (∞-place)
data** — the orientation bit (Bloch–Wigner is the archimedean regulator; +Vol/−Vol lives at the real
embedding H↪ℂ) and the ℂˣ scale/dilaton (the modulus of the same marking) — **while everything the object
forces is finite-place/torsion (the CS phase, μ₆) or place-independent structure.** The ∞-place is where the
metric/volume/gravity live. **So the observer's freedom = the data at the archimedean place = a candidate for
exactly the gravitational marking.** *Placement established; the identification "observer scale+orientation =
the gravitational data" is the §E hypothesis this contribution FEEDS, not a banked result* — cloud's ME1 (the
exact geodesic spectrum) and ME2 (the gravity ablation) attack it from their side; the residue is splitting
ℂˣ=ℝ⁺×U(1) (dilaton + axion?).

## Fences

No firewall crossing; Gate 5 clean (only Galois/dimension/torsion/regulator facts — no measured value
asserted as derived). Own-verified: the V₄ count + the √3 correction, the hypercharge homogeneity, the ME3
place-separation. The single-torsor identification (MA2) and the observer=gravity identification (§E) are
explicitly **unproven residues**, flagged, fed to the relevant seats — not banked as results.

## Routes

- **Meditation:** §A corrected (2+1 bits, not one; D2=√3 not √5); §D **settled** (firewall principled); §E
  placement established (observer=archimedean, the gravity identification the live hypothesis).
- **Relays:** MD1 classifier → the corpus commitment (this arc pins it); ME3 → cloud (re-scope W₀ to
  modulus-only); the MA2 single-torsor residue → whichever seat chases the disc-144 isomorphism.
- **cloud's five + codex MC1** still running; cc verifies as they land.
