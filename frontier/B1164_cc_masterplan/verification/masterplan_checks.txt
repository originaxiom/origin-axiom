(MA2) THE OBSERVER-BIT COUNT = 2 discrete + 1 continuous
   Gal(H/Q) = (Z/12)^x = [1, 5, 7, 11] order 4 = V4 = (Z/2)^2 => 2 DISCRETE bits
   H=Q(zeta12)=K(sqrt3): quadratic subfields = Q(sqrt-3), Q(sqrt3), Q(sqrt-1). zeta12+1/zeta12 = sqrt(3) = sqrt3.
   => bit1 = sqrt-3 (orientation/being, B1163 theorem); bit2 = sqrt3 (the sqrt3 hinge, B1155).
   *** CORRECTION (verify-don't-trust): the workflow guessed bit2 = sqrt5 (hearing) and flagged it;
       it is WRONG -- sqrt5 is NOT a subfield of Q(zeta12). The sqrt5/E8 'hearing' end is the SEPARATE
       two-ended structure, not this ring class field's V4. bit2 = sqrt3. ***
   + 1 CONTINUOUS: the C^x scale/dilaton (B1163 sec-B: ratios forced, scale withheld).

(MD1) THE FIREWALL CLASSIFIER -- the pinned criterion (principled + falsifiable, sec-D)
   d is STRUCTURE iff dim(d)=dimensionless AND prov(d)=forced; VALUE iff dimensionful OR fitted.
   Discriminator = PROVENANCE (dimensionlessness necessary-not-sufficient: 18/19 SM params are ratios).
   Hypercharge decomposes: DIRECTION (anomaly-forced, dimensionless) = STRUCTURE; NORMALIZATION/g' = VALUE.
   [U(1)]^3 under Y->lambda*Y: 0 = lambda^3 * 0  => homogeneous; DIRECTION fixed, SCALE free.
   Outcome-INDEPENDENT: sin^2 theta_W = 3/8 stayed STRUCTURE through a 16sigma downstream miss (B915).
   Falsifier: the value campaign (B1124-B1137, 0/18, Koide a proven coincidence). Line survived by structure.

(ME3) THE PHASE-TRIVIAL MARKING DOES NOT BREAK THE ORBIT
   shape z=e^{i pi/3}: z^6 = 1.0 (order 6, torsion mu_6); arg(z)/2pi = 0.166667 = 1/6 (finite place)
   D(z)= 1.0149416  D(zbar)= -1.0149416  sum= 0.0 (orientation = archimedean sign, mirror-odd)
   => the phase (finite/torsion) is Galois-INVARIANT (shared by both orientation branches);
      it does NOT select +Vol vs -Vol. Phase-trivial marking = orbit-PRESERVING. Prunes W0 to modulus-only.

(sec-E GRAVITY PLACEMENT, conservative) both observer bits + the scale are ARCHIMEDEAN (infinity-place);
   the object forces finite-place/torsion (the phase) + place-independent structure. The infinity-place
   is where the metric/volume/gravity live. => observer freedom = archimedean data = a CANDIDATE for the
   gravitational marking. PLACEMENT established; the IDENTIFICATION is the sec-E hypothesis (not banked).

REPRODUCES
