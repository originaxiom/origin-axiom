# B638 — C1: THE SWAP DECOMPOSITION (prereg; the owner's pick, 2026-07-16)

**Sealed before computing. Mathematics only; Gate 5 stands. MB13 (swept):
the swap/deck involution of any double has never been computed as an
operator on twisted cohomology anywhere in the bank (grep clean); the
banked ingredients: the weld intertwiner u (B637 part 1, det 1), the
corrected chains, the nine 3-form tables, the observed 24ζ₆ core law +
reality pattern + zero law (B637 stage 3).**

## The object

The swap σ of the weld double D (exchange the two copies:
(a,b) ↔ (c,d)) is an automorphism of π₁(D) (it permutes the four
relators up to inversion/conjugation). Its coefficient companion is the
ANTILINEAR map J = Ad(U₂₇)∘conj (the mirror side is the conjugate rep
intertwined by U). Together they act on cocycles:
(σ*z)(x) := J(z(σx)) — an antilinear operator on H¹(D;27), i.e. a
candidate REAL STRUCTURE on the five modes.

## The gates (locked order, each two-outcome)

1. **G1 (the involution property):** u·conj(u) = ±1 at the SL(2) level
   (⇒ J² = ±id); σ*z is a cocycle for every basis cocycle (the Fox rows
   annihilate it); σ*² = id on H¹ (mod coboundaries). FAIL ⇒ the swap
   does not act as posed; report the obstruction (the honest kill of
   C1's mechanism reading).
2. **G2 (the real structure):** the matrix of σ* on the 5-dim H¹ (mod
   coboundaries; antilinear = compute σ*(rep_i) in coordinates with
   conjugation); the fixed real subspace's dimension (expect 5 real
   dims if a genuine real structure).
3. **G3 (the transformation law of Y):** verify numerically
   **Y(σ*α, σ*β, σ*γ) = −conj(Y(α,β,γ))** on at least three
   independent triples (the sign from orientation reversal; the conj
   from antilinearity). Either outcome banks; the law's precise form
   (± and conj placement) is DERIVED FROM THE DATA if the guess fails.
4. **G4 (the reality theorem):** in a σ*-adapted basis, derive which Y
   components must be pure √−3-imaginary and which mixed; compare with
   the observed pattern (Y[123], Y[034], Y[124] pure imaginary; Y[023]
   mixed). PASS = the observed reality pattern is EXPLAINED by the
   swap.
5. **G5 (the 24ζ₆ mechanism):** does σ* mix the boundary-born pair
   {0,1} with a unit factor that forces Y[023] = 24ζ₆·Y[123]? Compute
   σ*'s action on the pair; if the identity follows, the LAW OF THE
   CHORD'S CORE is proved on the weld double (and the argument
   transfers to the involution doubles by the same structure).

## MB12 (computed at prereg)

Non-trivial: no swap-action computation exists. Can fail: G1's
intertwiner condition can fail (u·conj(u) ∉ ±1); σ* can fail to
preserve cocycles; the transformation law can differ. Can pass: the
banked reality pattern strongly suggests the structure exists. Every
gate's failure is a bankable structural fact.
