# xB005 — the node's ℤ/3 IS 2T/Q₈, realised dynamically — and that is exactly why the ℤ/3 recurrence is not evidence

**Status: banked (frontier, seat branch `sep16-branch`). Verdict PROVED.** Seat `xb`.
**PREREGISTRATION sealed and pushed BEFORE any cell ran** — `PREREGISTRATION.md`, sha256
`afb4dc8284d4e824df46d9e96d6d3a9e041b5e3b68badab4a5d229390b7a8a96`, committed `1b047ae` and
pushed to the remote before this file, before `verification/node_z3.py`, and before any result
existed. **The order is checkable from this branch's history** — the property xB001 found the
record's other seals cannot support. Gate 5 untouched. Lock: `verification/reproduce.sh`, three
cells, each asserting its own mathematics.

## The result against the sealed criteria

| cell | sealed criterion | outcome |
|---|---|---|
| **Q1** | monodromy fixes the node and acts there with order exactly 3 | **PASS** — `D³ = I`, `D ≠ I`, spectrum `{1, ω, ω̄}` on the unit circle |
| **Q2** | is that action the **outer** ℤ/3 of `Q₈`? | **PASS** — a 3-cycle, realised by an automorphism of `Q₈`, and that automorphism is **outer** |
| **Q3** | §2's linkage test on the programme's ℤ/3 family | **all four canonically linked** — the inconvenient outcome named in the seal |

The declared prior was Q1 PASS, Q2 PASS (moderate confidence), and — for Q3 — that the node's
order-3 would be **the one member not obviously linked**. **Q2 confirmed; the Q3 prior is
overruled by its own arc.** The seal named the consequence in advance:

> *"If Q3 shows all four linked, the correct verdict is that the whole ℤ/3 family is forced by the
> ramification of 3 in `ℚ(√−3)` and carries no object-specific information."*

That is what happened. The verdict below is the one the seal committed to, not one chosen after
seeing the answer.

## Q1 — the node, and its order-3 derivative

On the leaf `κ = −2` (the Markov surface, B1347) the trace map `F = F_L ∘ F_R` (xB003's monodromy,
proved there to be m004's) fixes `(0,0,0)`, and `κ(0,0,0) = −2` exactly. Its derivative there is

> `D = [[0,0,1],[−1,0,0],[0,−1,0]]`,  `D³ = I`,  `D ≠ I`,  spectrum `{1, ω, ω̄}`.

So the node carries an **order-exactly-3** linear action, with all eigenvalues on the unit circle —
an *elliptic* point of an otherwise pseudo-Anosov map. This re-derives B123's elliptic spectrum at
the node from the trace map directly, and it is the exact complement of xB004: the object's
*regular* fixed point is hyperbolic (multipliers `(5±√21)/2`, a saddle, no vacuum), while its
*singular* point is elliptic and of finite order. The two are not in tension — the node is where
`dκ` vanishes, so it is not a smooth point of the leaf and carries no leaf dynamics in xB004's
sense.

## Q2 — the identification, exhibited and acting

The node is the **quaternion character**: `tr a = tr b = tr ab = 0` is realised by
`ρ(a) = i, ρ(b) = j, ρ(ab) = k` in `SU(2)`, with `tr[i,j] = −2` — verified, so the character sits
on the object's own leaf and not merely at a point with the right traces.

Push the monodromy through in its literature presentation `φ(a) = ab`, `φ(b) = bab`:

> `(a, b, ab) = (i, j, k)  ⟼  (k, i, j)` — a **3-cycle** on `{i, j, k}`, with no sign correction
> needed.

Two things are then checked rather than asserted, which is what makes this a map that *acts* and
not a name that matches:

1. **It is an automorphism of `Q₈`** — verified on all `8 × 8 = 64` products, exactly.
2. **It is OUTER** — no element of `Q₈` conjugates `i` to `k` (checked over all eight elements).

`2T = Q₈ ⋊ ℤ/3` and `2T/Q₈ ≅ ℤ/3` is precisely the subgroup of `Out(Q₈) ≅ S₃` generated by the
3-cycle on `{i, j, k}`. So:

> **The monodromy's order-3 action at the node IS `2T/Q₈` — not a group of the same order, the
> same group acting the same way, realised dynamically on the character variety.**

This is an identification in B1231's sense, and it is **EARNED**: registered as **I-31**.

**Fence, stated because the ledger's discriminator demands it.** `Out(Q₈) ≅ S₃` contains exactly
one ℤ/3, so "the 3-cycle" fixes the subgroup uniquely; *which* generator (the 3-cycle or its
inverse) the monodromy realises depends on the orientation convention, and that ambiguity is real
but is an ambiguity *inside* the identified ℤ/3, not between two candidate groups.

## Q3 — the finding: the four ℤ/3s are one fact wearing four hats

The programme's record leans, in several places, on a recurrence of ℤ/3: `μ₃ ⊂ ℚ(√−3)` (the trace
field's roots of unity), `Z(E₆)` (order 3, `= det` of the E₆ Cartan matrix, verified), `2T/Q₈`
(order `24/8 = 3`), and now the node's order-3 action. §2's own standard for whether a recurrence
is evidence is whether the faces are **canonically linked** or **independent**. Run on all four:

| pair | link | status |
|---|---|---|
| `μ₃` ↔ `ℚ(√−3)` | `ℚ(ζ₃) = ℚ(√−3)` | **the same object**, not two |
| `2T/Q₈` ↔ `Z(E₆)` | McKay: the centre of the simply-connected group is the abelianisation of the binary polyhedral group | **canonically linked** (this is I-1, already EARNED) |
| the node's ℤ/3 ↔ `2T/Q₈` | Q2 above | **identical**, not merely isomorphic |

> **All four are canonically linked.** By §2's own standard the recurrence is **not evidence**: it
> is **one fact — the ramification of 3 in `ℚ(√−3)` — wearing four hats.**

**This is the same shape as I-17/B727** (*"One E₆, three ADE hats … the recurrence is FORCED, not
evidence; only the atom `ℚ(√−3)` is object-specific"*), and this arc extends B727's conclusion to
the fourth hat, which B727 did not have. B727 said the atom is the only object-specific content;
Q2 shows the node's ℤ/3 is **not a fifth independent witness** to that atom but a fourth
presentation of it.

## What this does and does not change

**Strengthened, not withdrawn.** Q2 is a genuine piece of mathematics and an upgrade in kind over
an order match — the record previously had `2T/Q₈` and the node's elliptic spectrum as two
separate facts, and they are now one, with the map written down. The **arithmetic** statement is
untouched: 3 ramifies in `ℚ(√−3)`, and everything here is downstream of that.

**What it costs.** Any argument of the form *"ℤ/3 shows up here, and here, and here — that is
evidence"* is now closed on this family, by this arc's own computation. The four are not four
independent coincidences to be multiplied; their joint content is the content of one.

**What stays closed.** The **ℤ/3-sameness question against the trinification grading** remains
closed **negatively** (chat1 closure handoff §4: *"the arithmetic ℤ/3 = 2T/Q₈ is the CENTRE of E₆,
not the trinification grading"*), and nothing here reopens it — this arc compared the node's action
with `2T/Q₈`, not with the grading. **I-14** (B305's grading ℤ/3 ≡ the commensurator's Eisenstein
ℤ/3) is likewise untouched and stays **UNEARNED**: the grading is a different side of E₆ from its
centre, and this arc supplies no map to it.

## Scope

Character-variety and finite-group theory. No value, no generation count, no physics reading,
nothing promoted to `CLAIMS.md`. Gate 5 absolute, as sealed.

**Provenance.** `verification/node_z3.py` → `verification/reproduce.sh` (three cells, all
asserting). Cross-refs B123 (elliptic spectrum at the node), B1347 (the leaf is the Markov
surface), xB003 (`F_L ∘ F_R` is m004's monodromy; the ramified prime), xB004 (the regular fixed
point is a saddle), B727/I-17 (the forced-recurrence template), I-1 (McKay, `2T ↔ E₆`).
