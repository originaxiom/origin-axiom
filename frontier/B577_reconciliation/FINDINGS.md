# B577 — the reconciliation: B575/B576 rediscovered the banked {4,8}-integrability program

**Date:** 2026-07-14. **Trigger:** the owner's audit question ("have we addressed the
unfinished tasks from the audit — might they affect our current math?"). Checking the
pre-existing L53 row surfaced **B352**, and B352 surfaced the whole thread.

## What was already banked (the {4,8}-integrability program)

| Bank | Content | Relation to the new arc |
|---|---|---|
| B264 | the E₆ character variety, H¹ = 6, Fox machinery | the foundation (was cited) |
| **B265** | **the density dichotomy: {4,8} generate dim 78 = e₆ (E₆-Zariski-dense); {5,7,11} generate f₄; exp-1 = the SL(2) curve** — Sage, exact over ℚ | **= B576's core result, banked weeks earlier** |
| B270 | the cup obstruction at the SL(2)/Sym² foundation; "deformations are cusp deformations" | the rigor step + the head start for L71 |
| B351 | the exact e₆ Chevalley basis + θ | the predecessor build |
| **B352** | **the cup-product obstruction: all six directions + the mix unobstructed at second order** — dps-100, coboundary + m=1 controls, θ-parity signature, "(conditional)" = computer-assisted | **= B575's core result, banked weeks earlier** |
| pre-existing L53 | the third-order Massey step, scoped with the B352 machinery | = the arc's "L60", registered as if new |

## What the new arc genuinely added

- **B575:** exactness and unconditionality — ℚ(√−3) symbolic end-to-end on an
  *independent* build (the gl(27) minuscule model vs B351's Chevalley basis); the
  constructive z₂ certificates; the coboundary-invariance and raw-nonzero probes.
  **Two disjoint pipelines now agree on all 21 zeros: B352's "(conditional)" is
  discharged.** This is the strongest possible epistemic state for the result —
  better than either computation alone.
- **B576:** completeness — the block-sum lemma (B265 computed three generated
  dimensions; B576 proves those exhaust the landscape for ALL mixtures: every
  sl₂-stable subalgebra is a block-sum, the closed-subset lattice is exactly
  {F₄-substrata} ∪ {e₆}), the six forcing channels, the parity-converse zero scan.
- **The synthesis:** the identification of the θ-odd escape sector with σ's
  imaginary axes (Q-A) and C3's living monodromy — the two-chiralities frame
  postdates B265 and is the arc's own contribution.

## The failure mode (new — not B571's)

B571 diagnosed *burial*: results never banked. This is different: **non-recall** —
B352 was properly banked *with locks* and then not surfaced when the same question
was re-asked under a new name ("the bridge obstruction" vs "the cup-product
obstruction"). Four layers missed it: the L51 registration, the B571 excavation
(which hunted *unbanked* treasure — banked-with-locks was invisible to its rubric),
Review 16 (window-scoped), and the B575 prereg. The Recurrence Atlas — built for
exactly this — was never queried. A side symptom: the Review-16 lead registrations
reused numbers L51–L57 that the file already assigned (now renumbered L62–L71).

## The guard (MB13, added to REPRODUCIBILITY.md)

> **MB13 — banked ≠ recalled.** Before preregistering any computation or
> registering any lead: (1) grep `frontier/` and `docs/OPEN_LEADS.md` for the
> MATHEMATICAL KEYWORDS of the question (not the campaign's name for it);
> (2) run the atlas oracle (`scripts/atlas/query.py`). A banked answer to the
> same question under another name defeats the prereg. The B575/B352 pair is
> the type case: "cup product" or "obstruction" as a grep would have surfaced
> B352 in one second.

## Also in this bank (hygiene)

- The B575/B576 heavy locks converted to the **B352 house pattern**: always-on
  fast tier + the full exact build gated behind `OA_SLOW=1` (the suite returns
  to ~36 min; reviews run with `OA_SLOW=1`).
- PC26's central theorem is credited jointly: **B265 + B352 (the thread) and
  B575 + B576 (exactness + completeness + the σ-synthesis)**.
- Pre-existing duplicate lead numbers L5/L20/L22 (not from this arc) flagged
  for the next review — not silently renumbered.

## The honest summary

The mathematics is *stronger* than yesterday — independently double-computed,
exact, complete. The narrative is corrected: the escape sector was found weeks
ago by the {4,8}-integrability program; this arc confirmed it exactly, completed
its classification, and connected it to the chirality frame. The process lesson
is banked as MB13. Credit runs to the thread that got there first.

Firewalled. Nothing to CLAIMS.md.
