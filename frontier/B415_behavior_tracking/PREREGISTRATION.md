# B415 (BEHAVIOR-TRACKING campaign) — PRE-REGISTRATION: track the object's destination, blind

**Committed before computation. Owner method (2026-07-04): do NOT hunt the SM; TRACK the
object's own behavior to where it leads, characterize the destination in pure math, and let
SM emergence be judged ONLY by the pre-registered bar below. Firewalled. The three
tracking disciplines are binding: (1) follow the object's forced next step, blind to
physics; (2) name every structure as pure math first; (3) the emergence bar is fixed HERE,
before looking.**

## What we track (the object's own asymptotic behavior)

The single-seed tower is a refining measure (B412) with a flat/Gauss-sum spectrum (B413);
its N→∞ / continuum limit is a measure μ_∞ on the profinite group lim(ℤ/3ᵏ × ℤ/5) = ℤ₃ × ℤ/5.
"Where it leads" = μ_∞ and its invariants. Track, blind:
- T1: the continuum limit μ_∞ — does the flat/Gauss-sum spectrum PERSIST in the limit
  (a well-defined limiting distribution), or degenerate? Characterize μ_∞ completely in
  pure-math terms (its Fourier/Mellin spectrum, its support, its symmetry group).
- T2: the object's forced next step from μ_∞ — whatever μ_∞'s structure POINTS to
  (its automorphism group, its L-function, its self-similarity), follow it, blind.
- T3: complete the pure-math characterization: name μ_∞ (Haar / Gauss-sum-modulated Haar /
  a known p-adic distribution / novel). No physics vocabulary.

## THE EMERGENCE BAR (registered before looking — the ONLY thing that judges "SM emerged")

A structural invariant of μ_∞ counts as "SM structure emerged" iff ALL of:
(i) it is FORCED (no parameter freedom, no choice in its extraction);
(ii) it was NOT sought — it appears in the blind pure-math characterization, named as math;
(iii) it matches a SPECIFIC SM structure (gauge-group rank pattern, generation count,
     anomaly lattice, a conservation-law group) EXACTLY, not approximately;
(iv) it survives a null gauntlet (the same invariant of a CONTROL object — a generic flat
     measure, a wrong-tower limit — does NOT match).
Anything short of all four = a coincidental shadow (the null), banked as "no emergence."

## Outcomes (two-outcome, either banks)

- EMERGENCE: a μ_∞-invariant clears all four bars → the first non-self-deceiving SM sighting
  → the dedicated adversarial session (owner present) before any voicing (S042/S043).
- NO EMERGENCE: μ_∞ is completely characterized as pure math with no bar-clearing invariant
  → the honest destination is a specific mathematical object (named), and the object does
  not lead to the SM under blind tracking → a strong, honest terminal statement.

Machinery: the banked tower values → the exact continuum limit (profinite Fourier / the
flat-spectrum limit); exact throughout; controls built in. Locks per T-step.
