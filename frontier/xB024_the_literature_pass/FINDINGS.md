# xB024 — THE LITERATURE PASS: five sources read at source, one defect found in our own work, and NO new insight

**Date:** 2026-09-17 · **Seat:** `xb` · **Branch:** `sep16-branch` · **Verdict: PROVED**

**PREREGISTRATION sealed `9880dc0189734102…`, committed and pushed at `700a4ae` BEFORE any source
was opened**, with a prediction for each of seven targets and three binding kill conditions.

---

## THE HEADLINE, INCLUDING THE PART THAT IS NOT FLATTERING

The seal named the failure this arc most risks:

> *"If this pass finds no insight beyond what the record has, it must say so plainly. Reporting a
> literature sweep as productive when it only confirmed things is the failure this arc most risks."*

**It found no new insight.** No paper changes how we see the object, the family, or their roots.
What it did produce is **one correction to our own work, two refinements, one fence retired, and two
sources that remain out of reach** — and those are reported at exactly that size.

| # | target | sealed prediction | outcome |
|---|---|---|---|
| **T1** | **CGHN** | *"the record's use is CORRECT"* | **PREDICTION UNTESTED — UNREACHABLE.** Project Euclid paywall; **not on arXiv**; Neumann's free survey (KAIST) returned **HTTP 503 on two attempts**. **Still the load-bearing unread citation in the L194 chain.** |
| **T2** | **Meyerhoff–Ouyang** | *"UNREACHABLE will stand"* | **CONFIRMED.** Cambridge paywall, no preprint. **Two new leads found and NOT read:** Ouyang, *A simplicial formula for the η-invariant of hyperbolic 3-manifolds*, Topology **36** (1997) 411–421; Yoshida, Invent. Math. **81** (1985) 473–514. |
| **T3** | **Neumann–Reid** | *"correct but carries hypotheses; the fence retires only if m004 meets them"* | **CONFIRMED, hypothesis MET — and it found a defect in xB022.** |
| **T4** | **Witten eq. 2.2** | *"CORRECT"* | **CONFIRMED, and stronger than the record states.** |
| **T5** | **Gukov §1.1** | *"CORRECT"* | **CONFIRMED, with a refinement the record does not carry.** |
| **T6** | **Floyd–Hatcher / Guéritaud** | *"no correction expected; read for insight"* | **No correction. No insight.** Located, graded CITED, not read at source. |
| **T7** | **Minsky** | *"re-check only"* | Left to B1404, which verified it against the arXiv record. **Not re-done, not re-claimed.** |

---

## T3 — NEUMANN–REID, AND THE DEFECT IT FOUND IN xB022 V4

Read at source from Neumann's own copy. The hypothesis, **verbatim**:

> *"…the invariant trace field `k(Γ)` in case `M = H³/Γ` is a **CUSPED** hyperbolic manifold. By
> [EP], M has at least one triangulation by ideal tetrahedra: `M = S₁ ∪ … ∪ Sₙ` … Denote the field
> `ℚ(z_j : j = 1,…,n)` by `k_M` or `k_Δ`. A priori `k_Δ` might depend on the choice of
> triangulation, but we have:* **Theorem 2.4. `k_Δ = k(Γ)`.**"

**THE DEFECT.** `k_Δ` is generated by **all n** tetrahedral parameters. **xB022 V4's instrument
computed the minimal polynomial of `z₁` alone** — `ℚ(z₁)`, which can be a proper subfield.
For m004 and m003 the two tetrahedra share a shape, so the reported value was right; **but the
40-word corpus comparison was of the wrong object.**

**L1 — the corrected computation.** `k_Δ` via a primitive element `Σ c_j z_j`, under a guard:
`m004` and `m003` both give **`x² − x + 1` = ℚ(√−3)**; **primitive-element stability** checked
against a second coefficient vector; **positive control** `m015` gives `x³ − x − 1`.

**L2 — the defect sized, and xB022's conclusion tested against it.** On 72 determined words,
**`ℚ(z₁) ≠ k_Δ` in 0 of 72**, and **A5 leaves `k_Δ` unchanged 72 of 72** — exactly what xB022 V4
reported for `ℚ(z₁)`.

> **xB022 V4's CONCLUSION IS UNCHANGED. Its INSTRUMENT was measuring the wrong field, and the
> answer it gave was right anyway.** Both halves are stated, because only reporting the first would
> be the failure this record keeps catching.

**AND A SECOND GUARD HAD TO BE ADDED MID-CELL.** The first run reported **one** difference
(`RLLLRR`) — a **degree-12 polynomial with 10¹⁸-scale coefficients confirmed at only 80 digits**.
A spurious `algdep` relation of degree `d` and height `H` becomes findable once precision falls
below `≈ d·log₁₀H ≈ 216` digits. **That detection was unsupported by its own precision — the exact
shape of this session's E56 instance.** A **height guard** (`dps > 2·d·log₁₀H`) was added; the
"difference" vanished, and **42 of 114 words are now reported UNDETERMINED rather than counted
either way.** `None` from this instrument means **undetermined, never "no field."**

**Consequence, at its exact size:** xB022 V4's fence — *"the identification with the invariant
trace field is NOT claimed"* — **is RETIRED for the cusped corpus**, since Theorem 2.4's hypothesis
is *cusped* and every manifold involved is cusped.

## T4 — WITTEN, READ AT SOURCE

> *"the coefficient of `Re W` must be an integer, while … the coefficient of `Im W` may be an
> **arbitrary complex number**. The action therefore has the general form*
> `I = −s Im W + ℓ Re W,  s ∈ ℂ,  ℓ ∈ ℤ.` **(2.2)** … `t = ℓ + is`, `t̃ = ℓ − is`. **(2.4)**"

**Confirmed, and stronger than the record states:** `ℓ` (our `k`) is forced integral **by the mod-2π
indeterminacy of `Re W`**; `s` (our `σ`) is an *arbitrary complex number*, independent of `ℓ`.

## T5 — GUKOV, READ AT SOURCE

> *"where `t = k + is` and `t̄ = k − is` are the corresponding coupling constants. Consistency of the
> quantum theory requires the "level" `k` to be an integer, `k ∈ ℤ`. **The other parameter, `s`, is
> not quantized. However, `s` must obey certain constraints imposed by unitarity [1].**"*

**Confirmed — with a refinement the record does not carry.** The record says `σ` is *"a free
continuous parameter"*. The source says **not quantized, but constrained by unitarity.** That is
looser in our phrasing than in the original. **It does not change xB016's closure**, which needs
only that nothing couples `σ` to `k` — and nothing does.

---

## WHAT THIS ARC DOES NOT DO

**It does not close L194.** It does not upgrade any grade without a quotation. **It claims no
novelty from an absence** — *"we could not find it"* is not *"it does not exist"*, and
`NOVELTY_AUDIT.md` exists for that purpose. **It leaves CGHN unread**, and CGHN remains the
load-bearing unread citation in the L194 chain — the kill condition applies: **an UNREACHABLE source
is used for NOTHING here.**

**Axes held fixed and named:** five sources read at source, two unreachable, two graded CITED
without reading · L2's word corpus is length ≤ 6 with 42 of 114 undetermined under the height guard ·
the shape-field instrument is precision-limited and says so.

**Gate 5 absolute. Nothing to `CLAIMS.md`.**
