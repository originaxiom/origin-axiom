# xB021 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell runs.**

## P0

Group actions on a torsion invariant; a synthesis over this seat's own banked arcs. Named
mathematics. No value, no generation count, no physics reading, nothing to `CLAIMS.md`. Gate 5
absolute.

## The owner's instruction

> *"the sum of all negatives should clarify the riddle for us and help us understand why our question
> might be ill posed. maybe there's a mechanism to make them interact and have what we need emerge"*

## The hypothesis this arc tests

Every negative this seat produced says some `X` **carries no information at the object**. The
hypothesis is that they are **one fact**:

> **The object sits at a FIXED POINT of exactly the symmetries whose action would carry the
> information — and a fixed point cannot report on its own stabiliser.**

Concretely, on `CS ∈ ℝ/(½)ℤ`, the three bits act as:

| bit | action on `CS` | established by |
|---|---|---|
| **A5** (the `−I` / knot-ness bit) | `x ↦ x + ¼` | xB015 K6, 494/494 words at 50 digits |
| **A7** (the `LR`/`RL` order bit) | `x ↦ −x` | xB018 C5, 240/240 words at 40 digits |
| **A6** (orientation / the squaring) | `x ↦ −x` (complex conjugation) | xB020 H2, derived |

**If the hypothesis is right, `A6` and `A7` lie in the stabiliser of the object's value `CS = 0`, and
`A5` does not** — and each banked negative should be an instance of "the thing asked about was in the
stabiliser."

## THE CONFIGURATION AXES — declared first (xB017 Addendum 4's rule)

| axis | values | this arc |
|---|---|---|
| **the invariant acted on** | `CS` / volume / `H₁` / trace field | **`CS` only — named, and the limitation is the arc's main scope note** |
| **the bits** | A5 · A6 · A7 | **all three** |
| **the locus** | the object (`CS = 0`) / the 2-torsion `{0,¼}` / the full circle `ℝ/½ℤ` / the family's ℤ/12 | **all four** |
| **the negatives audited** | this seat's arcs xB005–xB020 | **all, from their banked verdicts, not from memory** |
| the record's older negatives | B1012, B1234, B1224, … | **cited where they match; NOT re-derived** |

## The cells

**S1 — the group action, computed.** Generate `⟨A5, A7 = A6⟩` acting on `ℝ/½ℤ`; compute the
stabiliser of `0` and the orbit of `0`.
*Prediction:* group of order **4**; **stabiliser of order 2 = {identity, negation} = {A6, A7}**;
**orbit `{0, ¼}`**; orbit-stabiliser `4 = 2 × 2`.

**S2 — THE AUDIT, and the cell that can refute the whole hypothesis.** Take each banked negative
and classify it: is it an instance of *"the thing asked about is in the stabiliser"*?
*Prediction:* the CS-facing negatives fit. *Kill condition, binding:* **if fewer than half fit, the
hypothesis is a story and must be reported as refuted.** **And any negative that does NOT fit must be
listed explicitly — an audit that only reports its hits is not an audit.**

**S3 — the ill-posedness, stated precisely.** If the object is the fixed point, then *"derive `X`
from the object"* — for any `X` the stabiliser would have to carry — is **empty, not hard.**
*Required honesty:* this is a statement about a **class** of questions, and the arc must say which
questions it does **not** cover.

**S4 — the emergence mechanism, and its honest limit.** If the information is in the **orbit**, the
remedy is to work with the orbit. **Exhibit:** xB015's family index — the object realises **one**
value, the family realises **all twelve**. *Required honesty, declared now:* **xB016 already showed
an index cannot fix the free anchor `σ`**, so the orbit's content so far is **topological, not a
value**, and this arc must say that in its headline rather than let "the orbit has information" read
as "the orbit has the answer."

**S5 — what this does NOT do.** It derives no value. It re-poses a question.

**S6 — the verdict**, naming every axis held fixed.

## What this arc will NOT claim

Not that the fixed-point picture derives any physical quantity · not that the orbit supplies `σ`
(xB016 closed that) · not that this replaces B1234, which already found the mirror upstream of eight
walls — **this arc extends that from one stabiliser element to the whole stabiliser, and says so** ·
no identification (E82/I-10) · nothing to `CLAIMS.md`.

## The seat's prior, declared

S1 as predicted · **S2 is the real test and I expect some negatives NOT to fit — the ones about
arithmetic (xB011, xB017) rather than about `CS`** · S3 holds for the CS-facing class · **S4's honest
answer is that the orbit carries an index and an index is not a value** · **S6: this arc's best
outcome is a DIAGNOSIS, not a door — and if it reads as a door, it is over-claiming.**
