# B412 — the tower is a REFINING MEASURE (the scale question's productive afterlife)

**Status: a directional finding (owner-requested "what else" sweep), exact on all banked
levels. Not a promotion — a new organizing angle + its named home. Firewalled.**

## The exact structure (all values banked)

The single-seed tower is a MASS-CONSERVING REFINEMENT, not a value sequence:
- Total mass = 1 at every level (the sum rule, now proven exact over ℚ at depth 5).
- 15 → 45 → 135: 4 cells, each 1/4 (relabeling of support; value unrefined).
- 135 → 405: each 1/4 parent splits into the ζ₉⁺ ORBIT {(1+c₁)/12, (1+c₂)/12, (1+c₄)/12},
  which SUMS to (3 + Tr c)/12 = 1/4 exactly (Tr c = 0). A genuine measure-refinement.
- 405 → 1215: the 1/12-line carries all the mass (12 × 1/12 = 1); the triple is a
  TRACE-ZERO innovation (e₁ = 0, banked). Predictable-part + zero-sum-fluctuation.

## The law and its home

**Mass conserved; each refinement splits a parent into a cyclotomic orbit summing to it;
every innovation is trace-zero.** This is the defining shape of a distribution on the
cyclotomic tower lim(ℤ/3ᵏ × ℤ/5) — an **Iwasawa-type / p-adic measure**, NOT a hierarchy.

## Why this is the right reframe (from the negatives)

The scale wall killed HIERARCHY (all channels contract, B408 correction). But the SAME data
that has no hierarchy carries an exact MEASURE. The object's natural output is not a scale
but an integration structure — and integration is precisely where dimensions come from
(the honest content of the S043 energy meditation, now with an exact carrier). ACTIONABLE:
the tower-measure is a computable object with a named theory (Iwasawa / Mazur measures /
p-adic distributions); its Mellin transform / p-adic L-function is the natural next probe,
and it connects to the banked L-value dossier (entropy = 4·Reg = 2√5·L(1,χ₅)). This is a
Phase-2/Phase-5 lever the plan did not have: not "does the tower run" (no) but "what is the
p-adic L-function of the tower-measure."

**Provenance.** the exact refinement (inline, banked tower values B372/B392/B394 + the 1215
triple e₁=0); a directional finding, no lock beyond the arithmetic (all in banked JSONs).
