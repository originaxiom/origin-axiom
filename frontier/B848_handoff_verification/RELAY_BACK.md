# Relay back to the review seat — B848

For the owner to forward. Independent recomputation of the six-document bundle. **The headline
holds; five items need your attention, one of them mathematical.**

---

## 1. MATHEMATICAL — your metallic mechanism proves the wrong half

> *"for any symmetric A ∈ SL(2), `S A S⁻¹ = adj(A) = A⁻¹` … So symmetry is forced; **no choice of
> m yields an arrow**."*

The identity is **true** — verified symbolically for a general symmetric A. But **det S = +1**, so
it supplies the **det = +1** conjugator, which is **amphichirality**. *No arrow* requires the
**det = −1** conjugator, and symmetry does not supply it.

**Your own table contains the counterexample: `LLRLRR = [[2,5],[5,13]]` is symmetric, and it has an
arrow.** So "symmetric ⟹ no arrow" is refuted by a row you already computed.

**Your conclusion is correct.** Verified m = 1…12: no arrow throughout. The mechanism that carries
it is arithmetic, and it is one line for all m at once:

> **disc = m² + 4**, and the metallic mean `(m + √(m²+4))/2` has norm `(m² − (m²+4))/4 = −1`.
> The order therefore contains a unit of norm −1 ⟹ the det form represents −1 ⟹ the det = −1
> conjugator exists ⟹ no arrow.

**This also explains your error #4** (*"D = 8 has even period yet represents both"*). The relevant
order is the one generated by the **metallic mean**, of discriminant m²+4 — not `ℤ[√D]`. At m = 2:
`ℤ[1+√2]` has fundamental unit `1+√2`, norm **−1**; `ℤ[2√2]` has `3+2√2`, norm **+1**. Right field,
wrong order. The Pell instinct was sound; the order was not.

## 2. Your census reproduces exactly — 6 of 6

Two methods sharing no code path (saturated conjugator lattice → det form → reduction cycle; and
an exact integer box search over all four entries of P, which does not use the `tr(P) = 0`
derivation). Every trace, every discriminant, every verdict. **The arrow at trace 15 is real.**
Given that this took five attempts, the reproduction is worth stating plainly.

## 3. Base rates — you asked for them (§5 item 3); here they are

| length | classes | arrow | share |
|---|---|---|---|
| 2–5 | 13 | **0** | 0 % |
| 6 | 12 | 2 | 16.7 % |
| 8 | 34 | 12 | 35.3 % |
| **10** | **106** | **60** | **56.6 %** |

**The arrow is generic, not rare.** This *strengthens* your headline. "You can have the derivation
or the arrow" reads as a trade between two exotic conditions; it is not. **The arrow is the
majority behaviour and arithmeticity is the measure-zero condition** — two bundles in the whole
family. The object does not sit at a fork, it sits at a rare point whose price is time-symmetry.

Also computed, tested range only, not proved: **amphichirality never occurs at odd word length**
(0 of 2, 6, 18, 58 classes at lengths 3, 5, 7, 9).

## 4. The eigenvalue count is 17, and four of your six documents say 43

Measured in main: `frontier/B797_maass_spectrum_harvest/eigenvalues_final.json` → **17**.

Your final handoff records this correctly as its own **error #2** — and then the reframe (§4.1,
§4.2), the master handoff (§1.1) and the workplan (§1.2, *"43 eigenvalues (72 with multiplicity)"*)
all still carry 43. The same final handoff uses **17** in its Tier-2 spectral-rigidity paragraph.

**Same shape as the Riley polynomial**, which you corrected in the master handoff while the E₆
probe still states the withdrawn form `u² + (5−x²)u + 1`. (Adjudicated from the raw relation both
documents quote: the u-coefficient and the constant coefficient are **equal**, `3 − m² − m⁻²`, so
your correction `u² + (5−x²)(u+1)` is right. Not load-bearing — both are degree 2, so Gate C's
`deg ≥ 2` residue mechanism stands.)

**The pattern is the finding**: a correction recorded in one document and not propagated to its
companions leaves the withdrawn claim carrying the authority of a document that lists its own
errors. Registered in `PRACTICES`.

## 5. Two smaller items

- **`C = [[1,0],[−1,−1]]`** conjugates `LR` to its inverse only under the **swapped** L/R
  convention. Under `L = [[1,0],[1,1]], R = [[1,1],[0,1]]` it does not. Both traces are 3 and the
  matrices are conjugate, so no verdict moves — but a seat matching this against "the repo's banked
  amphichiral matrix" under the other convention gets a false mismatch. **State the convention.**
- **"Ship P5"** (workplan §1.2) describes **spectral** content, but `papers/P5_monoid/` is the
  **withdrawn** monoid paper. The workplan predates that verdict. **The spectral paper needs its
  own number.**

## 6. Confirmed without change

Σ 1/F′ = 0 identically; power sums P₀ = 4, P₁ = −4c−17, P₂ = 8c³+66c²+180c+187, and P₋₂, P₋₃ with
poles at c = ±2 and 4c+17 — which is exactly the zero of P₁, as you say; trefoil Riley degree 1;
Res Λ_K = 1/6 to 1e−35; Res φ = 1.7065521766281616088; vol = 12√3·Λ_K(2); E₆'s 78 = ΣSym^{2m} at
the exponents, det Cartan 3, h = 12; entropy 2 log φ.

**Your correction #9 is confirmed and sharpened.** Given `vol = 12√3 Λ_K(2)`, the identity
`2√3/vol ≡ 1/(6 Λ_K(2)) ≡ Res φ` holds **algebraically**, to 1e−35. The 3.6×10⁻²⁰ was never
evidence of anything — it is one equation written twice, and the withdrawal was right.

## 7. Not adjudicated here

The observer/SSB reframe (verbs, and your own §6 puts coverage at **12 %** of observer arcs);
φ_m004 = φ_orbifold (banked B737-P2, not re-derived); the SL(3) Euler–Jacobi run (needs homotopy
continuation); `pr = (p+1)(q+1)` on V₀; the foliation-algebra type (specialist — the corpus has
one file mentioning "hyperfinite"). Each needs its own arc, and the reframe in particular should
not be banked on a 12 % sweep whose one random sample overturned a three-round-old claim.
