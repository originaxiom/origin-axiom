# B631 addendum — the MB13 retro-sweep (run 2026-07-15, AFTER the sealed run; the lapse recorded)

**The lapse:** REPRODUCIBILITY.md's MB13 guard requires, BEFORE any prereg,
a keyword grep of `frontier/` + `docs/OPEN_LEADS.md` plus the atlas oracle.
The B630 design was sealed without it — the handoff's "genuinely held-out,
never in any comparison" claim was accepted with only a value-level check
(the circulant disclosure). This is the exact B577 failure mode (a banked
answer under another name defeats a prereg). Run retroactively; results:

## What the sweep found (contextual prior art — should have been in B630's MB12)

- **B322 (the value hunt):** the PMNS ANGLES were already null-tested —
  79 filling invariants + all pairwise ratios (6241 numbers) matched 12 SM
  parameters (incl. the PMNS angles) at exactly the chance level
  (p ≈ 0.5). Angle-level prior art; consistent with B631's outcome.
- **B342 (the trimaximal arc):** the object's ℤ/3 IS the trimaximal
  (magic) symmetry — the ℤ/3-DFT's democratic |F_jk|² = 1/3 circulant,
  TBM's magic column; the selected TM2 correlation is DISFAVORED by data
  vs TM1 (2.3° vs 0.9°). Structural prior art directly relevant to B631:
  a CIRCULANT moduli table (the DFT's, uniform) had already met the
  mixing data at the symmetry level and lost; B631's non-uniform
  circulant losing at the moduli level (p = 0.700) is the same lesson one
  layer up. Related: B343 (exact TBM irreducibility), B468.
- **No collision on the matrix-level comparison itself:** no prior arc
  compared any object 3×3 moduli table to |U_PMNS|² — the held-out claim
  SURVIVES. No prior arctan(1/φ) frame angle, no prior Latin-square/
  circulant closed form, no prior object-scale running (greps empty).

## Verdict

No computation was repeated (unlike B577's B575/B352 collision); the
design and run stand. The cost of the lapse: B342/B322 context was
missing from the sealed MB12, and the "first matrix-level comparison"
framing lacked its documented basis until now. Process repair: the MB13
sweep is part of the sealing checklist for every future design arc — the
sweep RESULT (hits and clean-greps) goes into the sealed document itself.
