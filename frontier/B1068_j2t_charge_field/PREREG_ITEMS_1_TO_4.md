# PREREGISTRATION — items 1–4 of the finishing loop

**Sealed 2026-08-17, committed BEFORE any of these cells run.** Rule 1.

---

## ITEM 1 — outer τ-composite conjugations

**Question.** The 63 inner real forms all return `so(10)` (126/126). Does any **outer**
form make `su(5)` real?

**Construction, and the trap.** σ, the `E₆` diagram automorphism with node map `(0 5)(2 4)`,
exchanges the **27** and **27-bar** as representations. But in the `E₈` realisation,
permuting the first six root coordinates **preserves** the block — σ is an automorphism of
`e₆`, not of `e₈`, and does not extend compatibly with the ℤ/3-grading.

**GATE, binding:** σ must carry the 27's weights onto the 27-bar's. If it does not, the
construction is wrong and **nothing below it is read**.

**Declared outcomes.** `reductive 24` → `su(5)` is real in that outer form: **physics**, and
it must be relayed to cc at once. `reductive 45` → `so(10)` again, as inner. Anything else
reported as found. Chirality checked separately: `B959` showed `Fix(τ)` makes the 27
self-dual, and if the outer forms inherit that, rank 4 is bought with chirality — which is
the trade-off already met three times and must be named as a fourth, not dressed as new.

---

## ITEM 2 — the orbifold projection by the object's own ℤ/3

**Why this is not covered by the banked no-go.** `B971` records "no orbifold projection, no
quotient, no boundary-condition truncation exists here" as theorem-grade — but its inputs are
entirely **π₁ and holonomy**: cyclic abelianization, abelian holonomy, toral finite images.
An orbifold projection **need not come from π₁**. The object carries a canonical ℤ/3 that is
not holonomy: `2T/Q₈ ≅ ℤ/3` grades the 27 into `3 + 3 + 3`, and it is the same ℤ/3 as the
centre of `E₆` (`ω₁ ∉ root lattice`, computed this session).

**Projection is not a centraliser, not a stabiliser, and not holonomy** — the three classes
every existing no-go covers.

**CONTROL, binding:** projection onto the **trivial** character must reproduce a subalgebra
already known from this arc before any ω sector is read.

**Declared outcomes.** Rank 4 with the 27 still complex → outside every banked no-go; relay
immediately. Chirality killed → the fourth instance of the same trade-off, reported as such.
Rank unchanged at 6 → projection does not reduce rank here, and the class is named.

---

## ITEM 3 — non-commutant quotients

Enumerate the quotients the object actually **determines** — by the ℤ/3, by `Q₈`, by the
Galois group `S₃` of `K` — and compute what each leaves. Rule 8: quotients the object
*determines*, not only ones it *has*.

## ITEM 4 — Jordan ranks 2 and 3

Every element used so far was rank 1. **GATE:** rank must be verified by stabiliser dimension
(rank 1 ⟺ 61) before an element is used, exactly as rule 4 requires.

---

## BINDING ON ALL FOUR

- **Rule 7:** no absence is read until the detector has been shown able to see the presence.
  The `su(5)`-as-matrices validation (`dim 12`, Killing rank 11) stands for rank-11 detection;
  any *new* target needs its own validation first.
- **Rule 3:** types from Killing rank and Casimir spectra, never from a dimension.
- **Rule 9:** every result sentence names its class.
- ≥3 primes for any numeric claim; exact computation for any claim about ℚ or ℝ.

**Both directions are live.** This session produced seven negatives that dissolved and one
near-miss positive (`ℚ(√77)` off a single prime on a biased sample). Neither failure mode is
privileged.
