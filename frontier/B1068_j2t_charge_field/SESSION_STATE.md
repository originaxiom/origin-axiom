# B1068 — state after the exhaustion loop

**Date:** 2026-08-17 · **Seat:** cc3 · **Gate 5:** algebra only; nothing here enters the
paper body.

## WHAT THE OBJECT SUPPLIES — established, none revised

| result | evidence |
|---|---|
| `J^{2T} ≅ K = ℚ[x]/(x³−12x−5)`, `disc 6237` | three independent verifications; then **again** from the 27-bar, sharing no code path |
| **`so(10)` with chirality, REAL in every inner real form** | **126 of 126** (63 sign-gradings × 2 pure spinors); `D₅`, `n` odd, `−1 ∉ W`, so **16** ≠ **16-bar** |
| the chain composes to `su(5)` over `ℂ` | one annihilator over `(e_i, ebar_j, s)`: dim 34, Killing rank 24 |
| pure spinors ⟺ `μ` splits completely | forward 30/30, reverse 0 misses 0 extras, 56 primes; field inside `K(√77)`, **totally real** |
| the 27 splits under `2T` as **3 + 3 + 3** | Molien; the ω-sector needs `√−3` and is where the spinor lives |

## WHAT NO EXHAUSTED CLASS REACHES

**Detector validated first** — su(5) built directly as 5×5 matrices, `Y = diag(2,2,2,−3,−3)`,
centraliser `dim 12, Killing rank 11`. The instrument can see the Standard Model algebra.
It took four attempts to establish this: random points miss the wall (measure zero); an
integer Cartan scan timed out; arbitrary basis elements are not root vectors.

With that validated:

- `su(3)⊕su(2)⊕u(1)` — **not reached** by any of: all `2²⁰` subsets of the canonical
  vectors (880 distinct annihilators); all subsets of the charge algebra `C` by
  meet-closure; 10556 charge directions.
- `su(5)` — **not real** in any of the 63 inner forms; the conjugation always returns
  `so(10)`.

## THE NAMED EXITS — per THE RULE, a negative is a boundary marker

1. **Outer `τ`-composite conjugations.** The inner 63 are exhausted; `B907` swept all 128
   inner **and outer** representatives and found this object's first measurement wall real
   in `e₆(2)` **and only there** — not compact. **Not attempted here**, and deliberately:
   the `E₆` diagram automorphism exchanges the 27 and 27-bar as representations, but in the
   `E₈` realisation permuting the first six root coordinates *preserves* the block. σ does
   not extend to `E₈` compatibly with the ℤ/3-grading without a further construction.
   Rushing it would produce a result that looks conclusive and is not.
2. **The exact factorisation** owed by the Axis 0 preregistration — the pure-spinor quadric
   over `ℚ(√−3, θ)`, agreeing with the splitting statistics. 56 primes is evidence, not
   proof.
3. **Jordan ranks 2 and 3**, and the two operation classes never touched anywhere in the
   corpus: **orbifold projection** and **non-commutant quotient**.

## THE METHODOLOGICAL RECORD, kept because it is the honest part

Seven negatives, seven corrections, in one session:

wrong character sector · wrong pairing (two 27s, not `27 ⊕ 27-bar`) · uncontrolled
genericity claim (44 is generic) · object list missing the 27-bar's sectors · object list
missing **derived** objects (su(5) itself was absent from that sweep) · charge directions
**sampled**, 81 of ~`p⁴` · and a detector reporting absence before it was shown capable of
detecting presence.

Against one near-miss in the opposite direction: `ℚ(√77)` named from a **single** split
prime against a density that contradicted its own model, on a sample biased by a silent
`except: pass`.

**Every positive above survived unchanged throughout.** See `THE_RULE.md`.
