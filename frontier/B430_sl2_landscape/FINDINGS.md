# B430 — the sl2 landscape of the 27: fermions EXIST in the landscape, and the object does not point into it

**Status: banked. Completes the delivered Chat-1 ask ("a different SL(2) ⊂ E₆ might — identify
and compute"). Firewalled; prices the fermion door exactly.**

## The computation

Criterion (exact): for an sl2 ⊂ 𝔢₆ with semisimple element h, the 27 carries half-integer spin
iff some weight has ODD h-eigenvalue.

- **Root sl2 (A₁ class):** 27 h-eigenvalues {−1:6, 0:15, +1:6} ⟹ **27 = 6 spin-½ doublets ⊕ 15
  singlets** — the standard A₁×A₅ branching 27 = (2, 6̄) ⊕ (1, 15). **Fermionic content exists.**
- **Principal sl2 (the object's unique forced embedding):** all h-eigenvalues even — bosonic
  (B428).

## The three-part wall (the fermion door, priced)

1. Fermionic spin assignments **exist** in the sl2 landscape of 𝔢₆ (root class: 6 doublets).
2. The object **forces only the principal** embedding (B428) — every other sl2 is a choice.
3. **No first-order deformation** of the object's E₆ rep reaches any other sl2-factoring
   (B429, Whitehead rigidity).

**Verdict: the fermion door via sl2-choice is open as mathematics and closed as derivation —
it fails bar leg (i) FORCED.** The landscape is there; the object does not point into it. Any
construction that picks the root (or any non-principal) sl2 must justify the pick from an
interface mechanism (filling, gluing) or it is decoration.

Prior art: the branchings are standard (flagged per novelty discipline). The in-repo content is
the pricing: existence + unforcedness + rigidity, in one place, locked.

**Provenance.** sl2_landscape.py → sl2_landscape.json; lock tests/test_b430_sl2_landscape.py.
Cross-refs: B428 (bosonic point), B429 (rigidity), Chat-1 handoff 2026-07-05 (the ask).
