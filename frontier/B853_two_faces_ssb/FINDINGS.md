# B853 — the incoming relay verified, and the SSB ingredients are not missing: they are on two different faces

cc banking seat, 2026-08-02. Mathematics scope; nothing to `CLAIMS.md`; Gate 5 untouched.
**Not preregistered** — this is verification of incoming material plus a synthesis; the numbers are
checked against independent computation, not against a seal.

## 0. My own error first, because it is the same one this arc exists to catch

**I cited B711 by grep and not by reading it, and the citation was wrong.** I claimed B711 locks
that Gal(ℚ(√5)/ℚ) and Cl(O₄) are orthogonal ℤ/2's. **It does not.** B711's two ℤ/2's are
**amphichirality** and the **ℚ(√−3) Galois involution**. I matched the phrase *"two ℤ/2's … not one
symmetry"* and assumed it covered a different pair.

That is exactly the cite-don't-compute failure this session has flagged in three incoming relays,
committed here by the seat doing the flagging. **Reading the arc produced a stronger and correctly
sourced claim** (§3) — which is the argument for reading over grepping, stated at my own expense.

## 1. The relay's three facts, verified

| | claim | verdict |
|---|---|---|
| **Fact 1** | both reduced forms of disc −48 have b = 0, so both CM points are purely imaginary and c fixes them | **CONFIRMED exactly** — forms (1,0,12) and (3,0,4), τ = 2i√3 and 2i√3/3 |
| **Fact 2** | Frob₂ generates the class group | **CORRECTED** — see below |
| **Fact 3** | h(−48) = 2 vs h(−3) = 1, so the broken symmetry is object-specific | **CONFIRMED**, and already banked in B737 |

**Fact 2's correction is structural, not typographic.** `−48 = 4²·(−3)`, so the **conductor is 4**,
and the Artin map `Cl(O_f) ≅ Gal(H_O/K)` is defined only on ideals **coprime to f**. Since
`gcd(2,4) = 2`, **Frob₂ is not an element of the group it was claimed to generate.** That 2 is inert
in ℚ(√−3) is true and does not repair it. The non-principal class (3,0,4) represents 7, 19, 31, 43;
the principal one represents 13, 37, 61, 73. **The smallest legitimate generator is Frob₇.** The
relay's *conclusion* survives the substitution — a Frobenius coprime to the conductor does lie in
Gal(K^ab/K), so the level mismatch dissolves for the correct element.

**Declined: "T1 overturned."** A class **group** is a candidate for *the symmetry that breaks*; an
**order parameter** is the quantity that *measures* breaking. Different objects. B849's computation
stands.

## 2. The finding: the two candidate SSBs sit on opposite faces

Computed here:

| object | field | moves under ℚ(√5) Galois? | moves under ℚ(√−3) Galois? |
|---|---|---|---|
| order parameter **m = √5/3** | ℚ(√5) | **odd** | **fixed** |
| B6's potential **V(τ)** critical points φ, −1/φ | ℚ(√5) | **swapped** | fixed |
| B6's critical **values** −(7+5√5)/12, (5√5−7)/12 | ℚ(√5) | **conjugate pair** | fixed |
| **Cl(O₄) = ℤ/2**, generated by Frob₇ | ℚ(√−3) | untouched | **acts** |

> **Everything in the proposed order-parameter story lives on the ℚ(√5) face. The symmetry it is
> said to measure lives on the ℚ(√−3) face. An order parameter must be ODD under the symmetry that
> breaks; m is FIXED under this one.**

**B711 says so directly, and this is the correct citation:** *"the being field (**ℚ(√−3**), the
geometric point) is Galois-**swapped** while the hearing field (**ℚ(√5**), the golden point) is
Galois-**fixed and real** on the amphichiral slice."* B711 adds that the golden point sits at
`x² = 5`, `tr[a,b] = 2` — the **reducible** locus.

## 3. The synthesis, and it is better news than either reading it replaces

The ledger merged two things. **Separated, each face has what the other lacks:**

**The hearing face (ℚ(√5)) has a COMPLETE set of SSB ingredients** — all verified here:
- an **order parameter** odd under the broken symmetry: m = √5/3
- a **derived potential**: B6's `V(τ) = κ(τ³/3 − τ²/2 − τ)`, not fitted
- **two vacua that are exact Galois conjugates**: φ and −1/φ
- **critical values that are exact Galois conjugates**: −(7+5√5)/12 and (5√5−7)/12
- a **thermodynamic arrow**: V(φ) = −1.515κ < V(0) = 0, with a **barrier** at V(−1/φ) = +0.348κ

**The being face (ℚ(√−3)) has the object-specificity** — h(O₄) = 2 against m003's h = 1, with Frob₇
as generator. **The hearing face has no comparable object-specific datum; the being face has no
order parameter.**

**cc's own overreach, corrected.** This seat wrote *"no arrow ⟹ Sakharov's second condition
unavailable."* Wrong twice: Sakharov's conditions are B-violation, C/CP-violation, and **departure
from equilibrium**, and **microscopic time-reversibility does not block the third** — the Standard
Model's own dynamics is CPT-symmetric and baryogenesis proceeds through an out-of-equilibrium
transition anyway. **B848 proved the MICROSCOPIC arrow absent and says nothing about the
thermodynamic one.** The barrier above is the thermodynamic ingredient, on the hearing face.

Likewise *"no scale"* conflated **absolute** scale (κ genuinely free — B6 says so) with
**dimensionless ratios**, which were swept only with **PSLQ against an algebraic tower** and
therefore could not see transcendentals by construction.

## 4. The bridge, and why it is now the sharpest open item

**The faces are not independent.** B261: *"One AJ operator carries **both ends**"* — the Kashaev
limit gives **exponential** growth → Vol, the hyperbolic/E₆/ℚ(√−3) end; the golden root `q = ζ₅`
gives **bounded, period-5** behaviour, the spherical/E₈/ℚ(√5) end. **Two regimes of one recursion.**

> **So the question is not "which face is the SSB on" but "is the regime change between the two ends
> of B261's operator a transition, and does m measure it?"**

**A lead worth a gate, flagged as an analogy and not a result.** B852 found the parabolic pressure
plateau: `P(s) > 0` (exponential) for s < 1, `P(s) = 0` (bounded) for s ≥ 1. B261's two regimes are
**exponential** and **bounded**. That is the same qualitative signature. **It is a structural
analogy between an interval-map pressure and a q-series recursion, not an identification**, and it
needs its own gate before it carries anything.

## 5. What is verified, and what is not

**Verified here:** the three relay facts (with Fact 2 corrected); m's parity on both faces; B6's
critical points, values and their Galois conjugacy; V(φ) < V(0) with a barrier; that the top block
of the 27 under the principal SL(2) is **V₁₆** (principal weight of ω₁ = 2×8 = 16, with the adjoint's
height-11 highest root → V₂₂ as control); multiplicative independence of 2+√3 and 4+√15 by the
field-intersection argument (**norms do not separate them — both are 1**).

**NOT verified:** that B6's potential is the free energy of any KMS transition (the relay's own
document says *"connection unbuilt"*); the V₈⊕V₀ remainder of the 27 from scratch; the 4-dimensional
2T centralizer by a **second** method; that trace 3 never occurs in Γ₄₁ (searched words ≤ 7, 82
distinct traces, integer traces {2,4,8} — **evidence, not proof**); and the §4 analogy.

**A relay claim refuted:** the E₆ adjoint does **not** decompose into 12 irreps under the principal
SL(2). Kostant gives one block per exponent; E₆ has rank 6; dims [3,9,11,15,17,23] sum to exactly
78, checked across A2/G2/F4/E6/E7/E8, and `tests/test_b848_handoff.py` already locks it. The relay's
centralizer conclusion is unaffected — it follows from no exponent being 0 plus det(Cartan) = 3 —
but ℤ/3 **does not discriminate**: SU(3) has the same centre.

## Carried forward

1. **The bridge (B261's regime change) is now the sharpest item** — sharper than the centralizer
   bracket, because it is what would join an order parameter to an object-specific symmetry.
2. **Transcendental ratios are OPEN, not closed** — but the relay's own STOP rule applies:
   *"testing spectral ratios against SM numbers is DOF-0 with an enormous menu."* Preregistered and
   DOF-positive, or not at all.
3. The 27's remainder and the centralizer's second method.

`tests/test_b853_two_faces.py`
