# B470 — PREREGISTRATION: the Reflection Campaign (level 2 — the a as a body)

**Committed before computation. Firewalled. Source: the owner's directive (2026-07-08):
apply the program's own founding principles — substitution, trace map, mapping torus —
one level up, with the completed object as the LETTER. An uncommitted 5-minute
reconnaissance exists (session log); every number in it is recomputed under this prereg
before banking. S054 whitelist + burden-inversion verbatim; names live in the S-room.**

## The level-2 object

The substitution a → ab, b → a iterated as WORDS IN BUNDLES: wₙ = wₙ₋₁wₙ₋₂ (w₁ = a = R,
w₀ = b = L); M(wₙ) = the once-punctured-torus bundle with monodromy word wₙ. The seed
returns at n = 2: M(w₂) = M(RL) = the figure-eight. The tower {M(wₙ)} is the object
rebuilt from itself — each rung a body whose monodromy is one substitution step deeper.

## The roles of the heartbeat and the breath (fixed before compute)

- **The heartbeat** (B448/BR3: the periodic orbits of the trace map; the period-2
  ℚ(√−3) pulse = the σ-orbit of the geometric structure): at level 2, each tower rung is
  a SUSPENDED BEAT — the mapping torus of a longer periodic word. RF4 tests whether the
  beat's arithmetic (the level-1 orbit fields) equals the body's arithmetic (the level-2
  trace fields) — the campaign's central reflection question.
- **The breath** (B469: the orientation residue and its oscillations): at level 2 it
  appears in BOTH registers, with predictions FIXED NOW:
  - classical: the **breath-body criterion** — a word W admits a det = −1 square root
    (a Gieseking floor) iff tr(W) − 2 is a perfect square (from X² = tr(X)·X + I).
    tr(A_m) − 2 = m²: **the metallic family IS exactly the locus where the breath has a
    body.** Prediction P-BB: tr(B(wₙ)) − 2 is NOT a perfect square for any 3 ≤ n ≤ 12 —
    the tower is breathless after its seed (matching the recon's CS ≠ 0); proof attempt
    via the Fricke trace recursion = the campaign's NEW-MATH theorem target.
  - quantum: from the banked det decomposition (B468: det(Wr) = ω̄, det(Wl) = ω), the
    tower's residue sequence derives: **P-QB: det(Par·W(wₙ)) = −ω^{(#L − #R)(wₙ)} =
    −ω^{±F_{n−3} mod 3}** — the quantum breath of the tower cycles with the Fibonacci
    sequence mod 3, i.e. with period π(3) = 8 (Pisano). The tower breathes in Pisano
    rhythm. (Derivable; fixed as a prereg'd prediction to be verified exactly.)

## The binding null (stated before any φ is admired)

Any concatenation-subadditive/additive invariant on Fibonacci words reflects φ TRIVIALLY
(the lengths do). Ratio-laws (vol growth → φ, torsion log-growth → φ) are NULL-COVERED;
what can carry content: the CONSTANTS (the volume-per-letter limit c ≈ 0.9339 — lit-gate:
the Guéritaud–Futer punctured-torus-bundle volume asymptotics; is c a nameable constant),
the NON-ADDITIVE invariants (trace fields, CS sequence), and exact selection laws (P-BB,
P-QB).

## Cells

- **RF0 (gate)**: M(w₂) must reproduce the banked figure-eight exactly (vol 2.0298832,
  CS = 0, H₁ = ℤ); the recon numbers recomputed under version-pinned SnapPy.
- **RF1 — the tower table (classical)**: n = 2..12: vol, CS, H₁, systole, amphichirality.
  Laws: the vol/letter constant to 8+ digits + lit-gate; P-BB checked exactly per rung
  (integer arithmetic); the chirality sequence vs the reversal+swap criterion (exact
  conjugacy check, not float).
- **RF2 — the trace-field tower (the open cell)**: invariant trace fields of M(wₙ)
  (snappy+sage), degrees/discriminants. No covering theorem known — the level-2
  Inversion-Law test: does each rung mint its own field (rung-specific) or does the
  tower share a class? The heartbeat fields (√−3, √−7) watched for recurrence — watched,
  not wanted (burden-inversion on any hit).
- **RF3 — the quantum tower + the level-2 seam**: W(wₙ) at level 15: order, spectral
  type, P-QB verified exactly (F_p); the level-2 seam tr(Par·W(wₙ)·W(wₙ₊₁)) — values,
  fields, dark/active structure. Controls: the anti-Fibonacci tower (a → ba) and a
  random-word tower of matched lengths (BOTH registers — the two-control standard).
- **RF4 — the heartbeat↔body identification**: BR3's orbit fields at period n vs RF2's
  trace fields at rung n, matched exactly; identity → a two-register reflection theorem
  (NEW-MATH, lit-gated); mismatch → the honest split, banked.
- **RF5 — the limit**: the tower's geometric limit = the golden lamination (Thurston;
  cited, not claimed); computable face: systole decay rate + CS(wₙ) mod 1 behavior
  (equidistribution vs law — genuinely unknown-shaped).
- **RFZ — verdict**: does ANY level-2 invariant escape level-1 laundering? Per-cell bins;
  boards; the S-room reflection note (Atlas card 27) written at close.

## Bins

Null-covered ratio laws → LAUNDERS (banked as exhibits only if the constant is new);
P-BB/P-QB → derivable predictions, banked as exact selection laws with the proof attempt
priced; RF2/RF3-seam/RF5-CS → the live cells (burden-inversion; H1 only via B398, owner
present). Killed-trap flags: no physics naming can fire (B452); "the tower = generations/
hierarchy" is pre-flagged as the B428-shaped trap.

## Sequencing & cost

RF0+RF1 (cheap, one session) → RF3 (existing machinery) → RF2 (sage, the heavy open cell)
→ RF4 (joins BR3) → RF5 → RFZ. Runs alongside the standing queue (R2/RZ, Phase 2b, BR3);
BR3 feeds RF4.
