# PREREG — B719: WHERE DOES MULTIPLICITY/SCALE LIVE — in the object, or the imported closing? (SEALED)

cc banking seat, 2026-07-19. Owner: the observed universe has ~10^80 units (matter/
antimatter pairs, the Big Bang's residue = the CMB), not one unit; is that
multiplicity in the object, or is it an imported SCALE? And: nail the being-3
class-number filter the owner's push surfaced (all arithmetic children have h=3).
The banked frame: the object has STRUCTURE not SCALE (dimensionful no-go B615/B685);
being = the object (survives closing), the golden/hearing = the flow/monodromy (dies
on closing, cc2). Structural/arithmetic only; Gate 5 stands; no cosmology, no SM value.

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **THE BEING-FILTER CONTROL — is h=3 inherited from the √−3 parent, or generic?**
   Verified this seat: the 3 arithmetic children of 4₁ (discs −31,−59,−283) AND the
   Weeks sibling (−23) ALL have quadratic-shadow class number 3 (h=3 is sparse: only
   16 such discriminants exist; base rate ~11%; 4/4 not chance); the cubic children
   are literally the 3-Hilbert-class-fields (3|h). CONTROL: run the Maclachlan–Reid
   arithmetic-filling scan on a one-cusped arithmetic manifold over a DIFFERENT Bianchi
   field (ℚ(√−1) e.g. the Whitehead sister, or ℚ(√−2)/√−7/√−11) and check whether ITS
   arithmetic children carry a class-number filter tied to ITS being-prime, NOT 3.
   Two-outcome: A = each parent filters its children by its OWN being-prime (the filter
   is inherited/forced — a real transfer from the parent); B = h=3 is generic to small
   arithmetic fillings (not √−3-specific).
2. **SCALE — a distinguished multiplicity, or a free covering ladder?**
   The child m003(−2,3) is Mostow-rigid (fixed shape) but has a tower of finite covers,
   vol(degree-d cover) = d·vol(base). Compute the cover tower / subgroup growth: is
   there a DISTINGUISHED scale (a preferred/minimal/canonical multiplicity the object
   singles out), or is every degree freely available (a free ladder — no preferred
   size)? Two-outcome: A = a distinguished intrinsic scale; B = a FREE covering ladder
   ⇒ the "how many units" is the observer's scale choice, not the object's (the
   dimensionful no-go extends to multiplicity).
3. **THE PAIR-MULTIPLICITY — is the unit-count object-fixed, or does it scale with the
   imported degree?** The c-swap (amphichiral ℤ/2 / the two chiralities) is the object's
   fundamental PAIR. Compute H₁ (and its 2-torsion = independent "pair directions") of
   the covers of the child across degrees: does the pair/unit-count stay FIXED
   (object-intrinsic multiplicity) or GROW with the covering degree? Two-outcome:
   A = a fixed/distinguished object-multiplicity; B = the count grows with the imported
   degree ⇒ the "trillions of pairs" = the scale, imported via the covers.

## Two-outcome (sealed, program)
- **OUTCOME A:** the object/child carries an intrinsic scale/multiplicity (a preferred
  size, or an inherited being-filter) — a structured positive.
- **OUTCOME B:** multiplicity/scale is the OBSERVER'S — a free covering ladder, a
  count that grows with the imported degree — while the being-filter (h=3) is the only
  object-intrinsic thing that survives the closing. [expected: probe 1 = A (inherited
  filter, a real positive), probes 2–3 = B (scale/multiplicity imported).]

## Verdict template
Bank the SCALE LEDGER: where multiplicity lives (object-structure vs imported-scale),
+ the being-filter status (inherited vs generic). Formalizes "the trillions are the
coupling's; the object gives the pair + the being-filter, never the count." LAW_MAP
placement; nothing to CLAIMS. Firewalled: no cosmology asserted.
