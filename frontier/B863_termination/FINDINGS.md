# B863 — the cascade TERMINATES: the SM is the terminal registerable algebra

cc banking seat, 2026-08-03, from the masterplan campaign's completeness critic (G1 — *"the
referee's opening shot"*). Mathematics scope; nothing to `CLAIMS.md`; Gate 5 untouched.
**Not preregistered** — the critic pre-stated both outcomes and their readings before this ran;
that document serves as the outcome table.

## 1. The gap

A descent rule selects nothing until it halts. B861's fused cascade descended three times and
stopped — but **no arc had asked why it stops**. If a registerable option existed inside the SM,
the principle would over-descend and "selects the SM" would be false.

## 2. The theorem

**Every proper descent of the SM kills registerability** (generation multiset test, dial-stripped,
per B860/B861 — with the positive control that the SM itself passes it):

| descent | descended generation | chiral? |
|---|---|---|
| (a) su(2) → u(1) | {3: 2, 3̄: 2, 1: 3} under su(3) | **NO** — vector-like |
| (b) su(3) → su(2)×u(1) (regular) | all su(2)-pseudoreal | **NO** |
| (b′) **su(3)₁ → su(2)₄, the GENUINE conformal embedding** (principal, index 4; c = 12/6 = **2** = c(su(3)₁) ✓) | the 3 branches to the su(2) **triplet — REAL** | **NO** |
| (c) full abelianization | dial only | **NO** |
| — the SM itself (control) | (3,2) unpaired | **YES** |

(b′) matters: it is the non-obvious case — a *bona fide* conformal embedding at the right central
charge — and it dies the same death, because the principal branching makes the colour content real.

> **The SM is the terminal registerable algebra. The descent halts not by fiat but because
> chirality has nowhere smaller to live — the (3,2) is the last unpaired complex structure.**

## 3. The kind-check that lands it in physics

What survives electroweak breaking — **QCD + QED — is vector-like**: the same fact seen from
below. The cascade stops exactly where nature's chiral gauge structure stops. This is a
consistency check, not an input: the halt was computed from the multiset test alone.

## 4. Status of the full selection rule after this arc

The fused principle is now a **complete selection-and-halt rule**: it descends uniquely three
times (B861) and **provably terminates** (this arc). Remaining spine unchanged: P5 (menu
completeness — the step-4 menu here is the structural + known-conformal list, same import class),
the ranking among registerable peers, and the registerability definition's stage-level
formalization (the critic's G5 keystone).

## 5. What this arc does NOT establish

- **Not exhaustive over exotic conformal embeddings** at the SM levels beyond the enumerated
  structural descents + su(2)₄; the menu import (P5) covers this arc exactly as it covers B861.
- Nothing about values, generations, the Higgs rep, the real form, or spacetime.

## Carried forward

1. **G3 next** — the anomaly script: the dial U(1)_ψ is traceless and cubic-anomaly-free over the
   full 27 (verified: 16−20+4 = 0, 16−80+64 = 0) but anomalous over the truncated ket (Tr = 16).
   Whether anomaly consistency *forces* the re-anchoring rule is the sharpest open lead.
2. G2 (the padding lemma), G6 (the three involutions), G4 (the false-positive control), G7 (the
   lift obstruction), G5 (the refined stage — the keystone).

`tests/test_b863_termination.py`
