# PREREG — B753: the mixing-structure adjudication (SEALED)

cc banking seat, 2026-07-22. chat-1 (via owner) disputes B751's Born-weight leg with a
structural claim: "the registered 1/(2φ) is the real part of the weld's θ-odd EIGENVALUES
e^{±i·108°} — basis-independent — not a matrix element." cc3's inspection-level receipt:
disanalogous both ways (mixing is eigenvector-anchored; eigenvalues don't determine it).
This arc replaces inspection with computation. Gate 5 + the stopping rule absolute: NO SM
comparison anywhere (program-internal mathematics only); the registered JUNO number is
untouched either way (the pin, cell 5). Pipeline: the banked B238/B593 code, standard
Hermitian inner product, the canonical θ-odd basis {u₃, u₆} (weight-label-forced).

## Cells (two-outcome, sealed)
1. **The explicit block.** Compute B_ij = uᵢ†W u_j (the 2×2 θ-odd block of the twisted
   weld at g = RL) from the banked pipeline. Verify B₀₀ = 1/(2φ) + i·sin(2π/5)/√5 (the
   banked B593 element) and B₁₁ = its conjugate. Print the off-diagonals.
2. **chat-1's eigenvalue claim, tested.** Eigenvalues of B: are they e^{±i·108°} (unit
   modulus, real part −1/(2φ) = cos 108°)? Declared candidate outcomes: (a) CONFIRMED;
   (b) REFUTED-with-content (e.g. the block is diagonal/near-diagonal in {u₃,u₆} with
   eigenvalues ≈ 0.309 ± 0.425i, modulus 0.5257 ≠ 1 — in which case the "eigenvalue
   reading" and the "matrix-element reading" COINCIDE and chat-1's distinction is empty);
   (c) other (report exactly).
3. **Mixing is eigenvector-anchored — the demonstration.** Two Hermitian 2×2 matrices
   with IDENTICAL eigenvalues and DIFFERENT eigenvector overlaps against a fixed basis
   (one line each): the proof-by-example that mixing angles are what eigenvalues don't
   determine. Kills the "basis-independent therefore better" argument as a category error.
4. **The well-posed overlap (the kind-correct object).** If B is diagonalizable: compute
   the overlap matrix P_ij = |⟨uᵢ|w_j⟩|² (w_j = B's normalized eigenvectors, standard
   inner product) — the program's first structurally kind-correct mixing candidate
   (flavor-analogue = the forced weight basis; mass-analogue = the weld eigenbasis).
   Record the exact values as PROGRAM-INTERNAL numbers. Check unistochasticity (rows/cols
   sum to 1) — fails if B is non-normal (report the deviation; a non-normal block means
   even the kind-correct object needs a declared convention, itself a finding).
5. **The one-number pin (co-signed).** cc3's governance pin adopted: one observable, one
   registered number — 0.30902 stays as registered for JUNO; no alternative registers
   post-hoc; any revision seals BEFORE JUNO reports or never. Ledger line in HINT_LEDGER.

## Verdict template
The dispute resolves into: chat-1's paraphrase of B751 (already corrected by cc3 — B751
proposed NO replacement prediction); chat-1's eigenvalue claim (cell 2 decides); the
analogy question (cells 3–4 decide structurally). Whatever cell 4 produces is recorded and
NOT compared to any SM value. Nothing to CLAIMS.
