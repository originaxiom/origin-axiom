# FINDINGS — B753: the mixing structure computed — the dispute dissolves into one exact identity

cc banking seat, 2026-07-22. chat-1's structural dispute of B751's Born-weight leg,
adjudicated by computing the actual object (prereg 406bd993 sealed first; the banked
B238/B593 pipeline; standard inner product; the weight-forced θ-odd basis {u₃,u₆}).
Program-internal mathematics only — NO SM comparison anywhere (the stopping rule + the
pin). Nothing to CLAIMS.

## The computed facts (cells 1–4, all exact)

1. **The block.** The 2×2 θ-odd block of the twisted weld at g = RL reproduces B593's
   element exactly (B₀₀ = 1/(2φ) + i·sin72°/√5); the off-diagonals are LARGE (|·| = 0.851)
   — the block is far from diagonal in the forced basis.
2. **chat-1's eigenvalue claim: numerically FALSE, structurally EMPTY.** The block is
   exactly unitary (‖[B,B†]‖ ~ 1e−15) with eigenvalues **e^{±i·72°}** — not the claimed
   e^{±i·108°} (trace = +1/φ, not −1/φ; cos 72° = +1/(2φ)). And the distinction the
   dispute rested on ("eigenvalue real part ≠ matrix element") is PROVABLY EMPTY at the
   level used: for this unitary block, Re(λ±) = Re(B₀₀) = 1/(2φ) — identically, since
   B₀₀ = e^{iθ}p + e^{−iθ}(1−p) gives Re B₀₀ = cos θ regardless of p. Both readings of
   0.30902 are the SAME real-part projection.
3. **Mixing is eigenvector-anchored (demonstrated).** Two Hermitians, identical spectra,
   different mixing (1.000 vs cos²(π/7)): eigenvalues do not determine mixing. cc3's
   inspection, now a computation.
4. **The kind-correct object exists and is golden-exact.** The overlap matrix between the
   forced weight basis and the weld eigenbasis (the exact |U_e2|²-shaped object chat-1's
   own test demanded) is unistochastic:
   **P = [[φ/√5, 1/(φ√5)], [1/(φ√5), φ/√5]] = [[0.723607, 0.276393], …]** — with the
   EXACT identity **|B₀₀|² = 1 − p = 1/(φ√5)**: the Born weight from the B751 addendum IS
   the kind-correct mixing entry (symbolically verified). The structurally correct
   "mixing probability" of the program's weld is 1/(φ√5) = (5−√5)/10 = 0.276393; the
   registered 0.30902 = cos 72° is a real-part projection — a different KIND of quantity.

## What this settles

- chat-1 was RIGHT that the kind-correct analogue is an eigenvector-overlap-squared, and
  right to demand the test — and the test, run, lands exactly on B751's Born weight,
  derived kind-correctly this time (a unistochastic entry, not merely |element|²).
- chat-1's specific eigenvalue claim (108°, tr = −1/φ, "different quantity") fails on all
  three counts. B751's addendum stands, upgraded: 1/(φ√5) is not just "what |·|² gives" —
  it is the program's first well-posed mixing number.
- **The pin (cell 5, co-signed cc + cc3): one observable, one registered number.** The
  JUNO registration stays 0.30902 exactly as banked (a hint, no mechanism — H-TUROK's
  wording unchanged); the kind-correct 1/(φ√5) is recorded as PROGRAM-INTERNAL structure
  and does NOT register as an alternative; no post-hoc revision — any re-registration
  would have had to seal before JUNO reports, and does not. Nature measures; the ledger
  stays honest either way.

Artifacts: compute.py + output.txt (+ the symbolic identity check in the lock). Locks
tests/test_b753_mixing.py. Gate 5-Q noted: this arc is spectral mathematics, not
phenomenology vocabulary; Gate 5 proper applied throughout.
