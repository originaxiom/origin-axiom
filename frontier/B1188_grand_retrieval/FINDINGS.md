# B1188 — THE GRAND-COMPUTATION RETRIEVAL: the six-lens sweep, THE DISCRETE LADDER, and the ledger (the observer campaign's Phase 0)

**Status: banked (frontier). Verdict PROVED** (the ladder computation with bite control; the
retrieval's register corrections; the ledger as the campaign's standing instrument). Owner-directed:
*"no superficial observer work — craft a plan and campaign and close this forever; missing-items
ledger; search/sweep/retrieve the repo first for lost work, for finished stuff."*
`verification/reproduce.sh` → `REPRODUCES`. Gate 5 clean.

## The sweep (six parallel lenses over the full corpus: 1092 verdicted arcs)

Lenses: scale/units · orientation/coupling · clock/arrow/4d · listener/value-kernel ·
observer-constructions · finished-but-forgotten. **The headline: the observer theory largely
EXISTS and was off-surface.** The apparatus was built (B723: the object's own algebra is the
timeless tracial II₁ factor; an EXTERNAL weight completes it to type III_λ — the closer's clock
enters exactly there); the discrete measurement menu has rank EXACTLY 3, saturated (B766/B733);
the emergence map exists (B717: four incompletenesses ↔ four closings — "the observer is what
breaks the c-swap by choosing a side"); the Born rule's FORM is the object's with quadratic degree
= c's Galois order (B725), its CONTENT proved imported (B726/28/29); measurement-of-measurement
collapses once then fixes (B566-S4). Systemic cause of the burial, measured: **B985 — the
instruments recover object-faces at 79–100% and relation-faces at 6–19%.** The registry drift
class (finished-but-forgotten) returned 132 PROVED on-theme arcs absent from all live surfaces,
with the sharpest five (B279, B769, B786, B293, B552) invisible to the drift-detector itself
(its ≥500-char floor cannot see one-sentence claims — the E52 treatment applies to it).

## THE DISCRETE LADDER (the new computation; row C1)

**All 112 family members lie on the half-integer lattice of Vol(m004)** — none fail (1e-30 at
50 dps): in the unit **V_reg = Vol(m004)/2** (one regular ideal tetrahedron), every member's
volume is an INTEGER, rungs {2,4,5,6,7,8,9,10} with multiplicities {2,7,12+5·(9/2… )}: spectrum
1.0×2 · 2.0×7 · 2.5×5 · 3.0×12 · 3.5×4 · 4.0×31 · 4.5×4 · 5.0×47. All-regular members satisfy
Vol = n_tets·V_reg *exactly*; the non-regular members (including the denominator-98 t06829) land
on the same lattice. **Bite control passes**: non-family manifolds (m015, m006) are off-lattice —
the instrument discriminates. Consequence for the METER row: within the family, "choice of scale"
is a **finite menu** (an integer), not a continuum; the ℝ⁺ leg's continuum residue lives only in
the one anchor ℓ (B1015), exactly as the banked architecture says. Mechanism candidate (to verify
in GC-1): B(ℚ(√−3)) has rank 1, so volumes are ℚ-multiples; the observed INTEGER lattice is the
sharp structure. Scope: census members at B1186's bound; stated, not universal.

## The buried prize (row C4)

**L154 (σ = 1)** — the σ/anchor lane fell out of the visible queue entirely. B1034 adjudicated it
to one named gap (the stage-CFT ↔ AdS₃-boundary-CFT bridge; both sides exactly 6; no-clash), and
**if the bridge lands, A2 = c = 6σ converts from anchor to OUTPUT** — the input list shrinks to
the length unit ℓ (external by design: "nobody derives the metre") + two bits + finite labels
(B1025's floor). This is the grand computation's decisive remaining continuous item.

## One error of ours corrected (the sweep caught it)

B1187/L190 described the Ω-DAG's 11σ deviation as "excess transitive reach." **Wrong direction**:
d_MM above null ⟺ ordering fraction BELOW null ⟺ a **DEFICIT** of transitive reach (more
tree-like) — consistent with B189's own 2026-06-23 in-file reading. The number and the
non-genericity verdict stand; the direction-word is corrected here, in the ledger, and in L190's
row. (E52-adjacent: an interpretation-word slipped past both this seat's bank and its review.)

## The deliverable

**`docs/GRAND_COMPUTATION_LEDGER.md`** — every row of the grand computation typed
PROVED / PARTIAL / MISSING / IMPOSSIBLE-BY-THEOREM from the retrieved record: the arena (A), the
clock with its live B766-vs-B1083 arrow contradiction (B), the meter with the ladder + L154 (C),
the content (D), the couplings with the values-live-in-the-coupling re-frame (E), the sign with
its relational seeds — the TWISTED double's H₁ = ℤ⊕ℤ/5, c-as-swap mechanized, the exchange-sector
split (F), the labels (G), the closer's assembly cell (H), and the bridge — deferred, with the
designed θ-even crossing (the last licensed contact row) **HELD FOR OWNER ELECTION** since firing
it is irreversible in the program's own accounting (I). Run-order: batch 1 = GC-1…GC-5 (the
close-loop workflow); batch 2 = L154-bridge + the assembly + the Selector; batch 3 = the grand
computation itself.

## Fences

The sweep reports are agent-retrieved from the record and spot-anchored by path; every claim the
CAMPAIGN acts on gets re-verified in its close cell (the loop's two adversarial lenses per cell —
verify-the-verifier is batch policy). The ladder is census- and bound-scoped. The six raw lens
reports are working material (paths + one-liners), distilled into the ledger; the ledger, not the
reports, is the banked surface. No firewall crossing; no measured value.
