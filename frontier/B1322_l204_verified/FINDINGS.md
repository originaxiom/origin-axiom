# B1322 — L204 VERIFIED ON MAIN: along the V₁₀ direction at the subregular point the cusped object's E₆ holonomy deforms to genuine non-self-dual representations and N(27) = 0 at every one of them — the SM-derivation seat's sm:B1350 re-read at its banked 100-digit points with main's own Fox calculus: relator residuals 10⁻⁶¹ … 10⁻⁶⁸, self-duality defects 10⁻¹⁰, h¹(M; 27) = h¹(M; 27̄) = 0, no cusp-fixed vector, N = 0 on all four directions; the θ-odd frame is closed on every sl₂ germ

**Date:** 2026-09-09 · **Seat:** cc (main) · **DESIGN sealed** `29ec11c3…` before the run · **Seat pinned:** the SM-derivation branch @ `7484ad01` (sm:B1350
PROVED), `d722714f` (its harvest and the relay's fourth note) · **Status:** **VERIFIED** (the pre-registered PASS at prior 0.85; the seat's own re-reader also
re-run here, RC=0, its summary reproduced) · **Price:** none new (this closes a door; it opens no identification) · **Credit:** the SM-derivation seat
(sm:B1350 @ 7484ad01 — the computation is theirs; sm:B1268's instrument with its two precision caps found and lifted; sm:B1280's pairing law that left
this one direction), B1299 (main's registration and pricing of the hole), B1297 (main's Fox engine the controls ran on).

## 1. What the seat did (its words) and what main re-read

The seat: *"the V₁₀ of the 42 at the subregular point … carries genuine, non-self-dual E₆ representations along every direction of its class space, and
N(27) = 0 on all of them"* — Newton at 600 bits to residuals 10⁻⁶¹ … 10⁻⁶⁸ once B1268's damping was lifted from 10⁻²⁴ to 10⁻⁵⁰, the cohomology re-read at 2000
bits with 200-digit ranks because B1268's 60-digit report misread pivots at 10⁻³⁹, the torus Euler characteristic and Poincaré duality as checks. Main loaded
the four banked points (two 27 × 27 matrices each, 100 decimal digits) and ran its own code — the one-relator presentation ⟨a, b | a w b⁻¹ w⁻¹⟩, left Fox
derivatives, ranks by full-pivot elimination at 110 digits with the pivot spectrum, the dual as the inverse transpose, the cusp fixed space of ⟨ρ(a), ρ(λ)⟩:

| direction | relator residual | self-duality defect | h¹(27) | h¹(27̄) | h⁰(∂M; 27), h⁰(∂M; 27̄) | N(27) | bound |
|---|---|---|---|---|---|---|---|
| class 1 | 6.0·10⁻⁶⁸ | 1.8·10⁻¹⁰ | **0** | **0** | 0, 0 | **0** | 0 ≤ N ≤ 0 |
| class 2 | 1.8·10⁻⁶¹ | 9.0·10⁻¹¹ | **0** | **0** | 0, 0 | **0** | 0 ≤ N ≤ 0 |
| class 1 + class 2 | 2.7·10⁻⁶⁶ | 8.4·10⁻¹¹ | **0** | **0** | 0, 0 | **0** | 0 ≤ N ≤ 0 |
| class 1 − class 2 | 7.5·10⁻⁶¹ | 1.5·10⁻¹⁰ | **0** | **0** | 0, 0 | **0** | 0 ≤ N ≤ 0 |

Every entry is the seat's. The d¹ pivot spectra show the genuine small pivots the seat's 60-digit report had rejected (10⁻³³ … 10⁻³⁹) sitting forty orders
above the zeros; the [ρ(a), ρ(λ)] commutators are at the points' precision. **Controls:** a point perturbed by 10⁻³⁰ in one entry fails the relator at
5·10⁻¹⁹ (the check can fail); main's Fox code is B1297's, whose Sym^k control (h¹ = 1, 0, 1, 0, 1, 0, 1) is in that arc's lock; the seat's `report_hp.py` re-run
in the pinned worktree reproduces its summary line for line (66 s).

## 2. What this settles

The last direction in which the cusped object could have carried a net count of 27s is computed, on two benches: the deformations exist, they are the
θ-odd ones they were predicted to be, and they carry nothing — the three classes of the 27 at ρ₀ die together with the cusp's fixed vectors, and B1268's
bound closes to 0 ≤ N ≤ 0. **L204 is CLOSED. The θ-odd frame is closed on every sl₂ germ of the cusped object.** Main's 15 % prior (B1299) resolves to 0.
The head sentence of the programme gains its last clause: the object counts 2 at every fixed locus, every flat deformation of its E₆ holonomy is
symmetry-paired or dead, and 3 arises only from the vector-like ℤ/3 descent. What is left for chirality on the object is what was outside this frame from
the start — the higher-rank components of the character variety, a general theorem, the singular G₂ closing — and, on the observer's side, the priced doors
(I-27 the charge locus, I-30 the sibling's cusp).

## 3. The cloud's newest memos, registered on the same landing

The cloud pushed five addenda after slice B's pin (memo 183 addenda 2–5: the repair of Park's eq (32) is determinate with a₁ the unique single-index
placement; the repaired blocks reproduce Park's Table 3 at p = −2, −3 and determine c₆; the single-index reconstruction cannot close — a negative on its
own method; a reproducibility defect in its certificates found and fixed — the hard-coded path slice B reported; memo 180 addendum 1: GM Theorem 1.3
verified). `park_ahat_repair.py` and `park_repair_cannot_close.py` re-run here from a clean checkout at e51afd6c (receipts); REGISTERED, not re-derived
(the determinacy argument is a bounded follow-up to slice B's Q6). The relay's §6b carries memos 183–184 into the cloud's standing relay.

## Receipts, lock

`verification/b1322_l204_verify.py`, `.out`, `.json`, `sm_B1350_report_hp_rerun.txt`, `cloud_memo183_park_ahat_repair_rerun.txt`, `cloud_memo183_park_repair_cannot_close_rerun.txt`,
`already_banked_at_seal.txt`; `tests/test_b1322_l204_verified.py`.
