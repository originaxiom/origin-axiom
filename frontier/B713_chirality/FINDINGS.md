# B713 (Frontier 1/4) — WHERE CHIRALITY LIVES: it is the OBSERVER'S, not the object's

*cc banking seat, 2026-07-19. Sealed prereg 5e583a40. Multiagent campaign
(compute→3-skeptic adversarial-verify→refine loops), 16 agents, 0 errors; all three
probes OUTCOME B; verified-on-receipt. Two-seat with cc2 (probe 1 = cc2's
triality-matter, reproduced bit-for-bit; M4 revisit integrated). Structural/
arithmetic only; Gate 5 stands; no SM value.*

## Verdict (OUTCOME B, all three probes)
**Chirality is NOT in the θ-symmetric (amphichiral) object; it is the observer's
hand — the c-breaking of measurement.** The object is vector-like "being" at every
accessible instrument; it cannot author chirality via multiplicity; and "which
chirality" is a non-canonical Galois torsor bit = the observer's fiber-functor
choice. This assembles B565 (vector-like) + B307 (single-knot impossible) + B711
(free c-swap) + B701 (measurement torsor) + cc2's triality-matter into one law.

## Probe 1 — the multiplicity/triality route is GAUGE-ONLY (two-seat verified)
The B299 trinification triality (θ,φ) permutes the abstract E₆ **representation**
(the 27 as 9 free size-3 orbits = the SU(3)³ gauge blocks) but does **not act on
the OBJECT**: transported into the geometric-holonomy basis it fails the exact
non-commutation checks against ρ(π₁) that H¹(M;27)=3 is built on — v₀'s support
{12,13,14} → disjoint {16,8,15}/{15,16,8}, so θ(v₀) is not ∝ v₀ — and independently
**Isom(4₁)=D₄, order 8, no order-3 element ⇒ no ℤ/3 acts on the manifold/character
variety.** Reproduced cc2 bit-for-bit; skeptics not refuted. (Honest nuance: the
"h_pr not fixed" check is generic to any non-identity Weyl element; checks (ii)–(iv)
and the D₄/Mostow argument are the dispositive, object-specific ones.)

## Probe 2 — chirality is a non-canonical ℤ/2 Galois TORSOR bit (the observer's)
The two chiralities are the two geometric characters ρ_geom=(2,(3+√−3)/2),
ρ̄_geom=(2,(3−√−3)/2) — the fiber of X(4₁) over the parabolic meridian x=2, cut out
by **y²−3y+3 (disc −3, irreducible over ℚ, NO ℚ-rational point).** Gal(ℚ(√−3)/ℚ)
acts **simply transitively** (transitive+free, 0 fixed) — no canonical/Galois-fixed
chirality. The torsor signature is IDENTICAL to the golden fiber-functor torsor
(B700, disc 5) — the two are legs of the same V₄ (being √−3 vs hearing √5). So
"which chirality" = a choice of complex embedding of the being field = the
observer's fiber functor, not an object property. *(The verify loop caught a real
bug — a silent √−3→√3 no-op that spuriously gave OUTCOME A; the corrected
formal-surd automorphism, cross-checked against complex conjugation, gives B.)*

## Probe 3 — the object is ROBUSTLY vector-like (Nielsen–Ninomiya rigid)
On the object-intrinsic instrument (the Tristram–Levine signature σ_ω, after a
lattice-index first attempt was correctly retracted as a boundary/parity artifact):
**σ_ω(4₁)=0 on the ENTIRE unit circle, FORCED by amphichirality** (σ_ω(mK)=−σ_ω(K),
and 4₁=mK). Amphichirality is witnessed three ways: an exact integer anti-congruence
P^T V P=−V^T (P=[[−1,−2],[1,1]], det 1, on the Seifert form V=[[1,1],[0,−1]]);
non-vacuity (the same test flips the chiral trefoil's σ-set [−2,−1,0]→[0,1,2], so
σ=0 is a real constraint); and SnapPy isometry to the mirror. The index is
**multiplicative under covers** (0→0, covers cannot create chirality) and **non-
evadable** (σ≠0 requires breaking amphichirality = leaving the object). A chiral
knot (trefoil σ=−2) evades; the object cannot.

## The re-grounded 3-generations no-go (cc2's M4 revisit, integrated)
The banked "3 generations closed" no-go (B298/B307/Gate C) HOLDS but was NARROWER
than its framing (each theorem covered only symmetric-multiplicity / C₃-field /
fixed-point-free scopes; none the cohomological route). Re-grounded and tested:
the "3" (h¹=3) is not three permutable generations because the classes sit in
**incommensurable blocks V₁₇⊕V₉⊕V₁** (B657), not a permutable triple, AND the
object has no order-3 symmetry (D₄). M2's compactness obstruction is **Boyle-
specific**, not fully general (M4 refinement). Credit: cc2 (M1–M4, sha per its
triality-matter packet 4d656bd4).

## Placement
LAW_MAP: chirality is the observer's (a non-canonical ℤ/2 torsor bit), NOT the
object's — the object is robustly vector-like θ-symmetric being. Sharpens
B565/B701/B711. Nothing to CLAIMS.
