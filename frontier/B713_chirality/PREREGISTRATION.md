# PREREG — B713 (Frontier 1/4): WHERE DOES CHIRALITY LIVE — in the object, or the coupling? (SEALED)

cc banking seat, 2026-07-19. Owner: clarify the physics of the object, in a
multiagent campaign, four frontiers one-by-one; chirality first. The sharpest gap:
the object is VECTOR-LIKE (B565: chiral index ≡ 0, Nielsen–Ninomiya-rigid), the
world is CHIRAL. Is chirality authored by the object (via multiplicity/triality),
or is it the OBSERVER-COUPLING's — the c-breaking of measurement, a non-canonical
Galois bit? Structural/arithmetic only; Gate 5 stands; no SM value.

## Banked ground (verify-don't-trust before leaning on it)
- B565-T3: chiral index ≡ 0, the object is gauge-VECTOR-LIKE, N–N-rigid (proven,
  ±E defect ~1e-14, amphichiral label pairing σ+(1−σ)=1 exact).
- B307: the single-knot route to chiral 3 generations is IMPOSSIBLE.
- B711 (this session): the conjugation ℤ/2 (Galois √−3→−√−3) freely SWAPS the two
  chiralities ρ_geom↔ρ̄_geom; no canonical chirality at the geometric point.
- B701: measurement = a non-canonical Galois torsor (the object gives the
  ambiguity, not the value). LAW_MAP line 122: the naive ℤ/3 triality does NOT act
  on H¹(D;27) (center = scalar).

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **The multiplicity/triality route (the DECISIVE probe — the only way chirality
   could be object-authored).** Test whether ANY accessible multiplicity structure
   gives nonzero chiral index: (a) does the ℤ/3 (McKay/triality) act on H¹(D;27),
   and if so can it split the vector-like 27 into chiral pieces? (b) finite covers
   — is the chiral index MULTIPLICATIVE under covers (0 → 0, covers cannot create
   chirality)? (c) any B302-style hidden-ℤ/3 / T[4₁;E₆] route that is computable.
2. **Chirality as a torsor bit (assemble B711+B701).** Is chirality a CANONICAL
   object-property or a NON-canonical torsor bit? Confirm the two chiralities form
   a ℤ/2 torsor (the c-swap), with no Galois-fixed canonical selection — i.e.
   "which chirality" = the observer's fiber-functor choice.
3. **Nielsen–Ninomiya robustness (extend B565).** Is the object ROBUSTLY
   vector-like — is the chiral index ≡ 0 forced by amphichirality/CS=0 at every
   accessible lattice/construction, or does some construction evade N–N?

## Two-outcome (sealed)
- **OUTCOME A — CHIRALITY IS IN THE OBJECT:** some accessible multiplicity/triality
  route gives chiral index ≠ 0 (probe 1), OR a canonical chirality selection exists
  (probe 2), OR an N–N-evading construction exists (probe 3). ⟹ the object can
  author chirality — the program's most interesting POSITIVE; escalate, verify at
  2 seeds/2 word-sets before believing.
- **OUTCOME B — CHIRALITY IS THE OBSERVER'S:** every multiplicity route is
  vector-like (index multiplicative, 0→0; naive ℤ/3 doesn't act), chirality is a
  non-canonical ℤ/2 torsor bit (the c-swap, B711/B701), and the object is robustly
  N–N-rigid (B565). ⟹ chirality is NOT in the θ-symmetric object; it is imposed by
  the observer-coupling (the c-breaking of measurement). [expected: B]

## Verdict template
Expected B: "the object is vector-like being; chirality is the observer's hand."
A major structural CLARIFICATION of the object's physics, assembling B565/B307/
B711/B701. LAW_MAP placement; nothing to CLAIMS. If A anywhere: escalate + reseal.
