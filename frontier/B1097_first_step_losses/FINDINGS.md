# B1097 — THE FIRST STEP'S LOSSES, RE-DERIVED — and their falsifiable prediction had ALREADY FIRED (L170b; the audit seat's B8091 + two joins)

**Date:** 2026-08-20 · **Verdict: PROVED (re-derivation + two new joins; the identification their arc registered as unsettled is SETTLED here by composition of banked facts)**

## 1. Their proved content, re-derived with own code

- **The monodromy is the SQUARE**: M² = [[2,1],[1,1]] = φ₁ (trace 3, det +1) with
  det M = −1 — squaring erases the sign. *(Already banked at B1083; re-confirmed.)*
- **Loss one, ORDER**: a→ab and a→ba have IDENTICAL incidence matrices — the matrix
  counts letters, never arranges; the mirror is invisible downstream. Bite control: a→aab
  DOES change the matrix — the blindness is not vacuous. ✓ re-derived.
- **Loss two, SIGN**: det M = −1 → det M² = +1. ✓ re-derived. The squaring FORCED by
  orientability (a punctured-torus bundle is orientable iff its monodromy preserves
  orientation; det −1 inadmissible) — CITED-grade bundle fact, their statement adopted:
  **orientability is what costs the sign.**
- **The unweighted observation**: φ₁ − I = M exactly. ✓ re-derived.
- Their inventory honesty carried verbatim: the two bits are DESTROYED; the circle
  (intercept) and ray (unit) were NEVER THERE — different kinds of absence, not merged.

## 2. JOIN ONE — their falsifiable prediction already fired and was confirmed

Their arc predicts: *"a new wall should appear exactly where a construction needs to
distinguish M from M², and nowhere else."* **B1095 — computed on this bench BEFORE this
arc was read — is that wall**: the mirror-isospectrality of the two hands holds at
even-Fibonacci windows (M²-closed) and breaks at odd (an unpaired M), by exactly the
two cut-adjacent letters. A registered falsifiable prediction, confirmed blind, by an
arc that did not know it was testing it. **Prediction-fired events are the rarest
currency this record has; this one is banked as such.**

## 3. JOIN TWO — their unsettled identification, settled by composition

Their arc registers as UNPROVED which loss carries which physical bit (order ↔ chirality?
sign ↔ arrow?), noting reversal and swap both send ab → ba. Composing three banked facts
settles it: **B1083** (the arrow is NOT a torsor bit at all — it is the monoid's
non-surjectivity, structural, never destroyed because never a bit); **B1095** (the
order/P-bit is physically the localization choice — P-invariant energies, P-equivariant
localization — exactly matching order-loss's downstream invisibility); **B1083's tick**
(det = −1 is the Breath ℤ/2 — the orientation pulse). The settled typing:
**order-loss = the P/hand bit; sign-loss = the orientability/Breath tick bit; the arrow
= NEITHER (structural, T).** Their conjecture table was one row too generous to the
arrow — the same over-assignment B1083 corrected in our own §0, now corrected in theirs.

**Locks:** tests/test_b1097_first_step_losses.py (the incidence identity + bite control,
the det erasure, φ₁ − I = M, and the M-vs-M² wall's existence via B1095's lock facts).
