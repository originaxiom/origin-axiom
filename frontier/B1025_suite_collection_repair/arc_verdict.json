{
 "id": "B1025",
 "verdict": "PROVED",
 "instrument": true,
 "claim_one_line": "THE SUITE DID NOT RUN. On a clone installing exactly requirements.txt (snappy commented out as OPTIONAL), `python3 -m pytest -q` returned `Interrupted: 3 errors during collection` and executed ZERO tests -- not three skips, since pytest aborts the whole run on a collection error -- so the entire lock layer was silent on the command REPRODUCIBILITY.md names as the verification path, contradicting that file's own contract ('the verified figure-eight constants are hard-coded and tested without it'). CAUSE: three module-scope imports, two direct (tests/test_b461.py:2, tests/test_b719_scale.py:2) and one indirect (frontier/B849_order_parameter/order_parameter.py:22, executed at collection time by tests/test_b849_order_parameter.py via spec_from_file_location+exec_module), against 37 modules already using the correct pytest.importorskip idiom and 9 more importing inside a function or try -- an ungated-practice drift of exactly the shape WORKING_RULES Enforcement predicts. REPAIR: all three reach snappy through importorskip; the frontier arc's own script is untouched (rule 9), the guard placed in the loader. MEASURED: 0 tests collected + 3 errors BEFORE, 3837 tests collected + 0 errors AFTER. LOCK: tests/test_b1025_optional_deps.py, six AST-based locks asserting the structural invariant rather than a transcript (rule 7), WITH A DEMONSTRATED POSITIVE CONTROL -- re-introducing the bare import fails 2 of 6 and restoring gives 6 of 6, so the criterion can both pass and fail (MB12). SECOND FINDING, registered not repaired: gate_chain_locks enforces CITATION HYGIENE, NOT LOCKS -- it checks that the cited test FILE EXISTS and never that the file tests the link, so THE CHAIN's own admission bar ('exact statement + banked computation location + green lock') is unenforceable by the gate that names it. Confirmed instance, disclosed by the ledger and re-verified here: C2 cites tests/test_b749_genesis_forks.py, which contains exactly four tests (f5, f6, f4, f7) and NO F3 test -- as B998 found BY HAND, since no gate could; B1003 later closed F2 and F8, F3 still has none, and the gate passed throughout. Same family as the earlier 'three gates passing while the files they guarded had been deleted', which was fixed for a MISSING FILE and does not reach a MISSING TEST INSIDE A PRESENT FILE. SCOPE: no chain link is false -- C1 and C2 are classical results cited not re-proved; what is unbacked is the repo's assertion that they are locked HERE. Not patched in-arc because gate semantics are versioned-amendment-only (GOVERNANCE 11); the cheap fix is named (require the cited file to mention the link's arc id, and REPORT THE COVERAGE FRACTION as views/COVERAGE.md does). This arc claims no green suite -- only that the suite now COLLECTS where it collected none -- and changes no mathematical claim, ledger row, or verdict.",
 "depends_on": [
  "B998",
  "B1003",
  "B849",
  "B461",
  "B719"
 ],
 "supersedes": null,
 "superseded_by": null,
 "authored_by": "consolidation-refresh seat (2026-08-10); instrument lane"
}
