# B634 — THE CONDUCTOR-CHORD ARC (the Yukawa on the double at κ = conductor; prereg)

**Preregistered 2026-07-15 at the owner's directive ("stop looking for a
second axiom — the mirror is it; compute the Yukawa on the double at
κ = conductor"), with this seat's verify-don't-trust adjudication built
in. Mathematics only; any SM-facing comparison of this arc's outputs
requires the owner's directive + the full sealing checklist (MB12 +
MB13-in-doc + pipeline controls + the Input-Completeness Ledger).**

## The adjudicated chain (status labels per claim)

- **PROVED (banked):** the solo is value-mute in every tested channel
  (Branch 3 + B615-R hardened); chirality is chord-borne (B584/B593);
  the symmetric mass-shaped texture cannot exist on the solo complement
  (B632 O1/O2, verified); amphichiral ⇒ conjugation-closed spectrum
  (B613, one-way; converse empirical).
- **EXACT IDENTITIES (verified again at this prereg):** for the fig-8
  monodromy A₁ (tr 3): det(A₁ − I) = −1 (the abelian invisibility
  unit), **det(A₁ + I) = 5 = tr² − 4** (the torsion base = the one-door
  reflection conductor, B617/B620); the mirror-twisted mapping torus
  (the −A₁ bundle = B585's C-twisted torus, a cusped hyperbolic
  once-punctured-torus bundle) has **H₁ = ℤ ⊕ ℤ/5** (SNF of A₁+I =
  diag(1,5); B591's tone-torsion).
- **THE WORKING PRINCIPLE (named, registered as such — the second-axiom
  reading):** the object is axiom one; its mirror-coupling is axiom two,
  supplied BY the object (amphichirality); the mirror-twist's torsion
  conductor selects the stage: **κ = 5 = det(A₁+I)** — the golden stage
  where outcome B's correspondence and the hearing amplitudes live.
  This answers seat 4's Level-2 incompleteness ("no stage-selection
  principle") with banked exact structure rather than a new input. Its
  STATUS is a principle-identification: the identities are theorems;
  "the conductor IS the selection" is the axiom being adopted, and it
  is falsifiable through this arc's outputs.
- **THE PRECISION CORRECTION (recorded against the directive's text):**
  "the double" names TWO different mirror-couplings. The MIRROR-TWISTED
  BUNDLE (−A₁; cusped) carries the conductor (H₁ torsion ℤ/5) and the
  banked chord Z_C = +1/φ — but being cusped it falls under B632's
  O1/O2: it CANNOT carry the symmetric texture either. The
  BOUNDARY-DOUBLE D(M) = M ∪_∂ M̄ (closed; H³ = ℂ) is where the
  symmetric texture is well-defined. The two roles split: the TWIST
  selects the stage (κ = 5); the DOUBLE carries the mass-shaped object.
  Both are "the mirror as second axiom."

## MB13 (swept at prereg)

The boundary-double's cohomology: never computed (B580 G1 registered
the mirror-double as the forced coupling; no H¹(D(M); ·) computation
exists). The gluing data EXISTS banked: B605's exact intertwiners for
all four orientation-reversing D₄ families (certified over K). The
conductor identities: B585 (the −A₁ naming theorem), B591 (tone =
torsion), B617/B620 (base = tr²−4 = the one door). Clean on the target
computation.

## The gates (locked order; each two-outcome)

1. **G1 (conductor identities; COMPUTED at prereg):** det(A₁+I) = 5 =
   tr²−4; SNF(A₁+I) = diag(1,5) ⇒ H₁(−A₁ bundle) = ℤ ⊕ ℤ/5. PASS.
2. **G2 (gluing existence):** from B605's banked intertwiners, exhibit
   the explicit flat E₆ structure on D(M) = M ∪_∂ M̄: the amalgamated
   rep ρ_D with ρ̄ = the mirror rep identified along the peripheral
   torus by a banked intertwiner. Gate: the identification is exact on
   both peripheral generators (meridian a; longitude abABaaBAbA).
   FAIL ⇒ the double carries no compatible flat structure from this
   family — banked, arc pivots to the alternative closed 3-cycles.
3. **G3 (the double's generation count):** H¹(D(M); 27) by
   Mayer–Vietoris with the exact res maps (res injective on M, banked).
   Deliverable: the dimension and the block structure. Either value
   banks; dim 0 kills the route honestly.
4. **G4 (THE YUKAWA TABLE):** the symmetric triple cup
   H¹(D(M);27)⊗³ → H³(D(M);ℂ) = ℂ through the explicit cubic C (B632's
   45-monomial object): the exact symmetric 3-tensor Y_ijk over the
   double's generation classes. Controls: coboundary invariance (the
   corrected 2-cell chains on BOTH copies), the O2-antisymmetric part
   must vanish on the closed manifold (Poincaré duality consistency),
   permutation symmetry of Y verified exactly.
5. **G5 (the conductor read):** the field/arithmetic of Y's entries and
   invariant ratios — does the conductor 5 (= κ) appear in the values'
   structure (norms, discriminants)? A structural read, NO SM number.

## MB12 (computed at prereg)

- Non-trivial: no gate's object exists in the bank; G4's tensor is the
  program's first well-defined symmetric texture anywhere.
- Can fail: G2 can fail (no compatible intertwiner); G3 can return 0 or
  ≠3; G4 can return the zero tensor (the closed-manifold texture EMPTY
  — the honest kill of the mirror-axiom's value route); G5 can show no
  conductor structure. Every failure banks and the principle's
  falsifiability lives exactly there.
- Can pass: dim H¹(D(M);27) > 0 with a nonzero symmetric Y — the
  program's first mass-shaped object; then and only then does a sealed,
  owner-approved comparison design (ratios + angles jointly, one
  diagonalization, Input-Completeness Ledger) become POSABLE.
