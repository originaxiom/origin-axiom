# B1024 — L153 ANSWERED: the frame bits ARE the torsor bits (d = 2), and the undercount scenario is refuted unconditionally

**Date:** 2026-08-11 · **Seat:** cc · **Sealed:** `dc823e86` (prior: uncertain, weak lean SAME —
**held**). Gate 5-Q; zero anchors.

**Verdict: PROVED — SAME (d = 2) under the primary construction; d ≤ 3 unconditionally;
INDEPENDENT (d = 4) REFUTED.**

## The instrument, validated before use

B936's explicit class map — a sign character χ ∈ T_ad[2] = (ℤ/2)⁶ has H¹ class
**(χ at node 1, χ at node 3)**, the two τ-fixed Bourbaki nodes; a *structure* H(σ_χ∘τ) carries
the class of its torsor coordinate χ·χ₊. **Consistency, checked first:** χ₋ → (0,0), exactly
B936's "D₂ is a coboundary"; all-minus → (1,1), exactly "D carries the nonzero class." Two
independent banked statements reproduced by the map before any new reading.

## The two generators (`b1024_cells.py`, exact)

- **CONJUGATION = σ_c**, the banked compact-flip character **CHI_C = (1,−1,−1,1,−1,1)** (B907,
  used verbatim by B939): class **(1,0) — NONZERO. Unconditional**: this is a banked character,
  not a construction.
- **REVERSAL**, constructed from the banked chain with **zero unforced choices**: word reversal ↦
  inverse word ↦ (B644's group functor) g ↦ g⁻¹ ↦ on the 27, the contragredient = **the bare
  τ-lift** (χ = 1; any sign-dressing would be an unforced insertion with no banked source).
  Structure class **(1,1)**.
- **Generation: span{(1,0), (1,1)} = ALL of H¹ = (ℤ/2)². SAME — the Hermitian-frame bits are the
  observer-torsor bits in a second presentation. The discrete deficit is d = 2.**

**Sensitivity, named not hidden:** the one banked *dressed* lift that acts diagonally (φ₊) would
give reversal the class (0,0) → PARTIAL (d = 3). **No construction yields INDEPENDENT** —
conjugation's nonzero class alone refutes d = 4.

## Consequences, executed in this batch

- **R11 v2's "2 ≤ d ≤ 4" TIGHTENS: d = 2 (primary), d ≤ 3 unconditional** — the input list does
  **not** undercount; `THE_CLAIM`'s hypothesis line stands as written.
- cc3's Blocker 1 is **resolved, not just conceded**: the identification it demanded now exists,
  with its convention-sensitivity stated.
- The τ-fixed-node indexing and the torsor indexing are two presentations of one structure — the
  "smells intrinsic to the 27" worry in the sealed prior was the coboundary part (χ₋'s triviality),
  not the classes.

## Scope

From banked characters and the banked class map; the reversal construction is the cell's one
built object and its alternative is priced in the sensitivity line. No new mathematics about the
object beyond the join; the join is the point.
