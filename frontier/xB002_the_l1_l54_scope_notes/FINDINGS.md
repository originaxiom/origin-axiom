# xB002 — THE TWO OWED SCOPE NOTES, WRITTEN: L1's selection and L54's quantifier — and a scope defect inside L1's own evidence arc, found and repaired

**Status: banked (frontier, seat branch `sep16-branch`). Verdict PROVED** (two scope notes
delivered; one new exact computation with its discriminating fact in-sandbox). **Seat: `xb`**, an
adversarial/review seat opened 2026-09-16; citation-only prefix, no main numbering consumed.
DESIGN sealed **POST-HOC and labelled**. Gate 5 untouched — pure topology, no SM quantity.
Locks: `verification/reproduce.sh` (two cells, both re-run green here).

## Why this arc

`docs/THE_LADDER.md` X15 ranks six over-wide closures and records all six scope-correction notes
as **"still unwritten as of B1101"**; `docs/OPEN_PROBLEMS.md` §7 carries the companion catalogue
of twelve results banked too narrow, every one still `PENDING` in
`docs/REPRESENTATION_TRIAGE.md`. Verified still outstanding at this head (2026-09-16): **11/11
PENDING, 6/6 notes unwritten.** B8097's own meta-finding is the reason: *"the buried results have
already been found. Twice… noticing has been treated as discharging. A row that says this is a
debt is not payment."*

This arc pays (1) and (2).

## Deliverables

- **`SCOPE_NOTE_L1.md`** — the object's own selection.
- **`SCOPE_NOTE_L54.md`** — gate A's quantifier.

## L1, in one line

**The genesis selects a family; knot-ness selects the member.** Of the three criteria, extremality
along the metallic diagonal selects a *row*, volume minimality **ties** (vol m003 = vol m004
exactly), and arithmeticity is a **commensurability-class invariant** and therefore cannot
discriminate between members at all. B1136's genericity control gives the residue and it is one
property: **`H₁ = ℤ`** — that m004 is a knot complement in S³.

## L54, in one line

**Gate A is sealed for `4₁` and its covers to index 6, not for "the object."** Under
`COMPUTE_THE_PROGRAM`'s binding definition "the object" is the full relations including the class;
covers **are** the commensurability relation, so the index horizon and the class boundary are the
same wall. Newly computed here: **5 of the 21 shape-field family members sit at volume ratio 2.5
to m004** (`m410, m412, s594, s595, s596`) and are therefore excluded from *every* cover census at
*any* index. B1418 corroborates from the other side — the class members carrying chirality and the
count of three (`m202, s959, o10_150726`) are none of them covers of m004.

## THE NEW RESULT — a scope defect inside B1136, found while re-deriving it

B1136's FINDINGS states its family as *"the orientable cusped census manifolds whose tetrahedron
shape field is ℚ(√−3) — exactly **14**."* Its scan (`verify_genericity.py:28-30`) breaks at
**census index > 1200**; that qualifier does not appear in the sentence.

Re-scanned by tetrahedron count instead (≤ 6 tetrahedra — the depth its own 14 already reach,
since s594/s595/s596 are 6-tetrahedron manifolds), the family is **21**. The seven missed —
`s955, s956, s957, s958, s959, s960, s961` — sit at census indices **1256–1262**, immediately past
the cutoff. The discrepancy is the cutoff and nothing else; verified exactly.

**The conclusion survives the repair and is strengthened:** on the corrected 21-member family the
separator set is still exactly `['h1_is_Z']`. L1 now rests on a family half again as large.

**Two of the seven are load-bearing elsewhere:** `s958` (one of the five class members where the
chirality index fires, B1418) and `s959` (one of the three carrying chirality, the arithmetic, the
door and the count of three together). Their absence from the banked family list is not cosmetic.

This is an instance of the very class X15 catalogues, occurring inside the arc that supplies L1's
evidence. Recorded here rather than left to be found. **B1136 is not withdrawn** — its conclusion
is confirmed and widened; only its family sentence needs its bound.

## The join this seat adds (banked as a join, not as a discovery)

Three banked results, joined in one sentence:

> **The unique property that selects m004 (B1136: `H₁ = ℤ`) is the unique property that silences
> it on the chirality index** — `H₁ = ℤ` ⟹ the meridian generates ⟹ the cusp-trivial character
> group is trivial (B1324's mechanism) ⟹ the root-of-unity construction that fires on the class
> has no input on the member (B1418: m004's locus is the golden one, real roots, no torsion).

Computed here for all 21 family members: m004's cusp-trivial group is the **only** trivial one
among the one-cusped members with torsion in the family, and the five class members where the
index fires all carry torsion (`[3], [6], [6], [6], [16]`).

**Scope, stated:** this is NOT B1222's thesis. B1222 proposed "the selection is the obstruction" as
a general law about *symmetry* and killed it properly (population test, `r(|Sym|, has-torsion) =
+0.50`, the wrong sign). The statement above is not a symmetry claim; it is a homological identity
on one instrument, and B1222's own data supports it (it notes m003 carries the ℤ/5 that m004
lacks). The general law stays dead.

## What is NOT claimed

No withdrawal of B1136, B985, gate A or L1's underlying mathematics. No claim that gate A fails on
the class — it is **not checked** there. No claim that the full-census family is 21 (the record's
own full-census figure is 112); 21 is the count at ≤ 6 tetrahedra, stated with its bound. No value,
no generation count, no physics reading.
