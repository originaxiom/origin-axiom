# B143 — strategic synthesis + Campaign-1 feasibility scope (the venue verdict)

Two deliverables: the strategic synthesis (`../../docs/STRATEGIC_SYNTHESIS.md`) and the gating
feasibility scope for the chirality-of-interactions campaign. MATH tier; firewalled.

**Venue verdict.** The chirality-of-interactions question is **invisible to the algebraic (trace) venue** — RIGOROUS:
B131's interaction lives in `(κ, trT)`, both trace invariants, and the mirror preserves all traces (B139/B140), so the
fork is automatically mirror-invariant. The algebraic venue carries the κ-**landscape** (Lead A), not chirality.
**Chirality-(ii)** (mirror = orientation-independently distinct; the real wall) needs an orientation-sensitive
invariant on the composite ⟹ the **topological venue** (closed JSJ manifold; needs **Regina**, not installed) or a
**2-cusped hyperbolic-link realization** (SnapPy-native; construction open). B131 reuse reproduces the `(1,2)` fork
`{−4,−2}`. an AI-assisted review's "588 reps / Massey" claim corrected (Massey dead for s776, the 3-chain link; K-I).

⟹ **B144** (the actual chirality campaign) runs in the topological venue (install Regina) or a link realization.

```
python frontier/B143_interaction_feasibility/probe.py
python -m pytest tests/test_b143_interaction_feasibility.py -q
```

**Tier.** MATH. New `docs/STRATEGIC_SYNTHESIS.md` + `REPRODUCIBILITY.md` MB11. Nothing to `CLAIMS.md`; P1–P16, B85,
B124–B142 untouched. See `FINDINGS.md`; ledger **V132**.
