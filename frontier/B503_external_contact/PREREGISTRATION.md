# B503 — EXTERNAL CONTACT (prereg; OWNER-GATED, fires only after the Closure Campaign's package merges)
Recipient rationale: Cantat's 2009 fixed-curve computation is the closest prior art to BOTH
APPEARS-NOVEL cores (held-breath field law; the σ_m fixed-locus method) AND the mixed semigroup is the
unexplored edge of Cantat–Dujardin's own program. Package: the two specialist notes (R1 held-breath;
the seam broken-lattice), P4, the monoid results (P5 outline), the proved drift ledger — regenerated
from corrected sources (the F4 rule: no known-corrected error leaves the house), consistency-checked
against the ledgers, merged with the Closure Campaign's verified package. **Send = owner's hand only.**
Honesty clause: "fully closed" is not claimable without this external read (K020 §6a).
