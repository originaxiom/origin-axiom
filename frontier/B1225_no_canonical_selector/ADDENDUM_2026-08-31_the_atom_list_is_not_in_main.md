# Addendum (2026-08-31) — the 17 atoms are not enumerated anywhere on this bench

**Prompted by the owner asking, simply, "what are these 17 atoms?" — and the answer is that we do
not know, which is worse than the fence this arc originally carried.**

## The gap

B1225's step 2 reads: *"the menu's 17 tier-1 atoms are object-canonical, and every menu value is
built from them by {+, −, ×, ÷, √}."* That is the hypothesis the whole no-go rests on.

**Searched: main, `outside-bench`, `codex/seat-r001`, `paper/structure-genesis-first`. The list does
not exist on any of them.** What is banked is:

- the **count** — 17 tier-1 atoms;
- the **property used** — all of them are real (B1203, and this is what made the c-filter cut zero);
- the **menu size** — 11,720, median gap 3.53×10⁻⁵, min 1.63×10⁻⁹, reproduced independently here.

B1203 **fetched cloud's enumerator, re-ran it, and reproduced the count** — but did not bank the
atoms themselves. So the single most load-bearing input to this arc is a list **no one on this bench
has seen**.

## What this does to B1225

**It does not refute it, and it does weaken it in a specific place.** The argument needs exactly one
property of the atoms: that they are **object-canonical** (Aut(D)-invariant). If they are, the
conclusion follows in one line and generalises B1203's zero cut to every G readable off D.

**But "object-canonical" is asserted, not checked here.** If even one of the 17 is *not*
Aut(D)-invariant — if some atom carries β-odd or dimensionful content — then G need not fix it, the
action need not be trivial, and **the no-go does not apply to any menu value built from that atom.**
That is precisely where a surviving selector could hide, and it is the one place this arc cannot see.

## The correct status

**B1225 is PROVED conditional on the atoms being object-canonical**, which is MENU-1's own
construction rule as reported, and unverified on this bench. That condition should be read as a
hypothesis of the theorem, not as background.

## Action

Relay to cloud: **send the 17 atoms.** Not the count, the list — with each atom's β-parity and
dimension, so B1168's law can be applied to it individually rather than to the set by assertion.
Until then the arc's headline should be read with its hypothesis attached.

*Recorded because the pattern is the day's pattern: a load-bearing input that exists as a number
rather than as a thing, and stays invisible until someone asks the simplest possible question about
it.*

---

## CLOSED, same day, by B1227 — and the answer was already in the record

The crack named above — *"if even one atom is not Aut(D)-invariant... that is precisely where a
surviving selector could hide"* — **is closed**, and not by cloud sending the list.

**B1227's theorem:** for an amphichiral object the mirror is a *self*-isometry, so any mirror-odd
invariant `I` satisfies `2·I = 0` in its value group. The atoms are **real**, and `ℝ` is
torsion-free, so a mirror-odd atom would have to be **zero**. Hence every nonzero atom is
mirror-even, *for free*.

So B1225's step 2 does not need "the atoms are object-canonical". It needs only "**the atoms are
real**" — which **B1203 verified on this bench** when it re-ran cloud's enumerator (11,720
reproduced; *"all 17 atoms real"*).

**What this addendum got wrong.** It treated an unverifiable *list* as an unverifiable *property*.
The property was already banked, one arc away. The residual exposure is not "enumerate 17 atoms and
audit each for β-parity and dimension" — it is the single question *are they real*, already answered.
Relay `CC_TO_CLOUD_2026-08-31_SEND_THE_SEVENTEEN_ATOMS.md` stands, but nothing now blocks on it.
