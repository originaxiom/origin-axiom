# B618 — the conductor unification: the third-object prediction PASSES

**Status: banked (frontier). The registered B617 prediction, tested and
confirmed. Lock `tests/test_b618_conductor.py`. Run:
`python3 conductor_test.py` (~10 min; scan to κ = 45).**

## The registered prediction and the result

B617 registered (before this run): the tr-5 bundle (R³L, torsion base
tr² − 4 = 21) shows its eigenvalue-field (ℚ(√21)) content in the odd
hearing trace at d|21 levels only (d > 1). THE RESULT: the
√21-bearing levels in κ = 4..45 are exactly **{14, 21, 35, 42}** — all
divisible by 7, with the full mod-21 levels included; no content at any
level coprime to 21. **PASS.**

## The three-object law (the conductor unification, now predictive-tier)

| object | tr | torsion base tr²−4 | hearing-trace field window |
|---|---|---|---|
| fig-8 (RL) | 3 | **5** | κ ≡ 0 (mod 5) — the [5|κ]/φ law (B601) |
| m136 (R²L) | 4 | **12** | the mod-12 levels (B610: (1−√3)-values at 12, 24) |
| tr-5 (R³L) | 5 | **21** | the mod-7/21 levels (this arc) |

> **The object's bundle-torsion base tr² − 4 (= its eigenvalue-field
> discriminant content, by B617's closed form) gates where that field
> enters the stage's hearing trace.** One number plays both roles —
> the semiclassical (torsion) side and the quantum (trace-law) side of
> the same object. Registered → predicted → confirmed on a third
> object; the mechanism (presumably through the Weil-conductor
> det(A ⊗ w − I) arithmetic of B587) is the next cell when wanted.

## Refinement noted

The tr-5 window is the 7-part of 21 (levels 14, 35 are mod-7-not-mod-3);
mod-3-only levels carry none. Recorded as data for the mechanism cell
(21 = 3·7; the content follows the 7).

**NAMING CORRECTION (2026-07-15, B621):** the object called "m136" in
this arc is the R²L (trace-4, vol 2.6667) bundle — census m136 is
actually the RRLL silver bundle (SnapPy-verified, B622). All
computations here concern the R²L bundle itself and stand unchanged;
read "m136" as "the R²L bundle" throughout.
