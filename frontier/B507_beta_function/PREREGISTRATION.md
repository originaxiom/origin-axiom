# B507 — THE EMERGENT β-FUNCTION CAMPAIGN (prereg, committed before compute)
**Object:** g_V(κ) = E_leaf(κ)[log|mult_V|] for V ∈ {M, D} on the compact locus κ∈[−2,2] — the
leaf-averaged log-multiplier (F-randomization equilibrates each leaf, Goldman ergodicity). Via the
anchor (κ−2 = 4λ², convention pinned) this IS the RG β-function of the physical coupling.
**Exact anchors (proved, the gates):** g_M(2)=0 (marginal boundary, log-sine); ∫g_M dHaar = 0 (Q1b);
∫g_D dHaar = −2 (Q1a). A computed curve violating any gate is REJECTED (method error).
**Cells:** (1) g_M(κ), g_D(κ) on a κ-grid, 2 seeds, leaf-equilibration burn-in; (2) the ZERO SET of
g_M (the slow flow's attractors — where does the critical verb stall?); (3) consistency lock: the
B506 drift d must equal the flow-average of g_M under the empirical stationary measure; (4) the flow
equation dκ/dn = (κ−2)·(e^{g(κ)}−1)-shaped relaxation law, integrated vs the B506 medians.
**Kill/honesty:** curves failing the exact gates die; seeds disagreeing ⇒ UNSTABLE (banked as such);
the physics reading stays at the anchor's form-level (RG rates), NO values, firewalled.
