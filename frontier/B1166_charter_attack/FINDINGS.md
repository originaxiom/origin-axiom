# B1166 — cc's attack on the GRAVITY_CHARTER's two cc-assigned sub-claims: C3 VERIFIED (one dilaton, not two moduli), C4 REFUTATION-CANDIDATE (the free-orbit torsor is √3-flavored, being×hearing is √5-flavored)

**Status: banked (frontier). Verdict OPEN** — a charter attack delivering one **confirmation** (C3) and one
**refutation-candidate** (C4). Cloud relayed THE_GRAVITY_CHARTER (`origin/outside-bench` 60bcf01d) with C3 and
C4 assigned to cc (C5 the owner's; C6 cloud's lane). Both are decidable in-sandbox; I attacked rather than
acknowledged. `verification/reproduce.sh` → `REPRODUCES`. Gate 5 clean (Galois/rigidity facts only, no SM
value). cc/codex-shared per the charter; relayed to both.

## C3 — the continuous observer freedom is ONE dilaton (ℝ⁺), not two moduli — **VERIFIED**

The charter's C3 predicts: in the ℂˣ = ℝ⁺ × U(1) freedom on the object's top datum, the U(1) phase is
object-paid (μ₆ torsion), leaving ℝ⁺ (the scale) as the sole free continuous datum. Proof condition: exhibit
the absorption; refutation: a second independent continuous modulus. **Proven from two banked facts:**

- **The phase pin (U(1) is object-paid).** CS(m004) ≡ 0 exactly (own-verified: Im of the complex volume =
  1.8×10⁻¹⁵; amphichirality, B1163). So the complex volume Vol + i·CS is **real** — the object supplies **no
  free U(1) phase**. The residual phase choice is the **discrete** μ₆ = roots of unity in the trace field
  K = ℚ(√−3) = ℚ(ζ₆) — a ℤ/6, a finite/torsion bit, *not* a continuous U(1). This **exhibits cloud's E1**
  ("the gravitational phase is μ₆ torsion") as the exact absorption C3 asked for.
- **No second modulus (Mostow rigidity).** m004's complete hyperbolic structure is unique; H₁=ℤ, one cusp,
  and the single cusp deformation is frozen by completeness. There is no continuous geometric modulus beyond
  the ℂˣ normalization of the top form. So the refutation condition (a second independent continuous modulus)
  **cannot be met** — it would require a non-rigid deformation the object provably lacks.

**⇒ ℂˣ = ℝ⁺ × U(1): the U(1) is object-paid (CS=0/μ₆), the ℝ⁺ is the scale (the one withheld continuous
datum, the scale wall). The observer's continuous freedom is ℝ⁺ alone. C3 stands.** This also confirms the
B1164 count "2 discrete + **1 continuous**" is exactly right at the continuous slot (one real dilaton), and
sharpens the B1164 price-reconciliation residue cloud flagged to cc.

## C4 — the three (ℤ/2)² presentations are one torsor — **REFUTATION-CANDIDATE**

The charter's C4 predicts the branch-selection V₄ (B1161), the being×hearing V₄ (B713), and the measurement
torsor (B1024) are **one torsor**, provable by **explicit equivariant isomorphisms** (named actions);
refutation: any pair proven non-isomorphic as torsors with named actions. The disc-144 chase delivers a
**live refutation candidate on the (branch, being×hearing) pair:**

- **The free-orbit torsor is √3-flavored.** B1161's V₄ = Gal(H/ℚ), H = the ring class field of disc **144** =
  ℚ(ζ₁₂) = K(√3) (B1067, own-verified). disc 144 = 2⁴·3² — **only 2, 3 ramify**. Its three quadratic
  subfields are ℚ(√−3), ℚ(√3), ℚ(√−1) (B1164, own-verified). The torsor's second generator toggles **√3**.
- **being×hearing is √5-flavored.** B713/B926 (W11) fixes being = ℚ(√−3) [prime 3] and **hearing = ℚ(√5)**
  [prime 5] — the value-rich E₈ end, provably *not* object-generated. Its second generator toggles **√5**.
- **√5 ∉ ℚ(ζ₁₂)** (own-verified: 5 does not ramify in disc 144). So the two second-generators toggle
  **different quadratic irrationalities over different fields** (√3 vs √5). As torsors **"with named actions"**
  — C4's own clause — the free-orbit and being×hearing V₄'s are **not isomorphic**; only the *abstract*
  (ℤ/2)²-torsor-on-4-points identity survives, which the "named actions" clause forbids.

**⇒ C4 as literally stated (all three one torsor with named actions) is REFUTED for the (branch,
being×hearing) pair** — one non-matching pair suffices. **Held at CANDIDATE, not hard-REFUTED**, pending two
things the charter itself makes cc/codex-shared: (a) **cloud's intent** — if C4 means *abstract*
torsor-iso (all (ℤ/2)²-torsors-on-4-points are the same), it survives but is nearly vacuous; if it means
*named-action* iso (its stated proof condition), it fails on √3-vs-√5; (b) **the exact B1024 frame-V₄
action** ⟨c, r⟩ (chirality × reversal) — whether *it* is √3- or √5- or object-mirror-flavored is the third
leg, not yet pinned. Relayed to cloud (whose claim it is) + codex (the shared disc-144 chase).

## Why the refutation candidate is not a surprise

It is the **B1164 subfield census** (ℚ(ζ₁₂) ⊃ {√−3, √3, √−1}, √5 absent) applied to the torsor question, and
it echoes the object's own two-ended geometry: √−3 (E₆/being) and √5 (E₈/hearing) sit at **opposite** ends
(Niven-forced), while √3 is the *ring-class hinge* internal to the √−3 end (B1155). A torsor that lives at the
√−3-end's ring class field (√3) and one that pairs the two *ends* (√5) are different objects by construction.
The charter's own E-table separates being (object) from hearing (coupling) — so unifying their V₄'s as *one
named torsor* runs against the being/hearing firewall the charter elsewhere keeps.

## Fences

C3 rests on CS≡0 (own-verified 1.8e-15) + Mostow rigidity (standard) + the μ₆-torsion identification (cloud
E1, cited). C4's refutation candidate rests on disc(ℚ(ζ₁₂))=144 (only 2,3 ramify; own-verified factorization)
+ the B713/B926 hearing=√5 identification (banked) + B1164's subfield census (own-verified) — it is as sound
as those; the *residual* is cloud's intent + the B1024 leg, which is why it is a CANDIDATE not a hard kill.
No firewall crossing; Gate 5 clean (Galois groups, ramification, rigidity — no measured value). Not routed to
the kill_graph (C4 is a candidate pending cloud's adjudication, not a confirmed negative).

## Routes

- **C3 → cloud:** VERIFIED; the charter can upgrade C3 from prediction to carried, citing B1166 + B1163
  (CS=0) + B1164 (the 2+1 count). cc-side anchor for the charter's C3 row.
- **C4 → cloud + codex:** REFUTATION-CANDIDATE; cloud to state whether C4 means abstract or named-action iso;
  codex to pin the B1024 frame-V₄ leg in the disc-144 chase. If named-action, C4 needs re-scoping (perhaps to
  the √−3-internal torsors only, dropping being×hearing).
- **C5 (definitional admission):** the owner's, unchanged — = B1165/B1163's terminal fork.
- **C6 (completeness):** cloud's lane; B1165's G2 typing feeds it (every gravitational datum lands frame-free
  or frame-completed).
