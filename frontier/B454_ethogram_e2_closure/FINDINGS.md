# B454 — Ethogram E2: growth, exhaustion, choice — the closure LEMMA (proved) 

**Status: banked (frontier). Firewalled. Prereg: `PREREGISTRATION.md` (PR #596, before
computation). Verdict: H0a, EARNED — by lemma, not by scan exhaustion. The word channel
cannot probe outside, ever, and the reason is class-generic.**

## The lemma (the primary deliverable, proved)

**Closure lemma.** For any representation of the free group ⟨a,b⟩ into SL(2), the trace of EVERY
word lies in ℤ[x,y,z], the ring generated by the seed traces x=tr(a), y=tr(b), z=tr(ab).
*Proof:* the Fricke/Cayley–Hamilton identity tr(UV) = tr(U)tr(V) − tr(UV⁻¹) (verified
symbolically on generic SL(2) matrices) + induction on word length (the reduction step verified
symbolically). ∎

**Consequence (the owner's question, answered by theorem):** the word-evolution channel can
NEVER leave its birth arithmetic — at the object's seed (the discrete-faithful point,
x=y=z=(3+√−3)/2, the B448 monodromy-fixed point), every invariant of every level lies in
**ℚ(√−3)** forever. "Does it probe outside naturally?" — **no, and it never can, through this
channel.** Honesty note (stated in advance in the prereg): the lemma is **class-generic** —
closure is the environment's property, not the organism's distinction. Scope: this channel only;
the quantization channel (level N) imports ℚ(ζ_N) by an EXTERNAL parameter — that is a different
door, already mapped (the thermodynamic campaign).

## The scan is non-vacuous (the sharp part)

The matrix-ENTRY field is *strictly bigger* than the trace field: realizing the seed traces
forces a generator entry b with b² − sb + 1 = 0, and s²−4 = (−5+3√−3)/2 is NOT a square in
ℚ(√−3) (proved: no rational (p,q) with (p+q√−3)² = s²−4). So the per-word collapse of traces
into ℚ(√−3) is a genuine cancellation, verified EXACTLY in the ring
K = ℚ(w)[b]/(w²+3, b²−sb+1) (elements = 4-tuples of Fractions; the check = "the b-components of
tr vanish"): **55/55 prefix traces at level 8 collapse; 1001/1001 control words (Thue–Morse +
1000 seeded random) collapse identically** — the genericity witness, exact.

## Choice vs compulsion (per-level)

The substitution continuation is deterministic at every level ≤ 8 (verified; trivially, but the
card demanded the check). No choice enters the word channel; the chain-level choice boundary
stays where B449 put it (L57: representation/pairing/theta).

## Bins

**H0a, earned by lemma** (not by bounded scan — the red-team's demanded upgrade delivered: the
induction kills all levels, so there is no UNDECIDED residue). No counterexample; no NEW-MATH
claim (Fricke's lemma is classical; the deliverable is the exhaustion card's closure, now a
theorem-backed entry).

## Reproduce
```
python3 closure.py    # ~5 s exact; prints ALL CHECKS PASS
```
