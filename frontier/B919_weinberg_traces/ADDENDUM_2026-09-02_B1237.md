# B919 — ADDENDUM: the runner cannot run from the repo; C42's witness relocated to committed code

*Addendum written 2026-09-02 under **B1237** (the physics-seat harvest, second ring). Source of the flag: the physics-seat evaluation branch @ d415423a; every number below recomputed on this bench — `frontier/B1237_physics_seat_r31_r38_harvest/verification/`. The arc's verdict file carries the same correction in its `note`.*

`traces.py:15` reads `RUN = os.environ["HANDOFF6_RUN"]` and opens `cw.py` in that directory. **`cw.py` was never committed** (`git log --all --diff-filter=A --name-only` finds no such path on any head), so the prime-tower derivation (40123 + 40639, rational reconstruction) is reproducible only from an uncommitted run directory. `tests/test_b919_traces.py` compares the strings in `results.json` — a drift lock on the stored output, not a recomputation.

**The three integers now recompute from committed material.** `frontier/B1237_physics_seat_r31_r38_harvest/verification/traces_from_b1236.py` takes B1236's committed `content_internal(F(−1,3), F(1,2), F(0))` — the SM-shaped 27 as a multiset of (colour, weak, Y) irreps — and sums over the multiplets with T₃ ∈ {±½} on doublets:

    Tr(T₃²) = 3   Tr(Y²) = 5   Tr(T₃·Y) = 0   ⟹   Tr(T₃²)/(Tr(T₃²)+Tr(Y²)) = 3/8

This is a **different derivation of the same integers** (from the exact multiplet content on the A₁ stratum), not a second prime of the original tower. The verdict stands; the THEOREM_LEDGER's C42 row now points at this witness beside the historical lock. Gate 5 unchanged: 3/8 is a trace-orthogonality ratio at the object's normalization, not a physics value.
