# B921 — the branch harvest, stage 1: the load-bearing corpus is home; four seals verified; thirty obligations routed

**Date:** 2026-08-06 · **Seat:** cc (banking) + computation agent · **Source:**
`origin/audit/b775-braver-questions` @ `d8f95511`.

## What came home

All 30 load-bearing files (cc3's Part-B ranking), branch paths preserved under
`harvested/`, every copy sized and non-empty:

- **The sealed prereg chain** — all four SEAL_LEDGER hashes verified as FULL
  sha256 matches (8424a335, da516046, 3ba81779, 169e9042); sealed copies
  byte-identical; the chain's internal citations close.
- **The Wave-1 negatives** — the Hecke gate's designed ABORT (the structured
  zero at π₇; the no-level-1-lift refutation: r_K = 2r_Q puts the first lift
  at 19.067 > the 13.5 ceiling) and the spin fork (ρ₁ non-Lie under both
  conventions ⟹ discrete Dirac spectrum ⟹ **the spinor-Hejhal is authorized
  unconditionally** — B804's missing Cell 3, now on main).
- **The three load-bearing second-round reports** — the BC/CM ℚ(√−3)
  groundwork (L113's input sheet), the Born-content territory (L120), the
  novelty corrections (F_K(4₁) EXISTS — Gukov–Manolescu 1904.06057; the
  Ẑ^G result — Park 1909.13002; the Bianchi-IX name-collision flag).
- **The context sweep** and the §16 verdict chain (the corank-2 no-go that
  binds rung i-b).
- **The generators** whose outputs main had banked without their scripts.

31 machine-path occurrences scrubbed in 7 non-sealed copies (per-file log in
the manifest); the 4 sealed files needed none and byte-match their seals.

## The registration-over-preservation rule, executed

`HARVEST_MANIFEST.md` §4 enumerates the corpus's 30 carried-forward items;
at banking they were ROUTED into OPEN_LEADS (4 marked covered by
already-banked work, 26 opened as B921-n rows). The failure shape the rule
kills — sealed artifacts whose inner obligations die — has no instance left
in this corpus.

## Standing

The branch-death risk is ENDED for the load-bearing corpus; stage 2 (the
full relay/scan/log corpus, ~148 remaining files) is optional and priced.
The branch remains protected until a stage-2 decision is logged.

## Files

`harvested/` (30 files) · `HARVEST_MANIFEST.md` · locks:
`tests/test_b921_harvest.py`.
