All checks are complete — prereg, script, artifacts, seal ledger, eigenvalue provenance, empirical API probes, and timing measurements. Here is the full second-pass verdict.

---

# §16 FACTUAL REVIEW VERDICT — CELL 9 RUNG (i) PREREG v2 (seal 3ba81779), SECOND PASS

Reviewer: non-authoring §16 seat, 2026-07-29. Seal verified: sha256 of `[machine-path]/frontier/B796_coupling_campaign/CELL9_RUNG1_PREREGISTRATION_v2.md` = 3ba81779... matches SEAL_LEDGER.md:442. All numerical claims below recomputed independently; all API claims tested against the installed python-flint 0.9.0.

## VERDICT: STOP

A much narrower STOP than the first pass. RC-2 and RC-3 are genuinely discharged, RC-1's core formula is sound and does repair Defect 1 — but (A) the conformant execution script cannot execute even one Newton iteration (empirically reproduced crash), (B) the sealed lam_1 mult-2 protocol is provably unsound at exactly the structure the seal itself asserts for lam_1, and (C) the sealed power box contradicts itself internally. (B) and (C) are defects in sealed text and force a v3 re-seal regardless of code fixes.

## RC-BY-RC

- **RC-1 (tolerance formula): formula SOUND, sealed text internally INCONSISTENT.** Verified on all six sealed (box, target) combinations: under max-normalization the true-relation residual floor is height-independent and ≤ (terms)·max_i(|v_i'|/|v_i|)·|dr|_stab; because every sealed box is monomial-structured, the formula's noise (max|v'|/max|v|·|dr|_stab) equals the worst per-term ratio exactly, so tol = 10× covers the ≤5-term worst case with 2× margin. No box is wrongly licensed or wrongly killed. **But the recomputed parent λ-minpoly example contradicts the seal's own note**: noise = 7.511e6·1e-26/6.772e6 = 1.11e-26, tol = 1.11e-25; dynamic range 6.83 digits, N_eff = 18.17, H_max = 10^(18.17/(1.43·d)) = 348 (d = terms = 5) or 1502→capped 1000 (d = degree = 4). Both are ≥ 10, so the formula declares the box **POWERED** — the sealed note "expected to be UNPOWERED at 25 digits... the honest outcome" is false under its own formula (unpowered would require dynamic range > 17.8 digits). The formula is right and the note is wrong: normalization removes the H-amplification that killed this box in v1. Two further sealed-text defects: the sentence "a relation is detectable only where H × noise < tol_box headroom" is a raw-convention leftover that literally implies H < 10 for every box, contradicting the H_max formula two lines below; and "d" is never defined (degree vs term count — a factor 2.9 in the banked H for the parent λ-minpoly). `cell9_pslq.py` does not exist, so no code disambiguates. STOP-grade for the PSLQ stage as sealed.
- **RC-2 (displaced control): DISCHARGED.** Midpoints verified: lam_1 (3.93891686 + 4.90008537)/2 = 4.4195 → 4.42; lam_2 (4.90008537 + 5.67072003)/2 = 5.2854 → 5.29; parent's nearest neighbor is ABOVE at r = 7.34952664 (scanD_refined.json, sector_projection_results, WAVE1_PREREGISTRATION.md:53 — just outside eigenvalues_final.json's (0.8, 7.35) refinement window; acceptable pedigree for a control start), gap 0.27752 matching v1's 0.2775, midpoint 7.2108 → 7.21. The control logic is now sound both ways: a target-parroting/attractor-broken pipeline converges back within 1e-20 and fails; a correct pipeline passes via convergence-to-neighbor or divergence (Newton from a gap midpoint, near a det extremum, overshoots — return to target is unlikely and fails safe as an investigable abort). One transfer to RC-5: the code crashes instead of passing on the divergence branch (below).
- **RC-3 (Y validation): DISCHARGED.** The banked artifact validates Y = 0.75: 200/200 rise, min t* = 0.868744, margin 0.118744 ≥ 0.05 — matches the sealed 0.1187, and the Y = 0.75 row is independently corroborated by groundwork.txt:26 (min t* = 0.8687). v2 uses 0.75; Y = 0.80 (margin 0.050327, a 0.65% pass of the 0.05 assert) is correctly called knife-edge and not used. Minor: `y080_rise_validation.txt` has no generating script anywhere in the repo (and the repo is not under git) — add one provenance line at re-seal; not stopping since the load-bearing row is doubly attested.
- **RC-4 (mult-2 protocol): PARTIALLY discharged; NEW STOP-GRADE DEFECT for lam_1 only.** See Defect B.
- **RC-5 (code conformance): NOT DISCHARGED.** See Defect A.

## DEFECT A (execution vehicle, blocking): the script crashes on the first Newton iteration of every target.

`cell9_rung1_v2.py` lines 227–236: the initial-eigenvector solve builds `M = flint.acb_mat(S.n+1, S.n+1)` and fills only columns 0..n−1 (n collocation rows plus the normalization row); column n is never assigned and stays exactly zero, so M is exactly singular. **Empirically reproduced with the identical construction under python-flint 0.9.0: `ZeroDivisionError: singular matrix in solve()`.** The script has never survived a smoke test; RC-5 cannot be verified against a script that implements nothing. Conformance gaps to fix in the same pass: (1) P3's sealed "divergence = control passes" branch is unimplemented — a wandering displaced iterate trips the in-loop P2 assert (line 265–266) or a singular solve and kills the whole run with an exception, after the ~10-hour main+P4 stages have already been paid for; (2) P2 as coded aborts only on >1.5× growth after a two-iteration grace, but the seal says a non-decreasing step aborts; (3) the mult-2 second row is chosen as the global second-largest |a| (line 335) with no implementation of the sealed factor-10-of-|a_j0| rule (and |a_j0| = 1 by normalization, so the rule and the code can disagree); (4) `K_table` is dead code. API risks all CLEARED by direct test on python-flint 0.9.0: `arb.str(n, radius=False)` works; `arb(fmpq(9,12))` works; `acb_mat(m,n)` zero-constructor and `acb_mat.solve` exist; and the Bessel orientation `flint.acb(x).bessel_k(i·r)` = K_{ir}(x) is CORRECT (relative deviation 4.9e-34 vs mpmath; the swapped orientation is off by 3 orders of magnitude and P1 would catch it). Sealed n approximations verified by recomputation: 2291/2393/2632 vs sealed 2290/2390/2630; x_cut, mode norm m1²+m2²/12, and h = 1e-9 (central-difference cancellation leaves M' good to ~26 digits — harmless for quasi-Newton) all check out.

## DEFECT B (sealed text, lam_1 only): the joint Newton is provably singular at exact multiplicity 2, and both pre-declared RC-4 outcomes mis-bank.

**The bordered Jacobian [[M, M'a],[e_j0, 0]] is singular whenever dim null(M(r*)) = 2, for every choice of j0**: the subspace {w ∈ null(M) : w_{j0} = 0} has dimension ≥ 2−1 = 1, and any nonzero w in it gives the null vector (w, 0). (At the simple targets lam_2 and parent — sv_tail in eigenvalues_final.json confirms nullity 1 — the bordering is the standard nonsingular one; they are unaffected.) Consequences at lam_1, in the seal's own asserted world (header: "mult 2"): if the truncated system inherits the degeneracy, arb's certified solve hits a provably singular matrix and raises (crash, no sealed outcome); generically the pseudo-random collocation breaks it, the double det-root splits by O(√ε_trunc) ≈ 1e-13–1e-14 at 27 digits, Newton converges to ONE sheet — correct to only ~13–14 digits — and the +5-digit cert moves the sheet (split rescales as √ε), so **|dr|_stab ≈ 1e-14 ≫ 1e-26 and the sealed proceed-requirement fails deterministically after ~20 hours of compute — an outcome the seal does not pre-declare.** Meanwhile both RC-4 outcomes condition only on the fixed-truncation dual-normalization split and mis-bank in that world: same sheet twice → "a single well-defined parameter (report it)" banks a value wrong at digit 14 as 25-digit lam_1; different sheets → banks a pure truncation artifact as "a FINDING about the multiplicity structure" — exactly the v1 Defect-4(b) mechanism, now with a sealed blessing. Note also that v1's required σ₂-of-the-collocation-matrix assert — the one check that would discriminate mult-2 from simple before interpreting the pair — was silently replaced by the rerun; RC-4(i)'s "assert near-nullity 2" asserts nothing. A prereg whose lam_1 procedure works only if the prereg's own multiplicity claim is false is not launchable for that target. Fix at re-seal: a genuine corank-2 bordering (two normalization rows / deflated system), or pre-declared cert-failure semantics plus the σ₂ assert, or defer lam_1 to its own rung.

## DEFECT C (sealed text, PSLQ stage): power-box internal inconsistency (detailed under RC-1).

Note contradicts formula; the detectability sentence contradicts both; d undefined (factor 2.9 in banked H); no PSLQ code exists to disambiguate. Text-only fix, but it is in the sealed file and the re-seal is happening anyway.

## FINDING (must be stated before launch, not stopping): the runtime claim does not survive.

Measured on this machine: 70.7–98.6 μs per acb bessel_k at prec 123 (148 μs at prec 140). As coded, each Newton iteration does 6n² Bessel calls (3 matrix builds × 2 calls per entry; the j-independent k0 is recomputed n times — the dead `K_table` was evidently meant to fix this). Parent (n = 2632): ~41.6M calls ≈ one hour per iteration; the full sealed protocol (main ~4 iterations + P4's two extra runs + P3 + stability cert at n ≈ 3524 and prec 140 at ~3 h/iteration) totals **≈ 21–26 hours; lam_1 ≈ 18–21 h including the mult-2 rerun; lam_2 ≈ 20 h — roughly 60–70 hours serial for the campaign, i.e. "a day per eigenvalue", not "hours per eigenvalue."** Caching k0 (trivial, one dict) halves it to ~10–13 h per eigenvalue — still tens of hours. Peak memory ~8–12 GB at the cert stage (phase tables + three row lists + two (n+1)² acb matrices). Also operational: the script's relative `sys.path` insert and OUT path require running from the repo root.

## VERIFIED CLEAN (credit, so v3 does not relitigate)

Seal hash and ledger entry; target list and certified starting values; gap midpoints including the 7.34952664 upper neighbor; Y = 0.75 decision and artifact numbers; the max-normalized convention and 10×-noise-floor tolerance formula on every sealed box; runtime-licensed H_max machinery (modulo defining d); sealed n values; Bessel orientation; every questioned python-flint 0.9 API; gate digit-overlap logic on all three certified strings; P4 threshold semantics; the +5-digit cert construction.

## REQUIRED CHANGES BEFORE RE-SEAL (v3)

1. Fix the initial-eigenvector solve (the zero column) and smoke-test the script end-to-end at small n before any launch claim of conformance.
2. Implement P3's divergence-passes branch (try/except around the displaced run), the seal's strict P2, and the sealed j1 rule — or re-seal the leniencies explicitly.
3. lam_1: replace the single-row bordering with a corank-2-capable procedure (or defer lam_1), add the σ₂ nullity assert, and pre-declare the stability-cert-failure outcome; keep the pair outcome but condition its "structure finding" label on split stability across the two truncations.
4. Power box: delete or repair the "H × noise < tol_box headroom" sentence, correct the parent λ-minpoly note (it is POWERED, H_max ≈ 348–1000), and define d (degree vs terms).
5. State the honest runtime (~a day per eigenvalue as coded; ~half with the k0 cache) and the ~10 GB cert-stage memory in the prereg before launch; add a one-line provenance note for y080_rise_validation.txt.

Files: `[machine-path]/frontier/B796_coupling_campaign/CELL9_RUNG1_PREREGISTRATION_v2.md`, `[machine-path]/frontier/B796_coupling_campaign/cell9_rung1_v2.py` (bug at lines 227–236; P2 at 265–266; P3 at 307–311; j1 at 334–336), `[machine-path]/frontier/B796_coupling_campaign/y080_rise_validation.txt`, `[machine-path]/frontier/B792_maass_m004_eigenvalues/eigenvalues_final.json`, `[machine-path]/frontier/B792_maass_m004_eigenvalues/scanD_refined.json`.