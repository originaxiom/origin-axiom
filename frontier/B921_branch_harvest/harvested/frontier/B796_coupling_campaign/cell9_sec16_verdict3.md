All evidence is in. Both targets are certified nullity 1 (lam_2: sv-tail 1.7e-10 vs σ₃ = 0.22; parent: 4.4e-10 vs σ₃ = 0.047), the 7.3495 neighbor is banked (FINDINGS.md:121, scanD_refined.json, mode_count_certification.json — merely absent from eigenvalues_final.json whose window ended at it), and the seals verify. The verdict follows.

---

# §16 FACTUAL REVIEW VERDICT — CELL 9 RUNG (i) PREREG v3, THIRD PASS (delta review)

Reviewer: non-authoring §16 seat, third pass, 2026-07-29. Seal verified: sha256 of `frontier/B796_coupling_campaign/CELL9_RUNG1_PREREGISTRATION_v3.md` = 169e9042... matches SEAL_LEDGER.md:443. v2 = 3ba81779 ✓ ledger:442. Seal convention (algorithm named, digest in ledger only) ✓.

## VERDICT: PASS-WITH-CONDITIONS

All five second-pass required changes are substantively applied in the sealed text, and every sealed number I could recompute checks out. The remaining defects are in the unsealed execution vehicle and in provenance (plus one inert false sentence in D-1, voided below) — none require touching sealed bytes, all have one-line fixes, and §16 holds execution until C2–C7 are done.

## FINDINGS BY CHARGE

**1. Method and bracket — SOUND for the two run targets; one sealed sentence false at mult 2.**
The regular n×n mechanism holds at nullity 1: M = [n−1 collocation rows; e_j0] is singular at r* iff a null vector of the collocation rows vanishes at coordinate j0; both targets are certified nullity 1 by sv_tail (lam_2: one singular value at 1.7e-10, next at 2.2e-1; parent: 4.4e-10 vs 4.7e-2), so with generic a*[j0] ≠ 0 the matrix is invertible at the root — Defect-A's bordered-singularity mechanism is genuinely gone, and the ~1e27 conditioning from truncation decay is absorbed by the +45 guard digits (239 bits), which the code comment correctly acknowledges. Defect-B (sheet selection) is inapplicable at nullity 1 and left with lam_1. **However, the sealed clause "and it remains regular at multiplicity 2" is FALSE**: any 2-dimensional null space contains a vector with v[j0] = 0, so with a single normalization row M(r*) is singular at mult 2 — the clause also contradicts D-3's own corank-2 requirement. Not load-bearing for rung (i); voided on the record (C1).
Bracket safety verified against the banked corpus: lam_2's nearest neighbors are 5.6707 (0.7706) and 3.9389 (0.9612); the parent's are 7.34952664 (0.2775 — banked in FINDINGS.md:121/scanD_refined.json, absent only from eigenvalues_final.json) and 6.6328 (0.4392). Brackets ±0.01 have ≥ 27× margin; no different eigenvalue can enter either bracket. Displaced midpoints recompute exactly (5.2854→5.29, 7.2108→7.21) and their ±0.01 brackets are ≥ 0.13 from any banked eigenvalue. Advisory: j0=0 normalizes a truncation-boundary mode whose true coefficient could be accidentally small — the failure mode is a clean certification abort, not a wrong value, but prefer a low mode or a post-solve ‖a‖∞ bound (C8).

**2. lam_1 deferral — clean in the sealed text, NOT in the script.** D-3 removes lam_1 from run targets with nothing banking; the only text residue is the inert 4.42 midpoint in D-2. But `cell9_rung1_v2.py` retains lam_1 in CERT/GAP_MIDPOINTS **and retains the v2 `--mult2` pair protocol that v3 just deferred as unsafe** — invoking it on 3.938916864 would emit a bankable-looking JSON stamped with the superseded v2 seal. Per-target invocation is not a guard; a guard is (C5).

**3. Power box — repairs verified.** The contradictory raw-convention sentence is declared deleted (the delta form leaves the em-dash byte-range slightly ambiguous, but the licensed-H formula is on the carried-clean list — acceptable). d is defined (entries; conservative vs the degree convention, which is logged). The POWERED correction reproduces independently: Δλ = 2rΔr = 1.41e-25, Δ(λ⁴) = 4λ³Δλ = 7.5e-20, max-normalized by λ⁴ = 6.77e6 → noise 1.1e-26, tol 1.1e-25, N_eff = 25 − 6.83 = 18.17, H_max = 10^(18.17/7.15) ≈ 348 (d=5), capped 10³ (d=4). All four D-4 numbers match.

**4. Guards in code — correctly implemented; two new bugs found.** Bracket (centered on the run's own start, so P3 gets its own bracket automatically), 1e-3 damping with log, and the P3 try/except (re-raises only 'P3 FAILED'; ZeroDivisionError = singular-solve pass) are all right. **Strict P2 is safe as coded — no relaxation needed**: hist holds [|g(r0)|, |g(r1)|, |g(r2)|, …] and the first enforced comparison (it=2) is |g(r3)| < |g(r2)| — the exempt window exactly covers the artificial +1e-9 offset transient (the only legitimate |g| increase); the noise-floor plateau cannot wrongly kill because the 45 guard digits put the g-evaluation floor ~40 digits below the last |g| evaluated before the |dr| < 1e-27 break fires (error sequence from a certified start: 5e-10, 1.5e-9 → 2e-18 → ~1e-26 → ~1e-44), and the cert-stage restart converges inside the exempt window. New bugs: **(a)** the sealed "14 iterations (abort if not converged)" is not implemented — the loop falls through after itmax and an unconverged r would proceed to the gate (C2); **(b)** SHAKEDOWN cannot contaminate stdout (defaults off, banner present, thresholds scaled) **but writes the identical JSON path as a real run with 'digits': 27 hardcoded and no shakedown field** — the persistent artifact is unlabeled (C3). Also: the prereg stamp in banner and JSON is the superseded 3ba81779 (C4), the module docstring still describes the v2 joint Newton, and a_last lags r_last by one iteration (immaterial). The mandated smoke test is not evidenced: cell9_shakedown_log.txt stops after "n = 782 modes" — no completed end-to-end run exists on disk (C7).

**5. Runtime claim — survives, with one unmeasured term.** n²+n = 5.73e6 bessel calls/iteration at 70–99 μs = 401–567 s ≈ **6.7–9.5 min/iteration** (lam_2; parent ~20% more). Budget ≈ 22–27 n²-equivalent iterations (main 4–5, P4 2×4–5, P3 exactly 5 — with CAP 1e-3 the displaced run cannot exit its 0.01 bracket in 5 iterations, so it ends far from target and passes via the ddist branch — cert 2–3 at 1.8× cost) → Bessel budget ≈ 2.5–4.5 h/eigenvalue; D-5's internal arithmetic (one day uncached /2 cache /3 one-build) is consistent. The 4–8 h band therefore holds **iff** the unmeasured per-iteration n×n acb LU (~4.6e9 acb multiplications at 239 bits) stays under ~10 min; D-5's per-iteration logging makes this auditable in the first hour — add the explicit checkpoint (C8). Memory ~10 GB is plausible (phase tables ≈ 3.5–6 GB at cert n≈3500 plus matrix and LU workspace; may brush 12 GB at parent cert — not stopping).

**6. Other.** The sealed v3 cites "Second-pass verdict: cell9_sec16_verdict2.md" — **that file exists nowhere in the repo**; a sealed citation to a nonexistent provenance document, with the second pass's required changes otherwise living only in session memory (C6). Y=0.75 provenance is now fully repaired: y080_rise_validation.txt matches v2's quoted numbers (0.75: 200/200, min t* 0.868744, margin 0.1187; 0.80: margin 0.0503, rejected), cell9_rise_check.py's imports all exist in hejhal_m004.py, and the code uses Y=0.75 — first-pass Defect 3 closed. Minor: v3's preamble says "targets … carried unchanged" while D-3 removes lam_1 — D-3 governs.

## CONDITIONS (execution blocked until C2–C7 done; no re-seal required)

- **C1.** The D-1 clause "and it remains regular at multiplicity 2" is VOIDED by this verdict (false at corank 2); binding on the rung (i-b) prereg, which must not inherit it.
- **C2.** Add the non-convergence abort after itmax in `newton()` (the sealed text promises it; the code returns silently).
- **C3.** Make shakedown artifacts self-labeling: distinct JSON filename plus shakedown flag and actual DIGITS in the result dict.
- **C4.** Correct the prereg stamp in banner and JSON from 3ba81779 to 169e9042; fix the stale v2 module docstring.
- **C5.** Guard lam_1 per D-3: refuse r = 3.938916864 and remove or gate the `--mult2` v2 pair-protocol branch.
- **C6.** Write the second-pass verdict to disk at the cited path `cell9_sec16_verdict2.md` (makes the sealed citation true).
- **C7.** After C2–C5, run the shakedown END-TO-END (through P4, P3, gate, cert, labeled JSON) and bank the complete log — the current log proves only P1 + system build.
- **C8 (advisory, non-blocking).** Normalize on a low-frequency mode or assert a post-solve ‖a‖∞ bound; add an iteration-1 wall-clock checkpoint (> ~25 min → revise the 4–8 h estimate before committing the second target).

Everything else on the delta — the secant-on-held-out-row method at nullity 1, both main and displaced brackets against certified spacings, the P3 pass/fail semantics, the deferral text, all recomputed power-box numbers, the P2 design, and the runtime/memory honesty — checks out.