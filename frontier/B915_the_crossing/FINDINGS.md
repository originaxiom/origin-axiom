# B915 — THE CROSSING (R4): the sealed verdict is MISS — the desert dies, the structure stands, and the failure geometry points at the object's own ladder

**Date:** 2026-08-05 · **Seat:** cc (banking) · **Prereg:** sealed `7a423aed…`,
committed `4c4531a6` BEFORE any data contact; seal verified at banking ·
**Owner authorization:** "moment has come."

## The verdict, verbatim against the sealed criteria

With the single input α_em(M_Z) and the object's boundary (E₆ unification,
the banked 3/8), the two-loop desert curve's nearest approach to the measured
pair (sin²θ_W, α_s)(M_Z) is

> **d = 15.97 σ_tot (Euclidean reading; 14.63 max-component) — OUTCOME MISS.**

Killed, exactly as pre-stated: **"the object's boundary + pure SM desert"** as
a physics identification. Standing, exactly as pre-stated: every structural
theorem (FMT/SMT, M(𝕆,ℂ), the classes, the atoms, T, I = −1, e₆(2), the
signature split, 3/8 as structure).

## The disclosed prior WON — the first time in five sealed cells

The prior (MISS, high confidence, for the literature-standard reason) was
correct. The seal discipline's scoreboard: the object overruled us four times
when we guessed structure; the one time we bet with the textbook against the
naive identification, the bet held. Both directions of the discipline work.

## The banked failure geometry (the deliverable)

- Nearest curve point: M_U = 1.54×10¹³ GeV, sin²θ_W = 0.22925 (gap +0.00197),
  α_s = 0.0766 (gap **+0.0414 — the miss is α_s-dominated**).
- The pairwise meeting scales from the measured point (two-loop):
  **g₁=g₂ at 1.09×10¹³ · g₁=g₃ at 1.72×10¹⁴ · g₂=g₃ at 2.91×10¹⁶ GeV** —
  a three-order triangle.
- Truncation band at nearest approach (1↔2-loop): σ_th = (0.0018, 0.0028) —
  the miss survives any honest inflation of the band by far.

## The registered continuation (its own seal, not this cell)

**R4b:** the object's compact-measurement ladder E₆ ⊃ D₅ ⊃ D₄ ⊃ D₃ ⊃ A₂
(the CMT lane, B909/B911) as the desert's replacement: intermediate
thresholds at the chain's OWN banked scales, ZERO new free parameters. The
sharp test: whether the chain's correction pattern closes THIS cell's
triangle and turns the α_s-dominated gap. A second crossing, harder than the
first — as it should be.

## Files

`PREREGISTRATION.md` (sealed) · `crossing.py` → `results.json` (the curve,
both loops, the distances, the geometry) · locks in
`tests/test_b915_crossing.py`.

## ANNOTATION (2026-08-05, the cc3-audit A1 repair)

The boundary value sin²θ_W(M_U) = 3/8 consumed by this cell's construction now
has its standalone derivation arc and lock: **B919** (the exact traces (3, 5, 0)
at the full-tower prime, three independent runs, rational reconstruction;
tier ONE-PRIME with the second prime open-diagnosed). At sealing time the value
was verified-but-unlocked — cc3's audit flagged it, and the gap is closed at
the honest tier.
