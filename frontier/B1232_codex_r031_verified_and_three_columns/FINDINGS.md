# B1232 — CODEX R031A/R031B VERIFIED, THREE RETRACTIONS, AND A THIRD COLUMN

cc banking seat, 2026-09-01. Owner: *"verify everything, trust nothing."* So codex's certs were
**re-run on this bench** (their seat is on this machine) rather than cited, and the positive lemma was
**re-derived independently** with its own controls. **Gate 5 absolute.**

## Retraction 1 — σ ∈ ℚ establishes nothing. **ℚ is dense in ℝ.**

B1229 called RCFT rationality its *"robust core"* and repeatedly wrote *"not a continuum."*
**A dense set is not a discrete one.** Rationality excludes nothing.

Codex's `r031b` cert, re-run here: *"rationality alone permits arbitrarily large finite subsets of
(0,1) and rational midpoints"* — **PASS**.

Every actual reduction came from MMS's ℓ = 0 two-character class, which the citation check already
showed is class-scoped and unproved for this boundary. **The core was not weakly grounded — it was
empty.** A plain logical error, not a scoping subtlety.

## Retraction 2 — "no receiver for k, therefore k = 1" is a default from absence

B1228's *level-forcing by inventory* was the one piece I said survived that arc's retraction. It does
not. **Codex's law, adopted:**

> **Absence of a typed receiver means either quotient-invariance or underdetermination. It never
> means a default value.**

Level blindness is **underdetermination**.

## Retraction 3 — C-2 resolves to **Outcome A**, and my "dimension tell" was the very error B1231 names

`r031a`, re-run here: `B_0 generator exponents = (0,0,0,0)`; C12 acts by **I₄ = ζ₁₂⁰·I₄**; after base
change **B₀ = K⁴, ℙ(B₀) = ℙ³_K**; **K-rank 4, not 1**. Controls bite — a primitive scalar would give
orbit (1,5,7,11) and charpoly Φ₁₂; neither occurs.

**My error, exactly:** the identity *is* a scalar — but the scalar is **1**, so B₀ is a module over
ℚ[ζ₁₂]/(ζ₁₂−1) = **ℚ**, not ℚ(ζ₁₂). I matched `dim 4` with `φ(12) = 4` and inferred a module
structure from two numbers agreeing. **That is the identification error B1231 exists to catch,
committed inside the analysis that motivated the discipline.** The ℙ³ is genuinely ℙ³_ℂ.

## Correction 4 — I-7 was worse than "unearned"

Verified: **Gal(ℚ(ζ₃)/ℚ) has order 2**, not 3. C-5b described *"the object's own ℤ/3 (trace field
ℚ(√−3) = ℚ(ζ₃); the trinification)"* — listing the trace field as a source of ℤ/3 when its Galois
group is **ℤ/2**. Codex: *"no arithmetic identification maps trace-field, trinification and fusion
ℤ/3 data"* — **three** distinct objects, no maps. Part of I-7 **conflated a ℤ/2 with a ℤ/3**: a
factual error, not merely a missing map.

**What reproduced:** my c = 6 four-solution scan is confirmed independently — but it is **WZW-class
exhaustive, not RCFT exhaustive**, and a rational-c family already exceeds it at **size 21**.

## The positive — the third column, and it is the important part

> *"No canonical Higgs line" does not imply "three physical Higgs parameters."*
> *The project has been treating a space of representatives as a space of physical parameters.*

**The lemma, re-derived here with MB12 bite.** Given `0 → C → V → T → 0` with dims **(3, 4, 1)**, if a
coupling Y annihilates C then Y factors uniquely through T:

| | spread of the observable over 2000 random splittings |
|---|---|
| Y annihilates C | **0.000e+00** — the ℙ³ choice is invisible |
| Y generic | **4.83** — the choice is visible; the test discriminates |

**Three coordinates, zero observable parameters.**

**The closure test, adopted:** (1) identify the raw choice space; (2) identify allowed field
redefinitions; (3) map choices to normalized invariant observables; (4) **count the image, not the
source.**

### This corrects B1226's typing

My four boxes sorted by **type** and never asked whether a parameter survives quotienting by field
redefinitions. There are **three columns**, not two categories:

**forced structure** · **genuine observer-supplied values** · **representational choices that
disappear from observables**

The repo conflates the last two.

### And it routes around B1225 without contradicting it

B1225 proves no invariant can **select** a point of ℙ³. Quotient invariance says **selection is never
needed** — the observable does not depend on the choice. Complementary, not competing, and **the
first closure mechanism proposed that does not require the naming wall to fall.**

## Not verified here — fenced

The lepton connecting block vanishing at three good primes, and the (3,4,1) sequence itself, are
codex's exact computations, **still running**. This arc verifies the **algebra** and the two
retrieval certs. It does **not** verify that the actual couplings annihilate the actual connecting
block. The live fork is codex's: either that nine-entry tail/connecting block vanishes — and the ℙ³
is a splitting artifact for both holomorphic Yukawas — or it does not, and those nine entries are the
exact obstruction.

## Reproduce

`sh frontier/B1232_codex_r031_verified_and_three_columns/rerun_codex_certs.sh` (re-runs codex's
R031A/R031B on this bench) · `python3 .../verify_quotient_lemma.py` (the independent re-derivation).
