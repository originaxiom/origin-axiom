# B258 — ADDENDUM: the m = 2 parenthetical is wrong (silver IS arithmetic); H27's split stands

*Addendum written 2026-09-02 under **B1237** (the physics-seat harvest, second ring). Source of the flag: the physics-seat evaluation branch @ d415423a; every number below recomputed on this bench — `frontier/B1237_physics_seat_r31_r38_harvest/verification/`. The arc's verdict file carries the same correction in its `note`.*

FINDINGS.md:15–16: *"SnapPy `find_field` gives the trace-field degrees: 4₁ → 2 (ℚ(√−3)), silver → 8, bronze → 8 (non-arithmetic)."*

The degrees are right (`test_b258` pins {1: 2, 2: 8, 3: 8}). The parenthetical **infers non-arithmeticity from the trace-field degree**, which is not a criterion: the *invariant* trace field of m136 is **ℚ(i)** (all 350 computed traces of squares are Gaussian integers), and Vol(m136) = 4G = 12 × covol(PSL₂ℤ[i]) to 50 digits — **silver is arithmetic** (B125/B147/B137/B850/B337 already said so). Bronze's status is unchanged here (not recomputed).

**H27's resolution is unaffected**: the trace field ℚ(√−3) is figure-eight-specific and the discriminant field ℚ(√(m²+4)) is metallic, coinciding only at m = 1 — a statement about fields and degrees, not about arithmeticity. The two-ended picture (E₆/ℚ(√−3) ↔ E₈/ℚ(√5)) stands as banked.

Error class **E55** instance #2 (source site; B1062 inherited it). Kill graph: `B258-silver-inference`.
