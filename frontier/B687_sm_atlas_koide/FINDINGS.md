# B687 — THE SM ATLAS + h(27) = 2/3: the Eisenstein-face turn's first
# result (main-seat integration of cc2's exploratory day, 2026-07-18;
# verified-on-receipt). The MOST-MOTIVATED sealed-question candidate —
# and NOT evidence. Gate 5 stands; E1/E2 running.

## THE SM ATLAS (cc2 A1; the honest triage)

Of 23 dimensionless SM invariants: 14 UNREACHABLE (α, the running
quantities — no topological structure can reach them), 8 DEAD
(every mixing angle — base-rate too high: sin²θ₁₂ has ~20 golden
values in its band), 1 LIVE: **Koide Q**. Exactly one target where a
sealed test could ever be informative. Convergent with B686 (the
mixing angles are base-rate-dead; Koide is the one low-base-rate place).

## THE KOIDE SPLIT (cc2; main-seat verified)

Q = 0.6666605 splits into a scale-free CONSTRAINT and the mass VALUES:
- **CONSTRAINT (object-addressable):** Q = 2/3 ⟺ the vector
  (√m_e,√m_μ,√m_τ) sits at 45.000° to the diagonal (verified: 44.9997°)
  — a pure Z/3 / 120° constraint with NO scale in it. This is the
  low-base-rate content (2/3 is the only distinguished value in Koide's
  tight band).
- **VALUES (firewalled):** the actual masses live in a separate phase
  δ ≈ 132.7° — non-topological, running, NOT object-addressable. Stays
  behind the firewall (as θ₀ did, B686).
This IS the observer-coupling thesis on a real SM observable: the object
could at most supply the CONSTRAINT (the 120°/ω structure), never the
mass VALUES.

## THE h(27) = 2/3 CLUE (cc2 A1; main-seat verified)

The object's own distinguished quantity nearest Koide's Q is
**h(27) = 2/3** — the conformal weight of the 27 of E₆ at level 1
(c(E₆,1) = 6, h(27) = C₂(27)/26 = (52/3)/26 = 2/3; VERIFIED). A
root-system fact of the object's OWN stage, not fitted. And the three
generations live in H*(D; 27) — the rep whose conformal weight is 2/3,
matching Koide's three-generation constraint 2/3. Sitting at 0.91σ (Q
consistent with exactly 2/3).

## THE FIREWALL (why this is the most seductive thing we've hit, and
## still NOT evidence)

Two simple rationals both = 2/3 is a COINCIDENCE until a derivation
FORCES the identity. And B686 makes the caution sharper: Q = 2/3 is
120°-TAUTOLOGICAL (holds for any three-fold structure), so BOTH 2/3's
could be nothing but "three generations ⇒ Z/3 ⇒ 2/3" — generic, not
forced. The object distinguishes several simple rationals; numerical
equality is cheap. Gate 5: NOT banked as physics; no LAW_MAP row.

## THE TEST (E1, running; the honest sealed-question candidate)

Does the being sector's Z/3 / ω-structure (the generation sum rule)
FORCE "the three-generation constraint = h(27), the conformal weight of
the rep the generations live in" as a THEOREM — or is the shared 120°
merely "both have three generations"? FORCED (non-generic) ⇒ a real,
owner-gateable, beautifully-typed sealed-question (a dimensionless
constraint, the one low-base-rate target). GENERIC ⇒ bank the kill, the
two 2/3's were a coincidence. E2 (the level-15 handshake — does the
coupling factor or genuinely entangle 3 and 5) runs alongside. Either
end is honest.

## CONVERGENCE

Both seats independently reached the same frame: the constraint (2/3)
is object-addressable, the values (δ) are coupling/firewalled, the
forced-derivation is the test (cc2 KOIDE_SPLIT + A1; main-seat B686 +
S067 two-hands + the observer-coupling thesis). The Eisenstein-face
turn (S067, this seat's directive + cc2's masterplan) paid off on the
first probe — with the discipline attached.

Locks: tests/test_b687_koide_split.py.
