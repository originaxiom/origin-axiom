# THE KOIDE SPLIT (cc2 main seat, 2026-07-18; exploratory, firewall-
# clean: a structural fact about the SM's Koide relation + base-rate
# calibration; NO object=SM claim)

FINDING: Koide's relation FACTORIZES into a topological-shaped part
and a non-topological part, and only the first is object-addressable.
1. Q = (Sum m)/(Sum sqrt m)^2 = 0.6666605 vs 2/3 = 0.6666667 (1 part
   in 1e5). Geometrically EXACT: Q = 2/3 <=> the vector (sqrt m_e,
   sqrt m_mu, sqrt m_tau) sits at 45.000 deg to the diagonal (1,1,1)
   -- a pure Z/3 / 120deg constraint with NO scale in it. This is the
   LOW-base-rate part: 2/3 is the only distinguished value in Koide's
   tight band.
2. The ACTUAL masses are encoded in a separate phase delta = 132.73
   deg (sqrt m_k = M(1 + sqrt2 cos(delta + 2pi k/3))). delta is HIGH
   base rate (2 distinguished angles within 2deg; nearest 131.81, 0.9
   deg off) -- NOT distinguished, NOT topological.
CONSEQUENCE (the thesis, made concrete on a real SM observable): the
object could at most supply the CONSTRAINT (Q = 2/3, the 120deg / omega
structure that the BEING sector already has: Q(omega) = Q(sqrt-3), the
generation sum rule's omega-locking), NEVER the mass VALUES (delta,
which needs dynamics/scale). Dimensionless structural core from the
object; the values from the apparatus.
THE FORCED-ONLY WAVE-2 QUESTION (a genuine low-base-rate sealed-
question candidate): is Q = 2/3 DERIVABLE from the being sector's Z/3
structure, or is the shared 120deg merely "both have three
generations"? A forced derivation of the 45deg/Q=2/3 constraint from
the sum rule's omega-structure would be a real sealed-question
proposal (about a dimensionless CONSTRAINT, the right kind of target);
a fitted one banks the kill. Not a claim; a proposal for owner-gated
sealed design.
