# THE BASE-RATE-FIRST PRINCIPLE (cc2, 2026-07-18; two-seat agreed with
# chat1) — for every future sealed-comparison design template

BEFORE aiming at any target, compute its BASE RATE: how many
structurally-comparable object quantities fall within the measured
band at current precision. Only aim where a hit would be GENUINELY
SURPRISING (low base rate). Loose measurement => high base rate => a
match is cheap and unconvincing EVEN IF the prediction is correct.
This is now a required line in any sealed physics-design prereg.

## THE INVERSION (established this session): aim at the MOST precisely
## measured dimensionless quantities, not the loosest. Koide (~2/3, 5
## digits), sin^2 thetaW, alpha are LOW base rate; the mixing angles
## are HIGH base rate.

## TWO TRAPS FLAGGED (chat1's proposals, corrected):
1. THE alpha_GUT NUMEROLOGY: "is alpha_GUT ~ 1/25, or 1/24 = 1/|2T|,
   or sin^2(pi/5)/k?" — REFUSED. (a) the pure SM couplings do NOT
   unify (a several-% miss), so there is NO sharp UV number to match;
   (b) {1/24, 1/25, 1/26, ...} near an ill-defined target is HIGH base
   rate; (c) "1/24 = 1/|2T|" is the exact smooth-number numerology
   already killed (MB16, the E6 denominators). WAIT for A3's actual
   running value AND its base-rate score; do not pre-excite.
2. THE "ANY NATURAL ALGEBRAIC OPERATION" KOIDE FISH: "does
   -(7983360/13)omega produce 2/3 under any natural operation?" —
   REFUSED as posed. "Any operation" = unbounded freedom = you can
   reach 2/3 from anything. A Koide probe is legitimate ONLY as a
   FORCED structural question (below), never a fitted numerical one.

## THE LEGITIMATE KOIDE PROBE (Wave-2 cell, FORCED-only): Koide's
## Q = 2/3 has a known geometry — (sqrt m_e, sqrt m_mu, sqrt m_tau)
## is a Z/3 orbit: sqrt m_k proportional to 1 + sqrt2 cos(delta +
## 2 pi k/3). The 2pi/3 = 120deg = arg(omega). The BEING sector lives
## in Q(omega) = Q(sqrt-3), Z/3, and the generation sum rule locks
## 2 of 3 pairs with coefficient proportional to omega. SHARED
## STRUCTURE (real): both are Z/3 / 120deg / cube-root-of-unity on
## three generations. THE PROBE: is Koide's mass-encoding PHASE delta
## a DISTINGUISHED object quantity (base-rate-scored against the menu),
## and is the omega-map from the sum rule to the Koide constraint
## FORCED or fitted? Forced + low-base-rate => a sealed-question
## proposal. Fitted or high-base-rate => bank the kill. Queued Wave 2.
