# B424 (GATE B — the Hessian spectrum) — PRE-REGISTRATION

**Committed before computation. The last uncomputed stone: the CS Hessian's individual
eigenvalues on H¹(4₁, 𝔢₆) (the "internal frequencies"), whose regularized product is the
Fibonacci torsion (B423). Compute blind; name as math; the bar decides. Firewalled.**

## The computation

H¹(4₁, 𝔢₆) = ⊕_i H¹(Sym^{2m_i}), one 1-dim sector per E₆ exponent {1,4,5,7,8,11} (B347).
The Hessian is block-diagonal in these sectors (distinct isotypic/monodromy sectors don't
mix). The per-sector Hessian determinant is the regularized torsion τ_{m_i} =
(−5)^{m_i} ∏_{j=1}^{m_i} F_{2j}² = ∏_j (−#Fix(A^{2j})) (B423). So the six "eigenvalues"
(the frequencies) are {τ_{m_i}}; the "mass ratios" are their ratios; the "frequencies" are
log|τ_{m_i}|.

## THE BAR CHECK (four-part, the owner's mass speculation tested by computation)

Compute the six eigenvalues, their pairwise ratios, and log|τ_m|. Then:
(iii) does ANY ratio (or the ordered spectrum) match an EXACT SM mass ratio (m_e/m_mu,
      m_mu/m_tau, quark ratios, etc.) — pre-registered targets, exact-only, no fitting?
(iv) CONTROL: the silver-monodromy Hessian spectrum must NOT match the same targets.
Golden characterization: are the eigenvalues/ratios pure Fibonacci-Lucas (golden) or do
they carry non-golden structure a symmetry read would miss (as B423 revealed Fibonacci
primes)?

## Outcome (two-outcome)
- GOLDEN/NO-SM: the spectrum is Fibonacci-golden, no ratio matches an SM mass ratio ⇒ the
  Hessian confirms the terminal statement from the last uncomputed side; the golden cat map
  principle is complete; the papers stand.
- STRUCTURE/MATCH: a ratio matches an SM mass ratio (forced, unsought, control-distinctive)
  ⇒ the escalation (dedicated session) — the one outcome that would reopen everything.

Machinery: exact ℤ[φ] / integer arithmetic; the B423 τ_m + F/L identities; SM mass ratios
from PDG (exact rationals of the central values, flagged). Locks from JSON.
