# B1042 — PM3: THE TRIT MORPHISM — JOINED: the trit's ℤ/3 is ω's ℤ/3 carried through McKay, every link exhibited, and the golden end is the control that makes it non-generic

*cc banking seat, 2026-08-12. Sealed b8544786, pushed both remotes before compute.
Exact ℚ(ω) arithmetic throughout (`b1042_cells.py`, log `b1042_output.txt`): the
McKay graph is COMPUTED from 2T's character theory, not cited. Gate 5 — the trit is
a label; no measured value. V0's prior-art and synonym sweeps ran first (clean: the
McKay-order-3 check was unrun; B487's "tensoring" is DGG field-theory tensoring,
different object; B946's "orbit labels" are belt-level, different structure —
recorded, excluded).*

## The verdict: JOINED — the descent chain, link by link

**ω's ℤ/3 (field-side, μ₆'s cube roots) → the trit's ℤ/3 (algebra-side, the
VEV-acceptance orbit) is one structure, carried by exhibited functorial links:**

1. **Field → group.** 2T's commutator subgroup is Q₈ (computed); 2T/Q₈ ≅ ℤ/3; and
   the quotient's three characters take values EXACTLY in ⟨ω⟩ = μ₃ ⊂ μ₆ = 𝒪₃^× —
   **the field's own cube-root group IS the value group of the group's
   abelianization.** (All seven characters constructed; 49 orthogonality pairs
   verified exact over ℚ(ω).)
2. **Group → diagram (McKay).** The McKay graph of 2T — A_ij = ⟨χ₂·χ_i, χ_j⟩,
   computed — IS affine E₆ (center degree 3, middles 2, tips 1). Tensoring by the
   ω-valued characters is an **order-3 automorphism of the diagram fixing the
   center** — the affine-E₆ rotation, realized as character arithmetic.
3. **Diagram → the trit.** Deleting the center node leaves **three disjoint A₂
   diagrams = su(3)³**, and the rotation permutes the three factors **in one
   3-cycle** — which is the banked factor rotation of the trit's orbit (B897:
   "explicit factor-rotation permuting the blocks in one 3-cycle, order exactly 3";
   B1030's orbit label). The banked upstream links complete the chain at point of
   use: κ − 2 = ω² (B1010's bridge); the 2T ↦ E₆ McKay assignment (the banked
   spine); B1011's tensor law for the hearing side.

## The control that decides V3 (the B757 accident-detector)

**The identical construction on the golden end yields NOTHING: SL(2,5) is PERFECT**
(computed — the commutator subgroup is all 120 elements), so 2I has no nontrivial
1-dimensional characters, no tensoring rotation, no trit. The E₆ end's carry is
therefore **discriminating, not generic**. The B757 shape (two order-3 subgroups
conjugate inside a big group — membership-only, zero content) does not apply: this
is a chain of canonical constructions (abelianization → character values → McKay
tensoring → diagram automorphism → factor permutation), each computed, with a
control that fails on the sister end. **DISSOLVED-FORCED is refuted, not merely
avoided.**

## Scope fences (stated so the result cannot be over-read)

- **B1030 §V4's parenthetical is UNTOUCHED for its own pair.** The seal fixed §V4's
  "unrelated log₂3's" as the #1-vs-#6 pair (the trit vs the endpoint-class COUNTING
  floor). Nothing here connects the menu-tree terminal count to ω — that pair
  remains unrelated as asserted. **This arc joined #1-vs-#5** (the trit vs the
  field's ℤ/3), a different pair, which §V4 never addressed.
- The FMT's S₃ was not touched (the seal's fence); the ℤ/3 ⊂ S₃ bridge remains
  L160-adjacent territory, now closed from the other side by B1041.
- PM1's oblique lesson was armed but not needed: the carry here is coordinate-clean
  at every link (the rotation IS the factor permutation).
- The typed alternates PLACED: the trit's home is **algebra-side by residence,
  field-side by descent** — #3 (the commensurator ℤ/3, relation-side) and #4 (the
  deck isometry, object-side) are NOT the trit's home; they remain their own
  structures, with B757 already having dissolved the #3-vs-#4 identification.

## What this means (the crossing consequence, stated at type level)

The price's third item — "one trit" (B1030's floor: one unit, two bits, one trit) —
**now has an arithmetic pedigree: the trit is ω's shadow through McKay.** The
label-crossing channel identified by the field exploration (the coupling is the
only ℤ/3 carrier — B1040) now has an arithmetic SOURCE on the field side: the same
μ₆ that makes ℚ(√−3) unique among the h=1 sisters is the value group feeding the
coupling's one ternary label. Per the sealed promotion rule, the cosmogony row "the
ℤ/3 (ω, triality, the trit)" upgrades **[LEAP] → [MATH]** — for that row only; the
frame's other rows keep their tags and their adversarial status.

## Verdict

**PROVED (JOINED).** L157-2's hook is discharged into this arc (the morphism demand
MET for the trit column: exhibited links, not output agreement). Locks:
`tests/test_b1042_trit.py`.
