# B1042 PREREGISTRATION — PM3: the trit morphism (the ℤ/3's descent, or its typed accident)

*cc banking seat, 2026-08-12. Sealed before compute; hash to docs/SEAL_LEDGER.md,
committed and pushed to both remotes before any cell runs. Gate 5 throughout (the
trit is a LABEL; no measured value anywhere in this arc). Relay checked before this
seal (latest: the trit candidates — supplied, verified, acked; no newer file). The
PM campaign's second cell; L157-2 is MERGED into this cell (same hook, per the
approved plan — its registered text receives the disposition note at bank).*

## The subject, disambiguated (two fences, both binding)

The cell's ℤ/3 is **candidate #1 of the audit seat's verified inventory: B1030's
VEV-ACCEPTANCE trit** — the triality orbit label (B897 substrate; "explicit
factor-rotation permuting the blocks in one 3-cycle, order exactly 3").

- **FENCE 1 (the meditation's verify-pass find):** B860's cascade-step-3 "trit" is a
  BIT (Borel–de Siebenthal gives exactly two options) — a DIFFERENT referent,
  excluded by name. A TERMINOLOGY row for the two-trit collision lands in the
  hygiene arc.
- **FENCE 2 (the inventory's #6/#7):** B1028's endpoint-class floor and B1029's
  golden-sum pricing are log₂3 PRICINGS — counting/pricing objects, not ℤ/3 groups.
  B1030 §V4's parenthetical "…and the trit orbit are unrelated log₂3's" is read as
  the #1-vs-#6 pair, and **that assertion — made in a parenthesis, never computed —
  is UNDER TEST in this cell only for that pair.**

## The question (the audit seat's §3 form, adopted; they adjudicate nothing)

**Is the trit's ℤ/3 (#1, algebra-side) the image of ω's ℤ/3 (#5, field-side — μ₆'s
cube root, the ambient's unique feature among the h=1 sisters) under the banked
descent κ−2 = ω² → ℚ(√−3) → 2T → McKay-E₆ → the cascade — or an independent
triality that happens to have order 3?** The four-type axis (algebra / object /
relation / field, with observer-side REMOVED by B1040) is what makes the question
decidable, and the seal names it.

## The cells

- **V0 — prior-art (PM6's question applied to this cell itself, before compute).**
  (a) Search whether any arc already runs the McKay-order-3 check (the audit seat's
  NOT-SEARCHED line, inherited as a duty); (b) the "trit" synonym sweep — "orbit
  label", "VEV acceptance", "the third bit", "acceptance label" — raising the recall
  floor before compute. Both searches run Unicode AND ASCII. Findings recorded
  either way; a prior run found = this cell verifies it instead of re-running.
- **V1 — the McKay finite check (the audit seat's discriminator, sealed as a
  computation).** 2T = SL(2,3) has elements of order 3 (its 2-Sylow is Q₈; the
  order-3 classes exist); the McKay correspondence sends 2T's irreducibles/classes
  to the affine-E₆ Dynkin diagram; the trit is an order-3 rotation of the SU(3)³
  factor structure inside E₆. The check: does the McKay correspondence carry 2T's
  order-3 structure (its ℤ/3 quotient 2T/Q₈ ≅ ℤ/3, equivalently its order-3
  classes) to the triality/factor-rotation ℤ/3 of the trit's orbit — exhibited at
  the level of the E₆ diagram's own ℤ/3 rotation symmetry and its action on the
  cascade's three 9-blocks? Finite, exact, in-sandbox. **PM1's lesson, binding: do
  NOT assume a carried ℤ/3 sits coordinate-wise — check oblique/diagonal sittings
  before declaring absence** (the cube identification was exhibited only obliquely).
- **V2 — the descent trace (only if V1 exhibits a carry).** Trace the carry through
  the banked chain link by link (B1010's bridge κ−2 = ω²; the 2T lift; B1011's
  McKay tensor law; the cascade's block structure). JOINED requires the morphism
  EXHIBITED at each link — an assertion at any link returns the cell to DISTINCT
  with the gap named.
- **V3 — the accident-detector (B757's precedent, built in).** If the agreement is
  forced by membership alone (the B757 shape: "never the same element; the
  coincidence is Sylow-forced" — any two order-3 subgroups conjugate, zero content),
  the verdict is **DISSOLVED-FORCED**, not JOINED. The outcome space is
  **{JOINED / DISSOLVED-FORCED / DISTINCT}** — all three live, meanings sealed:
  - JOINED: the trit descends from ω; the label-crossing has an arithmetic source;
    the [LEAP] row's identification is promoted to banked mathematics (and the
    cosmogony file's tag updates from [LEAP] to [MATH] for that row only).
  - DISSOLVED-FORCED: the agreement is contentless; §V4's "unrelated" is CONFIRMED
    for #1-vs-#5 with the mechanism named; the [LEAP] row stays a leap.
  - DISTINCT: the structures fail to correspond at a named link; the typed negative
    banks; the four-type inventory gains its answer (the trit is algebra-native).
- **The typed alternates** #3 (the commensurator ℤ/3 — relation-side by its own
  banked wording) and #4 (the deck ℤ/3 — the only isometry, with the banked
  degenerate-masses/breaking mechanism) are PLACED by the verdict (which type the
  trit's home turned out to be), not separately computed.

## Priors: NONE on V1/V2 — both arguments stated

FOR descent: the ambient's unique ℤ/3 (μ₆) + the banked descent chain exists +
the [LEAP] row's intuition. AGAINST: B757's precedent (the corpus's one prior
two-ℤ/3 identification DISSOLVED as Sylow-forced) + the trit's substrate is
algebra-side with no field citation. No lean declared.

## MB12 / non-vacuity

Each outcome is reachable: JOINED requires exhibited links (can fail at any);
DISSOLVED-FORCED has a banked precedent (B757); DISTINCT is the default if V1's
correspondence fails. The V1 check itself can pass AND fail (the E₆ diagram's ℤ/3
either matches the trit's rotation action on named nodes/blocks or does not).

## Hazards pre-registered

Gate 5 (label-level only); sum(..., K0) if K-valued reductions appear; the
"conductor" two-referent register (B1002) checked if conductors appear; the σ and
π/6 registers at write-up; window-read-as-whole; the B701 citation form; φ AND phi;
distinct commit subjects; the S₃-vs-ℤ/3 line (the FMT's S₃ appears ONLY if V2
reaches the FMT link, and then via its ℤ/3 subgroup with the quotient stated).
