# B1410 — THE POINT LEDGER: H5's falsifier is PRICED, and it costs one owed definition

*2026-09-15. Prereg sealed `9ac73d7a…`, **pushed at `5dde7b3e` before the first sweep ran**. Gate 5
untouched: no dimensionful quantity, no measured value, zero R11 anchors.*

## 0. The method error this arc corrects, and it was the previous seat's — mine

**H5 asks for a POINT.** `C16`/`B750`, `B1264`, `B1274` and `B1409` are all censuses of **REFUSALS**.
Enumerating refusals measures the pattern's **reach** — which is exactly what B1274 said it did — and
**cannot contain the falsifier**, which lives in the complement: the corpus's banked *positive*
points. **Three efforts swept the wrong side of the ledger.** This arc builds the other side.

## 1. THE RESULT

Quoted from the instrument, not narrated:

```
  point-census --selftest: PASS
    P1    B862    UNDECIDED  other-referential target but the derivation consumes
                             identifications_used=["registerability -- 'chirality-registering
                             measurement = a B599 pairing datum', recorded by B861 as STILL OWED"]
    P2    B287    UNDECIDED  object_unique is 'UNTESTED' -- UNTESTED is not true (E67)
    P3    B1345   UNDECIDED  object_unique is 'UNTESTED' -- UNTESTED is not true (E67)
  point-census: {'UNDECIDED': 3}
```

**Zero `SELF`. Zero `OTHER`. Zero `GENERIC`.** Three rows, three `UNDECIDED`, for two different
reasons — and both reasons are findings.

### 1a. H5's falsifier is one owed definition away from firing

`B862` selects **[SU(3)×SU(2)×U(1)]/ℤ₆**, one element of {1, ℤ₂, ℤ₃, ℤ₆} — a set **the Standard
Model defines** (Tong, arXiv:1705.01853), not the object. Its own words: *"the first place in the
programme where the selected structure outperforms the SM's own data: the SM cannot answer this
question about itself; the chain answers it."* Verified current: `superseded_by: null`, no
correction, independently re-affirmed by `B870` and `B969`.

Its derivation consumes exactly two things, and `B861` names both: *"Menu completeness (P5) is still
the imported spine … This is now the cascade's single external dependency"*, and one definition
*"still owed"* — registerability ≡ a B599 pairing datum.

**The adjudication, and the two clauses split:**

- **P5 is NOT external input.** Citing known mathematics is free under the corpus's own cite-don't-
  claim rule, and the programme rests on cited classifications (McKay, Kato–Yukie,
  Menal-Ferrer–Porti, Minsky). If citing a classification counted as input, **every** result in the
  corpus would have external input and H5 would be trivially true — the vacuity problem relocated.
- **The owed registerability definition IS external input.** *"chirality-registering measurement"* ≡
  *a B599 pairing datum* is a bridge from an observational notion to a mathematical one — I-13's
  shape exactly — and `B861` records it as **still owed**.

So `B862` is `UNDECIDED`. **And that prices the falsifier for the first time: discharge B861's own
carried item 1 and B862 becomes a clean `OTHER` against a target the Standard Model defines.**
*"The falsifier has never been hunted"* becomes **"the falsifier has a price, and here it is."**

### 1b. H5's EXCEPTION CLAUSE is unsupported too, and by the same standard

`P2` and `P3` are clean selections inside sets the object generates — and neither can be typed
`SELF`, because **object-specificity has never been measured.** `B1274`'s four MB12 controls are
each about a selector's internal soundness — *"Selector 2's discriminating power is exhibited"*,
*"Selector 1's uniqueness is checked against **all 30** integral labellings"*, *"compared as sets"*,
each *"recomputed here from its own definition"* — and **not one is a control against other
objects.** Concordance was established; specificity was not.

**So neither H5's claim nor its exception is currently supported by a single classified row.** That
is symmetric, and it is the honest state.

## 2. THE INSTRUMENT, AND ITS MEASURED WEAKNESS

```
    recall on MUST_RECOVER : 3/5  ['B287', 'B1248', 'B862']
    MUST_REJECT flagged    : 2/3 ['B1406', 'B1409']  (lower is better)
    total candidates        : 172
```

**Reported as measured, never tuned against the calibration set** — tuning it would be fitting the
matcher to its own controls. Diagnosed instead:

- The two misses (`B1224`, `B1345`) carry a distinguisher and **no candidate-set marker: the
  selection is simply not in the verdict line.**
- The two false positives fire on **method** language, not on result structure. The instrument's own
  reason for `B1409`, quoted: `distinguishers: ['selected', 'selection', 'selects']` ·
  `set_markers: ['ONE OF THE 20', 'ONE OF THEM', 'among them', 'of the five']` — an arc that
  *selects covers* and *counts cusps*, and selects no point at all.

> **The finding underneath the number: the corpus's `claim_one_line` does not encode whether a
> result is a SELECTION.** That is why no point ledger was ever derivable mechanically, and why
> three prior efforts stayed on the refusals side — refusals *are* encoded, because `B750` built
> them by hand into a sealed ledger with `can_fail_witnesses`.

**E78 caught in this arc's own instrument, and fixed rather than noted.** The count moved 172 → 173
the moment this arc's verdict was written, because B1410's own `claim_one_line` quotes the selection
vocabulary the matcher hunts — **a measurement written back into the corpus it measures.** The arc is
now excluded from its own sweep, on `already_banked.py`'s own stated precedent: *"A self-documenting
instrument quotes its own test phrases, so its arc matches them -- excluding it is honesty, not
evasion."*

## 3. THE LOOPHOLE IN MY OWN SEALED RULE — reported, not patched

The sealed rule blocks `OTHER` on `identifications_used` — **I-rows**. **P5 is not an I-row** (0 hits
in `IDENTIFICATION_LEDGER`), and the owed registerability definition is not a registered row either.
**So B862 classifies `OTHER` under the rule as literally sealed**, while its own arc says it is
*"Conditional on the cascade … Not an unconditional theorem."*

A derivation can consume real external input without touching any registered I-row. **This is
recorded as a defect of the predicate, dated, with B862 named as the candidate that forced it —
and deliberately NOT patched in place**, because silently widening the rule to absorb an awkward
candidate is precisely the closure-inflation the preregistration forbids, one level up. §5 of the
seal made amendment count the headline metric for exactly this reason.

**Amendments to `docs/OBJECT_CLOSURE.json`: 0.** The closure ships with **1 IN, 2 OUT, 1 UNRESOLVED**
— `2T`/`E₆` left UNRESOLVED rather than admitted on a paraphrase, so rows targeting them classify
`UNDECIDED` and say so.

## 4. THE REPAIR OWED REGARDLESS — and it was 3.5× bigger than I stated

`B1264`'s `arc_verdict.json` said **"EIGHT MEASURED INSTANCES"** while its own lock asserts
`len(CENSUS) == 8` **and** `len(measured) == 5`. The error was in the **verdict file**, so every
downstream copy was faithful.

**I wrote that this affected 4 places. A path-correct sweep found 11 live uses across 7 files** —
`docs/CAMPAIGN_STATUS.md`, `docs/THE_STATE_2026-09-06.md`, `B1264/FINDINGS.md`, `B1273`'s addendum
and verdict, `B1274`'s verdict and its `second_collapse.py` — plus 2 generated-view copies. **My "4"
was itself a join: two greps, two files, one generalisation.** All 11 corrected; append-only history
(`CHANGELOG.md`, `PROGRESS_LOG.md`, `docs/progress/`) deliberately **left intact**, on the same
principle `retraction_sweep.py` already encodes — a record of what was written at the time is
evidence, and rewriting it would destroy the evidence that a correction happened.

**The lock was replaced, and its first replacement was also too narrow.** Version 1 read three
hand-listed surfaces and **passed an injected use in an eighth file** — the enforcement narrower
than its rule (E66). Version 2 sweeps every tracked `.md`/`.py`/`.json` outside the generated views
and the append-only record, with a use/mention split. **MB12, both directions, demonstrated:** an
injected use **fails** the lock; the same line carrying a correction cue **passes**.

## 5. A LIVE INSTANCE OF THIS WINDOW'S OWN CLASS, caught mid-arc

My verification sweep used `grep -v 'docs/views/'` to exclude the generated views. **B1264's
correction text mentions `docs/views/`**, so the filter — applied to the whole output line rather
than to the path — **hid the very file I had just corrected.** A proxy (the output line) checked in
place of the thing it stands for (the path). Fixed with `--exclude-dir`; recorded here because the
class is the window's own and this is its cleanest instance.

## 6. WHAT THIS DOES NOT ESTABLISH

- **H5 is not banked**, in either direction. Its own text forbids citing it, and this arc does not.
  It is `SURVIVED-WITH-A-MEASURED-POPULATION`, and the population is **three rows**.
- The vacuity kill did **not** fire: the predicate demonstrably returns `OTHER` (selftest; and B862
  reaches it on the literal rule), so H5 is not analytic.
- The ledger is a **first pass, adjudicated small on purpose.** A wide auto-built ledger at 3/5
  recall would be a recollection with a test around it — the exact defect §4 repairs.
- Nothing here is a crossing. No value is compared to any measurement.

## 7. WHAT WOULD MOVE IT

1. **Discharge B861's owed definition** (registerability ≡ a B599 pairing datum). That alone decides
   H5, via B862.
2. **Run the E67 control B1274 never ran** — object-specificity against a control family — which is
   what `SELF` requires and what the exception clause rests on.
3. **Widen the ledger** once (1) or (2) lands; not before.

`verification/` · `tests/test_b1410_point_ledger.py` · `docs/POINT_LEDGER.json` ·
`docs/OBJECT_CLOSURE.json` · `scripts/checks/point_census.py`
