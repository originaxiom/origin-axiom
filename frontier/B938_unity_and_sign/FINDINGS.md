# B938 — THE UNITY COLUMN + THE SIGN OF FULL FLIP: the 7/11 split is a D₂-COSET datum, not a Klein invariant

**Date:** 2026-08-07 · **Seat:** computation agent (interrupted; completed and
banked by cc) · blind throughout.

## Part B — the sign question, answered

**NO SWAP — COSET INVARIANT.** Testing the level song against the other members
of the wall pair's 2-torsion {I, D₂, D, D₂D}:

- **D acts as the IDENTITY on the colorless register** — m_S = m_A = 0 exactly;
  **all twelve of D's flips are colored.** The wall twist never touches the
  vacuum/register arithmetic.
- Therefore **D₂D carries D₂'s level song UNCHANGED** (identical tables), and
  D alone is level-degenerate (N(m − ℓ) = −ℓ³ — no primes at all).
- **The 7/11 assignment does NOT track the Klein element.** The resolvent's
  factorization (11 at m = +1, 7² at m = −1) is a **D₂-coset datum**, invariant
  under the full 2-torsion modulo the wall twist.
- Orientation reversal moves only COLORED weight: tr W18 goes 6 → 10, the flip
  rank 11 → 15.

**The reading (fenced):** the two 2-torsion generators divide labor exactly as
the two-hemisphere doctrine says — D₂ owns the colorless/register arithmetic
(the hierarchy, the value primes, the resolvent split), D owns the colored.
The unmeasured world's primes are written by one generator only.

## Part A — the unity column

The rotation's unity direction and its register expression are computed in
`results.json` (part_A) — the exact line F ∩ W3 confirmed as B930 found it.

## Files

`unity_sign.py` → `results.json`, run log · locks: `tests/test_b935_b938.py`.
