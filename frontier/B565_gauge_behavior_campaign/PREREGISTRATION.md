# B565 — The Gauge-Behavior Campaign (preregistration)

**Mandate (owner, 2026-07-13):** run the lit-map's behavior-targets *properly* — the
program has only ever read its gauge-adjacent structures at the bookkeeping pole
(group actions, gradings); this campaign computes the **load-bearing diagnostics**
(anomaly, index, centralizer, RG-sign, loop observables) that decide whether any of
the object's symmetries is a *force* or a *label*. Plus: a proper second pass on the
five probation NEEDS-RERUN leads, and **two slots for incoming cross-seat handoffs**
(to be integrated verify-don't-trust before launch).

Source map: `docs/LITERATURE_GAUGE_SM_2026-07-13.md`. Discipline: prereg rubric per
cell BEFORE compute; adversarial verify per numeric result (a kill requires its
discriminating fact computed in-sandbox); model-tiered agents; agents never commit —
banking happens in the main loop, one PR per coherent group. Firewall: every cell is
a two-outcome structure/consistency computation; nothing to CLAIMS.md.

---

## Batch A — the two sharpest verdicts (never asked before)

| cell | computation | prereg rubric | tier |
|---|---|---|---|
| **T1 — the ℤ/11 't Hooft anomaly** | The discrete-anomaly class in H³(ℤ/11,U(1)) ≅ ℤ/11, computed TWICE: (UV) the projective/MPO-symmetry cocycle of the ℤ/11 action on the 4-letter substitution space (χ=(1,3,6,7), B552); (IR) the induced ℤ/11 action on the c=7/10 tricritical-Ising primaries (explicit fusion data, B221). | UV=IR≠0 → **LOAD-BEARING** (first dynamical fact about ℤ/11); UV=IR=0 → **gaugeable/non-anomalous** (also a verdict); UV≠IR → flow-identification fails or an error — locate before banking. CAVEAT gate: first check the ℤ/11 acts on the anyon-chain Hilbert space at all (it may act only on the letter space — that finding itself banks). | opus |
| **T3 — the chiral index over the ℚ(√φ) gap labels** | The spectral-flow / Chern-pairing index across the quartic-Pisot gaps via Kellendonk's Bragg↔gap-label machinery (arXiv:2107.00737), inputs = the B543/B546 degree-4 labels. Amphichirality/CS=0 **predicts 0**. | index ≡ 0 (proven, not sampled) → **the 4th mechanism-level wall** ("object vector-like; SM matter chiral, Nielsen–Ninomiya-rigid"); any nonzero → the program's most interesting positive — escalate, verify at 2 seeds/2 word-sets before believing. | opus |

## Batch B — the certified dictionaries

| cell | computation | prereg rubric | tier |
|---|---|---|---|
| **T8 — B71 external corroboration** (do first) | Cross-check B71's SL(3) component equations against Heusener–Muñoz–Porti, arXiv:1505.04451 (Illinois J. Math. 60 (2016)): component count, dims, the Sym²-distinguished vs Dehn-filling split. | match → B71 externally corroborated (upgrade its status line); mismatch → find whose error — either way banks. | sonnet |
| **T2 — the centralizer stratification** | For every component in hand of the SL(n) character varieties (n=2,3,4: geometric/Sym², W1/W2, reducible strata, B153 rank-drop loci): dim and type of Z_{SL(n)}(ρ(π₁)) as a function on the component — nullspace of [ρ(g),X]=0, exact over F_p/symbolic (existing CRT toolkit). | Deliverable = the object's **intrinsic breaking chain** (the one literature-certified dictionary). Compared against nothing SM — pure object grammar. | sonnet |
| **T6 — the Baez–Schwahn door** | arXiv:2606.15235: Stab(X)∩Stab⁰(B) ≅ SM group for nested Jordan h₂(ℂ) ⊂ h₃(ℂ) ⊂ h₃(𝕆). The FIFTH F₄→SM route. Test: does any object structure (the two arithmetic ends ℚ(√−3)/ℚ(√5), the amphichiral θ, the ℤ/11 charge, the cusp) **canonically select** a distinguished h₃(ℂ) ⊂ h₃(𝕆) and h₂(ℂ) inside it? Finite-dimensional linear algebra. | canonical selection exists → **LIVE** (the first surviving F₄→SM route — escalate, re-verify adversarially); none → the fifth closure, **by computation not analogy** (each candidate structure checked explicitly). | fable |

## Batch C — the dynamical invariants

| cell | computation | prereg rubric | tier |
|---|---|---|---|
| **T4 — trace-map β-sign** | Coupling ≔ distance from the critical/invariant surface; compute sign of d(Lyapunov)/d(coupling) along the escalator tower (rungs 0–2), Damanik–Gorodetski smoothness underwriting the derivative. ≥2 seeds + ≥2 word-sets (exploratory-numerics rule). | AF-like (weak-UV) vs screening — either sign banks as a new dynamical invariant; UNSTABLE > forced positive. | sonnet |
| **T5 — ℤ/11 loop observables** | On the golden-chain ground state (ED, modest N): decay of ℤ/11 charge-string/disorder-line operators; the Fredenhagen–Marcu ratio. | area-law → confining dynamics for the charge; perimeter → deconfined/screened bookkeeping. First loop observable on the object. | sonnet |
| **T7 — Diophantine gaugeability** | Lohitsiri–Tong-style: set the anomaly-cancellation equations treating the object's actual discrete data (ℤ/11, orientation ℤ/2, the B533 observation types as "matter") as unknowns; solve exactly. | consistent solution set → the object's charges are self-consistently gaugeable (characterize it); empty → not gaugeable — either banks. | sonnet |

## Batch D — the probation second passes (owed; corrected specs from B562 verify)

| cell | corrected rerun spec | tier |
|---|---|---|
| **R1 (P3/B85)** | NOT the vacuous SL(2)/SL(3) Newton identities: attempt the tangent-space/Jacobian ρₙ functor at SL(3)/SL(4) with the word-power Newton technique, targeting whether it can (or provably cannot) reach the multiplicity-2 char(M²) sector where the wall lives (n≥5). | sonnet |
| **R2 (P4/L5-L6)** | Close the proxy gap first: verify `apply_word` against SnapPy's SL(2) holonomy traces for RRLL (or derive K_w inside the same Lawton framework); only then run the off-sublocus sealing search. No kill on an uncalibrated proxy. | sonnet |
| **R3 (P6/Cell-3 count)** | A real attempt at the Bézout/BKK bound on the 16-variable gauge-fixed fixed-character system (mixed volume / multi-degree), vs certified length 248 ≤ N vs recovered 253. | opus |
| **R4 (P16/ρₙ)** | The module-iso ρₙ ≅ ρₙ₋₁ ⊕ Symⁿ ⊕ Symⁿ⁻³: attempt a branching/character-identity proof for general n, or extend verification to n=9,10 with the exact engine — no placeholder. | sonnet |
| **R5 (P19/L38 Higgs-scale)** | The "do-or-die" done honestly: compute whether the Hitchin/Higgs side (PVI/Schlesinger flow time, Higgs-field norm, B164/B169) yields ANY dimensionful scale or is provably a dimensionless modulus. A TOMBSTONE requires the in-sandbox discriminating fact; no citation kills. | fable |

## Batch H — the incoming handoffs (slots reserved)

| cell | content | protocol |
|---|---|---|
| **H1 — the TDV/Krasnov/Boyle literature bridge** (received 2026-07-13, `HANDOFF_LITERATURE_BRIDGE_F4_SM.md`) | The chain: our forced E₆→F₄ (B353) ∘ the Todorov–Dubois-Violette theorem (F₄=Aut(h₃(𝕆)) ⊃ H₁∩H₂ = [SU(3)×SU(2)×U(1)]/ℤ₆, canonical up to conjugation — arXiv:1806.09450; Krasnov 1912.11282; Boyle 2006.16265/JMP 67 (2026)). **Pre-verified on receipt:** Eisenstein ℤ[ω] = G₂ root lattice EXACT (6 unit short roots, 6 norm-3 long, 30° hexagonal); TDV dims consistent (12 vs generic 0); Boyle's 32/generation ✓; Boyle's JMP-2026 publication citation-verified in the lit map (A3). | **Cell computations (prereg):** (i) **Krasnov explicit, in-sandbox**: build Spin(9) on 𝕆²=ℝ¹⁶ (octonionic Clifford/gamma action), J = left-mult by a unit imaginary octonion, compute the centralizer algebra — verify dim = 12 and type su(3)⊕su(2)⊕u(1) *by computation, not citation*. (ii) **The real-form gap (the load-bearing verification)**: TDV/Krasnov/Boyle use COMPACT F₄/E₆; ours is the E₆(ℂ) character variety with θ the diagram involution — same folding at complexified level (standard), but *which real form does the hyperbolic holonomy select?* Honest scope; partial NEEDS-SPECIALIST allowed. (iii) **The object-specific residue**: all u ∈ S⁶ are G₂-conjugate (no selection needed — resolving T6's question in Krasnov's direction), but does the object's arithmetic (Eisenstein/ℚ(√−3), now = the G₂ root field EXACTLY) distinguish a canonical *arithmetic class* of u? Two-outcome. (iv) **Triality match**: does B299's (θ,φ) ℤ₃×ℤ₃ action on the 27 equal Boyle's SO(8) triality? (v) **PC25 framing**: new headline WITH the firewall clause from our own lit map — *the gauge group is cheap; group-level structure clears no value bar* — the object's contribution is Step 1 (geometric origin of F₄), the canonical Step 2 is generic to every F₄. |
| **H2** | *(owner handoff #2 — pending)* | verify-don't-trust: recompute every load-bearing claim in-sandbox; prereg on receipt |

## Execution

One workflow: `pipeline(cells, compute[tiered], adversarial-verify[sonnet])` →
`synthesize[fable]`. Order: T8 first (hours), then Batches A+B in parallel, C behind
them, D interleaved, H1/H2 slotted on arrival (before launch if they land first).
LIVE verdicts spawn B-numbered cells; kills go to TOMBSTONES.md with residual hints;
every banked number gets a pytest lock computed in-sandbox. Expected cost: ~15 compute
cells + verifiers + synthesis, model-tiered (2 opus, 2 fable, rest sonnet).
