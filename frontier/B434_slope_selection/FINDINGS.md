# B434 — slope selection: the object FORCES its minimal chirality input (±5), and the output is the Meyerhoff manifold — new arithmetic (disc −283), not golden

**Status: banked. Completes the B432 chirality mechanism's FORCED leg. Firewalled.**

## The forcing (leg i of the bar — cleared for the INPUT)

4₁'s exceptional (non-hyperbolic) slopes are exactly {0,±1,±2,±3,±4} — verified by direct
filling (vol ≈ 0 / flat / degenerate through ±4; vol 0.9814 at 5) — and this is the KNOWN
MAXIMAL exceptional set among hyperbolic knots (the control: no other knot ties it). So the
object's own topology selects **slope ±5 as the minimal chirality-generating input**. Nothing
is chosen: the boundary of the exceptional range is a topological invariant.

## The output (computed exactly)

**4₁(±5,1) = the Meyerhoff manifold ± mirror** (census m003(−2,3)): the second-smallest closed
orientable hyperbolic 3-manifold, vol 0.98136883, **CS = ±0.07703818** (the mirror pair — the
"chirality unit" the minimal forced input generates). Trace field: **x⁴ − x − 1, discriminant
−283** (algdep residual 2.8e−17), Galois S₄ ⇒ **no quadratic subfields: neither √5 nor √−3.**

## The verdict (bar-honest)

- The chirality mechanism is now complete AS MATHEMATICS: **forced source** (amphichirality
  breaks under every filling, B432) + **forced minimal input** (slope 5, this bud) + **computed
  output** (Meyerhoff, CS unit ±0.0770).
- But the output **leaves ℚ(√−15)'s world entirely** — disc −283, a new prime, no golden or
  Eisenstein content. Leg (iii) exact-SM fails; named negative: the interface's first forced
  product is *new* arithmetic, not the object's own.
- The two 5s are DIFFERENT and not conflated: B225's conductor-5 is the ramified prime of
  ℚ(√5); this 5 is the count of exceptional slopes per sign. Noted as coincidence, no claim.

**Provenance.** slope5.sage/slope5b.sage/mey2.sage (snappy/sage); lock
tests/test_b434_slope_selection.py (pure-sympy: the quartic's disc/irreducibility/S₄ ⇒ no
quadratic subfield). Cross-refs: B432 (mechanism), B225 (conductor), Origin Postulate (D at
the interface).
