# B545 — The ghost census: prereg refuted, elliptic-lock law, c = 1 PROVED

Chat-2's Path 3. Their prereg conjecture (ghosts share c = 22's signature)
was REFUTED by their own census — falsifier fired and logged; the discipline
working. Replacement law read from the wreckage: small ghosts are
**elliptic-locked** — the slot discriminant is a quadratic in a with leading
coefficient x²−4: |x| ≤ 1 opens downward (finite/negative-definite), |x| = 2
linear, |x| ≥ 3 upward Pell with bounded fundamental domain. The obstruction
is a shadow of SL(2,ℤ) ≅ ℤ₄ ∗_{ℤ₂} ℤ₆: elliptic-only trace data cannot close
into a pair.

**c = 1 PROVED here (exact, tests/test_b545.py):** class (1,0,0): all three
slot discriminants are negative-definite over ℝ (maxima −4, −3, −3);
h(−3) = h(−4) = 1 gives completeness ⟹ **the smallest possible ghost level.**
B537's (1,1,5) at c = 22 is the same lemma's elliptic instance.

**Census (evidence-grade, chat-2):** ghost-candidate levels in [1,50]:
{1, 2, 10, 14, 16, 18, 22, 25, 26, 28, 34, 37, 38, 44, 50}; mixed {32, 49}.
NOT verified here beyond c = 1 and c = 22; the ghost-scanner build (per-slot
exact trichotomy → PROVED-ghost / PROVED-realized / needs-class-work) is the
F3 follow-up that upgrades the census to theorems.

**DISCREPANCY RECORDED:** chat-2's corrections log calls (4,4,16) a "false
candidate already in CC's record." It is NOT in this record, and an
independent witness search here (entries ≤ 20; plus B537's six-ordering
companion scans to |a| ≤ 3000) finds no realization. Status stays CANDIDATE;
chat-2 is requested to produce the witness pair (instantly checkable) or the
slot data. B537's candidate line stands unchanged until then.

**Sharpened open question (tracker):** does any ghost level exist with
ALL-hyperbOLIC classes (every coordinate ≥ 3 in absolute value)? That is where
ghostliness stops being an elliptic shadow. Gate flag (hard): Fricke/free-
product trace theory + Whang/BGS pending before any novelty language.
