# B303 — The seam/clock thread: the CP sign IS the sign of the Chern–Simons clock

**Status: banked (frontier). The math is verified + new; the cosmological reduction is firewalled; nothing to
`CLAIMS.md`.** The result of the **seam/clock thread** — the one the meta-conclusion (B302) singled out as the *only*
place a value can enter, because the closing is the only operation that breaks the object's scale-invariance. It
attacks **the one live forcing** (triple-convergent: B295 + Chat-1 + Chat-2): *does the running clock gauge-fix the
CP sign?*

## The result (MATH — the CP sign is the sign of CS)
- The **cusped (amphichiral) object has `CS = 0`** — the **CP-symmetric origin** of the clock (no sign).
- Every closing has `CS` with a **definite sign**: along the `(1,n)` scale ladder, `CS(1,n)` is **all the same sign**
  (`n>0 → CS<0`), with **`CS(1,−n) = −CS(1,n)`** (B289) and **`|CS| → 0` as `n→∞`** (back toward the amphichiral
  origin).
- So the **CP sign** (B289: handedness = ℚ(√−3) Galois = the `±π/6` / τ swap) is **literally the sign of `CS`.** There
  is no separate CP sign to choose and then correlate with a clock: closing in one orientation gives one sign of `CS`,
  the mirror gives the other, and **the sign is constant as the clock runs** (you never cross zero along a
  fixed-orientation history). The CP sign and the clock are **the same quantity.**

| | |
|---|---|
| amphichiral object | `CS = 0` — the clock's zero / the CP-symmetric point |
| a closing (orientation +) | `CS < 0` — one CP sign |
| the mirror closing (orientation −) | `CS > 0` — the other CP sign |
| running the clock (n varies, fixed orientation) | `\|CS\|` changes, **sign constant** |

## The reduction (the one live forcing, resolved-conditionally)
*"Does the running clock gauge-fix the CP sign?"* — the CP sign **is** the clock's sign (both are `CS`), so the answer
is **yes, conditional on a single firewalled identification:**
1. **`[LEAP]` (Alexander, arXiv:1807.01381):** Chern–Simons time = the cosmological clock (conjugate to Λ).
2. **`[LEAP]` (S045):** the arrow (the orientation of the closing = the clock's direction) is itself forced (σ
   non-invertibility; the de Sitter entropy floor).

**IF both hold, the CP sign is internal** — matter-over-antimatter = the sign of the clock = the **arrow of time**.
This moves the forcing from **OPEN** to **REDUCED to two named firewalled inputs, with the math fully in place** — the
strongest honest outcome the thread can produce. (The matter-asymmetry-tied-to-the-time-arrow is a known deep idea;
here it has a specific mechanism: `CS` is *both* the CP-sign carrier and the cosmological clock.)

## The matter map, sharpened
- **CP magnitude `π/6`** — forced in the **bulk** (ℚ(√−3), B285);
- **CP sign** — the **sign of the CS-clock** = the **arrow of time** (B303): moved from *"external"* to *"the clock's
  sign, internal-conditional-on-(CS=clock)+(arrow-forced)"*;
- **baryon magnitude `η_B`** — **external** (freeze-out; `sin(π/6)=½` is not `η_B`, B285).

So the seam is confirmed as the value-source the meta-conclusion predicted: the closing breaks scale-invariance
(`CS: 0 → ≠0`), and the **first value it emits is the CP sign** — because the CP sign is just the sign of the one
quantity the closing creates.

## What it is / isn't
**Is:** new math (the amphichiral object = the `CS=0` clock-origin; the CP sign = the sign of `CS`; the definite
arrow along the ladder), reducing the one live forcing to two named firewalled inputs. **Isn't:** a derivation of the
baryon *magnitude* (still external), and **not** a claim that the sign *is* internal — only that it is internal *iff*
the two firewalled identifications hold. Nothing to `CLAIMS.md`.

`clock_is_the_cp_sign.py` (sage-python: the CS ladder) · `verdict.py` (pyenv) · `tests/test_b303_clock_is_the_cp_sign.py`.
Related: `B289` (CS sign law = the CP sign), `B293` (the peripheral clock), `B295` (the SSB/gauge status — the forcing
named), `B285` (±π/6 magnitude), `B286` (the seam), `B302` (the scale-invariance meta-conclusion), `S044`/`S045` (the
Alexander Λ↔CS-time; the forced arrow). Lit: Alexander–Magueijo–Smolin arXiv:1807.01381.
