# B211 — ADDENDUM: the character variety is Cremona **40a3** (the square-time curve), not 40a1; the point-count evidence could never tell

*Addendum written 2026-09-02 under **B1238** (the seat harvest, third ring). Every number below recomputed on this bench — `frontier/B1238_seat_harvest_40a3_bronze_octic/verification/`. The arc's verdict file carries the same correction in its `note`.*

**What is wrong.** This arc labelled `X^{na}: Φ(x,z) = z² − (x²+1)z + (2x²−1) = 0` as **40a1** (`y² = x(x−1)(x−5)`, j = 148176/25). Completing the square in z:

```
4·Φ(x,z) = (2z − x² − 1)² − (x²−1)(x²−5)
```

so X^{na} is the genus-1 quartic `d² = (x−1)(x+1)(x²−5)` in the chart `(c,d) = (x, 2z−x²−1)` — **B509's square-time curve, identically**, with c the same meridian trace (B509's geometric point (c=2, d²=−3) is this arc's complete-structure point x=2: Φ(2,z) = z²−5z+7 gives (2z−5)² = −3). Its Jacobian (`ellfromeqn` → [0,−6,0,−20,120]; minimal model **[0,0,0,−2,1]**; j = **55296/5**; conductor 40; torsion **ℤ/4**) is Cremona **40a3**, and the quartic has the rational point (1,0), so X^{na} ≅ 40a3 over ℚ (`verification/b211_phi_jacobian.py`).

**Why the evidence did not catch it.** `#X(𝔽_p) = p − 1 − a_p` over 23 primes (L34) is **isogeny-invariant**: a₃ = 0, a₇ = −4, a₁₁ = 4, a₁₃ = −2 are identical across the four members of class 40a (isogeny-degree matrix [1,2,2,2; 2,1,4,4; 2,4,1,4; 2,4,4,1]). The relation is true and stays locked (`test_b211`, `charvar_zeta.py` untouched — `ap_40a1` returns the class's a_p). This arc's own "torsion ℤ/4" was 40a3's number (40a1 has ℤ/2 × ℤ/2) — the correct datum sat beside the wrong label.

**Where 40a1 sits.** It is the quotient of X^{na} by the fixed-point-free involution σ(x,z) = (−x, x²+1−z) — the sign twist by the nonzero class of H¹(m004; ℤ/2) (x ↦ −x, z = tr(AB) fixed) composed with the swap of Φ's two roots: on Φ = 0, (X,Y) = (x², x(2z−x²−1)) satisfies Y² = X(X−1)(X−5) (Y² − X(X−1)(X−5) = 4x²·Φ exactly; σ has no fixed point on X^{na}: the only affine candidate (0, ½) has Φ = −5/4).

**What survives.** Genus 1; conductor 40 = 2³·5; non-CM; rank 0 (PARI `ellrank` bounds [0,0], B509's descent); the Weil zeta is the L-function of the weight-2 newform of level 40 — every isogeny-invariant statement, i.e. the whole "arithmetic of the variety ≠ arithmetic of the distinguished point" reading (bad primes {2,5} vs the trace field's 3). Corrected: the label (40a1 → 40a3), j (148176/25 → 55296/5), the torsion structure attributed to 40a1 in the text. The "Novelty — UNCHECKED" note stands.

**Propagation.** B1237 corrected B213's copy of this label yesterday without reaching this arc, the one that minted it — E53 instance #17 (mine). B509/B510 receive the sharpening (identity, not Jacobian-of-a-cover). The identification enters `docs/IDENTIFICATION_LEDGER.md` as I-12 (EARNED — the map is written down and acts). Live docs sites carry dated brackets (LAW_MAP:59, THE_LAMBDA_POSITION:65, STRATEGIC_SYNTHESIS:268, CLOSURE_2026-07-05:60/87, OPEN_LEADS L34/L44). Verdict **PROVED** stands with the corrected label.
