# B344 — the deviation space's forced geometry: a reciprocal (symplectic) pairing; κ is the scale door

**Status: banked (frontier) as a forced dimensionless RELATION (form, per the `sin²θ_W=3/8`/period-law precedent — not a
value). Target 1 of the deviation-structure strategy. Firewalled; nothing to `CLAIMS.md`.** The object is the symmetric
centre; physics is the deviation. The tangent *spectrum* is already banked (the metallic tower `char(M^k)`, B65); this
probe banks the deviation-space **geometry**.

## The forced relation
- **`det(dφ_m) = 1`** — the trace map is a **volume-preserving** polynomial automorphism (each Dehn twist has Jacobian
  det 1). Verified `m=1,2,3`.
- **κ-invariance (B167)** — the trace map preserves `κ = tr[a,b]`, so at any fixed point the **κ-direction is an
  eigenvector with eigenvalue 1**: the **Casimir** (Poisson-central, B293), the one **un-paired** deviation direction.
- **⇒ the remaining two deviation modes multiply to 1: a forced RECIPROCAL PAIR `(λ, 1/λ)`.** (Verified at a fixed point:
  eigenvalues `{1, λ, 1/λ}`, `λ·(1/λ)=1`.)

So the deviation space decomposes as **`{κ}` (central, un-paired) ⊕ `{(λ, 1/λ)}` (a reciprocal pair)**.

## Reading (form, held)
The reciprocal pair are symplectically **conjugate**: the mapping class group preserves the character-variety **Goldman
symplectic form**, of which `κ` is the Casimir (B293; Goldman 1986). So the deviation-space geometry is: a **symplectic
plane** carrying a conjugate reciprocal pair `(λ, 1/λ)`, plus the **central κ-axis** — and *`κ` is the scale/cusp door
(external)*. This is **why the metallic tower is reciprocal-paired** `λ^k ↔ λ^{−k}` (B65): the symplectic/volume-preserving
structure forces it. The relation is **dimensionless** (a symmetry of the deviation spectrum, `λ ↔ 1/λ`; the un-paired
direction is the scale door) — no magnitude is predicted. The one direction the object *cannot* pair (κ, the scale) is
exactly the external one — the firewall, seen in the symplectic geometry.

## The firewall (held)
A forced dimensionless relation on the deviation space (form); the un-paired central direction is the external scale. No
value. Nothing to `CLAIMS.md`.

## The fence
Exact Jacobian determinants + fixed-point eigenvalues + the Goldman-Casimir check (sympy). No physics values. Nothing to
`CLAIMS.md`.

`deviation_symplectic_pairing.py` (pyenv) · `tests/test_b344_deviation_symplectic_pairing.py`. Related: **B293** (the
Goldman bracket, κ Casimir), **B167** (κ first integral), **B65** (the tangent tower `char(M^k)`, reciprocal),
**B179** (the metallic `λ_m`, tower `±λ_m^k`), **B265** (the exponent split — Target 2/3). Lit: Goldman (the MCG
preserves the character-variety symplectic form); the reciprocal spectrum of a symplectic/area-preserving map.
