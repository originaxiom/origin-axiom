# B703 — chat1 Q3 RESOLVED: the Koide angle is 0.89σ from 2/9 (corrected), and firewalled

*cc banking seat, 2026-07-19. chat1's Q3 (the exact σ-distance of the Koide
angle θ₀ from 2/9). Firewalled SM-adjacent adjudication (the B655 pattern);
Gate 5 stands — nothing promotes to CLAIMS.*

## The corrected number
With PDG masses (m_τ = 1776.86 ± 0.12 dominant): **θ₀ = 0.2222296(83) rad**,
**2/9 = 0.2222222** → **θ₀ is 0.89σ from 2/9** (|Δ| = 7.4e-6, σ = 8.3e-6).
chat1's crude "~7σ" is REFUTED — it is sub-σ. The τ mass giving θ₀ = 2/9
exactly is 1776.75 MeV, INSIDE the measured 1776.86 ± 0.12. Koide Q = 0.666660
(2/3 to 6e-6). This is the strongest SM-adjacent numerical coincidence in the
program.

## Firewalled — on the honest grounds (NOT "it isn't close")
- **Not derived:** the framework does not produce θ₀; masses are outside the
  dimensionful no-go (LAW_MAP §E). θ₀ is a FIT to measured masses, not a
  prediction.
- **Convention-framed:** θ₀ is defined only mod the lepton-assignment + branch;
  "2/9" is the value in ONE convention (B686 flagged convention-dependence).
- **Second-layer:** Koide's Q = 2/3 is itself a 120°-parametrization TAUTOLOGY
  (B686); θ₀ = 2/9 would be a coincidence on top of a tautology.
- **Base-rate (honest):** 2/9 is the ONLY simple fraction (q≤12) within 3× the
  gap — so base-rate does NOT strongly kill it; but 2/9 was identified
  post-hoc (the closest fraction to the measured angle), so it is a fit, not a
  sealed prediction.

## Verdict
θ₀ = 2/9 to 0.89σ is a REAL, tantalizing near-coincidence (chat1's 7σ
corrected), but it is NOT evidence: not derived, convention-framed, a
post-hoc fit atop a tautology. Gate 5 holds; nothing to CLAIMS. **Falsifier
(standing):** a tighter m_τ that pushes θ₀ off 2/9 kills it — Nature's clock
runs independently. This is the honest ceiling of "SM values": the single
closest one is a sub-σ fit the framework cannot derive.
