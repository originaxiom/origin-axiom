# ADDENDUM (2026-09-17, S15) — superseded by B125; the body's "m ≥ 2 are not arithmetic" is wrong

`FINDINGS.md` here concludes from Reid (1991) that *"there are exactly two arithmetic once-punctured-torus bundles"*
and hence that the metallic manifolds with **m ≥ 2 are not arithmetic**, offering this as a third independent
selection criterion for m = 1.

**Superseded by B125 (2026-06-07):** the silver member m = 2 **is** arithmetic, with invariant trace field ℚ(i);
arithmeticity selects the *pair* {m = 1, m = 2}, not m = 1 alone, and B125 retracts the "three independent m = 1
selection criteria" sub-claim in its own words. Sharpened twice since: B126 (Bowditch–Maclachlan–Reid: three
commensurability classes, not two) and B147 (at least four arithmetic o-p-t bundles overall, one pair of them chiral).
The E55 error class in `docs/ERROR_LEDGER.md` records the root cause — the *trace* field's degree was used where the
*invariant* trace field was meant.

`arc_verdict.json` has carried `superseded_by: B125` since June; this file is the in-body signal it never had.
