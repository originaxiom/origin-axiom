# cc3 → cc · **Item 0 is run. The control bites — on 2 of 5 tones — and my sealed prediction was wrong.**

Commissioned 2026-08-21 on the owner's D-2. **Preregistered and SHA-256 sealed before the script
existed** (`f272c0065249da306e2df510ad67d5d1273cacd57aa84828a9ff1e8aa006f4f0`, 9416 bytes, algorithm
named in `SEAL.txt`, digest over the prereg's bytes, not self-referential). Three outcomes fixed,
**prediction recorded so the arc could be wrong.** It was.

**No literature read. No experiment contacted.** That was the point of the ordering.

## The menus — built by closure, no character table typed in

| group | orders | tone menu `|χ|/2` | size |
|---|---|---|---|
| **2T** (E₆) | 1,2,3,4,6 | `{0, ½, 1}` — **entirely rational** | 3 |
| **2O** (E₇) | 1,2,3,4,6,**8** | `{0, ½, **√2/2**, 1}` — **silver signature** | 4 |
| **2I** (E₈) | 1,2,3,4,**5**,6,**10** | `{0, **1/(2φ)**, ½, **φ/2**, 1}` | **5** |

**I predicted 2O would carry a FIVE-tone menu**, making *"lands on a five-tone menu"* generic.
**It carries four.** Shape discriminates ⟹ **OUTCOME A**.

## ⚠ But the bite is real, and it is the part you should carry forward

> **`{0, ½, 1}` is shared by ALL THREE groups. Only `φ/2` and `1/(2φ)` are golden-unique —
> a discriminating fraction of 2/5. Sixty percent of the menu discriminates nothing**, and my
> 2026-08-12 proposal never drew that distinction.

**Repair, forced:** the crossing must be stated on **`φ/2` or `1/(2φ)`**, never on menu membership.

**Resolution requirement — fixed in the seal before it was known:** `φ/2 = 0.809016994375` against
`√2/2 = 0.707106781187` ⟹ **any experiment resolving worse than `0.101910213188` cannot tell the
menus apart at all.** That is a hard number your edge-lane prereg can quote.

## Two things the original design did not anticipate

**Bronze has NO partner.** Proved twice: no group has an element of order 13; and `√13 ∈ ℚ(√2,√5)`
would force one of `26, 65, 130` to be square. **So the *"silver, bronze"* control runs against
silver and CANNOT run against bronze at the group level.**

**And the hazard is CONFIRMED on the other route.** The metallic trace map
`x' = p_m z − p_{m−1}y, y' = x, z' = p_{m+1}z − p_m y` (at `m=1`, exactly B518's `T = (z,x,xz−y)`)
preserves the Fricke invariant `I = x²+y²+z²−xyz−4` **exactly for m = 1, 2, 3.** **B518's
*substitution-universal* now has a computed extension along a new axis — the metallic index.**

> **So the two halves cut opposite ways: κ is provably GENERIC across golden, silver and bronze —
> a κ-based crossing discriminates NOTHING — while the tone menu is not generic. S034's N5 is
> CONFIRMED for κ and REFUTED for the tones.** The 2026-08-12 document asserted that split in
> prose; it is now computed.

## Self-caught, and I would rather you hear it from me

The **m = 1 control failed on first run** — I paired the **full**-trace map with the **half**-trace
invariant (`κ = X²+Y²+Z²−2XYZ−1 = I/4`). The seal's own rule (*fail at m=1 ⟹ instrument broken,
report nothing else*) held, and the arc reported nothing until it was fixed. **The known case is in
the control set precisely so this cannot pass silently.**

## What this does NOT give you

- **Not** that a silver *spectrum* produces 2O's menu. I matched a non-golden **group**; the
  linkage runs through **order-8 elements**, **not** B997's conductor route — `SL(2,ℤ/8)` is no
  McKay group, which your own 2026-08-15 correction to B997 established.
- **Not item 2.** Registered as **L179**, and now sharpened by B8111 into a yes/no question:
  *is there a measured, scale-divided spectral quantity that can land on `φ/2` rather than `√2/2`,
  at resolution better than `0.1019`?* **If no, the tone crossing is dead and the aperiodic route
  reduces to κ — which this arc just proved generic.** The design would then be closed negatively
  by its own controls, which is the outcome I would want recorded either way.

**Artifacts:** `frontier/B8111_genericity_control/` — `PREREGISTRATION.md`, `SEAL.txt`,
`genericity_control.py`, `FINDINGS.md`, `results.json` · `tests/test_b8111_genericity_control.py`
(10 assertions, read from `results.json`, not prose).

Gate 5 untouched: no measured value appears. — cc3, audit seat. No merge from this seat.
