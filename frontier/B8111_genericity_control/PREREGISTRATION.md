# B8111 — PREREGISTRATION: the genericity control for the aperiodic crossing (Phase-0 **item 0**)

**Sealed 2026-08-21, BEFORE any computation, and BEFORE any literature reading or experiment
contact** — which is the whole point of the Meditation-2 reordering that put this item first.
Seal digest in `SEAL.txt`: **SHA-256 of this file's bytes**, algorithm named there, not
self-referential.

**Commission:** cc, 2026-08-21, on the owner's D-2 ruling —
*"the genericity control's sealed design is yours … the matched non-golden substitution
(silver/bronze) that must FAIL, designed and sealed BEFORE any literature reading or experiment
contact."*

**Origin of the requirement — my own words, 2026-08-12, item 3 of the not-searched list:**
*"The genericity control, sealed in advance — a matched non-golden substitution (silver, bronze)
must FAIL the same prediction, or the crossing is B1019's door argument all over again with a
spectrum attached."*

---

## 0. WHAT IS UNDER TEST — and it is my own proposal, not someone else's

**The claim under test is the DISCRIMINATING POWER of a tone-level prediction**, i.e. of the
proposed crossing whose content is: *a measured, scale-divided spectral quantity of an aperiodic
system lands on the five tones* `{0, 1/(2φ), 1/2, φ/2, 1} = |χ|/2` over the conjugacy classes of
`2I` in its defining `SU(2)` representation.

**The hazard, in one line (S034's N5, and B518's own strength):** the renormalisation trace map is
**substitution-universal**, so *a prediction that holds for ALL aperiodic order discriminates
NOTHING.*

**This arc does not test the physics. It tests whether the prediction could ever have failed.** A
control that cannot fail is not a control, and this arc is willing to conclude that about my own
design.

---

## 1. THE BASELINE — declared now, so "new" is testable later

| banked | what it says | what it does **not** say |
|---|---|---|
| **B997** | the golden is the **unique** metallic grammar whose **own-conductor shadow** is a McKay group: conductor `m²+4`, and `SL(2,ℤ/N)` is genuinely a McKay group only at `N ∈ {3,5}` | **nothing about tone menus.** Its route is the arithmetic shadow `SL(2,ℤ/N)`; it does **not** claim no non-golden group carries a five-tone `|cos|` menu. **The gap between those two statements is what this arc measures.** |
| **B997 correction (2026-08-15)** | `SL(2,ℤ/4) ≇ 2O` — seven involutions vs one; **E₇ is unoccupied** on that route | says nothing about whether `2O` **itself**, reached by a different route, carries a matched menu |
| **B641** | the five-tone pentagon census; ear-independence at `2.14e-60` over all 360 elements | a statement about `2I`'s own frame, **not** a comparison against `2T`/`2O` |
| **B518** | the same trace map `T=(z,x,xz−y)` preserves `κ` across substrates — **substitution-universal** | universality is asserted for the **substrate** axis; whether it survives the **metallic-index** axis `m = 1,2,3` is **not** banked and is computed here |
| **B1006** | banked NULL: the object's own spectral value is **not** low-degree algebraic | the tones are separate objects; this is a warning against the "we emit algebraic numbers" story |

**The gap this arc addresses, as a fact about the baseline: golden-uniqueness is banked on ONE
route (the conductor shadow). The tone menu is a SECOND route, and it has never been compared
against the non-golden groups at all.**

---

## 2. THE COMPUTATION (fixed now; no adaptation after seeing output)

Exact arithmetic throughout — quaternions over ℚ(√2, √5) handled symbolically or as exact algebraic
integers; no floating-point value decides any gate.

1. **Build the three exceptional binary polyhedral groups `2T`, `2O`, `2I` as explicit unit-quaternion
   sets** (orders 24, 48, 120), by closure under multiplication from generators — **not** typed in
   from a character table.
2. **The tone menu of each group**, defined as `tone(g) = |χ(g)|/2 = |Re(q_g)|` over the **defining
   2-dimensional `SU(2)` representation**, collected as an exact set with multiplicities.
3. **The discrimination matrix:** for every pair of groups, the shared tones and the unique tones,
   and the **discriminating fraction** = (tones of `2I` not present in the other menu)/5.
4. **The field attachment:** the number field generated by each menu, and for each metallic index
   `m = 1,2,3` (golden, silver, bronze) whether `√(m²+4)` lies in some group's tone field.
5. **The metallic trace maps.** Build the substitution `a → aᵐb, b → a` for `m = 1,2,3`, its
   induced trace map on `(x,y,z)`, and test whether the **Fricke–Vogt invariant**
   `κ = x²+y²+z²−2xyz−1` is preserved — **exactly, symbolically, for general `(x,y,z)`**, not at
   sampled points only.
6. **The resolution requirement:** the minimum gap between the golden menu and the best-matched
   non-golden menu — the number any future experiment would have to resolve for a tone-level
   prediction to mean anything.

---

## 3. THE OUTCOMES — three, sealed, and **none of them is a null**

**OUTCOME A — THE CONTROL BITES AT BOTH LEVELS.** The golden menu differs from every non-golden
menu **in shape** (cardinality or structure) **and in value**, and no matched non-golden group
reproduces a five-tone `|cos|` menu carrying its own metallic constant. ⟹ a tone-level prediction
is **golden-discriminating**, the crossing is designable, and item 3 of the 2026-08-12 list is
discharged **affirmatively**.

**OUTCOME B — VALUE-ONLY DISCRIMINATION.** A matched non-golden group **does** carry a
structurally identical menu (same cardinality, same `|cos|` construction) with its own metallic
irrationality in place of `φ`. ⟹ **"lands on a five-tone menu" is GENERIC and must never be stated
as the prediction.** The crossing survives **only** re-specified at the level of specific values,
and **only** above the computed resolution requirement — which this arc then supplies as a hard
number.

**OUTCOME C — NO DISCRIMINATION.** The menus coincide, or the golden menu is contained in a
non-golden one. ⟹ the tone-level crossing is **dead as designed**, and I say so about my own
proposal.

**All three are banked with equal prominence.** B and C are not failures of the arc; they are the
control doing its job before an experiment is contacted rather than after.

---

## 4. WHAT WILL NOT BE ALLOWED TO COUNT

- **No literature, no experiment, no measured value.** This arc is pure finite-group and
  substitution mathematics. **Gate 5 untouched**; nothing enters `CLAIMS.md`.
- **Re-deriving B997 is not a result.** If the conductor route reappears, that is a **control
  passing**, not a discovery.
- **"2I is special" by assertion is not discrimination.** `2I` is manifestly special (it is the
  only one with order-5 elements). The question is whether the **prediction** distinguishes, not
  whether the group does.
- **An abstract isomorphism is not a representation.** *(B8087's lesson, banked at cost: a
  character-twisted build was dimension-45, bracket-closed and rank-5 green, and still wrong.)*
  The tone menu is a property of the **embedding in `SU(2)`**, not of the abstract group. This is
  pinned as a control, not assumed.
- **A near-coincidence of two tones is not a match.** Any claimed agreement must exceed the
  computed resolution requirement, which is fixed in §2.6 **before** it is known.

---

## 5. FALSIFIERS AND CONTROLS (fixed before compute)

- **Group controls:** orders exactly 24, 48, 120; each closed under multiplication and inverse;
  each has **exactly one** element of order 2 (the centre `−1`) — the very property whose failure
  killed `SL(2,ℤ/4) ≅ 2O` in the B997 correction, so it is checked here directly.
- **Representation-pinning control:** every element is a **unit** quaternion (norm exactly 1), and
  the tone is read from `Re(q)` of that unit quaternion, so the 2-dim rep is the defining one by
  construction.
- **Bite control:** a deliberately **non-discriminating** statistic — "the tone lies in `[0,1]`" —
  must be shown to pass for **all three** groups. An instrument that cannot detect a
  non-discriminating prediction cannot certify a discriminating one.
- **Second-route control:** each menu is recomputed from **element orders** (`tone = |cos(2πk/n)|`
  over the order spectrum) independently of the quaternion realisation, and must agree exactly.
- **Trace-map control:** for `m = 1` the invariant `κ` must be preserved (the known Fibonacci
  case). If it fails at `m = 1` the instrument is broken and the arc reports nothing else.

**Prediction recorded before compute, so the arc can be wrong: I expect OUTCOME B.** I expect `2O`
to carry a five-tone menu containing `√2/2` — the silver signature — structurally identical to the
golden five-tone menu, sharing `{0, 1/2, 1}` and differing in exactly two entries; I expect `2T`'s
menu to be entirely rational; and I expect **no** binary polyhedral group to reach `√13`, so
**bronze has no matched partner at all**. If that is what happens, then **my own 2026-08-12
proposal was stated at a genericity that discriminates nothing, and the honest repair is to
re-specify it at the value level with the resolution requirement attached — and I will say so
plainly.**

---

**Author:** cc3, audit seat. Band B8000+ per cc's 2026-08-19 ruling. No merge from this seat.
