# B609 — THE SEALED VALUE EXTRACTION (the Listening Campaign, Phase 2)

**Sealed 2026-07-15, BEFORE any cell below is computed; SHA-256 in the
B598 hash ledger. Gate G1 is open (B608, #978). CC's lane: compute and
seal; NO comparison to any measured quantity happens in this arc or
anywhere until the separate comparison-map prereg (Branch 3) is sealed
and seat 4 approves the null-model design (G2/G3). Firewall: the outputs
are numbers attached to the object's structures; no SM identification is
made here. Provenance: all verification internal.**

## The extraction list (nothing else counts as a Phase-2 value)

**E1 — the Killing-class mixing fractions (the mixing matrix made
quantitative).** For each θ-odd block line (V₄ and V₈, every nonzero
weight, positive side; the Cartan zeros separately): expand in the root
basis (the banked B604/B608 machinery), and compute the fraction of its
Killing-norm² carried by each G_SM class of B608's declared chain.
DECLARED METRIC: the Killing form on e₆ (root spaces mutually
orthogonal; per-root normalization B(x_α, x_{−α}) computed exactly from
the model's trace form tr(XY) on the 27, which is proportional to the
Killing form by irreducibility). The fractions are exact rationals.
CONTROL E1-a (machinery gate): fractions sum to 1 per line.
CONTROL E1-b (banked cross-check): the tips (wt ±16) give fraction 1 in
class q = (1,1,2).

**E2 — the amplitude components through the fractions.** For the two
hearing lines' banked amplitudes (classical: N₄(1+w), N₈(1−w); stage,
via the declared bijection: h₃, h₆): the class components = amplitude ×
the responding line's class fractions. (This is a DECLARED
decomposition — the honest form of the campaign's V2.1/V2.2 given the
mixed Rosetta Table; the components are bookkeeping of banked numbers
through E1's new fractions, so the NEW sealed content is E1 itself.)
For the E₆₂ amplitudes the same, through the m=4/m=8 lines per the
sealed STEP8 bijection; the third E₆₂ channel is recorded as UNMAPPED
(the rank-1 remainder — no classical partner exists; outcome B).

**E3 — the spectral hearing amplitudes at new levels.** The full
spectrum of the odd hearing form B_odd (the B601 construction, SU(3)
level k = κ−3) at κ = 4, 7, 13, in the SPECTRAL basis:
- GATES (pre-determined from banked facts, labeled as gates not
  discoveries): κ=4 has dim_odd = 1 and trace −1 ⇒ its single amplitude
  is exactly −1, REAL (the unit tone is chirality-deaf); trace(κ=7) = 0
  and trace(κ=13) = 0 (the trace law; both κ ∤∈ 4ℤ ∪ 5ℤ);
  κ=5, 10 spectra reproduce the banked golden values (machinery gates).
- THE NEW VALUES: the eigenvalue multisets at κ = 7 (dim_odd = 6) and
  κ = 13 (dim_odd = 36... computed), each to 1e-9 with conjugation-
  closure verified (B601's law), plus minimal polynomials where sympy
  certifies them exactly (best effort, labeled).

**E4 — the extended norm table.** For each new κ = 7, 13 eigenvalue λ
with a certified minimal polynomial: N_ℚ(λ) exactly; else |λ|² to
1e-9. Plus the banked anchors restated for the table's completeness
(1/5 golden, 1/49 E₆₂ — already public, marked PRE-SEALED).

## MB12 triple-check (computed at sealing, per the standing lesson)

- Operations non-trivial: the Killing-class fractions have NEVER been
  computed (E1 is genuinely new); the κ=7/13 spectra are unknown (only
  their traces are banked — 0 and 0 — which constrains but does not
  determine dim-6 and dim-36 multisets).
- Criteria can fail: E1-a fails on any expansion/metric bug; E1-b fails
  if the tip purity does not survive the Killing metric; E3's
  conjugation-closure can fail (nothing forces it at κ = 13 beyond
  B601's tested law — a genuine re-test at a new level).
- Pre-determined items are LABELED as gates (κ=4's −1; the traces; the
  κ=5/10 reproductions) — computed as checks, claimed as nothing.
- Discriminating facts computed now: dim_odd(κ=4) = 1 (weights (1,0),
  (0,1) — one pair); dim_odd(κ=7) = 6 (k=4: 15 weights, 3 self-conj);
  dim_odd(κ=13) = 45−9)/2·… computed in-cell and gated against the
  (n_weights − n_selfconj)/2 formula.

## The seal protocol

This prereg is hashed FIRST. The computation then writes
`SEALED_VALUE_TABLE.md` containing every E1–E4 number; its SHA-256 goes
into the hash ledger in the same bank. No value outside the table
carries Phase-2 status; anything else observed is exploratory FINDINGS.
Comparisons of ANY kind are out of scope (Branch 3, separate seal).

## Run order

E1 (l51-based, heavy) and E3 (b238-based, fast) may compute in
parallel; E2/E4 are arithmetic on their outputs; ONE bank at the end
containing the prereg hash verification, the table, and its hash.
