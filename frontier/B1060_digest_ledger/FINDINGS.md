# B1060 — THE DIGEST LEDGER (instrument): the fixed denominator over the cloud window, OPEN at 58/58 EMPTY

*cc banking seat, 2026-08-13. An INSTRUMENT arc: the object is
`DIGEST_LEDGER.md` — the adopted protocol's step-2 artifact. Verdict OPEN by
design: the instrument banks; the dispositions are the campaign; the digest
closes only at zero EMPTY, and the completion lock ships with the closing arc.*

The ledger holds 58 rows in six lanes: the epoch cell (first, BLOCKING — resolved
same-day on this bench, the fill staged: two grading languages pre-B809, era-tag
don't flip, the sweep rule for the consequence); the 30 cloud arcs qB1024–qB1053;
the manual's 12 rules with the convergence-standard row marked; the instrument
ports (two gates, E37/E38, verdict_drop); the eight findings including the
review-gets-reviewed row; the twelve leads. Vocabulary fixed: ACCEPTED requires
REBUILT; ACCEPTED-WEAK marks RUN; COLLIDES escalates; NOT-REACHED is the honesty
row. Triage on bodies, never verdict fields. The alias table is the resolver;
q-citations throughout. Locks: `tests/test_b1060_digest.py`.
