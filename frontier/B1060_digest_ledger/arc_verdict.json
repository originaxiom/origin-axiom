{
 "id": "B1060",
 "verdict": "OPEN",
 "instrument": true,
 "claim_one_line": "THE DIGEST LEDGER (the adopted protocol's step 2, executed at open): the fixed denominator over the cloud window -- 58 rows (the epoch cell first and blocking; 30 arcs qB1024-qB1053; the manual's 12 rules with the convergence-standard row marked; 2 gate-ports + E37/E38 + verdict_drop; 8 findings incl. their Review 1's review-gets-reviewed row; the 12 leads) -- every disposition EMPTY at open BY DESIGN (cherry-picking made impossible by shape), the vocabulary fixed {ACCEPTED(REBUILT-only), ACCEPTED-WEAK(RUN), CORRECTED, REFUTED, SUPERSEDED, COLLIDES(escalates), NOT-REACHED}, triage on BODIES never verdict fields, q-citations throughout with docs/CLOUD_ALIAS_TABLE.md the resolver. The digest CLOSES only at zero EMPTY; the completion lock ships with the closing arc. OPEN is this arc's honest verdict: the instrument banks, the dispositions are the campaign.",
 "depends_on": ["B1044"],
 "supersedes": null,
 "superseded_by": null,
 "locks": ["tests/test_b1060_digest.py"]
}
