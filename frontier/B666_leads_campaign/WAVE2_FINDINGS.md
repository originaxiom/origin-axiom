# B666 WAVE 2 — TWELVE CELLS ADJUDICATED (main seat, 2026-07-17/18;
# prereg 84e7CG5f-era seals; ten banked here, cellA2 crashed-diagnosed
# (deferred), cell 7 launches on cell 2's table)

## CELL 2 — L106 ANSWERED: THE FIFTH WALL, WITH THE INVARIANT TABLE

The exact 27̄-valued cup table computed and persisted
(cell2_cup_values.json; all controls: G0 banked-Y recompute exact,
coboundary, antisymmetry 25/25, zero floats). VERDICTS: the natural
operator on the generation quotient has **2 distinct eigenvalues of
3** (no mass-shape); every H²-functional slice is a FORCED
antisymmetric {0, +μ, −μ}; the PD/Hodge slice is SCALAR
(Y[234]·δ_ik — maximal degeneracy forced by the alternation); the
three star classes span rank 3 (**one H² direction per generation**)
but their v₀-contractions are normalization GAUGE (the
balanced-product lattice is trivial — no invariant product exists).
**The fifth wall.** What banks positively: the exact value table, the
σ²/swap laws at H² (verified), Y factors through the cup coordinates
(rank(P) = 5, surjective), and the field-of-definition instantiation
— 1081 values, ALL exactly in k(Γ) = ℚ(√−3) (the subfield theorem's
largest single test). CROSS-SEAT (B670-registered): cc2's C1
converges — same 6/6 nonvanishing pattern, same Y-shadow identity;
the witness-vs-coordinate difference is construction (their
functional evaluations; this seat's cup-generated basis), noted.

## CELL 8 — L92 + R21-4 ANSWERED: THE GLUED CUBIC IS NONDEGENERATE

The fiber-paired D_conjθ object built exactly (θ₂₇∘conj; both the
flat and the (C, C*)-intrinsic conventions): h¹ = 5, and **the glued
cubic has rank 5, kernel 0 — NONDEGENERATE in both conventions**
(the ordinary chord's alternating 3-form is degenerate; the
conjθ-glue completes it). The Q-structure joint kernel is 0. The
per-triple table banked (values in ℚ(√−3); the ±1 ± ⅓√−3 family
appears here as the (0,3,4)/(1,2,4) entries). L92 closes; R21-4's
machine was the (i₁,i₂)/MV route as predicted.

## CELL 9 — R21-2 + R21-3: certificates complete

Task A (m = 7/8/11 identifications): **all six exponents CONFIRM
EXACT INTEGERS** (the τ-values; the sealed m=11 digits adjudicated as
input-precision noise — the B627-era hazard resolved; sign law
(−1)^m holds on the exact values). Task B (discrete-branch IDs): the
J-spectrum and branch identifications banked per word (run-1 failure
preserved; run 2 clean). R21-2 and R21-3 tick.

## CELL 10 — L97 CLOSED CLEAN (13/13 gates)

The re-scope table: two items genuinely survived and RAN — **the
sector decomposition** (exact: sector sums (+½, −½)/(−½, +½);
tr_even ≡ 0 on every doubly-generic rung; 7 new ladder rows incl.
**Z₊(30) = Z₋(30) = 2−√5, the FIRST IRRATIONAL SECTOR VALUE** — the
even sector carries the entire resonant value where both channels
resonate) and **the Lemma-1 dependency** (stated + 426/426 instances).
SHARPENING of B656/G3: the mirror-spectrum parenthetical fails as a
necessary condition — witness r = 19 (spectrum-coprime, mirror-generic,
yet tr_even = 1 via the direct channel's Φ₉ saturation); the exact
statement replaces it. Everything else closed by citation.

## CELL B′ — WORD-DEPENDENT (the torsion landscape)

τ_m computed exactly by two independent routes (78/78 agree): the
ratio vector |τ_m|/|τ_1| is NOT family-constant (nor projectively:
witnesses banked); the family-constant content is EXACTLY B617's
theorem (parity sign + the perfect-square structure) and nothing
more. B581's six exterior torsions reproduced exactly as the t=3
gate. THE m=1 LOOKUP (links to H130): the m=1 bend is the ONLY bend
in {1,5,7,11} that changes the banked Y support (switching on
Y[024]); all five m=1-touched components contain CLASS 4 — the
invariant-line class. The exponent-1 mode talks exclusively through
the heart's channel.

## CELL C′ — RICHER, ON BOTH OBJECTS (the phase catalogue)

The dichotomy lemma proved (arg ∈ πℚ ⟺ z/z̄ ∈ μ(K)) and certified
per row two ways (40/40 agree): the invariant phase set is NOT
lattice-confined. First off-lattice phases named exactly: golden
arctan(13√3/3⁸) (the seal's exemplar) and arctan(13√3/7379); silver
analogues certified. The unique off-lattice golden deviation carries
**13** in the numerator — the 13-dial thread deepens: the being end
is arithmetically richer than its hexagonal lattice, and 13 marks
where.

## CELLS R1/R2/R3 — three revivals, three theorems

- **R1 (L63): Q-C = c.** The transport map CONSTRUCTED per B578-D2(a)
  (36/36 exact): σ₁* on the character variety sends χ_g ↦ c(χ_g) ≠
  θ(χ_g) — **the residue transports as conjugation/orientation, NOT
  as θ.** The two-chiralities crux's Q-C lane is answered; the
  T-NORM lineage conflict adjudicated in-cell.
- **R2 (L88): PROVEN.** The hearing matrix's Latin square DERIVED
  from Kac–Peterson: k(i,j) ≡ ±m_i·m_j (mod 7) — the square IS the
  multiplication table of the Galois-orbit coordinates; 29/29 exact
  gates in ℤ[ζ₂₅₂].
- **R3 (L24c/L82): PROVEN at both stages.** The per-term
  Landsberg–Schaar closed form: t_{±w}(κ) = Γ_{±w}(3κ)/(3√det B_w)
  — every term a finite quadratic Gauss sum; the tone-conductor
  identity det(A⊗(±w)−I₄) = det(±w)·det(B_{±w}) proven; **LAW-O's
  mechanism completes** (0/87 tables resisted).

## CELL A′ — crashed at P4 (deferred)

The 351/351′/78 projector construction failed its Casimir slot
search ("no independent slots for the minimal polynomial") after P1–P3
passed (the pairing equivariance gates green). Diagnosed as a
degenerate-slot sampling issue; DEFERRED to wave 3; cc2's loop-4 D2
(the 351 slots) runs the same object on their lane — cross-check or
repair on its arrival.

## Ledger deltas

LAW_MAP: LAW-O row gains its complete mechanism (R3); the hearing-
matrix Latin square → THEOREM (R2); the sector-decomposition identity
+ the 2−√5 value on the silence row; the phase-dichotomy on the
subfield/chord rows; the fifth wall + the cup-invariant table (L106
row); the glued-cubic nondegeneracy (L92 row). OPEN_LEADS: L97, L92,
L106 closed; L63/L88/L24c ticked via the revivals; R21-2/-3 tick.
The Q-C = c answer flows to the two-chiralities thread. Locks:
tests/test_b666_wave2.py.
