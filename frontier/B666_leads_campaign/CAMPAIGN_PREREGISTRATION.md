# B666 — THE LEADS CAMPAIGN (SEALED — owner approved 2026-07-17)
# Every remaining live lead, run as a multi-agent loop campaign in the
# B662 pattern: sealed prereg → waves of parallel cells → main-seat
# verification of every decisive fact → bank per wave → loop until
# exhaustion. House rules bind every agent (exact arithmetic,
# two-outcome criteria, agents never commit, Gate 5 absolute, no model
# names, outputs preserved verbatim).

## SCOPE RULING (what "all the remaining L's" honestly means)

Three tiers, treated differently:
- **TIER 1 — the live cells (this campaign's core):** L91(1–3), L92,
  L96, L97-remainder, L98, L105, L106, L107-remainder, L108, plus the
  R21 math items (R21-1, -2, -3, -4, -5, -9, -11).
- **TIER 2 — coordination-bound (NOT run here; tracked):** L95/R21-8
  (chat1's seal — verify-on-receipt), L107's cross-landscape matrix +
  A2's dyadic/deep-3 remainder + S1b (cc2's seat), R21-7 (the
  specialist bar — the dossier waits).
- **TIER 3 — the historical ledger (L5–L88 era):** ~60 leads from
  closed or superseded program lines. NOT blindly executed — wave 0
  triages every one into SUPERSEDED / STILL-LIVE / TOOL-GATED /
  HISTORICAL, with a revival shortlist (≤ 3) that joins wave 2 if
  genuinely live. Anything else gets its honest tombstone. This
  cleans the ledger instead of pretending 60 stale cells are work.

## WAVE 0 — THE TRIAGE (one cell, runs first)

- **CELL T:** sweep the full OPEN_LEADS historical block; for each
  lead: status vs the banked record (many are answered by later
  theorems — e.g. L77's clock is now the conductor-clock law;
  L26/L28's McKay questions partially absorbed by B644/B662-I);
  classify, cite the superseding arc, shortlist true revivals.
  Deliverable: the triage table + the ≤3 revival cells, specced in
  the wave-2 format. (Effort: one agent, repo-only.)

## WAVE 1 — INDEPENDENT COMPUTE CELLS (six agents, parallel)

- **CELL 1 — L105, the 2O/E7 silver hearing.** Build the silver
  word's hearing group at ITS OWN conductor (the mod-8 shadow of
  RRLL); test binary-octahedral structure (order 48, the √2
  character); decide: does the metallic ladder descend the
  exceptional series (golden 2I/E8 → silver 2O/E7)? Machinery: the
  B640/B644 shadow apparatus + B238-style stage data at the silver
  level. Outcomes: 2O CONFIRMED (a new McKay rung; LAW row) /
  NOT-2O (the conjecture dies; bank the actual group).
- **CELL 2 — L106, the cup-class values.** Compute the exact values
  of the nonzero classes [u_i ∪ u_j] ∈ H²(D;27̄) on the generation
  triple (cellH's machinery); their σ*-behavior; the discriminating
  question: does ANY function of them distinguish the three
  generations (three distinct values on a natural slice)? Outcomes:
  a distinguishing structure (the first one past four walls) /
  degenerate again (the fifth wall, with the values banked).
- **CELL 3 — L107-E₆, the E₆-level landscape.** The two-term/
  shadow-class method at the E₆ level-2 stage: is the three-value/
  five-tone structure stage-universal? Plus the generating-function
  form vs the Dirichlet character mod 5 (now theorem-adjacent via
  B664's closed form). Outcomes: stage-universal (the landscape
  becomes a stage-independent law) / stage-specific (bank the split).
- **CELL 4 — R21-9, the pair-evenness lemma.** Abstract proof of the
  ramified-prime step of the sign-hears-the-discriminant theorem
  (currently exhaustively verified). Pure algebraic number theory;
  the statement is one page. Outcomes: PROVEN (the theorem's last
  asterisk falls) / the precise obstruction.
- **CELL 5 — R21-11, the b353 leaker.** Deterministic bisection of
  the test-suite prefix that makes test_b353 fail in-order (E12
  instance); fix = per-test isolation of the leaked state. Outcomes:
  localized + repaired / localized + priced.
- **CELL 6 — L96, the lock-gap tickets.** The two unreproducible
  banked figures (B544 headline, B480): recover the scripts from git
  history if they ever existed, re-derive from the FINDINGS'
  described method, or demote the figures with dated notes. Outcomes:
  recovered / re-derived / demoted (all three bank; hygiene closes).

## WAVE 2 — THE MACHINERY CELLS (after wave-1 banks; four agents)

- **CELL 7 — R21-5, the 13-dial mechanism.** The two-object data
  (fig-8: 13-divisible deviations, 13 split in ℚ(√−3); silver:
  13·211-adic, 211 inert in ℚ(i)) against candidate mechanisms —
  the norm map on the lit-class certificates; the cellH cup-values
  (cell 2's output feeds this). Outcomes: mechanism / sharpened
  constraint set.
- **CELL 8 — L92 + R21-4, the fiber-paired object.** Build θ₂₇∘conj
  exactly (the invariant 27⊗27̄ pairing + weight negation from B575's
  data); realize the glued local system on the double; compute its
  (i₁, i₂) → the reduction gives its dimension grammar (B656/G5's
  machinery — this is R21-4's MV machine, now one cell); the twisted
  cubic if h¹ ≥ 3. Outcomes: dimensions + cubic banked / the exact
  obstruction.
- **CELL 9 — R21-2 + R21-3, the identification pair.** The m = 7/8/11
  exact identifications (B598/B624's apparatus) and the
  discrete-branch IDs per word (B626's Jacobian-reality apparatus) —
  one agent, two related ledgers. Outcomes per item: identified /
  bounded-negative.
- **CELL 10 — L97-rescope.** What actually remains of the ladder's
  proof queue after A2 (the resonant-phase law), A3 (the central
  character), cell G (the exact period), and the PSL-factoring
  theorem: re-scope, then either run the remainder (the sector
  decomposition; the Lemma-1 dependency statement) or close items
  with their superseding citations. Outcomes: the remainder run /
  the queue closed clean.

## WAVE 3 — THE THEOREM CELLS (the hard ones; three agents + synthesis)

- **CELL 11 — R21-1, the κ-unconditional reciprocity form** (B625's
  target). Now attackable with cell G's exact character-sum
  certificates (the 130-frequency support table is the reciprocity
  data). Outcomes: the unconditional form / the precise conditional
  boundary.
- **CELL 12 — L91 obligations (1)–(3), the stage-selection theorem.**
  Why the SU(3) modular family; why 5|κ becomes equality; why
  minimal bearing. Inputs now in hand: the landscape theorem +
  shadow-class law (B664/B665), the conductor-clock law, the γ₅′
  identification (B662/I), and — if landed — cc2's diagonal
  hypothesis (the falsifiable fixed-point form). Outcomes per
  obligation: theorem / hypothesis-link stated exactly (either
  banks; this is the program's hardest open theorem — a bounded
  honest attempt is the sealed expectation).
- **CELL 13 — L108, the γ₅′ functor's last leg.** Produce the
  weight-5 doublet forms from the framework's own tower: the
  candidate construction is the theta/eta-quotient assignment over
  the tower's τ (cell I's Sym^{5k} structure names the target
  exactly). Research + construction; the campaign's moonshot.
  Outcomes: the assignment (the functor completes — a program-level
  event) / the named missing ingredient.
- **CELL 14 — L98, the one-organ statistic.** Design (not push-depth)
  a statistic separating one-organ vs two structurally; register it
  as a sealed falsifier for the next data pass. Outcomes: the
  statistic + its power analysis / the impossibility note.

## CLOSE-OUT

Campaign synthesis (the tier-1 table + the triage ledger + the tier-2
handoff states); ledgers per wave (LAW_MAP rows where laws land;
OPEN_LEADS closures with citations; R21 items ticked); the owner
report. Loop protocol: workflow notifications primary, fallback
wakeups; the campaign continues autonomously wave to wave; STOP
condition = every tier-1 cell banked on a sealed outcome + the triage
tombstones written.

## Budget honesty

~15 agent-cells across four waves (wave 0: 1; wave 1: 6; wave 2: 4;
wave 3: 4), main-seat verification after each wave — comparable to
two B662s. Cells 12 and 13 are genuinely hard and may bank as
bounded-honest attempts; everything else has a decisive computation.

*(Sealed on the owner's "aprove", 2026-07-17; draft hash 7cb58f3a; the sealed hash is recorded in ARTIFACT_HASHES.txt.)*
