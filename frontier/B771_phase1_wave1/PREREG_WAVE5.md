# B771 PREREGISTRATION ADDENDUM — WAVE 5 (the final mechanical wave; 8 cells)

*Sealed before any cell computes. This drains the Phase-1 mechanical pool: the 3
Wave-4 carries (each with its named defect) + the last 5 pool rows. Same binding
method block as Waves 1–4. Cells write under cells/W5-*/.*

## The 3 Wave-4 carries (fixes named)
- **W5-017r**: strip the fabricated "verified in-cell" scope comment from W4-017r's
  compute.py (the class-3 degree-10 claim the code never solves) and RE-VERIFY the
  genuine result stands: L6a4 census class 10 non-empty, degree-4 quartic
  t⁴−9t³−12t²+27t+9, all degrees {2,4,8}, Ruelle gap ≥0.19. Outcome: correction
  stands with no fabricated narrative => RESOLVED-A / the class-10 point does not
  reproduce cleanly => RESOLVED-B.
- **W5-139**: complete W4-139 — either prove genus(A₃)≥1 via an actual birational
  Fxz→G map + Riemann–Hurwitz (compute it, don't assert it) and deliver the m=3
  spectral genus, OR re-tag the census row OPEN/partial (the eliminant computes,
  doesn't wall; the genus is the residual). Outcome: genus computed => RESOLVED-A /
  honest EXTERNAL with the residual named => RESOLVED-B/UNRESOLVED.
- **W5-194**: remove W4-194's vacuous B3 (open-closed differential, |det|-invariant
  by construction) and re-confirm the H121-dissolves negative on the NON-vacuous
  legs (B1 algebraic + the hardening sweep). Outcome: negative holds on non-vacuous
  evidence => RESOLVED-B / it does not survive removing B3 => re-open.

## The last 5 pool rows
- **W5-100** (OI-100): the band-method D₀ estimator (Damanik–Tcheremchantsev gate)
  fails to converge at V=2; implement the named adaptive tracker and report whether
  D₀ converges honestly. Outcome: converges with the tracker (value + conditioning)
  => RESOLVED-A / does not converge even adaptively (the gate is genuinely
  inconclusive at V=2, stated) => RESOLVED-B.
- **W5-105** (OI-105): compute the σ-signature (±1)⁶ pattern on the six H¹ directions
  of the E6 geometric point (the object-native part; T[M3,E6] class-S stays
  NEEDS-SPECIALIST, do NOT attempt it). Outcome: the (±1)⁶ signature computed with
  its structure => RESOLVED-A / it degenerates/is basis-dependent => RESOLVED-B.
- **W5-180** (OI-180): resolve the 31-collision tension (B406) — compute where 31
  arises on each side and whether the collision is a genuine identity or a base-rate
  coincidence (E20). Outcome: one identity behind both 31s => RESOLVED-A / the two
  31s independent (base-rate) => RESOLVED-B.
- **W5-204** (OI-204): the being-hand defect pattern (3-adic law self-generates,
  defect ≤2, exact at n=4). Compute the defect d(n) as far as the available GSWZ
  coefficient tower allows; if the full pattern needs the complete tower (unavailable
  in-sandbox), deliver the partial pattern + the honest EXTERNAL boundary. Outcome:
  full defect pattern computed => RESOLVED-A / partial + EXTERNAL at the tower limit
  => RESOLVED-B. Gate 5-Q: 'being'/'hearing' are the object's own structural labels.
- **W5-335** (OI-335): compute B647's named UNTESTED triples for the gauge-covariant
  defect law (the analytic trilinearity proof covers them in principle; verify on the
  explicit untested triples). Outcome: untested triples satisfy the law => RESOLVED-A
  / a triple violates it (the covariance is not universal) => RESOLVED-B.

## Method: the binding B771 block (exact preferred; ≥2 seeds; in-cell verdict logic
able to emit UNRESOLVED; results.json; no fabricated controls / comment-only
diagnostics / tautological theaters / hand-reads; Gate 5/5-Q; nothing to CLAIMS).
Launches only after B773 (the chord re-computation) completes — one heavy workflow
at a time to avoid the contention kills seen in W3/W4-270r.
