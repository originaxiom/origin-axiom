# B1195 — CLOSE-LOOP BATCH 5A

**Verdict**: `PROVED` · **instrument**: false · **creates_law**: false
**Banked**: 2026-08-28 (`256521f1`) · **This document authored**: 2026-08-29 (R52-4 discharge, B1207)

> **Provenance of this document.** The arc banked its verdict, its results file and its
> verification artifacts, but no findings document -- the gap B817's writer-safety gate caught in
> the first full OA_SLOW run (R52-4, 2026-08-29). What follows is authored **from this arc's own
> banked record**: `arc_verdict.json` is primary, with `b1195_results.json` and `verification/` beside it.
> Nothing is supplied from memory and no computation is re-narrated that the record does not
> carry -- the exact mirror of B1176's thirteen retro `arc_verdict.json` files, where FINDINGS was
> the primary and the verdict the missing half. Section 1 is the banked claim, segmented at its
> own enumeration; section 2 lists the artifacts that certify it.


## 1. The finding, as banked

CLOSE-LOOP BATCH 5A (the five highest-leverage closes, merged with cloud's routes at their 9915068b):

**(1)** SEAM-A's CS=0 DOOR SHARPENED (lens-scoped) -- the object's b=1 partition function factorizes as Z = (omega/sqrt-3) * 2 sinh(Vol/2pi) (GK-cited closed form confirmed at 7.6e-8 with specific competing phase-ansatze excluded at >= 5x10^6 times the residual); the SADDLE EQUATION IS THE FOUNDING QUADRATIC u^2+u+1=0 exactly; Hessians +/- sqrt(-3); the two saddle real parts EQUAL (the CS=0 degeneracy, unique to k=0 -- m003 same-volume control at k=3, m015 excluded at 1.5e14x); the prefactor alpha = omega/sqrt-3 with N(alpha)=1/|d_K| whose zeta12*3^(-1/2) split lives exactly in codex's ring class field H = K(sqrt3), and the b=1 quantum dilog's inversion constant IS zeta12 -- the Arakelov/W0 bar survives with an exact object-side floor and a new named hinge; the lens's overreach corrections adopted (exclusions are high-confidence ansatz-specific, not a universal proof).

**(2)** LAMBDA PLACED: P1 -- a SECOND independent EXTERNAL continuous observer datum, distinct in kind from sigma; the KMS/BTZ reduction route (P2) proved TAUTOLOGICAL by exact constructive computation -- both seats' top residue DECIDED; the continuous count amends to sigma AND lambda.

**(3)** THE E6-LATTICE CUSP ROUTE CHECK-FAILS STRUCTURALLY (cusp-boundary Grams are orthogonal direct sums; E6 is indecomposable -- dead at any scale/cover) -- reconciled with cloud's memo 104: their ALGEBRAIC boundary object (E6 as free rank-3 Z[omega]-module, disc group F_3 via the cusp-modulus theta, primaries {1,27,27bar}, c=6) is the living half; the bridge's one remaining object stays the record-side q-series map (new mathematics).

**(4)** THE RELATIONAL BIT'S LAW FOUND (survives): the classification is NOT the partner's unit norm (the charter conjecture falsified) but KAPPA(A,M) = tr(A)^2+tr(M)^2+tr(AM)^2-tr(A)tr(M)tr(AM)-4 -- THE PROGRAM'S OWN FOUNDING FRICKE INVARIANT (the same kappa whose kappa-2=omega^2 founds existence) via the exact identity gen_det = -kappa/g^2 -- THE EXISTENCE OBSTRUCTION AND THE OBSERVER-BIT CRITERION ARE ONE INVARIANT.

**(5)** THE P^3 LINE IS PERMANENT-AT-CURRENT-KNOWLEDGE: every banked symmetry that could touch B_0 is trivial (C12 by construction; the frame V4 = Gal(Q(zeta12)/Q) PROVED trivial by direct computation) -- the floor definitively carries dim_C P(B_0) = 3 continuous parameters. THE MISSING LIST AFTER 5A: items 1 (sharpened, specialist bar exact), 2 (CLOSED), 3 (halved: algebraic side built, q-series map remains), 4 (LAW FOUND, general proof of the kappa identity = the refinement), 5 (CLOSED-PERMANENT) -- three of eight closed or decided, two sharpened. Gate 5 clean.

## 2. The certifying record

- `arc_verdict.json`
- `b1195_results.json`
- `verification/batch5a_cells.json`

## 3. Status at authoring

The verdict field is authoritative and unchanged: `PROVED`. This document does not re-adjudicate it; it makes the arc readable without opening its JSON, which is the whole function the missing file was performing badly.
