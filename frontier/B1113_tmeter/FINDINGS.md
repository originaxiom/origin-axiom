# B1113 — THE t-METER: the dial, unmeasurable on one closed object, is a class function of the coupled pair (measurement-by-coupling, verified)

**Status: banked (frontier). Verdict PROVED. Harvest arc (the JORDAN_MEMO §B; cloud
seat credited) — QUADRUPLE confirmation: the outside probe + TWO fully independent
verifier benches (each never imports the certificate; each with its own crystal-27
BFS, own module action re-certified on all 3003 Chevalley pairs, own nilpotent
exponentials) that INDEPENDENTLY reconstructed the same operators (slot X8, group
commutator) and reproduced the SAME 12/12 exact field elements — including 40-digit
integers that cannot agree by accident — plus the banking seat's own re-run. One
verifier added a fifth control the memo never ran: h¹(M;27) = 3 by Fox calculus,
matching B1086's banked number. Gate 5 untouched. Lock `tests/test_b1113_tmeter.py`.**

## THE CLAIM, VERIFIED EXACT

On the twisted double of the figure-eight complement, with the θ-odd dial parameter
t ∈ {0, 1, 2, ω}:

- **tr(A·B_R(t)) is t-INDEPENDENT** — constant `141750 + 1011915q` at every dial value
  (and at a fake fifth control t = 3). Seam crossings commuting with the dial are
  **dial-blind**. Proven at the Lie level, not just numerically: A commutes with the
  dial slot ([e, X₈] = 0 for all t), so tr(A·B_R) = tr(A·B_L) identically —
  t-independence is FORCED.
- **tr(B_L·B_R(t)) and tr([B_L, B_R(t)]) SEPARATE all four dial values** — four-way
  pairwise-distinct exact field elements (the group commutator, confirmed: the
  additive commutator trace is identically 0, so the memo's "[B_L,B_R]" must be
  B_L B_R B_L⁻¹ B_R⁻¹, and that reading reproduces the numbers).

## THE LAW

> **Free data of one object = forced (character-level) data of the coupled pair.**

The dial is free and unmeasurable on a single closed copy (B1087: the charge is
undefinable on any closed assembly — non-commutation, O3). On the COUPLED PAIR it
becomes an exact class function: an explicit **Wilson-line meter for t** exists, and
it lives on seam-crossing words only. This is **measurement-by-coupling** made
concrete — the observer measures free data by forming mixed words with a second copy
(interference, not inspection) — and it instantiates the mechanism the whole value
question turns on.

## Consequences (banked, cited)

- Re-derives B1016's two value channels at the dial (B856 coupling-forced /
  B936 pair-frame-relative).
- Upgrades L79 sub-item iii: the dial-switch is literally a seam observable.
- B1087-consistent: the ENTIRE peripheral/cusp subgroup (meridian A + longitude) is
  dial-blind — Lie-level and numeric; the interior generator B is not.

## Verification chain / fences

Positive controls all passed: the D(t) entries take 5 distinct values across the dials
(the pipeline demonstrably sees t); the classical two-bridge relator on the 27; the
longitude commuting with the meridian at trace −2 in the ℚ(√−3) rep, cross-checked
against this repo's own locked `test_b1086_spectrum_law.py` (independent of the
certificate). The operator reconstruction (slot X₈ not X₁₆; B_R := D·B_L·D⁻¹) was
pinned by calibration against the 12 reported values — two nearby conventions scored
9/12 and 6/12 and diverge sharply on the large integers, ruling out coincidence; the
reconstruction is documented in the notes so the next bench can audit it. One cosmetic
display bug in the memo's original stdout (a "+−" glued sign) noted; values correct.
Live re-run needs the session certificate for the memo's own probe path; the banking
seat's verifier is certificate-free and IS the lock's slow gate.
