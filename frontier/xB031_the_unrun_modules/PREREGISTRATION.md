# xB031 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-18. Sealed, hashed and pushed before any cell runs.**

## P0

**xB030 established the cap and named what it does not cap.** `h¹ ∈ {0,1}` on 506 informative closed
hyperbolic rational homology spheres, so `|I| ≤ h¹ ≤ 1` closes the **`m = 1` / abelian** route to a
count of three. **B1418's own table breaks past that at `m = 3`: `|I| = 2` on `t12835`.** And B1418
stopped on a **1 800-second-per-member budget**, leaving **3 325 modules on `t12835` NOT RUN** — its
words: *"a fourth state, never folded into zero"*.

**This arc runs them.** It is the computation L222(iii) names and xB030 promoted to first.

## THE FENCES — DECLARED BEFORE THE FIRST NUMBER, NOT AFTER

**These bind whatever the cells return, and especially if they return a 3.**

1. **`I^ss = 0` everywhere in B1418's table and is expected to stay 0.** The index is non-zero only on
   the **non-semisimple** module; its semisimplification is identically zero.
2. **Menal-Ferrer–Porti Theorem 0.1, via B1413 R27, makes this index a THEOREM-ZERO on the geometric
   finite-twist class.** A non-zero value **requires leaving the geometric holonomy**. These modules
   are **not** the geometric representation and **not** unitary.
3. **`t12835` is a member of the figure-eight's commensurability class. IT IS NOT THE OBJECT.**
   `m004` itself gives **0 on every module B1418 ran** (100 and 135 of them, two fields).
4. **`|I| = 3` WOULD NOT BE "THREE GENERATIONS".** It would be an integer index on a non-geometric
   flat `SL(2)`-module reaching 3. **Gate 5 absolute. Nothing to `CLAIMS.md`. No physics reading is
   licensed by this arc under any outcome**, and if the write-up reads as one it is over-claiming.

## THE CONFIGURATION AXES — declared first

| axis | values | this arc |
|---|---|---|
| **manifold** | `t12835` / `t12833` / others | **`t12835` first** (it holds the only known `\|I\| = 2`); `t12833` if budget allows |
| **field** | `ℚ(ζ₁₂)` | **as B1418: `ℚ(ζ₁₂)`, `S` = the twelve 12th roots of unity** |
| **`m`** | 1, 2, 3 (B1418's range) / **4, 5, 6 (NEW, declared here)** | **both — X2 completes B1418's range, X3 extends it** |
| **twists `ψ`** | characters of order dividing 6, plus `χʲ` | **as B1418, unchanged** |
| **budget** | B1418's 1 800 s | **removed; X2 runs to completion** |

## THE CELLS

**X1 — THE CONTROL, AND THE ARC REPORTS NOTHING IF IT FAILS.** Re-run `t12835` on the loci B1418
reached and reproduce its banked numbers: **`|I| = 2` at the loci of generator-orders `(3,12,1)`,
`(3,12,2)`, `(3,4,1)`, `(3,4,2)` at `m = 3`**, `I` values `{−2,−1,1}`, **`I^ss = 0` throughout**, and
B1418's own `m010` witness `I = +1` from `control_m010()`.
*Prediction:* exact reproduction.
*Kill, binding:* **any disagreement and this arc reports NOTHING about the unrun modules** — a
borrowed instrument that cannot reproduce its owner's banked result is not measuring what it claims.

**X2 — THE 3 325 UNRUN MODULES, at `m ≤ 3`.**
*Prediction:* **`max |I|` stays at 2.** The unrun set is later loci and later twists of the same
kind, so this seat expects more `±1` and possibly more `±2`, and **no 3**.
*Required reporting:* the **NOT RUN** count must reach **zero**, or the remainder must be stated as
NOT RUN and never folded into zero — **B1418's own rule, inherited verbatim.**

**X3 — THE EXTENSION: `m = 4, 5, 6` on the firing loci. THIS IS THE CELL THAT COULD PRODUCE A 3.**
The banked values on the four `|I| = 2` loci run `m = 1 → ±1`, `m = 2 → ±1`, `m = 3 → ±2`.
*Prediction, sealed and specific:* **`|I| = ⌈m/2⌉` on those loci**, hence `m = 4 → 2`, **`m = 5 → 3`
— the first 3 appears at `m = 5`** — and `m = 6 → 3`.
*Kill, binding:* **`|I| ≤ 2` throughout `m ≤ 6` refutes the growth law**, and with it the last route
this class has to an index of 3. **That outcome must be reported as the headline, not buried as "no
change".**
*Control against a runaway:* `I^ss` must remain **0** at every `m`. **A non-zero `I^ss` means the
construction has drifted and the numbers are void**, not that something was found.

## PROVENANCE

`c2_reducible_index.py` and `c2_run.py` are **copied BYTE-IDENTICALLY** from B1418 (`diff` recorded in
the findings). **No line is adapted.** This arc supplies only a thin driver that calls B1418's own
`run()` with different `MS` and budget. **The instrument is B1418's; the axes are this arc's.**

## WHAT THIS ARC WILL NOT CLAIM

Not a generation count · not a physics reading · not that `t12835` is the object · not that a non-zero
`I` is geometric (B1413's theorem forbids it) · **no crossing of Gate 5** · nothing to `CLAIMS.md`.

## THE SEAT'S PRIOR

X1 reproduces · X2 finds no 3 · **X3 is the real test and this seat predicts `|I| = 3` at `m = 5`** ·
**and if that arrives, the correct response is the four fences above and not a headline** — the
record has been here before with an index that looked like a door and was a truncation (B1345's
question, *"is `I = t₀ − r₁` an index, or a truncation?"*, which is still open).
