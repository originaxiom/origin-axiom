# B1015 — the anchor set, declared and sealed (R11 step 0)

**Date:** 2026-08-10 · sealed `4acb8f7a` · executes `CROSSING_REQUIREMENTS` R11's step 0.

**Verdict: PROVED** (a binding declaration; content in `DECLARATION.md`, which is the sealed
artifact). **A1 = ℓ** (dimensionful unit). **A2 = c = 6σ** (the unquantized level — the one the
object provably cannot see, B1012). Alternatives priced ({c, α_em}: one bit — α_em consumes an
output-side quantity as a standing anchor; c consumes nothing). **The coupling channel is
anchor-free** (B1011's values are pure algebraic numbers) — on it, a single kind-admissible output
is non-vacuous. The declaration maps R11's allowance onto the framework's typed input list exactly
— **L151's MATCH shape, recorded as evidence, not closure.** Re-declaration = a new sealed act.
