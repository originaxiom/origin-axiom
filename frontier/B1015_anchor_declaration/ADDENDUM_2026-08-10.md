# B1015 addendum v2 (B1023, after cc3's two blockers) — the sealed DECLARATION is unedited; two of its lines are corrected here

The sealed `DECLARATION.md` (hash `4acb8f7a…`, untouched) contains two lines now known wrong:

1. *"licensed by theorem"* for A2 — **withdrawn**: the frame theorems prove a **discrete** deficit
   (and B936's frame classes are not yet identified with B782's torsor bits — **L153**; deficit
   range 2–4 bits). A2's permission rests on the **unit freedom + B915's precedent alone**.
2. *"the one level the observer can set and the object cannot"* — **withdrawn**: B1012 proves
   blindness in **k only**, and its own formula gives **∂S/∂σ = −Vol ≠ 0** (sighted in σ);
   **σ = 1 is a candidate object-forced value** (c((E₆)₁) = 6 = B254's split; c = 6σ) pending
   **L154**'s same-σ question. **If L154 lands σ-fixed, A2 is not an anchor but an OUTPUT** — the
   declaration's anchor SET stays operative meanwhile, its epistemic status marked HELD-PENDING-L154.
