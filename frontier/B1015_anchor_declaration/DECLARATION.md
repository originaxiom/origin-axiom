# THE ANCHOR DECLARATION — sealed under R11, before L150 and L149 (step 0)

**Date sealed:** 2026-08-10 · **Seat:** cc · **Authority:** `CROSSING_REQUIREMENTS` R11
(owner-approved 2026-08-10) — *the anchor's identity is sealed in advance; a post-hoc anchor is a
fit.* **This declaration binds every future crossing. Changing it is a re-declaration: a new
sealed act, priced, with the old one retained.**

## The set — two anchors, identities fixed

> **A1 (dimensionful): ℓ — the length unit.** The single scale in which every dimensionful bulk
> quantity is expressed (Brown–Henneaux c = 3ℓ/2G; σ = ℓ/4G; the on-shell action — the B1012
> closure). Its SI realization is a convention, not content: **no dimensionless number flows from
> A1** (the weight ledger; `WHAT_WOULD_COUNT` §2 — nobody derives the metre).
>
> **A2 (dimensionless frame): c — the central charge, equivalently σ = c/6, the UNQUANTIZED
> gravitational level.** The one continuous dimensionless external coupling the framework's own
> analysis names: the object is **provably blind to the quantized level k** (∂S/∂k = −CS ≡ 0,
> B1012), so **σ is the only level the observer can set and the object cannot** — cc3's S2 split
> ("one external input is really two, of different weight"), verified in the B1012 closure, where
> **c = 6σ is forced** and its freedom is *explained* (the surviving level is the unquantized one).

## Why these and not alternatives — the pricing ledger (R11 condition iii)

**Candidates considered for A2:** {c, α_em(M_Z)}. **α_em was B915's input** ("the observer's own
probe") and remains the natural *contact-time* probe — but as a *standing* anchor it consumes an
SM **output-side** quantity, worsening the outputs − anchors count of every future cell, and it is
scheme/scale-dependent (kind hazards). **c is the framework's own typed input** — anchoring on it
consumes nothing from the prediction side. **Price recorded: one bit of designer freedom** for
this binary choice, charged to the standing look-elsewhere ledger.

## The observation that makes the declaration cheap (recorded, not claimed)

**The coupling channel's forced values are ANCHOR-FREE.** B1011's theorems make the tone and
mirror sets pure algebraic numbers — {0, 1/(2φ), 1/2, φ/2, 1} and the quarter family — consuming
**zero** anchors. For that channel, outputs − dimensionless anchors = outputs − 0, so **a single
kind-admissible output would be non-vacuous.** (The kind gate still fronts any SM contact; B856's
JUNO refutation is the worked warning.)

## Relation to L151 (evidence, not closure)

This declaration maps the R11 allowance onto the framework's typed input list **exactly**: the
𝔽₂ bits and the Lie type are discrete (no continuous anchor needed); the scale slot is A1; the one
continuous dimensionless input is A2. **That is the MATCH shape L151 predicts — recorded here as
evidence toward it, not as its closure**: L151's test is whether the minimal *working* anchor set
of an actual crossing equals this declared set, and no crossing has run under it yet.

## Vacuity discipline (R11 condition ii)

Any future cell consuming A2 must show **independent dimensionless outputs ≥ 2** (outputs − 1 > 0),
or use only anchor-free channel values. A cell that cannot state this does not seal.
