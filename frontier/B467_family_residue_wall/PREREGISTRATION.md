# B467 — PREREGISTRATION: the owner's three thoughts (family / residue / wall)

**Committed BEFORE computation. Firewalled. Source: the owner's three questions
(2026-07-07) + Chat-1's synthesis ("compute them all and treat this seriously").
The S054 whitelist + burden-inversion apply verbatim.**

## Corrections registered BEFORE computing (Chat-1's factual layer)

- The metallic family are once-punctured-torus BUNDLES: m=1 = the figure-eight, m=2 =
  the silver bundle = census m136 — **NOT 5₂** (5₂ is NOT fibered, banked B449, so it
  cannot be a punctured-torus bundle; twist knots ≠ metallic family). Chat-1's "three
  knots" framing is corrected to "three family members" from birth.
- "Discriminants 5, 8, 13 — almost Fibonacci": true for m = 1,2,3 and DIES at m=4
  (m²+4 = 20 ≠ 21). Recorded as a small-number coincidence unless it earns more.
- Chat-1's decimals are wrong (α₁/α₂ = φ/(1+√2) = 0.67018…, not 0.6686; α₂/α₃ =
  0.73096…, not 0.7346). All values recomputed exactly here.

## Cell F1 — the family-ratio value scan (vs CKM)

**Killed-trap flags binding from birth**: B428 ("3 = generations" is the killed catalog
trap); HELD(value-matching) per B322/B398 — NO match can fire except through an explicit
complexity-capped closed form + an out-of-sample second prediction + B398 escalation with
the owner present. This cell's deliverable is a QUANTIFIED look-elsewhere verdict, not a
match list.

- Inputs (exact): α_m = (m + √(m²+4))/2 for m = 1,2,3; the three ratios r₁₂ = α₁/α₂,
  r₂₃ = α₂/α₃, r₁₃ = α₁/α₃.
- Targets (PDG-shaped, fixed now): sin θ₁₂ = 0.2250, sin θ₂₃ = 0.0418, sin θ₁₃ = 0.0037,
  δ_CP = 1.144 rad; also quoted as angles θ₁₂ = 13.04°.
- Function class F (fixed now, complexity-capped): for x ∈ {r₁₂, r₂₃, r₁₃, r₁₂/r₂₃,
  r₁₂·r₂₃, r₁₂−r₂₃}: f ∈ {x, x², x³, √x, 1/x, x/(1+x), (1−x)/(1+x), (1−x), log x,
  arctan(x), x/π, x·π/4} plus one level of composition of two listed f's. Nothing outside
  F is scanned.
- Tolerance: relative error < 1e-3 counts as a "hit."
- **Null model (the verdict-maker)**: 10⁴ random triples (u₁,u₂,u₃) uniform in the same
  ranges as (r₁₂, r₂₃, r₁₃); identical function class and tolerance; the null hit-rate per
  target calibrates every observed hit. Bins: hit-rate ≈ null → LAUNDERS (look-elsewhere);
  hit with null-p < 0.01 AND closed-form AND out-of-sample → B398 escalation (owner
  present) — no other path fires.
- H112 fold-in (exact, not a scan): the family table tr(A_m A_n) for 1 ≤ m ≤ n ≤ 6
  (candidate identity tr = (mn+1)² + m² + n² + 1, verify) against the pair-seam/conductor
  data — records whether the (1,2) coincidence tr = 15 = seam level generalizes or dies.

## Cell F2 — the parity residue (the smallest uncancelable bit)

- **Determinate version (computed)**: exact permutation signs of the classical actions on
  the discrete phase-space torus (ℤ/15)²: A₁, A₂, A₁A₂, −I, −A₁A₂, and σ_ab = [[1,1],[1,0]]
  (det −1); plus the Galois scalings x ↦ cx. Signs of linear maps on finite tori are
  classical (Jacobi-symbol-shaped) — the adjudication is derivability, stated per result.
- **Chat-1's per-point version** (sign of "the monodromy permutation at each of the 240
  torus points", summed): depends on their STILL-UNSHARED loop construction — recorded as
  awaiting-construction; not computable as posed.
- Naming rule: "the one bit = chirality / matter-antimatter" is HELD (value-matching /
  naming); the banked ℤ/2 residues it echoes are cited (B356 chirality window = ω; B289
  sign law).

## Cell F3 — the wall transfer function child(p)

- **GATE FIRST (decisive for Chat-1's premise)**: verify or refute "4₁(5,1) ≅ 5₂(5,1)"
  (their "two different parents produce the SAME child"). SnapPy volumes + is_isometric_to,
  both orientations. If FALSE, the two-parents-one-child story is corrected at the root
  and the cell proceeds as a single-parent transfer-function table.
- The table: for parents 4₁ and 5₂, slopes p = 1..12 (and (−p,1) for chirality): volume,
  CS, H₁, shortest geodesic. Banked anchors that must reproduce: 4₁(5,1) torsion
  H₁ = ℤ/121? (banked: torsion 121 = 11²… hmm — reproduce whatever the banked C-program
  values are: CS = ±0.07703818 per B434.)
- **Lit-frame (blocking)**: the smooth structure of child(p) is Neumann–Zagier asymptotics
  (vol(p) = vol(∞) − c/p² + …, CS quasi-periodic corrections) — KNOWN theory, cited; the
  cell's honest content is the exact table + which invariants the wall forgets vs
  transmits, read against the banked conductor law (B449) and Inversion Law.

## Bins (all cells)

LAUNDERS (null-rate hits; derivable signs; NZ-shaped smoothness) / NEW-MATH (an exact
family identity or table law with out-of-sample confirmation) / H1 only via B398 with the
owner present. Every naming ("generations", "chirality", "black hole") HELD at birth per
the standing rules; the S-room gets the narrative mapping, FINDINGS get the arithmetic.
