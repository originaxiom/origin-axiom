# C2 — THE COMPARISON DESIGN (Phase C; campaign a463c6aa; B653)

**Sealed by the main seat per the three-way separation. The predictor
(cc2) and comparator (the web seat) receive this document + their
handoffs. MB12 checked both ways in-doc; MB13 results section
pre-structured; the Input-Completeness row below.**

## C1 — the channel, by the frozen rule (no discretion)

Prior order evaluated:

1. **THE JUNO FORWARD ROW — PASSES ALL GATES (the selected channel).**
   The pairing (sin²θ₁₂ ↔ the second tone, 1/(2φ)) was banked as a
   forward row on 2026-07-15 (B633: "the zero-cost forward row"),
   BEFORE this campaign existed — it is pre-registered relative to
   every campaign artifact and cannot have been tuned to JUNO's
   unpublished result. Gates:
   - **R5 typing:** tone₂ = 1/(2φ) is T_coup^inv (ear-independent by
     the twist-frame law B641), F-class, core-coupled, dimensionless;
     sin²θ₁₂ is a dimensionless mixing parameter. Descriptors ship in
     the predictor's packet via `types_checker.py`.
   - **Held-out provenance:** the target is JUNO's FIRST published
     sin²θ₁₂ — unpublished as of this seal, hence absent from every
     Phase A/B input BY CAUSALITY (the only provenance grade that is
     unarguable). The current global-fit value (0.3088 ± 0.0067,
     NuFIT 6.1) IS in the banked record and is therefore NOT the
     target; it appears below only as pre-banked power context.
   - **Grammar independence:** with C′ (below) the calibration
     consumes NOTHING; the prediction traverses the tone law, the
     hearing-group theorem, and the congruence shadow (all FORCED).
   - **Sector coverage:** adjudicated N/A-by-construction: the gate
     prevents single-sector masking of a FIT; C′ fits nothing.
   - **Arity/power:** see the criterion section; the falsification
     side is sharp; the confirmation side's FP rate is printed.
2. The PMNS-typed current-data pair — **FAILS the held-out-provenance
   gate** (every current entry is in the banked inputs, B631 era).
   Recorded as evaluated-and-failed; not run.

Multiplicity: ONE passing channel; the branch orbit collapses (below);
look-elsewhere multiplier = 1. The pairing look-elsewhere (which tone ×
which observable) is closed by the 2026-07-15 pre-registration; the
honest note stands that the pairing was first NOTICED a posteriori in
older data and then registered forward — the FP rate below prices the
confirmation side accordingly.

## THE C′ EVENT — zero-calibration branch fix

The grammar's one freedom (GATE B: the Galois branch χ vs χ̄) maps
tone₂ to 1/(2φ) = (√5−1)/4 ≈ 0.309017 (branch χ) or to −φ/2 ≈ −0.809
(branch χ̄). A sin² observable lies in [0, 1]: **branch χ̄ is excluded
by range physicality — the bit is fixed with ZERO measurements.** Per
the sealed campaign, Phase C upgrades to the direct-prediction variant
C′: strictly more falsifiable; N_eff(channel) = 0.

## THE PREDICTION (to be formalized + sealed by the predictor)

> **sin²θ₁₂ = 1/(2φ) = (√5 − 1)/4 = 0.30901699437494742410…, exactly,
> zero adjustable parameters**, as the T-image of the second tone under
> the pre-registered pairing. The predictor's packet derives this
> through FORCED rows only and states the R5 descriptors; its interval
> is ZERO-WIDTH on the grammar side (the experimental interval is the
> target's alone — nothing to propagate; the C2 uncertainty-propagation
> clause is vacuous under C′ and recorded as such).

## The target, the statistic, the criterion (the comparator's lane)

- **The pinned target:** the JUNO collaboration's first refereed
  oscillation-analysis publication reporting sin²θ₁₂ with a stated
  uncertainty (the collaboration's own headline solar-sector number;
  if multiple analyses appear in one publication, the abstract/summary
  value). The comparator seals the extraction protocol NOW and
  executes ON PUBLICATION. No substitute dataset is permitted; global
  fits are NOT the target.
- **The statistic:** z = (measured − 0.30901699437494742) / σ_measured.
- **Pre-committed verdicts:** |z| ≤ 2 → OUTCOME A (consistency at the
  printed FP rate); |z| > 4 → OUTCOME B (the channel and the coupling
  thesis in its value form are FALSIFIED at this level — banked with
  equal prominence); 2 < |z| ≤ 4 → INCONCLUSIVE-AT-PRECISION (banked;
  the one-shot license is SPENT either way — no re-run, no second
  statistic, no next channel without a fresh owner directive).
- **THE PRINTED FP RATE (confirmation side):** with JUNO-projected
  σ ≈ 0.0015 (≈0.5% relative), the ±2σ window has width ≈ 0.006; on
  the pre-2026 3σ solar band ≈ [0.25, 0.36] (width 0.11), a wrong
  theory lands in the window with p ≈ **5–6%** (multiplicity 1). An
  OUTCOME A is therefore SUGGESTIVE, NOT DECISIVE — and it does NOT
  separate the grammar's value from the nearest banked rational
  neighbor (4/13 = 0.30769 sits ≈ 0.9σ_JUNO away; stated in advance).
  The DECISIVE side of this design is falsification.
- **Power context (pre-banked, not evidence):** the current global-fit
  central 0.3088 sits 0.03σ_current from the prediction; the same
  window holds 4/13, 8/25, 3/10 — priced in B633; carries no weight
  here.

## Governance

- The A4 qualifier attaches verbatim to any outcome: "the stage κ = 5
  is a declared minimal-bearing choice."
- The A3 clause: if the fiber-paired coupling's resolution surfaces a
  new CHOICE before the comparison executes, this seal REOPENS before
  comparison (the GATE B binding note).
- Gate 5: nothing here enters CLAIMS.md; outcome A banks as
  PREDICTION.md + a value-free pointer row (conditional-on-functor).
- Exposure lines: the main seat assembled Phases A–B (full grammar
  exposure; no JUNO-internal data exists to be exposed to); cc2 built
  the ladder grammar (stage-value exposure; no SM-target exposure
  beyond the banked record); the web seat carries B615-era SM-value
  exposure (harmless post-seal; their extraction is of an unpublished
  number). All three seats' exposure to the CURRENT solar value is
  total and irrelevant: the target is the future measurement.
- Input-Completeness (12-item): scheme = the collaboration's own
  reported parameter (no scheme conversion needed for a mixing
  parameter); scale = N/A (dimensionless, no running claimed — R5);
  posteriors = the published σ as stated; convention flags = NO
  (octant/ordering conventions do not affect sin²θ₁₂'s definition at
  this precision — the comparator confirms the publication's
  definition matches PDG-standard θ₁₂ before computing z);
  look-elsewhere = printed above; seed/path = N/A (no simulation);
  positive control = the statistic reproduces |z| = 0.03 on the
  pre-banked current value (a mechanical check of the pipeline, not
  evidence); calibration = C′ (zero); the remaining items (dataset
  version, uncertainty handling, verdict bands, one-shot) are pinned
  above.

## MB13 (results-in-doc, pre-structured)

- [ ] Extraction sealed (comparator's hash): ____
- [ ] Prediction sealed (predictor's hash): ____
- [ ] Publication appeared; extraction executed on: ____
- [ ] z = ____ ; verdict: ____
- [ ] Adjudication banked as: ____
