# B1073 — THE COMPOSITION GATE: being decides WHETHER, hearing decides WHAT (the web seat's theorem, bench-closed to a two-line law)

**Date:** 2026-08-19 · **Seat:** cc (banking; the theorem is the web seat's, integrated under
verify-don't-trust) · Gate 5 untouched — no measured number anywhere.

## The theorem (the two-line form)

For any word w in {R, L} with p R's and q L's, on the θ-odd plane:

> **h(w) = χ(w) · W(w)₁₁**, where χ is the homomorphism to μ₃ with χ(R) = ω, χ(L) = ω⁻¹
> (so χ(w) = ω^(p−q)), and **W(w)'s entries never leave ℚ(ζ₅)**.

Hence **h(w) ∈ ℚ(ζ₅) ⟺ p − q ≡ 0 (mod 3), or h(w) = 0**: the being end is a phase and
decides WHETHER the coupling is field-forced; the hearing end is the entire magnitude and
decides WHAT value it takes. B853's "each face possesses precisely what the other lacks,"
computed at the level of WORDS.

## Provenance — convergent and independent, the chronology plain

- **B641 (2026-07-16)**: the mechanism's origin — "the strict law was SU(2) membership,
  not a hearing theorem."
- **The web seat (2026-08-15)**: the word-level factorization and the field gate, proved
  exact in ℤ[ζ₃₀] — 510/510 words of length ≤ 8, with the honest repair recorded (32
  initial mismatches, all exactly h = 0: the argument had silently assumed h ≠ 0). Found
  BY a scope test on a result that looked clean (their §4 correction — the being end's
  ζ₃ content appears immediately on unbalanced words).
- **Track A / B1070–B1071 (2026-08-18/19)**: independently — the handoff reached this
  bench only 2026-08-19, after Track A ran and the seal closed — the all-2880 closure
  (M_odd = χ·W with W ∈ SU(2) exactly) and the pointwise Bloch form of the imaginary
  channel. **Neither derivation saw the other; the web seat's timestamp is prior on the
  word-level form; relay latency is the cause of the independence.**
- **This bench (2026-08-19)**: the closure argument that makes the field gate
  unconditional — det R_odd = ω², det L_odd = ω (so χ is as stated), and **the
  W-parts of BOTH GENERATORS have every entry fixed by the full Gal(ℚ(ζ₆₀)/ℚ(ζ₅))**
  (k = 11, 31, 41 — verified entrywise), so ℚ(ζ₅)-membership of the hearing part holds
  for EVERY word by multiplicative closure — converting the length-bounded census into
  a theorem. Independent gate check: 126/126 words of length ≤ 6, zero mismatches.

## The scope qualifier (the web seat's sentence, verbatim, binding on citations)

> The balanced family isn't merely *a* window where the phase is trivial — it's the
> window where p = q makes it **identically** trivial, so **every hearing-only result
> derived on R^m L^m is a theorem about ζ₃⁰, not about the being end's absence.**

B593/B856's instrument reads R^m L^m; the period-5 law, the π/10 quantisation, the
nine-letter alphabet are all seen through that window. The instrument's natural field is
ℚ(ζ₁₅) = ℚ(ζ₃, ζ₅) — ord(T) = 15 — and contains both faces' arithmetic by construction.

## What it does for the crossing (the AC6 consequence)

The type-conformance gate's odd-channel half closes BY LAW: every coupling value at the
derived pair is ℚ(ζ₅)-algebraic by construction — no generic real can appear. AC6's
remaining work is the even sector (the T_m values; the web seat's θ-even analogue —
½tr(A) for Re χ(A), gate at A ∈ Z(2T) — runs on their bench; Track A's A3 spectra
(±sin(2π/5) at m = 1, 4; ±√3/2 at m = 2; T₃ = T₅ = 0 forced) wait as the cross-check).

## Riders

- **R-INV closed** (the web seat's three routes): the central V₄'s three involutions give
  ONE decomposition (θ's sheet; the third lands scalar); the Galois V₄ fixes the
  alphabet (hearing involution = two golden transpositions; being involution = identity
  on the alphabet since every forced value is real); the phase quantisation's 4 is the
  cusp conductor. All three agreed on the balanced family — and were the same blind
  spot; the theorem above is what they were missing.
- **The method law, credited**: "scope-test the results you're most confident in — those
  are the ones nobody re-examines." The theorem exists because a scope test broke a
  clean-looking claim (their item 10). Registered as the inverse of the file-drawer
  species: not verify-more, but AIM the second pass at the confident results.
- The web seat's same handoff carries: a B933 correction owed (J COMMUTES with the 3d
  Dirac operator — quaternionic structure; separately registered, not this arc), and
  paper-review items on the paper lane (routed by the owner, not banked here).
