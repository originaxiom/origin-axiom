# B710 (Path 1) — ANALYTIC-T1: method-meeting + structure-INVERSION at the thimble level

*cc banking seat, 2026-07-19. Sealed prereg 88fb56c9. Campaign path 1/3
(multiagent, compute→3-skeptic-adversarial-verify→refine loop). Outcome **B**,
1 round, NOT refuted, high confidence, firewall clean. Verified-on-receipt
(re-ran b710_compute.py). Structural only; Gate 5 stands; no SM value.*

## Verdict (OUTCOME B — as sealed)
The figure-eight state integral and Turok's FRW mini-superspace lapse integral
**share the Picard–Lefschetz METHOD** (a 1-dim oscillatory integral with a
conjugation ℤ/2 on its saddles) **but their saddle SKELETONS invert.** This is
the computable *analytic shadow* of T1 (B709) — and it computes B709's inversion
and confirms B707's "Turok = method-only" at the path-integral level.

## The discriminating fact: real-vs-complex saddle actions
- **Fig-8:** shape z=e^{iπ/3}∈ℚ(√−3); Vol(4₁)=2·Im Li₂(z)=**2.029883212819307…**
  (reproduced two independent ways — Clausen Cl₂(π/3) and Bloch–Wigner D(z) — to
  4e-32). CS(4₁)=0 (amphichirality). The two non-abelian (geometric + conjugate)
  saddle actions = ±Vol, **Im = 0 exactly** — they sit **AT** the fixed point of
  the conjugation ℤ/2. Plus a peacock Borel lattice (ℚ(√−3) quantum modularity)
  as the object's extra structure.
- **FRW:** the lapse action S(N)=A N³+B N+C/N (coefficients A=Λ²/36,
  B=3−Λ(q₀+q₁)/2, C=−3(q₁−q₀)²/4 **re-derived symbolically** from the FLT
  minisuperspace Lagrangian, not merely cited) ⟹ dS/dN=0 is 3A N⁴+B N²−C=0, a
  **quartic ⟹ 4 saddles in 2 conjugate pairs**, actions **complex** (|Im S|>0
  across all 4 tested (Λ,q₀,q₁) sets; e.g. Λ=3,q₀=0,q₁=2 → saddles ±(1±i)).
- **Mod-conjugation orbit count: fig-8 = 1, FRW = 2.** Real-vs-complex actions is
  the single fact separating the two skeletons.

## Why it's an inversion, not a miss
CS(4₁)=0 (amphichirality — the *canonical* ℤ/2-A of B709) forces the object's
saddles onto the conjugation fixed point (real actions); FRW's saddles are
generically off it (complex). The still cancellation that makes our actions real
is exactly the fixed point Turok's cosmology *has* (the bang) and our measurement
side (ℤ/2-B) *lacks*. Same method, mirror skeleton.

## Caveats (the row carries these)
1. Stokes/thimble CONNECTIVITY (which saddles contribute) is QUALITATIVE — the
   load-bearing facts (#saddles, action-reality) are computed; the contour
   deformation is descriptive.
2. CS(4₁)=0 and the Garoufalidis–Gu–Mariño peacock lattice are CITED (CS=0 is the
   textbook amphichirality consequence), not recomputed.
3. The "real actions" property is 4₁'s amphichirality specifically (a generic
   knot has CS≠0 → complex actions) — object-specific, not a generic
   knot-vs-cosmology claim.
4. The geometric-metric T1 embedding remains NEEDS-SPECIALIST (unchanged, B709);
   this arc computes only its analytic shadow.

## Placement
LAW_MAP: the thimble-level method-meeting/structure-inversion (Turok=method-only,
computed). Nothing to CLAIMS.
