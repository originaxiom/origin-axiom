# B403 — PRE-REGISTRATION: scrutiny of the Chat-1 "six leads + prime-filter theorem" package

**Committed before computation. Verify-never-trust. The package's centerpiece — "the
primes dividing the denominators of the seam form's √−15 component are exactly the primes
represented by the non-principal form 2x²+xy+2y²" — is tested against BANKED data under
the MB12 vacuity rule (a claim must be able to fail).**

## The three registered checks

- **V1 (vacuity, denominators):** the banked seam denominators are {2,3,5}-smooth (48,
  96, 240, 1152, 2304, 6912, 13824, 19200, 2880-family). All of 2, 3, 5 are represented
  by the non-principal form (2: (1,0); 3: (1,−1); 5: (1,1)) — AND the DFT construction
  forces denominators to divide powers of the group orders (o₁o₂ ∈ 2^a3^b5^c), so NO other
  prime can EVER appear in a denominator. The claim's denominator form is then VACUOUS:
  its prediction ("17 can appear, 19 cannot") is undecidable by construction — neither
  can appear. Verify the form representations and the banked smoothness.
- **V2 (the numerator refutation):** seam-channel NUMERATORS already contain INERT primes:
  49 = 7² (the F₄ Gram norm 49/115200), 13 (the inner −13/57600), 7 (the (3,4) spectrum
  {7,5}/2304). If the σ-odd/Artin sketch filtered primes by class, these could not be
  there. Verify from the banked B398 locks.
- **V3 (the salvage boundary):** the honest surviving content of the class-field lens is
  what B401 banked (the orbit action, the covariance prediction, the selection rules) —
  NOT a prime filter. Record 17/19's form data for the file (17 = (3,−1) non-principal;
  19 = (1,2) principal) so any future NUMERATOR appearance of either is an immediate
  registered test.

## The six "leads" — mapped against the banked record (no computation needed)

L1 pair-breach ↔ B402 Q1 (banked: the wall splits by seed) + Q2 (queued) + P62. L2
brightness-derivation ↔ P67 + M1's stratification law (more exists than the package
knows; the rep-theoretic form remains open, correctly named). L3 McKay-Weil ↔ their
σ-bridge already FAILED our character check (B398 S4); "McKay at roots of unity" stands
as a literature direction. L4 frozen architecture ↔ literally W-A in flight (sum rule
banked; the walk = the running 1215). L5 Gram-derivation ↔ the S042 revival bar,
correctly restated. L6 arithmetic splitting ↔ V1–V3 above.
