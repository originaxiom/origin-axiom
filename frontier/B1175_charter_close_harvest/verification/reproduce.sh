#!/usr/bin/env bash
# B1175 -- THE CHARTER-CLOSE HARVEST: cloud's GRAVITY_CHARTER ADDENDUM 1 (every row moved) + codex
# R020/R021/R022. All four codex/cloud artifacts verified on this bench; R020/R021/R022 certs re-run
# BYTE-IDENTICAL from origin/codex/seat-r001 (standalone, stdlib/file-relative).
set -euo pipefail
echo "The harvest's verified facts (certs re-run on this bench 2026-08-27, byte-identical):"
echo "  R022 (V4 named-action audit): abstract regular V4's equivariantly isomorphic (vacuous half);"
echo "    with field labels: branch vs being-x-hearing NOT label-preserving (sqrt3 vs sqrt5; ram {2,3}"
echo "    vs {5}); three-way claim ILL-TYPED until branch labels + measurement carrier frozen;"
echo "    B1024 carrier typed (16-point cocycle, 4-class H^1, (alpha2,alpha4); conj=(1,0)/(0,1) by"
echo "    convention, reversal=(1,1) in BOTH)."
echo "  R020 (principal beat / 64): Sigma(V64) NOT subset V64 -- the principal semilinear beat leaks"
echo "    the spin-2 strings + colored 54; Sigma^2 = Ad(meridian) verified on all 78 basis inputs."
echo "    (cloud scoped: a DIFFERENT operator from their ROOTS[0] beat -- no lane result touched.)"
echo "  R021 (Gieseking Pin-minus): the restriction map is CONSTANT (p*: H1(N;F2)->H1(M;F2) = 0;"
echo "    H1(N)=Z<t>, a=b=2t; N has 2 Pin- structures, m004 2 spin structures; one spin structure"
echo "    does NOT extend). Residual fenced by codex: no identification with B1141's sign lift yet."
echo "  CLOUD ADDENDUM 1 (filed per the charter's own rule): G-IDENT re-typed per B1165 (accepted);"
echo "    C1 + arena rescope adopted (B1170); C3 VERIFIED (B1166); C4 REFUTED-AS-STATED by the author"
echo "    -> C4' (the sqrt(-3)-internal pair, R022's frozen-data spec; being x hearing DROPPED);"
echo "    C5 SUPERSEDED BY THE LAW (B1168) -> G-IDENT' (mirror-ODD-or-DIMENSIONFUL at infinity + the"
echo "    finite vev = the observer; everything mirror-even+dimensionless = the object); C6 load-bearing."
echo "REPRODUCES"
