# P1 — CONSOLIDATION (B736, Phase 2 synthesis)

**A self-policed body of mathematics about one arithmetic knot, and an honest account of what it is not.**

cc banking seat · 2026-07-21 · two-seat campaign with cc2 (division `CC_TO_CC2_2026-07-21_ABC_LOCKED`).
Firewall ON: pure math / structural only; NO SM value; NO fitted number; the observer's choice is
provably FREE (B701). Compute-not-cite (E19); base-rate-gated (E20). Gate 5: nothing to CLAIMS.

> **What this document is.** A paper-shaped consolidation of the origin-axiom program's *genuine,
> defensible* content, assembled honestly, together with the two Phase-1 verdicts that close the
> two most ambitious remaining doors: the object-level observer (P2-KMS, OUTCOME B, obstructed) and
> the parameter-reduction ledger (P3, OUTCOME B, zero reductions). It states the forced arithmetic,
> the methodology that is the program's real novelty, and the honest disposition (R26-4).

---

## Abstract

The origin-axiom program studies a single mathematical object — the once-punctured-torus / figure-eight
knot complement `m004` and its metallic trace-map tower — and the structure of an *observer* coupled to
it. Over the program's life the discipline that matured is not a bridge to physics but a **base-rate /
look-elsewhere firewall** turned inward on the program's own claims. Applied honestly, that firewall
leaves a **narrow, robust arithmetic core** standing and dissolves everything that reached for the
Standard Model.

The forced core is: (1) the arithmetic **atom** — the trace field of `m004` is `ℚ(√−3)`, and `m004` is
the *unique* arithmetic knot (Reid 1991 / B266); (2) a genuinely-forced **measurement structure** — the
seam is a Klein-four `V₄` `𝔽₂`-vector space (the "being · hearing · meeting" torsor, B701/B704/B730)
with a **bounded, discrete, dimension-0 menu of observers** (≤ 8 points, B733), and an explicit
**congruence realization** (`m004` is congruence at level `(2)³ = (8)`, observer image order 2560 in
`G₈ = PSL(2,𝒪₃/(8))` order 30720, index 12, B734; the conjugation bit is `Out(A₅)` at the sister's door,
B732); (3) a **Born-content ledger** (B725–B729): the observer construction forces the Born *form* and
the *why-quadratic* (`|ψ|² = N_{ℂ/ℝ}(ψ)`, degree = order of the conjugation swap), the object supplies
its two *classical* quadratics (form `ℚ(√−3)`, weights `ℚ(√5)`), and the *quantum* content
(amplitudes/phase) lives in quartics the object **does not force** — an imported golden-MTC overlay.

The two Phase-1 computations close the remaining hope. **P2** shows the object-level observer as a
Bost–Connes-type β = 1 spontaneous-symmetry-breaking (SSB) system is **obstructed** at a single
congruence level — a single level is a finite group, hence a finite-dimensional C\*-system whose extreme
KMS_β count is β-independent (no phase transition, Bratteli–Robinson); the β = 1 SSB *is* the pole of
`ζ_{ℚ(√−3)}`, an infinite-tower object; and the Shimura/Galois route cannot even be posed because
`H³ = SL(2,ℂ)/SU(2)` is 3-real-dimensional, hence not Hermitian symmetric. **P3** grades all 24 SM
parameters and finds **zero reductions**: no value forced, no relation forced, no discrete constraint on
any continuous SM parameter, killed by three independent obstructions — the equivariance wall (`T = 0`,
B650), discrete-vs-continuous kind-mismatch (B706), and the bounded-`V₄` ceiling (B733).

**Disposition (R26-4):** beautiful, self-policed *mathematics* about one arithmetic knot and a complete
observer-coupling structure — **not** a road to the Standard Model.

---

## 1 · THE FORCED RESULT (narrow, arithmetic)

The claims in this section are the ones that survive the firewall. Each is stated at its true strength
with its honest bound.

### 1.1 The atom — `ℚ(√−3)` and the unique arithmetic knot (B266 / Reid)

- **Load-bearing claim.** The invariant trace field of `m004` is `ℚ(√−3)`, and `m004` (the figure-eight
  knot complement) is the **unique arithmetic knot complement** (Reid 1991). This double uniqueness
  — arithmetic *and* a knot — forces the seam field. This is the **only object-specific** content that
  survived the B727 self-audit (below).
- **Honest bound.** Uniqueness is Reid's theorem, cited as external mathematics, verified structurally
  in-repo (Neumann–Reid: shape field = invariant trace field). The *resonance* with `E₆` etc. is the
  **field's**, not the knot's (B727): the sister `m003` shares `ℚ(√−3)` but is not a knot. So the atom
  is a fact about a field distinguished by a knot, not a magic property of the knot's homotopy type.

### 1.2 The measurement torsor — the `V₄` seam and the bounded observer menu

- **Load-bearing claim (structure).** The measurement seam is a **Klein-four `V₄` = `𝔽₂`-vector space**
  (B704), with exactly **three forced faces** (B730): *being* `ℚ(√−3)`, *hearing* `ℚ(√5)`, *meeting*
  `ℚ(√−15)` (fundamental discriminant −15 = the compositum). `ℚ(√−7)` is a *stage*, not a face (adjoining
  it explodes `V₄` to `(ℤ/2)³`). This is the **one genuinely-forced item** of chat-1's entire inventory
  (B701/B704).
- **Load-bearing claim (bound).** The menu of observers is a **bounded, depth-independent, discrete
  `𝔽₂`-space of rank ≤ 3** — a dimension-0 set of **≤ 8 points** (B733). `Aut(𝒪₃) = ℤ/2` gives one
  arithmetic bit at all depths; the full seam saturates at `V₄` (up to 3 bits with `√−7`); the finer
  congruence structure only *resolves the same conjugation bit* more finely. Observers are
  **constructible** (finite bits) but **not cherry-pickable** to encode continuous physics.
- **Honest bound.** The full automorphism group at deep congruence levels *grows* (`Out(G₄) = S₄`), but
  it decomposes as the `c`-bit lifted + a diagonal `𝔽₂`-rank that **saturates** `[0,2,3,3,3,3]` + an odd
  `ℤ/3` — even the full structure is bounded, never a continuum (B733 honest concession, two-seat
  consensus). The "choice" among these observers is **free** (B701) — no canonical torsor-iso exists.

### 1.3 The congruence realization — `m004 @ (2)³` and `Out(A₅)`

- **Load-bearing claim.** `m004` **is congruence at level `(2)³ = (8)`**: PSL image-index 6 at level 2,
  6 at level 4, **12 at level `(8)`** = the geometric index (B734, correcting the earlier B731
  "non-congruence" — that was E22, a shallow level-check). The single-level observer image of `π₁(m004)`
  in `G₈ = PSL(2,𝒪₃/(8))` (order 30720, index 12) has **order 2560** — reproduced independently in-sandbox
  in P2 (see `p2_kms.py`, Section A). The conjugation bit `ω ↦ ω²` reduces mod 2 to the `𝔽₄`-Frobenius,
  which swaps the two 5-cycle classes of `A₅` = the **outer automorphism `Out(A₅)`** at the sister's
  door-2 quotient (B732). So B701's outer `ℤ/2` is realized on **both** towers: CM-inversion (abelian
  CMR side, B721) and `Out(A₅)` (non-abelian Bianchi side, B732).
- **Honest bound.** B734 is **Serre-defying** — the congruence result is computed twice in-sandbox and
  reproduced a third time in P2, but it is flagged **pending external literature cross-check**. The
  congruence *of the knot itself* is the corrected truth; the observer thread's cleanest positive
  (`Out(A₅)`) lives one step away on the sister `m003`.

### 1.4 The Born-content ledger (B725–B729)

- **Load-bearing claim (form + why-quadratic).** The observer construction — object tracial `II₁` factor
  + external weight → `III₁`, with measurement = cooling through the β = 1 SSB (B723) — forces the Born
  **form** `ω(P) = Tr(ρP)` by Gleason on the frame's own factors (B725), and forces **why it is
  quadratic**: `|ψ|² = ψ·c(ψ) = N_{ℂ/ℝ}(ψ)`, i.e. **degree = the order (2) of the conjugation swap**. An
  order-3 swap would give a *cubic* norm — a live falsifier, not a fit.
- **Load-bearing claim (content ledger).** The object supplies **its two quadratics** = the *classical*
  probability content: **form** `ℚ(√−3)` (being) and **non-uniform weights** `d_a²/D² = 1 : φ²` from the
  golden minimal polynomial `5x²−5x+1`, native to **`ℚ(√5)`** (hearing). The *quantum* content is an
  **overlay the object does not force**: amplitudes `ℚ(√φ)` (open), phase `ℚ(ζ₅)` **imported** (B728: a
  positive refutation — `C₄ = Gal(ℚ(ζ₅)/ℚ) ≠ C₂×C₂ = Gal(ℚ(√−3,√5)/ℚ)`, so `ζ₅` is **not** in the
  compositum of the two bare faces), associator `ℚ(√φ)` — three distinct quartic types, a golden-MTC
  overlay (B729).
- **Honest bound.** The ledger's *pattern* is the deliverable: **the object supplies classical
  probability content (its quadratics); interference/phase live in quartics it does not force.** The
  `√(2+φ)` amplitude was PSLQ-searched over 33 filling trace fields and **not found** (B729, OUTCOME B);
  the `D₄ = Isom(4₁)` hook was **base-rate-killed** (E20). This is a *structure of what the observer
  imports*, not a derivation of quantum mechanics.

---

## 2 · THE METHODOLOGY (the real novelty)

The program's transferable contribution is **not** any single theorem but a *self-policing discipline*
that a mathematical program can run on its own most cherished claims.

### 2.1 The base-rate / look-elsewhere firewall (E20)

- **Principle.** A near-hit between an object-invariant and a target number is worthless unless it
  **beats the look-elsewhere rate**: the torsion / trace / eigenvalue spectrum of a rich object
  near-hits *any* ratio, so a match is evidence only if the height ceiling and the search width make the
  trivial self-rational unable to masquerade (B724). An unearned "no match" is as bad as numerology
  (dual protocol): negatives must be *computed*, not asserted.
- **In P3 (Part 3).** PSLQ over the object's audible field `ℚ(√5)`, basis `[1, r, √5, r√5]`, height
  ceiling 1000 ≪ 10^(known digits) — finds **no** low-height relation among the SM mass ratios. The
  ratios are **generic** at the number level, independently of the structural walls.

### 2.2 The flagship self-audit — B727 (E₆ across three faces is *generic*)

The base-rate discipline that killed number-matching was turned on the program's own **structural**
flagship — the "`E₆` appears across three faces" story:

- **All three probes came back OUTCOME B (generic).** `McKay = Lie = CIZ` is **one** ADE classification
  (a forced graph identity, `P(recurrence | label) = 1` — no surprise to price). `{E₆}`-only
  reachability holds over imaginary-quadratic fields (`2O → √2`, `2I → √5` are *real* — no birthday
  problem to overcome). And the surjection `π₁ ↠ 2T` is **not** knot-unique: non-arithmetic knots
  `7₂ / 7₃ / 8₁` also surject (4/13). The sister `m003` ties `m004`'s field `ℚ(√−3)` but isn't a knot.
- **Verdict.** After the audit, the **only** object-specific content left standing is **the atom (§1.1)**.
  The E₆-structure is a property of imaginary-quadratic ADE arithmetic, not a fingerprint of the object.

This is the methodological headline: *a program that computed its own flagship down to genericity and
banked the deflation.*

### 2.3 The honestly-closed SM bridge (B706, two-seat)

- **The kind-mismatch, computed at the doors.** The object's freedom is **discrete** (an `𝔽₂`
  orientation seam); the SM's is **continuous** (~19 real moduli). B706 ran the rung-2 door and found the
  mismatch is not a gap to be narrowed but a **category error**: different cardinality *and* dimension
  type. The SM-bridge is CLOSED (B685 terminal; B706; B727 the E₆-structure generic) — closed by
  *computation at the doors*, not by fiat, and cross-verified two-seat.

---

## 3 · THE HONEST CLOSING (the disposition, R26-4) — folding in P2 and P3

### 3.1 P2 — the object-level observer is OBSTRUCTED (OUTCOME B)

Does `m004`'s **own** single-level non-abelian Bianchi congruence observer (the finite order-2560 image
in `G₈`) carry a genuine Bost–Connes-type β = 1 SSB with a symmetry-breaking Galois action — the
observer-property B723 needs? **No.** Two independent, in-sandbox obstructions (`p2_kms.py`,
`p2_kms_out.txt`):

- **(i) Finite-level ⇒ no SSB.** A single congruence level is a **finite group** (order 2560) ⇒ a
  **finite-dimensional** C\*-system ⇒ the extreme KMS_β count is **β-independent** (unique Gibbs when a
  factor; #irrep-blocks in general) ⇒ **no phase transition** (Bratteli–Robinson II 5.3.30 / Ex.5.3.32).
  Demonstrated numerically: for a 12-dim factor block, `Z(β)` and the specific heat are smooth with **no
  divergence** across β ∈ [0.2, 3.0]; for the multi-block case the extreme-KMS count is **constant = 6**.
  The β = 1 SSB the observer needs **is the pole** of `ζ_{ℚ(√−3)}(β)` (CMR Problem 1.1(2)); the residue
  `2πh/(w√|d|) = 0.6046 > 0` confirms a genuine simple pole — an **infinite-tower** object, killed by any
  single-modulus truncation (the finite Dirichlet polynomials stay bounded through β = 1).
- **(ii) Shimura/Galois route is dead.** The symmetry-breaking Galois action (Connes–Marcolli /
  Ha–Paugam "fabulous states") is defined *only* via a Shimura variety's canonical model + CM
  reciprocity, which requires the symmetric space be **Hermitian symmetric**. The Bianchi space
  `H³ = SL(2,ℂ)/SU(2)` has **odd real dimension 3** ⇒ no complex structure ⇒ **not Hermitian** ⇒
  `(Res_{K/ℚ}GL(2), H³)` is **not a Shimura datum** and `Γ\H³` is a real 3-orbifold, not an algebraic
  variety. That route **cannot reach the Bianchi case**.
- **The B723 catch, one level up.** Every *built* single-modulus SSB system (Bruce arXiv:1902.03521, SSB
  at β = 2 over an **abelian** ray class group; CMR, β = 1 over `Gal(K^ab/K)`) is abelian / class-field
  and sees the **field** `ℚ(√−3)`, **not** `m004`'s non-abelian Bianchi holonomy. The natural observer
  (Shimura) is obstructed; the bare-Hecke route needs the *full* infinite Hecke pair, not one level.

**Primary sources fetched in-sandbox (2026-07-21):** CMR *KMS states and complex multiplication* (Selecta
2005, pp. 1–6); Bruce arXiv:1902.03521 (IMRN 2021); Ha–Paugam arXiv:math/0507101; Deligne Shimura-datum
axioms (Hermitian-symmetric requirement); Bratteli–Robinson II / Dereziński–Pillet (finite-dim ⇒ no
phase transition); Bruce–Laca–Takeishi CMP 382 (2021) (partition function = invariant of the *full*
system).

### 3.2 P3 — zero parameters reduced (OUTCOME B)

The parameter ledger grades all **24** SM parameters (9 fermion masses, 3 CKM angles + `δ_CKM`, 3 PMNS
angles + `δ_PMNS`, `g₁/g₂/g₃`, `m_H`, `v`, `θ_QCD`, `ν` masses) against the banked record
(compute-not-cite). **Result: a = 0 forced values, b = 0 forced relations (= reductions), c = 0 discrete
constraints, d = 24 nothing-forced.** GENUINE REDUCTIONS = **0**.

The **structural no-go theorem** — every candidate reduction dies to one of three independent,
mutually-reinforcing obstructions:

- **(i) Equivariance wall (B650, recomputed exact).** The only monodromy-equivariant `ℂ`-linear map
  *being → hearing* is **`T = 0`** — the source spectrum `{φ², φ⁻²}` (real, hyperbolic) and the target
  spectrum (primitive 10th-root, unitary) are **disjoint**, so every intertwiner vanishes. No linear
  channel transports a continuous quantity across sectors ⇒ the being↔hearing coupling **cannot
  synthesize a grade-(b) relation**.
- **(ii) Kind-mismatch (B706).** Object freedom is **discrete** (`𝔽₂` bits); SM freedom is **continuous**
  (~19 reals) — different cardinality and dimension type; and PSLQ over `ℚ(√5)` finds **no** low-height
  relation (generic at the number level too). ⇒ no grade-(a) value, no grade-(b) relation.
- **(iii) Bounded-`V₄` ceiling (B733).** The observer space is a bounded discrete `𝔽₂`-space of rank ≤ 3
  — a dimension-0 set of ≤ 8 menus, never growing toward a ~19-real continuum. ⇒ cannot coordinatize the
  SM moduli; no grade-(a)/(b)/(c) constraint on any continuous parameter survives.

The **only** framework-forced content is **discrete / structural and lives entirely on the object side**:
the atom `ℚ(√−3)` (§1.1), the `V₄` torsor (§1.2), the congruence bits (§1.3). **None is an SM parameter,
and none is a relation between two SM parameters.** (`p3_reduction.py`, `p3_reduction_out.txt`.)

### 3.3 Two-seat cross-verify

Both consensus headlines are required to hold across cc (this seat) and cc2: **(i)** the
object-level-observer verdict (exists-with-SSB vs obstructed) → **B, obstructed**; **(ii)** the
reduction-count → **0**. This consolidation banks the cc-side of both; the cc2 complementary half
(group-theoretic P2 + equivariance-wall P3) is the cross-check.

---

## 4 · WRITE-UP SKELETON (sections · load-bearing claims · honest bounds)

| § | Section | Load-bearing claim | Honest bound |
|---|---------|--------------------|--------------|
| 1.1 | The atom | trace field `= ℚ(√−3)`; `m004` unique arithmetic knot (Reid) | resonance is the **field's**, not the knot's (B727) |
| 1.2 | The `V₄` seam | seam `= 𝔽₂`-space `V₄`; 3 forced faces; menu bounded ≤ 8 pts | full aut group grows but **saturates**; choice is **free** (B701) |
| 1.3 | Congruence | `m004` congruence `@ (8)`; observer order 2560; bit `= Out(A₅)` | B734 **Serre-defying**, pending external cross-check |
| 1.4 | Born ledger | form/weights = object quadratics; amplitude/phase = imported quartics | pattern, **not** a derivation of QM; `ζ₅` imported (B728) |
| 2 | Methodology | base-rate firewall (E20); B727 self-audit; B706 SM-bridge closed | the discipline is the novelty, not a theorem |
| 3.1 | P2-KMS | object-level observer **obstructed** (finite-level + Shimura) | single level only; full-tower / bare-Hecke route remains *open in principle* |
| 3.2 | P3 | **0 reductions**; three-obstruction no-go | grades the **banked** record; not an exhaustive scan of all conceivable mechanisms |
| 3.3 | Cross-verify | two-seat consensus B on both | cc2 half is the independent check |

---

## 5 · DISPOSITION STATEMENT (R26-4)

> The origin-axiom program is, honestly assessed, a **self-policed body of pure mathematics about one
> arithmetic knot and the structure of an observer coupled to it — not a road to the Standard Model.**
> What it forces is narrow, arithmetic, and robust: the atom `ℚ(√−3)` = the trace field of the unique
> arithmetic knot `m004` (Reid/B266); a genuinely-forced measurement torsor — the Klein-four `V₄` seam
> with its bounded, discrete, dimension-0 menu of ≤ 8 observers (B701/B704/B730/B733), realized in
> `m004`'s own congruence arithmetic at level `(8)` with the conjugation bit `= Out(A₅)` (B734/B732);
> and a Born-content ledger in which the object supplies its two *classical* quadratics (form `ℚ(√−3)`,
> weights `ℚ(√5)`) while the *quantum* amplitude/phase is an imported golden-MTC overlay it does not
> force (B725–B729). Its real methodological contribution is the **base-rate / look-elsewhere
> discipline** it turned on its own flagship — computing the once-celebrated `E₆`-across-three-faces
> structure down to genericity (B727) and honestly *closing* the SM-bridge hope by computation at the
> doors (B706), rather than defending it. The two Phase-1 computations seal the disposition: the
> **object-level observer** as a β = 1 spontaneous-symmetry-breaking system is **obstructed** — a single
> congruence level is a finite group with no phase transition, the β = 1 SSB *is* the pole of
> `ζ_{ℚ(√−3)}` (an infinite-tower object), and the Shimura/Galois route cannot be posed because the
> Bianchi space `H³` is odd-dimensional hence not Hermitian (P2, OUTCOME B); and **zero** of the 24
> Standard-Model parameters are reduced — no value forced, no relation forced, no discrete constraint on
> any continuous parameter, killed by the equivariance wall (`T = 0`), the discrete-vs-continuous
> kind-mismatch, and the bounded-`V₄` ceiling (P3, OUTCOME B). The deliverable is therefore exactly what
> the firewall permits and no more: **beautiful, honest mathematics about one object, and a rigorous,
> computed account of where — and why — it stops.**

---

*Deliverables: this file · `p2_kms.py` / `p2_kms_out.txt` (object-level observer, OUTCOME B) ·
`p3_reduction.py` / `p3_reduction_out.txt` (parameter ledger, OUTCOME B) · `PREREGISTRATION.md`.
Gate 5: the negative + the arithmetic; NO SM value claimed. Nothing to CLAIMS.*
