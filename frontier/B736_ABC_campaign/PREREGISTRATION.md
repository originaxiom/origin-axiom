# PREREG — B736: THE A+B+C JOINT CAMPAIGN (cc's half) — consolidate · object-observer · reduction (SEALED)

cc banking seat, 2026-07-21. Owner: "run A+B+C (=cc2's 1+2+3) as a joint two-seat workflow campaign,
locked." Division LOCKED with cc2 (relay CC_TO_CC2_2026-07-21_ABC_LOCKED). cc's half = 3 tracks,
2 phases. Pure math / structural only; Gate 5; compute-not-cite (E19); base-rate-gated (E20);
firewall on (Path 3 CHARACTERIZES, never fits). cc2 runs its complementary half; cross-verify the
two headlines. Web-enabled where a primary source is load-bearing (Bost–Connes/Bianchi; Kim).

## PHASE 1 — parallel compute tracks (each: compute → 3-skeptic adversarial-verify → refine)
- **P2-KMS (Path 2, cc's angle — the KMS/dynamics of the object-level observer).** Does m004's OWN
  single-level NON-ABELIAN Bianchi congruence observer (level 𝔫, e.g. the order-2560 image in
  G₈=PSL(2,O₃/(8))) carry a genuine Bost–Connes-type KMS system with a β=1 SPONTANEOUS SYMMETRY
  BREAKING + a symmetry-breaking Galois action — the observer-property B723 needs — or is it
  OBSTRUCTED (the Shimura route needs H³=PSL(2,ℂ)/PSU(2) Hermitian-symmetric, which it is NOT; the
  bare-Hecke route)? Compute the Hecke-pair/groupoid structure; determine exists-with-SSB vs
  obstructed. Two-outcome: A = a single-level non-abelian Bianchi KMS observer with the SSB exists;
  B = obstructed (needs the full tower / Shimura obstruction stands). [Honest prior: leans B.]
- **P3-REDUCTION (Path 3, cc independent — the parameter-reduction ledger + equivariance wall).**
  For EACH of the ~19 SM free parameters, grade against the BANKED record (compute-not-cite): does
  the framework FORCE (a) a value, (b) a RELATION to another parameter [= a reduction], (c) a
  discrete constraint, or (d) nothing? AND run the EQUIVARIANCE-WALL test (B650: the 27-side
  symmetry acts as 0 on stage-side flavor ops) on the being↔hearing coupling — does any forced
  structure reduce an SM parameter via a forced relation, or does the equivariance wall kill it?
  Deliver the ledger + the reduction-count + the structural theorem. Firewall: characterize, do NOT
  fit. Two-outcome: A = ≥1 genuine forced relation (a reduction); B = zero + the rigorous no-go
  (equivariance wall + kind-mismatch B706 + bounded-V₄ ceiling B733). [Honest prior: B.]

## PHASE 2 — synthesis (consumes Phase 1)
- **P1-CONSOLIDATE (Path 1, cc's angle — the honest capstone + disposition).** Assemble the
  genuinely-forced results (the ℚ(√−3) atom / Reid; the V₄ measurement torsor; the congruence
  structure m004@(2)³ + B701=Out(A₅); the bounded 2-bit menu; the Born ledger) + the METHODOLOGY
  (base-rate discipline, the B727 self-audit, the honest SM-closing) + the P2/P3 outputs, into a
  PAPER-SHAPED consolidation + the honest top-level DISPOSITION (R26-4). Gate 5: the negative + the
  arithmetic; NO SM value. Deliverable: a structured write-up skeleton + the disposition statement.

## Cross-verify (with cc2's half) — the consensus headlines
(i) the object-level-observer verdict (P2: exists-with-SSB vs obstructed) — cc-KMS + cc2-group must
agree. (ii) the reduction-count (P3) — cc-ledger + cc2-equivariance-wall must agree.

## Two-outcome (sealed)
Overall A = a live positive survives (a single-level non-abelian observer with SSB, OR a genuine
parameter reduction). Overall B = both land negative-but-rigorous (obstruction + no-reduction) — in
which case the CONTRIBUTION is the consolidation: a clean, self-policed body of mathematics about
one arithmetic knot + the honest closing. [Honest prior: B on both computes; the write-up is the
deliverable regardless.]

## Verdict template
Bank the three tracks (P2 verdict, P3 ledger, P1 consolidation) + the two-seat cross-verify. LAW_MAP
placement; nothing to CLAIMS; no SM value. Firewall: the reduction ledger and the write-up assert
the negative + the arithmetic, never a fitted value.
