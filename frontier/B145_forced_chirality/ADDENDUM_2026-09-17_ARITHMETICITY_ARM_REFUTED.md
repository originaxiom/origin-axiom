# ADDENDUM (2026-09-17, S15) — the arithmeticity arm of this arc is REFUTED (B146/B147); read the body with that in force

`FINDINGS.md` and `README.md` here state *"every quadratic-trace-field bundle is amphichiral — there is no arithmetic
chiral o-p-t bundle in range. So the strongest canonicity (arithmeticity) ⟹ amphichirality"*, and the slogan
*"forced ⟹ self-mirror ⟹ no parity"*.

**That arm was refuted on 2026-06-10 by B147:** the mirror pair `RRL/RLL` over ℚ(√−7) are **arithmetic and chiral**
(both Maclachlan–Reid conditions, plus a Humbert volume-ratio cross-check); the scan finds four arithmetic
once-punctured-torus bundles to length 7, not two. **"Arithmetic ⟹ amphichiral" is FALSE.**

**What survives** (B146's A3): chirality is forced and generic, but a canonical *single* is always self-mirror and a
composite is mirror-paired, so no handedness is ever preferred — and that conclusion now rests on the
volume-minimality and palindromic-period arms only. `arc_verdict.json` has carried `superseded_by: [B146, B147]`
since June; this file is the in-body signal it never had. `knowledge/K017` and `speculations/S032` (corrected today)
carry the current statement.
