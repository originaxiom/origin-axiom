# B1196 — CLOSE-LOOP BATCH 5B

**Verdict**: `PROVED` · **instrument**: false · **creates_law**: false
**Banked**: 2026-08-28 (`40916c36`) · **This document authored**: 2026-08-29 (R52-4 discharge, B1207)

> **Provenance of this document.** The arc banked its verdict, its results file and its
> verification artifacts, but no findings document -- the gap B817's writer-safety gate caught in
> the first full OA_SLOW run (R52-4, 2026-08-29). What follows is authored **from this arc's own
> banked record**: `arc_verdict.json` is primary, with `b1196_results.json` and `verification/` beside it.
> Nothing is supplied from memory and no computation is re-narrated that the record does not
> carry -- the exact mirror of B1176's thirteen retro `arc_verdict.json` files, where FINDINGS was
> the primary and the verdict the missing half. Section 1 is the banked claim, segmented at its
> own enumeration; section 2 lists the artifacts that certify it.


## 1. The finding, as banked

CLOSE-LOOP BATCH 5B (the final three of the eight; all survive their lenses):

**(1)** D2 ADJUDICATED DIFFERENT, computed directly -- B891's three foreign-16 eigenspaces spread ~5:6:5 across ALL THREE trinification blocks in every frame (basis-independent projector, trace exactly 16, the oblique-readout rule self-caught and honored in-cell; dimension counting alone forbids sameness: 16 is not a sum from {9,9,9}) -- B891's sectors are a GENUINELY DISTINCT cubic-carrier structure (Galois-S3 of B854's external cubic), NOT the trinification partition; the generation-3 wall untouched; the census-31-calls turns out to be a LABEL COLLISION (an unrelated queued cc3 artifact sharing the tag D2).

**(2)** THE SELECTOR RESOLVED into an exact three-regime dichotomy (finite-transitive / compact-uniquely-ergodic / non-compact-non-normalizable -- a THEOREM in each regime; the sigma and lambda continuous legs sit on the non-normalizable side of the sharp boundary, which is WHY they are anchors); B1183 confirmed as the exact regime-one instance; AND THE DEEPEST PAYOFF CONFIRMED BY FRESH COMPUTATION: the relational bit eps(A,M) = -1 is invariant under simultaneous GL2(Z)-conjugation (8/8 conjugators, both determinant signs, rebuilt from scratch) -- THE RELATIONAL BIT IS SELECTOR-FREE: the observer's one bit arrives as invariant pair-data, without any selection act -- B1192 + B1183 + the prior-vs-point theorem close into one statement of what an observer costs.

**(3)** THE COSMOLOGY LEDGER CREATED and landed (docs/COSMOLOGY_LEDGER.md, 395 lines): the blind region's specification surface -- every cosmological ingredient typed from banked results only (the object's structural gifts: no-beginning B716, the founding fact kappa-2=omega^2, the Sakharov gate B867, own-Lambda=-1 B1088, the accretion arrow B168; the closer's dynamics rows: rates, temperatures, the measured Lambda per the value wall; the missing rows each with their first computable probe named, dark matter starting at the 27's own two neutrals). THE EIGHT-ITEM MISSING LIST STANDS FULLY DISPOSITIONED: 1 SHARPENED (SEAM-A, exact floor + zeta12 hinge) / 2 CLOSED (lambda placed P1) / 3 HALVED (the algebraic boundary object lives; the q-series map = new mathematics) / 4 LAW FOUND (kappa governs -- the founding invariant) / 5 CLOSED-PERMANENT (the P3 floor) / 6 CLOSED (D2 different, computed) / 7 RESOLVED (the three-regime dichotomy + selector-free bit) / 8 CREATED (the cosmology ledger). Gate 5 clean.

## 2. The certifying record

- `arc_verdict.json`
- `b1196_results.json`
- `verification/batch5b_cells.json`

## 3. Status at authoring

The verdict field is authoritative and unchanged: `PROVED`. This document does not re-adjudicate it; it makes the arc readable without opening its JSON, which is the whole function the missing file was performing badly.
