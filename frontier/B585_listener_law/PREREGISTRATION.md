# B585 — the listener's law (preregistration)

**Date: 2026-07-14. Preregistered BEFORE the computation. Firewall: nothing to
CLAIMS.md; no SM quantities (the B580 binding rule).**

## The questions

B584 left one data point per stage: golden clock order 10 (SU(3)₂), E₆₂ clock
order 4, and the surprise tr_even = 0 (the entire golden amplitude θ-odd).
Three questions:

- **N1 (the naming theorem, matrix-level):** on the stage, S² = −C (B584,
  B238's normalization) while in SL(2,ℤ) S² = −I. Therefore
  tr(C ρ(W)) = −tr ρ(S²W) — the C-twisted play is (up to the central sign) the
  play of the OTHER SL(2,ℤ) lift −M of the object's PSL(2,ℤ) monodromy, i.e.
  the (−A₁)-bundle (trace −3, a genuine distinct Sol manifold; −A₁ ≁ A₁ since
  traces differ). Consequence to verify: tr_odd = ½(Z(W) + Z(S²W)) and
  tr_even = ½(Z(W) − Z(S²W)) — the θ-parity channels are the lift-symmetric and
  lift-antisymmetric combinations. Verify on words RL, RRLL, RRRLLL, RRL.
  (Adjacent banked fact, cited not rediscovered: B279 — τ fixes both spin
  structures of the COMPLEMENT; this cell is the closed-bundle lift bit.)
- **K1 (the clock map, BLIND):** the SU(3)_k ladder k = 1..12: C central?
  odd-block dimension, tr_odd, tr_even, the odd-block clock order, Z. The two
  blind sub-questions: (a) is tr_even = 0 special to k = 2 or universal? (b) is
  the clock order an arithmetic function of κ = k+3? Where does 60 (L77's
  ord(A₁ mod 3)) appear, if anywhere?
- **E1 (cited):** E₆ level 1 (B569: odd sector inert) and level 2 (C3: order 4,
  tr 1) enter the cross-stage table as cited rows.

## Discipline

- Gates: k = 2 must reproduce the banked B584 values (tr_even = 0,
  tr_odd = −1/φ, order 10) and B238's Z = −1/φ; modular gate green at every k;
  C central at every k; even/odd decoupling exact.
- K1 is computed blind (the table prints before any law-hunting). Any law read
  off the table is POST-HOC and must then be verified on held-out levels
  k = 13..16 before it can be stated as a finding.
- Clock order capped at 2520; if no finite order, report the eigenvalue
  arguments as fractions of 2π via continued-fraction identification instead.

## Falsifiers

- N1: if tr(Cρ(W)) ≠ −tr ρ(S²W) for any word, the naming fails — report the
  matrix-level facts only.
- K1: no registered expectation (blind). If tr_even = 0 persists at all k, that
  is a stage-family law (bank); if it dies at k ≠ 2, golden-specialness
  sharpens (bank the contrast).

## MB13 sweep (done before this prereg)

B238 (su3_data(k) parameterized — reused), B584 (gates + the k=2 point), C3
(E₆₂), B569 (E₆₁), B279 (complement spin bit — adjacent, cited), L77 (clock
orders 4/60), L81 (this arc = its cells (a)/(b)/(c)). No prior −M-bundle /
two-lifts computation found in frontier/ or OPEN_LEADS.
