# B1191 — THE CLOSE-LOOP, BATCH 3: THE GRAND COMPUTATION v0 lands (GC-11…GC-15)

**Status: banked (frontier). Verdict PROVED.** The campaign's capstone batch: 3/5 cells survived
their lenses intact; the two refutations were a coverage list (GC-11 — six omitted rows, added at
banking verbatim) and a placement residue (GC-13 — λ distinct-but-unplaced). Cells + lens verdicts
archived verbatim in `verification/batch3_cells.json`. Gate 5 absolute throughout.

## The deliverable: `docs/GRAND_COMPUTATION_v0.md`

The universe in the object's own units, v0 — six sections (arena+clock in ticks of A₁ / the meter
with the whole family as integers in V_reg and S(member n) = −n·V_reg·σ / the forced content / the
dimensionless coupling shapes / the typed input slots / the deletion schedule), completed at
banking with the completeness lens's six omitted rows (§7) and the batch's amendments (§8). The
meter identities were re-computed fresh in-cell (Vol(m004) = 12·Vol_orb; V_reg = 6·Vol_orb;
residual 2.7e-51) AND independently reconfirmed by the lens at 60 dps from the Lobachevsky
integral. **The end-state input list: ℓ (external by design) + the relational c-bit (heterogeneous
door) + finite labels + σ (one bridge) + the λ-placement.**

## The cells

- **GC-12 (PROVED, survives): the L154 fingerprint route is DEAD, honestly.** The full survey
  finds exactly ONE banked object-side artifact that is even syntactically a q-series (B672's
  doublet) — and it fails the kind-map's non-negativity requirement at its first nontrivial
  coefficient; every other candidate (WRT, Kashaev, Habiro/GSWZ, state integrals, palettes) is not
  a q-series at all. The bridge needs a genuine boundary character no banked artifact supplies —
  L154's remaining object is now exactly typed.
- **GC-13 (PARTIAL): γ₅ typed and closed as the SIXTH leg** — a finite-place ℚ(√5)-hosted discrete
  bit, exactly the meeting-V₄'s √5-swap (8-element Galois table, lens-confirmed incl. the
  compositum degree 8 = (ℤ/2)³). **λ proved DISTINCT from σ** (field mismatch; the object's exact
  algebraic rigidity; λ's sealed freeness) but not positively placed — the one remaining assembly
  residue.
- **GC-14 (PROVED, survives): D3 closes positively.** Under B1100's correct self-conjugacy
  criterion (the centralizer's OWN Cartan), BOTH rank-4 centralizers retain complex constituents —
  in fact every occupied weight class has zero-multiplicity negation partner: all constituents
  complex. Rank-4 non-abelian proper points with complex reps EXIST in E₆.
- **GC-15 (PROVED, survives): the Selector's proved piece is a formal theorem** (three exact
  regimes; a PROVED finite-case correspondence to B1183's one-class theorem). And the B998 lock
  audit: **F2 and F8 were already banked (B1003)** — the finished-but-forgotten class striking the
  lock registry itself — only F3 was genuinely missing; its test is drafted and verified green
  (5/5), to land in the next test-landing pass.
- **GC-11 (PROVED; completeness-refuted, repaired here)**: the v0 as drafted omitted I1–I3, A2,
  F3, H3 — the lens enumerated them exactly; §7 adds them verbatim from the ledger.

## Fences

The v0 is a TYPING document: its one fresh computation (the meter identities) is dual-verified;
every other row carries its arc's grade, with batch-2's refutations honored not suppressed. The λ
residue and the GC-8 mechanism cell remain open and are typed as such. Scratchpad paths
sanitized. No firewall crossing; no measured value anywhere in the v0 (Gate 5 absolute).
