# B416 (TW2) — PRE-REGISTRATION: track the trace-map dynamics to its attractor (blind)

**Committed before computation. Destination Atlas wave 2. The three tracking disciplines +
the campaign emergence bar (fixed). Firewalled; name every structure as pure math.**

## What we track (the object's generative flow)

The trace map T₁(x,y,z) = (z, x, xz−y) with the figure-eight monodromy T₁², on the SL(2,ℂ)
character variety. Banked (B109): the void (2,2,2) is a fixed point; Jacobian eigenvalues
{φ⁴, φ⁻⁴, 1}; Lyapunov {0, ±4 log φ}; κ = x²+y²+z²−xyz−2 is the conserved invariant; the
void is a (2,1) saddle of κ.

Track to the DESTINATION (blind, exact where possible):
- T1: the full fixed-point / periodic-orbit structure of T₁² on the κ-level sets — the
  ATTRACTOR set and its exact symmetry group (the trace-map automorphisms permuting orbits).
- T2: the periodic-orbit COUNT by period (the dynamical zeta function's first terms) — a
  forced combinatorial invariant of the flow.
- T3: name the destination as pure math (an Anosov/pseudo-Anosov flow; the golden
  rotation; a known dynamical system).

## THE BAR CHECK (registered, four-part; see campaign bar)

Does any FORCED invariant of the attractor (its dimension; its symmetry group order; the
periodic-orbit counts / zeta coefficients) match an EXACT SM structure (a gauge rank
pattern, generation count, a conservation group)? CONTROL: the same invariants for a
DIFFERENT monodromy (e.g. the R²L² "silver" trace map, or a random SL(2,ℤ) word) — the
match must be figure-eight-SPECIFIC to count. Anything short of all four bars = named
negative.

## Outcome (two-outcome)
Named destination + bar verdict, either way. Machinery: frontier/B109_trace_map_dynamics
patterns; exact κ-arithmetic + numpy for orbit enumeration. Locks from the JSON.
