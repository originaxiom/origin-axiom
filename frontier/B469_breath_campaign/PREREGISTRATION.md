# B469 — PREREGISTRATION: the Breath Campaign — the residue as the program's object

**Committed before computation. Firewalled. Source: the owner's standing directive
(2026-07-08): "now the program is the residue, the breath." The methodological frame is
the one Chat-1 endorsed and B468 demonstrated: track the residue ON ITS OWN TERMS, no SM
reference; the names ("residue", "breath") live in the S-room/Concept Atlas; FINDINGS
carry arithmetic only. S054 whitelist + burden-inversion verbatim.**

## The object (pinned from the banked record)

THE RESIDUE = the orientation character — the single ℤ/2 that survived every cancellation
and every register tonight: the one odd classical action (σ, det −1 — B467-F2), the
uniform quantum determinant (det Par·W₁ʲW₂ˡ = −1 — B467/B468), the datum the wall refuses
to merge (5₂(5,1) = mirror of 4₁(5,1) — B467-F3), the Gieseking deck action (B466), the
banked CS sign law (B289) and chirality window (B356).

THE BREATH = its oscillation: det(Par@N) = (−1)^((N−1)/2) through the level tower
(15 → −1, 45 → +1, 75 → −1, …) — and whatever the family/topology registers do.

## Cells

- **BR1 — the two-register breath law (exact)**: at levels N ∈ {15, 45, 75, 225}: quantum
  det(Par@N) AND classical sign(negation-carrying σ_N on (ℤ/N)²) — prediction (fixed now):
  both equal (−1)^((N−1)/2), i.e. the two registers breathe together; the law is Jacobi
  arithmetic (derivable — the cell's value is the exact exhibit + any refutation).
- **BR2 — the family Gieseking theorem (symbolic)**: X_m = [[m,1],[1,0]] satisfies
  X_m² = A_m = [[m²+1,m],[m,1]] with det X_m = −1 — claim: EVERY metallic bundle is the
  orientation double cover of a non-orientable punctured-torus bundle (m=1 = the Gieseking
  manifold). Verify symbolically for all m; SnapPy cross-check at m=1 (the Gieseking
  census manifold n021? — identify); the family quotient manifolds identified where the
  census reaches. **Lit-gate**: Gieseking-type quotients of punctured-torus bundles
  (standard-shaped; cite before any novelty word).
- **BR3 — the breath fields (the σ_m-orbit tower)**: B466's computation at each m: the
  σ_m-action on Fix(σ_m²) per member — the period-2 orbit of the geometric structure and
  its field (golden: ℚ(√−3), banked B448/B466). The family table of orbit fields = the
  breath's arithmetic register. Controls: two m at least (golden/silver); exact
  (sympy/Groebner on the m-word trace maps).
- **BR4 — the breath-breaking law at the wall (table + theorem-frame)**: the amphichiral
  parent's children are chiral mirror pairs (CS(child(p)) = −CS(child(−p)), banked B289;
  the B467 census's orientation-reversing merges). The cell states the law exactly, checks
  it across the census window, and adjudicates: which walls PRESERVE the breath
  (orientation-preserving collisions exist: 4₁(1,2) = 5₂(−1,1), CS equal) vs BREAK it —
  the exact classification of the in-window collisions by orientation behavior.

## Bins

Everything derivable (Jacobi characters, CS sign law, double-cover existence) → LAUNDERS,
banked as the exact exhibit; a family regularity not reducible in-session (e.g. the BR3
orbit-field tower law) → NEW-MATH-eligible with lit-gate; H1 only via B398 (owner
present). Killed-trap flags: "chirality = matter/antimatter" and every physics name HELD
at birth (B452); the breath naming stays in `speculations/`.
