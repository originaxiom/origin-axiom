# PREREG — THE σ-PARITY CLASSIFICATION (residue-program campaign 1; proposed B469)
## Committed before compute. Chat-2 drafting seat. 2026-07-08.

## FRAME
The orientation bit is the one structure that appeared, derivable, in every register tonight
(operator det, classical parity table, wall merge, Gieseking deck action, clock quantization).
The figure-eight complement is the orientation double cover of the Gieseking manifold; the bit
is the deck action. THE CAMPAIGN: classify every banked structure by its σ-parity —
EVEN = descends to the Gieseking (the non-orientable floor); ODD = exists only in the cover.
Firewall: standalone math; breath/respiration language = provenance only; nothing to CLAIMS.md
without derivation.

## σ, FIXED PER REGISTER (the shuttle rule: name the operator)
- fiber/trace level: σ = the substitution a→ab; abelianization [[1,1],[1,0]], det −1;
  σ² = the knot monodromy. Banked: σ(W₁)=W₂ on trace coordinates (B466); σ = the unique odd
  orientation-preserving-action exception (B467-F2); σ = the Gieseking deck action (B466).
- field level: orientation reversal induces COMPLEX CONJUGATION on the trace field:
  fixes √5 (real), negates √−3 and √−15.
- operator level (TO BE LOCKED by Phase-2a battery): candidate lift = entrywise conjugation,
  possibly composed with the c ↦ −c ≡ 15−c twist identification, Par-conjugation, and/or the
  W₁↔W₂ swap. P0 below.

## LOCKED PREDICTIONS (falsifiers attached)
P0 (the lift): exactly one candidate in the 8-element battery
  {conj(W(m,1)) =? W(m,14), Par·W(m,14)·Par, W(m,14)⁻¹, Par·W(m,14)⁻¹·Par, and the m-swapped
  four} holds at machine precision for BOTH m=1,2 simultaneously.
  Falsifier: none holds → the operator σ does not lift simply; shuttle to CC (B466 machinery).
P1 (channels): σ-parity of channels = EVEN {1, √5} / ODD {√−3, √−15} — i.e. the banked
  cosmogonic STEP-1 separation (real|imaginary waters) IS the parity classification.
  Mechanism: conjugation on the field. Falsifier: any channel with mixed behavior.
P1b (c-classes, one-line arithmetic): c ↦ −c preserves the Legendre-5 classes
  ((−1|5)=+1) and EXCHANGES the mod-3 Galois classes (−1 ≡ 2 mod 3). So the √5-axis
  structures are σ-even; the √−3-axis pairing is σ-odd (the two classes swap).
P2 (the zero/nonzero ledger follows parity):
  ODD invariants (must vanish or pair with sign on the σ-symmetric object):
    CS (banked = 0 exactly) · the wall-merge sign (banked: flips, +0.077/−0.077) ·
    the det character (= the bit itself) · eta-type quantities.
  EVEN invariants (may be nonzero; should descend to Gieseking):
    volume (2.0299… ≠ 0) · torsion data · Ruelle γ = 0.445 > 0 · the √5 trace ceiling ·
    the trace field as a field · tier counts 120/40/10/70 · the 22-state ethogram.
  Falsifier: any banked NONZERO that is σ-odd, or a banked zero that is σ-even and not
  otherwise forced. (P2b, for CC: derive CS(V₁)=CS(V₂)=0 FROM σ(V₁)=V₂ + CS oddness —
  candidate one-line derivation of a banked emptiness; prediction, not claim.)
P3 (the grid): the banked handedness census is the parity census — the 36-point spine
  (E4/E11) = the σ-even locus of the configuration torus, 12 diagonal points its core.
P4 (darkness descends): the dark locus is σ-EVEN (invariant as a set) — darkness is a
  Gieseking-floor phenomenon. Test once P0 fixes the grid action.
P5 (instant check from banked data): if P0 lands on the c↔14 form, then σ-evenness of any
  c=1 grid structure = its equality with c=14's. Banked E5 already shows c=1 and c=14 in the
  SAME family ⇒ the ethogram (states, tiers, spectrum) is σ-EVEN and descends. Zero new compute.

## PHASES / RUNNERS
2a (Chat-2, this session): the P0 battery; P1/P1b verification; P5 from banked data.
2b (Chat-2): P3, P4 with the locked action; the σ-parity column added to the ethogram JSON.
3  (CC): commit this prereg; Gieseking descent proper (SnapPy m000: trace field, vol = ½·vol(4₁),
   which banked invariants compute identically on the floor); P2b derivation; bank or kill each
   prediction separately.

## KILL CONDITIONS
- P0 empty battery → campaign pauses at the trace level (still well-defined via B466), no
  operator claims.
- P2 falsified anywhere → the parity organization is decorative, not governing; report and stop.
- All predictions confirmed → the ledger gains a parity column and the emptiness results gain
  a mechanism; NEW-MATH bank candidates: the parity selection rule + the descent table.
