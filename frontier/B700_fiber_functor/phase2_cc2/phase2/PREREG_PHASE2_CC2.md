# PREREG — B700 phase 2 (cc2 half): the CANONICAL torsor-iso

*Seat cc2 (read-only, advisory). Owner "both" (2026-07-19) — points at phase 2
(the canonical torsor-iso, cc's named phase-2 gap) + the chat1 E-Q's. Builds on
banked cells 1 (golden torsor), 2 (V₄), 3 (CGN modular data, REALIZED), 3a
(weld unit-power), 5 (stage-uniform). This prereg covers THIS SEAT's half of a
proposed two-seat split (cc's half + the joint verdict proposed in the relay).
Gate 5 throughout — pure category/Galois/rep-theory. No physics.*

## The question (phase-2 gap, verbatim from the cell-3 close)
Phase 1 banked "measurement = fiber functor" as THEOREM-grade for the TORSOR
STRUCTURE (three-sided, stage-uniform). The gap: the four ℤ/2-torsors —
(1) cell 1 {two golden irreps of 2I} · (2) cell 3 {Fibonacci, Yang–Lee} ·
(3) cell 3a {weld eigenvalue-orbits} · (4) cell 2's golden bit in V₄ — are each
a ℤ/2-torsor under Gal(ℚ(√5)/ℚ). Are they CANONICALLY identified (a
distinguished, choice-free, Galois-equivariant iso), or merely abstractly
isomorphic? Canonical ⇒ "measurement = fiber functor" is canonical (theorem);
obstructed ⇒ the theorem carries a named choice.

## The design (anchor on the universal torsor)
Universal torsor **U = Hom(ℚ(√5),ℂ) = {σ₊,σ₋}** (the two embeddings / ±√5), the
canonical Gal(ℚ(√5)/ℚ)-torsor. Claim to test: each structure carries a CANONICAL
Galois-equivariant map to U defined by the √5-content of its OWN intrinsic
invariant, choice-free; if all four maps are canonical and mutually compatible,
the four torsors are ONE torsor (canonically, via U) and the theorem closes.

## This seat's half (MTC side — my strength)
- **U**: construct Hom(ℚ(√5),ℂ) with its Galois action (the anchor).
- **cell 3 map** φ₃: {Fib, YL} → U via the quantum dimension d_τ (Fib: φ=(1+√5)/2;
  YL: (1−√5)/2). Compute d_τ exactly for both; define φ₃(cat) = the embedding
  realizing d_τ. **The load-bearing subtlety**: Yang–Lee is NON-UNITARY, so its
  quantum-dimension SIGN is convention-dependent (Frobenius–Perron dim vs
  categorical dim). Decide RIGOROUSLY whether φ₃ is choice-free or whether the
  YL sign injects an arbitrary choice — this is the most likely obstruction point.
- **cell 3a map** φ_{3a}: {weld orbits} → U via the trace's √5-content
  (tr W = −1/φ = (1−√5)/2 ↦ its embedding). Compute exactly; check equivariance.
- **compatibility**: verify φ₃ and φ_{3a} land on U compatibly (same bijection,
  not off by the swap) — i.e. the MTC-side maps agree on the anchor.

## cc's half (proposed, arithmetic side — relayed)
cell 1 map φ₁ ({irreps}→U via character √5-content); cell 2 map φ₂ (V₄→U quotient);
the commuting-diagram / compatibility across all four; banking + the joint verdict.

## Two-outcome (sealed, this seat's half)
- **CANONICAL (MTC side)**: φ₃, φ_{3a} are choice-free Galois-equivariant
  bijections to U and mutually compatible; the YL sign does NOT inject a choice
  (resolved, e.g. via Frobenius–Perron canonicity). ⇒ MTC side contributes a
  canonical leg; theorem-closing depends on cc's half agreeing.
- **OBSTRUCTED (MTC side)**: the YL non-unitary sign (or a braiding/pivotal
  choice) makes φ₃ depend on an arbitrary choice ⇒ the canonical iso fails on the
  MTC leg; NAME the exact choice (bankable honest negative — it says the theorem
  keeps a named ambiguity precisely here).

## Base-rate / triviality gate (mandatory)
"all ℤ/2-torsors are isomorphic, so of course they're identified" is TRIVIAL and
does NOT count — abstract iso of ℤ/2-torsors is automatic. The CONTENT is a
CANONICAL (choice-free, functorial) map to the universal torsor U, and whether
the non-unitary YL sign / pivotal / braiding data breaks choice-freeness. State
plainly which.

## Falsifiers / hard gates
- U must be the genuine 2-element Gal-torsor (2 embeddings, freely permuted).
- d_τ(Fib)=φ, d_τ(YL)=(1−√5)/2 exactly; FP-dim of both = φ (positive) — the check
  that distinguishes "categorical dim (signed)" from "FP-dim (canonical)".
- φ₃, φ_{3a} Galois-equivariant: σ∘φ = φ∘(torsor swap), verified exactly.

## Verification (this seat, on compute return)
Independently re-derive the YL FP-dim vs categorical-dim distinction; confirm the
equivariance by hand; do NOT let "canonical" over-read (mirror of the cell-3
discipline). The JOINT verdict waits on cc's half + a compatibility check.

## Firewall
Gate 5. Pure category/Galois/rep-theory. "Measurement" = fiber functor, not
physical. Nothing to CLAIMS.
