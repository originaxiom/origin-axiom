# PREREG — B700 cell 5: the general theorem (SEALED before the verdict)

## Claim
The fiber-functor torsor is STAGE-UNIFORM FOR ALL PRIME STAGES p, by the
classical representation theory of SL(2,p): the two irreps of dimension
(p−1)/2 (and the two of dimension (p+1)/2) are Galois-conjugate with
character field EXACTLY the quadratic subfield ℚ(√p*) of ℚ(ζ_p)
(p* = (−1)^{(p−1)/2}·p), swapped SIMPLY-TRANSITIVELY by Gal(ℚ(√p*)/ℚ)=ℤ/2.
⇒ the K020-in-ear PLACEMENT (B642) upgrades to a THEOREM (the torsor
structure), stage-uniform; the fiber-functor IDENTIFICATION is the
program's own framing (NEEDS-SPECIALIST).

## Two-outcome (sealed)
- **THEOREM (uniform)**: for EVERY prime p tested (5,7,11,13,17,19), SL(2,p)
  has exactly two irreps of dim (p−1)/2, character field exactly ℚ(√p*),
  Galois-conjugate with no fixed one (simply transitive). ⇒ the pattern is
  the classical exceptional-character fact, not a small-case accident.
- **NOT**: some prime breaks the count / the field / the simple transitivity
  ⇒ the uniformity fails, cell 4 stays a 2-instance placement.

## Base-rate / convention gate
The content is the UNIFORM structural fact across many p (exactly two
(p−1)/2-dim irreps, field exactly ℚ(√p*), simply transitive), grounded in
the classical SL(2,p) character table (Schur/Frobenius; the exceptional
characters are Gauss-sum-valued) — NOT a per-prime coincidence.

## Firewall
Gate 5. Classical finite-group representation theory + Galois. No physics.
The fiber-functor/measurement reading stays firewalled; nothing to CLAIMS.
