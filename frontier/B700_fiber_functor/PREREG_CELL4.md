# PREREG — B700 cell 4: the second-stage torsor (SEALED before the verdict)

## Claim
The fiber-functor torsor of cell 1 is STAGE-UNIFORM. At the E₆ level-2 stage
(shadow group PSL(2,7), θ-odd dim D=3, B666), the hearing selects one of the
TWO 3-dim irreps of PSL(2,7); they have character field EXACTLY ℚ(√−7) and
are swapped SIMPLY-TRANSITIVELY by Gal(ℚ(√−7)/ℚ)=ℤ/2 — structurally
identical to the golden case (2I, D=2, ℚ(√5), cell 1).

## Two-outcome (sealed)
- **STAGE-UNIFORM TORSOR**: PSL(2,7) has EXACTLY two 3-dim irreps, character
  field EXACTLY ℚ(√−7), Galois-conjugate with NO fixed one (simply
  transitive). ⇒ the "measurement = fiber-functor-torsor" structure is
  stage-uniform (2 instances: golden ℚ(√5) @ SU(3)₂, E₆ ℚ(√−7) @ level 2),
  which is the evidence upgrading the K020-in-ear PLACEMENT toward a THEOREM.
- **NOT**: different count of D-dim irreps, a Galois-fixed one, or a
  different/larger character field ⇒ the torsor is golden-special, not
  stage-uniform.

## Base-rate / convention gate
The content is the STRUCTURAL MATCH — exactly two D-dim (D = θ-odd dim = 3)
irreps, character field exactly the quadratic ℚ(√−7), simply-transitive —
NOT the trivial "PSL(2,7) contains a ℤ/2". |χ₃| values {0,1,√2} =
|(−1±√−7)/2| confirm the ℚ(√−7) Galois pair.

## Firewall
Gate 5. Pure finite-group rep theory + Galois. No physics.
