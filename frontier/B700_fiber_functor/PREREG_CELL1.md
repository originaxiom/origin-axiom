# PREREG — B700 cell 1: the golden measurement torsor (SEALED before the verdict)

## Claim
The object's golden hearing (B640) selects one of the TWO 2-dim golden
irreps of 2I=SL(2,5); the two are swapped by Gal(ℚ(√5)/ℚ)=ℤ/2 (φ↔−1/φ);
the set {golden irreps} is a ℤ/2-TORSOR (simply transitive) = the
fiber-functor ambiguity of the golden stage.

## Two-outcome (sealed)
- **TORSOR**: 2I has EXACTLY two 2-dim irreps with character field ℚ(√5),
  Galois-conjugate (the nontrivial σ swaps them, no fixed one), so they form
  a simply-transitive Gal(ℚ(√5)/ℚ)-torsor; the golden trace values {−1/φ,+φ}
  are its orbit. ⇒ measurement = fiber functor (a choice of golden irrep);
  ambiguity = Galois ambiguity = ℤ/2, concretely.
- **NOT**: 2I has a different count of 2-dim irreps, or one is Galois-fixed
  (rational), or the trace field is not ℚ(√5) ⇒ the torsor claim fails.

## Base-rate / convention gate (mandatory)
- "the value is in ℚ(√5) ⇒ ℤ/2" is TRIVIAL and does NOT count. The content
  is (a) EXACTLY two golden irreps (not more), (b) simply-transitive (no
  fixed point), (c) the character field is ℚ(√5) not a larger field.
- Recompute the character table over the exact cyclotomic field; the golden
  irreps' traces must be exactly (1±√5)/2-valued, Galois-swapped.

## Firewall
Gate 5. Pure finite-group representation theory + Galois. No physics.
