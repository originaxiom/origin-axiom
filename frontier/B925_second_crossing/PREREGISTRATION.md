# B925 PREREGISTRATION — R4b, THE SECOND CROSSING (sealed before any computation)

**Date sealed:** 2026-08-06 · **Seats:** cc (banking) with the solo seat's chain
(§LVII–LVIII, banked B909) — the joint cell of the register · **Register:**
MASTERPLAN v2 Phase II · **Protocol:** the locked R4 discipline inherited verbatim
(sealed prereg before data-derived numbers; two outcomes; no fitting; the
anti-shopping clause).

## The question

B915 killed "the object's boundary + pure desert" and banked the failure triangle.
The object owns an intermediate ladder — the compact D-chain, all algebra banked:

> A₂-floor (12) ⊂ D₃ = cross-shadow (18, derived su(4)) ⊂ D₄ = compact wall
> (30, so(8)⊕u(1)²) ⊂ D₅ = FMT wall (46, so(10)⊕u(1)) ⊂ E₆ (78)

Does THIS ladder close THAT triangle with the object's own numbers?

## The design: INVERT, THEN MATCH (two parts, both sealed now)

**Part 0 — calibration.** Reproduce B915's desert curve (the machinery gate).

**Part 1 — the required scales (outputs, not choices).** The gauge ladder:
SM at M_Z → [M₃] the D₃ stage → [M₄] the D₄ stage → [M₅] the D₅ stage →
[M_U] E₆. At each threshold the coupling-matching follows the BANKED inclusions
(the embedding indices computed from this build's chain algebras — not from a
textbook chart; the su(4) = D₃-derived identification and the so(10) = D₅ wall
are banked); matter = three 27's branched per stage (complete reps at every
rung), minimal breaking scalars per stage (the set stated in the computation,
fixed before evaluation); one-loop primary, two-loop shift = the truncation
band σ_th (the B915 convention). With the measured (α_em, sin²θ_W, α_s)(M_Z)
as INPUT (all three — this cell tests the LADDER, not the one-input boundary;
the one-input form is the third crossing's business) and unification at M_U
with the banked 3/8 boundary: **solve for the REQUIRED (M₃, M₄, M₅, M_U)**.
If the system is over/under-determined as posed, the honest resolution is part
of the result (report the constraint surface; UNSTABLE only for numerical
failure).

**Part 2 — the sealed match.** Form the required ratios ρ₁ = M₃/M₄,
ρ₂ = M₄/M₅, ρ₃ = M₅/M_U (and M_U/M_Pl as a recorded observable). THE MENU
(fixed now, closed under the anti-shopping clause): the banked dimensionless
ratios of the object —
(a) the nine colorless scale ratios (B912/B914);
(b) the three colored |det|^{1/3} magnitude ratios (B913-sealed);
(c) √T ≈ 2.1854×10⁻¹⁶ and T;
(d) the μ- and κ-root ratios (the six wall-coordinate ratios);
(e) the prime anchors 13/19, 13³/19³, 953/2304, and N(d) = (953/2304)²;
(f) λ = 2304/953 and its powers λ^{±1..3}; 3! and the small forced integers
    of the structure (2, 3, 6, 27).
**MATCH CRITERION:** a required ρ equals a menu entry (or a product of ≤ 2 menu
entries — stated now, not widened later) within the truncation band σ_th
propagated to the ratio. **OUTCOME A (the ladder closes):** all three ρᵢ match
under the criterion. **OUTCOME B:** any ρᵢ unmatched — the D-chain-with-banked-
scales identification DIES, and the banked deliverable is the required-scale
table itself (the object's ladder demands scales it does not visibly own).

## The disclosed prior (stated with reasons; the cell decides)

**Part 1:** the required scales EXIST in a physically sensible range —
moderately confident: Chat-1's fitted scan shows one intermediate threshold at
~10¹⁵·⁵ GeV closes the α_s gap, and the chain's D₅ = so(10) is exactly the
right kind of stage. **Part 2: MISS, moderate-high confidence** — the menu is
finite and the chance of three independent required ratios all matching is
low; the seal-scoreboard's lesson (five priors, four object-overrules) is
priced in but the honest expectation is still B. A MISS here executes the
locked kill condition: Phases III–IV lose their physics motivation;
mathematics + Phases V–VI continue.

## What this cell does NOT do (pre-stated)

No new physics identification on a HIT (that is a *sealed sentence* for the
papers, written only with the owner); no menu widening; no threshold-count
changes (the chain has exactly these three intermediate rungs); no revisiting
of B915.

## Files (after sealing)

`second_crossing.py` → `results.json`; `FINDINGS.md` verbatim against these
criteria; locks in `tests/test_b925_crossing2.py` (seal-integrity first).
