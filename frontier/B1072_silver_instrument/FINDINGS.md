# B1072 — THE SILVER INSTRUMENT: Λ transplants, and the silver answers with a third shape

**Date:** 2026-08-19 · **Seat:** cc (banking) · 5 agents (build → run + cascade → two
adversarial lenses, both HOLD), 0 errors; the run agent re-derived the build 41/41 before
using it; the verifier used a different Weyl construction AND a different arithmetic
engine · Gate 5 untouched.

## The build (all exact)

SU(4)₂: 10 primaries; κ = 6; **Σ·Σ† = 864·I = 4·6³ exactly** — the silver instance of the
golden 75 = 3·5²; S lies in ℚ(ζ₂₄), T forces ℚ(ζ₄₈) (ζ₄₈⁵ primitive); R, L exactly
unitary; the weld C = (a,b,c)→(c,b,a) splits 7 even / 3 odd ({1, 6, 15, 20′} fixed;
4/4̄, 10/10̄, 20/20̄ swapped), the split exactly R,L-invariant.

## The group — NOT the golden pattern

|⟨R,L⟩| = **18432 = 2¹¹·3²** exactly (3-prime cross-check); scalar subgroup TRIVIAL; 252
classes; **purely {2,3}-primary — no order-5 anywhere (κ = 6 has no room for a 5)**; and
NOT a direct product: a proper Goursat subdirect product (kernels 2 and 8 into the
even/odd factors, common quotient 1152) — against the golden's clean 2T×2I.

## The transplant (AC3's row): Λ runs verbatim — outcome, THE THIRD SHAPE

The odd sector is **3-dimensional (ℂP², not ℂP¹** — the task's own warning, confirmed);
projective image order 384 = 2⁷·3; **five exceptional orbits (3, 24, 24, 64, 128, 192)**
plus 27 fixed lines from 270 true complex reflections — territory ℂP¹ cannot hold. Λ's
output: **the minimal exceptional orbit (size 3, stabilizer 128) is exactly RATIONAL —
(0,1,0), (1,0,1), (1,0,−1) — so the Galois criterion passes AUTOMATICALLY for all three;
Λ returns the full orbit**, with non-vacuity exhibited (other orbits' points genuinely
move). AC3's verdict: **the rule is FIELD-GENERIC (a template — it runs unchanged and
returns a distinguished finite set), while its SELECTIVITY is object-specific**: at the
golden stage Galois-rigidity does real work (2 of 12, the √5-flip pair); at the silver
the minimal orbit is rational and the criterion is trivial. The discriminating structural
ingredient: where the stage's irrationalities live relative to the odd sector.

## L149's cascade half — and a banked-value question, adjudicated

The register's McKay-door sense is already sealed (B1019 DIVERGES — cited). At the
modular-data sense: the golden construction machinery transplants verbatim (fresh
reverification), **but B684/G2's banked ladder value at k = 2 does not reproduce as
stated**: the fresh, quadruple-derived value is tr_odd(R²L²) = δ = 1+√2 — which is
B684's OWN claimed k = 6 value; their claimed k = 2 value is 1/δ = √2−1. A six-variant
convention sweep rules out word-order/sign artifacts; B684's k = 1 row REPRODUCES exactly
with a sharper mechanism (the odd block is forced to dimension 1 — the "abelian"
hypothesis was tried and refuted). ADJUDICATION: B684's k-ladder carries an indexing/
inversion defect between its k-rows; the k = 1 structural claim stands; the affected
rows get an addendum-beside pointing here (the correction treatment; this arc does NOT
rewrite B684). The deeper golden-side theorems (B664's landscape, B1011's factorization,
B1071's Λ-selection) remain genuinely unattempted on silver — typed open.

## Verifications

Both lenses HOLD (the transplant's two load-bearing claims reproduced by fully
independent code — own field, own formulas, own BFS, own primes; the cascade verdict
reproduced with the likely root cause of B684's original error identified and two scope
caveats, recorded in the artifacts).

## Artifacts
- `b1072_results.json` (all five agents' returns, verbatim, scrubbed).