# B825 — PREREGISTRATION: the Markov-cubic motif (attempt 2 of at most 2)

cc banking seat, 2026-07-30. **Sealed before any edit to `LEXICON`.** Repository-instrument scope.

## Attempt count, declared up front

**This is attempt 2, and the cap is 2.** B824 was attempt 1 and failed criterion 2 at 18.4 %.

> **If this attempt fails, `B537` stays a recorded `GAP` permanently under this line of approach,
> and no third pattern set is tried.**

The cap exists because "iterate the patterns until the share drops below the ceiling" has no natural
stopping rule and would eventually produce a passing motif **by search rather than by insight**.
Two attempts with the exposure stated is honest; unbounded iteration is not.

## What changed from attempt 1, and why it is a lesson rather than a tuning

B824's per-pattern measurement isolated the overshoot to a single pattern: **`character variety`,
13.8 % of the corpus on its own.** That is the programme's **subject matter**, not a topic within it
— structurally identical to `prereg` (27.6 %) killing B821's meta-motif.

**The change is to drop the ambient term and keep only the specific object**: the Markov cubic
`x²+y²+z²−xyz=c` and trace triples. Every remaining pattern measured **≤ 4.1 %** individually in
B824's diagnostic. **This is applying the finding, not searching for a passing configuration** —
and the distinction is checkable, because the dropped pattern is the one B824 *measured* as ambient.

## Criteria — IDENTICAL to B824's, not relaxed

1. **Hits the target.** `B537` matches.
2. **Not vacuous.** Matches **≤ 15 %** of the corpus. *(Unchanged. Relaxing the ceiling after failing
   it is the exact move this whole sequence exists to refuse.)*
3. **Not redundant.** Not **≥ 90 %** contained in any single existing motif's set.

**FAILURE on any → revert; `B537` stays a `GAP`; the line closes.**

## Pre-stated expectation

B824's diagnostic sums the surviving patterns to well under 15 % even before overlap, so I expect
**SUCCESS**, and I expect the matched share to land **near 6–9 %**. Recording the predicted *range*,
not just the direction, so a pass at (say) 14.5 % would itself be informative about how much the
patterns overlap.

## What a success would and would not mean

Success drops `docs/atlas/BLIND_ARCS.md` to **zero open `GAP` rows**. That must be read as
*"no known uncovered object topic among substantial blind arcs"* — **not** as *"the lexicon is
complete."* The 18 motifs remain grounded in K001–K022 and unrevisited since 2026-07-01; B806's
call for a full re-grounding is untouched by this arc.
