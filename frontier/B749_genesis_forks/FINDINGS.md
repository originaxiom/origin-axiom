# FINDINGS — B749: THE GENESIS FORKS — the axiom chain, priced

cc3, 2026-07-22 (branch genesis/axiom-chain; seals v1→v2 + reviews S16_REVIEW_1 (STOP,
remedied) and S16_REVIEW_2 (SEAL-READY); execution journal-gated, 10/10 agents, all skeptics
(F3 contra-prior, F5, F6) FAILED TO REFUTE; per-fork artifacts + check-lines under forks/;
verdict objects in RESULTS.json. PR-only: cc is the merge gate. Gate 5; nothing to CLAIMS.)

## HEADLINE — the priced chain

P019's one deep axiom + two realization choices, priced by computation:
**4 ROBUST · 2 FRAGILE · F8 GEOMETRY-NECESSARY.** The chain survives with its contingency
located exactly where cc's priors put it — and twice, more interestingly than predicted.

| fork | verdict | the computed price |
|---|---|---|
| F2 periodicity | ROBUST | the whole det=+1 |tr|≤2 family measured symbolically: finite-order/reducible, no pA, no hyperbolic carrier — inexhaustibility is cheap |
| F3 silver | ROBUST (contra-prior label; prior substance CONFIRMED) | silver = census m136, shapes exact over ℚ(i), vol = 4·Catalan, H₁=ℤ⊕(ℤ/2)² (not a knot), ARITHMETIC as B125 predicted (verified, not cited) — but **Bianchi ℚ(i), disc −4**: arithmetic-yet-alien; every banked face (√−3, √5, √−15) ABSENT. The silver world is real and is not ours |
| F4 shadow rules | ROBUST | σ_inert invertible-but-non-primitive (σⁿ(b)=b forever); σ_rev = Thue–Morse, det 0, unrealizable; the swap = Fibonacci conjugate (identification, not sibling). 61/61 checks |
| F5 orientability | **FRAGILE — the parent** | the det −1 sibling IS the Gieseking (m000): vol 1.0149… = half of m004's, one ideal tet, Klein-bottle cusp, dilatation φ (not φ²); orientation_cover ≅ m004 by BOTH sealed routes; exactly one orientable double cover. **A6's price, computed: orientation = passing to the double cover = squaring the dilatation. The discarded choice is the object's ancestor** |
| F6 closure | **FRAGILE — one face survives (contra-prior)** | Sol certified (Anosov trichotomy + the filled 4₁(0,1) model with certified homological longitude): BEING absent (fields distinct by resultant 31·…; earned), cusp absent, arithmeticity absent — but **HEARING (golden/ℚ(√5)) SURVIVES in the monodromy**. Closure kills the geometric faces and keeps the dynamical one |
| F7 Sturmian strata | ROBUST | F7a: transcendental-slope witness (e−2, Euler CF pattern to 40 digits) has no substitution generator; the self-similarity locus = the quadratic class (Lagrange). F7b's in-between stratum exists ([[3,1],[2,1]]) — measured, lacks the interface |
| F8 geometry-necessity | **GEOMETRY-NECESSARY** | all four pre-registered redundancy witnesses FAIL exactly: K₀ = ℤ[φ] (Effros–Shen), traces in ℚ(√5), x²+3 irreducible over ℚ(√5) (with instrument control), End = ℤ[φ]. **The combinatorial carrier sees only the hearing face; A5 — the step into geometry — is precisely what buys ℚ(√−3)** |

## The one sentence the forks wrote

The description's golden half is combinatorial-dynamical — it lives in K-theory and survives
closure (F8, F6). The being half is geometric — it is born at hyperbolization and dies with
the cusp (F8, F6). The un-squared origin choice is the object's own parent (F5), and the
nearest arithmetic rival lives in a different Bianchi world entirely (F3). **The chain's
contingency is now located: one deep axiom (A0+A2), and two realization choices whose prices
are "geometry buys being" (A5) and "orientation buys the child of the Gieseking" (A6).**
This sharpens B746's two-column law and B747/B748's interface-only census back to the origin
— with F6's surviving hearing face as the one contra-prior finding (a refinement, not a
contradiction: the V₄ interface is interface-only; the hearing DYNAMICS is not part of it).

## Discipline trail
Two §16 reviews (one real STOP: the false "quadratic=metallic" universal, remedied as F7b —
the erratum rides on P019 for owner+cc sign-off); cc's three execution notes binding
(ROBUST-earning enforced — every absence above names its invariant; F5 dual-route; F8
pre-vacuity with witnesses that demonstrably could have fired); cc2's lindep lesson applied
(exact confirmations throughout); priors recorded before computation, one falsified (F6's
total-face-loss) and honestly reported. Locks: tests/test_b749_genesis_forks.py.

## Addendum 2026-09-09 — fork F9, the substrate count (B1323; verdict by pointer, `RESULTS.json` left at its seven forks as `tests/test_b1003_f2_f8_locks.py` requires)

**F9 [A1's "not one, not three"; C1's two-letter alphabet] — ROBUST on both carriers.** One record: GL(1,ℤ) = {±1}, no mixed closure, no hyperbolic element (the degenerate control). Three records on the torus T³: 768 transvection closures to length 4, 162 mixed hyperbolic, minimal trace 3 at E₁₂·E₃₁·E₂₃ whose dilatation is ρ³ for ρ the plastic number (the three-record analogue of A = golden²); every T³-bundle has χ = 0 and no finite-volume hyperbolic 4-manifold does — no carrier. Three records on the surfaces with H₁ = ℤ³ (S₁,₂ and S₀,₄, words to length 3): the minimal hyperbolic bundles are the Whitehead link complement m129 (ℚ(i), 4G, chiral), s780 (ℚ(√−7)), the 4-chain link t12047 (ℚ(i), 8G, amphichiral) and o9_44206 — none keeps ℚ(√−3), none is in m202's class. The price of a third record is the atom; what it buys is a remembered handedness (`frontier/B1323_the_genesis_upgrades/FINDINGS.md` §1–§2; `verification/u1_substrate_count.json`; lock `tests/test_b1323_the_genesis_upgrades.py`).

## Addendum 2026-09-14 — F9's price carries a DEPTH BOUND, and past it the price is not paid (outside bench, memo 231 cell 5)

The addendum above states F9's price flatly: ***"The price of a third record is the atom; what it buys is a
remembered handedness."*** **That sentence is true at the depth B1323 enumerated and false past it.**

B1323 fenced this itself — its §5 "Not claimed" says *"that F9's ROBUST extends beyond **words of length 3**
on the two punctured carriers (a longer enumeration or a proof is the next step if anyone wants the general
statement)"*, and its scope line calls *"the atom or the remembered bit, not both"* **"a computed fork over
the enumerated set, not a theorem about all carriers."** **The sentence propagated into this file did not
carry that fence.** This addendum supplies it.

**Run with B1323's own `verification/u1_substrate_count.py :: part_c(maxlen)`, unmodified**, from
`maxlen = 3` to `maxlen = 5`: **332 → 10 684 words, 64 → 2 256 hyperbolic bundles, 4 → 35 isometry classes.**
The depth-3 result was reproduced first as a control — all four classes to 1e−5 (m129 `abC` 3.663862;
s780 `acB` 5.33349; t12047 `aB` 7.327725; o9_44206 `aaB` 8.929318), verdict ROBUST, keepers none.

**At depth 4 two three-record bundles keep ℚ(√−3):**

| word | manifold | vol | cusps | \|Sym\| | chirality |
|---|---|---|---|---|---|
| `abbC` | **m412** = otet05_00001 | 5.074708 = 2.5 × vol(m004) | 2 | 8 | **CHIRAL**, by both methods |
| `abAB` | L12n2208 | 12.179299 = 6 × vol(m004) | 4 | 48 | amphichiral, by both methods |

**m412's five tetrahedron shapes all have minimal polynomial x² − x + 1** — m004's own, the regular ideal
tetrahedron. It **uses all three loops**, and the **record swap sends it to a manifold with 0 isometries to
it**, so the A7 bit is a class invariant there.

> **So a three-record carrier keeps the atom AND remembers the bit. F9's surface verdict at `maxlen = 5` is
> FRAGILE, and "the price of a third record is the atom" is not the price.**

**What is NOT claimed:** that F9 is ROBUST or FRAGILE for *all* word lengths, or for carriers other than
S₁,₂ and S₀,₄. B1323's scope line is kept verbatim with only the number 3 replaced by 5. The closed genus-2
carrier stays out of frame (A5b). **F9's depth-3 verdict stands exactly as banked and is not edited here;
`RESULTS.json` is untouched.** Certificate: `outside_bench/certificates/require_and_test_cell5.py`; seal
`outside_bench/seals/REQUIRE_AND_TEST_CELL5_PREREG.md`. Gate 5 untouched.
