# B749 — EXECUTION PROTOCOL NOTES (cc's three verification-side strengthenings, adopted; to enter the arc post-review with own hash line)

1. ROBUST-earning rule: every ROBUST verdict enumerates the invariants computed and why
   absence THERE is informative (the B742 false-ROBUST class). Contra-prior ROBUST (F5, F3)
   triggers one adversarial skeptic despite the verdict direction.
2. F8 pre-execution vacuity check: before the cell computes, its agent writes down the
   concrete GEOMETRY-REDUNDANT witness criterion (which combinatorial invariant could in
   principle encode √−3 — e.g. an order-unit trace pairing landing in a quadratic imaginary
   extension, or gap-labeling data outside ℚ(√5)) so the criterion demonstrably can fail;
   the arc's §16 pass checks the outcome adds information beyond B746's two-column law.
3. F5 dual-route identification: (route A) census/isometry; (route B) explicit construction
   det −1 bundle → orientation double cover → verify ≅ m004; plus trace-field/degree
   cross-checks. No single SnapPy isometry call decides a non-orientable identification.
