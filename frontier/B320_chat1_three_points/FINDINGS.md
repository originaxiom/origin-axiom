# B320 — Chat-1's three push-backs, adjudicated (fusion refuted, mod-7 run + generic, observer=seam firewalled)

**Status: banked (frontier). Verify-don't-trust engagement (not dismissal) with Chat-1's three challenges to the
"specialist handoff" plan. Firewalled; nothing to `CLAIMS.md`.** Chat-1 argued the four problems are two (fuse A+B+C),
that exhaustion was declared before running the mod-7 chain, and that the observer=seam is mathematics. Each engaged with
computation.

## Point 1 — the A+B+C fusion is refuted
Chat-1: "A (firewall) and C (generations) are the same ℤ/3." **They are different symmetries of the same atom:**
- **A** (the firewall / no-forced-choice) = the Galois conjugation `√−3 → −√−3` = complex conjugation = the amphichiral
  involution (B314/B318) — **order 2**.
- **C** (generations) = the Eisenstein-unit rotation `ω` = the E₆ center / the commensurator hidden symmetry
  (B302/B305) — **order 3**.

The Eisenstein units are `ℤ/6 = ℤ/2 × ℤ/3`; A is the `ℤ/2` factor, C is the `ℤ/3`. They **share the `ℚ(√−3)` atom**
(already B309, four faces of one atom) but are **not the same group**. And Chat-1's "the democratic Yukawa `(3λ,0,0)` is
forced by ℤ/3, pure math" **overclaims**: a ℤ/3-invariant (circulant) matrix has **rank 3** generically; the rank-1
all-ones `(3,0,0)` needs **S₃-democracy**, which is *gated* (B307 multiplicity + the symmetry type), not forced by ℤ/3.
So A (the in-sandbox firewall — the object does *not* force values) and B/C (the specialist crossing) are **different in
kind**; do not fuse them. (Verified: `rank(all-ones)=1`, `rank(generic circulant)=3`.)

## Point 2 — the mod-7 chain, now run: real but fiber-generic
Chat-1: "you can't declare exhaustion while the mod-7 → Fano → G₂ → color-SU(3) computation sits unrun." Fair — so it is
run here. The chain is real math: `|PSL(2,𝔽₇)| = 168 = |GL(3,𝔽₂)| = |Aut(Fano)|`; the Fano's 7 points ↔ 7 imaginary
octonions ↔ the G₂ fundamental 7; `E₆ ⊃ G₂×SU(3)`, `27 → (7,3)+(1,6)` (B313). **But it is fiber-generic, not
object-specific:**
- The "**7**" is the **Heawood chromatic number of the torus** (`H(genus 1) = 7`) — a property of the *fiber* `Σ_{1,1}`,
  shared by **every** metallic once-punctured-torus bundle, not the figure-eight.
- "**168 = 7 × |2T|**" is a **numerical coincidence**: the index-7 subgroup is **S₄** (the Fano point/line stabilizer),
  and `|2T| = |S₄| = 24` but **`2T ≠ S₄`** — `2T = SL(2,3)` has **1** involution (`−I`), `S₄` has **9**. The coset
  decomposition is by S₄, not the figure-eight's McKay group 2T.

So the mod-7/G₂ chain does **not** select "which SU(3) is color" object-specifically — it is fiber-generic, exactly like
B313's generic `(G₂)₁` bridge. **The one lead Chat-1 flagged as unrun is now run, and it is generic — exhaustion stands.**

## Point 3 — observer = seam: firewalled, not "mathematics"
The complement/filling ↔ quantum-measurement parallel (complement = superposition, filling = measurement, CS = outcome)
is a **structural rhyme** — a firewalled `[HOOK]`/`[LEAP]`, **not a proven mathematical isomorphism** (Chat-1 overclaims
"it's mathematics"). Correctly firewalled. The genuine *math* residue — that the seam (B286) connects to Face IV (B293,
the Goldman/quantization) — is worth a `speculations/` note, but it does not cross the firewall.

## The net
Chat-1's push-backs are the healthy multi-seat pressure, and one (the mod-7 challenge) was a fair catch — I *had*
dismissed it without computing. Now computed: the fusion is refuted (`ℤ/2 ≠ ℤ/3`; democratic rank-1 needs S₃), the mod-7
chain is fiber-generic (Heawood 7; `2T ≠ S₄`), and the observer=seam is firewalled. **Exhaustion stands; the four
problems stay four (A in-sandbox, B/C/D specialist); the specialist handoff is unaffected.** This is a further instance
of the absorbing-loop caveat (K020 §6a) — but honestly resolved by computation, not asserted.

## The fence
Group-theoretic computation (circulant ranks, the SL(2,3) involution count by brute force, the Heawood number) + the
standard `PSL(2,7)/Fano/octonion/G₂` facts. The observer=seam reading is firewalled. Nothing to `CLAIMS.md`.

`chat1_three_points.py` (pyenv) · `tests/test_b320_chat1_three_points.py`. Related: **B314**/**B318** (the firewall
ℤ/2), **B302**/**B305** (the generation/center ℤ/3), **B309** (four faces of one atom), **B313** (the generic `(G₂)₁`
bridge, `27→(7,3)+(1,6)`), **B307** (the multiplicity gate), **B286**/**B293** (the seam / Face IV), **K020 §6a** (the
absorbing loop). Lit: `PSL(2,7) ≅ GL(3,2) = Aut(Fano)`; the octonion/`G₂`/Fano correspondence; the Heawood map-color
theorem.
