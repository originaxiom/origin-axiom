# B939 — THE KLEIN ASSEMBLY: the six shadows are NOT one group; they meet in the frame-invisible wall twist

**Date:** 2026-08-07 · **Seat:** computation agent + cc banking · blind.

## The sharp pair, decided

**K4 (the wall pair's 2-torsion — inner, Cartan-fixing) and W_frame (the solo
seat's §LIII frame group) are DISTINCT subgroups of Aut(e₆)**, overlapping in
exactly **{1, σ₋₁}**:

- **K4 does not act on the frame at all** — two of its legs break every charge
  line; neither pencil plane survives.
- **W_frame is realized by the OTHER Klein-carrying group**, C8 = ⟨φ₊, σ_c,
  σ₋₁⟩ (the C-compatible census subgroup), with P(C8) = W_frame and kernel
  {1, σ₋₁}.
- **The shared leg is the frame-invisible one**: σ₋₁ = the wall twist D — the
  same element B938 found blind to the colorless register and B936 found
  carrying the nonzero H¹ class. Three independent arcs, one element.

## The 27-shadow map (banked)

σ₋₁ → D (12 flips) · σ_χ₋ → **D₂ (the ELEVEN)** · σ_c → D_c (12). K4's
frame-invisible legs are 27-VISIBLE, and W_frame's carriers cast exactly the
census D-set. Invisibility and visibility trade places between the frame and
the 27 — the same complementarity B936 found between the register and H¹.

## What this closes and what it opens

CLOSED: the "six Klein groups are one" reading — refuted for the sharpest
pair, with the mechanism (different fixed structures). OPEN: the remaining
pairwise maps (the two chiralities, the hemispheres, the pencils, the
ω-branches) — each a future cell; the assembly diagram in `results.json`
records what is known, related, and UNKNOWN honestly.

## Files

`assembly.py` → `results.json`, `run.log` · locks: `tests/test_b936_b939.py`.
