# B407 — BANKED: the support-nesting claim — verified at (1,2), killed as a law

**Status: complete. Firewalled. (Processing the Chat-1 two-facts note.)**

- **Fact 1 (φ⁴+φ⁻⁴ = 7): correct arithmetic, ALREADY BANKED** (B109's void Jacobian;
  made explicit in B398 S4 — "neither was in the repo" is wrong on this one). The
  {3,5}-reduction reframing holds for 7 = L₄ and 11 = L₅ (standard Lucas arithmetic);
  **the extension to 23 is an OVERCLAIM** — 23 is neither Lucas nor Fibonacci and no
  φ-identity was given. Their own F₇ = 13 self-flag was correct.
- **Fact 2 (supp √−3 ⊆ supp √−15): VERIFIED AT (1,2)** — exactly 28 both + 16 s-only +
  0 z-only (their "16 cells" checks) — **but KILLED as a universal law:** (2,4) has both
  exclusives (8 z-only, 16 s-only — no nesting either way) and (1,3) nests in REVERSE
  (24 z-only, 0 s). Full table (nesting.json): (2,3) and (3,4) have exact support
  EQUALITY; (1,4) is fully real.
- **The alignment (the real find):** the per-pair nesting types mirror the M1 product
  strata exactly — (1,3)'s 24 z-only table cells ↔ its 24 z-only products; (1,4) fully
  real at both levels. The table-level imaginary anatomy IS the product-level
  stratification, pair by pair — a consistency law across two banked structures.

**Provenance.** the nesting census → nesting.json; locks tests/test_b407_nesting.py.
