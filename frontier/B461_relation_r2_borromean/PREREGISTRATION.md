# B461 — PREREGISTRATION: Relation R2 — the first link (the Borromean rings), budget-laddered

**Committed before computation. Firewalled. Campaign: OPEN_LEADS §Relation. The killed-trap
flag binds: "3 components = 3 generations" is the B428-lineage trap — the OBJECT is legitimate,
the motivation is quarantined.**

## The question (blind)
The program's first multi-component object: does any invariant of the Borromean complement
(L6a4) carry structure beyond its arithmetic class (ℚ(i)) — and which invariants see the
Brunnian property?

## The incoming-claims table (Chat-1/Chat-2, to verify exactly; two corrections already filed)
vol = 2·v_oct = 7.32772475… (NOT "8 octahedra" — 8 is the tetrahedron count) · trace field
ℚ(i) · H₁ = ℤ³ · geometric-component dim = 3 (= #cusps, Thurston — cited, standard) ·
μ̄(123) = ±1 with pairwise lk = 0 (textbook Milnor — CITED as the link-register facts).
Correction 2 binding: the field comparison is trace-vs-trace (ℚ(i) vs ℚ(√−3) — both imaginary
quadratic); "the Borromean lacks the fibered two-ended structure" is the honest contrast.

## The ladder (STOP criteria pre-committed)
1. SL(2) exact (Groebner, small) + the arithmetic-ℚ(i) gate (geometric shapes' minpolys).
2. SL(3): **F_p/CRT dimension probes per obstruction class first** (14 classes × 32 vars —
   the banked B80/B153 method) to find 0-dimensional classes.
3. Sage elimination per 0-dim class, **1 h timeout per class**.
4. Numerical multi-start + LLL/minpoly recognition as fallback.
5. Partial result = named boundary (never silent).

## The Brunnian test — two registers (binding, per the red team)
- LINK register (the discriminating invariants, cited facts for named links): lk-matrix ≡ 0 +
  μ̄(123) = ±1 + sublink triviality — L6a4 has them; s776 (chain) has nonzero adjacent lk.
- MANIFOLD register: **prereg'd EXPECTED-BLIND** (the B143 trace-blindness lesson: hyperbolic-
  manifold invariants cannot see the S³-embedding data; blindness ≠ laundering).

## Controls
Whitehead link (2-cusp, same ℚ(i) class) · **s776** (3-cusped, NOT Brunnian — banked B143) ·
**o9_39906** (3-cusped, H₁ has torsion ⇒ NOT an S³ link complement — the embeddability control).

## Bins
ℚ(i)-class arithmetic throughout (expected — the Inversion Law's first test on a NEW class) /
SL(3) fields not class-determined → the live cell (burden-inversion applies) / any manifold-
register Brunnian claim → must beat the embeddability control or it is cusp-count-generic /
ladder stall → named boundary. WRT scope fixed: multi-colored Jones at ζ_r, r ≤ 12 — DEFERRED
to a follow-up if the ladder consumes the budget (stated, not silent).
