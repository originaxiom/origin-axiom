# B1026 PREREGISTRATION — the crossing-candidate NOMINATION cell (sealed before compute; DATA-BLIND by construction)

**Date sealed:** 2026-08-11 · **Seat:** cc · **Owner authorization:** "go do it, the word is
given." Gate 5-Q. **NO measured value enters this cell** — nomination is ranked by STRUCTURE
alone; measurement is contacted only by the subsequent crossing seal (B1027), if a nominee exists.

## The inventory (object side — every banked, forced, anchor-free coupling-channel value)

| id | values | kind | grade |
|---|---|---|---|
| T1 | the five tones \|Re(ζ⁻¹ūMu)\| ∈ {0, 1/(2φ), 1/2, φ/2, 1} | amplitude-part magnitude [0,1] | THEOREM (B641→B1011) |
| T2 | the family values \|h\|² ∈ {1/(φ√5), φ/√5, 1} | probability | observed law (B856), period derived (B1011) |
| T3 | the mirror set {0, ±¼, ±1/(4φ), ±½, ±1/(2φ), ±φ/4, ±φ/2, ±1} | amplitude-part | THEOREM (B1011) — never compared to anything |
| T4 | arg h ∈ {0, +2π/3, −2π/3} | phase | THEOREM (the χ values, B1011) |
| T5 | h(m ≡ 0 mod 5) = −1 exact | signed amplitude | THEOREM (B1011) |

## The target classes (SM side — kinds only; NO values listed here, by design)

P (probabilities): sin²θ₁₂, sin²θ₁₃, sin²θ₂₃ — leptonic. A (amplitude moduli): |V_us|, |V_cb|,
|V_ub| — quark. Φ (phases): δ_CP — leptonic; δ — quark. (Ratios were B929's spent ground —
excluded.)

## The nomination criteria, sealed (structural, data-blind)

- **N1 — kind-exact** per `docs/KIND_TABLE.md`: T1/T3 ↔ A · T2 ↔ P · T4 ↔ Φ. No cross-kind pair.
- **N2 — forcedness**: THEOREM-grade object values outrank observed-law values.
- **N3 — canonical pairing map**: every residual choice is priced at seal — which target within
  the class (log₂ #options bits), which object value within the set (log₂ #values bits) **unless
  a structural selector exists** (a selector = a banked reason naming the element, e.g. "the
  unique nontrivial phases" or "the family's exact integer value"). Priced bits are FINAL.
- **N4 — spent ground**: the (T2 smallest value ↔ solar sin²θ₁₂) pairing is DEAD (B856, −2.3σ);
  its cost stays row 1 of the lane ledger; no re-pairing of the same two elements.
- **N5 — the cap**: a nominee's total priced freedom must be **≤ 3.0 bits**, else it is not
  nominated. If no pairing clears: **verdict EMPTY** — the lane is open but currently without a
  candidate, reported as such.

## The ranking rule (deterministic, so the cell cannot dither)

Score = (N2 grade: THEOREM = 0, observed = +1) + (priced bits under N3). **Lowest score wins; a
tie is a tie and both are reported with the tie-break deferred to the owner.** The single nominee
(or the tie, or EMPTY) is this cell's entire output.

## Declared prior

**A nominee EXISTS at ≤ 3 bits — weak lean, stated honestly:** T4's phases are few (3 values, two
nontrivial and conjugate — arguably one structural choice) and the phase class has few targets
(2). The lean is weak because N3's pricing may still exceed the cap once counted exactly.
Non-weakening applies: the cap and the criteria do not move after unsealing.

## What follows

NOMINATED → B1027, the crossing seal (all eleven requirements + this nomination cited), data
contacted only there. EMPTY → banked as the lane's honest state; the θ-even mirror sector
becomes the named frontier. Nothing in this cell touches Tier-2 status either way.
