# B1026 — the nomination: T4 at 2.0 bits, and the seal killed its own author's first draft

**Date:** 2026-08-11 · **Sealed:** `7e798db1` · data-blind throughout. **Verdict: PROVED —
NOMINATED: the χ phases ↔ the CP phases, at exactly 2.0 priced bits** (prior "a nominee exists at
≤ 3 bits, weak lean" — held).

**The scoring** (deterministic; `nominate.py`): T4 **2.0** (THEOREM grade; sign 1 bit + sector
1 bit; the object set reduced to {±2π/3} by the banked nontriviality selector) · T2 3.0 (clears,
runner-up) · T1 3.907, T3 4.585 (over cap).

**The recorded near-miss — the gate against its own author:** a first draft scored **T5
(h(5) = −1 ↔ |V_ij|) FIRST at 1.585** — and the sealed N1 killed it: the inventory types T5
*"signed amplitude"*, and `KIND_TABLE` licenses **no** signed-amplitude ↔ modulus pairing. The
nominee would have been boundary-degenerate (|h| = 1 is a *no-mixing* datum). **The kind-clause
caught what the scoring arithmetic missed**, and the near-miss stays in the source as a comment.
