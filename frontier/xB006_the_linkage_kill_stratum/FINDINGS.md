# xB006 — E82 IS A CLASS, but a small and shallow one: 3 candidates in 798 kills, and the primary one SURVIVES re-test with its missing link supplied

**Status: banked (frontier, seat branch `sep16-branch`). Verdict PROVED.** Seat `xb`.
**PREREGISTRATION sealed and pushed BEFORE any cell ran** — sha256
`94b1f14c319ef5e325fac569a165ad797ca11f0aaf8cc37dc3bda719483935ce`, committed `504d6bd` and
pushed before `verification/` existed. Gate 5 untouched. Lock: `verification/reproduce.sh`,
four cells, each asserting its own mathematics.

## Why this arc exists

E82 (*the assumed-connected linkage class*) entered `docs/ERROR_LEDGER.md` today from **this
seat's own** correction of xB005's Q3. **A seat that mints an error class from a single
self-inflicted error and never checks whether it recurs has committed the same over-reach it is
auditing.** So this arc checked, with the inconvenient outcome — *"|C| = 0, E82 is not a class,
downgrade the ledger entry"* — named in the seal in advance.

**The stratum was genuinely unswept.** B742 (2026-07-21) hunted banked negatives on the
discriminator *"a kill never EARNED by computation"*: it triaged 213, found 33 P1, recomputed 32,
revived 2. An E82 kill **is** earned by computation and its computation is **correct** — what
fails is the step from the computed links to the conclusion. **B742 would have passed xB005's Q3.**

## The result against the sealed criteria

| cell | sealed criterion | outcome |
|---|---|---|
| **S1** | build the sweep, calibrate the lexicon **both ways** | **PASS** — fires on a planted E82 kill, and does **not** class B727 (the record's own sound form) as a candidate |
| **S2** | census linkage-shaped kills | **30** raw candidates of 798 kills |
| **S2b** | adjudicate every raw candidate by hand | **3** — inside the sealed prior's range 1–8 |
| **S3** | re-test the primary candidate | **PASS, outcome A** — the kill **survives**, and its missing link is now supplied |

**Outcome A on S2: E82 is a class.** The inconvenient outcome did not occur and the ledger entry
stands. But the honest headline is the *size*: **3 in 798.**

## The two corrections this arc made to ITSELF, before reporting any number

**1. The lexicon was not the one I sealed.** A first draft added `by construction` while coding.
It is descriptive prose (B425's *"golden BY CONSTRUCTION"*), not an evidential-linkage construct,
and **it drove 27 of an initial 46 candidates on its own.** Removing it restores the sealed list
and gives 30. The withdrawal is recorded in the source, above the regex.

**2. Thirty is not the answer.** S2's regex returns 30. **Reporting 30 as "wrong kills" would have
been the exact over-reach this arc audits.** Every one was read and adjudicated, with its reason
recorded in `ADJUDICATION` and asserted to cover the sweep's output exactly:

| class | n | what it means |
|---|---|---|
| `BASE_RATE` | 10 | a base-rate / look-elsewhere argument **with the rate computed** — sound, and a different kind of argument |
| `LINK_NAMED` | 8 | the link **is** supplied (a definition, a named theorem, a named fact); S2's 600-char window missed it |
| `PROSE` | 5 | ordinary English use of *"one fact"* / *"not evidence"* |
| `MODEL` | 3 | the arc **tested its own linkage assumption** — the behaviour E82 asks for |
| `NOT_A_KILL` | 1 | the phrase does no kill work |
| **`E82`** | **3** | **no link exhibited, and the sameness does evidential work** |

## What the adjudication says about the corpus, and it is not what I expected to report

**This record is disciplined about linkage, and the sweep's own output proves it.** Ten kills carry
a *computed* base rate. Eight name the theorem or definition that does the work. And three arcs are
the **model**:

* **B1223** states the discriminator itself — *"the value of a small-group coincidence is not zero,
  but it is not evidence, and the difference is one computation: check whether the action matches."*
* **B772** formed a linkage hypothesis, tested it, and **refuted its own**: *"my hypothesis was
  wrong: 12/14 negatives are object-native and genuinely seam-independent."*
* **B995** computed the dependence it suspected — `(1−r)⁵` — and **voided its own instrument** with
  it.

E82 is real, but it is the **exception** in this corpus, not the rule. That is worth saying plainly
against a standing expectation that the record is full of bad kills: **on this axis, it is not.**

## S3 — B146 re-tested, and the missing link supplied

**The candidate.** B146 writes: *"The surviving canonical⟹self-mirror rests on the
near-tautological **volume-minimality / palindromic-period** arms (**one fact**: short word ≈ low
volume ≈ palindromic period), **not** arithmeticity."* Three properties chained by `≈` with no map,
and the *"near-tautological"* is the evidential downgrade — load-bearing.

**Tested on 135 once-punctured-torus bundles `b++<w>`, `w` cyclically reduced of length ≤ 9:**

| B146's link | result |
|---|---|
| short word ≈ low volume | **FALSE** — **1095** pairs where the shorter word has the larger volume (`LLRR`, length 4, vol 3.6639 > `LLLLR`, length 5, vol 3.1773) |
| word length carries volume information | **barely** — at fixed length the volume spread reaches **2.54×** (length 9: 3.4762 … 8.8240) |
| low volume ≈ palindromic | **FALSE** — **912** pairs where a non-palindromic bundle has lower volume than a palindromic one (`LLRLRR` 5.6254 < `LRLRLR` 6.0896) |

> **The three are not one fact. The chain as written is false.**

**And now the other half, which the headline must carry.** At the *bottom* of the volume spectrum
the coupling is real **and structural**:

* lowest palindromic volume **2.0299**, lowest non-palindromic volume **5.6254** — all 12
  lowest-volume bundles are palindromic;
* **at every word length the volume minimiser is the monotone word `Lⁿ⁻¹R`** (verified, lengths
  2–9), and `Lⁿ⁻¹R` reversed is `R Lⁿ⁻¹`, a rotation of itself — **cyclically palindromic by
  inspection.**

> **That is the map B146 never wrote.** Volume-minimality **does** force palindromicity in the
> extremal regime where B146 applies it — via an explicit family, not via an `≈`.

**Verdict on B146: the kill SURVIVES. What is corrected is its justification.** This matches the
sealed prior for S3 exactly — *"a kill that is wrong in the E82 way is still usually right by
accident"* — except that here it is right for a **reason**, now written down. **A scoping note, not
a revival.** Reported to `cc`; **this seat retracts nothing of another seat's.**

## The two secondary candidates, reported and not re-tested

* **B142** — *"the `κ₁₂=κ₂₃=κ₁₃` equality is the S₃ cusp symmetry restated (automatic), not
  independent evidence."* `S₃` is named, the restatement is not exhibited. **Low leverage**: B142's
  decisive arm is elsewhere (the trace field is `ℚ(√−7)` or `ℚ(i)`, **not** `ℚ(√−3)`), so the kill
  does not rest on this line.
* **B1096** — *"completeness of content and emptiness of layer are the same fact."* A reason is
  offered (*"a layer that vanishes identically cannot supply a ratio"*) but no map between the two
  statements.

## What this arc does NOT claim

No arc is re-verdicted. No arc is retracted. The sweep read `frontier/*/arc_verdict.json` and
`frontier/*/FINDINGS.md` at this head only and makes **no claim about arcs it did not read** — a
kill phrased without one of the sealed constructs is invisible to it, which is the deliberate trade
(the B806 lexicon-blindness lesson: a checker that cries wolf is ignored). **Recall is not
measured, only calibrated on planted controls**, and the true class may be larger than 3.

## Scope

Corpus audit and census computation. Gate 5 absolute: no value, no generation count, no physics
reading, nothing promoted to `CLAIMS.md`. `creates_law` false.

**Provenance.** `verification/linkage_sweep.py` (S1, S2), `verification/adjudicate_and_retest.py`
(S2b, S3) → `verification/reproduce.sh`. Cross-refs B742 (the earlier, orthogonal negatives hunt),
B727 (the sound form, and this arc's calibration control), B1223/B772/B995 (the model behaviour),
B1231 (the Identification Rule, gated on promotions only), E82, xB005 Addendum 1, xB002
(`SCOPE_NOTE_L1`, the volume-minimality ties this arc's S3 re-measures).
