# B671 — THE EXTRACTION FIRST (the web seat's priority call, adopted
# and executed; main seat, 2026-07-18; all decisive facts
# independently verified at the matrix level)

## The eigenvalues, extracted — and THE GAUGE VERDICT (step 4 rules)

Both banked scalarizations of the 351-cup's solo 3×3 stated exactly
(the full matrices, charpolys, and 40-digit eigenvalues in
extract_output.txt; the charpolys re-derived independently from the
banked matrices — exact match both). Both are symmetric, rank 3,
with irreducible cubic charpolys over ℚ(√−3). **BUT the
scale-invariant coefficient combinations (e₁³/e₃, e₁e₂/e₃) DIFFER
between the two scalarizations by ~7 orders of magnitude — the
eigenvalue RATIOS are SCALARIZATION-GAUGE, not object content.**
The two canonical gauges do not even approximately agree. The
"candidate mass matrix" reading of the eigenvalues dies at exactly
the step the web seat demanded be run first: their prioritization
was right, AND the answer is negative — both true. Also structural:
in both gauges the spectrum has the {ε, λ, ≈−λ} shape (two large
near-opposite eigenvalues, one tiny) — the antisymmetric-dominated
signature, consistent with the fifth wall's Hodge-slice scalarity.
WHAT SURVIVES AS INVARIANT: the 351-cup class table itself (banked,
B666/A2r), its slot-symmetry, rank, and field — the scalarized
spectra bank as DATA under their named gauges, nothing more.

## THE GENERATION SUM RULE, extracted (from cc2's completed D1;
## provisional pending their loop-4 packet)

The three solo-solo pairing vectors (31-dim H²-obstruction
coordinates) have rank 2 — ONE exact relation, and it is clean:

> **[P₂₃] + (7983360/13)·ω·[P₃₄] = 0, with [P₂₄] independent**
> (α, β, γ) = (1, 0, (7983360/13)·e^{2πi/3})

The relation involves only TWO of the three pairings: (2,3) and
(3,4) are PROPORTIONAL with ratio −(7983360/13)·ω — the Eisenstein
unit ω times the banked Y[023]-core magnitude with its 13
denominator — while the (2,4) channel (the one the m=1 bend
switches, the class-4/heart channel per cellB2/cell7) stands alone.
The being end's tightest generation constraint: two independent
pairings, not three, with the dependence Eisenstein-twisted and
13-weighted. Numbers read from the cc2 seat's d1_results.json
(their D1 complete on-seat; PROVISIONAL until their loop-4 packet
formally lands — the cross-check fires then).

Artifacts: extract.py, extract_output.txt. Locks:
tests/test_b671_extraction.py.
