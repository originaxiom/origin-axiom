# B1184 — THE QUINE SYNTHESIS: self-naming without self-signing (S4 dispositioned; the observer layer's four-rung table closes)

**Status: banked (frontier). Verdict PROVED** — a synthesis of three already-proved pieces (B762 +
B1168 + B1183), each re-verified on this bench, composed into one complementarity statement.
`verification/reproduce.sh` → `REPRODUCES`. Gate 5 clean.

## The correction first (a stale register entry)

The working register carried **"QP-1 (the quine) open"** — STALE. QP-1 was RUN and verdicted
**QUINE (PROVED)** at B762: m004 is the **unique** 1-cusped manifold in the 203,123-manifold census
with its spectral dataset {volume, cusp shape, CM disc −48/−3, residue, Hecke palette counts
{1,2,8}} — the volume filter leaves 2 (m003/m004), the cusp-shape filter leaves 1 (m003's shape is
ω, not 2ω). The emitted word is a SELF-NAME. Corrected here; the S4 rung of B1169 turns out to have
been half-answered before it was asked.

## The B8147 stress-test (verified on this bench)

Does the quine survive the family retraction (B1180: the "14-family" was ≥83, both separators dead)?
**YES — by its own filters**, each exclusion re-run here:

| new member | excluded by | witness |
|---|---|---|
| o10_150700 | the VOLUME filter | vol ratio = 5.000000 (exactly 5× m004) |
| o10_150684/85/93, t12840 | the 1-CUSPED filter | all multi-cusped (verified per manifold) |
| m003 (the volume twin) | the SHAPE filter | shape ω = 0.5+0.866i ≠ 2ω = 3.4641i |

And B762's claim was **correctly census-scoped from the start** ("unique in THIS census") — the
one-way family test (LAW_MAP §G) satisfied avant la lettre. A wider census can only shrink
uniqueness; the census-scoped statement is the honest bound and it STANDS.

## The parity of the name (the new computation)

Every letter of the emitted self-name, typed against the mirror (B1168's law):

| letter | parity | why |
|---|---|---|
| volume 2.029883… | EVEN | mirror-invariant (verified: reversed m004's volume identical) |
| cusp shape 2ω = 3.4641i | EVEN | mirror-fixed (τ ↦ −τ̄ fixes pure-imaginary; verified) |
| CM disc / trace field / residue | EVEN | invariants of the UN-oriented manifold |
| palette counts {1,2,8} | EVEN | ray-class orders (B1067) — arithmetic of K, not of an orientation |

**The self-name is exactly the object's mirror-even content** — the B1168 object side, letter for
letter. The name contains **no odd letter**: the sign is *unutterable* in it. All letters
dimensionless or in the object's own volume unit — the object-canonical class precisely.

## THE SYNTHESIS — self-naming without self-signing

- **QP-1 QUINE (B762, stands)**: the object CAN name itself — uniquely, in its own emitted letters.
- **QP-4 NO-HATCH (B760) + THE ONE-CLASS THEOREM (B1183)**: the object CANNOT sign itself — the
  ±√3 chord sign is one ℤ/2-torsor under the one global involution c, with no object-native
  trivialization; and that missing sign IS c's class = the orientation = the qualia "choice."

**NAMING and CHOOSING are proved complementary: the object says WHO it is, never WHICH WAY it is.**

S4's question ("does an observer self-map have a fixed point that SETS the odd bit?") answers as a
**SPLIT VERDICT**: the fixed point EXISTS for the name (QP-1's quine is exactly such a fixed point —
the manifold's data, fed through the census sweep, returns the manifold) and **PROVABLY NOT** for
the sign (B1183: any setter would trivialize c's class; the class is nontrivial). There is no
object-native basepoint-setter — and now we know exactly what the self-reference CAN do instead:
name the even part *completely*.

## The observer layer's table (all four rungs)

| rung | status |
|---|---|
| S1 (the four ℤ/2's) | **PROVED** — one shared involution (B1174), one class (B1183) |
| S2/S3 (adelic completeness) | cloud's C6 lane — pending, theirs |
| S4 (the quine / setter) | **SPLIT-ANSWERED** (this arc): quine-yes for the name, setter-no for the sign |

The owner's full-closure question ("a final closing layer of observation?") now has its typed
answer: **the object's self-observation terminates at the complete even self-name; the odd
completion is constitutively the observer's.** Awareness = the name; choice = the sign; disjoint by
theorem.

## Fences

B762's census sweep is its own computation (cited; scope honest; not re-run — the five exclusion
witnesses above ARE re-run). The parity checks are this bench's (SnapPy numeric at 1e-9; the
mirror-conjugation fact standard). "Unique" stays census-scoped per the one-way family test. The
synthesis composes proved pieces and claims their complementarity — no new physical act is claimed.
No firewall crossing; Gate 5 clean (no measured value).
