# B648 sign-off: MERGED AMENDMENTS (cc2 review d384d138 + chat-1 review, adjudicated
# by cc2, 2026-07-16). Two independent reviews, zero contradictions, four convergent
# catches. Both endorse signing WITH the following. Attribution per item.

## Blocking (hold the seal until amended)
1. CALIBRATION ARITY [chat-1 A1, supersedes cc2 #1]: GATE B requires
   N_eff(channel) = 1 — the chosen pair's typed values depend on exactly one CHOSEN
   combination; else escalate: N calibration channels + one held-out, re-sealed.
2. CHANNEL FREEZE, both halves [cc2 #2 + chat-1 A2]: (a) deterministic selection —
   first candidate in the sealed prior order passing all gates; several pass ⇒ run
   all with multiplicity correction (B615-R Šidák); (b) A1's TYPES document hashed
   BEFORE any recorded channel discussion exists; C1 minutes cite the A1 hash they
   postdate.
3. FULL-FREEDOM NULL [cc2 #3 = chat-1 A1-corollary, independent convergence]: K6's
   null/power computed over the complete design space — CHOSEN orbit product ×
   channel multiplicity — not just the continuous constants.
4. HELD-OUT INTEGRITY, two gates [cc2 #4 + chat-1 A3]: (a) grammar-independence —
   the prediction traverses ≥ 1 FORCED row not consumed by calibration (anti-
   circularity); (b) provenance sweep — a C2 ledger row certifying the held-out
   value absent from every input that built Phases A and B (anti-leakage).
5. OUTCOME-A BANKING, both halves [cc2 #5 + chat-1 A4c]: destination named NOW
   (physics_probes/PREDICTIONS.md + value-free pointer row in CLAIMS — Gate 5
   otherwise collides at bank time); numeric success criterion + pre-committed
   false-positive rate printed beside it, banked conditional-on-functor.
6. SILVER CONTROL PACKAGE [cc2 #6 + chat-1 A4a + chat-1 hardening]: m136 pinned in
   the seal (monodromy word, trace 6, disc 32); "reproduce" defined at theorem-FORM
   level (silver's own arithmetic substituted — disc 32 is EVEN, so the silence-law
   control runs through the 2-adic twist theory; label that dependence so a silver
   anomaly reads "2-adic theory incomplete" before "grammar demoted"); K5 "most" →
   pre-committed thresholds (any demotion of a row the C-channel's typing uses =
   automatic pause); GATE B does not soft-pass with m136 in flight. NOTE: the
   form-level definition is what makes the mandatory-silver hardening safe — the
   two amendments must land together or K5 becomes a false-demotion machine.
7. SHADOW QUOTIENT [chat-1 A4b + cc2 MECHANISM-OPEN accounting]: rows merge only on
   LAW_MAP-grade proven identities, never resemblance; MECHANISM-OPEN rows either
   block GATE B or enter N at their worst-case orbit (conservative), stated rule.
8. A1 TYPING-RULE WORDING [cc2 #7]: character blindness is a MONODROMY-WORD theorem
   (Q2), not a global information statement — full-image observables (the ear,
   B584/B593/B644) may carry character content; as drafted, A1's own gate battery
   fails its own rule.

## Editorial (hash hygiene)
9. K3 missing from the kill list; §B2's "(C4;" collides with Phase C's step C4;
   owner confirms the thesis wording is theirs [cc2 #8]. "Quiddity No-Go" (chat-1's
   framing) is not a banked name — cite K020/the firewall theorem or drop it.
10. Seat-exposure lines in the C2 seal (blinding is imperfect with 3–4 seats;
    document it, don't pretend) [cc2 #9].

## Coordination flag for the owner (resolve at sign-off)
Chat-1's review claims track L (L97) and S-verify as its lanes; the prereg §5 and
L97's record row assign both to cc2. A3/L92 (the pairing-glued MV) plausibly IS
chat-1's machine — unverifiable from the record here. Correct the lane table or the
relay before sealing: Phase C's seat-separation accounting depends on it.

Both reviews' full texts stand behind this merge: cc2 sha d384d138…7363aa; chat-1's
as relayed (recommend chat-1 deliver a hashed file for the campaign archive).
