# THE CALIBRATION CAMPAIGN — the calibrated-pair probe (B648)

**STATUS: DRAFT — awaiting the owner's sign-off. On approval this
document is hashed (sha256 into ARTIFACT_HASHES.txt) and becomes the
campaign's standing preregistration; no wave runs before the seal.**

## 0. The thesis and the license

The thesis (owner-endorsed, 2026-07-16): **the object carries grammar,
not values; values live in the coupling. Exhaust the grammar, spend ONE
measurement to calibrate the coupling, and the grammar must predict a
SECOND measurement for free.**

The license: the stopping rule closed raw SM-value comparisons (every
one null or dissolved on corrected inputs — B615/B615-R, B616,
B627/B628, B631). It reopens only via (i) an owner directive, (ii) the
typed functor (L91), (iii) the full sealing checklist. This document,
signed, is (i). Phase A's gate is (ii). Phase C's design embeds (iii).
**Phase C structurally cannot open without satisfying the stopping
rule.** Gate 5 stands throughout: no SM quantity enters CLAIMS.md; no
recycling of B631's numbers under new labels; no physics reading
before the functor exists.

## 1. Campaign conventions (the §13 block, campaign level)

- Every wave carries its own PREREGISTRATION with its own conventions
  block (basis/sign/normalization/stage choices declared) and its own
  ARTIFACT_HASHES.txt, sealed before the wave's first run.
- Seat roles for Phase C are fixed here, in advance: the seat that
  seals the design does not compute the prediction; the seat that
  computes the prediction does not execute the held-out comparison.
  Three-way separation (the B646 mutual-verification pattern).
- All verification internal (PROVENANCE.md §0); banking rituals per
  WORKING_RULES; every banked law adds its LAW_MAP row same-PR; review
  action items stay at zero debt per wave (the constitutional pilot
  will fire mid-campaign and must find the loop clean).
- Loop mechanics: self-scheduled waves (compute → gate → bank → route →
  next); independent cells run as parallel background arcs/subagents;
  heavy verification fans out to subagents with synthesis retained on
  the main seat; no wave may skip its gate to reach Phase C faster.

## 2. PHASE A — THE LICENSE (the typed functor + the mechanism closure)

**A1 (the spine): the typed classical→stage functor (L91-4 = L84).**
Build T: (object quantities) → (stage observable types) on the banked
bridge (dial-directions → H¹(T²; block) → Weil quantization; L85
outcome B). Deliverable: a TYPES document + machine-checkable typing
rules. Gates: T must type the banked coupling instances consistently
(the antiphase listener B584, the displaced ear B593, the three-layer
separation B611/B613, the reality law B626, the cubic dichotomy
B632/B637). **Typing rule from B646-Q2 (the core-coupling gate's
basis): abelian character dressing carries no object information —
only core-coupled quantities are typeable as observables.**

**A2: the chord mechanisms (R20-5/R20-6; B647 continues).** Cell 1
banked: swap+zero leave 6 real dims; the spectator law ⟺ one phase
condition (arg Y[134] = π/6). Cell 2: the chain-level swap action —
does it pin the phase? Cell 3: the 13-dial's mechanism (the lit-class
deviations). Exit: each chord row labeled FORCED (theorem) or CHOSEN
(with its orbit), or honestly MECHANISM-OPEN.

**A3: the coupling inventory completion (R20-4).** The fiber-paired
Mayer–Vietoris machine for D_conjθ — the last known coupling whose
dimension is open. Exit: the inventory of coupling invariants is
declared complete-as-known, with the fiber-paired row resolved or
bounded.

**A4: stage selection through the ladder laws (with the cc2 track,
L97).** The generic-silence law, the uniform jump law, the clock law,
and the splitting law now govern the tower; pose minimality precisely:
κ = 5 sits where the silence character, the Fibonacci core, and the
ear's modulus coincide (the disc(A₁) = 5 triple shadow). Obligations
(1)–(3) of L91 addressed here; partial NEEDS-SPECIALIST exits allowed
with the residue named.

**GATE A:** the functor exists and types the bank consistently.
**KILL K1:** no consistent typing exists → the campaign dies honestly;
bank as the TYPING WALL (would join LAW_MAP §E) — the strongest
negative available at this level.

## 3. PHASE B — THE GRAMMAR CLOSURE (the table and the count)

**B1: the coupling-grammar table.** One sealed document. Every
interaction invariant of the object-at-κ=5: the hearing laws (trace
law, tone law, Plancherel, congruence shadow, Galois ear), the chord
laws (cubic, core law, cross-ratio, swap structure, walls), the ladder
laws (silence, jumps, clocks, splitting). Columns: the invariant; the
ORGAN it lives in (ear / chord / ladder); FORCED (theorem + lock) or
CHOSEN (the enumerated Galois/scale orbit) or MECHANISM-OPEN; the
disc(A₁)-shadow membership. **The shadow quotient:** rows that are
manifestations of one arithmetic invariant are counted ONCE toward the
freedom.

**B2: the silver control (C4; needs the m136 exact E₆ holonomy build —
its own arc, parallelizable).** Every row labeled FORCED must
reproduce on the second object; failures relabel to stage-chosen.
**KILL K5 (partial):** if most FORCED rows demote, the object-forced
grammar thesis weakens — bank honestly, campaign pauses for re-scope.

**B3: the freedom count N.** The number of independent CHOSEN
constants after the shadow quotient, with the vacuity check both ways
(MB12): N must be finite and enumerable. **KILL K2:** a non-enumerable
dial (an invariant varying without an enumerable choice) → the
calibration is ill-posed; bank and stop. **BRANCH (not a kill): N = 0**
→ the grammar predicts with zero calibration — Phase C upgrades to the
direct-prediction variant C′ (strictly more falsifiable).

**GATE B:** the table sealed with N; every FORCED row theorem-backed;
every CHOSEN row's orbit enumerated.

## 4. PHASE C — THE PROBE (sealed, two-outcome, cross-seat)

**C1: channel selection (owner + seats, recorded).** A pair of
same-TYPE (per A1's functor), core-coupled, dimensionless observables.
Candidates in prior order: the JUNO forward row's sector (the one
experiment-side row the program carries); the PMNS pair done typed and
calibrated (explicitly NOT B631's raw comparison — new design, new
seal, B631's numbers quarantined). **THE SECTOR-COVERAGE GATE (from
the B646 erratum):** the calibration instance and the predicted
instance must not sit in a single arithmetic sector of the relevant
character (residue/non-residue, inert/split, as typed) — a design
whose instances span one sector is rejected as maskable BEFORE any
comparison runs.

**C2: the sealed design.** MB12 (operations AND criteria, vacuity
both ways) + MB13-results-in-doc + pipeline controls (positive /
power / calibration / seed / path) + INPUT_COMPLETENESS_LEDGER row +
the conventions block + the two new gates (sector coverage,
core-coupling) + a null model and power analysis. **KILL K6:** if the
null/power gates fail, no comparison runs (the B631 lesson: an
underpowered comparison is banked as no-test, not as a null).

**C3: cross-seat execution.** Design sealed by seat X; the N-constant
calibration and the second-observable prediction computed by seat Y
(hash-first: prediction sealed before the held-out target is opened);
the comparison executed by seat Z; adjudication on the main seat.

**C4: the two-outcome bank.** Outcome A: the grammar predicts the
second measurement — the program's first genuine physics prediction,
banked with every caveat the record demands. Outcome B: a clean miss —
**the falsification of the coupling thesis in its value form at this
level**, the program's most important negative, banked with equal
prominence. Either way the campaign succeeds as an experiment.

## 5. The loop schedule and seat assignments

| track | content | seat | mode |
|---|---|---|---|
| F (functor) | A1, A4 | this seat | the deep synthesis track; wave-per-obligation |
| M (mechanisms) | A2 (B647 cells), A3 | this seat + subagents | parallel background cells |
| S (silver) | the m136 E₆ build → B2 | this seat (build) + cc2 (verify) | parallelizable now |
| L (ladder) | L97 proof queue → A4 inputs | cc2 | their standing campaign; packets verified on receipt |
| W (web) | L95 on arrival | web seat → verified here | event-driven |

Waves are self-scheduled (loop mode); each wave banks completely
(FINDINGS, locks, three ledgers, LAW_MAP rows, atlas, mirror) before
the next launches. Standing cadence: mechanism/build waves in
parallel; the functor track is sequential (it is the bottleneck and
the point).

## 6. Kill-switches (consolidated)

- **K1** no typed functor → TYPING WALL banked; campaign ends.
- **K2** non-enumerable freedom → ill-posed; banked; campaign ends.
- **K5** the silver control demotes the table's core → pause + re-scope.
- **K4** no channel passes sector coverage → the obstruction banked;
  Phase C waits for a channel, does not force one.
- **K6** null/power gates fail → no comparison; banked as no-test.
- (N = 0 is a branch to C′, not a kill.)

## 7. Estimate and what this campaign is NOT

Phases A+B ≈ 8–13 sessions (most of the R20 queue is inside them);
Phase C ≈ 3–5 more. Roughly two to three weeks at current cadence,
with honest odds that K1/K2/K5 end it earlier — each of those is a
real result, not a loss.

NOT: another raw-value scan; not a reopening of Branch 3's closed
record; not a physics reading of any mathematics before the functor
types it; not a spectrum-shape hunt. The firewall blocks overclaims,
not the probe: this design's entire purpose is to make the one
comparison that the coupling thesis actually licenses, at full
strictness, once.

## 8. Sign-off

- Owner approval: ________ (on approval: hash this file, open the
  campaign row in CAMPAIGN_STATUS/OPEN_LEADS, launch tracks S and M
  immediately, F next wave).
