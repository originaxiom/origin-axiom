# B1138 — THE STRUCTURAL COMPLETION: the fork theorem, the exceptional installment ladder, the hypercharge spend, and the E₈ family triplet (cloud memos 11/14/13/15, verified two-bench)

**Status: banked (frontier). Verdict PROVED (four structural results; the object-forcing
fences held). Harvest arc — the cloud seat's phase-III structural memos 11 (FORK_THEOREM),
14 (AMBIENT_LADDER), 13 (Y_SELECTION), 15 (FAMILY_TRIPLET), golden_gate head 577712f. Each
re-derived on THIS bench with own code (own generic Chevalley builder with a flipped cocycle
convention; own W(E₆)/GF(2)/signature machinery), reusing only the banked Chevalley e₆
(B1102) + the 27 (B883). All CONFIRMED, no discrepancy. Cloud seat credited. Gate 5
untouched (Lie-algebra/group structure only; no SM value). Lock
`tests/test_b1138_structural_completion.py`.**

## Why one arc

These four memos are one story — what the closings' real-form taxonomy (B1134/B1135)
*costs and completes*: the fork (why the closings are forked, not one act), the exceptional
series' installment plan, the hypercharge spend, and where the generation count would live.
Banked together (integrate-don't-merge), one lock.

## 1. THE FORK THEOREM (memo 11) — CONFIRMED

On e₆, after the Lorentz double (two A₂ triples) + color (a third A₂): the centralizer
ladder **dim z(T₁)=16, z(T₁∪color)=8, z(T₁,T₂)=8, z(T₁,T₂∪color)=0** (reproduced under two
independent adjacent-simple-pair slot choices). The **joint centralizer = 0** is the fork:
**you get any TWO of {spacetime, color, hypercharge}, never three.** The three A₂ slots form
a full **S₃ frame torsor** — all 6 permutations, each realized by exactly 216 = 6³ = |W(A₂)|³
Weyl elements (|W(E₆)| = 51840, verified). **This is the theorem that grounds the B1134/B1135
correction: the observer's real-structure closing is FORKED (spacetime E₆(−26) XOR gauge
E₆(−14)), not one conjugation for the whole bill** (see the SM_VERDICT fork-fix, this window).

## 2. THE INSTALLMENT LADDER (memo 14) — CONFIRMED

The room left after buying Lorentz + color, up the exceptional series (own generic Chevalley
builder; root/dim counts externally validated 72/78, 126/133, 240/248; full Jacobi checked):
> **E₆ → room 0** (control: 16/8/0, matches the banked fork). **E₇ → room exactly 1** — one
> **pure-Cartan u(1)**, verified to commute with all 14 spent generators (35/9/1): *E₇ affords
> hypercharge, nothing to spare.* **E₈ → room exactly 8** — a fourth orthogonal A₂'s sl₃
> (mod-p sandwich [two primes agreeing] meeting an explicit commuting rank-8 exhibit): *E₈
> affords the whole EW slot; four A₂ slots stand orthogonal.*
The exceptional series sells the SM one layer per step: 0 → 1 → 8. **FENCE:** this is the
PRICE STRUCTURE of the ambient series — it does NOT claim the object's geometry reaches
E₇/E₈ (memo 14's own fence; the two ends E₆/ℚ(√−3) and E₈/ℚ(√5) are curvature-related, not
nested).

## 3. THE HYPERCHARGE SPEND (memo 13, = G-1) — CONFIRMED

The gauge closing spends the last hypercharge bit itself. Of the 128 factor-preserving
involutions, **16 (all in the δW coset) admit a gauge-row lift** ((0,8),(4,4),(4,4)); the 18
hypercharge directions (recomputed, matching B1102's exact fractional multiset) split **9+9**
across the two W-orbits; **no Y is ever kept split** (fixed=0 across all 16); the anti-set
sizes give the **9/6/1 hierarchy**; the **9 generic pairs straddle both orbits (9/9) and
partition all 18**. Cross-bench: the (0,8)/(4,4)/(4,4) row = 81 pairs, exact match to B1135's
banked menu. **Self-correction carried:** literal P-mirror closure holds for only 3/9 pairs —
the real invariant is orbit-straddling (the cloud seat's own retraction, confirmed). After the
closing the freedom ledger holds the spin lift alone (B1122's pair-invisible bit).

## 4. THE E₈ FAMILY TRIPLET (memo 15) — CONFIRMED (algebra fact; fence held)

On an **independently-built** correct E₈ (240 roots, dim 248, highest root the textbook marks
(2,3,4,6,5,4,3,2)): each of the 4 orthogonal A₂ slots has complement E₆ (72 roots) and crossing
projecting to exactly **(3,27) ⊕ (3̄,27̄)** — the sum-to-zero weight triplet and its antitriplet
verified literally, **248 = (8,1)⊕(1,78)⊕(3,27)⊕(3̄,27̄)**. The **27 enters three times**,
indexed by the family A₂. E₆'s 27 = 16⊕10⊕1 under D₅×u(1), charge multiset {4:1, −2:10, 1:16}
— exact match to B883's independently-derived (E₇-cubic route) multiplicities. **FENCE (held
explicitly):** this is the CITED-standard ALGEBRA fact (E₈ ⊃ E₆×SU(3); Slansky/Bars–Günaydin).
It says nothing about whether the OBJECT reaches/forces E₈ — that remains **EXHIBITS-NOT-FORCES**
(B1033: the object-forcing BLOCKS; the two ends are curvature-related, not nested). "The 27
enters 3×" is NOT "the object forces 3 generations."

## Verification + a cert-quality note

All four re-derived independently, own code, no discrepancy (details: the fork ladder + torsor;
the 0/1/8 ladder with E₇ pure-Cartan + E₈ sandwich; the Y 9+9/9-6-1/partition + the 3/9
self-correction; the family triplet on a correct E₈). **Cert note (relayed to cloud seat):**
`family_triplet.py`'s transcribed E₈ Cartan matrix is asymmetric (invalid simply-laced) — the
verifier built correct E₈ data independently and the RESULT stands; the cloud cert should be
corrected (result unaffected).

Cloud seat credited. Gate 5 untouched.
