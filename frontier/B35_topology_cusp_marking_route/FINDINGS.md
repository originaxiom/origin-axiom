# B35 -- Findings

> Logged observation, not a claim.

## Result

The half-step action on central signs has order 3 over `F_2`, matching the
projective period-3 behavior:

```text
(sa,sb) -> (sa*sb, sa)
```

The nonzero sign characters form a 3-cycle. This supports the topology/lift
interpretation of the B26 sign flip.

## Control

The sign-character cycle contains no invariant-surface parameter. It does not
select `I=1/4` or `lambda/h=1`.

## Verdict

**`STALLED`**

Topology/lift-sign data explains the order-3 projective behavior, but it does
not derive the selector.
