# B555 — THE PREDICTION: the object's one falsifiable forward statement

*Everything in the reframe program was preparation for this. The object cannot
predict SM values (proven null three ways: B533 Gate 3, B539, B541). It makes
exactly ONE concrete, buildable, falsifiable forward prediction — this one.*

## What the object predicts

**A four-coupling species-chain Hamiltonian has integrated-density-of-states
gap labels at the degree-4 ℚ(√φ) dictionary values — and NOT on the golden
lattice ℤ+ℤ/φ.**

### The exact predicted labels
| label | value | one-measurement law |
|---|---|---|
| f_b | 0.1681173890 | = f_a/φ |
| f_B | 0.2138486222 | = f_A/φ = τ·f_b |
| f_a | 0.2720196495 | = τ−1 (the anchor) |
| f_A | 0.3460143392 | = τ·f_a |
| f_a+f_b | 0.4401370385 | = τ²·f_a = 1/(1+τ) |

**The one-measurement law (the reframe's centerpiece, made physical):**
measure ANY single label x, and every other is FORCED — f_A = τ·f_a,
f_a+f_b = τ²·f_a, exactly, to full precision. Verified internally exact
(f_A/f_a = τ, (f_a+f_b)/f_a = τ² to 1e-12).

### The degree-4 signature (the discriminator)
Every label sits **2–3.6 ×10⁻²** off the golden lattice ℤ+ℤ/φ — a generic
golden (2-letter) chain would give exactly 0. This gap IS the object's
fingerprint: it is the first physical measurement that would read the ℚ(√φ)
(τ, "odd-beat") layer, not just the ℚ(φ) (φ, "even-beat") shadow.

### Controls (object-specificity)
- silver-mean chain → labels in ℤ+ℤ/(1+√2) (e.g. 0.4142), disjoint from the
  dictionary;
- a generic slope-0.44014 chain → degree-4 labels too, but in ℤ+ℤ(τ³−τ²) —
  fails CONTROLLED (the module is object-specific = the species LANGUAGE,
  B551: reachable only by the inflation structure, not any winding).

### Experimental requirement
Resolution 1/N must beat the ~2×10⁻² separation: N ≳ 1500 sites resolves it;
N = 24,001 gives 22–59× margins (B543 hardening, falsifier silent). Platform:
a four-coupling polariton/photonic Fibonacci-type chain (2-coupling versions
built and measured — Tanese 2014, Baboux 2017).

### Falsifiers (preregistered — the prediction can LOSE)
(a) labels drift to ℤ+ℤ/φ as N grows → object wrong (degree 2, not 4);
(b) labels coupling-dependent → not gap labels (violates gap-labeling theorem);
(c) labels match silver/tribonacci → wrong metallic member;
(d) f_A ≠ τ·f_a → not this object.

## What we still need (honest)

1. **The tiling-literature gate** (one search): the substitution-tiling
   K-theory corpus (Kellendonk–Savinien, Julien, BHZ 2000) was not searched
   by the lit-gate. It must clear before the paper claims "first degree-4
   gap-label measurement." Queued on the cost-tiered deep-research script.
2. **An experimental partner / realization.** External — a polariton or
   photonic group with four-coupling patterning. Not ours to compute; the
   prediction table above is what we hand them.
3. Nothing else is computationally blocking. The prediction is complete,
   exact, and internally locked.

## What we CANNOT predict (stated so the prediction isn't over-read)

SM coupling constants, mass ratios, mixing angles — proven null (B533/B539/
B541). Absolute scale (B532: ABSENT). Anything at mid-RG-flow / non-fixed-point
(S066). The object speaks only at fixed points and in built inflation
structures; this prediction is the one place both conditions are met and a
lab can be built.

Locks: tests/test_b555.py.
