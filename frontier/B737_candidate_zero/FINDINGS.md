# FINDINGS — B737: CANDIDATE ZERO — the voice carries ζ_K whole, but it is the FIELD's voice: the composition dies at the crux (OUTCOME B)

cc banking seat, 2026-07-21. Prereg sealed `9fd4ed33`. The pathfinder's hunt-cell #1 (wf_9ad8d5a2:
4 probes × compute → 3-skeptic adversarial verify → refine; 20 agents; P4 refuted round 1 and
recomputed — the loop worked). All load-bearing facts re-verified in-seat (20–40 digits). Pure
math; Gate 5; nothing to CLAIMS. Global look-elsewhere ledger: cell #1 CLOSED at width 1.

## VERDICT — OUTCOME B AT THE CRUX, with two genuine new positives and two open leads

### P1 — SCATTER (A, 0/3): the Bianchi-class scattering carries the completed ζ_K, verified at source
φ(s) = Λ_K(s−1)/Λ_K(s), Λ_K(s) = (√3/2π)^s Γ(s) ζ_K(s), ζ_K = ζ·L(·,χ₋₃) — Sarnak (Acta 151, READ)
assumes D≠1,3; the D=3 (w=6) case was RE-DERIVED in-sandbox (the order-3 cusp rotation handled; 69/69
checks). The pole structure is genuinely in the scattering: the residue chain passes literally
through **Res_{s=1}ζ_K = 2π/(6√3) = 0.6045997880… — the exact B736-P2 SSB number** — and closes
geometrically: Humbert vol(m004)/vol(orbifold) = 12 (quad-double).

### P2 — COVER (A, 0/3): the OBJECT'S OWN voice carries ζ_K whole — a new clean identity
The cover is non-normal (|Isom(m004)|=8<12), so the standard Venkov–Zograf transfer doesn't apply;
a **one-cusp exact-transfer lemma** was proved directly (one cusp over one cusp ⇒ the pullback
Eisenstein difference is L² with negative eigenvalue ⇒ vanishes): **φ_m004(s) = φ_orbifold(s)
identically**, giving the closed identity **Res φ = 2√3 / vol(m004) = 1.70655217662816…** (verified
40 digits, three routes + in-seat). Closing (Dehn filling) removes the cusp ⇒ destroys exactly this.

### P3 — SISTER (A, 0/3): the voice is NOT fully generic — the first object-specific spectral data
Two object-specific data enter the voice (in-seat verified):
- **The cusp shapes are NON-similar tori of equal covolume 2√3:** m003's cusp is the hexagonal
  ℂ/O_K (j=0 exactly, maximal order); **m004's cusp is ℂ/(ℤ+2√−3ℤ) with CM by the CONDUCTOR-4
  order (disc −48, h(−48)=2)** — j = the larger root of x²−2835810000x+6549518250000 (verified:
  mp.kleinj(2√3·i)·1728 matches to 1e-15). [Converges with cc2's independent observer-build finding
  that m004 sits at the conductor-4 suborder — two routes, same datum.]
- **The congruence level changes the available Hecke-character palette:** |(O/2^k)^×/μ₆| = 1, 2, 8
  at levels (2),(4),(8) (in-seat) — at m003's level only ζ_K can appear; m004's level admits
  nontrivial conductor-(4)/(8) Hecke L-functions. [Whether they occur with NONZERO coefficient in
  m004's actual scattering is open lead L2.]

### P4 — KMS (B, 0/3 after round-2 recomputation): the crux DIES — same ζ, different role
Round 1's A was refuted by the skeptics; round 2 proved **B** with two discriminators:
1. **MB12 vacuity:** the whole "same substrate" battery (one ideal monoid, shared pole, θ/Mellin,
   Sarnak-form) passes IDENTICALLY for all four smallest h=1 imaginary-quadratic fields
   **with no manifold present** — zero discriminating power.
2. **Object-deletion:** the CMR/BC KMS system contains NO parameter of m004 (verbatim source
   re-fetched); the flow weight is identical for O_K and m004's cusp lattice; the naive operator
   identification is computed FALSE (disjoint spectra).
**The ζ_K in the voice is the FIELD's; the object re-broadcasts it in the spectral-parameter role.**
The composition (voice = the observer's SSB carrier) dies honestly. The kill-form (genericity /
field-not-object) + faces (emittance × infinite-tower) enter the kill graph; the lattice re-ranks.

## What survives (banked as structure, no physics claim)
(i) φ_m004 = Λ_K(s)/Λ_K(s+1) with Res φ = 2√3/vol(m004) — the object's voice as an exact
zeta-quotient, a clean theorem-grade identity; (ii) the object-specific voice data (the disc-−48
conductor-4 cusp CM shape; the level-(4)/(8) palette) — the FIRST computed non-generic spectral
data in the program (partially answering B735's "everything spectral is generic"); (iii) open leads
**L1** (an Eisenstein→BC functor — an open construction problem of explicit-formula class, not
claimed) and **L2** (do m004's conductor-(4)/(8) characters appear with nonzero coefficients?).

Two-seat context: cc2's REVISIT REGISTER (209 negatives; ~130 overreach-class; 3 cracks) landed in
parallel — its cracks are folded into the shortlist and RETRACTIONS this same pass. Credit: the
pathfinder mechanism (the cell was its top-ranked composition); the skeptics (killing round-1's A).
