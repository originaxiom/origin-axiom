# B782 — C22, THE CHOICE-INCOMPUTABILITY WALL (the capstone Phase-4 wall)

*2026-07-24. The wall articulated across the session, now a verified group-theoretic
no-go and chain link C22. Phase 4. Gate 5-Q: the chain statement is the math only; the
observer/Born interpretation is priced to C18, firewalled. RESOLVED-A.*

## The theorem (verified, exhaustive)
The measurement torsor (C20/B766) is a **free transitive (ℤ/2)³-action** on its 8
closings — verified by exhaustive fixed-point check: no non-identity element of
(c, θ, γ₅) fixes any closing; the action is transitive (a torsor); the set of G-fixed
closings is empty. **A free action has no G-fixed point, so the torsor admits no
equivariant section.**

## The no-go
An equivariant (symmetry-respecting) selection of a closing would be a G-fixed point of
the closing action. There is none. Therefore **no process internal to the object's
symmetry group computes the choice of closing** — the selection must be broken from
OUTSIDE the object's symmetry. This is the exact group-theoretic form of "computation is
equivariant; choice is symmetry-breaking."

## Why it is the capstone wall
It closes the program's central structural question — *why the observer, and why can't the
object choose for itself?* — as a theorem: the object's closing set is a torsor, and a
torsor has no canonical point. The observer is not adding information the object lacks;
the observer is the symmetry-breaking the object structurally cannot perform on itself.

## Firewall (Gate 5-Q, binding)
The chain link C22 asserts ONLY: *a free torsor has no equivariant section*. The reading —
the observer completes the object from outside; the Born shape is amplitudes (the
equivariant, computable part of the wavefunction) versus outcomes (the section, not
computable); the type-vs-token distinction of the observer — is the **priced C18 bridge**,
kept in this arc and NOT in the chain statement, NOT in CLAIMS. No consciousness claim.

## The chain now
C1–C22, every link a theorem/census/identity/no-go/priced-axiom. C22 is the terminal
wall: the object's grammar is forced (C1–C17), its measurement is a rank-3 torsor with
a discrete T1 frame (C18–C21), and the selection within that torsor is provably not
computable from inside (C22). The map is closed at the boundary the program was built to
find. Lock: tests/test_b782_c22.py + the C1–C22 chain lock.
