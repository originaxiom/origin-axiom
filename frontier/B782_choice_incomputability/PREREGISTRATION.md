# B782 PREREGISTRATION — C22, THE CHOICE-INCOMPUTABILITY WALL (Phase 4)

*Sealed before bank. The capstone Phase-4 wall, articulated across the session as the
candidate C22. This BUILDS it as a verified group-theoretic no-go from the measurement
torsor (C20/B766). The chain statement is STRICTLY the verified math; the observer/choice/
Born interpretation is the PRICED C18 bridge, firewalled, not asserted here. Gate 5-Q.*

## The claim (two-outcome)
The closing set (C20) is the 8-element measurement torsor under (Z/2)³ = (c,θ,γ₅).
TEST: is the action FREE (no non-identity element fixes any closing)?
- FREE ⟹ no G-fixed point ⟹ no equivariant section selects a closing ⟹ **no
  symmetry-respecting (equivariant) process internal to the object computes the choice**
  — the choice must be broken from outside. C22 [NO-GO] minted. RESOLVED-A.
- NOT free (some element fixes a closing) ⟹ an equivariant selection could exist ⟹ the
  wall fails. RESOLVED-B.

## Firewall (Gate 5-Q)
The chain link asserts ONLY the math no-go (a free torsor has no equivariant section).
The reading "the observer completes the object from outside; the choice is
symmetry-breaking not computation; the Born shape = amplitudes(equivariant, computable)
vs outcomes(the section, not)" is the PRICED C18 interpretation, kept in the arc, NOT in
the chain statement and NOT in CLAIMS.

## Method: exact group computation (the (Z/2)³ regular action, freeness verified by
exhaustive fixed-point check). No forced result.
