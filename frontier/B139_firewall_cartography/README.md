# B139 — "SM through multiplicity": firewall cartography (NOT a crossing)

Three Chat-2 informatory calcs decomposing *"can multiplicity produce the Standard Model?"*, banked as
**cartography of the firewall** — firewall-CONFIRMING, not a result, not a physics crossing.

**The sharper firewall statement.** Multiplicity permits MORE structure (more abelian couplings = cusp count, a
discrete trace-fork B131, chiral objects = CS-sign) but cannot permit the two SM-distinguishing structures: a
**simple non-abelian factor** (contingently blocked; Gate-1/`S033`) and **irreducible chirality** (structurally
blocked). Banked as a framing note in `speculations/S029`.

**Item 1 — chirality articulation (verified).** The mirror (`swap_{R↔L} ∘ reverse`) sends the monodromy `M → Mᵀ`
(since `L = Rᵀ`), so it preserves trace / char poly / Perron field / volume and only flips the CS sign — chirality
is a **CS-sign, not an inequivalence**. The SM-side face of `det = −1 → CS = 0` (`../../philosophy/P009`). Verified
for six chiral words (and universally for achiral words too); SnapPy: vol invariant, CS flips, `H₁` invariant.
**Load-bearing caveat:** "structurally blocked" = blocked at all *standard* invariants, **not** a proof no invariant
distinguishes.

**Item 2 — MB9** (`../../REPRODUCIBILITY.md`): a non-abelian symmetry **group** ≠ non-abelian **gauge** content
(the firewall is on the abelian × discrete trace-ring / `T[M]`, not the monodromy group).

**Item 3 — open lead B139-G** (`../../docs/OPEN_LEADS.md`): does the chirality block survive the **genus ladder**?
(genus-1 special; the falsifier for Item 1; trace-level, unrun).

```
python frontier/B139_firewall_cartography/probe.py
python -m pytest tests/test_b139_firewall_cartography.py -q
```

**Tier.** MATH (cartography). Nothing to `CLAIMS.md`; P1–P16, B85, S031, B124–B138 untouched. See `FINDINGS.md`;
ledger **V128**.
