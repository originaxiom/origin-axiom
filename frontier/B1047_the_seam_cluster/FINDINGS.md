# B1047 — the seam cluster dispositioned from the bodies: the headline is refuted, the mechanism survives

**Status: banked (frontier). The campaign's step 6 executed on the largest candidate cluster of the
B300–B499 band. Consolidation only — no new mathematics is asserted, and everything the cluster
touches stays theta-lift sector, pending L57, firewalled, nothing to `CLAIMS.md`. Gate 5 untouched;
zero anchors.**

## Why this cluster, and why now

**B1045 mapped B300–B499 and refused to disposition it.** Its map is keyword-seeded from
`claim_one_line`, and campaign **step 1** forbids exactly that as a basis — *"read the bodies, not
the claim lines"*. B1045 published its own error rate (**5 of 58, 9 %**) and left the map as *"the
next pass's starting point"*.

This arc reads the bodies of its largest cluster — **the seam / level-15 (√−15) campaign, 14 rows**
— and dispositions five of them.

---

## THE DOMINANT FINDING IS NOT A LAW

> ### This cluster's claim lines **systematically overstate their own bodies**, in the one direction a consolidation pass cannot survive: they promote an **observed pattern** to a **law**.

| arc | its `claim_one_line` | its **own body**, same file |
|---|---|---|
| **B359** | *"the theta-lift seam form is pair-specific and **parity-selective**"* | *"A parity selection rule (**OBSERVED PATTERN, not claimed as law**) — **3 data points**, one declared prediction. **Three points do not make a law — the prediction is the test.**"* |
| **B361** | *"Across **8 pairs with zero counterexamples** … carries √−15 exactly when it contains a seed elliptic at both 3 and 5"* | *"stated as **a law of the computed range, not proved**"* |
| **B362** | *"extending the doubly-elliptic-seed brightness law to **11 pairs** with zero counterexamples"* | true as stated — but the law it extends is the one **B367 kills on the twelfth pair** |

**The bodies were honest at the site, every time. The claim lines dropped the tier.** B359 even
*pre-registered the test that killed it* — and **B360, the very next arc, ran it and refuted both
readings** (verdict NEGATIVE). The pattern-tier discipline worked exactly as designed **inside the
arcs**; what failed is the one-line summary a consolidation pass reads.

**That is step 1's argument made concrete a second time**, and it is why a claim-line ledger cannot
see this band.

---

## The dispositions

### DECLINE — RETRACTION, NOT RESTORATION: B359 · B361 · B362

**The B123 pattern, three more instances.** **B367**: *"the local law (B361) is **REFUTED** at pair
(3,4) … the banked selection rule **fails on the twelfth pair**"*, and its `arc_verdict` declares
`supersedes: B361`.

**Re-verified here from integers rather than cited** (campaign step 5). The seed `A_m` has char poly
`x² − (m²+2)x + 1`, `disc = m⁴ + 4m²`; it is **elliptic at `p` ⟺ `disc` is a quadratic non-residue
mod `p`**. Qualifiers below 9: **m = 2, 7, 8**.

- **(3,4) contains no qualifier** — m=3 is 3-trivial, m=4 is 5-parabolic — **yet B367's exact table
  gives it `Σs² = 1/192`, the second-largest aggregate in the corpus.** Bright.
- **The minimal repair dies in the same table.** *"Each prime covered by some seed of the pair"*:
  **(1,3) has the identical covering pattern** (3 covered by m=1, 5 covered by m=3) and is
  **exactly dark**, `Σs² = 0`.
- **And `W₁`, `W₄` share the level-15 order 20** — B367's ground for *"the support pattern is
  strictly finer than spectra + local ellipticity types"*.

**m = 8 is not an error in B361.** B361 scoped its list to `m ≤ 7` **and named the next qualifier
itself** — *"the first doubly-elliptic seed beyond 7 mod 15-classes"* — as its own next
discriminator. Checked before it could be reported as a defect.

> **What the retraction does NOT take: the DATA.** B367's own words — *"its **11 confirming pairs
> stand as data**"* — and B359's tables were **superseded by a better table** (B367's full exact
> identification), not refuted. **The declined rows lose their headline, not their arithmetic.**

### RESTORE — B393 · B410, as their OWN `LAW_MAP` row

> **`s`-darkness ⟺ the 5-side never donates `√5` to an imaginary product.**

Every convolution term `s(Π_H(X₃·X₅))` of a dark pair vanishes **individually** — **termwise mutual
annihilation, not cancellation**. And the statement is about **which field each product lands in**,
not about products vanishing: **`zero = 0` in every one of the five pairs**. The dark class itself
**stratifies** — (1,4) and (3,5) fully real (39/39, 15/15), (1,3) `√5`-free in its imaginary part
(15 real + 24 z-only) — while the bright controls put **exactly the complementary stratum** into
s-carrying (24 and 18). **B410 re-derived the criterion independently on the same object and it
separates 4/4.**

**This is what is left standing after its own explanation died:** B393's K2 — the Galois
eigen-pattern of X₃ — was **KILLED**; dark and bright spectra have identical Galois structure.

**Re-verified before restoring, not from memory.** `product_fields.py` was re-run end-to-end against
the live tree — **2m41s, pure `Fraction`, no external CAS** — and its output is **byte-identical to
the banked artifact** (`git status` clean after the run). That is what *"EXACT tier, regenerable
end-to-end"* is supposed to mean, checked rather than quoted.

**NOT folded onto B1029's `THE SEAM IS THE ENDS' CLASS FIELD`**, which an earlier draft of this
disposition assumed. Tested: **B393 and B410 mention class field / Hilbert / HCF / B334 exactly
zero times**, and B1029's row mentions neither theta nor darkness. **They share the field `√−15` as
vocabulary, not as statement** — the 9 % error B1045 measured, nearly repeated by the arc reading
B1045's map.

### Four riders travel, or the restoration is an overclaim

1. **COMPUTED RANGE** — five pairs in B393, four in B410's criterion. **Not proved.**
2. **THE PER-SIDE `Π_H` ERRATUM, four appearances.** The criterion **cannot** be stated as a
   per-side H-projection. **`k1_termwise.json` is a NEGATIVE ARTIFACT and must never be read as a
   result:** it reports `nonzero_terms = 0` on the **bright** controls too, so a reader taking it
   for data would conclude every pair is dark. It even carries different term counts (**9 against
   39**) — the two files are not two readings of one computation; one of them is a different object.
3. **THE RESIDUAL IS OPEN** — *why* the dark 5-side withholds `√5` at exactly the X₃-paired
   frequencies. Staged with its exact obstruction, and explicitly **not** a per-side statement.
4. **SCOPE** — theta-lift sector, **pending L57**, firewalled, nothing to `CLAIMS.md`.

**And the chain is carried, because B393 is not self-contained:** `B358/cyclo_engine.py`,
`B358/seam_certification.py`, `B367/step0_exact_matrices.py`, `B386/tensor_gate.py`.

---

## Three self-corrections, each caught by a check rather than by reading

**1. The plan's explanation of B410's missing generator was wrong.** It said the numbers survive
*"only because they are **byte-identical** to B393's `k1_fullfield.json`"*. **They are not** — 188
bytes against 485, different schema, different hash. `b2ii_fullfield.json` is a **derived summary**
whose four counts equal `k1_fullfield`'s four `nonzero_terms`, so the numbers are recoverable **by
re-derivation, not by finding a copy**. Right conclusion, wrong reason — and the wrong reason is
the kind that reaches a curated surface if nobody hashes the two files.

**2. A sentence about to be written into L165 was false.** The draft read *"none of the 15 sits
under a `CLAIMS.md` promotion."* **Three of the eleven arcs do**, and the bare-id grep missed them
because **`CLAIMS.md` cites its evidence by PATH** — the exact defect `DEBT_LEDGER`'s own Correction
2 records costing 49 arcs. **The three are three different things**, which is why L165 dispositions
them per-arc rather than counting them:

- **B156 (P39)** — the absent script is *"the handoff's own brute-force script"* that B156 itself
  says **over-counts and is wrong**; B156 replaced it with **six scripts that are all present**.
  **Not a defect.**
- **B877 (P69)** — the arc's §4.1 is titled *"Manifest gap"* and **discloses the absence itself**,
  with its reason for being non-blocking. **Nothing new.**
- **B379 (P60)** — **the real one.** `reduction_verification.py` is named as *the* `Reproducer:`,
  and **`frontier/B379_selection_rule_reduction/` contains no `.py` at all**. **P60's evidence path
  points at that directory.** Stated exactly, because the temptation is to overclaim it: **P60 is
  not unverified** — `tests/test_b379_selection_rule.py` recomputes the two closed-form traces from
  scratch through the B358/B367 engines, and it passes. **What is gone is the arc's own
  computation. Not a false claim — a false impression of provenance.**

**3. This arc's own check went red on this arc's own prose.** The first draft located B1029's
`LAW_MAP` row by matching its headline — and then **this** row quoted that headline while
explaining why it does *not* fold onto it, so the match returned two rows. Re-anchored on the arc
id. **E37 in its cheapest form: writing the prose broke the measurement of the prose.**

---

## A correction to the arc one before this one

**`SUPERSESSIONS.md` published *"72 candidates total; 21 are load-bearing … the remaining 51"*
while its own instrument reported 26 on the same tree**, one paragraph below a table that already
had **25 rows**. **B1046's locks pinned the dispositions and not the counts, so nothing caught it**
— the next arc's sweep did, which is the same way B1039's B564 miss surfaced. Corrected, and the
count is now **locked against the instrument** rather than typed.

**And the number moved again in this same commit, correctly.** Naming **B361** on `LAW_MAP` put a
superseded arc on a curated surface, the gate fired, and its registry row is the judgement the gate
demanded — **27 load-bearing over 26 arcs, backlog 45**. *Consolidating a cluster is exactly the act
that makes its superseded members load-bearing.* That is the instrument working, not drift.

---

## The instrument built two arcs ago could not see this cluster

**`law-siblings` (B1044) carried four fingerprints, and the first cluster it was pointed at
independently belonged to a `LAW_MAP` row that had none.** B1029's **THE SEAM IS THE ENDS' CLASS
FIELD** was on the curated surface the whole time and the sweeper was blind to it.

**Fingerprint added. It immediately surfaced three real siblings, all dispositioned:**

- **B449 — SAME-LAW, OWED, and it is the row's foundation.** It kills the disc×disc reading as
  *category-confused* and **restores `ℚ(√−15)` to FORCED status**. B1029's row asks what the seam
  field's class field is; **B449 is the arc that says the seam field is forced at all.**
- **B427 — SAME-LAW, OWED.** The slot exchange is the Galois element **σ₁₇, which fixes `√−15`** —
  and its **corollary correction must travel** (`tr(Q³A) = tr(QA)` is *false* on the actual
  matrices: `C = ζ₁₅`, `C′ = ζ₁₅²`).
- **B459 — SAME-LAW, OWED, with a self-correction that must travel.** The dual-torus patterns are
  the **five-subfield lattice of `ℚ(√5, √−3)`** — B1029's own compositum — but its ADDENDUM
  withdraws the object-level reading: *"the selection structure is the **QR-class's at level 15,
  not the object's**"*, an **E34** shape.

**And one false positive, kept rather than narrowed away.** B876 matched on **`annihilat`** — a
**Lie-algebra annihilator**, not termwise annihilation. **This is the dual of the limitation the
registry already records:** B485 was *one law in two vocabularies* (missed); B876 is *two laws in
one vocabulary* (falsely matched). **The token stays and the judgement is recorded**, because
narrowing on the first false positive is how a threshold erodes (**E38**).

**Coverage, published rather than implied: 6 fingerprints against 147 five-column `LAW_MAP` rows —
≈ 4 %.** The proposal is the defensible middle, **not** all 147: fingerprint the **campaign-fed
rows** (≥ 5 arcs cited — 55 at this arc, 58 after B1048's three), where a law demonstrably spans arcs and so can span bands. **The real cost
is the body reads each fingerprint generates, not the regex** — one cluster at a time, as here.
**A green `law-siblings` does not mean the corpus has no cross-band siblings; it means none among
the six laws that have fingerprints.**

## The band's arithmetic, dated and scoped

**14 rows** mapped to this cluster at B1045; **5 dispositioned here** (B393, B410 restored; B359,
B361, B362 declined). **9 remain, and they are not declined by silence** — B363 · B402 · B426 ·
B427 · B431 · B449 · B459 · B474 · B478 are **owed, un-read**, with **B408's trap** sitting inside
the same campaign (`SUPERSESSIONS.md` §B). **Its headline — since retracted and given a correction
banner at B1048 — asserted *"the one scale lever stands"* over a body that kills it.** Measured
with **B1047 and later excluded** (E37).

**The corpus figure, set-differenced rather than subtracted: 204 → 199** uncited substantive PROVED
arcs (50 uncited instrument; 634 PROVED in all). **The five retired are exactly the five
dispositioned** — `B359 · B361 · B362 · B393 · B410`, nothing else moved.

> **And two of those five were DECLINED, not restored.** Naming an arc on a curated surface *as
> refuted* retires it from this count, because the sweep asks **"is it cited?"**, not **"is it
> endorsed?"**. That is the right answer for campaign step 6 — *"restored **or explicitly declined
> with a reason**"* — but it means **the raw debt number cannot tell a restoration from a
> retraction**, and a reader watching only the number would see three refuted arcs read as
> consolidated.

**Crossing 200 broke a lock this refresh did not write.** `tests/test_consolidation_coverage.py`
asserted the absolute band `200 < sub < 280`. **`E38` (progress-eroded threshold), second
instance — a lock that fails because the work succeeded** — and the sharper one, because unlike
B1033's, nothing in its own prose had anticipated it. Repaired as B1033's was: the lower bound is
now the **share** (0.10–0.45, currently **0.314**); the upper stays a raw guard, because *that*
direction would mean the citation regex stopped matching. **The general rule is now in
`ERROR_LEDGER`: any lock whose number the programme is trying to move is an E38 waiting for the
programme to work.**

**Provenance.** `verify.py` (79 checks) · `tests/test_b1047_seam_cluster.py` (12 locks) ·
bodies read: B359 B360 B361 B362 B367 B393 B410 (+ B156 B379 B877 for L165) ·
`DEBT_LEDGER` §B300–B499 · `SUPERSESSIONS.md` (B361's row, and B1046's count corrected) ·
`OPEN_LEADS.md` **L165**.
