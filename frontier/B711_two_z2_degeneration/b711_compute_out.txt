==============================================================================
PART 1 -- X(4_1) as a plane curve; the geometric point
==============================================================================
Riley polynomial phi(m,u) numerator : -m**4*u - m**4 + m**2*u**2 + 3*m**2*u + 3*m**2 - u - 1
phi/m^2 in (x,u)                    : u**2 - u*x**2 + 5*u - x**2 + 5 = 0
CHARACTER-VARIETY CURVE  C :  -x**2*y + x**2 + y**2 + y - 1 = 0
   i.e.  y^2 - (x^2-1) y + (x^2-1) = 0,   x = tr(meridian), y = tr(a b^-1)

Alexander polynomial (Fox calculus): -t**2 + t + 1  (= -(t^2-3t+1) -> knot is 4_1)

Geometric point (parabolic meridian x=2):  y = [3/2 - sqrt(3)*I/2, 3/2 + sqrt(3)*I/2]
   trace field = Q(sqrt(-3))  [Eisenstein];  y = (3 +/- sqrt(-3))/2
discriminant_y(C) = (x - 1)*(x + 1)*(x**2 - 5)   -> real branch points at x^2 = 1 and x^2 = 5

explicit geometric rep: m=1 (parabolic), u = omega = -1/2 + sqrt(3)*I/2
   relator a w - w b = [[0, 0], [0, 0]]  (zero => valid rep)
   tr a = tr b = 2.00000000000000 (parabolic);  tr(a b^-1) = 1.5 + 0.5*sqrt(3)*I
   tr[a,b] = 1.5 - 0.5*sqrt(3)*I (!= 2  => IRREDUCIBLE)

SnapPy 4_1 cross-check:
   tetrahedron shape = 0.50000000000 + 0.86602540378*I  (= e^{i pi/3}, root of z^2-z+1 => Q(sqrt-3))
   Chern-Simons = -5.6244555 E-17  (= 0)
   symmetry group = D4  is_amphicheiral = True  is_full = True

==============================================================================
PART 2 -- involutions of C over Q; identify amphichirality
==============================================================================
j1 : (x,y) -> (-x, y)            preserves C: True;  involution: (x,y)
j2 : (x,y) -> (x, x^2-1-y)       preserves C: True;  involution: (x,y)
j3 : (x,y) -> (-x, x^2-1-y)      preserves C: True;  involution: (x,y)

rho_geom     p  = (2, 3/2 + sqrt(3)*I/2)
rho_geom-bar pb = (2, 3/2 - sqrt(3)*I/2)   ( = complex conjugate; sqrt-3 -> -sqrt-3 )
   j1  (p) = (-2, 3/2 + sqrt(3)*I/2)   -> other point
   j2  (p) = (2, 3/2 - sqrt(3)*I/2)   == rho_geom-bar (AMPHICHIRAL: mirror = conjugate)
   j3  (p) = (-2, 3/2 - sqrt(3)*I/2)   -> other point

group-realizability (numeric search, sigma(a),sigma(b) words up to length 3):
   trace-action id: realized by 20 group endomorphisms
   trace-action j1: realized by 0 group endomorphisms
   trace-action j2: realized by 24 group endomorphisms
   trace-action j3: realized by 0 group endomorphisms
   => j2 (amphichirality) IS a knot symmetry; j1,j3 (x->-x meridian sign,
      = tensor by a central character) are NOT knot symmetries.

==============================================================================
PART 3 -- Galois (complex conjugation) restricted to Fix(amphichirality)
==============================================================================

Two anti-holomorphic involutions of C (both defined via conj on the Q-curve):
  c   = GALOIS/complex conjugation      : (x,y) -> (conj x, conj y)
  tau = AMPHICHIRAL isometry = c o j2   : (x,y) -> (conj x, (conj x)^2 - 1 - conj y)
(j2 is the holomorphic amphichiral involution; tau is its 'realized isometry'.)

tau fixes the geometric points?
   tau(rho_geom)     = (2, 3/2 + sqrt(3)*I/2)  == rho_geom     : True
   tau(rho_geom-bar) = (2, 3/2 - sqrt(3)*I/2)  == rho_geom-bar : True

Galois c on the two geometric points:
   c(rho_geom)      = (2, 3/2 - sqrt(3)*I/2)  == rho_geom-bar : True
   => c SWAPS rho_geom <-> rho_geom-bar; they are DISTINCT (sqrt-3 != 0).
   both lie in Fix(tau) (shown above)  ==> Galois orbit {p, pbar} is FREE.

Fix(tau) = { x real, y + conj(y) = x^2 - 1 }  (the amphichiral slice).
   On C this is the arc of conjugate-pair characters over x^2 in [1,5];
   contains rho_geom (x=2). Galois c acts on it by y -> conj(y).
   c-FIXED points on Fix(tau)  <=>  y real  <=>  discriminant_y = 0  <=>  x^2 in {1,5}:
      x=1: y=[0]  tr[a,b]=0
      x=-1: y=[0]  tr[a,b]=0
      x=sqrt(5): y=[2]  tr[a,b]=2
      x=-sqrt(5): y=[2]  tr[a,b]=2
   x^2=5 -> (sqrt5, 2): REDUCIBLE boundary (tr[a,b]=2) = the GOLDEN real rep.
   x^2=1 -> (1,0): singular point, meridian a primitive 6th root of unity.
   NEITHER is the geometric (Eisenstein, x=2) point.

Klein-4 group V4 = {id, c, tau, j2} acting on the geometric orbit {p, pbar}:
   id  : FIXES p
   c   : p -> pbar (SWAP)
   tau : FIXES p
   j2  : p -> pbar (SWAP)
   stabilizer of rho_geom = {id, tau};  Galois c is in the SWAPPING coset
   => amphichirality (tau) and Galois (c) are DIFFERENT elements of V4: ORTHOGONAL.

==============================================================================
VERDICT
==============================================================================
OUTCOME B (INDEPENDENT / FREE, with the setwise refinement).

DISCRIMINATING FACT -- orbit structure of Galois on Fix(amphichirality):
  * Both rho_geom = (2,(3+sqrt-3)/2) and rho_geom-bar = (2,(3-sqrt-3)/2) lie in
    Fix(tau) (the amphichiral CS=0 slice), and they are DISTINCT (sqrt-3 != 0).
  * The Galois involution (complex conjugation, sqrt-3 -> -sqrt-3) SWAPS them:
    a FREE 2-element orbit inside Fix(tau).  Fix(tau) is setwise Galois-stable but
    POINTWISE-FREE at the geometric point.
  * Galois has fixed points on Fix(tau) ONLY at x^2 in {1,5} -- the REAL/reducible
    reps (x^2=5 is the golden point (sqrt5,2), tr[a,b]=2; x^2=1 is a 6th-root
    singular point) -- NOT at the geometric Eisenstein point.
  * In V4 = {id, c, tau, j2}: stab(rho_geom) = {id, tau}; Galois c sits in the
    swapping coset. Amphichirality and Galois are DIFFERENT, commuting Z/2's.

=> the two Z/2's are ORTHOGONAL at the geometric point; B709's canonical/
   non-canonical distinction is clean. Structural only; nothing to CLAIMS.md.
