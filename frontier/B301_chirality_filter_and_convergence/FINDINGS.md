# B301 — The E₆ chirality filter (corrected), the trinification connection, and the three-seat convergence

**Status: banked (frontier). Two cross-chat corrections + one elevation; nothing to `CLAIMS.md`.** After B298–B300,
both web-Opus seats flagged the same correction and the same connection. Banked honestly.

## (1) The chirality filter — a FACT, not "SO(10) forced" (downgrade)
Chat-2 tagged "stability forces chiral SO(10)" as FORCED-new in its handoff, then **caught its own overclaim**. The
honest version:
- The **27 of E₆ is complex** (Sage-verified: `27 ≠ 27̄` ⟹ E₆ matter is chiral).
- **Chirality filters** E₆'s maximal subgroups: it **keeps** the ones under which the 27 stays complex —
  `SO(10)×U(1)` (`27=16+10+1`), `SU(6)×SU(2)` (`27=(15,1)+(6̄,2)`), `SU(3)³` (`27=(3,3̄,1)+cyc`) — and **rejects**
  the ones where it becomes real — `F₄` (`27=26+1`), `Sp(8)`.
- So the filter keeps a **set of three chiral subgroups, not a unique SO(10)** — "selects SO(10)" was a
  *minimal-choice*, not a forcing.
- And **"chirality = stability" is itself a physics input**: non-chiral matter is perfectly stable (it just gets a
  mass); what is special about chiral matter is that it is **mass-protected** — it survives massless to low energy,
  which is an observation about *our world*, not a property of the object.

**Banked:** *"chirality filters E₆'s maximal subgroups to the chiral set `{SO(10), SU(6)×SU(2), SU(3)³}`"* `[FACT]` —
**not** *"stability forces SO(10)"* `[overclaim, retracted]`.

## (2) The trinification connection — the same structure, twice
The kept `SU(3)³` **is** the `(θ,φ)` **trinification triality** of B299 (the three SU(3)'s of E₆, permuted by the
orbifold Z₃×Z₃). So the **chirality filter** and the **seam/orbifold triality** are **one structure seen from two
sides** — the rep-theory filter and the geometric Z₃×Z₃ land on the same `SU(3)³`. This ties B301 ↔ B299 ↔ the seam.

## (3) The three-seat convergence — a first-class finding
Two independent brave SM-from-axiom attempts (Chat-1, Chat-2) **plus** the seam arc (this seat) all returned the
**same structural theorem**: the object forces **all** the dimensionless structure + the flow, and **none** of the
values. **Three seats, three roads, one boundary, in the same place.** That convergence is the strongest evidence the
program has produced that the boundary is **real** — a true fact about what self-reference can and cannot emit — and
not a failure of any one attempt. Banked as a finding, not bookkeeping.

## The root, restated (fresh-eyes, the reason the convergence is forced)
A **value is relational** — a coupling is the strength of a relation, a scale is a ratio to a rod, a sign is an
asymmetry to an other, a count is "how many." The object is a **monad** (P009): one thing, no internal other. So the
values are not *missing from* the object — they are **categorically absent from a monad**, emittable only through its
**relations**. The owner named exactly two: the **seam** (interaction-with-the-nothing) and **multiplicity** (more
items). The two open walls map one-to-one onto them — **Wall A (couplings) ↔ the seam**; **Wall B (generations) ↔
multiplicity**. The convergence is forced because three roads all hit the *monad/relation* boundary, which is real.

## The fence
Rep-theory facts (Sage-verified 27-complex) + a framing finding + a retraction of an overclaim. Nothing to
`CLAIMS.md`; the chirality-as-selection-principle is explicitly a physics input.

`chirality_filter.py` (pyenv; the facts, 27-complex Sage-recorded) · `tests/test_b301_chirality_filter_and_convergence.py`.
Related: `B299` (the (θ,φ) triality = the kept SU(3)³), `B298` (generations / Wall B), `B300` (Column B = 2 walls),
`B294`/`B297` (the structural theorem), `P009` (monadic closure), `P012` (the seam and selection).
