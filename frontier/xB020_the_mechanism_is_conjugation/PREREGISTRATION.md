# xB020 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell runs.**

## P0

Hyperbolic geometry, orientation, Chern–Simons. Named mathematics. No value, no generation count, no
physics reading, nothing to `CLAIMS.md`. Gate 5 absolute.

## Why this arc exists — and it opens by correcting ITSELF

xB019 named `PGL(2,ℂ)` vs `PSL(2,ℂ)` as "the mechanism", saying *"a non-orientable manifold's
holonomy lands in `PGL(2,ℂ)`, outside the `SL(2,ℂ)` theory."*

**That is wrong, and this session already banked the fact that refutes it.** xB012's V2:
**`PGL(2,ℂ) ≅ PSL(2,ℂ)`** — over an algebraically closed field every element has a square root, so
the determinant normalises away. **`PGL(2,ℂ)` is not a larger group; it is the same group.**
xB019 contradicts xB012 **within the same session**, and this arc lands the correction.

**The real mechanism is COMPLEX CONJUGATION:** `Isom(H³) = PSL(2,ℂ) ⋊ ℤ/2`, the extra factor being
the **anti-holomorphic** conjugation — not an element of any `PGL`.

## And the sweep found an open lead the mechanism bears on

**B1235 cell 2 registered `L194`:** the orientation double covers of the first **40** non-orientable
census manifolds **all have `CS = 0`** (40 at 0, **0** at ¼), against a **36 %** quarter-rate among
amphichiral manifolds generally — `P ≈ 0.64⁴⁰ ≈ 2 × 10⁻⁸`. Plus a **bite control**: none of the 13
quarter-class members of the 112-family is an orientation double cover of any half-volume manifold in
the 1260-member non-orientable census. **B1235 marks it "Data, not theorem"** and registers the
question: **free deck ⟹ `CS ≡ 0`?**

## THE CONFIGURATION AXES — declared first (xB017 Addendum 4's rule)

| axis | values | this arc |
|---|---|---|
| **the group** | `PGL(2,ℂ)` / `PSL(2,ℂ)` / `Isom(H³) = PSL(2,ℂ) ⋊ ℤ/2` | **varied — this is the correction** |
| **the involution** | orientation-reversing with fixed points / **free** (a deck transformation) | **both named; freeness is L194's hypothesis** |
| **what is forced** | `2·CS = 0` (the 2-torsion) / `CS = 0` (the selection) | **separated — the arc's whole point** |
| **census slice** | B1235's 40 / a larger slice | **varied — 5× at least** |
| the manifold | cusped only | **held FIXED, named** |
| normalisation | SnapPy's `cs` mod ½ | **held FIXED, named** |

## The cells

**H1 — the correction.** Verify `PGL(2,ℂ) ≅ PSL(2,ℂ)` and state the real mechanism.
*Kill:* if the isomorphism fails, xB019 stands and this arc is wrong.

**H2 — B1224 DERIVED, not observed.** Orientation reversal negates `CS`; an amphichiral manifold
satisfies `CS = −CS`; hence `2·CS = 0`, i.e. `CS ∈ {0, ¼}` mod ½. **B1224 banked this as a census
observation (6 of 6); this cell derives it from the conjugation mechanism and then checks no
amphichiral census manifold escapes.** *Kill:* one amphichiral manifold outside `{0, ¼}`.

**H3 — L194 at five times the slice (the blind cell).** Orientation double covers of the first
**200** non-orientable census manifolds: is `CS` always **0**?
*Declared prior:* **I cannot call this.** B1235 has 40/40 and a bite control, so the pattern is
strong — but 40 is a small slice and **a single counterexample would be worth more than the
confirmation**, because it would convert an open lead into a settled negative.
*Kill / the outcome that matters either way:* any cover with `CS = ¼` refutes L194 outright.

**H4 — what the mechanism explains and what it does NOT.** *Prediction:* conjugation **explains the
2-torsion** (H2) and **does not explain the selection of 0 over ¼** — freeness is an extra
hypothesis with no derivation here. **This arc must say so plainly rather than let H3's data imply a
theorem.**

**H5 — the verdict**, naming every axis held fixed.

## What this arc will NOT claim

Not that L194 is proved by data · not that the tools' breaking (xB019) is evidence for A6 · no
identification (E82/I-10) · no physics reading · nothing to `CLAIMS.md`.

## The seat's prior, declared

H1: the isomorphism holds and **xB019's mechanism sentence is withdrawn** · H2: derivable, and the
census has no escapees · **H3: I cannot call it** · H4: the mechanism gives 2-torsion only, and
**L194 stays OPEN with better data**, which is the honest outcome unless H3 finds a counterexample.
