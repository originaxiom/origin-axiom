# B724 — THE SEEING-STRATEGY RE-ADJUDICATION: every verdict COMPUTED (corrects the cited-negative first pass, E19)

*cc banking seat, 2026-07-20. Sealed prereg c99fc733. Comprehensive multiagent loop campaign
(20 agents, 0 errors; compute→3-skeptic verify→refine; base-rate gate mandatory; web-enabled
for Path 1). Every discriminating fact RECOMPUTED in-sandbox — no banked B-number cited as a
refutation. Supersedes both prior passes (the over-cited first, the over-generous second).
Structural/arithmetic only; Gate 5 stands; no SM value; all HINT-grade at most. Credit chat-1
the leads; the discipline is compute-not-cite (E19).*

## The corrected verdict — all COMPUTED, base-rate-gated
| chat-1 item | first pass (cited) | 2nd pass | **B724 COMPUTED verdict** |
|---|---|---|---|
| **C1 chirality** | "expected null" | sound | **SOUND** — recomputed: σ_ω(4₁)=0 (vector-like, anti-congruence PᵀVP=−Vᵀ, det P=1, non-vacuous vs trefoil σ∈{−2,0}); chirality = the fiber y²−3y+3 (disc −3) ℤ/2 torsor. The correspondence "parity = chirality is the observer's" is banked-SOUND (it IS B713). The V-A/E₆-lift test is a MECHANISM = OPEN. |
| **C2 signature** | refuted (B716) | open | **BLOCKED (computed)** — recomputed via Fox calculus (Riley rep, ρ(rel)=I): 27=Sym¹⁶⊕Sym⁸⊕Sym⁰ (dims 17/9/1, spins 8/4/0); the three H¹ classes are one-per-block in DISTINCT-dim irreps ⇒ INCOMMENSURABLE, no SO(3)/S₃ permutes them ⇒ NOT a symmetric spatial triad. h¹=3 is generation-like, not space-like. No route to 3+1 (H³ isotropic; E₆(ℂ) no real form, tr(AB)=5/2−√3i/2; Ω₃^SO=0). |
| **C3 arrow** | refuted | refuted | **REFUTED (computed)** — Fibonacci word p(n)=n+1 (Sturmian, ZERO topological entropy); letter entropy → 0.9594 (converges, not increasing); cat map amphichiral (Mσ M⁻¹=σ⁻¹) ⇒ entropy(σ)=entropy(σ⁻¹), no arrow. |
| **C4 hierarchy** | "numerology" | weak hint | **BASE-RATE COINCIDENCE (computed)** — the FORCED θ-odd ratio τ₈/τ₄=10^14.59 MISSES 10^17 by ×259; only the post-hoc τ₁₁/τ₈=10^16.84 hits, and it is NOT forced. LOOK-ELSEWHERE: 6/6 famous dimensionless ratios (p/e, 1/α, M_Pl/v, M_GUT/M_W, baryon, Avogadro) ALSO have torsion near-hits within ×3 (density ~2.3 combos/decade ⇒ ~2.2 expected at ANY target). Not p<0.01. Dead. |
| **C5 parameters** | refuted | refuted | **REFUTED (computed)** — a 4-element set (2 bits, V₄) is measure-zero / 0-dimensional in ℝ¹⁹; it covers 0 of the SM's ~19 INDEPENDENT continuous parameters. Base rate EXACTLY 0 (not small — impossible). B685 (values not forced) recomputed as the kind-mismatch. |
| **Path 1 GGM** | "numerology trap" | object-internal | **NO IDENTITY (computed)** — recomputed GGM's 4₁ rotated 3D-index from arXiv:2007.10190 eq(80): Ind=1−8q−9q²+18q³+46q⁴+90q⁵+…; Stokes const ±3 (eq 79). (chat-1's quoted q−q⁴−2q⁵−… is MIS-QUOTED, a different series.) Intersecting GGM's integer bag with the banked bag (six torsions + 7983360): the ONLY common integer is 3 — the FORCED ramified prime of ℚ(√−3) (the object IS the fig-8 trace map). No large-integer match, no q-series identity. |

## The honest resolution
Computing (not citing) lands BETWEEN the two prior passes: the first pass over-dismissed by
citation (E19); the second over-rehabilitated to "open." The COMPUTED truth: **exactly one
piece is sound — C1, which is simply B713 restated for the parity puzzle** (a real, banked
correspondence). Everything else is refuted / structurally blocked / base-rate-dead — but now
each with its recomputed discriminating fact and base rate, not an asserted B-number.

## Reusable lesson (banked)
The LOOK-ELSEWHERE result is the durable finding: the object's torsion spectrum spans 10^0.5–
10^37 densely enough (~2.3 signed-combos/decade) that a factor-3 near-hit exists for ANY fixed
dimensionless target — so a single "torsion combination ≈ physical ratio" is EXPECTED, not
evidence, unless the combination is FORCED (a stated rule) AND beats look-elsewhere. C4 failed
exactly there (the forced ratio missed by 259×). Standard for future torsion-ratio HINTs.

## Placement
LAW_MAP: chat-1's SEEING STRATEGY re-adjudicated by computation — C1 sound (=B713), C2/C3/C5
refuted-or-blocked, C4/Path1 base-rate-dead; the torsion look-elsewhere lesson. Corrects E19.
Nothing to CLAIMS. Credit chat-1.
