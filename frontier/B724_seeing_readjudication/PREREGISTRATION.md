# PREREG — B724: THE SEEING-STRATEGY RE-ADJUDICATION — compute the discriminating fact for ALL of chat-1's proposals (SEALED)

cc banking seat, 2026-07-20. Owner: my first adjudication of chat-1's SEEING STRATEGY
over-relied on CITED banked negatives (E19). Re-adjudicate ALL of it COMPREHENSIVELY —
compute the discriminating fact in-sandbox for every correspondence + Path 1, with
mandatory BASE-RATE discipline, never citing a banked B-number as a refutation without
recomputing its discriminating fact. Web-enabled (WebFetch GGM for Path 1). Structural/
arithmetic only; Gate 5 stands; no SM value; results HINT-grade at most (firewalled).

## Five/six probes (each: compute → 3-skeptic adversarial-verify → refine loop; BASE-RATE gate mandatory)
1. **C2 — h¹=3 → THREE SPATIAL DIMENSIONS?** Compute the structure of the object's three
   H¹ classes: are they a SYMMETRIC spatial triad (SO(3)-equivalent, → 3 space dims) or
   INCOMMENSURABLE (V₁₇⊕V₉⊕V₁, B657 — recompute, don't cite)? And does a Wick rotation of
   the complex-CS (B715) give 3+1 specifically? Two-outcome: A = symmetric triad → 3+1
   plausible; B = incommensurable, not a spatial triad → identification fails. COMPUTE.
2. **C4 — the torsion-HIERARCHY forced combination.** The six torsions τ_m (B581, recompute
   the magnitudes) span 10^0.5–10^37. τ₁₁/τ₈≈10^16.8 is a near-hit to the hierarchy 10^17
   (~3% base rate). Is there a PRINCIPLED/FORCED combination (a specific E₆ breaking cascade
   / the four unstable exponents, with a stated rule) that gives 10^17 and BEATS the base
   rate? Two-outcome: A = a forced combination gives ~10^17 (p<0.01 base rate + a rule);
   B = only base-rate near-coincidences, no forced rule → HINT-grade at best.
3. **PATH 1 — GGM Stokes/3D-index vs banked integers.** WebFetch GGM (2007.10190,
   2012.00062), extract the fig-8 3D-index / Stokes-constant integers explicitly (through
   ~q²⁰). Compare to the banked torsion spectrum (B581 τ_m) + the sum-rule 7983360 as
   OBJECT-INTERNAL consistency (both are ℚ(√−3) arithmetic), base-rate-gated. Two-outcome:
   A = a principled object-internal match (p<0.01); B = no match / base-rate coincidences.
4. **C1 — re-confirm SOUND (= B713), not refuted.** Recompute: the object is vector-like
   (chiral index 0) AND chirality is a non-canonical ℤ/2 Galois torsor bit (B713). So the
   correspondence "parity violation = chirality is the observer's" is banked-SOUND. The
   specific V-A/E₆-lift test is a MECHANISM — state it OPEN, not refuted. Confirm.
5. **C3 & C5 — re-confirm REFUTED by COMPUTING (guard against malinformed negatives).**
   C3: recompute the Fibonacci word entropy (Sturmian, zero topological entropy; letter
   entropy converges) + B721's amphichiral no-arrow (recompute σ~σ⁻¹). C5: recompute the
   discriminating fact — the SM has ~19 INDEPENDENT continuous parameters vs 2 bits (V₄);
   B685 values-not-forced (recompute the kind-mismatch). Confirm BOTH refutations hold on
   computation, not citation.

## Two-outcome (sealed, program)
- **OUTCOME A (a lead survives):** some probe yields a principled, base-rate-beating positive
  (p<0.01 + a forced rule) — escalate + reseal as a real HINT/lead.
- **OUTCOME B (honest re-adjudication):** the open leads (C2/C4/Path1) are computable but
  base-rate-only / structurally-blocked; C1 sound (=B713); C3/C5 refuted on computation. The
  corrected verdict replaces the over-cited first pass (E19). [expected: mostly B, HINT-grade;
  the value is the COMPUTED discriminating fact for each, not the outcome.]

## Verdict template
Bank the COMPUTED re-adjudication (per-item discriminating fact + base rate) as the corrected
record superseding the cited-negative first pass. Any surviving lead → HINT_LEDGER (firewalled).
LAW_MAP note; nothing to CLAIMS. Credit chat-1 the leads; the discipline is compute-not-cite.
