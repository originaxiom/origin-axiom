# B979 — L131: is A7 discharge-able? **NO — and the answer was in §5 of the same document**

**Date:** 2026-08-08 · **Seat:** cc (banking) · **Lane:** MATHEMATICS. Gate 5 untouched.
**On sealing:** no preregistration — the answer surfaced during the mandatory prior-art read,
**before any computation**. Sealing after seeing it would be worse than not sealing. Recorded
as an open-and-shut reading, as B957 and B960 were.

---

## The verdict: **A7 is load-bearing. It is not discharge-able.**

B949 registered L131 by pairing UNIQUENESS_THEOREM's A7 statement — *"not eliminable by any
matrix-level axiom… the smallest piece of inserted structure in the whole construction"* —
with B945's finding that the **R↔L swap fixes the object**. If A7's choice were that swap, it
would insert nothing.

**§5 of that same document is titled "The order choice is load-bearing (A7), not cosmetic",
and it already answers this.** B949 read §4.

## Why B945 does not discharge A7 — verified here

| | Möbius fixed-point polynomial | roots |
|---|---|---|
| one order | **τ² − τ − 1** | **φ, −1/φ** |
| the other order | τ² + τ − 1 | 1/φ, −φ |
| conjugated within the class | τ² − 3τ + 1 | φ², φ⁻² |

**LR and RL *are* conjugate** — via the record-swap P = [[0,1],[1,0]], verified here — so they
share trace, determinant, eigenvalues and translation length. **Same conjugacy class.**

> **But the Möbius fixed-point polynomial is a BASED invariant, not a class invariant.**

B945's result — that σ fixes the cyclic word — is exactly the **class-level** statement. A7
lives at the **based** level, which is precisely where B945's observation is blind. B949 even
flagged this risk ("B945 computes at word level, not in Isom(m004)"); the flag was right and
the document had already discharged the question.

**A convention note, honestly:** this bench's L/R naming is transposed relative to the
document, so *which* order carries the golden polynomial flips. **That is not a discrepancy —
it is a demonstration of A7's content.** The assignment depends on a naming convention, and
that dependence *is* the inserted bit.

## THE POSITIVE CONTENT — and it is the sharpest statement the programme has about the four letters

> **A7 isolates, to a single binary choice, where the specific number φ — rather than −φ or
> φ² — enters.**

So: **the four-letter principle plus six motivated axioms forces the object up to exactly one
bit, and that bit is identifiable, named, and is the origin of φ.**

That is a strong, precise statement of what "reality emerges from the aAbB combination" can
and cannot mean today:

- **CAN:** A1–A7 ⟹ A ⟹ the entire proven core P1–P16. The combination principle *does* force
  the structure.
- **CANNOT (yet):** A1–A6 are *"motivated but not laws of nature"* by the document's own words,
  and the unconditional direction — deriving the substrate from nothing — is **mapped dead**
  (B949).
- **AND:** the one residue is not vague. It is **one bit**, and it buys **φ**.

## The fourth instance

This is the **fourth** time today this seat declared open or absent something already banked:

| | declared | reality |
|---|---|---|
| B950 | ℤ₆ "not addressed" | B862 derives it |
| B976 | hypercharge "OPEN" | B864 derived it |
| B974 | the frame "cannot be rebuilt" | B911 built it |
| **B979** | **A7 "possibly discharge-able"** | **§5 of the same file says it is not** |

**The most embarrassing of the four**, because the answer was not in another arc or another
seat's material — it was in the next section of the document being read. `representation-sweep`
would not have caught this one: UNIQUENESS_THEOREM is not an arc.

---

**Verdict: L131 CLOSED, NO.** A7 is load-bearing; the swap-fixes-it observation is class-level
and A7 is based-level. **The positive content is the better half: the aAbB principle forces the
object up to one binary choice, and that choice is exactly where φ enters.**
