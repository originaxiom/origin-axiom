# B1029 — FINDINGS: the invariant ring of the frame action on the coupling data

**Seat:** cc · **Date:** 2026-08-11 · **Prereg sealed 9a46975f BEFORE compute** (in
`ARTIFACT_HASHES.txt`, seal pushed at 82e2ee7b). Campaign: THE WHY CAMPAIGN, Lane III-1.
Compute: `b1029_cells.py` (exact sympy throughout; run it, read it). **No data contact
anywhere in this arc** — object-side pricing only, per the seal.

## V1 — the action, derived (not asserted)

On the banked value family h = χ(A)·½tr V₂(B) (B1011), the two frame generators (B1024)
act as:

- **conjugation** (χ ↦ χ̄): h ↦ h̄ — arg h negated, Re h fixed;
- **reversal** (the bare τ-lift, g ↦ g⁻¹ through B644's functor): χ(A⁻¹) = χ(A)* because
  unitary characters conjugate under inverse, and tr V₂(B⁻¹) = tr V₂(B)* = tr V₂(B)
  because SU(2) traces are **real** — so h ↦ h̄, **the same map**.

**The placement theorem (new join, no new claim):** the value-level representation of the
frame group V₄ = {1, c, r, cr} **factors through ℤ/2**, and its kernel is exactly
cr = θ (reversal-and-contragredient). The banked θ-triviality — tr g⁻¹ = tr g fixes every
trace, the recurring conflation resolved across the retracted/refuted/corrected lineage —
**reappears as the value-kernel of the frame action**. θ-odd data exists only at the
matrix/rep level, exactly as the record says; the frame group sees values only through
{1, complex conjugation}.

**Scope:** proven for the sealed inventory (unitary characters × SU(2) traces — all the
banked coupling data). A functional reading non-real traces of non-unitary reps could see
θ; none is banked.

## V2 — the invariant ring, and the whole non-invariant complement

- Every tone, every mirror magnitude, every |h|² is **real → invariant**. Re χ ∈ {1, −½}
  is the invariant image of the phases.
- **The entire non-invariant complement is ONE COORDINATE: sign(arg h) = sign(Im χ)** —
  precisely the bit the fourth crossing (B1027) paid for. The frame action localises the
  crossing's price to the one coordinate it could not protect.
- Closure relations, recorded as ring arithmetic with **no significance claimed**:
  tone sum = φ² exactly; the golden |h|² pair sums to 1 (B856's banked identity,
  reproduced); the signed mirror set is ±-symmetric (sum 0), magnitude sum (7+3√5)/4.

## V3 — relational candidates, priced under the SEALED machinery (B1026 N3/N4/N5)

Object-side selector: "the two golden family values" = the two non-exact-integer members
of the |h|² family (the third is 1, the m≡0 mod 5 exact member, B1011-derived) —
structurally named, **0 bits**.

| candidate | shape | kind | price | cap 3.0 |
|---|---|---|---|---|
| (a) cos δ = Re χ = −½, sign anchored | absolute + 1 discrete bit | phase | **SPENT** (B1027, ALL-MISS) | — |
| (b) **the golden sum rule**: sin²θᵢ + sin²θⱼ = 1 exactly, unordered {i,j} ⊂ class P | **relational** (Lane III's licensed shape) | probability+probability, kind-exact | log₂C(3,2) = **1.585 bits** | ✓ under |
| (c) the absolute pair: both golden values to an ordered P-pair | two absolute predictions | probability | 6 − 2 dead (N4) = 4 → **2.0 bits** | ✓ under |

N4 honoured: the (smallest golden ↔ solar) pairing is dead ground (B856, −2.3σ) and the
two assignments containing it are excluded from (c)'s count. **Nothing is nominated here**
— nomination is a separate data-blind cell (the B1026 pattern) if Lane III elects to
spend; this arc only proves the shelf is stocked and priced.

## V4 — VERDICT: SHELF DELIVERED (the seal's "shelf" branch, not EMPTY)

1. **One placement theorem**: θ is the value-kernel of the frame action (V1).
2. **The ring is the real subring of the banked constants; the non-invariant complement
   is one sign** (V2) — the fourth crossing's price, localised.
3. **Two priced relational candidates under the sealed cap** (V3) — the first stocked
   shelf Lane III-2 has ever had, priced before any data is looked at.

Declared prior ("non-trivial invariants exist — moderate"): **HELD** in substance —
the invariants exist but are exactly the already-banked real constants; the new content
is the kernel theorem and the pricing, not new invariant values.

**Correction recorded (house rule):** this cell's first compute printed
"EMPTY-beyond-banked / blocked on index correspondences" for V3/V4; the sealed pricing
machinery refutes that — the correspondences are priceable and under cap. The script and
this report carry the corrected verdict; the near-miss stays visible in the git history.

Gate 5 untouched. Nothing promotes. No SM value appears in this arc.
