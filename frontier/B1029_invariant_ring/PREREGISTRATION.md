# B1029 PREREGISTRATION — Lane III-1: the invariant ring of the frame action (sealed before compute)

**Date sealed:** 2026-08-11 · **Seat:** cc. Gate 5-Q; zero anchors; no measured value.

## The question
B1024 established how the observer's frame group acts on the coupling channel's data (the
torsor generators' shadows: conjugation with class (1,0), reversal with class (1,1), acting via
χ ↦ χ̄ and the bare τ-lift respectively). **Compute the ring of invariants of that action on the
banked value sets** — the tones, the mirror set, the phases, the family map m ↦ h(m) — i.e. the
quantities our own theorems certify as frame-independent and therefore legitimately predictable.

## Cells
- **V1**: the action, made explicit on each value set (conjugation: χ ↦ χ̄ hence arg h ↦ −arg h,
  Re h fixed; reversal: via the τ-lift on the structure — derived, not asserted; the composite).
- **V2**: the invariant generators, exactly — for each set, the full list (magnitudes, Re-parts,
  symmetric functions across the family, cross-sector combinations), with the NON-invariants
  named as the complement (what a crossing may NEVER predict absolutely — e.g. the sign of
  arg h, which is exactly the bit the fourth crossing paid for).
- **V3**: the relational candidates — invariant RELATIONS involving two or more measured-side
  quantities (differences, ratios, sum rules) suitable for an anchored crossing; each stated
  with its kind and its anchor cost (0 or 1 calibration).
- **V4**: the verdict: the shelf for Lane III-2's nomination — or EMPTY if the invariant ring is
  trivial beyond what B856/B641 already banked (a real possibility, stated as such).

## Prior (honest)
**Non-trivial invariants EXIST — moderate lean**: Re h's listener-invariance (B856) is already
one; the family structure should supply symmetric functions beyond it. The lean's risk: the
invariants may all be the already-banked constants (then V4 = EMPTY-beyond-banked, and Lane III
needs III-3's new inventory first — an honest, useful null).
