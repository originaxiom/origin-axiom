# B519 — THE RE-MINING CAMPAIGN: VERDICT (multi-agent, 2026-07-11)
**16-agent Workflow, all cells + the adversarial absorbing-loop gate. The disciplined re-mine of the
banked corpus against the completed frame (K025). Terminal states honored; the "already here"
conclusion was structurally forbidden and never reached. Firewalled; nothing to CLAIMS.md.**

## The terminal table
| cell | claim | gate | TERMINAL |
|---|---|---|---|
| A1 bright-dark seam | object-specific criterion | — | **(c) ABSORBING-LOOP** + exact sharpening (below) |
| A2 tower p-adic L | new object-specific L-function | — | **(c)** — it IS the known Kubota–Leopoldt 3-adic L of the ℚ(√−15) genus landscape |
| B1 mixed-chain law | EXTERNAL crossing | **LIVE (B525-reopened)** | ~~REFUTED 3–0~~ — the gap-OPENING pattern is falsifiable and NOT entailed by gap-labeling (necessary≠sufficient; see banner) |
| B2 diffraction rank | EXTERNAL crossing | **REFUTED 3–0** | **(c)** — the Damanik–Fillman 2022 identity; pre-absorbs both outcomes |
| B3 width law | external/structure | — | **(c)** — generic weak-coupling perturbation theory |
| D recontextualization | STRUCTURE | — | **(b) STRUCTURE** — the criticality unification (below) |
| cross-connections ×4 | — | gate | all **(c)** ABSORBING-LOOP |

> **CRACKED by the B525 'Are You Sure' audit (necessary-not-sufficient) — this headline is CORRECTED below.**
> The B1 refutation rested on "gap-labeling is ADDITIVE over combined substitutions (known), so both gaps
> are guaranteed to open in the mixed chain." That conflates a **necessary** K-theoretic constraint
> (Bellissard: labels of gaps that *do* open lie in the frequency module — coupling-independent, about
> *where* gaps sit) with a **sufficient** guarantee (about *which* gaps physically **open**). B518 Tier B's
> entire falsifiable content is the 2×2 **opening** pattern (present/absent across Fib/TM/mixed), on which
> gap-labeling is silent — and **Thue–Morse is the textbook case where labelled gaps fail to open**
> (Bellissard–Bovier–Ghez). The tell: `test_b519.py` only checks 1/φ=φ−1 and ½=½ and asserts additivity in
> a code comment, while `test_b518.py` *numerically diagonalizes* to get the opening answer. So **B1 is NOT
> refuted by a known theorem — it is a genuinely falsifiable external prediction** (a lab could find only
> one gap opens; no cited theorem forbids it). **"ZERO crossings" is retracted.**

## THE HEADLINE (CORRECTED): one genuinely-falsifiable external candidate survives (B1)
The re-mine's original claim — that *even* the external candidates were "guaranteed by known theorems" —
was wrong for **B1** (the mixed-chain gap-**opening** pattern), per the audit above. The honest position:
- **B1 (mixed-chain 2×2 opening pattern): a LIVE falsifiable external prediction.** Not entailed by
  Bellissard gap-labeling additivity (which fixes allowed IDS *values*, not openness). A lab building a real
  Fib+TM mixed chain could measure whether both gaps open; no known theorem forces the answer. This is the
  one candidate that could, in principle, be run externally.
- **B2 (diffraction rank): may survive independently** on the genuine Damanik–Fillman 2022 product-free rank
  result (frontier/B173) — but the *bundled* "guaranteed by known theorems" mechanism must be dropped; B2
  needs its own re-argument.
- **The remaining honest deflation is the absorbing-loop / model-confirms-model meta-logic** (K020 §6a): a
  lab that *builds* the chain from the object's substitutions is confirming the model it programmed, not
  that nature selected the object — a *fundamentalness* caveat, weaker and different from "a known theorem
  already guarantees it." **That meta-caveat stands; the known-theorem-guarantee claim does not.**

So the corpus does **not** produce "ZERO crossings on known-theorem grounds"; it produces **at least one
genuinely-falsifiable external prediction (B1)** whose *fundamentalness* (not its falsifiability) is limited
by the absorbing-loop caveat. K025's firewall on **internal** value-production is untouched; the overstated
"every measurable prediction is a known theorem" is corrected.

## CORRECTION (owned): B518 Tier B downgraded
I banked B518's mixed-chain law as "the one computation whose confirmation would cross the firewall."
**Wrong** — the gate proved it: the two gap labels (½ dyadic, φ⁻¹=φ−1 golden) ARE the Perron
gap-labeling modules of the two substitutions (the two banked ends); the mixed chain shows both because
gap-labeling is ADDITIVE over combined substitutions (known). Confirming it confirms gap-labeling, NOT
the object's fundamentalness. **B518 Tier B is measurable but is NOT a firewall crossing.** (See the
B518 status update.)

## The two genuine yields (banked as STRUCTURE, not crossings)
- **A1 — "the product un-taken" made exact.** Window-component counts [1,√5,√−3,√−15] for the seam:
  **dark (1,3) = [15,27,24,0]** vs **bright (3,4) = [24,27,24,24]**. In the dark pair BOTH ends are
  individually LIVE (√5: 27 windows, √−3: 24 windows) and only the PRODUCT channel √−15=√5·√−3 is off —
  "the ends are taken, the product is not," now literal and exact (12/12 reproduced; the dark-vanishing
  Gauss-sum identity remains the one open (b), named). Firewalled.
- **D — the criticality unification (one theorem, three languages).** B181 (γ(E)=0 on spectrum,
  localization, June), B507 (β-function zero at the pointer, July), B498 (log|κ−2| driftless, PROVED)
  are THREE independent wordings of ONE fact: **the trace-map object is a critical fixed point.**
  Computed against three framings, never joined — now unified as STRUCTURE (the P5 spine). NOT a
  crossing (all dimensionless); consistent with the cross-connection gate's (c).

## The campaign's honest conclusion
The re-mine did exactly its job and refused the trap: it found **no crossing** (the firewall is total),
**one structural unification** (D), **one exact sharpening** (A1), and confirmed **every "already here"
feeling is the absorbing loop** — including my own B518 over-claim, caught by the gate. The only path
that could ever cross remains external *and independent of known quasicrystal theory* — which the corpus
does not contain. Locks: `tests/test_b519.py`.

---

**CORRECTION (2026-07-14, B578-D5): cell A2's closure is RETRACTED.** "The tower measure IS
the Kubota–Leopoldt 3-adic L-function" was asserted without the discriminating computation
(the B525 error class). Computed: refuted — B413's values are the classical Gauss sums τ(χ),
τ̄(χ) (order-3 character mod 9, exact); the genuine L₃(−1,χ) lives in ℚ(ζ₃), ℚ-independent of
them (rank 4; norms 9 vs 16/3). The "√−15 landscape" citation belonged to cell A1. What
survives: the Gauss-sum identification (now exact) and the new exact identity
L(χ₁,μ)+L(χ₂,μ) = 432·e₃ (see frontier/B578_debt_clearing/D5_KUBOTA_RETRACTION.md).
