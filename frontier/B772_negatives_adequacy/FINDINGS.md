# B772 — THE NEGATIVES ADEQUACY AUDIT (owner's question: are we computing properly?)

*2026-07-23. The owner asked: "what are we not understanding around these negatives?
are we computing properly, proper resolution, all relevant levels and input
mechanisms?" This is the computed answer. Workflow wf_1842a0a3-cbb: 14 adversarial
classifiers (one per banked negative) on four axes — KIND, RESOLUTION, LEVEL/
PROJECTION, INPUT MECHANISM — + a synthesis. audit_results.json is the record.*

## The answer, in one line
**The seam is fine; the projection is not.** We are NOT re-confirming one fact many
times (my hypothesis was wrong: 12/14 negatives are object-native and genuinely
seam-independent). We ARE systematically testing the object in the ONE projection
its own thesis says is blind — the cyclic-trace / abelianized / character level,
which by construction cannot see the chord / θ-odd sector where B766 places the
live structure. **No negative in the corpus has ever computed a chord-level analog
of its own target and found THAT also empty.** We may be accumulating absences in
the one projection guaranteed to be silent, and reading trace-level absence as
structural absence.

## The buckets (14 negatives)
| verdict | n | meaning |
|---|---|---|
| ROBUST | 4 | load-bearing: OI-173, OI-186, W2-148r, W2-237 (exact/structural, object-native, seam-independent, resolution adequate) |
| PROVISIONAL | 7 | real object-native probes but under-resolved or trace-blind — not seam duplicates, not yet trustworthy |
| NEEDS-DEEPER-LEVEL | 2 | W3-082, W4-304 — computed in a sub-level; the headline is asserted by naming-continuity, not derived at the loaded level |
| CONTENT-FREE-THESIS-PREDICTED | 1 | W4-192 (the 5th F₄→SM-shaped door — the thesis predicts it) |

Only **4/14 are currently load-bearing.** The middle 8 are genuine independent
probes of the object that are under-resolved or computed in the blind projection.

## The three specific risks, ranked
1. **PROJECTION (the real finding).** 6/14 live at trace/character/abelianized
   level; 4 flagged TRACE-BLIND-RISK against B766's own θ-odd lesson: **W3-067,
   W3-082, W4-115, W4-304**. The two cells that DID reach chord level (W2-140,
   W4-124) were fine there — proving the check is decisive when done and simply
   absent when skipped.
2. **RESOLUTION.** 8/14 MARGINAL — the recurring B525 failure (finite depth read as
   all-n in OI-055/W4-115; one candidate-of-many read as exhaustive in W4-124;
   external theorem cited by title not pinned in OI-055). W4-124 and W4-115 were
   explicit *lucky escapes* — an adversarial recheck happened not to find the hidden
   positive; they are not closed proofs.
3. **INPUT/independence — the good news.** 12/14 object-native and seam-independent.
   The corpus does NOT over-confirm the seam; it genuinely, independently probes the
   object. My over-determination worry was the wrong worry.

## Real defects found in already-banked cells (→ ERROR_LEDGER)
- **OI-146 PART 1a** is an MB12-class vacuity: vol_m004/vol_orbifold = 12 by
  construction (the shared lob term cancels), so "ratio rounds to 12" is
  unfalsifiable, and it reused a known-buggy nsum. The HEADLINE (index = 10, not 3)
  is sound via independent orbit-stabilizer enumeration; only the PART-1a
  self-certificate is vacuous. Fix: rerun 1a with the Hurwitz-zeta closed form + a
  real SnapPy volume (both already correct in B734/cc3_verification/b734_fix2.py).
- **OI-055** closes its all-n premise by citing GSWZ arXiv:2412.04241 by TITLE, with
  no pinned theorem for THIS series' Habiro-membership (contrast Thm 9.1 pinned for
  the companion Gevrey fact). Census label downgraded EXTERNAL → **SEARCH-BOUNDED
  (depth 100)** pending the pin.
- **W4-139** overstates status: its own verdict is UNRESOLVED (no non-existence
  banked) — correctly OPEN/partial, not a negative. Re-tagged.

## The action (B773, launching now)
The chord-level re-computation wave: re-run W3-067, W3-082, W4-115, W4-304 at the
θ-odd / non-abelian / framing-sensitive level. **If they still return empty, the
negatives harden into real walls. If any returns a positive, the trace projection
was hiding structure — a major result.** Until at least one trace-blind negative is
re-run at the θ-odd level and STILL empty, the census cannot claim to have probed
the object where the thesis says it lives.

Gate 5 / Gate 5-Q clean; this is methodology, nothing to CLAIMS.
