# B86 — unification synthesis, novelty positioning, paper skeleton (Phase E)

The capstone of the unification push: consolidates the whole arc into the publishable structure and
names the external check that gates any claim.

- **`probe.py`** — prints the three-thread structure (proof-status per piece) + the novelty assessment.
- **`FINDINGS.md`** — the synthesis.
- **`../../papers/SLN_FIGURE_EIGHT_SKELETON.md`** — the paper skeleton.

**Summary.** Three threads, one object (`Fix(T_1²)`): the **tower** (PROVED n≤4, structural n=5,6,
reduced to one symbolic step), the **A-polynomial connection** (Cooper–Long SL(2) exact, Falbel SL(3)),
and **degree=rank / the `Aₙ` family** `L=(−1)ⁿ⁻¹Mⁿ` (SL(4) `L=−M⁴` new). **Novelty:** SL(2)/SL(3) known;
the `Aₙ` family / SL(4) A-polynomial, the CRT/F_p method, and the parity-graded factorization are
`APPARENTLY_NEW` pending an external literature check (the `Aₙ` family is **#1 to check**). The physics
chapter is closed.

```bash
python frontier/B86_unification_synthesis/probe.py
python -m pytest tests/test_b86_unification_synthesis.py -q
```

No Origin-core claim; proven core P1–P16 untouched; no CLAIMS.md promotion; outreach dormant.
