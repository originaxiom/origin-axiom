# B1077 — THE INTRINSIC LAYER IS SPLIT: the even Clifford algebra of the object's bare norm form is M₈(ℚ)×M₈(ℚ) with center ℚ×ℚ, exhaustively — the 77 requires a twist, and B882 is the unique non-circular door

**Date:** 2026-08-19 · the owner's physics-first directive; the registered Route 1 of the
77-mechanism question (the W4 disposition) · 2 agents (compute + adversarial verify, holds
= TRUE on a full independent rebuild with its own basis-free discriminant route) · Gate 5
untouched; the circularity guard held (no pencil file read by either agent).

## The theorem (all exact, exhaustive where stated)

From B904's bare norm form N(x) = x₀x₇ − x₁x₄ − x₂x₅ − x₃x₆ (transcribed, verified
symbolically + 30 random vectors): the form diagonalizes over ℚ to (+1⁴, −1⁴) — Witt
index 4, genuinely split; the Clifford algebra's structure constants are literal ±1;
**associativity verified on ALL 128³ = 2,097,152 even-blade triples** (0 failures);
**Z(C₀) computed by complete enumeration** — of all 128 even blades exactly {1, ω}
commute with everything, ω² = +1 (two routes), so **Z(C₀) = ℚ×ℚ, the SPLIT quadratic
étale algebra**; the central idempotents exact; **C₀ ≅ M₈(ℚ)×M₈(ℚ) by an explicit
isomorphism** (the 16-dim Fock representation; block-diagonality on all 128; exact rank
128/128, det = 2²⁵⁶); the two 8-dim spinor spaces exhibited concretely (Λ^even/Λ^odd of
an explicit isotropic 4-space). Two independently-coded constructions agree on
everything. The verifier's own MB12 bite control: a non-split form's center is a genuine
quadratic field — the split verdict can fail and doesn't.

## What it closes and what it aims

**The two-level intrinsic theorem is complete**: at the trialitarian level the undressed
datum attaches the split cubic ℚ³ (the W4 cell); at the Clifford level it attaches the
split quadratic ℚ×ℚ (this arc). **Nothing 77-shaped exists anywhere in the object's bare
D4 geometry.** Every 77-bearing structure in the corpus is therefore DRESSING, and the
only dressing source in the bank is the pencil triple — forbidden as a twist-source by
the circularity theorem-let. **B882's conjecture (arithmetic S₃ = geometric S₃) is now
the UNIQUE non-circular route to a 77-mechanism**, its in-sandbox half done here; the
remaining half is the KMRT §43 connective proposition — a typed literature floor, the
named external need.

## Companion in the same bank: the L154 update

The σ-identification probe (same day, 2 agents, verify HOLDS with from-scratch
arithmetic + the E₈ sanity anchor): UNDECIDED with the obstruction FULLY MAPPED — the
two 6's are the same-convention Virasoro central charges (comparable, no normalization
gap), equal exactly at σ := 1 which the circularity guard forbids as evidence; no
forcing bridge banked; the direct construction site not-posable (B1064-O3). The three
named resolution routes are in the L154 register row. A2 stays an anchor.

## Artifacts
- `b1077_results.json` (both agents verbatim, scrubbed) · `l154_results.json` (the companion probe).
## THE FLOOR-LIFT (same day; the strongest unopened lead, opened)

Dolphin–Quéguiner-Mathieu (arXiv:1905.12526, HTML full text, fetched at execution):
**Definition 4.2** — a trialitarian triple IS the decomposition 𝒞(A,σ,f) ≅ B × C;
**Remark 4.8** — a triple has trivial discriminant BY DEFINITION (the Clifford center is
F×F); the S₃/A₃ permutation action (Thm 4.12) requires it. CONSEQUENCE, joining this
arc's theorem: the object's bare datum (center ℚ×ℚ, this arc; split cubic ℚ³, the W4
cell) forms an honest SPLIT trialitarian triple — and **ℚ(√77) can only enter through
the twisted ³D₄ form, where the cubic étale algebra is a field.** B882's conjecture is
therefore stateable in KMRT's own language: *the dressed datum is the ³D₄ twist of the
split triple by K.* The literature floor narrows from "KMRT §43 generally" to the ³D₄
twisting classification (Knus–Tignol arXiv:1409.1718's territory); the in-sandbox half
is complete as of this arc.
