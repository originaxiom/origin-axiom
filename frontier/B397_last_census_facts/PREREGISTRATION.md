# B397 (Closure II / M5) — PRE-REGISTRATION: the last two census facts

**Committed before computation. Time-box: 1 session (+1 only if (a) banks and (b) is
mid-flight). The final Closure II wave.**

## (a) The (2,3) half-group stabilizer

Banked (P61): the (2,3) table is stabilized by the ENTIRE √5-fixing half-group
{c ≡ ±1 mod 5} (8 of 16 elements) — unique among the tables; mechanism open. REGISTERED
CANDIDATE (the locality route, post-P67): the property is LOCAL — (2,3)'s 5-side spectrum
X₅ is pointwise fixed (up to the relabel) by the ζ₅-Galois action, i.e. its 5-local table
values lie in the σ-invariant subfield, forcing every c ≡ ±1 (mod 5) to act as a pure
relabel on the assembled table. Test: the Galois structure of X₅(2,3) vs X₅(1,2) (control:
(1,2) is NOT half-group-stabilized). KILL: no local distinction — the stabilizer is then
an aggregate phenomenon (mirror-class; staged).

## (b) Defect-tracelessness (quantified)

Banked observation: the sector CONSTANTS are exact σ_√5-conjugates across the u₅-twisted
models while the cell tables are not — "the trace washes out the intertwiner defect."
REGISTERED QUANTIFICATION: for ALL FOUR sector gradings (slot and 3-block, both faces),
the graded trace of the u₅ = 2-model equals the σ_√5-conjugate of the u₅ = 1-model's —
8 exact equalities (the defect is annihilated by every graded trace), while at least one
raw cell differs (the known contrast, re-verified). KILL: any graded trace where the
conjugacy fails — the "tracelessness" would then be grading-specific.

Machinery: the local-table code (tensor_gate.local_partrace_table at u = 1 vs 2), the
exact engine, SC.sigma. Locks from output JSONs. Firewalled.
