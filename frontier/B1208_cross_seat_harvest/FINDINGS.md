# B1208 — THE CROSS-SEAT HARVEST: seven results from three seats verified on this bench — two of their derivations corrected, one generalized, one of my own candidates convicted of vacuity, and one convergence neither seat had seen

**Verdict**: `OPEN` (harvest arc) · **2026-08-29** · **Gate 5 clean** (counts, ranks, characters,
Galois arithmetic; no measured value) · discharges the head of **R52-2**

Seven results arrived from three seats while this bench was on instrument work. All seven are
**verified here, from main's own data**, and the verification changed three of them. One turns out
to share its mechanism with a negative this bench banked two days ago, and neither seat had noticed.

---

## LEG 1 — cloud memo 128, the λ-term's rank: **CONFIRMED, derivation CORRECTED**

**Their result.** B1206 named three candidates for the missing linear condition on the ℙ³ and
called candidate (iii), *the λ-term's own rank*, the cheapest: rank 2 rather than 1 would close the
cut ledger. The rank **is** 2 — and the ledger **does not close**, because on a
doublet–doublet–singlet coupling **rank 1 is impossible**, so the fork's two branches were
*"impossible"* and *"always"*. Their certificate runs **green on this bench** (its three-deep
dependency chain extracted and executed here, not cited).

**What the verification changed.** Memo 128 derives the impossibility from **t₃-conservation**:
*"C conserves t₃, so the block is forced ANTIDIAGONAL, so its rank lies in {0, 2}."* That step does
not hold. Antidiagonal support permits **one entry to vanish**, which is rank 1 — this bench
exhibits the witness `[[0, −3], [0, 0]]`, t₃-conserving and rank 1. t₃-conservation is the
**Cartan** only.

The exclusion needs **full SU(2) invariance**, which this bench proves independently: solving
`XᵀB + BX = 0` for the three su(2) generators gives the invariant space `[[0, −c], [c, 0]]` —
**one-dimensional**, spanned by ε, with `det = c²`, so the rank is in {0, 2} and **rank 1 is
impossible**. A control confirms the exclusion is a fact about the *doublet* and not about the
enumeration: a hypothetical non-doublet H_u (t₃ = [−1, −1]) admits rank 1 under the same Cartan gate.

> **Their conclusion stands; their stated chain needed the group, not the torus.** The correction
> strengthens the result — the impossibility is representation-theoretic, not a weight accident.

## LEG 2 — cloud memo 129, the H_d menu: **ADOPTED**, and their one open check **POSED**

Memo 128 killed three *named* routes; memo 129 closes the *space* they were drawn from. Memo 80's
sector table is a closed census, and exactly three couplings touch H_d — `Hu·Hd·N₁` (canonical),
`q·dᶜ·Hd` and `l·eᶜ·Hd` (both matter-state dependent, i.e. texture data). **Exactly one is
canonical, so B1206's "one condition short" is structural, not an unfinished search.** Their §4
then argues the lepton determinant is not a second cut: both rows carry unit coefficients and
differ by exactly the colour factor (6 = 3×2 vs 2 = 1×2), the signature of one shared
`10·5̄·5̄_H` operator, i.e. `Y_e = Y_dᵀ`, giving the same cut.

**Their §6 hands this bench the single check they cannot run**, and it is the highest-leverage open
item on the value arm: *the generation-level embedding could distinguish the two rows and make
`det Y_e` an independent cut — which would close the ledger.* Pursued here to the point where it is
**exactly posed** rather than guessed:

- The object's construction grades by a **ℤ/12 character** (codex's height-308 spec: idempotents
  `e_r = (1/12) Σ ζ^(−rm) g^m`, selection `ρ + σ ≡ 8 mod 12`). That is a **Wilson-line-type
  breaking**, so SM components of one SU(5) multiplet **may carry different characters** and hence
  different multiplicities. The question is therefore *not* settled by the SU(5) relation.
- The down coupling's legs are named in the spec as `A₇ × B₆ × B₂` (characters 7, 6, 2; index
  ranges 3, 2, 3 giving the 1×18 conn/conn row). **The lepton row needs its own character triple**,
  and whether one exists that satisfies the same selection rule is the decisive datum.

> **THE FORK, preregistered here with three outcomes, because two of them are not "no change":**
> **(a)** the lepton legs carry the same characters → same tensor → `det Y_e` is the same cut,
> B1206's ledger stands at dim 1 and the ℙ³ row stays PERMANENT.
> **(b)** they carry different characters selecting different classes → a **second, independent**
> 3×3×4 tensor → generically `det Y_e` is an independent cubic → **the ledger closes at dim 0 and
> the ℙ³ row flips PERMANENT → FORCED.** This is the largest single lever currently identified on
> the value arm.
> **(c)** no character triple satisfies the selection rule → **the lepton Yukawa is absent from the
> object**, a new structural fact of the same species as SEAM-Y's μ_u = 0 (and no closure).
>
> **The decisive datum is the ℤ/12 character assignment of `l` and `eᶜ` in codex's frames** — not
> on this bench and not on cloud's. Relayed to codex beside R023, whose evaluator it neighbours.

**A cross-source caveat found while checking this** (recorded so it is not re-discovered): B1185
states the census `18+9+6+3 → 36 = 3×3×4` as *"re-derived exactly"*. The census alone does **not**
force that shape — this bench finds **three** factorizations consistent with its four counts:
(3,4,3), (3,12,1), (9,4,1). The 3×3×4 reading is fixed by data the census does not contain (the
spec's own index ranges plus B1161's sector table). **Corroborated by the census, sourced
elsewhere.** Nothing downstream moves: B1205's codimension count needs only a 3×3 matter block
linear in a 4-dimensional Higgs space.

Cloud's own cross-source finding is **accepted and reconciled**: memo 80's roster counts **states**
(H_u 2, H_d 2 = 2 doublets) while B1206's ledger leans on B1161's **multiplicity** table
(Q/dᶜ/H_d/H_u = 3/3/4/1). B1206's cited datum came from the first and its ledger from the second.
The count is the same under both readings — one canonical functional — so the verdict is robust,
but **B1206's stated reason ("H_u is 1-dimensional") was reading a multiplicity as a state count**,
and that is corrected here.

## LEG 3 — cloud memo 130, the D2 scope question: **CONFIRMED on main's own rows, and STRONGER**

B1197 returned SPLIT and routed the scope choice to the owner as *"both readings defensible"*. Memo
130 measures them. Verified here **against main's own banked B1197 data**, not against their rerun:

- **S5b — signed CS was never a function of Vol.** On the 156-row signed census, **156 of 156**
  same-volume pairs carry **exactly opposite** CS (witness: Vol 1.977609277375 carries
  ±0.070577360, sum 1.4e−17). That is B289's sign law — **B1197's own positive control** — so the
  variable reading was refuted by a law this bench banked *before* the run.
- **S5 — |CS|, the repair, is not a function of Vol either.** Their window reproduces **exactly**:
  width 0.005149 holding a spread of **0.143940**, ratio **28.0×**. And this bench finds a
  **stronger** witness they did not report: a window of width **0.000264** carrying an |CS| spread
  of **0.143205** — **541×**, twenty times steeper than the figure in the memo.

> The two readings are **not symmetric**: the variable reading fails single-valuedness twice over,
> and the trajectory reading was preregistered (their S6, on B289's own naming of the scale ladder).
> **The owner's D2 choice is therefore not "which of two equally-supported readings"** — it is the
> single question *"is a clock identified along one realized history, or between two variables on
> the space of possible histories?"*, and the second is dead either way. Their recommendation
> (Option A: pay LEAP-1 with the scope premise written in as its own priced line, refuter armed) is
> **presented, not taken** — D2's scope remains the owner's call. Their filed error (dropping this
> bench's 1e-9 tolerance on a first recount) is noted as filed rather than quietly fixed.

## LEG 4 — cloud memo 123, the dark-matter stabilizer census: **CONFIRMED entry-for-entry**

Re-run here from **main's own 27-weight generator** (B299's `_weights_27`), not their code, and in a
**stronger form** — quantifying over *every* charged set of size ≤ 2 rather than a chosen pair of
neutrals, so the negative holds for any identification of the neutrals:

| n | stabilizers | min \|charged\| | max level set |
|---|---|---|---|
| 2 | 0 | 11 | 16 |
| 3 | 0 | 12 | 15 |
| 4 | 0 | 11 | 16 |
| 5 | 0 | 11 | 16 |
| 6 | 0 | 11 | 16 |

**Zero stabilizers**; the smallest charged set any nontrivial character produces is **11**, the
largest level set is **16 of 27** where a stabilizer needs **≥ 25**. Both bounds fail by wide
margins, and the numbers match their table exactly. **MB12 control**: on a planted rep (25 neutral +
2 charged) the same instrument finds **486** stabilizers, so the census can succeed and its negative
is not vacuity.

> **The cosmology ledger's row 4 closes at character level**: dark-matter stability cannot come from
> an abelian symmetry of the 27. Non-abelian stabilizers and kinematic stability remain untested and
> are named, not claimed. **Fence carried from their memo**: this is a necessary-condition census —
> imposing coupling consistency could only shrink the candidate set, so the negative stands a
> fortiori. **And the standing fence repeated**: B1199's "dark" (Gauss-sum vanishing) has nothing to
> do with this row.

## LEG 5 — cc3 B8154, the mirror is `c`: **VERIFIED 6/6 with their controls**

Their five-link chain re-derived here symbolically: `t² = t − 1` makes t a primitive **sixth** root,
so `ω = t²` is a primitive **cube** root; `Φ₃(ω) = Φ₃(ω²) = 0`; `u ↦ u²` swaps the roots and is an
involution on them; `ω² = conj(ω)`; and `conj(t) = 1 − t` **satisfies the same relator**, so
conjugation acts on the character variety and reverses orientation. **⟹ mirror = conjugation = the
Φ₃-root swap = c.** Their controls reproduce: `u ↦ u³` is the identity on cube roots (not the swap),
and c fixes ℚ but not ω.

Their link 6 is the substantive addition: with `|G| = 2` **prime**, every subgroup is trivial or all
of G, so Paper IV's **Escape (i) — "shrink the group" — is vacuous**, and a selector must fail
conjugation-invariance outright. Their vacuity control reproduces exactly: |G| = 4, 6, 8, 9 each
admit a proper nontrivial subgroup, so the check **tracks primality, not the number 2**.

> **Convergence, not novelty, and worth saying which**: main already had mirror = chirality = Galois
> generator (B1174) and the one-class theorem (B1183). What cc3 adds is a **second, independent
> route to Φ₃ — from the holonomy rather than from the partition function** (B1200 met it at the
> saddle) — and the prime-order argument that closes an escape their paper had listed as open.


## LEG 6 — cloud memo 127, the mirror is irremovable: **CONFIRMED**, and it is **B1203's identity read backwards**

Their second, disjoint proof that the observer's bit cannot be absorbed. Re-derived here
symbolically, entry-for-entry:

- **The whole internal group preserves κ identically** — letter swap, word reversal, inversion, all
  three SL₂ lift signs, and both Fricke twists: `κ ∘ g − κ = 0` in ℤ[x,y,z], not merely at the
  object's point. **Control**: a squaring bite `x ↦ x² − 2` moves it, by
  `(x−2)(x+1)(x²+x−yz−2)`, so the identity can fail.
- **κ separates the object's point from its mirror**: `κ(P₀) = 1 + ω` and `κ(gal P₀) = 2 − ω`, with
  `P₀ = (2, 2, 2−ω)`. Since every internal operation preserves κ, **no composition of them carries
  gal(P₀) back to P₀ — the mirror is irremovable**, and the proof covers the whole group at once
  rather than candidate by candidate.
- **The invariant content is unmoved**: both values root `X² − 3X + 3` (sum 3, product 3, verified).
  The *value* moves; the *content* does not — their §4 kernel/odd split seen from the duality side.
- **Exactly 2 of the 8** elements of ⟨letter swap, lift signs⟩ fix P₀, so at the object's own point
  (x = y = 2) **the letter swap is its only internal duality**. Reproduced exactly.

### The convergence neither seat had seen

**B1203** (banked here 2026-08-28, verdict NEGATIVE) found that the owner's founding climb —
`(X,Y) ↦ (XY,X)`, i.e. `τ_a` in the substitution's own coordinates — **preserves κ identically**,
and read that as: climbing generates no new invariant, so there is **one bit and not a tower**.
Memo 127 uses the **same invariance** to prove the mirror **cannot be absorbed**. Checked here in
one script: the climb sits in the same table as the letter swap and the lift signs, `κ ∘ g − κ = 0`.

> **These are one identity read in two directions.** The internal group cannot change κ; the mirror
> does. So **the mirror is precisely the operation the object cannot perform on itself** — which is
> *why* the observer's bit is exactly one, and external. B1203's negative and cloud's
> irremovability proof are not two facts that agree; they are one mechanism, and the pairing
> explains both. It also re-reads B148 (the classical Fricke invariance of the Dehn twists) as the
> common root of both — the third time that banked fact has turned out to be load-bearing under a
> new name.


## LEG 7 — codex R021, the Gieseking Pin⁻ restriction: **CONFIRMED, and GENERALIZED**

Their theorem: the Gieseking manifold N has two Pin⁻ structures, m004 has two spin structures, and
the **affine restriction map is constant** because its linear part `p*: H¹(N;F₂) → H¹(M;F₂)` is
**zero** — cited from the abelianization `H₁(N) = ℤ⟨t⟩` with the cover's generators landing on `2t`.

Verified here on this bench: N is nonorientable with `H₁ = ℤ`, m004 is confirmed by isometry to be
a degree-2 cover, and the Smith normal form of `π₁(N) = ⟨a, b | aabbAB⟩`'s relation matrix gives
`H₁(N) = ℤ`, hence **H¹(N;F₂) has rank one**.

**And the reason is more general than their computation.** Rank one forces `H¹(N;F₂) = ⟨w₁⟩`, and
the orientation double cover is *by definition* the cover on which `w₁` pulls back to zero — so `p*`
kills a generator of the whole group and therefore vanishes identically. Their coordinate statement
is the same fact seen from the covering's definition: the cover is the index-2 subgroup of
**even exponent sum**, so its image in `H₁(N) = ℤ` lies in `2ℤ`, which is `a = b = 2t`.

> **THE THEOREM, in the form it actually holds**: *for any nonorientable manifold N with
> `H¹(N;F₂)` of rank 1, the restriction of Pin⁻ structures to the orientation double cover is
> constant.* The Gieseking is one instance. **Control**: the rank-1 hypothesis is load-bearing — at
> rank ≥ 2 there are classes other than `w₁` that need not die, so the restriction need not be
> constant, and their certificate is checking the right thing.

**Their residual is untouched and carried**: the constant image is deliberately left **unnamed**,
and naming it still needs their tangent-frame Pin⁻ lift plus the comparison with B1141's
beat-selected sign. The generalization says *why* the map is constant, not *which* structure it is
constant at. Their requested open child is minted on that comparison.

## LEG 8 — codex R020, the principal-beat refutation: **RECEIVED, NOT YET VERIFIED**

Their certificate reports `Σ(V64) ⊄ V64` for the selected B1140 representative (stored A₂ ordering,
`swapper#13`, `lift#0`), with both spin-two strings and the coloured 54 leaking, and verifies
ambiently that `Σ² = exp(ad E) = Ad(meridian)` on all 78 Chevalley inputs. **Typed as their own
relay types it** — *"an algebraic preservation refutation for this selected representative, not a
24-hit covariance theorem or a physical claim."*

**Not adopted here.** It refutes a preservation property of something main banked (B1140), and the
house rule is that a cross-seat result touching a banked claim is verified before it moves anything.
The re-derivation needs the E₆ Chevalley machinery stood up against their frozen representative, and
is **named as the next cell**, not left implicit. Nothing in B1140 is re-graded in this arc.

---

## LEG 9 — the self-catch: **B1206's candidate (iii) was an MB12 violation of mine**

The standing rule (`vacuity-check-definitions-mb12`, and WORKING_RULES) is that a criterion must be
able to **fail** before it is used. B1206 proposed the λ-term's rank as its cheapest discriminator
without checking whether the discriminating value was reachable — and it was not: rank 1 is
impossible for that coupling, so the test could return only one answer. **Cloud found it; the rule
that would have caught it is my own.** Recorded here rather than absorbed silently, and the
correction to B1206 is a dated addendum, not an edit.

## What moves, and what does not

- **B1206's ledger STANDS at dim 1** — all three of its named candidates are now negative, and the
  space they were drawn from is closed.
- **B1196's CLOSED-PERMANENT verdict on the ℙ³ is HARDENED twice**, and remains overturnable by
  exactly one datum, now named (fork branch (b)).
- **The cosmology ledger's row 4 closes**; the dark sector's remaining routes are named.
- **The observer layer gains a second, disjoint irremovability proof**, and its mechanism is
  identified with B1203's — one κ-identity, two directions.
- **codex R021's theorem holds in a stronger form** than it was stated in, and R020 is
  received-not-adopted with its verification named.
- **Nothing weakens V-3**, and no value is claimed anywhere in this arc.
