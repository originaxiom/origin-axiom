# PREREG — B715 (Frontier 3/4): THE OBJECT'S NATIVE GAUGE SYSTEM — on its own terms (SEALED)

cc banking seat, 2026-07-19. Owner: clarify the object's physics, frontier 3 =
NATIVE GAUGE DYNAMICS. B565 closed the gauge question at the BEHAVIOR level
(vector-like T3; ℤ/11 non-anomalous closed-by-decoupling T1; screening T4;
confining-like strings T5; holonomy in F₄(ℂ)/E₆(ℂ) in NO real form H1 — "compactness
is exactly what the object does not supply"). Frontier 3 = the POSITIVE
characterization (what the object's gauge system IS, on its own terms, not as a
failed SM) + the one open test (T7 Lohitsiri–Tong gaugeability). Structural only;
Gate 5 stands; no SM value.

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **T7 — gaugeability of the object's OWN charge content** (the one B565 discrete
   test not explicitly closed). Is the object's charge content (ℤ/11 + orientation
   ℤ/2 + the B533 types) self-consistently GAUGEABLE (Lohitsiri–Tong: the relevant
   finite-symmetry anomaly/bordism)? Compute the obstruction. Extends B565-T1 (which
   did ℤ/11 alone) to the full content.
2. **The COMPLEX / non-compact positive characterization.** Reverify B565-H1 (the
   geometric holonomy lands in E₆(ℂ)/F₄(ℂ) in NO real form; non-real adjoint traces
   kill all real forms; regular-unipotent meridian kills the compact forms). Then
   characterize the KIND: is the object's native gauge structure inherently COMPLEX
   — i.e. complex Chern–Simons of E₆(ℂ) at the trace field (consistent with the
   object = 3d Euclidean quantum gravity, Witten; the arithmetic-CS meeting B708) —
   rather than a compact Yang–Mills gauge theory?
3. **The ℤ/11 as an arithmetic (non-force) charge.** ℤ/11 = N(φ⁵−1), geometric in
   the 5-fold cover (B566-S5); non-descending (B565-T1). Characterize it as a
   UV-arithmetic charge that does not become an IR force — the object's charge is
   arithmetic bookkeeping, not a gauge force.

## Two-outcome (sealed)
- **OUTCOME A — HIDDEN COMPACT / SM-LIKE STRUCTURE:** the native system has a
  compact real form / a descending force / an SM-like gauge structure (T7 obstructed
  in an SM-relevant way, or a compact slice appears). ⟹ a positive toward physics;
  escalate.
- **OUTCOME B — COMPLEX, NON-COMPACT, VECTOR-LIKE, ARITHMETIC:** the native system is
  inherently complex (E₆(ℂ)/F₄(ℂ), no real/compact form) = complex Chern–Simons, not
  Yang–Mills; T7 gaugeable-but-non-SM; the ℤ/11 is a UV-arithmetic non-descending
  charge. ⟹ the object's gauge physics is its OWN kind (complex/arithmetic CS,
  consistent with 3d gravity + B708), NOT the SM's. [expected: B]

## Verdict template
Expected B: "the object's native gauge system is complex Chern–Simons of E₆(ℂ), not
a compact Yang–Mills — vector-like, non-compact, with an arithmetic (non-force)
ℤ/11 charge." A positive CHARACTERIZATION of the object's own gauge physics.
LAW_MAP placement; nothing to CLAIMS. Fold in any cc2 T7/ℤ11/F₄ material (verified).
