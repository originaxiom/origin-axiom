# B596 — L84's golden case at the operator level: the clock is the cat-map period (prereg)

**Date: 2026-07-14. Registered BEFORE the sweep. Firewall: nothing to
CLAIMS.md; no SM quantities.**

## The claim (registered with three hand-checks)

B587 proved the stage factors through the finite Weil representation of
SL(2,ℤ) — the QUANTUM CAT MAP of the monodromy on the finite torus P/κQ.
The dictionary's operator-level content should then be: **B585's open clock
table (the θ-odd block's multiplicative order) is classical arithmetic — the
period of the cat map, i.e. ord(A₁ mod κ)-data.** Hand-checked before the
sweep: κ = 5: ord(A₁ mod 5) = 10 = clock ✓; κ = 6: 12 = clock ✓;
κ = 7: 8 = clock ✓; κ = 8: ord = 6 vs clock 12 (a factor 2 — metaplectic).

## The cells

- **Q1 (BLIND sweep):** recompute the clock(κ) directly (the B585 machinery)
  for κ = 4..15 alongside ord(A₁ mod κ), ord(A₁ mod 2κ), ord(A₁ mod κ²), and
  the ±I-refined orders (the PSL vs SL distinction); read the law blind.
  Outcome registered: a uniform law clock(κ) = c(κ)·ord(A₁ mod f(κ)) with
  c, f simple (bank it — it resolves B585's open clock law via the classical
  side and constitutes the operator-level dictionary at golden); or no
  uniform law (bank the honest null, the dictionary stays bridge-only).
- **Q2 (word-universality spot check):** the same comparison for the silver
  word at two κ (the law should be word-generic if it is the cat-map period).
- **Q3 (assembly):** the quantization chain written down with each arrow's
  lock: A₁ on ℤ² → mod-κ reduction (B591's fixed-point/homology arithmetic)
  → the Weil rep (B587's proven factorization) → θ-antisymmetrization →
  B_odd. The STATE-level map (dial tangents → odd plane) remains L84-open —
  explicitly not claimed.

## MB13

B587 (the factorization = the cat-map identification), B585 (the clock table,
open law), AP3 (the banked clock theorem ord(A₁ mod N) = π(N)/gcd(π(N),2)),
B211 (Pisano periods), B591 (mod-κ arithmetic), L77/L81(b) (the clock leads —
this cell answers them if Q1 lands).
