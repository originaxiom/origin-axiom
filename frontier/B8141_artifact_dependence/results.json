{
 "distinct_from_the_cost_class": "a COST-class lock works but is never REACHED (B8139); an ARTIFACT-class lock IS reached and reports on the machine rather than the code",
 "extended_2026-08-26_the_manifest_route": {
  "discipline": "the ground truth is the observed failure, not the static scan. The scan was tuned until it reported exactly what was independently verified, and no more.",
  "found_by": "the suite sweep this arc prompted -- test_b646_wave2::test_archive_matches_manifest_except_disclosed FAILS with MISSING .log entries",
  "my_extension_over_reported_twice": {
   "first": "the extended scan flagged NINE manifests with 69 gitignored-missing entries. EIGHT are read by no test and break nothing. Reporting them would be noise -- the same disease as the permanent red this arc exists to cure. Gated on actual readers.",
   "second": "the first reader-attribution matched the manifest's PARENT FOLDER (cc2_packets) and credited test_cc2_r5_adopted.py, which does not read it. Fixed to match the ARC DIRECTORY, which resolves to test_b646_wave2.py -- the test empirically observed failing."
  },
  "so_the_class_has_two_routes": [
   "literal paths in test source (the original finding: 2 tests, 6 artifacts)",
   "manifest-listed paths read at runtime (1 test, 9 artifacts)"
  ],
  "the_second_route": "a test may read a MANIFEST that LISTS gitignored artifacts, so the paths never appear as string literals in any test source. B8141's original scan was STRUCTURALLY BLIND to this.",
  "verified_instance": "frontier/B646_wave2_integration/cc2_packets/ORIGINALS_MANIFEST.txt -- 112 listed, 14 absent, 9 GITIGNORED, read by test_b646_wave2.py"
 },
 "finding": {
  "affected_tests": {
   "test_b1062_bridge.py": 5,
   "test_b1063_refresh.py": 1
  },
  "artifacts": [
   "b1062_v1_v3.log",
   "b1062_v2_block1.log",
   "b1062_v2_block2n.log",
   "b1062_v2_block3.log",
   "b1062_verify_battery.log",
   "refresh_windows.log"
  ],
  "class": "artifact dependence",
  "observed_now": "2 of 5 tests in those files FAIL here with FileNotFoundError; they do not skip",
  "the_harm": "a lock that is permanently red trains readers to ignore failures, which costs more than the lock was ever worth",
  "why_they_can_never_be_committed": ".gitignore line 20 is `*.log`"
 },
 "my_instrument_failed_first": {
  "fix": "match BASENAMES and index every file actually present",
  "hardened": "the script now carries a POSITIVE CONTROL naming two independently-confirmed absent files and EXITS NON-ZERO if it cannot see them. A scan that cannot detect a known-missing file is not evidence of absence.",
  "what": "the first version of the scan matched only whole repo-relative paths written as a SINGLE string literal",
  "what_it_reported": "'0 gitignored' while two such tests were failing three metres away -- a FALSE NEGATIVE of exactly the class this seat keeps cataloguing",
  "why_wrong": "the tests build those paths from fragments (ROOT / 'frontier' / name), so the scan MISSED the very files that motivated it"
 },
 "not_claimed": [
  "that B1062/B1063's results are wrong -- only that their LOCKS cannot verify them on a clean checkout",
  "any remedy -- commit under a non-ignored name, regenerate in-test, or skip explicitly are all judgements for the arcs' owner"
 ],
 "not_in_this_class": {
  "AUDIT_B1076_ONE_NOTATION_DEFECT.md": "absent but NOT gitignored",
  "I_WAS_WRONG_THE_REAL_DEFECT.md": "absent but NOT gitignored",
  "why_legitimate": "this corpus keeps cross-seat relay files UNTRACKED on purpose (the ledger is the register); test_cc3_relay_index_complete.py asserts those NAMES appear in the index text, not that the files exist. Reported separately rather than folded into the finding."
 }
}