# B586 — the Round-3 handoff (chat-1), processed verify-don't-trust (prereg)

**Date: 2026-07-14. Preregistered BEFORE the computation. Firewall: nothing to
CLAIMS.md; no SM quantities in any cell (the B580 binding rule).**

## The handoff

Chat-1 proposes three cells: R3-A (E₆ chirality amplitude "through the six
Sym-blocks"), R3-B (identify the C-twisted mapping torus: census/volume/trace
field/CS), R3-C (the 27-colored antiphase invariant J₂₇ − J₂₇̄). Chat-1's
expectations are explicitly not inputs. Chat-1 has NOT seen B585.

## Frame corrections registered BEFORE computing (the verify-don't-trust pass)

1. **R3-A as framed mixes two decompositions.** The six Sym-blocks (exponents
   {1,4,5,7,8,11}) grade the ADJOINT via the principal sl₂ (B575/B581 —
   classical/homological side). The E₆₂ STAGE's θ-odd space is graded by the
   three conjugate primary pairs (27/27̄, 351′/351̄′, 351/351̄) — dim 3, not 6;
   there is no canonical bijection between exponents and primary pairs. Worse:
   through the principal-sl₂ shadow every rep is self-dual (C = 1), so the
   antiphase vanishes identically in that dictionary — the proposed "sign-law
   unification" is NOT a defined computation. The honest computable core: the
   **three per-pair chirality amplitudes** ⟨u_pair|ρ(A₁)|u_pair⟩ on E₆₂, blind.
2. **R3-B's premise is superseded by B585's naming theorem.** The C-twist (in
   the frame where the antiphase was computed) inserts S² = the central lift:
   the C-twisted mapping torus is the (−A₁)-bundle. |tr(−A₁)| = 3 > 2 ⇒ Sol
   geometry (Thurston's torus-bundle trichotomy, cited) — not hyperbolic, so
   census-name/hyperbolic-volume/CS/trace-field questions are ill-posed as
   asked. Chat-1's alternative construction (the complement doubled along the
   cusp torus) contains an essential torus ⇒ also not hyperbolic (graph
   manifold, two hyperbolic JSJ pieces; cited standard). To verify in-sandbox
   where cheap; the rest cited precisely.
3. **R3-C is answerable structurally.** 4₁ is invertible (its D4 symmetry
   group — banked in-sandbox, B279) and dual color = orientation reversal (RT
   functoriality, cited-standard) ⇒ J₂₇(4₁) = J₂₇̄(4₁) identically. Chat-1's
   cautious branch ("chirality lives in the chord, not the solo") is the
   expected outcome; the "extraordinary" branch would contradict invertibility.
   The full U_q(e₆) 6j computation is out of scope this round (chat-1 concurs);
   register the residual.

## The computation (R3-A core)

Rebuild the C3-gated E₆₂ stage (all gates must reproduce: |W| = 51840, S
unitary/symmetric, S² = the θ-flip permutation, (ST)³ = S², Verlinde
integrality, q-dims, two-word agreement). Then, BLIND:
- the three per-pair diagonal amplitudes ⟨u_p|ρ(A₁)|u_p⟩ and the full 3×3
  odd block in the pair basis (C3's trace 1 and order 4 are gates);
- Z, Z_C, tr_even, tr_odd and the identity tr_odd = ½(Z − Z_C);
- the lift identity on THIS stage: S² = +C here (S₀₀ > 0 convention), so
  Z_C = +tr ρ(S²·word) — record the sign convention against B238's;
- the blind question: does −1/φ appear (per-pair, as a ratio, or not at all)?

## Falsifiers / discipline

- If any C3 gate fails to reproduce, stop — machinery drift.
- The −1/φ question and chat-1's sign-law expectation are CHECKS read after
  the numbers print; either outcome banks.
- MB13: C3/B569 (stage), B584/B585 (mechanism + naming), B581 (torsions),
  B279 (D4 symmetry in-sandbox), B245/B240 (colored formulas) — all found and
  reused; no rediscovery.
