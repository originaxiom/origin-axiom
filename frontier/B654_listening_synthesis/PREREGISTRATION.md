# B654 — THE LISTENING SYNTHESIS (the campaign's harvest against the framework)

**The owner's reorientation applied to the new stratum: no physics
questions; the object's own laws are cross-examined against the banked
pattern-sets, and the object's interactions dictate the questions. All
two-outcome; all in the object's language.**

## The four interaction questions (sealed before any computation)

- **Q-AREA (the area↔anomaly question):** the fig-8's swap chain-defect
  factor is 2 (B647 cell 2: defect = 2·conj Y) and its peripheral
  commutator has van Kampen AREA 2 (banked). The silver's commutator
  has AREA 4 (B649 3b-ii). QUESTION: is the silver's single-slot
  chain defect = **4**·conj(Y) — i.e., is the anomaly factor THE AREA?
  (Two-outcome: factor 4 → the law "defect = area·conj(Y)"; factor 2 →
  the factor is universal and the area is not the mechanism.)
- **Q-BLOCK (the block-count law):** h¹(M; 27) = 3 = the number of
  principal-sl₂ blocks of the 27 (Sym¹⁶⊕Sym⁸⊕Sym⁰), h⁰ = 1 = the
  number of trivial blocks — on BOTH objects. QUESTION: does the
  adjoint module (78, blocks [23,17,15,11,9,3] — SIX blocks, ZERO
  trivial) give h¹(M; 78) = 6 and h⁰ = 0? (A clean discriminating
  test of the mechanism conjecture on a THIRD module.)
- **Q-PERIOD (the period's source):** the melody theorem's
  N₀ = 2⁶·3³·5⁴·7²·11²·19². QUESTION: is N₀ determined by the Weyl
  fixed-point groups' elementary divisors (the jump-law theorem's
  A_w data, banked in f1_certificates.json) — e.g., the lcm of
  {p^(2a)} over all divisors p^a appearing? (Pure arithmetic on
  banked data.)
- **Q-FIELD (the chord-field selector):** what selects ℚ(√−3) (fig-8)
  and ℚ(i) (silver) inside the trace fields? Candidate discriminated
  IN ADVANCE: "the roots-of-unity subfield" predicts ℚ(ζ₈) for the
  silver (ζ₈ ∈ L) — the observed ℚ(ζ₄) already REFUTES it; the
  question becomes: does the WELD intertwiner's field (computable,
  banked data both objects) coincide with the chord field?

## Conventions

All machinery as banked (B637/B647/B649 pipelines; the f1 certificates
JSON); exploratory-exact discipline (every claim an exact identity or
a stated refutation); no physics mapping anywhere in the arc.
