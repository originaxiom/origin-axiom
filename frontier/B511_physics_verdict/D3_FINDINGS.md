# B511/D3 — THE MEASURE DOOR: TERMINAL STATE = **CLOSED (nothing beyond B506/B507)**
**Executed 2026-07-11. Reproducers: `d3_measure.py`, `d3_wild_access.py`. The verdict CLOSES with a
sharper structural sentence than the prereg anticipated — but no new physics.**

| cell | result | gate |
|---|---|---|
| D3.1 stationary measure | concentrates on κ=2 (classical floor) with any decimation; median 2.0 across seeds/sizes | classicalization = B506/B507 restated |
| D3.2 arcsine | occupation one-sided (negative-drift-dominated); NOT driftless-arcsine — re-confirms B506's negative stationary drift | no new law |
| **D3.3 wild accessibility** | **P(κ≈2 classical) ≥ 0.84; P(wild-accessible κ) ≤ 0.10** across all mixes | see below |
| D3.4 anchor translation | κ→2 ⟺ λ→0: the coupling flows to the FREE point under typical history | B506 relaxation in coupling variables; no new scale |

## The sharper sentence (banked as structure, not physics)
The wild "creation" register — the geometry-typed S₄…S₁₁ fields born at IRREDUCIBLE (κ≠2) fixed points
(B499/B500/B510) — is **DYNAMICALLY SUPPRESSED**: a typical history classicalizes (κ→2, where the
torsion factory makes everything ABELIAN/cyclotomic) *before* it can reach the wild loci. So the two
registers are in tension: **creation (wild arithmetic) lives in the uniform-WORD enumeration; typical
HISTORIES birth only abelian/cyclotomic arithmetic.** The wild register has near-zero stationary
measure. This is the classicalization theorem (B506/B507) restated at the field level — a real
sharpening, but strictly within already-banked content.

## DOOR D3 — TERMINAL STATE: CLOSED (exhaustion)
No structure beyond B506/B507. The measure DOES select — it selects the classical/abelian register,
which is the classicalization result, not new physics. T-SCALE fails (λ→0, no forced ratio here).
No T-SIG/T-GEN/T-GAUGE candidate arose. (The tension "dynamics suppresses creation" is banked as a
structural observation, firewalled; it touches no value and opens no B398.)
