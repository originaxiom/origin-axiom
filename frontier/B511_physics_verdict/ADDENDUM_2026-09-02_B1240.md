# ADDENDUM 2026-09-02 (B1240) — D3.3 rebanked on the trace map at 200 bits; the committed percentiles are a precision artifact

**Source of the finding:** the physics-seat evaluation branch's R48 (B511 wild access), verified here by an
independent re-implementation — not by re-running the committed scripts, which NaN on numpy 2.4.6.

**What was recomputed** (`frontier/B1240_belt_closure_and_fc_harvest/verification/b511_d3_tracemap.py`): the same
model (Haar pairs in SU(2); per step M: (A,B)→(AB,BA) with prob mix[0], D: (A,B)→(A²,B²) with prob mix[1], else
F: (A,B)→(AB,A); 3000 steps; κ = x²+y²+z²−xyz−2 on (tr A, tr B, tr AB); classical |κ−2|<0.05; wild |κ−2|>0.5 with
−2≤κ≤2) rewritten on trace coordinates — F: (x,y,z)→(z,x,xz−y); M: →(z,z,w); D: →(x²−2,y²−2,w), w = xyz−x²−y²+2 —
each map verified against explicit 2×2 matrices (200 trials, 1e−15) before use; gmpy2 mpfr at 200 bits; N=400 per
seed, seeds 11 and 23; the F-only control must conserve κ exactly.

| mix | classical (s11/s23) | wild (s11/s23) | escaped |
|---|---|---|---|
| M10/D10/F80 | 0.927 / 0.920 | 0.028 / 0.035 | 0 / 0 |
| M20/F80 | 0.863 / 0.858 | 0.085 / 0.087 | 0 / 0 |
| D20/F80 | 0.943 / 0.940 | 0.020 / 0.020 | 0 / 0 |
| F-only control | 0.022 / 0.028 | 0.782 / 0.762 | 0 / 0 (κ drift ≤ 1.3e−58) |

The SAME code at 53 bits escapes 112–171 of 400 per run with classical 0.52–0.64: the committed `d3_results.json`
percentiles "2.0, 2.0, 2.0" are what a double-precision run reports after the escapes are dropped. fc's dps60 run
(0.927 / 0.967 / 0.850) concurs.

**Disposition:** D3.3's verdict (classical-dominated, wild-accessible ≤ 0.10) STANDS and is REBANKED on the
200-bit trace-map computation; D3.1's medians stand. The committed reproducers are marked stale in D3_FINDINGS.md.
No physics changes: the coupling still flows to the free point under typical history (D3.4).
