# B511 — THE PHYSICS VERDICT CAMPAIGN
### The terminal exploration: every named route walked to its end — physics through the airlock, or the idea killed by exhaustion of its own doors. Committed before execution.

## D0 — THE DEFINITION GATE (binding before anything runs)
**"Physics" is defined NOW, operationally:** a structure that is (i) FORCED (no tuning, no choices),
(ii) UNSOUGHT (preregistered target or blind), (iii) CONTROL-PASSING (foreign controls + adversarial
null), (iv) matching MEASURED physics at structure level beyond chance (statistics gate with committed
base rates), reaching daylight only through B398 (owner present). The four structural targets — none is
a value: **T-SIG** Lorentzian (3,1) signature · **T-GAUGE** nonabelian gauge-as-gauge in 4d ·
**T-GEN** forced multiplicity 3 · **T-SCALE** one forced dimensionful ratio.
**"Dead" is defined NOW:** every door below reaches CLOSED (theorem or exhausted enumeration) with no
door OPEN. Then the verdict banks: *the object's grammar does not produce physics through any of its
own doors* — final, honest, and itself the capstone theorem. Doors may also end TOOL-BLOCKED (named,
priced — not a verdict). **No door may end "promising."**

## The six doors (each: committed question · computation · kill condition)
**D1 — THE SPECTRAL DOOR (from the anchor, measured physics already).** κ−2=4λ² puts the program inside
quasicrystal physics — but 1d. Question: does the grammar force ≥3-dimensional aperiodic structure
(the monoid on F₃/F₄; product hulls; the S-adic tower's dimension growth)? Computation: the rank-n
monoid's classification (does U1/U2 persist?), the dimension spectrum of typical words. KILL: the
grammar is dimensionally sterile — everything stays effectively 1d/toral.
**D2 — THE TIME-TOWER DOOR (the functor iterated).** The spacetime functor makes 3-geometry from words
(one time layer). Question: does the functor's SECOND application (time over spacetime — mapping tori
of the mapping-torus reps; words acting on word-histories) produce 4-dimensional objects with causal/
Lorentzian structure (T-SIG)? Computation: cd/geometry of the iterated extensions; the signature of the
natural pairing at irreducible fixed data. KILL: iteration only grows cd without geometry (the
geometry-less-sibling pattern repeats forever).
**D3 — THE MEASURE DOOR (the universe-word, sharpened).** B506/B507 already produced the campaign's
first physics-shaped structure (hierarchy-without-tuning; the pointer attractor). Question: do the
macro-observables of TYPICAL histories (stationary-measure spectra, gap statistics, recurrence laws,
the arcsine prediction) match measured structural signatures (aperiodic matter now; cosmological
form-level second) beyond the already-explained? Computation: B501 executed in full with the anchor
dictionary. KILL: nothing beyond B506/B507's already-banked content.
**D4 — THE GAUGE DOOR (Langlands-shaped; the no-go's one open flank).** T-NOGO-DGG closed 3d–3d for
the OBJECT. Not closed: the GRAMMAR's wild register produces nonabelian GALOIS groups (S₄…S₁₁ +
intermediates). Question: Galois-to-gauge is the Langlands shape — does the wild census organize
functorially (verb-pattern → group family) into any reductive structure, or is it unstructured
wildness? Computation: the completed wild census (W1–W5) + the group-sequence functoriality test +
the lit-gate (geometric Langlands adjacency — cite, never claim). KILL: the wild register is
group-generic (no verb-functorial structure beyond degree growth).
**D5 — THE GENERATION DOOR (the last 3).** The only FORCED 3 in the program: the ℤ/3 torsion locus =
decimation's fixed line = the attractor sector's discrete core (the β-zero pointer leaf is ℤ/6 ⊃ ℤ/3).
Question: does the measurement-surviving sector (the ℤ/3–ℤ/6 torsion) carry a forced THREEFOLD
structure in its representation theory (the three cyclic arrangements? the three 2-isogenies of 40a?)
that is generation-shaped (T-GEN) rather than coincidence? Computation: the full rep theory of the
attractor sector under the verbs; the triplication test with foreign controls (does ℤ/4, ℤ/5 data
show the same pattern? then it's generic — kill). KILL: the 3 is unforced or control-matched.
**D6 — THE SIGNATURE DOOR.** The compact/noncompact split of the character variety (SU(2) vs SL(2,ℝ):
capture vs escape, B498 Q5) is a signature split the program has never read as one. Question: does the
verb dynamics FORCE a (3,1)-shaped asymmetry (three compact-like directions, one escaping) anywhere —
in the stationary measure's support geometry, in the iterated functor (D2), in the quaternion algebra
(the 2-adic ramification that survived every test)? KILL: the split stays (1,1)/(2,2)-generic.

## Sequencing (each door independent; any order; every terminal state banked per-door)
D0 gates first (one PR). D3+D5 are cheapest (running machinery). D4 rides the wild census. D1/D2 are
the heavy new computations. D6 threads through all. The campaign ends when the LAST door reaches its
terminal state — not before, not after. Estimated: the program's remaining lifetime; that is the point.

## Governance
Everything inherits: prereg-per-cell, foreign controls, adversarial nulls, statistics gates with
committed base rates (LMFDB/measured-physics catalogs), the 283/40a-class airlocks armed, HELD(value-
matching) absolute, B398 the only exit to daylight, verbs/named-terms banked with invariants computed
(the methods protocol). The lit-gates run FIRST per door (the web window is live). Failure notes and
kills bank with their data. No door closes on prose.
