# B624 — the observable bridge: the odd trace IS the Weil coset assembly (queue item 2 CLOSED)

**Status: banked (frontier). Campaign round-2 cell G; the definitional
identity spot-checked in-loop (κ=4); the bridge 6/6 per the seat.
Reproducers step1–step5.**

**THE RECONCILIATION (derived + verified, 0 exceptions at κ = 4..24 ∩ 4ℤ):**
the B601/B610 odd hearing trace and the B620/B621 Weil-coset decomposition
are the SAME twelve-term Gauss assembly. The charge conjugation Cm is
itself one of the twelve group elements — the signed Weyl reflection
(−1, s₁s₂s₁) — so trace(B_odd) = −½[tr W − tr(CmW)] (definitional) picks
up TWO copies of the reflection terms: the (−1)-coset copy is √3-locked
at EVERY 4|κ level; the (+1)-coset copy locks only when additionally
3|κ. The field-bearing closed form (1−√3)e^{iπ/3} appears exactly when
both copies align: 12|κ — resolving B618's {12, 24} vs B621's 4|κ gate
with no contradiction. The bridge constant is a fixed framing phase ω⁻¹
(deviation < 1e−13 across all tested κ):
trace(B_odd) = −(ω⁻¹/12)·Σ_{±,w} sign(w)·term(±, w). Verified 6/6 with
full twelve-term tables. Residual noted by the seat: the κ = 28..40
generality sweep was resource-killed and is NOT claimed.
