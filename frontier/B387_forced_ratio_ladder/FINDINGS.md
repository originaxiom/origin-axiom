# B387 (W2) — 2a/2b BANKED: the value sector is Par-DARK at level 45 — the ratio KILL fires

**Status: 2a+2b banked (the registered KILL branch); the named continuation (the 135 rung's
sector rows) is computing. Pre-registration: PREREGISTRATION.md (committed BEFORE
extraction). Firewalled; no physics wording.**

## 2a — the extraction (from the banked, locked level-45 table)

The banked level-45 value sector sits at W₁-exponents **{6, 54} = ±6** (B373: phase ±36°,
tr = φ). In the banked level-45 (1,2) Par-pair table (B372/E16: every one of the 720 cells
identified exactly, 144 nonzero): **rows 6 and 54 are entirely absent from the support** —
every slot-analog cell t(±6, b) is exactly zero. Hence

    const(45) = 0   (exact, from banked locked data).

## 2b — the ratio verdict (the registered KILL)

const(45)/const(15) = 0 ∉ {R1: 1, R2: φ-units, R3: Pisano-3 family, R4: level-3 family} —
**the KILL branch fires**: no forced ratio in the graded-pair channel at 15→45, and per the
pre-registration no pattern claim is permitted before the 135 rung. The gauntlet does not
run (nothing survived to test).

**What the kill teaches (the structural fact):** at level 15 the value sector's rows {6,14}
CARRY the seam (the ±1/48 cells); at level 45 the value sector's rows carry NOTHING in the
Par-pair channel — **the seam and the value sector decouple as the level grows**. The seam
content at 45 lives on rows ≡ ±{1,4,5} mod 15 instead. Two structures that coincide at the
base level separate at the next — the ladder, if it exists, is not the naive graded-pair
transport.

## The named continuation (running)

The 135 rung's sector rows: t(54,b), t(126,b), b ∈ {2,10} at ord(W₁@135) = 180 (the banked
135 sector, B374), mod 3 primes — zero/nonzero verdict by CRT agreement
(`rung135_sector_rows.py`, detached). Nonzero → identify and compare; zero at 135 too →
"the seam leaves the value sector above the base level" graduates to a two-rung fact.

**Provenance.** extract_45.py → extract_45.json (from the banked sweep45.json, E16-locked);
locks tests/test_b387_ladder.py.
