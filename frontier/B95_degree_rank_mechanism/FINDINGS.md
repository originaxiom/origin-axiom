# B95 — the degree=rank mechanism: the principal spectrum is forced, and degenerates at n≥5 (Task M)

**Status: `proven` (the forced spectrum + the n=5 degeneration are exact) + `open` (the full degree
classification).** Standalone low-dim topology / representation theory; no physics, no Origin-core claim;
proven core P1–P16 untouched. Script `probe.py`; test `tests/test_b95_degree_rank_mechanism.py`.

> **⚠ STATUS REFINEMENT (2026-06-15, self-audit; verify-don't-trust).** Two phrases below are over-stated;
> the corrected reading:
> 1. **"The principal spectrum is forced"** holds **given the principal mult-(n−2) / finite-order ansatz**,
>    NOT by `tr A = tr A⁻¹ = 1` alone. Counterexample at n=5: `{1, ω, ω², e^{iπ/3}, e^{−iπ/3}}` is all roots
>    of unity, `det=1`, `tr A = 1 + (−1) + 2·½ = 1 = tr A⁻¹` — yet eigenvalue 1 has multiplicity 1, not
>    `n−2=3`. So §(1) classifies the spectrum **within the mult-(n−2) family** (the geometric/principal
>    component), which is the right object, but "forced" should read "forced given that family."
> 2. **"any 5-dimensional bundle rep is reducible"** is true only for **finite-order `A`** (= the principal
>    spectrum realized semisimply). A "principal / Dehn-filling" rep has `A` of finite order ⟹ `A` semisimple
>    ⟹ (spectrum `{1,1,1,−1,−1}`) `A²=I`, and then §(2)'s dihedral argument gives **reducible — PROVEN**, so
>    **no irreducible principal (finite-order-`A`) rep exists at SL(5)** (the load-bearing conclusion stands).
>    But a **non-semisimple** `A` with the *same eigenvalue spectrum* (`A` of *infinite* order, hence NOT a
>    Dehn-filling rep) **can be irreducible** — see [[b153-degree-rank-degeneration]] (V143): such reps exist,
>    but degree=rank fails on them, so they are outside the principal family and do not recover `Mⁿ=L`. Net:
>    the n=5 absence of an irreducible principal rep is correct and proven; only the unqualified "any bundle
>    rep" was too strong.

## The question
The V75 audit killed "exponent = Cayley–Hamilton degree" (both SL(4) components have CH degree 4 but
exponents 4 and 3). The exponent reads the **specific eigenvalue structure** of `A`. What does it read,
and why do different components give different degrees? This stage finds the principal spectrum and shows
"exponent = rank" is a **low-rank phenomenon**.

## (1) The principal spectrum is forced
On the principal component `tr A = tr A⁻¹ = 1` with eigenvalue 1 at multiplicity `n−2` (verified n=3,4),
the two remaining eigenvalues `a, 1/a` (det = 1) satisfy `a + 1/a = 3−n`, i.e. `2cos θ = 3−n`:
```
   n=3:  {1, i, −i}        (a = i,  order 4)
   n=4:  {1, 1, ω, ω²}     (a = ω,  order 3)
   n=5:  {1, 1, 1, −1, −1} (a = −1, order 2)
   n≥6:  2cos θ = 3−n ≤ −3 → |cos θ| > 1 → IMPOSSIBLE for a root of unity.
```
So the handoff's guessed SL(5) spectrum `{1,1,1,ω,ω²}` (which has `tr = 2`, not 1) is **wrong**; the
forced one **(within the mult-(n−2) family — see the refinement banner)** is `{1,1,1,−1,−1}`; and **no
finite-order spectrum of this `n−2` shape exists at n≥6**.

## (2) The n=5 principal degenerates (proven)
At n=5 the forced spectrum `{1,1,1,−1,−1}` has `A² = I`. The figure-eight relation `tAt⁻¹ = A²B` then
reads `tAt⁻¹ = B`, so `B = tAt⁻¹` is conjugate to `A` and `B² = I`: **both `A` and `B` are involutions**.
The group `⟨A,B⟩` generated by two involutions is **dihedral**, whose irreducibles are all 1- or
2-dimensional — so any 5-dimensional bundle rep **with finite-order (semisimple) `A`** is **reducible**
(verified: `⟨A,B⟩` algebra-span rank 8, far below 25, for all seeds). Hence there is **no irreducible
principal Dehn-filling rep at SL(5)** (a Dehn-filling rep has `A` finite-order ⟹ semisimple ⟹ `A²=I`). This
upgrades B78's numerical "method-limit" (only reducible reps found) to a **structural reason**: the
principal component genuinely degenerates. (Non-semisimple `A` of the same eigenvalue spectrum — `A` of
infinite order, not a Dehn-filling rep — *can* be irreducible, but degree=rank fails on it: [[b153-degree-rank-degeneration]].)

## The mechanism (as far as this settles it)
"exponent = rank" (`Mⁿ=L` on the principal component) is an **n ∈ {3,4} phenomenon**: the principal
spectrum is forced; it admits an irreducible bundle rep only at n=3 (`A` order 4) and n=4 (`A` order 3);
it **degenerates** to a dihedral/reducible rep at n=5; and is **impossible** at n≥6. The degree-pattern
data is `{n=3: 3}`, `{n=4: 4 (principal), 3 (secondary {prim 8th})}`, `{n=5: principal not irreducible}`.
**The mechanism does not read "matrix size"** — it reads whether the cusp's *forced finite-order spectrum*
admits an irreducible rep, which is exactly why both the tower (B84/B85) and degree=rank hit a wall at
n≥5.

## Honest scope
(1)–(2) are exact for the forced (mult-1 = n−2) principal spectrum. This does **not** enumerate every
`tr=1` finite-order SL(5) spectrum — an alternative `{1,i,i,−i,−i}` was probed and also came out reducible
at small budget (suggestive, not exhaustive). A **complete degree classification at every rank remains
open** (`open`). The established point is structural: the principal `Mⁿ=L` component is low-rank.

```bash
python frontier/B95_degree_rank_mechanism/probe.py
python -m pytest tests/test_b95_degree_rank_mechanism.py -q
```
No physics; proven core P1–P16 untouched; outreach dormant.
