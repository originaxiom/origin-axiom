# B778 PREREGISTRATION — THE CLEANUP WAVE ("fix everything", owner-directed 2026-07-24)

*Sealed before compute. Sweeps every genuinely-open residual across the session (the
carry→fix chains all closed already; these 7 are the true remainder). Same binding
method (exact preferred, in-cell fact, adversarial verify, Gate 5/5-Q, no forced
result, the B774 chord discipline). Launches after B776 clears (one heavy workflow at
a time). Cells write under cells/CL-*/.*

## The 7 residuals

- **CL-W5139** (cosmetic): W5-139's genus(A₃)=41 result is VERIFIED — strip the dead/
  unreachable RESOLVED-B verdict branch the verifier flagged, re-run to confirm the
  verdict block now emits RESOLVED-A cleanly with no dead code. Do NOT re-derive the
  genus (it stands). Outcome: clean verdict block, genus=41 reproduced => RESOLVED-A.
- **CL-W5100** (cosmetic): W5-100's D₀ convergence (golden 0.4562, silver 0.4645) is
  VERIFIED — strip the decorative par_split_diff chord cross-check that promises a
  θ-odd analog it doesn't deliver, re-run to confirm the convergence stands without
  the decorative claim. Outcome: clean cell, D₀ reproduced => RESOLVED-A.
- **CL-W4115** (hygiene + re-verify): W4-115c (B773 chord-carry) claimed the
  cover-torsion/charge wall HARDENS at the non-abelian level but embedded a FABRICATED
  "adjoint 7,815..." content string. Strip the fabrication and RE-VERIFY the
  wall-hardening on real computed data. Outcome: wall hardens on real data => RESOLVED-B
  (the negative confirmed) / it does not => re-open.
- **CL-LATIN** (re-run the real question): P2W2-LATIN was downgraded — only the ℤ/3
  current row of the hearing-matrix Latin square is FORCED; the other two rows'
  3-valuedness was MEASURED. Re-run targeting ONLY the non-current 2×2 block: is its
  3-valuedness FORCED by any structural reason (a second fusion constraint, a Galois
  argument), or is it genuinely a measured/free datum? Outcome: forced (the full Latin
  square is a theorem after all) => RESOLVED-A / genuinely measured (the Latin square
  is exact + current-row-explained but NOT fully forced — the honest downgrade stands)
  => RESOLVED-B.
- **CL-DARKHYP** (re-run the dropped cell): the N=p² dark-hyperbola law (spectrum
  {0,1,√p,p}, exact counts across 7 levels) — the all-p SYMBOLIC proof (dropped on the
  output cap in Wave 2). Outcome: THEOREM / a counterexample level => RESOLVED-B / UNRES.
- **CL-H133** (cc3's new chord-suspect): H133 (Z₄ = Tr ρ(A₁) = 0 at E₆ levels 1,2,3;
  "dies at its own registered gate" per B600) — cc3 flagged Z₄=0 as possibly TRACE-BLIND
  (an even/odd cancellation). Chord-check it: isolate the θ-odd readout of Z₄ at the
  levels; is Z₄=0 a genuine structural zero or an even=odd cancellation (the W4-304
  signature)? Outcome: genuine zero (H133's death HARDENS at the chord level) =>
  RESOLVED-B / θ-odd sector nonzero (H133 lights up — a hidden structure) => RESOLVED-A
  (independently reproduced + hand-verified before banking).
- **CL-W3082** (document as EXTERNAL): W3-082c is genuinely NEEDS-SPECIALIST (the
  arithmetic-CS bridge to Kim's H³ action is uncomputed, self-flagged NEEDS-SPECIALIST
  in LAW_MAP). This cell does NOT attempt the bridge; it CONFIRMS the honest status —
  the trace-negative does NOT lift to a loaded wall, the loaded positive is NOT
  established — and reframes to the supported class-group fact Sylow₂(Cl(−8899))=ℤ/2.
  Outcome: EXTERNAL boundary confirmed with the class-group fact banked => RESOLVED-B.

## Method: binding B771/B775 block + the B774 chord discipline (a "θ-odd/chord" object
must be genuinely non-abelian, not a relabeled trace invariant). No forced result;
Gate 5/5-Q; nothing to CLAIMS; the one-number pin untouched. Adversarial verify each.
