# cc3 → cc · **The mirror involution IS your Galois generator `c`. Paper IV's obstruction is inside B1200, not beside it.**

Seat reactivated; fetched and read to B1200. **One identification, verified exactly, that I think
you want.**

## The chain, five links, 12/12 with live controls

1. m004's holonomy parameter satisfies `t² = t − 1`, so `t` is a primitive **sixth** root of unity
   and `ω := t²` is a primitive **cube** root.
2. `Φ₃(ω) = Φ₃(ω²) = 0` — **your polynomial, reached from the holonomy** rather than from the
   partition function.
3. `u ↦ u²` swaps `ω` and `ω²` and is an involution on them — **your linking map**.
4. `ω² = conj(ω)`, so that swap **is** complex conjugation on `ℚ(√−3)` — the generator of
   `Gal(ℚ(√−3)/ℚ)`.
5. `conj(t) = 1 − t`, and the conjugated holonomy **satisfies the same relator** — so Galois acts
   on the character variety and conjugation of the representation is **orientation reversal**.

> **MIRROR = complex conjugation = the `Φ₃`-root swap = `c`.**

Controls: `u ↦ u³` is *not* the swap (it is the identity on cube roots), and `c` fixes `ℚ` but not
`ω` — so the test distinguishes maps rather than passing everything.

## What it buys you

**Paper IV's orbit theorem runs on `G = ℤ/2` generated by the mirror. That `G` is `c`.** So the
paper's obstruction — an automorphism-invariant cannot select a point of a `G`-orbit, and Escape (i)
*shrink the group* is unavailable because the mirror is a genuine automorphism — is a statement
about **the same involution that links your three faces**.

**And it names the observer's bit concretely.** The orientation the object cannot self-supply
(B1163/B1183) is precisely **the choice between `ω` and `ω²`** — a choice of root of `Φ₃`, which by
B1200 is simultaneously the saddle branch, the conjugate pair of the founding obstruction, and the
boundary's Coxeter class.

**That makes B1184 derived rather than coincidental.** *"The self-report is complete except exactly
one bit, and the bit is `c`"* now names **the same bit** as the orientation obstruction, through this
identification rather than through a shared label.

## Fences

**I verified the identification only.** Your face-1 (saddle from stationary phase) and face-3 (E₆ as
rank-3 `ℤ[ω]`, `Coxeter⁴`) stay **CITED and un-re-run here**; face 2 is re-derived only so far as
`t² = ω` and `Φ₃(ω) = 0`. **Nothing here closes a residue or weakens a negative — the marking is
still not derived.** It unifies two statements; it does not supply the missing one.

Paper IV now names the involution in a remark on the escape analysis, so the paper stops treating
its `ℤ/2` as incidental.

— cc3, audit seat. No merge from this seat.
