# B773 PREREGISTRATION — THE CHORD-LEVEL RE-COMPUTATION (B772's action)

*Sealed before any cell computes. B772 found 4 banked negatives computed in the
trace/abelianized projection that is blind (B766 θ-T6) to the θ-odd/chord sector
where the object's live structure is claimed to sit. This wave re-computes each at
the chord level. THE DECISIVE QUESTION per cell: does the chord-level analog stay
EMPTY (the wall HARDENS — trace-absence was structural absence after all) or LIGHT
UP (the trace projection was HIDING structure — a positive the census missed)?*

## Cells (each: HARDENS = RESOLVED-B / LIGHTS-UP = RESOLVED-A / honest UNRESOLVED)

- **W3-082c** (arithmetic-CS, p=809): the mod-2 (H¹ Kummer/linking) computation is
  correct but the "Z/4 lift degenerates" headline is asserted by naming-continuity.
  Close the bridge lemma to B708's H¹-TORSOR ACTION level (the loaded CS invariant),
  or reframe to the fully-supported class-group claim; AND sweep past the single
  (11,809) point to the tower's other charge primes. Outcome: the CS-action-level
  gap genuinely degenerates (wall) / a non-degenerate lift appears (positive).
- **W4-304c** (level-45 pair-sector): the whole pipeline is trace-level (par_trace
  twisted-trace DFT). Recompute the NON-CYCLIC / chord-level analog at level 45 for
  exponents {6,54}; extend the level-family past 2 rungs (45,135); apply the
  ~60%-of-rows-zero base-rate correction the arc never performed. Outcome: chord-level
  sector also identically zero (wall) / nonzero chord-sector rows (positive).
- **W3-067c** (classical field-fusion): the classical side was invariant-trace-field;
  the quantum comparator is framing/Dehn-twist (chord) data. Test whether traces of
  the specific TWIST-CURVES entering the R/L word fuse (the chord-level analog of the
  mod-4 quantum twist); convert the PSLQ bounded-height (≤1e8) to exact symbolic
  trace-field certification on the 2-block cases. Outcome: chord-level fusion also
  absent (K016 wall hardens) / chord-level fusion present (positive).
- **W4-115c** (cover-torsion vs charge tower): both towers were trace/abelianized
  (H₁ of cyclic cover vs det(I−Mₙ)). Test a NON-ABELIAN / twisted (chord-level)
  refinement of the cover-torsion tower before "no law"; fold in the cheap mod-p
  determinant method to extend the charge side to n≥10–15 (resolution earned, not the
  2-point lucky escape). Outcome: no law at chord level either (wall) / a chord-level
  law relating the towers (positive).

## Plus one defect-fix cell (B772 → E2)
- **OI-146-fix**: rerun B332's PART-1a with the Hurwitz-zeta closed-form L(2,χ₋₃) +
  a real SnapPy vol(m004) call (both correct in B734/cc3_verification/b734_fix2.py),
  replacing the vol/vol=12-by-construction tautology. Outcome: PART-1a's congruence-
  level certificate earned in-cell (non-vacuous) / the certificate genuinely fails
  (then the index=10 headline rests on the orbit-stabilizer route alone, stated).

## Method (binding)
Every cell computes at the θ-odd / chord / non-abelian level its target lives at,
NOT the trace projection; the discriminating fact in-cell; a POSITIVE (LIGHTS-UP)
must be independently reproduced before it is believed (a false chord-positive is
as bad as a false trace-negative); UNRESOLVED honest. Gate 5/5-Q; nothing to CLAIMS.
Each cell writes compute.py + output.txt under cells/<id>/; every verdict
adversarially verified.
