# B897 — the sealed cell decides: OUTCOME A — G₂₀ organizes the 27 GENERATION-SHAPED (M2)

**Date:** 2026-08-05 · **Seat:** cc (banking) · **Prereg:** sealed BEFORE compute
(`e293f095…`, committed `0e1cdf4c` before any result existed; seal verified
unbroken at banking by `tests/test_b897_g20.py`).

## The sealed question

Does G₂₀ = su(3)_c ⊕ su(3)′ ⊕ su(2)′ ⊕ u(1) — the algebra generated by all
three frames' breakings at once — organize the 27's matter with
**generation-shaped** multiplet structure (su(3)′ as native flavor replicating
SM-type multiplets), or not?

## The computation (two primes, all gates)

At q = 40123 and q = 40693 (towers for ALL THREE roots derived per prime;
40693's ideal-split needed the widened retry — a computational note, not an
evidential one): G₂₀ rebuilt from the three walls (dim 20, derived 19 —
gates); color centralizer in the derived algebra dim 11, split su(3)′ ⊕
su(2)′ (8 + 3 — gates); intrinsic-Killing Casimirs of color/flavor/weak on
the 27; joint (C_c, C_f) blocks refined by C_w.

**The block table — IDENTICAL signature at both primes (tiles 27 = 9+9+9):**

| block | dim | su(2)′ refinement | reading |
|-------|-----|-------------------|---------|
| (1_c, 3_f)  | 9 | **3 + 6** (w-singlet triplet ⊕ w-doublet triplet) | lepton-pattern: L-doublet flavor-triplet ⊕ singlet flavor-triplet |
| (3_c, 1_f)  | 9 | 3 + 6 | the flavor-unreplicated color sector (the trichotomy's third leg) |
| (3_c, 3_f)  | 9 | **single value (trivial)** | one color-triplet type × three, bound as ONE flavor multiplet |

Casimir residues lift to the same exact rationals at both primes:
**C_c = C_f = 4/9** on their triplet sectors (= z6c's banked color spectrum —
the two su(3)'s act as exact twins on the 27) and **C_w = 3/8** on the
doublet pieces.

## The sealed criteria, evaluated verbatim

**OUTCOME A requires:** a joint block with nontrivial su(3)′ action of
dimension m = 3·d, d ∈ {1,3}, one color type as ONE joint isotypic; the
refinement clause asks the copies to carry the same su(2)′ type.

- The **(3_c, 3_f)** block: f ≠ 0, dim 9 = 3·3, one color type, one joint
  isotypic, su(2)′ single-valued. **A satisfied directly.**
- The **(1_c, 3_f)** block: under the su(2)′ refinement it is exactly the
  disclosed prior's decision branch — and each refined piece (dim 3 = 3·1
  w-singlet triplet; dim 6 w-doublet triplet) carries a single su(2)′ type.
  **A satisfied again.**
- UNSTABLE not triggered: the two primes agree on the full block signature;
  multiplicities tile 27 at both.

> **VERDICT (sealed branch A binds): the all-perspectives algebra organizes
> the 27 generation-shaped — su(3)′ is a native flavor symmetry that
> replicates fixed color ⊗ su(2)′ types into flavor triplets, in the
> off-diagonal trichotomy pattern, with the lepton-sector doublet/singlet
> split appearing in the color-singlet block.**

The disclosed prior (off-diagonal trichotomy; A via the singlet sector)
was directionally right — and A holds through the (3,3) block even more
directly than the prior's route.

## What this does NOT decide (pre-stated in the seal, unchanged)

Mechanism-hood of generations: this is replication at the algebra level.
The solo seat's §5 fence stands.

## Files

- `PREREGISTRATION.md` (sealed), `g20_decompose.py` → `results.json`
- Locks: `tests/test_b897_g20.py` (seal integrity + the block tables)

## Depends on

B854, B866, B883 (the 27), B892 (G₂₀ verified), B895 (color exact); the
z6c color generators.
