# B1171 — THE SEAM HARVEST: five cross-seat results landed on main in one sitting (the lose-nothing sweep's "one harvest sitting", executed)

**Status: banked (frontier). Verdict OPEN** — a batched integrate-don't-merge harvest (the B1158 pattern),
closing the sweep's urgent-reclaim #2. All five components verified on this bench before banking; cc3, cloud,
and codex credited throughout. `verification/reproduce.sh` → `REPRODUCES`. Gate 5 clean.

## (1) cc3's B8144 — the adelic split gets its MECHANISM (was: a description; now: a derivation)

cc3's follow-up (a6c35d08; **no lock on their branch — main supplies it**) derives *why* the observer splits
adelically: **the orbit-theorem escape pair.** A G-invariant can't select a point of a G-orbit; exactly two
escapes — shrink the group, or add non-invariant structure. The **archimedean half (orientation)** has
escape-1 unavailable (B1163: the mirror is a genuine automorphism — no smaller group) → observer-paid **full
bit**. The **finite half (VEV)** has escape-1 available (F₄(ℝ)→F₄(ℤ), finite orbits — Krutelevich, cited) →
a **finite label**. Own-verified against every banked cost (orientation ✓, scale ✓, VEV ✓, and the *paid*
spin bit is consistent — its choice-space collapses, no orbit survives). **Two preregistered predictions**
registered as standing typing tests (archimedean-iff-no-arithmetic-orbit-breaker; finite-always-cheaper).
The time's-arrow instrument queued, not run. → `frontier/B1164_cc_masterplan/ADDENDUM_adelic_mechanism.md`.

## (2) cc3's B8145 — L171 CLOSED (MOOD), the reason moved

Their 8/8 verification of B8093's CLPW typing against the source (eq. 24 Π=Θ(q); eq. 25 Tr 1=1 verbatim;
H_obs=q≥0 on L²(ℝ₊); ρ_max=1) — **cc independently spot-verified 4/4 by its own fetch of the paper**. MOOD
(consistent-not-distinctive) stands *with the reason relocated*: the successor (arXiv 2308.03663) drops the
ergodicity engine, so the original disanalogy is CLPW-scoped — but the successor's engine is
KMS-traciality, and **B721 places our tracial state complementary to, not inside, the KMS torsor**. Banks
exactly per the L171 row's own pre-typing. The stale WebFetch action retired. → OPEN_LEADS L171 row closed.

## (3) cc3's B8146 — L173's precision column NEGATIVE; the prereg re-posed as a COUNT

The banked prediction is a **count** (ρ ↦ edge content, 5-vs-6 at ρ=α), not an energy — so R6's "what
energy resolution is standard" was mis-posed. The anchor (1403.7124) is *"a demonstration paper, not
metrology"* (13–28 waveguides, no error bars, qualitative mode-ID). **The program supplies the KNOB (the
phason), not the READOUT.** Re-posed by **addendum-beside** (the sealed spec untouched, per its own R6
amendability clause): commission a **mode-count observable in a labelled gap** with enough sites to separate
5 from 6. cc3's B8094-results mutation NOT ported (their branch's cross-assert; main carries no B8094). →
`docs/EDGE_PREREG_SPEC_ADDENDUM_B8146.md` + SEAL_LEDGER row + L173 row.

## (4) cloud's memos 80/82 — both certs BYTE-IDENTICAL on this bench; L186 registered

Dep closure extracted (`twisted_double.py` + `paper/verify/check_charge_bracket.py`; `e7_ladder.py`), both
certs run clean and **byte-identical** to committed outputs. **Memo 80 (THE TEXTURE):** the unique invariant
coupling reproduces the full E₆ 27³ sector table (45/45), and **the up-type Yukawa SHAPE EXISTS (6 nonzero
entries)** — the object's kinematics *allows* what the heterotic dressing *forbids* — **so SEAM-Y's μ_u=0 is
the dressing's, not the object's: the two walls are proven different facts** (memo 75's "rhyme" sharpened to
a mechanism disagreement, exactly where link A's import bites). Their Y-conservation gate (machine-caught
relabeling slip) noted as good practice. **Memo 82 (FAMILY RANK):** in the E8 family triplet
(possibility-space fence intact), **810/810** family matrices have **rank exactly 2**, kernel = the Higgs's
own family — one exactly-massless family per coupling forced and dimensionless; *which* family priced to
the observer/vacuum. The σ-gauge trap hit twice, caught twice (their filed error). **→ L186 registered:**
are the three Yukawa-suppression mechanisms (SEAM-Y / live up-shape / rank-2 hole) one or three?

## (5) codex R015/R016 — two grade/scope adoptions + two asks

**R015's warning adopted** → `B1153/ADDENDUM_grade_note_R015.md`: the "exact 2-fold GUE" headline is bounded
— *surmise-level model identification* (D=0.024, preregistered gate), not a theorem; "exact" refers to the
factor structure ζ_K=ζ·L(χ₋₃), not the spacing law. **R016's fence adopted** →
`B1158/ADDENDUM_fence_note_R016.md`: R016 performed the load-bearing computation B1158 stated but didn't
ship (compatible-embedding coherence at all four frozen levels + the conjugate control); B1158's universal
phrases bounded to the verified scope (p∈{5,7}, r∈{1,2}, order ≤8). **Two asks relayed to codex:** the
**R018 numbering gap** (R017→R019, no R018 anywhere — asked, not assumed lost), and the **ζ₁₂/dP₆
construction cert** (the OA-C1000 registry rows exist; a *re-runnable cert* does not — the refined R49-1
second-half ask).

## Fences

Krutelevich (B8144's finite-orbit citation) carried cited-not-rederived (the L162 gate stands). The memo-82
E8 channel keeps its possibility-space fence (E8 not object-paid). B8145's successor read is one-fetch scope
(cc3's own fence). Nothing in this harvest asserts a measured value; Gate 5 clean. Not kill_graph-routed
(harvests + bounds; the one NEGATIVE — B8146's precision column — is cc3's arc on their branch, reflected
here as the L173 re-pose).
