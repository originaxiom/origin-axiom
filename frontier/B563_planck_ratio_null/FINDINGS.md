# B563 — Campaign 2c: the Planck-ratio bin retry NULLS (chat-2 handoff, verified)

A legitimate retry of the old Planck-scale kill, now that the **tower** gives the
object outputs at hierarchy magnitude (λₙ doubly-exponential to ~10²⁹ by rung 10;
|eₙ| to ~10⁶¹ by rung 5). **Unit-free Planck ratios are dimensionless**, hence fair
targets under the reframe (the *unit* itself remains theorem-absent, K020 — not
retried). Prereg-first (sha256 committed before any physics number). Processed
**verify-don't-trust**; the core statistics were independently reproduced
(`tests/test_b563_planck_null.py`). Firewall-clean; nothing to CLAIMS.md.

## Design (preregistered NULL)
- Object outputs: λ₀…λ₁₂ (law-generated; λ₅…λ₁₂ conjecture-conditional — labeled) +
  |e₁…e₅|. **No ratios/products of outputs** (look-elsewhere cap).
- Targets (10, unit-free): m_Pl/{m_e, m_μ, m_τ, m_p, m_W, m_Z, m_H, m_t, v_EW},
  ρ_Pl/ρ_Λ. Metric δ = |ln(object/target)|; ladder 0.10 / 0.01 / 0.001.
- Null: 10⁴ towers, **same law** λ→λ(1+√λ), seed log-uniform (1.05, 5.0), rungs 0–12,
  + 5 magnitude-matched charge analogs per trial. Controls: silver (1+√2), plastic
  (1.3247). Committed prediction: **NULL** (dialed bin).

## Result — NULL at every ladder rung, exactly as prereg'd

| statistic | real | null (10⁴) | p |
|---|---|---|---|
| best \|ln ratio\| | **0.6825** ( \|e₄\| vs m_Pl/m_μ ) | mean 0.7846 | **0.531** |
| hits @0.10 / 0.01 / 0.001 | 0 / 0 / 0 | 0.16 / 0.02 / 0.00 | 1.000 |

Controls: silver best 1.49; **plastic best 0.46 — closer than the real object.**

**Independent reproduction (this bank):** object best = **0.6825** exactly, realized
by |e₄| ≈ 2.29×10²⁰ vs m_Pl/m_μ ≈ 1.16×10²⁰ (a factor-~2 miss); a 500-trial null gives
p ≈ 0.53 (mean 0.79); the plastic-seed ladder brushes closer (0.46) than the object.
Every number matches the handoff. **Verdict: NULL, chance-level.**

## What the retry bought (banked)
1. The old kill upgrades from circumstantial ("the object had nothing that size") to
   **preregistered** ("the object's strongest-ever magnitude outputs tested; chance-
   level; a plastic ladder is closer").
2. The **dialed bin has now nulled at FOUR levels** — values, relations, gap-edges,
   and hierarchy-scale ratios. The reframe's boundary is complete on that face.
3. Scope: the tower's magnitudes are the object's **internal complexity schedule**,
   not addresses of physical scales. Its physics road remains the buildable-carrier
   ladder (rung-1 built; rung-2 = the T-1 octic, B557).

## Notes
- λ₅…λ₁₂ are conjecture-conditional (irreducibility ∀n; eₙ<0 ∀n); the null uses the
  same law, so the comparison is like-for-like regardless.
- ρ_Pl/ρ_Λ carries order-of-magnitude uncertainty; only the 0.10 rung is meaningful
  for it (missed by ln-distance ≫ 1 anyway).
- Connects to K020 (no-unit theorem), B558 (the three-level negative), B532 (the
  scale wall). The **plastic control being closer than the object** is the sharpest
  line: random growth-matched ladders brush hierarchy numbers as a matter of course.
