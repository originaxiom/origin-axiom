# B628 — the exterior held-out comparison design (sealed)

**Sealed 2026-07-15, AFTER the values (B627, sha 6275fc85…) and BEFORE
any distance is computed. Identical protocol to B614/B615/B616: the
same frozen targets, tiers, windows, and nulls — no new choices.**

## The grids

- **XG3:** the silver |τ_m| pairwise absolute ratios (15 values; complex
  τ's enter by modulus) × the same six T-R mass-ratio targets; linear
  1e-2/1e-3 tiers; the B615 G3 log-uniform null.
- **XG4:** the same 15 ratios × the T-H hierarchy target; the same
  half-decade log window; log-range 25.
- **CONTRAST rows (recorded, not new comparisons):** the fig-8's
  exterior family already went through the identical grids in B615
  (G3: 0 matches; G4: one chance-compatible hit). The silver rates are
  read against those.

## MB12 (computed at sealing)

Operations non-trivial (the silver ratios have never been compared);
each cell can match or miss (the integer/complex moduli are unconstrained
by the targets a priori); HONEST ANNOTATION: the fig-8's exterior rows
were already null-compatible, so the informative outcome here is
asymmetric — a silver EXCESS would be a surprise worth a fresh
registered look; matching nulls simply close the exterior families as
SM-silent, completing Branch 3's held-out round with the amplitude
families (B615/B616) as the only suggestion-carriers.

## The locked reading

- Both grids null-compatible → the exterior families are SM-silent on
  both objects; Branch 3's record completes as: fig-8 amplitude
  suggestion (p = 0.078, source-sensitive) + all controls null; the
  stopping rule's conditions for closing SM-values at this level are
  met pending seat-4's review.
- Any grid shows an excess (P(X ≥ k) < 0.05 raw) → banked as a
  registered anomaly; a fresh sealed design on a THIRD object decides.
