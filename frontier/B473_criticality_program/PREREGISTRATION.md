# B473 — PREREGISTRATION: the Criticality Program (the κ-reframe, both seats convergent)

**Committed before computation. Firewalled. The reframe (three-seat convergent, 2026-07-08):
the (1,2) pair is the metallic family's UNIQUE critical point (κ = −2, the parabolic/cusped
case — T-UNIQ is a κ-theorem); every other pair is gapped (κ = 2 − (mn(n−m))² < −2); the
year's emptiness = enhanced symmetry at criticality; masses/scales live in the flow away.**

## Cells

- **C1 — the off-critical seam control**: the (1,3) pair seam (κ = −34) through the exact
  B358/B459 machinery. PREDICTION (fixed now): the critical seam's structure COLLAPSES
  off-criticality — no analogous tier/selection architecture, or a qualitatively degraded
  one. This converts H112's death at (1,3) from a retirement into the control experiment.
- **C2 — the cusp-spectrum cell**: re-read the BANKED woven-chain spectra (B171–178)
  through the κ-lens: does the interaction spectrum remember κ = −2 — gap closure at the
  critical weave (golden+silver) vs gaps at off-critical weaves? The data may already be
  in the bank; new compute only where the banked scan lacks the off-critical points.
- **C3 — the quantum column**: κ_q (B472) extended along the family — tr[W(m)ʲ, W(n)ˡ]
  at the pair-appropriate levels; does quantum closure track CRT centrality at every
  (m,n) (the B472 mechanism as a family law)?
- **C4 — the T_c hypothesis (the warm arc's organizing prediction)**: the breath
  hierarchy's order parameters (root/mirror/balance/residue) die top-down in temperature,
  rarest first, the frozen −1 last; each death temperature = a new invariant. Runs when
  the warm campaign runs; the prediction is on record now.
- **C5 — the laundering group** (seat-2's positive-face proposal): write down the
  group(oid) generated by the banked laundering moves; CONJECTURE: its character lattice
  is exactly ℤ/2 (the breath). Phase 2b's σ-vs-mirror column = testing whether two
  characters coincide.

## Bins & gates

Predictions fixed above; controls are built into each cell (C1 IS a control; C2 uses the
banked scans; C3's control = the closure-address law). Killed-trap flags: "critical point
= the vacuum" and every phase-transition physics naming HELD at birth (S-room);
B398 for anything that fires. Lit-gates: parabolic-vs-hyperbolic commutator dichotomy
(classical, cite); gap-labeling at criticality (Damanik school); the Q₈/SL(2,5) images
(classical group theory, cite).
