# B928 — the sealed cell decides: OUTCOME FORCED — D₂ IS THE SECOND WALL CHARACTER ON THE 27

**Date:** 2026-08-06 · **Seat:** computation agent (for cc banking) ·
**Prereg:** `PREREGISTRATION.md`, sealed before compute; followed verbatim.
**Instrument:** `d2_decode.py` → `results.json` (98 checks, ALL PASS; runtime
~7 s after the frame cache). Exact arithmetic for every verdict-bearing claim;
all H-structures re-solved from their own defining equations (handoff-free);
no Rayleigh readouts; no measured number touched.

## The verdict, verbatim against the sealed criteria

**OUTCOME FORCED.** Candidate **(c)** — with **(a)** as its character form —
characterizes D₂ by a banked equation with NO residual freedom, and Q2
derives the pole prime and the leading arithmetic from it:

> **D₂ = ±ρ₂₇(σ_χ₋)** — the eleven-flip diagonal is the SECOND wall
> conjugation's sign character acting on the 27. Equivalently:
> **H₊D₂ = H(φ\*)**, the unique-up-to-scale invariant Hermitian structure of
> **φ\* = τ∘φ₊∘φ₋ = φ₊∘σ_χ₋** — the composite of the banked τ-lift with BOTH
> wall conjugations. φ\* is an involution and an automorphism (verified on
> all 78² bracket pairs), and it is the UNIQUE member of the 128-census whose
> invariant Hermitian structure is H₊D₂.

The verify-don't-trust gate passed first: H₊, H₋, and H(φ\*) were re-SOLVED
in-cell from their own invariance equations (support forced by the Cartan
rows, two-term propagation, every equation re-verified exactly); the
recomputed D₂ = H₊⁻¹H(φ\*) equals the banked B916 flip diagonal entrywise.

## Q1 — the characterization, candidate by candidate

**(a) Shifted character — SUCCEEDS, uniquely.** No un-shifted character
(−1)^⟨a,w⟩ matches D₂ (all 64 fail — the B916 addendum re-verified). But the
AFFINE candidate of the sealed list hits, and hits once in the 128-member
family:

> **D₂ = −(−1)^⟨a\*,w⟩ with a\* = (1,0,1,0,1,1) = s(χ₋)** — the exponent is
> exactly the sign vector of the second wall conjugation χ₋ = −χ₊.

The B916 addendum missed this by polarity alone: it tested whether a
character's odd set equals the flip set, not whether the flip set is a
character's EVEN set. The "other lattice" candidates add nothing: the
3C⁻¹-pairing (coweight-route) family coincides with the weight-lattice
family as a set of sign patterns (det(3C⁻¹) = 243 is odd, so the mod-2
reparametrization is invertible), and the 27̄-weight family is literally
identical ((−1)^⟨a,−w⟩ = (−1)^⟨a,w⟩). The affine shift is the only new
freedom — and it is the one D₂ uses.

**(b) One octet + three vacuum lines — REFUTED, exactly.** The abstract
octet Π-block (dim 8 over K, rebuilt as the R8-eigenblock at the octet
label, R16-scalar verified) and the vacuum lines meet the 11-dim flip
coordinate span in:

> dim(span_F ∩ octet_g) = **0** (every g) · dim(span_F ∩ vacuum line) =
> **0** · dim(span_F ∩ W3, the rational vacuum 3-space) = **1**.

The span reading was also impossible a priori: the flip span is rational
(σ-stable), so one octet inside it forces all three Galois images inside —
24 > 11. The true block content of the flip pattern is the exact TRACE
table of Q3 below, not a span identity. 11 = 8+3 was numerology.

**(c) The wall-conjugation formula — DERIVED symbolically.** All banked
composite identities recomputed exactly:

- φ₊∘φ₋ = σ₋₁ (the inner all-minus — the B907-completeness fact, re-proven);
- φ\* := σ₋₁∘τ = τ∘(φ₊∘φ₋) = φ₊∘σ_χ₋ — a banked-data composite, no choices;
- H(φ\*) = H₊D₂ with the solve unique up to one scale;
- **D₂ = ±ρ₂₇(σ_χ₋)** and **D (B912's wall twist, H₋ = H₊D) = ±ρ₂₇(σ₋₁) =
  ±ρ₂₇(φ₊φ₋)**;
- the three diagonals close the Klein group **{I, D₂, D, D₂D} =
  ±ρ₂₇({1, σ_χ₋, σ₋₁, σ_χ₊})**: the wall pair's whole 2-torsion, on the 27.

So the two Hermitian instruments of the value arc are the two wall-real
alignments' data seen through the SAME τ-lift: H₊ belongs to φ₊; the
τ-twisted H′ = H₊D₂ belongs to φ\* = τ∘(the walls' inner ratio); and the
character τ picks up between the two wall-real alignments is precisely χ₋
lifted to the 27 — the sealed candidate (c), confirmed.

**(d) The classification — the census does NOT pin D₂; one level up, it
does.** Exact sweep of all 128 census representatives:

- every INNER twist admits NO invariant pairing (the 27's weights are
  disjoint from their negatives — support empty; proven, not scanned);
- every OUTER composite σ_χ∘τ admits exactly ONE invariant pairing
  H_χ = H₊·ρ₂₇(σ_{χ·χ₊}) (the torsor theorem, verified for all 64);
- H_χ is **symmetric (a Hermitian structure) exactly when the composite is
  an involution** — the 16 flip-symmetric χ (a fact the run corrected: the
  draft expected all 64 symmetric);
- the C-compatible census (the banked 8): D-set = **{I, D_wall, D_c,
  D_c·D_wall}** (flip counts 0, 12, 12, 12) — **D₂ is NOT in it**. Its
  involution φ\* is not C-compatible (it does not act ±-diagonally on the
  charges) — which is exactly B916's observation that H′ has no
  charge-equivariance sign pattern, now explained;
- over the full involution sheet (16 Hermitian structures), **H₊D₂ arises
  from exactly one member: φ\***.

Verbatim against the sealed (d)-criterion: the set is LARGER than {I, D₂}
at the census level and does not even contain D₂ there — candidate (d)
alone leaves freedom. The pin is (c)/(a): the equation φ\* = τ∘φ₊∘φ₋ is
built from banked objects with no residual choice. The disclosed prior
("FORCED via (c) or (d)") lands on (c).

## Q2 — the arithmetic, derived from the equation; the exact residue

Everything below is COMPUTED FROM the characterized diagonal
ρ₂₇(σ_χ₋) — not matched against the belt:

- **the d-ratio minpolys, coefficient for coefficient** (the banked B916
  polynomials re-derived): S-family 2304²x³+9123840x²+5077008x+953²,
  A-family 2304²x³−1907712x²−2304·953·x+953²;
- **the norm law with its sign**: d = 1−2m per atom family (m = the flip
  mass), and N_{K/ℚ}(d_S) = N_{K/ℚ}(d_A) = **−(953/2304)²** exactly. The
  minus sign is supplied by the affine polarity — the global −1 in
  D₂ = −(−1)^⟨s(χ₋),w⟩ — a structural origin for the sign of the banked
  twist norm;
- **HIER's coefficients**: the diagonal CCl invariant in the characterized
  gauge is V_ccl ∈ K with 953⁴·charpoly(mult-by-V_ccl) = HIER exactly, and
  the canonical gauge collapses to −3 (B923's carried-by-D₂ theorem,
  re-verified end-to-end from the equation);
- **new**: the flip-mass minpolys themselves — m_S: 42467328x³−100196352x²
  +78499872x−20417473 (lead 2¹⁹3⁴), m_A: 42467328x³−56070144x²+19828224x
  −2113201 (const 29·72869);
- **new, colored**: see Q3 — the colored twist det-norm is
  **−(953/2304)³** — the norm law CUBES on the 3-dim atoms.

**The exact residue.** The characterization reduces the twist arithmetic to
the K-norm of (1 − 2·flip-mass) on the banked atom lines. What is NOT
derived: a pipeline-free closed form for 953 and 2304 = 2⁸3² — the specific
integers enter through the eigenline coordinates of the atom solve (the
value pipeline's own objects), and where each prime enters those
coordinates is open. Derivation stops exactly there.

## Q3 — THE SHAPE SHEET (the deliverable; computed blind)

The flip projector Π_F = (1−D₂)/2 against the generation functor. Every
family below is ONE abstract K-element; the three generations are its
Galois branches σ_g (the S3-orbit structure is the diagonal action of
B900's cocycle; branch order below = ascending ρ; the frame indexing of
branches is B900's banked vacuum_frame_map). K-coordinates are in the
basis (1, ρ, ρ²) of K = ℚ[ρ]/μ13.

**Sheet entry 1 — vacuum-line overlaps** m_S = h_F(S,S)/h(S,S) ∈ K:
> m_S = (52571/64896, −55825/2197, 27165600/28561)
> minpoly 42467328x³ − 100196352x² + 78499872x − 20417473
> branches: 0.86289845162610324642917053… / 0.80011290842674547149446460… /
> 0.69636363994715128207636485… (50-digit certificates in results.json)

**Sheet entry 2 — A-line overlaps** m_A ∈ K, with the FORCED equality
m_{A+} = m_{A−} (the τ-cross term of the twisted norm vanishes exactly, in
both gauges):
> m_A = (7291/32448, 31955/2197, 461815200/28561)
> minpoly 42467328x³ − 56070144x² + 19828224x − 2113201
> branches: 0.25724237788732187432453483… / 0.23305419858614214462122459… /
> 0.83001592352653598105424057…

**Sheet entry 3 — colored twist spectra.** Per colored atom the full
H-relative twist operator X = G⁻¹G′ (G, G′ = the H₊- and H₊D₂-Grams on the
3-dim atom). Its characteristic polynomial is **ω-free** — forced, so the
(g,+) and (g,−) atoms carry IDENTICAL twist spectra — with K-coefficients:
> e₁ = tr X = (3291/1352, −526680/2197, −2607897600/28561),
>   minpoly 256x³ − 768x² − 828x + 2859 (lead 2⁸, const 3·953)
> e₂ = (20123/10816, −334950/2197, 162993600/28561) · e₃ = det X =
>   (387917/778752, −1397935/17576, −258073200/28561), minpoly
>   12230590464x³ − 10239934464x² + 255728448x + 865523177 —
>   lead **2304³** = 2²⁴3⁶, const **953³**
> **N_{K/ℚ}(det X) = −(953/2304)³ exactly** — the colorless norm law squared
> becomes the CUBE law on the colored atoms (one factor per dimension).
> trace flip mass trM = (3−e₁)/2, branches 0.22209427878365588368609290… /
> 0.33788637047819001938027994… / 2.44001935073815409693362715… (the
> colored Grams are Lorentzian — the oblique trace legitimately exceeds 1).

**Sheet entry 4 — octet block overlaps** t_oct = tr(P_octet Π_F) ∈ K with
the FORCED decomposition **t_oct = 2m_A + 2·trM_col** (exact identity of
abstract K-elements — the octet's overlap is carried by its two A-lines and
two colored atoms in equal ± pairs):
> t_oct = (1267/1248, 45430/169, 271656000/2197)
> minpoly 5308416x³ − 45868032x² + 78736896x − 38004841 (lead **2304²**)

**Sheet entry 5 — the sum rule** (forced): Tr_{K/ℚ}(m_S) + Tr_{K/ℚ}(t_oct)
= tr Π_F = **11** exactly — the six Π-blocks' overlaps across the three
generations exhaust the flip rank.

**Sheet entry 6 — the angle ratios** d = 1 − 2m per colorless line (the
banked B916 twist ratios, now DERIVED): minpolys as in Q2; per-triple
products −(953/2304)² (the norm law).

**Forced equalities (all exact):** m_{A+} = m_{A−}; colored (g,+) ≡ (g,−)
spectra; t_oct = 2m_A + 2trM_col; the sum rule; the S3-orbits are single
K-elements (no cross-branch freedom).

**Numeric belt:** the nine colorless flip masses recomputed independently at
dps 60 (eigenvectors clustered, componentwise eigen-readout with residual
certificates < 1e-45) match the exact embeddings to **2.1e-57**.

The sheet is NONEMPTY: Stage 2's input sheet is the list above — four
abstract K-invariants (m_S, m_A, trM_col, t_oct), the colored charpoly
(e₁, e₂, e₃), their minpolys and norm laws (square for lines, cube for
colored), the forced equalities, and the sum rule. No measured value was
consulted.

## Two conjectures the run corrected (verify-don't-trust, on ourselves)

1. The first draft asserted all 64 outer pairings symmetric; the run
   refuted it and the repaired claim is sharper: **symmetric ⟺ involution**.
2. The first torsor formula carried a spurious ∘flip in the exponent
   (invisible on the involution sheet); the run refuted it; the correct
   theorem is H(σ_χ∘τ) = H₊·ρ₂₇(σ_{χ·χ₊}) for every χ.

## Honest gaps

1. **The Q2 residue** (stated above): 953 and 2304 are derived as the norm
   arithmetic of the characterized twist on the banked atom lines; a
   pipeline-free closed form for the primes themselves is open.
2. **Branch naming**: the sheet's branch order is the ascending-ρ
   convention; the identification of branches with B900's measurement
   frames is by the banked vacuum_frame_map (cited, not recomputed here).
3. **Census scope**: "unique carrier in the census" is relative to B907's
   128 representatives (with its banked completeness addendum at the
   pattern level); an automorphism outside every swept class is not
   excluded by this cell.
4. Locks (`tests/test_b928_decode.py`) are the banking seat's step; not
   written by this agent.

## Files

`PREREGISTRATION.md` (sealed) · `d2_decode.py` → `results.json` (98 checks,
all PASS; the D-classification, the character data, the Q3 sheet with
50-digit certificates) · this draft.

## Depends on

B854 (frame), B883 (the 27), B900 (cocycle/frames), B907 (τ, the census,
the wall pair), B912 (H±), B914 (cubic), B916 (D₂, the twist norms), B918
(HIER), B923 (the canonical collapse; colored machinery).


## Banking-seat note (cc, 2026-08-06)

The rerun confirms (98/98). The verdict banks FORCED verbatim; the disclosed
prior (FORCED via route (c)) WON — the seal scoreboard stands at seven cells:
four object-overrules, three disciplined wins. Two candidate refutations
inside the cell (11 = 8+3 as numerology; the naive-character miss explained by
the affine polarity) are the discipline working mid-cell. The Q2 residue (a
pipeline-free closed form for 953 itself) and the census-scope caveat are
carried as the arc's registered opens. **Per the program's registered gate:
Stage 2 — the comparison of this sheet against measured mixing shape — now
awaits THE OWNER'S WORD, and nothing else.** The sheet was computed blind and
sits sealed in this arc's results until that word.
