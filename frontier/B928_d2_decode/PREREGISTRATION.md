# B928 PREREGISTRATION — THE D₂ DECODE (the Twist Crossing, Stage 1: mathematics; sealed before compute)

**Date sealed:** 2026-08-06 · **Seat:** cc (banking) · **Register:** the Twist
Crossing program (owner: "seal it and run it"), Stage 1 — PURE MATHEMATICS,
consistent with B927's ruling (the seals-closed clause binds DATA crossings;
this cell touches no measured number). **Hemisphere check:** every consumed
structure is measurement-side (the twist D₂, the conjugations, the torus, the
cocycle) — no grafts; the mixing-shape license (B896/B906) is cited for STAGE
2 only, which this seal does not open.

## The question

B923 proved the entire generation hierarchy is carried by the 11-flip diagonal
D₂ = H₊⁻¹H₋ (B916). WHAT IS D₂? Three sealed sub-questions:

**Q1 — THE CHARACTERIZATION.** Which equation pins D₂? The candidate list
(exhaustive at design time; each tested exactly; new candidates may be ADDED
in-cell if a tested one fails and suggests a successor — additions logged, none
removed):
(a) a sign character on a lattice OTHER than the naive weight lattice mod 2
    (already refuted there, B916 addendum): the 27̄-realization's weights, the
    coweight lattice, shifted/affine characters (−1)^{⟨a,w⟩+c(w)};
(b) block/atom structure: is the flip set a union of banked structures
    (11 = 8+3: one octet Π-block + three lines? the B883-basis dictionary
    decides membership exactly);
(c) the two wall conjugations: derive D₂ from the pair (χ⁺, χ⁻ = −χ⁺) — is
    D₂ the character τ picks up between the two wall-real alignments (an
    explicit formula in the cocycle data of B900/B907)?
(d) uniqueness: is D₂ the UNIQUE ±1 diagonal D such that H₊D is an invariant
    Hermitian form for a DIFFERENT banked involution class — i.e., does the
    classification of invariant Hermitian structures on the 27 (finite, per
    B907's involution census) have exactly the two members H₊, H₊D₂, and does
    that classification PIN D₂ with no freedom?

**Q2 — THE ARITHMETIC.** For whichever characterization holds: derive the
twist-norm arithmetic FROM it — why the d-ratio minpolys carry 2304² and
953²; why the pole prime is 953; whether HIER's coefficients are computable
from the characterization without the pipeline. Full derivation = the
hierarchy's origin is an equation. Partial = state exactly the residue.

**Q3 — THE SHAPE SHEET.** Does D₂, against the generation functor (B900's
diagonal cocycle + the twists), define exact DIMENSIONLESS SHAPE invariants —
relative angles/overlaps between the flip pattern and the three generation
frames (the formal analogue of mixing-matrix shape)? If yes: list them
EXACTLY (closed form in the banked fields) — this list is Stage 2's entire
input sheet, computed blind to any measured value. If no: Stage 2 is stillborn
and the program ends as mathematics (a valid, bankable end).

## The two-outcome criteria

- **OUTCOME FORCED:** some candidate in Q1 characterizes D₂ by a banked
  equation with NO residual freedom, AND Q2 derives at least the pole prime
  and the leading arithmetic from it.
- **OUTCOME FREE:** every candidate fails or leaves freedom — D₂ is
  irreducibly a CHOICE among the classification's members; the hierarchy is
  observer-contingency, banked as a theorem about freedom (with the
  classification's size as its measure).
- Q3 reports its sheet (or its emptiness) under EITHER outcome.
- **UNSTABLE:** computational failure only.

## The disclosed prior

**FORCED via (c) or (d), moderate.** Reason: D₂ arose FROM the two wall
conjugations (B912's cross-check), and B907's census was small and rigid —
freedom has nowhere obvious to live. The scoreboard (six sealed priors: four
object-overrules, two disciplined wins) is priced in.

## What this seal does NOT open

Stage 2 (any contact with measured mixing/mass data) — that requires Stage
1's outcome, the anatomy's constraints verbatim, the hemisphere license
cited, and THE OWNER'S WORD at that gate. Gate 5 untouched throughout.

## Files (after sealing)

`d2_decode.py` → `results.json`; `FINDINGS.md` verbatim against these
criteria; locks in `tests/test_b928_decode.py` (seal-integrity first).
