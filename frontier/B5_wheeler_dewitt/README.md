# Probe B5 — Wheeler-DeWitt constraint, and a dead-adjacent Λ

> **Speculative frontier work.** Logged observations, not claims
> (`../../GOVERNANCE.md` §5). Nothing here is promoted to `CLAIMS.md`.
>
> This probe is recorded mainly to **document a dead-adjacent path** so it is not
> silently re-attempted.

## What was checked (`probe.py`)

1. **The Wheeler-DeWitt `H = 0` constraint** for the toy moduli Hamiltonian
   reduces to `r² − r − 1 = 0`, with solution `r = φ`. — This is *not* a new
   result: `r² − r − 1 = 0` is the golden-ratio fixed-point equation, the same
   structure as `χ_A` (claim P1). It reproduces known structure.

2. **`Λ = 2π²/Vol(4₁) ≈ 9.724`** in Planck units.

## Honest verdict — dead-adjacent; recorded as a warning

The `Λ` estimate is **~10²⁰⁰⁰… (≈10¹²⁰) times larger than the observed
cosmological constant**. It does **not** resolve the cosmological-constant
problem — it restates it. It is the same failure mode as the killed claims:

- **D1** — `k ≈ 137` cosmological constant (wrong normalization),
- **D2** — `Λ = φ⁻²ᴺ` (circular).

See `docs/ARCHIVE.md`. Per `GOVERNANCE.md` §5, dead ideas stay dead; a new idea
resembling one carries the burden of showing it is not the dead one again. The
`2π²/Vol` formula has not met that burden.

**B5's only real value is documentary:** it records that this path was tried and
leads back to the dead `Λ` work, so a future session does not re-discover it.
No claim promoted; **O5 (the cosmological constant) remains `open`.**
