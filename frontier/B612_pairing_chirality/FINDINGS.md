# B612 — the per-level pairing law FAILED; the quantifier is the law

**Status: banked (frontier). Nothing to `CLAIMS.md`. The sealed
prediction (the per-level biconditional with the dim ≥ 2 clause) is
honored as FAILED. Lock `tests/test_b612_pairing_chirality.py` (fast;
pins the seven-object data and the failure). Chirality classifications
by the GHH anti-palindrome criterion (banked B134/V123): RL, RRLL, RLRL,
RRRLLL amphichiral; RRL, RRRL, RRLLL chiral — all seven verified against
the cyclic-rotation test at registration.**

## The sealed verdict: FAIL

RRLLL (chiral) is conjugation-closed at exactly one non-degenerate level,
κ = 6 (dim_odd = 4). One accidental closure kills the per-level
biconditional — the second refinement death in two runs (B611's was the
dim-1 edge).

## The surviving statement (all seven objects, no exception)

> **Amphichiral ⇒ conjugation-closed at EVERY scanned level (4 objects ×
> 14 levels, 56/56). Chiral ⇒ closure FAILS at some level (3/3 objects;
> indeed at almost all levels — the exceptions are isolated: κ = 4 for
> RRRL (degenerate), κ = 6 for RRLLL).**

The correct law-form is quantified over levels: LEVEL-UNIFORM closure ⟺
amphichirality (on this data). Two refinement deaths in a row say the
next step must be a PROOF, not a wider scan: for a GHH anti-palindromic
weld word, reverse+swap is a cyclic rotation; the reversal of the weld
word relates W to Wᵀ-conjugates and the L↔R swap to S-conjugation, which
together should carry W|odd to its complex conjugate up to unitary
equivalence — the mechanism for spectrum conjugation-closure. Registered
as the proof cell (unrun); until then the boxed statement is
evidence-tier on seven objects.

## Also fixed on the record

The B611 refined candidate (dim ≥ 2 clause) is dead as a biconditional;
the amphichiral ⇒ closed direction has never seen a counterexample at
any level, degenerate or not (70/70 level-rows across five amphichiral
welds counting B611's).
