# Reader r4 — Memo 226 (JORGENSEN_IS_IT_RIGHT.md)

## 1. HEADLINE
"IS IT RIGHT, JØRGENSEN? The theorem is right, the uniqueness holds, and my axiom conclusion
was wrong." Table verdict: Jørgensen's inequality as stated — RIGHT; `J(m004)=1` — RIGHT
(reproduced a second, independent way); uniqueness of m004 — HOLDS (now tested, not only
cited); **memo 225's axiom conclusion — WRONG (BENCH ERROR #32)**. No date line given in the
memo body itself, but it is banked the same day as its own R135 register entry and the
antecedent R133/R134 ("make sure its right jorgensen," 2026-09-13); INDEX.md dates it
**"1B, banked 2026-09-13."**

## 2. CLAIMS
1. Precise definition recovered: a **Jørgensen group** is a non-elementary Kleinian group that
   can be generated by **TWO** elements with equality in Jørgensen's inequality
   `J(G)=inf{|tr²A−4|+|tr[A,B]−2| : G=⟨A,B⟩} ≥ 1` [Jørgensen 1976]. Callahan (2009), as the
   literature states it: "the only torsion-free Jørgensen group is the figure-eight knot
   group." — PROVED / LITERATURE, verified against a statement of the theorem rather than a
   paraphrase
2. The prior paraphrase (B1345 → memo 225) — "the only orientable hyperbolic 3-manifold with
   `J=1` is the figure-eight complement" — **drops "TWO-GENERATOR"** as part of the definition
   (a 3-generator group is outside the theorem's domain, not ruled out), and misstates
   "orientable" as an added hypothesis when it is automatic from `PSL(2,ℂ)` (so m000 is outside
   the domain, not a counterexample — B1345's own instinct, reached by a different route,
   confirmed right). — CORRECTED (paraphrase vs. source)
3. **BENCH ERROR #32**: memo 225's claim that the theorem discharges `UNIQUENESS_THEOREM`'s A1,
   A2, A4, A5, A6 (five axioms) **does not survive**. A two-generator group **IS** A1 in another
   language (A1: substrate is ℤ², "not one, not three") — the theorem **assumes** A1, it does
   not derive it; A3 (orientation) is likewise assumed via `PSL(2,ℂ)`. Corrected accounting:
   **ASSUMED = A1, A2/A3; DISCHARGED = A4 (primitive shears), A5 (torsion-free closure), A6
   (minimality)** — **three of six, not five**. — WRONG (memo 225), CORRECTED here
4. `J(m004) = 1.000000000000`, reproduced a **second, independent** way: SnapPy's own holonomy
   (default generators non-parabolic, `tr a = −1.5+0.866i`), minimized over Nielsen-move pairs
   (every pair provably generates the same group, so every value is a genuine upper bound). —
   PROVED / REPRODUCED (second method)
5. Uniqueness falsification-tested over the **orientable cusped census**: **323 two-generator
   census manifolds scanned; exactly one reaches 1** (m004); m009 = √2 = 1.414213562 (reproduces
   B1345's slack table independently), m023=1.632535071, m039=1.757093630, m310=1.758947219,
   m129 (Whitehead link) = 2.000000000 exactly. Spread 1.000…67.869 (factor of 68, "not
   vacuous"). — MEASURED / VERIFIED
6. Controls: nothing fell below 1 (a check on the instrument, not the theorem — Jørgensen's
   inequality forbids values <1 on a generating pair) — PASSED. — CONTROL, PASSED
7. Rule added, **#26**: "a theorem's hypotheses are load-bearing and a paraphrase is where they
   go missing" — before drawing a conclusion from a cited theorem, quote its hypotheses from a
   statement of the theorem, not a sentence about it, and check them one by one against what
   the conclusion needs. Noted as the **same error twice in one day** (memo 223's #30, "a
   ledger question answered from a synthesis document," now "a literature question answered
   from a paraphrase" — and B1345's own addendum had already stated this exact rule about
   itself). — RULE ADDED
8. Recommendation, not an action: `B1345` on `<remote>/paper-verification-ufp0zn` carries the
   imprecise paraphrase; its own conclusions are unaffected since it never drew an axiom claim
   from it, and its m000 reasoning is independently right — "that seat's call, this bench does
   not edit another branch." — RECOMMENDATION ONLY, NOT APPLIED
9. Closing line: "Memo 223's price stands at 12" (unchanged by this memo). — UNCHANGED

## 3. CERTIFICATE
- `outside_bench/certificates/jorgensen_is_it_right.py` — **EXISTS**; output
  `outside_bench/outputs/jorgensen_is_it_right.txt` — **EXISTS**. Tail (VERDICT block):
  `J(m004) = 1 reproduced from SnapPy's holonomy : True`, `m004 alone reaches 1 over 323
  two-generator census manifolds : True`, `nothing below 1 ... : True`, `the quantity varies
  over the census : True`, plus the correction text verbatim matching the memo's §2 table
  ("Callahan's hypothesis includes TWO-GENERATOR... a two-generator group IS that hypothesis in
  another language. The theorem ASSUMES the first axiom; it does not derive it.") — **agrees**
  exactly with the memo's headline and BENCH ERROR #32.
- **No seal** — the memo states this explicitly ("No seal — a literature check plus a
  falsification test over the orientable cusped census"), so there is no sha256 to compute or
  compare.

## 4. ON MAIN ALREADY?
1. `frontier/B1345_the_jorgensen_number` — **(a) already on main**, directory present; this is
   the arc whose paraphrase memo 226 corrects. The correction itself is a **recommendation, not
   applied** to B1345, per the memo's own §5 — so B1345's paraphrase sentence, if still present
   in that arc's own files, is **(c) NOT corrected on main** unless a separate seat did so
   later (not checked further here, out of the memo's own stated scope: "this bench does not
   edit another branch").
2. The owner register's **R134/R135** entries — **(b) applied via the merge**: current
   `outside_bench/THE_OWNER_REGISTER.md` (now part of main) shows, at the location this memo's
   correction targets, the struck-through original R134-4 claim ("It discharges FIVE axioms,
   not one...") followed immediately by `[WITHDRAWN 2026-09-13 — R135 / memo 226, on the
   owner's "make sure its right jorgensen". The struck text is kept for provenance.]` and the
   corrected "Discharged: A4, A5, A6. Assumed: A1, A2/A3. Three of six, not five." — this is the
   register's own copy of memo 226's correction, present verbatim on main.
3. `UNIQUENESS_THEOREM`'s axiom list (A1–A6) — **(a) already on main** as the object of both
   memo 225's and memo 226's claims; the corrected three-of-six accounting is the memo's own
   contribution and is **not independently re-derived elsewhere** in `docs/` — a targeted grep
   for "three of six, not five" / "Jørgensen assumes it" outside the memo/register pair returns
   nothing else, so beyond the register mirror in §4.2 this is **(c) NOT independently
   cross-cited on main.**
4. m000 (Gieseking manifold) `J=1`, non-orientable — the memo explicitly flags this value as
   **INHERITED from B1345 and not recomputed** here (its holonomy is not in `PSL(2,ℂ)`); this
   reader did not independently verify m000's value against main, so it is **unverified, not
   disputed.**
5. No contradiction with main was found — **no DISPUTED items.**

## 5. NEEDS COMPUTATION HERE
- Claim 4 (`J(m004)=1` via SnapPy holonomy + Nielsen moves): re-run in SnapPy —
  `M = snappy.Manifold('m004'); M.fundamental_group()` then compute
  `J = |tr(A)²−4| + |tr([A,B])−2|` over a small set of Nielsen-move-generated pairs and take the
  min; expected exactly `1.0` to machine precision (memo reports `1.000000000000`). Quick,
  well within the 2-minute bound.
- Claim 5 (323-manifold census scan, exactly one reaches 1): re-run over
  `snappy.OrientableCuspedCensus` (or a subset, e.g. first 50, to respect the time bound) and
  confirm no manifold in the sampled subset returns a value below m004's neighbors reported
  (m009 = √2, etc.); a full 323-manifold re-run may exceed 2 minutes depending on the Nielsen-
  move search depth used, so a **partial re-run (spot-check 10–20 manifolds including m004,
  m009, m129) is the practical discriminating fact** for a bounded verifier.
- Claim 3 (axiom accounting, three of six): DOCUMENTARY — no number to recompute; the
  discriminating check is textual: read Callahan's theorem statement (or the standard Jørgensen
  1976 definition) and confirm "generated by two elements" is part of the hypothesis, not a
  derived conclusion — this reader already did this cross-check against the memo's own
  Definition block (§1) and found it internally consistent and standard.
- Claim 1 (Jørgensen's inequality `J(G) ≥ 1`): DOCUMENTARY / standard literature fact
  [Jørgensen 1976], not independently re-derivable in 2 minutes but consistent with claim 6's
  control (nothing in the 323-manifold scan fell below 1).

## 6. SUPERSESSION
- Memo 226 **itself supersedes memo 225's §4 axiom-discharge claim** (BENCH ERROR #32) — this
  is the memo's entire purpose, and it is the single most important fact for any downstream
  reader: **do not cite memo 225 / R134's "discharges A1,A2,A4,A5,A6" claim; cite memo 226's
  corrected "three of six" instead.**
- No later memo number in `INDEX.md` (227+) was found further revising memo 226's own
  correction, and the owner register's R135 entry (which mirrors this memo) is not itself
  struck or further withdrawn anywhere this reader found.
- The memo names its own precedent/pattern: this is "the same error twice in one day" as memo
  223's #30 and structurally identical to B1345's own self-diagnosed addendum — none of those
  are contradictions, they are the pattern this memo instances a third time.

## 7. GRADE PROPOSAL
**REGISTER** — this is a documentary/literature-hygiene correction (a mis-paraphrased theorem
hypothesis, caught and fixed) combined with a cheap, already-completed falsification
computation (the 323-manifold census scan) rather than an open computation needing a fresh arc;
the numeric claims (`J(m004)=1`, uniqueness over the census) are already verified two
independent ways by the memo's own certificate and match the register's mirrored text on main,
so the main action item is registering the corrected axiom-accounting (three of six, not five)
and, separately and only as a **recommendation** already logged, flagging `B1345`'s uncorrected
paraphrase for that seat's own branch to fix — not a DISPUTED item against main, since main's
own register copy already carries the correction.
