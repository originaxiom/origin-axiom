# Reader r4 — Memo 198 (THE_GATE_HAS_NO_PRODUCT_ROW.md)

## 1. HEADLINE
"THE EXHAUSTION IS EXHAUSTION OF A TABLE WITH NO JOINT ROW IN IT" — none of the seven sealed
value crossings touches the meeting field ℚ(√−15) (0/0/0/0/0/0/0), and `docs/KIND_TABLE.md`'s
admissibility gate has **zero** rows built from more than one face, so the seven-for-seven miss
was exhaustion of a table that never contained a joint row — "not by prohibition, by absence."
**Date 2026-09-11** (addendum 1, next turn, same day).

## 2. CLAIMS
1. `sqrt-15`/`√−15` occurs 0 times across all seven sealed crossing directories (control C1:
   the English word "meeting" occurs 7× in B915, 3× in B925, counted separately as non-field
   text). — MEASURED
2. `docs/KIND_TABLE.md`'s field column, all object-side rows: 2× ℚ(√5), 2× ℚ, 1× ℚ(ω), 1× cubic
   K, 1× dash; **0 occurrences** of the meeting field; **0 rows** built from more than one face.
   (Control C3: an instrument bug — wrong table column indexed — was caught and fixed before
   the result was read.) — MEASURED
3. Class numbers (counter validated against `h(−4)=1`, `h(−23)=3`): being `h(−3)=1`, hearing
   `h(5)=1` (= B1069's `h=h⁺=1`), **meeting `h(−15)=2`** — the only non-trivial class group of
   the three, named "the genus bit" (B1276). Corpus mentions of the meeting field: "six times
   in total, none a crossing, none a value computation." — MEASURED, **later WITHDRAWN in part**
   (see below)
4. Self-correction to memo 195's addendum-2 sentence "the object's numbers are not nature's
   numbers": corrected to "the object's SINGLE-FACE numbers are not nature's numbers, and no
   multi-face quantity has ever been admissible." — CORRECTED
5. Explicit non-license: the memo does NOT claim the meeting face will yield values (meeting is
   imaginary quadratic, unit rank 0, so by the same Dirichlet argument it carries torsion not
   growth; a class-group bit is "still a bit, not a value"). — NEGATIVE (scope limit)
6. §6 poses (as an open question, not a claim) whether a joint quantity built from more than one
   face is even well-defined, and whether the gate (R5) would admit it — explicitly "a question
   for the gate's owner, not this bench." — OPEN QUESTION, NOT A CLAIM
7. **ADDENDUM 1** (same day, next turn): **TWO SENTENCES OF THE MEMO ARE WITHDRAWN.**
   (a) "mentions the meeting field six times... none a crossing, none a value computation" —
   **measured: 65 frontier arcs and 350 tracked files** name the meeting field (control C4
   excludes this bench's own lane) — off by a factor of eleven. **WITHDRAWN.**
   (b) "it has never been asked" — **FALSE**: `frontier/B698_the_meeting_probed/`, verdict
   **PROVED**, prereg sealed before verdict, asked exactly the joint question (does the
   level-15 meeting's analytic content COUPLE being-prime 3 and hearing-prime 5, or is it
   FACTORED?) and answered **FACTORED** via Flath's tensor-product theorem — a theorem, not a
   base-rate miss — supported by `L(15a,1)`, `L(15a,2)`, `L'(15a,0)` (Beilinson K₂ regulator),
   PSLQ at 60 digits (no relation found), class number two ways, base-rate control. **WITHDRAWN.**
   What stands unchanged: claims 1–3's raw numbers (0 in each of 7 directories; 0 rows in
   KIND_TABLE; h(−3)=1/h(5)=1/h(−15)=2) — all three came from the memo's own certificate; the
   two withdrawn sentences did not. — WITHDRAWN (2 sentences) / STANDS (3 measured facts)
8. Addendum 1's reframe: the answer is **sharper**, not weaker — for the *analytic* joint
   quantity the question is **settled negative by a theorem** (a product, not a fusion); the
   live open question narrows to "is there a joint quantity OUTSIDE the Flath-factored analytic
   channel, and would the gate admit it?" — the gate's zero rows (claim 2) "survives intact." —
   REFRAMED / NARROWED
9. **BENCH ERROR #20**, rule added: "A number in a memo must come from that memo's certificate."
   The two withdrawn numbers ("six times," "never asked") were asserted from prose beside the
   certificate's actual measurements rather than from the certificate itself. — RULE ADDED

## 3. CERTIFICATE
- `certificates/no_product_row.py` — **EXISTS**; output `outputs/no_product_row_out.txt` —
  **EXISTS**. Tail: *"LICENSED: the seven-for-seven is exhaustion of a table whose field column
  never contained the product face... THE OBJECT'S SINGLE-FACE NUMBERS ARE NOT NATURE'S
  NUMBERS, AND NO MULTI-FACE QUANTITY HAS EVER BEEN ADMISSIBLE"* — **agrees** with the memo's
  headline and §4 correction.
- Addendum 1's certificate `certificates/the_meeting_was_asked.py` — **EXISTS**; output
  `outputs/the_meeting_was_asked_out.txt` — **EXISTS**; tail confirms "THE MEETING IS A
  PRODUCT, NOT A FUSION" and restates BENCH ERROR #20 — **agrees** with addendum 1's verdict.
  CELL 1/2/3 = B/B/B as claimed, four controls passing (not independently re-run here, only
  file existence + tail content checked, per the ≤2-minute rule).
- No seal is named for memo 198 or its addendum (Gate 5 untouched, no measured physical value;
  INDEX grades it "1B"); no sha256 to verify.

## 4. ON MAIN ALREADY?
1. `docs/KIND_TABLE.md` — **(a) already on main**, present at `docs/KIND_TABLE.md`. Re-checked
   directly: grepping the file for "product," "joint," "multi-face," and "meeting" returns
   **nothing** — confirming the gate **still has no product/joint row on current main**,
   consistent with the memo's finding (this is a live-checked absence, not stale).
2. `frontier/B698_the_meeting_probed/` — **(a) already on main**, directory present with
   `FINDINGS.md` whose own first lines match the memo's quote of the sealed question almost
   verbatim ("Does the level-15 meeting's ANALYTIC content ... COUPLE the being-prime 3 and
   hearing-prime 5 ... or is it FACTORED", headline "a product with a ℤ/2 residue"). The
   withdrawal in addendum 1 is thus **verified correct against main**, not merely asserted.
3. Class numbers h(−3)=1, h(5)=1, h(−15)=2 — **(a) already on main** as independently-derivable
   standard facts, consistent with B1069's own `h=h⁺=1` claim cited by the memo.
4. The "0 rows, 0 occurrences" finding about KIND_TABLE.md is a **new synthesis fact** not
   otherwise stated anywhere else in `docs/` — grep of `docs/` for "no product row," "joint
   row," or similar returns only this memo/its outputs — **(c) NOT on main** as an independent
   claim outside this memo (which itself is on main via the merge).
5. No contradiction with main was found — **no DISPUTED items.**

## 5. NEEDS COMPUTATION HERE
- Claim 1 (0 occurrences of √−15 across seven crossing directories): DOCUMENTARY-adjacent but
  countable — `grep -rc "sqrt-15\|√−15\|√-15" frontier/B915* frontier/B925* frontier/B929*
  frontier/B1027* frontier/B1063* frontier/B1066* frontier/B1075*`; expected 0 in every
  directory, per the memo.
- Claim 2 (KIND_TABLE.md field column, 0 multi-face rows): re-run the field-column grep over
  `docs/KIND_TABLE.md`'s `| object quantity | kind | bounds | field | channel |` rows
  (confirmed present at line 20 of the file) and confirm no field cell contains more than one
  named field (e.g. "ℚ(√5) × ℚ(√−3)"); this reader spot-checked (§4.1) and found none, but a
  full row-by-row parse is the discriminating fact.
- Claim 7(b) (B698 answers the joint question, FACTORED via Flath): NEEDS COMPUTATION — recheck
  Flath's tensor-product argument's applicability and re-run the PSLQ-at-60-digits search
  described in `frontier/B698_the_meeting_probed/` (its own reference scripts) to confirm no
  relation is found among the meeting's and being's special L-values; expected: PSLQ finds no
  integer relation at 60 digits (a negative/no-relation result), matching "FACTORED."
- Claim 3 (65 frontier arcs / 350 tracked files name the meeting field): `grep -rlc "sqrt-15\|
  √−15\|ℚ(√−15)" frontier/ docs/ papers/ | wc -l` — expected close to 65 arc directories,
  though the exact number may have shifted since 2026-09-11 as the corpus grew.

## 6. SUPERSESSION
- Two of the memo's own body sentences are **WITHDRAWN by its own ADDENDUM 1**, same day: (a)
  the "six times, never a crossing/value" undercount, and (b) "it has never been asked." Both
  are explicitly named, quoted, and corrected in place — this is the single most important fact
  a downstream reader must not miss: **cite claims 1/2 (the seven-crossing / KIND_TABLE zero
  counts) but not the withdrawn "six times"/"never asked" language.**
- No later memo number in `INDEX.md` was found further revising or retracting addendum 1's
  correction, and no arc on main contradicts B698's FACTORED verdict.
- BENCH ERROR #20 (claim 9) is a standing rule addition, referenced later in the owner register
  (~line 2549, 2579) as still binding — not itself superseded.

## 7. GRADE PROPOSAL
**REGISTER** — the surviving, certificate-backed content (zero meeting-field occurrences in the
seven crossing directories; zero multi-face rows in the admissibility gate; the three class
numbers) is documentary/structural rather than a new physical-value computation, and the
memo's own addendum already did the necessary self-correction and cross-check against B698;
what remains open is the §6 question ("is there a non-analytic joint quantity, and would the
gate admit it?"), which is a design/policy question for the gate's owner, not a pending
computation — one sentence why: the numbers are solid and worth a citation-hygiene row, but the
memo's own two most citable one-liners were already retracted by its own addendum, and nothing
here should be re-banked without carrying that correction along.
