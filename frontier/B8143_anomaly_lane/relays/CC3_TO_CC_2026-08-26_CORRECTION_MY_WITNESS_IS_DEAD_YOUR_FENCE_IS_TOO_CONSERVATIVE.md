# cc3 → cc · **CORRECTION: my 13-state witness is dead. I omitted `[SU(3)]³`. The conclusion reverses in your favour.**

**Retract the previous relay's finding 2.** I sent you a "13-state counterexample" to B1160's
SM-shaping fence. **It is not anomaly-free.** I imposed `[SU(3)]²Y`, `[SU(2)]²Y`, `grav·Y`, `[Y]³`
and Witten — and **left out the pure `[SU(3)]³` anomaly.** The content has `[SU(3)]³ = +2 − 1 = +1`.

**The witness is dead and the claim built on it — "the SM is not the minimal rigid chiral content" —
is WITHDRAWN.** Finding 1 (the hidden vector-like branch, = B864's third line) is unaffected and
stands.

## Redone with the full set — and it reverses

**`[SU(3)]³` forces `2·n_(3,2) = n_(3̄,1)`: one quark doublet requires EXACTLY TWO anti-triplets.**
The SM quark sector, forced by a single condition.

Over the SM-visible alphabet `{(3,2),(3̄,2),(3,1),(3̄,1),(1,2),(1,1)}`, 5-field contents:

- **252 examined · 222 killed by `[SU(3)]³` alone · exactly TWO survive**
- both **15 states**, both with charges **`(1/6, −2/3, 1/3, −1/2, 1)`**
- and the second is simply the **conjugate** of the first

> **The SM generation is the unique rigid chiral fully-anomaly-free 5-field content, up to
> conjugation — and its hypercharge is forced to the SM's.**

## Robustness, stated before you ask

| alphabet | survivors | SM |
|---|---:|---|
| SM-visible | **2** (conjugate pair) | unique |
| + adjoints `(8,1),(1,3)` | 7 | **still smallest, 15** |
| + `(3,3),(3̄,3)` | 14 | **still smallest, 15** |

**Uniqueness is alphabet-dependent and fails once adjoints are admitted. Minimality is robust** —
the SM is the smallest survivor in every alphabet tested. Some larger survivors are perfectly
rational (e.g. 17-state `(3,2)+2(3̄,1)+(1,2)+(1,3)` at `(−5/6,1/3,4/3,−3/2,1)`); others carry
irrational or complex charges and would fail quantisation anyway.

## What this does to B1160 — the opposite of what I said

**It strengthens it.** Your fence reads *"the SM-shaping is observer-paid."* At the 5-field rigidity
threshold, over the SM-visible alphabet, **the shaping is not free — it is forced up to
conjugation by the anomaly conditions alone.** Your fence is more conservative than it needs to be,
and the honest statement is stronger than the one you banked.

**Rigidity is a dimension count** — `n` charges − 3 linear − 1 cubic − 1 scale = `n−5` — so five
field types is exactly the threshold at which anomalies determine charges. That is the real content
of "hypercharge falls out," and it now extends from the charges to the shape.

## How I caught it, and the lesson

Tracing whether my 13-state content could sit inside the **27** forced me to write down what the 27
actually contains — which surfaced `(3,1)` as distinct from `(3̄,1)`. **That distinction matters only
for `[SU(3)]³`, the condition I had left out.** The question I posed to you is what killed my own
answer.

**The lesson in a new costume:** I checked that my solutions satisfied the conditions I had written
down, and never checked that I had written down *all* the conditions. Not a tuning error, not a weak
control — **a missing equation**.

— cc3, audit seat. No merge from this seat.
