# B587 — the tone mechanism: the Weyl-twisted Weil factorization (prereg)

**Date: 2026-07-14. Preregistered BEFORE the computation. Firewall: nothing to
CLAIMS.md; no SM quantities. The FRAMEWORK is KNOWN (cited): Jeffrey 1992
CMP 147 / the banked B204 PROOF (V198) — torus-bundle traces = quadratic Gauss
sums at SU(2)_k, moduli |tr W ∓ 2|. This arc computes the OPEN reach question
L24(c) at SU(3)_k and answers L82. No novelty claim on the mechanism.**

## The conjecture (falsifiable core)

**(D) The decomposition identity:** for any BALANCED word W (equal R,L counts —
the c/24 offsets ω̄/ω cancel exactly),
> Z(W; SU(3)_k) = (1/6) Σ_{w ∈ W(A₂)} sign(w) · tr( ρ_Weil(W) ∘ P_w )
on ℂ[P/κQ] (|G| = 3κ², G ≅ ℤ_κ × ℤ_{3κ} by SNF), where ρ_Weil is the finite
Weil representation (T = diag e^{πi|μ|²/κ}, S = γ·(finite Fourier transform),
γ an 8th-root anomaly calibrated by (ST)³ = S²) and P_w permutes indices by the
Weyl action. The C-twist inserts −1 = S²: Z_C expands over the coset −W. Twelve
signed terms total.

**(M) The tone menu:** each term t_{±w}(κ) = tr(ρ_Weil(W)∘P_{±w}) is a Gauss
sum whose conductor is det(A ⊗ (±w) − I₄) = Π_{i,j}(α_i β_j − 1) (α = eigenvalues
of the monodromy, β = of ±w). Computed menus (registered NOW, before the sweep):
- golden (tr 3): w=I: 1; w=−I: 25; rotations: 16, 4; reflections: ±5 ⇒ the
  divisor menu {2,4,5} with 4 = √16 and 5 from ±I/reflections — LAW-O's tones.
- silver (tr 6): I: 16; −I: 64; rotations: 49, 25; reflections: 32 ⇒ menu
  {4,8,7,5,2^{5/2}} — **the observed mysterious {5,7} = the rotation terms**
  N(α·ω − 1) = tr² ∓ tr·1 + ... (the object's unit twisted by the stage's own
  ℤ₃ symmetry). This is the sharpest registered prediction.
- bronze (tr 11): I: 81; −I: 169; rotations: 144, 100; reflections: 117 ⇒
  menu {3,9,13,12,10,√117}; the observed κ=10 SILENCE must arise as
  cancellation between terms (to be exhibited numerically), and the κ=19
  firing must come from non-cancelling unit-strength (Jacobi-oscillating)
  generic terms — bronze's larger determinants break the golden cancellation.

## The cells

- **W1 (the identity):** build ρ_Weil per κ, verify (D) exactly against the
  banked B238 Z values for RL, RRLL, RRRLLL across κ = 4..20. Falsifier: any
  mismatch beyond numerics ⇒ the factorization is wrong — stop.
- **W2 (per-term loci):** tabulate the twelve t_{±w}(κ) for the three words,
  κ = 4..26; check each term's firing/oscillation against its registered
  conductor; exhibit LAW-O as the assembled sum; exhibit the bronze κ=10
  cancellation.
- **W3 (the channels):** the θ-odd/θ-even split per term (odd = the ∓ coset
  reweighting); locate which terms carry the 4-tone and 5-tone; explain why
  LAW-E resisted a divisibility law (unit-determinant terms oscillate as
  quadratic characters of κ, they do not gate).

## Discipline

Gates: ρ_Weil unitary; (ST)³ = S² after calibration; the antisymmetrized
subspace dimension = #primaries per κ; (D) exact on the banked values before
any menu reading. MB13: B204/L24/L33 (the SU(2) mechanism + period law,
PROVED; Jeffrey known), B210/B211 (congruence shadows), B585 (LAW-O, the
hold-outs), B586 (stage data) — found and cited; the SU(3)/Weyl-twisted menu is
the L24(c) open reach, computed here.
