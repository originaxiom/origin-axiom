# B930 — THE OVERLAP MATRICES: the canonical gauge sees nothing; the branches are superselected; the twist-vs-Galois rotation is exactly orthogonal with a rational spectrum

**Date:** 2026-08-06 · **Seat:** computation agent (instrument) + cc (four logged
design amendments + banking) · blind throughout — no measured number contacted;
any comparison with measured mixing is a future sealed cell behind the owner's word.

## The three overlap geometries (exact; independent dps-80 belt green, 81/81)

**(a) The two ω-branches (the up/down analogue): SUPERSELECTED.** Under H₊ every
cross pairing vanishes exactly (the even/odd label rules); under H′ the ±
projector overlaps *also* all vanish (the same-generation overlap is the K-element
0). The branch quantum number is orthogonal in BOTH gauges — at projector level
the object permits NO up/down mixing at all. Whatever the twist mixes, it is not
the branches.

**(b) The S-vs-A registers under H′: A GENUINE HIERARCHY.** The 3×6 collapses
exactly to one 3×3 (h′(S, A₋) = conj h′(S, A₊)); the moduli cascade
(0.869, 0.672, 0.583, 0.225, 0.195, 0.151 — pairs merged) spans a factor ~5.8;
the same-generation overlap-squared is ONE K-element with minimal polynomial
**953⁴x³ − 230571559875x² − 16394578125x − 5¹²** — the value prime leading, the
GOLDEN prime closing: the mixing geometry is where 5 enters the value layer.

**(c) The twist frame vs the Galois frame: AN EXACT ROTATION.** The flip
compression on the vacuum register W3 has rational characteristic polynomial
**(x − 1)(1536x² − 2088x + 677)** — principal cosines² = {1, 0.8254…, 0.5340…};
the register is uniformly NEGATIVE under h (signs threaded); the rotation
R ∈ O(3) EXACTLY, its unity column being precisely the banked F∩W3 line — one
exact unity principal angle between the twist and the vacuum register. The trace
sum rule tr W3 + tr W6 + tr W18 = 151/64 + 169/64 + 6 = **11** exactly (= the
flip rank; the B928 sum rule reappearing block-refined).

## The doctrine as a selection rule (verified, not assumed)

The instrument's FORCED checks all held: **under the canonical gauge there is no
overlap to read anywhere** — cross-register, cross-generation, cross-branch, all
exactly zero. Every mixing-shaped quantity in the object lives in the twist D₂
and only there. B923 said the hierarchy lives in the twist; B930 says the
*geometry of comparison itself* does.

## The instrument story (four amendments, all logged, all caught by its own aborts)

(1) a K/N-type slip (the first crash); (2) positivity assumed where B912's bank
says indefinite; (3) the register's uniform sign unthreaded through eigen
comparison; (4) solver phases unaligned on rational lines (the b/a belts were
phase-free and blind to it; the c-belt caught it). Definiteness-blindness three
ways — the same lesson the colored cones taught in B913, now a checklist item.

## Files

`overlap.py` → `results.json`, run logs 1–7 · locks: `tests/test_b930_overlap.py`.
