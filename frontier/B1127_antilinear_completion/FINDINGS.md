# B1127 — V-2′ THE ANTILINEAR COMPLETION: compact color is reachable INTO the object's own M(𝕆,ℂ), by a SECOND-LEVEL conjugation — but the object's arithmetic mirror is trivial on the color layer

**Status: banked (frontier). Verdict PROVED for the MATH (an antilinear conjugation gives
compact su(3) color (0,8) in E₆(−26)=M(𝕆,ℂ); specific 4/48, controls green, deterministic).
The FRAMING is FENCED, not claimed: whether the compactifying conjugation is "the object's
own" or "the observer's" is UNRESOLVED and relayed to cc3. Value-campaign cell V-2′ (the
antilinear completion of B1125/V-2). Gate 5 untouched. Lock `tests/test_b1127_antilinear.py`.**

## The question (V-2′, the owner's "another-level measurer")

V-2 (B1125): no LINEAR sign-lift gives compact su(3) color — compact real forms need an
ANTILINEAR conjugation. Does the object's own antilinear structure (the mirror,
amphichirality) supply it? Extended V-2's verified 48-element machinery to the antilinear
torsor (each element composed with τ, coordinate complex-conjugation).

## THE MATH (verified within construction; deterministic re-run)

> **σ_mirror = τ ∘ θ_A gives color I2 = (0,8) — COMPACT**, and its global form is (26,52) =
> **E₆(−26) = EIV = M(𝕆,ℂ)** — the object's OWN magic-square algebra (B882).

- **Required control passes**: σ_c = τ∘θ_split gives global (0,78) and color (0,8), both
  compact — the detector genuinely sees compact color when present.
- **Specific, not generic**: exactly **4 of 48** torsor elements give compact I2, ALL in
  antipodal/class-A (the mirror's "identity-on-color" class), all (0,8), all 300-trial
  automorphism-reverified. The other 44 (antipodal/B, permute/A, permute/B) give (5,3) or
  (4,4) — indefinite. Compact color is tied to the mirror's class, not to any conjugation.
- **Controls fence it**: a secondary construction σ'=τ∘θ_split∘θ_A gives (5,3) NON-compact
  (so it is not "any antilinear map compactifies"); the unsigned diagram fold fails the
  automorphism check (38/60) — a genuine signed lift is required, checked not assumed.
- **Bonus**: the SAME real form E₆(−26) that B1125 reached LINEARLY (where I2 stayed stuck
  at (5,3)) is reached here ANTILINEARLY with I2 genuinely compact — the near-miss resolved.

## THE FRAMING FENCE (load-bearing — the agent's own §5, upheld here over its headline)

**The object's OWN arithmetic mirror does NOT supply this.** B1114 proved I1, I2 are cut out
by **ℚ-rational** conditions; a ℚ-rational factor is fixed **pointwise** by any field
automorphism of ℂ/ℚ — so the literal ℚ(√−3)-Galois twist (√−3 ↦ −√−3, the object's mirror)
**acts as the identity on the color layer** and can carry no antilinear structure there.
(B1114's own words: it fixes I1/I2 setwise — here sharpened to pointwise, since the factors
are ℚ-rational-spanned; the sharpening is what the detector needs and it holds.) The τ that
actually flips (5,3)→(0,8) is the **GENERIC** complex conjugation — B1114's "the observer's
choice of real structure, **not the object's**." **So compact color is the OBSERVER'S
archimedean closing — into the object's own M(𝕆,ℂ) form.** The V-2′ agent's final-report
headline ("the object's own mirror-conjugation gives compact color") over-reads its own §5
fence; this bank keeps the fence, integrating the math and quarantining the framing.

**The one open bridge (relayed to cc3, the B1114 seat).** The agent notes τ "specializes to
√−3 ↦ −√−3 on the object's actual arithmetic HOLONOMY layer" — a DIFFERENT, cited-not-
rederived layer from the ℚ-rational combinatorial e₆ where I2 lives. If the object's
holonomy-layer mirror descends to a nontrivial antilinear action on the combinatorial I2,
the "object's own" reading could partially revive. That is a real-form question for cc3.

## What it settles, and what it leaves

SETTLED: compact su(3) color is reachable, and precisely inside the object's OWN algebra
M(𝕆,ℂ), by a second-level (antilinear) conjugation — V-2's "second-level closing" made
concrete. The object supplies the color ALGEBRA and even its compact-color FORM. OPEN: the
compactifying conjugation is generic (the observer's), the object's own Galois mirror being
trivial on the color layer; whether the holonomy-layer mirror bridges to it is cc3's to
determine. Either way, compactness is a real-structure closing at the archimedean place —
the exact twin of B1114's signature. Gate 5 untouched.
