# B637 — THE CORRECTED CELL 3 (the h¹ jump locus + the alternating cubic; prereg)

**Preregistered 2026-07-15, sealed BEFORE any B637 computation. This is
the audit-corrected successor of L89/B634-G2..G5 (see B634 ERRATUM_1 and
B635). Mathematics only; NO SM number; Gate 5 stands (no recycling of
B631's structured-null stage numbers under new labels).**

## The corrected theorem statement (audit Gate 1)

For each explicitly specified gluing/local system on the menu below,
compute twisted Mayer–Vietoris/amalgam cohomology H*(D; 27) exactly over
ℚ(ω); report the h¹ jump locus; where h¹ ≥ 3, compute the ALTERNATING
cubic 3-form Λ³H¹(D;27) → ℂ through the explicit 45-monomial invariant
cubic (B632), with the corrected inverse-letter cup chains and full
coboundary controls. The 3-form is alternating BY THEOREM (Koszul); no
symmetric object is claimed anywhere.

## The menu (permanent names per TERMINOLOGY.md; no invariant transfer without a map)

1. **D_bent(M; m), m ∈ {none, 1, 4, 5, 7, 8, 11}** — the B582/B598 weld
   (μ fixed, λ inverted: diag(1,−1); peripheral compatibility banked)
   with side-2 bent by c_m = exp(v_m) (the banked dial slots, step7).
   **REGISTERED PREDICTION (to confirm with a method-disjoint pipeline):
   h¹(D;27) = 2 at m = 4, 8 (⇒ Λ³H¹ = 0, no 3-form exists there) and
   h¹ = 5 at none/1/5/7/11 (⇒ Λ³H¹ is 10-dimensional — the 3-form is
   the arc's genuinely new object).** Source: the audit's reproducer,
   reproduced in-sandbox at B635; never computed by this seat's own
   machinery.
2. **D_g(M) for the four B605 D₄-intertwiner gluings** — the exact
   SL(2,K) intertwiners lifted through Sym¹⁶⊕Sym⁸⊕Sym⁰; THE GATE: the
   peripheral gluing equations must hold exactly in GL(27) (meridian
   and certified longitude abABaaBAbA); families failing the gate are
   recorded INCOMPATIBLE (the −A₁ precedent) — no forcing.

## Method disjointness (the B577 standard)

- THIS SEAT: the amalgam-Fox route — the 4-generator presentation
  ⟨a,b,a′,b′ | r, r′, μ′μ⁻¹-identification, λ′λ^{±1}-identification⟩,
  Fox calculus on 108-dim cochains, the B575 exact field toolkit; the
  mirror side via the ℚ(ω) conjugation automorphism (ω ↦ ω²).
- cc2 (coordinated): the M–V matched-pair route (their cell 3a).
- The audit's reproducer: the external third (already run twice).
All three must meet at the dimension table; any discrepancy is
adjudicated BEFORE any 3-form value is computed.

## Controls (before values, per the standing rule)

- Trivial-coefficient control per gluing: h¹(D;ℂ) from the same
  pipeline must match B591-M4's formula (g = ±I → b₁ = 2 [ℤ ⊕ the
  matched class]; the diag(1,−1) weld → b₁ per the banked formula).
- Every cup evaluation: the corrected inverse-letter chains + full
  coboundary-shift invariance (all slots) + the alternating checks
  (diagonals zero, all transposition signs) — assertions, not prints.
- h⁰/h²/Euler bookkeeping per gluing; the h⁰(D;27) = 0 audit datum at
  the bent welds re-confirmed by our route.

## Outcomes (locked)

- Predictions confirmed + 3-form ≡ 0 at every h¹ ≥ 3 point ⇒ the
  mirror-coupled classical cubic content is EMPTY: the mirror-axiom
  value route is closed at the classical-cohomology level; banked,
  report, stop (no "next construction" improvisation).
- A NONZERO alternating 3-form anywhere ⇒ a new exact invariant of the
  coupled object (10 components at h¹ = 5, exact values, fields,
  conductor-divisibility read) — mathematics; any physics reading waits
  on L91's typed functor; NO comparison without the owner + the full
  checklist.
- A dimension-table discrepancy between pipelines ⇒ adjudication arc
  first (the B577 pattern), values second.

## MB12 (computed at prereg)

- Non-trivial: this seat has never run the amalgam-Fox route on any
  double; the 3-form at h¹ = 5 has never been computed by anyone (the
  audit stopped at the dimension table).
- Can fail: predictions can be contradicted (pipeline discrepancy);
  the D₄ gluing gates can all fail (INCOMPATIBLE rows); the 3-form can
  vanish identically (the honest kill).
- Can pass: a nonzero exact 3-form at the unbent/odd-bent doubles is a
  live outcome (Λ³ of a 5-dim space through a nonzero cubic has no
  generic-vanishing theorem protecting it either way).
- MB13 (swept): no prior amalgam-Fox double computation (grep clean);
  the audit's M–V and cc2's 3a are the coordinated disjoint routes, not
  collisions; the bend machinery = banked step7/steps3and5 assets.
