# B857 — the being voice cannot say 5, and the reason is a SPLITTING TYPE, not a silence

cc banking seat, 2026-08-02. Mathematics scope; nothing to `CLAIMS.md`; Gate 5 untouched.
**Not preregistered** — this replaces a grep with a theorem-level fact; its footing is that the
arithmetic is elementary and exact.

## 1. The leg that was a grep

`docs/LAW_MAP.md`'s **H-EAR clause (iii) SILENCE** states that the golden is *"NEVER emitted through
the object's solo continuous channel."* Its supporting evidence is
`frontier/B746_golden_ledger/FINDINGS.md:30` — ***"Grep of the banked voice artifacts (B737/B739):
zero golden markers."***

A grep over artifacts is not a proof of absence. Here is the fact it was reaching for.

## 2. The fact

The voice is `ζ_K(s) = Σ a_K(n)/nˢ` over **K = ℚ(√−3)**, with `a_K(n)` = the number of ideals of
norm n. For an **inert** prime p, the only ideal above p is (p), of norm **p²** — so `a_K(p) = 0`.

| field | role | 2 | 3 | **5** | 7 | 11 | 13 |
|---|---|---|---|---|---|---|---|
| **ℚ(√−3)** | **being** — m004's trace field | inert | **ram** | **inert** | split | inert | split |
| ℚ(i) | silver — m136's trace field | ram | inert | **split** | inert | inert | split |
| **ℚ(√5)** | **hearing** — the pre-geometric carrier | inert | **inert** | ram | inert | split | inert |

> **a_K(5) = 0. The being voice's Dirichlet series has NO COEFFICIENT at the hearing prime.**
> **a_K(3) = 1 — the being prime is ramified, and the voice says it at every power.**

**And the blindness is mutual: 3 is INERT in ℚ(√5)**, so the hearing field cannot say the being
prime either. Neither face can pronounce the other's prime.

## 3. The correction to H-EAR's scope, which matters more than the fact

**5 SPLITS in ℚ(i): a(5) = 2. The SILVER's voice can say 5.**

So the phenomenon is **not a property of voices**, and not a property of the golden. It is the
**splitting type of one prime in one field**. H-EAR (iii) as written generalises a splitting type
into a law about emission. **The correct statement is narrower and sharper:**

> *the being field's voice is blind to 5 because 5 is inert in ℚ(√−3) — and a sibling field with 5
> split hears it perfectly.*

## 4. Why this is the right kind of replacement

The grep ran over B737/B739 artifacts that **predate B797 by a week**, and B739 itself records that
*"the discrete newform spectrum is now the UNIQUE home of the level's arithmetic"* — i.e. the one
place the claim is checkable is the place the grep did not look. **An absence established by
searching the wrong artifacts is not an absence.** The splitting type needs no artifacts at all.

## 5. What this does NOT establish

- **It does not test the discrete channel.** `a_K(p)` is the *continuous* voice's Dirichlet
  coefficient. Whether the **17 certified Maass newforms** carry golden markers in their **Hecke
  eigenvalues** is a different question and **remains uncomputed** — that is the actual blind spot.
- **It does not make the object special.** Inertness of 5 in ℚ(√−3) is a fact about the field, and
  the field is shared with the whole commensurability class.
- **Nothing to `CLAIMS.md`.**

## Carried forward

1. **Compute one Hecke eigenvalue** of one Γ₄₁ newform. That is the smallest computation that would
   test H-EAR (iii) where it is actually checkable, and the corpus has never done it.
2. **Correct H-EAR (iii) in `LAW_MAP.md`** from a silence to a splitting type.

`tests/test_b857_voice_splitting.py`
