# B675 PREREG ADDENDUM — the three-outcome vocabulary (sealed BEFORE
# the running sweep's result exists; cc2's pre-registration point,
# adopted; main seat, 2026-07-18)

The silver test ("does ℚ(i) select A₃?") has THREE sealed outcomes,
not two — because the silver is bifocally entangled (banked
B649/B663: its trace field ℚ(ζ₈) is the COMPOSITUM of ℚ(i) and
ℚ(√2); the golden's clean split does not exist there):

1. **CONFIRMED** — the silver cusp lattice conformally quantizes the
   A₃ weight lattice (certificate) and fails A₂ (field mismatch):
   H-CUSP predicts across objects; the descent and stage selection
   fuse.
2. **KILLED** — the failure is PURELY ℚ(i)-level: the cusp lattice's
   ℚ(i)-structure itself admits no conformal embedding into A₃'s
   weight lattice (the entanglement plays no role in the
   obstruction). The principle fails on a clean case.
3. **ENTANGLEMENT-CONFOUNDED** — the failure is attributable to the
   √2-part: the cusp lattice is not defined over ℚ(i) alone (its
   Gram/translation data genuinely needs the ζ₈-compositum), so the
   principle's PRECONDITION (a clean cusp field) is absent rather
   than its prediction wrong. Consequence runs OPPOSITE to a kill:
   the golden would be further distinguished as the unique metallic
   whose cusp field is clean enough for H-CUSP to apply — feeding
   L91's uniqueness line, not undermining it.

THE DISCRIMINATOR (stated now): compute the silver cusp translation
lattice's field of definition FIRST. Defined over ℚ(i) ⇒ outcomes 1
vs 2 decide by the embedding computation. Needs the compositum ⇒
outcome 3, and the A₃ test is then run on the ℚ(i)-projection with
its confounded status declared. The bronze leg carries the analogous
confound (f = 3 vs d_K = 13 ambiguity — declared, same discipline).
