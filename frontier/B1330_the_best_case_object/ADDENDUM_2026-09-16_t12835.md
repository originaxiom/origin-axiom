# ADDENDUM 2026-09-16 — the last target: t12835 computed exactly, and the index is zero there too (the outside bench's cell "THE LAST TARGET", re-run on main with this arc's own `final_index.py`; B1414)

**Nothing above is struck.** The "Not done" line of the FINDINGS names s958, t12833 and t12835; the two addenda of 2026-09-11 computed s958 (20 exact in-domain sectors, all zero) and t12833 (16 sectors, all zero) and left t12835 *"pending at write time"*. The outside bench (memo 232 §4 cell 13, certificate `outside_bench/certificates/the_last_target.py` over the vendored `final_index.py`'s raw log, sealed before the run) computed t12835 in two independent processes: relators `aaBcbac` and `aCAbcaCAbcaaaBB` both evaluate to −I (a projective lift, so only the even-power germs are valid — this arc's own lesson), 2 order-3 cusp-trivial characters, **16 exact in-domain sectors, (t₀, I) = {(1,0): 4, (2,0): 6, (3,0): 4, (4,0): 2}, 12 at t₀ ≥ 2, NON-ZERO INDEX: 0** — outcome A. Its control V1 reproduced s958 and t12833 from the addenda first.

**Main's re-run**: `verification/final_index.py t12835`, unmodified, from a scratch copy of this directory (E80: the script's `sys.path` line names a scratch path; the modules it needs sit beside it). Result, line for line the bench's: relators `aaBcbac` → −I and `aCAbcaCAbcaaaBB` → −I, RELATORS VALIDATED, 2 order-3 cusp-trivial characters, **16 exact in-domain sectors, (t₀, I) = {(1,0): 4, (2,0): 6, (3,0): 4, (4,0): 2}, 12 at t₀ ≥ 2, NON-ZERO INDEX: 0** (log filed at `frontier/B1414_the_outside_benchs_memos_185_233_harvested/verification/b1330_final_index_t12835.txt`; wall time about 50 minutes on this bench).

**What this closes and what it does not.** The three named targets are now all computed and all zero; nothing about the index's standing changes — memo 230's caveat is this arc's own: there is no live positive control in characteristic zero anywhere in the record (B1297's MB12; B1335's non-zero is over 𝔽_p and its characteristic-zero half is NEGATIVE), so these zeros are evidence about the instrument's standing behaviour, not a theorem about the tower. B1334 proves I = 0 on its stated domain; the live question is isotropy (B1331/B1332).
