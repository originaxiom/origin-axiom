# B1330 — the best-case object for the index exists, is named, and returns zero

**Verdict: NEGATIVE.**

## The object nobody had looked for

B1324 found that m004's chiral one-cusped covers have almost no cusp-trivial twists — their torsion
is generated by the meridian. **That is true of the tower.** The **family** was never searched.

Intersecting three conditions over the 112-member `Q(sqrt-3)` family (every tetrahedron shape in
`Q(sqrt-3)`):

| condition | why it matters |
|---|---|
| **chiral** | amphichirality cannot be what forces the vanishing |
| **in the commensurability class** | the arithmetic that carries every claim survives |
| **order-3 cusp-trivial characters exist** | Galois-**unprotected** sectors actually exist |

gives **eight members**, of which **four are one-cusped** — so the index applies in its own stated
domain with **no multi-cusp extension owed**:

```
s958      1 cusp   |cusp-trivial group| = 3   H1 = Z/3 + Z
v2873     1 cusp   |cusp-trivial group| = 6   H1 = Z/6 + Z
t12833    1 cusp   |cusp-trivial group| = 6   H1 = Z/6 + Z
t12835    1 cusp   |cusp-trivial group| = 6   H1 = Z/6 + Z
```

(plus `s959`, `t12843`, `o10_150689`, `o10_150692` at two cusps)

**This is the object §12 of THE PAPER says is still needed, and it needs none of the machinery §12
says is missing.** The named next computation — the 54 multi-cusped chiral covers, blocked on the
multi-cusp index — was never the only way to ask the question.

## And it returns zero

`v2873` computed in full: **952 in-domain sectors**, three primes, seven germ shapes.

| `t_0` | sectors | non-zero `I` |
|---|---|---|
| 1 | 272 | 0 |
| 2 | 272 | 0 |
| 3 | 272 | 0 |
| **4** | **136** | **0** |

**680 sectors at `t_0 >= 2`**, up to a 4-plane in an 8-dimensional space where half-dimensionality is
a real condition and not a consequence of rank.

**The first pass was in the weak regime and had to be corrected.** With odd `Sym` powers every
sector had `t_0 <= 1`, because `v2873`'s peripheral holonomy carries a `-1` that kills odd blocks.
Even powers were needed to reach `t_0 >= 2`. This is the **same bias** that B1329 corrected, caught by
the same check — look at the `t_0` distribution before believing a zero.

## What it means

This is the **fifth independent regime** in which the index returns zero — m004's tower, 98 census
manifolds, summed germs, the earlier 9 108, and now the best-case object. **The vanishing conjecture
is the strongly favoured reading**, and the 54 multi-cusped chiral covers look correspondingly less
likely to pay.

The honest consequence for the ledger: the chirality row should be driven toward **closure as a proved
boundary**, not kept open as a search. What is owed is the theorem, not more sectors.

## Not done

`s958`, `t12833` and `t12835` carry **three-generator** presentations, where brute-force
`SL(2,F_p)` enumeration is `336^3` and was not run. **They are named, not computed.** A vectorised
or table-driven enumeration would reach them.

Reproduce: `verification/target.py` (the family intersection), `verification/run4.py` (the v2873 index).
