## AUDIT SYNTHESIS — "Are You Sure" adversarial pass (9 negatives)

### (1) HEADLINE COUNTS
- **CONFIRMED: 4** — PHYS-REFUTED, SIG-GENERIC, NOT-3MFD, NOVELTY-COROLLARY
- **SHAKY: 2** — DGG-ABELIAN, GATES-SEALED
- **CRACKED: 3** — B519-NOCROSS, C3-CONE, CHILD-NOTSHORT

Two-thirds of the corpus survived clean or standing; one-third took concretely-recomputed damage. The damage is concentrated, not diffuse — and it clusters by failure mode (see §3).

### (2) CRACKS AND SHAKIES — error + reopen

**B519-NOCROSS (CRACKED / necessary-not-sufficient).** Error: the negative's root refutes B1 by asserting mixed-chain gap labels "ARE the Perron modules" and gap-labeling is "additive over combined substitutions (known)" — conflating a necessary K-theoretic constraint on *allowed IDS values* with a sufficient guarantee of *which gaps open*. Confirmed tells: test_b519.py only checks 1/φ==φ−1 and 1/2==1/2 and asserts additivity in a code comment (no spectrum computed), while test_b518.py actually diagonalizes and thresholds gap widths. Thue-Morse is the textbook case where labeled gaps fail to open. Reopen: re-float B1 as a genuinely falsifiable crossing candidate; if the negative is to stand, re-argue it on the model-confirms-model meta-logic (experimenter chooses the substitutions), NOT the false known-theorem guarantee; replace the comment-assertion with an actual demonstration that both gaps open. B2/diffraction may survive independently via Damanik–Fillman (frontier/B173), but drop the bundled "guaranteed by known theorems" mechanism.

**C3-CONE (CRACKED / backwards-argument + proxy-for-object).** Error: running the committed c3_malament.py contradicts the banked root — the certificate doesn't compute its own FINDINGS table (lyap()'s sgn convention sends TM's genuine zero eigenvalues to spacelike, so TM prints "(3,1) proper, preserves 1.000" where the table says degenerate); the clause "(3,1) is confined to the evolution verb" is false in the code (decimation-pd and TM also show (3,1)); the STRUCTURE nugget "causal ⟺ det=±1/evolution verb" is refuted by the code (TM det=0 and decimation-pd det=−8 both preserve at 1.000); "four causal types" is actually three. A Lyapunov magnitude count was substituted for a Lorentzian signature. Reopen: reissue resting ONLY on the two bug-independent grounds — (a) decimation-2I is a genuine (2,2) map that leaks M*'s cone (0.959) and cannot preserve any single-timelike cone; (b) TM/erasure has det=0, is non-invertible, so Malament's premise fails. Strike the false clause and retract the nugget; fix lyap() to classify near-zero eigenvalues as null; re-run and confirm the table before re-banking.

**CHILD-NOTSHORT (CRACKED / necessary-not-sufficient, census completeness).** Error: the census is arithmetically false. hunt_d5.py's alarm fires before the resultant, and the TIMEOUT handler logs "TIMEOUT" with no factor/polgalois/d_K check; the results file has 141 word lines but 26 are bare TIMEOUT with no HUNT COMPLETE. True coverage is 115/150 (77%), not the banked 141/150 (94%), and the 35-word gap is biased toward the D/M-heavy high-degree region where a degree-4 child would most plausibly appear. The KILL rests on a false census. Reopen: correct FINDINGS to 115 analyzed / 35 unchecked; re-run the 35 D/M-heavy words with removed timeout or a tractable method (Gröbner over F_p, targeted quartic-subfield search) before any KILL is banked. Among the 115 the signal is genuine (0 hits) so the qualitative claim may yet stand — but not on the current grounds.

**DGG-ABELIAN (SHAKY / generic-overclaim).** Uncertainty: the load-bearing universal "gauge group is abelian U(1)^{r_K} at EVERY rank K" is a bare literature citation (higher_ptolemy.py:29-30, DGG 2014), not a computed output; the code computes only rep counts and trace fields, which are orthogonal to the gauge conclusion. The one higher-rank corroboration (SL(4)) is tool-blocked (sage Gröbner >12 min, needs Magma). No lens independently computed a gauge group — their exoneration trusts the same citation. Reopen: anchor "abelian at every K" to a specific DGG-2014 proposition establishing rank-independence, AND/OR complete the SL(4) Ptolemy computation in Magma and derive the K=4 gauge group.

**GATES-SEALED (SHAKY / backwards-argument).** Uncertainty: Gate C's conclusion is correct and externally grounded (rank-2 Eisenstein ω-multiplication, 2∤3; e₆ = 24 ⊕ 27_ω ⊕ 27̄_ω²), so not CRACKED — but 2 of 3 in-node legs run backwards: T=companion(Φ₃) is the reduced isotypic block, and a genuine 3-cycle's reduced block ALSO has Fix=0 and no eigenvalue 1, so those tests don't discriminate. The decisive discriminators (16≠cube, rank-2) are cited/hard-coded, not derived in-node; "independently recomputed in full" is inflated. Reopen: rewrite the verifier to DERIVE the deck action as rank-2 ω-multiplication from the commensurator geometry, replace the backwards Fix=0 / no-eigenvalue-1 legs with the real discriminators (2∤3 forbids A⊕A⊕A; 27_ω⊕27̄_ω² has no fixed third 27, contrasted against a true permutation matrix P), and recompute |A|=16 from the object rather than hard-coding.

### (3) META-PATTERN — one mode, three faces

The cracks are **not** independent. Every failed negative is a variant of a single error: **a NECESSARY / constraining condition (or a cited proxy) treated as a SUFFICIENT verdict.**

- **necessary-not-sufficient outright:** B519 (gap-labeling constrains allowed values, not openness) and CHILD-NOTSHORT (77% census licenses a universal KILL). Same shape: a partial constraint promoted to a total guarantee.
- **backwards-argument / proxy-for-object:** C3-CONE (Lyapunov magnitude count substituted for Lorentzian signature; a non-computing certificate) and GATES-SEALED (companion-matrix Fix=0 test that a true 3-cycle also passes). Same shape: a proxy quantity read as if it were the object, with the inference direction reversed.
- **generic-overclaim:** DGG-ABELIAN (a single citation stretched by a universal "at every K"). Same shape: local/cited evidence promoted to a universal quantifier.

These are precisely the seat's two historically-banked failure modes — **backwards argument** and **proxy-for-object** — plus their close cousin, **necessary-mistaken-for-sufficient**. The audit did not find scattered slips; it found the seat's known blind spot recurring in exactly the negatives whose in-node certificates *did not actually compute the discriminating fact* (B519 and C3 both have tests that assert rather than diagnose; GATES posits rather than derives; DGG cites rather than computes; CHILD logs TIMEOUT rather than checks). **The reliable tell across all five is: the load-bearing line is asserted/cited/proxied, not recomputed** — and every negative where the certificate genuinely recomputed its own load-bearing arithmetic in-sandbox (the 4 CONFIRMED) survived.

### (4) DOES THE MASTER NEGATIVE STILL STAND?

**Yes — the master negative (physics refuted-as-stated) STILL STANDS, but on a narrowed and now-explicitly-audited base.**

The master claim itself is PHYS-REFUTED, which came back **CONFIRMED** under all five lenses with independent in-sandbox recomputation: h(−15)=2, units {±1} for d<−4, the ℤ/2-on-discrete-invariants Galois inference running in the *correct* direction, the empirical nulls (B322; B334's 137-test at ≈0.556), and the untouched Galois-orbit/trace-ring mechanisms. The one error found there (B333's h=2 undercount) is non-load-bearing and runs charitably. The three CONFIRMED structural pillars that flank it — SIG-GENERIC (signature is a splitting-type/Pisot proxy, not golden-forced, with a valid non-golden tetranacci control), NOT-3MFD (dilatation asymmetry 3.6762≠3.0523 recomputed by pure word-combinatorics), and NOVELTY-COROLLARY (elementary self-certifying derivation) — all held under independent recomputation.

**What the cracks actually cost:** none of the three CRACKED negatives is the master. They are (a) an *external-crossing* headline (B519: "ZERO external crossings" is not established on its banked grounds — B1 must reopen), (b) a *causal-structure* peripheral gate (C3-CONE: the "no shared cone / Malament n/a" bottom line survives ONLY on the two bug-independent rows, not the banked narrative), and (c) a *child-word* completeness census (CHILD-NOTSHORT: the KILL is un-banked pending 35 re-runs, though the analyzed 115 show 0 hits). The two SHAKY items (DGG abelian-at-every-K; GATES-C in-node seal) are corroboration gaps on claims that are externally true, not falsehoods.

**So the master now rests on:** the CONFIRMED core arithmetic genericity of the ℚ(√−15) seam plus its empirical value-nulls and untouched orbit/trace-ring mechanisms — NOT on (i) the "zero external crossings" headline (withdrawn to "B1 reopened"), (ii) the "single shared cone forbidden by four causal types" story (reduced to two rows), (iii) the "child is not a short word" universal (downgraded to 115/150, 0/115), (iv) an unverified "abelian at every K" citation, or (v) GATES-C's self-contained in-node seal. The value-firewall's *refuted-as-stated* verdict is intact; several of its advertised *supporting* walls are now known to be either proxy-argued or citation-thin and must be re-derived before they can carry weight again. The honest posture: **the negative holds, but the corpus overstated how much of it was independently computed — the seat's own proxy/backwards blind spot inflated five subsidiary seals, and those five must be rebuilt on recomputed discriminators, not re-asserted.**
