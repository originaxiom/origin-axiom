# FINDINGS — B735: the object's EMITTANCE (heartbeat / voice) — a real third face, same walls

cc banking seat, 2026-07-21. cc2's Track 2 (wf_16e0a185, owner-prompted: "the object's heartbeat/
voice/emittance"), verified-on-receipt by cc in-sandbox. The spectral/emitted face we'd ignored is
REAL and genuinely under-explored — but hits the SAME walls. Pure math; Gate 5; nothing to CLAIMS.

## THE THREE VERIFIED FACTS
1. **HEARTBEAT = ℤ[ω] (Eisenstein-constrained), realizations transcendental.** The length-spectrum
   traces (2cosh(L/2) for each complex geodesic length L) are Eisenstein integers ℤ[ω]=ℚ(√−3) —
   verified: (3±√−3)/2, 1±√−3, (5±√−3)/2, … So the object's "heartbeat" is ℤ[ω]-algebraically
   constrained (the BEING field). But the realized lengths ℓ=2log|λ| are TRANSCENDENTAL reals,
   dense in ℝ₊ (prime geodesic theorem). A THIRD kind of data: neither the finite 𝔽₂ observer-menu
   (B733) nor a free continuum — a countably-infinite ℤ[ω]-parametrized family with transcendental
   realizations. Growth rate GENERIC (every hyperbolic 3-manifold).
2. **VOICE = the cusp; "closing silences the voice" is non-metaphorical.** A cusped hyperbolic
   3-manifold has CONTINUOUS Laplacian spectrum [1/4,∞) (Eisenstein series at the cusp) + discrete
   resonances; m004 has 1 cusp (open). Dehn-filling CLOSES it → purely DISCRETE spectrum — verified:
   m004(5,1) has 0 cusps. So the continuous emission requires the OPEN cusp and vanishes the instant
   the object is closed. Ties to the child-integration (the hearing dies on closing, B718).
3. **The being's voice embeds in the object's.** m004 = degree-12 cover of the Bianchi orbifold
   PSL(2,O₃)\H³ ([PSL(2,O₃):Γ]=12, B734) → the being's whole automorphic spectrum pulls back into
   the object's voice as a sub-voice + index-12 "new" resonances (Koyama-type equidistribution;
   class number 1 = ℚ(√−3)'s case). Verified consistent.

## SAME WALLS (the honest verdict)
- The continuous-spectrum facts are **GENERIC** (base rate 1 — every cusped hyperbolic 3-manifold;
  the sister m003 shares them). Not diagnostic of m004.
- **VOICE's flagship over-claim, CAUGHT (cc2 internal critic):** "the cusp-scattering = B723's
  observer-coupling site" does NOT survive — B723 is a FIELD-level construction (shared by m003/
  m004/…); the OBJECT-level observer (KMS/Galois on m004's OWN Bianchi arithmetic + cusp) is the
  UNBUILT next door. An open door was reported as closed; retracted.
- The track stalls at "structure exists" (no Maass eigenvalue computed — Sage unavailable), so it
  never reached the value-matching stage (the worst look-elsewhere surface) — honest, not a result.

## What is worth keeping (two exact gains, both enrich the observer/closing picture, neither a value)
(1) **the emission = the OPEN cusp; closing the object silences it** — non-metaphorical, ties to the
being-only child (B718). (2) **the being's full automorphic spectrum embeds in the object's voice.**

The emittance is a genuinely different (continuous-flavored, analysis-level) face than the discrete
arithmetic — real, previously ignored — but it meets the same walls: generic, the observer
connection over-claimed (the object-level observer is still unbuilt), and reaching values would be
look-elsewhere. The genuine next door both the congruence work (B734) and the cusp emission now
point at, from two sides, is the OBJECT-LEVEL observer (m004's own Bianchi KMS/Galois + cusp).
Credit cc2 (the track). Nothing to CLAIMS; no physics value.
