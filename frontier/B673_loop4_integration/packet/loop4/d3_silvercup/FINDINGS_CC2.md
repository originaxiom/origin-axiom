# D3 — BANKED: THE K020 VERDICT IS FORM-MATCH — THE SUPRA-CLASS COUPLING IS
# STRUCTURAL (cc2 + subagent, 2026-07-17; prereg 8ffb0eb6; exit 0)

RESULT (exact, silver field L = Q(s,i), s^4 = 8s^2+16, zero floats; all
genuine gates True — the lone False flag is the fork-outcome "all zero?"
flag, False BY DESIGN): the SILVER vector cup pairing [u_i cup_x u_j] in
H^2(D_silver; 27-bar) has:
1. ZERO-PATTERN IDENTICAL TO GOLDEN'S FULL TABLE (D1): exactly ONE
   off-diagonal antisymmetric pair vanishes ([c_01] = [c_10] = 0) plus the
   Koszul diagonal; ALL other 18 off-diagonal pairs NONZERO with valid
   witnesses (e.g. witness[c_23] = 13i/9072000, pure imaginary; perfect
   antisymmetry witness[c_ij] = -witness[c_ji] across the table).
2. RANK IDENTICAL: the 25 classes span rank 5 in the 31-dim superspace —
   the same rank-5 fill as golden.
3. VALUES DIFFER BY FIELD exactly as K020 requires (golden Q(sqrt-3) vs
   silver Q(zeta_8)-tower).
VERDICT: **FORM-MATCH.** The nonvanishing vector cup — the supra-class
coupling — is a STRUCTURAL property of the metallic family, not a golden
accident. (The script's printed "PARTIAL/MIXED" compared against C1's
solo-only 6/6 pattern, written before D1's full table existed; against D1's
full golden table — one vanishing pair, rank 5 — the match is exact.)
CAVEAT + FRONTIER FEED: the silver boundary/solo SPLIT remains UNRESOLVED
(w2a); the vanishing silver pair is classes (0,1) in the w2_silver basis —
whether that pair is boundary-born (which would make the form-match exact
in MEANING, not just shape: "the boundary pair is mute everywhere") is a
new frontier item requiring the silver split resolution.
Controls: h0=1/h1=5 gates, C3 symmetric+equivariant, all c_ij cocycles,
Koszul 25/25, coboundary-shift invariance, exactness. Artifacts:
d3_silvercup.py, d3_results.json, d3_run.log. Repo untouched.
