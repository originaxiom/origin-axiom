# D2 — BANKED: ALL THREE SLOTS SPEAK — THE COUPLING SATURATES THE TENSOR
# SQUARE, WITH THE GRADED SIGN RULE REALIZED (cc2 + subagent + seat-main
# surgery, 2026-07-17; prereg 8ffb0eb6/56e5a59c; exit 0)

METHOD NOTE: the full-kernel step was replaced mid-loop by main-seat surgery
(d2_modnull.py): GF(p) discovery under BOTH sqrt(-3)-embeddings + CRT +
rational reconstruction + EXACT verification of every kernel vector over K,
completeness certified by the rank bound (dim ker_Q <= n - rank_Fp = verified
count). L351: kernel 397/1053 (rank 656). S351p: kernel 424/1134 (rank 710).
All 821 vectors exact-verified. Discovery-only-modular; certificates exact.

THE RESULT (all six solo off-diagonal pairs, exact witnesses, valid):
1. **THE 351 SLOT (Lambda^2) SPEAKS** — e.g. witness[c_34] = 37800 sqrt(-3).
2. **THE 351' SLOT (Sym^2 complement) SPEAKS** — e.g. witness[c_34] =
   -35280 sqrt(-3); the (2,3) pair carries the cubic's /13 denominators.
3. With C1/D1's 27-bar: **THE VECTOR CUP IS NONZERO IN ALL THREE SLOTS OF
   27 (x) 27 = 27-bar (+) 351' (+) 351 — the supra-class coupling saturates
   the full tensor square.** Nothing about the coupling is an artifact of
   the cross-product's 27-bar projection.
4. **THE SIGN RULE (the subtlety):** the exact witnesses show [c_ij] = [c_ji]
   IN THE 351 SLOT (equal witnesses) and [c_ij] = -[c_ji] in 27-bar and 351'
   (negated witnesses) — EXACTLY graded commutativity: swap acts +1 on
   Sym^2-valued slots (antisymmetric classes) and -1 on the Lambda^2 slot
   (SYMMETRIC classes). The script's Koszul gate applied the Sym-slot sign
   to Lambda^2 and printed False — a MIS-SPECIFIED CONTROL (the D3-comparator
   pattern); the data PROVES the correct prediction on all six pairs in both
   slots. Every genuine gate passed (27bar-reproduces-C1, Gram = 10*I, the
   section controls, slot cocycle identities, exactness).
FRONTIER FEEDS: (i) the Lambda^2 DIAGONAL — the sign rule permits NONZERO
self-couplings [c_ii] in the 351 slot (no Koszul kill); compute them.
(ii) per-slot span ranks (the symmetric 351 matrix has up to 6 independent
entries + diagonal). (iii) the witness arithmetic (37800/35280 small pure-
imaginary values vs the /13 family). Artifacts: d2_351.py, d2_modnull.py,
d2_results.json, d2_run.log. Repo untouched.
