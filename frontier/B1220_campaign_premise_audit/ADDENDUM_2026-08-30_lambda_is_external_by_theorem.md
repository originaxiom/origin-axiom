# Addendum (2026-08-30) — the owner re-probed Cell 1, and λ is placed: EXTERNAL BY THEOREM

**Not an edit. This note and the arc's Cell-1 section stand together.**

## What prompted it

This arc reported λ's gate as *written but unsatisfied* — "no (T,G) read off D." The owner asked
the right question about the B1196 contradiction it had just found:

> *"should we reprobe? maybe the cell solved it afterwards but didn't bank properly?"*

**It had. The probe was correct and this arc was incomplete.**

## What the re-probe found

**B1195 / GC-22, verdict PROVED — "THE LAMBDA PLACEMENT IS DECIDED":**

> *P1 stands (λ = a second, independent, **EXTERNAL** continuous datum of the total observer,
> **distinct in kind from σ**) and **P2 (λ = f(existing legs) via KMS/BTZ) is excluded by an EXACT
> constructive computation** — the BTZ/KMS bridge is provably **tautological** (it exactly
> reproduces B723's own already-sealed `T_mod = 2π/|log λ|` under a gravity-flavoured relabelling),
> **forced by B721's no-go theorem** (the object's own clock is exactly tracial, Δ = I, **zero
> continuous freedom**), and independently cross-confirmed by the cloud seat's own census.*

The exclusion is a computation, not an argument: BTZ horizon → Hawking temperature → the Euclidean
nome `exp(−β_H)`, matched against B723's Powers/ITPFI two-level dictionary, with the tautology
**exhibited numerically** — the sealed λ₀ = 2/5 gives r₊ = 2π/|log 0.4| = 6.85719618088, a number
already sitting in B723's own probe output.

**GC-22 also records that it resolves B1194's genuinely-missing item (2)** and amends the surface
rows: *"the lambda-placement resolves OPEN → PLACED-AS-SECOND-CONTINUOUS-DATUM."*

**B721 verified here independently** (banked NEGATIVE): *"the object's own clock is tracial type
II₁ with trivial modular flow, complementary to rather than inside the CMR KMS torsor."*

## The correct statement, and it is much stronger than this arc's

**λ is external BY THEOREM, not by default.** The object's own modular flow is *trivial* — Δ = I,
tracial — so the object has no continuous clock datum to give; an external weight is what completes
it to type III_λ; and the one concrete route that would import λ from the legs already in hand is
*exactly* tautological. The row is not an admission of ignorance. **It is a proved boundary.**

This also **reconciles GC-27 with GC-22** rather than leaving them in tension: GC-27's "λ fails the
theorem's first hypothesis — no (T,G) read off D" is not a gap awaiting work; **B721 explains why
there is nothing to read off.**

## What genuinely remains (narrower than before, and stated exactly)

- **Lemma II in full generality**: a proof that *no* (T,G) can be read off D — GC-22 excludes the
  KMS/BTZ route by exact computation and B721 removes the object's own clock as a source, but a
  general no-go over all candidate pairs is not banked.
- **Lemma I**: the uniform Kolmogorov selector-cost K(μ|D) across finite/compact/non-compact T.
  `GRAND_COMPUTATION_LEDGER` types it independently: **"H5. the Kolmogorov Selector — MISSING (no
  arc)."** Regime (i)'s log₂|T| remains the only clean bit-count.

## The class, again, and it is mine

Cell 0 swept for orphans; B1220 audited each cell's premise; **both missed this**, because GC-22
lives inside a *batch* arc's cell record — banked, PROVED, surfaced only as one clause in a long
verdict line. The one-liner is what gets read, and GC-22's finding never reached the ledger row it
amends in its own text. **Stop rule 1c: when a cell's finding amends a named surface row, the amend
is part of the bank, not a note inside the cell.**
