# B1238 — THE SEAT HARVEST, THIRD RING: the character variety IS the square-time curve (40a3, the map exhibited); bronze's invariant trace field is the octic; three propagations, one instrument rule

*2026-09-02. cc (main seat). Sources read in full, nothing merged (integrate-don't-merge): the codex seat's R037–R039 (`origin/codex/seat-r001` @ e40d46ad), the physics-seat evaluation branch's R39–R41 + the W-E absence sweep (@ 1c980e69). Every number below recomputed on this bench — `verification/` (`reproduce.sh` prints REPRODUCES). Verdict: **PROVED** (verifications + corrections; one identification EARNED). Gate 5: no measured value anywhere in this cell.*

## The prize, first

**The programme's two independently-found elliptic curves are one curve.** B211 (2026-06) found the figure-eight's non-abelian SL(2,ℂ) character variety `X^{na}: Φ(x,z) = z² − (x²+1)z + (2x²−1) = 0` and labelled it Cremona **40a1**. B509 (2026-07) found the square-time curve `d² = (c²−1)(c²−5)` = **40a3** and flagged the 2-isogeny as "verified, unInterpreted"; B510 discharged the flag as "40a3 = the Jacobian of 40a1's branch double cover". All three were reading the same equation. Completing the square in `z`:

```
4·Φ(x,z) = (2z − x² − 1)² − (x²−1)(x²−5)
```

so `X^{na}` **is** the square-time curve in the chart `(c, d) = (x, 2z − x² − 1)` — a linear change of one coordinate, an isomorphism of plane curves over ℚ, with `c` the *same* meridian-trace coordinate (B509's geometric point `(c=2, d²=−3)` is Φ's complete-structure point `x=2`, where `Φ(2,z) = z²−5z+7` gives `(2z−5)² = −3`). Its Jacobian from the quartic (`ellfromeqn` → `[0,−6,0,−20,120]`, minimal model **`[0,0,0,−2,1]`**, j = 55296/5, conductor 40, torsion ℤ/4) is 40a3 exactly, and the quartic has the rational point `(1,0)`, so the curve is isomorphic over ℚ to its Jacobian. **B211's label was wrong; its evidence could never have told**: `#X(𝔽_p) = p − 1 − a_p` over 23 primes is isogeny-invariant (a₃ = 0, a₇ = −4, a₁₁ = 4, a₁₃ = −2 across the whole class), and B211's own "torsion ℤ/4" was 40a3's number (40a1 has ℤ/2 × ℤ/2). Where 40a1 actually sits: it is the **quotient** of the character variety by the fixed-point-free involution `σ(x,z) = (−x, x²+1−z)` (the sign twist by the nonzero class of H¹(m004; ℤ/2) composed with the swap of Φ's two roots): on Φ = 0, `(X, Y) = (x², x(2z−x²−1))` satisfies `Y² = X(X−1)(X−5)` (`Y² − X(X−1)(X−5) = 4x²·Φ`). B510's covering picture had the roles inverted — the character variety is not the base of the branch cover, it is the cover; 40a1 is the base.

Consequences, in order: (i) B509's Rationality Theorem (rank 0, four rational points — `ellrank` bounds [0,0] here too) is now a statement about the character variety itself: `X^{na}(ℚ)` = {(±1, 1), two points at infinity}. (ii) Every conductor-40 / weight-2-newform / L-value statement in the record survives unchanged (isogeny-invariant); every "j = 148176/25", "40a1", "ℤ/2×ℤ/2" attached to *the character variety* is corrected to 55296/5, 40a3, ℤ/4. (iii) The identification is **EARNED** by the Identification Rule — the map is written down and it acts (point counts, torsion, j, the special points) — and enters `docs/IDENTIFICATION_LEDGER.md` as **I-12**; it is an identification of *curves*; what "square time" means physically is not asserted here. (iv) B1237 corrected B213's copy of the label yesterday and did not reach B211, the arc that minted it — an E53 instance of my own, one day old (#17 below).

## Bronze, closed: the invariant trace field is an octic (E55 completes)

B1237 left one ask open — bronze's *invariant* trace field. Computed here two independent ways at 1000 bits (`bronze_invariant_trace_field.py`; PARI `algdep`, a relation accepted only if irreducible, max|coeff| < 10¹², |p(t)| < 10⁻²⁰⁰; second route = the tetrahedra shape field, which equals the invariant trace field for cusped manifolds). Metallic monodromy `φ_m = [[m,1],[1,0]]` (det −1); the orientable bundle `φ_m² = RᵐLᵐ`:

| m | bundle | census | volume | H₁ | invariant trace field (polredabs) | disc | sign |
|---|---|---|---|---|---|---|---|
| 1 | `b++RL` | m004 | 2.02988… | ℤ | x² − x + 1 | −3 | (0,1) |
| 2 | `b++RRLL` | m136 | 3.66386… | ℤ/2+ℤ/2+ℤ | x² + 1 | −4 | (0,1) |
| 3 | `b++RRRLLL` | s464 | 4.81381… | ℤ/3+ℤ/3+ℤ | **x⁸ + 6x⁶ − x⁵ + 12x⁴ − 3x³ + 8x² − x + 2** | 391728981 | (0,4) |

Both routes agree (`nfisisom` True at each m). Bronze's invariant trace field has degree 8 — **non-arithmetic, decisively** (cusped ⇒ arithmetic iff the invariant trace field is imaginary quadratic). The arithmeticity axis of L161/B1062 is now **{golden, silver} arithmetic vs {bronze} not**, both halves computed from the criterion that decides them (B1237 fixed silver; this fixes the bronze half, which had been inferred from the trace field's degree). This also settles B840's "8 vs 6": the sites that recorded bronze's trace field as **degree 6** — B125 FINDINGS:32 ("canonical 6"), B137 FINDINGS:44 ("DEGREE 6"), B578 RESULTS D6 ("DEGREE 6, two precisions") — cannot be right, since a degree-6 field cannot contain the degree-8 invariant trace field; B578's own verifier objection (LEDGER_RAW:76: "canonical route gives irreducible degree 8, stable 400–1500 bits") was correct, and the D6 script never reached main, so its mechanism is unrecoverable. All four verdicts (non-arithmetic) are unaffected; the number is corrected at each site (E53 #18).

**The instrument lesson, banked as a class (E56).** My first bronze run accepted integer relations on residual alone (|p(t)| < 10⁻¹⁰⁰ at 170 digits) and produced degree-5 "minimal polynomials" with 50-digit coefficients — LLL noise that passes any residual test. A relation is a finding only when it is irreducible, its height is bounded, its residual is far below the working precision, **and** a second route confirms it. B578's D6 (residuals 1.7e-74 / 1.8e-142 at two precisions, wrong answer) is the class's prior instance.

## The seats' work, dispositioned

| item | seat | what it claimed | recomputed here | disposition |
|---|---|---|---|---|
| R037 | codex | restriction of the 48 2T-quotients of π₁(m000) to π₁(m004): exactly one Aut-orbit of m004's two extends | own SL(2,3) over 𝔽₃, Aut via GL(2,3)-conjugation, own Reidemeister–Schreier presentation of the index-2 subgroup (`r037_verify.py`, `r037_rs.py`): 48/48, orbits [24,24]/[24,24], restricted image = one orbit, the other is its H¹ twist | **VERIFIED** |
| R038 | codex | the lone VEV's stabiliser in su(6)+su(2) is u(5) (dim 25, rank 5), rank-reducing singlet exposed | own basis, numerical stabiliser (`r038_verify.py`): su(6) alone → 24/4, with su(2) → 25/5; charges and D-terms match | **VERIFIED** |
| R039 | codex | the A₄ → 2T lift torsor is finite: each A₄-map has two lifts, exactly one extends over m000 | `r039_verify.py`: Surj(·, A₄) = 24/24, lifts [2]/[2], per m004 A₄-map exactly [1] extends; m000's two lifts differ by the orientation twist | **VERIFIED** |
| R39 | fc | "recovered" the deleted `_verify_Z1` ladder, 21/21 levels agree, Z₁₈ = 2−√5 | the deleted script is **byte-identical to the live cell** `B775/cells/P2W4-Z1/compute.py`, whose `results.json` holds the ladder to k = 28 with certificates; rerun here to k = 22 (`Z1 rc=0`), 22/22 = main, 22/22 = the deleted copy's partial (`z1_compare.py`) | **VERIFIED; nothing was lost** — no new file (the absence rule: swept before creating; my own first move was to recover it from history instead of finding the live copy, E54 at the bench) |
| R40 | fc | B516's five Pisot quartics | own resultant route β_m = s² + s³, s⁴ − m s² − 1 = 0 (`r40_pisot_verify.py`): all five minpolys match B516's table; golden Pisot (other conjugates 0.44, 0.79, 0.79), m ≥ 2 not | **VERIFIED** |
| R41 | fc | four trace-map identities (κ preserved by T; B344's twists have det J = 1; g = −RL⁻¹ has tr −1, disc −3, order 3; RL tr 3, disc 5) | own sympy (`r41_trace_map_verify.py`) + the committed `det_is_one` | **VERIFIED** |
| W-E | fc | absence sweep of 1068 claims: CONTRADICTED 2, SUPERSEDED 44, OPEN_LATER 13 | see below | dispositioned |

**W-E's two CONTRADICTED verdicts (#393 B806, #573 B778) dissolve**: both cite the sentence "CL-W4115 never ran" — `frontier/B778_cleanup/FINDINGS.md:4, 20–24` — but the same file's `## COMPLETION (2026-07-24)` (line 39) records CL-W4115 RESOLVED-B (line 46) and 7/7, and the cell `B778_cleanup/cells/CL-W4115/` (chord `1,5,19,71` REAL) landed in 63286642. An append-only file superseding itself; B806 does not mention W4115 (the seat's attribution was a sweep artifact). Fix: a bracketed pointer at the pending block, the file's own convention. **44 SUPERSEDED**: the load-bearing ones spot-confirmed on main (B742/B745 SL(4) Jacobian; B273 e₆ cup product; B797/B795/B845 + B1007 Maass eigenvalues; B921's harvested cells; B892/B932 dim-14; B862/B1160 hypercharge; B771's OI-063/W3-067; B978→B911 has no arc dir on main; B993; B1019; B1207; B1125; B855/B995; B909); one of them is a **genuine E53**: **B362** (PROVED, "brightness law extended to 11 pairs, 0 counterexamples") is refuted at pair (3,4) by B367 — B367 FINDINGS:89 says so, B362's verdict file said `superseded_by: None` (#16; the same treatment B361 received in B1237). **13 OPEN_LATER**: the five on the qor5up lineage (qB1026/qB1030/qB1039/qB1043/qB1045) were all dispositioned by the B1060 digest (rows 1.03, 1.07, 1.16, 1.20, 1.22; CLOSED-PARTIAL at B1173, remainder under L185) — not open. One residue checked: digest row 1.07 cites "THE_FRAMEWORK's COUNT DISCIPLINE block", which does not exist under that name; the reconciliation it means is THE_FRAMEWORK's priced-input paragraph (lines 283–296: "one dimensionful unit, two 𝔽₂ bits, and one trit (B1030)") plus THE_CLAIM §1's floor sentence. qB1030's "one continuous dimensionless input remains (c)" and B1025's "no continuous dimensionless input remains (c is L154's)" are the same fact typed twice — main assigns c to L154's adjudication (B1034's lock), the branch counted it as an input; B1025 needs no addendum, the digest row gets a pointer note.

## What changes on main

- **B211** — verdict claim prefixed with the correction; addendum; `charvar_zeta.py` and `test_b211` untouched (the point-count relation is true and isogeny-invariant; a comment records that). **B213** note (the 40a3 label now derived, not cited). **B509/B510** — addenda + notes: the flag resolves *upward* (identity of curves, not Jacobian-of-a-cover; 40a1 = the σ-quotient); B510's claim is prefixed (its cover/base roles are inverted). **`docs/IDENTIFICATION_LEDGER.md` I-12 EARNED** (UNEARNED count unchanged at 5; baseline `total_rows` 12).
- **B362** — `superseded_by: "B367"`, claim prefixed, addendum (E53 #16).
- **B125, B137, B578, B840** — bronze degree 6 → 8 (the octic), verdicts unaffected; a dated bracket at each site in place, addenda, and notes where a verdict file exists (B578 has none — its RESULTS.md is the record) (E53 #18). **B1062** — the axis's bronze half now computed (addendum + note).
- **B778** — bracketed pointer at the pending block (W-E's CONTRADICTED pair). **B1060** — digest row 1.07's pointer resolved (addendum + note).
- Live docs sites of "the character variety is 40a1" carry a dated bracket: `LAW_MAP.md:59`, `THE_LAMBDA_POSITION.md:65`, `STRATEGIC_SYNTHESIS.md:268`, `CLOSURE_2026-07-05.md:60,87`, `OPEN_LEADS.md` L34/L44, `CAMPAIGN_STATUS.md` (B1237's block). `tests/test_b211_metallic_faces.py` carries a comment (a_p is isogeny-invariant; the lock is unchanged). Conductor-only sites (HINT_LEDGER H2/H16, B225/B406/B1067/B1068) are untouched — their statements hold for the class.
- **ERROR_LEDGER**: E53 #16–#18; **E56** (integer relation on residual alone). **No new kill rows** (no new NEGATIVE; the bronze verdict was already banked).
- Relays: `CC_TO_CODEX_2026-09-02_R037_R039_VERIFIED_R040_QUEUED.md`, `CC_TO_FC_2026-09-02_R39_R41_WE_HARVESTED.md` (untracked; RELAY_LEDGER rows).

## Not harvested here (next cell)

codex **R040** (free-deck CS: Kawauchi closed theorem + 1260/1260 cusped census) and the physics-seat branch's **R42–R48 + Phase C/D** (59 rerun packets, 38 certificate packets, R48's B511 D3.3 wild-register accessibility) arrived while this cell was being verified — read, not yet recomputed; queued as B1239 with the census rerun first.

## Found at the landing: E57, the lock-without-tool class (mine)

While landing this cell, `scripts/checks/paper_ledger_counts.py` showed up untracked. B1237 (43e32d38, already pushed) had committed `tests/test_paper_ledger_counts.py` — its CHANGELOG line says the tool was "fixed and locked" — but the tool itself was never `git add`ed: no history at all. Every local certificate was green: the suite because the bench has the file, `path-refs` because the path resolves. A fresh clone reds on that test. The `git add -u` + explicit-new-paths discipline (which exists to prevent `-A` accidents) puts the burden of naming every new file on the seat, and the seat named the test and not the tool.

Census: a scan of every tracked `.py` under `tests/` and `scripts/` for path literals (and `ROOT / "…"` chains) that exist on disk but are absent from `git ls-files` finds **1 instance in 1160 files** — this one. The scan is now the gate **`tracked-deps`** (`scripts/gates/gates.py`, the 30th), registered in `docs/PRACTICES.md`, run at push by the pre-push hook; it is the complement of `path-refs` ("does it exist?" vs "does git have it?"). The tool is committed in this cell's commit. `docs/ERROR_LEDGER.md` E57. Consequence for the corpus: none — the tool's own selftest passes and the paper's ledger counts it certifies were right; the defect was purely in what git held.

One more small thing from the same landing: the `doc-currency` gate tipped `docs/TOOLBOX_LIVE.md` to lag 61 > 60 when this cell's directory appeared. The page was read and extended with this cell's three instruments and the new gate — the update the gate asks for, not a freeze.

## Verification

`verification/reproduce.sh` (from the repo root; `B1238_SKIP_BRONZE=1` skips the ~10–15 min bronze step). Locks: `tests/test_b1238_seat_harvest_40a3_bronze_octic.py` (the completing-the-square identity and the quotient map by sympy; the Jacobian's minimal model by PARI; the bronze octic's discriminant, signature and irreducibility by PARI; the golden and m = 5 Pisot quartics by resultant; B362's `superseded_by`; B211's prefix; the in-place brackets; I-12 and the untouched baseline; the Z1 ladder k ≤ 22 against main's results; `reproduce.sh` under `OA_SLOW=1`).
