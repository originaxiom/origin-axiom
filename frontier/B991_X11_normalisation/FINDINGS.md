# B991 — X11 CLOSED: the hypercharge normalisation is not derivable *in principle*, and that is a theorem about the equations

**Date:** 2026-08-09 · **Seat:** cc (banking) · **Lane:** MATHEMATICS. Gate 5 untouched.
**Campaign order 11 — a "state and close" item. Closed by verifying the discriminating fact rather
than by citing it.**

**P0 — the quantifier:** this is about the **anomaly-cancellation equations** as algebraic forms.
Not a manifold, and not even this object specifically — it holds for **any** U(1) charge assignment
in **any** theory.

---

## The claim, and the fact that decides it

B951 recorded that the normalisation is not derivable "because anomaly conditions are homogeneous."
**That was cited, not computed here.** Computed now, symbolically:

| condition | scaling under q → λq | degree |
|---|---|---|
| [SU(3)]² U(1) = Σ qᵢTᵢ | C(λq) = **λ¹** C(q) | homogeneous 1 |
| [SU(2)]² U(1) = Σ qᵢWᵢ | C(λq) = **λ¹** C(q) | homogeneous 1 |
| U(1)–grav = Σ dᵢqᵢ | C(λq) = **λ¹** C(q) | homogeneous 1 |
| [U(1)]³ = Σ dᵢqᵢ³ | C(λq) = **λ³** C(q) | homogeneous 3 |

**Every condition is homogeneous.** Therefore:

> **If q solves the system, so does λq for every nonzero λ. The solution set is a union of lines
> through the origin — a cone — and never a set of isolated points.**

## What that separates, exactly

- **The DIRECTION is derivable, and B864 derived it.** Writing Q = aY + bχ + cψ over the chiral
  generation, the conditions give grav = 5b + 15c, [SU(3)]² = 2c, [SU(2)]² = 2c, **forcing b = c = 0
  exactly** — and ψ, χ are anomalous over chiral matter (Tr ψ = 16, Tr χ = 5), so they *cannot* be
  gauged. **Y is the unique gaugeable direction.**
- **The NORMALISATION is not derivable — by the form of the equations.** No theory derives it,
  because a homogeneous system cannot single out a scale on its solution cone.

## Why this closes rather than stays open

**This is not a limitation of the object.** It is a property of anomaly cancellation as such. Every
GUT, every model, every framework faces the identical situation: hypercharge normalisation is a
**convention**, fixed by embedding choice (the familiar √(3/5) of SU(5) normalisation) rather than
derived.

> **A rung that is impossible for everyone, for a stated structural reason, is CLOSED, not open.**
> Leaving it open would misrepresent the boundary — it would read as work the programme still owes,
> when in fact no one can do it and the reason is one line of algebra.

**Registered consequence:** any future arc claiming to *derive* a hypercharge normalisation is
claiming to break homogeneity, and must exhibit the non-homogeneous condition it used. That is a
checkable demand, and it is the useful residue of closing this.

---

**Verdict: X11 CLOSED — not-derivable in principle.** The ladder rung moves from HOLE to **BOUNDED**,
with the mechanism named: **homogeneity of the anomaly conditions**. Do not chase it.
