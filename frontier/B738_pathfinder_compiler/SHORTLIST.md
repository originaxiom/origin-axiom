# B738 — SHORTLIST: the candidate lattice seed

## !IMPORTANT — GLOBAL LOOK-ELSEWHERE (read before running any cell)

This shortlist defines the hunt's SEARCH WIDTH. The lattice below opens ~16 cells over a classified corpus
of 217 negatives (kill_graph.json), and behind each cell stand the many faces, rungs, and variants its
recomputation will scan. **Every future 'hit' from any cell must clear the width-corrected bar**: its
significance/base-rate must be computed against the FULL width of this lattice (all cells opened, all
variants scanned inside each cell), not against the single cell that fired. A p ~ 1/20 curiosity in one of
16 cells is exactly nothing. This is the pathfinder's !IMPORTANT rule; no cell result banks without stating
its width correction, and the hardened value-wall discipline (W1, Gate 5, B733 bounded menu) applies to
every cell unchanged. Compiling a negative into this list does NOT weaken its kill: each entry stays dead
unless its named discriminating recomputation actually fires.

Scores are the compiler's honest plausibility that the now-known anatomy (V4 faces B730, congruence B734,
Out(A5) B732, Born ledger B725-29, bounded menu B733, EMITTANCE B735) bears on the record — not predictions
of success.

---

## 0. B737 — Candidate Zero: the voice x zeta-pole composition [CLOSED 2026-07-21 — OUTCOME B at the crux]
**Verdict:** P1/P2/P3 = A (the object's voice IS the exact zeta-quotient phi = Lambda_K(s)/Lambda_K(s+1),
Res phi = 2sqrt3/vol(m004); the voice carries OBJECT-SPECIFIC data — the disc −48 conductor-4 cusp CM
shape and the level-(4)/(8) character palette — the program's first non-generic spectral data). P4 = B
(the crux dies: MB12 vacuity across all h=1 fields + object-deletion — the zeta is the FIELD's; "same
zeta, different role"). Kill-form: genericity/field-not-object. New leads queued: L1 (Eisenstein→BC
functor, open-problem class), L2 (do m004's conductor-(4)/(8) characters occur with nonzero
coefficient? — computable). Ledger: width 1→2 as L2 opens. See frontier/B737_candidate_zero/FINDINGS.md.

## 0-bis. [ORIGINAL ENTRY, kept for the record] B737 as posed [was IN-FLIGHT — hunt-cell #1]

Already running; listed first as the lattice's anchor, not scored here. The composition the corpus never
made: the object's native continuous voice (B735 EMITTANCE — cusp scattering, whose classical carrier is
the full Dedekind zeta ζ_K) composed with the zeta-pole structure that B736 showed is killed by every
FINITE congruence truncation (the Bost-Connes beta=1 SSB IS the zeta pole). The two structurally
under-consulted face-columns of the whole corpus (emittance, infinite-hecke) meet in this one cell.
All shortlist cells below are subordinate in priority to this one.

## 1. B731 — the congruence NO-GO [score 10 — ALREADY REVIVED; the calibration exemplar]

- claim_killed: m004 is non-congruence / has no finite congruence observer (index plateau at 6 read as stabilization).
- kill_form: finite-truncation. hatch: deepen-past-plateau. priority: P3.
- WHY the anatomy bears: it already did — B734 (error E22) computed level (8) and the index jumped to 12: m004 IS congruence. This is the proof-of-concept that the shallow-plateau hatch pays, and the calibration case for every deepen-past-plateau cell below.
- Discriminating recomputation: none left to run (done in B734). Kept at the head of the lattice as the pattern's exemplar; its residue (literature cross-check vs Grunewald-Schwermer) is logged in B734.

## 2. B500 — the child hunt, depth 5 [score 6]

- claim_killed: 'the child (x^4-x-1, d_K=-283) is a short word' — complete at depth 4, PROVISIONAL at depth 5 (115/150 analyzed, 35/150 unchecked).
- kill_form: absence-at-depth. hatch: deepen-past-plateau. priority: P3.
- WHY the anatomy bears: the FINDINGS text itself issues the reopen action (the kill must not be cited as complete); the children face is now anatomized and B732 (Out(A5)) is the natural frame for a resumed hunt.
- Discriminating recomputation: run the 35 unchecked D/M-heavy depth-5 words via F_p Groebner or a targeted d_K=-283 factor test (lock tests/test_b500_kill.py pins 115/35).

## 3. B712 — no canonical real anchor on the object's continuum [score 5]

- claim_killed: the object's one continuous modulus (the A-polynomial curve) carries a canonical real anchor — killed; non-canonicity declared TOTAL (with B706).
- kill_form: no-landing-site. hatch: native-continuous-channel. priority: LOW.
- WHY the anatomy bears: the probe consulted ONLY the A-polynomial curve as 'the object's one continuous modulus'. B735 supplies a second, canonical, choice-free continuous REAL channel (geodesic length spectrum, eigenvalues) that was never weighed by the TOTAL verdict.
- Discriminating recomputation: pose the anchor question on the emittance spectrum — is there a canonical nondegenerate real anchor among the choice-free spectral data (shortest geodesic, spectral gap, scattering-pole data), and does the non-canonicity argument survive it?

## 4. B433 — the 3d-3d route's scale/E6 death [score 5]

- claim_killed: the 3d-3d route yields physics in-sandbox (internal scale; the T[4_1,E6] assembly) — scale wall CITED (B426), E6 assembly priced as the L50 specialist gate; SL(2) calibration itself succeeded.
- kill_form: method-limit (fact_computed=false). hatch: route-through-atom. priority: P2.
- WHY the anatomy bears: the explicitly DEFERRED leg (Dimofte-Garoufalidis 1-loop = torsion -3) is emittance-adjacent (B735), and B707's arithmetic-CS meeting point offers a non-class-S route around the L50 method limit.
- Discriminating recomputation: run the deferred 1-loop/torsion calibration in-sandbox (target: torsion -3); re-pose the E6-assembly input-canonicity question through the arithmetic-CS frame rather than class-S.

## 5. B685 — the terminal generation-leg no-go [score 5, P1]

- claim_killed: a framework-derivable generator produces the {2/5,3/5} 5-adic flavor streams — terminal: the object generates its being-structure (3) but not its hearing-structure (5).
- kill_form: kind-mismatch (fact_computed=false). hatch: recompute-cited. priority: P1.
- WHY the anatomy bears: the Habiro-leg discriminating fact is a SOURCE RE-READ (GSWZ eq. 2) immediately after the same read produced the B682 misreading this arc corrects; and the arc's own positive reading — the 5 enters ONLY through level-5 congruence coupling — sits on exactly the face B734 now owns.
- Discriminating recomputation: recompute GSWZ integrality-away-from-3 in-sandbox; then re-pose the generator on the COUPLING side (level-15 = 3 x 5 congruence structure under B734), not the object side, before treating the leg as permanently closed.

## 6. B401 — the class-sorting bet and the L-function dossier [score 5]

- claim_killed: 'seam-channel primes are non-principal-or-ramified in Q(sqrt-15)' — killed by inert 7 in the bright (3,4) Gram spectrum.
- kill_form: other. hatch: deepen-past-plateau. priority: P2.
- WHY the anatomy bears: the probe's banked positive dossier (component basis = Gauss-sum basis; Lyapunov = regulator = L(1,chi_5); zeta_H = zeta.L(chi_-3).L(chi_5).L(chi_-15)) is the corpus's closest algebraic shadow of the emittance channel — cusp scattering carries zeta_K whole (Candidate Zero's carrier). The file itself names the finer genus-character sorting never run.
- Discriminating recomputation: re-pose the prime-sorting bet with the finer genus characters, and check whether the scattering-side zeta_K factorization sorts the seam primes where the coarse class-group bet failed.

## 7. B399 — the tower is a resolution generator, not a scale generator [score 5]

- claim_killed: the singles tower generates a SCALE — killed at rung 1215 (both candidates dead; monotone contraction, total frozen at 1).
- kill_form: kind-mismatch. hatch: native-continuous-channel. priority: P2.
- WHY the anatomy bears: three re-entry points are left IN-TEXT: the seam/boundary pair channel is 'the one channel not yet tested for hierarchy — the next lever'; the value field deepens past golden at 1215 (e2 = -1/48, the seam coefficient); and the e3 reconstruction (primes 7-10) is PENDING — it adjudicates the 31-collision/supersingular sentinel (with B406).
- Discriminating recomputation: run the pending e3 triple reconstruction, then the seam-pair-channel hierarchy test.

## 8. B183 — the arrow needs external openness [score 5]

- claim_killed: opening the metallic collective self-generates an arrow with an intrinsic scale — killed: the arrow is thresholdless, dimensionless, externally sourced.
- kill_form: kind-mismatch. hatch: native-continuous-channel. priority: P2.
- WHY the anatomy bears: the kill's premise (openness must be imposed) never consulted the object's own cusp scattering — resonances are NATIVELY complex (B735), a native non-Hermitian channel the kill is silent about; only the single-particle Hatano-Nelson channel was tested.
- Discriminating recomputation: compute the cusp resonance spectrum (complex scattering poles) of m004 and test whether a native, thresholded, internally-sourced non-Hermitian arrow exists with no imported gauge field.

## 9. B111 — the s_n -> c bridge (order 2 into order 4) [score 5]

- claim_killed: the tower sign s_n carries the peripheral scalar c — killed by the parity/order obstruction (Z/2 cannot reach Z/4) as a DIRECT identification.
- kill_form: kind-mismatch. hatch: nonlinear-transport. priority: P2.
- WHY the anatomy bears: a functorial/indirect carrying was never tested, and this is literally the live crux of the two-chiralities program (B570 Lane C, Q-A/Q-B/Q-C: can c be carried into theta) — the hatch is already being walked at a deeper level.
- Discriminating recomputation: pose the carrying as nonlinear/functorial transport at the theta-odd level-2 sector (the dynamically alive sector), not as a scalar identification.

## 10. B252 — zero object-side c-odd content [score 5]

- claim_killed: the object carries an intrinsic conjugation-odd invariant preferring 27 over 27-bar — killed: amphicheirality forces every c-odd invariant to vanish or pair.
- kill_form: zero-intertwiner. hatch: nonlinear-transport. priority: P4.
- WHY the anatomy bears: the kill is object-side and stands; the QUESTION reopens one level up — B713-16 relocate chirality to the observer's closing, and the Born ledger (B725-29) types exactly the coupling where a c-odd datum could live without contradicting this kill.
- Discriminating recomputation: the Lane C transport tests (Q-A/Q-B/Q-C): whether c can be carried into theta through the observer coupling — an intertwiner at the coupling level, where B252 computed only the object level.

## 11. B477 — the sterility law, one linear form killed [score 5]

- claim_killed: the universal LINEAR sterility law (sterile class = fertile class + one Z/2 sign-flip) — killed by s776's count (3 fertile classes, not a power of 2).
- kill_form: other. hatch: deepen-past-plateau. priority: P3.
- WHY the anatomy bears: the record is explicitly 'recon; law OPEN' — the fresh introspection read only 7 of ~30 sign entries, the queued steps (full sign-vector extraction, correct group structure, per-cusp evaluation) never ran, and the L6a4 pairing hint sits unexplained.
- Discriminating recomputation: extract the full sign vectors, redo the class count under the correct group structure, evaluate per-cusp; test the L6a4 pairing hint.

## 12. W11-B706 — the flavor-freedom kind-mismatch wall [score 4]

- claim_killed: SM flavor freedom matches the object (rung 1: ratios algebraic over Q(sqrt5) — no PSLQ relation; rung 2: the discrete F2 seam torsor vs a ~19-26-parameter continuous moduli space).
- kill_form: kind-mismatch. hatch: native-continuous-channel. priority: LOW.
- WHY the anatomy bears: this is the pathfinder's anchor for the deepest hatch — the torsor no-go is silent about the object's one native continuous channel, and the emittance columns are empty for it (B735: transcendental dense lengths, continuous cusp spectrum [1/4,inf), eigenvalues never computed).
- Discriminating recomputation: pose the kind-match question against the emittance spectrum instead of the F2 torsor: does the object's continuous spectral data have the right KIND (continuous, real, finitely-parameterized?) — a structure question under Gate 5, no value matching.

## 13. W10-B660/B666 — the dimensionful no-value theorem [score 4]

- claim_killed: the framework can output any dimensionful quantity as a value — killed by the standalone rigidity theorem Hom(G,R+)=0 for all finite/profinite G.
- kill_form: kind-mismatch. hatch: native-continuous-channel. priority: LOW.
- WHY the anatomy bears: airtight WITHIN its hypothesis class — but Definition 1 requires outputs in a number field stabilized by finite/profinite G, and B735's emittance outputs (transcendental lengths, continuous spectrum) are exactly what that hypothesis excludes. The revival target is the typing question, not a crack in the lemma.
- Discriminating recomputation: type the emittance channel against Definition 1 — establish formally whether the spectral outputs are inside or outside the theorem's output-algebra hypothesis, and what 'value' means there under the Born-ledger typing (B725-29).

## 14. B619 — the A5 bridge identities [score 4]

- claim_killed: 'theta-odd clock order at golden stages = 60 = |A5|' and 'projective <S,T> at kappa=5 is PSL(2,5)' — both exactly refuted (ord 20; image > 400).
- kill_form: value-numerology. hatch: none-apparent. priority: LOW.
- WHY the anatomy bears: the killed identities stay dead, but the A5 question demonstrably found its correct landing in the anatomy itself — Out(A5) (B732). The bridge AREA reopened even though these two identifications are refuted.
- Discriminating recomputation: re-pose the stage-to-A5 link through Out(A5) = Z/2 acting on the anatomy's faces (does the golden-stage structure realize the OUTER involution?), a group-action question rather than an order-matching identity.

## 15. B107 — tower eigenvalues as a physics spectrum [score 4]

- claim_killed: the metallic/SL(n) tower eigenvalues are masses/anomalous dimensions — killed twice (single scale +-phi^k; monodromy is not the fluctuation Hessian).
- kill_form: kind-mismatch. hatch: native-continuous-channel. priority: P2.
- WHY the anatomy bears: the kill requires the physics channel to be the DISCRETE tower; the object's actual native spectrum — the B735 emittance eigenvalue/resonance face — did not exist then and has never been computed by any kill (the emptiest column in the matrix). Fork E (off-principal multichannel, roots-of-unity data) also remains open in the kill's own text.
- Discriminating recomputation: compute the eigenvalue/resonance face (Maass spectrum / scattering poles) for m004 and the metallic family — as STRUCTURE only (density, multiplicity, arithmetic), with Gate 5 and the W1 record barring any value matching. The tower=masses identification itself stays dead.

---

Compiler note: entries 3-15 are ranked by revival_score then by directness of the anatomy connection.
The next tier (score 4, not seeded as cells): B166, B191, B225, B307, B367, B372, B374, B385, B394, B412,
B428, B515, B706, P21 — available if a running cell exhausts and the width budget allows widening, which
RAISES the look-elsewhere bar for every cell.


---
## FOLDED IN 2026-07-21: cc2's REVISIT REGISTER (209 negatives; ~130 overreach-class; 3 cracks, cc-verified where load-bearing)
- **NEW CELL (P1-class, CHEAP): B288 straggler re-run.** cc2's register crack #1, live re-execution:
  24 of 78 Dehn fillings were SILENTLY SKIPPED (a `continue` before the √−3-containment check) — the
  banked "NO filling re-sees ℚ(√−3)" headline reports a full-grid closure never computed. ACTION:
  re-run the 24 stragglers. (Width ledger +1.)
- **RESTATED (cc2's own crack #2):** the B736-P3 "no landing site" leg → "the landing is the C-cal
  conditional route (low-weight but LIVE)"; the kind-mismatch leg (B706) carries the rigorous negative
  unchanged. Reflected in B736 FINDINGS addendum.
- **FIXED (crack #3):** RETRACTIONS.md quadratic-arrow row corrected (its own cited source's D2
  retracts the "corrected form"; cc-verified against P1_ERRATUM).
- **STANDING RULE (register-minted, adopted): cite exact_scope, never headlines** — every "X REFUTED"
  is shorthand for "X refuted for THIS construction, tested THIS way, on THESE objects." The register's
  ~130 overreach-class items are the corpus-wide evidence.
- Cross-ref: relay CC2_REVISIT_REGISTER_2026-07-21.md; the register and this compiler are complementary
  (209 banked negatives vs 172 atlas-dead + walls + tombstones; both converge on the same discipline).

---
## CELL #2 CLOSED 2026-07-21: B739 — character-rigidity (OUTCOME A, 0/3 refuted)
L2 resolved: NO conductor-(4)/(8) character anywhere in m004's continuous spectrum (the one-cusp
pullback lemma at full strength; Fourier restriction proven; multiplicity-one exhaustion source-
verified). The level's arithmetic is DISCRETE-NEWFORM-ONLY — the sharpest remaining spectral
question (the newform Maass spectrum itself) is owner-gated (Hejhal-class machinery). Width ledger:
2 cells closed. Next: B288 stragglers (queued below) → B500 reopen → the fact_computed sweep.

---
## CELL #3 CLOSED 2026-07-21: B740 — the B288 grid completed (OUTCOME B, earned on 78/78)
The register's crack (54/78 banked) closed by full recomputation: 60 direct + 11 deep + 7 by the
amphichirality shortcut (7/7 isometries verified). No filling contains √−3 — the kill is EARNED.
Width ledger: 3 cells closed. Next: B500 reopen; the fact_computed sweep is in flight (B741).

---
## RE-RANK 2026-07-21 (post-B745): the two revivals are CONFIRMED — one new door opens
B742's revivals cross-verified CONFIRMED ×2 (B745: re-execution identical + 5 independent exact
checks). Ranking consequences: **B58 rises to an open DOOR** — "the SL(4) tower prediction cannot
be tested numerically" is negated; the ε-extrapolated pinv route computes the ambient fixed-line
Jacobian (validated vs the exact SL(3) anchor + B65's exact J(1)), so the SL(5)+ numerical tower
is now a computable target (the old B61/B66 n=6 ceiling gets a fresh instrument). **B225 returns
to OPEN** (score-4 tier stands): the octahedral-parent half is undecided, pending genuine
(Jacobian-conductor) 2-parts of non-twist 2-bridge character varieties — a well-posed cell, not
a revival-claim. Neither is a new positive (E20 untriggered).
