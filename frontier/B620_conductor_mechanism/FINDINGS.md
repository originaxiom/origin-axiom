# B620 — THE CONDUCTOR MECHANISM: the field has exactly one door

**Status: banked (frontier). Completes the conductor arc (B617 theorem →
B618 prediction → this mechanism). Promotion candidate with B613/B617.
Lock `tests/test_b620_conductor_mechanism.py` (~5 min). One iteration
recorded (the reflection-index set corrected 1,4,5 → 1,2,5 before the
verdict rows were read as final).**

## THE MECHANISM (proved + verified)

In the Weyl-twisted Weil factorization (B587), the twelve Gauss terms
carry conductors det(A ⊗ (±w) − I₄) determined by the Weyl class alone:

> **identity: (t−2)² · −identity: (t+2)² · rotations: (t+1)² ·
> −rotations: (t−1)² · THE SIX SIGNED REFLECTIONS: 4 − t².**

Proved: the reflection line symbolically; all five closed forms by
4-point integer interpolation (each det is degree ≤ 2 in t; evaluated
exactly on monodromies of traces 3, 4, 5, 6). **Every non-reflection
conductor is a PERFECT SQUARE — square discriminants yield no field
irrationality — so the object's field √(t²−4) can enter the stage trace
through the reflection coset and nowhere else.** The torsion base (B617)
is literally the reflection-coset conductor.

## The verification (two objects, bearing vs silent levels)

√5-content (fig-8) and √21-content (tr-5): present in the reflection
terms and ONLY there at every bearing level (κ = 5, 10 / 14, 21); absent
everywhere at silent levels (κ = 6 / 9). The κ-gating (which levels
fire) is the Gauss-sum arithmetic mod κ of the base — the remaining
computed-tier piece; the carrier identification is now proved.

## The arc, assembled

- **B617 (theorem):** τ_m = (−1)^m(t²−4)^m ∏U² — the base is the torsion's
  engine.
- **B620 (mechanism):** the same base is the unique non-square conductor
  in the stage's Gauss decomposition — the field's only door.
- **B618 (prediction, confirmed):** hence the field content appears in
  the hearing trace exactly at base-gated levels, on three objects.
One number — tr² − 4 — runs the object's semiclassical stiffness and its
quantum voice. The observer-coupling thesis's cleanest exact instance.
