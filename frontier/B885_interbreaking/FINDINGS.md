# B885 — the inter-breaking dictionary on the 27: two structural laws, uniform across all three Galois pairs — one frame's vacuum axis IS a Higgs-block direction of every other frame

cc banking seat, 2026-08-04. The queue's inter-measurement cell. Mathematics scope; nothing to
`CLAIMS.md`; Gate 5 untouched — structure only; **nothing resembling a mixing matrix of values
is computed or claimed.**

## 1. The setting

The same 27 carries each breaking's SM-grading (s_i, y⁽ⁱ⁾, y₂⁽ⁱ⁾ at each of the three Galois
roots; the fine Levi charges are per-root conventions, the coarse {1, 10, 16} blocks are
centralizer-canonical). All readouts oblique (the standing rule). All three pairwise tables
computed; every gradation reproduces [1 | 3,3,2,2 | 6,3,3,2,1,1] with coarse dims [1, 10, 16].

## 2. The coarse tables — and the two laws

Row = a K_i block's coefficient mass over K_j's blocks (rows sum to 1):

| | → 1 | → 10 | → 16 |
|---|---|---|---|
| **1** | **0** (≤ 10⁻²¹) | **1.000000** | **0** (≤ 10⁻²¹) |
| **10** | 0.22–0.28 | 0.26–0.35 | 0.42–0.50 |
| **16** | **0** (≤ 10⁻²²) | 0.018–0.194 | 0.81–0.98 |

- **LAW 1 — the vacuum-to-Higgs law**: each frame's 27-singlet lies **entirely** inside every
  other frame's 10-block. Zeros at 10⁻²¹–10⁻²⁶, all three pairs. The reason is exact: the
  singlet is a **Π-weight vector** (a common eigenvector of the whole charge plane — its
  s_j-eigenvalue is fixed by tri(ℂ)-weight geometry, and it lands on the 10's value). One
  measurement's vacuum axis is a Higgs-block direction of every other measurement.
- **LAW 2 — the 16/vacuum exclusion**: no frame's 16 has any component on another frame's
  singlet direction (≤ 10⁻²²), all three pairs.
- The zero **pattern** is S₃-uniform. The nonzero **magnitudes are not** (e.g. the 16→10 cell:
  0.194 / 0.018 / 0.194 across the pairs) — recorded as computed; the pair geometry is not
  S₃-isometric and no symmetry of the magnitudes is claimed.

## 3. The fine table (convention-carrying; pattern only)

K₁×K₂ at multiplet level (11×11): **13/121 zero cells**. The fine Levi frames are chosen
per-root by the same algorithm but remain conventions — only the existence of a nontrivial
fine zero-pattern is recorded; no fine numbers are claimed.

## 4. What this opens (flagged, unweighted)

Law 1 is the first computed statement of how the three measurements see *each other*: the
breaking direction of one frame presents as Higgs-sector structure in another. In the
across-breakings reading of the generations question this is the natural first entry of a
future dictionary — flagged for the joint queue with the solo seat, and for the exact-proof
lane (the Π-weight argument should make Law 1 and Law 2 exact theorems, not numerics).

`tests/test_b885_interbreaking.py`
