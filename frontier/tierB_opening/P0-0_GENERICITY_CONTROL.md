# P0-0 — THE GENERICITY CONTROL. **Verdict: NOT DESIGNABLE from what is banked, and the blocker is a named missing bridge — not the physics and not the literature.**

**cc3, 2026-08-13. Owner-elected campaign (L161); cc's brief: state in advance what
silver and bronze FAIL that golden PASSES, at a level an experiment could measure —
or say it cannot be done and end the campaign for the price of this document.**
**Gate 5-Q: no measured value appears.**

**cc3 proposed this direction and gets first shot at killing it. This is that shot.**

---

# §1 — THE REQUIREMENT, STATED SHARPLY

**"Different" is not "selects".** Every metallic `m` has its own everything — its own
`φ_m`, its own gap-label field, its own spectrum. **A control that merely separates
m = 1, 2, 3 measures the trivial fact that different substitutions are different.**

> **The control must exhibit a property golden PASSES and the siblings FAIL** — a
> pass/fail, not a difference — **and it must be measurable.**

**And one target is forbidden in advance, by cc3's own warning adopted into the brief:
κ-positivity is substitution-generic** (S034 N5: *"generic to non-abelian 2-generator
dynamics… explicitly does not make the metallic family special"*). **Any control
resting on it is dead on arrival.**

# §2 — THE TWO LAYERS, AND THEY DO NOT MEET

**Everything golden-specific in the corpus is ARITHMETIC. Everything measurable is
SPECTRAL. No banked arc connects them.**

| handle | golden-specific? | measurable? |
|---|---|---|
| **B997/B1019 — the own-conductor door**: golden is the **unique** metallic grammar whose own-conductor shadow **is** a McKay group; *"neither sibling's own-door shadow embeds in SU(2). **No McKay partner exists.** … there is **no door to walk through**"* | **YES — a genuine pass/fail over the infinite family** | **NO.** The shadow group is the reduction of a word matrix mod the conductor. `\|SL(2,ℤ/5)\| = 120 = \|2I\|` is a **group order**, not a spectrum. |
| **The five tones** `{0, 1/(2φ), ½, φ/2, 1} = \|χ_golden\|/2`, with the discriminating clause being the **class-size multiplicities** (B659: *"the value-set match alone has **low discriminating power**… the load-bearing novel clause is the **class-size multiplicity match**"*) | **YES via the multiplicities** | **UNKNOWN — and the corpus says so.** `docs/THE_PATTERN_MEDITATION_2.md` registers *"**are the tones observables**"* as an **OPEN QUESTION.** |
| **B641 — ear-independence** at `2.14e-60` over all 360 elements | **YES (2I-specific)** | **NO.** It is a coupling-layer invariance (`u†M_odd u`), not a spectral quantity. |
| **Gap-label field**: golden → `ℚ(√5)`, silver → `ℚ(√2)`, bronze → `ℚ(√13)` (`metallic_spectra.py::gap_label_field`, already locked for m = 1,2,3) | **NO — it separates, it does not select.** Every `m` gets its own field `ℚ(√(m²+4))`. | **YES** |

> ## **The one measurable handle is not selective. Every selective handle is not measurable. And no arc bridges them.**

**Checked, both directions, second source run:** of the five arcs mentioning the tone
set (B1011, B1032, B642, B654, B665), **only B1032 also mentions "spectrum" — and its
hit is the unrelated ℤ₆ line-spectrum falsifier.** A second search in different
vocabulary (`tone … measur|observ|quasicryst|experiment|spectrometer`) returns the
Meditation-2 open question and one theorem-level **negative**: the tones are
*"invisible to the chiral observable **by theorem**"* (`CAMPAIGN_STATUS`).

# §3 — FIVE-CLAUSE COMPLIANCE, AND THE TAXONOMY PREDICTED THIS

Against `docs/THE_WITHHOLDING_TAXONOMY.md`:

| class | the measurable handle (gap-label field) | the selective handles (door / tones / ear) |
|---|---|---|
| **I — type walls** | **passes** — a field/label is a finite algebraic datum, the kind the type law permits | **passes** — group orders and character values are finite labels |
| **II — blindness** | **passes** — IDS labels are dimensionless; the material supplies the scale | **passes** — no scale required |
| **III — arity** | passes | passes |
| **IV — GENERICITY DEFLATION** | ## **FAILS.** The property separates the family; it selects no member. | **passes** — B997 is a genuine uniqueness over the infinite family |
| **V — torsor** | passes | passes |

> **The measurable handle dies at Class IV; the selective handles die on
> measurability, which the taxonomy does not have a class for because it classifies
> how the OBJECT withholds — not how the RECORD fails to connect its own layers.**
>
> **So P0-0's death is the taxonomy's Class IV, arriving in the aperiodic campaign
> exactly where cc3's own S034-N5 warning said it would.** The warning was written
> against κ-positivity; **it applies with equal force to the gap-label field**, and
> cc3 did not see that when proposing the direction.

# §4 — THE VERDICT

> ## **NOT DESIGNABLE from banked material. The campaign does not open.**
>
> **This is a NOT-YET, not a NEVER, and the difference is a named, computable bridge:**
>
> ### **Are the class-size multiplicities `{0:90, 1/(2φ):72, ½:120, φ/2:72, 1:6}` — the tones' discriminating clause — realised in any spectral observable of the m = 1 chain?**
>
> **If yes**, the control is designable: golden shows the 2I multiplicity census,
> silver and bronze cannot (their shadow groups are not McKay, B997/B1019), and that
> is a pass/fail at a measurable level. **If no, the campaign is closed for good** and
> the aperiodic direction is a Class IV deflation like the others.
>
> **The corpus already registers this question as open** (Meditation 2). **cc3 did not
> discover it — cc3 walked into it from the other side and can now say what it
> blocks.**

# §5 — WHAT THIS COSTS, AND WHAT IT SAVES

**Cost: one document. No literature read, no lab approached, no seal spent** — the
meditation's reordering (*"no literature until the control seals"*) is what made the
death this cheap, and it was cc's ordering, not cc3's.

**Saved: the P0-1/P0-2 literature budget** (Damanik–Gorodetski, Sütő) **and whatever a
photonics approach would have cost** — both of which cc3's own scale-factorisation
document recommended and which would have been spent on a direction with no
discriminator.

> **cc3 proposed the aperiodic direction two days ago as the one route where "the
> object's own theorems do not forbid the crossing before it is sealed." That was
> right about the type law and wrong about genericity — and the genericity hazard was
> in cc3's own document, flagged, and then not applied to cc3's own proposal.**

# §6 — NOT SEARCHED, DECLARED

- Whether any **non-tone** 2I-specific quantity has a spectral realisation. cc3
  searched the tones, the door and the ear; **the space of golden-specific facts was
  not enumerated.**
- The **aperiodic-order literature** — the meditation's ordering forbids it until a
  control seals, and it holds.
- Whether the **multiplicity census** has ever been computed for a metallic chain's
  spectral measure. **This is the bridge question of §4 and it is unrun.**
- `metallic_spectra.py::gap_labels_fit` is **locked for m = 1,2,3** and cc3 did not
  re-derive it; the Class IV reading above rests on the field's *form*
  `ℚ(√(m²+4))`, which is generic in `m` by inspection.

---

**cc3 does not adjudicate this verdict.** It kills cc3's own proposal, which is the
configuration where a seat is least trustworthy in either direction — **too eager to
be seen killing its own, or too reluctant to lose the campaign.** **cc's bench decides
whether §4's bridge question is worth a cell, or whether the direction closes.**
