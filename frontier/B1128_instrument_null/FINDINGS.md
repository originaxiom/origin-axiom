# B1128 — P-INSTRUMENT: the listener-map crossing cell carries no SM content — INSTRUMENT-NULL

**Status: banked (frontier). Verdict PROVED (a NEGATIVE: INSTRUMENT-NULL, against a sealed
prereg; own-bench reproduced deterministic, self-validated vs banked B593/B856; a mid-run
degeneracy self-caught and reported). Value-probing wave, the crux cell. Gate 5 untouched.
Lock `tests/test_b1128_instrument.py`.**

## The question (the crux V-3 left)

V-3 (B1126) proved no RAW object-period is an SM ratio. The one route left: a PRINCIPLED
INSTRUMENT — a listener map u that reads a period AS a value (the owner's "one measurement
then predicts the rest"). Built on the banked `LISTENER_MAP_SPEC.md` (C⁶, the SU(3)-level-2
Kac–Peterson modular data, ⟨R,L⟩ = 2T×2I): u(θ) = cos θ·u₃ + sin θ·u₆, the real great
circle through the DERIVED listener pair (B1070/B1071) — ONE real dof, the only room a
calibration can live (B1070/B1071: Re h is constant on ℂP¹_odd; only Im h varies).

## THE ANSWER: INSTRUMENT-NULL — the coupling predicts φ, and φ misses

> The instrument's one falsifiable prediction is **|U_e1|/|U_e2| = φ (the golden ratio,
> 1.618)**; the measured PMNS e-row ratio is ~1.5 — a **~5σ MISS**. The coupling carries no
> detectable SM content. Extends V-3's null and the 7-for-7 coupling-channel record
> (B1027/B1063/B1066/B1075) to the one configuration those could not test — a CONTINUOUS
> calibration. The declared prior (MISS EXPECTED) held.

## The mid-run degeneracy (self-caught, reported not hidden)

All four calibration branches gave IDENTICAL predictions — because **h(R²L², θ) = −φ·h(RL, θ)
EXACTLY for every θ** on the curve (verified two ways, worst residual 1×10⁻⁵⁰). So θ
collapsed to a zero-parameter forced relation (Type Law): the intended "1 continuous input →
forced prediction" test was DEGENERATE — θ absorbed ZERO effective dof (the OPPOSITE of
hidden-freedom fitting), and the whole 8-word short-word family supplies at most 2
independent |h(θ)| curves, not the ≥3 the charter wanted. A computed boundary, not a
shortfall of effort. **Not numerology** (θ absorbed 0 dof; φ was not tuned — it is a forced
ratio recoverable from banked B856 numbers, sitting outside the measured range).

## The honest soft spot (named)

The specific curve (real coefficients in the canonical basis) is well-motivated but NOT the
only choice — a DIFFERENT meridian would see the invisible wy-component and report different
numbers, and this was not searched. So the instrument is null **as constructed**, with one
unsearched direction fenced — not exhaustively killed.

## Side-finding (flagged, NOT banked here — for the banking seat's judgment)

**h(R²L², θ) = −φ·h(RL, θ)** is an exact, twice-verified golden-ratio closed-form relation on
the real great circle, not previously stated as a law in the corpus — a candidate LAW_MAP
row on its own merits, SEPARATE from the crossing question. Judged/banked separately.
