# B660 — THE STRUCTURE CAMPAIGN: cc2's S1–S4 verified and banked
# (main seat, 2026-07-17; packet seals 8/8 on receipt; prereg 711773fe
# sealed before compute; post-JUNO rules honored — no value comparison
# anywhere in the packet or in this adjudication)

cc2's physics-by-structure campaign: four cells, every verdict internal
or priced. This seat's verification: seal audit 8/8; S2's decisive
obstruction INDEPENDENTLY RECOMPUTED; S4's matrix claims verified at
the artifact level; S3's assembly rows checked against the banked
record; S1 cross-corroborated by this seat's independent B659 sweep.
The origin docs (the other seat's dark-sector campaign design that the
invariant-line campaign corrected) are archived under origin_docs/ as
provenance context.

## S1 — THE γ₅′ CORRESPONDENCE (banked as PLACEMENT)

The correspondence table: the modular-flavor literature's Γ₅′ ≅ SL(2,5)
paradigm CHOOSES its group from a catalog (10–13 candidates; the
geometric origin is their acknowledged gap), postulates level-5
structure, and fits 8–10 real parameters; the framework DERIVES the
same group from the monodromy (B640/B644, theorem grade), supplies the
2-dim Galois pair as its unique physical bit (GATE B: N = 1), carries
φ at the same structural locus (the S-transform of the R = −I irreps),
and has zero free parameters after the bit. Verdict (sealed options):
**CONSISTENT-NOT-SELECTIVE** + THE UPSTREAM CLAIM stated factually —
the framework supplies exactly the inputs the paradigm postulates.
Their independent 1/(2φ) lit-gate agrees with this seat's B659/C12
sweep (the same day, both open). NOTICED (H-ledger): the 2-dim irreps
first appear at WEIGHT 5 = the level = the conductor = disc(A₁).
External-facing form, if ever: a mathematics paper ("a geometric
origin for Γ₅′ flavor structure") — Gate 5 untouched.

## S2 — THE CP QUESTION: OBSTRUCTED-CONVENTION (honest partial)

- Y is genuinely complex (all 6 nonzero unbent components have nonzero
  √−3 parts) — CONVENTION-TRIVIAL ruled out.
- Y and Ȳ share the generic GL(5,ℂ)-orbit — correctly flagged as
  FORCED A PRIORI (integer-polynomial invariants are Galois-stable);
  the ℂ-orbit match carries no information. The live question is
  K-rationality.
- Every structured K-equivalence FAILS through an IRREDUCIBLE CUBIC:
  the zero pattern forces monomial g to fix {0,1,3} and optionally
  swap {2,4}; both ansätze reduce to X³ = c with X³ − c irreducible
  over ℚ(√−3). **THIS SEAT'S INDEPENDENT RECOMPUTATION: both cubics
  re-factored over ℚ(√−3) with separate code — 1 factor, 0 linear
  factors — IRREDUCIBLE confirmed (verify_s2_cubic in the lock file).**
- Deciding FORCED vs CONVENTION is priced: the full 25-unknown
  GL(5,K) Gröbner system, or an arithmetic K-orbit invariant (L104).
- RECORD GAP CONFIRMED by this seat: the GOLDEN deck-swap σ*-matrix
  was computed in-flight (B638) but never persisted as an artifact —
  only the silver's sigma_matrix_L.json exists (B649). Filed as L103.

## S3 — THE DIMENSIONFUL NO-GO (banked as WALL, theorem-by-assembly)

No observable carrying nontrivial mass dimension is typeable in the
B650 type system; no such quantity can appear in any licensed
comparison. Assembly rows all banked and re-checked: R5's
dimensionless typing requirement (TYPES.md, frozen 306d6cde); GATE B's
N = 1 DISCRETE (no continuous dial to absorb a scale); B652's no-scale
wall; the equivariance wall's mechanism (everything heard factors
through finite mod-conductor data — no noncompact direction).
Consequences: masses, VEVs, Λ, cross-sections, running couplings are
permanently outside the framework's reach AS VALUES; every B615-era
null on such comparisons is retro-explained as a typing violation; the
licensed observable class = exact dimensionless algebraic data.
LAW_MAP wall 10.

## S4 — KOSZUL EXTENDS: no class-level mass-shape, even v₀-mediated

Seal correction owned by the sender (the prereg's "symmetric by N's
total symmetry" was WRONG — Koszul graded-commutativity forces
ANTISYMMETRY of the cup-mediated trilinear; the corrected question
survived and was answered). VERDICT: **ZERO** — the 5×5 matrix
M_ij = [N(v₀, u_i, u_j)] is exactly antisymmetric with row 4
identically zero, and the ENTIRE solo-triple {2,3,4} block is the ZERO
MATRIX; all five w-slices have charpoly λ³. Verified at the artifact
level by this seat (locks). THE THREE-WALL STATEMENT: Koszul + D1 +
S4 close the last class-level route — **the hierarchy question is
cochain/quantum or nothing.** The internal compass in negative form.
Honest gap carried: B637's Y uses a different presentation — no
basis-matched numeric cross-check available (part of L103's repair).

## Ledger deltas

LAW_MAP: wall 10 (the dimensionful no-go); the cubic-dichotomy row
extended (the v₀-mediated trilinear is antisymmetric AND solo-zero;
three walls); new PLACEMENT row (the γ₅′ correspondence). OPEN_LEADS:
L103 (persist the golden σ*-matrix + basis-matched Y cross-check),
L104 (the S2 decisive cell). HINT_LEDGER: the weight-5 coincidence.
Locks: tests/test_b660_structure.py. Privacy patches + originals'
hashes in ORIGINALS_MANIFEST.txt.
