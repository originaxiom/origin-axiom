# S2 — BANKED: THE CP QUESTION — OBSTRUCTED-CONVENTION, HONESTLY PARTIAL
# (cc2 + subagent, 2026-07-17; prereg 711773fe)

VERDICT: DEGENERATE-INDISTINGUISHABLE, with real content on both sides:
- Y is genuinely complex (all 6 nonzero unbent components have nonzero sqrt(-3)
  parts; conjugate differs entrywise — CONVENTION-TRIVIAL ruled out).
- Y and Y-bar share the generic (open, rank-4) GL(5,C)-orbit — but the agent
  correctly flagged this as FORCED A PRIORI (integer-polynomial invariants are
  Galois-stable), so the C-orbit match carries no information. The live question
  is K-rationality.
- EVERY structured K-equivalence FAILS: the zero-pattern forces monomial g to fix
  {0,1,3} and optionally swap {2,4}; both ansaetze reduce exactly to X^3 = c with
  X^3 - c IRREDUCIBLE over Q(sqrt-3) — a genuine cubic obstruction. The deck-swap
  sigma* has no banked matrix (grep zero hits — a record gap worth noting to cc)
  and abstractly gives -Y, not Y-bar.
- What would decide FORCED vs CONVENTION: (i) the full 25-unknown polynomial
  GL(5,K) system (priced, Groebner-hard), or (ii) an arithmetic K-orbit invariant
  (e.g. the field of moduli of the rank-<=2 locus's ideal — a future cell).

READING (factual): the cubic's 120-degree phase structure survives every natural
gauge; if a future cell proves FORCED, the framework carries a structural complex
phase — the SHAPE the SM's CP violation also has (no comparison made; typing via
R5 would be required first). Artifacts: s2_cp.py, s2_results.json, s2_run.log.
Repo untouched.
