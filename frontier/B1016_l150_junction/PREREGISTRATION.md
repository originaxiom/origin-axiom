# B1016 PREREGISTRATION — L150: is the D₂/twist-norm data an instance of the u†M_odd u coupling form? (sealed before compute)

**Date sealed:** 2026-08-10 · **Seat:** cc · **Lead:** L150 (registered by B1010) · **Anchors:**
none consumed — this cell compares two *banked, anchor-free* value structures. Gate 5-Q; no SM
quantity anywhere; the kind gate is not in play because no measured value is contacted.

## The question, stated precisely (so JOIN is not a strawman and SEPARATE is not a triviality)

**JOIN hypothesis (strongest reasonable form):** there exists a linear map Φ from the hearing
module (the 6-dim ⟨R,L⟩-representation over SU(3)₂, B1011) to the 27 — or to a sub- or quotient
module of the Hermitian pair's carrier — and a single global scalar λ in a declared field, such
that for the metallic words w the coupling form transports: u†M_odd(w)u ↦ λ·v†H(w)v with
compatible group actions on both sides.

**SEPARATE is proved by either obstruction:**
- **(O1) the ratio-field obstruction.** Value *ratios* are invariant under any global scalar. If
  the exact ratio set on one side generates a field that does not embed compatibly into the
  other's, no Φ of the hypothesised form exists.
- **(O2) the module obstruction.** The 6-dim side's constituents are (χ⊗V₂) and (V₂⊗V₂) of
  2T×2I (B1011, exact). If the pair's carrier admits no 2T×2I-action with a common constituent
  (checked at the level of the actions actually banked for the 27 — the frame/Weyl structure of
  B909/B928/B939), the transport has no equivariant carrier.

## The cells

- **J1 — both exact value inventories, from banked artifacts.** Old side: the tone set
  {0, ±1/(2φ), ±1/2, ±φ/2, ±1} and the mirror set (with quarters) — **theorems** (B1011), fields
  ℚ(√5) (·ℚ(ω) for phases). New side: the twist-norm law's diagonal data — **recomputed from the
  banked B936/B928 artifacts, not quoted**: the block ratios (banked as −17/384, +17/384, 1, −1),
  D₂'s spectral data on the 27 (eigenvalues and multiplicities, from the banked characterization
  D₂ = ±ρ₂₇(σ_χ₋), checked on the B961 frame instrument), and the pencil field (banked ℚ(√3129)).
- **J2 — the obstruction tests, exact.** O1: the fields generated by the two ratio sets; embedding
  or non-embedding over ℚ decided exactly (√5 vs rational/√3129 arithmetic — a field-membership
  computation, not an opinion). O2: does any banked action on the 27 restrict/extend to 2T×2I
  with a constituent in common with ρ₆? (Decided against the banked structures only — scope
  stated: a NOVEL action outside the banked set is not excluded by this cell.)
- **J3 — the verdict and its consequence.** **JOIN** ⟹ exhibit Φ and λ explicitly; B936's
  frame-relativity becomes a listener-frame statement and the frame-invariant residue is the
  crossing candidate class (R10 resolved toward the coupling side). **SEPARATE** ⟹ the value
  layer has **two independent value-shaped structures**; B1005's adjudication must price the
  coupling family separately (the `WHAT_WOULD_COUNT` falsifier-2 scope note is then actioned, its
  "never weighed" clause discharged one way), and R10's frame question is answered: **B856
  governs the coupling channel, B936 the pair channel, and they are different channels.**

## Declared prior (honest)

**SEPARATE expected.** The banked numbers already lean hard: the pair-side ratios in the record
are rational (±17/384, ±1) or live in ℚ(√3129); the coupling-side ratios contain φ (e.g.
(φ/2)/(1/2) = φ), and ℚ(√5) ∩ ℚ(√3129) = ℚ. **The cell's value is that either outcome closes
R10's frame question, and J1's recomputation is real** (the twist-norm inventory has never been
recomputed against the frame instrument). Non-weakening applies to this document.

## Scope

A statement about **two banked mathematical structures of this programme** — not about physics,
not about any measured value. If SEPARATE, nothing negative follows for either structure: two
channels is a *richer* value layer, priced honestly.
