# B1246 — the type dictionary reaches θ, not θ̄: a downstream seat corrects main

**2026-09-03 · cc (banking) · Gate 5 untouched — every parameter here is a symbol.**

## What happened

B1243 landed **I-18**, registering *"the object's CS 2-torsion ≡ the SM's strong phase"* as an
UNEARNED identification, with **θ̄** — the physical observable — named as the target.

The physics seat flagged it: the adversarial rounds had already established that the dictionary
constrains the **topological** θ, and that **θ̄ = θ + arg det(M_u M_d)** additionally requires the
Yukawa sector, which the chain does not derive.

**The flag is right.** Verified here rather than accepted — which matters, because in the same
exchange this seat had to withdraw a wrong correction of *that* seat's work (E58). Traffic ran both
ways in one window.

## The discriminating fact, computed

Under the anomalous chiral rotation ψ → e^{iαγ₅}ψ on N_f flavours:

```
theta      -> theta - 2 N_f alpha          NOT invariant
arg det M  -> arg det M + 2 N_f alpha
theta-bar  =  theta + arg det M            INVARIANT  (shift cancels, symbolically 0)
```

So **"θ = 0" alone is basis-dependent** — a chiral rotation moves the phase between θ and
arg det M — and is therefore not by itself a physical statement.

## Why a *type* argument cannot choose

T17's move is a ℤ/2 → ℤ/2 map of **symmetry types**. But **θ and θ̄ carry the same discrete type** —
(P-odd, C-even, T-odd) — because both multiply the same operator. So the map lands on *"an E-type
dimensionless parameter"* and finds **two candidates**, differing by the Yukawa phase, that the type
argument is constitutionally unable to separate.

*Scope, stated honestly:* part (i), the cancellation, is **computed** symbolically here. Part (ii),
the shared type, is **standard QFT — stated, not derived**. It is the premise the type argument runs
on, and it is precisely the premise that makes the argument unable to discriminate.

## Disposition

**I-18 stays UNEARNED — the ratchet does not move.** Only its *price* changes, and it becomes
**more** demanding: earning the row now needs the dictionary **and** the Yukawa phase, where before
it read as if the dictionary alone would do. A register row whose price is understated is worse than
one whose price is high, because it makes the debt look payable.

**Not disturbed:** T17 itself stays. Its computation is correct — CS is E-type, sits at 2-torsion,
and the group isomorphism maps 0 → 0. What moves is which parameter the map is entitled to name.
