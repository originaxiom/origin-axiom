# B575 — L51: THE BRIDGE OBSTRUCTION (preregistration)

**Date:** 2026-07-14. **Status:** running (owner: "take that seriously" — run, not queue).
**The question (B574, final form):** at the E₆ geometric point P = ι∘ρ (principal
composition of the figure-eight holonomy), H¹ = 6 while the SL(2)-factored locus is
1-dimensional. Do the five off-factored first-order directions (exponents 4, 5, 7,
8, 11 — including the θ-odd pair {4, 8}) survive the quadratic obstruction
Q: H¹ → H², Q(u) = [u ∪ u] (cup product with the e₆-bracket coefficient pairing)?

## Preregistered outcomes

- **Some off-factored direction unobstructed (Q-component-free):** honest rank-≥2
  E₆ deformations of the geometric representation exist to second order; the
  chiral-selector question (fifth wall) genuinely reopens on the deformed locus.
  Special attention: the exponent-4 (θ-odd) direction — the subalgebras of e₆
  whose exponents contain {1,4} include D₅ and A₅; a D₅-flavored escape would be
  the σ→θ bridge candidate.
- **All five obstructed:** THE SIXTH WALL — near the geometric point the E₆
  representation variety is exactly the SL(2)-factored curve; chirality is
  unreachable on the entire local variety. Bank the wall.
- Partial patterns (e.g. θ-even obstructed, θ-odd free, or vice versa) are the
  most informative outcomes; report the full 6-component quadratic form.

## Method (exact, gated)

1. e₆ built explicitly inside gl(27) from the minuscule weight model (27 weights,
   edge signs solved over GF(2) for the non-adjacent commuting-square constraints;
   adjacent double-squares proven absent in minuscule). GATES: [eᵢ,fⱼ] = δᵢⱼhᵢ;
   bracket closure has dim exactly 78.
2. The principal triple (e = Σeᵢ, h = the 2ρ∨ grading, f = Σcᵢfᵢ solved).
   GATES: sl₂ relations exact; adjoint isotypics = blocks (3,9,11,15,17,23).
3. The holonomy: ι₂₇(ρ(a)) = exp(e_pr), ι₂₇(ρ(b)) = exp(ω·f_pr), ω = (1+√−3)/2,
   exact nilpotent exponentials over ℚ(√−3). GATE: the fig-8 relator
   r = a·(ba⁻¹b⁻¹a)·b⁻¹·(a⁻¹bab⁻¹) maps to the exact identity in E₆.
4. Per-block group cohomology (Fox calculus on the 2-generator presentation):
   dim H¹(V_{2m}) = 1 and dim H²(V_{2m}) = 1 for each exponent m. GATE: the six
   dimensions; consistency with the banked H¹ = 6 (B347/B565).
5. The cup product on the relator 2-cell with the TRUE e₆ bracket (matrix
   commutators in gl(27) — no CG-normalization ambiguity); H²-classes read off
   modulo im(d¹) per block. **CONTROL GATE: Q(u₁) = 0 — the m=1 direction is the
   existing character-variety curve and MUST come out unobstructed, or the build
   is wrong.**
6. Deliverable: the full quadratic form Q (6 components in 6 variables), the
   per-direction verdicts, and the verdict on the preregistered outcomes.

**Honesty bar:** second-order unobstructedness is not full integrability (higher
Massey obstructions exist in principle); a Porti-style smoothness argument is the
literature companion (cited, not assumed). An obstructed verdict at second order
IS final for that direction. Firewall: no SM claim; the bridge either opens into
mathematics or closes into a wall.
