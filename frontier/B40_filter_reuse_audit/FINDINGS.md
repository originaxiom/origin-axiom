# B40 -- Findings

> Logged observation, not a claim.

## Result

The audit separates three levels:

```text
derived:      A1-A7 plus exchange give A, F, trace map, projective return
not derived:  tangent return must inherit integer/minimal/torsion filters
conditional:  if those filters are inherited, S1 follows
```

Therefore S1 is not derived from the current core. It is conditional on a new
explicit assumption:

```text
T1: the primitive projective tangent return inherits the original arithmetic
    persistence filters.
```

## Verdict

**`CONDITIONAL`**

S1 can be made honest as `T1 -> S1`, but T1 must be named.
