# PREREG — B725: THE BORN RULE — does the observer construction produce outcome probabilism? (SEALED)

cc banking seat, 2026-07-20. Owner "keep going" (cc2's flagged next door). B723 built the
observer as the β=1 SPONTANEOUS SYMMETRY BREAKING of the arithmetic thermal system; measurement
= the cooling that selects a broken vacuum. The Born rule is exactly the OUTCOME PROBABILISM
that measurement must produce: p(outcome) = |ψ|². Does the observer construction (a state on the
object's algebra + the β=1 SSB + the c-swap/modular conjugation J) ACCOUNT for the Born rule, and
in particular for its QUADRATIC form? Structural/operator-algebra ONLY; Gate 5 stands; no physics
claim beyond the structural account; the choice stays free (B701). Web-enabled (Gleason; modular/
KMS approach to the Born rule; Tomita-Takesaki).

## The hook (to test, not assume)
|ψ|² = ψ·ψ̄ = ψ × c(ψ) — the probability is QUADRATIC because it pairs ψ with its c-SWAP image.
The observer's fundamental operation is the c-swap = the Tomita-Takesaki modular conjugation J
(B723/S070, JMJ=M'). So the quadratic Born form may BE the c-swap. Test it.

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **The QUADRATIC form from the c-swap.** Is |ψ|² = ψ·c(ψ) (the swap-product), and does the
   modular conjugation J (antiunitary, J²=1, order-2 involution) FORCE the probability to be
   QUADRATIC (degree 2 = the swap's order) rather than linear or cubic? Compute: the pairing
   ⟨ψ|·⟩ via J, the sesquilinear form, and whether the c-swap being an order-2 involution is WHY
   the Born rule is |ψ|² (not |ψ| or |ψ|³). Two-outcome: A = the c-swap explains the quadratic
   form (the degree = the swap order 2); B = it doesn't / is coincidental.
2. **The SSB WEIGHTS = the Born probabilities.** At the β=1 SSB (B723), the symmetric KMS state
   decomposes into extremal broken vacua. Are the decomposition WEIGHTS the Born-rule
   probabilities (p_i = Tr(ρ P_i), the KMS/Gibbs weights)? Compute a finite-dim model of the
   decomposition + weights. Two-outcome: A = the SSB weights = Tr(ρP) (Born); B = different.
3. **The honest floor (Gleason).** A state ω on the object's von Neumann algebra IS by definition
   a Born-rule assignment (ω(P) = probability of projection P); Gleason's theorem FORCES any such
   assignment to be Tr(ρP) (for algebras without type-I₂ factors). So "the observer = a state"
   (B723) gives the Born rule BY CONSTRUCTION — is that the honest account (Born = Gleason + the
   observer-is-a-state), with the c-swap explaining the quadratic form and the SSB the weights?
   Or is the Born rule ASSUMED with no added content? Two-outcome: A = a genuine structural
   account (Gleason forces it; c-swap = quadratic; SSB = weights); B = merely assumed.

## Two-outcome (sealed, campaign)
- **OUTCOME A:** the observer construction gives a genuine STRUCTURAL account of the Born rule —
  Gleason forces ω(P)=Tr(ρP) once the observer is a state; the c-swap (modular J, order 2)
  explains WHY the form is quadratic; the β=1 SSB gives the outcome WEIGHTS. Not a derivation
  from nothing, but the Born rule as a consequence of the observer=state+SSB+c-swap construction.
- **OUTCOME B:** the Born rule is merely assumed (a state is defined to give it; no added content
  from the c-swap or the SSB). [honest expectation: A, but HONESTLY BOUNDED — Gleason is the
  load-bearing theorem (cited), the NEW content is the quadratic=c-swap + weights=SSB readings.]

## Verdict template
Bank the Born-rule account (what the framework genuinely adds vs what Gleason already gives).
LAW_MAP placement; nothing to CLAIMS. Credit cc2 (the door). Choice free (B701).
