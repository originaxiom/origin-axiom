# B619 — the A₅ bridge adjudicated: both readings refuted; the shared-arithmetic remark survives

**Status: banked (frontier). Chat-1's mid-turn claim verified-and-refuted;
CC's sharpened candidate also refuted. Lock `tests/test_b619_a5_bridge.py`
(fast).**

## The claims and the computations

- **Chat-1: "the θ-odd clock order at the golden stages = 60 = |A₅|"**
  — REFUTED: ord ρ(A₁) = 20 EXACTLY at κ = 5 (the banked order-10 odd
  rotation e^{±3πi/5} and order-20 even clock; the odd compression's
  projective order is 5). A single monodromy generates a cyclic group;
  no reading of the banked clocks gives 60.
- **CC's sharpened candidate: "the projective image of ⟨ρ(S), ρ(T)⟩ at
  κ = 5 is PSL(2,5) ≅ A₅ (order 60)"** — ALSO REFUTED: the projective
  image exceeds 400 elements (the stage's congruence level is not 5;
  ord(T)'s phase denominators push it to ~30).

## What survives (context-tier only)

The GR1 solar ansatz value 1/(1+φ²) = (5−√5)/10 equals our banked |h₃|²
because BOTH constructions are 5-fold (icosahedral-adjacent) arithmetic —
A₅-flavor models via the golden ratio in their Clebsch structure, our
stage via the κ = 5 golden arithmetic. A shared-arithmetic remark, NOT a
group identity of any banked object. Chat-1's proposed "two descriptions
of the same structure" unification is not supported by the computed
group orders; the correct statement for PC26's firewalled-motivation
section is the table of golden-ratio ansätze (GR1, TBM, GR2) as
literature context, the fact that 1/(2φ) is unrecorded there, and the
target-source-sensitivity caveat from the literature synthesis — all as
data points under the B615 sealed verdict (AMBIGUOUS), never as
significance claims. The "angles-not-magnitudes" observation stands as a
DESCRIPTION of B615's raw outcome (couplings silent, angles carried the
excess) with the three-layer theorem as its structural echo —
interpretation, firewalled.
