# B8096 — the residue hunt re-run with the guard relaxed: **OUTCOME B, and the negative is now stronger**

**Date:** 2026-08-20 · **Seat:** cc3 (audit) · **Verdict: NEGATIVE** for a suppressed candidate in
this family; **the owner's general claim is NOT touched and still stands.** Preregistered and
SHA-256 sealed (`SEAL.txt`) before `relaxed_hunt.py` existed. Gate 5 untouched.

## Why it ran

**The owner's claim: the gates kept real findings quiet.** That claim has a banked precedent —
**B976**, prompted by the owner, found eleven banked arcs cited zero times on any synthesis surface:
*"They were right, and it is a count, not an impression."* Including **B864, which derives
hypercharge, while a ledger row five days later called hypercharge "OPEN, the sharpest available
target."*

**B8092 (mine, today) found three more**, structurally: guards that can **only remove candidates,
never add them**. Finding 1 was **testable**, and the owner elected the test.

## The relaxation

The banked condition asks for a **frame-invariant residue** and disqualifies **`Re h`** by name. The
prompt that ran demanded *"not… any coupling-channel quantity… must be **PAIR-CHANNEL-NATIVE**"*.
This run **filters on frame-invariance alone**, disqualifies `Re h` by name and **nothing else**, and
drops the per-candidate companion demand for a single global bite-witness.

## Result: OUTCOME B — no candidate surfaced

All Klein-orbit invariants of the `λ`/`λ²` family computed exactly over ℚ — four elementary
symmetric functions, orbit product and sum, and the per-class products over the character's kernel
`{I, χ_a}` and its coset `{χ_b, D2}`. **None survives.**

## The 77 appeared, and died exactly where the seal said it would

`413·3047·953 = 1199265683`, and **77 divides it** — 77 being the resolvent radicand
(`disc μ = 6237 = 3⁴·7·11`). **Seductive, and vacuous:** `7 | 413` and `11 | 3047` are
**single-element facts B1076 already banked as an unweighted observation with no mechanism
claimed**. The orbit product's divisibility is **their product** — it adds no information, so it is
a **restatement, not a coset invariant**. Killed by the preregistered control.

**This is the second 77 to die to the same control.** B1076's hierarchy-discriminant 77 was forced
by `V_ccl` living in `K`. **A resolvent that appears everywhere is a property of the field, not a
residue** — and the seal predicted this appearance and this death in advance.

## WHAT THIS DOES AND DOES NOT SHOW — the distinction is the point

**It does NOT vindicate the guard.** B8092 finding 1 stands: the prompt is **strictly stricter** than
the condition it quotes, and a both-channel frame-invariant residue would still be excluded before
evaluation. **The guard remains a real structural defect; it simply did not happen to hide anything
in this family.**

**It does NOT touch the owner's general claim**, which rests on **B976** (eleven arcs, a count, "the
owner was right") and on **B8092**'s three guards — neither of which this run examined.

**What it DOES show:** B1074/B1076's negative **survives its own strictest objection**, reached with
the guard relaxed rather than in place. **That is a stronger negative than the one they banked**,
and it is stronger precisely because someone tried to break it on the owner's instruction.

## SCOPE

The `λ`/`CCC` family across `B¹`, using banked per-element values — **not** a re-derivation of them
(done independently today: `λ²(I) = −1` on all six couplings; `D_a = 16`, `D_b = 11`). Tests
**B8092 finding 1 only**. No measured number; mass-ratio targets as classes only.
