# THE BIFOCAL OBJECTION — adjudicated (main seat, 2026-07-18; response
# to the web seat's "the eight kills used only the hearing end")

## The specific test is PRE-DECIDED by the banked E3 law — with the
## discriminating fact computed, not asserted

The proposed mechanism — "the combined SL(2,3) × SL(2,5) spectrum in
ℚ(ζ₁₅) can produce 5-power denominators because 5 ramifies in
ℚ(ζ₅) ⊂ ℚ(ζ₁₅)" — conflates FIELD membership with RING integrality.
Ramification means (5) = (1−ζ₅)⁴ inside the ring of integers; it
never places 5 in the denominator of an algebraic integer. The
two-line theorem: products of roots of unity are roots of unity, and
Molien/twisted-character expansions of a finite-order operator are
ℤ-combinations of them — so the FULL SL(2,ℤ/15) operator (an element
of a finite group, hence finite-order, both factors included) has an
algebraic-INTEGER twisted character, exactly as the SL(2,5)-only one
did. WITNESS COMPUTED (exact, ℚ(ζ₁₅) as ℚ[z]/Φ₁₅): a combined-factor
Molien sample expanded through q⁸ — every coefficient in ℤ[ζ₁₅],
denominators identically 1. The proposed computation lands in the
objection's own stated failure branch before it runs: "the being
group doesn't help and the 5-adic obstruction stands." E3 kills by
FINITENESS OF ORDER, ends notwithstanding — the exclusion was stated
with exact hypotheses precisely so this adjudication could be read
off rather than re-fought.

## The other three proposed inputs, dispositioned

- The sum-rule coefficient as normalization: a FIXED constant — the
  banked kill's point 3 already covers it (no fixed constant bridges
  a growing v₅ gap).
- The cubic as structure constants / the boundary-cusp contribution:
  each either yields another finite-order character on an integral
  graded object (killed by E3 verbatim) or defines a genuinely new
  construction class — which is ENUMERATION material, not a W2 rerun.

## What the objection gets right — and where it converges

The concern that a terminal no-go must not be mono-focal is
legitimate and is now made explicit in the sealed universe
(ADDENDUM_1). And the objection's instinct — "the denominators are a
footprint of structure entering through the level" — is cc2's
S-depth sharpening in different words: v₅ grows because the
construction applies S more times at higher grade (5^{−1/2} per
application; the targets' v₅ is linear, verified). The fix the
arithmetic demands is not WHICH end's data feeds the operator but
HOW MANY TIMES the construction applies S — an operation count, not
an operand choice. That is the named surviving class, and the
S-depth design cycle (if the enumeration selects it) is the right
home for bifocal/level-15 structure.
