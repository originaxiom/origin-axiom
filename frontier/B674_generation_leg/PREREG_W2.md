# PREREG_W2 — THE MOLIEN CELL + THE INPUT-VERIFICATION CELL
# (frozen base; sealed 2026-07-18 BEFORE any step runs; changes only by
# separate sealed addenda + chain manifest, per the re-seal protocol)

## Provenance (the design's record)

- THE TWO-ROUTE LAW (both seats, independently): flat modular spaces
  (Riemann–Roch: bounded/linear graded growth) and flat skein modules
  (polynomial multicurve growth) are excluded TOGETHER against
  partition-like targets; any generating functor lives on an
  exponentiated (Sym/Fock) object.
- THE COLLAPSE (cc2 cross-check (b), verified on the main seat): no
  skein-side finite-order graded W exists — the weld acts on slopes by
  the hyperbolic Möbius action of A₁ (infinite order, irrational fixed
  points, grading not preserved; Sym inherits both defects). The
  exponentiated candidate is UNIQUE: the modular-side Fock space with
  the order-10 elliptic lift σ(A₁) (tr = φ, the banked law). ONE
  design point; one cell per seat.

## The candidate (exact, fixed)

V = the graded module generated by the vector-valued weight-1/5 system
(F₁, F₂) over the modular-form ring of Γ(5), graded by weight step
[PRIMARY]; the plain weight tower ⊕ M_k(Γ(5)) as CONTROL. W = σ(A₁)
(order 10, eigenvalues e^{±iπ/5}), lifted canonically to Sym(V);
candidate twisted character = the Molien series of W on Sym(V), one
per Galois conjugate over ℚ(√5).

## THE VACUITY GATE (runs first; sealed)

Compute the centrality of W on V exactly. If W is central (scalar on
every graded piece), ABORT the cell — a central W dresses the
untwisted character and any match is vacuous. No coefficient is
compared after an abort; the abort banks as DESIGN-KILL.

## cc cell — THE MOLIEN CELL (steps strictly ordered)

(i) The eigenvalue distribution of σ(A₁) on the graded pieces of V,
    exact over ℚ(√5), through enough grades to bound q^40 in Sym.
(ii) THE SUPPORT PRE-TEST (first falsifier): the Molien product's
    q-support classes mod 1 under the PER-COMPONENT Yang–Lee-
    convention η-dressing (cc2's sealed preliminary), REQUIRED to land
    {2/5, 3/5}; AND the component-swap map must be reproduced by the
    Molien doublet. Either failure ⇒ KILLED-AT-(ii); nothing further
    runs.
(iii) Coefficient comparison: ≥ 100 coefficients, both Galois
    conjugates against both banked doublet components (the W33
    targets). LOCKED until the cc2 gate below reports exact agreement.

## cc2 cell — THE INPUT-VERIFICATION CELL (the gate)

Derive the SAME eigenvalue distribution independently from the
holonomy/Fox side (the congruence shadow + the banked golden-rotation
law), no shared code with cc's step (i). EXACT agreement unlocks
(iii). ANY disagreement = STOP on both seats; diagnose before any
coefficient is compared; the disagreement banks as-is.

## Sealed outcomes (per the sealed labels; RR is not an input anywhere)

MATCH (→ banks as GENERATION; K020 silver control runs) /
STRUCTURED NEAR-MISS (the exact mismatch banked) /
KILLED-AT-STEP-(k) (the kill pattern banked alongside wave 1's seven) /
DESIGN-KILL (vacuity abort or gate STOP).
No post-hoc freedom: one rational constant in the η-bookkeeping,
per-component convention fixed above; support classes fixed; the
label rules fixed. Anything not covered here requires a sealed
addendum BEFORE the affected step runs.
