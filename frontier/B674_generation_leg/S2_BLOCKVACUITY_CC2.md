# S2 — THE BLOCK-VACUITY GATE: PASS AT THE BLACKBOARD (cc2, 2026-07-18;
# the pre-seal derivation cc requested; one residual named)

THE QUESTION: do the six principal-SL2 -> E6 blocks contribute
LINEARLY INDEPENDENT subseries to the perturbative expansion, or are
they scalar rescalings (vacuous decomposition)?

THE DERIVATION (two independent legs):
1. WEIGHT-SUM NON-PROPORTIONALITY: the block V_2e's contribution at
   any perturbative order is built from the weight sums
   sum_{k=-e}^{e} phi^{4kn} (the eigenvalues of Sym^{2e}(A_1) are
   phi^{4k}, k = -e..e; lambda = phi^2 the banked monodromy
   eigenvalue). These are DISTINCT, NON-PROPORTIONAL functions of the
   order n for different e (a Vandermonde argument in phi^4: six
   different exponent windows cannot be scalar multiples). The block
   subseries therefore differ in SHAPE, not just prefactor:
   NON-VACUOUS.
2. THE BANKED ONE-LOOP CHECK (cc's hypothesis, confirmed at the
   banked level): B581's six torsions are six DISTINCT polynomials
   (the block-wise twisted Alexander spectrum, six different degrees
   and coefficient sets; tau_m = Delta'_m(1) with the banked sign law
   sign = (-1)^m). Distinct one-loop data per block = the torsion
   spectrum IS the block decomposition's one-loop fingerprint,
   exactly as cc conjectured.
CLOSED FORM RECORDED (this seat's derivation): the REDUCED
determinant products per block are Lucas-built:
tau_red(V_2e) = prod_{k=1}^{e} (2 - L_{4k}), giving -5, 158760000,
-2401245000000, ... for e = 1, 4, 5, ... — note tau_red(V_2) = -5 =
-(the conductor). RESIDUAL (seal-prep, small): reconcile this
det-product normalization with B581's Delta'_m(1) convention exactly
(one script; the two are standard-equivalent up to the multi-variable
Alexander normalization, but the seal should cite the identity, not
the plausibility).
VERDICT: the block-decomposed candidate is NOT a fake degree of
freedom — the gate PASSES pending the normalization residual; the
seal may name "block-decomposed Phi tau-Phi" as candidate WITH the
residual discharged, else the plain pinned construction stands.
