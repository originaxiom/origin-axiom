# PREREG_W3_RUN — THE DESIGN CELL (frozen base; sealed 2026-07-18 under
# the owner's FULL GO, BEFORE any coefficient is computed). The one
# sealed shot. Model- and session-PORTABLE: any model resumes from this
# file + the repo. Changes only by a separate sealed addendum + chain.

## THE CANDIDATE (fixed, exact)

The figure-eight's Habiro/Nahm object at the GOLDEN root ζ₅ (GZ eq 1.7:
the t-deformed Nahm ring S = ℤ[t^{±1}, z^{±1}(t), δ(t)^{−1/2}] /
(1 − z − (−1)^A t z^A), δ(t) = the Nahm discriminant, eq (36)),
DECOMPOSED by the six principal-SL₂→E₆ blocks Sym^{2m}(ρ), m ∈
{1,4,5,7,8,11} (block-vacuity CLEARED, two-seat: BLOCK_VACUITY_GATE +
S2), ASSEMBLED as the holomorphic-blocks pairing Φ·τΦ (τf = f(q^{−1}),
the S²=c involution; GSWZ Prop 1.5(a)). The candidate twisted character
must (i) land support on {2/5, 3/5} — a PROGRAM DATUM the cell must
PRODUCE, not import (STAGE1_CC2: GSWZ's 4₁ is at ζ₆; GZ's ζ₅ is the
FOUR-class orbit, so nothing external selects two of four) — and (ii)
match the banked RR doublet (F₁, F₂) targets (B672 / W33).

## THE FROZEN FREEDOM CLAUSE (exactly two; STAGE1_CC2, replacing "one
## rational constant")

1. the √−δ sign (GSWZ Rmk 1.9; = the δ(t)^{−1/2} branch in the ring S);
2. the ε_α prefactor, canonical only up to c-th powers (GZ 9.3–9.4).
NOTHING ELSE is free. The normalization residual (cc2 det-product
τ_red(V_{2e}) = ∏(2−L_{4k}) vs B581 Δ′_m(1)) is discharged by RATIO:
the design compares coefficient RATIOS, which are invariant under the
per-block overall normalization — the det-product↔Δ′ unit cancels; the
seal cites this, so the residual does not enter the comparison.

## THE GATES (in order; a fail STOPS, it does not fire the shot)

- VACUITY (MB12): the block subseries must be non-proportional (CLEARED,
  two-seat) AND the {2/5,3/5} selection must be able to fail.
- THE SUPPORT PRE-TEST (first falsifier): the ζ₅ Φ·τΦ support classes,
  after the fixed η/δ bookkeeping, REQUIRED to land {2/5, 3/5}. Fail ⇒
  KILLED-AT-SUPPORT, the shot is NOT fired.
- THE TWO-SEAT GATE: two INDEPENDENT derivations of the assembled series
  — Γ(5)/modular side and holonomy/state-integral (Nahm) side, no shared
  code — must agree EXACTLY. Disagreement ⇒ STOP + diagnose (NOT a
  verdict; the shot is not fired on a disagreement).
- THE COMPARISON (the shot): ≥100 coefficients, both Galois conjugates
  vs both doublet components, under the two frozen freedoms only.

## SEALED OUTCOMES (the shot runs ONCE)

- MATCH → GENERATION (the object generates its own flavor streams AS
  MATHEMATICS); K020 silver control; bank as a theorem; stages 3–4 stay
  firewalled + owner-gated.
- STRUCTURED NEAR-MISS → bank the exact mismatch (which frozen freedom,
  the exact divergent coefficient).
- KILL → the terminal no-go banks as a THEOREM with the full
  specification attached; the leg closes; publish the mathematics.

## PROVENANCE (all banked, cited)

Divided-power law PROVED unconditional (B683/L1); block-vacuity CLEARED
two-seat (B684, BLOCK_VACUITY_GATE, S2); the exponentiation law + the
Molien kill (B674 W2); the Stage-1 constraints (STAGE1_CC2); the RR
targets (B672). Nothing here enters CLAIMS; the verdict is mathematics.
