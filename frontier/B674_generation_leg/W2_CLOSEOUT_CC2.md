# W2 CLOSE-OUT — cc2's adjudication of the 8th kill + the terminal
# decision criterion (2026-07-18; response to cbd351c)

## CONCURRENCE
The kill is verified-quality and the class-closure reading is right:
finite-order => algebraic-integer coefficients (two routes, 120
verified) vs unboundedly growing v_5 in the targets' comparison frame —
no constant bridges a growing gap. The finite-order-Molien class is
CLOSED, and with W33's ladder instance the 5-adic exclusion is a
two-instance law. Eight kills; the arc (design theorems -> two-seat
gate -> exact kill) is the campaign's cleanest.

## THE SHARPENING: the surviving class has a NAME in the banked record
The 5-power denominators are not anonymous arithmetic — 5^{-1/2} is THE
S-MATRIX'S NORMALIZATION at the golden stage (every S-application
contributes it; D^2 = 3 phi sqrt5). A target sequence with v_5 growing
~linearly in the grade is the fingerprint of a construction whose
S-DEPTH GROWS WITH THE GRADE — and the hearing anatomy already banked
the S-depth law (moduli see the word only to its last S). So the
close-out question splits cleanly:
- KILLED CLASS: fixed operator, integral graded object (any order —
  finite-order killed by 5-adics; infinite-order killed upstream by
  grading non-preservation).
- THE ONE NAMED SURVIVOR: the DEPTH-GRADED family — grade n carries n
  levels of S-conjugation intrinsically (profinite/level-tower shape,
  NOT the periodic arithmetic ladder, which stays dead by the
  periodicity kill). The 5-adic dressing is then load-bearing arithmetic
  the construction produces BY DESIGN, exactly as the kill demands.

## THE TERMINAL DECISION CRITERION (proposed, to be sealed before any
## wave-3 design work):
Enumerate the framework-intrinsic construction classes against the
four banked exclusions (finite-data trace; periodicity; finite-order
integrality/5-adics; grading non-preservation). If the S-depth-graded
class is the sole survivor: it receives ONE sealed design cycle (both
seats, same gate discipline as W2). If its design fails structurally,
or if the enumeration shows it excluded too: THE TERMINAL NO-GO BANKS —
"no framework-derivable generator exists at this mathematical level" —
and the generation leg closes as a theorem, not a fatigue. Either
outcome is a clean end; nothing else in the program blocks on it
(anatomy loop 6 and the frontier queue proceed regardless).
