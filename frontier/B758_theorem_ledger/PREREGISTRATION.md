# PREREG — B758: THE FORCED CORE — the theorem ledger (SEALED)

cc banking seat, 2026-07-22. Owner-approved approach: consolidate the FORCED part of the
record into explicit theorems; continue everything else under that discipline (every new
banked positive enters the spine or is explicitly a choice/hint/open). Gate 5 absolute:
no physics statement enters the spine; the SM-facing record enters ONLY as no-go entries.

## Admission criteria (sealed — the anti-inflation guard)
An entry is admissible iff (a) its statement is EXACT (numbers/objects named, scope
bounded); (b) its computation is banked with a repo location (the R27-7 convention);
(c) a GREEN LOCK covers its verdict-bearing content (the lock is named per entry);
(d) its grade is one of: **THEOREM** (symbolic/exact proof in-sandbox or
computer-assisted-finite) / **CENSUS-FACT** (bounded enumeration; the bound stated in the
statement) / **EXACT-IDENTITY** (a computed exact equality) / **NO-GO** (a dissolution or
obstruction whose discriminating computation is banked). Anything failing (a)-(d) stays
out — notably the JUNO hint (registered, NOT a theorem) and all HINT_LEDGER rows.

## Deliverable
docs/THEOREM_LEDGER.md — the numbered spine (T1, T2, …), each entry: grade, exact
statement, computation location(s), lock(s). Plus the going-forward rule appended to the
document header. Lock test: every cited lock file exists and collects; ledger structure
well-formed. The ledger is a VIEW of the bank (nothing new is asserted); errors in
transcription are the only failure mode and the lock guards the pointers.
