# B48 -- SL(3) Metallic Trace Maps

## Question

Does the `SL(3)` Fibonacci trace-lift observation from B27 extend to a
systematic metallic family

```text
phi_m(a)=a^m b
phi_m(b)=a
m >= 1
```

with reproducible trace-map dynamics, invariants, entropy, and fixed-line
arithmetic?

## Status

```text
FRONTIER EVIDENCE
PC12 SUPPORT
NO CLAIM PROMOTION
```

This probe is a governed intake of private side-work. It preserves the
reproducible mathematical payload while excluding raw bundles, private working
notes, and coordination artifacts.

## Run

```bash
python frontier/B48_sl3_metallic_trace_maps/probe.py
python frontier/B48_sl3_metallic_trace_maps/probe.py --deep
```

The default run checks the integer splitting classification on
`-120 <= c <= 120`, `1 <= m <= 120`. The deep run expands this to
`-300 <= c <= 120`, `1 <= m <= 260`.

## Scope

The probe checks exact trace-map algebra and compact-unitary controls. It does
not derive a physical dictionary, a selector theorem, a particle model, or an
Origin-core axiom.
