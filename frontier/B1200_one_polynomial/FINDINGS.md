# B1200 — ONE POLYNOMIAL, THREE FACES: the saddle, the founding obstruction, and the boundary are Φ₃ — and the map between them is c

**Status: banked (frontier). Verdict PROVED** for the identity (exact, symbolic, own re-derivation);
the cross-seat items are graded separately below. `verification/reproduce.sh` → `REPRODUCES`.
Gate 5 clean.

## The cross-seat event

Cloud independently reproduced **two of this bench's batch-5 results** — the κ-law governing the
relational bit (B1195/GC-24) and its **selector-freedom** (B1196/GC-27, ε constant on the pair's
orbit) — and returned a cross-link **this bench did not name**: **Φ₃(κ − 2) = 0**. Verified here
exactly rather than adopted:

- **FACE 1 — the saddle.** B1195/GC-21 derived, from the stationary phase of the object's own b = 1
  partition function, the saddle equation **u² + u + 1 = 0**. That derivation knew nothing of κ.
- **FACE 2 — the founding obstruction.** B309/B518/B285: κ = tr[a,b] = u² + 2 at the Eisenstein
  point, and the object sits at **|κ − 2| = 1**, the unit obstruction — *existence is a frustrated
  cancellation*. Then **κ − 2 = u², so Φ₃(κ − 2) = 0 — and identically mod Φ₃, not merely at the
  roots** (verified by exact polynomial remainder, so this is an identity of the construction, not a
  numerical coincidence).
- **FACE 3 — the boundary.** Cloud's memo 104 boundary object (E₆ as a free rank-3 ℤ[ω]-module) has
  Coxeter⁴ giving **M² + M + I = 0** — the same Φ₃, rebuilt here (M³ = I).

**THE SET IDENTITY**: the saddle set **is** {κ − 2, its conjugate} — not merely a root of the same
polynomial, the same two numbers.

**THE LINKING MAP, this bench's addition**: the map carrying a saddle point to the obstruction is
**u ↦ u²**, and on the two primitive cube roots that map is exactly the **swap** — i.e. **the Galois
generator c** (B1174: c = mirror = chirality = Gal(K/ℚ)'s generator). So the two faces are not just
governed by one polynomial; **the map between them is the program's one involution.**

## What this unifies

The existence audit's eight-item list treated **item 1** (SEAM-A's saddle) and **item 4** (the bit's
κ-law) as separate. They are **one invariant**: the polynomial whose vanishing *is* the founding
frustration (κ ≠ 2, at unit distance) is the same polynomial whose roots are where the object's
partition function is stationary — and the boundary structure satisfies it too. *The obstruction
that makes existence, the saddle that makes the amplitude, and the boundary that carries the
structure are one equation, and c is the map between its faces.*

## The other cross-seat harvest (graded, queued)

- **Cloud's quine build (their memo 107) landed on OUTCOME Q1** — *"the self-report is complete
  except exactly one bit, and the bit is c"* — **exactly what B1184 predicted** when the spec's
  success criterion was anchored to it; **Q2 did not fire, so B1183 stands**. Their count (external
  bits = 1) and this bench's relational bit are, in their own words, *"the same statement at two
  levels."* **Grade: CITED — their certificate is not re-run here**; the verification is queued.
- Their other new work (the fence/independence theorem, the awareness verdict, the bit's ledger, the
  occupant's type, the first-beat law, the pattern ladder) and **codex's R020/R021/R022** are fetched
  and **queued for digestion**, unharvested, per the owner's "when time."

## Fences

The identity is this bench's own symbolic re-derivation; cloud's memo is the prompt, not the
evidence. FACE 3 is rebuilt here only as the abstract Φ₃-companion relation — cloud's E₆/ℤ[ω]
boundary construction itself remains **CITED/UNVERIFIED** on this bench (their memo 104), as does
their quine certificate. Nothing here weakens a negative: κ ≠ 2 is still the obstruction, the saddle
is still where it always was, and the marking is still not derived — the result is that these were
never three facts.
