# PREREG — B751: THE ITERATED-HEARING ADJUDICATION — is 1/(2φ³) = α_s forced or found? (SEALED)

cc banking seat, 2026-07-22. Owner-directed adjudication (the owner's message explicitly
anticipates the base-rate check) of the incoming claim: "the average hearing density of the
Fibonacci word (1/φ²)·(1/(2φ)) = 1/(2φ³) = 0.118034 IS α_s(M_Z) = 0.1180(9); with
sin²θ₁₂ ≈ 1/(2φ) this gives the pattern 1/(2φⁿ)." Class: SM-value numerology at the
firewall — the stopping rule (LAW_MAP: CLOSED NEGATIVE RECORD) governs; this arc is the
DEFENSIVE lane (B655 precedent): it adjudicates the claim, it does not reopen the search.
Gate 5 absolute: NOTHING physics-facing banks regardless of outcome; SM values appear only
as adjudication targets (B655/B743 precedent). Two-outcome per cell, sealed before compute.

## Cells
1. **Arithmetic (expected TRUE).** 1/(2φ³) to 10 digits; b-density 1/φ² (Morse-Hedlund /
   substitution eigenvector — the forced part); the product identity; φ³ = 2+√5.
2. **The assignment audit (the crux).** Is "each b carries hearing amplitude 1/(2φ)"
   DERIVED or INSERTED? Facts to establish against the bank: the banked 1/(2φ) is the REAL
   PART of the COMPLEX word-level matrix element u₃†M_odd u₃ at the golden word RL (B593,
   symbolic zero) — (a) is there any banked per-LETTER hearing weight? (grep-audit);
   (b) does the claim use the full complex amplitude or silently drop i·sin(2π/5)/√5?
   (c) two-outcome: FORCED (a banked derivation exists making the composite a limit of the
   substitution) vs CONSTRUCTED (word-level Re × letter-density is a free composition).
3. **The target audit.** α_s runs: compute α_s(μ) at one loop (n_f=5) across μ ∈ {10 GeV,
   M_Z, 1 TeV} from the world-average anchor — print the span; enumerate the free knobs
   (scale, scheme, value source). Contrast: sin²θ₁₂'s banked comparison went through the
   B648 calibration machinery ONE-SHOT and was graded LOW weight (FP≈32%). Two-outcome:
   the α_s match is SCALE-PINNED by something object-side (name it) vs KNOB-DEPENDENT.
4. **Base-rate (the owner's requested check).** Candidate family fixed HERE: {1/(2φⁿ),
   n=1..6} ∪ {φ⁻ⁿ, n=1..6} ∪ {φ⁻ⁿ/2}. Target table fixed HERE: the standard dimensionless
   set (PDG central values, cited in-script): sin²θ₁₂/₁₃/₂₃ (PMNS), sin θ_C, |V_cb|,
   |V_ub|, α_em(0), α_em(M_Z), α_s(M_Z), s²_W(M_Z), m_μ/m_τ, m_e/m_μ, m_c/m_b, m_s/m_b,
   m_b/m_t, Koide Q. Matching rule fixed HERE: |cand/target − 1| < 1%. Print: total pairs,
   hits, expected hits under a log-uniform null, and the n=2 gap test (a genuine 1/(2φⁿ)
   pattern should populate n=2 = 0.1910 — is ANY target there?).
5. **The Fox fixed point (conditional).** If the incoming handoff defines the iteration:
   verify the 2-step convergence to (1/2)I AND run the artifact control — does a GENERIC
   seed converge to the same fixed point? (If yes: the fixed point is the normalization's,
   not the object's.) If no definition arrives: NEEDS-DEFINITION, uncounted.

## Verdict template (sealed)
The combinatorial identity (cell 1) banks as exact arithmetic — it was never in doubt.
The PHYSICS identification banks NOTHING under Gate 5 + the stopping rule; its epistemic
status = the AND of cells 2-4 (FORCED+SCALE-PINNED+above-base-rate would earn a
HINT_LEDGER row flagged for the L91-functor checklist; anything less = the observation is
RECORDED in HINT_LEDGER with its failure mode named, per rule 7 — record hints before
judging, never bury). No LAW_MAP row, no CLAIMS, no physics reading either way.
