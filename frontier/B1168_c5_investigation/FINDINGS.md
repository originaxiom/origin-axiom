# B1168 — the C5 investigation: the object/observer boundary at the ∞-place is NOT a definitional choice — it is a MIRROR-PARITY × DIMENSION law

**Status: banked (frontier). Verdict OPEN** (core resolved; completeness is cloud's C6 standing lane; one
piece relayed to cc3). Owner-directed: *"regarding C5 I don't think it's just a choice/decision, we need to
investigate it further."* The charter framed C5 as a definitional admission with two priced horns (object-data
⇒ G-IDENT fails / observer-data ⇒ G-IDENT stands). **The investigation finds a third answer the charter did
not price, and it is DECIDABLE, not a choice.** `verification/reproduce.sh` → `REPRODUCES`. Gate 5 clean.

## The result — a parity+dimension law

Classify each datum the object carries at the archimedean (∞) place by how the **mirror** (the
orientation-reversing self-isometry — m004 is amphichiral) acts on it, and by whether it is dimensionless:

| datum | mirror | dimension | side |
|---|---|---|---|
| Vol | **even** | dimensionless | **object** |
| cusp shape 2√3·i | **even** (fixed) | dimensionless | **object** |
| geodesic length spectrum | **even** | dimensionless | **object** |
| \|analytic torsion\| | **even** | dimensionless | **object** (magnitude) |
| Chern–Simons | **odd** | dimensionless | observer (orientation's shadow; =0 amphichiral) |
| torsion **sign** | **odd** | dimensionless | observer |
| orientation (±Vol) | **odd** | dimensionless | **observer** |
| scale (dilaton) | even | **dimensionful** | **observer** (scale-free no-go) |

All four "even/dimensionless → object" rows and the "odd → observer" rows are **own-verified** (Vol
mirror-invariant; the complex volume conjugates so CS→−CS, and CS≡0; the cusp shape τ=2√3 i satisfies −τ̄=τ;
lengths orientation-independent). 

**THE DECIDER: an archimedean datum is object-canonical iff it is mirror-EVEN and dimensionless** — i.e., iff
it is *well-defined on the un-oriented, scale-free object itself*. The observer supplies exactly the
complement: the **mirror-odd** completion (the **orientation** bit — which, once chosen, fixes the sign of
*every* odd datum: CS, the torsion sign, the complex volume's phase) plus the **dimensionful scale**.

## Why this dissolves C5 (and the charter's binary)

C5 asked: is the Mostow-canonical hyperbolic structure **object-data** or **observer-data**? The answer is
**neither horn** — it is *both, cleanly partitioned by parity+dimension*:

- **The structure IS object-data** — specifically its mirror-even, dimensionless part, which is the **entire
  geometry** (Vol, cusp shape, length/spectral data). This is Mostow-canonical: it exists on the object with
  no orientation and no scale chosen.
- **This does NOT refute G-IDENT or the adelic place-split.** The charter's dichotomy ("object-data ⇒ G-IDENT
  fails") is a **false binary**: the geometry being object-data is *compatible* with the frame
  (orientation + scale) being observer-data, because the frame is *exactly* the mirror-odd + dimensionful
  complement. The object reaches the ∞-place with all its even/dimensionless canonical data; the observer
  supplies only what the object provably cannot self-supply — the orientation (amphichirality, B1163) and the
  scale (scale-free no-go).

So **C5 is not a free choice.** It is settled by a law: *object = mirror-even ∩ dimensionless; observer =
mirror-odd ∪ dimensionful.* The owner's instinct was right — the earlier "definitional admission, the owner's
call" framing (B1163/B1165) was under-resolved; the mirror-parity makes it decidable.

## What made this investigable now — the B1167 cusp shape

The cell turned on cc3's B8138-extended cusp-shape separator (B1167). Before it, the only object-canonical
archimedean datum on the table was Vol; one datum can't reveal a *law*. The cusp shape 2√3·i is a **second**
object-canonical archimedean datum, and (B1167) it is **mirror-even** — so it lands on the same "object" side
as Vol by the same parity rule, turning a single data point into a pattern the decider can state. It is also
**√3-flavored**, tying the ∞-place geometry to the disc-144/ℚ(ζ₁₂) ring-class hinge (B1166) — the finite and
archimedean object-data share the same √3.

## Convergences (this is not an isolated claim)

- **cloud's C6** (completeness): *"every mirror-even gravitational datum is object-stated; every mirror-odd
  one requires exactly the orientation bit."* B1168 is C6's content, promoted from a *check* to the **C5
  decider** — the same parity, answering C5. The one open edge (is the parity+dimension law *complete* — does
  every archimedean datum classify?) is exactly C6's standing test, cloud's lane.
- **the firewall MD1** (STRUCTURE iff dimensionless AND forced): B1168 is MD1 *at the ∞-place*, with "forced"
  sharpened to "mirror-even" (forced-without-an-orientation-choice). Same classifier, one place deeper.
- **B1165's G-typing**: G2 (the gravitational sector) = the mirror-even dimensionless object-data; G3 (the
  frame) = the mirror-odd orientation + the dimensionful scale. B1168 is the *decider* behind the G-split.

## Fences

The core (Vol, cusp shape, CS, orientation, scale) is own-verified and textbook (CS(M̄)=−CS(M);
orientation-reversal conjugates the complex volume). **Fenced + relayed to cc3** (WebSearch budget exhausted,
so no literature sweep — per the owner's "relay to cc3 if no websearch"): the exact mirror-parity of the
Ray–Singer / Cappell–Miller analytic **torsion** (claimed mirror-odd by cloud memo 81) — an enriching example,
**not load-bearing** for the decider. The **completeness** of the law (every archimedean datum classifies
even/odd × dim) is cloud's C6 standing check, not exhaustively proven here — hence verdict OPEN, not a closed
theorem. No firewall crossing; Gate 5 clean (parity of geometric invariants; no measured SM value).

## Routes

- **C5 → owner:** resolved to a decidable parity+dimension law (not a choice); the Mostow structure is
  object-data in its even/dimensionless part, G-IDENT intact. Reported.
- **cloud (C6):** B1168 promotes C6 to the C5 decider; the completeness edge is cloud's to close (does every
  archimedean datum classify?). Relayed.
- **cc3:** confirm the analytic-torsion mirror-parity (the fenced piece; WebSearch unavailable). Relayed.
- **the adelic thesis:** refined — not "finite=object, archimedean=observer," but "object = mirror-even ∩
  dimensionless at *both* places; observer = the odd + dimensionful completion." A cleaner statement.
