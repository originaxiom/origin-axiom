# B539 — The Relations Campaign: Findings

*The reframe's final door. Prereg committed before compute; catalog frozen at
the end of R0; positive control required before the SM bin.*

## R0 — The catalog (exact)

17 read-out components; 26 of 136 pairwise ratios are pure powers of τ
(|k| ≤ 5: distribution 10/7/5/3/1). Frozen catalog of 7 two-variable integer
relations (SQ: y = x², GOLD, QUART, CUBE, RECIP, PERRON, SHIFT), every one
verified exact on its ℚ(τ) witness.

## R1 — Observer-invariance (prereg prediction partially wrong, recorded)

The canonical SQ relation ((x,y) = (τ,φ): cross-species² = within-species) is
exposed by the ratio pairs of 4 of the 6 coupling types (T1, T2, T4, T6) —
NOT by T3 or T5, whose component sets don't contain a (τ, φ) ratio pair.
Prereg predicted all 6; the falsifier fired and is recorded. SQ is
common-but-not-universal across observers; the catalog itself stands
(it is defined by the object, not by any single observer).

## R2 — The test

**Positive control (E8 forced bin, 1.5% = the published tolerance):** PASSES.
108 GOLD hits including m2/m1 = φ; the golden self-similarity
m6/m3 = m7/m4 = m8/m5 = m2/m1 exact. The detector fires where structure
forces ratios.

**Methodological catch (recorded):** the first run's only "significant" SM
hits were RECIP(1/α, α) — a tautology of list construction (both α and 1/α
were in the frozen list; likewise gW/gY = f(sin²θ_W)). The null model flagged
them as non-random precisely BECAUSE they are definitional. Both redundant
entries removed; rule for all future runs: no value and a function of itself
in the same test list.

**SM bin (19 independent dimensionless parameters, magnitude-matched null,
500 sets):**

| tol | SM hits | null mean (max) | family-wise p |
|---|---|---|---|
| 10⁻² | 1 | 1.9 (39) | 0.618 |
| 10⁻³ | 0 | 0.1 (18) | 1.000 |
| 10⁻⁴ | 0 | 0.0 (1) | 1.000 |

Hits surviving precision-tightening to 10⁻⁵: **zero**.

**VERDICT (per the frozen criteria): NO-MATCH.**

## What is now complete

The reframe's test surface is fully populated, in both directions:

| door | forced bin (critical/emergent) | dialed bin (SM parameters) |
|---|---|---|
| values | — (not the object's to give) | chance (B533 Gate 3) |
| structure | E8 ladder HIT, gap labels HIT (B538) | 0/3 checklist (B538) |
| **relations** | **control PASSES (this campaign)** | **NO-MATCH (this campaign)** |

The structural theorem in its final, fully-tested form: **the object owns the
ratios and relations that form determines — measurably, in laboratories — and
does not own the values, structure, or relations of the Standard Model's
parameter set.** The kill clause stands: this bound is falsifiable the day
anyone derives a dialed constant from pure structure.

Locks: tests/test_b539.py. No physics claim; the verdict row is the deliverable.
