# PREREG — B757: the two-ℤ/3 identity (L108, R28-7) (SEALED)

cc banking seat, 2026-07-22. cc2's DOOR4 residual: "is B326's congruence ℤ/3 the SAME ℤ/3
as B302's commensurator element? — the identity was never established." Gate 5; two-outcome
per level; E20 guard DECLARED BEFORE computing: any mod-N coincidence must be measured
against the conjugacy structure of GL(2,ℤ/N) — a coincidence forced by Sylow counting is
base-rate, not identity.

## The two objects (from the banked arcs, verbatim)
- B302: g = [[0,−1],[1,−1]] of order 3 in PGL(2,O₋₃) = Comm(Γ) (Neumann–Reid); g does NOT
  normalize Γ (the banked commensurates-not-normalizes lock); Γ torsion-free.
- B326: the deck ℤ/3 of the 3-fold cyclic cover, acting on the torsion (ℤ/4)² of H₁ as
  multiplication by t on the Alexander module, charpoly Δ ≡ Φ₃ (mod 4).

## Cells
1. **Element level (expected NO, theorem-grade).** Deck representatives live in Γ
   (covering translations = γΓ₃, γ ∈ Γ) and Γ is torsion-free ⇒ every representative has
   infinite order; g has order exactly 3 ⇒ they are never the same element. Verify g³ = I
   and charpoly(g) = Φ₃ exactly; cite the banked torsion-freeness.
2. **Congruence level (the coincidence, computed).** The deck matrix on (ℤ/4)² =
   companion(Δ mod 4) = [[0,3],[1,3]]; g mod 4 = [[0,3],[1,3]] — test EXACT equality and
   the orientation (does g match t or t²?).
3. **The E20 adjudication (the crux).** Brute-force GL(2,ℤ/4) (96 elements): count
   order-3 elements and their conjugacy classes. Expected (Sylow): ALL order-3 elements
   conjugate to C or C⁻¹ — so "conjugate mod 4" is near-forced for ANY two ℤ/3 actions
   and carries ~1 bit (the orientation) of content at most. Then adjudicate: is the
   orientation bit CANONICAL? (t is canonical — the meridian generator; g vs g² is NOT —
   B302 exhibited a representative, not an oriented class.) Expected: the orientation
   match is convention-dependent ⇒ not identity content.
4. **Verdict assembly (two-outcome).** IDENTITY (some canonical identification survives
   1–3) vs DISSOLVED (element-level NO + forced congruence coincidence + no canonical
   orientation ⇒ the shared content is exactly the already-banked Eisenstein atom (Φ₃/ω
   on both sides) and NOTHING MORE — L108 closes, DOOR4 stays ROUTE-OBSTRUCTED).
