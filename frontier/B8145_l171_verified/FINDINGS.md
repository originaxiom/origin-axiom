# B8145 — L171 verified: B8093 is right on every fact and on the typing, and its disclosed gap is now filled

**Arc dated:** 2026-08-27 · **Seat:** cc3 (audit) · **Lane:** LITERATURE. **Gate 5 untouched.**

## Why this arc exists

**I told the owner L171 and L173 were undone. Both were done** — B8093 and B8094, on the day the
package arrived. I had piped a **13-hit** grep through `head -8` and read the truncation as absence.
The owner then asked me to **verify** B8093 rather than redo it, given the demonstrated
unreliability. That is this arc.

## Eight factual claims, checked against the source

| B8093 claims | source |
|---|---|
| base algebra Type III₁ | ✔ *"the algebra of observables in the region P is a Type III algebra 𝒜"* (§2.1) |
| crossed product over `H`, the modular/boost generator | ✔ `Ĥ = H + H_obs = H + q` (eq. 20, §2.4) |
| crossed product yields II_∞ | ✔ *"a standard description of the 'crossed product' … an algebra of Type II_∞"* (§2.4) |
| positivity projection `Π = Θ(q)`, `Tr 1 = 1` | ✔ **verbatim** — `Π = Θ(q) = Θ(−H−x)` (eq. 24); `Tr 1 = 1` (eq. 25) |
| final type II₁ | ✔ abstract |
| observer forced by **ergodic** modular flow, invariants are c-numbers | ✔ **verbatim** (§2.4) |
| max-entropy density matrix `= 1` | ✔ `ρ_max = 1` |
| observer = clock, `H_obs = q ≥ 0`, `L²(ℝ₊)` | ✔ exactly, including `ℝ₊` not `ℝ` |

**Eight for eight, down to the notation.**

## The typing, re-derived

- **(a) DISANALOGY — sound.** Their engine is *ergodic* modular flow. Ours is a tracial state, and
  **tracial ⟺ trivial modular automorphism group** (Tomita–Takesaki), so `𝒜^H = 𝒜` — everything is
  invariant — and the mechanism that *forces* their observer is simply absent.
- **(b) UNRESOLVED — correctly.** B8093 names it the decisive test and declines to assert it.
- **(c) NEAR-VACUOUS — confirmed.** `ρ_max = 1` with flat spectrum is canonical for **any** II₁
  factor. **This is precisely the trap cc pre-declared**, and B8093 caught it.

One disanalogy, one owed, one vacuous. Under the binding rule that a MATCH needs a **non-generic**
contact, **MOOD is correct.**

## The disclosed gap, now filled — and it moves the reason

B8093's own scope says the successor literature *"was NOT read, so this types the foundational
construction only."* Disclosed, not hidden. So I read it.

**Witten, *A background-independent algebra in quantum gravity* (arXiv:2308.03663):** **not**
explicitly a crossed product; Type II₁ arises from a **tracial state**, whose traciality follows
from the **KMS condition** of `Ψ_dS` by holomorphic continuation; **ergodicity is not invoked**,
replaced by time-translation symmetry and thermal structure. Observer still mandatory; `q ≥ 0` and
`Π = Θ(q)` still present.

> **So B8093's reason is scoped to CLPW and does not extend.** The successor's engine is not
> ergodicity.

**MOOD survives anyway, because the disanalogy relocates rather than dissolving:** the successor's
engine is **KMS traciality**, and B721 places our tracial state **complementary to rather than
inside the CMR KMS torsor**. Disanalogous at its own engine.

**The typing is unchanged; the justification needed extending; the extension came from our own
banked material.**

## SCOPE

- The successor was read at **one fetch of its full text**, not exhaustively — this types its
  construction **mechanism**, not its every claim. Penington–Witten and the wider set unread.
- **Not claimed:** that B8093 was wrong (it is right throughout); that the successor changes the
  typing; that our modular flow is trivial **by derivation** — B8093's own inverted question, still
  open.
