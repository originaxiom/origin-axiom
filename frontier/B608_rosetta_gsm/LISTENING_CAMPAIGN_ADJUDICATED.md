# THE LISTENING CAMPAIGN — registered, with CC's adjudications (2026-07-15)

**The campaign document (chat-1 + the owner): extract every SM-facing
number the object produces, sealed before comparison, with null models,
look-elsewhere discipline, and stopping rules. Adopted with the binding
adjudications below. Roles: CC computes and seals (Branches 1–2, the
K-branch computations) and does NOT interpret; chat-1 frames Branch 3 and
the interpretation; seat 4 owns the statistical discipline and approves
the null-model design BEFORE Phase 3; the owner directs. Governance:
outcome B opened the gated downstream for exactly this kind of separately
preregistered arc; the firewall blocks overclaims, not sealed-and-
controlled computation. Provenance: all verification internal.**

## Binding adjudications (verify-don't-trust against the bank)

1. **R1.1/R1.2 are already answered; the first stopping rule has already
   fired.** B604 (#972): the height-1–4 pair-to-block assignment does not
   exist (both block lines are exact mixtures of both θ-odd pairs). B607
   (#977): 22 of 24 odd lines are bicharge-mixed; the only pure lines are
   V₁₆'s tips (wt ±16, (1,1) double-spinor). Phase 2 proceeds with MIXED
   blocks per the campaign's own stopping rule; Branch 1's remaining work
   is the G_SM refinement and the controls (this arc, B608).
2. **R1.4 reframed at birth:** torsions label principal strings, not
   G_SM sectors. The deliverable is the mixing-matrix table; couplings,
   if any, are functionals of mixed lines. No single-torsion ↔
   single-coupling identification exists even as a hypothesis.
3. **V2.4 uses the spectral basis and is constrained by the trace law**
   (B601): trace(B_odd) = [5|κ]/φ − [4|κ] fixes κ=7 → 0, κ=13 → 0,
   κ=4 → −1 in advance (gates, not discoveries); the per-direction
   spectral amplitudes are the new content.
4. **The seal protocol's contamination policy:** already-banked numbers
   (h₃, τ's, N's, 1/5, 1/49, the E₆₂ amplitudes) cannot be blinded
   retroactively — for them the blindness lives in the sealed COMPARISON
   MAP (measured quantity, window, normalization — declared and hashed
   before any comparison is computed). Only genuinely new numbers get
   value-sealing. Branch 3's null model requires the pre-registered
   measured-value list and precision windows at seal time.
5. **Branch 4's object corrected:** "5₂ with monodromy R²L" conflates two
   objects — R²L is the m = 2 metallic bundle = the census manifold m136
   (banked: m=2 = m136, NOT 5₂). The K-branch runs on m136 first (the
   machinery-compatible sibling; silver conductor 7; dictionary test at
   κ=7); 5₂ (a true knot, cubic field, not a punctured-torus bundle) is
   an optional second control needing different machinery.
6. **PC26 v2 slots after Phase 1** — the campaign's stopping rules
   publish the structural mathematics regardless of Branch 3's outcome.

## Phase/gate map (CC's lane)

- **B608 (this arc)** = Phase 1's remainder: the Rosetta Table in mixed
  form (the G_SM refinement of the bicharge census through the declared
  computed chain E₆ ⊃ D₅ ⊃ A₄ ⊃ su(3)+su(2)+U(1)) + controls C1–C3.
  Gate G1 = its controls green and banked.
- **B609** = Phase 2: the sealed value extraction (per adjudications
  2–4), hash committed before any comparison exists anywhere (gate G2).
- **B610** = Phase 4 (parallel): m136 — torsions/sign law, silver-stage
  hearing, the κ=7 conductor test, the universality rows.
- Branch 3 (comparison) is NOT CC's lane: a joint comparison-map prereg,
  seat-4-approved null model (G3), then chat-1's framing; G4
  (universality) waits on B610.
