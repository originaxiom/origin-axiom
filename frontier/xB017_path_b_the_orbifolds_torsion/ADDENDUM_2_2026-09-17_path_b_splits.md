# xB017 — ADDENDUM 2: the owner asked "are u sure about b". I was not. Path B SPLITS.

**2026-09-17. `PREREGISTRATION.md` untouched (`82134f36…`); new cells C1–C4 in
`verification/addendum2_reaim.py`. Two corrections, both to this arc's own headline claims.**

Four words, the same four that overturned xB014's kill. **He is right again, in two places.**

---

## C1 — N4 overreached: the distinction is CYCLOTOMIC + A6, not A6 alone

N4 concluded: *"`d = 3` is the unique field ramified only at 3 **and** has the smallest `|D|` — those
are **one fact**, and it is **minimality**, i.e. **A6 restated**."*

**The "one fact" step does not follow.** "Ramified only at 3" and "minimal `|D|`" are **different
properties that happen to coincide at `d = 3`**. Ramification at a single prime is common —
`d = 1, 2, 3, 7, 11, 19, 23, 31, 43, 47, …` all have prime discriminant. And `|D| = 3` is minimal
because a fundamental discriminant is `≡ 0 or 1 (mod 4)`, so `−1` and `−2` are excluded and `−3` is
the first — **a congruence fact, not a consequence of "3 is the only ramified prime."**

**The honest decomposition, which Addendum 1's A4 made visible and this arc failed to draw:**

> `|O_d^×| > 2` for exactly `d = 1, 3` — **and those are exactly the two imaginary quadratic fields
> that are CYCLOTOMIC**, `ℚ(ζ₄)` and `ℚ(ζ₃)`. Being cyclotomic is what supplies `μ₆`, hence the
> cusp's ℤ/3. It leaves **two** candidates; **minimality (A6) picks `ℚ(ζ₃)` between them.**

> **So `d = 3` is selected by CYCLOTOMIC — an arithmetic fact A6 does not supply — PLUS A6.** N4 said
> *"A6 restated."* **That was too strong: one bit of the selection is genuinely arithmetic, and
> xB017 gave it away.** `d = 3` is **more** distinguished than the arc said, not less. **The arc
> under-claimed against itself.**

## C2 — the headline overstated the kill: Path B's COVERING half is POSITIVE

The owner's Path B was *"the base orbifold's ℤ/2 and ℤ/3 torsion as **covering/deck** data."* xB017's
headline was *"the framing is refuted."* **That refuted the DECK half only.**

The cusp rotation `E = diag(ζ₆, ζ₆⁻¹)` has `det = 1` and `E³ = −I` — **order 3 in PSL**, with trace
`1 ∈ {−1,0,1}` exactly as N2's own argument requires. **The cusp's ℤ/3 is genuine 3-torsion of
`PSL(2,O₃)`.** And the cusp cross-section is `T²/(O^×)²`: `S²(3,3,3)` for `d = 3`, `S²(2,2,2,2)` for
`d = 1`, **`T²` for every other field**. That **is** covering data — it is how the cover's cusp torus
sits over the orbifold's cusp.

> **Path B's question has a POSITIVE answer: the orbifold's ℤ/3 sits at the cusp, is genuine
> 3-torsion, and is `d = 3`'s ALONE among all imaginary quadratic fields.**

**And what is new in it, measured against the record.** **B302 already banked the fact** — *"the
object has no order-3; `PGL(2,O₋₃)` has order-3 elements; these are **hidden symmetries**; the
figure-eight covers the order-3 orbifold, index 12."* **B302 did not measure whether that hidden ℤ/3
is generic. Addendum 1's A4 does: it is not.**

> **The new result is: B302's hidden ℤ/3 is `d = 3`'s alone.**

**xB017 shipped this as *"the invariant this arc missed"* and wrote that the exception *"is not the
one the path was built on."* It is exactly the one the path was built on.** A positive was filed
under a negative headline — **the same shape as xB014's kill, and caught the same way, by the owner.**

## C3 — and the kill this seat nearly walked into

The tempting next step was *"the orbifold's cusp ℤ/3 descends to m004 as a hexagonal cusp symmetry."*
**The record already killed it, as its 11th kill.**

**B486:** m004's cusp modulus is `τ = 2√−3` — verified here, `Re(τ) = −1.1×10⁻¹⁵` — a **rectangular**
lattice, the CM point of discriminant **−48**, *not* the hexagonal point `e^{2πi/3}` of discriminant
−3. B486's named error is precisely *conflating "the cusp modulus lies in `ℚ(√−3)`" with "the cusp is
the hexagonal torus `ℂ/ℤ[ω]`."* And **m004's symmetry group has order 8, not divisible by 3** — so
**m004 has no order-3 symmetry at all** and the orbifold's ℤ/3 is **not inherited** by the cover,
which is B302's banked point.

> **The ℤ/3 stays in the orbifold.** It is `d = 3`-specific **and** it does not descend. Both halves
> matter: the positive is real **and** it is fenced.

## C4 — the re-aimed verdict

**What xB017 got right and stands:** N2 (the torsion **orders** `{2,3}` are generic) · N3 (the
ramified-prime route to 2T is generic, 24.6 %) · **N5(b) — `Γ` is not normal, the cover is irregular,
there is no deck group**, confirmed twice over (the isometry count here; `N(Γ)/Γ ≅ ℤ/2` in the
literature sweep). **The deck half is dead.**

**What xB017 got wrong:** N4's *"A6 restated"* (it is **cyclotomic + A6**), and the headline's
*"framing refuted"* (**only the deck half**).

> **PATH B SPLITS. Deck data: REFUTED. Covering data: POSITIVE** — the orbifold's ℤ/3 lives at the
> cusp, is genuine 3-torsion, is **`d = 3`'s alone** (new: B302 had the fact, not the base rate),
> comes from the field being **cyclotomic**, and **does not descend** to m004.

**What it is not:** a mechanism, a physics reading, or a new identification. **The ℤ/3 that
distinguishes `d = 3` is precisely the one the object cannot see** — B302's banked point, now with a
base rate under it.

---

**The standing lesson, and it is the second instance.** *"u sure about extremality"* overturned
xB014's kill; *"are u sure about b"* has now re-aimed this one. **Both times the failure was the
same: a kill aimed at a half of the question, reported as a kill of the whole.** The rule this earns:
**when a path has two clauses, a kill must say which clause it kills — and check the other one before
the headline is written.**

**Artifacts:** `verification/addendum2_reaim.py` (C1–C4), `verification/addendum2_reaim.json`,
`verification/addendum2_reaim.out`.
