> **ADDENDUM 2, 2026-09-17 — THIS FILE'S HEADLINE AND ITS N4 ARE BOTH CORRECTED. READ IT FIRST.**
> The owner asked *"are u sure about b"*. **I was not.** **(i)** N4's *"those are one fact … A6
> restated"* **overreached**: "ramified only at 3" and "minimal `|D|`" are different properties that
> coincide at `d = 3`. The honest decomposition is **CYCLOTOMIC + A6** — `|O_d^×| > 2` exactly for
> `d = 1, 3`, which are exactly the two **cyclotomic** imaginary quadratic fields, and A6 picks
> `ℚ(ζ₃)` between them. **One bit of the selection is genuinely arithmetic, and this arc gave it
> away.** **(ii)** The headline *"Path B's framing is REFUTED"* **overstated the kill — it refutes
> the DECK half only.** The **covering** half is **POSITIVE**: the orbifold's ℤ/3 sits at the cusp,
> is genuine 3-torsion, and is **`d = 3`'s alone** — which is exactly what Path B asked, and which
> §6 shipped as *"the invariant this arc missed."* **A positive was filed under a negative headline.**
> **N2, N3 and N5(b) stand unchanged.**

> **ADDENDUM 1, 2026-09-17 — READ IT BEFORE THIS FILE.** The commissioned literature sweep returned
> after this arc banked. It **confirms N1–N5 and supplies citations** for what the arc computed — and
> it does two more things. **(i) It challenged B734's congruence level; adjudicated BY COMPUTATION on
> this bench, B734 is RIGHT and the challenge is an E21 instance** (an SL index read as a PSL index —
> the same slip the ledger recorded on the same group in July 2026). **(ii) It named an invariant
> this arc never tested: the UNIT GROUP.** `O_d^× = {±1}` for every `d` but two, and `d = 3`'s `μ₆`
> gives the Bianchi orbifold a cusp cross-section `S²(3,3,3)` where every other field but `d = 1` has
> a torus. **§2's N2 is SCOPED, not overturned** — the torsion *orders* are generic as proved, but
> **the cusp's ℤ/3 is d = 3's alone**, and it is neither the torsion orders nor the ramification this
> arc tested. **Path B's verdict is unchanged** (N5(b) stands: no deck group), but the honest answer
> to *"what distinguishes d = 3"* is its **unit group**, and this arc missed it.

# xB017 — PATH B: the torsion is every Bianchi group's, the deck frame is refuted, and d = 3's distinction is axiom A6 restated

**Seat `xb`, `sep16-branch`, 2026-09-17. PREREGISTRATION sealed `82134f36…` and PUSHED at `451a0ee`
before `verification/` existed. Verdict: PROVED — Path B returns a **paid debt and a refuted frame**,
which is what the seal predicted.**

Gate 5 absolute: no value, no generation count, no physics reading, nothing to `CLAIMS.md`.

---

## 0. The path, and the debt it pays

**Path B, owner-approved:** *the base orbifold's ℤ/2 and ℤ/3 torsion as covering/deck data.*

**xB012 booked this debt against itself, in its own V5**, and this arc is the payment:

> *"Base rate owed and unmeasured. Every Bianchi group `PSL(2,O_d)` has torsion and a reduction map.
> What is specific to `d = 3` is that the ramified prime has norm 3 and `SL(2,𝔽₃) = 2T` — whether
> that is object-specific or generic is **not** settled here."*

**It is settled here. It is generic.**

---

## 1. N1 — xB012's V3/V4 reproduce exactly

Torsion orders `{2,3}` (witnesses verified); the ring map `O₃/(√−3) ≅ 𝔽₃` via `a+bω ↦ a+b`, **0
failures over 28 561 pairs** under both `+` and `×`; `SL(2,O₃) ↠ SL(2,𝔽₃)`, image order **24**; and
the order spectrum `{1:1, 2:1, 3:8, 4:6, 6:8}` **identifies it as 2T by computation, not by
citation.**

## 2. N2 — the torsion is GENERIC, and half the path's premise dissolves

A finite-order element of `PSL(2,O_d)` is elliptic, so its trace is **real** and in `(−2,2)`; and
`O_d ∩ ℝ = ℤ` for **every** `d`, because `O_d = ℤ + ℤτ` with `τ` non-real (verified for all 37
squarefree `d ≤ 60`, both shapes of the ring of integers). So the trace lies in `{−1,0,1}` and the
order is **2 or 3**. And both orders are **actually present for every `d`**, because `[[0,−1],[1,0]]`
and `[[0,−1],[1,−1]]` have **integer** entries and so lie in `SL(2,ℤ) ⊂ SL(2,O_d)` for every field.

> **Orders `{2,3}` occur in every Bianchi group, and no other order occurs in any of them. The
> orbifold's ℤ/2 and ℤ/3 carry ZERO bits distinguishing `d = 3`.**

## 3. N3 — the reduction base rate (the blind cell): the mechanism is generic too

**Which `SL(2,𝔽_p)` is a binary polyhedral (McKay) group — computed by order spectrum, not asserted:**

| p | \|SL(2,𝔽_p)\| | binary polyhedral? |
|---|---|---|
| 2 | 6 | no |
| **3** | **24** | **2T — the E₆ end** |
| 5 | 120 | **2I — the E₈ end** |
| 7, 11, 13 | 336, 1320, 2184 | no |

Across the **122** imaginary quadratic fields with squarefree `d ≤ 200`:

| | count | rate |
|---|---|---|
| reach **2T** (3 ramifies) | **30** | **24.6 %** |
| reach **2I** (5 ramifies) | 21 | 17.2 % |
| reach **both** | 5 | 4.1 % |

> **About one imaginary quadratic field in four reaches 2T by the same ramified reduction.** The
> `ℚ(√−3) → 2T → McKay → E₆` spine is **not `d = 3`'s property as a mechanism** — the mechanism is
> generic, and xB012's V4 (*"the Bianchi group's own reduction"*) is true of a quarter of all Bianchi
> groups.

*A correction this cell made to itself:* a first draft of the headline said **"one field in three"**
against its own measured **24.6 %**. Overstating one's own number is the same failure as
mis-citing someone else's; corrected in the cell's text where a reader will see it.

**An observation the cell did not expect, recorded and NOT spent.** The fields reaching **both**
McKay groups are `d = 15, 30, 105, 165, 195`, and the smallest is **ℚ(√−15)**, whose ramified primes
are exactly `{3, 5}` — giving **2T (the E₆ end) and 2I (the E₈ end)**. **B336 reached the same field
from a completely different direction**, calling it the *"abstract compositum / class-field of the two
ends."* **Two independent routes to one field.** This is a **corroboration of B336's picture**, not a
new identification: nothing here claims the two 2T's or the two 2I's are the same group (the
I-10/E82 family, priced by xB005 at **0.58 bits**), and **B336's own conclusion — that the routes to
`√−15` are provably closed — is untouched.**

## 4. N4 — d = 3 IS distinguished, in exactly one way, and it is A6

`d = 3` is the **unique** imaginary quadratic field ramified **only** at 3, and it attains the
**smallest `|D|`** of any imaginary quadratic field (`|D| = 3`, against 4, 7, 8, 11, …).

> **Those are one fact, not two.** `|D| = 3` is minimal *because* 3 is the only ramified prime and 3
> is the smallest odd prime. **So the distinction is MINIMALITY — axiom A6 restated, not an
> independent arithmetic selector.** The arithmetic did not pick `d = 3`; **the minimality axiom did,
> and the arithmetic followed.**

This is the same shape xB013's Addendum 1 found for the two faces and xB015 found for A5 in the
k-coupling: **every finite step is an axiom.**

## 5. N5 — the deck frame is REFUTED

**The representation is pinned against a banked value rather than assumed.** For the Riley pair
`A = [[1,1],[0,1]]`, `B = [[1,0],[x,1]]`, `tr[A,B] = x² + 2`; xB013's X1 banked `κ = 3/2 − (√3/2)i`
with `κ − 2 = ω²` (verified), so `x² = ω²` and `x = ±ω`.

**(a) Is `Γ = π₁(m004)` the principal congruence subgroup at level `(√−3)`? NO.** Its image in
`SL(2,𝔽₃)` is the **whole group** (24 of 24), and in `PSL(2,𝔽₃)` **12 of 12 — index 1. `Γ surjects`.**
*This was derived and disclosed in the seal before running:* `(3) = (√−3)²`, so reduction mod `(√−3)`
factors through reduction mod `(3)`, and **B734's corrected table gives index 1 at level `(3)`.**

**(b) Is `Γ` even NORMAL in `PSL(2,O₃)`? NO — and this is the cell the seal named as the kill.**
The covolume index is **12** (re-derived, matching B734). A regular cover's deck group has order equal
to the index and acts **freely by isometries**; `PSL(2,O₃)` is orientation-preserving, so the deck
group embeds in `Isom⁺(m004)`, **of order 4**. `12 ∤ 4` — and `12 > 8`, the **full** isometry group,
so the conclusion does not depend on the orientation bookkeeping.

> **`m004 → H³/PSL(2,O₃)` is an IRREGULAR cover with NO DECK GROUP AT ALL. "The orbifold's torsion as
> covering/deck data" is the wrong frame for this object. Path B's framing is refuted by its own
> preregistered kill condition.**

**The coincidence, put through B757's standard.** `[PSL(2,O₃) : Γ] = 12 = |PSL(2,𝔽₃)|`. But `Γ`
**surjects** onto `PSL(2,𝔽₃)` rather than being its kernel — **so the two 12s are not the same 12.**
B757 dissolved the "two ℤ/3s" identity because the coincidence was Sylow-forced and *"carries ZERO
bits beyond membership."* **Same standard, same outcome: this equality is worth nothing, and this arc
does not spend it.**

---

## 6. N6 — the verdict

**What dissolves:** the **torsion** half of the premise (generic to every Bianchi group) · the **deck**
half (irregular cover, no deck group) · the `12 = 12` coincidence (Γ surjects).

**What survives, and it is xB012's rather than this arc's:** `SL(2,O₃) ↠ SL(2,𝔽₃) = 2T` is real — and
now **measured**, at a **24.6 %** base rate, so the mechanism is generic · and `d = 3`'s distinction is
**minimality, i.e. A6**.

> **PATH B RETURNS A PAID DEBT AND A REFUTED FRAME, NOT A MECHANISM.** That is exactly what the seal
> predicted, and it is the honest payment of a debt xB012 booked against itself.

**Where this leaves the three approved paths:** **A** closed (xB014/xB016 — does not cross, all three
routes closed, the third by citation). **B** closed here. **C** — *are the A5 bit (m003 vs m004) and
the A7 bit (LR vs RL) the same ℤ/2?* — **remains the only one not yet run**, and after xB015 it is
sharper than when it was approved: A5 is now known to be the bit the k-coupling sees.

## 6b. On citations — this arc is built not to need them

The session's standing lesson is that **four of five citations this seat gave from memory were
wrong** (E58's clause). **This arc therefore cites nothing load-bearing.** Every fact it uses is
computed or argued in-cell: the torsion orders from `O_d ∩ ℝ = ℤ` plus exhibited integral witnesses;
`2T` and `2I` identified from **order spectra** rather than from a remembered isomorphism; the base
rate enumerated; the congruence index computed; the normality settled by an isometry count. A
literature sweep on Bianchi torsion and congruence covers was commissioned and had **not returned**
when this arc banked — **if it corrects anything here, this arc owes an addendum**, and its cells are
re-runnable in about two minutes.

## 7. What this arc does NOT claim

Not that the orbifold's ℤ/3 **is** a trinification ℤ/3, `2T/Q₈` or `Z(E₆)` (xB005's priced family;
E82) · not that ℚ(√−15)'s double reduction identifies anything (B336's closure untouched) · not that
minimality is an independent selector · not any physics reading · nothing to `CLAIMS.md`.

**Locks / artifacts:** `verification/orbifold_torsion.py` (N1–N6),
`verification/orbifold_torsion.json`, `verification/orbifold_torsion.out`,
`verification/reproduce.sh`.
