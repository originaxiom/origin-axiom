# xB017 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell of this arc's
`verification/` runs.**

## P0

Bianchi groups, congruence subgroups, torsion, and covering data. Named mathematics. No value, no
generation count, no physics reading, nothing to `CLAIMS.md`. Gate 5 absolute.

## The path, and the debt it pays

**Path B, owner-approved:** *the base orbifold's ℤ/2 and ℤ/3 torsion as covering/deck data.*

**xB012 named this debt in its own V5, and this arc is its payment:**

> *"Base rate owed and unmeasured. Every Bianchi group `PSL(2,O_d)` has torsion and a reduction map.
> What is specific to `d = 3` is that the ramified prime has norm 3 and `SL(2,𝔽₃) = 2T` — whether
> that is object-specific or generic is **not** settled here."*

## The sweep, done first (xB010's rule), and it reshaped the arc

Read, not cited:

* **B731 → RETRACTED by B734.** B731 concluded m004 is non-congruence from a level check that
  stopped one level too shallow (error class E22). **B734's corrected table is the load-bearing
  input here:** `Γ = π₁(m004) ⊂ PSL(2,O₃)` has **geometric index 12**, and the PSL image-index by
  level runs `2→6, 3→1, 4→6, 5→1, 8→12, 16→12` — so **m004 IS congruence, at level `(2)³ = (8)`**,
  and at level `(3)` it has index **1**, i.e. **Γ surjects onto `PSL(2,O₃/3)`.** The sister m003 is
  congruence at level `(2)¹` with quotient `A₅`.
* **B757 — the precedent that governs this arc's temptation.** The "two ℤ/3s" identity was
  **DISSOLVED**: the coincidence was **Sylow-forced** (all order-3 elements of `GL(2,ℤ/4)` form one
  conjugacy class), so it *"carries ZERO bits beyond membership."* **A numerical coincidence here
  must clear B757 or it is nothing.**
* **xB012** — V3 (torsion orders {2,3} exhibited), V4 (`O₃/(√−3) ≅ 𝔽₃`, `SL(2,O₃) ↠ SL(2,𝔽₃)`,
  image order 24 = |2T|), V5 (the debt above). **To be re-derived here, not carried across.**
* **B302** — the order-3 symmetry *"absent from the torsion-free knot group"*.
* **xB005** — the linkage-kill class: identifying the orbifold's ℤ/3 with a trinification ℤ/3 or
  with `Z(E₆)` is an I-10/E82 claim, priced at **0.58 bits**. **This arc makes no such claim.**

## DISCLOSURE — what is derived in advance, and what is blind

**Not a blind arc, and it must not be read as one.** N5(a)'s answer is **derivable from B734's table
before running**: `(3) = (√−3)²`, so reduction mod `(√−3)` factors through reduction mod `(3)`, and
B734 gives index **1** at level `(3)` — so Γ must surject onto `PSL(2,𝔽₃)` as well. That derivation
is written here, in advance, and the cell verifies it directly rather than inheriting it.

**The genuinely blind cells are N3 (the base rate) and N5(b) (normality).** N5(b) is the one that can
overturn the path's framing, and I cannot call it.

## The cells, with predictions and kill conditions

**N1 — xB012's V3/V4, re-derived from scratch.** Torsion orders in `PSL(2,O₃)`; the ring map
`O₃/(√−3) ≅ 𝔽₃`; surjectivity of `SL(2,O₃) → SL(2,𝔽₃)` and the image order.
*Prediction:* reproduces xB012 — orders `{2,3}`, image order **24 = |2T|**.
*Kill:* any disagreement stops the arc until resolved.

**N2 — the torsion base rate (the debt, part one).** Are the orders `{2,3}` specific to `d = 3`?
*Prediction:* **NO — generic to every Bianchi group.** An elliptic element of order `n` in
`PSL(2,O_d)` has trace `2cos(πk/n)`, which must lie in `O_d ∩ ℝ = ℤ`, forcing `n ∈ {1,2,3}`. So the
orbifold's ℤ/2 and ℤ/3 **carry no information distinguishing `d = 3`.**
*Kill:* a Bianchi group with torsion outside `{2,3}` refutes the argument.
*Consequence if the prediction holds, stated in advance:* **half of Path B's premise dissolves** —
the torsion is not the object's, it is every Bianchi group's, and this arc says so in its headline.

**N3 — the reduction base rate (the debt, part two; BLIND).** Across imaginary quadratic fields:
the ramified primes, their norms `q`, and whether `SL(2,𝔽_q)` is a binary polyhedral (McKay) group.
*Expectation, not a prediction:* the binary polyhedral `SL(2,𝔽_q)` are `q = 3` (`2T`) and `q = 5`
(`2I`) only — **to be verified by construction, not asserted.**
*Kill / the outcome that would deflate the programme:* if **many** fields reach `2T` by ramified
reduction, the `ℚ(√−3) → 2T → E₆` spine is **generic**, and this arc must report that as its
headline rather than bury it.

**N4 — is `d = 3` distinguished at all?** Among fields whose discriminant is divisible by 3.
*Prediction:* `d = 3` is the **unique** imaginary quadratic field of discriminant exactly `−3` — 3 is
its **only** ramified prime, and `|D| = 3` is minimal.
*Required honesty, declared now:* if the distinction reduces to **minimality**, this arc must say
that it is **axiom A6 restated**, not an independent selector.

**N5 — the deck frame, and the cell that can kill the path (BLIND at (b)).**
*(a)* Is `Γ = π₁(m004)` the principal congruence subgroup at level `(√−3)`?
**Derived prediction, disclosed above: NO — Γ surjects onto `PSL(2,𝔽₃)`, image index 1.**
*(b)* **Is `Γ` even NORMAL in `PSL(2,O₃)`?** If it is not, then `m004 → H³/PSL(2,O₃)` is an
**irregular** cover with **no deck group at all**, and *"the orbifold's torsion as deck data"* is the
wrong frame for this object.
*Kill:* if (b) returns non-normal, **Path B's framing is refuted**, and this arc reports that as its
result rather than retreating to a weaker claim.
*The coincidence that must clear B757:* `[PSL(2,O₃) : Γ] = 12 = |PSL(2,𝔽₃)|`. **If Γ is not the
congruence kernel, this equality is a coincidence and is worth zero bits** — B757's standard,
applied to this arc's own temptation.

**N6 — the verdict.** What survives of Path B, what dissolves, and whether anything here is a
selector rather than a restatement.

## What this arc will NOT claim

Not that the orbifold's ℤ/3 **is** a trinification ℤ/3, `2T/Q₈` or `Z(E₆)` (xB005's priced family;
E82's class) · not that a numerical coincidence is structure without clearing B757 · not that
minimality is an independent selector if it is A6 restated · no physics reading, nothing to
`CLAIMS.md`.

## The seat's prior, declared

N1 reproduces · **N2 dissolves half the path's premise** · N3 I cannot call, and it is where the
programme is most exposed · N4 gives a distinction that I expect to be **A6 restated** · **N5(a) NO
by derivation, N5(b) I cannot call** · **N6: I expect Path B to return a base rate and a dissolved
frame rather than a mechanism** — which, if so, is the honest payment of a debt the record itself
booked, not a discovery.
