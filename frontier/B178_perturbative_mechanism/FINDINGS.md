# B178 — the perturbative mechanism: the width law and the golden privilege are one (textbook) thing

**Date:** 2026-06-18. **Status:** the "understand-completely" consolidation of the multibody arc — and itself a
multi-step **verify-don't-trust record**. It identifies the **one perturbative mechanism** behind both flagship
`[num]` results (the width law B175, the golden privilege B176), introduces the **contamination-robust index method**
for measuring labelled gaps, and states **honestly** what is and is not numerically confirmable in-sandbox.
**Firewall-side**: emergent quasicrystal / perturbation-theory math (`K010` boundary); no scale/Λ; **nothing to
`../../CLAIMS.md`**; P1–P16 frozen. Ledger V172. Reproducer `perturbative_mechanism.py` (`ALL CHECKS PASS`).

## The mechanism (textbook)

For the woven chain `V_n = λ₁ cos(2πα₁n) + λ₂ cos(2πα₂n)`, the combination gap of gap-labeling label `(n₁,n₂)` is the
**order-`|n₁|+|n₂|` term of the perturbation series** — generated by combining `|n₁|` quanta of `α₁` and `|n₂|` of
`α₂` — so its amplitude is `~ λ₁^{|n₁|} λ₂^{|n₂|}`, with energy denominators whose smallness is controlled by the
**Diophantine** quality of the frequencies. From this *one* formula:
- the **order / λ-powers** give the **width law** (B175);
- the **Diophantine small-denominator robustness** gives the **golden privilege** (B176).

This is standard nearly-free / KAM perturbation theory; B178's contribution is to *articulate* it for this object and
test what the in-sandbox numerics can and cannot confirm.

## What the numerics confirm (and the honest limit)

- **C1 [method].** The **index method** — pinning each gap to its gap-labeling eigenvalue index `k=round(IDS·N)` and
  taking `e[k]−e[k−1]` (±1) — is the **contamination-robust** gap measure (each label has a *distinct* index, so
  neighbors can't leak in, unlike a ±0.003-IDS window that spans ~80 indices). At the weak couplings used for the
  slopes it **agrees** with the window-max (both give the physical `w(2,1) < w(1,1)`), so the power shortfall below is
  **genuine saturation, not a measurement artifact**. *(The window-max **can** contaminate at stronger coupling — a
  ratio probe found `w(2,1)≈w(1,1)` at λ≈0.3 — so the index method is preferred; but B175's weak-coupling order-3 was
  measured where the methods agree and **stands**.)*
- **C2 [per-frequency structure — confirmed].** Independent-λ scaling (index method): the **power-1 directions are
  clean** (`(1,1)` both ≈0.98; `(2,1)` λ₂-power 0.97; `(1,2)` λ₁-power 0.90), and `(2,1)`/`(1,2)` carry a **distinct
  higher** λ-power (≈1.68 / 1.60 — clearly > 1). So the **structure `width ∝ λ₁^{|n₁|}λ₂^{|n₂|}` holds** — λ₁ and λ₂
  enter as separate powers, not just a lumped total order. **The honest limit:** the *exact integer* (power-2 = 2) is
  **textbook**; numerically it **plateaus at ~1.7** (saturation + finite-N) and is **not cleanly resolved to 2**
  in-sandbox — even at the weakest accessible couplings the local slopes hover at 1.7–1.8 and decline toward strong
  coupling. A clean integer-power confirmation would need much weaker coupling (gaps below resolution) — NEEDS-SPECIALIST.
- **C3 [golden privilege — heuristic].** The banked B176 dressing leads (golden/silver 8.9×, golden/bronze 3.4×) far
  exceed the *linear* Diophantine leads `D=1/√(m²+4)` (1.26×, 1.61×), while silver/bronze is ~unamplified (1.1×). So
  golden's modest *linear* irrationality lead is **amplified** (the small denominator enters at each of the
  `|n₁|+|n₂|` orders) into "golden stands alone," leaving silver≈bronze — reconciling B176. This is a **plausible
  heuristic**, not a derivation (the clean power law it rests on is itself saturation-limited).
- **C4 [synthesis].** One mechanism underlies both flagships; in-sandbox the *structure* is confirmed
  (contamination-robust), the *exact high-order integers* stay textbook + saturation-limited.

## The verify-don't-trust record (this finding earned its honesty the hard way)

A first attempt to "derive the width law" via the window-max method came out wrong; an over-hasty read called it a
**failure** ("can't be derived, saturation"); a sharper look (prompted by an owner challenge) found the **window-max
can contaminate** (`w(2,1)≈w(1,1)`, unphysical, at stronger coupling); switching to the **index method** restored the
physical ordering and confirmed the **per-frequency structure**, while showing the power-2 shortfall is **genuine
saturation** (the methods *agree* at the weak couplings used) — **not** contamination, and **B175 stands** (an
intermediate "B175 is contaminated" alarm was itself walked back). Net: *the law is textbook + structurally confirmed;
the exact high-order integer is not cleanly numerically nailed here.* Both over-claim directions were corrected.

## Scope / firewall
Emergent quasicrystal / perturbation-theory mathematics; no physical-magnitude claim; the mechanism is cited-textbook,
the structure numerically confirmed, the exact high-order integer textbook + saturation-limited, the golden
amplification a heuristic. **Nothing to `../../CLAIMS.md`**; P1–P16 untouched. B175/B176 unchanged (they stand).

## Anchors
`B175` (the width law — order/λ-powers; **stands**), `B176` (the golden privilege — **stands**), `B171`–`B173`
(the gap-labeling heights / the object), `K019` (the collective-spectrum synthesis), `K007`/`K010` (the object).
External: nearly-free-electron / KAM perturbation theory of quasiperiodic gaps; gap-labeling (Johnson–Moser /
Bellissard / Damanik–Fillman); Hurwitz (golden = worst-approximable).

## Reproduction
`python frontier/B178_perturbative_mechanism/perturbative_mechanism.py` — C1 the index method vs window-max
(agreement at weak coupling); C2 the per-frequency structure (power-1 clean, the distinct higher power ~1.7, exact
integer saturation-limited); C3 the golden Diophantine-amplification heuristic; C4 the synthesis. Prints `ALL CHECKS PASS`.
