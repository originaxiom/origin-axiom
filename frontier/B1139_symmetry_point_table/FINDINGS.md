# B1139 — THE SYMMETRY-POINT TABLE: one SM generation, sin²θ_W = 3/8 as a reproduction, anomaly-freedom as the realized E₆ theorem, and the honest naive-B−L negative (cloud memos 23/24, verified two-bench)

**Status: banked (frontier). Verdict PROVED (the structural/quantized facts; the value
typed honestly as a reproduction; the naive-B−L relation typed NEGATIVE). Harvest arc —
the cloud seat's phase-III memos 23 (WEINBERG_POINT) + 24 (SM_TABLE), golden_gate 577712f.
Re-derived on THIS bench with own code (own crystal-of-ω₁ BFS for the 27's weights in
e6_bracket_vendored's own frame; own fresh trinification decomposition; own exhaustive,
well-posed assignment search disambiguated by B1102's independently-banked 6Y multiset),
reusing only the banked e₆ (B1102) and B883's build_27 (actually EXECUTED as a live
cross-check, byte-identical, zero net repo change). All CONFIRMED, no discrepancy. This is
the value-completing campaign's Lane A (VC-A1 sin²θ_W + VC-A2 the table/anomaly), each
honestly TYPED. Cloud seat credited. Gate 5 untouched. Lock
`tests/test_b1139_symmetry_point_table.py`.**

## The typing (the campaign's spine, applied)

| datum | type | what it means |
|---|---|---|
| the 27's charge table = one SM generation | **QUANTIZED** | a discrete SM datum the object owns |
| sin²θ_W = 3/8 | **FORCED-DIRECTION + ASSUMED-NORMALIZATION** = a **reproduction** | direction forced, scale a B991 convention — not a fresh emission |
| the 8 anomaly traces = 0 | the **general E₆ anomaly-freedom theorem, realized** | a consistency check, not a new forcing |
| naive B−L = 2(Y−T₃R) | **NEGATIVE** (unphysical) | the true B−L Cartan is the open cell SP-1 |

## 1. THE EW TABLE = ONE SM GENERATION (memo 24) — CONFIRMED (QUANTIZED)

The closing assignment's 15-row (color, T₃L, T₃R, Y, Q, B−L) table is byte-for-byte identical
to the cloud certificate's. **27 states, Q multiset {0:5, ±1:2, ±⅓:6, ±⅔:3}** — exactly one
SM generation (18 colored + 9 singlets; the Q_L doublet at Y=⅙, the vector-like exotic
D-quark at Y=−⅓, the 9-singlet trinification bidoublet grid with the e^c-role state present).
Cross-checked non-circularly against **B1102's own independently-banked 6Y hypercharge
multiset** (an arc predating and unrelated to memos 23/24) — the assignment is pinned to the
standard embedding, not an arbitrary charge-consistent relabeling.

## 2. sin²θ_W = 3/8 (memo 23) — CONFIRMED, TYPED AS A REPRODUCTION

On the same assignment, **Tr T₃L² = 3, Tr Q² = 8 → sin²θ_W = Tr T₃L²/Tr Q² = 3/8** (exact),
closing-independent (72/72). **The honest type:** the *direction* (the hypercharge line, hence
the ratio Tr T₃L²/Tr Q²) is FORCED by the object's rep content; the *normalization* — the "/6"
that fixes the hypercharge scale so the ratio reads 3/8 — is exactly what **B991 proved is a
convention, not derivable** (the anomaly-homogeneous rescaling q→λq). So 3/8 is a
**REPRODUCTION** (forced-direction + assumed-normalization), the canonical GUT value the
object's structure is *consistent with*, **not a fresh SM number the object emits**. This is
the honest ceiling the value campaign established, applied to its headline candidate.

## 3. THE EIGHT ANOMALY TRACES = 0 (memo 24) — CONFIRMED, HONESTLY SCOPED

All eight — Tr Y, Tr Y³, Tr(T₃L²Y), Tr Q, Tr Q³, Tr(B−L), Tr(B−L)³, Tr(T₃R²Y) — vanish
exactly (rational arithmetic). **Honest scope (a calibration, not a refutation — memo 24 gets
this right, calling it "a consistency check"):** e₆'s basic invariant degrees are **2, 5, 6, 8,
9, 12** (exponents 1,4,5,7,8,11, verified in the vendored module) — **there is no degree-3
invariant**, so Tr(X)³ = 0 on the 27 for *any* Cartan element X, not only the physical one
(spot-checked: a random non-physical Cartan element also gives Tr X = Tr X³ = 0). So the
trace-vanishing is the **general E₆ anomaly-freedom theorem realized on the closing's charges**
— correct and exact, but a realized theorem, not an independent new forcing.

## 4. THE NAIVE B−L IS UNPHYSICAL (memo 24) — CONFIRMED (NEGATIVE, honest)

The naive relation **B−L = 2(Y − T₃R)** fails: colored naive-B−L multiset carries unphysical
members **5/3, −4/3**; singlets carry **±2** (physical would be only ±⅓/∓⅔ and ±1/0). B−L as a
*quantity* is not denied — the naive *relation* is what fails; the physical B−L is a **different
Cartan combination**, the open typed cell **SP-1** (the B−L 4th Cartan, cloud memo 25). Banked
as an honest negative beside the positives.

## Verification

Own script (scratchpad, repo tree untouched — `git status`/`git diff` clean before and after;
B883's build_27 executed unmodified, its own 1+10+16 validation passed, its write reproduced
byte-identical → zero net repo change). The 27's weights built via own crystal BFS in the
vendored frame (no cross-module basis risk); the trinification e₆ = su(3)_c⊕su(3)_L⊕su(3)_R
re-derived fresh (same recipe as banked B1134/B1135, every property re-checked). Cloud
certificates read for spec only, never imported. Cloud seat credited. Gate 5 untouched.
