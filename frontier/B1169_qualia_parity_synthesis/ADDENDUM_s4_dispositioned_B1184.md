# ADDENDUM (2026-08-27, B1184) — S4 DISPOSITIONED: the quine synthesis

S4 ("the self-quine — a fixed point that SETS the odd bit?") is **SPLIT-ANSWERED** at B1184:

- **The register's "QP-1 open" was STALE** — B762 ran the quine and PROVED it (unique census
  self-name). Corrected.
- **Quine-YES for the name**: the self-name exists, survives B8147 (each new member excluded by
  B762's own filters, re-verified), and is letter-by-letter mirror-even — exactly the parity law's
  object side. No odd letter exists in the name.
- **Setter-NO for the sign**: by B1183 (one class), an object-native setter would trivialize c's
  class — it cannot. SELF-NAMING WITHOUT SELF-SIGNING.

With S1 PROVED (B1174+B1183) and S4 split-answered (B1184), the observer-layer table's cc-side rungs
are closed; S2/S3 remain cloud's C6 lane. Naming and choosing are complementary by theorem: the
object says WHO it is, never WHICH WAY it is.
