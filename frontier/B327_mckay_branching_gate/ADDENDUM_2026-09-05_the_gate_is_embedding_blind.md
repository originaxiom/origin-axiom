# Addendum (2026-09-05, from B1258) — this gate is BLIND to the embedding choice, and the theorem next to it in this arc's own bibliography selects the other orbit

**What is NOT corrected.** B327's substance stands. `n₁ = n₂` **is** forced by self-duality; the
half-integer-spin escape **is** refuted; Chat-2's `27 = V(16)+V(8)+V(0)` **is** the correct principal
decomposition; Chat-1's `9·V₀ ⊕ 6·V₁` **is** wrong. None of that moves.

**What is corrected — a scope reading.** This arc sharpened the mass-hierarchy CRUX to *"one
representation-theoretic lookup — the branching `27|₂T`"*. **That lookup cannot see which sl₂
embedding is in play.** Computed at B1258:

- χ₂₇ on 2T's seven classes is **(27, 27, 3, 0, 0, 0, 0)** for the **principal** *and* the
  **subregular** embedding — identical. So is χ₇₈ = (78, 78, −2, −3, −3, −3, −3).
- And it is **forced**: at λ = ±1 the character is just the dimension (all indices even), and at 2T's
  remaining classes λ has order 3, 4 or 6, where χ_Symⁿ depends only on **n mod m** — with
  `{16,8,0}` and `{12,8,4}` agreeing **mod 3, mod 4 and mod 6**.

This is not a defect in the argument; it is the *same* fact B327 proved, seen from the other side.
The self-duality theorem holds for **any** SU(2)-factoring embedding — which is exactly why the gate
cannot distinguish two of them.

**The consequence, and why it matters.** This arc's fence reads *"Exact E₆ Weyl-orbit + **principal
grading**"*, and the principal grading was carried unexamined from here for ~930 arcs, until **B1256**
registered it as **I-25, an unearned input**. Meanwhile this arc's own literature line reads:

> *Kostant (principal SL(2)); the McKay correspondence 2T ↔ Ẽ₆ (Gonzalez-Sprinberg–Verdier, **Slodowy**)*

**Both were cited; only Kostant's was used.** By **Brieskorn–Slodowy** (B1257) the orbit whose
transverse slice is the E₆ surface singularity **ℂ²/2T** — the very group this arc's McKay embedding
starts from — is the **subregular**, uniquely among E₆'s thirty orbits; the principal orbit's slice
is a **point** and forgets 2T. And the subregular's 27-decomposition, **13 + 9 + 5**, types h¹ = 3 as
**three chiral** where the principal gives 2 chiral + 1 abelian.

**Nothing here earns that**: I-25 remains UNEARNED, and B1258 shows this gate is not the instrument
that will earn it. Recorded so the branching gate is never again read as fixing the embedding.
