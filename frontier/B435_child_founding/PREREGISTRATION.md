# THE CHILD PROGRAM — PRE-REGISTRATION (C0, committed before the wave computations)

**The campaign:** study the CHILD (the Meyerhoff manifold = 4₁(5,1), the parent's forced
minimal filling, B434) with the parent's full discipline; test the hypothesis "the SM is what
the object PRODUCES, not what it contains" against the standing four-part bar (forced ·
unsought · exact-SM · control). Plan of record: the approved session plan (Child Program).

## Controls (fixed NOW): the mirror child 4₁(−5,1); the UNFORCED sibling 4₁(7,1); a foreign
child (5₂ filling) for parent-specificity. A "forced" child invariant must differ in these.

## Registered two-outcome readings (named before computing)

- **C1 identity card:** (a) arithmeticity of the child — VERIFY (no memory cite); (b) volume ↔
  ζ′ of the disc−283 quartic (Borel reading): IDENTITY FOUND (PSLQ, rational coefficient with
  small height) or NOT; (c) CS = 0.0770381802…: clean closed form or NOT. No forcing; report
  as found. Sibling card computed alongside.
- **C2 abelian torsions:** the child's ℤ/5-twisted torsions via surgery from the parent's
  Alexander polynomial at ζ₅. REGISTERED QUESTION: does the GOLDEN field return in the
  child's torsion weights (Δ(ζ₅) ∈ ℚ(ζ₅) whose real subfield is ℚ(√5))? Outcomes: golden
  return / rational collapse / new field. E₆ weights via characters at the 25 Kac vacua.
  Control: the sibling's ζ₇ values (ℚ(ζ₇)⁺ is cubic — a DIFFERENT field signature).
- **C3 SL(2) vacua:** the count and fields of A_CL ∩ {M⁵L=1} (exact resultant). Outcomes:
  finite spectrum enumerated; per-vacuum trace fields; disc −283 must reappear at the
  geometric vacuum (consistency gate). Control: slope-7 intersection count/fields.
- **C4 E₆ lift:** composites principal∘C3 + order-5 twists; per-vacuum Sym^{2m} torsions.
  The intrinsically-E₆ residue is NAMED (child-L50), not computed.
- **C5 WRT:** τ_k(child) for small k via the surgery formula on the parent's colored Jones;
  registered question: the child's field content vs the parent's ℚ(√5)-at-5|N law.
- **C6 verdict:** the bar over the assembled spectra. Cleared → STOP, adversarial session
  (owner present). Not cleared → the Child Atlas (named mathematics; Paper-3 seed).

## Founding facts banked with this prereg (computed, exact)
H₁(child) = ℤ/5 (golden prime returns in torsion); abelian E₆ vacua = 75 Kac solutions =
**25 = 5² conjugacy classes** (rotation-free: 3∤5 forces zero fixed solutions; derivation in
founding.py); the S₃/outer refinement 17 recorded.

Firewall absolute; nothing to CLAIMS; physics-shaped ≠ physics.
