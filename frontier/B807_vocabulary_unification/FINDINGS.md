# B807 — the two vocabularies are ORTHOGONAL AXES, and the observer is a third

cc banking seat, 2026-07-29. **Prereg `40b7ff01274b4c01`, sealed and committed at `ad2edefe`
BEFORE any computation.** Repository-instrument scope — no physics, nothing to `CLAIMS.md`.

## The verdict, against thresholds fixed before the run

| quantity | measured | sealed threshold | |
|---|---|---|---|
| populated (face, motif) pairs `P` | **163** of 198 possible (**82 %**) | ≥ 60 | ✔ |
| share carried by the top 5 pairs | **0.168** | ≤ 0.50 | ✔ |
| normalised mutual information | **0.031** | *(corroborator)* | ≈ 0 |

> **VERDICT: SPREAD ⟹ ORTHOGONAL AXES. Do NOT merge — merging would destroy information.**

B806's zero overlap was **not a defect**. A *face* says **WHERE** on the object the work sits; a
*motif* says **WHAT** structure is in play. They are independent by construction, and the near-zero
mutual information (0.031) says the corpus agrees: knowing an arc's face tells you almost nothing
about its motif. **The only thing missing was that the relation had never been declared.**

## Honesty about the strength of this evidence

The prereg pre-stated *"I expect SPREAD"*, and SPREAD is what came back. **A confirmed expectation
is weaker evidence than a surprised one**, and that is recorded rather than glossed. What keeps it
from being a soft result is the **margins**: 163 against a floor of 60, 0.168 against a ceiling of
0.50, and an independent corroborator at 0.031. The thresholds were fixed in the sealed prereg and
are unchanged.

## The observer is a THIRD axis — confirmed, not assumed

The prereg hypothesised the observer is neither a missing face nor a missing motif, and made it
testable: does it cut across, or concentrate?

| | |
|---|---|
| arcs mentioning it | **57** |
| distinct faces touched | **6 of 11** |
| distinct motifs touched | **18 of 18** |

**It touches every single motif.** That is not a label sitting in one corner of the object — it is a
dimension crossing all of them, exactly as *"measurement closes the object"* predicts.

## The structure, now declared

The object's description is **three-dimensional**, and each axis has its own instrument and its own
cardinality:

| axis | question | vocabulary | size | status |
|---|---|---|---|---|
| **WHERE** | which face of the object | `kill_graph` faces | **11** | authored |
| **WHAT** | which mathematical structure | atlas motifs | **18** | authored, frozen 2026-07-01 (B806) |
| **WHICH CLOSING** | which observer choice | the saturated menu | **3** | **PROVED** — bounded (B733), **rank-saturated** (B766) |

The third axis is the only one that is *proved complete rather than authored*. B733 bounded it;
B766 proved it **rank-saturated at exactly 3** — conjugation, reversal, the golden branch — and that
this is *"PRECISELY the full discrete closing set."*

**So a complete accounting of an arc is a triple (face, motif, closing)** — and the owner's question
*"can we account for everything?"* becomes: **is the product of three vocabularies, one of which is
proved complete, populated?** That is finite and checkable, which is what the question needed.

## What follows, and what this arc deliberately did not do

**No instrument was edited**, per prereg §4 — and the verdict means none *should* be. The correct
action for orthogonal axes is **declaration, not merger**, which is free and non-destructive. B806's
numbers are therefore untouched and remain the evidence for the finding that produced this arc.

What B806's finding reduces to, given this: the atlas's frozen lexicon is a defect **on the WHAT
axis only** — the WHERE axis is fine, and the WHICH-CLOSING axis is proved complete. That is a much
smaller repair than "the vocabularies are broken", and it is now correctly scoped.

## Residual

- **Widen the WHAT axis** (B806's residual) — still needed, still its own sealed act, but now known
  to be a one-axis repair rather than a unification.
- **Record the WHICH-CLOSING axis in an instrument.** It is proved complete and appears in *no*
  instrument at all; of the three axes it is the only one both complete and unrepresented.

`joint.py` · `joint.json` · lock `tests/test_b807_vocabulary.py`
