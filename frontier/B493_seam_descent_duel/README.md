# B493 — CL-1a: the seam √−15 vanishing vs CRT/Galois descent (Closure Campaign, Phase 1)

**Pre-registered** in `docs/CLOSURE_CAMPAIGN_2026-07.md` (committed before compute). Question: does a
CRT/Galois-descent argument — through the exact tensor factorization `C = C₃·C₅` (P66), the locality
theorem (P67), and the character-gated orders (P68) — **derive** the one-node-absent subfield-lattice
vanishing of the seam's √−15 channel (B459 / Paper P1)?

**Outcome enum (committed):** DERIVED (proof + the mandatory out-of-sample check: predict the channel
content of a never-computed pair before computing it) · OBSTRUCTED (proof the descent route cannot
produce the vanishing, extending B491's signed-permutation obstruction) · PARTIAL (named residue →
DORMANT hint, not a kill).
**Control (invalid without it):** reproduce the banked bright/dark classification 12/12 before trusting
any new derivation step.
**Firewall:** mathematics only; nothing promotes; physics readings stay in `speculations/`.
