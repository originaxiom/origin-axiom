# B382 (D3(b) endgame) — WHY 1/12: pre-registration (before computation)

**The target: derive the normalization in the rigid-sector constant** (P60:
`Π_H tr(Par·H₁H₂) = −(φ/6)√−3`, i.e. seam components ±1/12; the cell value ±1/48 = (1/4)·(1/12)).
A derivation here is "the first value forced by symmetry alone" — subject, if it lands, to the
full pre-registered null gauntlet BEFORE any claim is voiced.

## The primary route (the trace formula — the cat-map frame's own weapon)

By P57 and J = the image of −I (P56 lineage): `Par·U_γ = ζ₆⁻¹·J⁻¹·XZ·U_γ`, so every raw
C-table entry is `tr(Par·U_γ) = ζ₆⁻¹·μ-phase·tr(U_{γ'}·XZ)` — the trace of a metaplectic
operator times the elementary Heisenberg translation. **Derivation plan:** (1) derive the
shifted Hannay–Berry trace formula for the level-15 theta model:
`tr(U_γ·X^m Z^n) = (multiplier) · e_N(Q_γ(m,n)) / |det(γ−I)|-normalization × Gauss factor`,
with every constant explicit; (2) VERIFY it exactly against the engine on a sample of words
(the hard gate: exact equality, all H-components, ≥6 words including the four slot words);
(3) evaluate the slot constant in closed form and watch which symmetry datum produces the 1/12.
KILL: the closed form disagrees with the engine on any word (then the formula's multiplier
conventions are wrong — fix or abandon); or the evaluation produces the 1/12 only as an
unexplained residue (then the route documents, not derives — bank honestly as a reduction).

## The three normalization cross-checks (each must be CONFRONTED by the derivation)

- Haar(2T×ℤ/2) = 1/48 (relayed reading #1) — does |2T| = 24 appear in the derived form?
- B₂/8 = 1/48 (relayed reading #2, Bernoulli) — does a Bernoulli/Dedekind term appear?
- ord(W₂) = 12 — the mechanical 1/12 from the Q-projector normalization (the "cheap" reading:
  the constant = (1/240)·(alternating C-sum); then "why 1/12" = "why is the alternating raw
  Gauss-sum 20·√−3-type" — the derivation must reduce past this restatement).
The derivation ADJUDICATES which reading is the mechanism (they are mutually exclusive as
explanations even though numerically compatible).

## Outcomes (all bank)

(A) The closed form derives the constant with every factor symmetry-identified ⇒ the escalation
protocol arms (a second forced constant at level 45 from the banked tables; the first forced
ratio; the null gauntlet). (B) The closed form verifies but the 1/12 remains an opaque residue
⇒ banked as the sharpest reduction; the residue is the new named wall. (C) The formula route
fails its gate ⇒ the three cross-check readings stay hints; the hunt re-registers on new footing.
