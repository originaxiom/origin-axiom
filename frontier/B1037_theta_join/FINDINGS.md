# B1037 — FINDINGS: the θ-join is DISTINCT — the prior was wrong, and the record holds it

**Seat:** cc · **Date:** 2026-08-12 · **Prereg sealed 63cd367a BEFORE compute**, pushed at
6be6c574. Compute: `b1037_cells.py` (record log `b1037_output.txt`); B647's banked
machinery loaded via B637's module exactly as B647's own cell does — nothing rebuilt.

## The verdict per the sealed table: **DISTINCT**

- **J2**: the raw chain-level defects are present (1/2 or 2/2 nonzero per banked triple —
  B647's law reconfirmed in passing), and **the sealed class projection (the alternating
  combination over evaluation orders) does NOT kill them** — the orders' defects differ.
  The JOINED criterion fails at its first clause.
- **J3**: the operator dictionary's positive sub-fact stands: **J² = +1 exactly** — the
  antilinear transport U₂₇∘conj is an honest involution (the θ-lift shape is real; the
  join through THIS projection is not).
- **The declared prior (JOINED, weak) was WRONG** — recorded, non-weakening applied.

## Scope, stated precisely

DISTINCT is the verdict **for the sealed projection** (evaluation-order alternation).
B647's own cohomological law lives at a different level (antisymmetrization over the
*arguments* of the 3-form), which was not the sealed criterion and is not adjudicated
here; a re-pose at that level would be a new cell with a new seal. What this cell
establishes: **the value-kernel (B1029) and the chain defect law (B647) are two banked
facts without an exhibited one-operator join** — the L155-style instrument's first
target resolves negative, cheaply, exactly as the instrument intends.

## Verdict: PROVED (the sealed question decided DISTINCT; the prior scored wrong)
