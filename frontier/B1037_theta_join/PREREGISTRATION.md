# B1037 PREREGISTRATION — plan Arc 4: THE θ-JOIN (B1029 ↔ B647; sealed before compute)

**Date sealed:** 2026-08-12 · **Seat:** cc · plan Arc 4. Gate 5-Q. The joined-results
instrument's first target, named by the audit seat's ONE-LAW relay and adopted: **is
B1029's value-kernel the value-level shadow of B647's chain-level defect law?** Relay
channel checked pre-seal (clear).

## The two banked sides (cited, to be phrase-checked in-arc)

- **B647 (chain level):** the swap law Y∘σ* = conj(Y) and THE DEFECT LAW
  S₂(J·z₁; λ,μ) − conj S₁(z₁; λ,μ) = 2·conj(Y) — "Y = ½·conj(the swap's chain anomaly)":
  the chord's cubic IS the obstruction to chain-level swap equivariance.
- **B1029 (value level):** reversal acts on the banked values identically to conjugation;
  the frame group's value representation has kernel exactly θ = c∘r.

## The sealed question, exactly

At the chain level the swap and conjugation DIFFER by the measured defect (2·conj Y). At
the value level they COINCIDE (B1029). **JOINED** means: the value-level projection of
B647's defect equation reproduces B1029's kernel identity — equivalently, **the defect
term is value-invisible** (its image under the value functionals — the trace/character
reads — is exactly zero), so the chain anomaly measures precisely what the value level
forgets: one operator, two levels, the anomaly = the θ-invisible part. **DISTINCT**
means: the defect has a nonzero value-image (the two statements concern different
operators) or the pushed identity fails to reproduce the kernel.

## The cells

- **J1** — both sides assembled from the banked arcs with phrase-checked citations
  (the B1028 pattern); B647's objects (Y, the swap, the S-forms) loaded from its banked
  artifacts, not rebuilt.
- **J2** — the value-projection of the defect, exact: compute the value-functional image
  of the defect term on B647's banked classes. Two-outcome: ZERO / NONZERO.
- **J3** — the verdict per the sealed table: J2 = ZERO and the projected identity =
  B1029's kernel statement ⟹ **JOINED** (a LAW_MAP placement row: one operator, two
  levels); otherwise **DISTINCT** (the join hypothesis dies; recorded plainly).

## Declared prior

**JOINED, weak** — the structural rhyme is suggestive and nothing more; stated as a lean.
Non-weakening applies. Either outcome banks; a DISTINCT verdict would itself sharpen both
arcs' scopes.
