# B1243 — the point-of-use gate: the chain was the vector, and the genesis theorem was uncited

**Date:** 2026-09-03 · **Seat:** cc (banking) · **Lane:** INSTRUMENT + chain admission.
**Gate 5 untouched** — no measured value appears anywhere in this arc.

## 1. The failure class, and that it caught the seat that named it

Seven errors in one exchange had one shape: **a claim USED without checking its status where it
is used.** Four were a downstream seat reading our own surfaces; two were attribution; **one was
mine** — I graded another seat's theorem (T17) as "resting on a REFUTED identification" from a
*summary* of it in a third seat's chain, never reading T17, whose own docstring fences B813
verbatim. The wrong grade was accepted downstream within the hour. **An incorrect correction
propagates faster than the error it replaces, because it arrives with authority.** Logged as E58.

The ledgers already record every retraction and scope correction. Nothing read them at the moment
a document *leaned on* the claim. That is the hole this instrument fills.

## 2. The first design was wrong, and the way it was wrong is the finding

v1 forbade a THEOREM link to cite an arc whose verdict is NEGATIVE or RETRACTED. Against the live
corpus it produced **seven violations, all false**:

- **B731 is RETRACTED *because* it established that m004 IS congruence** — exactly what C9 asserts.
- **B282's NEGATIVE** kills a genericity claim while its surviving arithmetic atom is what C6 cites.

An arc can prove one thing while killing another, and the converse holds. A verdict-based
prohibition would have red the build on seven honest citations and made the fastest path to green
**deleting provenance** — the B1222 shape, aimed at ourselves. So the verdict layer **reports and
never fails**. The failing trigger is an **unacknowledged correction banner**: a judgement about
whether the reader was warned, not about the mathematics. Four arcs in the corpus carry one.

## 3. Direction (a) — the chain was the vector

> **C25** asserted the second measurement lands on su(3)⊕su(2)⊕u(1)³ **EXACTLY**, citing **B892**,
> whose own banner reads: *"the phrase 'take E₆ to the Standard Model algebra' overstates by two
> abelian factors."*

So the 14-vs-12 error two downstream seats made was **ours** — sitting in the document both of them
read. They did not invent it; they inherited it. C25 now carries the scope, with both extra
directions named and their fates separated: **ψ** is anomalous over any chiral content and cannot be
gauged (free); **χ ≅ B−L** is anomaly-**free** over the object's derived 16 (C45/B1096) and is removed
by the second VEV ⟨ν^c⟩ instead (B1017), whose direction is itself forced (B1092). **14 → 13 is
forced and free; 13 → 12 costs exactly that VEV.**

## 4. Direction (b) — coverage, and the genesis theorem

The first direction asks whether a cited claim is stale. The second asks whether a load-bearing
claim is **cited at all**. `docs/UNIQUENESS_THEOREM.md` — **A1–A7 ⟹ A = LR = [[2,1],[1,1]] ⟹
P1–P16**, seven individually-motivated axioms of a minimal record-transfer system forcing the
monodromy (trace 3, eigenvalues φ², φ⁻²), machine-checked 144 → 1, banked **2026-05-28, day nine of
the repo**, lock `tests/test_uniqueness_theorem.py` 9 green — had gone **uncited by
`docs/THEOREM_LEDGER.md` for three months** while appearing on CLAIMS, LAW_MAP and INDEX. B977's
third gate cannot fire on it for exactly that reason: it *is* on synthesis surfaces, just not the
chain. **Nothing checked the chain's own coverage.** `docs/CHAIN_COVERAGE.json` now pins it.

**Kept in force, its own §6 limit:** it does **not** derive A1–A7 from anything weaker — attempts to
derive even L, R from a count-substrate STALLED, and "the substrate, positivity, primitivity, and
order remained inserted." `paths/PATHS.md`'s 25-path survey stands at 4 STALLED, 1 DEAD, **17
UNTOUCHED**, so this is not selection-by-elimination. **And not independent of C1–C6:** A1's ℤ² *is*
the punctured torus's H₁, so the geometric content enters early rather than not at all — agreement
between two axiomatizations, never two independent derivations.

## 5. The two admissions

- **C44 [NO-GO — the fork].** z(T1) = 16, z(T1+colour) = 8, z(T1,T2) = 8, **z(T1,T2+colour) = 0**:
  any two of {spacetime, colour, hypercharge}, never three. — B1138.
- **C45 [NO-GO — the anomaly layer is complete].** Over the derived 16 every channel vanishes
  identically; **anomaly matching cannot further constrain this matter content**. — B1096.

Census: **43 → 45 links, NO-GO 5 → 7, axioms unchanged at {C3, C4, C5, C18}, forced 41 of 45.**

## 6. The register

**I-18** registers a downstream seat artifact's declared dictionary (CS 2-torsion → θ̄) as
**UNEARNED** — *registered, not withdrawn*. It is explicitly **not** B813's refuted I-4: that killed
a *value* identification filling a coefficient slot; this is a ℤ/2 → ℤ/2 map of symmetry types,
fenced as such in the artifact's own text, with its own failability control. Baseline raised by hand
9 → 10.

## 7. What this does not do

It does not judge mathematics, only whether a reader was warned. It cannot catch a claim used with
its scope silently dropped where no banner exists (fc's B864 error) — that needs scope as a field on
the arc, and is left named and unbuilt. And a pin is only worth what its target is worth: the
coverage test therefore also asserts the genesis theorem's lock is green.
