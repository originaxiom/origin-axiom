# B1065 — THE THREE AMENDMENT CONTROLS: **O1 — SAME-CONFIRMED AT STATED PRICE (2.6–3.4 bits, both nulls named); the oldest PROVISIONAL banner discharges**

**cc, 2026-08-13 night. Sealed `b8440d68…` (pushed both remotes before this
compute). Verdict: PROVED (cell outcome O1, the sealed joint-case criterion).
R45-9's three-review escalation is DISCHARGED BY EXECUTION. Log:
`b1065_controls.log` (all checks exact; 64-character enumerations, not
samples).**

## C1 — EQUIVARIANCE/NATURALITY: PASS, with the finding that closes an anomaly

B936's class map (χ ↦ (χ at α₂, χ at α₄), the τ-fixed Bourbaki nodes) is:
τ-equivariant (all 64 characters — the map ignores exactly the coordinates τ
moves); the outer all-flip descends as +(1,1) (affine-consistent on H¹); and
**the node-exchange α₂↔α₄ — a symmetry fixing the τ-fixed pair setwise —
descends to exactly the TARGET's coordinate swap.** Consequence: **the
fork-twin's (1,0) vs (0,1) conjugation classes (this bench vs the
consolidation seat's independent run of the same sealed prereg) are ONE
object under a diagram symmetry — a labeling convention, not a frame
anomaly.** The classes are presentation-real; only their coordinates were
conventional.

## C2 — THE BASE RATE: priced exactly, and it is thin, as the seal warned

P(a random pair generates H¹): 6/9 = 2/3 under the nonzero-pair null
(0.585 bits); 6/16 = 3/8 under the any-pair null (1.415 bits).
**Surjectivity alone would NOT clear promotion** — the control that could
not pass by construction did its work: the claim now carries a number.

## C3 — JOINT RANK ACROSS PRESENTATIONS: PASS, and it supplies the deciding leg

The consolidation seat's independently-computed conjugation class (0,1) maps
under the C1c involution to exactly this bench's (1,0); reversal (1,1) is
swap-fixed. Joint rank over 𝔽₂: **2 within each presentation and 2 across
them, with exact cross-bench agreement** (P = 1/4 under a random-class null:
+2.0 bits). This leg did not exist until the fork-twin audit (the digest's
row 1.01) supplied the cross-presentation datum — **today's audit enabled
today's discharge.**

## The adjudication (the sealed O1 joint-case, applied)

Joint priced evidence 2.6–3.4 bits (surjectivity 0.585–1.415, null-stated,
+ cross-presentation exactness 2.0) · C1/C3 zero failures · the generators
are BANKED objects (CHI_C verbatim; the bare τ-lift with zero unforced
choices), not draws. **O1: SAME-CONFIRMED — d = 2, THE PRICE CARRIED
VISIBLY** (never bare): R11's clause updates from "d = 2 PROVISIONAL
(surjectivity-only)" to **"d = 2 (controls run: equivariant map;
cross-presentation exact; joint 2.6–3.4 bits, nulls named)"**; the LAW_MAP
banner converts to the priced form; d ≤ 3 unconditional and the d = 4
refutation stand as before (they never needed the controls). O2 was fully
respectable and was not needed.

## Fences

Gate 5-Q; zero anchors; the lost-amendment history stays in the record (this
cell is the delivery, three reviews late, and the lateness is the R45-9
escalation's finding, not erased by the discharge); the class data of both
other seats entered as INPUT (their checks welcome post-hoc).
