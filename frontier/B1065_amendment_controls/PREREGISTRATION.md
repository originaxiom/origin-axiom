# B1065 — THE THREE AMENDMENT CONTROLS for B1024's SAME (R45-9's forced disposition, R46-2)

**PREREGISTRATION — DRAFT (seals after the review bank lands; shasum →
SEAL_LEDGER → commit → push both → compute).**

## §0 — Provenance and what is at stake

B1024 answered L153: the frame bits ARE the torsor bits (d = 2), with the
d = 4 refutation UNCONDITIONAL and the generator classes three-ways
independent ((1,0) this bench from banked CHI_C; (0,1) the cloud seat in a
different frame; cc3's confirmation). **What is PROVISIONAL: SAME itself —
it rests on surjectivity alone** (span{(1,0),(1,1)} = H¹), and at k = 2 a
random pair of nonzero classes generates H¹ better than one time in three —
the convenient answer is cheap. A three-seat amendment naming three controls
was drafted in the credit-gap window and never delivered; the cell ran
without it; the loss is the recorded finding (B1024's banner). **This cell
runs the three controls, three reviews late, as Review 46's forced
disposition — the oldest live PROVISIONAL banner either discharges here or
converts to a typed LAW_MAP debt row.**

## §1 — The three controls, each with a criterion that can fail (MB12)

**C1 — EQUIVARIANCE / NATURALITY of the class map.** B936's map sends a sign
character χ ∈ T_ad[2] = (ℤ/2)⁶ to (χ at node 1, χ at node 3) — the two
τ-fixed Bourbaki nodes. The control: the map must commute with the banked
symmetries that act on both sides — (a) the τ-action itself (the map factors
through the τ-fixed data); (b) W_frame's Klein four (B860-era: {id, Cartan
pair-flip, outer all-flip, product}) acting on characters vs on H¹; (c) the
diagram symmetry that exchanges the τ-fixed nodes (if it extends to the
banked frame: does the map's TARGET labeling (node 1, node 3) transform
covariantly or is it an unforced coordinate choice?). PASS = every banked
symmetry's action on characters descends through the map to a well-defined
action on H¹ classes with the generators' classes transforming
consistently. **FAIL = some banked symmetry moves a character but fixes its
class-image inconsistently (or vice versa) — the classes are presentation
artifacts and B1024's reading is STRUCTURE-BROKEN.**

**C2 — THE BASE RATE, priced exactly.** Under the declared null (a pair of
classes drawn from the nonzero elements of H¹ = (ℤ/2)², or from ALL
elements — BOTH nulls computed and stated), the probability that the pair
generates H¹: nonzero-pair null = 6/9 = 2/3; any-pair null = 6/16 = 3/8
(computed exactly in-cell, the arithmetic is four lines). The evidence
SAME adds over the null is then log₂(1/P) bits under each null — ≤ 1.42
bits at best. The control's criterion: **the cell REPORTS the priced
evidence and adjudicates honestly whether "SAME at ~0.6–1.4 bits of
support + three-way generator independence" clears the promotion bar the
banner requires** — with the explicit alternative stated: if it does not
clear, SAME stays qualified (d = 2 provisional-priced) and R11 keeps the
d ≤ 3 unconditional clause as its primary. **This control cannot "pass" by
construction — it converts an unpriced claim into a priced one, and the
price may be judged insufficient.**

**C3 — JOINT RANK across presentations.** The two generators were computed
in one presentation; the cloud seat's (0,1) came from a DIFFERENT frame.
The control: assemble the full available class data — this bench's
(1,0)/(1,1), the cloud's frame's classes mapped through the banked
frame-change, cc3's confirmation data — into one system and compute the
joint rank over 𝔽₂ TWICE: (a) within each presentation; (b) across
presentations after the banked identification. PASS = rank 2 everywhere
with consistent identifications (the generation is presentation-stable).
**FAIL = the cross-presentation rank drops (the generators collapse under
the identification — SAME was an artifact of one presentation) or exceeds
consistency (the identification itself is broken).**

## §2 — Outcomes (exhaustive, sealed)

- **O1 SAME-CONFIRMED**: C1 pass · C2's price adjudicated sufficient
  (criterion: the priced evidence PLUS the three-way independence PLUS
  C1/C3 passing — the joint case, stated before compute) · C3 pass. THEN:
  the PROVISIONAL banner discharges; R11 reads d = 2 (the frame bits are
  the torsor bits, controls run); LAW_MAP's row updates.
- **O2 SAME-PRICED-INSUFFICIENT**: C1 and C3 pass but C2's honest
  adjudication finds the support does not clear promotion. THEN: d = 2
  stays PROVISIONAL-PRICED (now with its number), d ≤ 3 unconditional
  stays primary, the banner converts to a priced note (no longer the
  oldest unpriced banner — the escalation's demand is met by pricing).
- **O3 STRUCTURE-BROKEN**: C1 or C3 fails. THEN: B1024's SAME reading is
  RETRACTED to its unconditional core (the nonzero classes; the d = 4
  refutation — both survive any C1/C3 failure by their own proofs); L153
  partially reopens with the failure named.
- **O4 NOT-COMPLETABLE**: a control cannot be run from banked data (e.g.,
  the cloud frame's identification insufficient for C3's cross-rank). THEN:
  that control's row states exactly what datum is missing; the banner
  converts to a typed debt row naming it (the review's alternative).

## §3 — Prior, declared

Lean **O1 weakly** (the three-way independence and the two banked
consistency checks B1024 already ran suggest the structure is real), with
C2 the live risk (the base rate is genuinely high; ~1.42 bits is thin) —
O2 is a fully respectable landing and this bench will not lean on it.

## §4 — Fences

Gate 5-Q; zero anchors; the class map used only through B936's banked form;
the lost-amendment history stays in the record (this cell is the
delivery, not the erasure); cc3 and the cloud seat may verify post-seal
(their own class data is INPUT here — their checks are of the assembly,
not re-adjudication of their contributions).
