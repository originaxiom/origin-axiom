# B541 — Stage-2a closure: two independent designs, both NULL (cross-seat)

Chat-2's independent stage-2a run (prereg sha256 48f8f74f…, committed before
compute; expectation NULL — a HIT was the extraordinary branch). Design
deliberately DIFFERENT from B539: object set = the 17 dictionary components
exactly (no ratios/transforms — closure excluded to cap look-elsewhere);
20 SM constants; V1 anchored set-matching + V2 unanchored relation structure.

**V1:** real dictionary matches the SM WORSE than chance (hits@1e-2 p = 0.785;
best-dev p = 0.593; closest pair f_A vs CKM η̄ at 5.7e-3 — noise; spot-checked:
0.348/0.3460143 − 1 = 5.7e-3 ✓). Silver/tribonacci controls also null.

**V2 (repaired):** NULL on every statistic (p = 0.19–0.80) after fixing a
BROKEN first null. The incident is the transferable lesson and is preserved:
(a) coefficient-resampled cubics produced no in-window events → fake p = 0.000
from event-count mismatch; (b) "best relation" was domain abuse (feeding
m_e/m_τ ≈ 3e-4 into g(x) = 1+x). **Rules extracted:** relation-test nulls must
be event-count matched; cubic read-outs are only meaningful on their
calibrated domain. (Companion to B539's α/(1/α) list-tautology rule.)

**Convergence:** B539 (ratio-relations, magnitude-matched null) and this run
(set-matching + calibrated-domain relations, resampled null) are independent
designs reaching the same verdict. The 2a question is CLOSED from two sides.

**Cross-seat packet standard (adopted):** per seat publish (1) prereg hash +
timestamp; (2) object-set closure decision; (3) SM target list; (4) null
construction + broken-null incidents; (5) p-table + single best pair. Any
seat's future hit must survive the OTHER seats' nulls.

## Addendum: the third seat's null (chat-1) + the null-calibration flag

Chat-1's preregistered value-matching run: ZERO matches at 1% and 0.1%
(closest: α_s vs c15 at 1.5%; PMNS sin²θ12 vs c5 at 2.2%). Third independent
design, same verdict. **Cross-seat packet flag:** chat-1's null expected ~6
hits at 1% where chat-2's expected 1.58 and B539's 1.9 — the nulls disagree
by ~4×, so chat-1's "below the null / the SM actively avoids the dictionary"
over-read is NOT adopted; the verdict stays plain NULL pending null
reconciliation (this is exactly what the packet standard exists for).
Chat-1's caveat list (ratio-of-ratios, full cubics, wide-range values) was
already covered by B539/B541's designs. Chat-1's gap-edge proposal is closed
by theorem (κ(λ) = 2+λ² ≥ 2 never reaches the forced κ = −2; B532 classifies
edge data CONDITIONED); the relay went out before their session froze
mid-computation on it.
