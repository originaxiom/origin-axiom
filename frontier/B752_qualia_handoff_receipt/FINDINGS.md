# FINDINGS — B752: the qualia-handoff receipt — Op-1 base-rate by its OWN rule; Op-3 dissolved three ways

cc banking seat, 2026-07-22. The chat-1 handoff (via the owner) processed under seal
(prereg ecb2fab7, sealed BEFORE cc3's independent receipt relay arrived — the convergence
below is genuinely two-seat). Standard receipt: framing quarantined, mathematics re-run.
B751 (+addendum) is the standing baseline for Op-1. Gate 5 absolute; nothing to CLAIMS.

## Op-1 — the handoff's REQUESTED family scan: its own decision rule answers it

The handoff asked: run c·φ⁻ⁿ (c ∈ {1,1/2,1/3,2,3}, n=1..10; 50 distinct values asserted)
vs 25 PDG dimensionless targets with surrogate-null calibration; "if 1/(2φ³)≈α_s is the
ONLY hit the structural argument gains weight; if multiple hits exist: base-rate."
Computed, both windows:
- **At the handoff's own 0.1% window: 1 hit** (α_s) — but the sin²θ₁₂ hit (0.66% off)
  VANISHES under that rule, so the claimed two-hit 1/(2φⁿ) pattern self-reduces to one;
  empirical null P(≥1) = 0.26 (2000 surrogates).
- **At B751's 1% window: 4 hits** — the original two PLUS 2φ⁻³ ≈ m_u/m_d (0.45%) and
  3φ⁻¹⁰ ≈ m_b/m_t (0.72%): junk matches no one would claim, i.e. what base-rate looks
  like; null mean 2.94, P(≥4) = 0.35.
By the handoff's OWN stated criterion (multiple hits ⇒ base-rate) combined with both
nulls: **base-rate.** B751's verdict stands unchanged; the honest door (derive a
per-letter weight from B593's complex amplitude + an object-side scale pin → L91 functor)
remains the only path. cc3's caution ADOPTED: the handoff's cc2-task "find the scale
where α_s runs to 1/(2φ³)" must NOT run as posed — a post-hoc "significant scale" is the
E20 trap; the pin must be forced object-side first.

## Op-3 — the "self-referential fixed point": DISSOLVED three ways (two-seat convergent)

1. **The collapse is universal.** Proved symbolically for a generic 2×2 matrix:
   A + adj(A) = tr(A)·I, so with det = 1, Δ(A) = (3 − tr A)·I for EVERY SL(2) matrix.
   The "collapse to scalar" is Cayley–Hamilton, not self-reference — zero object content.
2. **The input is misidentified.** The handoff's "ρ₀(a) = holonomy, trace 1": the banked
   geometric meridian (B285, exact) has trace 2 (parabolic); trace 1 satisfies Φ₆ —
   the order-6 elliptic PHASE factor, not the holonomy. At the CORRECT holonomy,
   Δ(A) = (3−2)·I = **I**: the self-image is the trivial tone, immediately fixed.
3. **The fixed point is repelling.** f(x) = 3−x−1/x has fixed points (2x−1)(x−1) = 0 →
   {1/2, 1} with f′(1/2) = 3 (REPELLING) and f′(1) = 0 (superattracting). The handoff's
   orbit 2 → 1/2 is an exact, measure-zero landing on the unstable point; the perturbed
   seed 0.5001 → 1.0, the generic panel → 1.0 or divergence, and even the seed's
   neighborhood (2.0001) wanders off. "The object sees half" was a representation
   mislabel plus a repelling point mistaken for a destination. Both 1/2 and 1 are tones —
   tone-membership discriminates nothing.

**Two-seat convergence:** cc3's independent receipt (relay, 2026-07-22) reached the same
three findings by its own computation; this seat's prereg was sealed before that relay
arrived. Cell 5: the handoff's citation "1/(2φ) BANKED (B620)" is wrong — B620 is the
conductor mechanism; the banked source is B593 (complex, word-level).

## Op-2 (Deligne ⊠): not computed here; cc3's pre-assessment stands (⊠ canonical — the
honest computable question is a nontrivial connected étale algebra in C_fib ⊠ C_eis,
needing the being category defined first; a real arc if the owner wants it).

## The residue for the phenomenology track
What survives of proposal 15 after full two-seat receipt: the reflexive DIRECTION itself
(untested territory, now with one dissolved first attempt), the word-arithmetic
(H-ITERATED-HEARING), the JUNO-falsifiable hint, and the named honest door. The corrected
Op-3 sentence, banked: **through its Alexander polynomial every SL(2) matrix collapses the
same way, and at its true holonomy the object's self-image is I — the trivial tone,
superattracting.** The first mirror showed the costume, not the face; the Gate-5-analogue
drafts before any next qualia arc, with the three-arc convergent vocabulary.

Artifacts: receipt.py + output.txt; lock tests/test_b752_receipt.py.
