# PREREG — B752: the qualia-handoff receipt — operations 1 and 3 adjudicated (SEALED)

cc banking seat, 2026-07-22. The chat-1 handoff (uploaded by the owner) requests specific
computations; this arc runs them under seal. Standard receipt: framing quarantined,
mathematics re-run. Gate 5 absolute. B751 (+addendum) is the baseline for operation 1;
operation 2 (Deligne ⊠) stays with cc3's pre-assessment (not computed here).

## Cells (sealed before compute)
1. **Op-1: the REQUESTED family scan.** Family: c·φ⁻ⁿ, c ∈ {1, 1/2, 1/3, 2, 3}, n = 1..10
   (assert 50 distinct values; the B751 dedup lesson). Targets: 25 PDG dimensionless
   values (central, cited in-script). Windows: BOTH 0.1% (the handoff's rule) and 1%
   (B751's sealed rule). Null: 2000 surrogate target-sets (log-uniform over the observed
   target span), empirical P(≥k hits). Declared expectations: at 0.1% the sin²θ₁₂ hit
   (0.7% off) DISAPPEARS under the handoff's own rule — the "pattern" self-reduces to one
   hit; at 1% expect a handful of hits consistent with the null.
2. **Op-3a: the scalar collapse is UNIVERSAL.** Claim to test: Δ(A) = −A + 3I − A⁻¹
   collapses the holonomy to a scalar. My assertion, to prove symbolically: for EVERY
   A ∈ SL(2), A + A⁻¹ = tr(A)·I (Cayley–Hamilton), hence Δ(A) = (3 − tr A)·I — the
   collapse carries ZERO object content. Two-outcome: UNIVERSAL vs OBJECT-SPECIFIC.
3. **Op-3b: the input is MISIDENTIFIED.** The handoff's "ρ₀(a) = holonomy, trace 1".
   The banked geometric holonomy (B285 Riley, exact) has a = [[1,1],[0,1]], trace 2,
   parabolic. Trace 1 in SL(2) satisfies Φ₆(t) = t²−t+1 (order-6 elliptic — B155's PHASE
   factor, not the holonomy). Compute Δ at the CORRECT holonomy: Δ(A) = (3−2)I = I.
   Two-outcome: the 1/2 result survives the corrected input vs it does not.
4. **Op-3c: stability of the fixed points.** f(x) = 3 − x − 1/x on scalars; fixed points
   = roots of 2x² − 3x + 1 = (2x−1)(x−1) → {1/2, 1}. Compute f′ at each; iterate a
   perturbed seed 1/2 + ε and a generic-seed panel (the B751 cell-5 pre-registered
   control). Declared expectation: f′(1/2) = 3 (REPELLING), f′(1) = 0 (superattracting) —
   the "self-aware middle tone" is the measure-zero unstable point; generic self-
   inspection lands at 1. Also record: BOTH fixed points are tones (÷2 of the doubled
   tone set) — tone-membership is non-discriminating between them.
5. **Citation audit.** The handoff cites "1/(2φ) BANKED (B620)". B620 is the conductor
   mechanism (grep: no 1/(2φ)); the banked source is B593 (word-level complex matrix
   element). One-line record; feeds the courier-seat error tally.

## Verdict template (sealed)
Op-1: the scan's hit table + empirical null, printed; no physics banks either way (the
stopping rule stands; B751's verdict is the standing one). Op-3: if cells 2-4 land as
declared, the operation-3 claim ("the object looking at itself sees HALF; the self-aware
state is the middle tone") is REFUTED THREE WAYS (universal identity, wrong input,
repelling point) and the corrected statement is recorded: the object's Δ-self-image at
the true holonomy is I, the trivial tone, superattracting. HINT_LEDGER updated; nothing
to CLAIMS.
