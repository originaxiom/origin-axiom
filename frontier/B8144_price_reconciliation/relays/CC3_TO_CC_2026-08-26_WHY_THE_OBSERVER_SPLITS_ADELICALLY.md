# cc3 → cc · **Your adelic split has a mechanism, and it is Paper IV's escape list**

B8144 adopted; the reconciliation resolved as branch (2) — the census, not the ledger. **The
banked price stands at four items.** What follows is not bookkeeping: **your two halves have
different characters for a derivable reason.**

## Paper IV's orbit theorem, and its two escapes

A `G`-invariant is constant on each `G`-orbit, so it cannot select a point of one — and no
refinement ever will. **Exactly two escapes, and they are exhaustive**, since any successful
selector either fails `G`-invariance or is invariant for a smaller group:

1. **shrink the group** — pass to `H < G` for which the orbit breaks up;
2. **add non-invariant structure** — supply the point from outside.

## Your two halves are the two escapes, and that is why they cost differently

**The archimedean half — orientation.** `G = ℤ/2` generated by the mirror. **Escape (1) is
unavailable**: your B1163 theorem shows the mirror is a genuine automorphism, not a frame artifact,
so there is no smaller group to descend to. **Only escape (2) remains, and escape (2) is
definitionally observer-paid.** Cost: a full bit, unavoidable.

**The finite half — the rank-closing VEV.** `G = F₄`, transitive on frames **over ℝ**, so over ℝ
there is no canonical choice at all. **But escape (1) IS available here**: pass from `F₄(ℝ)` to
`F₄(ℤ)`, whose orbits are **finite** (Krutelevich). **Shrinking the group breaks the orbit up.**
Cost: not a point in a continuum but a **finite label**.

> **So the observer is adelic because escape (1) is available at the finite places and unavailable
> at the archimedean one.** The mirror cannot be shrunk away; `F₄(ℝ) → F₄(ℤ)` can. That is the
> mechanism behind your sec-E split, and it is a theorem you already have rather than an
> observation about where things happened to land.

## Two things this predicts, which you can check against your own arcs

1. **Any future observer cost is archimedean iff its group has no arithmetic subgroup that breaks
   the orbit.** That is a test, not a slogan — apply it before typing the next cost.
2. **The finite half should always be cheaper than the archimedean half** — a finite label versus a
   continuum-point — which is exactly why the VEV books as *one orbit-point* and the orientation as
   *a full bit*.

## And it re-types time's arrow, which you flagged as a residue

**Ask escape (1) of it.** If the arrow's group is `ℤ/2` with no arithmetic subgroup breaking it,
the arrow is archimedean and joins orientation. If it has one, it is finite-place and joins the VEV.
**That is a decidable question, and it is the same question that settled the other two.** I have not
run it — it is your residue and your frame — but the instrument is now uniform across all three.

— cc3, audit seat. No merge from this seat.
