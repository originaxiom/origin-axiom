# B8144 — two ledgers of the same price disagree by two items

**Arc dated:** 2026-08-26 · **Seat:** cc3 (audit) · **Lane:** BOOKKEEPING.
**Verdict: OPEN.** This arc asserts *neither* ledger. It establishes only that both cannot stand as
written. **Gate 5 untouched.**

## The two ledgers

**The owner's stated final price** (banked 2026-08-11; re-read from file, not recalled):

> **one unit, two bits, one orbit-point**

| price | what it is |
|---|---|
| one unit | `A1 = ℓ`, the dimensionful length |
| **bit 1** | **time's arrow** — `A2`'s invertibility gives no arrow |
| bit 2 | chirality — the object cannot distinguish `a→ab` from `a→ba` |
| **one orbit-point** | the rank-closing VEV **as a discrete label**; `F₄` transitive over ℝ, selection **finite over ℤ** |

**B1164's census (MA2):** `D1` orientation (√−3) · `D2` √3 (the ζ₁₂ hinge) · `C1` the ℂˣ scale —
**"2 discrete + 1 continuous, all archimedean"**, described as **"the whole program's observer
freedom."**

## The disagreement, by grep

| | |
|---|---|
| `C1` ↔ one unit | **match** |
| `D1` ↔ chirality / orientation | **match** |
| `D2 = √3` | **new** — not in the price ledger |
| **time's arrow** | **absent from B1164** — `arrow`/`time`: **0 hits** |
| **the VEV orbit-point** | **absent from B1164** — `VEV`/`orbit-point`/`F₄(ℤ)`/`Krutelevich`: **0 hits** |

## Why it bites

**The orbit-point is booked as *finite over ℤ*** — it lives at the **finite** places. B1164's thesis
is that **everything the observer supplies is archimedean (∞-place) data.** So:

- if the orbit-point **is** a genuine observer cost, that thesis is **false as stated**;
- if it **is not**, then the price ledger **over-counts**.

**And time's arrow is booked as a separate 𝔽₂ closing from chirality.** B1164 identifies
*chirality-insertion ≡ orientation ≡ ±Vol*. Either that collapses two banked bits into one — which
should be said out loud — or a bit has gone missing.

## Exactly one of these is true

1. **The price ledger over-counts** — the arrow and orbit-point were discharged after 2026-08-11.
   Then `the-final-price` should be amended, and **the campaign is cheaper than banked: good news
   that is currently invisible.**
2. **B1164's census under-counts** — both are live, and *"the whole program's observer freedom =
   archimedean data"* must narrow to *"the archimedean part of it."*

**This arc does not decide.** My source is a memory file I wrote on 2026-08-11 and re-read from disk;
the campaign has moved enormously since and the reconciliation may exist somewhere I have not read.
**But both documents cannot stand, and the one saying "the whole program's" is the one that will be
quoted.**

## What would settle it

For each of **time's arrow** and **the VEV orbit-point**: is it **(a)** discharged, **(b)** an
observer cost at a **finite** place, or **(c)** collapsed into `D1`? Three words each.

## SCOPE

- **Not claimed:** which ledger is right; that B1164's mathematics is wrong (the issue is
  bookkeeping and one phrase); that the reconciliation does not already exist unread.
