# B662 — THE SUCCESSOR CAMPAIGN (owner-directed 2026-07-17: "do them
# all, multi agent loop campaign"). Every open computational cell from
# the B656–B661 integrations, run as a multi-agent waved campaign.
# House rules bind every agent: exact arithmetic wherever possible,
# two-outcome criteria sealed here, outputs preserved verbatim, no
# model names in any deliverable, agents never commit (the main seat
# verifies and banks), Gate 5 absolute (no SM values anywhere).

## WAVE 1 (independent cells, parallel)

- **CELL A — L101, the (i₁,i₂) metallic-uniformity proof.**
  Claim to prove: for EVERY metallic member, i₁ = dim V^holonomy = 1
  and i₂ = dim V^{peripheral ℤ²} = 3 on the 27 (blocks Sym¹⁶⊕Sym⁸⊕Sym⁰
  of the principal SL(2); Zariski-density kills k>0 invariants;
  parabolic peripheral holonomy fixes one line per block). Deliver the
  proof note + EXACT verification on both banked objects (fig-8 via
  B637's machinery; silver via B649's letters27_L). Outcomes:
  PROVEN+VERIFIED / gap stated exactly.
- **CELL B — Q-BLOCK, (i₁,i₂) of the adjoint 78.**
  The banked adjoint blocks are [23,17,15,11,9,3] (no Sym⁰) ⇒ predict
  i₁ = 0, i₂ = 6 ⇒ (h⁰,h¹) = (0,6) by the reduction. Compute exactly
  on the fig-8: the adjoint action of the holonomy on B575's e6 basis
  (78-dim rep), joint fixed spaces, Fox h⁰/h¹. Outcomes: the predicted
  (0,6) CONFIRMS the reduction on a third module / deviation stated
  exactly (either banks).
- **CELL C — L103, the golden σ*-matrix repair.**
  Rerun the B638 swap machinery on the B637 class basis; persist
  sigma_matrix_golden.json (exact, machine-readable); cross-check the
  banked σ*-laws (σ*² = id, C·conj(C) = I analogue) and ONE Y-related
  invariant against the B660/S4 evaluator at the invariant level
  (cross-ratio, not raw entries — different presentations). Outcomes:
  gap REPAIRED / obstruction stated.
- **CELL D — the σ*-equivariance cell (the subfield law's proof path).**
  Prove: the swap law Y∘σ* = conj(Y) + the real structure (J² = +1,
  C·conj(C) = I) forces the chord data into the invariant trace field
  k(Γ). Formalize over both banked objects (fig-8 ℚ(√−3); silver
  ℚ(i) ⊂ L). Outcomes: PROVEN (the law upgrades to THEOREM) / the
  precise missing lemma stated.
- **CELL E — L102, the canonical silver decomposition.**
  Build the boundary-restriction decomposition on the silver double
  (ker/image of H¹(D;27) → H¹(cusp torus)); re-express B657's silver
  portal matrix in that canonical basis. Outcomes: BLOCK-DIAGONAL
  (sector-respecting proposes FORCED, two objects) / GENUINELY
  TRIANGULAR (knot-specific geometry; banks as the honest split).

## WAVE 2 (after wave 1 banks)

- **CELL F — L104, the CP decider.** Field-of-definition attack on
  g·Y = Ȳ over K = ℚ(√−3): exploit the zero pattern beyond monomial
  ansätze; mod-p point evidence at split/inert primes; the
  stabilizer/field-of-moduli route. Outcomes: FORCED (no K-point,
  proven) / CONVENTION (explicit g) / OBSTRUCTED-DEEPER (priced,
  honest).
- **CELL G — L100, the resonant-phase law.** Certify the jumped terms'
  phases across unit classes at resonant rungs from the banked
  certificate structure (B646/B651 packet scripts as templates) ⇒ the
  exact minimal period of the melody. Outcomes: CERTIFIED /
  machinery-gap stated (bounded attempt banks).
- **CELL H — the cochain program, first cell.** Design + compute the
  triple Massey products ⟨u_i, u_j, u_k⟩ on the double's H¹(D;27)
  (bar-cochain machinery of B637/B647) contracted with the cubic:
  does ANY higher operation distinguish the generation triple where
  class-level cannot (three walls)? Outcomes: a nonzero
  distinguishing higher structure (MASS-SHAPE-AT-COCHAIN-LEVEL
  candidate) / zero again (the fourth wall; banks).

## WAVE 3 (close-out)

- **CELL I — the γ₅′ upstream claim, first mathematics.** The exact
  map between the ear's S-matrix (the 2I golden character, B640/B644)
  and the Γ₅′ level-5 forms' S-transformation on the R = −I doublets;
  the weight-5 (H129) mechanism check. Outcomes: an exact
  correspondence statement (PLACEMENT → candidate functor) / the
  precise mismatch.
- Campaign synthesis + ledger close-out.

Two-outcome discipline: every cell banks on either outcome; no cell
reopens the one-shot license or touches CLAIMS. Prereg sealed before
any agent launches; per-cell outputs land in the campaign directory
with hashes; the main seat verifies every decisive number before
banking. Loop mode: the campaign continues autonomously across waves
until all cells bank or block.
