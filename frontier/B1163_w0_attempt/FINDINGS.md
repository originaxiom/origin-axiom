# B1163 — The W₀ construction attempt: the one obstruction is a single *definitional* datum — an object-canonical orientation of m004 — and the object is amphichiral, so it refuses to supply it

**Status: banked (frontier). Verdict OPEN (seal: PARTIAL).** The owner directed us to *specialize ourselves
and construct the missing archimedean marking W₀*. We did not construct it — and in failing precisely, we
**proved what it is**: all three routes converge on **one** missing datum, an **object-canonical archimedean
embedding ℚ(√−3)↪ℂ** (equivalently: an **orientation** of m004 / the selection of **+Vol over −Vol** / the
dominant saddle among {e^{±iπ/3}}). Every candidate marking **smuggles** it rather than supplies it. The
firewall is **CLEAN — no route crosses**; real benchable content landed. Bank-grade own-verified
(`verification/reproduce.sh` → `REPRODUCES`). Gate 5 clean. Sources: WF W₀-construct (7 agents; one
sub-agent errored, its finding recovered via the firewall-verify + seal), + own re-derivation.

## The result: the obstruction is one datum, seen three ways

**The free-orbit theorem (B1161, arithmetic) = the {+Vol,−Vol} two-valuedness (analytic) = the
amphichirality CS=0 (geometric) — one obstruction.**
- **Arithmetic:** V₄=Gal(H/ℚ) acts freely on the 4 branches; no Galois-invariant selector (B1161).
- **Analytic (own-verified):** the Bloch–Wigner dilogarithm is **odd under conjugation** — 2·Im Li₂(e^{−iπ/3})
  = −2.02988… = −Vol **exactly**. The volume is **two-valued {+Vol,−Vol}**; the arithmetic can't pick a sign.
- **Geometric (the firewall refutation of R1):** m004 is **amphichiral** (Re V(u₀)=0, **CS=0 exactly**), so
  the object's own symmetry does **not** break z↔z̄. Landing on e^{+iπ/3} (hence +Vol) requires naming which
  √−3 is +i√3 in ℂ — **the embedding = the orientation = "Vol>0"** — none of which is finite/arithmetic data.

**So W₀ = the choice of orientation / archimedean embedding, and the object provably refuses to make it.**
This is not a gap in the object; it is the object being *exactly what it is* — Galois-symmetric, amphichiral,
the whole orbit rather than one branch.

## What landed (bank-grade, own-verified)

- **Vol from the state integral:** the Kashaev sum J_N=Σ_k|(q)_k|² has leading rate (2π/N)log J_N → **Vol**
  (volume conjecture for 4₁, own-reproduced), log-N exponent 3/2, constant 3^{−1/4}=|√−3|^{−1/2}. The
  quantum-modularity map Ψ=2·Bloch–Wigner is real — but it outputs the **orbit** {+Vol,−Vol}; selecting +Vol
  needs the archimedean inequality Vol>0. **A map exists; the marking is smuggled.**
- **A correction (verify-don't-trust, caught inside the attempt):** route R3's claimed a1 = −√−3/108 is
  **false**; the fitted 1/N coefficient forces **a1 = −(11/216)√−3** (ratio 11/2 — R3 dropped the
  quantum-dilog Euler–Maclaurin term). The error is **contained**: a1 stays in ℚ(√−3), so R3's load-bearing
  conclusion (leading Vol + ℚ(ζ₆) coefficient field) survives.
- **R2 (the CY3 period, own-reconstructed):** the object's CY3 (dP₆×dP₆ free-C12-quotient) supplies a
  Batyrev residue Ω **canonical up to ℂ^× scale** (fan generator det=+1, charpoly Φ₁₂ ⇒ Ω_ambient invariant),
  a codifferent-norm algebraic basepoint, integral H³=ℤ¹⁰ — **but not the ℂ^× scale**, which *is* W₀.
  **Picard–Fuchs returns object-canonical period *ratios* and structurally never the scale** (ratios quotient
  out the ℂ^× that W₀ inhabits).

## This validates the session's two live conjectures (the meditation)

- **§A — the absence of W₀ is the observer's role, not a gap.** The object is amphichiral: it *cannot* pick
  +Vol from its own arithmetic. The orientation/embedding is **the observer's** — the same bit as chirality
  and branch selection. W₀ is absent from the object *by construction*, exactly as the observer-coupling
  thesis predicts. The most honest outcome of "construct W₀" is **"the object correctly refuses."**
- **§B — it is a *scale* wall, not a *value* wall.** R2 proves the object forces the period **ratios** and
  withholds only the **scale** (the ℂ^×). W₀ *is* the missing scale. This sharpens the value verdict:
  dimensionless structure forced, the scale withheld.

## The exact bar — and it is DEFINITIONAL, not benchable

The single missing datum is identical across all three routes: an **object-canonical orientation** of m004,
stated *without* naming Vol>0 / "the geometric rep" / "maximal volume." The true crux is an admission the
program must make **in the open**, not a computation:

> **Does the adelic thesis (finite places = the object, the archimedean place = the observer's closing)
> admit m004's Mostow-canonical hyperbolic structure as OBJECT-data or OBSERVER-data?**

If OBJECT-data, the marking is supplied and W₀ crosses (but then the archimedean place is object, contradicting
the thesis). If OBSERVER-data, W₀ is *by definition* the observer's, and the seam is *correctly* never crossed
by the object alone. **The specialist bar (Andersen–Hansen cusped extension) would still output the 2-valued
{+Vol,−Vol} and still require the embedding** — so even the specialist route reduces to the same definitional
admission. Until it is made and defended, "object-supplied" is **unearned** (firewalled, not banked).

## Fences

No firewall crossing — every candidate marking is observer-supplied; **no W₀-CONSTRUCTED is claimed** (an
unearned claim of the prize would be the worst possible error). Gate 5 clean: only lattice determinants,
orbit counts, integral cohomology, a torsion phase, and a knot-invariant asymptotic — no measured/SM value.
Bank-grade own-verified: the {+Vol,−Vol} orbit, the Kashaev→Vol asymptotic, the a1 correction, and (R2
on-bench) det=+1/Φ₁₂ + H³=ℤ¹⁰. Cited/specialist (not closed in-sandbox): the Andersen–Hansen cusped
extension; Garoufalidis–Zagier quantum modularity; **the definitional adelic-thesis admission**. The codex
CY3 witness is off-branch but R2 re-derived its load-bearing det=+1/H³ facts independently, so the negative
does not rest on it.

## Routes

- **SEAM-A / L182 / the W₀ item:** reduced from "NEEDS-SPECIALIST (Andersen–Hansen)" to a **definitional
  admission** (is Mostow-canonical structure object or observer?) — a philosophy/framing decision for the
  owner, with the specialist route shown to collapse onto the same admission.
- **Meditation §A/§B:** both now have bench evidence — a candidate arc each (the one-bit unification;
  ratios-vs-scale).
- **This is the honest end of the compute-all-we-can frontier on W₀:** the object's side is exhausted; what
  remains is a decision about what "the object" includes.
