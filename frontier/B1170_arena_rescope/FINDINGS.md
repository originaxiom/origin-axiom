# B1170 — THE ARENA RESCOPE: cc3's B8143 + codex's R019 rescope the gravity charter's G1/E2 — the anomaly forcing is arena-generic; the object supplies the arena, the anomalies supply the content

**Status: banked (frontier). Verdict OPEN** — a three-seat reconciliation harvest (integrate-don't-merge;
cc3 + codex credited), closing the highest-risk item from the lose-nothing sweep: *the program's headline
charter row was stated stronger than its own three-seat evidence.* `verification/reproduce.sh` →
`REPRODUCES` (independent own-code re-derivation + two cross-seat certs re-run on-bench). Gate 5 clean.

## What the two seats found (both post-dating my reconciliation addendum)

**cc3's B8143** (4-commit chain, with an honest mid-chain CORRECTION — bf3e426d: *"I omitted an anomaly
condition; the conclusion reverses and B1160 gets stronger"* — the correction *added* [SU(3)]³, which killed
their own 13-state counter-witness): over the SM-visible 5-field alphabet {(3,2),(3̄,2),(3,1),(3̄,1),(1,2),(1,1)},
**252 contents, 222 killed by [SU(3)]³ alone, exactly TWO rigid+chiral+fully-anomaly-free survivors** — the
SM 15-plet with charges (1/6,−2/3,1/3,−1/2,1) up to scale, and its conjugate. **Zero object tokens in the
executable code** (no E₆, no 27, no m004, no trace field — their step-6 token audit; one prose comment
disclosed, not swept). Robustness honestly: adjoints → 7 survivors, (3,3) → 14 — **uniqueness is
alphabet-dependent; minimality is not** (the SM is the smallest survivor in every alphabet tested).
Their verdict: *"THE OBJECT SUPPLIES THE ARENA; THE ANOMALIES SUPPLY THE CONTENT."* Not a refutation of
B1160 — a quantification of B1160's own fence, extended from charges to shape.

**codex's R019** (4652f450): the in-frame hypercharge theorem *fenced* — 36/36 SM in all three color frames,
three-color covariance PASS, and the **universal reduction** (Yl/Yq=−3, Ye/Yq=6, (Yu+Yd)/Yq=−2, cubic
= −18(Yu/Yq−2)(Yu/Yq+4)) holds *"independently of E₆"*; *"the frame, physical 15-plet, gauging and overall
normalization are not selected."* Independently corroborates B8143 from the in-frame side.

## Verified on this bench (three legs)

1. **Independent re-derivation (own code path)** — `verification/independent_enumeration.py`: Fraction
   Gaussian elimination for the linear system + explicit homogeneous-cubic analysis (no `sympy.solve`).
   Confirms **252 / 222 / exactly 2**, both 15-state, charges the SM multiset up to scale. One
   convention note: the two (3̄,1) slots are identical letters, so the (1/3,−2/3) assignment order is a
   slot-labeling choice — compared as multisets.
2. **cc3's lane re-run** (steps 4–6 from their tree): same 2 survivors; the extended-alphabet counts (7/14);
   the token audit returns NONE.
3. **codex's R019 cert re-run**: **byte-identical** to their committed output (needed their vendored
   `r006_e6_invariants/` stack incl. `paper/verify/check_charge_bracket.py` — extracted, run clean).

## The rescope (what changes in the charter reading)

The gravity charter's **G1/E2 row** ("grav²Y is load-bearing in the forced hypercharge") and my B1165
reconciliation carried it as an **object-side** fact. The three-seat picture is sharper — the same
generic-form / specific-which / withheld-completion template as B1165's G-split, one level down:

- **In-derivation (cloud memo 78, STANDS):** *within* the in-frame anomaly system, the grav²Y condition is
  load-bearing — dropping it degrades the forcing (36 → 18 + 27 solution-planes) while the cubic is
  redundant in-frame. Nothing in B8143/R019 touches this.
- **In-scope (cc3 B8143 + codex R019, NEW):** the forcing *package* is **arena-generic** — the conditions,
  the alphabet, and the uniqueness-at-rigidity-threshold are pure SM-anomaly arithmetic with **zero object
  tokens**; they would force the same charges in any theory supplying the same arena. **What is
  object-specific is exactly the ARENA**: that the object's rank-3 abelian sector exists and makes an
  SM-shaped 15-plet available (B1160's realization; the B996 lesson — "reaching is generic, the grammar is
  the specialness" — one level further down).

**Consequence for the charter:** E2/G1 splits into *"load-bearing in-derivation"* (kept, cloud's) and
*"object-specific"* (dropped — the forcing is arena-generic; the object's contribution is the arena).
Relayed to cloud for a **filed addendum** (their charter's own amendment rule). My
`ADDENDUM_gravity_charter_reconciliation.md` (B1165) gains a dated §2 saying the same — the G1 row is
**bounded**, not withdrawn.

## Fences

cc3's mid-chain correction is *their* catch, credited (the FINDINGS' lesson — "I checked that my solutions
satisfied the conditions I had written down, and never checked that I had written down *all* the
conditions" — is the vacuous-check class, MB12-adjacent, worth carrying). Novelty: **none claimed** for the
SM-uniqueness-at-minimal-content result (very likely known in the model-building literature — cc3's own
note); the claim is only the *scoping* conclusion. The independent enumerator's irrational-root branch never
fired on the base alphabet (no survivor rests on numerics). No firewall crossing; Gate 5 clean (anomaly
arithmetic; the hypercharge ratios are derived structure per B950; no measured value).

## Routes

- **Charter G1/E2** → bounded (in-derivation kept / object-specificity dropped); cloud relay sent, filed
  addendum requested; C-namespace collision flagged in the same relay (charter C1–C6 vs genesis C1–C6 vs
  Kashaev C₀–C₄ — their relabel choice).
- **B1160** → *strengthened* (its own fence quantified; cc3's words).
- **The arena as the object's contribution** is now the sharp question: what exactly prices "supplies a
  rank-3 abelian sector with an SM-shaped 15-plet available"? — the residue of the whole anomaly lane,
  folded into the standing arena/content template.
