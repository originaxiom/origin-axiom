# B780 PREREGISTRATION — THE GALOIS-VS-REVERSAL GATE (cc3's Move 3, built + verified)

*Sealed before compute. cc3 (B777 Move 3) DESIGNED the gate that formally separates c
(Galois/conjugation) from θ (reversal) via three banked signatures, and claimed "no new
math needed — computable from B759/B766/B769." This arc BUILDS it and VERIFIES the claim
by direct computation. Phase-3 construction; Gate 5-Q (structural math distinguishing two
involutions — NO consciousness claim; the phenomenological application is separate).*

## The three signatures (cc3, grounded in banked results)
- **S1 rank-onset:** c non-trivial at SL(2); θ trivial at SL(2) (trace symmetry), non-trivial
  only at SL(3)=Sym². [B759 Part 2, B769]
- **S2 action-type:** c acts DIAGONALLY (field conjugation, each coord → its conjugate);
  θ acts by PERMUTATION (word reversal exchanges coords). [B769 SL(3) 8-space]
- **S3 solo-flip:** c has a solo flip-axis (flips chirality T4 alone); θ has NO solo flip-axis
  (appears only in the chord T6 = T4 ⊕ θ). [B766 flip-table]

## The build + the two-outcome test
Construct the gate as a discriminator: given an involution's (S1,S2,S3) signature, classify
c-type vs θ-type. Then TEST:
1. The REAL c (conjugation) classifies c-type AND the REAL θ (reversal) classifies θ-type
   — the gate is correct on ground truth.
2. The SWAPPED assignment (c's filler in θ's slot and vice-versa) FAILS ≥1 signature test
   — the gate genuinely discriminates.

Outcome: both hold, each signature computed in-cell from the banked SL(2)/SL(3)/flip data
=> **VERIFIED** (the gate is real; it halves the Phase-3 enumeration 8→4) => RESOLVED-A.
Any signature not genuinely discriminating (c and θ share it) => the gate is incomplete
=> RESOLVED-B (report which signature fails). No forced result.

## Method: exact/symbolic; each signature computed from the banked objects (not cited);
the swap must genuinely fail. Gate 5 / Gate 5-Q. Nothing to CLAIMS.
