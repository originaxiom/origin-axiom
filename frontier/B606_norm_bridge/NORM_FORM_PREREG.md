# B606 — THE NORM-FORM PREREGISTRATION (the value bridge, second generation)

**Sealed 2026-07-15 before any comparison below is computed; SHA-256 in
`frontier/B598_l85_campaign/ARTIFACT_HASHES.txt`. Successor to B602
(D-norm): only FIELD-COMPATIBLE invariants appear — everything compared
lives in ℚ. Firewall: no SM quantities; a positive outcome only enables a
mechanism cell, not a bridge claim. Provenance: all verification internal.**

## The day's lessons, applied at sealing (MB12 discipline)

- No ℚ(√−3)-vs-ℚ(ζ₅) phase-ratio criteria (field-degree impossible —
  B602/N1's registered diagnosis).
- No unit-modulus magnitude criteria on finite-order objects (the V3
  lesson).
- Pre-checked against banked facts for pre-determined cells: the naive
  norm-ratio cell N(c₈)/N(c₄) = N(h₆)/N(h₃) is PRE-DETERMINED-FAIL
  ((N₈/N₄)² ≠ 1 from banked integers) and is therefore EXCLUDED from the
  verdict set, documented here.
- Every retained cell is exact integer/rational arithmetic (no tolerances).

## Frozen inputs (banked; nothing else may enter)

Classical: N_{K/ℚ}(c₄) = 4N₄², N_{K/ℚ}(c₈) = 4N₈²
(N₄ = 2⁹·3²·5·7·13, N₈ = 2¹⁵·3⁵·5³·7²·11); the integer torsions
τ₄ = 2⁷·3·7·97, τ₈ = 2¹⁴·3³·5·7³·11·13·31·607·49297 (B581).
Stage: h₃ (B593); the three E₆₂ amplitudes (step 6, exact); the B601
trace-law values; the labels from B603 (center charges (1,2); the
currents) and B602-N0 (d = (φ,1)).

## The cells (sealed order; each banked blind)

- **NF-0 (pre-comparison stage constants, gates not comparisons):**
  compute exactly (i) N_{ℚ(ζ₅)/ℚ}(h₃) — expected 1/5 by hand-derivation,
  the cell certifies it; (ii) the E₆₂ amplitude norm-product
  ∏ⱼ|ampⱼ|² — hand-derivation gives (4/7)³·(7/64) = 1/49; certify.
  Both are stage-side constants (the 5-vein and the 7-vein).
- **NF-1 (the torsion-normalized norm scan — structure-search tier):**
  over the declared finite family (k₄, k₈) ∈ {−3..3}², test the EXACT
  integer identity
      N(c₄) · τ₈^{k₈} = N(c₈) · τ₄^{k₄}   (k's applied with sign:
      negative k means the power multiplies the other side).
  Equivalently: is (N₈/N₄)² = τ₈^{k₈}/τ₄^{k₄} for some |k| ≤ 3?
  MB12: each identity is a genuine exact test (shared primes 2,3,5,7,13
  make hits possible; the large primes 97/607/49297 make them hard —
  both outcomes live). A hit is a LAW-candidate requiring a mechanism
  cell BEFORE any bridge language (the discovery-engine gate); all-fail
  closes the torsion-normalized norm route.
- **NF-2 (conditional; runs only on an NF-1 hit):** the 7-adic and
  5-adic consistency of the hit against the stage veins (NF-0's 1/49 and
  1/5): ord₇ and ord₅ of the normalized classical ratio must match the
  stage assignment under the declared bijection. Exact valuations.

## The locked outcome table

- **A-nf** — NF-1 hits and NF-2 passes: a norm-bridge LAW-CANDIDATE
  exists; the next step is a mechanism cell (separately registered), NOT
  a bridge claim.
- **B-nf** — NF-1 hits, NF-2 fails: the hit banks as an isolated exact
  identity (the lawless-exact-numbers index) with no bridge status.
- **D-nf** — NF-1 all-fail: the torsion-normalized norm route closes
  negative; residual factorizations banked; the value question then
  needs input beyond the (N, τ) data (e.g. the word-response norms or
  the E₆₂ off-diagonal data).

No other comparison carries verdict weight. Run order NF-0 → NF-1 →
(NF-2) → the outcome statement; each stage's output committed before the
next runs.
