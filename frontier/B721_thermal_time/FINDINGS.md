# B721 — THE THERMAL-TIME COUPLING: field-matched but a rung-mismatch — the observer's thermal clock ≠ the object's own tracial clock

*cc banking seat, 2026-07-20. Sealed prereg 0c6313f5. Advanced multiagent loop campaign
(web-enabled; compute→3-skeptic adversarial-verify→refine), 12 agents, 0 errors; all 3
probes OUTCOME B; probes 2&3 clean/high-confidence (not refuted), probe 1 skeptic-CORRECTED.
Citations WebFetched + confirmed real (CMR math/0501424, Marcolli–Xu 1602.04890,
Connes–Rovelli gr-qc/9406019). Structural/operator-algebra only; Gate 5 stands; the
modular flow is a structural clock, no cosmology. Credit cc2 (the lead).*

## Verdict (OUTCOME B — honest downgrade, with precise new structure)
cc2's LEAD 1 (is B701's torsor the CMR KMS-torsor over ℚ(√−3)?) is **field-matched to the
being field but structurally a RUNG-MISMATCH**: it clears rung 1 (field) + a genuine rung-2
RELATION, but NOT the identity Outcome A needed. The coupling's thermal clock is a
DIFFERENT KIND than the object's own clock — reconfirming B706 at the operator-algebra
level, and adding two precise new facts.

## Probe 1 — the Marcolli–Xu QSM is the WRONG tool (knot-independent) [skeptic-corrected]
The fig-8 carries TWO disjoint quadratic fields: GOLDEN ℚ(√5) (its cyclic branched-cover
homology is Fibonacci/Lucas — |H₁(Σ_n)|=L_n² odd, 5F_n² even; Alexander t²−3t+1, roots φ^±²)
and BEING ℚ(√−3) (the invariant trace field). BUT the Marcolli–Xu QSM (1602.04890, verified)
does NOT carry knot-specific number-field arithmetic: its Hamiltonian is (crossing# + genus)·
log q and its abelian-extension analog is ρ:π→ℚ/ℤ — a UNIVERSAL target (ℚ^ab), identical for
every knot, seeing NEITHER field discriminatingly. (A skeptic's 4 WebFetch passes caught the
compute agent grafting the branched-cover homology onto the paper — corrected.) So: the
two-disjoint-fields fact is real, but the QSM is not the tool that reads them. INCONCLUSIVE
on the QSM route; the being/golden split lives on the knot, not in the Marcolli–Xu QSM.

## Probe 2 — being-ℤ/2 is the COMPLEMENT of the CMR torsor, not a piece of it (clean)
CMR's zero-T KMS torsor is Gal(K^ab/K) — the infinite profinite-abelian KERNEL, over base
K=ℚ(√−3). Our being-torsor (B700 cell 2) is Gal(K/ℚ)=ℤ/2 — the order-2 QUOTIENT, over base
ℚ. In the exact sequence **1→Gal(K^ab/K)→Gal(K^ab/ℚ)→Gal(K/ℚ)→1** they are the KERNEL and
the QUOTIENT of one group — COMPLEMENTARY, not nested (different base fields K vs ℚ). Our ℤ/2
does not embed in the CMR torsor; it ACTS on it — **complex conjugation (the being-ℤ/2) acts
on the CMR torsor by the CM/anticyclotomic INVERSION.** So: rung-1 field match (ℚ(√−3) on
both sides) + a genuine rung-2 RELATION (being-ℤ/2 = the CM-involution acting by inversion),
but NOT the sub-torsor embedding Outcome A required. Field-matched, structurally complementary.

## Probe 3 — TWO clocks: the object's own time is tracial (type II₁), the thermal clock is type III (clean)
The object's own internal time (B716, the Anosov suspension of σ=[[2,1],[1,1]]) is a REAL,
hyperbolic, **MEASURE-PRESERVING** flow (det σ=1) — so its von Neumann algebra is **tracial
type II₁**, and a tracial state has **TRIVIAL modular flow** (Δ=1; verified σ_t(a)−a ~ 1e-41).
A genuine CMR/Connes–Rovelli thermal clock is **type III** (non-tracial ⟺ nontrivial modular
flow). So the two "times" differ in FIELD (hearing real ℚ(√5) vs being imaginary ℚ(√−3)) AND
in operator-algebra TYPE (II₁ tracial/trivial-modular vs III thermal/nontrivial). **The
object's intrinsic time is an EQUILIBRIUM clock with no thermal arrow; the observer's thermal
time (type III) is genuinely a DIFFERENT clock.** Two clocks.

## The precise new content (what the campaign ADDS beyond "B706 again")
1. **The being-ℤ/2 ACTS on the CMR torsor by CM-inversion** — a genuine rung-2 relation
   (quotient acting on kernel in Gal(K^ab/ℚ)), the honest maximum the lead clears.
2. **The object's own time is tracial type II₁ (trivial modular flow)** — an equilibrium
   clock, NOT thermal. Sharpens B716: the object's intrinsic time carries no temperature; the
   "thermal time = the observer's" picture requires the type-III thermal clock, which the
   object's own Anosov flow is NOT. The observer's time is the type-III thermal upgrade.

## Placement
LAW_MAP: the thermal-time coupling is field-matched (ℚ(√−3)) with a rung-2 CM-inversion
relation, but a rung-mismatch (complementary torsors; tracial-II₁ object-clock vs type-III
thermal clock) — reconfirming B706 at the operator-algebra level. Nothing to CLAIMS. Credit cc2.
