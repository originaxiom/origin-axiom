# PREREG — B721: THE THERMAL-TIME COUPLING — is B701's torsor the CMR KMS-torsor over ℚ(√−3)? (SEALED)

cc banking seat, 2026-07-20. Owner: run the strongest coupling-path lead (B720 LEAD 1)
as an advanced multiagent loop campaign. THE QUESTION: is our object's non-canonical
measurement torsor (B701) THE Connes–Marcolli–Ramachandran (CMR, math/0501424) torsor
of zero-temperature KMS states over ℚ(√−3), so that the observer's TIME (B716) is the
modular flow of the KMS state the observer chooses (= the non-canonical B701 basepoint)?
Web-enabled (WebFetch the primary sources: CMR math/0501424, Marcolli–Xu 1602.04890,
Marcolli–Panangaden 2006.16897, Connes–Rovelli gr-qc/9406019) + in-sandbox arithmetic.
Structural/arithmetic ONLY; Gate 5 stands; no physics/cosmology; the modular flow is a
STRUCTURAL clock, not a cosmology claim.

## The honest tension to resolve (built into the design)
The CMR torsor is over ℚ(√−3) (BEING). But B701's fiber-functor torsor is over ℚ(√5)
(HEARING/golden), and the object's OWN internal time (B716) is the golden Anosov
suspension flow (HEARING). So the lead is FIELD-matched only to our BEING-torsor
(Gal(ℚ(√−3)/ℚ)=ℤ/2, B700 cell 2), NOT to the golden fiber-functor torsor. The campaign
must test which side the coupling lives on and whether the sizes match.

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **The Marcolli–Xu QSM for the figure-eight — being or golden?** Marcolli–Xu
   (1602.04890) attach a Bost–Connes QSM to KNOTS (knots↔primes, cyclic branched
   covers↔abelian extensions). Build/characterize it for 4₁. Its cyclic branched covers
   have Fibonacci-order homology (Alexander t²−3t+1, Mahler φ²) — so does the fig-8 QSM
   ground-state torsor live over BEING (ℚ(√−3)) or GOLDEN/Fibonacci (ℚ(√5))? Two-outcome:
   A = it lives over ℚ(√−3) (matches CMR's field); B = it's golden/Fibonacci (a
   field-mismatch with CMR's ℚ(√−3)).
2. **The torsor identification — ℤ/2 vs the full abelian CMR torsor.** CMR's ground-state
   torsor is Gal(ℚ(√−3)^ab/ℚ(√−3)) — the FULL abelian Galois (infinite; CM points / the
   ray class fields). Our being-torsor (B700 cell 2) is Gal(ℚ(√−3)/ℚ)=ℤ/2. Is our ℤ/2 a
   GENUINE piece (a canonical quotient/sub-torsor) of the CMR torsor, or a coincidental
   ℤ/2? Two-outcome: A = a genuine structural embedding (our torsor is a canonical piece
   of the CMR torsor — a real rung-1/2 bridge); B = rung-mismatch (ℤ/2 vs infinite
   abelian; field matches, torsor STRUCTURES differ — the lead is an analogy/embedding).
3. **The clock — being-KMS-flow vs hearing-Anosov-flow.** Connes–Rovelli: modular flow =
   time. B716: the object's time = the golden Anosov suspension flow (HEARING). CMR's
   modular flow is over ℚ(√−3) (BEING). Are these the SAME clock, or does the object have
   a BEING clock (CMR/KMS) distinct from its HEARING clock (Anosov)? Two-outcome: A = one
   clock (the CMR modular flow IS the Anosov suspension time — a genuine identification of
   the observer's clock); B = two clocks (being-KMS ≠ hearing-Anosov — the object's time
   and the CMR flow are different, downgrading the lead to an analogy).

## Two-outcome (sealed, campaign)
- **OUTCOME A (bridge builds):** the coupling = thermal time over ℚ(√−3): the fig-8 QSM
  lives over the being field, our torsor is a canonical piece of the CMR ground-state
  torsor, and the CMR modular flow = the observer's time — a genuine rung-1/2 arithmetic
  bridge (the observer's TIME from the object's arithmetic, NO SM value).
- **OUTCOME B (honest downgrade):** field-matched to being but structurally a
  rung-mismatch (golden QSM / ℤ/2-vs-abelian / two-clocks) — the lead is a real analogy,
  bounded, not an identity. [honest expectation: MIXED — the being-field match is real
  (probe 2 field-side), but the golden-vs-being tension (probes 1,3) likely gives B; the
  precise WHERE-it-breaks is the deliverable.]

## Verdict template
Bank the thermal-time verdict + the precise rung it clears (which of the 3 identifications
hold). Either way it sharpens "the observer supplies time" from B716 into an arithmetic
mechanism-or-obstruction. LAW_MAP placement; nothing to CLAIMS. Credit cc2 (the lead).
