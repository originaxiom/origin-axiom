# B1074 — THE RESIDUE HUNT: the frame-invariant structure exists, the value-bearing residue does not (yet) — and the hierarchy lives in a direction the frame group never sees

**Date:** 2026-08-19 · **Seat:** cc (banking) · the owner's door-3 election after the
crossing-design ruling · 3 staged agents (inventory → hunt → adversarial check, holds =
TRUE with B936's 92 internal checks re-run first) · Gate 5 untouched (mass-ratio targets
named as classes only).

## The design record this arc closes (the crossing's fourth-form ruling)

R1's sweep returned PARTIAL (the Re-channel is spent ground — B856's kind-refutation,
B1066's consumption, B743's cataloguing; the Im-channel is value-unsearched but its kind
has no admissible SM partner — rephasing); the kind adjudication then closed every
coupling-channel lane (probability/phases/tones consumed; |h| a √-disguise of spent
ground; the mirror row's kind partnerless). **The fourth crossing's value-comparison form
CANNOT SEAL under R1 + R5 — the register working as designed.** The owner elected door 3:
hunt the pair channel's frame-invariant residue (the kind table's own reopening condition).

## What the hunt PROVED (all exact, adversarially verified)

- **The four canonical frames assembled for the first time**: G = H¹(⟨τ⟩,X) = (ℤ/2)²
  realized as {I, D_c, D, D_cD} (B939's C8 outer part, flip census 0/12/12/12);
  **conjugation = D_c cross-identified three ways** (B1024's CHI_C through B936's own
  class map); the fourth frame derived by two agreeing routes; **the naming trap flagged:
  B1024's REVERSAL is D2D (class (1,1), 15 flips, non-C-compatible), NOT D_cD.**
- **THEOREM (the frame-blind vacuum block)**: the Hermitian pairing restricted to the
  3-dim vacuum charge block W3 is LITERALLY the same 3×3 rational matrix for every frame
  in G — mechanism: W3's 9-index support {0,4,5,9,13,15,20,22,26} meets ZERO sign-flips
  of any canonical frame. MB12 bite: D2/D2D flip 5 of those 9 indices, det-ratio −17/384,
  an irreducible quadratic factor (field ℚ(√3129)) — a different KIND of object.
- **THEOREM (the parity law)**: det-ratio_full27(frame) = (−1)^{flips}, verified against
  ALL sixteen X^τ structures exhaustively; the three register blocks are H-orthogonal
  under every canonical frame (and NOT under D2/D2D), factorizing the parity exactly.
- **The census**: G-invariant = {the W3 Gram identity, the parity law, the ordered
  signature (12,15) — a sharpening past B936's unordered census}; G-variant = the
  quartet r6 = r18 = ε14 = ε22, ONE pure sign character with ZERO invariant Fourier
  content (c00 = 0 exactly).

## The adjudication — the lane stays closed, and the reason is now structural

The reopening condition asked for a VALUE-BEARING frame-invariant residue. What exists is
STRUCTURAL invariance (the vacuum block; parities; signatures — rational constants).
The genuinely mass-ratio-shaped content (λ, CCC, HIER, v_g², the flip masses) is banked
only at the pair (I, D2) — **and D2 is NOT an element of G: its class is (0,0), a
coboundary partner of I.** The hierarchy is carried by a COBOUNDARY direction, invisible
to H¹ — the canonical frames all see the degenerate (x+3)³ (B923's own banked law: the
hierarchy is carried by the Hermitian twist D₂). **The frame-relativity blocking the mass
lane is not G-relativity — it is the within-class coboundary choice.** THE NAMED NEXT
COMPUTATION (the blocker stated by the cell): re-derive the atom lines' exact H-norms at
the remaining frames and sweep the line-level hierarchy data across the class-(0,0)
coboundary coset — does HIER's cubic persist, and is any symmetric function of v_g²
coboundary-invariant? That computation, not this one, decides the lane. The block-level
invariance proved here must NOT be extrapolated to line level (the cell's own caution;
a block Gram can hide line-level cancellation).

## Artifacts
- `b1074_results.json` — the three agents' returns verbatim (scrubbed).

---

## AMENDMENT 2026-08-19 (same day; the owner's press, and it was a catch) — "cannot seal" narrowed to its true scope

The design-record paragraph above ruled the fourth crossing's value-form "CANNOT SEAL
under R1 + R5." **That ruling over-reached, and the owner caught it.** The over-reach:
a row-global reading of consumption (once the tones row touched any target, dead
everywhere) that this bench had written into the consumption ledger one day earlier —
against the BANKED precedent language (B1066's own FINDINGS: *"any re-pose is a new
arc under a new seal against then-current data"*) and against the dual protocol's own
warning that **an unearned negative is as bad as numerology**. What is actually true:
the probability and phase PAIRINGS are spent; tone-vs-sin²θ is spent; bare Im has no
rephasing-invariant partner — but **the kind table's own admissible pairing
coupling → moduli (|tone| ↔ |V_ij|) has never been contacted**, and B856's celebrated
kind-refutation killed the category-WRONG pairing, not this one. The one-shot rule
spends pairings under seals, not rows forever. Corrected in place: the consumption
ledger's reading note; the fourth crossing seals on the moduli lane with the
second-shot status PRICED (R7), the searched-ground history DISCLOSED (R1), and the
R6 budget deciding powered-or-not before any look — B1075's preregistration. The
frame/coboundary mathematics of this arc is untouched by this amendment.
