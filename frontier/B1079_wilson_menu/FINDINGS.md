# B1079 — THE WILSON MENU VERIFIED AND THE SELECTOR'S HONEST SILENCE: the row-choice is free by computation, and the object's prior on it is the counting measure

**Date:** 2026-08-19 · the new seat's menu (owner-relayed) under verify-don't-trust; 3
agents, the adversarial verifier HOLDS both halves at their typed grades on full
independent re-derivation · Gate 5 untouched.

## The menu — VERIFIED 9-for-9, with its mechanism

All 5⁶−1 = 15,624 order-5 coweight points enumerated from scratch (twice, differently
structured): the nine rows bit-for-bit — counts 4320/4320/2160/1728/1440/864/540/144/108
(sum exact) — dims all cross-checked. The A₂⊕A₁ (dim-14) point ABSENT at orders 5, 2,
AND 3. **THE MECHANISM (Kac)**: E₆'s marks (1,2,2,3,2,1) are all ≤ 3, so no order-5
element reaches a center-0 type — every order-5 row must carry u(1)'s, while orders 2/3
each hold exactly one center-0 row (su(6)+su(2) at 36/63; su(3)³ at 80/728 — the MB12
witnesses proving the ℤ/5 filter is real). The apparent B932 clash DISSOLVED: the
conformal chain's disqualified object is the center-0, index-(2,3,1) S-subalgebra —
row 1 is the regular center-2 Levi (B932's own text concedes its existence; the 5
realizations independently recounted).

## The selector — B932's grammar run literally against all nine rows

**It kills NONE.** LEG1 (the singlet count) has a subject on only 2 of 9 rows and
approves both — behind it a NEW UNIVERSALITY THEOREM (this cell's, PROPOSED-NOT-BANKED
as a register row until now): **every index-1 (root-generated) su(3) in E₆ gives
exactly 9 singlets in the 27 — all 120 A₂ subsystems, zero exceptions**; the unique
deviant is the conformal chain's index-2 su(3) (0 singlets) — B932's original kill,
the criterion's bite already spent there. LEG2 (center ≥ 1, regularity) clears all
nine trivially — they are literal centralizers. **VERDICT: the banked grammar cannot
choose the row. The Wilson menu is a TRUE torsor family; the row-choice is FREE by
computation — the free inventory's instance #2 CONFIRMED, and by §3 of THE FORCED AND
THE FREE the object's prior on the choice is the counting measure: the multiplicities
are the object's own weights.**

## Registry riders

- **B932's T1 bullet, scope-flagged**: read as universal ("every charge wall has center
  ≥ 1") it is computably FALSE (the order-2/3 center-0 counterexamples); B932's verdict
  stands on its actual sweep — the sentence needs its scope word (addendum owed).
- The 120-A₂ universality theorem → LAW_MAP row (with this arc).
- The selector interlock's first resolution recorded: honest silence on the true torsor
  (the thought's prediction), discrimination only where homogeneity was apparent (the
  gauges — B1078, control sweep in flight).

## Artifacts
- `b1079_results.json` (all three agents verbatim, scrubbed).