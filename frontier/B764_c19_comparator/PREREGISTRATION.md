# PREREG — B764: the C19 discriminant law's first out-of-field test (SEALED)

cc banking seat, 2026-07-22. C19 (banked from B759): the single-trace θ-off-block norm at
the geometric representation = √|disc K|, K the invariant trace field — claimed as a
TRACE-FIELD invariant. B759's comparator (m003) shares m004's field, so the law has never
met a different field. Gate 5-Q Q2b in its strongest form: the law must predict, sight
unseen, on objects with different fields. Gate 5; nothing to CLAIMS.

## DECLARED PREDICTIONS (sealed before any computation)
- **5₂** (2-bridge, Riley setup identical in form; trace field = the cubic x³−x²+1-class,
  disc −23): the law predicts off-block = **√23 ≈ 4.7958** at the geometric root.
- **CONTROL (same-field)**: m004 re-run must give √3 (the banked value).
- Convention note, declared: B759's "single-trace off-block" = |Im d(tr)/du| at the
  geometric root for the distinguished trace coordinate (SL(2): tr(AB); the SL(3)/Sym²
  norm aggregates per B759's PART-3 recipe). The 5₂ test uses the SAME recipe with 5₂'s
  Riley representation (a = [[1,1],[0,1]], b = [[1,0],[−u,1]], word conventions per
  B211/B225's validated 2-bridge pipeline) and ITS geometric root (the complex root of
  the Riley polynomial with positive imaginary part corresponding to the discrete
  faithful rep — identified by the maximal-volume criterion if the root choice is
  ambiguous; ambiguity handling declared: test ALL complex roots, report each).
## Two-outcome
- **LAW-HOLDS**: 5₂'s off-block = √23 (within exact simplification or 1e-9) — C19
  survives its first out-of-field test and upgrades from single-family to two-family.
- **LAW-BREAKS**: any other value — C19 gets a scope-correction header (m004-family only)
  and the deviation is recorded exactly. Either way informs; E20 untriggered either way
  (one prediction, one target, declared first).
