# B616 — the held-out control: STILL-AMBIGUOUS; the second object behaves as noise; the sign law is universal

**Status: banked (frontier). Run once under the sealed design (sha
a11491e6…, verified in-run). Lock `tests/test_b616_heldout.py`. Full raw
log `heldout_output.txt`.**

## The verdict (per the locked table): STILL-AMBIGUOUS

- **HG2 (the decisive grid):** m136's 63-value amplitude family produced
  2 coarse-tier matches of 378 pairs — expected 1.44 under the null
  (Poisson P(X ≥ 2) ≈ 0.42: completely unremarkable) — and both matches
  are the SAME duplicated value (|d|² = 0.02184 appearing at two pairs)
  hitting sin²θ₁₃; under the SET clause that is ONE match. Honest note:
  the run filtered to (0,1] but did not collapse duplicates before
  counting; with the collapse the rate drops further and the verdict is
  unchanged (the rate lands strictly between the table's "dies" and
  "specificity" thresholds either way: r_m136 ≈ 0.02–0.03 vs
  r_fig/2 = 0.06 and k ≠ 0).
- **Reading:** the second object matches at exactly its noise
  expectation, while the fig-8's threefold coarse-tier excess
  (3 vs 0.57, raw p = 0.02) remains uncorroborated and unexplained.
  Per the sealed middle band: banked; the SM-values question stays OPEN
  pending the heavy ports (the m136 exterior torsions and a third
  object); NO escalation.
- **HG4:** one m136 torsion ratio sits at log-dev 0.53 — just OUTSIDE
  the half-decade window (data row; no weight).

## The structural surprise (exploratory; registered)

**The m136 bundle torsions obey the SAME sign law as the fig-8's:**
sign pattern [−,+,−,−,+,−] = (−1)^m — positive exactly at the θ-odd
exponents {4, 8}. The sign law (B581's headline for the fig-8) appears
OBJECT-UNIVERSAL for punctured-torus bundles at the bundle-determinant
level. Mechanism candidate: det′(I − Sym^{2m}(A)) sign depends only on
the count of eigenvalue-pair factors, not on λ — likely provable in one
line for any hyperbolic A ∈ SL(2,ℤ) (registered as a lemma cell; if
true, the sign law is a THEOREM of the family and its appearance in the
fig-8's exterior torsions is the object-specific fact).

## Where Branch 3 stands after two sealed rounds

Fig-8: corrected p = 0.078 (suggestive). m136 control: noise-compatible.
The angle-family suggestion is neither killed nor corroborated; the
designed next probes are (i) the m136 EXTERIOR adjoint torsions (the
heavy field port — the true held-out analog of the fig-8's τ-family),
(ii) a third object, (iii) tighter targets (the PMNS uncertainties
currently dominate the windows). All under fresh seals. The mathematics
(B575–B613, PC26 v2) is untouched by any of this.
