# B616 — THE HELD-OUT CONTROL DESIGN (Branch 3's decisive follow-up)

**Sealed 2026-07-15, BEFORE any held-out value is computed; SHA-256 into
the hash ledger at banking. This is the arc the B615 AMBIGUOUS clause
mandates: a fresh sealed design on genuinely held-out quantities. The
hash is the gatekeeper (the director's standing order); seat 4 reviews
the raw output alongside this design.**

## The logic (the control-contrast protocol)

The fig-8's B615 result: three coarse-tier angle-family matches (raw grid
p = 0.020; corrected 0.078). Two readings compete: (a) the angle windows
are easy for this class of algebraic numbers (the suggestion is generic
and dies); (b) the fig-8 is special (the suggestion gains specificity).
The second object decides by CONTRAST: run the IDENTICAL grid protocol on
m136's equally-natural value families, computed fresh. m136 = the R²L
punctured-torus bundle (monodromy [[3,2],[1,1]], eigenvalue field ℚ(√3),
volume 3.6639, one cusp; the trace field is Sage-gated and recorded as
to-be-determined — not needed below).

## The held-out families (never computed anywhere)

- **HA (the m136 amplitude family):** the diagonal values of the odd
  hearing form B_odd = −(odd compression of ρ(R²L)) in the pair-direction
  basis, at the object's FIELD STAGE — the mechanical rule "the lowest κ
  where the object's eigenvalue-field content appears in the odd trace"
  (fig-8 → κ = 5, giving its 2 diagonals = h₃, h̄₃; m136 → κ = 12 per the
  banked B610 scan, giving its 25 diagonals). The comparable quantities,
  mirroring B615's A-family: |d_i|² for the 25 diagonals, plus Re d_i and
  |arg(d_i)|/π for each (75 values in [0,1] after filtering to the unit
  interval, mechanical).
- **HR (the m136 bundle-torsion family):** the six reduced determinants
  det′(I − Sym^{2m}(A_{m136})) = ∏_{k≠m}(1 − λ^{2(m−k)}), λ = 2+√3,
  m ∈ {1,4,5,7,8,11} (exact integers up to sign, sympy), their sign
  pattern, and the 15 pairwise absolute ratios.
- **EXCLUDED, with reason:** the mixing-fraction family is
  OBJECT-UNIVERSAL (the E₆ principal decomposition does not depend on the
  knot), so it cannot be held-out-tested; the fig-8's F-matches
  (e.g. 4/13) are therefore untestable by a second object and carry no
  specificity weight either way. The spectra and traces stay excluded per
  the three-layer principle (B611/B613).

## The grids (IDENTICAL protocol to B614/B615 — no new choices)

- **HG2:** HA × the same six angle targets (T-M), tiers δ ∈ {1e-2, 1e-3},
  uniform-[0,1] null, per-pair p = 2δt.
- **HG4:** HR-ratios × the same hierarchy target (T-H), the same
  half-decade log window, log-range 25.
- The same PDG central values as frozen in B615 (verbatim reuse — no
  refreezing drift).

## The contrast statistic and the locked verdict table

Let k_fig = 3 (the fig-8's G2 coarse-tier count, 25 values) and k_m136 =
the HG2 coarse-tier count (75 values; expected-under-null ≈ 3 × the
fig-8's 0.57 ≈ 1.7 by the value-count ratio). The per-value match RATES
r_fig = 3/25 = 0.12 and r_m136 = k_m136/75 are compared:

- **SUGGESTION-DIES** — r_m136 ≥ r_fig/2 (the second object matches at a
  comparable per-value rate): the angle windows are generic for this
  class of numbers; the fig-8's excess carries no specificity; the
  campaign's stopping rule CLOSES the SM-values question at this level.
- **SPECIFICITY-EVIDENCE** — k_m136 = 0 AND the binomial probability of
  the fig-8's k=3-of-25 at m136's observed per-value rate (with Laplace
  smoothing (k_m136+1)/(75+2)) is < 0.01: banked as evidence of
  object-specificity; the NEXT step is then the m136 exterior-torsion
  port and a third object — still NO physics claim (the walls stand).
- **STILL-AMBIGUOUS** — anything between: banked; the SM-values question
  remains open pending the heavy ports; no escalation.
HG4 is reported as data (the hierarchy row was already null-compatible
for the fig-8; no verdict weight).

## MB12 (computed at sealing)

- Non-trivial: none of HA/HR has ever been computed (B610 printed m136
  traces and eigenvalues at scan levels, never the κ=12 pair-basis
  diagonals; the bundle torsions of m136 have never been evaluated).
- Can go either way: the 25 diagonals are unknown complex numbers; their
  |·|², Re, arg triples can land in or out of the six windows freely;
  k_m136 ∈ {0, …} is genuinely open. The verdict table covers the full
  outcome space with no gap (dies / specificity / ambiguous partition).
- The known hazard: HA is larger than the fig-8's A-family (75 vs 25
  comparables) — handled by comparing per-value RATES, not counts, and
  by the identical windows.

## Protocol

One mechanical run (B616's script): verify this design's hash; compute
HA and HR exactly/high-precision; evaluate the grids; print every pair;
compute the contrast; state the verdict from the table. Bank raw.
