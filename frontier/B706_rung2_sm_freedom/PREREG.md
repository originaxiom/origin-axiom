# PREREG — B706: the rung-1/rung-2 structural comparator on the SM flavor sector
# (SEALED before the verdict; owner-authorized Gate-5-SM design; Listening Protocol)

## The question (structure, NOT value)
Does the Standard Model's irreducible, un-derivable freedom (the flavor sector)
match the object's structure at:
- **RUNG 1 (field/reality):** is a dimensionless, convention-reduced SM flavor
  RATIO algebraic of low degree (≤4) over an object field — ℚ(√5) (hearing,
  audible) or ℚ(√−3) (being) — to measurement precision, AND does the relation
  SURVIVE more digits than found it (the falsifier)?
- **RUNG 2 (torsor/seam):** does the SM's un-derivable freedom ORGANIZE as the
  B704 seam — a discrete 𝔽₂-multiquadratic vector space (stages=basis,
  meetings=𝔽₂-sums, NO canonical origin) — in KIND (not value)?

## Two-outcome (sealed)
- **MATCH**: (rung 1) some SM ratio is provably low-degree-algebraic over a
  predicted object field, surviving precision beyond its discovery digits; OR
  (rung 2) the SM flavor freedom has the discrete 𝔽₂-multiquadratic / torsor
  structure with no canonical origin. → the first real structural bridge.
- **NO-MATCH**: (rung 1) the ratios are generic to precision (no surviving
  low-height relation over the object fields); AND (rung 2) the SM freedom is a
  CONTINUOUS moduli space (≠ the discrete 𝔽₂-seam), and the object provably
  lacks the SM's discrete structure (B307 no-3-generations; B604 no-gauge-
  alignment). → the flavor is fully in the coupling; the object gives the
  ambiguity STRUCTURE (a discrete bit per stage), not the SM's continuous freedom.

## Expected outcome (stated up front, NOT a target)
NO-MATCH — the honest prediction of the observer-coupling thesis (flavor in
the coupling). A rung-1 "hit" would be the surprise; it must clear the
precision-survival falsifier + base-rate to count.

## Mandatory gates (Listening Protocol §4)
Name-the-rung (done: 1,2); Galois-invariance (only dimensionless
convention-reduced ratios; no bookkeeping params); base-rate cell (how many
object-field algebraic numbers are near each ratio); precision-survival
(rung-1 relation must hold beyond discovery digits); swap-vs-weld/field-type.

## Firewall (Gate 5-SM)
STRUCTURAL comparison only; NO SM value is claimed or predicted. A clean
NO-MATCH is a real result (the honest ceiling). Nothing to CLAIMS.
