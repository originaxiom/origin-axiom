# ADDENDUM 2026-09-02 (B1236) — the A₁ landing has no residual where this arc's A₂ still does

§3 leaves the A₂ landing's hypercharge test at *collapse form, float grade, generic*, with the exact value-match
as a named residual. B1236 lands the **A₁** stratum (B1098's other SM-compatible class) at **exact multiplet
grade** in exact arithmetic — the SM-shaped 27 reproduced irrep by irrep, hypercharge unique within support. Its
control cell is the precise reason the distinction matters: a triple that reproduces the *charge histogram*
(this arc's collapse pattern 6,6,4,3,3,2,2,1) can fail at multiplet level. The A₂ residual is unchanged by this;
the comparison is recorded so nobody reads the A₂'s pattern-level compatibility as equivalent to the A₁'s
multiplet-level one. The A₂ remains the selected landing (rank exactly 4, zero extra u(1)); the A₁ costs one.
