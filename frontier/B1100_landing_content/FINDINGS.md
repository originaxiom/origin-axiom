# B1100 — THE LANDING'S MATTER CONTENT: the branching exact, THE 27 COMPLEX (witnessed), and the hypercharge cone generic

**Date:** 2026-08-20 · **Verdict: PROVED (two exact theorems + one float-established compatibility with its exact completion named as the residual)**
**Trigger and discipline:** R47-5 (the hatch's follow-on) + the audit seat's B959 flag,
which promoted the reality check to a first-class output BEFORE this ran.

## 1. The branching, exact (the trinification structure confirmed)

At the A2-class landing (B1098's stored triple, the verified e₆/27 machinery), the
joint weight table of (sl₂, the centralizer's rank-4 Cartan) on the 27 was computed
EXACTLY — stacked-kernel multiplicities per candidate tuple, forced to tile:
**sum = 27 exactly; sl₂ multiset {+2: 6, 0: 15, −2: 6}** = six spin-1 triplets + nine
singlets = (3,3̄,1) ⊕ (3̄,1,3) as triplets and (1,3,3̄) as singlets under the eaten
factor — the trinification branching at proof grade.

## 2. THE 27 IS COMPLEX AT THE LANDING — exact, witnessed (the B959-facing theorem)

The c-weight multiset is NOT negation-symmetric: **witness = a multiplicity-3 weight
class whose negation class has multiplicity 0** (the tuple's coordinates are cubic
algebraic numbers — the generic-kernel Cartan basis is arbitrary; the ASYMMETRY is
basis-free). Consequence, stated with B959 cited: **B959's headline sentence ("every
route to rank 4 makes the 27 real") is FALSIFIED BEYOND ITS TORAL SCOPE** — its proof
stands untouched on its own ground (Steinberg torality over the object's finite
images, simply connected form, its own §5 boundary), and the required correction is
the re-scope to "every TORAL route" (the addendum-beside lands with this bank; the
audit seat's flag credited as the trigger). The landing's matter is
representation-level chiral-capable; whether any COUNT-level chirality follows is
governed by the four-language wall and is NOT claimed here.

## 3. The hypercharge test, in its honest three-layer state

- **Bijective form EXCLUDED, exact**: no direction maps the fifteen exact weight
  classes (sizes 3⁶·1⁹) bijectively onto the banked 6Y multiset's eight value classes.
- **Collapse form ESTABLISHED at float grade, and generically**: the banked target's
  degeneracy pattern (6,6,4,3,3,2,2,1) is reproduced by the FIRST random direction
  tried (trial 0) — the compatible cone is open/generic in the rank-4 Cartan, not a
  tuned point.
- **The named residual**: the exact value-match (solve the 4 unknowns from a collapse
  assignment and verify all 27 exactly) — heavier because the raw Cartan coordinates
  are cubic irrationalities; honest as a residual, not silently dropped. Until it
  lands, the claim is COMPATIBILITY (pattern + genericity), not identification.

## 4. What this does and does not change

DOES: the hatch's landing (B1098) now carries exact matter structure — complex,
trinification-branched, hypercharge-pattern-compatible; B959 gets its mandatory
re-scope; THE RANK WALL's sentence extends: *the non-abelian door is open AND the room
behind it has the right furniture at pattern level.* DOES NOT: chirality at count
level (the wall stands); values (nothing here touches a measurement); the A2-class
selection price (unchanged, ~4.3 bits, no mechanism claimed).

**Locks:** tests/test_b1100_landing_content.py (the exact table's sum/sl₂-multiset/
asymmetry-witness from the stored JSON + the pattern-hit record).
