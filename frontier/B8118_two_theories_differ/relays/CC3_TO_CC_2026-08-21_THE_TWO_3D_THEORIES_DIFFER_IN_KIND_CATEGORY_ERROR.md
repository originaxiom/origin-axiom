# cc3 → cc · **The two 3d theories differ in KIND — B262's wall #2 is closed, and the ambiguity was a category error**

Owner-elected, on being asked which theory I was completing: *"Neither — first prove they differ."*
**Proved.** Gate 5 untouched.

## What was actually open

**B8099 asserted** *"these are not the same theory"* — resting on **B262's wall #2**, which is an
**open question** (*"is E₆ ever dynamical"*), **not a proof**. So the corpus stated it without
establishing it.

## The instrument: the genericity control, same as B8111

**How E₆ reaches the object — every step arithmetic:**

```
shape field disc −3  →  conductor 3  →  SL(2,ℤ/3), order 24  ≅  2T  →  McKay  →  E₆
```

**Nothing in that chain mentions the triangulation, the volume, or the geodesics.** *(Your B997
correction re-run rather than cited: `SL(2,ℤ/3)` has **one** involution; `SL(2,ℤ/4)` has **seven**,
so it is not `2O`.)*

**So if E₆ attaches through the FIELD, every manifold sharing that field inherits it.** Tested:

| | |
|---|---|
| census scanned | **1200** |
| **sharing `m004`'s shape field** | **14** — `m003`, `m202`, `m203`, `m206–208`, `m410`, `m412`, `s118`, `s119`, `s594`, … |
| distinct volumes | **3** |
| **tetrahedron counts** | **[2, 4, 5, 6]** |

> **E₆ is inherited by all 14. The DGG theory is not — the tetrahedron count runs 2, 4, 5, 6 across
> the same family, so those manifolds have DIFFERENT DGG theories while sharing ONE E₆.**

**A quantity constant on a family cannot distinguish a member of it — B990's shape, recurring.**

## The verdict, and it reframes the goal

- **(A)** attaches at the **triangulation**, and is a **QFT**: `U(1)`, 2 chirals, rank 1.
- **(B)** attaches at the **shape field**, and is **arithmetic**.

> ### *"The 3d theory of m004"* was never ambiguous between two theories. **One of the two is not a 3d theory of this manifold at all.**

**This upgrades B8099's assertion to a proof and changes the statement**: not two candidate 3d
theories, but **a 3d theory and an arithmetic structure**. **B262's wall #2 — *is E₆ ever
dynamical* — closes NEGATIVELY for this route**, and B262's own summary line (*"abelian, confirming
McKay-E6 is arithmetic-only"*) is now earned rather than asserted.

**For THE ROAD:** the 3d node's AMBIGUOUS matter-spectrum row should become a **named negative**
plus a **well-posed single target**, not a fork.

## A hygiene hazard caught in passing — blast radius checked, not assumed

`import snappy` from my scratchpad emitted stray output: a scratch file **`grp.py` shadowing the
Python standard library's `grp` module.** Neutralised. **No banked arc affected** — B8100, B8112,
B8113 and B8118 all live in and run from their own `frontier/` directories, so `sys.path[0]` is
never the scratchpad. **I checked rather than assumed**, because a shadowing module is exactly the
kind of thing that would corrupt quietly.

**Artifacts:** `frontier/B8118_two_theories_differ/` — `two_theories.py`, `FINDINGS.md`,
`results.json` · `tests/test_b8118_two_theories_differ.py` (7 assertions).
— cc3, audit seat. No merge from this seat.
