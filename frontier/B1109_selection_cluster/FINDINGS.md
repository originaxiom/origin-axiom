# B1109 — THE SELECTION CLUSTER (F1/F1b/F4/F4b): the frame map cannot count to eight, and the eighteen are two named orbits

**Status: banked (frontier). Verdict PROVED (four computed results, each exact,
each complete over its finite domain). The map-finalization wave's first arc
(owner: "GO do them all"). Gate 5 untouched. Lock
`tests/test_b1109_b1110_b1111_wave.py`.**
Run: `python3 frontier/B1109_selection_cluster/f1_f4_frame_and_orbit.py` and
`f4b_orbit_swap.py` (seconds, exact rationals throughout).

## F1 — THE FRAME-MAP KEY CLOSES NEGATIVE, by CARDINALITY (III.4 key 3)

The question (B1102's third typed key): can an ADMISSIBLE frame map (overall
rescaling + sign — B991's licensed freedom, since the hypercharge normalization
is provably convention) reconcile a COLOR-COMMUTING direction with the banked 6Y
multiset? **No — and not by a failed search but by counting:** color-commuting
directions are pure on one ideal's Cartan, and the 27's weight classes PROJECT to
only **7** distinct classes there (both color choices; merge-lines can only
reduce further). Seven classes produce at most seven distinct values; **the
banked pattern needs eight.** No bijection-preserving frame map can repair a
cardinality gap. **And the owner-demanded cross-verification made it a DOUBLE
kill:** the verified projections carry a size-9 class, exceeding the target's
maximum multiplicity of 6 — a second, independent obstruction. The
incompatibility B1102 located is **frame-robust twice over**. *(Verification:
the coordinate-to-ideal pairing was NOT assumed — it was certified by
bracket-annihilation against a freshly recomputed 8+8 ideal split with
cross-brackets-zero: the four stored generators test exactly PURE-A, PURE-A,
PURE-B, PURE-B.)*

## F1b — THE DIAGONAL-COLOR LEMMA (III.4 key 2)

Color realized as any DIAGONAL su(3) ⊂ su(3)⊕su(3) (twisted or untwisted) has
TRIVIAL centralizer: (a,b) commuting with all (x, φx) forces a and b central,
and su(3) is centerless. **No hypercharge u(1) exists at all for diagonal-type
color** — key 2 closes negative in three lines. With F1: **two of B1102's three
keys are dead; only the other-stratum key (F2, in flight) remains.**

## F4 — THE EIGHTEEN ARE TWO ORBITS OF NINE (III.5)

Under the Weyl symmetry W(A₂)×W(A₂) (order 36, acting by the contragredient on
the adapted Cartan; 36 of the 72 tested moves preserve the solution set — none
involving the ideal swap), **B1102's 18 exact directions decompose into exactly
2 orbits of 9.** The selection price re-prices from ≈4.17 bits (bare) to
**1 bit** (orbit choice) + free Weyl gauge.

## F4b — WHAT THE BIT IS (the orbits named)

The ideal swap maps NO solution to a solution (0/18): the two orbits are not
swap-partners. Their representatives differ by an exact **(2, ½) relative
rescaling between the two ideals' components** — the classic shape of
trinification's TWO standard hypercharge embeddings (the flipped pair). Stated
as a RECOGNITION (cited-grade observation), not pressed: the residual bit is
"which of the two trinification-style Y-embeddings," a physically meaningful
binary, not an arbitrary 1-of-18.

## Road dispositions at this bank

- III.4: keys 2 and 3 CLOSED NEGATIVE (this arc); key 1 = F2 (in flight).
- III.5: re-priced — [PROVEN-FREE at 1 bit, the bit NAMED]; the order-36 lead
  RESOLVED (it was exactly W(A₂)² and it acts with two orbits).
