# P4 — THE SECTOR-SPLIT EXTENSION + THE CORRELATE STRESS TEST (sealed prereg;
# cc2, 2026-07-16)

**Mechanism:** at E6, Theta = charge conjugation = the image of S^2, so
Tr(Theta rho(A1)) is the -A1-bundle invariant = the Jeffrey pipeline at P_WORD = -3
(up to a fixed sign convention: W(E6) does not contain -1, so a det(w0)-type factor
may enter — the SU(3) D4 lesson). Sector split: tr_odd = (Z_{+3} - eps*Z_{-3})/2,
tr_even = (Z_{+3} + eps*Z_{-3})/2 with eps in {+1, -1} FIXED BY THE GATE, not fitted
per-rung.

**GATE (hard stop):** with a single global eps, the P = -3 run at r = 13..20 must
reproduce the ENTIRE banked sector ladder (tr_odd, tr_even) =
(1,0),(1,0),(0,1),(0,0),(0,1),(1,0),(1,1),(0,1) for k = 1..8. Any per-rung eps
flipping = convention failure, STOP.

**SEALED PREDICTIONS (the inert<=>even correlate, currently 3/3, tested at the
fresh unit-carrying rungs r = 21..25; Z totals from the banked prediction bank
Z = 1, 2, 1, 2, 0):**
- r = 21 (3 ramified, 7 split; NO inert odd prime): tr_even = 0 => split (1, 0).
- r = 22 (11 === 2 mod 3 INERT): tr_even != 0.
- r = 23 (23 === 2 mod 3 INERT, prime): tr_even != 0 => split (0, 1).
- r = 24 (2^3*3; NO inert odd prime): tr_even = 0 => split (2, 0).
- r = 25 (5 INERT but Z = 0): correlate vacuous at silent rungs — the split banks
  as DATA (the k=4 precedent was (0,0)).
Falsifier: any unit-carrying rung violating "tr_even != 0 <=> an inert odd prime
divides kappa". A single miss kills the correlate (it is 3/3 NOTICED, not law).

**Deliverables:** the full (tr_odd, tr_even) ladder k = 1..13; the correlate verdict
(EXTENDS-TO-8/8-or-dies); the sector-resolved prediction bank for cc (enriching the
banked Z_9..Z_13 predictions with sector splits, all conditional on the two-pipeline
identity + the gated eps convention).
