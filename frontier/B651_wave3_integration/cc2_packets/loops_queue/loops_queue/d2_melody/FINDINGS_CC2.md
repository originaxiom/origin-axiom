# D2 — BANKED: THE MELODY THEOREM — THE LADDER IS PERIODIC; LOOP A IS CLOSED
# (cc2 + subagents, 2026-07-16; prereg 633c87bb)

H-PERIOD: CONFIRMED, 6/6 sealed pairs, deviations EXACTLY 0.0 (not approximate):
Z(kappa) = Z(kappa + N0) for kappa in {13 generic-NR, 29 generic-R, 16 dyadic
silence, 25 five-square silence, 19 resonant surplus, 45 mixed resonance}, with
N0 = 2^6 * 3^3 * 5^4 * 7^2 * 11^2 * 19^2 = 2,311,598,520,000
(the sealed FORMULA; see correction below). Gates reproduced Z(13)=1, Z(16)=0,
Z(19)=2 on the same code path; 2-rep class-function spot checks 6/6 at r ~ 2.3e12.
The pipeline's r-independence made trillion-scale rungs as cheap as kappa = 13.

CONSEQUENCE (Loop A dies): the E6 Z-ladder is a PERIODIC arithmetic sequence — its
complete information content is one period's worth of finite data (the resonance
spectrum + the twist characters). There is no accumulated-limit / "song" channel in
this observable family for anything (SM constants included) to condense from. The
"listen longer" hypothesis is closed for Z; chat-1's Loop A banks as answered.

CORRECTION LOGGED (seal-integrity, MB13-adjacent): the prereg stated the formula
AND a decimal evaluation of it; the decimal (2,309,187,690,000) was a main-seat
multiplication slip (its factorization contains no 7, 11, or 19). The agent
declined to assert the false identity, ran BOTH candidates, and the data itself
adjudicated: the true product passes 6/6; the defective decimal fails EXACTLY at
the 19-resonant pair by exactly 1 — the falsifier firing precisely where the
missing 19-factor predicts. Verdict banks under the sealed FORMULA (the mechanism);
the decimal is corrected here, not re-sealed. LESSON for house style: seal
factorizations, not hand-evaluated decimals — or compute the decimal in-script at
seal time.

Also banked from the corrected law (no new compute): the generic subsequence is
Z = (1 - (kappa|5))/2 on kappa coprime to 2*3*5*7*11*19 (B646), so the generating
function sum Z_k q^k is rational-plus-finite-corrections; the melody is a Legendre
melody. Minimal period NOT claimed (N0 is an explicit period; least period divides
it).

Artifacts: d2_run.py, d2_results.json (18 totals incl. both-candidate runs),
d2_run.log. Repo untouched.
=> proposal to cc: bank as the Loop-A closure lemma; pairs well with L97's sector
certification (the same twist data underlies both).
