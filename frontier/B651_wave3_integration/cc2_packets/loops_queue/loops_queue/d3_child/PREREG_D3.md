# D3 — THE CHILD CELL (sealed prereg; cc2, 2026-07-16). Loop C done honestly.

**Correction carried in this seal:** chat-1's "compute the child from banked
operators" is WRONG as stated — our banked Z is the SOL TORUS-BUNDLE invariant
(mapping torus of T^2), not the cusped 4_1 complement's colored theory; the true
quantum child 4_1(5,1) at E6 needs colored knot invariants we do not have (track iii
prices it). What IS in reach: (i) the child's CLASSICAL invariants; (ii) the
stage-side twisted-bundle family from banked operators.

**(i) CLASSICAL child (SnapPy; agent waits on the install gate):**
4_1(p,1) for p = 5, 6, 7 (p <= 4 are the KNOWN exceptional fillings — slope 5 being
the FIRST hyperbolic integer slope is Thurston-classical and does NOT count as a new
distinction). Bank: volume, Chern-Simons, shortest/core geodesic length, H_1
(elementary: |H_1(4_1(p,1))| = p, so the (5,1) child carries Z/5 first homology —
the mod-5 theater is automatic; note, not discovery). SEALED comparison list for
"slope 5 distinguished BEYOND the known facts" (look-elsewhere denominator = 4):
(a) CS(5,1) arithmetic vs CS(6,1), CS(7,1) (any exact rational/golden structure);
(b) vol(5,1)/vol(parent) vs golden quantities {log phi, 1/phi, phi^-2} (tolerance
1e-6 for NOTICED); (c) core-geodesic length vs 2 log phi (= the parent's monodromy
translation length); (d) any exact identity among the three fillings' data. No
promotion from this cell regardless (n = 3 fillings).

**(ii) STAGE-SIDE twisted family (banked operators; cheap identity:**
Tr(T^n rho) = sum_a t_a^(n+3) S_aa — needs ONLY diag(S) from the counts npz and the
T exponents): compute Z(T^n A_1) for n = -6..6 at k = 1..8 (levels 1..3 rebuilt
fresh, 4..8 from npz). GATE: n = 0 reproduces the banked ladder {1,1,1,0,1,1,2,1}
exactly. Banked observables: the n-profile per level (periodic in n mod ord(T_k) —
stated, not discovered); SEALED question: does the n-profile at fixed k carry mod-5
substructure BEYOND the trivial ord(T_k) periodicity (e.g. the profile's nonzero
support or its value multiset organizing by n mod 5)? Falsifier-shaped: if the
profiles' structure is fully explained by ord(T_k) and the known resonance data, the
answer banks as NO-EXTRA-STRUCTURE (a fine outcome).

**(iii) THE PRICED LEDGER (text):** what the true quantum child needs — E6 colored
invariants of 4_1 (cabling/R-matrix at rank 6, or the E6 Kashaev-type state sum),
feasibility assessment, NEEDS-SPECIALIST label per house convention.
