# D4 — THE SU(3) RESONANCE SPECTRUM + THE LISTENER-LAW STRESS TEST
# (sealed prereg; cc2, 2026-07-16)

**The question:** derive the SU(3)-stage ladder laws from the A2 Jeffrey pipeline,
and stress-test the BANKED B585 additive law
    tr_odd(kappa) = 1*[4|kappa] - (1/phi)*[5|kappa]
off its tested range (kappa = 4..15) — the E6 precedent: our own clean law there hid
a Legendre character that only decisive off-range rungs exposed (the B646 erratum).
Does the SU(3) law hold, or bifurcate?

**Main-seat pre-derivation (part of this seal):** the A2 resonance spectrum is
{1, 16, 16, 5, 5, 5}: identity (det 1), two 3-cycles (eigenvalues at 120 deg:
(3 - 2cos120)^2 = 16), three reflections (eigenvalues +-1: (3-2)(3+2) = 5, sign -1).
Primes {2, 5} ONLY — exactly the [4|kappa], [5|kappa] indicators of the banked law.
PREDICTION P-D4-a: the derived law's resonant primes are exactly {2, 5}; no other
prime can enter at any kappa (falsifier: any non-{2,5} arithmetic in the totals).

**The pipeline (port of the banked E6 script, conventions pinned):** coroot Gram
C2 = [[2,-1],[-1,2]]; W = S3 as integer matrices on the coroot basis; B_w = P*I - w
- w^{-1}; Gauss sum over Z^2/B_w Z^2 of exp(-i pi r <mu, B_w^{-1} mu>) with the C2
form, exact Fractions; Z(r) = (1/6) sum det(w) |det B_w|^{-1/2} G_w(r). TWO WORDS:
P = +3 (the A_1 bundle) and P = -3 (the -A_1 bundle, B585's naming theorem), giving
the sector split via the BANKED identity tr_odd = (Z_{+3} + Z_{-3})/2,
tr_even = (Z_{+3} - Z_{-3})/2. r = kappa = k + 3.

**GATES (hard stop, in order):** (1) reproduce B584's k=2: tr_odd(5) = -1/phi,
tr_even(5) = 0 to 1e-8. (2) reproduce the banked additive law on kappa = 4..15
(values: kappa=4: 1; 5: -1/phi; 8: 1; 10: -1/phi; 12: 1; 15: -1/phi; else 0).
(3) cross-pipeline: direct engine_v7 full traces at k = 1..3 (mp builds, both words
via S^2-conjugation or the -A_1 stage) must equal the Jeffrey totals — the SU(3)
two-pipeline identity is itself a sealed expectation (P-D4-b; falsifier: mismatch
after convention audit).

**DECISIVE OFF-RANGE RUNGS (sealed; the stress test):**
kappa in {16, 20, 24, 25, 28, 35, 40, 44} (deep dyadic; the FIRST JOINT rung 20
where the additive law predicts 1 - 1/phi; 4-and-deeper; 5^2; 4*7; 5*7; joint*2;
4*11) PLUS the hidden-character probe pair {21, 23} (both predicted 0 by the banked
law; both coprime to 10; they sit in OPPOSITE Legendre sectors mod 5 — if a masked
(kappa|5) character exists in the "silent" range, they differ). Additive-law
predictions at all ten rungs written BEFORE the runs (above); any deviation = the
law bifurcates and the derived form supersedes.

**Deliverables:** the derived closed-form law (generic + resonant), the full table,
the two-pipeline identity verdict, and the stress verdict: LAW-HOLDS / LAW-BIFURCATES
(with corrected form) / PIPELINE-DISAGREES (convention audit first, per the E6
jeffrey lesson).
