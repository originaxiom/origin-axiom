# D4 — BANKED: THE SU(3) RESONANCE SPECTRUM DERIVED; THE BANKED LAW HOLDS
# (cc2 + subagents, 2026-07-16; prereg 9f6767ca)

VERDICT: LAW-HOLDS. B585's additive law tr_odd(kappa) = [4|kappa] - (1/phi)[5|kappa]
survives all ten sealed decisive rungs (16, 20, 24, 25, 28, 35, 40, 44 + the
opposite-Legendre probe pair 21/23) at ~1e-51 — no bifurcation, no masked character,
no third prime. The first JOINT rung kappa=20 hits the predicted 1 - 1/phi =
0.3819660113 exactly.

THE DERIVED MECHANISM (the law's first derivation; was DATA in B585): the A2 Jeffrey
pipeline (W = S3) has resonance spectrum {2, 5} ONLY — identity det 1; two 3-cycles
det 16 = 2^4 (carrying the mod-4 resonance = the [4|kappa] term); three reflections
det 5 (sign -1, carrying the mod-5 resonance = the golden -(1/phi)[5|kappa] term).
P-D4-a CONFIRMED. The additive form is real at A2 (unlike E6, where the clean law
masked (kappa|5)) because the A2 classes' twist structure cancels sector-wise — the
21/23 pair proves no residual character.

TWO-PIPELINE IDENTITY FOR SU(3) (P-D4-b): ESTABLISHED, with one honest convention
audit per protocol: W(A2) does not contain -I, so the -A1 word's direct trace
carries det(w0) = -1 (w0 a reflection); with Z_{-3} = det(w0) * Tr(S^2 T^2 S T) the
engine_v7 cross-check matches at ~1e-50 for k = 1..3. The audit is documented in
d4_results.json (the E6 jeffrey lesson applied, not skipped).

Gates: tr_odd(5) = -1/phi and tr_even(5) = 0 at 1e-51; the banked range kappa = 4..15
reproduced (12/12); sector identities via the two words as sealed.

NOTICED (cross-stage contrast, for the record): the stage governs whether the silence
law is additive-indicator (A2: yes) or character-bifurcated (E6: (kappa|5)) — the
resonance-spectrum SIZE (2 classes vs 25) and the odd-rank-5 sector's presence decide.
The disc(A1) = 5 shadow appears at BOTH stages (reflection dets = 5 at A2; the
chi_5 sector at E6) — same discriminant, two silhouettes.

Artifacts: d4_su3_run.py, d4_results.json (22 rungs, full Zp/Zm/sectors + audit),
d4_run.log. Repo untouched.
=> proposal to cc: B585's law row upgrades DATA -> DERIVED (mechanism above);
the kappa=20 joint-rung value banks as a fresh confirmed prediction.
