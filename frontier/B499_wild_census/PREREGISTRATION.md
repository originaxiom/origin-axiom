# B499 — THE WILD CENSUS (adopting chat1's W1–W5 verbatim; successor to the dead Level Law)
Committed before compute (2026-07-10). Depth ≤4, all words, real+complex; mandatory columns
κ / field / Galois-type / CLASSICAL–IRREDUCIBLE. W1 which Galois groups (intermediate D₄/A₄/C₄/V₄?
degree-vs-word-length bound? S₅ by depth 4?). W2 discriminant structure — committed null: wild discs
unstructured; **any recurrence of 283 = a B398 airlock event, STOP + convene**; statistics gate FIXED
NOW: match = FIELD ISOMORPHISM (not disc); base rate = S₄-quartic count |d_K| ≤ census max (LMFDB);
thresholds committed pre-run. W3 verb attribution — hypothesis: wildness needs all three verbs; a
two-verb wild point kills it. W4 saturate the MFD positive-dim component. W5 ramification at FIELD
level (d_K, not poly disc); the 2-ramification parity signature (interaction-born even {12,−1588} vs
geometry-born odd {−283}) — one odd interaction-born disc kills it. Protocol (named, binding): every
monoid identity banked with φ(a), φ(b) AND the level (End(F₂) / trace-map / U2-toral) spelled out —
the L1 sandwich lesson: M Fᵏ M = F_{k+3} M holds at the U2 TORAL (abelianization) level only, and
B497's bank says so; it does not lift to trace maps (chat1's flag: resolved by level, protocol adopted).
Also banked on arrival: κ = 23/4 ± 3√3 (both FFD branches); the S₄ family is MULTI-POINT (chat1's 3
pts κ=1.133/−1.22±0.075i + CC's κ=2.806 — both real quartic roots represented); newborn d_K=−1588 ≠
child −283 (fields provably distinct, firewall-safe); chat1's det(N±I) torsion-factory derivation.
