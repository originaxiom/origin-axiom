# B804 — PREREGISTRATION: the Dirac spectrum of m004 (revised after literature-first review)

**Sealed before any computation.** cc banking seat, 2026-07-29. Mathematics only — **no physics
reading, no Gate 5 surface, nothing to `CLAIMS.md`.** No spinor is read as a fermion.

## 0. What changed, and why the first draft was decorative

The first draft gated an expensive cell behind a **bounded falsifier at Cell 2** — *"if both spin
structures give continuous spectrum, the campaign is over"* — and placed the literature gate
**after** it. Review established that **the kill branch is excluded by a theorem**, so the falsifier
could never have fired.

**The flaw was ordering, not reasoning.** The campaign correctly demanded a literature gate and then
put it downstream of the falsifier it governs. Recorded in `docs/PRACTICES.md`:
*a bounded falsifier declared before its governing theorem is retrieved is not yet known to be a
falsifier.*

## 1. Cells 0–2 collapse to a citation

**The cobordism argument (computable, and computed in this arc):**

- `Ω^spin₂ ≅ ℤ/2`, detected by the **Arf invariant** and generated by `T²` with its **Lie**
  (non-bounding) spin structure.
- The cusp cross-section of a one-cusped spin 3-manifold **bounds** — it is `∂` of the compact core.
  A class that bounds is `0` in `Ω^spin₂`, so **Arf = 0**, so the induced spin structure on the cusp
  torus is the **bounding** one.
- This holds for **both** of m004's spin structures, and for **any** one-cusped spin 3-manifold —
  hence for **m003** identically.

**The cited step (Bär, *J. Diff. Geom.* **54** (2000) 439, restated via Martelli–Reid):** the
spectral type of the Dirac operator on a finite-volume hyperbolic 3-manifold is determined by the
induced spin structure on the cusp cross-section. **This arc cites that implication; it does not
re-derive it.**

**Consequence:** the dichotomy is **determined, identical for both spin structures, and identical
for m003** — so it is **CLASS-level by construction (E34)**, and the m003 scope sub-cell is a
**corollary, not a check**. The campaign therefore reaches its expensive cell directly.

## 2. The real falsifier — and it is expensive, which is stated rather than hidden

> **If m004's and m003's Dirac data agree to the pre-declared floor, the Dirac spectrum is
> CLASS-level and the Laplacian remains the only separator the programme owns.**

Two-outcome, bankable either way. **It is not cheap.** The first draft left the impression that a
low-cost kill existed; it does not. Saying so is the point — §5 of the draft claimed a property the
design no longer has.

## 3. THE WEYL CAVEAT — declared BEFORE any computation

m003 and m004 have **equal volume** (2.0298832128, verified). Bär's setting admits a **Weyl law**,
so their Dirac counting functions **agree to leading order by construction**.

> **Therefore: leading-order agreement is NOT a result.** Any genuine separation lives in the
> **fluctuations** — exactly where B790 found the Laplacian separation.

A Cell 4 testing leading-order behaviour would find agreement and mean nothing. **This is declared
here so that such an agreement can never later be read as a finding.** Any Cell-4 comparison must be
fluctuation-level or it does not count.

## 4. The cells that remain

| cell | question | two-outcome |
|---|---|---|
| **1** | the cobordism computation: is the cusp spin structure bounding, for both structures, both manifolds? | bounding (expected, and it is a *theorem*, so a non-bounding answer means the computation is wrong) |
| **2** | eigenvalues or rigorous bounds, to a **pre-declared floor** | obtained / not obtained in-sandbox |
| **3** | **fluctuation-level** comparison m004 vs m003 | **differ** → a second manifold-level separator · **agree** → Dirac is class-level, banked with equal weight |
| **4** | relation to the Laplacian (only if 3 separates) | mathematical only; physics firewalled |

## 5. What would make this arc a failure

- Re-deriving Bär instead of citing it, or **asserting its implication's direction from memory**.
- Comparing at **leading order** and reporting agreement as a result (§3 forbids it in advance).
- Declaring the precision floor **after** seeing eigenvalues.
- Reporting the dichotomy as an object-level finding — §1 predicts it is class-level.
- Claiming an in-sandbox eigenvalue computation that was not actually performed.

## 6. Pre-stated expectation

Cell 1 returns **bounding** (it is forced). Cell 2 is the honest risk: a cusped Dirac eigenvalue
computation is a **major numerical undertaking** comparable to the Maass work, and this arc may
return *"the structure is settled; the eigenvalues need the numerical machinery"* — which is a
legitimate terminal state for this arc, not a failure, provided it is reported as such and not
dressed up.
