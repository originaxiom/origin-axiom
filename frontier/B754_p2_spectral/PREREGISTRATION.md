# PREREGISTRATION — B754: THE P2 STRATUM (the spectral-face re-adjudication of 19 reconfirmed kills)

cc3, 2026-07-22 (branch `hunt/p2-spectral`, reserved #1250; cc = sole merge gate; RUN-WITH:
plain python3, not Sage). The handoff's P2: "no old negative ever consulted the spectral
face." B742 earned 30 kills as originally scoped; its B′ annotations name the B735
emittance/spectral face as unconsulted for 19 of them (TARGETS.json, sealed — incl. WALL-1
and WALL-7). This arc asks, per target: does the spectral face AS NOW BANKED bear on the
killed claim in a way the original kill never tested?

## The banked spectral surface (frozen for this arc per cc's ack #1250 — the ONLY
face-sources; one-hop verified inside the cited arc, never imported bare)
B737 (the zeta-quotient voice, residue, CM/palette) · B739 (character-rigidity) ·
B746 (the two-column law: gait transmits, name does not) · B753 (the unitary θ-odd block,
eigenphases ±72°, the kind-correct mixing matrix, the one-number pin).

## Three-outcome per target (MB12: each reachable, each earnable)
- **KILL-EXTENDS** — a computed spectral-face fact upholds/extends the kill (the wall gains
  a column). Requires the computation in-cell, with the extension stated as exactly what was
  computed and no more.
- **FACE-OPENS** — a specific, well-posed, computable spectral question exists that the
  killed claim's subject depends on and the frozen surface does not answer: stated as
  {operator, input, discriminator, what-an-answer-would-change}. NOT a revival; NOT a
  positive claim; an entry for the next arc's queue. The claimed GAP in the banked surface
  must be demonstrated (the cell shows where the four arcs fall short), not asserted.
- **FACE-IRRELEVANT** — the face provably does not touch the claim's subject (the B′
  exposure flag was over-cautious). Must be earned with the argument, and contradicts a
  banked annotation → skeptic mandatory.

## Gate 5-Q compliance (this is the first campaign under the adopted firewall)
Q1: all face vocabulary bound to the four frozen arcs. Q2: any consultation operator a cell
defines must FIRST show it discriminates the object from a generic input (a cell whose
operator returns the same answer for a generic census manifold stops there — the B752
lesson). Q3: inputs identified against banked exact sources. Q5: no experience vocabulary
anywhere. Q6: any "the face specially touches THIS claim" carries the specialness budget
(the sister/comparator question named in-cell). Q7: each cell's prereg'd headline states
what would kill it. Gate 5 proper absolute: no SM values; WALL-1's cell in particular
re-examines the KILL's basis against the face — it does not go hunting for values.

## Skeptic rule (declared)
Every FACE-OPENS and every FACE-IRRELEVANT gets one adversarial skeptic (the opening's gap
/ the irrelevance argument attacked with computation). KILL-EXTENDS: deterministic 1-in-3
sample (TARGETS order, indices 0,3,6,…) + any whose extension computes something novel
beyond the frozen surface. Banking-seat clean re-run on every skeptic-surviving
FACE-OPENS before the PR (E18).

## Conventions
Deterministic scripts + printed CHECK: lines per verdict-bearing fact (cc's re-execution
gate); artifacts under cells/<id>/; verdicts journal-only, ids stamped by the harness
script; count-gate 19/19; E23 discipline on any congruence-level statement (name the
filtration); E25 on any integer-relation use (exact verification mandatory); depth/E22
items explicitly OUT OF SCOPE (deferred to P3). Nothing to CLAIMS; substrate untouched;
PR to cc; cc3 never merges.
