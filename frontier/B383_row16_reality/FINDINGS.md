# B383 — the row-16 reality THEOREM (banked; T1 proved, T2 pattern exhibited, T3 contrast)

**Status: banked (frontier). Pre-registration: PREREGISTRATION.md (committed first). Firewalled.**

**T1 (THE THEOREM, finite exact verification at level 15):** t(16,b) ∈ ℚ(√5) for ALL b ∈ ℤ₁₂ —
the √−3 and √−15 components vanish identically across the whole row (support {0,4,8} carries
(1/24, 1/120) and (5/48, −1/240) — the banked darkness values; all other b are full zero).
The sharpest residue of the P60 arc is now a theorem: row-16 silence = ℚ(√5)-reality (this
result) + the mirror law killing b=0's imaginary face (P61).

**T2 (the mechanism, exact pattern):** the per-(j mod 5)-class anti-parts (√−3/√−15 components
of the class-summed Par-table) are individually NONZERO (e.g. b=4: classes carry −1/160…−7/480),
but the ζ₅-window at row 16's exponent (ζ₂₀^{−16j} = ζ₅^{−4j}) annihilates the class vector —
the darkness is a ZERO OF THE ANTI-TABLE'S ζ₅-SPECTRUM at exactly the 16-exponent, while the
row-4 (ζ₅^{−j}) and row-0 (trivial) windows see it. Registered acceptance test passed: the
mechanism distinguishes 16-dark from 4-bright by the character exponent alone.

**T3 (scope):** row 4 is bright at b ∈ {4,8} with (0, −1/40, ±1/48, ∓1/48) — new exact row-4
data banked alongside.

**Provenance.** row16_reality.py (~4 min) → row16.json; locks tests/test_b383_row16.py.
