# B775 PREREGISTRATION — PHASE 2, WAVE 1 (the core structural questions)

*Sealed before any cell computes. Phase 2 is the structural substance: derive a
mechanism, prove a theorem, or hunt for a realization. Unlike Phase 1's mechanical
closures, a WALLED (no-go proven) or CONSTITUTIVELY-OPEN (openness IS the structure)
outcome is a legitimate terminal result here, not a failure. The negatives base is
verified sound (B774), so these build on solid ground.*

## The 7 core cells (each: a two-or-three-outcome criterion)

- **P2-T1MOVER — the T1-mover hunt.** B769 proved T1 is a discrete 3-frame torsor
  whose mover must be an OUTER V₄-automorphism (S₃), and that the inner action is
  trivial. Does ANY object-native operation (a banked involution, a Galois action, a
  geometric symmetry) realize a nontrivial element of Out(V₄)=S₃ on the closing
  frames? Outcome: REALIZED (exhibit the operation + its S₃ image) / WALLED (prove no
  object-native operation is outer — the expected result, the frame choice is
  genuinely unbroken) / UNRESOLVED.
- **P2-AABB — the aAbB derivation door** (chat-1's, adopted). Derive the three closing
  bits {c, θ, γ₅} FROM σ: a→ab's own algebraic/statistical structure (the transition
  matrix, the substitution's eigenstructure, the letter-doubling), showing they emerge
  — NOT by analogy. Outcome: DERIVED (the three bits fall out of σ, with the map
  shown) / DISMISSED (no derivation; the framework is a suggestive analogy, tombstoned)
  / PARTIAL (one/two bits derive, the rest don't — state which).
- **P2-WELD — H-WORD-WELD derive-or-dismiss** (OI-040). The Fibonacci word's
  subdominant Markov eigenvalue −1/φ equals the banked weld θ-odd trace (E20-flagged:
  numerically true, mechanically unlinked). Derive the mechanism connecting the word's
  transition-matrix spectrum to the weld's θ-odd trace, OR dismiss the coincidence
  (base-rate: how many banked golden quantities equal −1/φ?). Outcome: MECHANISM
  (derived link, E20 flag lifts) / DISMISSED (base-rate coincidence, tombstoned) /
  UNRESOLVED.
- **P2-ENUM — the correspondence enumeration + permutation null.** Build the explicit
  enumeration of the 9-signature ↔ phenomenology correspondence (B768) and a
  PERMUTATION NULL: shuffle the phenomenology assignments many ways, score each against
  the sealed discriminators, and report whether the courier's assignment scores better
  than chance. Outcome: ABOVE-NULL (the assignment beats the permutation null at a
  stated margin) / AT-NULL (no better than chance — the correspondence is decorative).
  Gate 5-Q STRICT: structural scoring only, no consciousness claim.
- **P2-D5 — the D₅=Spin(10) McKay check** (chat-1's T4). Run the McKay correspondence
  for the binary dihedral/relevant D₅ and test the Spin(10) connection the handoff
  proposed. Outcome: the McKay/Spin(10) correspondence holds for D₅ (structure shown)
  / it does not (the D₅ analogy fails, stated). Gate 5-Q: structural Lie/McKay only.
- **P2-NOFORCE — S032-A no-forced-choice theorem** (OI-009). Upgrade the algebraic
  no-forced-choice result (no trace-ring invariant is discretely-multivalued AND
  unsymmetric) to a full theorem: prove NO invariant whatsoever (not just trace-ring)
  is discretely-multivalued-and-unsymmetric. Outcome: THEOREM (proven, all-invariant)
  / COUNTEREXAMPLE (an invariant that IS multivalued+unsymmetric — reopens forced
  choice) / NEEDS-SPECIALIST.
- **P2-SELRULE — the ±1/48 selection-rule theorem** (OI-016). Prove the row-16 silence
  / ±1/48 selection rule as a theorem (why that row is silent and why the ±1/48 sign
  is selected). Outcome: THEOREM (proven) / the rule fails at a computed instance
  (RESOLVED-B) / UNRESOLVED.

## Method (binding)
Exact/symbolic preferred; the discriminating fact IN-CELL; a positive/derivation must
be reproduced; the B774 discipline (a "chord/θ-odd" object must be genuinely
non-abelian, not a relabeled character invariant); UNRESOLVED honest → the terminal
state (WALLED / CONSTITUTIVELY-OPEN / EXTERNAL) named. Verdict logic in compute.py.
Gate 5 / Gate 5-Q (structural language, no SM values, no consciousness claims, nothing
to CLAIMS; the one-number pin untouched). Every verdict adversarially verified.
Cells write under cells/P2-*/.
