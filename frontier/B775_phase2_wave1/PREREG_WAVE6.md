# B775 PREREGISTRATION ADDENDUM — PHASE 2 WAVE 6 (8 cells: 5 repairs + 3 fresh)

*Sealed before compute. Same binding structural method, with the accumulated lessons now
explicit: NO MB12 vacuity (every branch must FIRE and FAIL — B414); NO unearned numeric
negative (≥2 sizes AND an identifiable estimator — D3); NO forced reason (a stated
discriminator must not be a corollary of the premise — GATEB); NO undeclared selection
(if a level/sample choice flips the verdict, DECLARE it — B465). Cells: cells/P2W6-*/.*

## Repairs (5)
- **P2W6-GATEB-r**: restate the Gate-B closure on the ONLY independent argument — the
  object's principal route is TWIST-RIGID (χ_principal = 0 on every ε≠0 class, so its
  μ₃-twist torsor collapses and has no chiral member), with the Y4 non-vacuity witness
  (the non-principal SL(2)-route DOES twist to a chiral member). Drop the F₄- and
  SL(2)-exclusion arguments from the load-bearing position (they are corollaries of
  chirality via det Hess I₃ ≡ 2·I₃⁹). Also fix the OI-128 wording. Outcome: Level 3 stays
  CLOSED on twist-rigidity alone => RESOLVED-B / twist-rigidity fails => RESOLVED-A (reopened).
- **P2W6-Z1-r**: fix Z1's two false statements — (a) the irrationality claim is
  ONE-DIRECTIONAL (irrational ⟹ 5|κ; the converse FAILS at κ=15,20,25), (b) correct the
  characteristic-prime exemplars (the computed C5-failing set is {14,15,18,20,21,28,29,31}).
  Re-verify the surviving content (lawless in the level; every Z_k ∈ ℤ[φ], scope k≤28) and
  replace the hardcoded lock values with a recomputation. Outcome: corrected + re-verified.
- **P2W6-B414-r**: rebuild with a verdict branch that can actually FAIL (the RESOLVED-A
  branch was logically forced). Outcome: the content-wall frame's status re-decided on a
  non-vacuous gate.
- **P2W6-D3-r**: redo the entanglement scaling with ≥3 sizes AND an identifiable estimator
  (no argmax at scan bounds); if still underpowered, report EXTERNAL WITHOUT the negative.
- **P2W6-B465-r**: repair the four defects — state the support law truthfully (a fitted
  flip+translate, not im(A−I)), earn or drop the |ker|=15 negative (saturation check), and
  DECLARE the level selection (N=21 flips the verdict). Outcome: honest restatement.

## Fresh (3)
- **P2W6-B106** (OI-141): complete the rank-4 Fix(T₁²) census + the exact c=i point
  (16 variables, below the 25-var wall). Outcome: census completed / walled.
- **P2W6-B138** (OI-142): extend the S031 sealing capstone SL(3) → SL(4) via the CRT
  engine. Outcome: extended / obstruction named.
- **P2W6-PD22** (OI-133): the phase-map riddle — after the pentagon-pair phase prediction
  was killed at rung 75, what does the existence law actually determine? Outcome: the
  phase map characterized / it is genuinely free (tombstone).
