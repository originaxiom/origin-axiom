# PREREGISTRATION — B775 Phase-2 Wave-1 cell P2-WELD (H-WORD-WELD)

Prereg parent: B775 4f73e186. Structural cell. pyenv python3. Nothing to CLAIMS;
program-internal spectral mathematics only; Gate 5-Q (structure, no SM, no sentience).

## The E20-flagged coincidence (OI-040 / B768 V3)
The Fibonacci word letter-transition (Markov) matrix T = [[1/φ², 1/φ], [1, 0]] has
spectrum {1, −1/φ}; its subdominant eigenvalue −1/φ EQUALS the banked untwisted weld
θ-odd trace (B753 addendum-2 / B593): tr_odd(RL, untwisted) = 2cos(108°) = −1/φ.
Banked status: numerically TRUE, mechanically UNLINKED (E20).

## The question
DERIVE a structural mechanism linking the word's Markov spectrum to the weld's θ-odd
trace (⟹ E20 lifts, RESOLVED-A), OR DISMISS as a base-rate golden coincidence by
enumerating how many distinct banked/structural sources produce −1/φ (⟹ RESOLVED-B),
OR UNRESOLVED.

## Conventions / choices (declared before run)
- φ = (1+√5)/2. −1/φ = 1−φ = φ′ (Galois conjugate of φ), root of x²−x−1.
- Weld pipeline: banked B238 su32_wrt.su3_data(level=2); R=T, L=S⁻¹T⁻¹S; untwisted
  weld = R·L; twisted weld = C·R·L (C = charge-conjugation permutation). θ-odd basis
  U = {u₃, u₆} = antisymmetric combinations of conjugate weight pairs (B593/B753 basis).
- Word matrices: substitution a→ab, b→a (incidence M=[[1,1],[1,0]]); Markov T as above.
- Second object (discriminator): m136 = R²L bundle, monodromy [[3,2],[1,1]] (B610/B621);
  its weld word RRL; field ℚ(√3) via ζ₆ phases (B610, banked).

## MB12 — the criterion can pass AND fail
- CAN resolve-A: a derivation predicting tr_odd(weld) = subdominant(word-Markov) as an
  identity that GENERALIZES across objects would lift E20.
- CAN resolve-B: if ≥3 independent structural sources produce −1/φ AND the correspondence
  fails to generalize to a second object, the equality is base-rate.
- CAN be UNRESOLVED: if neither a derivation nor a clean base-rate/non-generalization
  emerges.

## B774 self-test (binding)
The "θ-odd trace" must be genuinely non-abelian to count as a chord. Self-test: if it can
be written as a symmetric trace polynomial (an eigenvalue-sum), it is a character/trace
invariant, not a chord. Checked in-cell.

## Discriminating fact (computed in-cell, never cited)
Object-change test: for m136, compute BOTH the monodromy subdominant eigenvalue AND the
weld θ-odd trace at level 2 from the pipeline; check whether the word↔weld equality
survives the object change.
