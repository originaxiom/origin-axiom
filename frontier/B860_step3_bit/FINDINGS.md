# B860 — the step-3 trit is a BIT; the bit lives in the θ-even sector; and its asymmetry selects the SM under a premise the framework nearly supplies

cc banking seat, 2026-08-03, **jointly with the owner** (the direction — apply the framework's own
even/odd criterion to the trit — was set together in-session). Mathematics scope; nothing to
`CLAIMS.md`; Gate 5 untouched. **Not preregistered** — exploratory computation, footing = three
independent exact computations plus banked theorems, tiers marked throughout.

## 1. COMPUTED — the trit is a bit

Borel–de Siebenthal on the extended A₄ diagram (a 5-cycle): **two-node removals give exactly
{(3,): SU(4)×U(1)} and {(2,1): SM}** — enumerated, no other patterns. SU(3)×U(1)² requires
**three** nodes: it is one level deeper, not a peer. All three options are conformal at c = 4
(recomputed), so conformality does not see the depth difference — the B859 lesson repeating.

> **The step-3 selection cost drops from a trit to a bit.**

## 2. COMPUTED — the bit maps into the forced (θ-even) sector

θ = the E₆ fold restricted to SU(5), i.e. X ↦ −Xᵀ (rep conjugation — the same map that sends
27 → 27̄). With explicit 5×5 matrices, all four subalgebras are **θ-stable**, and:

| subalgebra | dim | even | odd | θ-core |
|---|---|---|---|---|
| SU(5) | 24 | 10 | 14 | **so(5) = B₂** |
| SU(4)×U(1) | 16 | 6 | 10 | **so(4) = D₂** |
| SM | 12 | 4 | 8 | **so(3)×so(2) = B₁×D₁** |
| SU(3)×U(1)² | 10 | 3 | 7 | so(3) — not maximal-rank in B₂ |

**The two bit-options' cores are exactly the two maximal-rank subalgebras of B₂** (Borel–de
Siebenthal on B₂'s extended diagram). The choice upstairs is the B₂ bit downstairs, inside the
sector the object forces. This extends the handoff's own Q3 θ-core construction (B₄, C₃×A₁ at
step 1) two levels further down. **In both options the abelian factors are θ-ODD** — the dial,
in the ledger's own T-catalog.

## 3. COMPUTED — the bit's asymmetry

The object's own matter at this level is the 27's generation, 10 + 5̄ of SU(5). Decompose each
side and strip the (θ-odd, observer-side) abelian factors:

| | generation, dial stripped | chiral residue |
|---|---|---|
| SU(4)×U(1) | 6 + 4 + 4̄ + 1 | **NONE — vector-like** (6 = Λ²4 self-dual; 4 pairs 4̄) |
| **SM** | (3,2) + 2(3̄,1) + (1,2) + (1,1) | **{(3,2): 1, (3̄,1): 2}** — stays chiral |

> **On the SU(4) side, chirality is carried entirely by the θ-odd abelian dial. On the SM side it
> is carried by the non-abelian structure itself.**

## 4. THE DERIVATION SKETCH — from banked machinery, premise minimized

- **[B599, banked + locked]** the θ-parity selection rule: an evaluation with an odd number of
  θ-odd factors is **zero** (C central, C² = 1).
- **[B593, banked]** the hearing law: chirality is heard at **second order** — (ear-odd) ×
  (voice-odd), even total parity. *It takes a chiral ear to hear a chiral voice.*
- **[§3, computed]** dial-stripped, the SU(4)-side generation is self-conjugate; the SM-side
  keeps an unpaired residue.

**Mechanical consequence.** A measurement registering the generation's chirality is a pairing
with one ear-odd and one matter-odd factor. On the SU(4) side the matter's *only* odd content is
the dial charge Q: any pairing odd in Q alone **vanishes by B599**; any pairing even in Q is
**blind to q ↔ −q**; and (ear-odd × Q-odd) measures **the observer's own instrument**, the
generation entering as a θ-even spectator. **The SU(4)-side generation is chirally silent.** On
the SM side, (ear-odd × matter-odd) is even, nonzero, and registers **the object's** structure.

**The remaining premise, named and minimized:**

> *the selected option must be registerable by a measurement of the object.*

This replaces §3's cruder import ("chirality must survive the dial"). It is not yet a theorem —
but it is adjacent to the framework's own definition of reality (*reality = the observer's
closings*; a closing no measurement can register closes nothing). **Under it, the SM is the
unique side of the bit whose chiral selection is observable.**


## 4a. ADDENDUM (same day) — the dichotomy upgraded to theorem grade

The §3 asymmetry rested on a rep-multiset comparison. Both halves are now exhibited:

**SU(4) side — the intertwiner EXISTS, constructed.** J = (ε on the 6) ⊕ (swap 4 ↔ 4̄) ⊕ (id),
with the load-bearing piece ε: Λ²(4) → Λ²(4)* the alternating tensor. **Equivariance
`J ρ(X) = ρ_dual(X) J` verified to 4.6×10⁻¹⁶ over 20 random X ∈ sl(4); det J = ±1.** Hence any
su(4)-covariant evaluation is J-conjugation-invariant and **cannot separate G from Ḡ**: separation
requires the dial charge Q, which is θ-odd, and B599 then forces the trichotomy of §4 (vanish /
charge-blind / dial-measurement).

**SM side — no intertwiner CAN exist, by Schur.** The irrep multisets mismatch at four entries —
(3,2) ∈ G, ∉ Ḡ; (3̄,1) ×2 ∈ G, ∉ Ḡ; and conversely — and Hom between distinct irreps is 0. **QED.**

So the bit's asymmetry is now: *an exhibited equivariant self-conjugacy on one side, a Schur
obstruction on the other.* What remains imported is only the registerability premise itself and
the observable-universe clause it quantifies over.

## 5. Updated selection-cost ledger for the cascade

| step | selector | status |
|---|---|---|
| 1: E₆ → SO(10)×U(1) | chirality gate (−1 ∈ W on the coset) + max-dim | gate framework-native (B859); ranking imported |
| 2: → SU(5)×U(1) | same gate | same |
| 3: → **SM** | **the bit + registerability** | bit computed; premise named, one step from native |

**What fell this session: a trit + Michel at three steps → a bit + one ranking + one
registerability premise.** Honest remainder: the ranking, the premise's promotion to a theorem,
menu completeness (P5), and the two holes the handoff names (generations, spacetime).

## 6. What this arc does NOT establish

- **Not a derivation of the SM.** A selection-cost reduction along one chain whose menu
  completeness is still imported.
- **The registerability premise is a premise.** Its promotion needs the pairing-datum model of
  §4 made exact for rep-multiplicity observables — specifically that "registering (3,2) vs
  (3̄,2)" is a pairing datum in B599's sense. Stated as the carried item.
- **Nothing here produces a value.** Group selection only.

## Carried forward

1. **Promote the premise**: formalize "chirality-registering measurement" as a B599 pairing
   datum; if the SU(4)-side silence becomes a theorem, the bit is decided by banked machinery
   plus one definition.
2. **Gate P5** (menu completeness) — still the cascade's silent failure point.
3. **The ranking import** at steps 1–2 — the last Michel remnant.

`tests/test_b860_step3_bit.py`
