# B722 — THE RESURGENCE COUPLING: the two phases ARE being/hearing, but the continuum is arithmetically RIGID (not the free coupling)

*cc banking seat, 2026-07-20. Sealed prereg ea377d0e. Advanced multiagent loop campaign
(web-enabled; compute→3-skeptic adversarial-verify→refine), 16 agents, 0 errors. Probe 1
OUTCOME A (rung 1, skeptic-corrected from an over-rung label); probes 2&3 OUTCOME B (proven
in-sandbox). Citations WebFetched + confirmed real (Galakhov–Morozov 2605.31588/2606.24497).
Verified-on-receipt (I re-derived Φ''(t*)=√−3, catching my own sign-bug first). Credit cc2
(the lead) + Galakhov–Morozov (corroboration).*

## Verdict — a genuine field-level positive + a rigid-continuum negative
The Kashaev resurgence is the ONE discrete→continuous machine that consumes the object's
A-polynomial today, and its two phases ARE exactly being/hearing (rung-1 field match,
externally corroborated). BUT the continuum it produces is **arithmetically RIGID over
ℚ(√−3)** — NOT the SM's free coupling. So the resurgence reconfirms B706 at the analytic
level, and completes the picture: every continuum the object produces is rigid/scale-free.

## Probe 1 — the two Kashaev phases = being/hearing (OUTCOME A, rung 1)
Galakhov–Morozov (verified real; abstract quoted verbatim): the fig-8 Kashaev double-scaling
limit has TWO quasiclassical phases — Phase 1 (non-trivial classical action) → the classical
A-polynomial + hyperbolic volume Vol=2.029883…=2 Im Li₂(e^{iπ/3}) [BEING, shape z²−z+1, disc
−3, ℚ(√−3)]; Phase 2 (vanishing classical action) → the inverse-Alexander of Δ=t²−3t+1 [roots
φ^±², disc +5, HEARING, ℚ(√5)]. The two phases carry the two disjoint quadratic fields =
exactly the program's being/hearing split, object-invariantly. (Rung: a FIELD match — two
named phases landing in the two fields — not a torsor claim; skeptic-corrected.) Corroborating
gift: the Alexander polynomial sits INSIDE the A-poly's L=1 slice — A(M,1)=(M²+1)²(M⁴−3M²+1)
with M⁴−3M²+1 = Δ(M²) — both faces in one A-polynomial (echoes the B707 two-faces result).

## Probe 2 — the Borel/resurgence structure is arithmetically RIGID (OUTCOME B, proven)
PROVEN in-sandbox (exact, via the Riccati recursion for the quantum-CS potential
Φ(t)=Li₂(e^t)−Li₂(e^{−t}) at its saddle t*=iπ/3): the **1-loop constant Φ''(t*)=√−3** (the
trace-field discriminant — re-derived + verified this seat, √−3=i√3 exactly), so the entire
ħ trans-series has coefficients in ℚ(√−3) (a₁=−√−3/108, …; every Φ^(n)(t*)∈ℤ[√−3]). The
leading Borel singularity sits at i·Vol(4₁) (the object's OWN complex-volume period; ~0.2%
numerically over 28 terms); the Stokes constants are integers (GGM peacock lattice, cited).
**NO free continuous parameter** — every freedom is DISCRETE (a lattice index n∈ℤ, a ±
Galois-conjugate sign). A free coupling (Outcome A) would need a continuous real dial in the
coefficients or singularity positions; both are FIXED by the object's ℚ(√−3) arithmetic.

## Probe 3 — the ħ is the object's OWN quantization, not the free coupling (OUTCOME B)
ħ (the Chern–Simons quantization; level N=2π/ħ∈ℤ, discrete; ħ→0 = the rigid classical
A-polynomial) is the object's own quantization parameter, NOT a place the SM's continuous
coupling could enter. The resurgence produces a continuum (the ħ-series), but it is the
object's OWN rigid quantum continuum over ℚ(√−3), not the SM's free coupling.

## The completing picture — every object continuum is rigid/scale-free (B706 total)
The object DOES produce continua, but NONE is the SM's free coupling:
- the A-polynomial deformation curve (B712): rigid, no canonical real anchor;
- the covering scale / multiplicity (B719): a free ladder = the observer's scale;
- the object's own time (B721): tracial type II₁, trivial modular flow (equilibrium, rigid);
- the ħ-resurgence series (B722): arithmetically rigid over ℚ(√−3).
So the SM's free coupling lives in NONE of them — B706 reconfirmed at every level probed
(value · field · dimension · cluster regime · von Neumann type · resurgence).

## Placement
LAW_MAP: the resurgence's two phases ARE being/hearing (rung-1, corroborated), but its
continuum is arithmetically rigid over ℚ(√−3) — completing "every object continuum is
rigid/scale-free, none the SM's free coupling." Nothing to CLAIMS. Credit cc2 + Galakhov–Morozov.

---

## TWO-SEAT RECONCILE (cc2's independent seeing-strategy adjudication, verified, 2026-07-20)
cc2 independently adjudicated chat-1's SEEING STRATEGY (converging with B724: Path-1 core
refuted, kept the reading; C4/C5 refuted; C3 = the arrow is the observer's B723 SSB, not the
object's chaos). It raised a B722 RECONCILE this seat verifies + adopts:

- **A² = M (verified exact).** The golden Fibonacci monodromy A=[[1,1],[1,0]] (eigenvalues φ,
  −1/φ ∈ ℚ(√5) = HEARING; the 2I rep, B640) SQUARES to the geometric cat-map M=[[2,1],[1,1]]
  (eigenvalues φ²,φ⁻²; the being's monodromy). So HEARING (A) squares to the being's monodromy.
- **The resurgence is 2+1 sectors, not 2 (reconcile of B722 probe 1's "two phases").** The
  fig-8 published structure is: σ₁,σ₂ (the two GEOMETRIC saddles, shape e^{±iπ/3}, ℚ(√−3) =
  BEING, the volume) + σ₀ (the ABELIAN / Alexander sector, Δ=t²−3t+1 roots φ²=M's eigenvalues,
  ℚ(√5)). B722 probe 1's "phase 2 = inverse-Alexander = hearing" is FIELD-correct (σ₀ carries
  ℚ(√5)), but STRUCTURALLY σ₀ is the abelian/Alexander THIRD sector — NOT the golden hearing
  MONODROMY itself (which is A, with A²=M). The ℚ(√5) in σ₀ is M's eigenvalues φ² (= A²'s), so
  the two are linked but distinct: σ₀ = abelian sector; A = the hearing monodromy. Adopted:
  the resurgence carries being (σ₁,σ₂) + a golden-arithmetic abelian sector (σ₀), with the
  hearing monodromy A the deeper source (A²=M).
- **B701's outer swap = the σ₁↔σ₂ Kashaev exchange (concrete, verified).** Gal(ℚ(√−3)/ℚ)=ℤ/2
  (complex conjugation) swaps the two geometric saddles σ₁=e^{iπ/3} ↔ σ₂=e^{−iπ/3} — this IS
  B701's outer measurement/chirality swap, now realized concretely inside the GGM resurgence.
  So the being's conjugation (B701, the observer's c-swap) lives literally in the Kashaev
  saddle exchange. Credit cc2 (the A²=M / 2+1 / σ₁↔σ₂ identifications).
