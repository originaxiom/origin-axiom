# PREREG — B722: THE RESURGENCE COUPLING — does the Kashaev ħ-series reach the free coupling, or only the object's own rigid continuum? (SEALED)

cc banking seat, 2026-07-20. Owner "push": run the second coupling-path lead (B720 LEAD 2).
The Kashaev / quantum-A-polynomial ħ-expansion is the ONE discrete→continuous machine that
consumes our A-polynomial TODAY, and the being/hearing split appears as the TWO Kashaev
quasiclassical phases (Galakhov–Morozov 2605.31588 "Two roles of Alexander in two Kashaev
phases", 2606.24497 — both VERIFIED REAL). Same resurgence family as B710's Turok thimble
comparison. THE QUESTION: does the Kashaev/resurgence continuum REACH the SM's free coupling,
or does it produce only the OBJECT'S OWN arithmetically-rigid quantum continuum (the ħ-series
+ the ℚ(√−3) Borel lattice)? Web-enabled (WebFetch Galakhov–Morozov + Garoufalidis–Gu–Mariño
resurgence). Structural/arithmetic only; Gate 5 stands; no physics/SM value.

## Banked context
B710: the fig-8 state-integral has 2 REAL-action saddles (CS=0) + a "peacock" Borel lattice
(GGM, ℚ(√−3) quantum modularity). B712/B719: the object's OWN continua (the A-poly deformation
curve; the covering scale) are rigid/scale-free, not the SM's free coupling. B707 open door:
are the Kashaev resurgence coefficients MOTIVIC PERIODS (Brown coaction)?

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **The two Kashaev phases = being/hearing.** Verify (WebFetch Galakhov–Morozov + compute):
   the fig-8 Kashaev double-scaling limit has two quasiclassical phases — one → the classical
   A-polynomial + hyperbolic volume (BEING, ℚ(√−3)), the other → inverse-Alexander (HEARING,
   golden ℚ(√5)). Two-outcome: A = the two phases ARE exactly being/hearing; B = a different
   structure.
2. **The Borel/resurgence structure — RIGID or FREE?** The Kashaev ħ-expansion is a trans-series
   with Borel singularities (the "peacock" lattice, GGM) at the complex-volume lattice over
   ℚ(√−3), + Stokes constants. Compute/characterize: are these arithmetically RIGID (the
   object's own ℚ(√−3) arithmetic — the singularities at nℤ·CV, Stokes constants integers/
   ℚ(√−3)) with NO free parameter, or does the resurgence open a FREE continuum? Two-outcome:
   A = a free continuum (a coupling could live in it); B = arithmetically rigid (the object's
   own lattice, no free parameter).
3. **The coupling verdict — does resurgence reach the FREE coupling?** Is the ħ (the Chern–
   Simons quantization/Planck parameter) a place the SM's free coupling could enter, or is it
   the object's OWN quantization (fixed by the object, not free)? Together with probes 1–2:
   does the Kashaev resurgence give a bridge to the SM's free parameters, or only the object's
   own rigid quantum continuum? Two-outcome: A = a bridge to the free coupling; B = the
   object's own rigid continuum only (reconfirming B706 at the resurgence/analytic level).

## Two-outcome (sealed, campaign)
- **OUTCOME A:** the Kashaev/resurgence machine opens a genuine FREE continuum reaching the
  SM's coupling — a real bridge (escalate).
- **OUTCOME B:** the resurgence consumes the object's A-poly and produces the object's OWN
  rigid quantum continuum (2 phases = being/hearing; Borel lattice = ℚ(√−3); ħ = the object's
  fixed quantization) — a discrete→continuous machine that stays arithmetically RIGID, NOT the
  SM's free coupling. The object produces continua (A-poly curve B712, scale B719, ħ-series
  B722) but ALL rigid/scale-free. [honest expectation: B — same shape as thermal-time.]

## Verdict template
Bank the resurgence verdict + whether the Kashaev coefficients look motivic (the B707 door,
even partially). Either way it sharpens "the object's continua are rigid; the SM's is free."
LAW_MAP placement; nothing to CLAIMS. Credit cc2 (the lead) + Galakhov–Morozov (corroboration).
