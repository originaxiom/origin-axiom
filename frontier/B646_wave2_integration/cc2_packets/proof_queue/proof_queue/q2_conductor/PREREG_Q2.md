# Q2 — THE ANOMALY-ZONE MECHANISM (sealed; cc2, 2026-07-16)

**The question (N2's residual):** what kills beyond the k>=3 law at the shallow rungs?
Two candidate mechanisms, one per stage, sealed with falsifiers:

**P-Q2-a (E6 k=2 = CONDUCTOR DROP):** the odd-block T-order (the block's true Bantay
conductor) is c_odd = lcm of ord(zeta^e_a) over PAIRED primaries only. Sealed
prediction: **c_odd(E6, k=2) = 21** — the mod-4 factor is simply ABSENT from the block
(no mod-4 death to explain: ord(A1 mod 21) = 8, split-7 central kill halves to 4 =
banked). Falsifier: c_odd = 84 or 42 (mod-4 present => the death needs another
mechanism entirely).

**P-Q2-b (consistency sweep):** replacing ord(T) by c_odd in the k>=3 law must change
NOTHING at any working rung: for every E6 k=1..8 and SU(3) k=1..13, clock =
ord(A1 mod c_odd) with the stage-typed kills must equal the banked/measured clock
wherever one exists. Any break kills the conductor refinement. (Note: at rungs where
c_odd = ord(T) this is vacuous; the content is at conductor-drop rungs.)
Bonus: record whether the E6 k=8 prediction changes (Q1 adjudicates if the two
versions diverge).

**H-Q2' (SU(3)_2 = CHARACTER BLINDNESS, not conductor):** hand arithmetic (part of
this seal): the paired primaries (1,0)/(0,1) and (2,0)/(0,2) have T-exponents 2/15,
8/15 => c_odd(SU3, 2) = 15 — the 3-part is PRESENT, so conductor drop cannot explain
the -I mod 3 kill there. Sealed mechanism hypothesis: the banked V7 conduit
(theta-odd block = rho_Fib) holds as an exact TENSOR factorization
**rho_odd = rho_Fib (x) chi** with chi a 1-dim character of order 3 (either Galois
convention of Fib permitted), and the kill is chi's blindness on the A1-word:
chi(T^2 S T) = 1 while ord(rho_Fib(A1-word)) = 10 = the banked clock. This would
unify the ENTIRE k<=2 anomaly zone as abelian/character blindness (clause 1 = the
dim-1 case of the same phenomenon). Falsifiers: rho_odd(g) rho_Fib(g)^{-1} not scalar
for g in {S, T} (no factorization); or chi(A1-word) != 1 (blindness wrong); or
ord(chi) != 3.

Machinery: T exponents from stored npz / fresh enumeration (E6; no S needed except
SU(3)_2's 2x2 block from engine_v7 at dps 50); exact Fractions for SU(3); integer
quantization keys for scalar checks. Gates: c_odd must equal ord(T) wherever N2's
table showed the full-conductor law already working faithfully with equality
(k=3,5 E6 style rungs) — else the c_odd computation itself is buggy.
