# Q2 — BANKED: THE ANOMALY ZONE MECHANIZED — every k<=2 anomaly is CHARACTER
# BLINDNESS around a non-abelian core (cc2, 2026-07-16; prereg 287bfe86).

SEALED-PREDICTION VERDICTS (honest ledger):
- P-Q2-a REFUTED: c_odd(E6, k=2) = 84 = ord(T) — NO conductor drop (sealed guess 21
  dead). Sweep of all 21 rungs (q2_table.txt): the block conductor equals the full
  conductor everywhere except the degenerate SU3 k=1 (12 -> 4); conductor refinement
  changes NOTHING. The anomaly zone is not a conductor phenomenon.
- P-Q2-b: consistency holds (vacuously — no drops; E6 k=8 prediction unchanged at 60).
- H-Q2' CONFIRMED WITH ONE CORRECTION: SU(3)_2 odd block = rho_Fib (x) chi EXACTLY
  (S- and T-ratios scalar at 1e-35; the (c,h) = (14/5, 2/5) convention — the Galois
  twin fails, pinning the conduit's side). chi has ORDER 4 (sealed guess 3 — wrong,
  reported): chi(A1-word) = 1 exactly (BLIND) and ord(rho_Fib(A1-word)) = 10 = the
  banked clock. The "-I mod 3 kill" dissolves: nothing is killed — the abelian twist
  simply cannot see the A1-word; the Fibonacci core carries the whole clock.
- H-Q2'' (coordinate decomposition at E6 k=2) REFUTED: all three pair T-orders are 84,
  S_odd has no zero structure (moduli circulant-like) — the block is irreducible.

THE E6 k=2 MECHANISM (H-Q2''', post-hoc label, every step exact-checked or
standard-table-cited): the congruence image factors CRT: SL(2,Z/84) = SL(2,4) x
SL(2,3) x SL(2,7); a 3-dim irrep must place its non-abelian core at ONE prime.
Placement decided by the banked eigenvalue fingerprint {1, i, -i} (N1 characters):
a core at 3 (A4's 3-dim on an order-2 image) forces a doubled eigenvalue {c,-c,-c} —
REFUTED; a core at 7 = the 3-dim of PSL(2,7) on the order-4 class (A1 mod 7 has order
8 in SL, 4 in PSL) has trace +1 => eigenvalues EXACTLY {1, i, -i} — MATCH. The two
character factors are BLIND on A1 by direct computation (this cell): SL(2,Z/3)ab =
Z/3 and SL(2,Z/4)ab = Z/4 with A1's image TRIVIAL in both. Therefore:
  clock(E6, 2) = ord_{PSL(2,7)}(A1 mod 7) = 4  — the banked value, derived.
Bonus identification: the split-7 central kill (the E-rule's kill at this rung) IS
PSL-factoring — the local core factors through PSL(2,7), killing -I structurally.

THE UNIFICATION (the k<=2 anomaly zone dissolves): every anomalous rung = abelian
character factors (provably A1-blind) around a non-abelian core that keeps the law:
k=1 rungs: the block IS a character -> clock 1. SU(3)_2: Fibonacci core, clock 10.
E6 k=2: PSL(2,7) core, clock 4. NOTICED (one line, no claim): the two shallow cores
are the icosahedral/golden (2I via Fib) and the Klein-quartic group PSL(2,7) = GL(3,2)
— kappa = 5 and kappa = 14 = 2*7 hear their own conductor's simple groups.

=> proposal to cc: N2's clock law now has NO anomaly zone — restate as: clock = the
order of A1's image through the block's non-abelian core (character factors blind);
for k >= 3 the core is the full congruence image and the stage-typed kill rule applies
(at E6 k=2 the "kill" = PSL-factoring, suggesting the k>=3 split-kill rule is ALSO
PSL-factoring at split primes — promotion-grade target, one theorem candidate).
Lit-gate note: PSL(2,7) character table is standard (no novelty claimed on it);
the BLOCK-LEVEL mechanism identification is the new content.
Artifacts: q2_table.txt, q2_mechanism.log, q2_conductor.py, q2_mechanism.py,
SEAL_Q2.txt; ab computation inline in session log (24/8/3 and 48/12/4 with A1 -> 1).
