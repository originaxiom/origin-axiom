# N2 — THE CLOCK LAW (sealed prereg; cc2, 2026-07-16)

**The question (registered open at L81(b): "no κ-law found"):** does the H-R2 kill
mechanism (sealed sha 0982d8e0, confirmed prediction-first at E₆ k=7) DERIVE the full
clock column — E₆ k=1..7 AND, out-of-family, SU(3) k=1..12 (B585's banked data)?

**The rule (verbatim transfer from H_R2_SEALED.md; ZERO refit):**
1. n = ord(T_k); CRT-decompose n = Π p^a.
2. Killed components: split primes (p ≡ 1 mod 3) and deep 3-powers (3^a, a ≥ 3).
   The kill removes the central −I of that component from the image.
3. clock = ord(A₁ mod n) / |⟨A₁ mod n⟩ ∩ K| where K = the subgroup generated by
   CRT-vectors that are −I exactly on a subset of killed components and I elsewhere.
   (Equivalently: A₁^j ≡ I at every surviving component and ≡ ±I at killed ones.)
4. KNOWN UNEXPLAINED ANOMALY (flagged in the seal): at E₆ k=2 the ENTIRE mod-4 factor
   dies, not just a central. The rule above does NOT predict this. Any rung where the
   rule fails is an anomaly datum, not a refit license.

**Honesty labels:** the E₆ column {1,4,60,36,36,18,36} and the SU(3) column
{1,10,12,8,12,36,60,20,12,28,24,60} are ALREADY KNOWN to this seat → parts (a)/(b) are
VALIDATION, not prediction. The only true prediction in this cell is part (c).

**Procedure:**
(a) For all 19 rungs compute n = ord(T) (exact, engine-certified), the CRT factors,
    ord(A₁ mod p^a) per factor, the rule's predicted clock; compare to banked. Bank the
    full table blind (script prints predictions BEFORE the banked row comparison).
(b) Localize every mismatch; characterize the anomaly set (is E₆ k=2 alone? do SU(3)
    rungs show whole-factor deaths, and at which p^a?). If a pattern emerges, seal an
    AMENDED rule in an addendum (sha'd) BEFORE part (c).
(c) FRESH RUNG, prediction-first: SU(3) k=13 (κ=16, dyadic — the interesting stress
    case). Predict clock from the (amended) rule in the addendum, THEN build the level
    and measure ord(B_odd). Falsifier: measured ≠ predicted kills the amended rule.

**Machinery:** engine_v7.An_Level (SU(3), exact) + level_ladder engine (E₆, banked
readouts reused where stored); fib-order/matrix-order mod m in exact integer arithmetic;
dps 50 order certificates. Gates: SU(3)₂ must reproduce B584 (clock 10, tr_odd = −1/φ);
E₆ rows must reproduce the banked clocks before the table is read as validation.

**Verdicts:** LAW-DERIVED (19/19 + fresh rung) / MECHANISM-PARTIAL (mismatch set
nonempty but structured) / DEAD (unstructured failure out-of-family).
