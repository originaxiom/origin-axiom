# N2 ADDENDUM — the amended (stage-split) clock law (sealed BEFORE the fresh-rung
# measurement; cc2, 2026-07-16; parent prereg 8393e404)

Part-(a) result (n2_table.txt): sealed rule verbatim = 12/19. One banked-value
correction during gating: E6 k=4 clock = 12 from disk (level4_readouts.json), not the
36 mis-carried in context. The mismatch set is structured BY LEVEL, not scattered:

**THE AMENDED LAW (three clauses):**
1. k = 1 (both stages): clock = 1 — the odd block is 1-dimensional, the rep is an
   abelian character; A1-word ↦ 6 in SL(2,Z)^ab = Z/12 and the block's character has
   order dividing 6 ⇒ total blindness. (Mechanism-sketch, checkable.)
2. k = 2 (both stages): the SHALLOW ANOMALY ZONE — deaths beyond clause 3, stage-shaped:
   E6 kills the whole <A1 mod 4> ≅ Z/3 factor (plus the clause-3 split-7 central kill);
   SU(3) kills the central -I mod 3. UNEXPLAINED — the residual of this cell.
3. k ≥ 3: clock = ord(A1 mod ord(T_k)) with stage-typed central kills:
   - E6: kill -I at split primes (p ≡ 1 mod 3) and deep 3-powers (3^a, a ≥ 3);
     halving iff the component's <A1> order is even (else vacuous).
   - SU(3): NO kills — the congruence image is faithful on <A1>.
   Validation: 15/15 (E6 k=3..7 incl. the non-vacuous kills at k=6 and the vacuous one
   at k=7; SU(3) k=3..12 incl. the three would-be-kill rungs 4, 6, 11 where banked =
   faithful). The kill set is a STAGE INVARIANT, not universal arithmetic.

**FRESH-RUNG PREDICTIONS (sealed here, before any block is built):**
- P-N2-c1 (measured in this cell): SU(3)_13, κ = 16: ord(T) = 48, ord(A1 mod 48) = 12
  ⇒ **clock = 12** (clause 3, A-type, no kills; k=13 ≥ 3 so no anomaly-zone escape).
  Falsifier: any other value; 6 or 24 would specifically indict the no-kill clause.
- P-N2-c2 (registered for a future cell / for cc — E6 k=8 build is expensive):
  E6 level 8, κ = 20: ord(T) = 60 = 4·3·5, killed set empty (5 inert, 3 shallow),
  ord(A1 mod 60) = 60 ⇒ **clock = 60**, equality (first predicted clock EXCEEDING 36).

Gate for the measurement run: rebuild SU(3)_2 first and reproduce the banked clock 10
and tr_odd = -1/phi (B584) before k=13 is trusted.
