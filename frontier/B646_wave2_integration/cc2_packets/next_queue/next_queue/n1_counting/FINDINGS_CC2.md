# N1 — BANKED: WHAT Z COUNTS (cc2, 2026-07-16; prereg aa47092d).
# THE COUNTING THEOREM-CANDIDATE + a six-rung prediction bank for the unmeasured ladder.

HEADLINE: Z_k is the signed Weyl-average of Gauss sums over twisted-sector fixed-point
groups, and every departure of the ladder from +1 is a KAPPA-RESONANCE against the
object's fixed-point spectrum. Structural identity (exact, from tr A1 = 3, det A1 = 1):
det B_w = det(3I - w - w^{-1}) = |det(A1 (x) w - 1)| = #Fix(the w-twisted A1-action on
the coroot 12-torus) — Jeffrey's B_w-groups ARE the twisted flat-connection sectors.

PIPELINE GATE (no normalization freedom): Z_J(13..19) = +1,+1,+1,0,+1,+1,+2 EXACTLY —
the disjoint Gauss-sum pipeline reproduces the whole banked ladder including a fresh
independent confirmation of Z6 = +1 and the Z7 = 2 discovery (jeffrey_run.log).

SEALED-PREDICTION VERDICTS:
- P-N1-a CONFIRMED: the k=4 zero is pure cross-class INTERFERENCE — all 25 Weyl classes
  individually nonzero at r=16 (largest +-2/3), sum exactly 0.
- P-N1-b REFUTED AS STATED (support is 25/25 at every r) — actual mechanism SHARPER:
  the entire +2 surplus at r=19 lives in ONE class, charpoly Phi_9 (order-9 Weyl
  elements, size |W|/9, det B_w = 361 = 19^2): its Gauss sum saturates fully
  constructively on its (Z/19)^2 fixed-point group (per-element jump exactly x19).
- P-N1-c (8-classifier sealed list, 7 rungs): one hit, NOTICED only — "kappa has an
  inert odd prime (Q(sqrt-3))" <=> the unit sits in the EVEN sector (15, 17; 0/5 false
  positives). Consistent with the splitting law's inert real-quadratic imports.

THE JUMP LAW (150 cells checked vs the generic r=13 row):
|term_w(kappa)|/|term_w(generic)| = sqrt(kappa-part of det B_w) — EXACT for odd primes
(incl. partial powers: sqrt5, sqrt45, sqrt1125 at r=15); dyadic terms obey the full law
at v2(kappa)=4 (r=16) but are CAPPED at v2(kappa)=1 (r=14,18): observed 2^floor(v2/4),
20/20. (2-adic local-factor subtlety; v2(kappa)=2 discriminated at r=20 in extension.)

THE RESONANCE SPECTRUM of the object: {det B_w} = {1,5,16,25,45,80,81,100,121,125,180,
225,245,256,320,361,400,576,605,625,1125,1280,1600,4096} — primes {2,3,5,7,11,19} ONLY.
GENERIC-RUNG LAW (theorem-candidate): kappa coprime to all of these => Z = +1 (the
abelian/Funar value; det(A1 - I) unimodular). Confirmed in-pipeline at kappa = 23.

PREDICTION BANK (jeffrey_extension.json; conditional on the two-pipeline identity,
7/7 exact where testable; class-function spot check 18/18 at new r):
  Z_8..Z_13 (kappa 20..25) = +1, +1, +2, +1, +2, 0.
  - kappa=22: second surplus — the 11^2 class (det 121) saturates, same mechanism as 19.
  - kappa=24: third surplus — dyadic+3 constructive combination.
  - kappa=25 = 5^2: SECOND SILENCE predicted (5-heavy classes interfere destructively).
    Both silences at prime-power squares: 16 = 2^4, 25 = 5^2 (pattern, n=2, no claim).
Falsifiers for cc: any measured Z_k (k=8..13) differing from the bank kills the
resonance model (the conformal-block side is expensive but k=8 is within reach).

D1 CHARACTERS (characters.json; all 7 levels GATE-OK: sector traces = banked ladder,
orders = banked clocks, integer multiplicities certified at 1e-30, dims match):
exact root-of-unity multiplicity vectors per sector per k. Noticed: Z != dim Fix
(k=3 has NO invariant vector in either sector yet Z=+1: Z is the signed Fourier count,
not a fixed-space dimension); k=6 odd = one distinguished +1 line + uniform mult-5
shells; k=7: (m_0 odd, even) = (7, 4), both sector orders exactly the clock 36.

DEVIATION LOG: none — power-trace DFT at dps 50 as preregged for ALL k (the persisted
exact counts made k=7 feasible); the CSP-bound cap was never exceeded (assert armed).
=> proposal to cc: bank the counting mechanism at NOTICED->CHECKED (every number above
is exact-certified); adopt the prediction bank as sealed falsifiers; the generic-rung
law + the jump law's 2-adic cap are the promotion-grade targets (need proofs, not more
data); L24(b)'s "does P read off Jeffrey's conductor" now has a concrete apparatus.
