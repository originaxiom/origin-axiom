# LEVEL-7 CELL (κ = 19) — THE SPLIT-PRIME RUNG (sealed; cc2 residuals loop R1)

**Constants (formula-determined, part of this seal):** P⁺₇(E₆) = 231, θ-fixed 21,
odd dim 105 (MB12 guard). ord(T₇) = 228 = 2²·3·19 (Bantay conductor → ℚ(ζ₂₂₈));
ord(A₁ mod 228) = 36.

**SEALED PREDICTIONS:**
- PRED-L7-1 (the split form — the law's decisive split test since 7|14): 19 ≡ 1 (mod 3)
  is SPLIT in ℚ(√−3) ⇒ the odd block mints ℤ/19 sine-kernel-type content (odd-19-conductor
  cyclotomic structure in the magnitudes/S_odd), and NO real-quadratic import appears
  (√19 absent — split primes give the kernel form, not the import form).
  Falsifier F-L7-1: a ℚ(√19) quadratic with odd 19-exponent disc (inert-form at a split
  prime kills the law's typing); F-L7-1b: no 19-content at all (saturation clause fails).
- PRED-L7-2 (H-R2's clock, sealed separately at sha 0982d8e0…): clock = 36 — EQUALITY,
  because ord(A₁ mod 19) = 9 is odd so the split-−I kill is vacuous.
- Z₇ banked blind (no value predicted; the Z-ladder question is data).
- Parity traces banked exact per the ladder convention.

**Procedure:** the B600 engine unchanged; runner = level5_run.py pattern with Level(7)
(the level-6 lesson: grep-verify `Level(7` before launch); k ≤ 6 banked-ladder
reproduction is the in-run hard stop (incl. L6's Z=+1, ord 18); blind bank to
outputs/level7_*; exact-Z by integer reduction mod Φ₂₂₈; orders at dps 50 (105-dim odd
block eig — the heavy step); minpolys ≤ deg 16 with the Bantay bound φ(228)/2 = 36 noted.
Runtime estimate: S-pairs 26,796 × 51,840 (~4× level 6); mp block work on dim 105.
