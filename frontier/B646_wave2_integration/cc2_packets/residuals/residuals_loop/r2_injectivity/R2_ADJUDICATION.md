# R1 (level 7, kappa=19 SPLIT) + R2 ADJUDICATION — BANKED (cc2, 2026-07-16)

R1 OUTCOME A on both sealed clauses + one discovery:
- PRED-L7-1 (split form): CONFIRMED — no sqrt(19) import (flag False; inert-form absent at
  a split prime as the law types it); the 19-content appears as CUBICS with 19^3 leading
  coefficients and 19^8 even-power discs = the cubic subfield of Q(zeta_19): cyclotomic/
  split-kernel form. The splitting law now holds at BOTH prime types predictively.
- PRED-L7-2 = H-R2 (sealed sha 0982d8e0, prediction clock=36): CONFIRMED PREDICTION-FIRST
  — ord(B_odd) = 36 = ord(A1 mod 228), equality (certified 2.6e-49). THE MECHANISM STANDS:
  the odd-block rep kills central -I at split primes/deep 3-powers; the kill halves a
  component iff that component's <A1> has even order; at 19 (order 9, odd) it is vacuous.
  Injectivity locus explained: {3,4,5,7} equality, {2,6} proper (the k=2 mod-4 anomaly
  stays flagged). Proposed L77/LAW-MAP upgrade text included below.
- DISCOVERY: Z_7 = 2 — the first ladder value outside {0,1}; Tr(Theta rho) = 0 EXACTLY
  (zero-poly certificate) => (tr_odd, tr_even) = (+1, +1): both sectors carry the unit at
  the split prime. Ladder now {+1,+1,+1,0,+1,+1,2}; parity-hop gains a fourth state
  (odd,odd,even,null,even,odd,BOTH). residual-hint: is Z_k counting unit-carrying sectors,
  and why does split 7|14 give odd-only while split prime 19 gives both?
Gates: full k<=6 ladder reproduced in-run (incl. L6's Z=+1/ord 18); MB12 (231/21/105) as
sealed; seals in outputs/L5_SEAL.txt (PREREG_L7 0c4df92d, runner 70a41662).
