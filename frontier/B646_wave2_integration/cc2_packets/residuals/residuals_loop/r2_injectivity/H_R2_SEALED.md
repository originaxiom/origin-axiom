# H-R2 — the injectivity-locus mechanism (SEALED before R1 unblinds; cc2, 2026-07-16)

DATA (banked, k<=6): kernel elements A1^clock mod ord(T_k) computed exactly:
k=3,4,5: I (faithful on <A1>); k=6: (I mod 4, -I mod 27); k=2: (A1 mod 4, I mod 3,
-I mod 7); k=1 degenerate (dim-1 block).

THE MECHANISM HYPOTHESIS (sealed): the odd-block representation kills the central -I of
specific CRT factors of n = ord(T_k) — observed kills: -I at the SPLIT prime 7 (k=2) and
at the deep twist power 27 (k=6); inert primes' -I (5, 17) and moderate factors survive.
The clock halves a component exactly when that component's cyclic group <A1 mod p^a> HAS
EVEN ORDER (so contains -I); a killed -I at an odd-order component changes nothing.
ANOMALY FLAGGED (not explained): the whole mod-4 factor dies at k=2 only.

THE SEALED k=7 PREDICTION: n = ord(T_7) = 228 = 4*3*19; ord(A1 mod 228) = 36;
19 is SPLIT (19 = 1 mod 3) so its -I is killed under the hypothesis — BUT
ord(A1 mod 19) = 9 is ODD, the cyclic group misses -I, the kill is vacuous:
**clock_7 = 36 (EQUALITY RESTORED).** Falsifier: clock_7 = 18 (a 19-side halving would
refute the odd-order-vacuity logic); clock_7 = 12 or other → mechanism wrong entirely.
