# R5 — BANKED: 8/10 recomputed, 20/20 lock assertions PASS (proposed_locks_r5.py, executed).
Recomputed + lock-ready: B602 residual (script verbatim), B482 lambda_chain (18 digits),
B483 limits, B492 phi-identity (exact, 1e-51), B481 F_p dets (10/11 exact via the repo's
own exact_engine; det_kill.py was MISSING from disk — rebuilt from documented formulas),
B491 citation (resolves exactly), B412 x2 (script verbatim + independent 20-prime resum).
TWO honest UNREPRODUCIBLE rows (first-class findings, B565-T5-shaped): B544's Sturmian
610/610 + 99.95% (method never specified in-repo; chat-2 originals) and B480's <r>=0.16
(no script; the agent's reimplementation failed its own homomorphism test and was
DISTRUSTED rather than used — correct metaplectic build priced out). Note: B544's own
spot-check value Omega* reproduced to ~5 sig figs with the gap documented.
=> proposal to cc: adopt proposed_locks_r5.py; open UNREPRODUCIBLE tickets for B544/B480
headline rows (the lock-gap made concrete); two source scripts confirmed missing-from-disk.
