# R4 — BANKED: THE KILL. Both sealed predictors (Perron root; rotating-pair length-sum)
fail the promotion threshold on 8 fresh out-of-sample substitutions AND flip sign
(rho = -0.214 p=0.61; rho = -0.556 p=0.15 vs banked post-hoc +0.89/+0.93, n=6).
V2b's lead dies; corpse #19's mechanism stays open with one fewer candidate. Seal
verified (predictions file hashed pre-computation, sha 96c005cc, mtime-checked).
Design note kept: the predictors were deliberately decorrelated (within-set rho 0.52) so
the test could distinguish them — it killed them both. residual-hint: the measured order
is data for any future mechanism candidate; not a 1-parameter monotone law.
