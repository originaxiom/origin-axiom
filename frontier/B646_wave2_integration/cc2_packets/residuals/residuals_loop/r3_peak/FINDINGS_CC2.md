# R3 — BANKED: MECHANISM-CANDIDATE (cc2 verify note: gate reproduced banked depth-13/14
values exactly at 0.0 diff; sealed-list discipline held incl. two pre-data refinements).
The middle-organ box-dim feature = a PLATEAU on kappa [0.8, 1.5], top kappa* = 1.1 ± 0.3,
cliff onset (1.5, 1.8]. Of the three sealed diagnostics: MST-gap/diam has a >4x
non-monotone hump maxing at kappa = 1.2 — ADJACENT to kappa* => the peak is
GAP-DOMINATED FRAGMENTATION; gamma is monotone through it (not aligned); liftoff is
featureless-linear (its 1.6 "inflection" = an unequal-step artifact, debunked in-run).
Depth-15: ranking preserved, separation grows (+0.0853 -> +0.0955) — PEAK-ROBUST one
level deeper again (absolute drift remains the known box-count bias; claim = ranking/
separation only). residual-hint: one organ or two — gamma's lone inflection (1.4) vs the
gap max (1.2) are 2 steps apart inside the plateau; a depth-15 FINE grid decides.
Artifacts: residuals_loop/r3_peak/ (script, 128-line output, results.json).
