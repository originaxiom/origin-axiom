# B1066 — LANE III, STAGE 1: THE RELATIONAL NOMINATION (design-only, data-blind, zero spend)

**PREREGISTRATION — DRAFT (seals after the desk bank; shasum → SEAL_LEDGER →
commit → push both → then the STAGE-1 computation, which touches NO measured
data). Stage 2 (execution against measurements) is **PRE-ELECTED by the owner
(2026-08-13: "stage 1 and 2 are priority now") CONDITIONAL on Stage 1
landing S1-DESIGNED with positive net priced strength — it then fires
without a second ask. If Stage 1 lands S1-WEAK or S1-NOT-POSABLE, Stage 2
HALTS and the decision returns to the owner with the number.** The L156 quarantine (the
audit seat's exploratory file with measured values) HOLDS through all of
Stage 1.**

## §0 — The lane and the license

THE WHY CAMPAIGN's Lane III — the relational bridge, the licensed arrow-shape
no crossing has used: **the object's coupling law forces RELATIONS among
received values; a crossing nominates a relation, not a constant.** The
licenses, all banked: the observer-coupling reorientation (values live in the
coupling); B1011's closed form (the tones, the period-5 law, the mirror law —
THEOREMS); the type law (B1032: crossings target relations or finite labels,
never generic reals — this lane is type-legal by construction); the anchor
rule (R11); the channel separation (B1016: the coupling channel is forced,
listener-invariant, anchor-free — and NEVER base-rate-tested as a channel;
this cell is its first test-of-strength).

## §1 — The channel declaration (R10)

**The COUPLING channel** (B856/B1011), not the pair channel (B936,
frame-relative). No channel-mixing: nothing here touches the Hermitian-pair
data, D₂, or the twist-norm law. The menu is the value-set registry's
(B1032), each with its own base rate: the tones (5) = |χ_{V₂(2I)}|/2; the
mirror (8 magnitudes); **the listener family (3): {(1−1/√5)/2, (1+1/√5)/2, 1}
— a character-RING element**; the phases (ℤ/3).

## §2-PRE — THE RELATION INVENTORY (the owner's question made this Stage 1's first computation)

**Before any nomination: enumerate "the rest."** From B1011's closed form
(every forced coupling = a character value × a trace, over the four menus),
derive the COMPLETE set of forced relations, exhaustively, as exact algebra:

- **I1 — intra-menu identities**: all polynomial relations over each menu
  (the listener family's sum-to-1; the tones' pairwise ratios and any
  vanishing combinations; the mirror-8's internal structure; the phases'
  ℤ/3 relations) — computed by elimination over each finite menu, not
  recalled.
- **I2 — cross-menu relations**: identities the tensor structure forces
  BETWEEN menus ((E₆ char)⊗(E₈ spin) ⊕ (E₆ spin)⊗(E₈ spin) — which tone
  is which mirror combination; the |h|² family's expression in tones).
- **I3 — the one-anchor conditional tier**: for each menu member X, the
  full list of values forced for every other observable GIVEN one measured
  datum identified with X — "measure one, the law forces the rest,"
  computed as a table, its width the tier's honest predictive count.
- **I4 — the completeness statement**: the inventory declares its
  generation method (elimination ideals over the menus' defining fields)
  so "every relation" is a checked claim, not a hope. MB12: I1–I3 can
  each return EMPTY-beyond-known — itself a finding (the law's full
  content = the known identities, no more).

**The nomination below then re-derives from the inventory**: if I1–I3
surface a sharper or better-priced relation than §2's, the INVENTORY's
choice supersedes (a pre-committed override — the design cannot cling to
the remembered candidate). The pricing (§4) runs over the inventory's full
denominator. **The junction fence carried verbatim (B1016): "the rest"
means the coupling channel's own observables ONLY — the Hermitian/value-
layer channel is SEPARATE and no relation here transfers to it. The
LISTENER MAP (which physical apparatus is which u) is NAMED as the residual
beyond this cell — the kind table is its coarse form; the fine form is a
separate, harder cell, registered not attempted.**

## §2 — The nominated relation (primary) and its form

**THE GOLDEN SUM RULE (anchor-free, the sharpest type-legal form):**

> The listener family's two nontrivial members are exactly complementary:
> **(1−1/√5)/2 + (1+1/√5)/2 = 1**, an identity of the character ring.
> The nomination: there exists a kind-licensed measured pair (s_i, s_j)
> with **s_i + s_j = 1 within the sealed window AND {s_i, s_j} matching the
> menu pair {(1∓1/√5)/2} within the sealed window** — the CONJUNCTION, so
> the test is two-sided: the relation (sum) and the menu (membership),
> both required. A sum-only hit off-menu is recorded as PARTIAL-SHAPE,
> never as the nomination confirmed.

Secondary (menu-membership only, no relation): any kind-licensed measured
value matching a menu member — enumerated, Šidák-priced, graded strictly
below the primary per R9.

**Uses ZERO anchors** (a pure relation + membership test). The one-anchor
"measure one, predict the rest" tier is NOT nominated at Stage 1 — with a
two-member family it adds no content beyond the sum rule; it becomes
available only if the kind analysis (§3) licenses a LARGER family.

## §3 — THE IDENTIFICATION — the cell's core work, and the honest hazard

**The hazard, named first:** B856's one prior SM reading (JUNO) was REFUTED
ON KIND — the identification of measured quantity with coupling value is
where this channel has already died once. Stage 1's principal computation is
therefore the KIND ANALYSIS, run entirely on banked structure:

- **K1 — and the banked table already answers it (design-time read,
  2026-08-13)**: KIND_TABLE Part 1 licenses the pairing EXACTLY:
  *"sin²θ_ij (mixing) | probability | admissible partner: |h|²-type ONLY"*
  — and the |h|² menu {1/(φ√5), φ/√5, 1} IS the listener family
  identically: 1/(φ√5) = (√5−1)/(2√5) = (1−1/√5)/2 and φ/√5 = (1+1/√5)/2
  (exact; the cell re-verifies the two identities symbolically as its
  entry gate). Part 2's RG row: PMNS/CKM angles run negligibly below the
  GUT scale — *"best open lane — matches the coupling channel's admissible
  kinds"* (R2 satisfied by the table's own taxonomy). **The B856/JUNO
  death differs structurally by this very row**: that reading paired
  sin²-type (probability) with TONE-type (amplitude-part, [−1,1]) — a
  kind mismatch the table now forbids; the present nomination uses the
  |h|² family, the table's ONLY licensed partner. K1's remaining in-cell
  duty: confirm the table rows are quoted faithfully and the identities
  hold; state the parametrization convention the probability-kind assumes
  (the PDG-standard sin²θ_ij as |U|²-derived quantities, declared as the
  scheme row of the twelve).
- **K2**: if K1 licenses a class, derive whether the banked structure
  FORCES a specific pair a priori (a structural selector: e.g., the pair
  transforming as the object's P-pair under the banked basis
  identification). If yes: the nomination is SHARP (one trial). If no: the
  nomination is the FULL ENUMERATED FAMILY of licensed pairs with
  Šidák/Bonferroni over every pair, sector, and convention variant —
  selection-immunity by exhaustion.
- **K3** (now expected NOT to fire, since K1's license is banked —
  retained as the fail-safe): if the table-quote check fails, Stage 1 lands **NOT-POSABLE** — recorded
  as a real finding: the coupling channel has no kind-legal SM contact,
  and the type law's last value-adjacent door closes at the kind gate.
  (MB12: this outcome is live, not decorative.)

## §4 — The pricing (Stage 1's deliverable)

All declared freedoms priced, from scratch (the B1029 shelf's 1.585-bit
figure is superseded by this fuller enumeration): the shelf's candidate
count; the four menus; pair-selection freedom (if K2 returns no selector);
sector and convention variants; the window width itself (priced by the
precision-budget rule R6, N ≈ 1.43·d·log₁₀(H)). **The deliverable is one
number: the nomination's net priced strength in bits after look-elsewhere**,
published to the owner BEFORE any Stage-2 election. If the pricing eats the
candidate's bits, Stage 1 says so plainly and Stage 2 is expected NOT to be
elected.

## §5 — Contamination, declared honestly

This bench cannot unknow approximate textbook mixing values. The blindness
that matters is SELECTION-blindness, achieved by construction: the relation
and menu are exact banked identities; the identification is decided by the
kind table and the structural selector (K2), or else by full-family
exhaustion; the windows are set by the precision-budget formula, not by
proximity to remembered values; and no measured number appears anywhere in
this prereg or in Stage 1's computation. (The sweep's priming-immunity
precedent: exact-match and full-enumeration tests cannot be steered by
expectation.)

## §6 — Both checklists (the reconciliation's rule)

The prereg fills R1–R11 AND the Input-Completeness twelve, N/A written as a
row each — R1 (outside B687's atlas and B743's tower: yes, the coupling
family was never in either — B1016); R2 (RG: the relation targets
RG-insensitive dimensionless mixing moduli — K1 must confirm kind-level);
R4/R6/R7/R8/R9 as above; R10 declared (§1); R11: zero anchors. Items 1–12:
scheme (the parametrization convention, declared per K1), scale (N/A-by-
construction for moduli — stated as a row), uncertainties/multi-modal/
fit-vs-direct/look-elsewhere/matched-null (the null: pairs drawn uniform on
the licensed family's simplex — matched to the criterion's measure)/MB13
grep/firewall (this cell asks the object for RELATIONS the law forces —
licensed)/source-freshness (Stage 2 only, with the B1063 rule)/sealing.

## §7 — Outcomes (Stage 1's own)

- **S1-DESIGNED**: K1 licenses, K2 selects or enumerates, the pricing lands
  a positive net strength → the armed nomination publishes; Stage 2 waits
  on the owner.
- **S1-WEAK**: designed but the pricing eats the bits → published as weak;
  Stage 2 expected unelected; the channel's base-rate question answered
  "not with this candidate."
- **S1-NOT-POSABLE**: K3 fires → the kind gate closes the lane; L156's
  quarantine question dissolves (there is nothing legal to be primed FOR);
  the type law's completeness sharpened.
- **S1-BLOCKED**: a needed banked datum is missing → typed debt row naming
  it.

## §8 — Prior, declared

The value layer is 0-for-4 and M0 is the standing default; the coupling
channel has never been base-rate-tested. **Prior: S1-DESIGNED-but-WEAK-
leaning** — the kind gate has killed here before, and the honest expectation
is that pricing is unkind to a two-member family. Held loosely; the cell's
value is the NUMBER either way. This bench pre-commits to publishing the
pricing unspun.

## §9 — Fences

Gate 5 (nothing to CLAIMS.md from this cell); the L156 quarantine HOLDS
(nothing from the audit seat's exploratory file is read at any point in
Stage 1; Stage 2's design does not change even if that file is later
opened — the seal is the selection); the B1063 fetch-currency rule governs
Stage 2 only; cc3 recusal not required (no registered stake in Lane III),
but their post-seal structural review is welcome; the digest and B1065
proceed unaffected.

---

## APPENDIX A — STAGE 1 EXECUTED (exploration mode per the owner's election; the results ARE the design, sealed here)

**The inventory (I1–I2, exhaustive within the declared scope — sums and
squares to unit target, arity ≤ 3, ratios to φ-powers; sympy-exact, the
computation reproduced in `b1066_inventory.py`):**

- **M = T ∪ T/2 exactly** (the mirror menu is the tones union their halves —
  one menu, two dressings).
- **Unique sum-to-1 pair, any menu**: the listener pair {(1∓1/√5)/2}. No
  tone pair or triple sums to 1 linearly.
- **Unique Pythagorean triple, any menu**: the tones (1/(2φ), 1/2, φ/2) —
  squares sum to 1; internal ratios (φ, φ, φ²); equivalently THE
  φ-GEOMETRIC ROW, and conversely any unit-norm φ-geometric triple is
  forced to exactly these values (a²·4φ² = 1).
- **Typing**: on a unitary row, squares-sum-to-1 is unitarity —
  target-trivial; **the φ-geometry is the extra constraint unitarity
  leaves free** (§2.1-criterion shape, parameter-free). For the listener
  pair on two sin²θ's, the sum is target-NONTRIVIAL.

**The two candidates (both inventory-forced by uniqueness — selection-blind
by exhaustion):**
- **R-A**: two licensed sin²θ's equal {(1−1/√5)/2, (1+1/√5)/2} (membership
  implies the sum; sum-only near-misses = PARTIAL-SHAPE, demoted).
- **R-B**: some row or column of CKM or PMNS has sorted moduli exactly
  (1/(2φ), 1/2, φ/2) ≈ (0.30902, 0.5, 0.80902).

**The pricing (two seeds, stable to 2 decimals; matched nulls: Haar-row
Dirichlet(1,1,1) for R-B, declared-conservative U[0,1] for R-A; look-
elsewhere: 15 pairs / 12 vectors / ×2 portfolio):**

| ε | R-A net bits | R-B net bits |
|---|---|---|
| 0.001 | 12.2 | 10.3 |
| 0.005 | 7.3 | 5.6 |
| 0.010 | 5.4 | 3.5 |
| 0.020 | 3.4 | 1.5 |
| 0.050 | 0.7 | −1.1 |

**Contamination event, declared**: while typing target-triviality the bench
briefly recalled rough textbook values in private reasoning; stopped,
unreported, unused. Containment: both candidates are unique-by-exhaustion
(no selection existed to steer); windows are formula-set. Recorded per the
honesty rule rather than silently.

## APPENDIX B — THE SEALED STAGE-2 DECISION RULE (no freedom remains at fetch time)

1. Fetch the current PMNS global fit and PDG CKM moduli under the
   fetch-currency rule (release stated; arXiv mirror checked; tables pasted
   verbatim into the packet before any comparison).
2. **Windows: ε_i = 2σ_i per quantity from the fetched table** (asymmetric
   errors: the larger side). No other window is computed.
3. **R-A test**: all 15 sin²θ pairs; hit = both sorted members inside their
   2σ windows of the targets. **R-B test**: all 12 sorted modulus-vectors;
   hit = all three inside. Report EVERY trial's distance, not the best.
4. **Evidence = the Appendix-A curve read at the realized ε** (per-quantity
   ε's combined conservatively at the largest); Šidák already inside the
   curve. Verdict vocabulary: HIT (positive net bits) / PARTIAL-SHAPE
   (R-A sum-only) / MISS. Published unspun, whatever lands.
5. The refresh is one-shot (the B1063 precedent): this fetch is the
   adjudication; any future re-pose is a new arc under a new seal.
