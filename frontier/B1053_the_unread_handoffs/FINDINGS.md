# B1053 — the handoffs I wrote into but had not read

**Status: banked (frontier). A gap in this refresh's own audit, found by the owner, and what
closing it turned up. No mathematics asserted or disturbed; Gate 5 untouched; nothing to
`CLAIMS.md`.**

---

## The gap, first, because it is mine

**B1052 wrote a handoff into `docs/handoffs/` after opening twenty-two lines of one of the two files
already there — purely to copy the seat-to-seat convention. Neither was read.**

The campaign's own step is *read every doc in `docs/`*. That directory holds
**`NEGATIVES_HUNT_HANDOFF_2026-07-21.md`**, an **owner-directed full-repo scrutiny brief** —
*"scrutinize the whole repo — is everything calculated properly, have we made mistakes?"* — whose
remit overlaps this refresh's directly, and **`PHYSICS_PATHFINDER_PROMPT_2026-07-21.md`**, its
declared companion.

**The gap surfaced because the owner asked whether the handoffs were mine. Not because I checked.**

---

## What reading them found

### 1. The hunt ran — stated plainly, because the opposite conclusion was available

**B742** (P1: 213 banked negatives triaged → 162 true → 33 P1, two revivals) · **B745**
(cross-verified both) · **B754** (P2, the spectral face) · **B765** (P3, depth) · **B770** (the
closure census). *A standing owner directive left unexecuted would have been the more dramatic
finding, and it would have been wrong.*

### 2. Its **P4** stratum has no arc

The handoff names four strata and describes the last as *"the early era (pre-B300) … Oldest = least
anatomy = **highest prior of a buried positive**"*. **B770's census summarises the execution as
"P1–P3".** No arc's claim line claims an early-era stratum.

**Pre-B300 is exactly the region B1050/B1051 just closed — on a different axis.**

### 3. The finding that lands on my own arc

> **The six arcs B1050 restored as a WALL were structurally invisible to that hunt.**

The hunt selected **banked negatives**. **B19, B21, B28, B30, B34, B35 all carry
`arc_verdict: PROVED` over bodies reading `STALLED`**, and their atlas statuses are `banked` or
`open` — **never `dead`, the hunt's primary hunting ground**. Neither selection surface could see
them, and **none of the six appears anywhere in the hunt's arcs**.

**This is L166's defect with a far larger consequence than L166 stated.** L166 priced it as reader
confusion — someone tracing B16 meets `PROVED` over a body saying `STALLED`. **The real cost is that
fourteen arcs were skipped by a repo-wide owner-directed audit**, because both surfaces it selected
on say something other than *negative*. **Registered in L166; it materially strengthens option 3
(repair the metadata), which remains the owner's call.**

**And the hunt knew the shape first:** B742 flagged **"51 atlas-miner mis-classes"** from its own
status heuristic. The same defect family, from the other side, three hundred arcs earlier — this
refresh's recurring lesson (*a record describes its own era*) arriving once more.

### What this does **not** claim

**It does not claim the wall is wrong.** B1050's six are `STALLED`, not kills, and the row's scope
is *"does not **derive** the selector"* — a statement about routes tried, which is exactly the
posture the pathfinder brief's hatch table asks for. **What is claimed is that the repo's own blade
for pre-B300 negatives has never been applied to them, and the row now says so.**

---

## Four attempts to prove a negative, and the fourth was to stop

Establishing *"P4 has no arc"* took four tries, and the sequence is the transferable part:

| attempt | pattern | what it wrongly matched |
|---|---|---|
| 1 | directory names containing `p4` | `B673_loop4_integration`, `B760_qp4_closure` |
| 2 | the strata **language** | `B401` (*"P4 — the exact-identity dossier"*), `B570` (*"AP4 — …"*), `B737` (*"P4 — KMS"*) |
| 3 | `P4` inside the hunt's own arcs | the hunt **cites B737's P4** |
| 4 | **stop pattern-matching** | — |

> **`P4` carries at least four distinct meanings in this corpus**, plus two filename collisions.
> **That is the `E1` vocabulary-drift class — the error this refresh named as its most expensive
> recurring one — arriving inside the arc that reports it.**

**So the claim does not rest on a regex.** It rests on two documents saying so directly: the handoff
**names** a fourth stratum, and the census summarising the execution **writes "P1–P3"**. The four
false-positive families are named in the checks so the narrowing is auditable rather than a silent
retreat.

*Third over-broad pattern in two arcs — the metallic fingerprint (B1051), the directory regex, and
this. Each time the fix was the same: **ask the precise question, and test both directions.***

**Provenance.** `verify.py` (30 checks) · `tests/test_b1053_unread_handoffs.py` ·
both handoffs read in full · `OPEN_LEADS` **L166** (the invisibility consequence) ·
B1050's `LAW_MAP` row (the unapplied-blade rider) · the B1052 handoff (the gap, owned).
