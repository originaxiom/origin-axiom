# Metric/descent audit: preserve the witness, scope the uniqueness

Independent exact reproduction and the unchanged upstream locks passed after
integration of a8fd2460. The original producer/findings are retained.

The solved metric equals the inverse Bourbaki Cartan matrix already built in
B970 `exotics_levi.py`; root norm 2 is imposed by B1252's solve, whereas weight
norm 4/3 is an independent check. Thus the computation survives but the
repository-wide novelty and “neither imposed” phrases need these limits.

The descent loop returns its first hit, not an exhaustive count. A new sealed
finite-box check on its fixed A2+A1 centralizer finds two normalized
SM-histogram directions. Uniqueness inside a *specified SU5* is a different,
narrower claim. No existence result is retracted.

The published witness now also passes actual color/weak matrix-action and
anomaly-trace checks. A cubic-preserving Weyl element explicitly connects
B1250's grading to the physical audit's conditional classical vacuum.

Evidence, precise scope, run and code:
`reports/physical_bridge_2026_09_05/UPSTREAM_AUDIT.md`,
`upstream_first_run.json`, `upstream_check.py`.
