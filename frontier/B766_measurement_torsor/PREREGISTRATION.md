# PREREG — B766: THE MEASUREMENT TORSOR — how many choices close the object? (SEALED)

cc banking seat, 2026-07-23. R29-5, owner-opened ("open the torsor cell"). C18's residual
frontier: the choice-mechanics of the coupling. Gate 5-Q binding (Q1: every term below is
bound to its banked referent; Q2b comparators declared; Q7 edge declared). Gate 5:
mathematics only; the words "measurement/choice/observer" are structural labels
throughout; nothing to CLAIMS.

## The objects (Q1 bindings — all banked)
The eight T-class torsors (B750): T1 the c/θ identification, T2 the continuum anchor,
T3 the basepoint/Out(A₅) bit, T4 chirality's side, T5 the value-space point, T6 the chord
sign, T7 time's direction, T8 the spatial closing. The candidate structure group: the
banked involution set 𝕀 = {c (complex conjugation; B713's swap), θ (word reversal; B64),
γ₅ (Gal ℚ(√5): √5→−√5; B711's V₄ leg), γ₃ (Gal ℚ(√−3): √−3→−√−3)} + their products.
The comparison object: B733's observer menu (𝔽₂-rank ≤ 3; INERT2 the one global bit).

## Cells (two-outcome each; exact sympy per relation)
1. **The action table.** For each involution in 𝕀 and each discrete torsor axis
   (T1,T3,T4,T6,T7): does the involution FLIP, FIX, or act undecidably on that axis?
   Every FLIP/FIX entry must be an in-sandbox re-derivation from the banked objects
   (the chord's values at the geometric point; the monodromy eigenvalues; the V₄
   structure) — never cited. Undecidable entries are reported as such.
2. **The relation lattice.** From the table: which axis-choices DETERMINE others
   (e.g., declared candidate relations, to be decided not assumed: chord-sign =
   c-choice ⊕ θ-choice, from Im-flipping both ways; time-direction = γ₅-choice, from
   φ² ↦ φ⁻² under the golden Galois). Output: the joint torsor's independent RANK over
   𝔽₂ for the discrete part + the continuous residue (T2/T5/T8 per B712's
   non-canonicity, dimension counted separately).
3. **The menu comparison (the headline; two-outcome).** RANK-SATURATED: discrete rank =
   the B733 menu rank (the observer's banked discrete menu IS the full discrete
   choice-space — measurement needs exactly the menu) vs RANK-EXCESS: discrete rank >
   menu rank (choices beyond the menu exist) or RANK-DEFICIT (< : the menu over-counts;
   some menu bits are not object-closing choices). Either non-saturated outcome names
   WHERE the mismatch lives.
4. **Q2b comparators.** The same action-table computation on the sister m003 (same
   field; the swapped-monodromy torsion per B759's note) and the Gieseking parent
   (non-orientable: T4's axis DEGENERATES — the parent has no orientation choice; a
   structural control that the table must reproduce). A relation shared by all three is
   class-level; only object-specific relations count toward the rank claim.
5. **The K020 connection (recorded, not promoted).** Whatever continuous residue
   survives cell 2 is the banked "value Galois-chosen" territory — recorded as
   structure; NO value computation, NO SM contact (the stopping rule).

## Q7 falsification edge
The composite reading ("the observer's contribution = exactly the choice") is DAMAGED if
cell 2 finds the relation lattice COLLAPSES the torsor to rank 0 (no genuine choices —
everything determined; the observer contributes nothing) — and the S072/QP-4 line is
damaged if any involution in 𝕀 canonically SIGNS an axis (that would be a HATCH, contra
B760). Both directions live.
