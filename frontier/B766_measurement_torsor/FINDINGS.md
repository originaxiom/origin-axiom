# FINDINGS — B766: THE MEASUREMENT TORSOR — three bits close the object; time's arrow IS the golden branch

cc banking seat, 2026-07-23. R29-5, owner-opened. Prereg sealed sha256 c371e18e (Gate 5-Q
battery in-seal; every action-table entry an in-sandbox re-derivation, never cited).
Gate 5; structural labels throughout; nothing to CLAIMS.

## Verdict: RANK-SATURATED — the discrete choice-space has rank EXACTLY 3 = the banked menu

The action table (computed): on the five discrete T-class axes, the banked involution set
𝕀 = {c, θ, γ₅, γ₃} acts by:
- c (conjugation) FLIPS chirality's side (the curve's two geometric roots swap — derived)
  and the chord's sign (its geometric value √3 is purely odd — derived);
- θ (reversal) FLIPS only the chord (SL(2) trace symmetry fixes the rest — derived);
- γ₅ (the golden Galois) FLIPS time's direction ((1−φ)² = φ⁻² exactly — the eigenvalue
  inversion IS the Galois action, derived) and the basepoint bit (Out(A₅) = the 5A/5B
  Frobenius swap, banked B701);
- **γ₃ ≡ c on every axis** (identical columns — the being-Galois IS conjugation on the
  closing axes; it drops as a generator).

The relation lattice over 𝔽₂ (flip-vectors in the (c, θ, γ₅) basis):
- **T7 = T3: TIME'S DIRECTION AND THE BASEPOINT BIT ARE ONE CHOICE** — both are the
  γ₅ branch. Choosing time's arrow = choosing √5's sign = choosing the sister's
  Out(A₅) side. (Vector (0,0,1), twice.)
- **T6 = T4 ⊕ θ: the chord's sign is the XOR of the conjugation and reversal choices**
  (vector (1,1,0) = (1,0,0) ⊕ (0,1,0)) — the chord is not an independent bit.
- The independent generators: exactly **(c, θ, γ₅) — conjugation, reversal, the golden
  branch. Rank 3.**

**The headline: rank 3 = B733's menu rank (𝔽₂ ≤ 3, saturated) — RANK-SATURATED.** The
observer's banked discrete menu is not an artifact of the congruence construction: it is
PRECISELY the full discrete closing set. Measurement closes the object with exactly three
bits.

## The honest residue
- T1 (the c/θ pairing axis) is UNMOVED by 𝕀 — its choice, if genuine, lies outside the
  banked involutions' reach (reported, uncounted; a named door).
- The continuous residue (T2 anchor / T5 values / T8 space) stays non-canonical per B712
  — the banked "value Galois-chosen" (K020) territory, recorded as structure, NO value
  computation (the stopping rule + the pin).
- Q2b: the relations are FIELD/CLASS-level (the sister shares them; object-specificity
  lives in T3's own bit) — stated, not blurred. The Gieseking control PASSES structurally:
  T4's axis exists only downstream of the orientation choice (C5) — the torsor's axes are
  themselves choice-dependent, exactly as C18 states.
- Q7: neither damage direction fired — the rank is nonzero (the observer contributes
  genuinely) and no involution SIGNS an axis (they permute choices; none selects —
  consistent with B760's NO-HATCH).

## The sentence for the chain
The object is closed by exactly three independent discrete choices — conjugation,
reversal, and the golden branch — with time's arrow and the basepoint identified as one
of them, the chord's sign determined by the other two, and the values living beyond all
three in the Galois-chosen continuum. Locks: tests/test_b766_torsor.py (compute-grade).

## AUDIT ADDENDUM (2026-07-23, cc3's independent scrutiny — CONFIRMED, one derivation upgraded)

cc3 re-derived all five action-table entries independently: RANK-SATURATED CONFIRMED
(rank 3 = the menu). One genuine catch against this arc: the θ-on-T6 entry was hardcoded
"definitional" rather than derived — and cc3's first-pass TRACE-level test returned FIX,
because traces are cyclically invariant (θ-even always). The chord sign is a MATRIX-level
observable: d/du[Sym²(AB) − Sym²(BA)] at the geometric point has 6 nonzero entries with
trace exactly 0 (cc re-verified independently at the gate). **The lesson, banked: any
trace-based θ test is blind to the chord — matrix-level observables need matrix-level
derivations.** The hardcoded entry is now backed by cc3's audit_compute.py derivation.
Also upgraded: γ₃ ≡ c is FIELD-THEORETIC (the CM-field theorem), not an axis coincidence;
the T1 door probed against amphichiral τ (central, τ = −I — the door is genuine).
