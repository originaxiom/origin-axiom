# B1104 — THE 4D SUSPENSION SELECTION TEST: NO SECTION — the object's own structures name DIFFERENT fourth dimensions

**Status: banked (frontier). Verdict PROVED (a computed theorem-grade typing over the
complete finite menu). L177, owner-elected same day ("go to the end"). Prereg sealed
before computation. Gate 5 untouched; no physics claim on any branch. Lock
`tests/test_b1104_fourd_suspension.py`.**
Run: `python3 frontier/B1104_fourd_suspension/b1104_selection.py` (~10 s).

## THE PRIZE FIRST

The owner's question was: *can the observer math embed one more layer up — is the
dimension we lack nameable?* The computed answer, on the suspension route, all three
sealed criteria decided:

**C1 — the genesis act cannot repeat (FAIL, informatively).** The object is itself a
suspension by an INFINITE-ORDER act (the Anosov φ = RL). One level up, MCG(M) ≅
Isom(m004) = D₄ — **finite** (Mostow rigidity, CITED). No infinite-order suspension
of M exists: **the tower cannot repeat its founding move in dimension four via
mapping tori.** The criterion is non-vacuous (a torus fiber would pass it) — the
failure is the object's hyperbolicity speaking. Retro-typed: this is WHY the banked
escalator (B556) is a trace-map tower rather than a manifold tower.

**C2 — SELECTION: NO-SECTION (the sealed grammar's third branch).** The object's own
two structural filters select **disjoint** nontrivial classes:

- the **θ-filter** (centrality — compatibility with everything, the amphichiral
  structure included) selects EXACTLY ONE element: the central involution (order 2,
  orientation-preserving, and — the instrument fact below — the unique isometry
  acting TRIVIALLY on the cusp: the level-up hyperelliptic);
- the **tick-filter** (B1083's pattern: orientation-reversing involutions,
  det = −1) selects EXACTLY TWO: the reflections e4, e6;
- **the joint survivor set is EMPTY.**

No canonical fourth dimension exists on this route: the choice of ψ is a genuine
free choice the object declines to make — **Class V (torsor no-sections) acquires
its four-dimensional instance**, with an empty-intersection witness instead of an
elimination argument. The layer-up picture's honest form: *"the dimension we lack is
the choice we are"* is now a computed statement, not a metaphor.

**C3 — the Gieseking analog EXISTS (PASS).** Both tick-selected reflections give
**non-orientable 4-manifolds M_ψ whose orientation double cover is M × S¹** — the
one-tick/double-tick pattern (Gieseking ↔ m004, B1083) verbatim one level up; each
has H₁(M_ψ) = ℤ². The PATTERN replicates even though the SELECTION does not resolve:
one level up, the object still buys orientability with a double tick — it just
refuses to say which tick.

## The complete table (8 elements — printable, per the prereg's method clause)

Orders [1, 2, 2, 2, 2, 2, 4, 4]; conjugacy class sizes [1, 1, 2, 2, 2]; center of
size 2. Per element: cusp matrix, det (= orientation character), order, centrality,
H₁(M_ψ) ∈ {ℤ², ℤ ⊕ ℤ/2} — all in `b1104_results.json`. The two order-4 elements are
orientation-REVERSING (the amphichiral quarter-turns); their suspensions are
non-orientable with H₁ = ℤ ⊕ ℤ/2.

## The instrument fact (recorded, load-bearing for anyone repeating this)

**The cusp action is NOT faithful**: kernel ℤ/2, generated by the central
involution — 8 isometries, only 4 cusp matrices (the image is abelian, ℤ/2 × ℤ/2).
The computation therefore ran on the ABSTRACT multiplication table (SnapPy's
`multiply_elements`), with the element→matrix map SELF-CERTIFIED as a homomorphism
against the full table before any downstream use. A cusp-only enumeration would have
silently halved the group — the assertion that caught this is kept in the script.

## Scope and fences

- This decides the SUSPENSION route only. The FILLING route (B716: fillings exist,
  infinitely non-unique, none canonical) stands untouched and is now the pattern's
  SECOND 4d instance — two independent routes, same no-section shape. Wall #4
  (B770, the chiral 4d lift) declared adjacent, not collided.
- Mostow rigidity and the D₄ symmetry group are CITED-grade inputs (I4); everything
  downstream is exact integer arithmetic on the table.
- No cosmological or physical reading is claimed. The firewalled observation the
  owner's question pointed at — the observer at level n as the object at level n+1
  making the choice level n couldn't — now has its precise in-corpus form: **the
  choice exists at level 4 (three distinguished candidates by two filters), the
  chooser does not (empty joint selection).** What supplies it remains, exactly as
  at every level below, outside the object.
