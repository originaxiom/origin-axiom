# B1032 — FINDINGS: the resolution bound verified, amended, and banked as THE TYPE LAW

**Seat:** cc · **Date:** 2026-08-11 · Verification arc (incoming-results protocol) on the
construction seat's Phase-1b resolution bound, relayed by the audit seat 2026-08-11.
Compute: `b1032_typecheck.py` — the 2T and 2I spin-character tables computed exactly
in-arc (orthogonality sanity |2I| = 120, |2T| = 24), nothing cited where it could be
computed. Quarantine honored throughout: the audit seat's data-contact file remains
unopened on this bench.

## The incoming claim, and what verification did to it

**Claim (the construction seat):** any forced value equals Re χ_V(M)/dim V — a character
value, hence coarse (it resolves the object only to its group data); finite labels exempt.

**Verified against the ENTIRE banked dimensionless coupling inventory:**

- **V1 — the five tones ARE bare normalized character values**: |χ_{V₂(2I)}(g)|/2 over the
  nine classes gives exactly the banked {0, 1/(2φ), ½, φ/2, 1} — **with the census
  30/24/40/24/2**, which is B659's named discriminating clause, reproduced from the class
  sizes, not cited.
- **V2 — the mirror set IS the tensor character menu**: |χ_{V₂(2T)}/2|·|χ_{V₂(2I)}/2|
  gives exactly B1011 C6's eight magnitudes {0, ¼, 1/(4φ), ½, 1/(2φ), φ/4, φ/2, 1};
  **tone(5) ≠ mirror(8)** — two distinct banked value sets.
- **V3 — THE AMENDMENT THE BOUND NEEDS**: the listener family {1/(φ√5), φ/√5, 1} is **not**
  of the bare form — it is (1 ∓ 1/√5)/2 exactly, with **√5 = φ + φ⁻¹, a sum of two
  normalized character values**. The family lives in the **ring generated by character
  data over ℚ(√5)**, not in the bare menu. The bound's universal is amended accordingly;
  its decisive consequence — **a finite algebraic menu, no fine arithmetic** — survives
  unamended.
- The phases arg h ∈ {0, ±2π/3} are arguments of a linear character of order 3 — a ℤ/3
  finite label, inside the exemption by construction.

## The four deaths, typed honestly (correcting the relay's "all four type-wrong")

| crossing | target type | verdict under the law |
|---|---|---|
| B915 | a coarse boundary value (3/8, group data) **RG-dressed into a fine continuous prediction** | the dressing IS the type violation — and is B915's banked named defect ("assumed interpolation"), now with a mechanism |
| B925 | never reached values — **the object's own algebra refused the identification before data** | typing not reached; the honest row |
| B929 | Tier-2: measured continuous ratios | fine arithmetic — type-wrong; the miss carries no information by construction |
| B1027 | **a finite label** ({±2π/3}, a ℤ/3 menu) read on a continuous dial | **TYPE-PERMITTED** — which is exactly why it is the only crossing that could die clean and powered, as it did |

One mechanism sorts all four — and it *predicts* the banked post-mortems: the two
fine-arithmetic deaths were uninformative by construction; the label-shaped death was a
real exclusion.

## THE TYPE LAW (banked; scope fenced)

> **Scope: the dimensionless coupling channel** — the sealed inventory whose closed form
> is B1011's. There, the object's forced outputs live in a **finite algebraic menu** (bare
> character values; the character ring for the listener family). **Therefore a crossing
> may target (i) a RELATION among measured quantities, or (ii) a FINITE LABEL read on a
> dial; it may NOT target a generic real by value.**

**Fences:** dimensionful forced quantities (Vol; the CS = 0 amphichirality label) are
outside; **c = 6σ is outside** (not coupling-channel — the audit seat's own pre-declared
refutation attempt failed on exactly this fence, recorded); other channels need their own
closed form before the law extends. The look-elsewhere companion (the audit seat's
data-free counts: trial space ~14–21; O(1) chance agreements at the 1% level against the
five tones) belongs **inside any future nomination seal**, together with this law's
type-check as a sealed criterion.

**What is type-correct on today's board:** B1029's sum-rule shelf (a relation, 1.585
bits); the trit and the ℤ₆ global form (finite labels); the z6 line-spectrum falsifier
list. The numeric route's four post-mortems compress to one sentence: *they asked the
object for a kind of number it does not make.*

## Verdict: PROVED (bound verified-as-amended; the law banked with its fences)

TERMINOLOGY rows registered this batch: the **two banked "θ-even"s** (the F₄ exponent set
{1,5,7,11} vs B1011's mirror value set — a corpus-level collision, found by the audit
seat's error and this seat's verification of it) and the **value-set registry**
(tone 5 · mirror 8 · family 3 · phase ℤ/3). Gate 5 untouched; no measured value appears
anywhere in this arc.
