# PREREG — B740: THE B288 STRAGGLERS — completing the "no filling re-sees ℚ(√−3)" grid (SEALED)

cc banking seat, 2026-07-21. Hunt-cell #3 (cc2 register crack #1, cc-verified at the code: B288's
line-40 `continue` drops every filling whose DEFAULT solution isn't positively oriented — catching
both genuinely exceptional slopes AND hyperbolic fillings needing retriangulation — BEFORE the
√−3-containment check; line-45 records '>20' unknowns). The banked headline "NO closed hyperbolic
filling re-sees ℚ(√−3)" (the E₆ atom is an open-object property) reports a full-grid closure that
was never computed on ~24/78 slots. THE RE-RUN: for every skipped/unknown slope in the grid
(|p|≤8, 1≤q≤8, gcd=1): (a) classify exceptional (the known 10 of 4₁ = {∞}∪{(p,1): |p|≤4} — out of
the hyperbolic claim's scope, recorded) vs hyperbolic-recoverable (retriangulate/randomize/HP —
every q≥2 or |p|≥5 slope is hyperbolic); (b) for each recovered filling compute the invariant
trace field (sage, prec→1000, degree→24) and TEST √−3-containment; (c) push the '>20' unknowns
harder. Two-outcome: **A** = a straggler filling DOES contain √−3 (the B288 kill cracks — a
revival); **B** = the completed grid RECONFIRMS the kill (no filling re-sees ℚ(√−3)), now actually
computed on the full grid. [Honest prior: B — but the point is to COMPUTE it; an unearned negative
is as bad as numerology.] Pure math; Gate 5; width ledger cell #3.
