# SEALED ADDENDUM — Stage B′ (verdict annotation; changes no verdict, no selection)

cc3, 2026-07-21 (sealed before Stage B/C completion; prompted by the owner's design-scrutiny
question: are the recomputations accounting for the anatomy learned SINCE the kills banked?).

Scope boundary made explicit: B739's RECONFIRMED means THE KILL AS ORIGINALLY SCOPED IS NOW
COMPUTED-TRUE (the P1 question). It does NOT mean "dead against the full current anatomy" —
that is the handoff's P2/P3 strata. To keep the banked record honest and build the next
stratum's sealed input, EVERY RECONFIRMED verdict receives two annotation flags from a
non-authoring checker (opus tier; reads the verdict + recompute artifacts + the kill record):

- **depth_exposure (E22 class):** did the recompute only replicate the original finite
  depth/range while the killed claim quantifies more broadly? If yes: what a stabilization
  proof or deeper sweep would require.
- **anatomy_exposure (P2 class):** does the record's revival_hypothesis name post-B730
  anatomy (V₄ faces B730 / congruence tower incl. the level-(4) refinement B734+ / Out(A₅)
  B732 / Born ledger / bounded observer menu B733 / emittance faces B735) that the recompute
  did not consult? If yes: the specific face(s).

Flags + notes enter the FINDINGS verdict table verbatim; the flagged set = the sealed
candidate list for the P2/P3 arc. REVIVED verdicts are untouched (they already face the
3-skeptic gate). BLOCKED untouched. No verdict may be changed by B′; a checker who believes
a verdict is WRONG writes that in its note — the banking seat then routes that item through
the skeptic lane before banking (logged in the ledger).
