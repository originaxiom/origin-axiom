# PREREG — B723: BUILD THE OBSERVER — the type-III thermal completion of the object (SEALED)

cc banking seat, 2026-07-20. Owner "build it": construct the OBSERVER concretely. From
B721: the object's own von Neumann algebra is TRACIAL type II₁ (trivial modular flow —
an equilibrium, timeless clock). A genuine observer's clock is type III (nontrivial
modular flow). The observer is therefore the TYPE-III THERMAL COMPLETION of the object's
tracial equilibrium, obtained by supplying an EXTERNAL weight/state (the free,
non-canonical choice — B701). This arc BUILDS that apparatus and computes its emissions.
Structural/operator-algebra ONLY; Gate 5 stands; no SM value; the observer's CHOICE is
provably free (B701), so we build the APPARATUS and compute outputs GIVEN a state — never
derive the choice. Web-enabled (Connes–Takesaki flow of weights; Tomita–Takesaki; CMR).

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **The object's algebra + its type (confirm II₁).** Build the von Neumann algebra from
   the object's data (the measure-preserving Anosov suspension B716 / the character-variety
   / the fiber-functor B700). Confirm B721's TRACIAL type II₁ (finite; trivial modular flow
   Δ=1). Two-outcome: A = II₁ confirmed (equilibrium, no intrinsic thermal clock); B = a
   different type (a hidden intrinsic thermal structure).
2. **The type-III completion = the observer apparatus.** By Connes–Takesaki: a type III
   factor ↔ (its type-II_∞ core) ⋊ (a scaling/modular flow). The object gives the tracial
   core; its OWN flow is trivial ⇒ the type-III observer requires an EXTERNAL weight/flow.
   Construct/characterize the crossed product M ⋊_σ ℝ (or the CMR QSM over ℚ(√−3)) as the
   observer apparatus; confirm it is genuinely type III (non-tracial) once a nontrivial
   external weight is supplied. Two-outcome: A = a well-defined type-III observer apparatus
   (external weight ⇒ III); B = an obstruction (no III completion / trivial).
3. **The emissions dictionary — what the observer supplies as a function of the chosen state.**
   Given a chosen KMS state/weight ω (the free datum): (a) its modular flow σ^ω = TIME (B721/
   Connes–Rovelli); (b) its Galois sheet = CHIRALITY (B713); (c) its torsor basepoint = the
   VALUES (B701). Make the DICTIONARY explicit: the observer's free choice (the weight) ↔ the
   SM's free parameters; the object's structure is fixed, the observer's state is the freedom.
   Two-outcome: A = the dictionary holds (each closing = a datum of the chosen state); B = a
   channel doesn't fit (a closing that isn't a state-datum).

## Two-outcome (sealed, campaign)
- **OUTCOME A:** the observer is a CONSTRUCTIBLE object — the type-III thermal completion of
  the object's II₁ equilibrium via an external weight — and its emissions (time/chirality/
  values) are the data of the chosen state; the free choice is explicit and provably free
  (B701). We have BUILT the observer apparatus (not the choice).
- **OUTCOME B:** an obstruction to the construction (no III completion, or a closing that is
  not a state-datum) — the observer is not this apparatus. [expected: A — the construction is
  standard operator algebra; the honest content is that the weight is external/free.]

## Verdict template
Bank the OBSERVER CONSTRUCTION: the apparatus (type-III completion) + the emissions dictionary
(the choice ↔ free-parameters map) + the honest theorem (the choice is free, B701). LAW_MAP
placement; nothing to CLAIMS. This is the constructive inverse of the object.
