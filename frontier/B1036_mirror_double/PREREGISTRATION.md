# B1036 PREREGISTRATION — plan Arc 2: THE MIRROR-DOUBLE SYMMETRIC TEXTURE (sealed before compute)

**Date sealed:** 2026-08-12 · **Seat:** cc · plan Arc 2 (owner-approved). Gate 5-Q: no
measured value anywhere. Type-law compliant (all outcomes structural/finite). Relay
channel checked pre-seal (clear). **This is the cell B632 named and never registered**
(verified by artifact: no cell-3 prereg exists on disk) — the lane's one live door after
B1033, on the space where B308's multiplicity mechanism can first genuinely appear.

## The banked floor (cited, not re-proved)

B632 (PROVED): on the SOLO complement no symmetric mass-matrix-shaped object exists
(O1: cd = 2 kills scalar triples; O2: H²(M;ℂ) = 0 + graded-commutativity force
antisymmetry); the maximal solo structure is the antisymmetric Ω, full; the symmetric
texture is well-defined exactly on a closed 3-cycle — canonically the mirror-double
M ∪_∂ M̄ (H³ = ℂ, B580 G1). h¹(M; 27_ρ) = 3 = the block count (B632; B598's six
per-block 1s; the audit seat's additivity theorem). 27_ρ = Sym¹⁶ ⊕ Sym⁸ ⊕ Sym⁰ of the
geometric holonomy.

## The design (step 0, fixed here so no cell can be vacuous)

**Per block m ∈ {16, 8, 0}, Mayer–Vietoris over M ∪_T² M̄ gives
h¹(dbl; Symᵐ) = 3 − rank(r_m)** for m > 0, where r_m: H¹(M)⊕H¹(M̄) → H¹(T²) is the
peripheral restriction (h⁰(T²) = 1, h¹(T²) = 2 per unipotent block — computed in-cell,
gated). **rank(r_m) ∈ {0, 1, 2} and every value is representable — the multiplicity
question is two connecting ranks, non-vacuous by construction.** The m = 0 block runs
with its trivial-system corrections stated.

## The sealed cells

- **V1 — the hygiene control FIRST**: B632's coboundary-invariance control re-run on the
  corrected chain (the B575 naive-evaluation hazard); **the arc HALTS if it fails.**
- **V2 — the boundary data + MV assembly, exact, per block**: h*(T²; Symᵐ|_∂) computed
  (not assumed); the mirror side built by the banked twisted-double machinery (B639
  precedent; the mirror rep composed with conjugation — sp.conjugate, never subs);
  h¹(dbl; Symᵐ) = 3 − rank(r_m) evaluated exactly. **Banked-number gates on every
  subspace export** (h¹(M; Symᵐ) = 1 must reproduce per block before any dbl number is
  read; B961's law).
- **V3 — the symmetric texture on the double**: the symmetric pairing
  Sym²H¹(dbl) → H²(dbl) → H³(dbl) = ℂ computed on the blocks; **SUPPORT ONLY** (B884's
  fence); the sl₂ triangle rules fix the PERMITTED cells in advance; outcomes per cell
  PRESENT/ABSENT with the forbidden set named before compute (the B632 cell-2 template).
  Existence verdict: the symmetric object EXISTS on the double iff the pairing's support
  is nonempty.
- **V4 — THE MULTIPLICITY READ (the generation-relevant output)**: total
  h¹(dbl; 27) vs the solo 3. **> 3 ⟹ the double carries classes beyond the block count
  — the first genuine multiplicity candidate by B308's mechanism, typed against B1033's
  within-27 dissolve (these are cohomological classes of the DOUBLE, not a decomposition
  of one 27). = 3 (or < 3) ⟹ no multiplicity gain; the honest negative, and the lane's
  last named door closes on this route.**
- **V5 — the companion falsifier**: h¹(M; ad) = 6 computed directly (ad = the six
  Sym-blocks m ∈ {11,8,7,5,4,1}). The additivity theorem predicts 6; **this is that
  theorem's own named falsifier — a value ≠ 6 kills the four-threes dissolve.**

## Hazards, all five pre-registered

sp.conjugate never subs (√−3) · no floats near charges · the cup_on_relator control =
V1, halting · support-only criteria · rref()[1] returns pivot COLUMNS (every export
gated against a banked number). Instruments reused, not rebuilt: B632's Fox machinery,
B639's twisted double, B961's frame.py, B883's rep27.json.

## Declared priors (non-weakening)

- symmetric-texture existence on the double: **lean YES** (B632's own forward sentence).
- **the multiplicity read: NO PRIOR — genuinely open**, stated so; either outcome banks.
- h¹(M; ad) = 6: **lean YES** (theorem-implied), with the falsifier's teeth stated.

## Outcomes

V4 > 3 → the multiplicity candidate registers (a candidate, NOT a generations theorem —
the B1033 (iii)-style fence pre-stated); V4 ≤ 3 → the honest negative and the route
closes; V5 ≠ 6 → the dissolve falls and the audit seat is notified same-day; V3 empty →
B632's obstruction extends to the double (a strengthening, bankable). Every outcome is
a result.
