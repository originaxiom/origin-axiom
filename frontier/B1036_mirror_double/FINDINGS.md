# B1036 — FINDINGS: the double gains classes, not the symmetric pairing

**Seat:** cc · **Date:** 2026-08-12 · **Prereg sealed a10ae240 BEFORE compute**, pushed at
31ccaaa7. Record compute: `b1036_final.py` (one script, end-to-end; the two HALTED earlier
routes remain on disk as the disclosed process record). All exact over ℚ(ω); every
subspace export gated against a banked number (B961's law); Gate 5-Q throughout.

## V2 + the cross-gate — the double's cohomology, twice over

Per block, the MV route (identity gluing; the first draft's (μ,λ)↦(μ,λ⁻¹) error produced
impossible negatives, was caught by the added compatibility gate, and is kept as the
lesson): **rank(r) = 1 per block — the Poincaré–Lefschetz half-lives value, computed not
cited — giving h¹(dbl; V(16)/V(8)/V(0)) = 2/2/1.** The independent cross-gate — direct
Fox calculus on the double's 3-generator, 3-relator presentation ⟨a,b,d | R(a,b), R(a,d),
L(a,b)L(a,d)⁻¹⟩ — **agrees on every block.** Two routes, one answer:

> **h¹(dbl; 27) = 5, against the solo 3.**

## V4 — the multiplicity read: the branch FIRES, with its fence

The double carries **two extra 27-block classes** (one per chiral block, seam-born — the
H⁰(T²)-connecting contribution). **B308's multiplicity mechanism is LIVE for the first
time in the record: a banked operation that increases the class count.** The fence, as
sealed: a candidate, NOT a generations theorem — and the honest number is **5 = 2+2+1,
not 3**. The count the coupling move produces is one-extra-per-chiral-block; any
generation reading would need a further mechanism and is not claimed.

## V3 — the symmetric texture: the obstruction EXTENDS through the seam

Three routes, in order of what they taught:
1. The 3-relator corrected-chain generalization **HALTED at its own sealed control**
   (coboundary contamination) — the presentation complex's H² is not the manifold's.
2. The per-side scalar (Bth-contracted) route **HALTED the same way — and the diagnosis
   is a finding: the scalar route's per-side vacuity IS B632's O2 restated**
   (H²(M;ℂ) = 0 kills every scalar class before any pairing is read).
3. The record route: **the seam-sector pairing** — the double classes' direct T²
   restrictions, cup through the holonomy-invariant Bth form (invariance re-verified per
   block), gauge-control PASS — and the verdict is total:

> **The symmetric-part support is EMPTY — every cell, every block, including the two
> seam-born classes.** The one sector the closed double newly opens (H²(T²) ≠ 0) carries
> no symmetric mass-shaped pairing. **B632's solo antisymmetry wall extends through the
> seam sector of the double.**

**Scope fence:** the full V-valued (C3-route) pairing assembled over the double was not
computed (both attempted broader routes halted); it is the named residual, with the
honest note that its per-side content is exactly B632's banked cell-2 and the seam
sector — the only new scalar room — is now closed.

## V5 — the additivity falsifier: PASSES at exactly 6

h¹(M; ad) = 6 = the audit seat's block-count theorem's prediction, computed on the six
banked adjoint blocks through this arc's independent Fox pipeline. **A never-banked
number, predicted by the theorem, confirmed — the four-threes dissolve survives its own
named falsifier.** (Same banked matrices, independent code path; stated as such.)

## Priors scored (non-weakening)

- existence lean YES (B632's forward sentence) — **NOT REALIZED in the computed sectors**:
  the seam is empty and the scalar route is O2-vacuous; the lean survives only into the
  unrun V-valued residual, and is so recorded.
- multiplicity NO PRIOR — outcome **5**, banked as computed.
- ad = 6 lean YES — **HELD**.

## What Lane II holds after this arc

The coupling move (the double) **does** create multiplicity — the first mechanism in the
record that does — and **does not** create the symmetric texture where it newly could.
The mass-shaped object remains antisymmetric-only in every computed sector, now including
the seam. The lane's residuals, named: the V-valued double assembly (the one unrun
sector) and B298's cubic-carrier direction. The two-route agreement (MV = double-Fox,
blockwise) is the arc's method credential.

## Verdict: PROVED (all five cells decided; two sealed HALTs disclosed; the wall extended)
