# B1141 — THE SPIN PAYMENT: the object's own beat selects the spin structure; the last free discrete bit is assigned, not free (cloud memo 28, hostile-verified two-bench)

**Status: banked (frontier). Verdict PROVED (the three load-bearing claims independently
confirmed, hostilely, by two engines). Harvest arc — the cloud seat's twenty-eighth memo
(THE SPIN PAYMENT), re-derived on THIS bench with own code (own relator search, own exact
intertwiner-rank computation, own norm-form + χ-parity proofs; SnapPy + exact-symbolic +
50-digit-numeric, three independent engines), nothing imported from the cloud cert. The
selection HOLDS. Two non-load-bearing documentation errata caught and relayed. Cloud seat
credited. Gate 5 n/a (pure topology/algebra, no SM value). Lock
`tests/test_b1141_spin_payment.py`.**

## The question memo 28 answers

The discrete freedom ledger closed (B1118) to exactly {C, P, spin lift}. The **spin lift** —
the choice between the two SL(2,ℂ) lifts of m004's holonomy, i.e. the two spin structures
(H¹(M;ℤ/2)=ℤ/2, since H₁=ℤ) — was the observer's *last genuinely free* discrete purchase,
and the load-bearing one: **fermionicity and chirality both hang on it.** B1122 proved no
coupling can read it; memo 20, no collision geometry sources it. Neither no-go touched
**extension-consistency with the object's own ℤ/2.** Memo 28 does — and it takes the bit off
the observer's counter.

## The theorem (each step re-derived independently, CONFIRMED)

The object's holonomy A=[[1,1],[0,1]], B=[[1,0],[−ω,1]], ω=e^{2πi/3} (a root of **x²+x+1**,
the trace field ℚ(√−3)). The relator `abABaBAbaB` holds exactly; SnapPy confirms m004=4₁,
Vol=2.02988…, H₁=ℤ → exactly two spin structures. The **relator census**: R(A,B)=+I and
R(−A,−B)=+I, while R(−A,B)=R(A,−B)=−I — the two lifts, differing by χ(a)=χ(b)=−1.

1. **The intertwiner space is EXACTLY 1-dimensional** (the crux). The full linear system
   W·conj(g) − ρ(beat(g))·W = 0 (g∈{a,b}) — 8 complex equations in the 4 entries of W — has
   **exact rank 3, nullspace dimension 1** (Gaussian elimination over the number field),
   cross-validated by 50-digit complex SVD (4th singular value ~7.6×10⁻⁵¹). Every beat
   implementation is a scalar λW₀.
2. **N(λ) ≥ 0 — the twisted lift is impossible.** The base W₀ satisfies all four beat
   equations exactly (W₀·conj(A)·W₀⁻¹=+A, W₀·conj(B)·W₀⁻¹=+ρ(beat(b)), W₀·conj(W₀)=+A,
   det W₀=1). For any λ∈ℂ, (λW₀)·conj(λW₀) = |λ|²·A — so a *twisted* self-consistent square
   would need |λ|² = −1, **impossible**. Positivity is automatic: the beat's "Galois
   conjugation" *is* complex conjugation, so the imaginary-quadratic norm is positive-definite.
   And the *reason* the selection happens: because χ is beat-invariant (below), the twisted
   lift satisfies the identical equations (i),(ii) via the same W₀ — the obstruction lives
   **only** in the self-consistent square.
3. **χ is beat-invariant.** beat(a)=a (length 1), beat(b)=b⁻¹aba⁻¹b (length 5) — both odd;
   proved *in general*: length(beat(w)) ≡ length(w) (mod 2) for every word, so χ(beat(w))=χ(w)
   (20/20 random-word spot check + the general argument). The beat descends to π₁(m004)
   (respects the relator) and beat² = conjugation-by-a.

**So the Gieseking extension exists over EXACTLY ONE of the two spin structures.** The object's
own non-orientable ℤ/2 — the symmetry that descends every layer — **selects the lift.** For any
observer whose closing respects the beat, the freedom ledger's last bit is **not free: it is
assigned.** And it *explains* B1122 instead of fighting it — the bit is invisible to coupling
because it is a **topological consistency condition, not a dynamical observable** (readable only
by asking whether the beat closes).

## The honest fences

- **Two non-load-bearing errata (relayed to the cloud seat, like the family_triplet fix):**
  (1) the memo's ω is a root of **x²+x+1** (not x²−x+1 — that's e^{iπ/3}, a *different*
  generator of ℚ(√−3), the ideal-tetrahedron parameter); (2) the norm form is **X²−XY+Y²** for
  ω=e^{2πi/3} (not X²+XY+Y²). Both are positive-definite (disc −3), so **the selection is
  untouched** — the source memo should be corrected.
- **NEEDS-CERT (honest scope):** the exact *cohomological packaging* (the "w̃ extension element"
  sentence) and the *"Galois∘Fibonacci" naming* of the beat were taken on the memo's say-so —
  the **substance** (1-dim intertwiner + N≥0 + χ-invariance) is fully verified, but re-deriving
  that formalism/motivation needs the beat-trilogy (memos 16/17/18) construction. The selection
  does not depend on it.

## What it completes, and what it does not

The discrete kinematic skeleton loses its last smuggled choice: the spin bit is assigned by the
object. **NOT yet a fermion:** this is fermionic *kinematics* (the right to half-integer spin),
not fermionic *fields* — dynamics (a Dirac operator, an action) stays the unpaid verb. **SP-2**
is the live hinge: is the beat-selected structure the one B1112's odd, fermion-capable strata
require? (Yes → the generation's kinematic seat closes on-object; No → chirality is exiled to
the E₈/2I door — either banked-grade.) Cloud seat credited.
