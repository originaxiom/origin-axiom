# PROPOSAL (2026-09-01, fresh physics seat; a grading act — owner / banking seat decides, nothing changed here) — re-word `arc_verdict.json` `claim_one_line`: "type coincidence", not "the same Lie object"

**Status of this file.** A proposal only. Re-grading and re-wording a registry line is an
owner/banking-seat act, not one the delegation rule covers. Original files unedited.

**What B267 proves** (Ring R3 HELD re-read H1,
`reports/fresh_physics_seat_2026-09-01/recompute/H1_b267_grade/`): with every hypothesis
imported — 2T McKay = affine E₆ from B266; 2T character data hard-coded from GAP; the E₆ Dynkin
diagram hard-coded in `e6_dynkin_adjacency()` (not derived from the McKay graph); B264's
exponents hard-coded, with B264's geometric side E₆ by construction — the E₆ adjacency
eigenvalues are 2cos(πm/12), m ∈ {1,4,5,7,8,11}; Σ marks(2T) = 12 = h; Σ exponents = 36;
dim 78; Molien(2T) = (1+q¹²)/((1−q⁶)(1−q⁸)). All reproduce. Corollary: **type coincidence** —
both roles are of type E₆ and therefore share every type invariant.

**What it does not prove.** No map between the McKay-E₆ (a diagram) and the character-variety
E₆ (a group acting on flat connections) is exhibited, and none can be from this evidence: five
type invariants agreeing is one fact (same type) checked five ways, not five independent
measurements — which B272 (2026-06-28) already appended to FINDINGS in those words, and which
the arc's own "Honest guardrail" concedes.

**The defect is in the registry line only.** `arc_verdict.json:4` `claim_one_line` reads
*"…the arithmetically-selected E6 and the character-variety E6 are the same Lie object, the
McKay exponent set matching the tangent-space grading"* and propagates to VERDICT_LEDGER l.165
and THE_SPINE l.330. "Are the same Lie object" is an "X IS Y" identification on evidence that
only establishes type — the species of IDENTIFICATION_LEDGER row I-6, one floor up.

**Proposed wording** (verbatim, for the owner to accept, edit or reject):

> Coherence check passes: the arithmetically-selected E₆ and the character-variety E₆ are of
> the same Lie type, so all type invariants agree (McKay exponents = tangent-space grading;
> h = 12; dim 78; Molien series). No identification of the two E₆ roles is exhibited or
> claimed.

Also proposed: an IDENTIFICATION_LEDGER row (UNEARNED, and re-baseline the ratchet if this row
was counted), and HINT_LEDGER H64's "YES — one E₆ on five invariants" reads "consistent on five
type invariants". PROVED stands for what is proved. Note: a sweep-level citation that B1228
showed "the two E₆-sources can come apart" is a misread — B1228's pair is McKay-E₆ vs the
PSL(2,ℂ) connection's A₁ boundary, not B264's e₆ flat connections; it should not be used to
support this re-wording. Filed as R3_REPORT **G1**.
