# xB014 — PREREGISTRATION (sealed BEFORE any computation of this arc)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell runs.**

## P0

Hyperbolic geometry, homology growth and dimensionless ratios. Named mathematics. No value, no
generation count, no physics reading, nothing to `CLAIMS.md`. Gate 5 absolute.

## The path, and the owner's order

Path A of three the owner approved: **does the tower carry a dimensionless scale ladder, where the
record's "no dimensionful quantity" wall was stated for a single manifold?**

## The sweep, done first (owner's instruction), and what it found

**B207 is a banked NEGATIVE standing directly in front of this path**: *"the family carries no
intrinsic exponential hierarchy."* Read rather than cited, it tests a **different family**:

* **B207 varies `m`** — the metallic family (golden, silver, bronze): regulator `log λ_m ~ 0.91 log m`,
  **logarithmic**, volumes **bounded** (`→ ≈ 2·v_oct`), max/min ratio `O(1)`.
* **This arc varies `n`** — the **cyclic tower** `(LR)ⁿ` over **one** object: volume **linear**
  (`24n·v₀`) and **unbounded**, homology torsion **exponential**.

**B207's negative does not cover this tower**, and B207 itself names where a hierarchy would enter:
*"any large hierarchy must come from the quantization LEVEL … with rate set by the
entropy/volume."* **This arc tests exactly that ratio, on a geometric ladder rather than a
quantization level.** B207 must be **re-derived** (xB010), and if its numbers fail this arc stops.

**B1012 / B1088**, the wall itself: *"blind-to-k iff CS = 0 iff the object equals its mirror"* — so
the no-scale wall **is** amphichirality, which B1234 showed is **construction-forced** and xB011
showed is what makes `√−3` cancel. The three join.

**Literature (cited, and to be verified, not assumed):** Silver–Williams / González-Acuña–Short —
for a fibred knot the homology-torsion growth of cyclic covers equals the **Mahler measure** of the
Alexander polynomial. For `4₁`, `Δ = t²−3t+1`, roots `φ^{±2}`, so `M(Δ) = φ²`. **Kojima–McShane**:
`vol(M_φ) ≤ 3π·log λ(φ)`, i.e. `log λ / vol ≥ 1/(3π)`.

## The question

**Does the tower carry anything beyond a pure function of `(n, φ, v₀)` — and is the both-faces
ratio `log λ / vol` special, or generic?**

## Cells and two-outcome criteria — declared before running

| cell | question | outcome A | outcome B |
|---|---|---|---|
| **Z1** | re-derive **B207**: does its negative cover this tower? | it does **not** — it varies `m`, bounded and logarithmic, while this tower varies `n`, unbounded and exponential; **B207 SCOPED, not disputed** | it does cover it — then this path is already closed and the arc stops here |
| **Z2** | the ladder: volume, torsion and their growth along `(LR)ⁿ` | the rate converges to `log φ²`, matching Silver–Williams' Mahler measure | it does not — the cited theorem misapplies and the arc says so |
| **Z3** | the **both-faces ratio** `log λ / vol`, exact and constant along the tower | computed exactly, with **both** cornerstones identified in it | — |
| **Z4** | **THE DECISIVE CELL — the base rate.** is m004 extremal for `log λ / vol` among fibred census manifolds, against the Kojima–McShane bound `1/(3π)`? | m004 is **extremal or near-extremal**: the ratio is object-specific and the ladder carries something | **unremarkable**: the ratio is generic, **PATH A DIES**, and this arc says so in one paragraph |
| **Z5** | verdict against the kill condition declared here | — | — |

## Declared prior, so the arc can overrule it

Z1 **A** (B207 scopes). Z2 **A** (the theorem is standard). **Z4 is genuinely uncertain and is the
cell that matters.** This seat leans **B, weakly**: exponential homology growth along cyclic covers
is **generic for every fibred knot** — Silver–Williams applies to all of them — so the *existence*
of the ladder is generic and only the **rate** is m004's, and the rate is just its Alexander
polynomial, already banked. **If Z4 returns B, PATH A DIES and the honest report is one paragraph,
not an edifice.**

**The over-claim this arc must not make.** That a dimensionless ladder is a *scale* in the physical
sense, or that it crosses B1012's wall. The wall is about a **dimensionful** quantity; a ladder
supplies a **dimensionless index**, and the two are different objects. **No identification (E82),
no physics reading, and the base rate decides, not the story.**

## Scope

Census computation with the cutoff **reported**. `creates_law` false. No axiom amended. Nothing
reaches `CLAIMS.md`, F2 or Gate 5.
