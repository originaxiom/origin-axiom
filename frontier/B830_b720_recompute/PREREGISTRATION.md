# B830 — MASTERPLAN: close the session's two open threads, sealed before computing

cc banking seat, 2026-07-30. **Sealed before any computation.** Gate 5 absolute — this computes
*classification* facts, no physical value, nothing to `CLAIMS.md`.

## The division

| cell | task | kind | verification |
|---|---|---|---|
| **A1** | ℚ(ζ₃) vs ℚ(i): the cyclotomic branch mismatch | exact arithmetic | lock + negative control |
| **A2** | the Markov quiver is finite-**mutation** but not finite-**type** | exact enumeration | lock + ADE control |
| **A3** | a 3-manifold's flat-connection moduli is finite-dimensional ⇒ no local DOF | exact, from banked H¹ | lock |
| **A4** | the irreducibly-cited residue, named as such | honesty | lock asserting it stays labelled |
| **B** | reconcile `REVIEWS.md` and `ROADMAP.md` forks, then delete | diff-driven | zero-unique-content proof + gate |
| **C** | the carried `B731 revival_score: 10` item — verify, then delete or re-derive | audit | evidence recorded |

**A1–A3 replace a label-lock.** B720's only lock asserts three string literals are distinct — it
verifies that three different strings were typed (B828). Each cell below computes one of the three
discriminating facts that lock was pretending to hold.

## Two-outcome, and the kill branch is real

**This computes facts that SUPPORT an existing banked negative, so the confirmation risk is the
whole danger.** Each cell is therefore stated so it can refute:

- **A1 REFUTES if** ℚ(ζ₃) and ℚ(i) share a subfield larger than ℚ, or if either equals the other —
  then "wrong cyclotomic branch" is not a mismatch and B720's first NO-MATCH weakens.
- **A2 REFUTES if** the Markov quiver's mutation class is **finite-type** (mutation-equivalent to an
  ADE Dynkin quiver) — then ABHY's finite-type requirement is met and B720's third NO-MATCH **falls**.
- **A3 REFUTES if** the flat-connection moduli of the object is **infinite-dimensional** — then a
  3-manifold could carry local DOF and B720's second NO-MATCH weakens.

**Any refutation is banked as a refutation and B720's negative is downgraded accordingly.** No cell
is permitted to be reported as "supports the negative" without its stated kill branch having been
computable and not fired.

## The residue that will NOT be computed, declared now

**"Connes–Marcolli's cosmic Galois group is mixed-Tate over ℤ[i]"** is a fact about *someone else's
construction*. It is **not computable in this sandbox** and will remain a **citation**. A1 computes
only the *object's* side (that ℚ(ζ₃) and ℚ(i) are different branches). **The composite claim
therefore stays part-computed, part-cited, and must be labelled that way** — labelling it fully
computed would be exactly the necessary-vs-sufficient error the B525 audit was built to catch.

## Pre-stated expectations

- **A1: no shared subfield beyond ℚ.** disc(ℚ(ζ₃)) = −3, disc(ℚ(i)) = −4; distinct quadratics.
- **A2: finite-mutation, NOT finite-type.** The Markov quiver (3 vertices, doubled arrows) is the
  standard example of a mutation-finite non-Dynkin quiver; I expect a mutation class of size **1**
  (self-mutating up to iso) and no ADE match.
- **A3: finite-dimensional.** Banked H¹ = 6 at E₆, 1 at SL(2) — finite either way.

Recorded so none can be softened. **A2 is the one that would hurt if wrong**, since it is the only
cell whose refutation would overturn a banked NO-MATCH outright.

## What this masterplan does NOT do

It does not re-open B720's *conclusion* (B706 is reconfirmed three independent ways and that is not
under test), and it does not touch the two arithmetic LEADS. It converts **three cited classification
facts into computed ones where possible**, and says plainly where it could not.
