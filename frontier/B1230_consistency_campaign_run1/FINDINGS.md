# B1230 — THE CONSISTENCY CAMPAIGN, RUN 1

cc banking seat, 2026-08-31. Owner: *"continue, craft a campaign and do it properly."*
Campaign charter: `docs/CONSISTENCY_CAMPAIGN.md`. **Gate 5 absolute. Verdict OPEN.**

**Headline: the campaign refuted its own premise-arc's stated support, and the result got stronger.**

## The charter's stop rules (declared before any cell ran)

1. No cell may claim a row **DELETED** — cells re-type rows or name the decisive retrieval.
2. **Every continuous count states its field** (C-1's own lesson, binding on all later cells).
3. A cell that cannot fail does not run (MB12); each declares its bite.
4. `already_banked` before every novelty claim (B1202).
5. **Cited ≠ verified** — say which one happened.

## C-1 — the base-field audit → **OVER-COUNTED**

| row | claimed | field taken over | verdict |
|---|---|---|---|
| σ | 1 continuous dimensionless anchor | ℝ⁺, no field stated | **OVER-COUNTED as a continuum** |
| the ℙ³ Higgs line | up to 3 continuous parameters | ℚ (dim_ℚ B₀ = 4) | **SUSPECT — no field stated** |
| λ | a continuous time-candidate | none stated | **SUSPECT** |
| ℓ | 1 dimensionful unit | n/a — a *unit* | CLEAN BY TYPE |

MB12: the audit **could** have returned CLEAN. It did not.

> **The binding rule: every continuous-parameter count must state the field it is taken over.**
> A count over ℚ of a d-dimensional ℚ(ζ₁₂)-space reports **4d−1** parameters where the object has
> **d−1** — three spurious ones at d = 1.

Same species as two rules already banked: *type the FULL TRIPLE, never the eliminated coordinate*;
*decomposition readouts solve in the full basis, never project.*

## C-5 — the menu robustness → **REFUTES B1229's stated support**

Designed to break the headline, and it did. Dropping the two-character / Wronskian-0 restriction and
scanning **all** simply-laced (G,k), k ≤ 12 — 168 models, 141 distinct central charges — **c = 6 has
FOUR solutions**, not one:

| A₂ @ 9 | A₆ @ 1 | D₆ @ 1 | E₆ @ 1 |
|---|---|---|---|

Obvious in hindsight: **at level 1, c = rank**, so *every* rank-6 simply-laced algebra gives 6.
B1229's *"σ = 1 selects E₆ uniquely"* was leaning on the restriction, and the restriction was doing
real work. **Recorded as a correction, not buried.**

## C-5b — the recovery, on a better footing

The ℤ/3 cut was **never** the two-character restriction. Primary counts of the four solutions:

| A₂@9 | A₆@1 | D₆@1 | **E₆@1** |
|---|---|---|---|
| 55 | 7 | 4 | **3** |

**The object's own ℤ/3** — trace field ℚ(√−3) = ℚ(ζ₃), the trinification (B727/B1161) — cuts four to
**one**, restriction-free. The corrected statement is **stronger** than B1229's:

> Among all simply-laced (G,k) scanned, **c = 6 has four solutions, and the object's own arithmetic
> selects E₆ alone.**

That is exactly what **K6** demanded: the object's datum, not a modelling convenience.

## The gate caught me, and the fix was not the allowlist

B1229's FINDINGS paired Brown–Henneaux with (E₆)₁ — the pairing **B1034's lock guards** — **without
ever stating L154's adjudication**, so it read as asserting the join. The suite went red on
`test_v2_no_exhibit_adjudicated`. **The fix was not to widen the allowlist.** B1229 gains a section
stating the adjudication (NO-EXHIBIT stands; *"c = 6 is NOT derived here"*; the arc quantizes the
**menu** — the codomain, not the map; B1228's retraction of σ = 1 stands in full; the bridge remains
L154's and UNDECIDED), and *only then* is it admitted on B1064/B1190's ground with a dated reason.

## Net, and what's next

σ is **not a continuum**, and the object's own ℤ/3 pins its menu to one entry **restriction-free**.
Two ledger rows are exposed as field-unstated. The base-field rule binds going forward.

**C-2 is the campaign's highest-leverage retrieval**: codex's ℤ/12 character assignment decides
identity-vs-scalar on B₀ — hence whether the ℙ³ is **three** continuous parameters or **zero**.

**Not claimed:** no row deleted, no map exhibited, σ = 1 not established.

## Reproduce

`sh frontier/B1230_consistency_campaign_run1/reproduce.sh` · lock `tests/test_b1230_campaign.py`
