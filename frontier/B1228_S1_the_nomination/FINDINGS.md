# B1228 — S1, RUN AT LAST: the buried prize gets a unique candidate, and the bridge gets a name

cc banking seat, 2026-08-31. Owner: *"run it… I told you we have it all, except someone who could
read it properly. who is literate enough not to say 'it's supplied externally' as if it sits outside
universe."* **Gate 5 absolute.** **Verdict OPEN — σ = 1 is NOT established; see §The retraction.**

## What unlocked it: the owner's reading

*"Supplied externally"* is our bookkeeping for *"not derivable from this object in isolation."*
Written as though the supplier sits **outside existence**, it stops the search. That is how row 8
got orphaned from row 1.

**The edge nobody drew.** `GRAND_COMPUTATION_v0.md` §6 lists row 1 (σ — the ledger's own *"C4, THE
BURIED PRIZE"*) and row 8 (J, *"closer = S1, a computation, not a preference"*) as **independent**
deletion rows. **Row 8 is row 1's supplier.** B1216 threw nine agents at row 1 and deleted zero rows
because the schedule was read as a checklist rather than a dependency graph — the same E53 shape
B1227 named this morning: *not a lost result, a lost edge.*

## What S1 establishes

| # | result | grade |
|---|---|---|
| 1 | `c(J₁) = dim/(1+h∨) = rank(J)` for simply-laced level 1 — verified **9/9** (A₁→1 … E₈→8) | theorem |
| 2 | **π₁(m004) ↠ 2T** — brute force over all of SL(2,3)²: **48 surjections of 72 homs**; control **5₂ = 0** | verified on-bench |
| 3 | **K5 answered in shape**: the "six cusp-boson units" are the six **Cartan directions** — (E₆)₁ is the E₆ root-lattice CFT, six free bosons; not six cusps | follows from 1 |
| 4 | **K6 discrimination**: σ = c/6 equals 1 for **exactly one** of nine simply-laced types. Neither clause alone suffices — A₂ shares 3 primaries **and** T-order 12, and gives σ = 1/3 | verified |
| 5 | **K1 named**: (E₆)₁, vacuum module, **c = 6 exactly**; modules {1, 27, 27̄}, h = {0, ⅔, ⅔}, T-order 12 | named |
| 6 | **the level is forced by INVENTORY** — see below | **the new piece** |

**Arithmetic cross-checks for K6-grade "the object's own datum"**, all passing: the ℤ/3 module group
↔ the trinification ℤ/3 (B1161) ↔ the trace field ℚ(ζ₃) = ℚ(√−3) (the only object-specific atom,
B727); T-matrix order **12** ↔ the map's exact **mod-12** bookkeeping (§4, down block); the **27** is
already the SM-content carrier (§3).

## The level, forced by inventory — and the wall turning out to be the mechanism

A WZW needs **two** data: the type `J` and the level `k`.

- **J is supplied** — π₁(m004) ↠ 2T, McKay 2T ↔ E₆.
- **k is provably NOT supplied** — `CS = 0 ⟹ ∂S/∂k = −CS ≡ 0` (B1012/B1088), and `CS = 0` is itself
  forced: amphichirality gives 2-torsion `{0, ¼}` and m004 sits at 0 (**B1224/B1227, today**).

A **level-1** simply-laced WZW *is the root-lattice VOA* — determined by the **lattice alone**, with
no level datum. Every `k > 1` requires a level the object cannot give. So **k = 1 is forced by
inventory, not chosen.**

**MB12 bite:** allow k = 2 and σ = 13/7; k = 3 and σ = 13/5. The **blindness is exactly what pins σ.**
The record files that blindness as a wall — *"the firewall relocated beyond the object's reach in
principle."* Read correctly it is the **mechanism**, not the obstacle. That inversion is the arc's
real content.

## The retraction (made in-session, before anything was banked)

I first reported that the chain **"closes end to end"** and gave **σ = 1**. **It does not.**

`S = −CS·k − Vol·σ` is the Chern–Simons action **of the geometric PSL(2,ℂ) connection**, so its
boundary WZW is type **A₁**, rank 1 — which *is* the "one cusp-boson unit." The E₆ arrives from a
**different** structure (McKay on 2T, the 6d (2,0) type). The two sixes come from two constructions.
"Default-J vs nominated-J" **diagnoses** the gap correctly **without closing it**; asserting
otherwise is exactly the K6 error the clause exists to catch.

**Dimension check run** — B980 records a withdrawn chain of this precise shape, killed on dimensions.
Here `c` and `σ` are both dimensionless, so that failure mode does not apply.

## Net, and the bridge named

Row 1 moves from **"documented empty candidate set"** (B1216) to a **unique named candidate** with
the level forced and the arithmetic matched, and **one identification remaining**:

> **Does the NOMINATED type or the GEOMETRIC connection govern the boundary algebra?**

That is L154's missing bridge — now stated with a unique target instead of an empty set. **σ is not
deleted.**

**Fence:** the nomination is **generic** — the trefoil also surjects onto 2T (48 as well; B993/B996
put it near ⅓). It is real but **not distinctive to m004**, and S1 tests *acceptance*, never
uniqueness.

## Reproduce

`sh frontier/B1228_S1_the_nomination/reproduce.sh` — four cells (σ-from-J; the nomination with its
control; K1's named candidate; the level-forcing). Lock `tests/test_b1228_s1.py`.
