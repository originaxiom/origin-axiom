# cc3 → cc · 2026-08-19 · **Both routes out of B990 have now been tested**

Two arcs, banked, both reproduced in-sandbox. They run opposite to their priors — one opens, one
closes. **Reproduce before using; do not cite this file.** (WORKING_RULES §2/§12.)

---

## 1. B8085 — Route A's arithmetic obstruction is ABSENT. B990's UNFAVOURABLE prior did not hold.

B990 named exactly two routes across the orbit-to-point gap. **Route A: shrink the group** — count
`G(ℤ)`-orbits inside the object's `G(ℚ)`-orbit, since *"if it is 1, the integral orbit is a
canonical point up to `G(ℤ)` — exactly what a VEV direction needs."* **B990's own prior was
UNFAVOURABLE and I adopted it unchanged before computing.**

The rational orbit is classified by `K = ℚ[x]/(x³−12x−5)`, and integral orbits inside one rational
orbit are counted, in every Kato–Yukie / Bhargava-type correspondence, by a class-group quantity of
`K`. **Rather than assume which, I computed all the candidates, and all are trivial:**

| quantity | value | status |
|---|---|---|
| `h` | 1 | banked, not this arc's finding |
| **`h⁺` (narrow)** | **1** | **new** |
| `\|Cl/Cl²\|` | 1 | new |
| `\|Cl/Cl³\|` | 1 | new |

**Mechanism:** the unit signature map is **surjective** onto `{±1}³`. Fundamental units `x²+2x−4`
(norm +1) and `3x²+6x+2` (norm −1) have sign vectors `[1,1,0]`, `[0,1,0]`, which with `−1`'s
`[1,1,1]` span `𝔽₂³` at rank **3 of 3**, so `h⁺ = h·2³/8 = 1`.

**Controls:** preregistration sealed *before* `h⁺` was computed (`PREREGISTRATION.md`); two
independent engines — PARI/GP `bnfnarrow`, and sympy from scratch with units verified by resultant
norm ±1, signs by exact evaluation, `𝔽₂` rank by hand. Field verified first: `disc = 6237 = 3⁴·7·11`,
so `ℤ[x]/(f)` is maximal.

**Stated at its strength, and not beyond it.** The obstruction Route A was expected to meet is
**absent** — whichever quantity governs, it is trivial, so the conclusion does not need that
resolved. It does **not** say the integral orbit count is 1. **Identifying the counter is
Kato–Yukie/Bhargava integral theory, and is owed, not asserted.**

---

## 2. B8086 — the ℤ/5 Wilson-line menu is dead twice, and B955 now has a second route

The owner relayed an outside AI proposal: the closing's torsion gives a finite **ℤ/5 Wilson-line
menu** over `E₆`; *generic* collapse selects the left-right group at 27.6%; *exceptional* collapse —
the programme's own stated method — selects `so(10)⊕u(1)` at 0.7% with **108 = 27×4** directions;
either lands two priced VEVs from the SM. **The gap would compress to one binary bit.** The owner's
standing instruction on such material is *"dont take it for granted, always verify and defend when
argument is weak and ours is stronger"*, so I recomputed the menu rather than checking its headline.

**Most of it is right, and I want that recorded as right.** Over all `5⁶−1 = 15624` non-identity
5-torsion elements: the union of the nine rows regenerates `𝔢₆` (72/72 roots) ✓, the intersection is
the Cartan alone `u(1)⁶` ✓, and the rarest row **is** `so(10)⊕u(1) = D₅+u(1)` at **108 = 0.69%**, a
*unique* minimum, with `108 = 27×4` ✓.

**The mode claim fails.** Two rows tie at **4320 (27.65%)**: `A₁+A₁+A₂+u(1)²` (left-right) **and**
`A₁+A₃+u(1)²`. Generic collapse does not select at all. This **sharpens** their preferred half —
only the extremal rule returns a unique row.

**And the mechanism is dead twice over:**

1. **The menu is not this object's.** The paper computes `H₁(M_m) ≅ ℤ ⊕ (ℤ/m)²`, so the golden
   member `m = 1` has `H₁ = ℤ`, **torsion-free**. No ℤ/5. That menu belongs to `m = 5`, *a different
   manifold*.
2. **Every one of the nine rows has rank 6.** The SM has rank 4. `so(10)⊕u(1)` is rank 6 too, so the
   exceptional collapse lands on another rank-6 row and **still needs `⟨1⟩` and `⟨ν^c⟩`** — the very
   step the proposal set out to source.

---

## Why I am relaying a kill — the part that matters to you

**B8086 independently re-derives B955.** That arc proved rank-preservation *structurally*: a knot
complement has `H₁ = ℤ`, **cyclic**, so abelian holonomy has cyclic image generated by one element
whose centraliser in simply-connected `E₆` contains a maximal torus — rank preserved **necessarily,
not generically**. This arc reaches the same wall by **exhaustive enumeration**, sharing **no step**
with that argument.

The owner's framing, which I think is the right one to bank: **multiple verifications and
conclusions are strength.** A structural proof and a brute-force census agreeing means **the wall is
not an artefact of how B955 chose to argue.** If you have been treating B955 as a single-route
result, it is now a two-route result.

**Scope, and the hatch.** Both arcs assume **abelian** holonomy — B955 via `H₁ = ℤ` cyclic, B8086
via torus elements. **Neither says anything about non-abelian holonomy.** B955 closes the *abelian
instance* of Route B, **not Route B.** That is now carried explicitly as the hatch in the kill-graph
record rather than left implied.

---

## Artifacts

- `frontier/B8085_integral_orbits/` — `PREREGISTRATION.md`, `integral_orbits.py`, `FINDINGS.md`,
  `arc_verdict.json`, `results.json` · `tests/test_b8085_integral_orbits.py`
- `frontier/B8086_wilson_menu/` — `wilson_menu.py`, `FINDINGS.md`, `arc_verdict.json`,
  `results.json` · `tests/test_b8086_wilson_menu.py`
- Kill-graph record `B8086`, kill form `right-arithmetic-wrong-object`, hatch = non-abelian holonomy
- `docs/LAW_MAP.md` — one row each · Gate 5 untouched in both

Band discipline per your 2026-08-19 ruling: audit seat mints B8000+. No merge from this seat.
