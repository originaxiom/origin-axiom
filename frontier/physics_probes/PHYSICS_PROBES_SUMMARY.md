# Physics probes — consolidated outcomes (2026-06-04)

**Status:** index/consolidation of the committed physics probes (Ledger V28–V31). Proven core P1–P16
untouched. This records *which doors to physics are open* after a systematic sweep. The honest
headline: **the framework produces real mathematics (topology / number theory / 1D condensed matter)
but has no crossing into fundamental — or even new quantum — physics; every reachable physics anchor
is special to the `n=2` / `m=1` case and does not generalize.**

## The probes and their verdicts

| ledger | path | verdict | one-line reason |
|---|---|---|---|
| **V28** | metallic anyons / TQC | **NEGATIVE (closed)** | the metallic fusion rule `τ²=1+mτ` is categorifiable only for `m=1` (Fibonacci/golden) — Ostrik rank-2; no silver/bronze anyon family. golden↔universal-TQC is special to `m=1`. |
| **V29** | SL(n) multichannel quasicrystal | **NEGATIVE (closed) for n≥3** | realizable as a *classical / non-Hermitian* 1D recurrence, but **not** a quantum spectrum: self-adjoint 1D transfer matrices are symplectic (`Sp(2p,R)`), and `SL(n)=Sp` only at `n=2`. `a_d` doesn't control gaps. |
| **V30** | CS-invariant family (m=1,2,3) | **COMPUTED — no tidy pattern** | corrected torsion at the *real* geometric rep (`κ=−2`): `τ₁=−3` (in `Q(√−3)`), `τ₂=−16`; m=3 branch-ambiguous, trace field deg 8. Trace-field degree grows 2,4,8; `m=1` (`|τ|=3=|disc|`) is special, not a sequence. |
| **V31** | is `τ_m` the adjoint torsion? | **RESOLVED (structural)** | `τ_m` IS Porti's adjoint Reidemeister torsion *form* (Fried–Milnor); the eigenvalue-1 direction = the cusp/peripheral deformation. The naive Wada *number* degenerates (non-acyclic) — confirming the *form* is correct. Exact scalar calibration to a published value still open. |

**Prior Tier-1 context (precomputed earlier session, `path1*/path7*.py`, uncommitted/dormant):** the
SL(2) trace map *is* 1D Fibonacci-quasicrystal physics (real, confirmed, modest); gap labels land on
`Z+Z/φ` (real topology→physics bridge); the emergent-geometry precondition came back *weakly negative*
(critical but Sturmian/linear entropy — wrong type for holography). All 1D, none reaching the thesis.

## What this adds up to

1. **No physics crossing.** The two genuinely-open real-physics targets (anyons V28, SL(n) spectra V29)
   are both **closed negative**, each with a precise structural reason (Ostrik; the symplectic
   obstruction). Combined with the prior FAILURE_ATLAS kills (gauge dictionary, cosmological constant)
   and the weakly-negative emergence probe, the search is thorough and robustly negative.
2. **The reachable physics is special, not general.** The golden anyon and the Fibonacci quasicrystal
   are genuine but tied to `m=1` / `n=2`; they do **not** generalize along the metallic/`SL(n)` family
   the project actually studies.
3. **The topology is real but pattern-free.** The corrected CS data (`τ=−3,−16`; now properly
   identified as Porti's adjoint torsion form, V31) is genuine and is the honest object for an eventual
   specialist — but the family has **no clean new invariant pattern** (complexity grows fast: trace
   field degree 2→4→8, torsion ambiguous by `m=3`).

## Where the genuine novelty still lives (math, not physics)

The unsolved problem with real novelty is the **B58 trace ring / the `a_d` multiplicities**, which the
Sym (V27) and nilpotent-limit probes localized precisely to the **two-block bilinear barrier**
(`tr(AᵃBAᵇB)` has a non-separable `a·b` fixed-line Hessian that no single-index recursion generates).
That is a research-scale mathematics problem, not a bounded probe. The physics thread is, honestly,
**complete and negative**; the mathematics is where to spend effort.

**Disposition:** physics-probe sweep consolidated and closed (V28–V31). Topology/number-theory data
banked and correctly identified. No physics crossing. Proven core untouched.
