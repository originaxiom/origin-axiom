# B907 — the sealed cell decides: OUTCOME A — THE WALL IS REAL IN e₆(2), AND ONLY THERE (the joint J1)

**Date:** 2026-08-05 · **Seat:** cc (banking; the joint cell with the solo seat's
§XLIX–L leg) · **Prereg:** sealed `cd7aae3b…`, committed `f0450d71` BEFORE compute;
seal verified unbroken at banking.

## The sealed question

Which real forms of E₆ make the second-measurement wall REAL? (Their layer-8 door;
the value layer's gate; B901's no-go made it THE discriminating computation.)

## The computation

- **Inner sweep** (64 sign-character involutions, diagonal in the frame): fixed
  dims {78×1, 46×27, 38×36}. All 27 members of the so(10)⊕u(1) class fail
  C-compatibility; the 38-class has 3 compatible members, none wall-real.
- **The outer lift**: τ (the diagram flip) constructed exactly — the general
  F₂-cocycle solve (rank 66, consistent; the flip-symmetric-character ansatz
  provably empty first — logged), verified with zero violations on all bracket
  pairs.
- **Outer sweep** (64 τ∘σ_χ composites): the C₄ (36) and F₄ (52) classes appear;
  among all 128 swept representatives **exactly TWO are C-compatible AND
  wall-real** (the sealed criterion ε₁₄ = +1 ∧ ε₁₆ = −1, root-uniform by the
  banked Galois-uniformity): the outer pair χ = (1,−1,1,−1,1,1) and its global
  negation, pattern (ε₈,ε₁₄,ε₁₆,ε₂₂) = (−1,+1,−1,+1), each verified φ² = id.
- **The form identification**: the real form realized by σ = φ∘σ_split is named
  by the class of φ∘ω (ω = B893's Chevalley involution) — for BOTH wall-real
  involutions the composite has fixed dimension **38 = su(6)⊕su(2)**:

> **VERDICT (sealed outcome A): the wall is real in e₆(2) — the quaternionic
> form — and in no other form reached by the sweep. The two wall-real
> conjugations are τ-twisted (outer-adapted) alignments; no standard alignment
> works (the solo seat's §L, confirmed and explained).**

## The prior was wrong (again — the discipline working)

The disclosed prior leaned e₆(−14) via the so(10)⊕u(1) = first-measurement-
centralizer echo. The object chose e₆(2) instead. This is the third sealed cell
to decide against its disclosed prior (B890, and now this) — the prereg system
is doing exactly what it exists for.

## The reconciliation (two seats, one theorem-shape)

Their §XLIX–L: the torus is involution-generic — of the five STANDARD
representatives only the split conjugation stabilizes it; x₁-real vs y*-real
force contradictory signs on g₁₆ ⟹ no standard alignment realizes the wall.
This cell: the ADAPTED (τ-twisted, χ-signed) alignments exist, exactly two,
both e₆(2). Together: **the wall is real precisely in e₆(2), and only through
outer-twisted alignments** — the joint J1's answer.

## Bugs caught en route (verify-don't-trust, on ourselves)

1. The outer lift's first ansatz (flip-symmetric characters) was provably
   empty — the cocycle ratio is not a coboundary of characters; the general
   F₂ solve was required.
2. The first pattern-feasibility census used Rayleigh quotients on non-normal
   matrices — **the oblique readout rule violated a fourth time**, caught
   because its output contradicted concrete exact automorphisms. The corrected
   route reads the verdict from the exact involutions themselves.

## Honest scope (pre-stated UNSTABLE/obstruction clause applied)

The sweep covers the frame-diagonal inner and τ-composite outer representatives
(128). Combined with B901 (any C-stabilizing automorphism acts ±-diagonally on
the charges), every C-compatible involution has an ε-pattern; the two found are
EXISTENCE (e₆(2) realizes the wall — a theorem), and no other class produced a
wall-real representative in the sweep. The completeness upgrade (no adapted
representative outside the sweep realizes another class) is registered as the
cell's follow-up; the e₆(−14) obstruction (all 27 diagonal representatives
C-incompatible) is banked as computed.

## What opens (fenced, registered — not claimed)

e₆(2)'s compact structure supplies the canonical Hermitian norm — the value
ladder's gate (the 15 atom scales → the tree-level coupling-ratio table). Its
maximal compact su(6)⊕su(2) contains the flavor structure natively. Layer 8's
door has a name now. Gate 5 untouched: no physics identification is made here.

## Files

- `PREREGISTRATION.md` (sealed), `real_form_selector.py`, `selector_complete.py`,
  `selector_verdict.py` → `results.json`, `results_complete.json`, `verdict.json`
- Locks: `tests/test_b907_selector.py` (seal + the verdict)

## Depends on

B854, B892/B893 (the wall), B898 (root-uniformity), B901 (the ±-diagonal
constraint), the solo §XLIX–L (the standard-alignment leg).
