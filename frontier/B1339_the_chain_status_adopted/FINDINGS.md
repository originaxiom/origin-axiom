# B1339 — the chain status ADOPTED, and memo 152's own fix discharged

B1338 found the mechanism behind the rediscovery loop: **four consolidations, each *added* and never
*adopted*, none of them on main**, and a fifth being drafted. This closes it — by adopting one and
demoting the rest, not by writing a sixth.

## What was done

**`docs/THE_CHAIN_STATUS.md`** — the chain's status of record, on main, adopted from cloud memo 152
(`e51afd6c:outside_bench/THE_CHAIN_GAP.md`, banked 2026-08-30) with a currency pass. It carries
memo 152's one-sentence answer, its fifteen-link chain table separating **absent (❌)** from
**proved unobtainable (🔒)**, the four gates, and the join defect. `THE_TOE_GAP.md`,
`THE_FULL_ACCOUNTING.md`, `THE_GRAND_TABLE.md` and memo 152 itself are demoted to **sources**.

## Adoption meant a currency pass, and it was needed

**Memo 152 is stale on its own headline.** Its §4 recommends *"gates B, C, D enter the queue …
**gate C is the three-generation question**. If the programme wants a feature, that is the door."*

`OPEN_PROBLEMS.md` §C records **Gate C CLOSED 2026-08-30 — the same day memo 152 was banked** — by
its own stated refutation condition (B323: the `E₆`-level and commensurator `ℤ/3`s are distinct,
`3 ∤ 8`; B324: the three images are `g`-conjugates sharing one character; codex's Wave-8
strengthening reaching the closure without B307 at all, the orbit having size one so **the
commensurator does not manufacture multiplicity**).

The closure **relocates** the generation question rather than answering it, and the record corrected
its own successor twice the same day: `h¹(M;27) = 3` means one class in each inequivalent principal
block `V(16)+V(8)+V(0)`, **not three copies**; *"the actual live generation computation"* is
**WITHDRAWN**; and the question is tracked as codex **`OA-C0009`**.

> Had this been copied rather than adopted, the queue would now carry a specialist question about a
> gate that closed thirteen days ago.

## This week's five arcs, folded into row 10 and row 14

B1334 (the vanishing is dimension-counting, not the object), B1336 (no non-abelian gauge group
survives the flat connection), B1332/B1333 (isotropy is one-cusp; 38070 sectors on all 54 covers,
zero), B1335 (the identities do not imply `I = 0`), B1337 (memo 79's character-invisibility is an
`SL2` fact true on every manifold). **Five directions, one statement**: the flat-`E₆`/cohomology
sector of the cusped object does not contain chiral matter, and the reasons are not about the
object. The paper's SM algebra comes from the closing `Y₉`; nothing here touches that.

## Memo 152's actual fix, discharged

Memo 152 measured the join between the registers and the specialist queue: **4 gates + 1 hatch, of
which 3 + 1 are SPECIALIST, of which 0 were in the queue.** Re-measured here — **still true**. The
queue carried Q1–Q6 and not one was a gate or the hatch.

Now drafted into `SPECIALIST_SEND_QUEUE.md`:

| | | why |
|---|---|---|
| **Q7** | Route A — the K-refined integral-orbit count | the hatch; obstructions proved absent, coarse count closed positive |
| **Q8** | **Gate B — the CRUX `T[4₁;E₆]`** | tangent computed (`dim H¹ = 6 = rank E₆`); the `SU(3)²` sub-path closed-negative; the exceptional state integral needs a specialist |
| **Q9** | Gate D — the non-Hermitian spectral theorem | carries the warning that `B1085`/`B1095`'s numerics ran Hermitian and **do not transfer** |

**Gate C is deliberately absent — it is closed.** That is the difference a currency pass makes.

**DRAFTED, NOT SENT.** Memo 152's fence (*this bench drafts; the owner sends*) and the standing
directive at the foot of the queue (*nothing leaves before the mathematics is exhausted*) both hold.

## Gated

`tests/test_b1339_chain_status_adopted.py`: the status exists on main; it supersedes rather than
joins (all four sources named); it carries the currency correction its source lacked (gate C's
closure and `OA-C0009`); and the gates reached the queue with the send fence explicit. A future
consolidation edits `THE_CHAIN_STATUS.md` — it does not open a sixth.

## What this does not do

It does not advance the mathematics by one line. It makes the record say, in one adopted place,
what it already knew in four unadopted ones — and it puts the three doors the record itself names
in front of the owner instead of in a memo nobody reads.
