# B1132 — the listener instrument is null on the WHOLE sphere, and the golden meridian law, exact

**Status: banked (frontier). Verdict PROVED (closes B1128's fenced gap: INSTRUMENT-NULL-
FULL-SPHERE, and an exact new structure law for LAW_MAP; own-bench: B1128 reproduced
byte-identical, the φ-law y-component re-derived symbolically here). Value-probe wave
remainder (B). Gate 5 untouched. Lock `tests/test_b1132_meridian.py`.**

## What it closes (remainder B, from B1128's own fence)

B1128 found the listener-map coupling NULL on the real great circle, but fenced one
unsearched direction: a different meridian (the wy-component) "would see different numbers."
This cell searched it — the FULL ℂP¹_odd via u(θ,ψ) = cos θ·u₃ + e^{iψ} sin θ·u₆ (closed
form cross-checked vs brute-force, worst diff 7e-51).

> **INSTRUMENT-NULL-FULL-SPHERE.** The degeneracy that collapsed B1128's test DOES break
> off the real circle (proved), so the freed direction is a genuine coordinate — but it
> carries **no SM content**. With 2 real dof and 1 calibration input a point-prediction
> isn't even well-posed (a residual dof survives → the "prediction" is a RANGE). Graded most
> generously, the ranges bracket the PMNS targets only by consuming 5–7% of the freed
> freedom — matched by a seed-stable ~6% whole-sphere coincidence rate (Monte Carlo, 5
> seeds); multiple-testing (this channel's 7 prior nulls + 5–6 sub-tests here) puts the
> chance of a coincidence this good past 50% under the null. Nothing "exceeds what the
> unknown can absorb" (charter rule 4) — NOT a cc3-flaggable positive. B1128's null now
> holds on the entire object, no meridian unsearched.

## The golden meridian law (NEW — proposed LAW_MAP row)

> **h(R²L², u) = −φ·h(RL, u) − iφ·n_y(u)** exactly, everywhere on ℂP¹_odd, where n_y(u) is
> u's Bloch y-coordinate — equivalently **axis(R²L²) = −φ·[axis(RL) + ŷ]** as Pauli
> 4-vectors. B1128's simpler relation h(R²L²,u) = −φ·h(RL,u) is exactly the **n_y(u)=0**
> special case — i.e. it holds IFF u lies on the real great circle through u₃, u₆ (a
> complete closed-form domain characterization, not sampled).

Verified 50-digit two independent ways (worst residual 1.15e-50 across poles / circle / 16
random sphere points) + a symbolic proof of the load-bearing sub-identity (wy(RL) = −φ/2,
wy(R²L²) = (1−φ)/2, reduced mod φ²−φ−1); the y-component re-derived on THIS bench. A grep
found no existing LAW_MAP row it restates. It is a STRUCTURE law (golden ratio on the
listener curve), NOT a value.

## Honest open items (registered, not dropped)
No wider McKay-literature search for the law; the other 6 period-5 words not individually
re-verified off-circle; wx/wz closed forms left as unreduced radicals. Gate 5 untouched.
