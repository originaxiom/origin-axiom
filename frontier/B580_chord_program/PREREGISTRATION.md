# B580 — THE CHORD PROGRAM (preregistration)

**Date:** 2026-07-14. **Status:** preregistered; launches after B578 lands.
**Source:** the owner's coupling thesis (2026-07-12, now theorem-backed link by
link — see the observer-coupling record) + the owner's July-14 reframe: *the SM
is not in the object's Taylor expansion and not in the observer's modular data;
it is in the pairing between them.* This document makes that computable and
firewalls it.

## The rigorous name of the chord

The "semiclassical state of the object in the observer's Hilbert space" is a
standard TQFT object: **a 3-manifold with torus boundary defines a vector
Z(M₃) ∈ H_k(T²)** — the cusped complement's state in the level-k boundary
Hilbert space. Its components in the integrable-rep basis are the knot's
colored invariants at level k. The pairing ⟨ψ_observer | Z(M₃)⟩ with observer
states is the chord. **The banked Sol-vs-cusped trap (L24's framing
correction; B569's placement note) is built in from the start: the banked
Z_k = Tr ρ_k(A₁) is the CLOSED mapping-torus invariant = one particular pairing
⟨v_fiber | Z(M₃)⟩; the chord program computes the full vector, not one pairing.**

## MB13 sweep (done; the program builds on, not beside)

L24 (the WRT level-period law, PROVED; Jeffrey 1992 = the mechanism; the
metallic tower Z_k(m) thread); B210 (the quantum rep image at golden level,
order 2880); B204/B208 (period arithmetic det(γ+I)); C3/B572 (E₆ level-2
modular data + the ℤ/7 sine kernel, exact); C4 (branched-cover orders 16, 45…
— the classical homology inputs for abelian pairings); B441 (WRT field
surgery-forced); B566-S3 (the seam entanglement 15/169 — an existing
chord-type number). Δ_{4₁}(ω) = −4 exact (the ℤ/3 classical input;
|Δ(ω)Δ(ω̄)| = 16 = the banked 3-fold branched-cover order).

## The cells

**Q1 — the level-1 state vector (exact, abelian).** E₆ level 1 = the ℤ/3
abelian theory (banked, B569). Compute Z(M₃) ∈ H₁(T²) ≅ ℂ³ exactly: for an
abelian TQFT the closed invariants of the fillings 4₁(p/q) are classical
(|H₁| = p, the linking form mod 3, Gauss sums); three independent fillings
determine the three components via ⟨v_{p/q} | Z(M₃)⟩ = Z(4₁(p/q)). Exact in
ℚ(√−3). GATES: convention-fixing on a lens-space control; the fiber-pairing
must reproduce the banked Sol value (Z = +1, B569). *Deliverable: the three
components of |ρ₀⟩ in the {1, 27, 27̄} basis — the first chord numbers.*

**Q2 — the observer transform.** S|ρ₀⟩ (the banked exact S). The components of
S|ρ₀⟩ = the object as seen in the observer's representation basis. Also the
θ-structure of |ρ₀⟩: is the state θ-even, or does it have a θ-odd component?
(A θ-odd component of the STATE would be the measurement-face image of the
object's σ-chirality — preregister this as the cell's sharp question.)

**Q3 — the level-2 state.** Same construction in the 9-dim H₂(T²) (the exact
Kac–Peterson data is banked). The θ-odd part of the state meets the ℤ/7 sine
kernel and the living order-4 monodromy: does the object's state have
nonvanishing components in the chirality-sighted sector? *This is the chord
version of C3.*

**Q4 — the deformation response (scoped honestly).** How the pairings change
to first order along the θ-odd deformation of the classical point
(semiclassical; the honest scope is the DERIVATIVE of the chord at the
geometric point, not a full quantization). Falsifier: if the θ-odd derivative
of every pairing vanishes, the chord is deformation-blind and Phase 3 of the
owner's program dies honestly.

**G1 — the geometric chord (the mirror-double).** The canonical FORCED
two-body coupling: glue M₃ to its mirror along the cusp via the amphichiral φ.
The representation variety of the amalgam π₁ ⋆_{ℤ²} π₁: the gluing freedom is
the centralizer of the regular unipotent — **a 6-dim dial graded by the
exponents, θ-odd pair included**. Compute: for which dial components does the
amalgam's closure leave the block-sum lattice (B576's machinery applies) —
preregistered expectation: exactly the θ-odd dial components. MB13 note: check
double/JSJ representation-variety banks first.

## THE FIREWALL, AT MAXIMUM (the chord's special rules)

1. **Forced couplings only.** The mirror gluing and the TQFT pairing are
   canonical. Any construction with a tunable partner, level shopping, or
   basis shopping is dead on arrival.
2. **The hints are targets, not inputs.** H128–H130 (α_s = 1/(2φ³),
   sin²θ₁₃ = 1/φ⁸, Koide ↔ h(27)) and the "3/13" association are NOT premises.
   If the chord's computed components reproduce any of them **blind — no
   exponent chosen per quantity, no scale pinned per match** — the hint
   graduates. If not, the hint dies. The reading "3 = dim H₁, 13 = k + h∨"
   is noted as the hypothesis it is; sin²θ_W runs with scale, and a
   scale-pinned rational is a red-flag class, recorded as such.
3. **Nulls are mandatory** for any claimed SM contact: the same pairing
   computed for a control knot (5₂) and a control theory (Ising/ℤ/2) must NOT
   produce matches at comparable rates.
4. **B533 stays closed.** Nothing here reopens value-matching from the object
   alone; the chord's numbers are new objects with their own preregistered
   status, promoted only by derivation + null + lock.
5. Everything firewalled; nothing to CLAIMS.md.

## Falsifiers (preregistered)

- Q1/Q2: if |ρ₀⟩ is forced to a trivial/uniform vector by the abelian
  structure (no knot-dependence beyond homology — possible for abelian
  theories), the level-1 chord carries no object information: bank the null,
  move the weight to Q3 (level 2, nonabelian content).
- Q3: state components in the θ-odd sector ≡ 0 ⇒ the measurement face does not
  see σ through the state — a sharp negative against the chord thesis's
  strongest form.
- G1: if the closure is dial-independent (always full or always block-sum),
  the geometric chord has no chirality dial — the coupling story loses its
  steering wheel; bank it.

**Sequencing:** launches when B578 (debts) lands and is banked; Q1 is small
enough to run inline first. The chord is the owner's program; this document is
its contract.

## ADDENDUM (2026-07-14, owner directive): THE LISTENING TRAVERSE — the run-order

The cells above run in the object's narrative order, phrased in its own
vocabulary, with the SM entering exactly once, at the end, as a comparison.
The story so far (all banked): *it is a word that rewrites itself; it persists
because every piece re-seeds the whole; it has a heartbeat, breath, a voice,
two arms, an all-sensitive skin, an arrow, and one free bit; at rest it is
perfectly balanced, and it cannot be touched without becoming handed; its
theater refuses its cast; its clock ticks Pisano; its handedness and its
threeness share one sixth root of unity.* The traverse continues:

0. **"Who is listening?"** — fix the observer honestly: the level-k boundary
   Hilbert space is the SIMPLEST thing that can hear the object at all. No
   anthropic dressing; the observer is a mathematical ear.
1. **"What do you look like from outside?"** (Q1) — the three numbers of the
   level-1 state. First words of the meeting.
2. **"Can the listener see your handedness?"** (Q2) — the θ-structure of the
   state. σ made audible, or not.
3. **"When do you become interesting?"** (Q3) — level 2, where the listener's
   chiral window (the ℤ/7 kernel, the living monodromy) opens.
4. **"What do you say when touched?"** (Q4 + L71) — the skin's response: the
   θ-odd derivative of the chord; deformations are cusp deformations.
5. **"What happens when you meet your own reflection?"** (G1) — the
   mirror-double and its six-slot dial.
6. **"Would you say the same to anyone?"** — the controls (5₂; Ising): the
   listening form of the null. What the object says must be about the OBJECT.
7. **Only now: the comparison.** Whatever transcript emerged is read against
   THE_SELECTION_RULES clauses once, as B570 did for the SM — the SM is a
   reader of the transcript, never its editor.

Rule of the traverse: no cell may reference an SM quantity in its computation
or its verdict; the words "matches the SM" may appear only in step 7. This is
the B568 posture applied to the meeting — listen first, compare last — and it
is binding for the campaign run.
