# B1306 — THE OLDER DEBT, SLICE C: the never-read physics items of four seats, read, re-run (17/17) and re-derived where main's machinery reaches (8 own computations, all agreeing) — cc3's ρ rebuilt (order 2880, 63 classes), geodir h¹ = 6, the positivity bridge (241 = 241 to length 10), the cusped one-loop gap; fc's Kac classes (80/90), the Tits lift (order 3; 24, 27, 27), the E₈ rotation; the codex seat's listener finding turned into I-29; the hostile seat's beat identity and unit dictionary (16 geodesics)

**Date:** 2026-09-09 · **Seat:** cc (main) · **DESIGN_C sealed** `1ee32603…` before any of main's computations · **Seats pinned (unchanged):** cc3 @
`a31456d2`, fc @ `659487bb`, codex @ `f7a49536`, hostile @ `6fc86147` · **Status:** **VERIFIED** (C1, C2, C3, C5-half, C6, C7, C10, C14 with own code; every
re-run green), **REGISTERED** (C4, C8, C11, C12, C13 with re-run receipts), **REPAIRED on main** (C9: I-29) · **Price:** none new (I-29 registers an
identification B1231 already admitted; the baseline migrates deliberately) · **Credit:** cc3 (B8081–B8083, B8102), the physics seat (R64–R67), the codex
seat (R033), the hostile-review seat (memos 16–21), B351 / B1275 / B1297 / B1302 (main's own machinery the re-derivations ran on).

## 0. The seats said, first — and what main found

| # | item | the seat's verdict (verbatim) | here |
|---|---|---|---|
| C1 | cc3 B8081 ρ rebuilt | "It is built here, from the Kac–Peterson data alone" | **VERIFIED** — own S and T for SU(3)₂ (c = 16/5): T exponents over 15 = [13, 2, 8, 2, 7, 8] on the seat's primaries, (ST)³ = S², **⟨S, T⟩ has order 2880, ord T = 15, 63 conjugacy classes** — B8081's numbers to the digit |
| C2 | cc3 B8082 geodir h¹ | "PROVED for the H¹ count; the unobstructedness is registered as owed" | **VERIFIED** — B1297's exact engine on m004's geometric ρ: h¹(Sym^{2e}) = 1 and h⁰ = 0 for each E₆ exponent e ∈ {1, 4, 5, 7, 8, 11} (and for the non-exponents 2, 3, 6, as B8082 also records); **h¹(m004; 𝔢₆) = 6**; the owed half stays owed |
| C3 | cc3 B8083 positivity bridge | "two positive words in R, L are conjugate in SL(2,ℤ) iff they are cyclic rotations of one another" | **VERIFIED** by an independent complete invariant (the Gauss reduction cycle of the fixed-point form (r, s − p, −q)): on all **2 026** mixed positive words of length ≤ 10, **241 rotation classes = 241 invariants**, rotations share their cycle and the cycle separates classes; the 44 trace-18 words fall into 6 classes with 6 invariants (B8083's φ₁³ ≠ φ₄) |
| C4 | cc3 B8102 cusped one-loop gap | "a literature position, each claim cited" | **REGISTERED** — arXiv:2507.05364's abstract scopes to cusp-free quotients as B8102 quotes; the cusped-determinant mathematics it cites is the input main holds (the L204/Phase 3 lineage) |
| C5 | fc R64 | "the object's order-3 element is the family rotation TIMES an E₆ Weyl element" | **VERIFIED (the w_{A₂} half)** on B1275's own E₈: the Weyl rotation of an A₂ plane has order 3, fixed dimension 6 and fixes exactly the 72 roots of its centraliser E₆; the L_g half (fixed dim 0) needs the icosian model — the seat's, re-run RC=0 |
| C6 | fc R65 | "40 of B1264's 85 trinification gradings are trinification elements and 45 are not" | **VERIFIED** from the E₆ Cartan matrix alone: the 27's weights as the W-orbit of ω₁; **170** labellings modulo the centre (the central shift is (1,0,2,0,1,2) mod 3 — main's first count of 510 forgot it, recorded), fixed-subalgebra dimensions **{24: 80, 30: 90}**, colourings **{24: 40, 30: 45}** |
| C7 | fc R66 | "w₃'s Tits lift in the exact e₆ is of order 3 with Ad-multiplicities (24, 27, 27)" | **VERIFIED** on B351's exact e₆: the seat's word of length 24 lifted as nᵢ = exp(ad eᵢ)exp(ad e₋ᵢ)exp(ad eᵢ) (exact 78 × 78, ad e nilpotent of degree ≤ 4): **N³ = I; eigenvalue 1 with multiplicity 24; ker(N² + N + 1) of dimension 54** (61 s) |
| C8 | fc R67 | "L_g, w_{A₂}, w₃ lift to order-3 elements of E₈ in the SU(9), E₇×U(1) and E₆×SU(3) classes" | **REGISTERED** with the re-run (RC=0); main has no exact e₈ bracket table |
| C9 | codex R033 | "B1231 admits an unpriced listener identification absent from its ledger … registering that admitted debt gives 3 UNEARNED > baseline 2" | **VERIFIED (re-run: every PASS line) and REPAIRED** — **I-29, the listener map u**, registered UNEARNED in the identification ledger with the dated baseline migration the ratchet requires; the codex's six Phase-C candidates recorded as typed, not adjudicated |
| C10 | hostile 16 FIRST_BEAT | "W = [[1, q],[0,1]] … its square is the meridian because q + q̄ = 1" | **VERIFIED** exactly in ℚ(√−3): W W̄ = [[1, 1],[0, 1]] |
| C11 | hostile 17, 18 | "Σ = exp(ad qE)∘gal extends the Gieseking element to e₆, Σ² = Ad(tick)"; "Ω² = A₂₇" | **REGISTERED** with the re-runs (RC=0 both) |
| C12 | hostile 19 S₄ torsor | "W(E₈) realizes the full S₄ on the four A₂ slots … the normalizer is computed exactly" | **REGISTERED** with the re-runs (RC=0; three certificates) |
| C13 | hostile 20 AW typing | "no transversal pair of the flat cone's order-96 group generates an ADE (SU(2)-type) stabilizer" | **REGISTERED** as the seat's typed negative (RC=0 both) |
| C14 | hostile 21 unit dictionary | "every geodesic eigenvalue is a unit of an explicit palindromic quartic … geodesic length is its log-Mahler measure" | **VERIFIED** on SnapPy's length spectrum of m004 to length 3.0: all **16** geodesics have trace t = a + bω ∈ ℤ[ω], the quartic (x² − tx + 1)(x² − t̄x + 1) palindromic and a unit, the eigenvalue a root, and **log M = ℓ to 10⁻⁹** |

Re-run receipts: all seventeen certificates exit 0 in their pinned worktrees (`verification/sliceC/*_rerun.txt`; fc's `r66_tits_lift.py` needs main's
B351 on its path, recorded). Two of main's own slips on the way, recorded: the Kac-class count without the central quotient (510 = 3 × 170), and a root
finder that failed on a squared quartic (replaced).

## 1. What this slice settles

Eight seat results now stand on main's own code; five more are re-run and registered; one omission of main's own ledger (the listener map) is a row.
None of it moves the chirality bit; all of it is structure the bit would have to live in, and the ledger says so item by item.

## Receipts, lock

`verification/sliceC/`: `c_su3_level2.py`, `c_geodir_h1.py`, `c_positivity_bridge.py`, `c_e8_types.py`, `c_kac_classes.py`, `c_tits_lift.py`, `c_beat.py`,
`c_unit_dictionary.py` with their `.out`/`.json`, the seventeen `*_rerun.txt`, `already_banked_at_seal.txt`; `tests/test_b1306_the_older_debt.py` (slice C tests).
