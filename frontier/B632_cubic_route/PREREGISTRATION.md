# B632 — THE CUBIC ROUTE, cell 1: the generation count h¹(M; 27_ρ) (prereg)

**Preregistered 2026-07-15, BEFORE running. Mathematics only — no SM
value enters this arc; any comparison of downstream outputs is a
separate sealed round requiring the owner's directive (the stopping
rule stands). This arc answers B308's named CRUX gate.**

## The MB13 sweep (run at prereg; results recorded here per the standing rule)

Chat-1's reframe ("we never computed the cubic — the Yukawa") swept
against the bank:

- **HIT — B308 (the Yukawa last redoubt):** the cubic question is NOT
  new. Banked: the E₆ Yukawa is the UNIQUE cubic invariant 27×27×27 → 1
  (multiplicity 1, Sage-verified); the COUPLING is forced but the
  TEXTURE S_ij is not; the texture needs three generations, gated by
  B307. Chat-1's "never touched the cubic" is FALSE at the invariant
  level and TRUE only of the texture — which B308 explicitly queued:
  "can the multiplicity route produce three generations with the right
  Yukawa texture?" This arc computes that gate's first fact.
- **HIT — B307 (the generation theorem):** no hyperbolic knot has a C₃
  trace field (C₃ cubics are totally real; trace fields never are) —
  the single-knot trace-field route to three generations is CLOSED;
  generations, if arithmetic, come only from a MULTIPLICITY mechanism.
- **HIT — B302/B305/B285:** the commensurator's hidden ℤ/3 (the one
  multiplicity mechanism previously named); the ω-trinification is
  WITHIN one 27, not across generations; ±π/6 lives in the state.
- **HIT — B299/B578-D8:** the triality ℤ/3 grades the 27 as
  {1: 9, ω: 9, ω²: 9} — a second, different "three" on the same 27.
- **CLEAN:** no arc has ever computed (i) the principal-sl₂
  decomposition of the 27 AS A LOCAL SYSTEM over the knot group,
  (ii) h¹(M; 27_ρ), (iii) the cup-product cubic texture on H¹(27).
  These are the genuinely uncomputed objects (grep "H1(27)", "h1 27",
  "cup.*27.*cubic", "Yukawa texture" → only B308's structural
  discussion).

## The observation that makes cell 1 decisive (derivable from banked facts)

ρ = principal ∘ ρ_geo (B575, exact in gl(27) over ℚ(ω)), so the
27-local-system decomposes into Sym^{2s}(ℂ²)-blocks of the GEOMETRIC
SL(2) local system, with spins s given by the principal-sl₂
decomposition of the 27. If each nontrivial block has h¹ = 1 (banked:
B575-G4 gives h¹ = 1 at every adjoint exponent; Menal-Ferrer–Porti
gives h¹ = #cusps = 1 in general) and a trivial block has h¹ = b₁ = 1,
then **h¹(M; 27_ρ) = (number of blocks)** — the heterotic-style
generation count (generations = matter-valued H¹), a multiplicity
mechanism DIFFERENT from B302's and NOT blocked by B307 (whose theorem
kills symmetric trace-field triples, not cohomology multiplicity).

## Cell 1 — the computation (two-outcome, locked before running)

1. **The decomposition:** eigenvalues of h_pr (B575's principal h,
   27×27 exact) on ℂ²⁷ → the spin multiset of the 27. (Candidates:
   {8,4,0} (17+9+1), {6,4,2} (13+9+5), or another partition — computed,
   not assumed.)
2. **h⁰ and h¹ of 27_ρ directly** by Fox calculus on ℂ²⁷ with B575's
   exact A27/B27 and the same relator walk (the module-agnostic L_a/L_b
   construction): h⁰ = dim ker(δ⁰), h¹ = (54 − rank[L_a|L_b]) −
   (27 − h⁰). Exact arithmetic over ℚ(ω); Euler gate h⁰ − h¹ + h² = 0
   with h² = 27 − rank[L_a|L_b].
3. **The cross-check (must agree):** h¹ from the block decomposition +
   banked per-block values.

## The locked verdict table

- **h¹(27) = 3:** the object carries a THREE-generation cohomological
  multiplicity — B308's gate opens; cell 2 (the cup-product cubic
  texture Y_ijk on H¹(27), via the unique invariant) becomes the queued
  computation, as mathematics, its own prereg.
- **h¹(27) = 2 or < 2:** chat-1's Yukawa route DIES at the same wall as
  B307/B308 (no three-slot structure in the 27's cohomology); banked as
  the honest kill; no texture computation.
- **h¹(27) > 3:** more slots than generations — banked as data; any
  generation reading requires a selection principle stated before use.

## MB12 (computed at prereg)

- Non-trivial: neither the decomposition nor h¹(27) exists anywhere in
  the bank (sweep above); the operation (exact Fox calculus on a NEW
  27-dim module) is a fresh computation, not a re-derivation.
- Can go either way: the spin multiset is not banked; h¹ = #blocks only
  if every block has the MFP value — the direct Fox computation can
  contradict the block prediction (that is what the cross-check is
  for); 2, 3, and >3 are all live a priori.
- Controls: the Euler-characteristic gate (must be 0); the relator
  gate (B575-G3, re-fired in the build); the block/direct agreement
  gate. Pipeline standard: exact arithmetic (no rank tolerance issues).

## What this arc does NOT do

No cubic texture yet (cell 2, own prereg, only on verdict h¹ = 3). No
diagonalization, no ratios, no angles, no SM numbers anywhere. The
adjudication of chat-1's five reframe points is banked in FINDINGS
alongside the result.
