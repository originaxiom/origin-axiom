# B418 (TW4) — PRE-REGISTRATION: the upstairs ℚ(ζ₆₀) structure (blind) — the B411 test

**Committed before computation. Destination Atlas wave 4 — the consequential one: track the
object where it is LOCAL (before Π_H). Disciplines + emergence bar fixed. Firewalled.
Machinery: cyclo_engine + seam_certification.sigma over the FULL 16-unit Galois group.**

## What we track (the object upstairs, before the Π_H projection)

The banked emergent symmetries (mirror M3, class action 2b-i, (2,3) stabilizer M5) fail
CELL-LOCALLY after Π_H. B411 hypothesis: they may be LOCAL in the full field ℚ(ζ₆₀), and
Π_H (averaging over the order-4 Gal(ℚ(ζ₆₀)/H)) is what makes them emergent. Track:
- T1: the FULL Galois-isotypic decomposition of the raw (1,2) t-table under
  Gal(ℚ(ζ₆₀)/ℚ) ≅ (ℤ/60)* (order 16) — apply sigma(·,c) for all 16 units; which isotypic
  components are nonzero (the seam √−15 is ONE of 16 — what are the other 15)?
- T2: THE B411 TEST — does the mirror t(a,−b) = (single Galois element)(t(a,b)) hold
  CELL-LOCALLY on the RAW (pre-Π_H) t-table for some c ∈ (ℤ/60)*? (M3 tested only the
  H-projected cells; upstairs is untested.) Same for the class action and the stabilizer.
- T3: name the upstairs object + its symmetry group.

## Bar check (four-part)
Does the upstairs symmetry group (whatever makes the emergent symmetries local) match an
EXACT SM structure (a gauge group, a finite group of the right order)? CONTROL: the same
decomposition for a canonical-lift (seam-null) table — the structure must be theta-specific.
BONUS (not the bar, but the campaign's deepest open): if the emergent symmetries DO become
local upstairs, that unifies 3 banked negatives into one positive theory (a real result
regardless of SM).

## Outcome (two-outcome, either banks)
- Upstairs-local: the emergent-symmetry family has a local origin ⇒ bank the upstairs
  theory (+ bar verdict on its group).
- Upstairs-nonlocal too: the emergence is intrinsic ⇒ bank that sharper negative.
Machinery exact ℚ(ζ₆₀); locks from JSON.
