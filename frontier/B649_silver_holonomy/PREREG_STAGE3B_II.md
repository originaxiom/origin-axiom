# B649 stage 3b-ii — TRACK S WAVE 6: THE SILVER Y-TENSOR (campaign a463c6aa)

**The last §B2 silver input: the alternating cubic on H¹(D_silver; 27),
computed with the certificate machinery ported to the silver
presentation, and the FORM-level comparisons against the banked fig-8
laws. Y's overall scale is basis-dependent; only patterns and ratios
are form-level content — sealed as such.**

## Conventions block

- All stage-3a/3b-i conventions (letters, weld, the 5 H¹ representatives
  in the computed flag order — the σ*-triangular order IS the mode
  order, mirroring the fig-8's mode labeling).
- The certificate machinery ported: `certify` generalized to BOTH
  silver relators (insertions from {R1±, R2±}, beam ≤ 1500, depth ≤ 24);
  the corrected relator bar 2-chain (the B632/B637 inverse-letter
  correction, prefix-BEFORE-the-letter) built PER RELATOR; `zval` for
  three generators; `fox_positions`, `make_omega`, `S_eval` as in
  b637 with the multi-relator certificate loop pairing each step with
  its own relator's chain.
- Sections: σ(μλ) = freduce(μ·λ) used for BOTH orders (the (λ,μ)
  defect = the peripheral commutator — the one nontrivial certificate);
  side 2: μ₂ = μ-word, λ₂ = inv(λ-word), σ = freduce(μ·inv(λ)).
- Y(x,y,z) = [S₁(μ,λ,σ₁) − S₁(λ,μ,σ₁)] − [S₂(μ₂,λ₂,σ₂) − S₂(λ₂,μ₂,σ₂)].

## Gates

- **G1 (machinery):** the δS = ω validation battery passes EXACTLY on
  both sides, including a certificate-bearing test (the gate class
  that caught both fig-8 bugs); the peripheral-commutator certificate
  found (its AREA reported — the fig-8's was 2; no prediction).
- **G2 (the table):** all 10 triples Y[ijk] exact over L on the flag
  basis.
- **G3 (form-level, two-outcome each, NO predictions):**
  (a) the zero pattern vs the fig-8's (Y[01k] ≡ 0-type slots?);
  (b) the core-ratio SHAPE: is Y[0jk] = u·Y[1jk] for a single u ∈ L
  across the spectator pairs where both are nonzero — and is u of the
  form (rational)·(root of unity in ℚ(i))?;
  (c) the cross-ratio (Y[023]·Y[134])/(Y[034]·Y[123]) where defined —
  equal to 1?;
  (d) the σ*-law Y∘σ* = conj(Y) spot-checked on 2 triples (consistency
  with the banked real structure).

Outcome: whatever the table says, it banks; each (a)–(d) verdict is a
grammar-table row entry (FORM reproduced / FORM differs, with the
exact silver values).
