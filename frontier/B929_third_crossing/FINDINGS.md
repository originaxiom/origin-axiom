# B929 — THE THIRD CROSSING: HIT-SHAPE — the twist's register sheet is a mixing-shaped cascade; the magnitudes are not CKM's

**Date:** 2026-08-06 · **Prereg:** sealed `672b5afb…`, pushed `c8421f14` BEFORE
data contact; owner authorization "keep going" at the registered gate.

## The sealed verdict, verbatim

- **TIER 1 (shape): PASS.** The blind triple s = (0.8629, 0.8001, 0.6964)
  (the |m_S| branch values, ascending-ρ) is a same-side descending cascade
  with acceleration index **a = ln r₂/ln r₁ = 1.838 ∈ (1, 3)** — the declared
  second-order cascade signature. The measured cascade's own index is 1.39
  (reported for transparency): both sit in the band; they are not close.
- **TIER 2 (quantitative): MISS.** r = (0.927, 0.870) vs q = (0.182, 0.094):
  off by factors ≈ 5–9 in both orientations — far outside the sealed
  factor-2. The direct identification "m_S ratios = CKM ratios" is DEAD.
- **Verdict: HIT-SHAPE** (T1 ∧ ¬T2), with the two priced bits (family,
  orientation) and the broad T1 band weighing the result DOWN by design:
  suggestive, not probative.

## The honest reading (fenced)

What passed is real but weak: a blind, exact, flavor-input-free computation
produced a mixing-*shaped* accelerating cascade on the generation registers —
the T1 prior (lean MISS) was mildly overruled. What failed is decisive at its
own level: the sheet's raw ratios are NOT the measured mixing ratios; if the
twist speaks to measured mixing at all, it is through a nontrivial map, not
by direct reading. Per the seal: other sheet readings (m_A primary; colored
invariants; derived functions) are NOT licensed retries — each would need its
own seal, its own priced bits, this record cited, and the owner's word.

## The scoreboard (eight sealed cells)

Object-overrules: five (B890, B891, B897, B912, B928 — now with B929-T1 as a
half-grade overrule). Disciplined wins: four (B915, B925, B929-T2, and B928's
route-prior). The instrument reads true in both directions, still.

## Secondary recorded (non-verdict, per seal)

m_A ratios (0.9-ish cascade, same character); the v-weight ratios
(1.209, 1.529 — an ASCENDING cascade, a different shape class from mixing —
recorded); the cube law and sum rule printed as internal anchors.

## Files

`third_crossing.py` → `results.json`; locks in `tests/test_b929_crossing3.py`.
