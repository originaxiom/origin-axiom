# B594 — the E₆₂ hearing law: the sine kernel is the ear's coefficient

**Status: banked (frontier). Nothing to `CLAIMS.md`; no SM quantities.
Preregistered. Locks `tests/test_b594_e6_hearing.py`.**
Run: `python3 e6_hearing.py` (pyenv, ~30 s).

## The structural unlock (H1): the law is state-independent

The hearing law's chiral amplitude −2ε²(u†W(g)u) does not involve the
listener's state components — ψ enters only the baseline and the
O(ε)-cancellation, which needs only Cψ = ψ (guaranteed for bare knot states
structurally, B586-R3C). Verified: the law holds EXACTLY for random
C-symmetric states (3 seeds × 3 directions × 2 welds × 2 ε × twisted/plain).
**The E₆ colored-invariant method gap that blocked R3-C does not block the
hearing law.**

## H2 — the coefficients are the banked amplitudes (the handoff's prediction)

On E₆ level 2, the three diagonal hearing coefficients equal MINUS B589's
banked sine-kernel closed forms, to 1e-9:
> u_j†(Cρ(A₁))u_j = −(2/√7)·sin(2πj′/7)·ζ₁₄^k, (j′,k) = (1,+3),(3,−2),(2,−1),
each with Im ≠ 0 — **the ℤ/7 sine kernel is the E₆ ear's coefficient.** The
two-stage picture is complete: same law, stage-specific numbers (golden:
1/(2φ) + i·sin(2π/5)/√5; E₆₂: the sine kernel × 14th roots).

## H3 — the full 3×3 odd form (new numbers)

The complete quadratic form of Cρ(A₁) on the θ-odd 3-space is banked in the
run output (off-diagonals new); trace = −1 (= −C3's odd trace ✓) and the
untwisted block has order 4 (C3 ✓).

## Adjudication note on the arc handoff

The handoff's program-state row "Three generations: MATCHED at weight level,
mechanism pending" is an overclaim relative to the (paused, unbanked)
B590-V3 blind tables: the φ-orbit distribution across the three 16s is
HETEROGENEOUS (five distinct orbit profiles; pairwise 16∩16 = 10, triple = 6)
— no clean orbit ↔ generations correspondence at the weight level as
computed. The row should read "orbits ↔ 16s: computed, no clean
correspondence (bank pending — B590 paused by owner)." Also, in the handoff's
field remark, ℚ(√5, ζ₅) = ℚ(ζ₅); the golden-stage amplitude is exactly
(√5−1)/4 + (ζ₅ − ζ₅⁻¹)/(2√5) ∈ ℚ(ζ₅).

## Anchors

B593 (the law), B589 (the amplitudes — now the coefficients), B586 (stage +
R3-C's structural theorem), B592-OPEN (sign flip/parity), C3 (order 4,
trace 1).
