# B594 — the E₆₂ hearing law (prereg)

**Date: 2026-07-14. Registered BEFORE the computation. Firewall: nothing to
CLAIMS.md; no SM quantities. The arc handoff's endorsed item 1.**

## The key structural point (registered as a claim to verify)

The hearing law's chiral amplitude −2ε²(u†W(g)u) is **state-independent**: the
listener's state ψ enters only the baseline A₀ and the O(ε)-vanishing (which
needs only Cψ = ψ — guaranteed for bare knot states by the structural theorem,
B586-R3C, WITHOUT computing the components). Therefore the E₆ colored-
invariant method gap (which blocked R3-C) does NOT block the hearing law:
the E₆₂ chiral amplitudes are computable exactly.

## The cells

- **H1 (the law on E₆₂, ψ-generic):** verify A_ε = A₀ − ε²(u†Wu) with NO O(ε)
  term for RANDOM C-symmetric states ψ (several seeds — the law must hold for
  every such ψ; the true 4₁ state is one of them), u in the θ-odd 3-space,
  welds g ∈ {I, A₁-word}; the sign-flip under the twist.
- **H2 (the coefficients = B589's amplitudes):** the diagonal ε² coefficients
  u_j†(Cρ(A₁))u_j must equal −p_j where p_j are B589's banked closed forms
  (2/√7)·sin(2πj′/7)·ζ₁₄^k — i.e. **the sine-kernel amplitudes ARE the
  hearing coefficients** (the handoff's prediction). Registered check: exact
  to 1e-7 against the banked values, and Im ≠ 0 (the hearing is chiral).
- **H3 (the full form):** the 3×3 odd quadratic form of Cρ(A₁) (off-diagonal
  entries recorded — new numbers); order-4 consistency with C3.
- **Adjudication note (the arc handoff):** the handoff's program-state row
  "three generations: MATCHED at weight level" is corrected against the
  (paused, unbanked) B590-V3 blind tables: the φ-orbit profile across the
  three 16s is heterogeneous (five distinct profiles), NOT a clean match;
  the row should read "orbits ↔ 16s: computed, no clean correspondence
  (pending bank)".

## Falsifiers

- H1 failing for any C-symmetric ψ ⇒ the state-independence argument is wrong
  — stop.
- H2 mismatch ⇒ either B589's values or the law's E₆ transfer is wrong —
  diagnose before any claim.

## MB13

B586/B589 (the stage + the banked amplitudes), B592-OPEN (sign flip, parity
conservation), B593 (the law on the golden stage), C3 (order 4). Nothing
rediscovered; the E₆₂ law itself is uncomputed anywhere.
