# B3 + THE GATE B VERDICT (B652; campaign a463c6aa)

## The freedom count N

Per-organ, from the sealed table (GRAMMAR_TABLE.md):

- **EAR: N = 1, discrete, orbit size 2** — the Galois branch of the
  golden character (χ vs χ̄ of SL(2,𝔽₅)); one choice across trace law,
  tones, and shadow character (proven identifications, B642/B644).
- **CHORD: N = 0** — every quantity is FORCED, COMPUTED (exact, per
  object), CONVENTION (gauge), or WALL. The lit-class adic deviations
  are outputs, not inputs (adjudicated in-table). A discrete gluing
  choice exists ONLY if a Phase-C channel uses chord observables
  (orbit = the design's enumerated menu; counted at C1 if used).
- **LADDER: N = 0** — the melody theorem bounds the organ's entire
  content by one finite period of computed integers.
- **SCALE: N = 0 structurally** — R5 restricts Phase C to
  dimensionless observables; the object's no-scale walls make this a
  theorem-backed restriction, not a modeling preference.
- The stage itself (κ = 5) is a DESIGN choice under a declared
  minimality rule (A4 residue below), not a fitted constant; it enters
  the look-elsewhere accounting only if multiple stages are probed
  (the frozen channel rule does not).

**N = 1, discrete, orbit size 2.** The vacuity check both ways: N ≠ 0
(the branch genuinely cannot be derived — B642's sealed 3/3 shows both
branches are realized across Galois-conjugate stages; the grammar
cannot prefer one), and N is finite and enumerable (orbit = {χ, χ̄}).
The N = 0 branch (C′) does not fire — but the design lands one step
away from it: **after ONE BINARY calibration, every further same-type
dimensionless observable is a zero-parameter prediction.**

## The arity condition

N_eff = 1 discrete bit ≤ dim(any admissible calibration observable):
**satisfied for every candidate channel** — a single measurement
(indeed a single sign/branch discrimination) fixes the orbit point.
The look-elsewhere budget for C2's null: orbit size 2 × (number of
passing channels) — a multiplicity of 2–4, to be counted exactly in
the C2 seal per the campaign's full-design-space rule.

## The A3/A4 residues (stated per the gate's honesty clause)

- **A3 (the coupling-inventory completeness):** the fiber-paired
  D_conjθ coupling's dimension remains open (the external machine's
  packet has not arrived). BOUNDED as the campaign's exit clause
  allows: B639 proved the object is not an amalgam local system; any
  invariants it carries are, by the same typing as Y
  (T_coup(partner)), computed-per-object quantities — the same
  adjudication that keeps chord outputs out of N. A future resolution
  ADDS PREDICTIONS, not freedoms, unless it surfaces a new choice —
  in which case the C2 seal reopens BEFORE comparison (binding note).
- **A4 (stage-selection obligations (1)–(3)):** open as banked; κ = 5
  remains the declared minimal-bearing CHOICE. Obligation (4) is
  discharged (B650 + B644 + the equivariance wall). The probe's
  license does not require (1)–(3) — the stopping rule's condition
  (ii) reads on the discharged artifact set — but the VERDICT of any
  Phase-C outcome must carry the "stage = choice" qualifier verbatim.

## THE GATE B VERDICT

**PASS, with the two residues above named and binding.** The grammar
table is sealed; the freedom count is N = 1 (discrete, binary); the
arity condition holds for every candidate channel; the silver control
completed with the verify column filled (B649 + B651 receipts); no
kill fired (K2 vacuity checked; K3 never triggered — no FORCED row
demoted, one row scope-refined with the fig-8 scoping it always had).

**Phase C is licensed to open** — with one structural dependency that
is not this seat's to satisfy alone: the campaign's three-way seat
separation (sealer ≠ predictor ≠ comparator) requires cross-seat
execution, and C1's frozen channel rule needs the owner-side relay to
the other seats. The campaign therefore PAUSES HERE FOR THE OWNER with
GATE B passed, per the sealed design.
