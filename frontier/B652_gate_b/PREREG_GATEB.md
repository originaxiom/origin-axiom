# B652 — GATE B ASSEMBLY (campaign a463c6aa; B1 + B3)

**Objective: the coupling-grammar table (B1) and the freedom count (B3),
assembled from the banked record ONLY — every label cites its theorem/
lock; the shadow quotient merges rows only on LAW_MAP-grade PROVEN
identities; MECHANISM-OPEN rows are adjudicated (resolve / worst-case
orbit / shown-to-carry-no-freedom with the argument stated); the arity
condition evaluated. The verdict states the A3/A4 residues honestly.**

## Conventions block

- Sources: LAW_MAP as of B651; the silver verdicts (B649); the gauge
  adjudication (B647 cell 3); the equivariance wall (B650); the ladder
  upgrades (B651). No new computation — assembly and adjudication only;
  any claim not backed by a banked lock is labeled accordingly.
- "Free constant" = a value the grammar provably cannot determine that
  a Phase-C calibration must fix; computed-but-unexplained values
  (MECHANISM-OPEN) are NOT free — they are predictions-in-waiting —
  unless a hidden choice is identified, in which case worst-case orbit.
- R5 (dimensionless, core-coupled, same-type) restricts the observable
  class; T_coup(listener) quantities are excluded from N by type.

## Gates

- **GB-1:** every table row carries organ, label, and evidence pointer;
  zero rows assert beyond their banked strength.
- **GB-2:** the shadow quotient's merges each cite a PROVEN identity.
- **GB-3:** N computed with the per-organ decomposition and the
  discrete/continuous split; the arity condition stated; the N = 0
  branch checked.
- **GB-4:** the A3/A4 residues stated; the verdict declares PASS /
  CONDITIONAL / BLOCKED honestly.
