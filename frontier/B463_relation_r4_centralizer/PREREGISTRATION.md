# B463 — PREREGISTRATION: Relation R4 — the principal centralizer (the 4d-commutant cell, scoped)

**Committed before computation. Firewalled. Campaign: OPEN_LEADS §Relation. Runs FIRST (it
gates the 4d framing of the campaign).**

## The question (blind)
Chat-1's path 4 claims "the 4d theory's gauge group is determined by the commutant of ρ₀ in
E₆." The commutant of the PRINCIPAL SL(2) (the program's banked E₆ route — B347/B351/B445) is
uncomputed (B247/B254 did the long/short A₁ — different embeddings). Compute it exactly.

## The computation
C_𝔢₆(principal sl2) = {x : [x,e]=[x,h]=[x,f]=0} from B351's exact algebra. Prediction (stated):
ker(ad e) has ad-h weights {2,8,10,14,16,22}, all > 0 ⇒ the weight-0 intersection = 0 ⇒
**algebra centralizer = 0** ⇒ the group centralizer is FINITE. The identification "= Z(E₆) =
ℤ/3 (simply connected; trivial for adjoint)" is **Kostant's theorem — a lit-gate citation, not
booked as computed.** The Zariski-density step stated: the holonomy is irreducible ⇒ its
centralizer equals the SL(2)'s.

## The scoped consequence (binding language)
No continuous 4d gauge group arises from the **principal** lift. NOT claimed for every lift:
the long-root lift gives SU(6) (banked B247) — which is the CONTROL GATE: the same computation
on the long-root A₁ must reproduce dim C = 35 (𝔰𝔩₆) before the principal result is read.

## Bins
Algebra centralizer = 0 (predicted) → the commutant hope closes for the banked route; the 4d
lift remains the four-times-named specialist boundary (B278 W4, B259 W4, Gate B/Dimofte, L50).
Centralizer ≠ 0 → contradicts Kostant/B429's rigidity — anomaly, adversarial recompute first.
