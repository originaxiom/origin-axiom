# B985 — cc3's relational batch verified and integrated: one closure falsified, six scoped, three taxonomy rulings

**Date:** 2026-08-09 · **Seat:** cc (banking) · **Lane:** MATHEMATICS + governance. Gate 5 untouched.
**Integrate-don't-merge:** nothing from `origin/audit/b775-braver-questions` was merged. The
load-bearing numbers were **re-derived on this bench** before any of it was acted on.

---

## cc3's one finding, and it is a measurement rather than a policy

Three campaigns ran on unrelated material — a lead ledger, an arc corpus, a face taxonomy — and
each returned the same defect **without looking for it**:

| instrument | built to hold | what fell through |
|---|---|---|
| lead closures | statements about *the object* | a closure survives the relational re-read **exactly when its scope sentence names no manifold** |
| the face classifier | faces named by an **object** — infinite-hecke 100%, hearing 83%, sln-tower 79% | faces named by a **relation** — meeting **6%**, coupled-double **8%**, mtc-overlay **19%** |
| the anatomy itself | kills (`kill_graph`) | **every positive result** — so the eleven faces describe **what the object is NOT** |

> **Our instruments hold objects, and relations fall through them.**

The owner's standing judgement — *after B800 the object is the maturity of its relations* — arriving
as a **measurement**, three times, from three directions. A fourth instance is the sharpest because
it happened *inside the audit*: a re-read agent marked L63 REOPEN by quoting B578's *"L63 stays
OPEN"* while missing B666, which **constructs** the demanded map 36/36 exact. Withdrawn on
verification and **recorded rather than deleted**, which is the correct treatment.

## Verified on this bench before acting

**The torsion table — 6/6 exact** (snappy 3.3.2, homology read directly, not via |2 − tr M|):

| manifold | row | cusps | H₁ | \|torsion\| |
|---|---|---|---|---|
| **m004** | golden | 1 | **ℤ** | **1** |
| m003 | golden | 1 | ℤ/5 + ℤ | **5** |
| m206 | golden | 1 | ℤ/5 + ℤ | 5 |
| m136 | silver | 1 | ℤ/2 + ℤ/2 + ℤ | 4 |
| m129 | silver | **2** | ℤ + ℤ | 1 *(excluded: two cusps)* |
| m135 | silver | 1 | ℤ/2 + ℤ/4 + ℤ | 8 |

**The base-rate control — reproduced.** cc3: 60.8% of one-cusped census manifolds have trivial
torsion, 47.8% at vol < 4.1. This bench on a 30 000-manifold volume-ordered sample: **63.2%** and
**48.5%**. **Trivial torsion is a coin flip.**

## What that settles

1. **L73's closure is FALSIFIED** — and it needed no base rate. The closure banks abelian
   invisibility as a property of *the object*; **its own sister m003 has |torsion| = 5**, at the
   hearing prime. A property that fails *inside the commensurability class* is not a property of
   the class.
2. **The attractive successor is REFUTED — by cc3, in the same file, within the hour.** The
   inversion *"invisibility is not a property the object HAS but one that SEPARATES it from every
   one-cusped relative"* is a base-rate artefact: post-hoc statistic, non-independent draws, n = 6,
   ≈1.9% under an assumption that does not hold. **No selection claim survives.**

**That second point is the transferable one.** The day produced three surveys and one computation,
and **the computation is the only thing that changed a verdict** — first by falsifying a closure,
then by refuting its own attractive successor. *Discovery was never the bottleneck.* And proposing
a reading then killing it **in the same file** is exactly right: it cannot be re-registered later as
a fresh idea by a seat that reads only the first half.

## Decisions executed

**1. The `FINDINGS.md` glob is fixed** (`scripts/forcing/build.py`). It required that exact
filename and silently skipped **42 arcs** carrying real content under another name — **B1–B5, the
programme's first five arcs** (`README.md` only), B68 `FINDINGS_E.md`, B473 `FINDINGS_C1.md`, B511
`D3_FINDINGS.md`. Hygiene, not discovery — cc3 checked and the 77% moves only within 58–79% either
way — **but any re-run of the attachment inherits the blind spot until it is fixed**, so it is
fixed *before* the re-run.

**2–3. Relays 1 and 2 are applied together, and the 441 are not treated as attachments** (48%
strict precision, 49% recall — cc3 reported both as poor rather than dressing them up). A worklist,
not an attachment.

**4. Scope-correction notes attached to the six OVER-WIDE closures** — L54 (most dangerous), L1,
L57, L84, L93, and **L77 withdrawn rather than re-tested** (E₆ gives ord(T_k) = 12, 84, 180, 48
against 3κ = 39, 42, 45, 48 — agreement at k=4 only, a coincidence). Each names the quantifier
actually proved and the one computation that settles it. **Notes, not reopenings.**

**5. L73 and L98 held out of any "closed" count.**

**6. Three taxonomy rulings, made explicitly rather than by default:**
- **`character-variety` ADMITTED as the twelfth face** — it is *structure of the object*, not a
  chart on it, and **the primitive shears L, R are axioms A2–A4 themselves**. It is Layer 0 of the
  framework. Three readers named it independently from residue.
- **`observer` is NOT a face — registered as an AXIS.** It *acts on* the object rather than being a
  region of it; B717 agrees (the object supplies incompletenesses, the observer supplies closings,
  and closings are operations, not organs).
- **`physics-door` is NOT a face — a BOUNDARY LABEL.** Making it one would repeat the category
  error that produced the current anatomy.

## Scope

The falsification and the base rate are **computed and independently reproduced**. The six scope
corrections are **judged** (cc3's agents, this seat's adjudication) and are **notes on quantifier
width, not new mathematics**. The taxonomy rulings are **decisions**, not findings. cc3's own
calibration is adopted verbatim: *the mechanical claims are reproducible and were re-derived; the
judged claims are agent work and are labelled as such.*
