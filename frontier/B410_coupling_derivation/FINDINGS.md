# B410 (Phase 2b) — the coupling-channel derivation: advanced, crown residual STAGED

**Status: 2b-i banked (negative); 2b-ii partially derived + the deep residual staged with
its exact obstruction. Prereg committed first. Firewalled.**

## 2b-i — the class action is EMERGENT, not window-local (banked)

No affine window involution ι realizes σ_cl (t(ι) = σ_cl·t) on the banked cells —
exhaustive search over all affine (a,b)↦(αa+β,γb+δ), αγ units: EMPTY. The class-group
generator acts at TRACE/aggregate level (B401's constant-orbit) but has NO cell-local
window origin — the M3 pattern (the mirror is emergent) now confirmed for σ_cl too. The
ℤ/2-everywhere family is genuinely aggregate. (b2i_window.json.)

## 2b-ii — the cancellation criterion (derived on the correct object) + the staged why

- **The Π_H-per-side hazard, 4th appearance (caught by the bright controls reading equal
  to dark):** the criterion CANNOT be stated as a per-side H-projection (all pairs read
  √−3-free on the 3-side under per-side Π_H — the trap). Recorded.
- **The criterion on the CORRECT full-field object (B393 product strata): SEPARATES 4/4** —
  bright ⟺ some full-field product X₃·X₅ carries √−15 (counts: (1,3)→0, (3,5)→0 dark;
  (3,4)→24, (2,3)→18 bright). This is P67 at PRODUCT resolution, with the mechanism =
  M1's stratification law (s-darkness ⟺ the 5-side never donates √5 to an imaginary
  product) — BANKED. So the crown's acceptance test is met AT THE CRITERION LEVEL.
- **The deep residual (STAGED with the exact obstruction):** WHY the dark-C 5-side
  withholds √5 at exactly the X₃-paired frequencies — this is NOT a per-side statement
  (the Π_H hazard forbids it), so it requires the full-field convolution's frequency
  structure; it is the genuine specialist-adjacent core and STAGES here. The coupling
  theorem = M1 stratification (banked) + this residual (staged).

## Verdict
The coupling channel's WHY reduces to the banked M1 stratification law; the class action is
emergent (2b-i); the irreducible residual is cleanly bounded and staged. Phase 2b advanced
the crown to its exact boundary — a two-outcome success (derivation-to-M1 + a named wall),
not a forced closure.

**Provenance.** b2i_window_action.py, b2ii_criterion.py (the hazard exhibit),
b2ii_fullfield.py → three JSONs; locks tests/test_b410_coupling.py.
