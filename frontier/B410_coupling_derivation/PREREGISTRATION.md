# B410 (Phase 2b) — PRE-REGISTRATION: the coupling-channel derivation

**Committed before computation. The masterplan's crown (content, not scale). Posed as a
QUESTION, not a foregone conclusion; reconciled with M3 (the mirror is EMERGENT — no
cell-local origin). BANK-or-STAGE per leg. Firewalled.**

## The registered question (2b-i)

Does an involution ι on the level-15 windows (a,b) ∈ ℤ₂₀×ℤ₁₂ exist with

    t(ι(a,b)) = σ_cl·t(a,b)   on ALL banked cells,

where σ_cl = (x,y,z,s)↦(x,−y,−z,s) is the nontrivial class of Cl(ℚ(√−15)) (P1/B401) and ι
is a window map (affine (a,b)↦(αa+β, γb+δ) or a small extension)? P61's mirror t(a,−b) =
τ₃(t) is the KNOWN Galois-covariance; σ_cl is a DIFFERENT Galois element (the class-group
generator, flips √5 and √−3, fixes √−15). Test all affine ι exhaustively.

- If ι EXISTS (cell-wise): the class group acts geometrically on the windows — bank the
  window-level class action (the ℤ/2-everywhere family's origin, cell-locally).
- If ι does NOT exist cell-wise: this is the M3 pattern again (σ_cl is emergent) — bank
  the negative; the class action is trace-level only (consistent with B380's √5-flippers
  failing at cell level). Either way the STATUS of the class action is pinned.

## The binding acceptance test (2b-ii) — the crown's two-outcome gate

Derive the convolution-cancellation mechanism: WHY {(1,3),(1,4),(3,5)} cancel against the
exact rank-2 pairing B while {(3,4),(2,3)} (local-bright, P67) do not. The registered
candidate: the dark pairs' 3-side spectrum X₃ lies in the LEFT-KERNEL-COMPLEMENT sector
that B maps into the σ_cl-ODD subspace, whose H-average kills s; test whether a single
exact linear-algebra criterion on (X₃-span, X₅-span, B) separates the 3 dark-C pairs from
the 2 bright controls 5/5. DERIVE the criterion (state it, verify 5/5) or STAGE with the
exact obstruction. This is the channel's WHY; passing it is the coupling theorem's core.

## Machinery
B401 σ_cl; the banked step0 tables; B390's pairing B + field_coords; the M1 spectra.
Exact throughout; locks per leg. 2b-i this session; 2b-ii is the multi-session long pole.
