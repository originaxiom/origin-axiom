# B502 — THE PARITY SIGNATURE: does 2 ramify in everything interaction creates? (prereg)
**Observation (preregistered as question):** interaction-born fields so far have EVEN d_K ({12, −1588});
the geometry-born child is ODD (−283). **Conjecture to prove-or-kill:** every irreducible isolated
fixed-point field of a mixed word has 2 | d_K. Route (prove, not census): the trace-coordinate model —
fixed-point ideals of words in F,M,D over ℤ; reduction mod 2 structure (F,M,D all have distinguished
mod-2 behavior: x²−2≡x², xyz−x²−y²+2≡xyz+x²+y²); test whether the eliminants are forced to be
non-separable mod 2 (⇒ 2 ramifies) — a Newton-polygon / mod-2 factorization argument. Numeric layer:
the B499 census 2-ramification column. **Kill: ONE interaction-born field with odd discriminant.**
If proved: the orientation ℤ/2 (the program's residue — norm/parity/breath) reappears as an arithmetic
superselection rule separating the two creative registers. Firewalled; the reading stays in S063.
