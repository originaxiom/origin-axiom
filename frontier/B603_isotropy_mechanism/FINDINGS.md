# B603 — the J-isotropy mechanism (a one-line theorem) + the E₆₂ center data

**Status: banked (frontier). Nothing to `CLAIMS.md`; no SM quantities.
Predictions registered in the script docstring before running (MB12
triple-checked). Lock `tests/test_b603_isotropy.py` (OA_SLOW).**

## P-A/P-B — THE ISOTROPY LAW, weight-forced at every block

g_m = (v_m v0)ᵀ J (v_m v0) = 0 at ALL six blocks (P-A confirmed), and the
mechanism is total:

> **THEOREM (weight isotropy).** v0 (the Ω-null state) is weight-PURE at
> weight 0 (computed: support = {0}). Hence every bent state v_m·v0 is
> weight-homogeneous of weight 2m ≠ 0; J is supported on weight-opposite
> pairs; so ⟨v_m v0, v_m v0⟩_J pairs weight 2m with 2m and is empty.
> Every bent state is J-isotropic, for every block. ∎

The move-across identity g_m = −ε_m⟨v0, v_m²v0⟩_J verified exactly at all
six blocks (the same weight argument kills the right side: weight 4m ≠ 0).

## THE LABEL CORRECTION (to the step-5 lemma cell / steps3and5 narrative)

The banked L1 zeros ⟨v_m v0, v0⟩_J at the θ-odd blocks were labeled
"GENUINE (not forced)". That label was CRITERION-RELATIVE and is hereby
sharpened: the PARITY criterion (ε_m·c ≠ −1) does not force them, but the
WEIGHT criterion does — weight(v_m v0) = 2m pairs with weight 0 only if
m = 0. **All single-generator L1 contractions are weight-forced.** What
remains genuinely unforced (and yet holds) is step 4b's R1 ≡ 0 on WORDS:
group letters (exp of root vectors) mix weights, so weight-counting does
not apply there; the odd-block word-level t¹ silence keeps its
not-forced-yet-true status. No verdict changes; the forced/genuine map
now names its criterion. (Supersession note only; the P3 outcome is
untouched.)

## P-C — the E₆₂ center measurement (registered as measurement; data)

- Quantum dimensions of the nine primaries: {1, 2cos(π/7)-family,
  1.80194-family, 1, 1}; **the simple currents are primaries 3, 4 =
  (2,0,0,0,0,0) and conjugate** — the E₆₂ echo of SU(3)₂'s (2,0) simple
  current (B602-N0's d = 1 line).
- **The θ-odd sector is entirely center-CHARGED:** the three odd pairs
  carry conjugate ℤ₃ charges (2,1), (1,2), (1,2) — no charge-0 odd
  direction exists. The hearing sector lives entirely off the center's
  trivial sector.
- The V3 real channel (eigenvalue −1) is a three-way mixture with
  |components| ≈ (0.3626, 0.4522, 0.8149) on (27, 351′, 351) — dominated
  by the 351, aligned with no single pair (recorded as data; no closed
  form claimed).

## Consequences

The J-Gram was structurally incapable of carrying the value bridge (B602's
N0, now explained). Canonical per-line scales must come from
weight-INHOMOGENEOUS objects (torsions, word responses) or field-norms.
The center data gives the stage lines new exact labels (charges, currents)
for the norm-form arc's pairing constraints.
