# B547 — The Ghost Scanner (pre-registration; build queued)

*Committed 2026-07-12. Next cell of the window; skeleton only in this PR.*

Per level c ∈ [1,50], per canonical class, per slot: |x| ≤ 1 → finite
negative-definite/downward-parabola check (exact); |x| = 2 → linear (exact);
|x| ≥ 3 → Pell leg with fundamental-domain bound (exact where h(x²−4) = 1;
else class-work flagged). Output per level: PROVED-ghost / PROVED-realized /
needs-class-work. Targets: upgrade chat-2's evidence census to theorems;
resolve (4,4,16)/(4,16,60) (the recorded discrepancy); the all-hyperbolic-
ghost question. Predictions: c ∈ {1, 2} PROVED ghost (c=1 done, B545);
(4,4,16) resolves one way or the other by the |x| ≥ 3 leg — no prediction
which (the discrepancy stands open). Falsifier for the elliptic-lock law:
a proved ghost with an all-hyperbolic class.
