# Probe B24 -- Anyon / quantum bridge

> **Speculative frontier work.** Logged observations only.

## Question

Can the half-step kernel derive the `SU(2)_3` Fibonacci anyon level, or is
`k=3` inserted?

## Result

For `SU(2)_k`, the fundamental quantum dimension is

```text
d = 2 cos(pi/(k+2)).
```

Setting `d=phi` gives `k=3`. The half-step kernel supplies several appearances
of `5` (discriminant, index), but no derived rule mapping that `5` to `k+2`.

## Verdict

`STALLED`: Fibonacci level `k=3` remains inserted unless a level-selection rule
is derived.
