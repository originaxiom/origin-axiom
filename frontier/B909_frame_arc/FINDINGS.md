# B909 — the frame arc banked: the Compact Measurement Theorem verified (with the corrected instrument), the six-cubic law, the invisible 12 — the LAW_MAP debts PAID

**Date:** 2026-08-06 · **Seat:** cc (banking, verifying solo handoff 6 §XLIX–LVIII)
· the arc whose absence was the loss audit's most-urgent item, now closed.

## The Compact Measurement Theorem (§LVI) — VERIFIED, corrected-instrument

At κ's genuine roots — **five (root, prime) pairs including the fully-split
prime 40039** — the compact wall centralizer types uniformly:

> **dim z = 30 · derived ≥ 28 · z∩core = 18 · center 2 ⟹ z = so(8)⊕u(1)²,
> core-shaped, core-distinct (z∩core = the cross-shadow 18).**

**The instrument story, told whole:** the shipped cmt.py carried the RETRACTED
§LIV septic as its wall polynomial — its "roots" were phantoms (κ mod 40031
has none), and centralizers there read the generic floor (12), contradicting
the ledger. Caught by root mismatch against κ; the corrected instrument
(`cmt_correct.py`, κ as the wall polynomial) confirms the ledger at MORE pairs
than the original claim. The retraction-propagation failure (a retracted
section's instrument circulating in a handoff) is routed to B920/RETRACTIONS.

## The rest of the frame (verified on this bench, per script)

- **kfield**: the One-Field theorem — κ splits [1,2] over K; the wall-triples
  in canonical Galois bijection under one S₃.
- **nu_exact**: ν = c·κ⁶ EXACT (degree 18; the §LV correction reproduced);
  κ's constant −19³; disc-kernel {7,11} — **the six-cubic √77 law** stands.
- **mixed**: the nesting/branch table — the cross-shadow 18 (derived 15,
  center 3); **the Killing-perp/invisible 12** with zero torus-invariance
  defects, full-rank charge actions, M12∩core = 0, and the SMT-block kernel-4
  fingerprint — the §LVIII identification's data verified.
- **census**: the FA3 atom-census seal CONFIRMED 8/8 (the law's fourth face).
- **cw**: banked separately as B919 (3/8, one-prime tier, honest).
- Also standing from earlier session verification: the diagonal (2,2) Killing
  Gram (independently rederived exactly in B907's completeness pass).

## Lessons (three instrument findings)

(1) cpen's 20 CPU-hours in a redundant symbolic determinant — sympy blowup;
the quotient leg is an optional tooling follow-up, NOT a mathematics gap
(nu_exact's interpolation already proves the κ⁶ structure). (2) The stale
pre-retraction instrument — the sharpest retraction-hygiene datum the
programme has. (3) Root-set comparison against the certified polynomial as
the cheap wall-instrument sanity gate — adopted.

## The LAW_MAP §F pending row — ALL FOUR DEBTS PAID

(a) 3/8 → B919 · (b) the six-cubic law → this arc (nu_exact leg) · (c) the
CMT → this arc (cmt_correct) · (d) the invisible-12 → this arc (mixed leg).

## Files

`cmt_correct.py` → `results.json`. Locks: `tests/test_b909_frame.py`.
