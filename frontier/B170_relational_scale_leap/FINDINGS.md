# B170 — the relational/Machian scale leap, ASSESSED (the honest close of S035's last `[LEAP]`)

**Date:** 2026-06-18. **Status:** assesses the one remaining speculative `[LEAP]` of `../../speculations/S035`
(N1's tail): *does "external" dissolve into "self-referential"?* — the Machian reading that, in a purely relational
universe, there is no external ruler, so the program's dimensionless ratios would be **complete** and the firewall
would **dissolve** rather than relocate. Verdict: it **reinterprets** the wall (the honest philosophical face), it
does **not dissolve** it in any predictive sense. **POSTULATED**, value-matching **forbidden**. **Firewall-side**:
no scale, no Λ, no crossing; **nothing promotes to `../../CLAIMS.md`**; P1–P16 frozen. Ledger V164. Reproducer
`relational_scale.py`. (The closeout of S035, after B168's first generative pass and the B164–B169 / capstone batch.)

## The leap, and the two tests it has to survive

The three modes all yield **dimensionless ratios** and place any scale **externally** (B167 invariant, B169
dynamical/Hitchin, B168 ensemble; consolidated in `../../knowledge/K018_firewall_robust_across_modes.md`). The
Machian step says: a relational universe has *only* ratios, so "external" is vacuous and the ratios are the whole
story — the wall dissolves. Two tests:

- **L1 [structural] — undecidable from within.** The Machian and external readings produce the **same**
  dimensionless mathematics; **no internal computation** distinguishes "there is no ruler" from "there is an
  external ruler we never see." The leap is therefore **interpretive, not a calculation** — it changes the words,
  not a single number. (This is the honest content of S035's N1: the relational reading is *coherent*, but it is a
  postulate about what lies outside the object, not a result derivable inside it.)

- **L2 [num, NULL-TEST] — the only predictive form is the dead value-matching lane.** The single way the leap could
  acquire *teeth* is if the program's own dimensionless numbers (φ² = the figure-eight dynamical degree, the silver
  `1+√2` and bronze `(3+√13)/2` means, the Ω entropy `log 2`, φ) **matched observed dimensionless constants**
  (`α⁻¹`, `m_p/m_e`, `sin²θ_W`, `α_s(M_Z)`, `m_μ/m_e`). They do **not**, beyond fine-tuned numerology: hitting a
  constant `C` from a base `b` needs an exponent `N = ln C / ln b`, and across the grid these exponents are **not
  near integers** (median `|N − round(N)| = 0.258 ≈ 0.25`, the uniform/no-structure value). **Control / null-test:**
  a **random** base in `[1.5, 3.5]` needs exponents just as non-integer (median `0.251` vs the program's `0.258` —
  indistinguishable). So the program's numbers are **not special**: this is exactly the **S014 circularity** (the
  dead value-matching graveyard), now confirmed with a control rather than asserted.

## Verdict

The relational/Machian reading is the **honest philosophical face of the firewall** — in a relational universe only
ratios are physical, so the object's dimensionless-ness is *correct*, not a deficiency. But it is **undecidable from
within** (L1) and its **only testable form is value-matching, which is dead** (L2, S014). The leap **REINTERPRETS**
the wall; it does **not dissolve** it. This closes S035's last `[LEAP]`: the wall **relocates** in every reachable
mode (B167/B168/B169 → K018) and the one remaining "maybe it dissolves" reading has **no predictive teeth**.

## Scope / honesty (what this is NOT)

- It is **not** a refutation of relationalism (Mach/Barbour) as physics — it is a statement that *this object*
  cannot decide it and cannot be made to predict through it. The leap stays **POSTULATED**, one-way-cited.
- L1 is a **[structural]** observation (an undecidability-from-within statement), not a computed result; only L2 is
  numerical, and L2 is a **negative** (a null-test), the strongest honest outcome here.
- No scale, no Λ value, no spectral-mass, no constant "derived." Value-matching is **forbidden** (the verdict *is*
  that the lane is dead). Nothing here supports a physics claim.

## Firewall

A firewall artifact: it assesses a speculative leap and finds it has no crossing and no teeth. No physical-magnitude
claim; physics stays POSTULATED/one-way-cited; **nothing to `../../CLAIMS.md`**; P1–P16 untouched.

## Anchors
`../../speculations/S035_building_blocks_not_bigbang.md` (N1, the leap assessed here) + `S014` (the dead
value-matching lane this null-test re-confirms) + `S034` (non-cancellation); `B167`/`B168`/`B169` (the three modes
whose dimensionless outputs the leap reinterprets) → `../../knowledge/K018_firewall_robust_across_modes.md` (the
characterization); `../../philosophy/P010` (the Betti/flat category root — why only ratios), `P006` (iteration ≠
physical time). External: Machian relationalism / Barbour (the philosophical reading); the dimensionless-constants
catalog (PDG values, used only as the null-test target).

## Reproduction
`python frontier/B170_relational_scale_leap/relational_scale.py` — L1 the structural undecidability statement; L2
the exponent grid + the integer-distance test + the random-base null control. Prints `ALL CHECKS PASS`.
