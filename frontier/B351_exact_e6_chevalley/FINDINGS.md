# B351 — the exact Chevalley 𝔢₆: Jacobi-verified structure constants, the principal sl₂, and θ (conditional)

**Status: banked (frontier) as exact standalone Lie theory — part 1 of the `{4,8}`-integrability program
(the B265/B270 cup-product obstruction, the gate-B-adjacent open item B347 flagged). Firewalled; nothing
to `CLAIMS.md`; no physics claim.**

The cross-session cup-product push stalled at "correct E₆ *signed* structure constants (first sign cocycle
failed Jacobi)". This probe removes that blocker with the standard guaranteed construction and verifies
everything **exactly over the integers** (python ints/Fractions; no floats anywhere).

## Verified (all exact)
- **(i) The root system.** 36 positive roots generated by height induction from the Bourbaki E₆ Cartan
  matrix (chain 1–3–4–5–6, node 2 on node 4).
- **(ii) The algebra.** Frenkel–Kac/Kac asymmetry cocycle `ε(a,b) = (−1)^{a·B·b}` (upper-triangular `B`,
  `ε(a,b)ε(b,a) = (−1)^{(a,b)}`, `ε(α,α) = −1` on roots); brackets `[e_α,e_β] = ε(α,β) e_{α+β}`,
  `[e_α,e_{−α}] = −h_α`. Antisymmetry and **the full Jacobi identity verified on all 76,076 basis triples:
  0 violations, exact integer arithmetic.**
  **The sign trap, isolated:** with `[e_α,e_{−α}] = +h_α` exactly **1440 triples** fail — the mixed triples
  `(e_α, e_{−α}, e_β)` with `(α,β) = −1` violate Jacobi by `2e_β`; the Cartan-return sign is *forced* to
  `−h_α` by the cocycle convention (hand-derivation in the module docstring at the fix site). This is
  precisely the failure the earlier cross-session attempt hit; the exact enumeration finds it in seconds.
- **(iii) The principal sl₂.** `h = Σ cᵢhᵢ`, `c = 2A⁻¹𝟙 = (16,22,30,42,30,16)`, `e = Σe_{αᵢ}`,
  `f = −Σcᵢe_{−αᵢ}`; relations `[h,e] = 2e`, `[h,f] = −2f`, `[e,f] = h` exact.
- **(iv) The exponent decomposition.** `dim ker(ad e) = 6`; the ad-h weights on the kernel are
  `{2,8,10,14,16,22}` = twice the E₆ exponents `{1,4,5,7,8,11}` — B347's framing
  `𝔢₆ = ⊕ᵢ Sym^{2mᵢ}`, now exact.
- **(v) The diagram involution θ.** Built on Chevalley generators (Dynkin flip 1↔6, 3↔5) and propagated
  through recorded bracket chains: **automorphism** (all 78² pairs), **involution**, fixed subalgebra
  **dim 52 = 𝔣₄**, `(−1)`-eigenspace **dim 26** = the `𝔢₆/𝔣₄` coset.
- **(vi) θ on the exponent lines.** θ commutes with the (σ-symmetrically built) principal sl₂ and acts on
  the six 1-dim highest-weight lines by **exactly `(−1)^{m+1}`**: `+1` on the F₄ exponents `{1,5,7,11}`,
  `−1` on `{4,8}`.

## What (vi) settles — and what it does not
B347 found numerically that the manifold's hyperelliptic involution grades its six `H¹` lines by
`(−1)^{m+1}` and flagged "is this literally θ?" as OPEN. **Settled at the algebra level:** the
`(−1)^{m+1}` pattern on exponent lines *is* θ's eigenvalue pattern — the two gradings coincide because
both are the `𝔢₆ = 𝔣₄ ⊕ 26` split; there is only one such grading for a principal-sl₂-compatible
involution with these signs. **Not settled (the geometric identification):** that the manifold's
involution *induces* θ on the character variety — that needs the H¹-level intertwining, part 2.

## Part 2 (next)
Map B347's numeric `H¹` cocycles for `m ∈ {4,8}` into this exact basis (weight-basis identification) and
evaluate the cup-product obstruction `[z ∪ z] ∈ H²(4₁, 𝔢₆)` against the four F₄ blocks (the θ-even
reduction), settling the B265/B270 integrability question. The exact bracket table here is the pairing
that computation needs.

## The firewall (held)
Standalone Lie theory; the only outputs are structure constants, dimensions, exponents, and signs — all
classical facts re-derived exactly to serve as verified infrastructure. Nothing to `CLAIMS.md`.

## The fence
Pure python ints/Fractions; no numerics, no SnapPy, no mpmath. `exact_e6.py` ·
`tests/test_b351_exact_e6_chevalley.py`. Related: **B347** (the tangent + the flagged θ question),
**B265/B270** (the cup-product target), **B264** (the E₆ character variety), **OPEN_PROBLEMS** gate B.
Lit: Frenkel–Kac cocycle construction (Kac, *Infinite Dimensional Lie Algebras*, §7.8); Kostant's
principal sl₂ and exponents; the E₆→F₄ folding (standard).
