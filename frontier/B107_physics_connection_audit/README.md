# B107 — the physics-connection audit (the headline is a NEGATIVE)

> **⚠ FIREWALL — POSTULATED.** All physical readings are **POSTULATED / quarantined**, firewalled to
> `../../speculations/archive/PHYSICS_RESONANCES.md`. **Nothing reaches `CLAIMS.md`; the physics chapter stays
> CLOSED; P1–P16 untouched.**

Banks the CC-web physics exploration as a first-class **dead-end log**. Taking the physics seriously produced a
clean negative plus one open fork — recorded honestly, **none of it promoted**.

- **`probe.py`** — the two computational anchors:
  - **A (`quasicrystal_anchor`)** — the metallic trace map `φ_m: a→aᵐb, b→a` **is** the KKT/Fibonacci trace map;
    `tr[A,B] = x²+y²+z²−xyz−2` (Sütő/Fricke–Vogt) is conserved for all `m`; `φ_1 = (z,x,xz−y)`. *Verified.*
  - **B (`tower_roots_are_golden`)** — the headline **negative**: every SL(3) `m=1` tower eigenvalue is `±φᵏ`
    (`k ∈ {−3..3}`) — **one geometric scale `log φ`**, dressed by signs/powers. A mass spectrum is a Hessian,
    not one ratio; this is re-presented moduli-space monodromy, **not new physics**. *Verified.*
- **`FINDINGS.md`** — A–E classified:
  - **A** VERIFIED anchor · **B** VERIFIED headline-negative · **C** CORRECTED overclaim (tower/torsion ≠
    masses; what's real is the moduli-space `M_SUSY ≅ M_flat` + three-branch ↔ three-fixed-point map) ·
    **D** confirmed citations (GKLP 1305.0937; DGG 1108.4389, 1112.5179) · **E** the one OPEN fork (off-principal
    multichannel reps) · **Addition 3** — B106's root-of-unity D1 data confirms the non-principal thesis
    (the bridge B↔E; the `c→θ` check is the open math link to `ρ_n`).

**Result.** The metallic tower is a **known quasicrystal trace map carrying one golden scale** — not a spectrum
of new physics (B). The only citable physics is **moduli-space-level** (C); the mass/dimension identifications
are **withdrawn category errors**. The live fork is the **off-principal** sector (E), where the root-of-unity
data breaks the single-scale pattern.

```bash
python frontier/B107_physics_connection_audit/probe.py
python -m pytest tests/test_b107_physics_connection_audit.py -q
```
Physics POSTULATED + firewalled; no `CLAIMS.md` promotion; physics chapter CLOSED; proven core P1–P16 untouched.
