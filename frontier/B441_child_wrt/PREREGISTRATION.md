# C5 preregistration — the child's WRT/quantum invariant field content

**Registered 2026-07-05, BEFORE computing any field content. Tool validated (wrt.py: τ(S³)=1,
amphichirality) before this prereg. Firewalled.**

## The question

WRT is the Chern–Simons partition function — the first invariant fine enough to separate
commensurability neighbours (which C3 could not) and the first that *is* a physical quantity.
For each odd r, τ_r(4₁(5,1)) is an algebraic number in ℚ(ζ_{4r}); as r varies, the pattern of
which subfields it occupies is "the language the child speaks." The parent 4₁ speaks ℚ(√5) at
5|N (banked B208/B214). **What language does the CHILD speak — golden (√5), Eisenstein (√−3),
√−15, the disc −283 field, or nothing knot-specific?**

## The forced-vs-residual split (MANDATORY — the C3/golden-inversion lesson, locked)

The golden inversion (B440, retracted) was a striking result that turned out to be forced
structure (the ℤ/5 abelian characters) misread as distinctive. To not repeat it, C5 **separates
the forced content from the residual before comparing**:

- **FORCED SKELETON** = τ_r(unknot(5,1)) = **τ_r(L(5,1))** — exactly what H₁=ℤ/5 + the slope-5
  surgery force into the invariant for *every* knot's child (the lens-space Gauss-sum content).
- **RESIDUAL** = the knot-specific part, isolated as τ_r(K(5,1)) relative to the forced
  skeleton (ratio and/or field-difference). **Only the residual is compared across knots.**

A field (√5, √−3, …) appearing in the FORCED skeleton is numerator-forced and carries no signal.
Only a field in the RESIDUAL, present for 4₁ but not the controls, is a figure-eight fingerprint.

## Controls (fixed now, all computed alongside — never after)

- **forced skeleton:** unknot(5,1) = L(5,1).
- **slope control:** 4₁(7,1) — a slope the object does not select.
- **commensurability control:** 5₂(5,1) — the tight neighbour (shares C1's −283 field, C3's
  vacuum count). *The* discriminating control (the C3 lesson: beat 5₂, not just the trefoil).
- **parent control:** trefoil(5,1) — a non-golden parent at the same slope (its colored Jones
  must be independently validated, like cj_fig8 was).

## Three registered outcomes (bins)

1. **Bin 1 — BREAK / inheritance.** The RESIDUAL field is golden(√5)/Eisenstein(√−3)/√−15/−283-
   traceable, present in 4₁'s child but ABSENT in 5₂'s, the trefoil's, and the forced skeleton.
   ⇒ figure-eight-specific quantum content ⇒ ESCALATE (dedicated adversarial session, owner
   present). This is the only outcome that stops the loop.
2. **Bin 2 — general surgery fact.** The residual is the SAME across all knots' children ⇒ a
   general slope-5 surgery phenomenon, its own bank, claimed by neither inheritance nor break.
3. **Bin 3 — laundering (the six-instance prior).** The residual is generic knot-sensitivity —
   each child differs (WRT always distinguishes distinct manifolds) but by generic complexity,
   NOT traceable to the parent's √5/√−3. ⇒ the Inversion Law holds at the WRT floor.

## Discipline (locked, binding)

- **Verify-before-headline.** No C5 result is reported (to owner or cross-chat) until an
  independent adversarial recompute confirms it. Especially any *striking* result — the
  golden-inversion shape (striking + confirms prior) is treated as artifact-suspect FIRST.
- The forced-vs-residual split is **not optional**; comparing raw τ across knots (without
  removing the forced skeleton) is the exact error that produced the retracted headline.
- Exact where possible (the field content read at high precision + algdep, cross-checked by the
  Galois-fixing subgroup); two/three-outcome; the lean (Bin 3, held loosely) does not bias the read.

**Prior (honest, post-correction):** Bin 3 (laundering), held LOOSELY — looser than the SL(2)
floor, because WRT operates exactly where C3 could not see (it is not a commensurability
invariant), and because the anti-signal that had tightened the prior (the golden inversion) was
false. A real break (Bin 1) is not argued for, but its probability is not negligible.
