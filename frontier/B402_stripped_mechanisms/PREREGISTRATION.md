# B402 — PRE-REGISTRATION: the self-reference narrative, stripped to mechanisms

**Committed before computation. Two probes extracted from the owner-relayed narrative
(hint ledger 2026-07-04) after stripping the poetry; both exact, both two-outcome.**

## Q1 — the pair-requirement mechanism (why are all single channels τ₃-real?)

The banked fact (the single-object wall): every single-seed quantity is ℚ(√5)-real; the
√−3/√−15 content exists only in pair channels. REGISTERED TEST: is this reality CELL-WISE
— is every raw single trace tr(Par·W_mʲ) itself τ₃-fixed (z = s = 0 in its own H-average)
— or aggregate-only (cells carry imaginary content that every DFT window kills, the
emergent-family pattern)? Either verdict is a mechanism statement: LOCAL (the single
word's Galois structure forbids √−3) or EMERGENT (the wall joins the aggregate family).
Run for m = 1 and m = 2, all powers.

## Q2 — the characteristic 3-orbit (the twist's address is one of three)

FACT (2×2 arithmetic): the monodromy γ₁ ≡ [[0,1],[1,1]] (mod 2) permutes the three
nonzero half-characteristics in a 3-cycle (1,0) → (0,1) → (1,1) → (1,0). Our theta twist
(D-exponent j(j−1)/2, the half-characteristic at (1,1)-type) is therefore ONE POINT OF A
ℤ/3 ORBIT, not canonical. REGISTERED CONSTRUCTION: build the two sibling lifts (the
diagonal cocycles with the other two characteristics — D-exponents j(j−1)/2 + r·j type
shifts realizing the (1,0)- and (0,1)-characteristics), compute each sibling's slot-analog
sector constants exactly. REGISTERED PREDICTION (the orbit bet): the three sibling seam
constants form a single orbit under relabeling/Galois (equal up to the established
symmetry group), i.e. the theory spontaneously selects one of three equivalent addresses.
KILL: the siblings differ structurally (different existence/support) — then the (1,1)
address is DISTINGUISHED and the "why this characteristic" question becomes a sharper
open (bank the distinction exactly). SIDE-QUESTION (registered, exploratory): does the
3-orbit relate to the (2,3) ℤ/3 Mercedes structure (any exact map), or are they disjoint
threes (the W-B1 pattern)?

Machinery: the ζ₆₀ engine; build_theta_W-pattern with shifted D-cocycles; the slot DFT
machinery. Locks per verdict. Firewalled.
