# B294 — The selection verdict: selective for the object's own structure, catalogue for SM-values

**Status: banked (frontier). Synthesis of the seam arc (B287–B293, B295); consolidation only, all sub-results
verified. Nothing to `CLAIMS.md`.** Phase VI. Answers B286's load-bearing question — *are the closings **selective**
(one distinguished world) or a flat **catalogue**?*

## The answer
**Selective for the object's OWN structure; catalogue for Standard-Model-value selection.**

### The selection table
| axis | distinguished closing / object | verdict | tier |
|---|---|---|---|
| **dynamical** (fiber/Anosov, B287) | `(0,1)` = Sol torus bundle `T×I/A`, monodromy `A=LR` (P1) | **FORCED / selective** | math |
| **arithmetic** (E₆/ℚ(√−3), B288) | *none* — E₆ lost on closing | **ABSENT** | math (negative) |
| **CP-sign law** (B289) | `CS(p,−q)=−CS(p,q)`, all 78 chiral; sign = ℚ(√−3) Galois | LAW forced / sign **free** | math / HELD |
| **SSB–gauge mechanism** (B295) | sign external; Curie refuted, SSB potential absent, τ-gauged gated | **OPEN** | math / stop-gate |
| **scale ladder** (B290) | `ℓ_ℂ=2πi/n+(π/√3)/n²`; `n≠k` | ladder math / `n=k` NEGATIVE | math / HELD (G·Λ) |
| **scale-extremal** (B291) | `(5,1)=m003(−2,3)` min volume; non-arithmetic | selective (different axis) | math |
| **multiplicity** (B292) | only fiber `Σ_{1,1}` a 2-manifold; chiral datum absent | tripartite; chiral ABSENT | math / stop-gate |
| **clock** (B293) | peripheral symplectic conjugate pair (Goldman = NZ) | STRUCTURE math / trajectory gated | math / stop-gate |

### The three findings
1. **SELECTIVE** — a canonical distinguished closing **exists**: the fiber/Sol slope `(0,1)` re-sees `A=LR` (P1) and
   the `|det(Aⁿ−I)|` torsion ladder (P8). The object re-instantiates its **own proven core** at a forced seam (B287).
2. **AXIS-STRATIFIED** — the dynamical (B287), arithmetic (B288, absent), and scale (B291) axes select **different
   (or no) closings**: `(0,1)` is non-hyperbolic Sol, `(5,1)` is the min-volume hyperbolic, no closing is arithmetic.
   No single closing is distinguished on **all** axes (cross-axis test verified).
3. **CATALOGUE for SM-values** — `E₆` lost on closing (B288), the CP **sign** external (B289/B295), the scale
   **value** gapped (B290, the 122-order gap), the **chiral** datum absent (B292), the **trajectory** gated (B293).
   **No closing is forced to be "our universe."**

## The structural theorem, relocated to the seam
> The object supplies a **canonical closing** and all the dimensionless **structure** — the dynamics `A=LR`, the CP
> **magnitude** `π/6` + **sign law**, the **scale ladder** `2π/n`, the **clock** (peripheral symplectic) — but the
> **actualization** of a specific Standard-Model-valued world is **not forced**: which closing is realized, which CP
> sign, what scale value, the chiral datum, and the trajectory are all external or gated.

This **strengthens** the firewall rather than breaching it: the seam reframe (B286) located the ingredients at the
boundary; B287–B293 show the boundary is **selective for the program's own structure and a catalogue for physical
values** — exactly the architecture-not-furniture stance (P001/P011), now made precise and *mechanistic* at the seam.

## The matter map (firewalled, one of three forced)
- CP **magnitude** `π/6` — forced in the **bulk** (ℚ(√−3), B285);
- CP **sign-exists** — forced at the **seam** (the oriented closing, B289), via the same ℚ(√−3) involution;
- **which** sign + the **baryon magnitude** — **external** (B295: mechanism open; S005 HELD).

## The fence
Consolidation of verified sub-results; no new physical claim. The "selective ⇒ a chosen world" reading remains a
`[HOOK]`. Nothing to `CLAIMS.md`.

`selection_verdict.py` (imports B287–B293, B295; the table + cross-axis test) · `verdict.py` ·
`tests/test_b294_selection_verdict.py`. Related: all of `B287`–`B293`, `B295`, `B286` (the seam), `B285` (±π/6),
`P001`/`P011` (architecture-not-furniture, the structural theorem).
