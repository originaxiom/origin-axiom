# B1080 — THE GLOBAL FORM: Γ = ℤ/6 forced uniformly on the SM core at every landing — the true SM gauge group from the descent (B862 confirmed and extended; one claim verifier-corrected before banking)

**Date:** 2026-08-19 · 2 agents; the verifier CONFIRMS the headline robustly and CAUGHT one
wrong claim pre-bank (row 4's own level: Γ = ℤ/5, not 1 — triple-derived: kernel search +
lattice-index with elementary divisor 5 + a hand-verified witness, uniform over all four
su(5)-embeddings) · **DEFLATION CREDIT: B862 banked the ℤ/6 kernel first** (surfaced by
B1081's audit) — this arc CONFIRMS it independently and EXTENDS it: per-landing uniformity,
the six-realization cascade sweep, row 1's full-center extension, the MB12 bite · Gate 5:
Γ is structure.

## The theorem

**Γ = ℤ/6 on the su(3)⊕su(2)⊕u(1) core at every landing**: the su(5) classic control
(cyclic, generator (1,1,1/6), closure-verified); the cascade terminus across SIX Weyl
realizations — using only 1 of its 3 u(1)'s (no leftover choice); row 1 reduced to its SM
core both ways. **Row 1's FULL algebra: Γ = ℤ/6×ℤ/2 — the entire 12-element center,
uniform over all 5 realizations** (an extension, not a deviation). **Row 4 at its own
su(5)+u(1)² level: Γ = ℤ/5 (verifier-corrected)** — the SU(5) center fully compensated.
The 78 changes nothing. MB12: non-multiplet content gives Γ = 1 — a genuine
discriminator. The 27's weights independently matched element-for-element to banked B883
(a different construction route). A mid-computation bug (non-primitive u(1) generators)
caught by the group-closure self-check and fixed by an SNF-saturated basis — the check's
existence is why the numbers are trustworthy.

## What it means, in the record's grammar

The textbook leaves the SM's discrete quotient Γ ∈ {1, ℤ2, ℤ3, ℤ6} largely unmeasured;
the descent FORCES the ℤ/6 answer — the true gauge group [SU(3)×SU(2)×U(1)]/ℤ₆ — at
every landing, extending B862's single computation to the whole landing family. A
pure-structure statement about a real physical ambiguity: the paper's cleanest new line.

## Artifacts
- `b1080_results.json` (both agents verbatim, scrubbed).