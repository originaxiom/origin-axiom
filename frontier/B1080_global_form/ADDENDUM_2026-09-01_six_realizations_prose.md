# ADDENDUM (2026-09-01, fresh physics seat; prose finding for the banking seat) — "six independent Weyl realizations" names only five valid A2+A1 subsets; the verdict is unaffected (all ten give ℤ/6)

**Scope.** Prose only. The numbers reproduce (Ring R3 cell R26,
`reports/fresh_physics_seat_2026-09-01/recompute/R26_b1080_b1011/`, two independent routes:
direct kernel search on the 27 and the representation-free coroot-lattice index): cascade core
Γ = ℤ/6; row 1 full Γ = ℤ/6×ℤ/2 (order 12, ℤ/3×ℤ/2×ℤ/2, not cyclic); row 4 at its own
su(5)+u(1)² level Γ = ℤ/5 (the corrected value from the arc's own `v` block, not the `c`
block's Γ = 1). Original files unedited; nothing banked by this seat.

**The prose defect (R3_REPORT D12).** `b1080_results.json`, claim "UNIFORM across 6
independently-checked … Weyl realizations": *"2 different su(3) choices (Bourbaki{1,3} and
Bourbaki{2,4}) crossed with different su(2) partners (Bourbaki nodes 2, 5, 6, 1)"*. In
Bourbaki numbering (chain 1–3–4–5–6, node 2 on node 4) the pairings that are actually A2+A1
(su(2) node not in, and not adjacent to, the su(3) pair) are {1,3}+2, {1,3}+5, {1,3}+6,
{2,4}+6 and {2,4}+1 — **five**. {1,3}+1 and {2,4}+2 repeat a node, and {2,4}+5 is **A3** (4–5
adjacent). The sixth realization is not identifiable from the text. The arc's own verifier (`v` block) already enumerated **all ten**
A2+A1 node-subsets of E₆ and found Γ = ℤ/6 on every one, which R26 confirms.

**Proposal (owner action, prose):** replace "6 independent Weyl realizations" with "all ten
A2+A1 node-subsets of E₆ (exhaustive)", citing the `v` block, or name the sixth subset
explicitly. Immaterial to the verdict; recorded so the count in the headline matches a list
that exists.
