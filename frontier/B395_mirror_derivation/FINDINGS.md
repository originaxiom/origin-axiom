# B395 (M3) — BANKED: the registered kill — the mirror is EMERGENT, not cell-local

**Status: M3 complete (kill banked with structure). Prereg committed first. Firewalled.**

- **The cell-wise σ-law FAILS: 140/240 cells** (both Galois lifts σ₄₁, σ₁₁ of τ₃|ℚ(ζ₁₅));
  the banked mirror t(a,−b) = τ₃(t(a,b)) (P61) nevertheless holds table-wide — so the
  mirror is a WINDOW-LEVEL (summation-emergent) symmetry with no cell-local Galois origin.
  This is the second twist-era symmetry with no operator-local mechanism (after B389's
  twist-blocked inversion): the twist admits the mirror only in the aggregate.
- The 100-cell passing set is NOT the ±I-mod-3 sublattice (30 / 60 cells) — its exact
  characterization is STAGED (named residue; re-entry alongside M4's all-pairs view).

**Provenance.** mirror_derivation.py, passing_domain.json; locks
tests/test_b395_mirror_derivation.py.
