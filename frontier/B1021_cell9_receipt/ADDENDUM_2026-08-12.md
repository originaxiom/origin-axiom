# B1021 — addendum (2026-08-12): the two HELD rows CLOSE

The θ receipt and the falsifier-register adjudication, HELD here per
verify-before-banking, are **closed by B1035**: both files were located on the pushed
audit branch (present, byte-stable, on both remotes since 2026-08-10 — the hold was a
retrieval failure, not an evidence failure), re-verified hash-first
(`f0f336ce…` / `7ea68d34…`, exact), and integrated — the θ withdrawal accepted (it
strengthens k-blindness), the register recount adopted, and the register itself now
lives at `docs/FALSIFIER_REGISTER.md`. *(Addendum-beside; the banked FINDINGS is
untouched.)*
