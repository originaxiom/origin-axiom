# xB003 — ADDENDUM 4 (2026-09-16): the founding-cluster audit — B67 is ONE SUBSTITUTION from the object's point, and the genesis probes diagnosed this error class at arc 2

**Owner-directed extension of ADDENDUM 3 to the founding cluster B1–B9 and B67. Every statement
below is asserted by `verification/founding_cluster_audit.py`.**

## The headline: B67 is one substitution away, and never took it

**B67** derived the figure-eight A-polynomial exactly (matching Cooper–Long 1996) from the trace
map's fixed-point set. It banked, verbatim:

* the fixed locus of `T²`: **`y = z = x/(x−1)`** — the same locus xB003 works on;
* the longitude trace on it: **`κ(x) = (x⁴ − 3x³ + x² + 4x − 2)/(x−1)²`**.

Set `κ(x) = −2`, the object's leaf. Verified here:

> `x²(x² − 3x + 3) = 0` ⟹ `x = 0` (the node) or **`x = 2+ω, 2+ω̄`** — xB003's fixed point, with
> minimal polynomial **`x² − 3x + 3`, discriminant exactly `−3`**, the object's own trace-field
> discriminant.

That is cleaner than xB003's own presentation of the point, and it was **one line** from B67.
B67 did not take it: its target was `A(M,L)`, and the locus was a means.

**And B67's own second identity settles what the point is.** It banked
`κ = tr(t)⁴ − 5 tr(t)² + 2`. At `κ = −2` this gives `m ∈ {±1, ±2}`, and **`m = ±2` is the
parabolic meridian** — so the fixed point is not merely an algebraic solution, it **is the complete
hyperbolic structure**. The strongest available confirmation that it is the object's own point.

## The taxonomy the genesis probes fall into

| class | arcs | what it is |
|---|---|---|
| **wrong space — self-diagnosed at inception** | **B2** (NEGATIVE) | *"two moduli spaces conflated — the monodromy acts on the character variety of the **FIBER**, not on the peripheral `(M,L)`"*. Routed to B13 and B67. |
| **wrong type / weight** | **B5** (NEGATIVE) | `Λ = 2π²/Vol`: `Vol` is weight 0, `Λ` is weight −2; *"the scale is supplied, not derived"*. |
| **missing object** | **B1**, **B3** (OPEN) | the CS action and level; a 4-complex to do Regge on. Constructions owed, nothing refuted. |
| **right computation at a point the object does not occupy** | **B13**, and the **B6 → B7 → B8 → B9** tower | see below |

## The two instances of the class this arc is about

**B13** — `J(T)` at `(1,1,1)`, which is `κ = +2` (verified). ADDENDUM 3's finding.

**B6 → B9** — verified here: `V'(τ) = κ(τ² − τ − 1)` has its stationary points at the **Möbius
fixed points of `A = [[2,1],[1,1]]`**, and since `|tr A| = 3 > 2` the map is **hyperbolic and fixes
no point of `H`**: both roots are **real, on the boundary `∂H`**. So the "golden vacuum `τ = φ`" on
which B7's Fisher–KPP front, B8's particle spectrum and B9's fusion vertex are built is an **ideal
boundary point** — the attracting fixed point of the pseudo-Anosov, not a point of the moduli space.

**B6 flags this itself.** Its inserted-list reads *"the interpretation of `τ` as a **field** rather
than a coordinate on the upper half-plane `H`."* The caveat is at the root of the tower and was
carried forward without it.

## What the audit says

The record diagnosed this error class **at arc 2**, correctly and in its own words, and then
instantiated it twice more in the founding cluster — once in the dynamical layer (B13's leaf), once
in the field-theoretic layer (B6's boundary point) — and reached the object's own point in neither.
B67 got within one substitution and was pointed elsewhere.

This is `B1234`'s shape again — *"the squaring buys orientability and costs every value"*, one
upstream choice under many downstream walls — but the upstream item here is not a choice. It is a
**location**: the point at which the founding computations were evaluated.

## What is NOT claimed

Nothing is withdrawn. B2 and B5 are correct negatives; B1 and B3 are honest OPENs; B67's
A-polynomial derivation is exact and is its own result; B6–B9 are each correct given their inserted
reading, which B6 states. No physics reading is added and no value. The claim is only about
**where** each stands relative to the point the object occupies — and that B67's own κ, set to −2,
reaches it in one line.
