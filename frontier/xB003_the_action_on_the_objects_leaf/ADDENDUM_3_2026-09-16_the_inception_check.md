# xB003 — ADDENDUM 3 (2026-09-16): the inception check — the derivative line has sat on the WRONG LEAF since arc 13, and that is why the golden 3 kept coming back

**Written at the owner's direction to check the initial bankings, "as they study the object since
inception to how it evolves when investigated." It is the most useful thing this seat has done.**

## The lineage, verified arc by arc

| arc | what it computed | where | result |
|---|---|---|---|
| **B13** (inception) | `J(T)` — the trace map's derivative | **`(1,1,1)`** | `(t+1)(t²−3t+1)` — **golden**, `det = −1` |
| **B21 / B34** (founding) | the half-step is anti-Poisson, the `A`-level map Poisson | — | the structure B1341 later makes variational |
| **B123** | `J` at the **node** of `κ = −2` | `(0,0,0)` | `λ³+1`, spectrum `e^{±iπ/3}` in `ℚ(√−3)` — "the same as the tetrahedron shape" |
| **B1341** | the variational theorem; the explicit `S` | `I = 0` | trace 3; and its own check finds **the object is not on that leaf** |
| **B425** | the adjoint twisted Alexander polynomial, Fox calculus at `ρ_geo` | — | `(t−1)(t²−5t+1)`, roots in `ℚ(√21)` |
| **xB003** | `J` at the **regular** fixed point of `κ = −2` | `(2+ω̄, 2+ω, 2+ω)` | `(λ−1)(λ²−5λ+1)` — **= B425's polynomial**, by an independent route |

## The finding

**`(1,1,1)` is on `κ = +2`.** Verified here: in half-traces `I = 0`; in full traces `(2,2,2)` gives
`κ = 2`. That is B1341's leaf, and — by **B1342** — the leaf that is *a fixed point of the entire
monoid*: the "nothing" leaf. **It is not the object's leaf.**

So the golden `t² − 3t + 1` that B13 found, and that has been quoted forward ever since, is a
property of **that point on the degenerate leaf**. It is the derivative at the trivial parabolic
character, where every trace is 2. The object sits at `κ = −2`, and there the answer is
**`t² − 5t + 1`**, not `t² − 3t + 1`.

**B13 is not wrong.** It is headed *"Logged observation, not a claim"* and it evaluated the
derivative at the natural symmetric point. What happened is that the point was carried forward —
through B21, B34 and into B1341's generating function — for something over 1,300 arcs, until
B1341's own check caught the leaf mismatch and recorded it as an open gap rather than tracing it
to its origin.

## And the two lines were computing the same thing

The record has been running **two independent investigations of one object**:

* the **dynamical** line — B13 → B21/B34 → B1341 — the trace map, its derivative, its Lagrangian,
  all at `κ = +2`;
* the **arithmetic** line — B425 → B581 → B894 — the twisted Alexander torsions at `ρ_geo`.

They never met. B425's adjoint polynomial `t² − 5t + 1` **is** the derivative the dynamical line
would have found had it stood on the object's leaf, because for a fibred knot the twisted Alexander
polynomial is `det(t·φ_* − I)` on `H¹(fibre; Ad ρ)`. xB003 is where the two lines meet, and the
meeting is what gives the number its name.

## The pattern this instance belongs to

`B1234` proved *"the squaring buys orientability and costs every value"* — one upstream choice
under eight downstream walls. This is the same shape in the dynamical layer: **one evaluation point,
chosen at arc 13 for its symmetry, under every subsequent appearance of the golden trace 3 in the
derivative line.** Recorded because the owner's standing question — *"maybe at some point we
followed a wrong sub-branch that built the walls around us"* — has, here, a concrete and checkable
answer: not a wrong branch, but a **right computation at a point the object does not occupy**.

## What is NOT claimed

B13, B21, B34, B123, B1341 and B425 are all correct on their own ground and none is withdrawn. No
physics reading. No value. The `κ = +2` point remains the right place to compute what B1341
computed there; the claim is only that it is not the object's point, and that the object's point was
never visited until now.
