# xB003 — ADDENDUM 1 (2026-09-16): the multiplier is NOT forced by the monodromy's homology class, and the field coincidence is DECLINED

**Written immediately after the arc, at the owner's direction to go after the un-interpreted
number. The seat's own deflationary hypothesis was stated first and is REFUTED by this cell.**

## The hypothesis this seat proposed, and killed

Everything the golden side of this record produces tends to be one fact — `LR` is the golden
matrix — refracted. So the natural deflation was: **the derivative trace 5 is forced by
`tr(LR) = 3`**, hence carries no information. First guess, `d = t² − 4`, fits `t = 3 → 5` exactly.

**Refuted.** `t = 4` gives 9, not 12; `t = 6` gives 18, not 32.

## The decisive control

Two different words with the **same** abelianised trace:

| word | `tr(M)` | derivative leaf-trace at the regular fixed points |
|---|---|---|
| `LR` (m004) | 3 | **5** |
| `LLR` | 4 | 9 |
| `LLLR` | 5 | {4.7033, 14.2967} |
| `LLRR` | **6** | **18** |
| `LLLLR` | **6** | **{4.2781, 20.861}** |
| `LLRLR` | 10 | {6.0, 7.743, 42.257} |

`LLRR` and `LLLLR` share `tr(M) = 6` and do **not** share the derivative. So:

> **The multiplier is not a function of the monodromy's homology class.** It carries information
> beyond `tr(M)`, and the object's 5 is therefore *not* a restatement of its 3.

**Method check, done before the table and passing exactly:** m004's fixed point is also fixed by
`(LR)²`, where the derivative must square — leaf-trace `5² − 2 = 23`. Computed: **23**. (An earlier
pass of this cell reported a spurious value for `LRLR` because the solver returned a *different*
fixed point of the square; the check above is what caught it, and the table is recomputed with
numeric root-finding over all regular non-real fixed points.)

## The coincidence, recorded and DECLINED

The discriminants of the multiplier polynomials are `t_d² − 4`:

* `LR` → `5² − 4 = 21 = 3·7`
* `LLR` → `9² − 4 = 77 = 7·11` — **the record's resolvent field is ℚ(√77), and `disc K = 3⁴·7·11`**
* `LLRR` → `18² − 4 = 320`, squarefree part **5**

**We decline it**, by this record's own §2 standard. `n² − 4` for small `n` lands on products of
small primes as a matter of course — the catalogue is short, so a hit is not evidence. Worse for
any positive reading: the `77` appears at **`LLR` = m009**, *not* at the object, and in an
unrelated context (a character-variety derivative, not the `E₆` cubic pencils). A resemblance that
fires on a different manifold in a different layer is the E20 pattern exactly, and it is recorded
here so that nobody finds it later and reads it forward.

## Status

**What is established:** the multiplier is a genuine invariant of the mapping class together with
its fixed point, not a function of `tr(M)`; the seat's deflation is refuted by its own control.

**What is NOT established:** any meaning for the number 5, for `ℚ(√21)`, or for the pattern across
words. No reading is offered. The honest words are **not interpreted**, never "the object supplies
it."
