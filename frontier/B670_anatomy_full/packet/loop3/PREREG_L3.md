# ANATOMY LOOP 3 — sealed prereg (cc2, 2026-07-17)
C1 (THE VECTOR-VALUED MASSEY / the fifth-wall-or-first-structure cell): with
coefficients in 27-bar via the Jordan cross product (the portal's pairing):
(a) compute the CUP classes [u_i cup_x u_j] in H^2(D; 27-bar) for all solo
pairs (i,j) in {2,3,4} — (u_i cup_x u_j)(g,h) = u_i(g) x (rho(g) u_j(h)),
values in the dual system; h^2(D; 27-bar) = 5 by duality (gate). SEALED FORK:
if any cup class is NONZERO — that is THE FIRST nonvanishing coupling structure
above the class level (bank its exact matrix and STOP there, that is the
discovery); if ALL vanish — solve the vector bounding cochains and compute the
vector triple Massey products (the B2 pipeline with 27-bar values), verdict
ZERO / NONZERO with indeterminacy stated.
C2 (h1 against the F4 split): project the local system onto the F4 blocks
1 + 26 (the singlet = v0's line; the 26 = its T2-orthogonal complement w.r.t.
the invariant bilinear — construct the projector exactly from v0 and the
banked invariant form); Fox h0/h1 per block on the SOLO manifold. GATES: sums
= (1, 3); the singlet block gives (1, 1) (the Betti class). Expected: 26 gives
(0, 2) — the two generation modes live in F4's fundamental. Any other split
banks as-is.
C3 (THE SIGN TABLE -> the formula): compute the central scalar chi_block(-I_q)
at every realized central across stages and rungs: E6 k=2 (q=7, banked +I —
verify at rep level from npz), k=3 (q=9, SURVIVES: scalar = -I predicted),
k=5 (q=3), k=6 (q=27, +I banked), k=7 (q=3), k=8 (q=3); SU(3) k=3 (q=9),
k=5 (q=8?), k=6 (q=27, -I banked), k=9, k=12 (q=9); build the (stage, p, a,
scalar) table and FIT the sign formula — sealed candidates: (i) the scalar =
(-1)^{a * something(c mod 24)}; (ii) the local epsilon-factor/Gauss-sign of
the stage's T-arithmetic at p; (iii) the theta-parity of the block dimension.
Report the formula that fits ALL rows exactly or the honest non-fit.
