# ANATOMY LOOP 2 — sealed prereg (cc2, 2026-07-17). The room.
B1 (THE F4 CELL): sealed hypotheses — (i) the Lie algebra of the norm-preserving
group str0 = {X : N(Xx, x, x) = 0 for all x} has dim 78 (e6); (ii) adding
X v0 = 0 cuts it to dim 52 = f4 (the isotope automorphism algebra); (iii) the
27 branches under the stabilizer as 1 + 26 with v0 spanning the 1 — making
h0 = 1 CANONICAL (v0 = the F4-fixed vector) and naming the heart's ambient
organ: THE F4 SKELETON. Computation: linear conditions from the banked exact
cubic's coefficients (rank over the exact field, or finite-field rank with
exact spot-verify — method stated in-run); the holonomy containment check
(rho(g)-conjugation preserves the 52-dim algebra) on generators. Falsifiers:
dim != 78 at step (i) (the cubic is not the Jordan norm-form normalization —
audit), dim != 52 at step (ii) (v0's stabilizer is not F4 — bank whatever it
is), the branching not 1+26.
B2 (THE MASSEY CELL): S4 banked that the v0-mediated cup vanishes identically
on the solo triple {2,3,4} — the defining condition for triple Massey products.
Compute <u_i, u_j, u_k> for the solo triple on the double: solve the exact
cochain equations delta(a_ij) = -c(u_i, u_j) (the S4 2-cocycle, exact Fox
machinery), form the Massey 2-cocycles, reduce mod indeterminacy (the
coboundary + cup-ideal), and report: ZERO / NONZERO with exact classes; if
nonzero, the induced 3x3 structure on the triple and its degeneracy. THE FIRST
COCHAIN-LEVEL INVARIANT of the object. Falsifier-shaped outcomes all bankable:
MASSEY-ZERO (the room is empty one level up — the hierarchy question escalates
again), MASSEY-NONZERO-DEGENERATE, MASSEY-NONZERO-NONDEGENERATE (the first
hierarchy-shaped structure in the framework).
B3 (THE SILVER TRICHOTOMY): v0_silver's Jordan invariants on the banked B649
letters (cross-square support/values, N(v0), the sharp-fixedness) — is
unit-direction-ness metallic-universal? (K020: expect same FORM — sharp-fixed
invertible — different values.)

# ADDENDUM (sealed before compute; the landscape cells from chat-1's handoff)
B4 (THE CROSS-LANDSCAPE): the matrix |tr_odd(word, stage)| for words = the
single-L family R^{n-2}L (tr = 3..15) PLUS the multi-block controls RRLL, RLRL,
RRLRL (the H3 counterexample set), stages kappa = 4..15 (SU(3) levels 1..12).
SEALED HYPOTHESES: (i) the shadow-class law: every entry = 2|cos(class of the
word mod the stage's governing modulus)| — at kappa with 5|kappa the mod-5
shadow governs; the general-column law to be READ OFF the data (each column's
value set c the five tones of ITS shadow group? — bank what appears);
(ii) chat-1's diagonal hypothesis: each word is QUIETEST (minimal nonzero
modulus) at stages its own conductor divides — e.g. the tr-4 word (disc 12) at
kappa = 12 should give a minimal/distinguished value (their guess: 1/(2+sqrt3)
= 2-sqrt3 = 0.268); the golden at kappa = 5 gives 1/phi (banked). Falsifier:
any word whose minimum sits off its conductor stages. (iii) the realness
criterion: catalog which (word, stage) entries are exactly real; test
"det(A-I) unit => real" vs the H3 counterexamples (RRLL real without unit det)
— extract the true criterion (self-inverse-conjugate shadow class?).
