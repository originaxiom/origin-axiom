# B1 — BANKED: THE F4 SKELETON — A NEW ORGAN (cc2 + subagent + seat-main
# completion, 2026-07-17; prereg f9ae4a9e; all sealed gates exact)

THE THEOREM-GRADE FACTS (mod-p dims at p = 2^31 - 1 with exact spot-verifies +
exact containment checks; method status stated in-run):
1. dim{X : N(Xx,x,x) = 0} = 78 — the norm-preserving Lie algebra is e6 (the
   cubic IS the Jordan norm normalization; gate).
2. dim(stab(v0)) = 52 = f4 — **the isotope automorphism algebra of the unit
   direction is F4.**
3. The 27 branches 1 + 26 under it: v0 spans the singlet (independent of the
   image span, dim checks 26/27 exact), the 26 IRREDUCIBLE.
4. The holonomy conjugation preserves the 52-algebra (6/6 exact membership
   checks + basis-wide mod-p) — **rho(pi_1) c F4(v0).**

CONSEQUENCES:
- h0 = 1 is CANONICAL: the invariant line is the singlet of E6 > F4 — the heart
  is the F4-fixed point, no longer a computed accident.
- With B3 (both metallic holonomies fix the SAME v0): all metallic
  representations live inside ONE F4-form — THE F4 SKELETON is the
  metallic-universal frame. The 26 (the traceless exceptional Jordan algebra,
  F4's fundamental) is the common stage on which the objects differ.
- The generations organ inherits a refinement (fed to frontier): h1(27) = 3
  should split as h1(1) + h1(26) = 1 + 2 — the Betti class is the singlet's;
  the two remaining solo modes live in the 26. Recompute W0b's table against
  the F4 split; one cheap cell.
Artifacts: b1_f4.py, b1_results.json, b1_run.log + b1_main.log, _exact_cache.pkl.
Repo untouched.
