# B4 — BANKED: THE CROSS-LANDSCAPE — SELF-SELECTION IS HALF A LAW, DELIMITED
# (cc2 + subagent, 2026-07-17; prereg addendum c6d4fabd; gates digit-exact
# vs banked H3/chat-1 data)

THE MATRIX (16 words x 12 stages, exact): per-column tone sets banked (kappa=5
= exactly 2x the golden tone set; kappa=10 the richest with 8 values; each
column a shadow-class value set — the column law is the H2 mechanism per stage).

THE DIAGONAL (self-selection) HYPOTHESIS — MIXED, with clean edges:
- HOLDS distinctively on 8/16 rows: minima ENTIRELY on conductor-divisibility
  stages — the golden's 1/phi recurs at kappa = 5, 10, 15 (all multiples of 5);
  the tr-4 word's row minimum sits EXACTLY at its conductor stage kappa = 12
  (chat-1's qualitative claim CONFIRMED; their value guess 2-sqrt3 FALSIFIED —
  the true value is sqrt3 - 1 = 0.7320508).
- FAILS cleanly on the rest: n=11 (cond 117) minimum has zero conductor
  alignment; RLRL's distinctive minimum (0.382 at kappa=10) is off-conductor;
  6/16 rows have only the generic background 1.0 as "minimum" (non-distinctive).
VERDICT: the fixed-point selection ("the word is quietest at its own
conductor's stage") is a REAL PATTERN OF THE SMALL-CONDUCTOR REGIME, not a
universal law — the golden remains its cleanest instance. L91 feed: the golden's
triple-recurrence (5, 10, 15) + realness + prime conductor is now a bounded,
checkable selection statement rather than rhetoric.

REALNESS: "det(A-I) unit => real" is DEAD (RRLL, RLRL, n=15 always-real with
non-unit dets); realness is mostly stage-dependent; 4/16 words always-real —
class-inversion-closure suggested, boundary caveat flagged honestly.
Artifacts: b4_landscape.py, b4_matrix.json, b4_run.log. Repo untouched.
