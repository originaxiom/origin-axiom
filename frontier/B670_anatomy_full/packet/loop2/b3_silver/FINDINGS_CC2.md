# B3 — BANKED: THE UNIT DIRECTION IS METALLIC-UNIVERSAL — AND IT IS THE SAME
# ELEMENT (cc2 + subagent, 2026-07-17; prereg f9ae4a9e)

The silver v0 (support {12,13,14}, coefficients (1,-1,1)) has EXACTLY the golden
Jordan invariants: sharp-fixed with factor -2, N(v0) = -6, RANK 3 INVERTIBLE.
Beyond the prereg's K020 expectation (same form, different values): the values
are IDENTICAL — honestly traced to the shared rational Cartan-cubic apparatus
(both tracks use the same E6/27 tensor; v0's pattern is combinatorially the
same). THE INVARIANT STATEMENT: both metallic holonomies fix the SAME
sharp-fixed invertible direction of the common Jordan model — v0 is not "the
knot's vector"; it is an intrinsic Jordan direction that every metallic
holonomy computed so far stabilizes.
CONSEQUENCE (sharpens B1's stakes): if hol c F4(v0) confirms, the F4 skeleton
is the METALLIC-UNIVERSAL frame — all metallic representations live inside one
F4-form, differing only in how they sit within it. The two-object evidence is
in; B1's dimension count decides the ambient claim.
Artifacts: b3_silver.py, b3_results.json, b3_run.log. Repo untouched.
