# PREREG — ANATOMY LOOP 4: THE VECTOR PAIRING (sealed before compute, 2026-07-17)
# Feeds: FRONTIER v4 items 1-3 (C1's FORK-NONVANISHING successors).

## D1 — THE FULL VECTOR PAIRING'S STRUCTURE
Object: ALL 5x5 pairs [u_i cup_x u_j] in H^2(D; 27-bar) — extend C1's solo-only
scope to the boundary classes (indices 0,1) and mixed boundary-solo pairs.
Deliverables (exact, Q(sqrt-3)): (i) each class's coordinates in ONE fixed full
obstruction basis of H^2(D; 27-bar) (not just a single psi witness); (ii) the
rank of the span of {[c_ij]} (antisymmetry => candidates [c_01],[c_02],...,
[c_34], 10 independent slots; solo block rank <= 3); (iii) the block structure
(boundary-boundary / boundary-solo / solo-solo) — does the boundary/solo split
diagonalize the pairing as it did the portal; (iv) THE CUBIC-VISIBILITY MAP:
for each nonzero [c_ij], is there any k with banked Y[ijk] != 0 (cubic-visible)
or none (CUBIC-BLIND — the zero law broken upstairs, C1's [c_24] the first)?
Sealed falsifiers: coordinates must be coboundary-shift invariant; the solo
3x3 subblock must reproduce C1's six banked witnesses; Koszul antisymmetry
[c_ij] = -[c_ji] on every computed pair; zero floats.
Sealed outcomes: RANK-r + block table + visibility map (bankable at any r);
if any boundary pair nonzero => FIRST BOUNDARY SUPRA-COUPLING (new headline).

## D2 — THE 351-DECOMPOSITION (the other components of 27 (x) 27)
Fact: the cross product is the SYMMETRIC projection Sym^2(27) -> 27-bar
(Sym^2 27 = 27-bar (+) 351'; Lambda^2 27 = 351). C1's nonzero classes used
ONLY the 27-bar slot. Question: do the OTHER components carry classes too —
i.e. is the full [u_i cup u_j] in H^2(D; 27 (x) 27) nonzero in the 351 and
351' slots? Method (witness-based, NOT full 351-basis Fox): build the
Kronecker-square letters (729-dim, exact), form the cup cocycle in 27(x)27,
project to each of the three slots (27-bar via cross; 351 = antisymmetric
part; 351' = symmetric complement via the banked J/trace form), and prove
non/vanishing per slot by the C1 witness method (a functional annihilating
all coboundaries in that slot). Do pairs (2,3),(2,4),(3,4) minimum; boundary
pairs if budget allows. Sealed falsifiers: the 27-bar slot must reproduce C1
exactly; witness validity = annihilates a spanning set of coboundaries;
exact arithmetic. Sealed outcomes per slot: SPEAKS / SILENT (both bankable;
"which representation slots of the square carry the coupling" is the
351-question from the triage, now decided at the cocycle level).

## D3 — THE SILVER VECTOR CUP (K020 on the discovery)
Same computation as C1 (solo off-diagonal vector cups, witness method) on the
SILVER exact 27-letters (B649, F4-verified). Gates first: reproduce silver
h^0 = 1, h^1 = 5 from the letters before any cup work. Sealed falsifiers:
Koszul antisymmetry; coboundary invariance; exact Q(zeta_8)-compatible
arithmetic (Fraction pairs in the silver field tower as banked). Sealed
outcomes: FORM-MATCH (nonzero pattern matches golden — the supra-coupling is
STRUCTURAL, K020 satisfied, values differ) / FORM-MISMATCH (golden-specific —
bankable frontier fork) / degenerate (silver h^1 basis fails gates — STOP,
report, no improvisation).

## Discipline: repo READ-ONLY; work only under anatomy/loop4/<cell>/; subagent
## agents compute, main seat verifies; bank only from finished runs (MB13).

## D4 — TRACK-H CONTROL + THE CEILING LAW (addendum, sealed before compute)
Chat-1's TRACK H seals "the chiral control's |Z_k|^2 GROWS with k". CC2's
sealed counter-prediction: EVERY metallic word's SU(3) ladder is BOUNDED —
the banked melody theorem (exact periodicity) and the uniform jump law
(Pi p^{min(v,a)/2}, a = v_p(disc w)) are word-generic; growth is impossible.
Compute: Z_k(R^{n-2}L) for n = 3..10, k = 1..12 (kappa = 4..15), exact, with
the banked ladder engine. Deliverables: (i) the full Z table + |Z|^2;
(ii) per-word boundedness verdict vs chat-1's sealed growth prediction;
(iii) THE CEILING MAP: max_k |Z_k(w)| vs conductor tr^2-4 and disc — sealed
refined hypothesis: the ceiling is governed by the jump-law cap (a disc-
built constant), so it GROWS with the word's conductor and the golden sits
at/near the MINIMUM nonzero ceiling. Controls: the fig-8 row must reproduce
the banked ladder exactly (find the banked values in seat-work, do NOT trust
chat-1's table); the generic-law rows Z = (1-(kappa|5))/2 where applicable;
exactness. Outcomes all bankable: UNIVERSAL-BOUNDED (+ceiling map) /
ANY-GROWTH (would overturn the melody theorem's generality — escalate, stop).
