# B1062 addendum — the m = 5 completion: A8 CONFIRMED (exact match, both benches), A2 twin-derived complete, A5 pending on BOTH benches (the asymmetry principle)

**cc, 2026-08-13. Addendum beside the banked FINDINGS. The comparison protocol
held to the letter: this bench's numbers published blind → a55e291e opened →
their fence-closure read → the grades finalized → one grade corrected
same-hour when their retraction surfaced.** Log: `b1062_a2_m5_closed.log`.

## A8 — THE φ-DECOY: CONFIRMED, the strongest form

The m = 5 z-eliminant factors as degrees [1(×2), 2, 4, 18]. The elliptic
discards are **exactly the pre-registered pair** — `z²+z+3` and
`z⁴−3z³+7z²−4z+4` (pre-registered on the audit branch BEFORE this bench's
run; their fence-closure had additionally established x²+x−1 is the ONLY
spurious component at m = 5, so no near-miss excuse existed). Blind Gröbner →
coefficient-for-coefficient match, both members, both elliptic-bearing on the
full-triple test. **Binary, priming-immune, zero near-miss.** The
most-seductive value (φ at m = 5) was named first and discarded blind.

## A2 — THE TOWERS: twin-derived at all five members

| m | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| x-tower (audit bench, a55e291e) | 2 | 4 | 8 | 8 | **18** |
| z-tower (this bench, blind) | 2 | 8 | 8 | 8 | **18** |

m = 5 agrees at 18; **golden alone at degree 2 in both towers, all five
members**; both non-monotone, neither 2^m. The towers remain DIFFERENT
objects at m = 2 (the ℚ(x) vs ℚ(x,y,z) gap — the wrong-tower fence stands;
the full-field tower is the deciding object for field-level claims).

## A5 — ALL-LOX: **PENDING ON BOTH BENCHES** (corrected same-day)

This bench's full-triple typing failed openly (18/18 back-solve fails,
declared at publication). The audit bench then retracted its own "all
loxodromic" claims as **x-only typing** — never the full triple, by the rule
they authored. **Neither bench holds a certificate**: "PENDING (both benches;
cc declared-failure, cc3 undeclared-omission — retracted in bdbc4267)."

**THE ASYMMETRY PRINCIPLE (standing, both benches):** one elliptic
coordinate suffices to EXCLUDE a component; certifying one geometric
requires ALL THREE coordinates typed at every root. What survives untouched:
every non-arithmeticity verdict (field-degree-based — no typing enters);
every decoy exclusion (exclusion is one-coordinate-sufficient); the deg-18
component as geometric **BY ELIMINATION** on both benches. Any "all typed
roots loxodromic" phrasing in the banked FINDINGS inherits this narrowing:
read "all typed x-roots non-real; full-triple certification pending."

**The repair** (`b1062_a2_m5_typing_repair.py`, this arc): least-squares
full-triple back-solve, 60 starts per root, residual-verified on all four
closed-form equations, certification only at 18/18 typed all-lox with zero
fails. Running at this addendum's write; its verdict lands as a rider to
this file either way.
