# B1062 — PRE-COMPUTE ADDENDUM (2026-08-13): two labels and an ordering, BINDING on the compute and the write-up

*cc banking seat. The seal (ad8d60f1) is untouched; this addendum-beside is hashed
into SEAL_LEDGER and pushed BEFORE any cell runs. Source: the relay seat's
non-vacuity defect report against its own V2 design — the only time such a repair
is free. The seal's outcome space is unchanged; what changes is what each arm is
CALLED and which test is load-bearing.*

## 1 — The m = 1 arm is a PIPELINE GATE, not a result

Tr(Γ₁) lies in (a finite-index-controlled subset of) ℤ[ω] — the Eisenstein lattice,
covolume √3/2 in ℂ. **A lattice contains O(1) points per unit box by definition**,
so m = 1's box-flatness is ENTAILED by the banked degree-2 trace field, not tested
by the enumeration. The seal's "consistency, not proof" clause is hereby sharpened:
**it is not even consistency — it is entailment.** The m = 1 arm is RELABELLED: a
pipeline gate (an m = 1 arm that failed to come out flat proves the enumerator
broken — HALT, machinery fault, not a result). The FINDINGS must not present m = 1
flatness as corroboration. **MB12 non-vacuity for V2 rests entirely on the m ≥ 2
arm**, and the write-up says so.

## 2 — Maclachlan–Reid LEADS; the box-count ILLUSTRATES

The load-bearing V2 test is the **conjugate-boundedness criterion** (Hao Thm 2.3):
derived-from-a-quaternion-algebra ⟺ ℚ(Tr Γ) a number field, Tr(Γ) ⊂ O_K, K ⊄ ℝ,
and **every non-identity non-conjugation embedding φ has φ(Tr Γ) bounded**. This is
the MECHANISM (B-C fails ⟺ a Galois conjugate is unbounded), is decidable per
object, carries no window hazard, and **discharges R3 (m ≥ 2 non-arithmeticity) in
the same computation** — already an in-cell duty. The box-counts remain in the cell
as the EXHIBITABLE ILLUSTRATION of what conjugate-unboundedness looks like through
a finite window — reported second, labelled as illustration.

## 3 — One constant fenced

The sharp "≲ 3 per box" for m = 1 depends on the exact inclusion Tr(Γ) ⊂ O_K,
which holds cleanly for groups DERIVED from a quaternion algebra; for the
arithmetic-vs-derived distinction (Γ vs Γ⁽²⁾), the inclusion may hold up to finite
index. **The lattice argument (O(1) per box) survives either way; the specific
constant does not** — the pipeline gate checks O(1)-flatness, not "≤ 3".

*Nothing else in the seal moves. The relay seat designed V2, reported this defect
against itself, and does not adjudicate the cell.*
