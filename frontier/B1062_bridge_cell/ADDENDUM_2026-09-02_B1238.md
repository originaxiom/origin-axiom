# B1062 — ADDENDUM: the axis's bronze half is now computed — invariant trace field degree 8

*Addendum written 2026-09-02 under **B1238** (the seat harvest, third ring). Every number below recomputed on this bench — `frontier/B1238_seat_harvest_40a3_bronze_octic/verification/`. The arc's verdict file carries the same correction in its `note`.*

B1237's addendum corrected V2's silver half (m136's invariant trace field is ℚ(i); silver arithmetic) and left bronze "not recomputed". Done now: bronze's invariant trace field is the octic x⁸ + 6x⁶ − x⁵ + 12x⁴ − 3x³ + 8x² − x + 2 (disc 391728981, signature (0,4)), by two routes at 1000 bits with `nfisisom` agreement (`verification/bronze_invariant_trace_field.py`; golden and silver recomputed in the same run: x² − x + 1 and x² + 1). **The E55 axis is complete and computed from the deciding criterion at every member: {golden, silver} arithmetic, {bronze} not.** V2's *control* reading is as B1237 left it — arithmeticity separates bronze from the pair, and the silver half of L161's genericity control rests on B997's McKay-door uniqueness. The tower degrees (golden alone at degree 2 in every tower) are untouched. Also relevant to this arc's own history: the bronze octic it found by complete Gröbner (the "missed octic", the trace-field route) and the invariant-trace octic here are different fields of the same degree; the arithmeticity statement needs only the latter.
