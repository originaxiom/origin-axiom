# B1062 PREREGISTRATION — THE BRIDGE CELL: the arithmetic↔spectral bridge, the B-C control, and the externality questions — one cell, both crossing lanes waiting on it

*cc banking seat, 2026-08-13. Sealed before compute; hash to docs/SEAL_LEDGER.md,
committed and pushed to both remotes before any cell runs. Gate 5-Q throughout (no
measured value appears anywhere in this arc). Relay checked before this seal
(latest: the two paid debts, acked). The L161 campaign's control gate AND L164's
externality question, jointly. The audit seat supplied V0's theorem and V2's design
and DOES NOT ADJUDICATE this cell (its own fence: the cell rescues or kills its own
proposal); the audit seat's five-clause scoring is adopted as this seal's table.*

## What this cell decides

L161 (the aperiodic campaign) is bound control-first: no literature until a
genericity control exists. P0-0 v1 found the two-layer gap (everything
golden-specific is arithmetic; everything measurable is spectral; no banked
bridge). This cell computes the bridge's candidates. L164 (the doors) needs its
externality side named. V1/V3 carry it. One cell, four questions, every outcome
sealed.

## V0 — CLOSED POSITIVE PRE-SEAL (recorded, with its riders as in-cell duties)

**The 3D theorem exists and is named: Hao, arXiv:2303.01395, Theorem A** — for
nonuniform lattices in PSL(2,ℝ) or PSL(2,ℂ): B-C property ⟹ arithmetic; with the
forward direction (arithmetic ⟹ B-C, Luo–Sarnak) stated by Hao to extend to
Kleinian groups: **B-C ⟺ arithmetic on exactly this corpus's object class**
(cusped finite-volume ⟹ nonuniform ⟹ parabolics present — Hao §2). The
**Kleinian definition is the UNIT BOX in ℂ**: S(m,n) = {z : m ≤ Re z ≤ m+1,
n ≤ Im z ≤ n+1}; B-C ⟺ ∃K with |Tr(Γ) ∩ S(m,n)| < K for all (m,n) — never the
real-interval form (the reading corrected two errors in transit; the hazard row
carries both). Riders, in-cell duties: **R1** the theorem is leaned on AS
PUBLISHED (Hao §§4–5's proof unchecked — stated); **R2** Luo–Sarnak's forward half
on Hao's authority (stated); **R3** m ≥ 2 NON-ARITHMETICITY INDEPENDENTLY
VERIFIED IN-CELL (the control's load-bearing leg — B705's "only" is the prior,
not the proof; the trace-field/invariant-trace-field arithmeticity criteria run
per object); **R4** nonuniformity per object CHECKED not assumed; **R5** the
semi-arithmetic consequence (a semi-arithmetic non-arithmetic member must FAIL
B-C by the iff) checked as a consequence.

## The cells

- **V1 — THE GAP-LABEL QUESTION (the externality side, for L164).** Compute the
  gap-labeling data of the m = 1 (Fibonacci) chain from banked machinery
  (K007/K010's neighborhood; the trace-map dynamics) and state its algebraic
  structure exactly (the classical expectation: a φ-module — ℤ + φℤ-type — inside
  ℝ). Then the typed comparison: does the corpus's tone/menu structure (the
  |χ_golden|/2 values with their φ-arithmetic) map to the label group by an
  EXHIBITED φ-equivariant correspondence — or is the agreement
  vocabulary-only? Outcomes: EXHIBITED / OUTPUTS-ONLY / UNDECIDABLE-with-gap.
  (The L142 pattern; labels are finite-menu objects measured in real materials —
  a YES here names L164's external side.)
- **V2 — THE B-C BOX-COUNT CONTROL (the door-internal control, for L161).**
  Compute Tr(Γ_m) for m = 1, 2, 3 exactly via the Fricke/trace-map recursion at
  each bundle's geometric point (banked values; exact arithmetic in each trace
  field), enumerate to a declared word-length/trace-bound window, and count per
  unit box. **The sealed prediction (the control's form): m = 1's box-counts stay
  BOUNDED-LOOKING (flat maxima) while m = 2, 3's GROW in the same window.** The
  asymmetry is stated in the seal: a finite computation CAN refute B-C (a growing
  box) and CANNOT confirm it (golden's flatness is consistency, not proof —
  reported as such). Companions: **Gap(Tr(Γ)) > 0** per object (the computable
  sibling; targets derived-from-quaternion-algebra), and the **Maclachlan–Reid
  trace-field criteria** (R3's instrument — the corpus's native currency).
  Outcomes: CONTROL-EXHIBITED (m ≥ 2 grow, m = 1 flat, R3/R4 verified — L161's
  gate OPENS) / CONTROL-FAILS (m = 1 grows too, or any m ≥ 2 stays flat with R3
  confirming non-arithmeticity — the direction returns NOT-YET with the anomaly
  named) / INVERTED (R3 finds an m ≥ 2 arithmetic — the control inverts, stated).
- **V3 — THE TONES QUESTION (P0-2's computable half).** Do the tones' class-size
  multiplicities {90, 72, 120, 72, 6} appear in any computed spectral structure of
  the m = 1 chain (band/gap degeneracies, integrated-density plateaus' arithmetic,
  the label module's coefficients — the candidate observables enumerated in-cell
  before looking)? Outcomes: REALISED-with-the-observable-named / NOT-FOUND-in-
  the-enumerated-observables (a floor, not an absence) / UNDECIDABLE.

## Priors, declared

V1: NONE (the φ-module expectation is classical; whether OUR menu maps is open —
both readings live). V2: the PREDICTION is the control's form, not a lean — it is
what the arithmeticity dichotomy plus R3's expected confirmation entail; if R3
inverts, the prediction inverts with it (stated). V3: lean NOT-FOUND, weak (the
audit seat's v1 analysis found no mechanism; its §6 declared the space
unenumerated — the lean is inherited skepticism, and it protects nothing).

## The location discipline (binding on the write-up)

V2 is DOOR-INTERNAL (the location clause: door) — it can open L161's gate but
does NOT supply externality. V1/V3 carry the externality question. The FINDINGS
reports them separately; neither wears the other's clothes.

## Five-clause table (the audit seat's scoring, adopted)

I (type: menus/labels only) ✓ · II (scale: none consumed — trace sets and labels
are scale-free) ✓ · III (arity: n/a — no pairing) ✓ · V (torsor: no point
selected) ✓ · IV (genericity): **discharged by V0's iff for V2**; for V1/V3, IV
is exactly what the cells test (a vocabulary-only agreement fails it; the seal
says so).

## MB12 / non-vacuity

V1 can go both ways (an exhibited map is constructible-or-not; OUTPUTS-ONLY has
the B757/B1037 precedents). V2's counts can grow or stay flat (both behaviors
exist across lattices by the dichotomy itself); R3 can confirm or invert
(arithmeticity criteria are decidable per object). V3's enumerated observables
either contain the multiset or do not.

## Hazards pre-registered

The Kleinian unit-box definition ONLY (the real-interval and integrality forms
are the two recorded transit-errors — the hazard row); exact arithmetic in each
trace field (φ AND phi; sum(..., K0)); the window/word-length bound DECLARED in
the log before counting (no post-hoc window selection); "not-found" never
"absent" (V3's floor discipline); the B701 citation form; distinct commit
subjects; seat labels by role only; verdict vocabulary {PROVED, NEGATIVE, OPEN,
RETRACTED, PARTIAL} — a mixed outcome reports as the ledger.
