# B1062 — ADDENDUM: the V2 control is CORRECTED — silver IS arithmetic

*Addendum written 2026-09-02 under **B1237** (the physics-seat harvest, second ring). Source of the flag: the physics-seat evaluation branch @ d415423a; every number below recomputed on this bench — `frontier/B1237_physics_seat_r31_r38_harvest/verification/`. The arc's verdict file carries the same correction in its `note`.*

## What was wrong

FINDINGS.md:30 says of silver (m136): *"ℚ(x) degree 4 (x⁴−4x²+8; x² = 2+2i), full field degree 8 — reconciled exactly | NON-ARITHMETIC (degree alone decides)"*, and :32 *"The arithmeticity axis separates {golden} from {silver, bronze}"*.

**Degree of the trace field does not decide.** Arithmeticity is a property of the *invariant* trace field kΓ = ℚ(tr Γ⁽²⁾) (traces of squares), which can be strictly smaller than ℚ(tr Γ). The row itself recorded the invariant datum — **x² = 2 + 2i ∈ ℚ(i)** — beside the wrong inference.

## What was computed (`silver_arithmetic.py`)

- Polished holonomy of m136 (400 bits); all words of length ≤ 6 in the generators and inverses; **350 distinct values of tr(g²), every one a Gaussian integer** (sample: −550−28i, −510−388i, …).
- Vol(m136) = 3.66386237670887606021841405972953644309659749712668853706599 = **4G** (Catalan) = **12 × covol(PSL₂ℤ[i])**, with covol = |D|^{3/2} ζ_K(2)/4π², D = −4, ζ_{ℚ(i)}(2) = ζ(2)·G — equality to 50 digits.

Invariant trace field ℚ(i) (one complex place, no real places), integral invariant traces, volume a rational multiple of the Bianchi covolume: **silver is arithmetic**, commensurable with PSL₂(ℤ[i]). This is what B125/B147/B137/B850/B337 already banked; this cell contradicted them without any instrument noticing (E53 shape).

## What survives

- **The degree statement survives untouched** — *"golden alone at degree 2 in every tower"* is a trace-field statement, and silver's trace field is degree 8 (B258, `test_b258`).
- **V1** (tones are not gap labels) and **V3** (not-found floor) untouched.
- **V2 is still CONTROL-EXHIBITED**, with the corrected partition: the arithmeticity axis separates **{golden, silver} from {bronze}** (bronze not recomputed here; the seat's R33/R35 concur with the bank). What is lost is the *"golden alone is arithmetic"* reading — the arithmetic side of the metallic family has two members, and ℚ(√−3) vs ℚ(i) is the discriminating datum, not arithmeticity itself.

## What this does to L161 (the gate this cell opened)

This cell wrote *"L161: the gate OPENS — the control exists (arithmetic golden vs non-arithmetic
siblings)"*. P0-0 demanded a handle the golden passes and **both** siblings fail; arithmeticity separates
the golden from **bronze only**. The silver half of the genericity control now rests on **B997**'s
own-conductor McKay uniqueness (golden-specific, proved over the infinite family) with **B1019** (the
siblings have no McKay door) — the gate stays open on that handle, not on this one. L161 carries the
addendum. **The SM chain is untouched:** m = 1 enters E₆ through the door (π₁ ↠ 2T; B237/B1019/B997),
never through arithmeticity; silver is now a control of the right type — same arithmetic status,
different atom (ℚ(i)), no door.

Error class: **E55** (trace field vs invariant trace field), instance #1; B258 is #2. Kill graph: `B1062-V2-axis`.
