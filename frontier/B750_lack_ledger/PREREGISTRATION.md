# PREREG — B750: THE LACK LEDGER — the exact nature of the object's refusal to close (SEALED)

cc banking seat, 2026-07-21, owner's GO. Number reserved pre-use. The dual of P019: the
axiom chain maps where WE chose; this ledger maps where the OBJECT cannot choose. Gate 5:
structural/arithmetic classification only; nothing to CLAIMS; no physics reading.

## Admission criteria (fixed BEFORE the sweep — the anti-pseudo-unification guard)
An entry is admissible iff (a) it cites a banked two-outcome computation (or a classical
theorem witnessed by in-repo computations — flagged CITED) whose negative/rigid outcome IS
the refusal; (b) the refusal is OBJECT-side (observer-side bounds are inadmissible);
(c) thematic resemblance without a computation is inadmissible. Open cruxes (not yet proven
refusals) go to an OPEN ANNEX, uncounted.

## Classification enum (fixed; each class carries a banked CAN-FAIL witness — MB12)
- **T — no basepoint (the torsor lack).** Requires: an option-space the object supplies +
  a symmetry acting on it + a PROVEN absence of canonical point. CAN-FAIL witness: B711's
  golden real end IS Galois-fixed (a canonical point exists there) — so T-grading can fail.
- **R — no bandwidth (the rigidity lack).** Requires: proven absence of deformation or of
  additional channels. CAN-FAIL witness: the metallic family deforms (m varies) — variation
  exists at family level, so object-level rigidity is a nontrivial grade.
- **P — no name-transmission (the inheritance lack).** Requires: proven loss/indistinguish-
  ability at the interface or under closing. CAN-FAIL witness: B437 — golden dynamics
  traces DO transmit to the (5,1) child's abelian tower; transmission can succeed, so a
  proven non-transmission is a nontrivial grade.
- **X — unclassifiable.** The falsifier bucket: an admissible refusal fitting no class.

## Two-outcome verdict (fixed)
UNIFIED-k: all admissible entries fall into the k ≤ 3 named classes, X empty (k reported) —
vs SCATTERED: X nonempty (the lack has no unified nature; that outcome is equally banked).
Declared expectation (from the pre-GO assessment, on record): k ∈ {2,3}; P's status depends
on whether the census triple and B532 admit under (a) — the sweep decides, not the wish.

## Candidate list (declared; the sweep may DROP but not silently add — additions get logged)
B711, B712 (non-canonicity); B701 (sister basepoint); B713–B716 (the four closings, graded
individually); the chord sign (B64 parity + B711); B739 (character-rigidity); Mostow
rigidity (CITED; in-repo witnesses); B746's voice-content restriction; the census triple
B288/B740+B747/B748 (identity non-inheritance); B532 (the gap as objectivity's edge).
OPEN ANNEX candidates: B570 Lane C (c-into-θ); QP-2 (privacy kernel, uncomputed).
Excluded-by-criteria demonstration (the admission rule can fail): B733 (observer-side menu
bound — inadmissible by (b)).

## Deliverables
The graded ledger (entry → class → banked citation → the computation's verdict line); the
distilled verdict sentence; the falsification edge (what future banked refusal would break
the classification); lock test. Spot-checks in-sandbox for any fact the ledger itself
asserts beyond citation.
