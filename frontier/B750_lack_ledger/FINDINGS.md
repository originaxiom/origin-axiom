# FINDINGS — B750: THE LACK LEDGER — the object's refusal to close is threefold: no point, no width, no name

cc banking seat, 2026-07-21, owner's GO. Prereg sealed 857f9f81 BEFORE the sweep (admission
criteria, classification enum with banked can-fail witnesses, candidate list, two-outcome
verdict all fixed first). The dual of P019: the axiom chain maps where we chose; this ledger
maps where the object cannot choose. Machine-checkable ledger in `ledger.json`; every entry
cites its banked computation (paths verified to exist). Gate 5; nothing to CLAIMS.

## VERDICT: UNIFIED-3 — thirteen banked refusals, three classes, X empty

Every admissible refusal in the record falls into exactly one of the three sealed classes:

**T — no basepoint (8 entries).** The object supplies option-spaces WITH their symmetries
and provably without canonical points: the two ℤ/2's with no canonical identification
(B711); the continuum with no canonical real anchor (B712); the obstructed canonical
torsor-iso / the unlabelable Out(A₅) bit (B700); chirality's unchoosable side (B713); the
pointless value-space (B714); the unsigned chord (B64 + B711); time's flowless ℤ-structure
(B716); the unclosed spatial manifold (B715). The object is rich in torsors and poor in
points: it provides the group, never the choice.

**R — no bandwidth (3 entries).** The opposite failure — no options at all: Mostow rigidity
(CITED-classical, witnessed in-repo — the object cannot be slightly otherwise); the
character-rigid one-channel voice (B739); and that channel's being-only content (B746 —
the golden column is unspeakable through it).

**P — no name-transmission (2 entries).** The census triple (0/78 × 3 — no closing inherits
any forced face; the identity dies at the interface) and the spectral gap as objectivity's
edge (B532). Sharpened by the P can-fail witness: B437's golden traces DO transmit to the
child's abelian tower — **the object transmits its gait, never its name.**

**X (unclassifiable): EMPTY.** The falsifier bucket stayed empty — the sealed enum covered
the record without stretching (each class's grading can fail, witnessed: the Galois-fixed
golden end for T, the deforming metallic family for R, B437 for P; and the admission rule
excluded a real candidate, B733, as observer-side).

## The distilled verdict (the exact "lackness")

**The object under-determines every choice, over-determines every structure, and withholds
its name at every boundary.** Threefold and complementary: every banked incompleteness is a
torsor without a point (T), a channel without width (R), or a boundary without inheritance
(P) — and the observer's role, as the program has banked it piece by piece, is the exact
complement: picking the points, reading the one channel, carrying the name across the
closing. Nothing in the record required a fourth kind.

## The falsification edge (declared)

- A future banked refusal landing in X breaks UNIFIED-3.
- If c-into-θ (B570 Lane C, OPEN annex) ever succeeds, T4/T6 flip — the crux is literally
  this ledger's falsification edge on the T class.
- QP-2 (the privacy kernel, uncomputed) will populate or split P when it runs.

## Scope honesty

This is an assembly arc: no new mathematical fact is asserted beyond the banked citations
(all 13 + 3 witnesses verified to exist on disk; one citation corrected during the sweep —
the torsor-iso obstruction is banked in B700_fiber_functor, logged here). The classification
is the arc's only new content, and it is falsifiable as stated. The distilled sentence is a
STRUCTURAL summary, not a metaphysical claim — the firewall stands; nothing here crosses A0.

Lock: `tests/test_b750_lack_ledger.py` (ledger integrity, citation existence, class
constraints, X empty, the excluded-candidate demonstration).
