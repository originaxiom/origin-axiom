# ADDENDUM (2026-09-18, B1431) — the headline was the (0,0) class standing in for the invariant

This arc's §3 reports that the object and its sibling m003 carry the **same** index, and states the scope
correctly: *"what is shown identical here is the `(0,0)` class… whether some other class separates them is open
and computable."* That question is now run, in **B1431**, and the answer is **yes**.

**The 3D index does separate m004 from m003.** m004's own published meridian series occurs at no class of
`H₁(∂m003; ℤ)`, and m003's longitude series occurs at no class of `H₁(∂m004; ℤ)`; both differ from every
competitor at `q¹`.

**Why this arc's measurement was nonetheless right.** There is an exact identity of the two state sums,
`I_m003(γ) = I_m004(Aγ)` with `A(x,y) = (−(2x+y), −y/2)` of determinant 1 — but `A` identifies the *saturated*
lattices, not the two `H₁`s, which land on different index-2 sublattices. The half where the identity is honest is
`y` even, and **that half contains `(0,0)`**. So the collapse this arc measured is real; it is not the whole
invariant.

**Also corrected by B1431:** this arc's grouping of twelve census manifolds into seven classes *by volume* is an
artefact of the single class. The full collection separates m006/m007, m015/m016, m015/m017 and m016/m017, all of
which have identical `(0,0)` series.

**What stands unchanged:** every number in §2 and §5, the published cross-checks, the bug in §4 and its controls.
What changes is §3's emphasis — and the change is in the programme's favour, since the invariant can see the
distinction the object's chirality lives on.
