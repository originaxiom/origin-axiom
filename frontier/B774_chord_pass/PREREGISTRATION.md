# B774 PREREGISTRATION — THE CHORD-PASS CAMPAIGN (owner-directed, post-W4-304)

*Sealed before any classification/re-computation. B773 proved a banked negative
(W4-304) was a trace-projection artifact — the θ-odd/chord sector carried structure
the trace read as zero. The owner directed a chord-pass over the OTHER banked
trace-level negatives across the whole record before Phase 2. This campaign does it
in two sealed stages.*

## Stage A — SCAN (identify the chord-blind negatives)
Agents survey the banked-negative record across its surfaces — the census
WALLED/EXTERNAL rows, the wave RESOLVED-B negatives, LAW_MAP no-go rows,
speculations/TOMBSTONES, and the program's headline negatives (the "walls", the
rung-1 algebraic sweep, the L-function sweep). For EACH banked negative, classify:
- **projection**: does it compute at TRACE / character / abelianized level (where
  par=even−odd or tr() can hide a θ-odd cancellation), or is it already chord-level /
  structural (an order mismatch, a Galois/totient fact — B766-immune)?
- **chord_could_differ**: could a θ-odd / non-abelian / framing-sensitive analog
  return a different answer? (The W4-304 signature: a par-zero that is even=odd
  cancellation, not both-vanish.)
- **priority**: LOAD-BEARING (the program leans on this negative) vs incidental.
Output: the ranked list of chord-recompute candidates. A STRUCTURAL negative
(B766-immune) is NOT a candidate — it needs no chord test.

## Stage B — RE-COMPUTE (the flagged candidates)
Each flagged candidate re-computed at the θ-odd/chord level, exactly as B773:
the decisive question is HARDENS (chord analog also empty → the wall is real) vs
LIGHTS-UP (chord analog nonzero → the trace projection hid structure, an overturn).
**Symmetric discipline (the W3-082c lesson): a chord-positive must be a genuine
non-abelian/θ-odd object, NOT a finer abelian invariant relabeled — independently
reproduced in-cell before it is believed.** UNRESOLVED honest → NEEDS-SPECIALIST.

## Outcomes and the bar
- Every candidate: HARDENS (RESOLVED-B) / OVERTURNED (RESOLVED-A, reproduced) /
  NEEDS-SPECIALIST (UNRESOLVED). Overturns are the high-stakes finding — each
  independently verified before banking, hand-checked by cc.
- The campaign's deliverable: a re-projected negatives ledger — which walls are
  chord-hardened (real), which were projection artifacts (overturned), which are
  structural (never at risk). Phase 2 builds only on the chord-hardened + structural set.
- Gate 5 / Gate 5-Q; overturns are structural θ-odd facts of the object; nothing
  to CLAIMS; the one-number pin untouched.

## Method: the binding B771 block + the B773 chord discipline. Sealed first; Stage B
launches on Stage A's candidate list; one heavy workflow at a time (contention rule).
