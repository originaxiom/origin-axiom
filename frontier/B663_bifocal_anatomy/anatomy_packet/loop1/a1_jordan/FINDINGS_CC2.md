# A1 — BANKED: THE HEART NAMED — v0 IS AN INVERTIBLE JORDAN ELEMENT WHOSE SHARP
# IS ITSELF (cc2 + subagent, 2026-07-17; prereg 247ace23)

THE TRICHOTOMY RESOLVED (exact, all gates passed):
- v0 x v0 != 0: the cross-square s(z) = N(v0, v0, z) is supported on EXACTLY
  v0's own three indices {12, 13, 14} with values (-2, 2, -2) = -2 x v0's own
  pattern (1, -1, 1): **v0-sharp is proportional to v0 itself.**
- N(v0, v0, v0) = -6 != 0: **RANK 3 — INVERTIBLE.**
VERDICT: the invariant line is NOT a Cayley-plane point (the sealed rank-1
romance dies) — it is the OPPOSITE and better: an invertible element of the
exceptional Jordan algebra with sharp-fixed direction — the IDENTITY-TYPE AXIS
of the Jordan structure, singled out by the holonomy. The heart's name: the
Jordan unit direction.

THE RETRO-EXPLANATION (the coherence check that makes this a discovery):
W1's mystery — why is the portal a rank-5 ISOMORPHISM? — is now mechanism:
cross-multiplication by an INVERTIBLE Jordan element is nondegenerate; the
portal is invertible BECAUSE the heart is. One organ's classification explains
the other organ's law. (Also consistent: sharp-fixedness is the identity's
signature — e-sharp = e, N(e) = 1; v0 = a scaled isotope unit.)
In-flight tail (completes in a1_run.log): the dim of invariant symmetric
bilinears + T2(v0, v0) — appended to the ledger when done.
Frontier feed: (i) the silver v0's Jordan invariants (the same trichotomy on
m136 — is unit-direction-ness metallic-universal?); (ii) the isotope structure:
which Jordan isotope has v0 as ITS identity — compute the isotope's norm form
and compare with the banked cubic (a derivation cell).
Artifacts: a1_jordan.py, a1_run.log (+json when the tail lands). Repo untouched.
