# B1042 — the corpus names its lessons where it finds them, and the register that generalises them stopped 122 arcs ago

**Date:** 2026-08-11 · **Lane:** the consolidation refresh — the governed registers. Gate 5
untouched; zero anchors; nothing to `CLAIMS.md`; **no mathematics asserted or disturbed.**
**Files:** `verify.py` → `results.json` (11 checks) · lock `tests/test_b1042_error_ledger.py`.

**B1041 found Review 42's governing finding recurring within two days**, because its prescribed
action was a prose checklist row. **This is the same shape one level up — and it is the reason.**

---

## 1. THE REGISTER STOPPED, AND NOTHING SAYS SO

`docs/ERROR_LEDGER.md`, whose own header reads *"One entry per ERROR CLASS, not per incident.
**Reviews check the window's disclosed errors against this taxonomy**: a recurrence strengthens the
class's standing rule; a new class gets a new entry"*:

| | |
|---|---|
| classes | **36** |
| **highest arc cited** | **B920** |
| latest date | **2026-08-06** |
| corpus at | **B1042** — a lag of **122 arcs** |

**And `doc-currency` cannot see it.** The gate watches **15** documents; the three registers whose
entire job is currency are **all outside it**:

- `docs/ERROR_LEDGER.md` — the error taxonomy
- `docs/RETRACTIONS.md` — whose *own* maintenance rule is *"every future retraction adds its row in
  the PR that banks the correction"*
- `docs/REPRESENTATION_TRIAGE.md` — the gated register of unrepresented arcs

> **Exactly the gate hole B1036 found for `knowledge/INDEX.md`**, in the room one level more
> load-bearing. A register that is never measured for staleness cannot report that it stopped.

## 2. E1's INSTANCE LIST SAID THREE. THE REFRESH FOUND FIVE MORE.

`GOVERNANCE` §13 calls E1 *"the program's single most recurrent error class."* The register's
evidence for that, read from git at the pre-repair commit, was **three instances, all from
2026-07-16**.

**This refresh found five more, each banked with a lock, each counted by the arcs themselves** —
B1034 writes *"four undeclared-convention collisions"*, B1036 adds *"the fifth"*:

| collision | arc |
|---|---|
| `θ` naming **three** distinct objects | B1026 |
| **`B62 = 2 × P33`** — one identity, two conventions | B1026 |
| B939's **transposed** shadow-map prose, which a later sealed cell had to clear before it could run | B1024 |
| **`κ` naming two quantities** — a constant ≈0.86 that can never be 2, and the coordinate whose `κ = 2` *is* the founding sentence — **one of them exported by the certified core** | B1034 |
| the **entropy factor of two**, `4 log φ` vs `2 log φ` (Pesin's `Σλ⁺` against summing `|λ|` both ways), in two documents a new seat reads first | B1036 |

**The claim was becoming *better* supported while the register that carries it stood still.**

## 3. THE PASS'S MOST RECURRENT FAILURE HAD NO CLASS — AND HAD BEEN NAMED TWICE

**Self-measurement** — *an arc that both measures a gap and fills it invalidates its own metric* —
reached **ten instances across nine arcs** in this refresh, self-counted in the arcs' own words:
B1033 *"SIX instances across five arcs"* (**and it moved a published headline, 234 → 232, before it
was caught**), B1035 *"eighth … the first predicted in advance"*, B1036 *"ninth … the sharpest
yet"*, B1037 *"Tenth … the first to break a passing lock"*.

**It was named twice before this arc, and generalised neither time:**

- `THE_LADDER` **X31**: *"Registering a gap creates hits for the gap, so the coverage count
  self-inflates; a future review must exclude recording-arcs."*
- **Review 42** — an annotation of that same row.

> **Both are attached to the one row they were found on.** Neither reached the taxonomy, so every
> subsequent arc met the hazard fresh — ten times. **This is B1041's finding one level up: the
> corpus names its lessons at the site of discovery, and the generalising register is not part of
> the loop.**

## 4. WHAT WAS ADDED, AND WHAT WAS DELIBERATELY NOT

**Two new classes** — and the ledger's header (*"one entry per ERROR CLASS, not per incident"*) is
what decides which findings earn one:

- **E37 — self-measurement.** Standing rule, which is **B1033's, not invented here**: *a
  measurement published by the same commit that changes what it measures must name its own
  exclusion set, in the artifact, next to the number* — scoped by **authorship**. Credits X31 and
  Review 42 as having named it first.
- **E38 — progress-eroded threshold.** *A lock encoding a structural claim as an absolute count,
  inside a programme whose purpose is to change that count.* It passes until the work succeeds,
  then fails **with the finding intact** — so the failure says nothing about the claim. Instance:
  B1033's `> 200 rows dropped`, broken at **198 of 216** by B1038+B1039+B1040 while the lowest
  above-bar row stayed **B870** and the share stayed **92 %**. **B1033's own prose had written the
  correct form and the check did not implement it.** Distinct from E27 (that flag was fully wired)
  and E31 (those preconditions were met).

**Two one-offs filed as INSTANCES, not classes** — the header forbids per-incident entries:

- **E31** gains **B1039's anti-homomorphic `sym_power`**: the instrument was algebraically wrong
  while *every downstream number stayed correct*, so **nothing would have looked wrong**; only the
  control knew.
- **E36** gains **B1041's clobber (mine)**: importing `d3_measure` to inspect it ran its
  `__main__` and overwrote banked artifacts.

**NOT done — the gate.** Adding the three registers to `doc_currency`'s `LIVING` changes what fails
the build, and **the tolerances are the real decision**: `CLAIMS.md` sits at 40, `CAMPAIGN_STATUS`
at 5. *A taxonomy should probably be slack; `RETRACTIONS`, whose own rule is same-PR, should
probably be tight* — but that is judgement, not computation. **Registered as L163**, with the
repair it would have caught already made.

---

**Verdict: PROVED** as an audit-and-repair. 11 checks; nothing here is a new error, and every
instance cited was already banked, locked and disclosed in its own arc. **This arc moves nothing
into the record that was not already in it — it makes the register reflect the arcs.**

**And E37 fired on this arc within the hour — the eleventh instance, and the first caught by a
LOCK rather than by its author.** Two of B1042's own edits tripped checks in earlier arcs:

- **B1032** measured that B889/B890/B891 sit on no curated surface. **This arc's currency read on
  `THE_SM_VERDICT` cited them** — correctly, as the live second route — and B1032's check went red.
  Its exclusion dropped rows naming *B1032*, which scopes an arc out of its **own** measurement but
  not out of **later** ones. **Widened to "this arc and every later one"** (B1037's pattern), so the
  figure means *"as at B1032"* — which is what it always claimed. *A second fix was needed on my
  side too: my prose cited the cluster without naming the arc that established it, so it was
  unattributable by construction. Now it credits B1032.*
- **B1033** locked that the phrase *"most recurrent"* is **not sourced** in `ERROR_LEDGER` — it
  lives in GOVERNANCE §13. **My E1 extension quoted it**, and the lock fired. Reworded to reference
  the characterisation without reproducing the phrase, so B1033's finding stands untouched.

> **Both were caught in seconds by locks written by earlier arcs in this same refresh.** That is
> the argument for E37 existing as a class rather than as two notes: *the hazard is not that an arc
> makes this mistake — it is that the next arc has no way to know it is a known mistake.*

**Self-correction, and it is E37 by construction.** This arc **measures a register and edits it in
the same commit**, which is precisely the hazard it mints a class for. The pre-repair state is
therefore read **from git** (`git show HEAD:docs/ERROR_LEDGER.md`), not from the working tree and
not from memory — the exclusion set is the commit boundary, named here next to the number, which is
E37's own standing rule applied to the arc that writes it.
