# B912 — the sealed norm cell decides: OUTCOME B — the obstruction is EXACTLY the colored sector; H has signature (15,12); nine scales exist

**Date:** 2026-08-05 · **Seats:** computation agent (per the sealed prereg) + cc
banking verification · **Prereg:** sealed `93d420ea…`, committed `878137e2`
BEFORE compute; seal verified unbroken at banking.

## The verdict, verbatim against the sealed criteria

**OUTCOME B — OBSTRUCTION**: some atom-Gram is indefinite. Six of the fifteen —
**exactly the six colored (3-dim, quark-type) atoms, every one with signature
(1,2,0)**; none degenerate (margin 0.489 vs residuals < 10⁻⁴⁰). All nine
colorless 1-dim atoms are **positive-definite**, with scales (normalized to +1
on the first canonical vacuum line):

> vacuum lines: 1, 19.14202074…, 1.68032027… · conjugate pairs:
> 53.07863844…×2, 0.78626003…×2, 3.08893572…×2
> colored |det Gram|^{1/3}: 1867.68825…×2, 702.46346…×2, 451.71618…×2
> (indefinite — recorded as magnitudes with signature tags, NOT scales)

Per the seal: the scale assignment fails as posed; R2/R3 require a different
construction, and the register receives a dated amendment (below, executed).

## What was PROVED on the way (exact)

- **J exists and is unique up to one real scale**: Hom(27, 27∘φ) = 0 verified,
  so the intertwiner threads the dual; the rational system ρ(x)ᵀH + Hρ(φx) = 0
  solved EXACTLY over ℚ — **H± are integer ±1 signed-permutation symmetric
  matrices** on the weight pairing wt ↦ −flip(wt); equivariance verified on
  all 78 generators, both φ±; uniqueness by the connected ratio-graph
  elimination (zero conflicts).
- **The signature of H on the 27 is (15, 12)** — exact by cycle count. Fenced
  observation, banked as structure: 15+12 is precisely the su(6)⊕u(2)-side
  K-type dimension split of e₆(2).
- **The atom identification is exact**: the 15 banked atoms equal the joint
  charge eigenblocks span-for-span (rebuilt at the full-tower prime, then
  dps-60 certified on the ℂ-side; residuals ≤ 10⁻⁴⁴).
- **The σ± cross-check**: the entire declared readout is identical between the
  two wall involutions (< 10⁻⁴⁰ relative). At matrix level H₋ = H₊·D with D a
  charge-commuting diagonal involution (+1 on all nine colorless atoms, tr 3)
  — a PROVEN inner-twist relation. The seal's literal proportionality gate is
  unsatisfiable by this theorem; the banking seat RATIFIES the agent's
  invariant reading (the gate's intent — readout agreement — holds exactly);
  logged as a seal-interpretation ruling, not UNSTABLE.

## The reading (fenced)

The norm cell did not fail — it drew a line through the middle of the matter:
**the colorless world is Euclidean under the object's canonical Hermitian
structure; the colored world is Lorentzian, one cone per colored atom.** The
prior (definiteness throughout) lost — the fourth sealed prior in a row to
lose. Whatever R2 becomes, it must speak signature, not just scale.

## Files

`PREREGISTRATION.md` (sealed), `norm_cell.py` → `results.json`,
`DRAFT_FINDINGS.md` (the agent's full record). Locks:
`tests/test_b912_norm.py` (seal + the verdict data).

## Depends on

B907 (the two conjugations), B893 (ω), B889 (the vacuum lines), B883 (the 27),
B906 (the atoms), B901/B898 (the constraint machinery).
