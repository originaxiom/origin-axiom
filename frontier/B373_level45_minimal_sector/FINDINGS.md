# B373 (PD1.1) — the kill test: MOVED. The minimal sector is gapless-trending down the tower

**Status: banked (frontier) + E17 promoted at banking (certified data). The pre-registered
verdict: the MOVED outcome, hit verbatim (the registered example was "±36° = 108°/3, exponents
a = ±6 of 60" — exactly what the computation returned). The relayed independent bet ("pinned")
LOSES — recorded as the protocol working. Campaign PD1.1. Firewalled throughout.**

## The census and the sector (exact; 3 primes, held-out embeddings, level-15 gate inherited)

At level 45 (ord W₁ = 60): six mult-1 opposite pairs exist; **exactly one** is invariant under
the full Weil image — **{6, 54} = exponents ±6**. The dihedral relation `ŜW₁Ŝ⁻¹ = W₁⁻¹` holds
globally at 45 (as at 15). On the sector, exactly:

- `tr ρ(A₁) = 1/2 + (1/2)√5 = φ` — the golden mean itself (= 2cos 36°); at level 15 it was
  `1−φ` (= 2cos 108°). **The golden quasi-energy moved: ±108° → ±36° = 108°/3.**
- `tr ρ(A₂) = 1` (±60°, unchanged from level 15 — the silver phase is pinned; seed-dependent
  behavior, banked as data).
- `tr ρ(Ŝ) = 0` — the helicity pairing persists.

## The structural reading (the tower hinge)

The eigenvalue EXPONENT is pinned at ±6 while the order grows with the level
(ord = 20, 60 at N = 15, 45 — i.e. 4N/3): the angle is `2π·6/(4N/3) = 9π/N → 0` along the
tower. **Gapless-trending**: the finite-level "mass-shaped" phase is a lattice artifact of the
level, not a pinned topological charge — precisely the signature the tower-limit door (PD2.2)
needs to be alive. The pinned alternative (anyon-shaped) is now the refuted branch at this rung.

**Pre-registered prediction for the next rung (level 135, BEFORE computation):** the unique
invariant two-state sector sits at exponents ±6 of ord(W₁) = 180 — quasi-energy ±12°, trace
2cos 12°; helicity pairing and the global dihedral relation persist. KILL: any deviation
(different exponent, non-uniqueness, broken pairing) refutes the pinned-exponent law.

**Provenance.** PREREGISTRATION.md (PR #460, committed first); B372 (engine + level-45 gate);
P56/P57 (the level-15 sector and conventions). Reproducer: `kill_test.py` (~8 min); locks:
`tests/test_b373_kill_test.py`.
