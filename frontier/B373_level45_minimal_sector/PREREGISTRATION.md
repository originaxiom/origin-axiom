# B373 (PD1.1) — the level-45 minimal-sector KILL TEST: pre-registration (before computation)

**The mass-word's price, made decisive.** At level 15 the minimal two-state sector (P56) has
quasi-energy exactly ±108° per monodromy period (the pentagon angle; `tr ρ_slot(A₁) = 1−φ`).
A photon-shaped continuum story needs the phase to MOVE toward 0 along the level tower; the
pentagon rigidity suggests instead that it is PINNED — anyon-shaped, topological, gapped.

**Pre-registered outcomes (both bank; the relayed independent bet is on "pinned"):**
- **PINNED**: the level-45 minimal invariant two-state sector exists and its golden quasi-energy
  is again exactly ±108° (`tr ρ_slot(A₁) = 1−φ` exactly, eigenphases a = ±18 of 60) ⇒ the
  sector is anyon-shaped; the photon costume is retired for this object (KILL photon-shape).
- **MOVED**: the phase is strictly smaller (e.g. ±36° = 108°/3, exponents a = ±6 of 60) or
  otherwise level-dependent ⇒ gapless-trending; the tower-limit door (PD2.2) gets a live hinge.
- **THIRD OUTCOME** (reportable): no 2-dim invariant sector exists at 45, or more than one —
  the minimality structure itself is level-dependent; report the full invariant-sector census.

**Construction (declared):** level-45 theta-lift conventions verbatim from B372 (F_p engine,
3 primes ≡ 1 mod 720, held-out embeddings; the level-15 pipeline gate re-run as the hard gate).
Census: for every mult-1 eigenvalue-exponent pair {a, −a mod 60} of W₁@45, test invariance of
P_a + P_{−a} under both generators (F_p zeros across all primes; the found sector re-verified
in exact ℚ(ζ₁₈₀)-free form via the same F_p certificate at 2 extra primes). For each invariant
2-dim sector: exact `tr ρ(A_m)` (m = 1,2,3,4), the Ŝ trace and its square's phase, and the
dihedral check Ŝ W₁ Ŝ⁻¹ = W₁⁻¹ at 45. Verdict read ONLY against the outcomes above.
