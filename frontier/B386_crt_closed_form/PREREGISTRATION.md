# B386 (W1) — PRE-REGISTRATION: the CRT closed form of the 1/12

**Committed before computation. Target: derive the banked class partials −1/16 (generic)
and −1/48 (5-boundary) — hence the 1/12 — in closed form, by the level-15 = 3×5 tensor
split. Time-box: 2 sessions (campaign W1), then BANK or STAGE.**

## L1 — the tensor identity (the gate)

The level-15 theta model tensor-factorizes by CRT (P63's structure): with the local theta
models at q=3 and q=5 (their own W_m = WR^m·D^m at the CRT-induced multipliers), the claim:
    C[j,l] = tr(Par·W₁ʲW₂ˡ) = C₃[j,l] · C₅[j,l]   for ALL (j,l) ∈ ℤ₂₀×ℤ₁₂,
with C_q the corresponding local Par-graded trace (indices mod the local orders). The
multiplier convention is FIXED by the banked tensor law ((u₃,u₅) = (2,2) at 15, B377) —
if the naive (1,1) fails, (2,2) is the registered second (and last) convention attempt.
KILL: no convention matches — the Par-trace does not tensor-split (bank the defect table).

## L2 — the windowed split (conditional on L1)

The slot windows and the class-1/class-5 restrictions both factorize by CRT (invertibility
mod 15 = invertibility mod 3 AND mod 5). Then each banked partial is a PRODUCT of local
windowed sums:
    partial(cls) = Σ₃(cls₃) · Σ₅(cls₅) / (240-normalization split),
each local sum a ≤ (ord₃×ord₅)-term Gauss-type sum over a 3- or 5-dimensional model —
small enough to evaluate in closed form (Gauss sums + geometric series). REGISTERED
SUCCESS: −1/16 and −1/48 reproduced as explicit products with every factor named
(local Gauss sums, window norms, class densities). KILL: the local sums do not close
(bank them as exact data + the obstruction).

## Outcomes

Two-outcome at each leg; L1's kill is itself a sharp structural fact (the Par-grading
would be the non-tensor ingredient — localizing the seam's obstruction to CRT). Machinery:
cyclo_engine (ζ₆₀ ⊃ ζ₁₂, ζ₂₀), step0 par_trace pattern at q=3,5. Locks from output JSONs.
Firewalled level-15 statements throughout.
