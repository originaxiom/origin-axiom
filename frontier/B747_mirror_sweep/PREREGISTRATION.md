# PREREG — B747: the √5 MIRROR sweep — do any children re-see the HEARING field? (SEALED)

cc banking seat, 2026-07-21. The golden ledger's door #1 (B746). Number reserved pre-use.
B288/B740/cc2-door1 established: NO child in the |p|≤8, q≤8 grid re-sees the being ℚ(√−3)
(78/78, two-seat). The MIRROR question was never run: does any child's invariant trace field
contain √5 (the hearing)? Gate 5: arithmetic only; nothing to CLAIMS.

## Sealed input
cc2's door1_final.json (sha256 77818016… — matches cc2's SEALS.txt), copied into this arc:
78 slopes with two-seat-verified field DEGREES (B740's mutual residual closure).

## Protocol (fixed)
- Stage 1 (theorem): the 54 ODD-degree slopes contain NO quadratic subfield at all (odd
  degree ⇒ no index-2 subfield) — closed from the sealed degree table, no computation.
- Stage 2 (exact): the 24 EVEN-degree slopes (degrees 4,6,10,14,18,26,30; incl. ±5,1 and
  ±7,1): recompute the invariant trace field via the B740 pipeline (snappy find_field,
  prec ladder 600→2000, degree cap per sealed degree + safety margin), CROSS-CHECK the
  computed degree equals the sealed two-seat degree (mismatch ⇒ slope flagged UNRESOLVED,
  never silently swapped), then exact containment: does x²−5 have a root in K?
- Controls BEFORE the sweep (all must pass or the run aborts): x²−5 contains √5 (routine+);
  x⁴−x²−1 (=ℚ(√φ)) contains √5 (routine+, nontrivial embedding); x²+3 does NOT (routine−);
  x³−2 does NOT (odd−); m004 cusped field contains √−3 but NOT √5 (pipeline control).
- Flag rule: (±5,1) and (±7,1) are a-priori-interesting (B437 golden traces live in the
  (5,1) child's ABELIAN tower — a DIFFERENT object than the trace field); declared here so
  a hit there is not silently promoted — any hit gets the E20/base-rate gate before reading.

## Two-outcome verdict
- MIRROR-SILENT (0/78): closing silences BOTH columns — the interface loses both faces;
  "the atom lives at the cusp" extends to the full V₄-relevant quadratics tested so far.
- MIRROR-ALIVE (hit list): the hearing SURVIVES closing where the being does not — a sharp
  two-column asymmetry; E20 gate + look-elsewhere (78 slopes × 1 field tested) before any
  reading; no physics language either way.
