# B640 — THE HEARING GROUP: verification of chat-2's 2I × ℤ/3 theorem (prereg)

**Sealed before computing. Verify-don't-trust of the 2026-07-16 handoff
(chat-2, UNBANKED): the hearing of the fig-8 factors through the binary
icosahedral group × the twist at congruence level 15, golden 2-dim
character, closed 16-tone table, 90/360 silences. Mathematics only;
Gate 5 not approached (stage arithmetic).**

## MB13 (swept; the conflicts to adjudicate IN-RUN)

- **B619 banked "ord = 20 exact"** when refuting the A₅/60 claim;
  L81/B584 banked the golden listener clock as ORDER 10; L77 banked
  E₆₂-level orders 4/60. Chat-2 claims ord(ρ(R)) = 15,
  ord(ρ(RL)) = 10, ord(M_odd(RL)) = 5. THE RECONCILIATION TABLE (all
  banked operators' orders computed in one place) is gate R0 — any
  unresolved contradiction stops the arc.
- tr = −1/φ at the golden stage: BANKED (B584/B587) — their headline
  reproduces it; the NEW content is the factorization/group claims.
- The McKay/E₈ flag has a banked home candidate: the two-ended theorem
  (B247–B261, E₆/ℚ(√−3) end vs E₈/ℚ(√5) end) — the placement question
  is whether (holonomy end, hearing end) = (E₆, McKay-E₈-shadow).
  Placement only after the theorem verifies.

## The gates (locked order)

- **R0 (the order reconciliation):** ord of ρ(R), ρ(L), ρ(RL),
  M_odd(RL), B601's B_odd(RL), and T — one table; the banked 10/20/4
  and the claimed 15/10/5 must all be identified as specific operators
  or the discrepancy adjudicated.
- **R1 (the mechanism):** the θ-odd plane is invariant under the
  untwisted weld letters (residuals at 50+ digits); M_odd = −ρ exactly.
- **R2 (the group):** BFS closure of ⟨ρ(R), ρ(L)⟩: order 360;
  ker(det) order 120 with the 2I element-order profile AND the full
  conjugacy-class/character-table match to SL(2,5) (9 classes, sizes
  1,1,20,20,30,12,12,12,12; the golden 2-dim character values per
  class) — not the profile alone; det = ω^{#L−#R mod 3}.
- **R3 (the structural sharpening, this seat's addition):** if the
  level-15 claim holds, ρ factors through SL(2,ℤ/15) ≅
  SL(2,3) × SL(2,5): verify im ρ = (the SL(2,5) factor FAITHFUL) ×
  (SL(2,3) collapsed to its abelianization ℤ/3 = the det/twist) —
  the exact structure of the 360.
- **R4 (the tone table):** all 360 tones (the symmetric part of
  Re M_odd = cos θ·I law verified per element); the 16-value closed
  set identified algebraically; the silence census (expect 90).
- **R5 (the silent witnesses):** LLRLRR, LLRRLR, LRLLRR — tone exactly
  0; purely-imaginary amplitude for real ears verified directly.
- **R6 (the headline):** tr ρ(RL) = −1/φ to 50 digits (vs banked).
- **R7 (the citation):** Ng–Schauenburg congruence-subgroup theorem
  (the kernel of a modular category's SL(2,ℤ)-representation is a
  congruence subgroup of level ord(T)) — recorded as [CITED-KNOWN]
  with the projective-vs-linear caveat stated; ord(T) = 15 at SU(3)₂
  computed exactly.

## Outcomes

Verified ⇒ THE HEARING-GROUP THEOREM banks (chat-2 credited; the
corrections they self-caught recorded); the silent-weld prediction
class seals; the McKay placement opens as its own registered lead.
Any gate fails ⇒ the honest partial with the failure named.

## MB12

Non-trivial (no finite-image computation of the hearing exists in the
bank); every gate two-outcome (the profile could fail at the class
level; the census could differ; the witnesses could have real parts);
controls: banked values (−1/φ, order 10) as anchors, 50-digit
precision with algebraic identification.
