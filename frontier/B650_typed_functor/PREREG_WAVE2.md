# B650 wave 2 — TRACK F: the L84 map (campaign a463c6aa)

**The question (A1's morphism-level debt): the functorial,
monodromy-equivariant linear map H¹-classical → H_stage-odd — or prove
none exists. The banked constraints: B595-D1 (dims match golden-only),
B595-D2a (det(I−B_odd) = φ² = λ(A₁)), B586 (gradings), B596 (clock
multipliers), and NEW since L84 was posed: B644 (the hearing factors
through mod 5 — ρ_hear(A₁-word) has ORDER 10) and B646-Q2 (the clock =
ord through the non-abelian core).**

## Conventions block

- Source: any classical 2-dim A₁-module with the banked hyperbolic
  action (eigenvalues λ(A₁)^{±1} = φ^{±2} — H₁(T²) of the fiber, or
  the character-variety tangent; the argument needs only the spectrum).
- Target: the θ-odd hearing plane at κ = 5 with the banked
  ρ_hear(RL) action (order 10, tr = −1/φ; B640/B644).
- Equivariance: T·A_source = ρ_hear(RL)·T, ℂ-linear T.

## Gates

- **W2-G1 (exact):** the Sylvester equation's solution space computed
  exactly: eigenvalue disjointness (φ^{±2} real hyperbolic vs
  primitive-10th-root unitary) ⇒ T = 0 — verified by explicit exact
  solve, not only by the spectral criterion.
- **W2-G2 (the positive identification):** the functor that EXISTS is
  group-level: (reduction mod 5) ∘ χ_golden = B644's congruence-shadow
  functor, composed with B650's type system at the quantity level;
  stated with the banked witnesses.
- **W2-G3 (routing):** L84 and L91-4 rows updated: the linear module
  map = THE EQUIVARIANCE WALL (zero-only, proved); the typed functor
  license = B650 types + the B644 group functor; the B595-D2a bridge
  and B596 multipliers annotated as invariant-level data of the group
  functor (no overclaim).

Outcome either way completes Phase A's F-track; this is the "prove
none exists" branch of L84 executed honestly, with the existing
functor named in its place.
