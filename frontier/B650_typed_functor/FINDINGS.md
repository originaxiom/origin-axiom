# B650 — TRACK F WAVE 1 BANKED: the type system under the held-out battery (A1, part 1)

**Campaign seal a463c6aa; the contamination ordering honored: TYPES.md
v1 frozen (hash 306d6cde) from the DECLARED subset only (B584,
B611/B613) before the held-out check; no Phase-C channel discussion
has been recorded post-seal.**

## The deliverable

TYPES.md — three base types (T_spec / T_field / T_coup(X), the
three-layer separation made mechanical) + three flags (word-class vs
full-image; core vs dressed, scoped per B646-Q2; dimension) + six
rules R1–R6, encoded verbatim in `types_checker.py` (the
machine-checkable artifact GATE A demands).

## The battery (the prereg's held-out discipline, executed)

- FREEZE set: reproduces the bank 3/3 (B584 → T_coup(listener),
  W(RL), core; B613 → T_spec predicate; the B611 trace exemplar →
  T_field).
- HELD OUT (mechanical, post-freeze): B593 ✓ T_coup(listener)/F;
  B626 ✓ T_field/W(μ); the chord's Y ✓ T_coup(partner-object)/F —
  the X-generalization (external system, not just listener) was
  frozen from the campaign thesis BEFORE the check and held.
  **ONE FAILURE: B632's v₀ — ILL-TYPED** (an element, not a
  quantity). **The one disclosed revision cycle used:** R0 (domain
  rule) added — the system types QUANTITIES; representation-space
  elements are CARRIERS, typed through their quantities (v₀'s
  quantity: dim h⁰ = 1 → T_spec). Re-run: 4/4 pass. **No second
  failure — K1 does not fire.**

## What wave F2 owes (registered)

The morphism-level content: the functorial, monodromy-equivariant MAP
(L84) between same-type quantity spaces — the type system says WHAT is
comparable; F2 must produce the map or prove none exists, against the
banked constraints (B595-D1 dimensions golden-only; the spectral
bridge det(I−B_odd) = φ² at κ=5; B586 grading mismatch; B596 clock
multipliers). The R5 physics rule is already binding for Phase C's
design regardless of F2's outcome.
