# B422 — the ℤ/2 (mirror/class-group) frame: NO FRAME — the content wall holds from both sides

**Status: banked, the registered NO-FRAME branch. Firewalled; the Markov-blanket framing
was refused, the testable core was tested. Bar: the content wall.**

## The result

The golden slot line AND its mirror image (b↦−b) are BOTH exactly orthogonal to the (2,3)
Mercedes plane in the shared W₂-label space (cos² = 0, exact). The ℤ/2 (the amphichiral
self-mirror = the class-group generator σ_cl) does NOT connect the golden and Mercedes
sectors. The content wall holds under the ℤ/2, exactly as it did under the ℤ/3 (W-B1).

## What this closes

W-B1 tested the ℤ/3 frame → orthogonal. B422 tests the ℤ/2 frame → orthogonal. **The
content wall now holds from BOTH of the object's natural symmetry structures.** B414's last
named in-object frame-provider is closed. The frame the SM content would require is
genuinely EXTERNAL — the object does not contain it under any of its symmetries.

## The one untested variant (honest, with its prior)

"Two full copies interacting" (the tensor double, object ⊗ mirror-object) was NOT built.
Prior against it (strong but not a proof): the object's measures are FLAT (B413) and
flatness is product-stable (flat ⊗ flat = flat → no scale); its sectors are orthogonal and
orthogonality is product-stable (→ no frame). So the tensor double is expected to inherit
the same walls. Offered as the last untested construction, with this prior stated.

**Provenance.** mirror_frame.py → mirror_frame.json; locks tests/test_b422_mirror.py.
