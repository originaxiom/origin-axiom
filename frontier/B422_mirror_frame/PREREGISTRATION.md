# B422 — PRE-REGISTRATION: does the ℤ/2 (mirror / class-group) provide the content-frame?

**Committed before computation. The disciplined core of the owner's "interacting mirror"
intuition, converging with B414's named untried probe: W-B1 tested the ℤ/3 Mercedes frame
and got ORTHOGONALITY (golden line ⊥ Mercedes plane). This tests the OTHER structure —
the ℤ/2 (the amphichiral self-mirror = the class-group generator σ_cl). Firewalled; NO
Markov-blanket/FEP framing (unfalsifiable, refused); this is exact linear algebra.**

## The registered question

In the shared W₂-label space ℤ₁₂ (W-B1's setting): the golden slot line lives at labels
±2; the (2,3) Mercedes plane at the complementary labels. They are exactly orthogonal
(W-B1). Does the ℤ/2 action — the mirror b↦−b (P61) and/or σ_cl (the class generator) —
CONNECT them:
- Q1: does the mirror image of the golden line land IN (or overlap) the Mercedes plane?
- Q2: does σ_cl acting on the value constants provide a canonical map golden↔Mercedes?
- Q3: is the amphichiral ℤ/2 a NON-orthogonal frame (does it span golden ⊕ Mercedes)?

REGISTERED TWO-OUTCOME:
- **FRAME (wall cracks):** the ℤ/2 canonically connects golden and Mercedes (non-orthogonal,
  no arbitrary choice) ⇒ the content-wall's no-canonical-frame verdict is EVADED by the ℤ/2
  ⇒ a genuinely new opening (→ the escalation, dedicated session, per the bars).
- **NO FRAME (wall holds):** the ℤ/2 preserves the orthogonality / needs arbitrary choices
  ⇒ the content wall stands under the ℤ/2 too ⇒ B414's last in-object door closes; the
  frame is genuinely external (the honest terminal, confirmed from both ℤ/3 and ℤ/2 sides).

VACUITY GUARD: FRAME is genuinely possible — σ_cl is exact and central (it acts on all
constants); if it linked the sectors it would be a real canonical frame. Both outcomes live.

## Machinery
Banked step0 tables (the golden line ±2, the Mercedes plane); the mirror b↦−b; σ_cl =
(x,y,z,s)↦(x,−y,−z,s). Exact. Locks from JSON.
