# PREREG — B728: THE STOKES RESUMMATION — does the coherent whole reach ℚ(ζ₅)? (SEALED)

cc banking seat, 2026-07-20. Owner chose "run the Stokes resummation first" (before any top-level
disposition) — the deepest un-run test both seats flagged. cc2 pre-ran it (workflow wf_c3326acc,
prereg eb448f3f); this arc VERIFIES the decisive theorem in-sandbox and banks the two-seat verdict.
THE QUESTION: B726 found the Born interference PHASE lives in ℚ(ζ₅) — owned by NEITHER bare face
(being ℚ(√−3), hearing ℚ(√5)). Does the object's OWN Stokes-resummed trans-series (the B722-rigid
cross-branch connection between the saddles σ₁,σ₂,σ₀) reach ℚ(ζ₅) INTRINSICALLY, or is ζ₅
IMPORTED (a golden-MTC overlay / an evaluation artifact)? This is the last computation that could
still add FORCED content to the Born story. Base-rate-gated (evaluating a q-series at q=ζ_c
trivially lands in ℚ(ζ_c) — not evidence). Structural/arithmetic only; Gate 5; no SM value.

## The decisive discriminating fact (checkable theorem)
Can the compositum of the two bare faces, ℚ(√−3,√5), CONTAIN ℚ(ζ₅)? Both are degree 4 over ℚ.
- Gal(ℚ(ζ₅)/ℚ) = (ℤ/5)* ≅ C₄ (CYCLIC) — a UNIQUE quadratic subfield (ℚ(√5)).
- Gal(ℚ(√−3,√5)/ℚ) ≅ C₂×C₂ (Klein four) — THREE quadratic subfields {ℚ(√−3),ℚ(√5),ℚ(√−15)}.
Different Galois groups at the same degree ⇒ the fields are UNEQUAL ⇒ ζ₅ ∉ ℚ(√−3,√5). Bridging
FORM and WEIGHTS gives biquadratic Klein-four, NEVER the cyclic-quartic ℚ(ζ₅). (Verified in-sandbox.)

## Two-outcome (sealed)
- **OUTCOME A (closes):** the Stokes-resummed trans-series reaches ℚ(ζ₅) INTRINSICALLY (a forced,
  non-evaluation consequence of the object's own resurgence) ⇒ the coherent whole closes the Born
  interference phase; the fragmentation hypothesis is vindicated for the PHASE too.
- **OUTCOME B (imported):** the resummation's intrinsic arithmetic is ℚ (σ₀) ⊕ ℚ(√−3) (σ₁,σ₂) with
  INTEGER Stokes constants (ℤ[[q,1/q]], B722); ℚ(ζ₅) is NOT a structural home — ruled out by the
  C₄≠C₂×C₂ theorem (positive refutation) AND the base-rate trap (ζ₅ only via evaluation at q=ζ₅,
  4₁'s level N=1 not 5). The phase is a golden-MTC overlay, not forced by the object. [honest
  expectation: B — verified crux.]

## Verdict template
Bank the final Born-content ledger (form=being FORCED-CLOSED; weights=hearing FORCED-CLOSED but by
a mechanism orthogonal to the resummation; amplitudes=quartic ℚ(√φ) OPEN; phase=ℚ(ζ₅) IMPORTED).
Credit cc2 (ran the resummation). Two genuinely-open items stated not closed over (the ℚ(√φ)
amplitude sector untouched; the Stokes-σ₀ = Alexander-σ₀ identification asserted-not-proven).
LAW_MAP placement; nothing to CLAIMS. This completes the coherent-whole test the owner required
before any top-level disposition.
