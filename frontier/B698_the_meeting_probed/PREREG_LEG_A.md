# PREREG — B698 Leg A: the level-15 meeting, analytic side (SEALED before the verdict)

## The object
The figure-eight A-polynomial's elliptic model = the conductor-15 curve
15a8 (j=−1/15, N=3·5, banked B674). Its L-function L(E₁₅,s). We already
know (banked): the AL/local data a₃=−1, a₅=+1 is generic even-rank (B695,
c-symmetric); m(A_{4₁})=Vol/π is the BEING value L(χ₋₃,2), NOT the elliptic
K₂ (B683/L3).

## The question (one, sharp)
Does the level-15 object's ANALYTIC content (special L-values) carry a
NONTRIVIAL arithmetic relation coupling the being-prime 3 and hearing-
prime 5 — beyond "both divide the conductor" — OR is it the generic
analytic content of a rank-0 conductor-15 curve, with 3 and 5 entering
only as independent Euler factors (automorphic restricted-tensor-product
factorization)?

## Two-outcome (sealed)
- **COUPLED/STRUCTURED**: some special value or simple combination of
  {L(E₁₅,1), L(E₁₅,2), L'(E₁₅,0)} equals — to ≥20 digits — a nontrivial
  closed expression in the being value L(χ₋₃,2), the golden φ, and π,
  where the SAME relation FAILS on a base-rate control of random rank-0
  N=p·q curves. (Must pass BOTH the base-rate gate AND a convention/unit
  recompute.)
- **FACTORED/FLAT**: L(E₁₅,1) is generic BSD (=Ω·∏c_p·|Ш|/|tors|²), the
  Euler product / automorphic rep factors over primes (3 and 5
  independent by construction), and NO 3×5 special-value relation
  survives the base-rate gate. The meeting is a SHARED CONDUCTOR, not an
  entanglement.

## Base-rate control (mandatory, sealed)
Compute the same candidate relations for ≥12 other rank-0 curves of
squarefree conductor N=p·q (LMFDB), and for the being value against a
handful of unrelated small-disc Dirichlet L(χ,2). A "relation" only
counts if 15a8 matches and the controls DON'T (≤1/12 false-positive).

## Convention gate (mandatory, sealed)
Any candidate closed form recomputed under (a) the alternative curve in
the isogeny class (15a1) — a genuine relation must be isogeny-robust or
explicitly period-scaled; (b) L-value normalization (analytic vs
arithmetic; the √N, (2π), Γ factors) stated explicitly.

## Expected outcome (stated up front, NOT a target)
FACTORED. The automorphic representation of a squarefree-level newform is
a restricted tensor product π=⊗π_v, so the primes 3,5 are independent by
construction; B695 already showed the AL data is generic. The falsifier
(a surviving 3×5 relation) would be the surprise. We compute to find out.

## Firewall
Gate 5 stands. This is arithmetic; no physics value is claimed. The
dimensionful no-go (LAW_MAP §E) means even a surviving relation would be
dimensionless algebraic data, never a physical quantity.
