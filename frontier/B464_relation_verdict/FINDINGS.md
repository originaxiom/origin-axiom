# B464 — RZ: the Relation Campaign verdict

**Status: banked; the campaign CLOSES. Firewalled. The campaign question (plan v2.1,
PR-committed): does ANY relational invariant escape class-laundering?**

## The answer: NO — in every computed cell; two named boundaries hold the residue

| probe | live cell(s) | outcome |
|---|---|---|
| R4 (B463) | (pre-forced, labeled) | C_𝔢₆(principal) = 0 exact; long-root control 35 ✓; no continuous 4d gauge group from the principal lift |
| R1 (B460) | per-vacuum CS of the child's vacua | Kirk–Klassen multi-path method VALIDATED (0.0606170 = SnapPy); new SU(2) CS values banked (lit-gate pending); spectrum table class-forced as prereg'd |
| R2 (B461) | SL(3) elimination fields; Brunnian two-register | ℚ(i)-class confirmed; controls pass; the √−7 DEFLATION (the field belongs to a control too); L6a4 SL(3) census = named boundary; no Brunnian manifold-detector |
| R3 (B462) | φ-scan phases; 5₂ chirality payload | every exact structure forced (KM symmetry, transpose, amphichirality); the 5₂ H/B split = the piece's own chirality re-expressed; κ-diagonal = derived curve data (silver control) |
| R5 (B451) | subleading resonances | leading eigenvalue = the escape rate (Bowen–Ruelle, three-method; caught the B186 window bias); subleading = named boundary (certified enumeration required); spectral gap ≥ 0.19 in every enumeration |

**No relational invariant escaped class-laundering.** What the relational register
produced instead — and it is not nothing: the two banked-number corrections (B186's γ;
the B443-era framing), the Kirk–Klassen per-vacuum machinery, the collision census (which
seeded B467's wall results and the orientation-bit convergence), the gluing κ-diagonal,
and the two named boundaries that carry the campaign's only remaining live cells:

1. **The L6a4 SL(3) census** (machinery ready, priced — B461).
2. **The certified resonance enumeration** (n ≥ 7 orbits — B451).

## The S-room extension (S046, the relational register)

The relational register's verdict extends the one-body story unchanged: what two objects
share through any wall, gluing, pairing, or spectrum is the class's arithmetic; what is
theirs alone is derivable from their own banked curves. The interaction does create
structure the single object lacks (forks, collisions, combination gaps — the banked
K014 face) — but every such structure, once created, launders. The campaign's deepest
single output is the orientation bit's persistence through the wall (B467-F3, found by
the census this campaign built).

## Three-seat note

The campaign ran through the largest correction-traffic window in the program's history
(the residue saga) and closed with all three seats convergent on every disputed item.

## Boards
OPEN_LEADS Relation section → CLOSED (rows updated); CAMPAIGN_STATUS → closed; the
decadal review (Review 9) covers the window; the atlas re-mined at close per the cadence.
