# B920 — the register sweep: seven items, one dead contradiction, the registries current

**Date:** 2026-08-06 · **Seat:** cc (banking review of agent working-tree edits;
the cc3 audit's A2+A5, executed whole). `SWEEP_REPORT.md` carries every edit
with before/after quotes; the banking review accepted all seven.

1. **NORMSPLIT — the contradiction is dead.** The reconciling artifact
   (trace_norm_split.*) harvested byte-faithfully AND rerun locally BEFORE any
   edit: trace-level exclusives 139/37/one-odd; norm-level 12/zero-odd; the
   four norms SHARED. Both seats right about different objects. HINT_LEDGER
   row 7 amended (the hint SURVIVES at norm level); B794's FINDINGS gains the
   level-reconciliation append-only; the wrong lock replaced by the reconciled
   one (5/5); E28's instance figures corrected.
2. **ROADMAP Tier-3** — three falsehoods corrected, including catching the
   audit's own citation being off by a section letter (checked, not trusted).
3. **The five LAW_MAP orphan rows** homed into their sections; provenance
   gate green.
4. **COMMS_PROTOCOL v1.2** — cc3 and the solo seat are legal seats.
5. **THEOREM_REGISTRY/LEDGER** — the window's rows (C24–C43), with the lock
   extended and a new invariant ("Part V admits no axioms").
6. **RETRACTIONS** — same-PR catch-up for the ≥5 events, plus today's
   stale-septic instrument (the retraction-propagation failure).
7. **ERROR_LEDGER** — instance rows for the fourth oblique violation, the
   clobber class, and the silent-substitution near-miss.

All 20 gates green after the edits. Locks: the amended
`tests/test_b794_congruence.py` + `tests/test_b758_chain.py`.
