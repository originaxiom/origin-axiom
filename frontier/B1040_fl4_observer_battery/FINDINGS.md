# B1040 — FL-4: THE OBSERVER CONSTRUCTION THROUGH THE BATTERY — the ledger splits every step: the field supplies every TEMPLATE, the object makes every SELECTION, and the ℤ/3 is consumed NOWHERE

*cc banking seat, 2026-08-12. Sealed e358be1b (docs/SEAL_LEDGER.md), pushed both remotes
before compute. The compute is a structured-reading adjudication: every step of the
sealed inventory adjudicated from its arc's banked text, evidence quoted per row —
`b1040_ledger.md` is the arc's output object. Gate 5; no measured value. Mixed ledger
reported AS A LEDGER per the sealed format; the counts and the split are the table's
shape, not an aggregate verdict.*

## The three results (the counts, from the ledger)

1. **V1 — the observer construction NEVER consumes μ₆'s ℤ/3: 0 ω-essential steps of 11
   decidable.** Ten steps run on quadratic-Galois ℤ/2 machinery; one (the external
   weight) consumes no field structure at all; B723's two Galois-label clauses are
   EXCLUDED as RETRACTED (B942/B957). The corpus's own sharpest witness is B733's door
   count: **the (√−3) door — the ω-adjacent, ramified door — contributes ZERO observer
   bits.** The audit seat's μ₆ discriminator (Task B §1) therefore discriminates in the
   ℤ/2 direction: nothing in the observer stack would break on a μ₂ sister for
   unit-group reasons.
2. **V2 — every step's TEMPLATE transplants VERBATIM to the μ₂ sisters** (ℚ(√−2), and
   ℚ(√−1) mutatis the μ₄ irrelevance); field-specific VALUES (B733's door numbers)
   change as numbers, not as structure. One step has no template at all: S3, the
   geometric-characters torsor (y² − 3y + 3 is m004's character-variety data — "the
   object provides the AMBIGUITY"). **√−7 is not a clean sister and is typed
   separately throughout: B733's own k = 3 extension makes it a LEG of the observer's
   seam** — transplanting the construction "to ℚ(√−7)" partially maps the system into
   itself.
3. **V3 — the split is TOTAL and UNIFORM: every step separates into a field-generic
   template that STANDS object-deletion and an object-made selection/instance that
   COLLAPSES.** Nine steps carry both components; the external weight (S7) is
   pure-template; the geometric-characters torsor (S3) is pure-selection.

## What the sealed outcome-meanings make of this

Neither clean branch fires. The construction is **not FIELD-SCOPED wholesale** (V3
does not stand wholesale: every selection collapses) and **not OBJECT-CARRYING
wholesale** (every template stands; V1 is unanimous). Per the seal, the identification
is scoped to the passing steps and says so:

> **The observer ≡ arithmetic-ambient identification holds at TEMPLATE level — the
> field supplies every door the observer construction walks through, and supplies them
> for every sister field identically. The OBJECT makes every selection: which fields
> (the trace field, the weld field), which primes are stages, which polynomial cuts
> the characters, which monodromy instantiates the core, which axes the operations
> flip.** The frame's identification and the rename-reading were both one level too
> coarse: the honest statement is TEMPLATE = FIELD, SELECTION = OBJECT.

This is the construction-level form of the banked door-structure (B997/B1019,
the registered frame's N5 row: seed-selection = the field's door): **the field builds
the doors; the object does the walking.** FL-5's two readings of B957 are RECONCILED
by the same split — B957 was right that the construction runs on Galois data of a
field (the template), and the frame is right that this is a positive statement (the
templates are exactly the field's contribution).

## The typed corollary (feeds Lane III and the L160 cell)

**The observer's group-content is ℤ/2-machinery throughout — the observer cannot
carry the trit.** The ℤ/3 (μ₆'s cube root, the VEV-acceptance label, the trit's
orbit) lives on the coupling/value side, never in the observer stack. Consequences:
(i) any crossing that needs the trit needs the COUPLING, not a finer observer —
Lane III's nomination should not look to observer refinement for ℤ/3 structure;
(ii) for L160: the operation cube is confirmed pure-(ℤ/2)³ from the observer side,
so the open θ ↔ √−7 leg is a question about GALOIS legs, not about hidden unit
structure — the cell's comparability check (B782's basis vs B766's) carries no
ω-hazard.

## L158's discrimination (recorded, not over-claimed)

L158 asked: who owns the seam space V? The ledger's answer at construction level: the
field owns V's STRUCTURE (S1's V₄, S5's growth law, S11's 𝔽₂-linearity — all
templates, all standing deletion); the object owns V's INSTANTIATION (which legs,
which closings). FL-1's collision (S069 "I am a vector space…" vs B766/B993's
field-level) dissolves the same way as FL-5: both texts are right about different
components. A one-line TERMINOLOGY note is warranted at next batch; the lead closes
to that note unless the θ-leg cell (L160) reopens it.

## Scope and honesty

- The adjudications are READINGS of banked text, each with its quoted sentence; no new
  mathematics was computed. A step whose consumption could not be decided from the
  banked text alone would have been marked UNDECIDABLE — none needed the mark, and
  that itself is a statement about how explicitly the stack's arcs recorded their
  inputs.
- The retracted S8 clauses are excluded, not adjudicated; the record of their
  retraction (B942/B957) is part of why the surviving stack is ℤ/2-clean.
- "Not-searched" fences: the inventory is the SEALED five-arc stack (+B723 via B735's
  denotation, recorded in the ledger's inventory note). The object-level observer
  (m004's own Bianchi KMS/Galois + cusp) is UNBUILT (B735's sentence) and NOT covered
  by this ledger — if it is ever built, it enters the battery as a new arc, and it is
  the one place an ω-consumption could still appear.

## Verdict

**PROVED** (the ledger: 12 rows adjudicated, 0 ω-essential, the template/selection
split total). The frame's identification is scoped — not killed, not confirmed
wholesale: **observer-as-template ≡ field; observer-as-instance = the object's walk
through the field's doors.**
