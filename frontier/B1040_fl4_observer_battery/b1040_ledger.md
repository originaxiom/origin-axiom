# B1040 — THE PER-STEP LEDGER (the sealed cells V1/V2/V3, adjudicated from banked text)

Method: each step of the sealed inventory (B700 · B733 · B735→B723 · B766 · B782) is
adjudicated from its arc's FINDINGS as written, with the load-bearing sentence quoted.
V1 = what the step consumes (ω's ℤ/3 / the quadratic ℤ/2 / neither). V2 = the sister
transplant (ℚ(√−1), ℚ(√−2), ℚ(√−7) — the last typed separately: it is also the E₆
stage leg, B704). V3 = object-deletion (STANDS / COLLAPSES, per component).

Inventory note (recorded, within the seal's text): B735's FINDINGS constructs no
observer; its observer-content is the caught-overclaim sentence typing **B723** as
"a FIELD-level construction (shared by m003/m004/…)" with "the OBJECT-level observer
… UNBUILT". The prereg's phrase "B735's field-level construction" therefore denotes
B723's construction, entering via B735's citation; B735's sentence itself is a banked
V2 answer for those steps.

| # | step (arc, evidence) | V1 | V2 | V3 |
|---|---|---|---|---|
| S1 | **V₄ = Gal(ℚ(√−3,√5)/ℚ), being·hearing = meeting as the group law** (B700 c2: "verified: the biquadratic field is Galois…"; "verified as the group law, not asserted") | ℤ/2×ℤ/2; **no ω** | template: any two quadratic fields; **the PAIR (being = the trace field, hearing = the weld field) is object-selected** | template STANDS; pair-selection COLLAPSES |
| S2 | **the stage-uniform torsor theorem, ALL prime stages, PRINCIPLED** (B700 c4/c5) | per-stage Gal(ℚ(√p*)/ℚ) = ℤ/2; **no ω** | VERBATIM — the theorem is per-prime general | theorem STANDS; the STAGE-SET (which primes are stages) is object-derived — indexing COLLAPSES |
| S3 | **the two geometric characters, y² − 3y + 3, Galois simply-transitive** (B713/B701: "no ℚ-rational point… simply transitively") | the acting group is the conjugation ℤ/2; the roots live via √−3; **no ℤ/3 acts** | does not transplant: the polynomial is m004's character-variety data | **COLLAPSES whole** — the arc's own thesis ("the object provides the AMBIGUITY") |
| S4 | **the congruence-tower observer bit-count on O₃ = ℤ[ω]** (B733: "exactly one nontrivial arithmetic automorphism (Aut(O₃) = Gal(ℚ(√−3)/ℚ) = ℤ/2)… ≤ 1 𝔽₂ bit at every congruence depth") | the ring CONTAINS ω; the extracted observer structure is **Gal = ℤ/2 per depth — the ℤ/3 of μ₆ is never an observer bit** | tower template VERBATIM (any O_K, Aut = ℤ/2); the door VALUES ((2)=1, (3)=1, …) are field-specific numbers | tower STANDS field-side; **the field-SELECTION (ℚ(√−3) = the object's trace field) COLLAPSES** |
| S5 | **the seam extension k = 2 forced → 3** (B733: "up to k=3 (with the stage prime √−7, B704)") | (ℤ/2)^k; **no ω** | the compositum construction transplants; **√−7 is typed separately: for ℚ(√−7) the "sister" is already a leg of the system** | growth law STANDS; the PRIME-LIST is object-derived — COLLAPSES |
| S6 | **the tracial core L^∞(T²) ⋊_σ ℤ = II₁** (B723 probe 1; σ = the cat-map monodromy from B716) | operator-algebraic; **no ω** | **banked answer (B735): "shared by m003/m004/…"** — the template is any once-punctured-torus bundle; σ's INSTANCE is m004's | template STANDS (B735's sentence); the monodromy INSTANCE COLLAPSES |
| S7 | **the external weight ω_λ → type III_λ** (B723 probe 2: "carried ENTIRELY by the external free weight") | **neither** — generic Powers/Araki–Woods | VERBATIM (no field enters) | **STANDS** — external by construction (B701's freedom) |
| S8 | B723's two Galois-label clauses | — | — | **EXCLUDED: RETRACTED** (B942; B957 "one level too high") — retracted content cannot be consumed |
| S9 | B735's typing sentence | — | — | meta-evidence; consumed as S6/S7's V2 row |
| S10 | **the operation cube {c, θ, γ₅}, γ₃ ≡ c** (B766: "the being-Galois IS conjugation… it drops as a generator") | three commuting ℤ/2's; **no ω** | the OPERATIONS transplant (any field's conjugation; any real-quadratic's Galois for the γ₅-analog); **the AXES (chirality's side = S3's roots; the chord √3; time's direction) are object/coupling data** | operations STAND; axes COLLAPSE |
| S11 | **the free transitive (ℤ/2)³ on the 8 closings, no equivariant section** (B782: "no non-identity element… fixed point") | 𝔽₂-linear; **no ω** | the action-structure transplants; the 8 CLOSINGS are the object's incompletenesses (B717) | structure STANDS; closings COLLAPSE |
| S12 | **the ramified door is bit-silent** (B733: "(√−3) = 0" in the door count) | the ω-adjacent door contributes **ZERO observer bits** | — | direct in-corpus evidence for the ℤ/2-only reading |

## The counts (the prereg's output format)

- **V1: ω-essential steps = 0 of 11 decidable.** ℤ/2-machinery = 10; neither = 1 (S7);
  excluded-retracted = 1 (S8). **The observer construction never consumes μ₆'s ℤ/3.**
- **V2: the TEMPLATE of every step transplants VERBATIM to the μ₂ sisters** (with
  field-specific values where values exist); S3 alone has no template (pure object).
  √−7 typed separately throughout: it is not a clean sister — it is a leg of the seam.
- **V3: the SPLIT is total and uniform** — every step separates into a FIELD-GENERIC
  TEMPLATE that STANDS object-deletion and an OBJECT-MADE SELECTION/INSTANCE that
  COLLAPSES (S7 pure-template; S3 pure-selection; the other nine both).
