# B1040 PREREGISTRATION — FL-4: the observer construction through the field-not-object battery

*cc banking seat, 2026-08-12. Sealed before compute; hash to `docs/SEAL_LEDGER.md`,
committed and pushed to both remotes before any cell runs. Gate 5 throughout. The relay
channel was checked before this seal (latest: the Slansky delivery, verified and
integrated).*

## Provenance

The Field Ladder's FL-4 (the audit seat's Task A): the genericity/field-not-object
kill-form (B737's battery) has fired on three constructions and never on the observer
construction itself — "a computable check nobody has run." Sharpened by Task B §1's
field fact (supplied by the audit seat WITHOUT a prediction on this cell, their
discipline recorded): **the four smallest h=1 imaginary-quadratic fields do not share
μ₆** — ℚ(√−3) alone contains the cube root of unity (μ₆ ⊃ ℤ/3 = ⟨ω⟩); ℚ(√−1) has μ₄;
ℚ(√−2) and ℚ(√−7) have μ₂. So B737's identical-pass test genuinely discriminates:
a construction consuming ω's ℤ/3 CANNOT pass identically for the sisters; one consuming
only h = 1 or the quadratic Galois ℤ/2 CAN.

## The question (fixed before compute)

Is the observer construction FIELD-scoped (runs on the ambient's data alone — the
seed–field frame's observer ≡ ambient identification gains its first forced content) or
does it carry OBJECT data (the rename-reading strengthens; the identification is scoped
down)? Discriminated per-step, not by verdict-averaging.

## The construction under test (fixed inventory)

The observer stack as banked: B700's per-stage fiber-functor torsors (phase 2 = B701,
the dir-less arc) · B733's congruence-depth observer bit-count · B735's field-level
construction · B766's operation cube (c, θ, γ₅; γ₃ ≡ c banked) · B782's closings
action. Steps are enumerated from these arcs' FINDINGS as written; no new construction
is built in this cell.

## The cells

- **V1 — the ω-consumption ledger.** For each build step: consumes μ₆'s ℤ/3 (ω
  essentially — the step's statement fails with ω deleted) / consumes only the
  quadratic ℤ/2 (conjugation) / consumes neither. Output: a per-step three-way ledger.
  Decidable per step; no averaging.
- **V2 — the sister transplant (B737's identical-pass form).** Transplant the
  field-only steps to ℚ(√−1), ℚ(√−2), ℚ(√−7). Outcomes per sister per step:
  VERBATIM-PASS / ω-BREAK / OTHER-BREAK (named). **Special flag, pre-registered:**
  ℚ(√−7) is also the E₆ stage leg (B704) — a √−7 result is typed separately from the
  μ₂-sister comparison (it is not a clean "sister": the corpus already assigns it a
  role).
- **V3 — object-deletion (B737's second discriminator).** Mark every step referencing
  m004 data (κ, the trace field, LONG/REL, the 27, banked eigendata) vs abstract-field
  data only. Outcome: with the object deleted the construction STANDS (field-scoped) /
  COLLAPSES at named steps (object-carrying).

## Sealed outcome meanings (stated before any result)

- Predominantly ℤ/2-only (V1) + VERBATIM-PASS sisters (V2) + STANDS (V3) →
  **FIELD-SCOPED**: the frame's identification gains forced content; L158 is
  discriminated toward the field; FL-5's re-reading of B957 strengthens.
- Any ω-essential step (V1/V2) or COLLAPSE (V3) → **OBJECT-CARRYING at the named
  steps**: the rename-reading strengthens; the identification is scoped to the passing
  steps and says so.
- **Mixed ledgers are reported AS LEDGERS.** No aggregate verdict beyond the counts;
  the per-step table is the arc's output. "Not-searched" is never "absent"; any step
  whose consumption cannot be decided from the banked text is reported UNDECIDABLE
  with the blocking sentence quoted.

## Priors: NONE DECLARED on V2/V3

The audit seat supplied the field fact and explicitly declined to predict; B733's
≤1-𝔽₂-bit line leans ℤ/2-only, but B733's own k = 3 extension cites the √−7 stage leg
(the bridge sentence both seats initially truncated); genuinely open. This bench
declares no lean.

## MB12 / non-vacuity

Each criterion can pass AND fail per step (ω-consumption is a decidable property of a
step's statement; deletion either breaks a step or does not). Both battery outcomes are
realized historically: Candidate Zero PASSED identically for the sisters (B737's kill);
object-bound constructions fail deletion (B737's own positive control). Not vacuous.

## Hazards pre-registered

φ AND phi in every grep · sum(..., K0) for K-valued reductions · functional-read only ·
the B701 citation form (= B700 phase 2, dir-less) · **window-read-as-whole: every
quoted sentence is read to its final period, past line-wraps** (the twice-struck
species of this window) · verdict vocabulary {PROVED, NEGATIVE, OPEN, RETRACTED,
PARTIAL} only.
