# B1122 — L179 THE LIFT-BIT METER: PAIR-INVISIBLE — the spin bit is unmeasurable even on the coupled pair

**Status: banked (frontier). Verdict PROVED (a clean NEGATIVE: PAIR-INVISIBLE, exact
over ℚ(q), controls green incl. reproducing B1113's t-meter number to the digit). A
GLOBAL-OBJECT THESIS cell (#282). Gate 5 untouched. Lock `tests/test_b1122_liftbit.py`.**

## The question

B1113's t-meter: free data of ONE object (the dial) becomes FORCED data of the COUPLED
PAIR. B1112: the spin lift (H¹(M;ℤ/2)=ℤ/2, the −I choice) is "the observer's first
payment." L179 asked: does the t-meter mechanism EXTEND to the spin bit — is the
relative lift pair-measurable by seam-word traces? If yes, projective+Lorentz+Jordan
FUSE through a shared pair-meter.

## THE ANSWER: PAIR-INVISIBLE — the mechanism does NOT extend

> **"Free-of-one = forced-of-pair" is FALSE for the spin bit.** The relative lift is
> not detectable by any seam-word trace on the mirror double.

**The one fact that decides it:** the lift's entire content is the sign operator
S = φ(−I) = diag((−1)^weight), which is **CENTRAL** (it is φ of the SL(2) center). It
commutes with everything, so it produces NO twist: S·B·S⁻¹ = B. Contrast the dial,
which is a CONJUGATION B_R = D·B·D⁻¹ that centralizes the seam A but NOT the interior B
— and that asymmetry is exactly what a seam trace reads. The group commutator (the
t-meter's own separating meter) is identically blind to the central S everywhere.

**Two independent roads to invisible:**
1. The object's principal 27 is EVEN → S = I → the two lifts are the IDENTICAL
   operator; nothing distinguishes them, on one copy or the pair.
2. The relative lift is topologically forced trivial: χ is read on the seam meridian,
   the seam forces χ_L = χ_R, and the opposite-lift configuration breaks the gluing
   (S·A ≠ A), so it is not even a representation.

In the ODD reps (2-dim Riley, single-root 27) the bit is ALREADY a single-copy
observable (the meridian trace flips 2 → −2, 27 → 3), so it is not "free-of-one" at
all. Controls all pass (B1113's tr(A·B) = 141750+1011915q reproduced to the digit;
dial centralizes A not B; 3003 Chevalley brackets; both lifts valid and distinct).

## What it means (the negative that sharpens)

B1112's located bit — "the first thing the observer pays that the object cannot" — is
**REINFORCED, not dissolved.** The dial is a DEFORMATION direction ([H,X]=2X grades the
family) the coupling can read; the spin lift is a discrete CENTRAL torsor choice the
coupling CANNOT read. **This is a STRONGER no-go than B1087's** (which was
undefinability on one closed copy): the spin bit is unmeasurable on the coupled pair
TOO. And it is consistent with B1114's all-even gravity-reading 27 — the Lorentz
double is lift-blind for the same reason (S = I on even reps).

## The thesis consequence (honest)

The hoped-for FUSION (projective + Lorentz + Jordan through one shared pair-meter) does
NOT happen — the spin bit and the dial are measurably different KINDS of freedom
(central-discrete vs deformation-continuous). That is itself the finding: **the
freedom ledger's three bits are not one mechanism.** {C, P} and the dial are
coupling-readable (forced-of-pair); the spin lift is irreducibly the observer's, at a
depth no coupling reaches. Gate 5 untouched.
