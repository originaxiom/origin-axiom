# B1358 — THE E6 APEX FAMILY

## 1. HEADLINE

Verdict: **PROVED** (per `arc_verdict.json`).

claim_one_line, first sentence verbatim:

> THE E6 APEX FAMILY: the twistor cones of S^4/(2T x Gamma_R), Gamma_R in {1, Z2, Z3, Z4, Z6, Q8, 2T}, are G2 cones whose first pole carries E6 and whose second pole carries the companion {+-1}.Gamma_R (A1, A1, A5, A3, A5, D4, E6 -- always of even order, never A2), with mixed A-loci over the cone circles of S^4/Gamma whenever Gamma_R shares an eigenvalue with 2T (none, none, 4 A2 orbits, 2 A1, 4 A2, 3 A1, 1 A1 + 4 A2) and no fixed point off the loci.

## 2. CLAIMS

1. Γ = 2T × Γ_R acting diag(g,h) on ℂ⁴=ℂ²⊕ℂ² preserves the nearly-Kähler structure so the cone over CP³/Γ is a G₂ cone (twistor cone of S⁴/Γ). — **ASSERTED** (cites Acharya–Witten hep-th/0109152 §3 for the general twistor-cone/nearly-Kähler fact; not independently re-derived here, just invoked).
2. For each of the 7 groups Γ_R ∈ {1,Z2,Z3,Z4,Z6,Q8,2T}: the full census table (§1 of FINDINGS) — first-pole type/degree, second-pole (companion) type/degree, mixed loci by orbit count and type, enhanced-point counts. — **COMPUTED** by `verification/apex_family.py`, function `census(name, GR)` (eigenspace/fixed-line enumeration via `numpy.linalg.eig`, orbit computation via `img()`/line_orbits, stabiliser typing via `group_type()`); output recorded in `apex_family_run.txt`.
3. "No fixed point lies off every fixed line in any case" (isolated points = 0 for all 7 groups). — **COMPUTED** (script's `orbits_iso` accounting, explicit in `apex_family_run.txt`, e.g. "isolated fixed points ... : 0 points" printed per group).
4. b₂(CP³/Γ; ℚ) = 1 throughout ("a linear action fixes the hyperplane class"). — **ASSERTED** (stated as a general fact, not computed by the script — no b₂ computation appears in `apex_family.py`; same status as B1355's use of the fact, per FINDINGS §1's own phrasing "as in B1355").
5. The companion {±1}·Γ_R has even order always (contains −1 since −1∈2T), hence companion type ∈ {A_odd, D₄, E₆}, **never A₂**. — **COMPUTED**-supported ASSERTED: proved by direct group-theory argument in §2 of FINDINGS (elementary, not delegated to the script beyond confirming the resulting types numerically per group).
6. The mixed loci (over shared-eigenvalue lines) are ADE-typed via a G₂-holonomy argument: stabiliser of an associative 3-plane in G₂ acts on its ℝ⁴ normal as an SU(2) subgroup, so the abstract stabiliser group types the locus (cyclic n → A_{n−1}, Q₈ → D₄, 2T → E₆). — **ASSERTED** (a geometric/Lie-theoretic argument in FINDINGS §2, not a script computation; the script computes only the abstract stabiliser group via matrix conjugation/`group_type`, the G₂-normal-bundle identification is argued in prose).
7. Companion table read against B1357's background (which of the census's companions match a locus B1357 already has: SU(2)/Y₃ for Γ_R=1; SO(8)/Y₃ for Q8 (needs 3 extra branches); E₆/Y₃ self-touch for 2T (needs branches); A₅ for Z3/Z6 apex-on-knot has **no match**; A₃ for Z4 has no match). — **COMPUTED** (matching logic is in the script's final summary loop against a hard-coded `background` dict) but **CITED** for the background data itself (B1357's loci are an input, not recomputed here).
8. Inflow ratio at the clean apex (Γ_R=1): A(U(1)·E₆²) : A(U(1)·SU(2)²) = 12 : 1 exactly, from degree(E₆ line)=1, degree(A₁ line)=12. — **COMPUTED** (degrees come from the census; the ratio-of-degrees-equals-ratio-of-inflow-coefficients principle is **CITED** from Witten hep-th/0108165 §3 / B1355 §3).
9. With one 27 of charge q (T(27)=3, T(2)=½ Dynkin normalization) the SU(2)-charged matter satisfies Σq_iT(R_i)=q/4, i.e. Σq_i=q/2 over doublets. — **CITED** (standard Dynkin indices, per FINDINGS caveat 3) applied to the computed ratio.
10. Doublet-count parity (even/odd per apex) as the residual Witten-SU(2)-global-anomaly consistency condition with 3 apexes — left **undecided**, registered as L213(v). — **ASSERTED as open**, explicitly not computed ("the doublet number itself is not derived").
11. Scope restriction: only twistor-family cones Γ⊂Sp(1)×Sp(1) of product form are censused; Γ⊂Sp(2) not of product form is outside. — **ASSERTED** (methodological caveat 1).
12. Numerical method: floating point, tolerance 1e-7, groups of order ≤576 (2T×2T=576 is the largest case run). — **COMPUTED**/stated as the method's own precision bound (caveat 2).
13. "0 of 19" identification count in `arc_verdict.json`. — **ASSERTED**/boilerplate (repo-wide identification ledger metric, not produced by this arc's own script; not independently checked here).

## 3. LOAD-BEARING

The headline rests on:
1. Claim 2 — the full 7-group census (companion types/degrees/mixed loci), the arc's central computed object.
2. Claim 5 — companion-always-even-order lemma (rules out A₂ companion in principle, independent of any specific group).
3. Claim 7 — the match/no-match table against B1357's actual background (turns the abstract census into the physics conclusion: only Γ_R=1 fits cleanly).
4. Claim 8 — the exact 12:1 inflow ratio at the surviving clean-apex model.

## 4. ON MAIN ALREADY?

- (b) **Not on main.** `grep -rn "B1358" frontier/ docs/` on main returns nothing; `grep` for "twistor cone", "apex family" over main's `frontier/`+`docs/` (excluding the wt-sm worktree) also returns nothing. This arc has no footprint on main.
- Partial precedent: B1358's Γ_R=1 case (the "clean apex," B1355's own cone) **was already independently harvested and re-derived on main** — `frontier/B1411_the_sm_seats_v10_arcs_harvested/FINDINGS.md` §5 ("B1355 — the E₇ point made explicit. VERIFIED (geometry) / CITED (physics)"), with its own script `frontier/B1411_the_sm_seats_v10_arcs_harvested/verification/main_b1355_geometry.py` confirming 2T order 24, the E₆ locus = P(ℂ²_std), the A₁ locus from −1, and b₂(CP³/2T)=1 — exact over ℚ(i). So the **base case only** of B1358's census (Γ_R=1, and its E₆/A₁ pole types) has independent confirmation on main; the extension to the other 6 groups, the mixed-loci census, and the companion-vs-background matching table are new material not yet on main.
- **Numbering note (not a math contradiction, but worth flagging):** the seat branch's `L213` ("THE DECK'S U(1)² AND THE APEX ON THE KNOT", registered 2026-09-15 by B1356, extended by B1358/B1359) is a **different lead than main's own L213** ("WHICH BANKED RESULTS ASSUME CANONICITY, AND WHICH ONLY USE SnapPy'S TRIANGULATION?", registered 2026-09-14 by B1404 — `docs/OPEN_LEADS.md:2579` on main). Same label, unrelated content, one day apart. This is a lead-numbering collision that a harvester merging this branch into main will need to renumber (the seat's L213 sub-items (i)–(vii) would need a new lead id on main).

## 5. NEEDS COMPUTATION HERE

1. **Re-derive the Γ_R=1 sub-census independently** (already partly done via B1355 in B1411, but not the specific degree-12/degree-1 pole-degree numbers used for the 12:1 inflow ratio): build 2T (order 24, e.g. as unit quaternions or as SL(2,3) matrices), act diag(g,1) on ℂ²⊕ℂ² (Γ_R=1 trivial), find the CP³ fixed-line locus of every non-scalar element, compute pointwise stabilisers and orbit sizes. Expected: exactly 2 fixed lines (E₆ at degree 1, A₁ at degree 12), 14 enhanced points (8 with Z₆ stabiliser, 6 with Z₄), 0 isolated points. **Prefer exact quaternion/rational arithmetic** (as B1359's script does with `fractions.Fraction`) rather than the arc's own floating-point numpy at 1e-7 tolerance, to remove any doubt about the tolerance choice.
2. **Re-derive the Γ_R=Z3 (apex-on-knot) sub-census**, the one load-bearing negative result (claim 7/L213(ii)): 2T×Z3 (order 72, effective), companion should come out A₅ (from {±1}·Z3=Z6, order 6) at degree 12, first pole E₆ at degree 3, and exactly 4 orbits of 4 mixed lines each typed A₂ (16 mixed lines total), 16 enhanced points. Recipe as above with Γ_R=Z3 instead of trivial.
3. **Independently verify the companion-even-order lemma** as a clean, group-theory-only fact (no linear algebra needed): for any finite Γ_R⊂Sp(1)=SU(2)-unit-quaternions, show {±1}·Γ_R contains −1 and has order |Γ_R| or 2|Γ_R| (even in both cases since |Γ_R| is itself always even for the groups considered, or trivially 2 for Γ_R=1) — a one-paragraph proof-check, not a program, but should be re-derived by the verifier rather than accepted.
4. **Spot-check one "mixed loci" ADE-typing claim on-bench** (Q8 or Z4 case): compute the stabiliser of one shared-eigenvalue line as an abstract matrix group (via `group_type`) and confirm it is literally the cyclic/Q8 group predicted, not just its order — the FINDINGS' G₂-normal-bundle argument (§2) that "the twistor structure is the other one on that ℝ⁴" is asserted, not computed; recomputing the normal representation's determinant pattern (`dets == [0.0]` vs not) for at least the Z3-mixed case (where the arc itself flags "determinant = 1: False") is the discriminating check that the ADE identification, not just the group order, is right.

## 6. FENCES

Verbatim from FINDINGS.md:

- "The family is Acharya–Witten's twistor family with Γ ⊂ Sp(1) × Sp(1); Γ ⊂ Sp(2) not of product form, and cones that are not twistor cones, are outside this census. The negative for the apex on the knot is within the family."
- "The census is floating-point (tolerance 10⁻⁷) on groups of order ≤ 576, every reported number is an integer count of orbits, stabilisers and degrees. The ADE typing of the mixed loci rests on the G₂ argument of §2."
- "The inflow ratio is normalisation-free; the translation to doublet counts uses the standard Dynkin indices and the cited 'one 27' (B1355 caveat 2); the doublet number itself is not derived."
- "The touching design remains a design (B1357 §5.4)."

## 7. GRADE PROPOSAL

**VERIFY-BY-COMPUTATION** — the census (claims 2, 5, 7, 8) is a bounded, exact-in-principle finite group/linear-algebra computation (quaternion group closures up to order 576, eigenspace enumeration) that a verifier can and should independently re-run, ideally with exact rational/quaternion arithmetic instead of the arc's floating-point tolerance, to remove any doubt about the 1e-7 numerics and to confirm the single most consequential negative (Γ_R=Z3's A₅ companion, no match in B1357's background).
