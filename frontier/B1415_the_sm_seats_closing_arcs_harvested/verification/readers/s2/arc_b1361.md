# B1361 — THE DECK'S TEXTURE IS B1273'S

## 1. HEADLINE

Verdict: **NEGATIVE** (per arc_verdict.json; FINDINGS header also lists a PROVED sub-component for the selection rule/identity).

claim_one_line, first sentence verbatim:
> "THE DECK'S TEXTURE IS B1273'S: under the two C-field U(1)s born with the apexes (charges (1,-2), (1,1), (-2,1), an equilateral triangle), the only invariant E6 cubic among the three 27s is 27_1 27_2 27_3, so with the Higgs in the apex 27s every tree-level charged-fermion mass matrix is complex symmetric with zero diagonal -- B1273's texture from the flat classes, by a second route -- and with a neutral Higgs there is no tree-level Yukawa at all."

## 2. CLAIMS

1. Apex charge vectors (1,−2), (1,1), (−2,1) form an equilateral triangle in the U(1)² charge lattice; no two are opposite; sum to zero. — **CITED** from B1356 §2 (a different arc, not re-derived in this arc's own script beyond restating the numbers) + **COMPUTED** trivially (pairwise sums printed by `decks_texture.py`).
2. Among the 10 symmetric cubic couplings λ_{ijk} (i≤j≤k) of three 27s, only 27₁27₂27₃ is invariant under both U(1)s. — **COMPUTED**, `decks_texture.py` §1, brute-force enumeration over `itertools.combinations_with_replacement`.
3. With a neutral Higgs (charge 0), no 27_i27_jH coupling is U(1)-invariant → no tree-level Yukawa at all. — **COMPUTED**, same enumeration, `neutral == []` confirmed in run output.
4. The deck (Z₃ cyclic permutation) alone, without the U(1)s, would allow 4 invariant cubics (Σxᵢ³, two cyclic mixed sums, x₁x₂x₃). — **COMPUTED** symbolically via sympy orbit-sum construction.
5. With Higgs in the apex 27s, the resulting tree-level mass matrix is complex symmetric with zero diagonal (M_{jk} = λ|ε_{ijk}|⟨H_i⟩). — **ASSERTED**, a direct but unverified-by-script restatement of claim 2's consequence (the specific matrix form is not built and checked in the script; only the coupling-selection combinatorics is computed).
6. **The identity**: for any hollow (zero-diagonal) complex symmetric 3×3 matrix M, σ₁ = σ₂ + σ₃ exactly (not merely ≤, as B1273 had it). — **COMPUTED**, symbolically: `e1 = tr(MM†) = 2S` and `e2(MM†) − S² = 0` both shown to vanish identically by `sympy.simplify`, hence e₁²−4e₂ ≡ 0 identically in a,b,c. I independently re-derived this numerically outside the arc's own script (5 random complex triples, `e1**2 - 4*e2` ≈ 0 to float precision) — **confirmed independently**, this is a solid symbolic identity.
7. Numerical confirmation: 10⁵ random hollow symmetric complex 3×3 matrices give |σ₁−σ₂−σ₃|/σ₁ ≤ ~1e-15 and min σ₂/σ₁ ≈ 0.5091 (bound ½). — **COMPUTED**, `decks_texture.py` §2 Monte Carlo loop.
8. Data: m₃/(m₂+m₁) at low scale = 273 (up), 50 (down), 17 (charged leptons); the hollow texture demands 1 in every sector. — **COMPUTED** from the hard-coded mass table in the script (`data = {...}` — these numbers themselves are **CITED** inputs, i.e. standard low-scale running quark/lepton masses, not independently sourced/verified in this script; the *ratio arithmetic* on top of them is computed).
9. At B1273's (GUT?) scale the ratios are 136, 43, 17 — **CITED** from B1273 §4, not recomputed here.
10. The "diagonal source" that could lift the hollow texture is, in the design, "the sector that breaks the apex U(1)s" — Witten's axionic mass / charged M2-instantons. — **ASSERTED** (a physical identification/hypothesis), registered as open work (L213 (vii)), explicitly **not computed**.
11. Currency notes at the bottom (added 2026-09-15) forward-reference B1362 (Weyl-inequality sharpening) and B1365 (neutrino face) — **CITED**, pointers to sibling/later arcs not evaluated here.

## 3. LOAD-BEARING

The headline (NEGATIVE: B1273's texture is refuted by the data) rests on:
- Claim 2 (only 27₁27₂27₃ survives the U(1)² selection): COMPUTED, solid, simple enumeration — easy to independently re-check.
- Claim 5 (Higgs placement ⇒ hollow complex-symmetric matrix form): ASSERTED, a modeling step not itself computed/verified by code.
- Claim 6 (σ₁ = σ₂+σ₃ identity, sharpening B1273's inequality to equality): COMPUTED and independently spot-confirmed by me — the strongest, most load-bearing result in the arc.
- Claim 8 (data ratios 273/50/17 all ≫ 1): COMPUTED arithmetic on CITED mass inputs — this is what actually delivers the "refuted by the data" verdict.

## 4. ON MAIN ALREADY?

- B1361 itself: **not on main** (no hits searching `docs/HARVEST_LEDGER.md`, `docs/CAMPAIGN_STATUS.md`, `docs/SEAT_REGISTER.md`, `docs/SM_SPECIFICATION_LEDGER.md`, `docs/views/THE_SPINE.md`, or `frontier/` for "B1361").
- B1356 (source of the charge vectors) and B1357/B1358/B1359: **not on main** — same negative grep result as B1360's report.
- **B1273 numbering collision (contradicted/confusing, not contradicted-in-content):** the seat's own B1273 is `audit/wt-sm/frontier/B1273_the_three_fold_closing` ("THE OBJECT'S OWN THREE-FOLD CLOSING… the texture they force is refuted by m_t ≤ m_c + m_u", dated 2026-09-06). Main's `frontier/B1273_seat_harvest` ("THE SEAT HARVEST: three independent routes separate the same binary…", also dated 2026-09-06) is a **completely different arc that happens to share the number B1273** — a genuine numbering collision between the seat branch and main, of the kind flagged repeatedly in the project's own memory/ledgers. A reader grepping main for "B1273" to check this arc's dependency will land on the wrong document. This is a documentary hazard, not a mathematical contradiction — the mathematical content of "B1273" as cited by B1361 (the flat-classes texture with bound σ₁≤σ₂+σ₃, refuted by data) is real and lives only on the seat branch at `audit/wt-sm/frontier/B1273_the_three_fold_closing/FINDINGS.md`.
- **B1276 numbering collision:** the seat's `B1276_the_relations_the_chain_forces` (cited here as "the one cubic") collides with main's unrelated `frontier/B1276_legs_are_faces`.
- Net: none of this arc's load-bearing content is on main; it is un-harvested seat material, and two of its cited dependency numbers (B1273, B1276) are ambiguous against main's own numbering.

## 5. NEEDS COMPUTATION HERE

1. **The selection-rule enumeration (claim 2/3).** Trivial to re-run independently on main without touching the seat's script: given the three charge vectors (1,−2),(1,1),(−2,1), brute-force all 10 symmetric triples and all 3 pair-sums against zero. Expected: exactly one triple survives (1,2,3); zero pairs survive for a neutral Higgs. (Seconds; pure combinatorics, no dependency on geometry.)
2. **The σ₁=σ₂+σ₃ identity (claim 6), the single most load-bearing computed fact.** Recipe: symbolically expand tr(MM†) and the sum of 2×2 principal minors of MM† for M = hollow complex-symmetric 3×3 with entries a,b,c ∈ ℂ, verify e₂(MM†) − (tr(MM†)/2)² ≡ 0 as a polynomial identity in a,ā,b,b̄,c,c̄ (a quick sympy/Mathematica-equivalent expansion, or a handful of random complex numerical instances as I did: `e1**2 - 4*e2` should vanish to floating precision). Expected: identical zero. I already did a 5-sample independent spot-check here and it holds — a verifier on main should still do the full symbolic expansion for a proof-grade certificate rather than relying on my numerical spot-check or the arc's own script.
3. **The mass-ratio arithmetic (claim 8) against a second, independently-sourced mass table.** Recipe: pull low-scale running quark/lepton masses from a standard reference (e.g. PDG pole/MS-bar values or a independent RGE table already used elsewhere on main, e.g. in B1273-seat-harvest-on-main or other SM-mass arcs) rather than the arc's own hard-coded `data` dict, and recompute m₃/(m₂+m₁) for up, down, lepton sectors. Expected: all three ratios ≫ 1 (order 10–300), robust to the choice of scale/table (the arc itself notes GUT-scale values 136/43/17 vs low-scale 273/50/17 — same qualitative conclusion).

## 6. FENCES

Verbatim, from §4 and Caveats:
- "The data are low-scale running masses here and GUT-scale in B1273; the verdict is scale-independent." (caveat 3)
- "A Higgs placement… covers the natural cases; a Higgs carrying a charge that pairs with one generation… would allow one diagonal-free coupling per Higgs — still hollow." (caveat 1)
- "The charges are B1356's coset representative; any triple in (1,1,−2) + 3A₂ has the same equilateral property… so the selection rule is representative-independent." (caveat 2)
- On the open item: "It is not computed here; it is where the design's flavour must come from (L213 (vii))." — explicitly fencing off the Froggatt–Nielsen/instanton mechanism as unconstructed.

## 7. GRADE PROPOSAL

**VERIFY-BY-COMPUTATION** for the selection rule and the σ₁=σ₂+σ₃ identity (cheap, decisive, and I already independently spot-confirmed the identity) — these should be promoted to fully verified on main with a from-scratch symbolic re-derivation. The overall arc verdict (NEGATIVE) is well-supported once those two facts are locked in; flag the **B1273/B1276 numbering collisions** as a documentary DISPUTED-adjacent item for the harvest process (not a mathematical dispute) so nobody merges this arc's dependency pointers into main's differently-numbered B1273/B1276.
