# PREREG — RUNG-1 WIDENED: the real-subfield sweep + the forced-limits census (cc2, 2026-07-21)

*Seat cc2, read-only, advisory. Owner forwarded chat-1's proposal (widen rung-1 beyond ℚ(√5) to the
V₄/cyclotomic fields). ADJUDICATION: the door is Register-sanctioned (my audit flagged B706 rung-1's
narrow scope), ACCEPTED with a forced correction — SM ratios are REAL; the V₄ field's real elements
are exactly ℚ(√5) (CM field, conjugation fixes √5, flips √−3/√−15), so chat-1's bases 2/3/4 are
vacuous-or-collapsed for real targets. The genuinely NEW territory = the REAL subfields of the
programme's own tower, never tested. SEALED before compute. Gate 5: this is rung-1 of the Listening
Protocol — a base-rate-gated algebraicity TEST; a null is banked as a clean negative; a hit is a
CANDIDATE for the protocol's higher rungs, never a claim. Model economy: 2 sonnet agents (PDG table;
census) + main-loop PSLQ compute + 1 sonnet verifier.*

## The bases (pre-registered; real targets vs real fields)
- B1 ℚ(√5) {1,√5} — hearing; CONTROL (reproduce banked no-hits)
- B2 ℚ(√3) {1,√3} — the real shadow of the being/cusp ζ₁₂-conductor (never tested)
- B3 ℚ(√15) {1,√15} — the meeting's real resolvent (never tested)
- B4 ℚ(ζ₁₅)⁺ deg 4 {1, c, c², c³}, c=2cos(2π/15) — the meeting-level real cyclotomic (never tested)
- B5 ℚ(ζ₂₀)⁺ = ℚ(√(2+φ)) deg 4 — THE BORN AMPLITUDE FIELD (never tested)
- B6 ℚ(√φ) deg 4 (x⁴−x²−1) — the F-symbol/associator field (never tested)
- B7 (complex, one target only): ζ₁₅-combinations vs e^{iδ_CP} — low weight (poor digits)

## The targets (pre-registered class; exact list from the PDG cell)
All SM dimensionless ratios with a stated digits budget: lepton mass ratios (me/mμ, me/mτ, mμ/mτ),
α(0)⁻¹, α_s(M_Z), sin²θ_W (on-shell + MS-bar), m_W/m_Z, quark mass ratios (low digits, flagged),
CKM |V_us| etc., Koide Q, m_p/m_e (famous control), θ_C. Each with 2024-25 PDG central value +
uncertainty, web-verified by the PDG cell. DIGITS RULE: PSLQ runs at the measured precision only;
a candidate relation must define an exact algebraic number lying INSIDE the experimental interval.

## The gates (pre-registered; a "hit" must pass ALL)
1. Relation found at the value's own digit budget, coefficient height ≤ 64 (deg-2 bases) / ≤ 16
   (deg-4 bases) — small heights only; big-height relations are automatic noise.
2. The exact algebraic number it defines lies within ±1σ of the measured value.
3. EMPIRICAL NULL: the same search over ≥50 surrogate random values (same magnitude, same digit
   budget) — the per-basis surrogate hit-rate must make the real hit's probability < 0.02.
4. Survives the sonnet adversarial verifier (independent re-run, higher precision, ±1σ endpoints).
NULL RESULT = a genuine deliverable: rung-1 CLOSED over the full real tower (the honest widening
the Register demanded), killing the limit-hypothesis's forced-algebraic form at full scope.

## The forced-limits census (companion cell, the other half of the limit-adjudication)
Enumerate from the repo every FORCED iterative limit the object owns (no chosen processes): Perron
data (φ², 2logφ), letter density 1/φ, the tones {0, 1/(2φ), 1/2, φ/2, 1}, the weld cycle, Thurston
volume limits, Neumann–Zagier asymptotics, entropy log φ². Cross-list against the same targets with
the same gates. Expected: all algebraic-in-the-tower → covered by the sweep; the census makes the
"values = forced limits" hypothesis CLOSED at exact scope either way.

## FIREWALL
Gate 5. Rung-1 protocol: a hit is a rung-1 CANDIDATE (base-rate-passed algebraicity), promoted only
through the Listening Protocol's higher rungs (mechanism, forcing) — NEVER banked as "derived."
A null closes the door at honest full scope. JUNO remains the one registered forward test.
Hashes in SEALS.txt.

## ADDENDUM (sealed before compute, 2026-07-21): the ANALYTIC + DEG-8 doors (chat-1 round 2)
PRE-NAMED analytic list (NOTHING else; free mixing DECLINED as look-elsewhere): N1=L(E15a8,2);
N2=L'(E15a8,0); N3=the banked Mahler measure m(A_41) (B683/L3 value); N4=Ω_re(15a8);
N5=L(E,2)/(π·Ω_re) (Beilinson-normalized); N6=L'(E,0)/π. Compared vs all 18 PDG targets by direct
±1σ interval test, WITH the summed chance-expectation reported (Σ 2·rel_unc per pair); only
pairs with per-pair chance <2% count. DEG-8 door: the CM-collapse leaves ONE genuine new real
basis — Q(zeta_60)+ (deg 8, the full-tower real compositum); PSLQ only on targets with >=10
digits, fresh 50-surrogate null for that basis/class. Curve verified in-seat (conductor 15,
j=-1/15) before use.
