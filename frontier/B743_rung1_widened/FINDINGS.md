# FINDINGS — B743: the RUNG-1 WIDENED sweep + the ANALYTIC door (cc2's package, cc-verified and banked)

cc banking seat, 2026-07-21. Number reserved pre-use (collision protocol). cc2's two-part package
(seat-work/rung1_widened, artifacts copied into this arc; seals verified on receipt: report
453f8f12, PDG e93efeaa, forced-limits 93edd0fd, nulls 0f866b57 — 4/4 exact; the prereg's current
hash 19139443 = the in-place addendum seal, consistent with cc2's note). Gate 5: a hit would have
been a rung-1 CANDIDATE only; the NULL is what banks. Nothing to CLAIMS.

## Part 1 — the CM-collapse correction (theorem-level) + the real-tower sweep: 0 GATED HITS
- **The correction that reshaped the courier seat's proposal:** SM ratios are REAL; ℚ(√−3,√5) is a
  CM field whose real elements are EXACTLY ℚ(√5) [cc-verified: basis {1,√5,√−3,√−15}, real span =
  ℚ(√5)] — so "widen to the V₄/cyclotomic fields" collapses; the GENUINE new territory is the real
  tower: ℚ(√3), ℚ(√15), ℚ(ζ₁₅)⁺, ℚ(ζ₂₀)⁺=ℚ(√(2+φ)) (the Born-amplitude field), ℚ(√φ), + ℚ(√5) rerun.
- **Empirically calibrated discipline:** 18 web-verified PDG targets (Koide Q = 0.6666645(76e−6),
  0.43σ from 2/3 — cc-reproduced exactly from PDG masses); 1800 surrogate PSLQ runs establish the
  noise floor (54–84% random "hits" at 4–6 digits ⇒ all low-digit targets formally UNTESTABLE —
  named, not papered over; 0/50 at 10–12 digits = clean territory).
- **RESULT: 0 gated hits.** The clean nulls where they matter: α⁻¹ (~10 digits), m_p/m_e (~10),
  m_e/m_μ — NO relation in ANY of the six bases in the zero-noise regime. **The instrument caught
  Koide:** PSLQ re-found Q=2/3 in all six bases identically ⇒ the relation is RATIONAL (zero
  object-content), and the gates correctly refused to promote it — the sweep independently found
  the one famous coincidence in the data and rejected it per prereg.
- The four transcendental forced limits (entropy 2logφ, Vol, π/√3, systole) match nothing.

## Part 2 — the ANALYTIC + DEG-8 doors (addendum, same day): CLOSED
- The curve verified [cc-reproduced: [1,1,1,0,0] ⇒ c₄=1, Δ=−15, **j=−1/15 exact** = 15a8/B698].
- The pre-named analytic list (L(E,2), L′(E,0), L(E,1), Ω_re, Mahler both normalizations, the
  Beilinson combos) vs all 18 targets at ±1σ: **ZERO hits** (crude chance-expectation over 126
  pairs was ~6; got 0). The narrative-trap flagged: L(E,2)=0.6615 vs Koide 0.66666 is **683σ apart**
  [cc-recomputed] — NOT a near-hit.
- Deg-8: the CM-collapse kills it for real targets; the one genuine real deg-8 base ℚ(ζ₆₀)⁺ is
  NOISE-FLOOR-BLOCKED (58% random hits) — formally untestable until ~15-digit measurements.
- The "mixing door" DECLINED as posed (combinatorial look-elsewhere); only canonical combos ran.

## What this closes
**The courier seat's limit-hypothesis in its FORCED form is closed by computation**: the object's
entire real algebraic tower (including the never-tested Born-amplitude and F-symbol quartics) and
its canonical analytic values contain NO SM dimensionless ratio at every testable precision, with
the untestable targets NAMED (digit-budget honesty). The unforced form remains the B724
look-elsewhere trap. JUNO stays the program's ONE registered forward test. Two-seat: cc2 computed;
cc verified-on-receipt (4 spot-checks reproduced exactly: CM-collapse, Koide Q, j=−1/15, the 683σ).
