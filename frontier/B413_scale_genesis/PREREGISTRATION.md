# B413 (SCALE-GENESIS, owner-approved) — PRE-REGISTRATION

**Committed before computation. THE decisive precondition for any SM-derivation ambition:
does the tower-measure (B412) carry SCALE content, or does it reduce to the already-banked
regulator/L-value (entropy = 4·Reg(ℚ(√5)) = 2√5·L(1,χ₅))? Binding two-outcome gate.
Firewalled — "scale" names the wall; no physics claim without the full escalation.**

## The object (from B412, exact)

The single-seed tower is a mass-conserving refining measure μ on lim(ℤ/3ᵏ × ℤ/5): total
mass 1; each parent's 1/4 splits into a ζ₉⁺-orbit summing to it; innovations trace-zero.
Its finite Fourier / Mellin transform L(χ, μ) = Σ_a χ(a)·μ(a) over the tower characters is
the tower-measure's "L-function"; the trivial character gives the mass (1).

## G1 — the Mellin/Fourier spectrum (this session)

Compute L(χ, μ) EXACTLY for the nontrivial ℤ/3ᵏ-characters at levels 405, 1215 (banked
values). REGISTERED THREE-OUTCOME classification:
- **(a) TRIVIAL:** all nontrivial L(χ,μ) = 0 ⇒ μ is Haar on its support (no structure, no
  scale) ⇒ the scale wall is closed at the measure level too; bank the terminal negative.
- **(b) REDUCIBLE:** the nontrivial values are rational multiples of powers of √5 / the
  regulator / L(1,χ₅) ⇒ the measure repackages the banked golden L-value, no NEW scale;
  bank the reduction (still a clean structural result, no scale genesis).
- **(c) NEW:** a nontrivial L(χ,μ) that is NOT reducible to {√5, ζ-values, the regulator}
  ⇒ the measure carries genuinely new arithmetic ⇒ Scale-Genesis PASSES G1; proceed to
  G2 (does any dimensionless RATIO of these L-values have scale-setting shape) then the
  null gauntlet.

## G2 (conditional on (c)) — the scale-ratio + gauntlet

If (c): form the dimensionless ratios of the L-spectrum; pre-register a null gauntlet
(shuffled-measure control, wrong-tower control, Haar control) BEFORE interpreting; any
survivor goes to a DEDICATED adversarial session (owner present) per the S042/S043 bars —
NEVER voiced before the gauntlet. If (a)/(b): Scale-Genesis returns the honest terminal
statement — the object is a self-computing arithmetic measure with NO scale genesis — and
Paper 1 carries it.

## Machinery
Exact ℚ(ζ) arithmetic on the banked tower values (B372/B392/B394 + the 1215 triple);
the Fourier sums are small and exact. Locks from the output JSON. VACUITY GUARD: all three
G1 outcomes are live (Haar/reducible/new are genuinely distinguishable on the ζ₉⁺ orbit).
