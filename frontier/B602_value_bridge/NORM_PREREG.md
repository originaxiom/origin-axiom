# B602 — THE VALUE-BRIDGE PREREGISTRATION (the cross-domain normalization)

**Drafted and SEALED 2026-07-15, before any comparison below is computed.
SHA-256 recorded in `frontier/B598_l85_campaign/ARTIFACT_HASHES.txt` at
sealing. This is the arc that P3's outcome B opens: P2_MAP slot 8 made
testable. Provenance: all verification internal (owner + AI seats).
Firewall: no SM quantity anywhere in this arc; a positive outcome only
enables the NEXT (separately preregistered) arc.**

## The question

Outcome B established the structural correspondence at golden κ = 5 with
per-line magnitudes as GAUGE. This arc asks: does any **structurally
declared** normalization — fixed here, from banked structure, with no
tuning — make the classical two-line data and the golden-stage pair
numerically commensurate? This is the owner's "one measurement predicts
the rest" question in its first falsifiable form: if a declared
normalization closes the gauge, the value bridge exists; every subsequent
value is then a prediction, not a fit.

## The frozen inputs (all banked; nothing else may enter)

- Classical: c₄ = N₄(1+w), c₈ = N₈(1−w) with N₄ = 2⁹·3²·5·7·13,
  N₈ = 2¹⁵·3⁵·5³·7²·11 (step 4b); the widths τ₄, τ₈ (B581, monic
  convention); the bending directions v_m and the vector v0, J (steps
  4b/5) — used ONLY to form the declared classical norms below.
- Stage: h₃ = 1/(2φ) + i·sin(2π/5)/√5, h₆ = h̄₃ (B593); the S-matrix
  vacuum column at SU(3)₂ (quantum dimensions d₍₁,₀₎, d₍₂,₀₎, exact
  sine ratios).
- Embeddings as frozen in STEP8_P3_PREREG.md (w = +i√3; ζ₅ = e^{2πi/5});
  the declared bijection m=4 ↔ u₃, m=8 ↔ u₆.

## The declared normalization candidates (fixed now; no others may be tried)

- **W-norm (classical):** ĉ_m = c_m / τ_m — the torsion width as the
  line's canonical scale (the width-respect structure itself).
- **J-norm (classical):** ĉ_m = c_m / g_m with g_m = (v_m·v0)ᵀ J (v_m·v0)
  ∈ ℚ(√−3) — the J-Gram of the bent state, a canonical per-line scalar.
  g_m has never been computed; it is a pre-comparison classical quantity
  and is computed exactly during cell N0 below.
- **D-norm (stage):** ĥ₃ = h₃ / d₍₁,₀₎, ĥ₆ = h₆ / d₍₂,₀₎ — quantum
  dimensions as the stage lines' canonical scales; and the identity
  D-norm ĥ = h (the stage amplitudes are already trace-normalized:
  h₃ + h₆ = 1/φ = the B601 trace law at κ = 5).

## The comparanda (scale-free; computed only after this seal)

For each declared pairing (W ↔ identity-D, W ↔ D, J ↔ identity-D, J ↔ D)
— four cells, all sealed now:

- **X1 (phase):** arg(ĉ₈/ĉ₄) = arg(ĥ₆/ĥ₃) under the frozen embedding,
  exactly where the arithmetic is exact, else to 1e-9.
- **X2 (magnitude):** |ĉ₈/ĉ₄| = |ĥ₆/ĥ₃| to 1e-9 (both sides exact
  algebraic numbers; equality decided symbolically where feasible).

## MB12 triple-check (per the standing rule; recorded before sealing)

- Operations non-trivial: the four normalizations produce genuinely
  different numbers (τ_m ≠ g_m ≠ 1; d ≠ 1); g_m could vanish — if any
  g_m = 0 the J-norm cells are declared INVALID-DEGENERATE, not failed.
- Every criterion can fail: nothing forces any match.
- Every criterion can pass: nothing in the banked record excludes X1 for
  any candidate, or X2 for the J-norm cells (g_m unknown).
- **Honest annotation (no pretense of blindness):** the banked magnitudes
  already in the record (N₈/N₄ ≈ 2.6×10⁵ vs |τ₈|/|τ₄| ≈ 3.9×10¹⁴ vs
  |ĥ₆/ĥ₃| = 1) make the W-norm X2 cells EXPECTED-FAIL. They are sealed
  anyway: a registered expected-fail that passes would be the loudest
  possible signal, and its failure data (the exact mismatch factor)
  becomes an input to the next candidate generation.

## The locked outcome table

- **A-norm** — some declared cell passes X1 AND X2: the value bridge
  exists under a structural normalization. Consequence: the NEXT arc
  (separately preregistered before any run) tests the bridge's
  predictions on held-out banked values (the E₆₂ amplitudes, the weld
  values). Nothing runs from this document.
- **B-norm** — some cell passes X1 only: the phase bridge exists; the
  magnitude gauge remains open. Bank which, with exact residuals.
- **D-norm** — no cell passes X1: no declared normalization carries even
  the phase. The value question needs a structurally new idea; the
  residual data is banked and the arc closes negative.

## Run order (each banked blind before the next)

N0 (compute g₄, g₈ exactly — classical-side, pre-comparison) → N1 (the
four X1 cells) → N2 (the X2 cells, skipping INVALID-DEGENERATE) → the
outcome statement from the table above. No other comparison carries
verdict weight; anything else observed goes to FINDINGS as exploratory.
