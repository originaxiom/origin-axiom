# B602 — the value bridge (the normalization arc; runs under NORM_PREREG.md)

**Prereg sealed 2026-07-15 (sha256 8fd90acc…, in the B598 hash ledger)
before any comparison. Firewall: no SM quantities; a positive outcome only
enables the next separately-preregistered arc.**

## Post-seal, PRE-RUN realization (registered before any cell produced output)

After sealing but before running N0, a field-degree obstruction to the X1
cells was noticed and is registered here so it predates every outcome:

- The stage cross-ratio phase is arg(h̄₃/h₃) = −3π/5 exactly (arg h₃ =
  3π/10, the B584 eigenvalue half-angle — structural).
- Every classical normalized cross-ratio lies in ℚ(√−3)^×, whose elements
  have tan(arg) ∈ √3·ℚ ∪ {∞} (degree ≤ 2 over ℚ).
- Exact X1 equality would need tan(π/15) ∈ √3·ℚ; tan(π/15) has degree 4
  over ℚ. **Exact X1 match is impossible for any ℚ(√−3)-valued
  normalization.** The sealed cells remain formally open at their 1e-9
  numerical tolerance (specific numbers could in principle land close),
  so this is NOT a vacuity in the sealed sense — but if X1 fails, the
  registered diagnosis is the role-separation law (B598 adjudication:
  structure from the object's field, values from the stage's field): a
  naive phase ratio cannot bridge the two fields. The field-compatible
  comparanda are exactly the X2 magnitudes (|r_cl|² ∈ ℚ; |r_st| = 1) —
  and, beyond this arc, norm/trace-type invariants (the B601 trace law is
  ℚ(√5)-valued on the stage side for exactly this reason).

Run log follows; each cell banked blind per the sealed order.

## THE RUN (all cells, sealed order, each banked blind)

- **N0 (#968):** g₄ = g₈ = 0 EXACTLY — **the bent states are J-isotropic**
  (a new structural zero: the hearing lines carry no J-scale); the J-norm
  cells are INVALID-DEGENERATE per the seal. Quantum dimensions
  d₍₁,₀₎ = φ, d₍₂,₀₎ = 1 — **the two stage hearing directions are
  anyonically distinct** (the φ-anyon pair vs the simple-current pair),
  the stage-side echo of the classical width distinctness τ₄ ≠ τ₈.
- **N1 (#969):** both live X1 cells FAIL, residual EXACTLY −π/15 —
  classical −2π/3 (∈ ℚ(ζ₆)-land) vs stage −3π/5 (∈ ℚ(ζ₁₀)-land),
  adjacent multiples of 2π/30. As the registered pre-run realization
  predicted (field degree: tan(π/15) ∉ √3ℚ).
- **N2:** both live X2 cells FAIL (the sealed EXPECTED-FAIL), exact
  residual (N₈τ₄)/(N₄τ₈) = 1455/2194752821534 =
  (3·5·97)/(2·7·13²·31·607·49297) ≈ 6.63×10⁻¹⁰ — the torsions' large
  primes survive uncancelled: N-integers and τ-integers are structurally
  different quantities, not a rescaling of each other.

## THE OUTCOME (per the locked table): D-norm

> **No declared structural normalization carries even the phase. The
> naive value bridge does not exist; the arc closes negative with the
> residual data banked.**

## What the negative teaches (the residuals ARE the finding)

1. **The phase gap is one unit of ζ₃₀:** the object's phase lattice
   (multiples of 2π/6) and the stage's (multiples of 2π/10) meet only in
   the compositum ℚ(ζ₃₀) — the role-separation law quantified. Any real
   bridge must be built from compositum-valued or field-symmetric
   invariants.
2. **Field-compatible invariants already work:** |·|²-type and trace-type
   quantities (the B601 trace law, B595's det = φ²) DO cross the fields.
   The next candidate generation should be norm-forms (N_{K/ℚ},
   N_{ℚ(√5)/ℚ}) of both sides, not ratios.
3. **The J-isotropy** removes J-Grams from the candidate pool permanently
   and is itself a clean structural fact worth a mechanism cell (why are
   the bent states null? likely the same parity forcing as the L1 zeros).
4. **The anyonic split (φ vs 1)** gives the stage lines canonical,
   UNEQUAL labels — a future bridge must respect it (it pairs naturally
   with the width hierarchy τ₈ ≫ τ₄, same ordering).

Downstream stays gated; nothing SM-facing ran. Next candidate generation
requires a NEW prereg (norm-form cells), not an amendment — this table is
exhausted.
