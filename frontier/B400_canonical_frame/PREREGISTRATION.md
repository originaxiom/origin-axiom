# B400 (Wall W-B1) — PRE-REGISTRATION: the canonical-frame question

**Committed before computation. The derivation-first content-wall attack: does the banked
seam data define canonical frame geometry WITHOUT arbitrary choices? Per S042's revival
bar this file computes GEOMETRY ONLY — exact algebraic angles, banked as mathematics; no
comparison against physical observables anywhere in the banked outputs.**

## The canonical common space (the discovery motivating this probe)

Pair (1,2) tables are indexed (a, b) = (W₁-exponent mod 20, W₂-exponent mod 12); pair
(2,3) tables by (a, b) = (W₂-exponent mod 12, W₃-exponent mod 6). **The W₂-label space
ℤ₁₂ is shared:** (1,2)'s rows (fixed a, functions of b) and (2,3)'s columns (fixed b,
functions of a) are both vectors in the same 12-dim label-function space. No
identification choice is needed.

## Registered computations (all exact, from step0_tables.json s-components)

- **F1 (the Mercedes frame):** the (2,3) triple (columns b ∈ {0,2,4} as ℤ₁₂-vectors) —
  verify: 2-plane + kernel line (the symmetric direction), exact 120° geometry
  (Gram ∝ [[1,−½,−½],…]).
- **F2 (the golden vectors):** the (1,2) slot rows t(6,·), t(14,·) as ℤ₁₂-vectors —
  norms, mutual angle, exact.
- **F3 (the canonical relative geometry):** the exact Gram of {Mercedes 2-plane basis} ∪
  {golden vectors}: the canonical angles between the (2,3)-plane and the (1,2)-plane
  (principal angles, exact algebraic numbers), and the component of each golden vector
  along the Mercedes kernel (the symmetric direction).
- **F4 (the unitary question):** does the combined structure single out a 3×3 orthonormal
  frame canonically (kernel line + two principal directions — the candidate)? Registered
  outcomes: CANONICAL (the frame is choice-free — bank its exact matrix) / OBSTRUCTED
  (state exactly which choice cannot be avoided — degenerate principal angles, sign
  ambiguities beyond relabeling, or rank deficiency).

KILL for the whole probe: the shared-space geometry is degenerate (zero overlaps — the
planes are orthogonal, no relative structure). That too banks (the content wall would
then stand exactly at "no canonical frame").
