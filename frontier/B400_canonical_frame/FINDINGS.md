# B400 (Wall W-B1) — BANKED: NO canonical frame — the registered kill, at maximal sharpness

**Status: W-B1 complete in one session. Prereg committed first. Firewalled — geometry
only; no observable comparisons anywhere.**

- **F1 ✓** the (2,3) triple is an exact Mercedes frame (Gram ∝ [[1,−½,−½],…], columns sum
  to zero) in the shared W₂-label space.
- **F2** the (1,2) slot rows are exactly ANTIPODAL (⟨g₆,g₁₄⟩ = −‖g‖², so g₁₄ = −g₆): the
  golden seam defines a single LINE, not a plane.
- **F3 — THE KILL:** the golden line is EXACTLY ORTHOGONAL to the Mercedes plane (both
  projections identically zero). Mechanism: SUPPORT DISJOINTNESS in the shared spectral
  space — the golden line lives at W₂-labels {2, 10} = ±2; the Mercedes support is
  {0,1,3,4,5,7,8,9,11}, omitting exactly {±2, 6} (the slot pair and the dead label).
- **F4 verdict: OBSTRUCTED, canonically.** The only choice-free common space puts the two
  structures in disjoint spectral sectors with zero relative geometry. **No canonical 3×3
  frame exists; any mixing-type object linking these structures requires arbitrary
  identification choices.** The content wall is priced exactly here — and this
  retroactively grounds the B398/S5 verdict: the numerological bridge had no canonical
  geometric counterpart even in principle.

**Provenance.** canonical_frame.py → canonical_frame.json (+ the support check); locks
tests/test_b400_canonical.py.
