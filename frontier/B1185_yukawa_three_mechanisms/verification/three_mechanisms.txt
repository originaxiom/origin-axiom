LEG A -- the down-evaluator's exact selection algebra (codex spec, verified independently)
  raw (7, 6, 2) sum=3 mod 12; shifted (8, 4, 0) sum=0 mod 12 -> the two records CONSISTENT,
  the compensating (det-comparison) phase accounted ONCE. chi_-3 carried by Delta_G (spec).
  census {'conn/conn': 18, 'tail6/conn': 9, 'conn/tail2': 6, 'tail6/tail2': 3} -> 36 = 3x3x4: the object-forced tensor SHAPE (B1161) re-derived exactly.
  pure-tail selected pairs [(0, 8), (2, 6), (4, 4)]; the physical (6,2) pair selected.
  THE SKEW ZERO: the repeated 1-dim (4,4) pure-tail channel vanishes EXACTLY at char 0
  (antisymmetry in the two B-legs) -- proved without the missing evaluator T_cal.
  remaining: T_cal (Delta_G, Tr, S) -- commissioned to codex (their spec, their frames).

LEG B -- L186: THE THREE-MECHANISMS THEOREM (pairwise distinct; none is another)
  INV-1: up-coupling rank 0 (dressing) vs 6 nonzero (object) => (1) != (2). PROVED.
  INV-2: rank 0 + sector-selective vs rank 2 + sector-universal => (1) != (3). PROVED.
  INV-3: family index external (degree-2 trace field, multiplicities never 3) while
         (2) is roster-internal with zero kills => (2) != (3). PROVED.

  VERDICT: GENUINELY THREE -- L186 ANSWERED. The 'one mechanism, three costumes'
  direction is FALSIFIED by three exact pairwise invariants (arena / rank+scope / index space).
  SYNTHESIS (fenced, organizing): three mechanisms = three LAYERS of the stack
  (imported dressing / object channel / E8 possibility fence); restoration costs type as
  wall-irrestorable / nothing-to-restore (down READOUT = continuous P^3, C12-trivial on B_0,
  re-run this arc) / finite 3-label -- the B1182 archimedean-finite rhyme, NOT an identification.
REPRODUCES
