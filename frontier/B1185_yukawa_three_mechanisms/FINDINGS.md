# B1185 — L186 ANSWERED: THE THREE-MECHANISMS THEOREM (genuinely three, none is another) + the down-Yukawa char-0 evaluator's benchable half

**Status: banked (frontier). Verdict PROVED** (the pairwise-distinctness theorem; the evaluator's
remaining half typed OPEN and commissioned). Owner's GO on the remaining-math queue.
`verification/reproduce.sh` → `REPRODUCES`. Gate 5 clean (zero/nonzero, rank, and index facts only —
no value, no scale, no hierarchy).

## The question (L186, registered B1171)

The record carried three named Yukawa-suppression mechanisms "none identified with another" (cloud's
own fence): **(1) SEAM-Y** — μ_u=0 on the heterotic dressing (H¹-naturality); **(2) the object
channel's kinematics** — the up-shape EXISTS (6 nonzero components, memo 80); **(3) the family-rank
hole** — 810/810 family matrices rank exactly 2, kernel = the Higgs's own family (memo 82, E8
fence). One mechanism wearing three costumes, or genuinely three?

## THE THEOREM — genuinely three, by three exact pairwise invariants

- **INV-1 (arena contrast)**: the SAME up-coupling is **rank 0** in the dressing (codex R017's
  naturality factorization — **re-run PASS on this bench this arc**: "the Wilson-projected up matrix
  is the exact 1×6 zero matrix") and **6-nonzero** on the object channel (memo 80, byte-identical
  B1171). One mechanism cannot both kill and allow the same coupling ⇒ **(1) ≠ (2)** — the
  difference is located in the ARENA (B1171's "proven different facts," re-anchored).
- **INV-2 (rank + selectivity)**: (1) yields rank **0**, sector-ASYMMETRIC (up dead; the same
  arena's down block well-defined and selection-unobstructed — C12 imposes no texture zero, B1161);
  (3) yields rank **exactly 2**, sector-UNIVERSAL (all 45 internal triples × all leg assignments ×
  3 Higgs families). 0 ≠ 2, selective ≠ universal ⇒ **(1) ≠ (3)**.
- **INV-3 (index space)**: (3)'s kernel is a **family direction** — an index the object provably
  does not carry (B1161 generation-NULL: trace field x²−x+1 degree 2 ⇒ multiplicities ∈{1,2},
  never 3); (2) lives on ONE 27's roster and kills nothing (up 6/6, down 6/6 components present).
  An identification would need the family index to be object-internal — refuted ⇒ **(2) ≠ (3)**.

**L186 CLOSED: THREE.** The "one costume" direction is falsified exactly; each invariant is a
verified zero/nonzero, rank, or degree fact.

## The synthesis (fenced: organizing observation, not a new act)

The three mechanisms are three **layers of the stack** — the imported heterotic dressing / the
object's own channel / the E8 possibility fence — and their restoration costs type as
**wall-irrestorable** (nothing in-arena restores μ_u; OA-C1055 re-run: coefficient variation cannot
repair it without changing the spectrum) / **nothing-to-restore** (presence; the DOWN readout's
missing piece is the observer's **continuous ℙ³ Higgs line** — and the scope cert re-run shows
**C12 acts trivially on B₀**, so that choice is purely continuous, no discrete part) / **a finite
3-label** (which family is the Higgs's). The wall/continuous/finite trichotomy rhymes with B1182's
archimedean-finite leg typing and B1170's arena/content split — **not one mechanism; one stack,
each layer suppressing in its own type.**

## The down-Yukawa char-0 evaluator (the B1161 BENCHABLE artifact) — benchable half built

Codex's committed spec (`YUKAWA_DOWN_RESIDUE_SPEC_308.md`) is the construction contract. Verified
independently here, exact: the **mod-12 bookkeeping** (raw (7,6,2) sum ≡ 3; physical shifts
(+1,−2,−2) applied ONCE → (8,4,0) sum ≡ 0 — codex's and B1161's records consistent, χ₋₃ carried by
Δ_G); the **36-entry census** 18+9+6+3 = 3×3×4 (the object-forced shape re-derived); the **tail
selection rule** ρ+σ ≡ 8 (mod 12) → pure-tail pairs {(0,8),(2,6),(4,4)}, the physical (6,2)
selected; and **THE SKEW ZERO** — the repeated 1-dim (4,4) channel vanishes **exactly over
ℚ(ζ₁₂)** by the proved B-leg antisymmetry: the down block's first theorem-grade char-0 statement,
no evaluator needed. Codex's two R017 certs (scope + no-go) **re-run PASS on this bench** — μ_u=0
dual-homed in this arc's transcript.

**The remaining half** (not built here, commissioned): the normalized cyclic/Serre quasi-iso
𝒯 = (Δ_G, Tr_(Y,Ω), S) over ℚ(ζ₁₂) — codex's own declared "single next artifact," in their frames.
**A debt flagged**: their committed audit's load-target `certify_yukawa_down_tail_cech_308.sage` is
NOT committed (verified absent from their tree) — the GF(1009) tail record is single-homed; their
own spec records a runtime-vs-markdown row discrepancy with runtime the source of truth. The
dual-homing rule (E51 class) applies → relayed.

## Routes

- **L186 → CLOSED (THREE)** in OPEN_LEADS; cloud's fence line upgraded from "none identified" to a
  theorem — relay to cloud with the invariant table.
- **Codex relay (R023 ask)**: (a) commit the audit's load-target; (b) commission 𝒯 per their spec;
  (c) the exact-algebra confirmations + the skew zero + C12-trivial-on-B₀ attached.
- **cc3's B8151** (verified-vs-used sweep, four papers) — paper-lane insight, queued for uptake per
  the owner's standing direction.

## Fences

Inputs cited by bank where not re-run (memo 80/82 byte-identical runs — B1171; B1161's selection
arithmetic; codex's committed proof boundary "rank 16, 33+5, signs — no entry evaluated"). The
distinctness theorem uses only proved invariants; the layer-stack synthesis is an organizing
observation, fenced. No firewall crossing; no measured value. The E8 fence stays a
possibility-space fence (memos 53/62/74) — mechanism (3) is a fact about that fence, not about the
object; INV-3 is precisely the proof that it cannot be pulled back to the object.
