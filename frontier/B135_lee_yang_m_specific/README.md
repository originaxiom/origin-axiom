# B135 — the Lee–Yang bridge is m=1-specific (Phase D)

The honest Lee–Yang physics push. **The Lee–Yang bridge is m=1-specific** — only the golden mean `λ₁=φ<2` is a quantum
dimension; m≥2 (`λ_m>2`) cannot be. So there is **no metallic family of Lee–Yang CFTs**; the metallic *family* is the
spectral/quasicrystal one (`K010`, ℚ(√(m²+4))). The single golden bridge (m=1 → M(2,5)) holds at **modular-data level**
(fusion + quantum dimension + S-matrix Galois conjugate + central charge `c=−22/5`), not a full RCFT identification.

`probe.py`: **D1** m-specificity (`λ_m<2` iff m=1); **D2** the σ₃ Galois conjugate Fibonacci→Yang-Lee (`d_τ: φ→−1/φ`,
S-matrix, central charges); **D3** the honest S030 calibration.

```
python frontier/B135_lee_yang_m_specific/probe.py
python -m pytest tests/test_b135_lee_yang_m_specific.py -q
```

**Tier.** MATH + emergent-physics (POSTULATED, firewalled — the Lee–Yang edge is observed condensed-matter physics, not
fundamental). S030 sharpened (m=1-specific, modular-data level). Nothing to `CLAIMS.md`; P1–P16, B85, S031, B124–B134
untouched. See `FINDINGS.md`; ledger **V124**.
