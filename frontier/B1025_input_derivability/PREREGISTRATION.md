# B1025 PREREGISTRATION — L155: the input-derivability audit (sealed before compute)

**Date sealed:** 2026-08-11 · **Seat:** cc · **Owner-approved** ("yes please"). Gate 5-Q; zero
anchors; no measured value anywhere.

## The question

For each input on the counted list: **not** "is it absent?" (B1000's census answered that) but
**"is it computable from banked results that nobody has joined?"** — the σ-pattern (Blocker 2:
c((E₆)₁) = 6 = B254's split existed banked and unjoined) run deliberately against every input.

## The inputs audited — and the two deliberately excluded

| cell | input | declared prior (per input, non-weakening) |
|---|---|---|
| **I1** | the arrow bit (reversal) | **SURVIVES external — HIGH** (B782 no-section; B716; B721 trivial flow; S3-a direction-freedom) |
| **I2** | the chirality bit (conjugation = τ) | **SURVIVES external — HIGH** (B713/B760; B942's four closed hatches; B963's structure) |
| **I3** | ℓ, the unit | **SURVIVES external — HIGHEST** (weight ledger Hom(G,ℝ₊)=0; B811; B1012) — audited anyway, because "highest confidence" is exactly where the σ-pattern bites hardest |
| **I4** | the 6d type J | **FALLS to internal — MODERATE lean** (the object names E₆/E₈ at its ends, B248/B261; the ends appear inside the hearing instrument, B1011; "possibly internal" is already the ledger's phrase) |
| **I5** | the rank-closing VEV direction | **UNCERTAIN — the real fight** (unsourced per B1017; L138 FIRED — the object picks the ORBIT, K 3/3; B990's two doors unwalked; B990's own prior on route 1 was unfavourable) |
| — | c = 6σ | **EXCLUDED: L154 owns it** (double-booking forbidden) |
| — | the frame classes | **EXCLUDED: B1024/L153 owns them** |

## Method, fixed per input (the σ-pattern, made a protocol)

1. **The absence proof's exact scope** — quote the banked theorem and its stated boundary (what
   class of mechanisms it excludes; what doors it names).
2. **The derivation sweep** — grep the CODE and the FINDINGS BODIES (P3 step 5; never claim
   lines) for material bearing on the input: forced values, canonical choices, structures at the
   ends, exact identities touching the input's home.
3. **The join attempt** — connect any found pieces into a candidate derivation; verify any
   candidate EXACTLY on this bench before it counts (the Blocker-2 standard: re-derive the
   arithmetic, never quote it).
4. **Verdict per input:** **SURVIVES-EXTERNAL** (the sweep's findings all fall inside the absence
   proof's scope) / **FALLS-DERIVED** (a verified join computes it — stated with the join shown) /
   **NARROWS** (partial: a constraint found, the choice space shrinks, the residue stated).

## The meta-deliverable

The updated input count with per-input evidence grades, propagated to `THE_CLAIM` §1's hypothesis
line in the same batch **only for verdicts at FALLS or NARROWS with verified joins** — SURVIVES
changes nothing. If I4 falls and I5 narrows, the floor statement ("one unit, two bits, one
orbit-point") gets its honest revision either way.

## Scope

Governance/audit over banked material; any new mathematics found routes through its own sealed
cell before entering the count. Non-weakening applies to every per-input prior above.
