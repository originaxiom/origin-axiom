# B1025 — L155, the input-derivability audit: three SURVIVE, two NARROW — and the "Lie-valued input" dissolves into unique lines

**Date:** 2026-08-11 · **Seat:** cc · **Sealed:** `8def4962`, per-input priors declared.
Gate 5-Q; zero anchors. Method: the σ-pattern per input — quote the absence proof's scope, sweep
code + FINDINGS bodies, attempt the join, verify any join **exactly** before it counts.

**Verdicts: I1 SURVIVES · I2 SURVIVES · I3 SURVIVES · I4 NARROWS · I5 NARROWS (with a verified
join that retypes the input).** No prior was violated; two leaned-verdicts sharpened.

---

## I1 — the arrow bit: SURVIVES (prior HIGH — held), with the propagation note

**Scope quoted:** B782 (no equivariant section computes the closing choice — theorem), B716 (no
canonical arrow), B721 (trivial modular flow). **The join candidate found and bounded:** B303 —
along the (1,n) ladder CS is same-signed: *the arrow PROPAGATES internally once seeded*. But the
cusped object itself sits at **CS = 0, the symmetric origin** (`audit_checks.py`): the seed is the
closing choice, exactly what B782 covers. **The bit survives as the seed; its propagation is the
object's.**

## I2 — the chirality bit: SURVIVES (prior HIGH — held)

**Scope:** B713/B760 (not self-supplied; the object robustly vector-like), B942 (c ∉ Gal(K^ab/K),
computed two ways, four hatches closed), amphichirality itself — the object *equals its mirror*
and is precisely the thing that cannot prefer a hand. **Sweep:** every chiral construction found
(B576, B582, B944, B1019's door) routes through a closing; none computes the choice. Doubly
mechanized; nothing joins.

## I3 — ℓ, the unit: SURVIVES (prior HIGHEST — held, and STRENGTHENED by the σ-episode)

**Scope:** the weight ledger (Hom(G, ℝ₊) = 0), B811, B1012. **The sharpest sweep result is a
negative that matters:** even the week's maximal derivability wave — σ sighted, σ = 1
candidate-forced — **leaves the unit untouched**: at σ = 1, G = ℓ/4, and ℓ is still free
(`audit_checks.py`). The one-parameter family (cc3's C3) is the exact residue. **This is the one
input whose externality survives every attack from every seat.**

## I4 — the 6d type J: NARROWS (prior "falls-internal, moderate" — partially held; non-weakening: NARROWS, not FALLS)

**Scope:** B277's banked phrase — "the 6d type is still a free input" — is a statement about the
*general class-S construction*. **The join:** the object **canonically nominates** its types — E₆
at the hyperbolic end, E₈ at the spherical (B248/B253/B261, the McKay chain B266/B727 within its
stated scope, and B1011: the two ends' McKay data sit *inside the hearing instrument*). **What
remains external is not a selection from the ADE menu but the ACCEPTANCE of the nomination** —
precisely S1's unrun identification (the E₆ compactification's SW curve vs the banked
A-polynomial). **The choice-space collapses from {all J} to {accept / construct-otherwise}; the
closer is named (S1) and is a computation, not a preference.**

## I5 — the rank-closing VEV: NARROWS, with the audit's verified join — the "Lie-valued direction" dissolves

**Scope quoted:** B952/B959/B960 (no centralizer route — unconditional in class), B962 (𝕆P²
homogeneous), B990 (orbit invariants cannot pick points; two doors; route-1 prior unfavourable).

**The join, verified exactly:** the cascade's VEV *directions* are **unique lines** — the 27
contains its SO(10)-singlet with **multiplicity 1** (B884's banked grading) and the 16 its
SU(5)-singlet with **multiplicity 1** (B1017's retraced traceless charges) — both re-verified in
`audit_checks.py`. Given the derived chain (B994: endpoint rule-independent), **rep theory fixes
the directions completely. There is no continuous direction-choice to import.**

> **The input RETYPES: not a Lie-valued direction (that part is canonical, multiplicity-one) but
> (i) the discrete ACT — which is B1000's charge-sector closing, already counted — and (ii) the
> MAGNITUDES, weight-1, whose possible folding into the ℝ₊/unit sector is cc3's still-open scope
> note.** B990's obstruction is **rescoped, not contradicted**: it concerns canonical *points* of
> pair-orbits (the Kato–Yukie setting); the single-VEV *line* was never its subject.

## THE META-DELIVERABLE — the input count after the audit

| input | verdict | residue |
|---|---|---|
| arrow bit | external | the seed only; propagation internal (B303) |
| chirality bit | external | doubly mechanized |
| **ℓ** | external | the one-parameter family — survives everything |
| J | **narrowed** | accept-the-nomination; closer = S1 |
| VEV | **narrowed & retyped** | the act (already counted) + weight-1 magnitudes; directions canonical |

> **The floor statement revises: "one unit, two bits, and two acceptances whose content the
> object already nominates."** No continuous dimensionless input remains on the list *anywhere*
> (c is L154's; the frame bits died in B1024; the VEV's continuum dissolved here).
> `THE_CLAIM`'s hypothesis line gains the retyping note in this batch.

## Scope

Verdicts audit the *inputs' status against banked material*; S1 and the magnitude-folding remain
open computations, named as the closers they are. Nothing here effects a VEV, derives an arrow,
or claims Tier 2.
