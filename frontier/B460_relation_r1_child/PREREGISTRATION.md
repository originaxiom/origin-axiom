# B460 — PREREGISTRATION: Relation R1 — the child's own dynamics (one live cell + one table cell)

**Committed before computation. Firewalled. Campaign: OPEN_LEADS §Relation.**

## Cell 1 — the length spectrum (TABLE/NEW-MATH-ONLY; verdict pre-forced, stated per MB12)
The child 4₁(5,1)'s geodesic length spectrum (SnapPy; closed ✓). Trace-field + arithmeticity
theorems force class-laundering BEFORE computation (geodesic traces lie in the −283 field by
theorem; arithmetic ⇒ commensurability-class data) — this cell cannot fire H1 and says so; the
deliverable is the exact table. **Blocking lit-gate: Chinburg-era Meyerhoff literature** (the
geodesic table may be published). Controls: 4₁(7,1) (the non-exceptional sibling), the WEEKS
manifold (arithmetic, disc −23 — separates arithmetic-generic from child-specific), one generic
closed census manifold.

## Cell 2 — the per-vacuum CS of the child's 4 SL(2) vacua (LIVE)
Algorithm (named, per the red-team's F8): locate the 4 vacua (roots of t⁴−3t³+t²+3t−1, banked
B439/B440) as points of the PARENT's Ptolemy/A-polynomial variety satisfying the (5,1)-filling
holonomy equation; evaluate complex volume via extended-Bloch + the filling correction
(Neumann flattenings / Kirk–Klassen variation); substrate = the B458 engine.
**ENGINE GATE (binding): the geometric vacuum must reproduce the banked CS = ±0.07703818
(B434) before any non-geometric number is read.** Timebox: 4 h. Past the timebox = the named
boundary (reachable only by a failed attempt, never by skipping).

## Corrections carried in this prereg (the Chat-1 premise fixes)
The child was studied exhaustively (C0–C6; B443 falsified "physics is what the object
produces"); CS = ±0.077 is the banked MIRROR PAIR (B434) with the exact sign law
CS(p,−q) = −CS(p,q) (B289: the closing forces *a* sign; *which* sign is external); "nonzero
CS = CP violation" ignores that every chiral closed hyperbolic manifold has CS ≠ 0.

## Bins
Cell 1: table (class-forced; NEW-MATH only if the lit-gate clears). Cell 2: per-vacuum values
LAUNDER (−283-class arithmetic, expected) / a non-whitelist exact structure → burden-inversion →
escalate / TIMEBOX-BOUNDARY (named).
