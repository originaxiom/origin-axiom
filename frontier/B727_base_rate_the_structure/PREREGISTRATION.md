# PREREG — B727: BASE-RATE THE E₆-ACROSS-THREE-FACES STRUCTURE (SEALED)

cc banking seat, 2026-07-20. Owner "yes" (cc2's naivety-review probe 1). B724 applied base-rate/
look-elsewhere discipline to NUMBER-matching (6/6 famous ratios have a torsion near-hit ⇒ near-hits
expected, not evidence). That same test has NEVER been run on the program's FLAGSHIP STRUCTURAL
claim: "E₆ recurs across three faces — McKay (2T), Lie (character variety), CIZ (modular
invariant)." The ADE/exceptional catalogue is short and promiscuously reused, so "E₆ shows up more
than once" may be exactly the cheap-and-expected thing B724 killed — one level up, UNAUDITED. This
is the single missing calibration on the program's best remaining asset. Web-enabled (McKay
correspondence; CIZ/A-D-E modular invariants; arithmetic Kleinian groups). COMPUTE-NOT-CITE.
Firewall: structural/arithmetic only, no SM value; this is a SELF-AUDIT — a negative here is a
real deliverable, not a defeat (do NOT rescue the claim; kill it if the base rate says so).

## The prior context (verify, don't lean)
- B266: π₁(m004) ↠ 2T=SL(2,𝔽₃), exactly 2 surjections (the FORCED arithmetic atom, ℚ(√−3)).
- 2T ↔ McKay-E₆ (McKay correspondence): FORCED but OBJECT-INDEPENDENT (any 2T gives it).
- B282: the E₆ CHARACTER VARIETY (Lie face) is GENERIC — present for every hyperbolic knot via
  E₆'s principal sl₂ embedding (zero fig-8 content). So the Lie face is already known non-specific.
- CIZ: E₆ is the SU(2) level-10 modular invariant (Coxeter number 12 = h(E₆)).

## Three probes (each: compute → 3-skeptic adversarial-verify → refine loop)
1. **Is McKay↔Lie↔CIZ landing on the SAME label a THEOREM or independent coincidences?** The ADE
   meta-pattern (McKay quiver = affine Dynkin = CIZ graph = Kleinian singularity) is a canonical
   web of equivalences. Compute/establish: once E₆ appears in ONE face, are the others FORCED by
   the ADE linkage (so NOT independent evidence), or genuinely independent? Two-outcome: A =
   independent/coincidental (recurrence could be a real signal); B = FORCED by the ADE linkage
   (the three faces are one label viewed three canonically-equivalent ways — not three evidences).
2. **The base rate across comparable objects.** Assemble a comparison set of arithmetic hyperbolic
   objects (Bianchi groups PSL(2,O_d), d=1,2,3,7,11; other small arithmetic Kleinian/Fuchsian
   groups; small knots' finite SL(2) quotients). For each, run the SAME face-pipeline (McKay label
   of the finite quotient; the CIZ label at the arithmetically-tied level; the generic Lie face).
   Measure: how OFTEN does SOME ADE/exceptional label recur across the faces the way E₆ does for
   m004? Compute the frequency + a look-elsewhere/birthday estimate given the short catalogue.
   Two-outcome: A = RARE (m004's recurrence is special/discriminating); B = COMMON (expected).
3. **VERDICT: real structural signal, or the number-bag one level up?** Synthesize probes 1–2 +
   B282: is "E₆ across three faces" object-specific evidence BEYOND the arithmetic atom (2T from
   ℚ(√−3)), or does it reduce to (atom → one label) + (ADE linkage → the other faces forced) +
   (Lie face generic)? Honest verdict — and if it's the number-bag, SAY SO plainly. Two-outcome:
   A = survives base-rate (a real structural signal beyond the atom); B = the recurrence is the
   ADE meta-pattern + the atom; the flagship "three faces" claim is over-belief (Naivety 1
   confirmed), and the ONLY object-specific content is the already-banked atom.

## Two-outcome (sealed, campaign)
- **OUTCOME A:** the E₆-across-three-faces recurrence is RARE/independent across comparable
  arithmetic objects ⇒ a genuine object-specific structural signal beyond the atom. The flagship
  claim survives the base-rate discipline it had dodged.
- **OUTCOME B:** the recurrence is COMMON/forced (the ADE classifications are canonically linked;
  the Lie face is generic, B282) ⇒ the "three faces" reduce to the one arithmetic atom viewed
  through equivalent classifications. The flagship structural claim is the number-bag one level up
  — Naivety 1 confirmed. [honest expectation: leans B — but COMPUTE it; the atom itself (B266)
  stands regardless as the program's strongest object-specific fact.]

## Verdict template
Bank the base-rate of the structural claim (rare/real vs common/number-bag), computed not asserted;
LAW_MAP placement (and if B, an ERROR_LEDGER note that structure-skepticism lagged number-
skepticism); nothing to CLAIMS. Credit cc2 (the naivety review). This is a self-audit — the
honest result, either way, is the deliverable.
