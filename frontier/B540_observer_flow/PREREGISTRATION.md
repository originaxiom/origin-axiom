# B540 — The Observer Flow (pre-registration)

*Committed before compute, 2026-07-12. The first probe whose question is
generated by the object: its one intrinsic verb (induce) applied to its own
observers. The coupling space (B535: 7 canonical systems, saturated) becomes
a DYNAMICAL system: derive each canonical system with respect to its own
length-1 prefix window; map system → system; iterate to closure.*

**Predictions (stated before compute):**
1. Type 1 (σ itself) is a FIXED POINT (banked self-recovery).
2. The flow is eventually periodic into a small core (finite set, Durand).
3. If the object stays in character, a ℤ/2 appears in the cycle structure.

**Falsifiers:** (1) σ not fixed → the self-recovery reading was wrong;
(2) the flow leaves the 7-system census → B535's saturation was
window-length-limited (extend and correct, as with the 5→6 types);
(3) no ℤ/2 anywhere → prediction 3 recorded as a miss.

**Method:** each canonical system is itself a substitution (its canonical
codes); grow its fixed point (depth ~12), induce on the length-1 prefix
window with the B530 engine, canonicalize, match. Locks: tests/test_b540.py.
