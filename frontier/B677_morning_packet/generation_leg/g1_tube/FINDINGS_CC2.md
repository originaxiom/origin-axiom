# G1 — BANKED: WAVE 1 = THREE KILLS, TWO FAILURE AXES, ONE THEOREM
# (cc2 + subagent, 2026-07-18; prereg 9160337c frozen; run 1.3s exact,
# reproducible; target reproduction 3 independent routes, 160 terms)

VERDICTS (all sealed candidates): T2 KILL. T1 KILL. T0 KILL.
The generation leg stays OPEN; six kill patterns now constrain wave 2
(B672's four + B674's ES tower + this cell's three-in-one).

THE TWO FAILURE AXES (the wave-2 design input):
1. TRACE-BLINDNESS (third and fourth instances, now with a THEOREM):
   T0 is killed STRUCTURALLY — Fib modular => Z(Fib) fusion => Tube(Fib)
   SEMISIMPLE (dim 7, built from first principles; F/R data verified,
   B484's tr(braid) = -3/phi reproduced) => HH^{>=1} = 0 IDENTICALLY and
   HH^0 <= 7; the targets exceed the bound 480x by n=40. T1's fully-
   diagonal sum collapses to the scalar tr(W)tr(W') — a bare number, the
   exact parallel of B674's trace-silent tower.
2. VALUE-IRRATIONALITY (the NEW axis): the character-level candidates
   (right SHAPE: unbounded, aperiodic) die at n=0 — every weld-weighted
   combination has a genuinely Q(zeta_5)-irrational leading coefficient
   (the weld's matrix elements are degree-4 over Q; a from-scratch
   Q(zeta_5) engine was required), and the targets are INTEGER series;
   the prereg's one rational rescaling cannot fix it. Plus the structural
   support-class kill: chi_1/chi_tau's fractional supports (11/60, 59/60
   YL; 53/60, 17/60 Fib) NEVER meet the targets' classes (2/5, 3/5).
CROSS-CHECKS BANKED: weld trace = -1/phi reproduced by an independent
mechanism; ANOMALY: the SU(2)_3 4x4 block-extraction does NOT give the
standalone Fib S,T (S mixes all four spins) — the standalone category is
the correct object (sympy cross-check).

WAVE-2 IMPLICATION (for the director + cc; not launched): the surviving
candidate space = MODULE-LEVEL objects with STRUCTURALLY INTEGER graded
coefficients. The natural family: the WELD-TWISTED MODULE CHARACTERS
tr_{V_a}(W q^{L0 - c/24}) — W acting INSIDE the infinite-dimensional
module (the twisted sector of the sigma-mapping-torus — which IS the
figure-eight complement), not finite W-matrix-weighted sums of untwisted
characters. This is cc's positive formulation made concrete: it sees the
module itself; its grading survives the order-10 rotation; and integer
coefficients arise as graded traces of a finite-order operator on integer-
graded pieces. Needs: the weld's lift to the chiral modules (design work,
main seat + cc's Gamma(5) module-side analogue). Artifacts: g1_tube.py,
g1_results.json, g1_run.log, FINDINGS_RAW.md. Repo untouched.
