# E2 — BANKED: THE MUTE PAIR IS THE BOUNDARY PAIR — THE FORM-MATCH BECOMES
# A LAW (cc2 + subagent, 2026-07-18; prereg 7cc62400; ALL GATES PASS)

CONVENTION (stated, golden-calibrated): "boundary-detected" := the class's
restriction to the peripheral subgroup <mu, lambda> is a COBOUNDARY of the
restricted action (trivial in H^1(T^2; V)) — this is the criterion that
exactly reproduces the banked golden split (classes 0,1 vs 2,3,4; the
golden control ran FIRST and gated the silver run; note the banked label
"boundary-born" therefore means TRIVIAL peripheral restriction).

RESULT (exact, silver field L = Q(s,i), zero floats): under the identical
criterion, the silver boundary-detected set is EXACTLY {0, 1} — precisely
the mute vector-cup pair from D3 (cross-checked against d3_results.json).
Controls: peripheral commutativity at rep level; rho(mu) != I; h0=1/h1=5
gates; exactness.

THE LAW (upgraded from D3's form-match): **the boundary pair is mute at
BOTH golden and silver — the vector-cup pairing's unique zero is the
boundary-boundary coupling, structurally, across the metallic family.**
The generations couple to everything (including the boundary); the
boundary does not couple to itself. Frontier feed: state as a conjecture
for the whole family (test at bronze/(i1,i2) when those letters exist).
Artifacts: e2_silversplit.py, e2_results.json, e2_run.log. Repo untouched.
