# PREREG — ANATOMY LOOP 5 (sealed before compute, 2026-07-17 late)
# Feeds: FRONTIER v5 items 1-2. Two cells launch now (D4 annex still runs);
# items 3+ queue for the next wave.

## E1 — THE LAMBDA^2 DIAGONAL (the self-couplings)
D2's graded sign rule leaves the 351-slot diagonal UNKILLED: [c_ii] in
H^2(D; Lambda^2 27) has no Koszul constraint. Compute [u_i cup u_i]'s
Lambda^2-component for ALL FIVE classes i = 0..4 (boundary included), by
the D2 pipeline (Kronecker cup, antisym projection, witness method with the
certified modular-nullspace kernel). Sealed falsifiers: the off-diagonal
L351 classes must reproduce D2's banked witnesses; slot cocycle identities;
exactness. Sealed outcomes per i: SELF-SPEAKS (witness) / SELF-SILENT
(exact coboundary solve) / UNDECIDED — all bankable. If any [c_ii] != 0:
the FIRST SELF-COUPLING (a diagonal invariant per class; compare i=0,1
boundary vs 2,3,4 solo pattern).

## E2 — THE SILVER SPLIT RESOLUTION
Resolve the silver H^1 basis into boundary-born vs solo classes (the w2a
UNRESOLVED caveat), then decide: IS THE MUTE PAIR THE BOUNDARY PAIR?
Method: build the silver boundary restriction map (the peripheral subgroup
of the silver double from the banked presentation; restrict each H^1 class
to the boundary torus generators; boundary-born = nonzero restriction /
image of the boundary map per the golden-side convention in cell3/B637 —
STATE the convention used and apply it identically to a golden re-run as a
control). Gates: the golden control must reproduce the banked 2 boundary +
3 solo split exactly. Sealed outcomes: MUTE-PAIR-IS-BOUNDARY (the form-match
becomes exact in MEANING: "the boundary pair is mute at golden AND silver")
/ MUTE-PAIR-NOT-BOUNDARY (bankable fork: the silver mute pair is a
different structure) / split-degenerate (report, stop).

## Discipline: repo READ-ONLY; work only under anatomy/loop5/<cell>/; exact
## arithmetic in verdict paths; bank only from finished runs.
