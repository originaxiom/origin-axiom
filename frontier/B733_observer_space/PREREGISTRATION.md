# PREREG — B733: THE MENU OF OBSERVERS — the observer-space at the object's congruence doors (SEALED)

cc banking seat, 2026-07-20. Owner: "map the observer-space at each door, fully" — make concrete
the moduli of symmetry-breaking CHOICES (the "menu of observers") the object offers at each
congruence level, and whether it grows with depth or stays a bounded discrete set. Follows B731
(m004 non-congruence), B732 (B701 conjugation = Out(A₅) at the sister m003's level-2 door), B706
(the object's freedom is DISCRETE 𝔽₂ bits, the SM's is CONTINUOUS ~20 params — the deepest wall).
Pure math (finite group + representation theory); Gate 5; nothing to CLAIMS. VACUITY-GATED: the
observer-space must be a PRINCIPLED object (the torsor of B701-conjugation / arithmetic-Galois
symmetry-breakings on the level-n congruence quotient), not "any choice."

## Principled definition (to hold the campaign to)
At congruence level n the object's finite shadow is G_n = PSL(2, O₃/n). B701's conjugation c
(ω↦ω²) acts as an automorphism c_n of G_n. An OBSERVER = a symmetry-breaking = a section of the
torsor of the arithmetic outer-symmetry (⟨c_n⟩ and any further Galois/Frobenius symmetry) acting on
G_n's measurables (conjugacy classes / irreps). The OBSERVER-SPACE at door n = that torsor = an
𝔽₂-vector space of independent binary "swap" choices; its DIMENSION = the observer bit-count.

## Grounded (level 2, pre-computed): the A₅ door offers ONE bit that couples being & hearing
c_2 = Out(A₅) = ℤ/2 (B732). It simultaneously orients the two 5-cycle classes AND the two 3-dim
irreps of A₅ (which live over ℚ(√5)=HEARING, Gal-conjugate). So the single observer bit LOCKS the
being-conjugation to the hearing-swap — being·hearing meet in the one ℤ/2. Menu at door 2 = ℤ/2.

## Three probes (each: compute → 3-skeptic adversarial-verify → refine)
1. **Door 2 (A₅), fully.** The complete observer-space: c_2=Out(A₅)=ℤ/2; the swapped measurables
   (5A/5B, the two ℚ(√5) 3-irreps); confirm the being-conjugation bit COUPLES to the hearing-swap;
   the menu = ℤ/2 (one bit). Two-outcome: A = a small bounded 𝔽₂-structure (≤ few bits); B = larger.
2. **Door 4 and the being-prime door (3 / √−3) — does the menu GROW?** Compute c_4 on PSL(2,O₃/4)
   (order 960): inner or outer, and the observer bit-count. Compute the being-prime door
   (level (√−3) / (3)): what conjugation does at the ramified prime, and its bit-count. Combined
   level 12=2²·3. Does going deeper ADD independent observer bits, or reproduce the SAME few
   (being/hearing/meeting = V₄) at finer resolution? Two-outcome: A = stays BOUNDED (same 𝔽₂ bits);
   B = GROWS with depth.
3. **The full menu + the honest bound (the growth law).** As the object → the full profinite
   congruence completion, is the observer-space a BOUNDED discrete 𝔽₂-space (the V₄/seam B704 — a
   handful of coin-flips: chirality/sheet/meeting-sign) or does it grow toward the CONTINUOUS
   ~20-parameter space physics needs (B706)? State the full menu and the growth law. Two-outcome:
   A = BOUNDED discrete (confirms B706 concretely at the congruence doors — the object offers
   finitely many discrete observer-bits, never a tunable continuum: cherry-picking to match
   continuous physics is impossible in-kind); B = grows toward continuous.

## Two-outcome (sealed)
- **OUTCOME A (expected):** the observer-space is a BOUNDED discrete 𝔽₂-space — a small fixed
  number of independent bits (the being/hearing/meeting V₄ + any stage bits), realized at every
  congruence door at finer resolution but NOT growing into a continuum. The "menu of observers" is
  a handful of coin-flips, not a dial. This confirms B706 at the congruence level and answers the
  owner: an observer can be CONSTRUCTED (pick the bits) but not CHERRY-PICKED to encode continuous
  physics (wrong kind + size). New content: the being↔hearing coupling inside the bits (door 2).
- **OUTCOME B:** the observer-space GROWS with congruence depth toward a continuum — the object
  offers unboundedly-refinable observer freedom. [would reopen the discrete-vs-continuous seam.]

## Verdict template
Map the observer bit-count door-by-door + the growth law; bounded-discrete vs growing. LAW_MAP
placement; nothing to CLAIMS. The honest "menu of observers" for the owner. Firewall: pure math,
no physics value; the cherry-picking bound is structural (kind/size), not a fitted claim.
