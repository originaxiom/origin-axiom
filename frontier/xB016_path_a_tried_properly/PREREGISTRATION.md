# xB016 — PREREGISTRATION (sealed before the verification code exists)

**Seat `xb`, `sep16-branch`, 2026-09-17. Sealed, hashed and pushed before any cell of this arc's
`verification/` runs.**

## P0

Hyperbolic geometry, homology-torsion growth, complex Chern–Simons levels. Named mathematics. No
value, no generation count, no physics reading, nothing to `CLAIMS.md`. Gate 5 absolute.

## Why this arc exists — the owner's instruction, verbatim

> *"lets verify parh A once more, so were sure we dine it priperly and were sure it doesnt cross,
> and we tried right way"*

**Path A has now been wrong twice in this seat's hands.** `xB014` killed it on a sort-order error
(withdrawn in its own Addendum 1 after the owner asked *"u sure about extremality"*). `xB015` then
showed the *reason* xB014 gave for non-crossing — *"the wall is dimensionful"* — was also wrong.
**Both times the conclusion was asserted rather than attempted.** This arc's job is to **actually
try**, on named routes, and to be checkable about what was tried.

## What "crossing" means, fixed here so it cannot drift

The framework's one continuous dimensionless anchor is **A2 = c = 6σ** (B1015, sealed). **Crossing =
deriving a numerical value for `c` or `σ` from the object's own data with no new input.** Anything
weaker — a suggestive ratio, a matching denominator, a structural resemblance — is a **LEAD**, named
as one. Constraining `k` is **not** crossing: `k` is not the free anchor.

## The sweep, done first (xB010's rule)

Read, not cited:

* **B290** — *"the filling `n` is NOT the level `k`"*: a **banked negative** on exactly the move this
  arc is tempted by. The filling integer is topological, `k` is a quantum root-of-unity parameter,
  *"independent axes … a formal substitution, not an identity."* **This precedent binds this arc:
  any identification of a topological integer with `k` must clear B290 or it is dead on arrival.**
  B290 also carries the **NZ core-geodesic ladder** — a *different* ladder from this one (Dehn
  filling `(1,n)`, not cyclic covers), so it does not settle this path, and its own scope note
  (the tidy real coefficient is m004-specific) is a warning about reading a tower's number as an
  object's number.
* **B259** — the gravity brick: `Λ = −1` exact, action = complex volume, and a **firewalled**
  `k = 6π/(GΛ)` route with a 122-order magnitude gap. A prior attempt to fix a level; firewalled,
  not banked.
* **B291** — the scale-extremal closing is `(5,1)`, **non-arithmetic**, and *not* the fibre closing:
  selection is axis-stratified.
* **xB014 + its Addendum 1** — the tower's facts (`Vol_n = 24n·v₀`, torsion `L_n²`/`5F_n²`, rate
  `→ log φ²`, ratio `0.474127597` constant) and m004's three-way extremality. **To be re-derived
  here from scratch, not carried across** (xB010).
* **xB015** — `S = −CS·k − Vol·σ`, `∂S/∂k = −CS`, `c = 6σ`; the family's ℤ/12 CS index; and the
  standing correction that the wall is **dimensionless**.
* **B1012 / B1015 / B1088** — the wall and the anchor declaration.

## The routes, each with a prediction and a kill condition

**P1 — the tower, re-derived.** Independently rebuild: `Vol_n = n·Vol(m004)`; **`CS_n` along the
tower**; homology torsion `= L_n²` (n odd) / `5F_n²` (n even); growth rate `→ log φ²`; the ratio
`log λ / vol`.
*Prediction:* reproduces xB014, and **`CS_n = 0` for every n** (the tower is covers of m004, and
K4 of xB015 forces it).
*Kill:* any disagreement with xB014's numbers stops the arc until it is resolved.

**P2 — ROUTE 1, the rate route (the crossing attempt this path has never actually made).** Along the
tower the action is `S_n = −CS_n·k − Vol_n·σ`. If `CS_n = 0` then `S_n = −n·Vol·σ`, **linear**, while
`log|H₁(M_n)_tors| ≈ n·log φ²`, **also linear**. Their ratio is a pure number over σ:

> `log|tors| / (−S) → log φ² / (Vol·σ) = 0.474127597… / σ`

**So an identification of `log|H₁ tors|` with `−S` (an entropy-equals-action statement) would FIX
`σ = 0.474127597…` and `c = 6σ = 2.8447655…`.** This arc states that number **in advance** precisely
so it cannot later be presented as a discovery.
*Prediction:* **the identification is NOT licensed** — a cusped hyperbolic manifold has no horizon,
homology torsion is not a state count, and the one place torsion genuinely enters a CS partition
function is Ray–Singer torsion at **one loop**, not the on-shell action. A literature check is run
rather than assumed.
*Kill (i.e. what would make this a crossing):* a named result licensing entropy `= −S` for this
class of object. *Reporting rule:* if unlicensed, the number `2.8447655` is recorded as a
**refuted candidate**, never as a prediction, and this arc says so in its headline.

**P3 — ROUTE 2, is the rate the OBJECT's or the TOWER's? (the genuinely blind cell).** `0.474128`
is `log φ² / Vol(m004)`, computed along **one** tower. Measure `log|H₁ tors| / vol` along **other**
cover towers of the same m004 — degree-matched covers that are not the cyclic ones.
*Declared prior:* **different towers give different rates**, so the number is a property of the
tower, not of the object — which would kill Route 1 independently of licensing.
*Kill:* if every tower returns `0.4741`, the rate **is** the object's and Route 1 becomes much more
serious; this arc would then say so.
*Control:* Bergeron–Venkatesh-type congruence towers are reported to approach `1/(6π) = 0.0530516`.
**If any tower here approaches that instead, the two rates are the decisive evidence.**

**P4 — ROUTE 3, what the mod-½ indeterminacy forces.** For cusped manifolds `cs` is well defined
only modulo `½` (Neumann; and the indeterminacy is essential, not a defect). Then `S = −cs·k − …`
is defined modulo `k/2`, so `exp(2πiS)` is defined up to `(−1)^k`.
*Prediction:* **well-definedness of `exp(2πiS)` forces `k` even.** A real constraint — **but on `k`,
not on `σ`, so by this seal's own definition it is NOT a crossing**, and will not be dressed as one.
*Kill:* if the framing anomaly `exp(2πi c/24)` absorbs the ambiguity, the constraint instead couples
`c` and `k` — **which WOULD be a crossing candidate** and must then be worked out, not waved at.

**P5 — B290's precedent, applied to this arc's own temptation.** Re-derive B290's negative and test
whether it kills identifying either the tower index `n` or xB015's ℤ/12 CS index with the level `k`.
*Prediction:* it kills both. *Kill:* if B290's argument does not reproduce, B290 is disputed and
this arc stops to resolve it.

**P6 — the verdict.** State plainly: what was tried, what each route returned, and whether Path A
crosses. **If it does not cross, say which of the three routes is the one a future seat should
re-open and what it would need.**

## What this arc will NOT do

Not identify a topological integer with `k` without clearing B290 · not present `σ = 0.4741…` or
`c = 2.8448` as a result unless P2's licence is found · not treat a constraint on `k` as a crossing ·
no physics reading, no identification (E82's class), nothing to `CLAIMS.md`.

## The seat's prior, declared

P1 reproduces · **P2's identification is not licensed, so Route 1 fails** · **P3 is the cell I cannot
call** — I expect tower-dependence, which is why it is the one worth running · P4 gives `k` even, a
small honest positive that is *not* a crossing · P5 kills the identification · **P6: Path A does not
cross.** If I am wrong it will be at P3, and that is where the arc is aimed.
