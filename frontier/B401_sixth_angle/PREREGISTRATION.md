# B401 (Wall W-C, the owner's angle) — PRE-REGISTRATION: the arithmetic of ℚ(√−15)

**Committed before computation. The proposal (owner, 2026-07-04): interrogate the seam
through the INTRINSIC arithmetic of ℚ(√−15) — class field theory, prime splitting, the
ℤ/2 class group — as a sixth angle the other lenses don't see. Framing facts (textbook,
cite-not-claim): disc(ℚ(√−15)) = −15; h = 2; the genus field = Hilbert class field =
ℚ(√−3, √5) = H, the seam field; the genus characters are χ₋₃·χ₅ — the program's CRT
tensor split is genus-theory-shaped. Firewalled; exact-only (no ≈ matches count).**

## P1 — the class-group action (hand-verified; to be locked)

σ_cl: (x, y, z, s) → (x, −y, −z, s) — the lift of the class-group generator
Gal(H/ℚ(√−15)). Registered checks: (i) σ_cl(slot constant) = −(3-block constant) exactly
(hand-verified from banked P60 values; lock it); (ii) σ_cl on ALL banked sector/face
constants — the full orbit table; (iii) at TABLE level σ_cl is one of P61's failing
√5-flippers — verify, and test at which AGGREGATE level it becomes a symmetry (the
emergent-symmetry family's candidate origin).

## P2 — prime spectroscopy by ideal class

Classify every prime appearing in banked exact constants (values, denominators, Gram
spectra, character magnitudes): ramified {3,5} / split-principal / split-non-principal /
inert in ℚ(√−15) (forms x²+xy+4y² vs 2x²+xy+2y²). Hand-anchors to lock: 2 and 23
non-principal; 7 inert. REGISTERED PATTERN-BET (two-outcome): the primes entering the
SEAM channel (s-components, the boundary partials, the Gram's exceptional 23) are
non-principal-or-ramified; primes entering classical channels (the singles, the generic
partials) are principal-or-inert. KILL: any clean counterexample (bank the exact table
either way; VACUITY GUARD: the bet is falsifiable only if both classes are populated —
report the census sizes).

## P3 — the genus-character assembly

Re-express the B386 branch constants (e.g. GH[00] = 25/4 − √5/4 − √−15/2) and the
class-1/class-5 partials (−1/16, −1/48) through the genus characters χ₅, χ₋₃ (Gauss sums
g₅ = √5, g₋₃ = √−3, g₋₁₅ = √−15 — the banked S-normalizer). Registered question: do the
principal/non-principal genus contributions align with the generic/boundary split of the
1/12 (P64)? Exact identities only.

## P4 — the class-number-formula constants (exact-only, vacuity-guarded)

h = 2, w = 2, L(1, χ₋₁₅) = π·h·... the exact rational relations between {h, w, disc} and
the banked normalizers {12, 48, 240, √−15}. ONLY exact identities with a stated derivation
path count; no numerical coincidences. KILL: none found — banked as the negative.

Machinery: banked JSONs + the ζ₆₀ engine; pure Fraction arithmetic; locks per probe.
