# B1082 — THE ORDER COMPARISON: the chain's forcing DAG verified edge-by-edge; the cosmological match holds CONDITIONAL-ON-GUT-MODEL, with the pre-E₆ layers honestly NOT-COMPARABLE

**Date:** 2026-08-19 · 2 agents; the adversarial verifier FAILED the mapping as first
typed (the model-dependence disclosure was missing) and PASSED its computational spine —
this FINDINGS carries the corrected grade, the verifier's ruling baked in · Gate 5:
sequence only, no scale/time/value anywhere.

## The chain's forcing DAG (verified verbatim, nine lock files on disk)

Axioms A1–A6 → L/R (C1, conditional — 'the smallest piece of inserted structure' is A7's
order convention) → A = LR → m004 (Thurston/Riley; the trace-3 sieve) → κ = tr[a,b] →
ℚ(√−3) (κ−2 = ω²; the Eisenstein×golden factorization) → the ramified 3 → 2T = SL(2,𝔽₃)
→ McKay affine E₆ (GAP marks verified) → the 27/torus (B904's 0/3003; B877's four
charges) → the measurement cascade: 78 → 46 → 30 → 18 → the A₂-floor 12, with the
SECOND measurement's dim-14 landing a PARALLEL SIBLING branch (the verifier's slip-fix:
not a descendant — the DAG branches, the prose must not flatten it) → the u(1)-rich
terminals (B952's rank obstruction: rank-preserving measurements never reach rank 4).
Scope caveats carried: the 2T-access is CLASS-generic (B993/B996); the field is the
m004-specific step (Reid's uniqueness).

## The comparison — the corrected verdict

**CONSISTENT-ORDER, CONDITIONAL-ON-GUT-MODEL, on the type-comparable segment.** From
E₆ onward the chain's descent (0 abelian factors → 1–4, dimension strictly falling)
matches the breaking order of E₆/SO(10)-type cosmologies edge-for-edge, no inversion —
and does NOT match minimal SU(5), which has no U(1)-rich intermediate stage. The
conditional is honest twice over: (i) the match is model-relative among GUT roads —
stated plainly, the lens's demand; (ii) per the corpus's own banked row, minimal SU(5)
is EXCLUDED (proton decay) — so the surviving standard roads are exactly the U(1)-rich
ones the chain's shape matches. The PRE-E₆ layers are NOT-COMPARABLE, not unmatched:
they select which algebra plays the GUT role; they are not a physical era. And the
chain's own no-go (B952) stops its descent BEFORE cosmology's SM and EM-only stages —
the match is a bounded prefix. **An order-match is a consistency of shape, never a
mechanism claim** — the big-bang reading stays in THE FORCED AND THE FREE's register.

## Housekeeping caught by the cell

- The B909/B952 promotion-currency lag (LAW_MAP says ALL PAID; THEOREM_LEDGER's C-links
  don't yet carry them) — registered for the ledger's next pass.
- The 'motivated but not laws of nature' quote lives in THE_FRAMEWORK/UNIQUENESS_THEOREM,
  not CLAIMS C1 verbatim — attribution fixed here.

## Artifacts
- `b1082_results.json` (both agents verbatim, scrubbed).