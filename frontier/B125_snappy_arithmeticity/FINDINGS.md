# B125 — SnapPy arithmeticity of the metallic `R^m L^m` bundles (overturns K009) — V114

**A correction of a banked `SUPPORTED` claim by new computational evidence.** With SnapPy now runnable in-sandbox,
the invariant trace field `kM` of the metallic family is computable directly. The result **overturns K009**:
arithmeticity does **not** uniquely select `m=1`. It selects **{m=1 golden, m=2 silver}** and kills `m≥3`. MATH tier
only; physics untouched; nothing to `CLAIMS.md`; the functorial `Sym(W)→trace-ring` wall (B85) is not touched.

## §1 — the structural spine (pure numpy, the orientable metallic members are the `R^m L^m` bundles)

`M_m = [[m,1],[1,0]]`, `det=−1`. Its orientable square is a positive `LR`-word:
```
   M_m² = R^m L^m ,   R = [[1,1],[0,1]],  L = [[1,0],[1,1]],   tr(M_m²) = m²+2  (→ 3,6,11,18,27,38).
```
Verified exactly for `m=1..6`. For `m=1` this is `RL` = the figure-eight (`m004`). So the orientable metallic
members **are** exactly the `R^m L^m` once-punctured-torus bundles — built in SnapPy as `b++` + `R`*m + `L`*m, and
`m=1` reproduces `m004` (`vol 2.029883`) as a build sanity check.

## §2 — the result (verified in-sandbox, two independent ways)

The arithmeticity criterion (Maclachlan–Reid, *The Arithmetic of Hyperbolic 3-Manifolds*, Thm 8.3.2, non-cocompact):
> a non-cocompact finite-covolume Kleinian group is **arithmetic ⟺ `kM` is imaginary quadratic AND every trace is
> an algebraic integer.** (Cusped ⇒ the invariant quaternion algebra is automatically `M₂(kM)`; the parabolics split
> it, so there is no separate algebra condition.)

Computed both via the **shape field** (Neumann–Reid: the field generated by the ideal-tetrahedron shapes = `kM`) and
the **lift-independent traces-of-squares** field, SnapPy 3.3.2 + cypari 2.5.6:

| m | metal | `kM` | degree | imag. quadratic? | traces integral? | **verdict** |
|---|---|---|---|---|---|---|
| 1 | golden | `ℚ(√−3)` (`Φ₆`, shape `e^{iπ/3}`) | 2 | yes | yes | **ARITHMETIC** |
| 2 | silver | `ℚ(i)` (roots `1±i`) | 2 | yes | yes | **ARITHMETIC** *(new)* |
| 3 | bronze | degree ≥ 4 (canonical 6) | ≥4 | no | — | **non-arithmetic** |
| 4 | — | degree 4 | 4 | no | — | **non-arithmetic** |
| 5 | — | degree ≥ 4 (canonical 6) | ≥4 | no | — | **non-arithmetic** |
| 6 | — | degree ≥ 4 (canonical 6) | ≥4 | no | — | **non-arithmetic** |

`ℚ(√−3)` (ℤ[ω]) and `ℚ(i)` (ℤ[i]) have **distinct squarefree discriminants** → different Bianchi families → **not
commensurable**: two genuinely distinct arithmetic metallic manifolds. This closes cleanly — K009 already cited
"exactly **two** arithmetic once-punctured-torus bundles" (Maclachlan–Reid); they are exactly `m=1` and `m=2`.

## §3 — honest scope (verify-don't-trust applied to this handoff)

The **two arithmetic verdicts** (`m=1` `ℚ(√−3)`, `m=2` `ℚ(i)`) and the **non-arithmetic verdict for `m≥3`** (`kM`
not imaginary quadratic) reproduce robustly and precision-stably — both confirmed twice (shapes + traces-of-squares).
The **exact non-arithmetic field degree** for `m=3,5,6` is primitive-element/precision-sensitive in a numpy+cypari
reproduction (observed 4 or 8; the canonical Maclachlan–Reid value is 6) — it does **not** affect any verdict
(anything `> 2` rules out imaginary-quadratic). `m=4` is robustly degree 4 (clean integer minimal polynomial,
`16x⁴−596x³+33441x²−476018x−557618`). So we bank the **verdicts + the two fields**; we do not over-claim the exact
`m≥3` degrees. Status: **TESTED-POSITIVE** (the tooling gate is lifted).

## §4 — what is preserved (NOT corrected)

- **Reid 1991 — the figure-eight is the unique arithmetic *knot* — STANDS.** It concerns knot complements in `S³`;
  the `m≥2` metallic manifolds are once-punctured-torus **bundles**, generally not knot complements. `m=2`'s `R²L²`
  being arithmetic does **not** contradict Reid — it confirms the theorem's scope (`m=2` is necessarily not a knot,
  else a second arithmetic knot). The B123 error was applying a *knot* theorem to *bundles*.
- The **order-6 echo** (the `(0,0,0)` Jacobian `λ³+1` at `κ=−2`, also `ℚ(√−3)`) stays an **observation**, not a
  connection (unchanged from B123).

## §5 — the correction of K009

The "three independent `m=1` selection criteria" sub-claim is **retracted** and replaced with the honest version:
- **systole** (B92/S001): still uniquely selects `m=1`.
- **expansion threshold** (P004/B120): still uniquely selects `m=1`.
- **arithmeticity** (corrected here): a **two-element selector `{m=1, m=2}`** — *not* a unique `m=1` criterion; kills
  `m≥3`.

So `m=1` is the **most-selected** point (two unique criteria + arithmetic), but the clean "threefold convergence on
golden" was partly an artifact of applying a knot theorem to bundles.

**Anchors:** B123/V112 (the corrected claim + its audit caveat — the origin of the catch), B92/S001 (systole),
P004/B120 (expansion threshold), `knowledge/K002`, `K004`, `K009`. External: Maclachlan–Reid, *The Arithmetic of
Hyperbolic 3-Manifolds* (Thm 8.3.2); Bowditch–Maclachlan–Reid 1995 (**three** commensurability classes of arithmetic
once-punctured-torus bundles — "m=1,2 arithmetic" is family-restricted, *not* the global count; corrected by B126);
Reid 1991 (the unique arithmetic
knot); Neumann–Reid (shape field = invariant trace field). Tooling: SnapPy 3.3.2 + cypari 2.5.6, in-sandbox.
