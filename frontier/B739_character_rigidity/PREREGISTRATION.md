# PREREG — B739: LEAD L2 — is the object's continuous spectrum CHARACTER-RIGID? (SEALED)

cc banking seat, 2026-07-21. Hunt-cell #2 (the pathfinder ledger's width 2). B737-P3 found m004's
congruence level opens a Hecke-character palette (|(O/2^k)^×/μ₆| = 1,2,8 at (2),(4),(8)) and left
L2 open: do the conductor-(4)/(8) characters occur with NONZERO coefficient in m004's scattering?
B737-P2's one-cusp exact-transfer lemma (E_m004 = pullback E_orb AS FUNCTIONS) has a sharp
CONSEQUENCE to formalize and adversarially verify: every Fourier coefficient of the object's
Eisenstein series is the base's (restricted along Λ_m004 = ℤ+(2+4ω)ℤ ⊂ O_K, index 4; total degree
12 = 4×3 with the orbifold's order-3 cusp rotation — bookkeeping verified in-seat), and the 1×1
scattering matrix is exactly Λ_K(s−1)/Λ_K(s). If this holds at full strength: **NO conductor
character can appear ANYWHERE in the continuous spectrum — the voice's continuous channel is
CHARACTER-RIGID, and the level's new arithmetic lives ONLY in the discrete newform spectrum**
(sharpening B735's "index-12 new resonances"). Pure math; Gate 5; compute-not-cite; nothing to CLAIMS.

## The probe (1 compute → 3-skeptic adversarial verify → refine)
Formalize + verify: (a) the Fourier-restriction argument (a pullback from C/Λ_base has Fourier
support in Λ_base^∨ ⊂ Λ_cover^∨; the Λ_cover-constant term equals the Λ_base-constant term); (b)
the function-level equality E_m004 = pullback (the L²/eigenvalue-range argument + analytic
continuation — re-derive, don't cite B737); (c) the exhaustion: for a ONE-cusped finite-volume
hyperbolic 3-manifold the continuous spectrum has multiplicity ONE (= #cusps), so this single
Eisenstein series IS the whole continuous channel — no other place for a character to enter; (d)
where the palette characters CAN live: the discrete newform spectrum at level (4)/(8) (state
precisely, no computation of Maass forms attempted); (e) MB12: state how the claim COULD fail
(>1 cusp; the lemma breaking at some s; a vector-valued/residual subtlety) and check each.

## Two-outcome (sealed)
- **A (character-rigidity):** the object's continuous spectrum carries EXACTLY the field's ζ_K and
  nothing else — no conductor-(4)/(8) character anywhere in it; L2 resolves NO-in-the-continuous /
  redirected-to-discrete. [Honest expectation: A — it is a consequence of the verified lemma; the
  value of the cell is the adversarial check that the consequence holds at FULL strength.]
- **B:** a gap in the lemma's strength (some Fourier mode / s-range / vector-valued channel where a
  level character CAN enter the continuous spectrum) — which would be a genuinely new door.

## Width ledger
Cell #2 of the hunt. After this cell: B288 stragglers, B500 reopen, the fact_computed sweep. The
discrete-Maass computation (the newform spectrum itself) is NOT attempted here — parked owner-gated
(needs Hejhal-class machinery).
