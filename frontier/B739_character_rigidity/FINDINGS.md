# FINDINGS — B739: the object's continuous spectrum is CHARACTER-RIGID (OUTCOME A, 0/3 refuted)

cc banking seat, 2026-07-21. Prereg sealed `62922f0c`. Hunt-cell #2 (wf_e7536900: 1 compute →
3-skeptic adversarial verify; 54/54 checks; all three skeptics re-ran AND re-derived independently
— 0/3 refuted). Pure math; Gate 5; nothing to CLAIMS. Resolves lead L2 from B737.

## THE RESULT (weight-0 scalar scope)
**The continuous spectrum of m004 is a single channel ([1,∞), multiplicity exactly 1 = #cusps),
furnished entirely by the one Eisenstein series — which equals the pullback of the Bianchi-orbifold
Eisenstein series AS FUNCTIONS. Hence every Fourier coefficient of the continuous channel is
restricted from the level-one base, the 1×1 scattering is φ_m004(s) = Λ_K(s−1)/Λ_K(s) exactly, and
NO conductor-(4)/(8) Hecke character (the palette 1/2/8 opened by m004's congruence level) appears
ANYWHERE in the continuous spectrum.** The level's new arithmetic lives ONLY in the discrete
newform spectrum — B735's "index-12 new resonances," now given an exact home. L2 = NO-in-the-
continuous, redirected-to-discrete.

## The proof structure (per prereg items, proof-status honest)
- **(a) Fourier restriction — PROVEN-IN-SANDBOX.** The coset character sum over O/Λ ≅ ℤ/4
  (Λ = ℤ+2√−3ℤ = ℤ+(2+4ω)ℤ, proved equal; index 4) equals 4·[μ ∈ O^∨] EXACTLY (symbolic on all
  four dual classes + a 75-coefficient exact-DFT battery, max err < 1e-24, including the
  ℤ/3-symmetrized case). The pullback's Fourier support cannot leave O^∨ — every mode where a level
  character could enter is closed.
- **(b) Function-level equality — PROVEN modulo 3 NAMED classical inputs** (S1 meromorphic
  continuation, S2 Gaffney essential self-adjointness, S3 moderate growth — cited, flagged, not
  re-proven). Both kill-zones re-derived from scratch (s>2 absolute convergence; 1<s<2 via a
  self-contained countable-point-spectrum argument), constant-mode + Bessel K/I radial equations
  symbolic + numeric to 1e-22, global L², then the identity theorem.
- **(c) Multiplicity-one exhaustion — SOURCE-VERIFIED** at two independently fetched primary
  documents (Sarnak Acta 151 p.264 verbatim; Friedman math/0702030 Lemma 2.1/eq 2.6 resting on
  EGM ch.3) + #cusps(m004)=1 computed. The single Eisenstein series IS the whole continuous channel.
- **(e) Failure modes computed shut**, including the planted residue puzzle: Res φ = 2π²/(9ζ_K(2))
  = (√3/6)/vol_orb = 2√3/vol(m004) to 40 digits — **consistent because covol(cusp)/vol(M) is
  COVERING-INVARIANT** (both scale by the degree 12 = 4×3; skeptic-1's independent resolution) —
  and logically redundant anyway (the identity theorem already forces it). The cusp ROTATION acts
  on the base side only (composite covering handled; R(Λ)≠Λ exact; symmetrized battery passes).
  The vector-valued escape closes at the trivial character (V_α = V always ⇒ k_α = 1).

## Honest bounds
Scalar weight-0 Laplacian only (vector-valued/forms = a different question, nothing claimed); the
three classical inputs are cited not re-proven; no numerical E_m004 exists (Hejhal-class machinery,
owner-gated); the B734 covering data is re-verified only in its computable shadows (Humbert 12,
cusp modulus 2√−3, |Isom|=8). Run-1 failed on a tooling bug before any claim-bearing check
(transcript preserved: b739_probe_out_run1_FAILED.txt).

## What this adds to the record
(i) A clean theorem-grade rigidity: **the object's voice is exactly the field's ζ_K-quotient and
can carry nothing else in its continuous part** — the B737 positives sharpened into a closed
statement; (ii) the discrete newform spectrum is now the UNIQUE home of the level's arithmetic —
the sharpest remaining spectral question (owner-gated, Hejhal-class); (iii) hunt-ledger: cell #2
CLOSED (width 2); next cells B288 stragglers → B500 reopen → the fact_computed sweep. Credit: the
skeptics (three independent re-derivations, incl. the covering-invariance resolution).
