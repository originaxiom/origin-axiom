# L156 — PREREGISTRATION. Does the resolution bound have a domain, and what is in it?

**cc3, 2026-08-11. Sealed BEFORE any search. The owner holds the hash.**
**Gate 5-Q: no measured quantity is a target here. This crosses the object against
itself.**

---

## ⚠ CONFLICT DECLARED FIRST, AND THE CONVENIENT ANSWER NAMED

**cc3 tonight produced R13** — the rule that no crossing may report a margin
without the quantized base rate beside it — and **R13's numbers depend entirely on
the forced set being exactly the 8-element θ-even set.** If the set is bigger, the
base rates fall and R13 weakens. If the set's **domain** is narrow, R13 does not
apply to most SM targets at all and the screen cc3 just built is largely inert.

> **The answer convenient to cc3 is: the bound has a BROAD domain covering the
> object's forced dimensionless outputs generally.** That is the answer that makes
> tonight's work load-bearing. **It must therefore clear the higher bar.** If any
> clause below looks tuned to favour BROAD, reject it.

## The claim under test

**chat1's resolution bound:** any value the object **forces** is
`Re χ_V(M) / dim V` — a **normalized character value** of a finite group. B1011
instantiates this as the θ-even set

`{0, 1/(4φ), 1/4, 1/(2φ), φ/4, 1/2, φ/2, 1}` = `{0, .1545, .25, .309, .4045, .5, .809, 1}`

## THE PRIOR, AND IT IS DECLARED AS A REFUTATION

**cc3's prior is NOT "the bound holds."** cc3 has already located, without
searching, one forced dimensionless value that is **not in the set**:

> **`c = 6`.** B1012 derives `c = 6σ`; B254 gives `16/5 + 14/5 = 6`;
> `c((E₆)₁) = 78/13 = 6`. **Dimensionless. Forced. Not in [0,1], not in the set.**

**So the bound as stated is already false on its face for `c`, and the only
question is whether `c` is IN ITS DOMAIN.** Normalized character values lie in
`[-1,1]` by character theory; `c` is a central charge. **These are different kinds
of quantity.**

## THE DICHOTOMY, PRE-NAMED — BOTH BRANCHES COST cc3

| verdict | what it means | consequence for R13 |
|---|---|---|
| **NARROW** — the domain is normalized character values only | the bound is **near-tautological**: it says character values are character values. **chat-2's M11 vacuity shape.** | **R13's screen applies only to targets in that domain.** Most SM parameters are not obviously in it. **Tonight's table would need its domain stated per row, and rows outside the domain are unscreened.** |
| **BROAD** — the domain is the object's forced dimensionless outputs generally | **REFUTED by `c = 6`** unless the bound is amended | R13 survives only with the amended set, **and the base rates must be recomputed against it** |
| **MIXED** — the corpus has no consistent domain statement | the bound is **UNDER-SPECIFIED** and was consumed tonight without one | **R13 is withdrawn pending a domain** |

> **There is no branch on which cc3's evening survives intact.** That is the point
> of sealing it before looking.

---

## WHAT WILL BE SEARCHED, FIXED NOW

**Search 1 — the bound's own statement.** Locate every banked statement of the
resolution bound. **Record whether any states a domain.** If none does, that is
MIXED, and it is recorded as such **regardless of how natural a domain looks in
hindsight.**

**Search 2 — the census.** Enumerate every **forced dimensionless** value banked in
the corpus. Inclusion rule, fixed now and not revisable after looking:

- **IN:** the value is dimensionless, and its arc's claim line says it is
  **forced / derived / determined** by the object (not measured, not assumed, not
  an anchor).
- **OUT:** dimensionful (carries the length weight or any ledger weight ≠ 0);
  an input/anchor; a value the arc itself fences as conjectural.
- **Weight-ledger tie-break:** the ledger is the arbiter of dimensionless-ness
  (length +1, area +2, volume +3, Laplace −2, entropy −1; traces, Galois, CS,
  torsion, cusp shape, level all weight 0).

**Search 3 — the test.** For each censused value, is it in the 8-set?

- **Any value forced, dimensionless, in-domain, and NOT in the set ⟹ REFUTED.**
- **All in ⟹ CONFIRMED,** and the count `n` is reported, because **n is the bits.**
- **Domain ambiguity on any value ⟹ that value is reported as AMBIGUOUS, never
  quietly excluded.** The count of ambiguous values is part of the verdict.

## NO-SMOOTHING CLAUSES

1. **No post-hoc domain.** If the corpus does not state a domain, cc3 **may not
   supply one** and call the bound narrow. That is MIXED.
2. **`c` is reported explicitly on every branch**, with the domain question stated,
   whatever the other values do.
3. **Absence discipline (WORKING_RULES §0).** No sentence of the form "the corpus
   does not contain X." Only "search S, run as specified, returned no match."
   **cc3 has broken this four times and it is the standing failure mode.**
4. **The census is reported whole.** No selection of favourable values. Choosing
   after looking is E29 post-hoc selection.
5. **Quotation carries its path.** No quoting from a filename-suppressed grep —
   one flag character produced three misattributions in this campaign.

## WHAT THIS DOES NOT TEST

- Whether the 8 values are **correctly derived** (taken from B1011 as banked).
- Whether **other channels** have other sets (R10/B1016 — channel must be declared).
- Any Standard Model quantity. **Nothing here contacts measured data.**

## WHO RUNS IT

**cc3 runs the searches. The owner holds this seal and verifies the hash before
the first search and after the report.** cc3 cannot amend this file after the
hash is handed over; an amended file has a different hash and is a different cell.

**Seal algorithm: SHA-256 over the bytes of this file, as committed, excluding
nothing. Computed by `shasum -a 256`.**
