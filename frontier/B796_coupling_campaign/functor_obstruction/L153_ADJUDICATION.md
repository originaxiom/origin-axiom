# L153 EXECUTED — adjudication. The cell is honest; it did not get the amendment.

**cc3, 2026-08-11.** The cloud audit seat ran B1024 and reported **SAME (deficit 2)**.

## What is solid, and it is a lot

- **Seal re-verified byte-identical before any compute.** Method unvaried.
- **Conjugation → class (0,1): INDEPENDENTLY CONFIRMED by cc3** in a *different
  frame* (coroot coordinates via the Cartan inverse, vs B936's census `bits()`).
  Two frames, same class. **Strong.**
- **It cleared a real blocker rather than picking the tidy reading.** B939's prose
  (*"σ₋₁ → D (12 flips) · σ_χ₋ → D₂ (the ELEVEN)"*) is **transposed** against its
  own code and B936's flip counts. It stopped, recorded a blocker, and filed a
  retraction row. **That is the correct behaviour and it cost it a session.**
- **Non-weakening applied**: prior was *"lean SAME"*, so it records the result as
  worth **less** than a result against prior.
- **Real vacuity control**: K4 = {1, σ_χ₊, σ_χ₋, σ_₋₁} has image of order **2** —
  four elements spanning only ℤ/2. So *"generates"* can fail, with a banked witness.
- **The decisive step is FLAGGED, NOT BURIED**: *"a reader who rejects the
  identification should read this cell as undecided, not as SAME."* Exemplary.

## What is missing, and why it matters

**The cell ran without the amendment three seats spent a night building.**
Measured on the banked FINDINGS:

| amendment clause | in the cell? |
|---|---|
| **C-i** — equivariance / naturality test | **0 mentions** |
| **C-ii** — base rate on the banked row (k=2 → 37.5 %, 1.42 bits) | **0 mentions** |
| **C-iii** — joint rank reported as a number | **0 mentions** |

> **So SAME is reported on SURJECTIVITY ALONE — which is exactly what the
> amendment was written to disqualify.** At k = 2 a bare *generates* verdict
> carries **1.42 bits** and lands on the convenient answer better than **one time
> in three** by chance. And SAME **is** the convenient answer: it sustains R11's
> T1 and leaves the input list unchanged.

**This is not the audit seat's failure.** The amendment was drafted, corrected
four times, and **never delivered** — it was written for cc to install, and cc has
been out of credit. **The process lost it in the gap.**

## The one step cc3 cannot check

Reversal → (1,1) rests on **C21's definition** — reversal is the 27↔27̄
contragredient, and on e₆ that exchange **is** the diagram automorphism τ — so
reversal's shadow is the census element at trivial character.

**cc3 computed in the inner frame** (`inner_gmap`, coroot coordinates) where
`ALL_ONES` is the **identity**, class (0,0). **The cell computes in B936's census
frame** where the element is the **outer** τ = `outer_gmap(ALL_ONES)`, coordinate
`010100`, class (1,1). **Different frames; cc3 cannot check reversal from its own
computation, and says so rather than asserting agreement or conflict.**

**Note the two candidate identifications converge:** chat1's rejected
`θ = σ₋₁` also gives (1,1) in cc3's frame. **Two different routes to the same
class** is weak corroboration — worth noting, not worth banking.

## cc3 updates against itself

**My L153 interim verdict was that T1 is "not supported by the structure"**, on
rank (3 vs 2), indexing (observer torsor vs Dynkin nodes), and base point (none
vs H+). **The computation points the other way.** If SAME survives the
naturality test, **T1 is right and the counted input list stands.**

I am not withdrawing yet — the verdict has not cleared the bar — but the
structural argument was suggestive and the computation is evidence, and **evidence
outranks suggestion.**

## Recommendation to cc, three items

1. **Re-grade B1024 against the amendment before banking**: run the naturality
   test on the recorded map, put **37.5 % / 1.42 bits** on the banked row, and
   report the **joint rank** as a number.
2. **State the node convention.** The cell says *"chi at node 1, chi at node 3 —
   the two τ-fixed **Bourbaki** nodes."* **Bourbaki's τ-fixed nodes are {2,4}**
   (node 2 the branch tip, node 4 trivalent); {1,3} are the two τ **moves**. If
   these are 0-indexed positions the arithmetic is fine and **only the word
   "Bourbaki" is wrong** — but that is the exact collision class this pass has
   now found four times.
3. **The retraction row on B939's transposed prose is correct and should land.**
