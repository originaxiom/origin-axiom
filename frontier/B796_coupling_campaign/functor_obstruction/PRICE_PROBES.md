# THREE PROBES AGAINST THE PRICE — all terminate UNRUN

**cc3, 2026-08-11.** The owner states the full argument's price as
**one unit, two bits, one orbit-point**. cc3 attempted to disprove it.

| probe | direction | result |
|---|---|---|
| **1 — a fourth input** | up, to four | **FAILS.** Rested on B987's *"the split is an external input, **exactly like** chirality, rank reduction and scale"* read as *in addition to*. On a careful read **"exactly like" is about KIND, not COUNT.** And whether the split and rank reduction are **one act or two** is **UNRUN** — verified: **no arc links the VEV direction to the colour choice, in either direction.** |
| **2 — the bits ARE the orbit labels** | down, to two | **UNRUN.** No arc anywhere relates the closing bits to an orbit label. chat1 proposed it as candidate (c); nobody ran it. |
| **3 — is the orbit set finite** | changes the third item's type | **UNRUN** for F₄(ℤ) on integral frames. B710 carries *"Mod-conjugation orbit count: fig-8 = 1"* — **a different object**; conflating them is the error this corpus has made three times (two conductors, two levels, two E₆'s). Flagged, not used. |

## The finding is the status, not a verdict

**cc3 could not disprove the price — and not because it withstood test. Because
all three deciding questions have never been run.**

> **The price is currently UNADJUDICABLE from the corpus.** That is a third status,
> distinct from *verified* and from *wrong*.

## The three questions, named and bounded

1. **Are the split and rank reduction one act or two?** Does the VEV direction in
   the 27 fix which SU(3) is colour? Structurally it should — a VEV in one 9-block
   picks that block — **but no arc says so.** Cheapest, and it decides probe 1.
2. **Are the closing bits the orbit labels?** If yes the price is **one unit, two
   bits** and the third item is free.
3. **Is the F₄(ℤ) orbit set on the split form finite?** Decides whether
   *"one orbit-point"* is a label or a continuum. Literature: Krutelevich /
   small orbits, LOCATED-NOT-READ.

## cc3's own conduct on this probe, recorded

**The attack was built on an over-reading and withdrawn on a careful reading of
the same sentence.** Fourth over-reach of the session, and the first inside
adversarial work rather than summary. **The probes are worth more than the verdict
was.**
