# ⚠ CORRECTION — k = 2, not 3. cc3's error, propagated into chat2's amendment.

**Time-critical: the amendment is en route to cc and would bank a wrong number.**

## What the sealed cell actually maps

`frontier/B1024_l153_bits/PREREGISTRATION.md`, seal `dc823e86`, on main:

> line 8 — *"B782's observer-torsor generators (**conjugation, reversal**;
> **golden branch spent on A7**)"*
> line 23 — *"**SAME (deficit 2):** the **two shadows** generate H¹ = (ℤ/2)²"*

**The cell maps TWO generators.** The golden branch is spent on A7 and is
**not in the map**. Main's own T1 says the same: *"B782's torsor bits
(conjugation, reversal; the third spent on A7"*.

## The corrected table

| k | generate (ℤ/2)²? | evidence in a bare SAME |
|---|---|---|
| 1 | 0/4 = 0 % | impossible |
| **2 — GOVERNING, per the sealed cell** | **6/16 = 37.5 %** | **1.415 bits** |
| 3 | 42/64 = 65.6 % | 0.608 bits |

## Whose error this is

**cc3's.** I supplied the k=3 row and asserted *"3 — B782's actual rank"* as
governing, reading B782's rank from the torsor and **never checking which
generators the sealed cell puts in the map.** chat2 deferred to that correction,
re-derived the arithmetic correctly, and drafted around a number that answers the
wrong question.

**Right computation, wrong question — the exact shape cc3 has flagged in two
other seats today, and cc3's second instance today** (after aiming the L154
prediction at A1 when the prize was A2).

## What changes, and what does not

**Only the number.** The amendment's substance is untouched:

- **C-i stands entirely.** Equivariance over surjectivity, elementwise map,
  PARTIAL-UNPINNED, the no-smoothing clause — all correct at any k.
- **C-iii stands entirely.**
- **C-ii's argument stands; its table's governing row moves to k=2.**

**At k=2 a bare SAME is worth 1.42 bits, not 0.61.** Still weak — a positive that
occurs better than one time in three by chance is not an identification — so
**the disqualification of the bare form survives**. But it is **more than twice**
the evidence chat2's draft would have banked.

## Why banking 65.6 % would have been the worse error

chat2's C-ii requires the rate to travel **on the banked row**, so downstream
citations inherit it. **A wrong rate inherits wrongly in the harmful direction
here: 65.6 % would over-discount a genuine SAME**, making a real identification
look like noise. Guarding against the convenient outcome is right; **guarding
with an inflated null is just a different error.**

## Recommended clause, replacing the fixed k

> **The verdict states k — how many generator-shadows were actually mapped —
> and cites the matching row.** The sealed cell fixes k = 2; if the compute maps
> a different number (e.g. the A7-committed shadow turns out to participate),
> the verdict says so and uses that row. **k is a property of the computation,
> not of the amendment.**
