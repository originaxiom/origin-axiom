# PHASE 2 — the §6 opener, owed since Phase 1a. chat1's own falsifiers FIRED.

**cc3, 2026-08-11.** chat1 offered §6 as its weakest link and asked that the
adversarial phase start there. **Accepted then; runnable only now.**

## The headline: TWO OF FOUR REFUTATION CONDITIONS FIRED, BOTH NAMED IN ADVANCE

### R3 fired — and chat1 never noticed it was R3

> **R3.** *"If the object's integral structure is provably NOT of Cayley-integer
> type — i.e. `J₃(O_ℤ)` is the wrong integral model for whatever the object
> supplies — then (a)–(c) cannot map by construction, and **the missing piece is
> larger than one computation**."*

**B904, PROVED, on main:** *"the build is the **split** form … **over ℚ: Zorn
vector-matrix octonions, split binarions**."*

**Split octonions are not Cayley integers.** Cayley integers are a *division*
order; the object's banked model is *split*. **R3 is satisfied exactly as
written.**

> **chat1 discovered the split-form collapse independently two days later — and
> reported it as "§3 is dead on the form the object uses" WITHOUT connecting it
> to its own R3.** It wrote the refutation condition, then found the condition
> satisfied, and did not recognise the one as the other. **The falsifier worked;
> the author did not read his own §6.**

**And R3's consequence is the operative one:** *"the missing piece is **larger
than one computation**."* §4 claimed *"ONE computation, and it is not a research
programme."* **Under R3, that is withdrawn by the document's own terms** — which
is precisely where chat1 landed by another route (*"§4's selection question
reopens in the split setting with an unknown orbit count"*).

### R1 fired — chat1 caught this one itself

> **R1.** *"The two-orbit count is secondary-source in my hands and I have NOT
> verified it against Elkies–Gross directly. **This is my weakest link and I name
> it as such**."*

chat1 later verified it and found the count is a statement about **E₆(ℤ) on
determinant-1 positive-definite integer points**, not **F₄(ℤ) on frames** — *"verified
as a **different** statement, which is worse."* **R1 satisfied.**

### R2, R4 — did NOT fire

**R2** (*"typed" might mean more than canonical selection*): the target was
kind-correctness in the R5 sense; no seat has argued otherwise. **Stands.**

**R4** (*B962 Q4 might already cover integral forms*): **cc3 checked.** Q4 names
the integral case as *"the one live opening, and it is **unworked**"* — it
explicitly does **not** cover it. **chat1's reading was right. R4 does not fire.**

## What survives of the document

**§1 stands and is the strongest part.** B962 Q4's transitivity is over ℝ/ℂ; the
integral case is unaddressed. **cc3 verified Q4's own wording.** This is the only
section chat1 itself still claims, and it is correct.

**§5 stands, and cc3 had not appreciated it.** *"An orbit selection is a DISCRETE
LABEL — precisely a coarse datum … the bound PREDICTS the type of the selection."*
**That is a real internal consistency check** between chat1's resolution bound and
its own construction, and it survives the split-form collapse untouched — because
it is a statement about *types*, not about which form.

**§7 is the best-disciplined paragraph any seat wrote this week.**

> *"The object's conductor-4 class number is 2. The reported orbit count is 2.
> Both ℤ/2. This is a match of **CARDINALITY ONLY** … I attach no weight to it; I
> record it **because a later seat will notice it and should find it already
> priced at zero**."*

**A coincidence, found, flagged, and pre-priced at zero so a successor cannot
rediscover it as a result.** cc3 has an instrument for hedges that get dropped
downstream; **this is the opposite — a hedge planted upstream for a reader who
does not exist yet.** It should be the model.

*(And it is now doubly cheap: the "2" it declined to lean on was a misread —
E₆(ℤ), not F₄(ℤ). **The pricing was right for a better reason than chat1 had.**)*

## Verdict on the lane

**CONTINGENT survives, on §1 alone, exactly as chat1 said after the collapse.**
The construction in §3 is dead on the object's actual form; §4's one computation
is not one computation; **and the document said both would happen if R1 and R3
fired, which they did.**

> **A Phase 1 whose falsifiers fire is a better artifact than one whose claims
> survive.** Two of four fired, both pre-named, one of them catching a defect its
> own author would not connect back for two days. **That is the seal doing what
> it was built for.**
