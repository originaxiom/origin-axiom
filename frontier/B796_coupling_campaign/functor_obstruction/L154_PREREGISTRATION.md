# L154 — PREREGISTRATION (proposed). The cell with the prize and no verdict form.

**Drafted by cc3, 2026-08-11, for cc to seal or reject. NOT a seal — cc3 does not
seal a cell it has a prediction on.**

---

## ⚠ CONFLICT DECLARED FIRST

**cc3 has a registered prediction on this cell**: *the two σ's ARE one, so
**A2 discharges** and the input list shrinks* — **the prize-paying answer.**

**Every clause below makes that answer HARDER to claim.** That is the intended
handling, and it is stated so cc can check the guard is not self-serving. **If any
clause looks tuned to help SAME, reject it.**

## The question

> **Is the σ in `c = 6σ` (R4 / S2, Brown–Henneaux) the same σ as in B1012's
> Gukov split `t = k + iσ`?**

## The prize, and therefore the hazard

**B1015** declares **`A2 = c = 6σ`** — *"the one continuous dimensionless external
coupling."* If the two σ's are one, then `c = 6` is derived object-internally
(`c((E₆)₁) = 78/13 = 6`, independently `B254: 16/5 + 14/5 = 6`), **A2 is not
external, and the input list shrinks by one.**

> **SAME is the convenient answer. It shortens the declared input list — the
> number the programme's headline is built on. That is exactly the configuration
> L153's amendment was written for, and L154 currently has no guard at all.**

---

## D-i — THE NULL. The corpus prior points DIFFERENT, and it must be beaten.

**One symbol denoting two objects is a documented, recurrent failure in this
corpus. Instances cc3 has personally verified:**

| # | instance | status |
|---|---|---|
| 1 | **B265**: *"the **input-E₆** (chosen 6d type) vs **output-McKay-E₆** (trace field) selection is still **a conjecture**"* | **verified** — read on main |
| 2 | **L110**: *"number collision — two distinct leads"* | **verified** — read in the leads triage |
| 3 | **B62 = 2 × P33**, neither declaring its convention | **verified** — printed on cc3's bench running B1026's `verify.py` |

**Reported by other seats, NOT verified by cc3:** θ naming three distinct objects;
"conductor" and "level" collisions registered by B1013; B939's flip-count
annotation.

> **B265 IS THE PRECEDENT AND IT IS EXACT.** Two objects sharing a name, whose
> coincidence was **registered as a conjecture** rather than assumed — **and it
> has stood unresolved since June without anyone noticing it was load-bearing.**
> **L154 is the same shape with a prize attached.**

**Required of the verdict:** the banked row states the prior as **DIFFERENT**, and
a SAME verdict must say **what beat it.**

## D-ii — THE IDENTIFICATION MUST BE EXHIBITED, NOT INFERRED FROM AGREEMENT

**Both quantities equal 6. That is not evidence.** Brown–Henneaux's
`c = 3ℓ/2G` and the level-1 E₆ WZW `c = 6` are **a priori different objects**;
numerical agreement is exactly what a symbol collision looks like from outside.

**Required, on every branch:**

1. **Exhibit the map.** Name the construction carrying B1012's `σ` (the imaginary
   part of the Gukov level `t = k + iσ`) to R4's `σ` in `c = 6σ`. **A citation
   that both are called σ is not a map.**
2. **State what would distinguish them** — a quantity the two σ's assign different
   values to — **and then show it doesn't.** A verdict that cannot name a
   discriminant has not tested identity; it has tested agreement.
3. **Symmetric negative form.** If they differ, record **where** the map breaks —
   that feeds D-iii.

**No-smoothing clause:** if the map is not exhibited, the verdict is
**AGREEMENT-UNPINNED**, never SAME. **Numerical coincidence is not identity, and
this corpus has three verified instances proving it.**

## D-iii — REPORT THE CONSEQUENCE AS A NUMBER, AND BOTH ANCHORS SEPARATELY

**The deliverable is the input count, and A1 and A2 must be reported apart:**

| | A2 = c (dimensionless) | A1 = ℓ (dimensionful) |
|---|---|---|
| **SAME** | **discharges** | **does NOT** — σ is dimensionless and supplies no length |
| **DIFFERENT** | stands | stands |

**Note `c_BH = c_{(E₆)₁}` fixes `3ℓ/2G₃ = 6`, i.e. `ℓ = 4G₃` — a RATIO.** It fixes
`ℓ/G₃`, **never `ℓ`.** A verdict claiming SAME **must not** be read as discharging
A1.

## Scope

**This adds criteria to a verdict form. It fixes no outcome, no prior, and no
computation.** If any clause would alter the cell rather than sharpen its reading,
**reject the clause and run the cell as cc seals it** — an amendment that changes
the cell is not an amendment.

**Who runs it:** cc or chat1. **Not cc3** — the conflict declared at the head.

**Establishes-no-conventions clause** (chat-2's, adopted): where a banked artifact
does not fix a notation, this document uses a convention-free description and
asserts no numbering.
