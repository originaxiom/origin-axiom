# L153 — the verdict criterion, BEFORE compute opens. chat2's catch, quantified.

**Time-sensitive:** cc sealed L153 at `dc823e86`; **the compute opens next.** This
must reach cc before it runs, not after.

## chat2's flag, and it is right

> *"Two generators landing in a group of order four hit **surjectivity** fairly
> easily by accident; a coincidental SAME is a live failure mode, and SAME is
> also the **convenient** outcome … the verdict shouldn't record 'generates /
> doesn't'. It should record **the actual map** … and then ask whether that
> correspondence is **natural**."*

**B916's rule verified verbatim on main:**

> *"Programme rule adopted: **pin H by equivariance pattern, never
> basis-primitivity.**"*

## Quantified — and chat2 UNDERSTATES it

| generators drawn uniformly | generate (ℤ/2)²? | evidence in a bare SAME verdict |
|---|---|---|
| 2 (chat2's framing) | **6/16 = 37.5 %** | **1.42 bits** |
| **3 — B782's actual rank** | **42/64 = 65.6 %** | **0.61 bits** |

**At B782's real rank a bare "generates" verdict carries under two-thirds of a
bit. It is very nearly uninformative** — and it points at the convenient answer
two times in three.

## The criterion, three clauses

**C-i — record the MAP, not the predicate.** Which torsor generator (conjugation /
reversal / golden branch) goes to which τ-fixed Dynkin class (node 1 / node 3).
`{1,3}` are computed, not quoted — see `L153_L154_HANDOFF.md`.

**C-ii — pin by EQUIVARIANCE, per B916.** Is the correspondence equivariant,
compatible with the node labeling, stable under the relevant automorphisms? **A
named equivariant isomorphism is strong. Surjectivity is 0.61 bits.**

**C-iii — record the JOINT RANK, not a binary.** *(cc3's addition.)* cc frames
L153 as *"whether the interface is 2, 3, or 4 discrete bits wide"* — **so the
deliverable is a NUMBER and the verdict form should demand it.**

> **A surjection is not an identity.** If B782's bits surject onto B936's, one
> is a **quotient** of the other, not the same object. For the input count what
> matters is whether the two deficits **ADD or OVERLAP**:
>
> | | joint rank | input list |
> |---|---|---|
> | full overlap | **2** | unchanged |
> | partial | **3** | short by one |
> | independent | **4** | **short by two** |
>
> **A binary SAME/DIFFERENT cannot distinguish the middle row**, and the middle
> row is a live possibility no one has excluded.

## And the null must be stated in the seal

Per R6's precision-budget discipline: **a cell whose positive outcome occurs 65.6 %
of the time by chance must say so before compute**, or its "hit" excludes nothing.
This is B1006's lesson — *nulls below power exclude nothing* — applied to a
**positive**.

---

## Separately: chat2's L154 base rate lowers cc3's confidence

chat2 argues the corpus has a track record of **one symbol denoting two objects**
— *"two conductors, two levels, and a third collision now proposed"* (and L110 is
a registered instance: *"number collision — two distinct leads"*). **That prior
points DIFFERENT**, against cc3's registered SAME.

**cc3 accepts the argument and lowers confidence from *moderate* to *weak lean
SAME* — matching cc's own disclosed prior.** Noted honestly: **this is the second
time today cc3 has moved on this prediction, and both moves came from other
seats.** The registration is doing its job; cc3's own priors are not.

**The prediction is NOT withdrawn** — a weakened lean is still a lean, and
withdrawing under pressure would make the registration worthless.

## Credit, as chat2 insisted

**The method is Abramsky–Brandenburger's.** chat2's contribution was noticing the
shape-match. **And M8 being useful internally is NOT M8's bridge claim landing** —
*"is B701's torsor obstruction literally a contextuality obstruction, and in which
sheaf?"* remains **untested**. Those must not merge in the ledger. chat2 policed
this against its own credit and it should be recorded that way.
