# Phase 2 — cc3 vs chat1's integral-frame result

**VERIFIED CORRECT.** `phase2_frames.py`-equivalent run: the identity
`(e²)₁₁ = a² + N(z) + N(y)` confirmed on 300 random integral Hermitian
matrices with explicit Cayley–Dickson octonions (and `zz̄`, `ȳy` verified
**pure scalars**, which the argument requires); `a(a−1) ≤ 0` over ℤ gives
exactly `{0,1}`; three primitive idempotents; **exactly one frame**.

**The argument is valid. Both attacks below are on its HYPOTHESIS and its
RELEVANCE, not its logic.**

---

## Attack 1 — the hypothesis has not had the scrutiny the conclusion just got

Everything rests on *"the Elkies–Gross integral structure (integer diagonal,
Cayley-integer off-diagonal)."* The argument needs **two** properties:

1. the diagonal is **ℤ**, not ½ℤ — if `a = ½` were admissible then
   `a(a−1) = −¼` and the forcing step fails at the first move
2. the off-diagonal norm form is **integer-valued and positive definite**

Both hold for J₃(𝕆_ℤ) with Coxeter octavians. **But chat1 misread Elkies–Gross
once today already**, and its own diagnosis of that error was
*"verified as a **different** statement, which is worse."* **The hypothesis is
now carrying the whole result and has had none of the checking the conclusion
received.** Not asserted as wrong — flagged as unverified in exactly the place
the previous failure occurred.

---

## Attack 2 — THE RELEVANCE GAP, and it is the same error shape

**Nothing connects the OBJECT to the integral structure.**

chat1 has shown: *over ℤ, the Jordan frame is canonical and unique.* True, and
now verified. But the question was whether **the object's arithmetic** selects a
VEV direction. That requires the object's data to **live in J₃(𝕆_ℤ)** — and
nothing in the argument establishes it. m004's arithmetic is **Eisenstein**,
ℚ(√−3), trace field of the figure-eight; the Cayley integers are an **E₈**
structure. **No arc supplies the bridge.**

Without it, *"over ℤ the frame is canonical"* is **true and irrelevant** to the
VEV question — which is precisely the shape chat1 named an hour ago:

> *"a source that supports something **adjacent** to what I claimed, read as
> supporting the claim."*

**Third instance today, same shape, and this one is the seat's own new result
rather than a citation.** That is worth more than the finding itself.

---

## Interaction with cc3's C4 — flagged, NOT asserted

If the frame deficit were **zero** over ℤ, that would **strengthen** cc3's C4
candidate defect: R11 licenses a *continuous* dimensionless anchor on theorems
that establish a *discrete* deficit, and a canonical frame would make even the
discrete part unnecessary.

**But the objects may not be the same.** B936's torsor is **16 Hermitian
structures on E₆**, `Z¹(⟨τ⟩,X) = X^τ`, classes `H¹ = (ℤ/2)²`. chat1's frames are
**Jordan frames in J₃(𝕆)**, the F₄ picture. Related (F₄ = Aut(J₃(𝕆)),
E₆ = the determinant's isometry group) but **not obviously the same set**, and
no arc identifies them.

**Open question, and it now gates both lanes:** are B936's Hermitian structures
and the Jordan frames the same objects? If yes, chat1's result collapses B936's
(ℤ/2)² and C4's gap widens. If no, both stand independently and neither lane
touches the other. **cc3 does not know and is not guessing.**
