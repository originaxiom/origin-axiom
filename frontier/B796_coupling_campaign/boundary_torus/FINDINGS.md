# THE BOUNDARY TORUS IS A CM TORUS OF CONDUCTOR 4

cc3, 2026-08-09. Gate 5-Q. Structure only. **One observation and one question.**

S2 treats m004 as a Euclidean 3d gravity saddle. Truncate the cusp and the
boundary is a torus — and Plate A has been drawing eigenfunctions *on that
torus* since 2026-08-08 without anyone asking what the torus **is**.

## The computation

```
banked cusp shape   τ = 3.4641016151377544 i
2√3                 = 3.4641016151377546        ⟹  τ = 2√−3

minimal polynomial of τ   :  t² + 12
discriminant              :  −48
field generated           :  ℚ(τ) = ℚ(√−3)          ← the BEING field
order                     :  ℤ[τ] = ℤ[2√−3], disc −48
conductor over ℤ[ω]       :  −48 = f²·(−3)  ⟹  f = 4
```

**The boundary torus has complex multiplication by the order of conductor 4 in
the object's own trace field.**

## The question, stated as a question

**Γ₄₁ is a congruence subgroup of level exactly (4)** (Theorem 1, this branch;
B734's refinement). So there are two 4s — one from the *boundary geometry* (the
cusp's CM conductor), one from the *group* (the congruence level).

Two readings, and this arc **does not choose**:

1. **The same fact seen twice.** The peripheral lattice is ℤ + ℤ(4ω+2), so the
   cusp translations and the congruence level are plausibly two faces of one
   arithmetic condition. If so, the clean statement is **the level *is* the
   cusp's CM conductor** — a structural identification worth banking, and one
   that would explain a number the programme currently just records.
2. **Two facts.** Then it is a coincidence in need of an explanation, which is
   the less likely and more interesting outcome.

**Reading 1 is the prior**, and the arc says so rather than dressing the
observation as a discovery. Determining which is a bounded computation: reduce
the peripheral subgroup mod (4) and compare with the level-(4) kernel.

## Why it matters beyond bookkeeping

If the object is a bulk saddle whose boundary is this torus, then in CFT₂ terms
**τ is the modular parameter of the boundary partition function** — and it is a
CM point, purely imaginary, over the being field. That is not a generic torus.
CM points are exactly where modular forms take algebraic values.

The Cell 9 null just established there is no relation of the tested shapes among
the *eigenvalues* at 25 digits over six fields. **This is a different target
class entirely** — boundary modular data at a CM point — and no box in the
sealed prereg contains it.

**Not claimed:** that anything algebraic follows, or that a value is computable.
Only that the boundary torus is arithmetically special in a specific, named way,
in the object's own field, and that nobody has looked.

---

# THE CONDUCTOR IS THE ONSET OF THE CLASS GROUP — and a base rate for it

Added 2026-08-10, following the seam control's redirect: the largest family of
separations in this corpus is **field / Galois / conductor / level (33 %)**, not
being/hearing (15 %). So the arithmetic line is the one to push.

## 1. Where ℚ(√−3) first acquires a class group

Class numbers of the orders of conductor f, by brute-force enumeration of
reduced **primitive** binary quadratic forms of discriminant −3f² (no table
consulted):

| f | 1 | 2 | 3 | **4** | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| h | 1 | 1 | 1 | **2** | 2 | 3 | 2 | 4 |

**Conductor 4 is the first conductor at which ℚ(√−3) has a class group at all**,
and there `Cl(O₄) = ℤ/2`, generated by the form (3, 0, 4) against the principal
(1, 0, 12).

And the cusp lattice **is** the order of conductor 4; the congruence level **is**
(4). So the object sits at the **onset**.

## 2. The base rate — is that special, or does every ℚ(√−3) manifold do it?

Cusp shapes and their CM conductors for the **eight** manifolds with exact
invariant trace field ℚ(√−3) found in the earlier Sage sweep:

| manifold | τ | disc | conductor |
|---|---|---|---|
| m003 | (1+√−3)/2 | −3 | 1 |
| **m004** | **2√−3** | **−48** | **4** |
| m206 | √−3 | −12 | 2 |
| m207 | (1+…)/7 | −192 | 8 |
| m208 | (1+√−3)/2 | −3 | 1 |
| m410 | −¼ + … | −300 | 10 |
| s118 | √−3 | −12 | 2 |
| s119 | √−3 | −12 | 2 |

Conductors observed: **{1, 1, 2, 2, 2, 4, 8, 10}**.

- **m004 is the only member at conductor 4.**
- Five of the eight (f = 1, 2) sit **below** the onset, where h = 1.
- Two (f = 8, 10) sit **above** it, at h = 4 and h = 6.
- **m004 is the unique member of its own trace-field class sitting exactly at
  the onset of the class group.**

## 3. Scope — and what is NOT claimed

**Computed and solid:** the class-number table; the eight conductors; m004's
uniqueness at f = 4 *in this sample*.

**Not established, and deliberately not asserted:** that any of this is
*significant*. The sample is **eight manifolds**, drawn from a 400-manifold
window. With eight samples spread over conductors 1–10, landing one of them on
the onset value is not obviously improbable, and **this arc computes no p-value
and claims none**.

### THE CONTROL — RUN, AND IT WITHDRAWS THE UNIQUENESS

The obvious control was named as "bounded and unrun". It was run the same day,
on the owner's instruction, over **20,000** one-cusped census manifolds (proxy
trace-field test, which has **0 measured false positives**, so hits are genuine):

| conductor f | 1 | 2 | **4** | 8 | 12 | 16 |
|---|---|---|---|---|---|---|
| count | 2 | 1 | **3** | 1 | 1 | 2 |

**m004 is NOT unique at conductor 4.** `s961` and `t12839` are there too.

**The uniqueness claim is withdrawn.** What survives is weaker and stated
plainly: conductor 4 is the **modal** value at 3/10 (30 %), and it is the onset
of the class group — but m004 shares it with at least two other manifolds, so
being at the onset is **not** a distinguishing property of the object.

That is the second observation in two days to survive a small sample and
dissolve at a larger one (the first was A4's "one-bit signature", 2 classes at
n = 250 becoming 8 at n = 3,112). The pattern is worth more than either result:
**this corpus's small-sample structures do not survive scaling, and the cheap
control catches it every time.**

*(Sample caveat retained: 10 ℚ(√−3) manifolds in 20,000 is still small in
absolute terms. What it is large enough to do is kill a uniqueness claim, which
takes only one counterexample — and it found two.)*

## 4. The question it does make well-posed

L113 records, from a completely different direction (the BC/CMR question), that
the **ray class group mod (4) of ℚ(√−3) is ℤ/2**. This arc finds that the
**ring class group of the cusp's own order, conductor 4, is also ℤ/2**.

**Are they the same ℤ/2?** That is a bounded computation in class field theory,
it connects the cusp geometry to the BC/CMR thread that L113 was opened for, and
it is unrun. If they coincide, the programme has one ℤ/2 reached from two
directions — geometry and arithmetic — rather than two coincidental ones.

Reproduce: `python3 class_group_onset.py` (asserts the onset is 4).

---

# THE TWO ℤ/2s ARE ONE — and the mechanism is elementary, which is worth saying

Computed 2026-08-10, answering the question the previous section made well-posed.

## The answer: the same group, not merely isomorphic

Working explicitly in ℤ[ω]/4 — 16 elements, no formula quoted, no table
consulted:

```
   |ℤ[ω]/4|            = 16
   |(ℤ[ω]/4)*|         = 12          (2 is inert in ℚ(√−3))
   image of O_K* = ⟨ω, −1⟩  : order 6   (all six global units stay distinct mod 4)
   image of (ℤ/4)* = ⟨3⟩    : order 2 = {1, −1}

   RAY   Cl₄(K)  = 12/6 = 2
   RING  Cl(O₄)  = 12/6 = 2
```

And the cosets are **identical**, asserted in the script — not two isomorphic
groups but one group. The reason is one line:

> **(ℤ/4)\* = {±1} is already inside the global units of ℤ[ω]**, so
> `⟨O_K*, (ℤ/4)*⟩ = O_K*` and the two quotients are the same quotient.

**So the ℤ/2 the boundary geometry produces and the ℤ/2 L113 found from the
BC/CMR side are ONE ℤ/2, reached from two directions.**

## The control: is that special to 4? No.

The coincidence holds exactly when `(ℤ/f)* ⊆ O_K*`, i.e. when `φ(f) ≤ 2`:

| f | 2 | 3 | **4** | 5 | 6 | 7 | 8 | 9 |
|---|---|---|---|---|---|---|---|---|
| ring = ray | ✓ | ✓ | **✓** | ✗ | ✓ | ✗ | ✗ | ✗ |
| ray class | 1 | 1 | **2** | 4 | 3 | 6 | 8 | 9 |

**It also holds at f = 2, 3, 6.** So the identification is *not* a special
property of the object's conductor — it is a consequence of ℚ(√−3) having six
units, and it would hold at any f with φ(f) ≤ 2.

## What this is and is not worth

**Worth:** a genuine consolidation. Two threads of the programme — the boundary
geometry (this branch) and the BC/CMR arithmetic (L113) — were carrying what
looked like two ℤ/2s. They are one. That removes a duplicate from the ledger and
connects the cusp directly to the thread L113 was opened for. It also means
**conductor 4 is the smallest conductor at which this shared group is
nontrivial**, since f = 2 and 3 give the trivial group.

**Not worth:** any claim of depth. The mechanism is `φ(4) = 2`. This arc says so
explicitly rather than letting a two-directions-one-object result read as more
than it is. The programme's own error ledger has a name for the opposite habit.

**Net:** L113's ℤ/2 and the cusp's ℤ/2 should be merged into one register entry,
and the merge is free.

Reproduce: `python3 two_z2s.py` (asserts identical cosets, not just equal order).
