# B1154 — SEAM-Y RESOLVED: **MISMATCH** — codex's up-Yukawa=0 (cohomological emptiness) and our period-value disjointness (arithmetic non-overlap) are two *independent* walls, both confirming the one thesis

**Status: banked (frontier). Verdict PROVED (the discriminating characterization); seam outcome
**MISMATCH** per the sealed grammar (`frontier/B1146_seam_b/THREE_SEAMS_PREREG.md`, sha
e699ebc79c06a823). Grounded in committed sources only (our banked wall + codex's on-branch material);
codex's off-branch primary derivation is flagged as a provenance debt, and the verdict does not lean on
it. Gate 5 untouched. Lock `tests/test_b1154_seam_y.py`.**

## The seam

**Is codex's exact up-type-Yukawa = 0 obstruction the SAME arithmetic fact as our period-value
disjointness** (B1124–B1127: "no object period is an SM value-ratio")? Sealed outcomes: MATCH (one
wall, two languages) / MISMATCH (two independent walls, both still confirm structure ⊥ values) /
INDETERMINATE (codex's primary derivation not in hand).

## The two walls

| | **codex: up-Yukawa = 0** | **ours: period-value disjointness** |
|---|---|---|
| **claim** | mu_u = ⟨i(a)∪i(b), c⟩ = 0 identically (OA-C1034/OA-C1055) | no object period/regulator is an SM value-ratio (B1124/26/29/37) |
| **mechanism** | **cohomological naturality:** H¹(X,G_X)=0 ⟹ i is the zero map ⟹ mu_u = ⟨0∪0,c⟩ = 0 | **arithmetic transcendence:** the object's period/regulator ℚ-algebra over ℚ(√−3)/ℚ(√5) contains no SM ratio (PSLQ, 60+ dps) |
| **type** | **EMPTINESS** — the coupling is structurally *absent* (rank 0); field-independent; **no SM number, no PSLQ, no regulator** | **NON-OVERLAP** — two *nonempty* number-sets miss; **requires the SM target list** (strip it → no statement) |

codex is explicit: "an identity of cohomology classes, not a statement about a chosen basis, residue
normalisation or **one arithmetic point**." The ℚ(ζ₁₂) Wilson-character arithmetic is only a **selector**
(which class is H_u), not the cause. Its on-branch companion cell B774 (`CP-W2-yukawa`) makes a *second*
structural vanishing (the alternating family tensor ⟹ a symmetric Yukawa on identical families is 0 by
graded-commutativity — a Pauli-antisymmetry mechanism) — again structural, no SM number.

Our wall (B1137) states its own type: "the regulators are computed with **no SM input**; only the final
PSLQ step admits a target" — 0/18 SM values algebraic over the object's higher regulators, null base
rate 0.0.

## The discriminating fact

The two walls **share zero common data**, and no bridge object exists:
- codex's obstruction contains no SM number / no PSLQ / no regulator; ours *requires* the SM target list
  and contains no cohomology.
- codex's is **emptiness** (rank-0, the coupling cannot form); ours is **non-overlap** (two nonempty
  sets miss). **Emptiness ≠ non-overlap.**
- codex's mu_u is not among our 16 sealed periods; no object period enters codex's H³(O_Y); disjoint
  geometries (a heterotic CY3 cup product on a C12 norm hypersurface vs the figure-eight trace-field
  Kashaev/volume/regulator data).

**⇒ MISMATCH: two independent walls** — holomorphic bundle cohomology vs special-value/regulator
transcendence, *different mathematics and different logical type* — **both confirming the one thesis:**
the object supplies **structure** (the E₆ cubic coupling's symmetry; the golden/Eisenstein arithmetic
form), not the SM's **values** (codex because the up-texture is structurally absent, ours because the
object's rich arithmetic is disjoint from the measured values). This banks as **two-routes-one-verdict**
— independent confirmation, still strong.

## Provenance debt (flagged, not leaned on)

codex's **primary** height-308 derivation (`YUKAWA_CUP_PRODUCTS_308`, `YUKAWA_EXACT_SPECTRUM_NO_GO` +
verify scripts) is **off-branch and unversioned** — `~/oa-audit-seat/`, which `git` reports as
"not a git repository". `origin/codex/seat-r001` carries only the one-line claim + the B774 structural
cell. **The MISMATCH verdict rests on the *kind*-difference** (structural emptiness vs arithmetic
non-overlap), established from committed sources — it is robust regardless of that proof's details and
does **not** lean on the unversioned files. **FLAG:** codex should commit its primary yukawa derivations
to the branch (single-homed content debt, L181/L183).

## Fences

This seat did **not** re-run codex's off-branch proof. The MISMATCH is independent of that proof's
correctness — it needs only the *kind* of obstruction, which the on-branch one-line claim + B774 supply.
The prereg's "MATCH gets cc3's third opinion before banking" does **not** trigger (this is MISMATCH). No
SM value entered anywhere — the entire content is that the object does not supply one. Gate 5 untouched.
