# B1039 — ADDENDUM (2026-08-12): X4's reclassification — this arc is a CONSISTENCY CHECK on the seam-locality forbid, not a test of it

*cc banking seat, same day as the bank. The audit seat's Task C Phase B §4 (X4, grade
K2 — accepted) arrived in the relay channel before this arc sealed but was read only
after (a channel gap on the banking side: two relay documents landed during a context
window that predated the monitor's stamp). The seal is untouched; this addendum records
what the seal did not contain.*

## X4, as established from the machinery (not the outcome)

The Mayer–Vietoris sequence for D = M ∪_T M̄ gives, by exactness:

```
0 → coker(H⁰(M) ⊕ H⁰(M̄) → H⁰(T)) → H¹(D) → ker(H¹(M) ⊕ H¹(M̄) → H¹(T)) → 0
      = the connecting map's image            = the bulk-inherited part
      = "seam-born", BY CONSTRUCTION          = solo classes surviving, not created
```

H¹(D) has exactly TWO sources and exactness permits no third. "New structure not
attributable to the seam" is therefore **not a possible outcome of an MV computation**
— the forbidden result of the seam-locality forbid (the frame's F1) was structurally
unreachable by this arc's machinery. The same holds for B1036's classification.

## The reclassification

- **B1039 is a consistency check on F1, not a test of it.** Its EXISTENCE result (the
  V-valued symmetric support is EMPTY on the double; the antisymmetry wall
  sector-complete: solo B632 · scalar/seam B1036 · V-valued B1039) is UNTOUCHED — that
  question never depended on seam-attribution.
- With the support EMPTY, the conditional seam-locality cell was VOID in any case.
  **F1's status is UNTESTED, not confirmed.**
- F1's corrected scope (the audit seat's restatement, adopted verbatim): *"F1 is a test
  only for invariants whose seam/bulk decomposition is not a construction of the method
  used to compute them."* Any future F1 cell must name an attribution criterion
  independent of the computing method, or declare itself a consistency check in its
  seal.

*The banked FINDINGS' "the seam-locality forbid drew a void conditional" line stands;
this addendum adds that even a non-void conditional could not have refuted F1 by this
route.*
