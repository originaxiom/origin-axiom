# B1039 PREREGISTRATION — L157-1: THE V-VALUED RESIDUAL (sealed before compute)

**Date sealed:** 2026-08-12 · **Seat:** cc. Gate 5-Q; type-law compliant (all outcomes
structural). Relay channel checked pre-seal (the S034 query answered; the motivating
document's modality corrected BEFORE this seal — the entry now reads "generically does
not cancel", S034/B161 binding). **Double duty, both roles sealed**: Lane II's last
uncomputed sector AND the seed–field frame's first forbid test (seam-locality).

## The question

B1036 proved the double gains classes (h¹ = 5 = 2/2/1, two routes agreeing) and that the
symmetric mass-shaped pairing is EMPTY in the scalar/seam sector. **One sector remains:
the V-valued (C3-route) pairing assembled over the double** — B632's own cup machinery
(C3: V⊗V→V, the cubic as multiplication; the corrected 2-cell; the two coker
functionals per side, h² = 1 per block per side) evaluated on the double's five explicit
classes and MV-assembled. Does the symmetric object exist THERE?

## The cells

- **W1 — the hygiene control FIRST (halting)**: B632's coboundary-invariance gate re-run
  on the C3-route per side, on this arc's class representatives — the value read ONLY
  through the banked functionals (B1036's lesson: raw evaluations are never classes;
  two routes halted to learn it).
- **W2 — the per-side tables**: the C3-cup symmetric values of all 15 pairs of the five
  double-classes, per side (M and M̄), functional-read. Exact.
- **W3 — the MV assembly**: the double's symmetric support per pair = the (M, M̄)-pair
  of functional values modulo the computed T²-correction image, with the orientation
  sign. PRESENT/ABSENT per cell — support only (B884's fence).
- **W4 — the two sealed verdicts**:
  (a) **EXISTENCE (Lane II)**: nonempty support ⟹ the symmetric object EXISTS on the
  double in the V-valued sector — the antisymmetry wall FALLS at last, and Lane II
  reopens with a located object. Empty ⟹ **the wall extends to every sector of the
  double** — the mirror-coupling route closes completely, and the honest sentence is
  banked: one coupling move does not create the symmetric half anywhere.
  (b) **SEAM-LOCALITY (the frame's forbid)**: conditional on existence — the support's
  carriers classified (seam-born classes/pairs vs glued). **Non-seam support ⟹ the
  forbid FIRES: the frame's creation-verb dies its first death and the speculation
  entry records it.** All-seam support ⟹ the forbid survives its first test.

## Hazards (all pre-registered)

The five standing modes + B1036's own: functional-read only (never raw); the
presentation-complex H² trap (per-side one-relator functionals only; the double's H²
enters ONLY through the MV pair-mod-correction, never through the 3-relator complex);
sum(..., K0) on every K-valued reduction; banked-number gates (per-side h¹ = 1, h² = 1
per block; the double's 2/2/1 reproduced before any pairing is read).

## Declared priors (non-weakening)

- **Existence: lean NO, moderate** — the wall has extended in every sector yet computed
  (B632 solo; B1036 scalar/seam); stated as the honest lean, reversible by the compute.
- **Seam-locality: NO PRIOR** (conditional cell; genuinely open).

## Outcomes

Every branch banks: existence-yes reopens Lane II with a located object; existence-no
closes the mirror-coupling route completely (and B298's cubic-carrier direction becomes
Lane II's only survivor); the forbid firing kills the frame's verb; the forbid surviving
is its first passed test. Nothing is unfalsifiable here — the frame's critic designed
this cell's teeth.
