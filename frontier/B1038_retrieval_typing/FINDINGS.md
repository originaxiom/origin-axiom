# B1038 — FINDINGS: the census reproduces exactly; the retrieval instruments install

**Seat:** cc · **Date:** 2026-08-12 · Verification arc (plan Arc 3; no outcome-prior —
the content is the verification). Compute: `b1038_verify.py`.

## V1 — the at-risk census: **REPRODUCED EXACTLY**

An independent implementation on main returns **54 claim lines banking a 3+dp decimal,
23 mentioning invariance-family keywords, 31 without — internally consistent** — the
audit seat's corrected figures precisely (their occurrence-vs-line unit error, self-caught,
does not recur here). The at-risk sample overlaps their adjudicated categories (instrument
numbers, bit-counts, spectral invariants, external identifiers) plus this window's own
new arcs (bit-prices and verified constants — the same benign classes). **Their census
NEGATIVE (zero unflagged gauge-exposed content) stands corroborated at the count level;
their category adjudication was already complete and is cited, not re-done.**

## V2 — the synonym-set principle: verified, with the criterion difference stated

This arc counts arcs *mentioning* the gauge/frame vocabularies (192 + 3, union 193) — a
**looser criterion** than the audit seat's "arcs *running* a gauge check" (their 12 + 7 =
19 floor). The check verifies the PRINCIPLE (a single-vocabulary query undercounts —
the frame-vocabulary arcs are invisible to the gauge-keyword query) without conflating
the two measurements. Any "how many arcs do X" figure remains a floor unless the query
runs the synonym set — now standing practice (COMPUTE_THE_PROGRAM §5).

## V3 — the instruments installed this batch

- **The π/6 two-referent TERMINOLOGY row** (arg κ, a Test-1-clean trace carrying the
  matter face, vs arg Y[134], proved pipeline gauge — one symbol, opposite types).
- **The point-of-use citation practice** (PRACTICES.md): every load-bearing term in a new
  FINDINGS resolves against the record before sealing — the corpus's measured retrieval
  defect, converted to a practice with a named future gate-shape.
- **The B1027 refresh design notes** (CROSSING_REQUIREMENTS): the Jarlskog flag (δ is
  parametrization-defined; J is the convention-free carrier) and the CP-body citation
  requirement (arg κ = ∓π/6 declared and related) — both binding on the pre-committed
  refresh when its trigger fires.
- The fresh claim-drop held-out slice (B101–B158, named by the audit seat, untouched by
  them) is **noted as available** for a future adjudication on this bench or a web seat —
  not run here.

## Verdict: PROVED (verification affirmative; instruments installed; criteria differences stated)
