# B940 PREREGISTRATION — THE SEALED DIRAC RUN (sealed before compute)

**Date sealed:** 2026-08-07 · **Seat:** cc (banking) · **Owner authorization:**
"do it" at the completion gate · **Protocol:** the B922 pattern as adapted in
B933's DESIGN.md §7, with §8's anchor substitution binding.

## The question

Does the object's Dirac operator (m004, spin structure ρ₁ — the harvested
cell3 authorization: non-Lie under both conventions ⟹ discrete spectrum,
unconditional) have a certifiable first eigenvalue?

## The sealed criterion (B933 §10, VERBATIM — vacuity-checked there)

> In the window |λ| ≤ 4, the instrument produces ≥ 1 eigenvalue passing ALL
> of: two-Y bar |Δλ| < 10⁻⁹ at 10-digit working precision; two seeds; P4
> restart spread under the sealed bar; P3 displaced-λ control finds nothing;
> gates G1, G2, G2b, assembly cross-check pass; the ± partner is present
> within the same bars (§5a is a theorem — enforceable).

- **OUTCOME A**: a certified Dirac eigenvalue on a cusped hyperbolic
  3-manifold, banked **carrying verbatim** the literature-blank caveat (O3),
  the internal-only-validation sentence (§8), and the multiplicity caution
  (§5c).
- **OUTCOME B**: an instrument-negative (the probe's 8-digit reproducibility
  becomes the recorded anomaly). **NOT bankable as a spectral no-go** — the
  Weyl tension forbids that reading. Routes to a debug arc.
- No third outcome. **The kernel (λ = 0, dim 2) is EXCLUDED from this seal**
  (its claim shape needs the O2 instrument).

## THE "FIRST" GATE (O3, binding on the banking seat)

**No banked sentence may contain the word "first" until the prior-art sweep
reaches MathSciNet/zbMATH grade.** B933's search (arXiv-only, one session)
found no computed Dirac eigenvalue on any hyperbolic 3-manifold, with a
must-pass control returning Bär 2000 and the Bolte–Stiepan surface trace
formula. A deeper adversarially-verified panel runs in parallel with this
cell; **if it finds prior art, OUTCOME A banks as a REPRODUCTION** (equally
valuable, differently worded), and that is not a demotion but the sweep
working.

## The disclosed prior

**OUTCOME A, moderate-high** — the probe already reproduced 2.97455058 to 8
digits across three independent instruments (two Y's, two seeds, two word
sets) and G1 validated the operator against an independent implementation at
1.9e-13. The risk is not the mathematics but the certification battery
(P3/P4 at working precision). Scoreboard priced: ten sealed cells, five
object-overrules, four disciplined wins, one split.

## Files (after sealing)

`dirac_sealed.py` → `results.json`; `FINDINGS.md` verbatim against these
criteria; locks in `tests/test_b940_dirac.py` (seal-integrity first).
