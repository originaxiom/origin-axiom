# B686 — THE KOIDE CLAIM: FIREWALLED (main seat, 2026-07-18; adjudicating
# the web seat's "Koide is entirely Eisenstein arithmetic; θ₀ = 2/9 =
# 2/3²"). Gate 5 stands. NOT banked as physics.

## The claim

Q = 2/3 (Koide) is Eisenstein: the 120° spacing = ω, the offset
θ₀ = 0.22222 = 2/9 = 2/3² (being prime squared), so "every number in
Koide is a power of 3."

## THE FIREWALL (computed, not asserted)

1. **Q = 2/3 IS A PARAMETRIZATION TAUTOLOGY.** For √m_k ∝ 1 + √2 cos(θ +
   2πk/3), Q = (Σm)/(Σ√m)² = 2/3 EXACTLY for EVERY phase θ (verified at
   four unrelated θ). The 120° spacing FORCES Q = 2/3 regardless of the
   arithmetic; so "Q = 2/3 ⇒ Eisenstein" reads ω INTO the ansatz — the
   120° is ASSUMED, not derived from the object. Q = 2/3 is zero evidence
   of anything Eisenstein-specific. (chat1's "Q = 2/3 follows
   algebraically" is TRUE and exactly why it proves nothing.)
2. **The empirical Koide fact is real but not ours.** The ACTUAL lepton
   masses give Q = 0.6666605 (6e-6 from 2/3) — a famous 1981 coincidence,
   in the literature, NOT derived here, NOT from the object.
3. **θ₀ = 2/9 is CONVENTION-DEPENDENT + base-rate.** In the natural
   branch (k=0 = m_e) θ₀ = 2.317 rad — 2.09 AWAY from 2/9; landing on
   0.2222 requires a specific ordering/branch/offset choice. And
   2/9 = 2/3² is a small-smooth number: the base-rate control (B615/B655)
   kills such matches — many simple fractions sit within 0.1% of a
   redefinable phase. NOT a discriminating fact.
4. **Gate 5.** These are SM lepton MASSES. The observer-coupling thesis
   (and B685) says SM values live in the coupling, not the object. A
   "Koide = Eisenstein arithmetic" identification is a physics claim,
   typed/sealed/owner-gated, never banked from a numerical coincidence.

## VERDICT: NOT banked. No LAW_MAP row, no CLAIMS.

## What IS legitimate (separated cleanly)

The STRUCTURAL directive — map the Eisenstein/being face the way we
mapped the golden — is SOUND and independent of Koide (see S067; the
owner + web seat + this seat converged). The being face carries real
banked MATH (the cubic, the sum rule, the swap, the phases, the volume)
that we studied as numbers but never mapped as an anatomy. That mapping
is the next program. The HONEST test of the Koide intuition is NOT
"does 120° give Q = 2/3" (tautology) but "does the Eisenstein arithmetic
FORCE the 120°/ζ₆ structure WITHOUT assuming it?" — computed under the
base-rate control, with Gate 5 intact. If the being face forces it, that
is signal; assuming it and reading off 2/3 is numerology.

Locks: tests/test_b686_koide_firewall.py.
