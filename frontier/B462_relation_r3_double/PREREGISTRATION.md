# B462 — PREREGISTRATION: Relation R3 — the double, redesigned (the φ-scan is the probe)

**Committed before computation. Firewalled. Campaign: OPEN_LEADS §Relation.**

## Geometry statement (binding)
D(M) = M ∪_φ M' is a closed manifold with an essential JSJ torus and TWO HYPERBOLIC PIECES
(a mixed manifold — B143's wording; B174's "graph manifold" is a terminology slip, flagged).
Non-hyperbolic; the Regina wall cited, not re-hit.

## Cells
- **ENGINE GATE**: Z_r(M ∪_id M̄) = ⟨Z_r(M), Z_r(M)⟩ = ‖Z_r(M)‖² from the banked B441 WRT
  machinery — determined numbers, computed as the gate only (zero-information as a verdict).
- **R3a′ THE PROBE**: the φ-scan pairing ⟨Z_r(M̄), ρ_r(φ) Z_r(M)⟩ over φ ∈ {id, T, T², S, ST,
  TS, STS} (the B174 set) at levels r ≤ 9 — the PHASES are not determined by ‖Z‖; read against
  B174's classical fork-size table. Conventions fixed: SU(2)_r S/T matrices in the standard
  normalization; ρ_r(φ) by word in S,T; M̄ = complex-conjugated vector.
- **The chirality payload (the red-team's F2 fix): 5₂'s double** — 5₂ ∪ 5̄₂ vs 5₂ ∪ 5₂
  (4₁'s version is equal-by-theorem via amphichirality + B144 — CUT, stated).
- **R3b**: the self-gluing κ-diagonal of the B174 landscape (m₁ = m₂ = 1) — the uncomputed
  diagonal of a characterized family (trace-ring machinery, `B174/gluing_landscape.py`).

## Controls
The mixed pairing 4₁ ∪_φ 5₂ through identical machinery.

## Bins
Pairings whitelist-derivable from the S/T arithmetic (expected — LAUNDERS) / a φ-structure not
reducible to the banked fork table + S/T data → burden-inversion / NEW-MATH on the exact
quantum-vs-classical landscape table. Lit-gate: the gluing formula is standard TQFT (cited; no
formula novelty).
