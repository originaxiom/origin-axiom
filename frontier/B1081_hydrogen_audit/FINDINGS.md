# B1081 — THE HYDROGEN AUDIT: nothing orphaned — the atom's skeleton is forced ∪ free-with-prior ∪ typed-outside, and its neutrality is a theorem

**Date:** 2026-08-19 · the inverse chain (the owner's question run as computation) · 2
agents; the verifier HOLDS after its own deep rebuild from the E₆ roots (and correctly
graded the audit's in-cell re-derivation 'real but shallow' — the deep rebuild is the
verifier's, its non-fatal defect list carried in the artifacts) · Gate 5: charges as
exact lattice rationals are structure; no measured value anywhere.

## The verdict: YES, nothing orphaned

**FORCED (each with its banked arc, opened, and the decisive ones recomputed):** the
hypercharge direction unique (B864's anomaly arithmetic: b = c = 0 exact) and a u(1)
surviving every Wilson-menu row (B1079's Kac-marks mechanism); **charge quantization in
exact e/3 multiples with the 1:3 lepton:quark ratio PREDICTED, not fitted** (B970: three
normalizations in, ten hypercharges out, Σ Q_em over the 27 = 0 exact; the non-vacuity
witness BITES — the 78's leptoquark Y = −5/6 fails the very test the 16's fields pass);
the electron's and quarks' gauge quantum numbers at exact weights; the color singlet in
3⊗3⊗3 with multiplicity exactly one; **THE NEUTRALITY THEOREM: 2·(2/3) + (−1/3) + (−1)
= 0 in exact fractions — hydrogen's charge cancellation is forced by the weight lattice,
not assumed.** The global form [SU(3)×SU(2)×U(1)]/ℤ₆ surfaced as ALREADY BANKED (B862,
kernel computed exactly) — B1080's landing credit prepared.

**FREE-WITH-PRIOR (the torsors, with the counting-measure prior):** the
chirality/antimatter sign; the Wilson-menu row.

**OUTSIDE-THE-CHAIN (typed precisely, the honest boundary):** u(1)_Y → U(1)_em needs
EWSB (the Higgs layer — the ledger's own open row); **spin-statistics is an imported
convention, and the record PROVES gauge numbers can't fix it** (the same 27 populates
H_u/H_d — identical gauge numbers to the lepton doublet, read as scalars); generations,
masses, dynamics — value/dynamics-kind per the kind table. A Tier-1 mathematical
reproduction of GUT structure, not a physics derivation — said plainly, as the cell's
own headline requires.

## Artifacts
- `b1081_results.json` (both agents verbatim, scrubbed; the verifier's defect list inside).