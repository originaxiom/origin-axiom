# PREREG — B708: Kim × the seam (the sharpest meeting-point test, SEALED)

## The question
Does Kim's arithmetic Chern–Simons theory at F=ℚ, with G=ℤ/2, reproduce our
B704 seam — NOT just structurally (H¹(G_ℚ,ℤ/2)=ℚ*/(ℚ*)² contains any
multiquadratic field, semi-tautological by Kummer) but in its PAIRING (the cup
product = Hilbert symbol = Morishita arithmetic linking number)? I.e. does the
arithmetic-CS linking on the stage primes reproduce our SPECIFIC structure —
the two-hands coupling (being 3 / hearing 5) and the audibility law?

## Two-outcome (sealed)
- **MEET (non-trivial)**: (a) the seam = the subspace ⟨{p*}⟩ ⊂ H¹(G_ℚ,ℤ/2)=
  ℚ*/(ℚ*)², group law = 𝔽₂-addition = genus (being·hearing=meeting) [structural];
  AND (b) the arithmetic-CS PAIRING reproduces our structure NON-trivially:
  being(3)–hearing(5) have arithmetic linking number 1 (= the banked mutual
  inertness); AND the AUDIBILITY law (p≡1 mod4) = the linking with the
  archimedean/real prime −1 (audible ⟺ Hilbert symbol (−1,p) trivial). Beyond
  the tautological inclusion ⇒ the two constructions are ONE.
- **NO-MEET (superficial)**: the arithmetic-CS pairing is unrelated to our
  two-hands/audibility structure ⇒ only the tautological "both use ℚ*/(ℚ*)²"
  holds ⇒ superficial, like Turok.

## Base-rate / rung gate (Listening Protocol)
Rung-2/3. The tautological inclusion (seam ⊂ ℚ*/(ℚ*)²) is Kummer and does NOT
count by itself. The CONTENT is whether the CS pairing (linking/Hilbert)
reproduces the SPECIFIC two-hands + audibility structure (not generic).

## Firewall
Gate 5. Pure Galois cohomology + arithmetic (Kummer, Hilbert symbols,
Morishita linking). No physics claim; a structural meeting-point.
