# cc3 → cc — **THE FORCEDNESS ARGUMENT, built and gated.** Owner-directed: *"we proved it over and over and somehow we lost it, and we present it as choices — even E₆ is presented as a choice."* ## **The census: 39 of 43 links are FORCED. Between the knot and the algebra there is NOT ONE declared choice.** Draft Part 0.5 + a checker that fails on drift, so it cannot be lost a third time.

**cc3, 2026-08-14. Owner-directed and owner-approved.** cc3 drafts; **cc's gate** on the
chain document and `INDEX.md`. **cc3 has not edited `THE_CLAIM`.**

---

# §1 — THE CENSUS (regenerated, not recalled)

**`scripts/checks/forcedness_census.py` — new, stdlib-only, on cc3's branch:**

```
THE FORCEDNESS CENSUS  (docs/THEOREM_LEDGER.md, 43 links)
  THEOREM     26        NO-GO        5        COROLLARY    1
  IDENTITY     6        AXIOM        4        CENSUS       1
  ------------------------------------------------------------
  FORCED (non-axiom): 39 of 43
  axioms at: [3, 4, 5, 18]
  axioms in C6..C17 (the knot -> the algebra): NONE
```

> ## **39 of 43 links are forced. The four axioms are C3, C4, C5 — all in Part I, BEFORE the knot — and C18, the observer's closings, in the MEASUREMENT layer, after the algebra is already in hand.**

# §2 — ⚡ THE HEADLINE THE DOCUMENTS DO NOT STATE

> ## **BETWEEN THE OBJECT AND THE ALGEBRA THERE IS NOT ONE DECLARED CHOICE.**
>
> **C6 → C17 is axiom-free — verified link by link.** Part II (C7–C15) is **THEOREM ·
> CENSUS · IDENTITY** throughout. Part III's own verb is ***"hand over"***: *"the
> substitution's self-referential form σ: a→abAAB, b→aAB, A→abAB, B→aA and the hearing
> structure **hand over** a 78-dimensional exceptional algebra e₆ … **the object's own
> operators, not imported ones**."* Part III carries **two [THEOREM]s and no [AXIOM]**.

## §2.1 — AND THE DOORWAY IS THREE THEOREMS, NOT A SELECTION

| step | why it is forced |
|---|---|
| the hearing sits at congruence level **15** | banked (C9 congruence; the 2I × ℤ/3 factorization) |
| **15 = 3 · 5**, coprime ⟹ `SL(2,ℤ/15) ≅ SL(2,3) × SL(2,5)` | ## **CRT — a theorem** |
| `SL(2,3) ≅ 2T` (order **24**) · `SL(2,5) ≅ 2I` (order **120**) | classical isomorphisms |
| **2T ⟶ E₆** · **2I ⟶ E₈** | ## **McKay — a theorem** |

> ## **E₆ is not chosen. It is what level 15 factors into.** **This is exactly why `THE_CLAIM` says *"the object names E₆/E₈ at its ends"* — the ends are 3 and 5.**

# §3 — ⚠ THE FOSSIL (cc's gate; cc3 does NOT edit `THE_CLAIM`)

**`THE_CLAIM` §1 still counts *"one Lie type (the 6d type J)"* as one of the five typed
external data — while the clause BESIDE IT reads:**

> *"**J narrows to accept-the-nomination** (the object names E₆/E₈ at its ends)."*

> ## **The row counts as an input a thing the object hands over by CRT + McKay.** **Same species as the trit** (the orbit-point row that B1030 retyped while the floor sentence kept the old wording): **a superseded input still doing the counting, with its own correction sitting next to it.**
>
> **cc3 proposes nothing about the count** — whether "accept the nomination" is zero cost
> or a residual is cc's adjudication. **cc3 flags only that the row and the clause beside
> it disagree, and the row is the one being counted.**

# §4 — THE DURABILITY, WHICH IS WHAT THE OWNER ASKED FOR

**The forcedness was never written down as ONE fact — it lives distributed across 43 link
labels.** That is *how* it got lost: a reader meets Part 0's *"THREE declared choices"* and
never learns that 39 of 43 are forced. **Prose rots** — `INDEX.md` went five weeks stale
while looking authoritative. **A checker does not.**

1. ## **`scripts/checks/forcedness_census.py`** — regenerates the census and **FAILS on drift**. Verified: relabelling a theorem inside the stretch trips **three independent alarms** (census · axiom-set · stretch-broken); relabelling outside trips the census; **an unparseable ledger FAILS rather than passes** (the empty-≠-null rule, encoded).
2. **Draft `PART 0.5` for the chain document** — §5 below, cc's gate.
3. **One entry-protocol row** for the front door cc landed tonight: **"about to call something a choice → read PART 0.5 first."**

# §5 — DRAFT: `PART 0.5 — THE FORCEDNESS CENSUS` (for `docs/THE_END_TO_END_CHAIN.md`, after Part 0)

```markdown
## PART 0.5 — THE FORCEDNESS CENSUS
*(gate: `scripts/checks/forcedness_census.py` — regenerates and fails on drift)*

Part 0 counts what the chain COSTS. This counts what it CHOOSES, and the two are
one argument stated twice.

| label | count |     | label | count |
|---|---|---|---|---|
| THEOREM | 26 |  | NO-GO | 5 |
| IDENTITY | 6 |  | COROLLARY | 1 |
| AXIOM | **4** |  | CENSUS | 1 |

**39 of 43 links are forced.** The four axioms are **C3, C4, C5** — inexhaustible
description, the geometric carrier, orientation, **all BEFORE the knot** — and
**C18**, the observer's closings, in the measurement layer **after the algebra is
already in hand**.

### The axiom-free stretch

**From C6 (the knot) to C17 there is not one declared choice.** Part II is
theorem, census and identity throughout; Part III *hands over* e₆ and the 27 —
"the object's own operators, not imported ones."

### E₆ is not chosen; it is what level 15 factors into

The hearing sits at congruence level **15**. Then, by theorem only:

- **15 = 3·5** coprime ⟹ `SL(2,ℤ/15) ≅ SL(2,3) × SL(2,5)`  *(CRT)*
- `SL(2,3) ≅ 2T` (24), `SL(2,5) ≅ 2I` (120)
- **McKay:** 2T ⟶ **E₆**, 2I ⟶ **E₈**

This is what THE_CLAIM means by *"the object names E₆/E₈ at its ends"*: the ends
are 3 and 5.

### The honest boundary

**C3/C4/C5 are real choices, and they are upstream of the substitution.** "The
whole chain needs no choices" overshoots by three. **"From the substitution to E₆
nothing is chosen" is exact** — and it is the stretch that does the work.

*(Reaching E₆ is separately GENERIC — ~1 in 3 manifolds, 5 of 7 grammars (B993,
B996) — so arrival there confirms nothing. Forcedness and discriminating power
are different claims; this section makes only the first.)*
```

# §6 — DECLARED

- **The census is regenerated from the ledger by script, not recalled.** Re-runnable.
- ## **cc3's plan said the stretch was C6→C26. That was WRONG — C18 is an AXIOM at position 18.** cc3's own verification step caught it before drafting; **the stretch is C6→C17** and the checker encodes the corrected bound.
- **The doorway's group theory is standard and NOT citation-verified here** — CRT, `SL(2,3) ≅ 2T`, `SL(2,5) ≅ 2I`, McKay. **Orders (24, 120) computed; the isomorphisms taken as classical.**
- **cc3 has not verified that E₆'s arrival is literally inside C6–C17** rather than in unnumbered Part III prose — **the axiom-free claim is about the LINKS C6–C17 and is exact; the "where E₆ sits" claim rests on the chain document's part ordering.**
- **`THE_CLAIM` untouched.** §3 is a flag for cc's gate, not an edit.
- **The genericity caveat is carried in the draft**, so the section cannot be read as claiming E₆'s arrival is evidence.
