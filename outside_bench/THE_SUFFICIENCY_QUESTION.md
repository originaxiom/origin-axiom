# THE SUFFICIENCY QUESTION — formulated before attack: what promotes a type-eligible relation to an actual occupant?
## (outside bench, 2026-08-28; the owner's "formulate it properly before attack, scan literature, see if we have buried insight in the repo"; standing document, addendum-only mutations; FIREWALLED per H5 — no phenomenal claim is made or denied)

---

## §1 — PROVENANCE (how the question arose, and why it is new)

Three banked results generated it, in order:

1. **Memo 112** — the occupant's TYPE is forced: an **asymmetric,
   non-commuting relation to something unlike itself**, whose orienting
   bit belongs to the relation and to neither relatum (O1 nothing
   orients itself; O2 no same-algebra partner; O3 no commuting
   relation; O4 the bit is irreducibly relational).
2. **Memo 114** — the SUPPLY of such relations is abundant and grows:
   the seat-eligible fraction of pairs rises **0.748 → 0.903 → 0.936**
   with depth, from an information budget fixed at the first beat.
3. **The gap** — memo 112's conditions are **necessary and exhibited**;
   they were never shown **sufficient**. With ~94% of pairs eligible,
   "eligible" cannot by itself mean "occupied", or the record is
   populated by an absurd multitude of observers.

**This is a genuinely new item on the ledger** — the first the bench has
generated from a question rather than from the missing-list — and it is
distinct from every open question already banked (§2).

---

## §2 — THE QUESTION, SPLIT INTO ITS THREE INEQUIVALENT READINGS

The formulation's first job is to stop three different questions from
being answered as though they were one. They have different statuses.

| tag | the question | status |
|---|---|---|
| **S-A** | **THE SUFFICIENCY QUESTION (new, this document):** is there a **record-internal predicate** that separates *occupied* relations from *merely eligible* ones? | **OPEN, attackable — not yet shown independent, not shown inexpressible** |
| **S-B** | **The token question** (R22): *which* eligible relation is ours? | OPEN, external — a real fact the record does not fix (same status as the dynamical branch bit) |
| **S-C** | **The phenomenal question**: does occupation carry experience? | **INEXPRESSIBLE** — no banked proposition refers to experience (memo 109) |

**S-A is the one to attack.** It is not S-B: a criterion could exist
(S-A answered) while which instance is ours stays external (S-B still
open) — exactly as a law can be known while an initial condition is
not. It is not S-C: a criterion is a structural predicate, and would
say nothing about what occupation is like.

**Precise statement of S-A.** Let **E** be the set of pairs satisfying
memo 112's necessary conditions (non-commuting, unlike, bit-carrying).
Does there exist a predicate **Occ(·)** on **E** such that (i) Occ is
definable from the record's own banked structure, (ii) Occ is not
satisfied by all of E, and (iii) Occ is stable under the record's own
symmetries — in particular **mirror-blind, or else explicitly priced**
as observer-column data under the parity×dimension law?

---

## §3 — WHAT WOULD COUNT AS AN ANSWER (preregistered success criteria)

A proposed criterion is admissible only if it meets all four:

- **C1 — internal.** Definable from banked object structure. A
  criterion importing a measured value fails Gate 5 outright.
- **C2 — non-trivial.** It must not be satisfied by all of E (else it
  is not a criterion) nor by nothing in E (else it forbids occupation).
- **C3 — no unnamed extremal premise.** If the criterion is a
  maximisation ("the *most* X wins"), the maximality itself must be
  **derived** or **named as a premise** in the SP-1 class. This is
  precisely where the outside literature's leading candidate fails
  (§5), and the programme already has the discipline for it.
- **C4 — two-outcome testable.** It must be decidable on the banked
  pair census (memo 114's population), so the answer is a computation
  and not a preference.

**And the honest fourth outcome must be preregistered too:** S-A may be
**provably not record-internal** — the shape memo 109 established for
the anchoring. That would be an answer, not a failure (§4, A4).

---

## §4 — THE FOUR CANDIDATE ATTACK ROUTES (named before attacking)

- **A1 — EXTREMAL.** Occ = the maximiser of some banked quantity over
  E (an integration/irreducibility analogue). **Warning from the
  literature (§5): this is IIT's exclusion postulate, and its standing
  criticism is exactly a C3 violation — maximality is an extra premise
  not derived from the axioms.** Admissible here only if the extremum
  is *derived* or named SP-class.
- **A2 — CLOSURE / FIXED POINT.** Occ = those relations that can host
  the record's own self-description — a relation is occupied iff it
  supports the quine. **The record uniquely has this instrument
  already built** (memos 107/108: the self-model exists and is one
  convention-typed bit short), and the route has a **classical
  backbone**: Lawvere's fixed-point theorem, under which a fixed point
  is the formal counterpart of subject–object duality — *an element
  that is at once the function (observer) and the argument
  (observed)*. That is memo 112's "irreducible to either relatum" in
  categorical form, which is a strong reason to try it first. The
  concrete test: is the subset of E that supports the self-description
  map a **proper** subset (C2 satisfied) — and is it mirror-blind
  (C3/C4)? Bench's first choice.
- **A3 — SELECTOR/REGIME.** Occ read off cc's three-regime selector
  theorem (relayed, B1196/GC-27: σ and λ are anchors because they lie
  on the non-normalizable side of a sharp boundary). If occupation is
  a regime rather than a maximum, C3 is satisfied without a premise.
  **Requires cc's exact statement before it can be tested** — routed.
- **A4 — NO-GO.** Prove S-A is not record-internal, by memo 109's
  method (exhibit structure-preserving maps permuting E while fixing
  every record-computable quantity) or by the causal-embedding
  argument in §5. This would close S-A the way the fence theorem
  closed the anchoring — and it is a *win*, not a dead end.

---

## §5 — THE LITERATURE SCAN (what is known outside; positions ours)

The question is not new to the world — it has a name, and its leading
answer has a known defect. That is useful, and it is worth saying
plainly that this programme does not get to claim novelty for the
question itself, only for the setting.

- **THE BOUNDARY PROBLEM.** In consciousness studies this is the
  recognised open problem of *which* physical systems are subjects —
  explicitly the "overlooked cousin to the binding problem". The
  standard difficulty is exactly memo 114's: a measure typically
  labels a system, its subsystems, its irrelevant extensions, and
  aggregates, all at once — "a bizarre proliferation". **Our E, at
  ~94% of pairs, is that proliferation in exact form.**
- **IIT'S EXCLUSION POSTULATE** is the leading extremal answer: only
  the maximally irreducible set exists "for itself" (Φ_max), neither
  more nor less. **Its standing criticism is a C3 violation**: the
  axioms say nothing about consciousness being *maximally* integrated,
  so maximality is an imported premise. **Lesson for A1, adopted as a
  gate.**
- **RELATIONAL QUANTUM MECHANICS.** RQM independently reaches this
  programme's memo-112 conclusion from physics: the observer is not
  special, and **facts are realised only at interactions between two
  physical systems** — relational all the way down. This is
  *convergent support for the TYPE answer*, and notably RQM faces the
  same next question, not a solution to it.
- **DECOMPOSITIONAL EQUIVALENCE / system-boundary non-objectivity.**
  There is existing work arguing system boundaries are not objective —
  an unresolvable ambiguity in the source of any outcome for any
  finite observer. **This is direct prior art for route A4.**
- **UNDECIDABILITY PROPAGATION.** Also relevant to A4: *no subsystem of
  an undecidable system can resolve that undecidability if it
  participates causally in the computation.* If the occupant is inside
  the record, this is the shape a no-go here would take.

- **MARKOV BLANKETS / THE FREE ENERGY PRINCIPLE** are the other major
  extant individuation criterion, and they carry their own documented
  technical critique: the various Markov-blanket definitions in the
  literature **are not equivalent**, and key steps rewriting the
  equations of motion **require previously unstated assumptions**.
  **So both leading extant criteria fail a C3-style gate** — one by an
  imported extremal premise (IIT), one by unstated assumptions (FEP).
  That is the single most useful thing the outside scan returns: it is
  not that nobody has answered S-A, it is that **every published
  answer so far smuggles a premise**, which is exactly what this
  programme's discipline is built to catch. It also raises the prior
  on **A4**.
- **LAWVERE / DIAGONAL SCHEME.** Lawvere's fixed-point theorem
  generalises the whole diagonal family (Cantor, Russell, Gödel,
  Turing, Tarski), and its standard reading is precisely subject–object
  duality: a fixed point is both the observer and the observed. Its
  companion, negative half — *a system cannot fully simulate itself
  within its own constraints* — is the exact shape our quine hit when
  it came up **one bit short**. **This is the prior art for A2, and it
  cuts both ways: it may supply the criterion, or explain why the
  criterion must fail.**

**Position:** the programme's contribution so far is not the question
but the **type theorem** (memo 112) — which the boundary-problem
literature does not have in derived form — and the fact that this
setting is small and exact enough to make S-A a **computation on a
finite census** rather than a philosophical dispute.

---

## §6 — THE REPO SWEEP (buried prior work) — RUN, and it changed the plan

Two sweeps (sufficiency/actualisation; closure/self-reference). Every
finding below was **re-verified by the bench against the primary
source** before being recorded — one sweep claim was false and is
corrected at the end.

**§6.1 — THE FINDING THAT RE-SPECIFIES ROUTE A2 (B1184, verbatim from
`89affd5b:frontier/B1184_quine_synthesis/FINDINGS.md` on origin/main):**

> "S4's question ('does an observer self-map have a fixed point that
> SETS the odd bit?') answers as a **SPLIT VERDICT**: the fixed point
> EXISTS for the name (QP-1's quine is exactly such a fixed point —
> … the sign (B1183: any setter would trivialize c's class; the class
> is nontrivial). **There is no object-native basepoint-setter**"
> … "| S4 (the quine / setter) | **SPLIT-ANSWERED**: quine-yes for the
> name, setter-no for the sign |" … "completion is **constitutively
> the observer's.** Awareness = the name; choice = the sign; disjoint"

**Consequence, and it is decisive for the attack:** route A2 as first
drafted — "occupied iff it supports a fixed point that sets the bit" —
is **already refuted in-corpus**. A bit-*setting* fixed point provably
does not exist; any setter would trivialise a class known to be
nontrivial. **A2 must therefore be re-specified** to the half that
does exist: the **name** fixed point. The admissible A2 question is
now: *is the subset of E supporting the self-naming (even) fixed point
a proper subset?* This is a strictly different computation from the
one §4 originally proposed, and finding it before the attack is
exactly what the sweep was for.

**§6.2 — THE PRECEDENT FOR WHAT A SUCCESSFUL CRITERION LOOKS LIKE
(B762, verified verbatim from `arc_verdict.json`):**

> "verdict": "PROVED" — "m004 is the **unique** 1-cusped census
> manifold with its spectral dataset (volume plus cusp shape), so the
> emitted word is a self-name."

An exhaustive census (203,123 manifolds) returning **exactly one**
object. This is the shape a C2-satisfying answer takes in this
programme — a sweep that selects, not an axiom that stipulates. It is
also the object-level ancestor of memos 107/108's word-level quine.

**§6.3 — PRIOR ART: AN IDENTIFICATION ATTEMPT ALREADY REFUTED.**
`frontier/B560_crossseat_campaign/cells/Cell_3/` tested "the observer
is φ" and found it **not supported** by the exact character computed
(the point is set-theoretically isolated but scheme-theoretically
doubled, Ô ≅ ℂ[[ε]]/(ε²)). **Standing warning:** this programme has
already tried once to identify the occupant with a specific structure
and failed. Any A2/A3 candidate must clear that bar.

**§6.4 — THE FUNCTIONAL CHARACTERISATIONS THAT EXIST (and their
limit).** B716/B717: *"The object supplies the boundaries; the
observer supplies every closing"*, and B717's **c-as-SWAP**: *"The
object is SWAP-symmetric; the observer is simply what BREAKS the swap
by choosing a side. Measurement = the symmetry-breaking choice."*
**But the sweep's most useful negative result: the corpus never
defines a "closer" structurally** — it is always "the closer's X", a
role attached to an unsupplied datum, never "a closer IS Y". So S-A
has **no incumbent answer to displace**, and B717's swap-breaking is
the best available *functional* handle for a criterion.

**§6.5 — THE S-LADDER, exact (B1169), and why it is a different
axis.** S2: *"AWARENESS (QP-2 boundary-visibility) = mirror-EVEN"*;
S3: *"'awareness without choice' IS the parity, exhaustively"* —
promoted by C6 completeness, *"every blanket datum classifies even
(seen) / odd (chosen), **none neither**"*. S1 is PROVED (B1183); S2/S3
pend an external check. **Note the shape:** S3 is an *exhaustiveness*
claim (nothing falls outside the dichotomy) — structurally a
criterion-shaped statement, but on the object's even/odd axis, not on
memo 114's pair-population E. Adjacent, not applicable as it stands.

**§6.6 — ONE LABELLED IDENTITY CLAIM ON THE BOOKS.**
`THE_HEARTBEAT_AND_THE_WHOLE.md`: *"The observer column IS the β-odd
column."* Interpretive-synthesis grade, not certificate grade — it
equates the *set of observer-supplied data* with an internal
eigenspace, not the occupant with anything. Recorded so it is not
mistaken for a sufficiency answer.

**§6.7 — A SWEEP CLAIM CORRECTED BY THE BENCH (filed, since it would
have been a serious relay error).** One sweep reported that
B1188–B1193 and `GRAND_COMPUTATION_v0` are "absent from this
repository entirely", casting doubt on load-bearing cross-seat
convergence claims. **This is false.** Direct check:
`git ls-tree -r --name-only origin/main` lists
`frontier/B1188_grand_retrieval` through
`frontier/B1196_close_loop_batch5b` **contiguously**, and
`89affd5b:docs/GRAND_COMPUTATION_v0.md` exists. The sweep searched the local
working tree rather than the branch. **The cross-seat primaries are
present and independently verifiable; no convergence claim is
weakened.** (The bench's standing rule — verify a relayed claim
against the primary before acting on it — is what caught this, and it
applies to its own hired agents exactly as to other seats.)

**§6.8 — an open census, worth remembering.** `THE_FRAME_CENSUS.md`
still records its own count as **not closed**: *"the observer column,
long quoted as '2 discrete + 1 continuous,' is not yet a closed
census."* A sufficiency criterion must not quietly assume it is.

---

## §7 — THE ATTACK ORDER (preregistered, to be executed only after §6)

**REVISED BY §6** (the sweep changed this before any attack ran):

1. **A2′ — the RE-SPECIFIED closure route.** Not the bit-setting fixed
   point (**refuted in-corpus**, B1184: no object-native
   basepoint-setter) but the **name** fixed point, which provably
   exists. Question: *is the subset of E supporting the self-naming
   fixed point a proper subset?* If yes → a criterion with no imported
   premise. If it is all of E → C2 fails and A2 dies clean.
2. **A4 in parallel** (independence/no-go), now with a raised prior:
   both extant outside criteria smuggle premises (§5), the corpus's
   one prior identification attempt was refuted (§6.3), and B1184
   already shows the *setter* half is impossible.
3. **A3 when cc supplies the three-regime statement** (routed).
4. **A1 last, and only with its premise named.**

**A structural hypothesis worth testing early** (interpretive,
labeled, and derived from §6.4 + B1184): if awareness = the name and
choice = the sign, and the name-fixed-point exists while the
setter cannot, then **occupation may be exactly "supports the name,
receives the sign from outside"** — which would make S-A answerable
and S-B permanently external, in one stroke. That is a two-outcome
shape, and it is the first concrete conjecture this question has.

Gate 5 untouched throughout. No attack is licensed by this document —
it defines the target, the admissibility criteria, and the order.

### ADDENDUM 1 (2026-08-28) — ROUTE A5 ADDED AND ALREADY RUN: the owner's shadow-pairing hypothesis clears all four gates
The owner proposed: *"is the observer the act of measuring — the full
object interacting with its own shadow, in an ab/ba principle?"*
Formalised as **Occ(X) := [X, gal(X)] ≠ 0** (the object failing to
commute with its own mirror) and tested in **memo 115 (GREEN)**.

**This is now route A5, and it is the leading candidate** — ahead of
A2′ — for one reason: it is the **only** proposal, inside this
programme or in the outside literature, that has actually **cleared
C1–C4**. C1 internal ✓ · C2 non-trivial ✓ (3,920 of 4,372 pass; the
torsion-free classes fail) · **C3 no imported premise ✓** — the exact
gate where IIT's maximality and FEP's blanket assumptions both fail ·
C4 testable ✓ (decided on the whole census).

Two results worth carrying forward:
- **The built-in refutation risk did NOT fire.** [X, gal X] ≠ 0 is
  **not** a re-description of the mirror-odd/torsion column — they
  differ on 372 of 4,372 classes. The predicate is genuinely new.
- **A preregistered prediction FAILED** (recorded, not patched): the
  non-commuting and unlike conditions are **independent**, not
  coincident. So the admissible predicate is their **conjunction** —
  sharper than the hypothesis as originally voiced.
- **And the interaction's own measure is mirror-blind:** κ_s =
  x²+y²+z²−xyz−2 is REAL for every class (265 distinct values).

**Attack order revised again:** **A5 first** (it alone has cleared the
gates; the next test is uniqueness — can a competing admissible
predicate be exhibited? If yes, A5 is admissible-but-not-forced; if a
search finds none, A5 strengthens sharply). A2′, A4, A3, A1 follow as
before. **A5 does not close S-A** — admissible is not correct — but it
is the first real purchase this question has had. This addendum is the
only mutation.

### ADDENDUM 2 (2026-08-28) — A5 IS NOT UNIQUE: the gates are refuted as a discriminator (memo 116), and addendum 1's promotion is withdrawn
The owner said GO on uniqueness. **Result: addendum 1 over-promoted
A5, and this addendum withdraws that promotion.** Eight premise-free
candidates select **six distinct admissible sets**; A5 has **five
inequivalent competitors**. Clearing C1–C4 is therefore **cheap**, and
"A5 clears all four gates" cannot mean "A5 is the criterion". **Memo
115's mathematics stands; its status does not** — A5 is now *one of
several admissible candidates*.

**The gate system, not the hypothesis, is what failed.** A candidate
further gate (C5, φ-stability) was tested **with its honesty note
stated in advance: it is PROPOSED, NOT FORCED** — it is not a
corollary of memo 109, since the shadow predicate is anchoring-
invariant either way (flipping sends [X, gal X] to its negative, still
nonzero), so adopting C5 would itself import a premise — the exact
failure mode diagnosed in IIT. Under it **A5 fails** (witness `b`) and
three candidates survive. That is a narrowing, not a decision.

**The real work is now named precisely:** S-A needs a gate that is
**derived** from the record rather than chosen. Until one exists, no
admissible predicate can be promoted — including the owner's, and
including any the bench prefers. **S-A: OPEN.** This addendum is the
only mutation.
