# WAVE-2 MASTERPLAN — the multi-agent workflow loop over the full residue queue
## (outside bench, 2026-08-26; preregistered BEFORE launch; owner-directed "do them all". Orchestration: one deterministic workflow; drafting agents produce candidate certificates; independent verifier agents adversarially re-run each; a completeness critic gates a retry loop; THE BENCH ITSELF re-runs and banks every surviving cell — no agent commits, no agent banks.)

### The queue (all remaining unprocessed items)
| cell | question | lane |
|---|---|---|
| A1 family_portal_fine | E8 channel: family-resolved class typing of all 270 triples; which family carries the 10-role/1-role, uniformity, and the epsilon-sign fine structure | algebra |
| A2 exchange_class | the full three-family kinematic vertex: exchange-symmetry ledger (spin −, family −, internal +) verified on the explicit sparse tensor; the bosonic-shaped total sign made exact and fenced | algebra |
| A3 mirror_deepest | the beat induced on the deepest graded layer gr3 (dim 6): involution (meridian = id there), its matrix, fixed structure, and whether it mixes the 5+1 family split of the chain tops | algebra |
| A4 peripheral_dims | dim ker(rho_Psi(lambda)−I) alone; invariance probes of ker N; the 12/15 split of ker N vs the 27's 12/15 parity split (the "coincidence" test) | algebra |
| A5 minimal_factor | the OA-C1087 positive direction: exact Weyl-dimension table for small E6 irreps; 27/27bar minimal nontrivial; the admissible category named; the residual choice fenced | algebra |
| A6 census_distinct | memo-30 census enrichment: exact centralizer/orbit dimensions for all 20 characteristics; cross-check against the cited Bala–Carter dimension list (OA-C1058 corroboration, not completeness proof — fenced) | algebra |
| B1 gaudin_close | B1151/memo-55 named follow-up: exact Gaudin (sine-kernel Fredholm) spacing law replacing the Wigner surmise; per-factor and superposition re-tests, two-outcome gates | numeric |
| B2 zeta_count | the 2469-vs-2468 data note: verify the committed zero file against direct recomputation; locate the discrepancy | numeric |
| B3 habiro_at_zeta | memo-39/40 named follow-up: the one-germ property tested at base zeta_3 (Taylor tower at a root of unity, exact in cyclotomic fields), the memo-40 table as the analogy target | numeric |
| C1 corpus_fetch | fetch origin; digest new corpus activity since main@9440f3fd, codex@78421ab3, paper@1f455266 | corpus |

### The loop
Cells draft (parallel, pipelined) → each GREEN cell goes straight to an independent adversarial verifier (fresh-dir rerun + integrity checklist: preregistration matches asserts, no tautological checks, no dead code, fences present) → completeness critic reads all verdicts → cells that failed or drew issues get ONE retry with the issue list attached → re-verify. The workflow returns a structured summary; the bench then re-runs every surviving certificate in-lane, writes the memos, INDEX rows, and commits — banking authority stays with the bench.

### Discipline carried into every agent prompt
Exact arithmetic only; preregistered asserts; two-outcome cells; errors filed at point of occurrence; Gate 5 untouched; CITED steps labeled; no git commands; no model identifiers in artifacts.
