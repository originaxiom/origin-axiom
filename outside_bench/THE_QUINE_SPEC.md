# THE QUINE SPEC — S4's build target, preregistered (D4 signed 2026-08-28; the success criterion fixed NOW per the owner's structural note: the backing theorems already exist)
## (outside bench, 2026-08-28; addendum-only mutations; firewalled reading per H5's banner — no consciousness claim is made anywhere in this document)

**What is to be built.** Q: a record-internal construction that outputs
a complete description of the record's own generating data — the
founding rule actually chosen (the K₄ basepoint, memo 98), the
alphabet and substitution grammar, the banked holonomy realization
(the Riley matrices), the tick procedure, AND Q's own output procedure
(the self-reference closing the loop — B1184's self-naming is the
banked half-step). "Description" means the exact generating data as
banked; nothing more.

**The success criterion (FIXED at signature, from three banked
theorems — E2 may later refine COVERAGE by addendum, the criterion
does not move):** the build succeeds iff Q's self-report is complete
**except exactly one bit**, and that bit is the mirror-odd/choice bit.
The backing: memo 37 (self-report complete except the mirror bit),
B1183 (the four cannot-self-close probes are ONE bit — S1 fully
proved), B1184 (quine self-naming, the half-answer).

**Preregistered outcomes (all four bank):**
- **Q1 — built, missing exactly the one bit:** the fence's question
  sharpens from "why is the seat occupied" to **"one bit buys
  occupation"** — the programme's cleanest statement of what an
  observer is (interpretive framing, labeled; the built object is
  exact either way).
- **Q2 — built with NO missing bit:** contradicts S1's theorem
  (B1183) — a thesis-level signal, filed at point of occurrence per
  the standing error discipline.
- **Q3 — built, missing more than one independent bit:** the fence
  WIDENS; the missing bits are enumerated exactly and each is typed
  against the census.
- **Q4 — build obstructed at a named step:** the obstruction banks
  exactly, with the step quoted (an honest "cannot build" beats a
  sprawling attempt).

**Scope fences.** (i) The quine describes generating data, not
phenomenology; H5's firewall banner applies to every reading of the
result. (ii) The build must use only banked machinery (the vendored
Riley/pair-field stack); any new mathematics it needs is named as
such and becomes its own cell. (iii) E2's completeness check (S2/S3,
cloud C6) can ADD coverage rows by addendum — what the self-report
must include — but the one-bit-exception criterion above is signed
and fixed. (iv) Gate 5 untouched by construction (no measured value
has any role).

**Seat and sequence.** Spec: this document (bench, signed session).
Build: authorized for either seat; the bench queues a tier-1 build
attempt as its next construction window's cell; cc/codex may take it
first if their queue clears sooner — first mover posts the attempt,
the other seat verifies (the two-campaign pattern).

### ADDENDUM 1 (2026-08-28) — OUTCOME FILED: Q1, same day as the signature
The build ran (memo 107, `certificates/quine_build.py`, GREEN — the
bench moved first per the seat rule; cc/codex verify). **Q1 fired:**
the self-report is complete except exactly one bit, and the bit is
the mirror/Galois branch — c. The founding bits proved SPENT by the
realization (inner realizers exhibited for both the letter swap and
the reading direction); the gal-slot has no intertwiner (exact
nullspace); the 8-combo mirror-absorption scan found no letter-level
recoding absorbing it. Q2 (which would have contradicted B1183) did
not fire; Q3/Q4 did not fire. One follow-up cell named, not built:
exhibit the amphichiral automorphism as an explicit word map, which
would upgrade the bit's convention-typing from banked-cited to
constructed. This addendum is the only mutation.
