# SEAL — SIX CUSPS, AND WHETHER GOING THERE RESTORES WHAT B1064 DELETED

**Sealed 2026-08-31, pushed BEFORE the census runs.** Preparation for posing L154 as a specialist
ask (the owner's *"lets pose it"*). Gate 5: pure topology, no measured value.

## 0. Why this census, and what it is FOR

`B1190`/GC-6 types the σ gap as **"six cusp-boson units where `T[4₁]` supplies one"**, attributing
the *one* to the object having **one cusp**; `B139` banks *"coupling count = cusp count."* So the
obvious move is: **go to a cover with six cusps.**

But `B1064` names a **deeper** obstruction than the count, and it is the one the ask must carry:

> *"the object's amphichirality deletes the quantized sector of its own boundary action
> (`CS = 0 ⟹ ∂S/∂k ≡ 0`), and rational `(E₆)₁` data is quantized-sector content; the surviving
> unquantized sector carries no banked chiral algebra."*

with the missing mechanism typed as either **(a)** restore a quantized boundary sector the
amphichirality does not delete, or **(b)** attach rational data to an unquantized sector.

**A cover is not "the object's own action", so a cover is the natural candidate for (a).** This cell
tests it before anyone is asked.

## 1. Cells

### C-1 — **do six-cusped covers of m004 exist, and at what degree?**
Enumerate covers of `m004` by degree; record degree, cusp count, volume. **C1-SIX-EXISTS** (with the
minimal degree named) vs **C1-NO-SIX** (within the searched degree range, stated).

### C-2 — **does the cover route restore the quantized sector?** *(the load-bearing cell)*
Compute the Chern–Simons invariant of every cover found. **C2-ALL-ZERO** (every cover has
`CS ≡ 0`, so `∂S/∂k ≡ 0` survives the cover and **route (a) via covers is CLOSED**) vs
**C2-SOME-NONZERO** (a cover carries a quantized level, and **route (a) is OPEN with an exhibit**).

### C-3 — **the volume ledger.** Report each cover's volume in units of `V_reg = Vol(m004)/2` and of
`Vol_orb = Vol(m004)/12`, to connect the census to the banked meter (`B1191`).

## 2. Declared priors, before running

- **C1-SIX-EXISTS.** I expect six-cusped covers to exist, at degree ≥ 6.
- **C2-ALL-ZERO**, and I expect it **for a reason I state now so the computation can refute it**:
  the Chern–Simons invariant is **multiplicative under covers**, `CS(M̃) = d·CS(M)`, so
  `CS(m004) = 0` forces `CS ≡ 0` on every cover. **If C2 returns SOME-NONZERO, my stated reason is
  wrong and that is the more interesting outcome** — it would exhibit route (a).
- **Consequence I am pre-committing to:** if **C1-SIX-EXISTS and C2-ALL-ZERO** both land, then
  **six cusps are available and do not help**, and memo 168's inference (*the σ bridge and the
  multi-cusp wall are the same wall*) is **too weak**: the cusp count is reachable and the real wall
  is `B1064`'s quantized sector. **I will say so against my own memo of this morning.**

## 3. Fences

- A cover census is topology. It says nothing about whether any `T[M̃]` has `c = 6`.
- `B1064`'s O3 stands whatever this returns; this cell tests **one named route past it**, not the
  obstruction itself.
- `B486`'s kill stands (the cusp is rectangular, disc −48) and is not revisited.
- Nothing is sent. Preparing an ask is not sending it.

---

## ADDENDUM (2026-08-31) — OUTCOME: **C1-NO-SIX | C2-ALL-ZERO.** One prior lost, and it sharpens the ask.

- **C-1 — `C1-NO-SIX`, AGAINST my declared prior.** I sealed `C1-SIX-EXISTS` at degree ≥ 6. **No
  cover of `m004` has six cusps up to degree 8** (38 covers examined). **Max cusps by degree:**
  2→1, 3→1, 4→2, 5→**3**, 6→2, 7→**3**, 8→2. The cusp count does not grow with degree; it caps at
  **3** in the whole low-degree tower. **The naive six-cusp route is not merely unhelpful — it is
  not available.**
- **C-2 — `C2-ALL-ZERO`, as declared, for the declared reason.** All 38 covers have
  `|CS| ≤ 8.662e-16`, i.e. ≡ 0, exactly as `CS(M̃) = d·CS(m004) = 0` predicts. **`∂S/∂k ≡ 0` survives
  every cover, the quantized sector stays deleted, and `B1064`'s route (a) via covers is CLOSED.**
- **C-3 — the volume ledger is clean and exact:** every cover's volume is `2d·V_reg = 12d·Vol_orb`
  for degree `d` (4/8/12/… in `V_reg`, 24/48/72/… in `Vol_orb`), reproducing `B1191`'s meter with no
  residual.

## The pre-committed consequence, honoured

The seal said: *if C1-SIX-EXISTS and C2-ALL-ZERO both land, memo 168's inference is too weak and I
will say so against my own memo.* **The outcome is stronger than that branch.** Memo 168 inferred
*"the σ bridge and the multi-cusp wall are the same wall."* **That inference is WRONG as an
identification of the operative wall**, and now for two independent computed reasons:

1. six cusps are **unavailable** in the cover tower (max 3 through degree 8), and
2. they **would not help** if they were — `CS ≡ 0` is inherited by every cover.

**The operative wall is `B1064`'s, not the cusp count**: the object's amphichirality deletes the
quantized sector of its own boundary action, and `(E₆)₁` is quantized-sector content. **The cusp
count was a symptom; the deleted quantized level is the disease.** Memo 168 §4 is corrected in place.
