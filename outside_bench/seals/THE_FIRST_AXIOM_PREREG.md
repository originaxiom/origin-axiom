# PREREGISTRATION — THE FIRST AXIOM: what F9 prices, and what A3 actually buys

**Sealed 2026-09-13, before the certificate was written.**
**Outcomes state what will be OBSERVED. Consequences live in the memo.**

## Occasion

The owner: *"check new work from other branches, theres a theoreme about figure 8 being only with
cusp that fixes first axiom."*

The first axiom is **`docs/UNIQUENESS_THEOREM.md` A1** — *"Two-record substrate. The state space is
`ℤ²` … (Not one; not three.)"* The theorem is **fork F9, banked at B1323 on main 2026-09-09** — not
on a branch. **CELL 1 verifies it rather than repeating it.**

The adjacent question, which is the one that could move the programme's price: the physics-seat
branch's **T5 A6-relabeling audit** proposes re-typing the **orientation** axiom as the observer's
closing #0, under which *"the axiom count at the entrance drops from three to two-plus-one-closing"*
— i.e. **4 axioms → 3**, and memo 223's price **12 → 11**. That audit records **one typed residual**:

> *"the GL(2,ℤ)-level uniqueness statement — that A1, A2, A4–A6 WITHOUT A3 force M up to the swap
> (the matrix-level parent of the forcing, whose mapping torus is m000) — is **NOT computed anywhere
> in the record**. … Type: a finite symbolic computation over GL(2,ℤ) monoid words."*

**CELL 3 computes it.** Nothing here adopts the relabel; the bench does not edit main's axiom count.

---

## CELL 1 — does F9 say what it is being cited for, and is it on main?

* **OUTCOME A** — B1323 is present at `origin/main`, its verdict is **F9 ROBUST on both carriers**,
  and its own text states the cusp discriminator: the two-record carrier `S₁,₁` returns **m004 with
  ONE cusp in ℚ(√−3)**, and the minimal three-record surface bundle is the **Whitehead link
  complement with TWO cusps in ℚ(i)**.
* **OUTCOME B** — any of those is absent or differs.

## CELL 2 — does F9 DISCHARGE A1, or price it?

* **OUTCOME A** — B1323's own *"Not claimed"* section disclaims discharging the genesis, so A1
  remains an axiom and **the axiom count and memo 223's price are unchanged at 4 and 12**.
* **OUTCOME B** — B1323 claims A1 is derived, in which case the count must drop.

## CELL 3 — the typed residual: without A3, what do A1, A2, A4–A6 force?

Primitive one-channel updates (A4) generalized to `GL(2,ℤ)` (A2) **without** the `det = +1`
restriction (A3). The certificate enumerates monoid words over the primitive generators **and the
swap `S = [[0,1],[1,0]]`**, applies **A5** (torsion-free closure: `|det(B − I)| = 1`) and **A6**
(minimal hyperbolic complexity), and reports what survives.

* **OUTCOME A** — the filters select, up to the `L ↔ R` swap symmetry (A7), **exactly one** mixed
  class, and it is `M = L·S = [[1,1],[1,0]]` — `det = −1`, the unsquared golden matrix, whose
  mapping torus is the Gieseking manifold **m000**; and `M² = LR = A`, the SL-level answer.
* **OUTCOME B** — the filters select something else, more than one class up to A7, or nothing.

## CELL 4 — is the SL-level minimum recovered by the same code?

* **OUTCOME A** — restricting the same enumeration to `det = +1` returns **`A = LR`, trace 3**, the
  banked answer, so CELL 3's instrument reproduces `UNIQUENESS_THEOREM` §3 before being trusted on
  the new case.
* **OUTCOME B** — it does not.

---

## CONTROLS

* **C1 (the Anosov test must bite in both directions).** For `det = −1` the hyperbolicity condition
  is **not** `|trace| > 2`. The certificate must exhibit a `det = −1` matrix its test calls
  hyperbolic **and** one it calls non-hyperbolic, with eigenvalues printed, or CELL 3 is void.
  *(memo 164: control passing is not instrument working.)*
* **C2 (the torsion formula is the corpus's, not a fresh one).** `|H₁ torsion| = |det(B − I)|` must
  reproduce `UNIQUENESS_THEOREM`'s own locked values on the `SL` grid before being used at `det = −1`.
* **C3 (the manifold identification is not assumed).** The claim *"M's mapping torus is m000"* is
  taken from the record (`B749/F5`, the paper's own orientation paragraph) and **labelled as
  inherited, not recomputed here** — no SnapPy identification is claimed by this certificate.
* **C4 (every quotation located).** Each verbatim quote is asserted as a substring of the tracked
  file it is attributed to, at the stated ref. A quote that cannot be located FAILS the run.

## FENCES, declared in advance

* **This certificate does not adopt the A6 relabel and does not change any axiom count.** It
  computes the datum the audit named as missing; whether that datum licenses the relabel is main's
  call, not this bench's.
* **CELL 3 settles a MATRIX-LEVEL statement only.** It says nothing about which manifold the
  observer should take, and nothing about orientation being "really" a closing.
* **Gate 5 untouched. Nothing promotes. No arc retracted.** `docs/` outside the owner-authorized
  `THE_SM_VERDICT` fix is not edited.
