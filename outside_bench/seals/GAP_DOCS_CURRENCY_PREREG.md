# SEAL — THE CURRENCY PASS ON THE THREE GAP DOCUMENTS

**Sealed 2026-08-30, committed and pushed BEFORE any retrieval.**

Subject: this bench's own three gap documents — `THE_TOE_GAP.md`, `THE_FULL_ACCOUNTING.md`,
`THE_GRAND_TABLE.md`, all written **2026-08-28** against main at the B1193/B1194 band — audited
against main at **`89affd5b`** (B1217 + the paper), the same pin memos 148–152 used.

**Why:** `THE_CHAIN_GAP.md` §4 item 4 found one row already stale — `THE_FULL_ACCOUNTING.md`'s open
item 5, *"the 953 class-group step — disc-6237 cubic field"*, closed at proof grade by `B1093`
(h = h⁺ = |Cl/Cl²| = |Cl/Cl³| = 1). An open-items list carrying a closed item is exactly the
currency hazard the paper's own spec §9 names, and these are **this bench's** documents. A seat that
audits others' currency and not its own is not an auditor.

---

## 0. The binding clauses

1. **No mechanical hit is a verdict until the arc is read.** Four keyword detectors of mine have
   needed checking against themselves this session (`vol`/`involves`, the Q2 staleness flag, the
   audit-harness drift, the queue-join status filter). **Any count this cell cannot defend by
   reading will be reported and discarded**, as the queue-join count was.
2. **Addendum-only.** Stale rows are corrected by dated addenda, never by editing the original text.
   A gap document whose history is rewritten cannot be audited for currency again.
3. **Blind/confirmatory declared per cell**, below.

## 1. The cells

### E1 — are the documents' open items still open? **CONFIRMATORY** (one stale row already known)
Extract every item the three documents assert is OPEN / MISSING / ABSENT / to-do. For each, search
the arcs at the pin for a closure, and **adjudicate by reading**.
- **E1-MINOR** — at most the one already-known stale row (`FULL_ACCOUNTING` item 5).
- **E1-SYSTEMIC** — two or more.

*Declared honestly:* I already know one. This cell is therefore not blind, and its interest is
entirely in whether the count is 1 or more than 1.

### E2 — the reverse direction: did anything the documents call CLOSED get reopened? **BLIND**
A currency pass that only looks for "still open?" will miss the more dangerous error: a row banked
as delivered, later weakened or refuted.
- **E2-NONE** — no delivered/closed row is contradicted downstream.
- **E2-REOPENED** — at least one is.

### E3 — do the documents carry the absences banked *after* they were written? **BLIND**
Arcs from B1194 to the pin. A gap list that does not grow is not being maintained.
- **E3-COMPLETE** — no post-08-28 absence is missing from all three documents.
- **E3-INCOMPLETE** — at least one is.

### E4 — do the three documents agree with each other? **BLIND**
They partition the same material three ways (KIND A/B/C · Registers 1/2/3 + nine items ·
DELIVERED/CONDITIONAL/SEALED/MISSING/NULL). Test whether the same row carries a consistent status
across them.
- **E4-CONSISTENT** — no row is typed incompatibly across documents.
- **E4-DIVERGENT** — at least one is, and each is named.

## 2. What this cell may not do

It may not rewrite the documents. It may not re-rank the gaps. It may not use a count it cannot
defend by reading. And **finding this bench's own documents stale is the expected outcome of a
currency pass, not a scandal** — the documents were correct when written; the corpus moved. The
failure mode worth reporting is not staleness but **staleness that changes an answer given to the
owner.**

## 3. Gate 5

No measured SM value enters. This cell reads text and compares text.

## 4. Standing

Nothing transmitted. `golden_gate` receives nothing.
