# SEAL — Q7: THE ROUTE THE LITERATURE FLOOR DID NOT REACH

**Sealed 2026-08-30, pushed BEFORE any computation.**

## 0. Why re-open a question I typed NOT OURS

`THE_GATE_OWNERSHIP.md` typed Q7 external because what remains is *"a theorem that does not
exist."* **Gate C taught me not to trust an inherited typing** (memo 157: the gate's answer was
banked and uncited). So the typing gets tested.

**Exhausted first, and the result is the reason for this cell:**
`already_banked.py` on `strong approximation class number stabilizer simply connected` — and a
direct regex over all 1119 arcs — returns **ZERO arcs mentioning strong approximation.**

**Strong approximation is the standard tool for exactly this question.** `B990` asks for the count
of `G(ℤ)`-orbits inside a `G(ℚ)`-orbit. That count is governed by the **class set of the
stabilizer**, and Kneser–Platonov strong approximation makes a class set **trivial** when the
stabilizer is simply connected, ℚ-simple, and isotropic at ℝ. `B1099`'s literature floor read
Krutelevich, Thorne and Bhargava–Gross — **all orbit-parametrisation sources — and did not reach
the group-theoretic tool.**

## 1. Cells

### R-1 — the arithmetic of K · **exact, computable**
`K = ℚ[x]/(x³−12x−5)`. Verify irreducibility, the signature (real vs complex places), the
discriminant, and re-derive `B1093`'s `h(K)=1` independently if cheap.

### R-2 — the stabilizer's dimension · **exact, computable**
By dimension count over the chain E₆ → (stabilizer of a rank-3 element of the 27) → (stabilizer of
a generic further element). Report the dimension and the type it forces.

### R-3 — the strong-approximation hypotheses · **the adjudication**
For each hypothesis (simply connected · ℚ-simple · ℝ-isotropic · the orbit-count bijection), state
**HOLDS / FAILS / UNVERIFIED-HERE**, with the reason.

### Verdict
- **R-ROUTE-NAMED** — a standard, checkable route exists that the literature floor missed, with its
  unverified hypotheses named exactly.
- **R-NO-ROUTE** — strong approximation provably cannot apply here, and the reason is stated. **That
  is also a result**: it would tell a specialist not to spend time on it.

## 2. THE OVER-CLAIM FENCE — binding, and the reason this seal exists

If this route worked it would **cross `B990`'s orbit-to-point gap** and convert a freedom-ledger row
from a supplied menu to a derived point. **That is the single most over-claimable result available
to this bench**, and this session has already produced a vacuous control, a hardcoded audit cell,
and two false state claims.

So, binding:

1. **This cell may NOT conclude that Route A crosses.** The most it may conclude is that a named
   route exists and which of its hypotheses are unverified. Anything stronger requires the
   stabilizer to be *identified*, not *guessed from a dimension count*.
2. **A dimension count is not an identification.** Dimension 28 is consistent with D₄ and with other
   things; matching a dimension is evidence, not proof, and must be labelled evidence.
3. **`B990`'s declared prior was UNFAVOURABLE and stands unrepudiated.** Nothing here repudiates it.
4. **Every step of the group theory I do not verify in-cell is marked UNVERIFIED-HERE**, even where
   I am confident, because confidence is what produced three of this session's errors.

**Declared prior:** **R-ROUTE-NAMED, with the stabilizer identification UNVERIFIED.** I expect to
name a route and *not* close it. If I find myself concluding Route A crosses, the fence above says
that is the moment to stop and re-read.

## 3. Gate 5

No measured value. Number theory and Lie theory only.
