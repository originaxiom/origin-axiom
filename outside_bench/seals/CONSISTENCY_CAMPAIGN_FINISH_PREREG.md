# PREREGISTRATION — FINISHING THE CONSISTENCY CAMPAIGN (C-2, C-3, C-4, C-6)

*Outside bench, 2026-09-14. Sealed before the certificate runs. Gate 5 untouched.*

## Why

`docs/GRAND_COMPUTATION_v0.md` §6b (B1229): *"This schedule asks the OBJECT to supply each row — **the
one thing B1225 proves it cannot do**. Physics reduces parameter counts by **consistency conditions**,
not derivations. … **before trying to DELETE a row, ask what CONSISTENCY says about it — a classified
finite menu plus a picker is already a parameter-free theory with one calibration.**"*

That became `docs/CONSISTENCY_CAMPAIGN.md`, opened **2026-08-31** on the owner's instruction *"craft a
campaign and do it properly"*. **Three of six cells ran** (B1230: C-1, C-5, C-5b). **C-2, C-3, C-4, C-6
did not.** This cell discharges what can be discharged and reports the rest as blocked, on whom.

## THE CAMPAIGN'S OWN STOP RULES — inherited verbatim, binding here

1. **No cell may claim a row DELETED.** Cells re-TYPE rows (continuum → finite → bit) or name the
   decisive retrieval.
2. **Base-field discipline:** every continuous-parameter count states the field it is taken over.
3. **A cell that cannot fail does not run** (MB12).
4. **`already_banked` before every novelty claim.**
5. **Cited ≠ verified.** A cert re-run is not a re-derivation; say which one happened.

## THE CELLS, with their two outcomes COPIED VERBATIM from the campaign

| cell | question (verbatim) | outcomes (verbatim) |
|---|---|---|
| **C-2** | *"The ℤ/12 character assignment (codex holds it; B1216's R1). Decides identity-vs-scalar on B₀."* | *"**A: identity** → ℙ³ is real, floor carries 3 continuous params / **B: scalar** → B₀ is 1-dim over ℚ(ζ₁₂), ℙ = a point, **zero**"* |
| **C-3** | *"λ as a KMS weight. Is the modular (Tomita–Takesaki) flow periodic for a rational boundary? Periodic ⇒ λ is a finite label."* | *"**PERIODIC** (λ joins the finite menus) / **NOT** (λ stays a genuine continuum; the floor carries it)"* |
| **C-4** | *"The r-label via the T-matrix. T has finite order (12 at E₆). Is the arrow a branch choice of finite order?"* | *"**FINITE-PLACE CONFIRMED** (matches B1182's typing from an independent direction) / **UNRELATED**"* |
| **C-6** | *"The ledger amendment. Rewrite §5's input slots with the consistency typing and the field of each count."* | *(bookkeeping; runs last)* |

## WHAT EACH CELL WILL ACTUALLY DO HERE — declared, so nothing is dressed up afterwards

- **C-4 — RUN.** Build the **(E₆)₁ modular data exactly** — 3 primaries (E₆'s centre is ℤ/3),
  **c = 6**, conformal weights **h = (0, 2/3, 2/3)**, `T = diag(e^{2πi(h_j − c/24)})`, `S_jk = ω^{jk}/√3`
  with ω = e^{2πi/3} — and compute **the exact order of T**. Then ask C-4's question against **B1182's
  banked typing**, which `GRAND_COMPUTATION_v0` §5 records as: *"the r-label (arrow) | one ℤ/2 seed,
  **FINITE-place** | B1182: r → k7 (fixes K pointwise, flips √3), finite orbits"*.
  **This is a CROSS-CHECK, not a new reduction** — the r-label is already typed finite; C-4 asks whether
  modular data confirms it from an independent direction. Stated now so outcome A is not reported as a
  fresh deletion.
- **C-3 — NOT RUN, and reported as blocked.** Tomita–Takesaki periodicity for a rational boundary is
  operator-algebra, not arithmetic; this bench has no instrument and will not manufacture an argument to
  avoid saying so. What would settle it is named instead.
- **C-2 — NOT RUN, and reported as blocked.** The campaign's own status column reads **RELAY** and
  *"codex holds it"*. Externally held; named, with what is needed and from whom.
- **C-6 — RUN, over what actually ran**, and no further.

## CONTROLS

| # | control | catches |
|---|---|---|
| **T1** | the modular relations must hold on the constructed data: **S² = C** (charge conjugation), **(ST)³ = S²**, S symmetric, S unitary | data that is not an MTC at all — the single strongest check available, and it can fail |
| **T2** | **T's order must come out 12**, matching the campaign's own parenthetical *"(12 at E₆)"* — computed from the exact roots of unity, **not asserted** | a T built to give the answer |
| **T3** | the central charge must satisfy the **Gauss-sum condition**: `Σ_j d_j² θ_j / √(Σ d²) = e^{2πi c/8}` with c = 6 | a wrong c/24 phase convention silently producing a wrong order |
| **T4** | the count of cells RUN vs BLOCKED is printed, and **at least one must be blocked** if C-2/C-3 are genuinely blocked — a campaign that reports everything green is not this campaign | the fourth mis-specification of this programme: a seal looser than the thing it tests |

## WHAT THIS MAY NOT CONCLUDE

**No row is DELETED** — stop rule 1. **σ is not claimed to be 1**: the campaign's own closing line says
so, and C-5 already **refuted** B1229's uniqueness argument (*"c = 6 has FOUR solutions, not one"* —
A2 level 9; A6, D6, E6 all at level 1, since at level 1 c = rank), so any statement leaning on the σ
menu must carry the two-character restriction **as a hypothesis** (#26). **Nothing about the SM.** No
value. Gate 5 untouched.
