# PREREGISTRATION — DOOR 3: does opening KMRT §43 open B882's conjecture?

**Sealed 2026-09-14, before the certificate was written.**
**Outcomes state what will be OBSERVED. Consequences live in the memo.**

## Why this cell exists, in the record's own words

`WHAT_WOULD_COUNT` §4A.3 (Tier-VALUE-COMPRESSION) names **three successor doors** as *"the only
remaining licensed targets"*. The third is **B882's arithmetic-S₃ = geometric-S₃ conjecture**, and
**B1077 (2026-08-19)** ranks it:

> *"**B882's conjecture (arithmetic S₃ = geometric S₃) is now the UNIQUE non-circular route to a
> 77-mechanism**, its in-sandbox half done here; the remaining half is **the KMRT §43 connective
> proposition — a typed literature floor, the named external need**."*

`NOVELTY_SWEEP_LEDGER` row 6 fixes the decision route:

> *"Row state: the echo stays OBSERVATION-UNWEIGHTED … **moves only by (a) KMRT §43 opened + an
> independent dressing computation, or (b) B882's conjecture proved** — either converts the echo to
> DERIVED, not FORCED-from-the-algebra."*

**Clause (a)'s first half is now supplied** — the owner delivered KMRT on 2026-09-13 and memo 222
read §§36, 43, 44 and banked the ⁶D₄ typing plus `[E] = 1`. **This cell asks what that buys.**

## The conjecture, as W4 states it non-presumptively

> **C77:** the cubic étale `L` canonically attached to the **object-dressed** `(A,σ)` is `≅ K`.

with the banked sub-results this cell must not re-litigate:
* the trialitarian datum is `z(⟨x₈,x₁₆⟩) = so(8)⊕u(1)²` at the measured plane, packaged as KMRT's
  `(A,σ) = (End(𝕆_split), norm involution)`;
* **the UNDRESSED datum attaches the SPLIT cubic `ℚ×ℚ×ℚ`** — *"the 77 is NOT in the bare algebra"*;
* **the circularity theorem-let:** the bank contains exactly ONE order-3 Galois-permuted triple (the
  enhancement lines) and its resolvent already **is** `K`, so **every in-bank dressing route makes
  C77 true BY FIAT**.

---

## CELL 1 — does KMRT attach a cubic to `(A,σ)` at all, or is the cubic separate data?

* **OUTCOME A** — the cubic `L` is **part of the trialitarian datum** `T = (E, L, σ, α)` and is not
  determined by `(A,σ)` alone; what `(A,σ)` canonically carries is the **quadratic** discriminant
  algebra (the centre of `C(A,σ)`), not a cubic.
* **OUTCOME B** — KMRT exhibits a canonical cubic attached to `(A,σ)` alone, in which case this
  cell computes it for the object's `(A,σ)` and compares it to `K`.

## CELL 2 — is the same Cayley algebra compatible with MORE THAN ONE cubic?

KMRT **§36.C** extends the twisted-composition construction *"to an **arbitrary cubic étale
algebra** `L`"* from a para-Hurwitz `(C, ⋆, n)`, and **§43.7** makes `End(Γ)` a trialitarian algebra.

* **OUTCOME A** — from **one** Cayley algebra `C`, a trialitarian algebra exists for **every** cubic
  étale `L` — so `C` does not select `L`.
* **OUTCOME B** — the construction obstructs for some `L`, and the obstruction is stated.

## CELL 3 — is the cubic a FREE classifying parameter? (the decisive one)

KMRT **§44.1** gives a **split** exact sequence `1 → PGO(C,n) → PGO(C,n)⋊S₃ → S₃ → 1`, and **§44.5**
says `H¹(F, PGO₈⁺⋊S₃)` classifies trialitarian algebras with the second map sending `T = (E,L,σ,α)`
to **the class of `L`** in `H¹(F, S₃)`.

* **OUTCOME A** — the sequence is split, hence the map **trialitarian algebra ↦ its cubic** is
  **SURJECTIVE** onto all cubic étale algebras: **every cubic, `K` included, is some trialitarian
  algebra's cubic, and the formalism forces nothing.**
* **OUTCOME B** — the map is not surjective, and the image is computed.

## CELL 4 — what the decision route's clause (a) is worth, now that its first half is paid

* **OUTCOME A** — clause (a)'s **second half — "an independent dressing computation" — is untouched,
  and nothing in KMRT supplies it**: the literature floor is paid and the door does not open.
* **OUTCOME B** — KMRT supplies or constrains the dressing, and the constraint is stated.

---

## CONTROLS

* **C1 (every quotation located).** Each KMRT quotation is asserted as a substring of the supplied
  PDF's extracted text, NFC-normalised, with its section. A quote that cannot be located **fails the
  run**. *(memo 226's RULE #26: a theorem's hypotheses are load-bearing and a paraphrase is where
  they go missing — so hypotheses are quoted, not summarised.)*
* **C2 (the decoy).** A sentence built to look like KMRT's and to be false about it must be reported
  **ABSENT** by the same instrument, or C1 is vacuous.
* **C3 (the record's own statements are quoted, not recalled).** B1077's ranking, W4's C77 and the
  circularity theorem-let are asserted as substrings of the tracked files at `origin/main`.
* **C4 (this cell may not prove C77 by fiat).** If any step uses the enhancement triple, or anything
  whose resolvent is `K` by construction, **the run fails** — that is precisely the circularity the
  theorem-let forbids.

## FENCES, declared in advance

* **This cell does not attempt B882's conjecture.** It settles what clause (a) buys. Route (b)
  remains open either way.
* **No arc is retracted.** B1077, W4 and B888's *"observation, unweighted"* stand.
* **Gate 5 untouched. Nothing promotes.** `docs/` is not edited.
