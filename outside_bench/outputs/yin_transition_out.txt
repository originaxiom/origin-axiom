Y1 — THE CUSP COINCIDENCE:
    z_1 = 2 + -4*omega,  Re(z_1) = 0  => PURELY IMAGINARY
    and z_1 = -(4 omega - 2) = -tau EXACTLY, where tau is memo 100's
    GRAMMAR-DERIVED cusp modulus (the longitude baBAABab's holonomy,
    the disc-48 lattice generator, the r-bit's carrier).
    z_1^2 = (-12, 0) — real, because (2 omega - 1)^2 = -3.

Y2 — THE IMAGINARY -> REAL TRANSITION (omega-component per level):
     n       Re(z_n)  Im-coeff(z_n)   Im-coeff(k_n)  status
     0           3/2             -1               1  complex
     1             0             -4               0  z imaginary
     2           -10              0               0  REAL
     3           146              0               0  REAL
     4         14402              0               0  REAL
     5     306950402              0               0  REAL
     6  63666916514841602              0               0  REAL
    => the tower is NON-REAL for exactly levels 0 and 1, and REAL from
    level 2 onward.  Once real it stays real: the recursion is
    integer arithmetic in (z, kappa), with no route back to the
    omega-component.  The crossing happens ONCE and is irreversible.

Y3 — THE TRIPLE COINCIDENCE AT LEVEL 1 (three unrelated arcs, one level):
    (i)   kappa_1 = -2 — EXACTLY B496's Markov surface kappa = -2
          (the locus whose mixed semigroup B496 studied);
    (ii)  z_1 = -tau = -2 sqrt(-3) — EXACTLY memo 100's grammar-derived
          cusp modulus, the disc-48 carrier of the r-bit;
    (iii) Re(z_1) = 0 — the coordinate is PURELY imaginary, the being
          field's pure part, and squaring it is what makes everything
          real thereafter.
    Three facts, from B496, from memo 100, and from the field split,
    meeting at the same single level of the record's own tower.

INTERPRETATION (labeled; the computation above is exact, this is not):
  The record's own arithmetic already HAS a two-sided structure — B721
  puts it plainly: the two clocks "differ in FIELD (hearing REAL
  Q(sqrt5) vs being IMAGINARY Q(sqrt-3))".  So the programme is not
  built on the positive side alone; it is built on a field that IS the
  imaginary one, with a real partner.
  And this cell locates the crossing precisely: the record's layering
  dynamics begins in the imaginary/being field, passes at level 1
  through the PURELY imaginary cusp modulus — the same 2 sqrt(-3) the
  grammar derives as the r-bit's carrier, at the same level where
  kappa sits on B496's Markov surface — and is REAL from level 2
  onward, irreversibly.
  So the honest shape of the answer to "are we doing yin only": the
  two sides are both present and the record knows which is which; what
  this cell adds is that the CROSSING BETWEEN THEM is a specific,
  computable event, happening once, at the cusp.  Whether the
  programme has WORKED both sides equally is a different question —
  a corpus-balance question, not a mathematical one — and it is not
  answered here.
Gate 5 untouched.
