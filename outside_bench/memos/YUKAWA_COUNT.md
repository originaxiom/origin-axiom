# THE YUKAWA COUNT — the closing admits exactly FOUR independent Yukawa-shaped couplings on one 27: the three slot determinants and the mixed trace, of which e₆ keeps exactly the Jordan one — and that one is the beat's
## (outside bench, 2026-08-25; thirty-fifth memo; campaign cell B2; structure only, no values, Gate 5 untouched; every claim exact)

### The question
Memo 32 proved the e₆-invariant trilinear on the 27 is unique (the Jordan cubic C,
±1-integral, beat-covariant). The physical coupling space, though, is governed by the
CLOSING's symmetry, not all of e₆: how many independent Yukawa-shaped (symmetric
trilinear invariant) couplings does the trinification subalgebra admit? Preregistered:
4 — the classical det+det+det+trace count — realized exactly on the bench's crystal
basis, or a finding either way.

### THE THEOREM (`certificates/b2_yukawa.py`, all exact; the A2-triple found from scratch in-frame)
An orthogonal A₂-triple (3 × 6 = 18 roots, pairwise orthogonal, full rank) is
constructed by search in the working frame; under it the 27 splits into the three
9-dim bi-fundamental blocks (slot signatures (1,1,0), (1,0,1), (0,1,1) — 9+9+9,
verified). Then, by exact nullspace over the 45 weight-zero triples:
1. **dim Inv_sym³(27) under sl₃³ = 4.** The nullspace basis self-identifies
   structurally: **three vectors of exactly 6 nonzero coefficients, each supported on
   a single block** — the three slot determinants (a 3×3 determinant has exactly six
   monomials) — **and one vector of exactly 27 coefficients touching all three
   blocks** — the mixed trace tr(abc). The classical count, realized monomial-count
   for monomial-count.
2. **Adding the remaining 54 root generators cuts the space to dim 1** — e₆ keeps a
   single combination, and memo 32 already identified it: the Jordan cubic C, the
   beat-covariant one.

> **So the coupling ledger of the landing reads: FOUR structural Yukawa slots at the
> closing's symmetry; ONE of them survives to the object's full algebra; and that
> one is exactly the coupling the beat preserves on the nose. Any Yukawa texture the
> programme ever writes decomposes over these four exact directions.**

### What is and is not claimed
Claimed: the dimension and the block structure, exact. NOT claimed: any coupling
VALUE, any texture, any generation structure (this is one 27; three families would
live in 27⊗27⊗27 of the family triplet — a different, larger count, a named
follow-up); no dynamics. The frame is the working frame's A₂-triple; by the S₃ frame
torsor (memo 26) the count is frame-independent (the torsor permutes the three
determinant directions and fixes the trace direction — visible in the block table).

### Certificates
`certificates/b2_yukawa.py`; output `outputs/b2_yukawa_out.txt`.

### One sentence for the ledger
Four exact directions is all the room the closing leaves for Yukawa-shaped structure —
three determinants the frame torsor permutes and one trace it fixes — and the single
direction the full algebra retains is the same one the object's own beat carries
through unchanged.
