# THE SUSY NO-GO — the carrier admits no supercharge: no odd operator squaring to the meridian exists under π₁- or gauge-equivariance, and the object's one square-root of the tick (the beat) is even and semilinear
## (outside bench, 2026-08-26; seventy-first memo; D5 of the MSSM-debt programme; three definitions, three exact negatives; contradicts no observation — superpartners are not observed)

### THE FACTS (`certificates/susy_test.py`; asserts GREEN)
- **Definition A (π₁-equivariant Q, odd w.r.t. the lock, Q² = meridian): NONE
  EXISTS.** The locked and unlocked sectors have DISJOINT holonomy-closure
  contents (6·spin1 ⊕ 6·spin0 vs 15·spin½ — derived in-run), so every
  equivariant odd map is zero (Schur + closure, both CITED as in memo 48;
  the content arithmetic asserted). dim(odd commutant) = 0.
- **Definition B (gauge-equivariant Q): NONE EXISTS — proved without
  Schur.** The 27's weights are multiplicity-free and its 12-generator
  weight graph is connected (verified), so the 27's commutant is scalars and
  the e₆-commutant on Ψ is exactly {M⊗I}; (M⊗I)² = M²⊗I can never equal
  A₂⊗A₂₇ since A₂₇ ≠ I (6 off-diagonal entries exhibited).
- **Definition C (the beat): NOT a supercharge.** β² = meridian, but β is
  Galois-semilinear (banked; OA-C1090) and EVEN — it commutes with the lock
  (re-verified). The mirror is the object's square root of the tick; a
  supercharge it is not.

> **The "M" in MSSM is unsupported by the object's kinematics: the
> NMSSM-shaped skeleton (no bare μ, forced singlet portal, parities) is a
> COUPLING structure, not a supersymmetric one, as far as the carrier can
> say. Owner-directive note: SUSY is not observed structure — this negative
> contradicts nothing; it aligns with the empirical absence of
> superpartners. 4d field-theoretic SUSY with genuine spacetime
> translations lives behind Gates 2/3 and is untouched.**

### Certificates
`certificates/susy_test.py`; output `outputs/susy_test_out.txt` (in-lane
rerun byte-identical).
