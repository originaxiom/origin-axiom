# THE FOUNDING TORSOR, VERIFIED — cc's basepoint theorem reproduced from scratch, and two lane additions: the reading direction is invisible to the whole character variety, and the branch bit is neither founding bit
## (outside bench memo 98, 2026-08-28; certificate `certificates/founding_torsor.py`, GREEN; independent verification of the founding-bit answer relayed by cc via the owner)

THE CLAIM VERIFIED (cc, from A7 + B1083 + THE_FORCED_AND_THE_FREE):
writing the founding rule σ: a→ab, b→a was a basepoint-taking — the
four natural substitution rules form a free transitive K₄-torsor under
C (letter swap) and P (reading direction); the arrow is NOT a torsor
bit (it is monoid non-surjectivity).

### The verification (all exact)
1. **The torsor reproduced**: K₄ = {1, C, P, CP} verified as a
   commuting involutive action; the founding rule's orbit is exactly
   the four rules {(ab,a), (ba,a), (b,ba), (b,ab)} — free, transitive,
   trivial stabilizer.
2. **The A-level indistinguishability shadow**: all four rules
   abelianize to matrices with the one golden polynomial x²−x−1; P is
   abelianization-invisible; C is swap-conjugation.
3. **LANE ADDITION — P is character-invisible** (proved via the Fricke
   argument: x, y, z all reversal-fixed; every trace is a polynomial
   in them; machine-checked exactly over ℤ[ω] with m004's Riley
   holonomy for ALL 1364 words of length ≤ 5, control: the banked
   systole trace 2−ω reproduced). **The object cannot even SEE the
   reading direction.** (Convention note, filed in-run: in this Riley
   normalization the 2−ω trace is the class [ab] = memo 81's [AB]⁻¹ —
   same trace; the first-pass control mislabeled it [ab⁻¹].)
4. **LANE ADDITION — the branch bit is neither founding bit**: C acts
   on the Fricke slice as s (memo 97: does not swap branches); P acts
   trivially (fact 3). With memo 97's "not c", the branch ℤ/2 is now
   independent of c, C, and P — word-level input filed to the
   branch↦r identification cell (cc's batch).
5. **The arrow's re-typing confirmed**: σ's image submonoid (87 words
   to length 8) contains no word starting with b — σ is non-surjective,
   hence non-invertible; the arrow is this monoid fact (memo 86's
   ORDER), not a K₄ coordinate.

### What this closes for the owner (register R1's founding level)
> **The a/ab/ba question's origin story is now three-seat solid: the
> first written symbol took exactly one K₄-basepoint of choice —
> spendable as the chirality-type bit C and the reading-direction bit
> P — and the arrow was never part of that choice. The order is the
> object's (κ, memo 86); the direction rides the branch/seed
> structure; and the reading direction P turns out to be a choice the
> object cannot detect at all** — the purest "priced to the describer,
> invisible to the described" bit in the record.

Fences: this cell verifies the torsor and its computable shadows; A7's
full "A1–A6 cannot distinguish" is the corpus's UNIQUENESS_THEOREM
(cited, not re-proved here); the founding-bit ↦ c transport (L63) is
cc's, verified on their bench. Gate 5 untouched.
