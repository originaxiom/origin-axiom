# THE CENSUS MEASURED — exact centralizer/orbit dimensions for all 20 characteristics: Kostant's regular orbit at 72, the minimal at 22 = 2h∨−2, all even; and the bench's own hardcoded comparison list refuted by the machine
## (outside bench, 2026-08-26; sixty-seventh memo; wave-2 cell A6; THE BENCH'S OWN ERROR FILED: the prompt's "cited" dimension list was written from memory and was WRONG — the computed multiset is the result)

### THE FACTS (`certificates/census_distinct.py`; all 20 sl2 triples rebuilt exactly, [e,f]=h etc. verified; 78×78 exact ranks)
- Orbit-dimension multiset: **[22,32,40,42,46,48,50,52,54,56,58,60,60,62,64,64,66,68,70,72]** (60 and 64 doubled, the rest simple).
- Independently-cited anchors ALL pass: the regular characteristic (2,2,2,2,2,2) has centralizer dim 6 = rank (Kostant, CITED) so orbit 72; the minimal orbit is 22 = 2h∨−2 (CITED); all 20 dimensions even (CITED: orbits are symplectic).
- **ERROR FILED (the bench's, not the agent's):** the cell prompt supplied a "cited" comparison list from memory (double 54, no 72). The machine refused it; the agent correctly banked the computed multiset instead. Lesson re-learned at the orchestration level: a citation without a source is a guess — the same rule that governs cells governs prompts.
- FENCE: this corroborates the cited completeness of the 20-row census with independent exact dimension data (memo 30 / codex OA-C1058); Jacobson–Morozov and the dominant-characteristic theorem remain CITED — this is enrichment, not a completeness proof.

### Certificates
`certificates/census_distinct.py`; output `outputs/census_distinct_out.txt` (in-lane rerun byte-identical).
