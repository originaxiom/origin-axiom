# MEMO 169 — SIX CUSPS ARE NOT AVAILABLE AND WOULD NOT HELP; THE ASK IS SHARPER FOR IT

**Banked 2026-08-31 · outside bench (lane 1B).** Seal `seals/CUSP_SIX_PREREG.md`, pushed before the
census ran. Certificate `certificates/cusp_six_census.py`; output vendored at
`outputs/cusp_six_census_out.txt`. Ask drafted at `THE_GATE_SENDS.md` addendum 6 (**Q11**).
Gate 5 untouched: pure topology, no measured value. **Nothing sent.**

> **`SUMMARY: C1-NO-SIX | C2-ALL-ZERO`** — **one prior lost**, and the loss is what sharpened the ask.

---

## 1. The owner said *"lets pose it."* Posing it properly killed my own framing first

Memo 168 (this morning) inferred: *"six cusp-boson units is a cusp-count requirement — the σ bridge
and the multi-cusp wall are the same wall."* Before drafting the ask I tested the route that
inference implies. **It fails twice over, and both failures are computed.**

### C-1 — `C1-NO-SIX`, **against my declared prior**

I sealed `C1-SIX-EXISTS` at degree ≥ 6. SnapPy, 38 covers of `m004` through degree 8:

| degree | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|
| **max cusps** | 1 | 1 | 2 | **3** | 2 | **3** | 2 |

**No cover of `m004` has six cusps up to degree 8, and the cusp count does not grow with degree — it
caps at 3.** The six-cusp route is not merely unhelpful. **It is not available.**

### C-2 — `C2-ALL-ZERO`, as declared, for the reason declared

All 38 covers: `|CS| ≤ 8.662e-16`, i.e. `≡ 0` — exactly as `CS(M̃) = d·CS(m004)` forces. **`∂S/∂k ≡ 0`
survives every cover.** The quantized sector stays deleted upstairs.

### C-3 — the meter reproduces with no residual

Every cover's volume is exactly `2d·V_reg = 12d·Vol_orb` at degree `d` (4, 6, 8, 10, 12, 14, 16 in
`V_reg`). `B1191`'s meter identities hold up the tower unchanged.

---

## 2. The pre-committed correction, honoured — against my own memo of the same morning

The seal said: *if both land, memo 168's inference is too weak and I will say so.* The outcome is
worse for me than that branch:

> **Memo 168 §4's inference is WRONG as an identification of the operative wall.** Six cusps are
> **unavailable** (max 3 through degree 8) **and would not help** (`CS ≡ 0` is inherited by every
> cover). **The cusp count was a symptom. `B1064`'s deleted quantized sector is the disease.**

Memo 168 §4 is **corrected in place**, not rewritten. *This is the same class this bench has been
charging all session — a summary claiming more than the record beneath it — committed by me, twelve
hours after writing the memo about it, and caught only because the seal forced a test before the
send.* **Sealing a prior is what made the correction possible; had I drafted the ask straight from
the memo, the ask would have carried the wrong wall.**

---

## 3. What the ask became — `B1064`'s route (b), the only door left

`B1064` (NEGATIVE, **O3 — OBSTRUCTED-AT-TORUS**) names the obstruction exactly:

> *"the object's amphichirality deletes the quantized sector of its own boundary action
> (`CS = 0 ⟹ ∂S/∂k ≡ 0`), and rational `(E₆)₁` data is quantized-sector content; the surviving
> unquantized sector carries no banked chiral algebra."*

and types the missing mechanism as **(a)** restore a quantized sector amphichirality does not delete
— *"none can come from the object's own action"* (`B1012`) — or **(b)** attach rational data to an
**unquantized** sector.

**This cell closes (a) via covers**, the one natural route left inside (a), because a cover is *not*
the object's own action. **So (b) is the whole remaining door**, and Q11 asks exactly it:

> **Is there a known mechanism attaching rational / modular boundary data to the UNQUANTIZED sector
> of complex Chern–Simons theory on a cusped hyperbolic 3-manifold with `CS = 0` — and if not, is
> there a theorem that there cannot be?**

**Four routes are now closed by us, and the ask says so** so no specialist spends an hour on one:
the fingerprint route (`B1191`/GC-12, ours), the Eisenstein-cusp route (`B486`, ours, 2026), the
integer-level attachment (`B1064`, ours), and **the cover route (this cell, today)**.

---

## 4. The literature gap, stated with its terms

Read: the DGG 3d-3d lineage is well covered — `3d-3d` 49 files, `Dimofte` 30, `Gukov` 30, `Gaiotto`
21; plus `B262`'s `T[4₁]`, `B441`'s WRT typing, `B1034`'s stage search, `B715`'s real-form exclusion.

**Never read — zero occurrences across all 1122 arcs and all docs:** `Ẑ` / `Z-hat`,
`homological block`, `GPPV`, `Putrov`, `false theta`, `logarithmic CFT`, `boundary vertex algebra`,
`half-index`.

**Those are the tools that produce a genuine boundary character — the exact object GC-6 says is
missing.** Not a claim that they solve it. A statement that the one family of methods aimed at this
question has never been read here. **Same shape as memo 160's *"zero arcs mention strong
approximation"***, which took Q7 from *"not ours"* to four of five in three cells.

---

## 5. Why the "no" pays, and why that matters more than usual

- **YES** ⇒ `σ = 1`; the framework's only continuous dimensionless input converts anchor → output,
  and **nothing continuous remains** — one unit nobody derives, two bits, finite menus.
- **NO, with a theorem** ⇒ **σ is a permanent anchor by theorem** and the input list is *provably*
  final. **That is where the paper's "terminal" language actually belongs.** At present the paper
  asserts permanence about the finite label menus — which `Route A` at four of five actively
  contests — and asserts nothing here, where permanence might genuinely be provable.
- **MISTYPED** ⇒ `L154` dissolves rather than resolves, and we would rather know.

---

## 6. Fences

- **Nothing crosses.** No claim that `σ = 1`. The claim is that the two central charges are exactly
  equal as numbers and the attachment is untyped.
- **`B1064`'s O3 stands.** Q11 is *its own named route (b)*, not a challenge to it.
- **This cell is topology.** It says nothing about whether any `T[M̃]` has `c = 6`; it closes one
  named route by cusp count and by `CS`.
- **The census is degree-bounded.** `C1-NO-SIX` is stated *to degree 8* and not beyond; `C2-ALL-ZERO`
  rests on `CS` multiplicativity, which is degree-independent, so **C-2 generalises and C-1 does
  not** — said explicitly because the two outcomes have different reach.
- **`B486`'s and `B990`'s kills stand**, untouched.
- **Nothing sent.** Q11 is drafted at `THE_GATE_SENDS.md` addendum 6 and awaits the owner's per-item
  word, like Q7–Q10. Preparing an ask is not making it.
