# THE OCCUPANT'S TYPE IS FORCED — the owner's challenge answered by computation: the record does constrain what can occupy the seat, and the bit is irreducibly relational
## (outside bench memo 112, 2026-08-28; certificate `certificates/occupant_type.py`, GREEN; the owner's catch: "are we saying the object doesn't do this before checking we already have a way to supply it? whatever occupies the seat should be forced by the same principles")

**The challenge was right, and it lands as a reporting correction (bench
error #9, filed).** Earlier cells said the record "cannot locate an
agent" for the seat — true of the *act* of choosing (P invisible, ε
selector-free), but it is **not the whole answer**, and stopping there
under-reported what GC-16's apparatus already forces. Type is forced;
only token and phenomenon are not.

**The instrument (exact, no search boxes):** the simultaneous-mirror
realizers of a pair solve a *linear* system; compute its solution space.
dim 1 ⟹ single-signed (Y ↦ tY scales det by t² > 0), so **ε = sign(det)
is an invariant of the pair**; dim ≥ 2 ⟹ det is a binary quadratic form,
indefinite ⟹ both signs ⟹ no bit. Sign classification over ℚ equals that
over ℤ (clearing denominators multiplies det by a positive square).
Control: memo 99's crown pair reproduces **dim 1, ε = −1** with no
integer search.

- **O1 — nothing orients itself.** Every thing paired with itself (three
  exhibits) gives dim 2, both signs, **no bit**. An isolated system,
  however rich, supplies nothing. *The occupant cannot be a substance.*
- **O2 — no self-algebra partner.** (A, A²), (A, A⁻¹), (A, A³) all give
  both signs. **The partner must lie outside the first one's own
  algebra**; a system cannot orient itself out of its own powers.
- **O3 — the relation must not commute.** A commuting partner gives both
  signs; the crown pair has [A, M₁] ≠ 0 and carries the bit.
- **O4 — THE BIT IS IRREDUCIBLY RELATIONAL (and a refutation).** Two
  matrix families were scanned over eight real quadratic fields: the
  natural unit matrices, and the uniform companion matrices of
  x²−tx+1. **They disagree on 6 of 8 fields** — the same field with a
  different representative flips the verdict. So ε is **not determined
  by either side's arithmetic**. The naive norm law routed as **B9**
  ("norm +1 hosts the orienting bit, norm −1 cannot") **FAILS in both
  families**, with Q(√10) an explicit counterexample (admits a norm −1
  unit, yet yields ε = −1 in family N). cc's own lens-correction is
  vindicated; the refutation is relayed to them.

**THE ANSWER.** "Who occupies the seat" has a real, forced answer at
**type** level: **an asymmetric, non-commuting relation to something
unlike itself, whose orienting bit belongs to the relation and to
neither relatum.** Not a thing that observes — a relation irreducible to
its sides. This is cc's observer-cost theorem ("one asymmetric relation,
and nothing else") with the irreducibility now *demonstrated* rather
than assumed. What remains open is the **token** question (which such
relation is ours) and the phenomenal one — **not the type.**
Gate 5 untouched.
