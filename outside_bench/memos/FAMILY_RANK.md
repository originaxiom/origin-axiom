# THE FAMILY RANK THEOREM — every Yukawa matrix in the record's three-family channel has rank EXACTLY 2, and its massless direction is the Higgs's own family: one exactly-massless family per coupling, forced and dimensionless (810/810 matrices verified on the full tensor)
## (outside bench, 2026-08-26; eighty-second memo; wave-3 MB2 / meditation §B; the σ-gauge trap hit a second time and machine-caught a second time — filed)

### THE FACTS (`certificates/family_rank.py`; asserts GREEN)
Memo 53's E8 machinery rebuilt verbatim (81 family roots = 3 × 27, vertex
T with 1620 ordered entries, κ = −60 reproduced). For each of the 45
internal weight-zero triples, each of the 6 leg assignments, and each
Higgs family f3 — **810 family matrices** M[f1][f2] = T(root(f1,ia),
root(f2,ib), root(f3,ic)) read from the FULL tensor, not the
factorization:
- every M has EXACTLY 2 nonzero entries, at transposed off-diagonal
  positions spanning precisely the two non-Higgs families;
- det M = 0 and rank M = 2 EXACTLY, all 810;
- **ker M = the line of e_{f3} — the Higgs's own family — all 810.**

**ERROR FILED:** the draft also preregistered VALUE-antisymmetry on the
raw entries; the machine refused — the same σ-gauge trap as memo 62/53
(raw Chevalley entries carry per-root signs; antisymmetry is a σ-degauged
statement, banked in memo 53). Hit twice, caught twice; the
gauge-INVARIANT facts above carry the theorem alone.

> **In the record's only three-family channel, mass generation comes with
> a forced hole: every coupling annihilates exactly one family direction,
> and WHICH family is massless is set by where the Higgs sits — a
> dimensionless rank theorem, with the selection priced to the
> observer/vacuum column, not answered. §B's ledger again: what is
> dimensionless is forced; what would be a value (which family, what
> mass) is withheld. FENCES: E8 is possibility-space (memos 53/62/74);
> the record now carries THREE distinct Yukawa-suppression mechanisms —
> cc's SEAM-Y (their bundle's cohomology), the object channel's live
> up-shape (memo 80), and this channel's rank-2 hole — all named, none
> identified with another. Gate 5 untouched.**

### Certificates
`certificates/family_rank.py`; output `outputs/family_rank_out.txt`
(in-lane rerun byte-identical).
