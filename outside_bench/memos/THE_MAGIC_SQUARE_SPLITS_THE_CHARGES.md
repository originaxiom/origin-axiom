# 202 — THE FOUR CHARGES ARE FORCED, AND THE MAGIC SQUARE SPLITS THEM

**Date** 2026-09-11 · **Lane** outside bench · **Branch** `<seat>/outside-bench`
**Certificate** `certificates/charges_and_the_magic_square.py` ·
**Output** `outputs/charges_and_the_magic_square_out.txt`
**CELL 1/2/3/4 = B/B/B/B · six controls, all passing · Gate 5 untouched**

**Occasion — the owner:** *"go after physics"* → *"how do we probe this more to understand it and
contemplate on it"* → **"continue, lets see how much of it is real."** This is the answer to the
third.

---

## 0. The artifact risk, found by reading the source and named before it was tested

`frontier/B854_centralizer_exact/e6_centralizer.py` contains the line

    for n in [8, 14, 16, 22]:

**The four superselection charges are a hardcoded list.** `e₆` under a principal `sl₂` is six
blocks `V_{2m}` at exponents `m = 1,4,5,7,8,11`, and B854 never looks at the blocks at exponents
**1 and 5**. If those blocks carry an invariant, memo 201's *orphan* structure is an artifact of a
hardcoded list and most of its reading collapses.

## 1. CELL 1 — the four charges are **FORCED**

Invariant dimension of the selecting group (`|G| = 24`) in every block:

| `n` | exponent | dim invariants | B854 |
|---|---|---|---|
| 2 | **1** | **0** | not used |
| 8 | 4 | 1 | used |
| 10 | **5** | **0** | not used |
| 14 | 7 | 1 | used |
| 16 | 8 | 1 | used |
| 22 | 11 | 1 | used |

**The blocks at exponents 1 and 5 carry no invariant; the four used carry exactly one each.**
B854's hardcoded list is the **correct answer**, not a choice. Control C1: the projector finds the
trivial invariant at `n = 0` and the group's own generating degrees 6, 8, 12.

**The rule:** *exponent `m` carries a charge iff `2m` lies in the numerical semigroup generated by
the group's invariant degrees.*

## 2. And that is what makes the orphans — Coxeter duality as a **residue**

E₆'s Coxeter number is `h = 12`; its dual pairs are `(1,11)`, `(4,8)`, `(5,7)`.

    dual pair ( 1,11):  charge at  1? NO    charge at 11? yes   BROKEN -- leaves an ORPHAN
    dual pair ( 4, 8):  charge at  4? yes   charge at  8? yes   COMPLETE -- the dual pair
    dual pair ( 5, 7):  charge at  5? NO    charge at  7? yes   BROKEN -- leaves an ORPHAN

**The invariant condition kills exponents 1 and 5 — one member each from two different pairs —
stranding their partners 11 and 7, while (4,8) survives intact.** The Coxeter split of memo 201 is
not a pattern imposed on the charges; **it is what is left after a forced condition removes two
exponents.**

## 3. CELL 2 — the dual pair's centraliser, with its base rate

    plane (x8,x14)  exps (4, 7)  centraliser dim = 12
    plane (x8,x16)  exps (4, 8)  centraliser dim = 30   <-- DUAL PAIR
    plane (x8,x22)  exps (4,11)  centraliser dim = 12
    plane (x14,x16) exps (7, 8)  centraliser dim = 12
    plane (x14,x22) exps (7,11)  centraliser dim = 12   <-- ORPHANS
    plane (x16,x22) exps (8,11)  centraliser dim = 12

    C2 -- BASE RATE: eight random 2-planes in the same space: [12,12,12,12,12,12,12,12]

**30 is unique to the dual pair: 1 of 6 coordinate planes, 0 of 8 random planes.**

## 4. CELL 3 — the dual pair **is** `tri(ℂ)`

The 30 is verified a **subalgebra** by exact brackets (435 basis pairs, none leaving the span), and

    dim 30 = derived 28 + centre 2
    centre == span(x8, x16) : True
    complement of the 30 in e6 : dim 48
    all four charges lie inside the 30 (the orphans, not being central, lie in the 28)

**Not isomorphic to `u(1)²` — equal to it.** The centre of the Coxeter-dual pair's centraliser is
that pair itself.

*(Control C4 earned its place: the centre must be computed from the structure constants of the 30
in its own basis. This bench's first pass tested whether individual nullspace basis vectors happen
to be central — a wrong test, since the centre need not align with a nullspace basis — and it
returned 0.)*

## 5. CELL 4 — the three 16s, **structurally**

Under either dual-pair charge, on the 48-dimensional complement:

    ad(x8)  : charpoly = (t^6 - 15095808t^4 + 56970854793216t^2 - ...)^8   -> 6 eigenvalues, mult 8
    ad(x16) : charpoly = (4826809t^6 - 22946910796460851200t^4 + ...)^8    -> 6 eigenvalues, mult 8

**Six 8-dimensional weight spaces — the three 16s, each splitting into a ± pair.**

## 6. And the 28 is `so(8)` — by structure theory, not by this computation

The charge space is **Killing-nondegenerate abelian** (B854's own output: *"rank 4 (nondegenerate,
as on a torus)"*), hence **toral**. So the 30 is the centraliser of a torus, and its root system is
a **closed subsystem** of E₆'s. **E₆ is simply-laced, and every subsystem of a simply-laced system
is simply-laced**, so `g₂ ⊕ g₂` — the only other semisimple algebra of dimension 28 and rank 4 — is
**excluded**. Among simply-laced rank-4 simple algebras, dimension 28 is **D₄ uniquely**.

## 7. The statement

> **`e₆ = tri(𝕆) ⊕ tri(ℂ) ⊕ 3×(𝕆⊗ℂ) = so(8) ⊕ u(1)² ⊕ 48` — B882's magic square — and the object's
> four forced superselection charges sit ACROSS it: the Coxeter-dual pair SPANS `tri(ℂ)`, and the
> two orphans lie inside `tri(𝕆) = so(8)`.**

## 8. What is NOT claimed, and the distinction is the whole discipline

* **`E₆ ⊃ D₄ × T²` is a textbook maximal-rank subgroup and `78 = 28 + 2 + 48` is the standard
  decomposition. FINDING IT IS NOT A NEW FACT ABOUT E₆.** The content is narrower and is the entire
  claim: **the object's Coxeter-dual charge pair is that `T²`.**
* **No value, no ratio, no prediction, no measured quantity.** Gate 5 untouched.
* **B882's conjecture is not proved.** The remaining half is the ³D₄ twisting classification, a
  literature proposition this bench does not close.
* The resolvent V₄ is still **not** asserted to be B730's face V₄ (B1223 on the action; memo 200 on
  the kind), and the `19` in the (7,11) discriminants is still an unexplained **question**.

## 9. What it does to the door

B1077's blocker — *"the only dressing source in the bank is the pencil triple, forbidden as a
twist-source by the circularity theorem-let"* — is now dead three ways:

1. **77 is not a property of that pencil.** It is what a generic plane of the charge space sees
   (memo 201 addendum, 5 of 5).
2. **K is reachable with no pencil at all** — restricting either dual-pair charge to the orphan
   kernel (memo 201 addendum).
3. **And the plane the pencil used is the one whose centraliser IS the triality algebra** the
   conjecture is about (this memo).

**The blocker is gone. The door is still not open** — but it is no longer closed for the reason the
record gave.

## 10. BENCH ERROR #22 — filed at the point of occurrence

An earlier pass read the multiplicities on the 48 by **grouping the output of `eigenvals()`** and
got twelve eigenvalues with multiplicities (six of 6, six of 2), from which this bench reported to
the owner that the three 16s were *"dimensional match only, not confirmed."* **That was one
algebraic number written two ways, split by the simplifier** — 6 + 2 = 8. The number was never in
doubt; the instrument was.

**The rule: read an eigenvalue multiplicity off the FACTORED CHARACTERISTIC POLYNOMIAL, never off a
simplifier's grouping of `eigenvals()`.** Control C6 now enforces it in the certificate.

*Gate 5 untouched — no measured value is used or named. Nothing promotes to `CLAIMS.md`. No arc is
retracted.*

---

# ADDENDUM 1 (2026-09-11, same session) — **MOST OF THIS MEMO IS A REDISCOVERY, AND I DID NOT RUN THE CHECK THAT WOULD HAVE CAUGHT IT**

Found while searching the literature at the owner's instruction, in
`docs/NOVELTY_SWEEP_LEDGER.md` **row 6** — a row this bench did not read before banking memo 202.

## What was already banked, and where

| memo 202 | already banked |
|---|---|
| **CELL 2** — the 30/12 split across the planes, with a base rate | **B874** (2026-08-03), *the measurement ladder*: **all 15 coordinate subtori** censused exactly, the *"two-value cliff"* 30 vs 12, the (8,16)-plane named *"the unique soft direction"*, the ladder `78 → 46 → 30 → 12` with no intermediate stratum. **A month and a week before memo 202, and over 15 subtori where I did 6 pairs.** |
| **CELL 3** — `dim 30 = 28 + 2`, hence D₄ | **B877**: *"core (30, 28, 2)"*, *"reductivity ⟹ (28, 2); I3 ⟹ D₄"* |
| **CELL 4** — the 48 under the dual-pair charges | **B898** (PROVED), *the exact signature census*: `ad(x₈)` and `ad(x₁₆)` have **identical exact signatures** `{0³⁰, 48 real}`, and `ad(x₁₄)`, `ad(x₂₂)` likewise — charpoly over ℚ, factorisation, Sturm, no floats |
| the whole locus | **W4, executed 2026-08-18**: *"the trialitarian datum is `z(⟨x₈,x₁₆⟩) = so(8)⊕u(1)²` at the MEASURED plane"*, and *"the 77-bearing charges x₁₄/x₂₂ are DISJOINT from it (B874: adding either collapses 30→12, past D₄ entirely)"* |

**So memo 202's §3, §5 and §6 are rediscoveries.** The base rates I computed are real and the arithmetic
is right; they were simply already in the bank, in a stronger form.

## What survives as new

* **§1 — the four charges are FORCED.** The invariant dimension in *every* block, including the two
  B854 never examines. Row 6 shows the *"1 and 5 unaccounted"* question was raised by another seat
  and adjudicated through an entirely different route (a sign-law entry, a ramification channel) —
  **not** by computing invariant dimensions in the `sl₂` blocks. The semigroup rule appears to be new.
* **§2 — the Coxeter-duality framing.** Row 6 banks that `(7,11)` is the unique product-77 pair among
  all 15 exponent pairs. That `{4,8}` is the unique **complete Coxeter-dual pair** — `4+8 = 12 = h` —
  and that the invariant condition breaks the other two pairs by killing exponents 1 and 5, is a
  different statement and is not in the row.

## The failure, named exactly

**I ran `already_banked.py` on the pencil question and never on the centraliser question.** The
standing rule (memo 153) says the searched terms must be stated with the claim — I stated terms for
memo 201's question and then carried memo 202 on the same exhaustion, although memo 202 asks a
**different** question. **An exhaustion is scoped to the question it was run for. Re-running is
cheap; assuming coverage is not.**

This is the **fifth** measurement of the citation gap this bench has made — and the first one where
**this bench is the offender**, one memo after banking the rule about it (memo 199, §3). The rule
did not fail. **I did not run it.**

**BENCH ERROR #23. The rule it buys: `already_banked.py` is run PER QUESTION, not per session, and
`docs/NOVELTY_SWEEP_LEDGER.md` is read before any memo that touches a novelty-swept claim.**

*Nothing in memo 202's arithmetic is retracted. Its priority claim is.*
