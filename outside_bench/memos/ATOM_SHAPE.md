# THE ATOM SHAPE — the record forces that neutral atoms are possible, exactly: quantized charges (color-singlets integer), composable baryon/meson singlets, and Q(uud)+Q(e) = 0 to zero — the hydrogen-shape's neutrality is a script row, not an accident of values
## (outside bench, 2026-08-27; eighty-fourth memo; wave-4 S1, the cosmogony campaign's first cell; one arithmetic preregistration slip corrected in-run)

### THE FACTS (`certificates/atom_shape.py`; asserts GREEN)
The forced hypercharge (70) with measured-Y labels and the Y-gate (80),
and the standard electric-charge combination Q = T3 + Y (CITED
convention; ratios are derived structure per B950):
- **FACT 1 — the forced charge table.** u 2/3, d −1/3, u^c −2/3,
  d^c 1/3, ν 0, e −1, e^c 1; the Higgs doublets' members {+1, 0, 0, −1}
  (one charged + one neutral per doublet — the draft preregistered the
  wrong list, the machine corrected it, slip filed in-code); exotics
  ∓1/3; the two neutrals 0.
- **FACT 2 — charge quantization.** Every color-singlet state carries an
  INTEGER charge; every colored state carries charge in ⅓ℤ∖ℤ. The
  preregistered failure mode (BRANCH FRACTURE: a fractionally charged
  singlet would contradict observed structure — a thesis-failure signal
  under the owner's directive) did not fire.
- **FACT 3 — composability.** The three color weights of the 3 sum to
  zero and the 3̄ weights are their negatives: baryon-shaped (qqq) and
  meson-shaped (qq̄) color singlets exist at the weight level. Existence
  only — binding is dynamics, walled.
- **FACT 4 — the punchline.** Q(u)+Q(u)+Q(d)+Q(e) = 0 and
  Q(u)+Q(d)+Q(d) = 0, EXACTLY. **The hydrogen-shape and the
  neutron-shape are forced neutral** by the same anomaly arithmetic that
  forces Y — which, per memo 78, needs the gravitational condition.

> **The script's last visible rung: the exact neutrality of atoms is the
> fact that lets the large-scale universe be gravity-dominated rather
> than electrically dominated — and in this record it is FORCED
> structure, traceable through the anomaly system to the gravitational
> consistency condition itself. The circle closes: gravity's one
> entry-point into the forced structure (grav²Y) is load-bearing for the
> charge quantization that makes a gravity-dominated cosmos possible.
> Whether atoms FORM — binding, rates, abundances — is schedule and
> dynamics: observer-side or walled, exactly as memo 83 typed it.
> Gate 5 untouched.**

### Certificates
`certificates/atom_shape.py`; output `outputs/atom_shape_out.txt`
(in-lane rerun byte-identical).
