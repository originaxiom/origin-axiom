# GRAVITY IS LOAD-BEARING — deleting the grav²Y anomaly condition breaks the forced hypercharge (36→18, with 27 assignments blowing up into solution planes), while deleting the cubic changes NOTHING: in the object's frame, gravity, not the cubic, does the forcing
## (outside bench, 2026-08-26; seventy-eighth memo; wave-3 ME2 / meditation §E; BRANCH LB fired)

### THE FACTS (`certificates/grav_ablation.py`; asserts GREEN)
The memo-70 enumeration (frame A, all three su(2)_L choices, every
SM-shaped assignment) re-run under five variants of the anomaly system:
- **V0 FULL (anchor):** 36 solutions, all SM, zero higher-dim — memo 70
  reproduced exactly.
- **V1 NO-GRAV:** the forcing DEGRADES — 18 SM lines survive, 36 lines are
  killed only by the cubic, and **27 assignments acquire 2-dimensional
  solution spaces** where hypercharge is no longer unique up to scale.
- **V2 NO-CUBIC:** **identical to V0 in every count** — on the 27's
  realized charge functionals the [Y]³ condition is REDUNDANT: no line
  the three linear conditions allow is ever killed by the cubic.
- **V3 GAUGE-ONLY:** 27 solution planes + 36 non-SM lines — the
  pure-gauge floor forces nothing like uniqueness.
- **V4 NO-SU2 (control):** 18 non-SM lines appear — the gauge conditions
  carry weight too, as expected.

> **BRANCH LB: the one forced value-structure in the record — the SM
> hypercharge pattern (memo 70, held by three seats) — NEEDS the mixed
> gauge-GRAVITATIONAL anomaly condition. And the surprise inside the
> headline: the cubic [Y]³ condition, the celebrated Diophantine one, is
> completely redundant in-frame; the gravitational LINEAR condition is
> what does the work the cubic is usually credited with. Gravity enters
> the record exactly once at the value level, and where it enters, it is
> load-bearing. (Universal-reduction context per R019/memo 75: on an
> abstract 15-plet the linear trio leaves a 1-param ratio family that the
> cubic cuts; in the object's realization the roles rearrange — the
> realization plus gravity does everything, the cubic nothing.) Gate 5
> untouched.**

### Certificates
`certificates/grav_ablation.py`; output `outputs/grav_ablation_out.txt`
(in-lane rerun byte-identical).
