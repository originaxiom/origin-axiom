# Memo 228 — DOOR 3: the named external need is PAID, and the door does not open — it changes type

**Certificate:** `outside_bench/certificates/door_three.py` ·
**Output:** `outside_bench/outputs/door_three.txt` ·
**Seal:** `outside_bench/seals/DOOR_THREE_PREREG.md`
(sha256 `0b2d7b60afa32bd8f24f4f9393e5e24bbc75a612e9c39385c6b8ff1eb93b9afd`, committed before the
certificate was written)

**Occasion — the owner:** *"lets go for them"* — K3 and the three §4A.3 doors.

| cell | question | outcome |
|---|---|---|
| 1 | does KMRT attach a cubic to `(A,σ)`, or is the cubic separate data? | **A** |
| 2 | is ONE Cayley algebra compatible with MORE THAN ONE cubic? | **A** |
| 3 | is the cubic a FREE classifying parameter? | **A** |
| 4 | what is clause (a) worth, now its first half is paid? | **A** |

**C1–C4 all PASS.**

---

## 0. First: K3 is already dead, so three targets, not four

Memo 196 addendum 1 ended *"K3 (the crossings sort) and the three §4A.3 doors carry the weight."*
**K3 ran the next day and was refuted** — register addendum R100, memo 195 addendum 2, 2026-09-11,
on its own FALSE branch: the three hearing-side crossings asked hearing for **magnitudes, its own
commodity**, and all three missed at 4.7σ, 3.4σ, ≈5σ. **That sentence was stale when I quoted it
yesterday.** Only the three doors remain.

## 1. Door 3 is the one this session has a key for

`WHAT_WOULD_COUNT` §4A.3 names three successor doors as *"the only remaining licensed targets"*.
**B1077 (2026-08-19) ranks the third**, quoted from `origin/main`:

> *"**B882's conjecture (arithmetic S₃ = geometric S₃) is now the UNIQUE non-circular route to a
> 77-mechanism**, its in-sandbox half done here; the remaining half is **the KMRT §43 connective
> proposition — a typed literature floor, the named external need**."*

`NOVELTY_SWEEP_LEDGER` row 6 fixes the decision route:

> *"moves only by **(a) KMRT §43 opened + an independent dressing computation**, or (b) B882's
> conjecture proved"*

**The owner supplied KMRT on 2026-09-13.** Clause (a)'s first half is paid. **This memo reports what
it bought.**

## 2. What KMRT actually says — three readings, each quoted

**CELL 1 — the cubic is not a function of `(A,σ)`.** A trialitarian algebra is the four-tuple
`T = (E, L, σ, α)`: `L` is the cubic étale algebra and `(E,σ)` is central simple **over `L`**. What
`(A,σ)` canonically carries is **quadratic**, quoted from KMRT:

> *"it follows that the center `Z` of `C(A, σ, f)` is a **quadratic** étale `F`-algebra"*

> So *"the cubic canonically attached to `(A,σ)`"* is **not a quantity KMRT defines for `(A,σ)`
> alone.** It is defined for a trialitarian algebra — `(A,σ)` **plus a packaging**.

**CELL 2 — one Cayley algebra feeds every cubic.** §36.C extends the twisted-composition
construction *"to an **arbitrary cubic étale algebra** `L` and a para-Hurwitz algebra"*, and §43.7
sets `End(Γ)` as the trialitarian algebra of `Γ`. With `L` split the **same** `C` gives §43.2's
type-`G₂` algebra. **So `C` does not select `L`.**

**CELL 3 — and the cubic is a free classifying parameter.** §44.1: the sequence
`1 → PGO(C,n) → PGO(C,n)⋊S₃ → S₃ → 1` is **split**, and §44.2's proof says the restriction map
*"has a section"*. §44.5: `H¹(F, PGO₈⁺⋊S₃)` classifies trialitarian algebras, and

> *"The second map associates **the class of `L`** to the trialitarian algebra `T = (E, L, σ, α)`."*

A split surjection induces a **surjection** on `H¹`, and `H¹(F, S₃)` **is** the set of cubic étale
`F`-algebras.

> ### The map *"trialitarian algebra ↦ its cubic"* is SURJECTIVE onto every cubic étale algebra.
> `K` is one. `ℚ³` is one. **The formalism forces nothing.** *"Which cubic"* is a **torsor**
> question of exactly the shape the record meets everywhere: **the object supplies the group, never
> the point** (C16's NO POINT class, ×8).

## 3. CELL 4 — so the literature floor is paid, and the door does not open

**A formalism that accommodates every cubic cannot single out `K`.** The 77 must still come from the
**dressing**, and the circularity theorem-let stands unchanged, quoted from `origin/main`:

> *"the bank contains exactly ONE such triple (the enhancement lines) and its resolvent already **is**
> `K` — so every in-bank dressing route makes C77 true **BY FIAT**."*

with W4's decidable sub-result also unchanged: *"the **UNDRESSED** datum … canonically attaches the
**SPLIT** cubic `ℚ×ℚ×ℚ`, trivial discriminant: the 77 is NOT in the bare algebra."*

> ### THE DOOR CHANGES TYPE, WHICH IS THE RESULT.
> It was **"blocked on a book nobody here could read."**
> It is now **"blocked on an INDEPENDENT DRESSING — with the literature half settled, and shown not
> to supply one."**
>
> That is a **smaller** door and a **better-posed** one. A literature blocker is an unknown; a
> torsor is a known shape with a known price. **And it removes the hope that reading KMRT would
> force `K`** — a hope the record had carried, reasonably, for 26 days.

## 4. What is NOT claimed

* **Route (b) is untouched.** B882's conjecture is not proved, disproved, or made harder — only its
  clause-(a) route is priced out. **Door 3 stays open on (b).**
* **No arc is retracted.** B1077's ranking stands, W4's four findings stand, B888's *"observation,
  unweighted"* stands, and memo 222's ⁶D₄ typing and `[E] = 1` stand.
* **This is a reading of KMRT, labelled CITED throughout** — the surjectivity step is standard
  (split sequence ⟹ surjection on `H¹`), and the quotations are the load-bearing part.
* **C4 held:** no step used the enhancement triple, the pencil triple, or anything whose resolvent is
  `K` by construction. The forbidden move is named in the certificate and not made.

## 5. Operational — rule #26 fired twice, on schedule

**C1 caught a needle I had taken from memo 203's SEARCH SUMMARY of KMRT rather than from KMRT** —
the *"Clifford algebra … center a quadratic étale F-algebra"* phrasing is the summary's, not the
book's. The run **failed** until it was replaced with KMRT's own sentence. **That is rule #26
(added yesterday, memo 226) catching the exact thing it was written for, one day later.** And C3
caught a mistyped `𝕆` (I wrote `𝕂`).

*Gate 5 untouched. Nothing promotes. No arc retracted. Memo 223's price stands at 12.*
*Every quotation above comes from `outside_bench/outputs/door_three.txt`.*
