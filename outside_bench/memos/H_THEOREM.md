# THE H-THEOREM INSTRUMENT — S002's D2 test run after ~1100 batches queued: the H-theorem as stated is REFUTED, and the refutation is a law — the arrow is BRANCH-CONDITIONAL, monotone entropy on the escaping branch only
## (outside bench memo 94, 2026-08-28; certificate `certificates/h_theorem.py`, GREEN; campaign THE SECOND HALF, weld-book instrument (ii); feeds LEAP-2's price statement and AR6)

THE INSTRUMENT (S002's D2, "[HOOK] (bounded calc)", pending since the
corpus's earliest era; recovered by the wave-1 sweep): iterate the
trace map's square on the unstable manifold of the trivial fixed
point and ask whether the running entropy is monotone (an H-theorem).

### Setup (exact)
The substitution σ: a→ab, b→a induces on Fricke coordinates the map
T(x,y,z) = (z, x, zx−y) — **κ-preserving, verified symbolically**
(κ = x²+y²+z²−xyz−2, the lane's Casimir/B293). At the trivial rep
(2,2,2): the Jacobian of T² has eigenvalues **{1, φ⁻⁴, φ⁴} exactly** —
one golden unstable direction, exact eigenvector (3−√5)/2-family.
Orbits at mp.dps = 3000, κ-conservation asserted at every step
relative to orbit magnitude (integrity gate; the seed sits at
κ = 2+O(ε²) since (2,2,2) is a critical point of κ — a corrected
first-pass gate, filed in-run).

### The data (six runs: ε ∈ {1e-8, 1e-6, 1e-4}, both branch signs)
- **PLUS branch (all three seeds): monotone-INCREASING** local
  expansion, accelerating from 2 log φ = 1.9248… into
  double-exponential growth — the escaping, hyperbolic side.
- **MINUS branch (all three seeds): decreases, then falls into the
  bounded character region and OSCILLATES** (~0.55…1.47 band) — no
  arrow there.

### THE VERDICT (preregistered NOT-MONOTONE, refined by the machine)
> **S002's H-theorem as stated — monotone along ALL forward orbits —
> is REFUTED. The violation is a clean TWO-BRANCH LAW: the running
> entropy grows monotonically if and only if the orbit takes the
> escaping branch. The arrow's existence-on-a-branch is the object's;
> the BRANCH is a bit.**

This lands exactly on the record's standing split, sharpening it at a
new level: memo 86 (order object-paid, direction free), AR6 (leaning
SPLIT), and the census's seed-bit blocker all said the same thing at
other levels; the orbit level now agrees — **the instrument does not
pay LEAP-2, it prices it to the branch bit**. B124 stands untouched
(the linearized spectrum remains two-headed; this is the nonlinear
orbit-regime refinement S002's LEAP always pointed at).

Consequences filed: weld book (LEAP-2's cost line gains this
instrument's result — the buy requires paying the branch bit, which
is exactly the seed/direction bit already in the census's blocker
row); AR6 (the SPLIT lean strengthens: irreversibility structure
exists but is branch-gated); the census (candidate 8's "seed" reading
gains its dynamical face: the seed IS the branch choice on the
unstable manifold).

Fences: instrument run — finitely many seeds and steps, the SL(2)
Fricke slice only (B106's higher-rank towers untouched); not an
all-orbit theorem; the minus-branch oscillation is measured, not
characterized (its bounded region's dynamics is a follow-up hook).
Error filed in-run: first-pass κ-gate demanded κ=2 instead of
κ-conservation (the trivial rep is a critical point — deviation is
O(ε²) by construction); corrected, preregistration note in the
docstring. Gate 5 untouched.
