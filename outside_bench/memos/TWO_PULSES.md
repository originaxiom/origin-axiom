# THE TWO PULSES — the beat cannot drive expansion (theorem, from banked pins); the record's one stretching pulse is the golden substitution, whose square IS the fiber monodromy in one GL₂(ℤ) class, stretch φ per tick — and the expansion question typed, with the unpaid weld named
## (outside bench memo 90, 2026-08-27; certificate `certificates/two_pulses.py`, GREEN first run; owner question, same-session register row R13)

The owner asked (2026-08-27): **"could the heartbeat drive the expansion
of the universe?"** This memo is the machine half of the answer, banked
the same session per the register's standing rule.

### FACT 1 — the heartbeat is isometric: it CANNOT drive expansion
Re-pinned from the banked record (output-string pins, `has()` style):
memo 46's `beta_Psi^2 = rho_Psi(a) (the MERIDIAN on the carrier): True`
and memo 81's mirror action on the exact complex-length spectrum —
**lengths invariant, torsion negated** (exhibit: 3.450219 vs 3.450219).
A map that fixes the entire length spectrum changes no distance. The
beat's stretch factor is exactly **1**. Expansion is not in its verb
set. (Prose context, not machine: Mostow — the isometry class *is* the
object.)

### FACT 2 — the record's stretching pulse, exact
σ: a↦ab, b↦a abelianizes to S = [[1,1],[1,0]]: char poly **x²−x−1**,
Perron root **φ = (1+√5)/2** (verified exactly: φ²−φ−1 = 0). And
S² = [[2,1],[1,1]] exactly, char poly **x²−3x+1** with root **φ²** —
the *same* golden trace-3/disc-5 polynomial banked in memo 49
(pin verified against `trace_three_out.txt`). Stretch per σ-tick: φ.

### FACT 3 — the twin, closed (two-outcome cell, FOUND branch)
Preregistered two-outcome: is memo 49's fiber-tick matrix
F = [[0,−1],[1,3]] GL₂(ℤ)-conjugate to S²? **FOUND**:
P = [[−3,−1],[5,2]] (det −1) gives P·F·P⁻¹ = S² exactly. So the
substitution's square and the fiber monodromy are **one conjugacy
class** — σ² *is* the monodromy, as β² *is* the meridian. The record's
two squared steps, side by side:

| pulse | its square | stretch per tick | banked where |
|---|---|---|---|
| the beat β | the MERIDIAN | **1** (isometric) | memo 46, memo 81 |
| the substitution σ | the MONODROMY | **φ²** (golden) | memo 49 + this cell |

Entropy cross-check: log(φ²) = 2 log φ = the banked fiber entropy
(memo 49 pin: "hyperbolic (entropy 2 log phi) on the geometric fiber").

### FACT 4 — the typing (fences printed, not crossed)
Through the banked parity×dimension law (B1168):
- **FORM** of a pulse-driven expansion law — "lengths multiply by φ per
  σ-tick", a dimensionless ratio, exponential in tick count — would be
  object-side **conditionally**: conditional on H90's **LEAP-1** (the
  σ-clock = the cosmological clock), which is **UNPAID**.
- **RATE** of the observed expansion — a dimensionful scale (per
  second) — is observer-side **by theorem** (the dimension half of the
  law): the object prices no seconds.
- **Named tension, not resolved**: the object's geometry is hyperbolic
  (Λ<0 flavor); the observed expansion is Λ>0. The weld, if any, is the
  owner's to price.

### The answer, in one line
> **No — the heartbeat provably cannot drive expansion (it preserves
> every length). If anything in this record drives expansion it is the
> other pulse — the golden substitution, stretch φ per tick, whose
> square is the monodromy — and only across LEAP-1, which remains
> unpaid.**

Prior art (retrieved, not new): S045 (the σ-flow cosmogony), H90/B303
(LEAP-1 and LEAP-2), CRYSTALLIZATION §6 (σ(a)=ab asymmetry), memos
46/49/81. Interpretive passages above are labeled as such; everything
else is pins and exact matrix arithmetic. Gate 5 untouched.
