# THE BRANCH BIT TEST — no object symmetry swaps the branches: the arrow's ℤ/2 is the branch bit, external and not c; and the record's own time-reversal is exhibited as an explicit polynomial involution
## (outside bench memo 97, 2026-08-28; certificate `certificates/branch_bit.py`, GREEN; campaign THE SECOND HALF — census candidate 8 typed, weld instrument (i) unblocked; run under the owner's full GO)

THE QUESTION (the census's candidate-8 blocker, given its dynamical
face by memo 94): the two-branch law made the arrow branch-conditional
— but is the branch "choice" real, or does the object's own symmetry
group swap the branches (making it gauge)?

### The test (exact, exhaustive over the natural finite symmetry set)
Candidates: the κ-preserving character sign-twists Σ = (ℤ/2)² (B782's
torsor face — they move the trivial rep, so cannot swap branches at
P₀; tested and recorded), the isometry swap s: (x,y,z)→(y,x,z), the
fourth-trace flip e′: z→xy−z, and their compositions. For each:
κ-preservation (symbolic), fixing P₀ = (2,2,2), the flow relation
(gT²g⁻¹ = T² / T⁻² / neither), and the derivative action on the exact
golden unstable eigenvector v_u (re-pinned from memo 94).

### The two results
1. **NO SWAP — the branch bit survives.** No tested symmetry commutes
   with the flow, fixes P₀, and reverses v_u. At the orbit level the
   branch choice is a genuine EXTERNAL residue. **The weld
   instrument (i)'s blocked identification is answered in-lane: the
   arrow's ℤ/2 is the BRANCH ℤ/2 (v_u ↦ −v_u) — and it is NOT c** (c
   acts trivially on this real slice; consistent with memo 94's
   mirror-even drift and AR6's SPLIT). Census candidate 8 types toward
   the FRAME column: a second discrete frame datum candidate beyond
   D1, sharpening the open-at-2+1 finding.
2. **THE REVERSER, exact.** R = s∘e′: (x,y,z) → (y,x,yx−z) is an
   involution with **R T² R = T⁻², R(P₀) = P₀, and DR carrying the
   unstable eigenvector to an exact φ⁻⁴ stable eigenvector** — the
   record's own symmetry group realizes orbit-level TIME REVERSAL on
   the nose. B124's two-headed time now has an explicit polynomial
   face. And this is exactly WHY the arrow needs a bit: forward and
   backward dynamics are symmetry-equivalent — the branch/direction
   choice breaks precisely what the object keeps whole.

### Consequences filed
- THE_FRAME_CENSUS: candidate 8 re-typed from BLOCKER to
  FRAME-CANDIDATE (the branch ℤ/2, distinct from D1 — subject to the
  completeness fence below). The census's open-at-2+1 finding
  SHARPENS: a genuine third discrete candidate now stands.
- THE_WELD_BOOK instrument (i): unblocked — the arithmetic
  orbit-breaker test now has its identified ℤ/2; the remaining half
  (does an ARITHMETIC subgroup break the orbit — beyond the geometric
  symmetries tested here) goes to cc with this cell as its input.
- THE_ARROW_DOSSIER: AR6's SPLIT now holds at three levels with the
  reverser as its exact mechanism.

Fences: completeness of the symmetry set is NOT claimed — the tested
set is the natural finite stabilizer family of the banked Fricke
structure; an exotic symmetry outside it is not excluded (cc's
arithmetic half covers the remaining route). Gate 5 untouched.
