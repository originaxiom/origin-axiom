# WHAT A VEV WOULD BREAK — the standard dark-parity-preserving vev (the E6 singlet) breaks the object's lock; the only directions neutral under both ℤ/2s are the five 10-class bridge-singlets, which the clock freezes
## (outside bench, 2026-08-26; sixty-first memo; the dark arc's last kinematic rung; necessary conditions only — no potential, no vev asserted, no values)

### THE FACTS (`certificates/vev_ledger.py`; asserts GREEN; full 27-row ledger in the output)
- **FACT 1.** Kinematic survival (a vev direction preserves a diagonal ℤ/2
  iff it is even under it): P-preserving directions = the 11 states of
  10 ⊕ 1; lock-preserving = the 15 bridge-singlets (wt 0); **both = exactly
  the 5 class-10, wt-0 directions**.
- **FACT 2 — the sharp finding.** The E6 singlet (wt = −1): a singlet vev
  **preserves the matter parity but breaks the lock** — the object's one
  group-paid ℤ/2 (memo 59), the longitude's sign.
- **FACT 3.** Portal participation per direction tabulated (the singlet
  sits in its 5 portal triples; every 16-state couples).
- **FACT 4.** E₂₇ annihilates all 5 both-preserving directions: they are
  meridian-frozen (depth 1) — breaking there leaves the escalator's rungs
  uncharged.

> **The dark arc closes on a fork the record can state exactly: keep the
> frame's dark parity and pay the object's lock, or keep the lock and
> confine breaking to five clock-frozen directions. Which branch — if
> either — the world takes is dynamics and values, behind the gates.**

### Certificates
`certificates/vev_ledger.py`; output `outputs/vev_ledger_out.txt`.
