# THE EXCHANGE LEDGER — the three-family vertex is exactly symmetric under full exchange of its two spinor-family slots (−1 × −1 = +1), and the internal slot is never exchangeable with them
## (outside bench, 2026-08-26; sixty-third memo; wave-2 cell A2, verified CONFIRMED; one preregistration corrected in-run)

### THE FACTS (`certificates/exchange_class.py`; 3240-entry sparse vertex S = ε_spin ⊗ T built exactly from the E8 trilinear, κ = −60)
- S is **symmetric on every support entry** under full exchange of slots 1↔2: ε_spin contributes −1 (verified), T's antisymmetry in its first two arguments contributes −1 (verified on all 1620 entries), product **+1**.
- Slot 3 is never exchange-symmetric with slots 1,2: under the 1↔3 swap, 1620 entries flip sign and 1620 leave the index set entirely (the corrected mechanism, filed — the third slot carries no spinor contraction).
- FENCED (interpretive): the bosonic-shaped +1 contrasts with the single-family antisymmetric vertex (memo 47); spin-statistics is a field-level input behind Gates 2/3 — this is a kinematic sign ledger, not a statistics claim.

### Certificates
`certificates/exchange_class.py`; output `outputs/exchange_class_out.txt` (in-lane rerun byte-identical).
