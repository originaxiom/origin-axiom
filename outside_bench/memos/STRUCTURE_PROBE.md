# COSMOLOGY ROWS 8 AND 7 — the ledger's last two never-run probes, RUN: the three blind rows are ONE obstruction seen three times
## (outside bench memo 134, 2026-08-29; certificate `certificates/structure_probe.py`, GREEN; ranked remainder item 1b)

Row 8 is the ledger's own *"most purely and simply MISSING row — no
attempt, no negative, no adjacent theorem. It is not walled; it has never
been started."* Its named probe: does B168's retention-decay curve,
**treated purely as a dimensionless growth-law shape (never as a value)**,
have any structural analog to a growth-of-structure exponent?

## ⚠ THE GATE-5 TRAP, NAMED BEFORE AVOIDING IT

B168's retention runs **0.583 → 0.397**. The cosmological growth index is
a number in that neighbourhood. **Comparing them would be exactly S008's
retracted *"iterations ≈ e-folds"* failure mode** — a coincidence dressed
as a derivation, which this record killed once already. **No value
comparison of any kind is made in this cell.** Only shape, which is what
the ledger asked for.

## THE TEST, PREREGISTERED

A growth-of-structure exponent is **γ = d ln D / d ln a** — a *power-law*
slope taken with respect to a *scale*. Two separately checkable
requirements: **(i)** the growth must be power-law in its own depth
variable; **(ii)** there must *be* a scale variable to differentiate
against.

**(i) FAILS.** The log-log slope is not constant — it climbs
monotonically **2.81 → 10.14** (a **3.6×** spread) across the measured
range, while the log-**linear** slope is near-constant (1.25×). **The
ensemble is EXPONENTIAL-class with a slowly drifting base; a
growth-of-structure exponent is a POWER-LAW object. Different growth
classes, so no exponent exists to compare — whatever its value.**

**(ii) FAILS, by theorem.** γ needs a scale factor *a*, and the record has
none — banked **twice, independently**: B168's own **G4** (*"no scale
emerges from the ensemble"*), and **memo 132** (no net volume growth —
det J_T = −1 constant, det J_L = 0, det σ² = +1 with eigenvalue product
exactly 1, so the Lyapunov sum is zero **at every point, as an
identity**). A scale factor *is* net volume growth; there is none to be
had.

> **OUTCOME G-B, both halves failing.** Row 8's probe returns a
> **structural mismatch**, and neither failure is a value comparison.

## THE UNIFICATION — the cell's real contribution

**Rows 2 and 8 are not two independent gaps.** Row 2 (inflation) needs
e-folds = the log of a **volume ratio**. Row 8 (structure formation) needs
an exponent with respect to a **scale factor**. **Both are the same
missing object: net volume growth**, which memo 132 proved absent by
determinant identities holding at every point.

**Row 7 (the CMB) dispositioned in the same pass — target by target, not
by blanket inheritance:**

| named target | what it needs | disposition |
|---|---|---|
| **spectral index n_s** | a tilt — a preferred sign of growth across modes | **BLOCKED BY THEOREM** (memo 132: reciprocal closure forced by det = 1) |
| **acoustic-peak location** | sound horizon / angular-diameter ratio — integrals over an expansion history a(t) | **BLOCKED BY THEOREM** (no net volume growth ⟹ no a(t)) |
| **amplitude** | a normalization against a scale | **BLOCKED BY THEOREM** (B168 G4 + the scale-torsor no-go) |

**But the honest split must be stated, and it weakens the claim:** all
three are blocked *given* a perturbation spectrum to tilt, normalize or
resolve into peaks — **and the record has no perturbation theory at all.**
So **row 7 is PART proved-negative** (its three *named* targets) **and
PART still MISSING** (any perturbation structure whatsoever). That is
weaker than rows 2 and 8 support, and it is made weaker deliberately.

**PROPOSED LEDGER CHANGES:** row 8 **MISSING → PROVED NEGATIVE**, sharing
row 2's mechanism; row 7 **split** as above; and the ledger records rows 2
and 8 as **one obstruction, not two blind regions**. **The blind region is
smaller than the ledger currently reports** — honestly arrived at, and it
revises this bench's own mission-status ranking, which called cosmology
the dominant genuinely-missing item.

## ⚠ DECLINED, DELIBERATELY

B168's sub-branch 1 asks whether **12·r_∞** is a recognizable algebraic
number (metallic/φ-related) and flags it **OPEN**, needing the L ≥ 11
enumeration. **Only six points exist.** Extrapolating a limit from six
points and testing it against a family as rich as the metallic numbers is
a **false-positive generator** — the same shape as the retracted S008
coincidence. **This cell does not attempt it**, and records the refusal so
the row is not later read as having been tried and failed. **It remains
OPEN exactly as B168 left it.**

**Fence.** Shape and growth class only; no measured value enters and no
number here is compared to any observed quantity. Gate 5 untouched.
