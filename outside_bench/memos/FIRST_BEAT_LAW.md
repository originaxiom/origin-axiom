# THE FIRST BEAT LAW — the owner's requested next a/ab/ba principle, derived: the second beat adds no invariant
## (outside bench memo 113, 2026-08-28; certificate `certificates/first_beat_law.py`, GREEN; the owner's proposal "add another a/ab/ba principle from that one beat")

Memo 86 read **order** off the first two letters (ab ≠ ba, with
tr[a,b] = κ). The owner asked for the next principle of the same kind.
It exists, and it is stronger than expected.

> **THE PRINCIPLE.** The record's entire trace content — every class,
> at every length, forever — is a universal **integer polynomial** in
> the three numbers the first beat fixes: **x = tr(a), y = tr(b),
> z = tr(ab)**. Nothing at length 3 or beyond is a new invariant.

**The mechanism** (why it is a theorem, not a survey): for SL₂ the
algebra generated by A and B is spanned by just **four** elements
{I, A, B, AB} over ℤ[x,y,z], with a closed table forced by
Cayley–Hamilton and the SL₂ identity (A² = xA − I, B² = yB − I,
BA = xB + yA + (z−xy)I − AB, A⁻¹ = xI − A, B⁻¹ = yI − B). Every word
reduces to four polynomial coefficients; its trace is
2c_I + x·c_A + y·c_B + z·c_AB. **A four-dimensional space carries the
whole record.**

- **P1:** the table reproduces the true holonomy exactly (spot-checks).
- **P2:** **all 4,372 reduced words to length 7** — integer
  coefficients in (x,y,z), and each evaluates at the record's own beat
  (2, 2, 2−ω) to the directly computed trace. **Zero mismatches, zero
  non-integer coefficient sets.**
- **P3:** κ **re-derived from the beat** — the Fricke commutator
  polynomial x²+y²+z²−xyz−2 at (2, 2, 2−ω) gives **exactly 1+ω**.
  Memo 86's conserved quantity is a function of the first two letters
  and nothing more.
- **P4:** to length 8 the record has **13,120 classes but only 180
  distinct trace values**, every one a polynomial image of the same
  triple.

**THE THREE-PART CLOSURE.** With its companions the statement is
complete: the **field** never grows (memo 111 — all traces in ℤ[ω],
the one embedding charged once, forever); the **invariant algebra**
never grows (this cell — everything is a polynomial in one triple,
fixed at length 2); and **order already exists** at that same length
(memo 86). So the record's entire informational content is settled by
the first beat. What long words supply is **multiplicity** — more
classes, more geodesics, more structure to inhabit — drawn from an
information budget that never increases. *(Interpretive, labeled: a
universe of this shape does not learn anything new about itself by
running longer; it only becomes more populated.)* Gate 5 untouched.
