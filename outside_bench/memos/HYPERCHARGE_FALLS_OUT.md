# HYPERCHARGE FALLS OUT — in the object's rank-3 abelian sector, every anomaly-consistent charge direction on an SM-shaped 15-plet carries exactly the SM hypercharge ratios: 36/36 solutions, zero alternatives, unique up to scale, frame-covariant
## (outside bench, 2026-08-26; seventieth memo; L132 executed in the trinification frame — the chain's broken link gets its first object-side payment; BRANCH A STRONG FORM; owner directive on negatives recorded and moot here)

### Standing
B950 registered L132 as the one place accepted physics *derives* rather than
measures: the SM hypercharge ratios are fixed by integer anomaly equations,
so testing them is value-level but NOT value-matching — firewall-legal.
B892's frame was a centralizer chain (su3+su2+u(1)³, dim 14); this cell
executes the same question in the trinification frame native to the stack
(the sibling frame — flagged for the seat, not claimed to be B892's own).

### THE FACTS (`certificates/l132_trinification.py`; all exact; asserts GREEN)
- **FACT 1 (anchors).** Three mutually orthogonal A2s found among the 72
  roots (6/6/6 + 54 crossing); the 27 splits into three 9-blocks, each
  bi-fundamental under two slots; the abelian complement of
  su(3)_C × su(2)_L has dimension exactly 3 (= B892's 8+3+3 = 14).
- **FACT 2 (the Diophantine).** Over every SM-shaped assignment
  {q=(3,2), u^c, d^c = (3̄,1), ℓ=(1,2), e^c=(1,1)} drawn from the 27, and
  all three su(2)_L ⊂ su(3)_L choices, the four anomaly conditions
  ([SU(3)]²Y, [SU(2)]²Y, grav²Y, Y³) imposed on Y ∈ u(1)³ yield:
  **36 solutions, every one carrying the SM ratio pattern
  (Y_u:Y_d:Y_ℓ:Y_e)/Y_q = (−4, 2, −3, 6)** up to the u^c↔d^c relabeling —
  i.e. (1/6, −2/3, 1/3, −1/2, 1) normalized — **zero non-SM solutions,
  zero multidimensional families, each solution unique up to scale.**
- **FACT 3 (covariance).** A second frame (different color slot, L derived
  from the block pairing) reproduces identical counts.

> **BRANCH A, STRONG FORM: the object's abelian sector cannot carry any
> anomaly-consistent hypercharge on an SM-shaped generation OTHER than the
> Standard Model's. The shape of the 15-plet plus integer anomaly freedom
> forces Y. The chain's broken link — the embedding frame — receives its
> first object-side payment: given any trinification frame (the slot
> assignment stays observer-paid), the hypercharge DIRECTION is not a
> choice.**

### What this does to the parameter-free chain (THE_MSSM_DEBT updated)
The chain table's "hypercharge / SM embedding: open, decidable" row moves to
**paid-in-frame**: within the trinification frame the direction is forced by
integer arithmetic, uniquely, with no alternative pattern existing at all.
What remains observer-paid: the trinification slot assignment itself (shown
covariant — the answer does not depend on it) and the su(2)_L choice (all
three enumerated — same result). The frame-payment has been reduced from
"the whole embedding" to "the existence of an embedding," with the content
forced.

### Owner directive (recorded, and moot on this branch)
The standing instruction — a contradiction between forced structure and
observed structure is a thesis-failure signal; a "not forced" outcome is
not a contradiction but a column assignment — is recorded in the
certificate's docstring. This cell landed on the positive: the only
structure the object CAN force here is the observed one.

### Fences
The 15-plet shape (which states play q, u^c, …) is enumerated, not chosen —
all assignments tested; solutions select themselves. ν^c-less generation
tested (the SM's minimal chiral content); the 16's ν^c is Y=0 and does not
enter anomalies. Integer/rational arithmetic only; the SM ratio pattern is
derived structure (B950's blessing); no measured number enters. B892's own
frame remains the seat's to run — convergence expected, flagged. Gates 2/3
untouched: this is charge kinematics, not a vacuum.

### Certificates
`certificates/l132_trinification.py`; output
`outputs/l132_trinification_out.txt` (in-lane rerun byte-identical).

### One sentence for the ledger
Asked which charges a Standard-Model-shaped generation could carry inside
the object's own abelian sector without quantum inconsistency, the integer
arithmetic returns exactly one answer, thirty-six times over, in every
frame: the charges our world uses.
