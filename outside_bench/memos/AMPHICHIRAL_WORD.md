# THE MIRROR IS A WORD MAP — the quine's one bit constructively convention-typed: φ(a) = a, φ(b) = ba⁻¹b⁻¹
## (outside bench memo 108, 2026-08-28; certificate `certificates/amphichiral_word.py`, GREEN; memo 107's named follow-up, run at once)
Found by exhaustive search (1456 reduced words to length 6, 104
parabolic candidates, exact product-trace filter, intertwiner
nullsolves): **gal(ρ(x)) = g ρ(φ(x)) g⁻¹ with φ(a) = a, φ(b) = bAB
= b a⁻¹ b⁻¹ and holomorphic g = [[1, ω̄],[0,1]]** — the Galois-flipped
record is the SAME realized record, rewritten in its own alphabet.
And φ is an **automorphism, constructively, no rigidity theorem
cited**: gal is an involution, so φ² must be inner with witness
h = gal(g)·g — and **h = ρ(a) exactly** (the off-entry is ω̄, and
ω + ω̄ = 1), so φ² = conjugation by a⁻¹ and φ⁻¹ = φ∘conj_a, verified
on words. Memo 107's residue is PAID: the quine's one external bit is
now constructively convention-typed — its two settings are related by
the record's own operations, so the bit carries no object content,
only the frame's anchoring choice (W3's weld and B1174's c, with the
witness in hand). The quine story closes at full strength:
self-report complete except ONE bit; the bit is c; **and c's two
values name the same world.** Gate 5 untouched.
