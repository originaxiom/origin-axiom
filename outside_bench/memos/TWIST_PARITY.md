# THE TWIST MEETS THE MIRROR — the hierarchy carrier is MIRROR-EVEN: the gauge choice is NOT an observer bit, and the generation hierarchy belongs to the object's side of the boundary
## (outside bench memo 92, 2026-08-28; certificate `certificates/twist_parity.py`, GREEN; campaign THE SECOND HALF — the census's closing instrument for candidate 4; the wave-2 flagship)

THE QUESTION (THE_FRAME_CENSUS candidate 4, the campaign's sharpest):
the entire generation hierarchy is carried by one diagonal — B928's
Hermitian twist D₂ᵗʷ = −(−1)^{⟨a*,w⟩} (11 flips on the 27; the
canonical gauge is generation-degenerate (x+3)³). Is the choice of
that twist priced to the observer column? The record's own decision
instrument is the parity×dimension law (B1168); the missing ingredient
was an action of the mirror on the 27 — which the main corpus lacks
and THIS LANE has: the beat Ω = exp(ρ₂₇(q·e))∘gal, the banked carrier
realization of the c-leg (memos 31–33/46; B1174).

### PART 1 — the class survey (honesty first)
All 128 affine sign diagonals on the lane's 27: the beat-parity law is
proven and verified matrix-vs-formula (beat-even ⟺ ⟨a, shift(r₀)⟩
even); flip-count histogram {0:1, 11:27, 12:36, 15:36, 16:27, 27:1}.
The banked signatures (polarity −1, 11 flips, flip weight-sums
{−1×2, 0×7, +1×2}) cut 27 candidates to 15 — still parity-MIXED (10
even, 5 broken). **The signatures alone do not pin the class** — a
finding in itself (B916's "is D₂ a character" question has a
27-member affine shadow). Fell through to Part 2 as preregistered.

### PART 2 — the decisive transport
B916's `results.json` banks the flip set as eleven explicit weight
vectors (vendored verbatim in the cert with a* = (1,0,1,0,1,1) from
B928 Q1a). Transported into the lane over all 6! coordinate
permutations × global negation:
- embeddings found: 2; **distinct transported flip sets: 1** — the
  transport is unambiguous;
- **the identity permutation embeds**: cc's B883 weight coordinates
  and the lane's crystal coordinates AGREE (no dictionary needed — a
  useful cross-stack fact banked en route);
- **B928's formula verified end-to-end in lane coordinates**:
  D₂ᵗʷ = −(−1)^{⟨a*,w⟩} reproduces the flip set entrywise (27/27);
- **beat parity: EVEN** — Ω D₂ᵗʷ Ω⁻¹ = D₂ᵗʷ exactly (commutator
  criterion AND full unipotent conjugation, exact pair arithmetic).

### THE RESULT
> **The hierarchy carrier is MIRROR-EVEN and dimensionless. By the
> parity×dimension law it sits on the OBJECT'S side of the boundary:
> the Hermitian gauge choice is NOT an observer-column bit, and the
> generation hierarchy is NOT carried by observer frame data.**

Census consequences (THE_FRAME_CENSUS addendum to follow): candidate 4
typed **NOT-FRAME** — one of the three excess candidates is retired.
The gauge crux does not dissolve — it TRANSFORMS: "which Hermitian
structure is physical" is now an object-side question (mirror-even
data are exactly the kind the object can force), i.e. **the twist may
be forceable** — a new, attackable problem in place of a priced bit.
Relayed to cc with this cell: (i) the result, (ii) the
coordinate-agreement fact, (iii) the proposal to attack twist-forcing
on the object side (their B907 wall-reality machinery is the natural
tool).

Fences: the decision rests on B916's explicit flip vectors (vendored
pin; the two banked summary signatures under-determine the class — 15
candidates, mixed). The beat is the lane's banked mirror realization;
if cc ever exhibits a second inequivalent mirror action on the 27, the
parity question re-opens (none exists in the record). Gate 5
untouched: weights, signs, and exact pair arithmetic only; no measured
value anywhere. B928's Stage-2 seal is untouched by this cell.
