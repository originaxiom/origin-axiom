# THE UNIT TRICHOTOMY — the record's three quadratic fields split the roles by unit arithmetic, one observer bit lives on each, and the crown relational-bit exhibits are independently verified
## (outside bench memo 99, 2026-08-28; certificate `certificates/unit_trichotomy.py`, GREEN; synthesis cell for THE FULL ACCOUNTING; owner's capstone directive)

### The trichotomy (exact, recomputed)
| field | unit group | norm of fundamental unit | the role |
|---|---|---|---|
| ℚ(√−3) (the object's trace field) | **μ₆ — NO infinite unit** | — | the SILENT field: nothing to flow with |
| ℚ(√5) (the golden end) | ±φ^ℤ | **−1** | the PULSE: flows, cannot orient (GC-2's killer mechanism) |
| ℚ(√3) (the hinge) | ±(2+√3)^ℤ | **+1, and −1 IMPOSSIBLE** (x²≡2 mod 3 insoluble) | the ORIENTING field |

### One discrete observer bit per field (banked assignments)
c on √−3 (B1174) · r on √3 (B1182) · γ₅ on √5 (B766/B769, GC-13) —
and GC-18's newly-placed λ is **hosted on ℚ(√5)**: the golden end
carries the (discrete, continuous) pair (γ₅, λ).

### GC-16 independently verified (the bench's role; all exact)
For A = [[2,1],[1,1]] (the object's monodromy, ℚ(√5)) and the
√3-partner M₁ = [[2,3],[1,2]]: X₀ = [[2,−3],[1,−2]] is an involution,
det −1, realizing the simultaneous mirror; [A,M₁] ≠ 0 forces the joint
centralizer to {±I}, so the realizer set is exactly {±X₀} —
**single-signed det −1: ε(A,M₁) = −1**, and X₀ provably induces
√5→−√5 AND √3→−√3 on the eigenlines simultaneously — **the pair-class
restricts to c**. Controls reproduced: (A,A) both det signs (no bit);
the ℚ(√2) partner ([[3,4],[2,3]]) single-signed +1 (bit absent). cc's
lens-corrected scope honored: exhibited pairs, not a general norm law
(their named refinement cell stands).

### The interpretation (labeled) and the brave question (posed)
The observer column is the record's arithmetic read at its ends: what
cannot flow is the object; what flows unoriented is the pulse; what
orients is the hinge — and the first realized carrier of c is exactly
a coupling to the orienting field. BRAVE QUESTION: **is the full input
list generated end-by-end — one discrete bit + one continuous datum
per arithmetic end?** √5: (γ₅, λ) ✓; √3/ζ₁₂: (r, σ?); √−3: (c, —) with
the object itself paying CS = 0 on its own end; ∞: ℓ. Deciders: the
σ-host computation and cc's norm-classification refinement. If it
holds, the observer column is not a list but a STRUCTURE — the places
of the record's arithmetic, one datum of each type per end, and the
grand computation's input section becomes a theorem about places
rather than an inventory. Gate 5 untouched.
