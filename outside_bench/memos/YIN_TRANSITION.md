# THE TOWER'S IMAGINARY→REAL CROSSING — the record's dynamics passes from the being field to the hearing field exactly once, at the cusp
## (outside bench memo 124, 2026-08-28; certificate `certificates/yin_transition.py`, GREEN; the computable half of the owner's yin/yang question)

**Why this bears on the question.** The owner asks whether the
programme is "yin only" — the positive side. The record's own
arithmetic already carries the split: **B721** states the two clocks
*"differ in FIELD (hearing **REAL** ℚ(√5) vs being **IMAGINARY**
ℚ(√−3))"*. So the sharp, computable version is: **where does the
record's own dynamics cross between them?**

**Y1 — THE CUSP COINCIDENCE.** The tower's level-1 coordinate is
**z₁ = 2 − 4ω, with Re(z₁) = 0 exactly — purely imaginary** — and

> **z₁ = −(4ω − 2) = −τ**, where τ is **memo 100's grammar-derived
> cusp modulus** (the longitude `baBAABab`'s holonomy, the disc-48
> lattice generator, the r-bit's carrier).

And z₁² = −12, real, because (2ω−1)² = −3.

**Y2 — THE CROSSING, exactly once.** Tracking the ω-component per
level: the tower is **non-real for exactly levels 0 and 1**, and
**real from level 2 onward** (−10, 146, 14402, 306950402, …). Once
real it stays real — the recursion is integer arithmetic in (z, κ)
with no route back to the ω-component. **The crossing happens once and
is irreversible.**

**Y3 — THE TRIPLE COINCIDENCE AT LEVEL 1.** Three facts from three
unrelated arcs meet at one level of the record's own tower:
1. **κ₁ = −2** — exactly **B496's Markov surface**, the locus whose
   mixed semigroup B496 studied;
2. **z₁ = −2√−3** — exactly **memo 100's** grammar-derived cusp
   modulus, the disc-48 carrier of the r-bit;
3. **Re(z₁) = 0** — the being field's *pure* part, and squaring it is
   precisely what makes everything real thereafter.

**Interpretation (labeled).** The programme is **not** built on the
positive side alone: it is built on a field that *is* the imaginary
one, with a real partner, and the record knows which is which. What
this cell adds is that **the crossing between them is a specific,
computable event — happening once, at the cusp.** Whether the
programme has *worked* both sides equally is a different question — a
corpus-balance question, not a mathematical one — and it is **not**
answered here. Gate 5 untouched.
