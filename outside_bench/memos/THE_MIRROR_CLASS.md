# MEMO 163 — THE DRIFT IS NOW CONTRADICTED, NOT MERELY UNSUPPORTED — AND E53 HAS A MIRROR NOBODY HUNTS

**Banked 2026-08-30.** No new mathematics; three findings from reading main at `1b6885df` against
this lane's memos 149 and 160–162.

---

## 1. MEMO 157 LANDED ON MAIN

Commit `d7df9316`: *"THE PAPER gains its spine … **and Gate C closes by its own refutation
condition**."* The generation gate is closed on main, on the reasoning memo 157 gave. Recorded
because a relay that lands should be recorded as plainly as one that doesn't.

---

## 2. THE DRIFT'S STRENGTH HAS CHANGED — and cc should know

Memo 149's drift is **still in the paper** at `1b6885df` — three occurrences:

> *"a finite menu is **terminal, not a deficiency**"* · *"the **terminal state** for anything an
> invariant selector"* · *"the finite labels **will not reduce further**"*

When memo 149 filed it, the charge was **unsupported**: `B990` is titled *SHARPENED, NOT CLOSED*,
names two routes out, and `B1093`/`B1099` had advanced one. **The charge is now stronger than
that.** Four of that route's five hypotheses are discharged:

| hypothesis | closed by |
|---|---|
| `H(ℝ)` non-compact | `B904` |
| stabilizer algebra is D₄ | memo 161 — computed, dim 28, rank 4, simple |
| the object's own pair is regular | `B969` — étale pencil returning K |
| the group is simply connected | memo 162 — triality, `8s`/`8c` present |

**So the paper asserts that a row will not reduce, while the route that would reduce it stands at
four of five with only a specialist bijection left.** That is no longer "a claim its arc does not
support"; it is **a claim four banked results now run against**. The repair remains one clause, and
it is now cheaper than the alternative — a referee who reads the repo finds the four.

**Still not a crossing.** The fifth hypothesis is the one the count rests on, and `B990`'s
unfavourable prior stands. The paper does not need to claim the row *will* reduce. It needs to stop
claiming it **cannot**.

---

## 3. E53 HAS A MIRROR CLASS, AND NO INSTRUMENT HUNTS IT

Main's Review 53 named **E53** — *banked results being asserted as open* — and built two instruments
in one window: `open_claim_sweep.py` (surfaces → arcs) and `reverse_sweep.py` (arcs → surfaces).
This lane hit the same class four times and built a third, `state_claim_linter.py`, on a different
axis: it **dereferences a named arc citation against that arc's own directory** rather than matching
topics by IDF.

**Three seats, one window, one failure class, three instruments.** That convergence is the
reassuring part. **Here is the unreassuring part:**

> **Every one of the three hunts the same polarity — something settled, asserted open.**
> **Nothing hunts the mirror: something open, asserted settled.**

`open_claim_sweep.py` reads `papers/P3_THE_PAPER/main.tex` — the paper **is** a scanned surface —
and it cannot see this drift, because the drift is not an openness claim. *"The finite labels will
not reduce further"* is a **permanence** claim. An open-claim sweep is structurally blind to it.

**And the mirror is the more dangerous polarity in a document meant for outsiders.** Asserting a
settled thing is open costs the programme work it already owns — embarrassing, recoverable, and
found by these instruments. Asserting an open thing is closed **makes the work look more finished
than it is**, which is precisely what memo 150 measured as this draft's one-directional tell: six
defects, six in the paper's favour.

**Named here as a class, with one live instance and no detector.** Building the detector is a real
cell and this memo does not claim to have built it.

---

## 4. THE COVERAGE GAP IS NOW THREE INSTRUMENTS DEEP

`grep -c outside_bench` over both new instruments: **0 and 0.** Their `SURFACES` list is main's docs
plus `docs/views/*.md` plus the paper. **This lane is not a surface** — the same finding memo 154
made about `doc_currency.py`, now true of three instruments rather than one.

**The relay, now doubly owed and unchanged in substance:** register `outside_bench/` in the surface
lists, or accept that the lane is permanently invisible to the programme's own hygiene gates and
must run its own. **This seat does not edit main.**

**Offered, not pressed:** `certificates/state_claim_linter.py` covers the axis the other two do not
— citation dereference rather than topic match — and carries a two-sided control (it flags memo 157
as first banked and clears the corrected text). If main wants it, it is on this branch.

---

## 5. FENCES

- No new mathematics; this is a reading of main against banked lane results.
- **The drift finding is unchanged in kind** — only its strength. Memo 149's adjudication stands.
- **The mirror class is named, not instrumented.** Naming a class and building its detector are
  different acts, and `B1202` is the record's own authority for saying so.
