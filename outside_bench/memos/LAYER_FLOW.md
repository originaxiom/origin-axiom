# THE LAYER FLOW — where ab/ba attacks the dominant bottleneck: the layering map is a closed-form dynamical system, and it is NOT the banked Fricke dynamics
## (outside bench memo 118, 2026-08-28; certificate `certificates/layer_flow.py`, GREEN; the owner's "where can an ab/ba principle solve bottlenecks again? read atlas and toolbox")

**Why this target.** The atlas names the ab/ba principle at the
foundation — node `noncommutative_cancellation_residue` (PC04): *"if
order matters, noncommutative residue can remain"*, with the L/R shear
system and the commutator certificates. THE_TOE_GAP names **dynamics**
as the one load-bearing missing ingredient. Memo 117 showed the
layering map never collapses. The question joining them: **is L a
dynamics?**

**THE DERIVATION (exact, and it closes to two variables).** On
(x, y, z) = (tr A, tr B, tr AB), layering acts as x' = y' = z and
z' = tr(AB²A). Using B² = yB − I, tr(A·AB) = xz − y, tr(A²) = x²−2,
and substituting xyz = x²+y²+z²−κ−2:

> **z′ = z² − κ  ·  κ′ = z²(2 − κ) + κ² − 2**

**D1 — verified against memo 117's independently computed tower at
every level 0–8: MATCH throughout.** The ab/ba layering *is* a
discrete dynamical system in (z, κ).

**D2 — and it is NOT κ-preserving:** κ′ − κ = −(κ−2)(z²−κ−1), zero
only on a curve. **Memo 94's banked Fricke map T(x,y,z) = (z, x, zx−y)
IS κ-preserving. So L ≠ T — layering does not generate the record's
banked dynamics, and the two must not be conflated.**

**D3 — exactly three fixed points:** (z, κ) = (1, 0), (−1, 2), (2, 2).
The record's own tower starts at κ = 1+ω, at none of them — which is
why it runs forever.

**D4 — THE HONEST SCOPING.** *Supplies:* a canonical, dimensionless,
exactly-specified flow on the record's own invariants, generated by
nothing but ab ≠ ba — two variables, closed form, no free parameter,
no imported premise. **That is the FORM of a dynamics, and the record
did not have one.** *Does not supply:* a **rate**. There is no time in
it; the schedule wall is untouched. **Where it lands:** the dynamics
deficit is typed as exactly (one dimensionless flow datum + one unit)
— this supplies a candidate for the **first half** and provably not
the second, the same form/rate split memo 90 found for expansion.
**Named next test:** is the layer flow's step B723's λ-weight? — the
level-crossing test already seated with cc, now with a *concrete
candidate flow* to test against instead of an abstract one.
**Fence:** since L ≠ T, nothing here inherits memo 94's two-branch law
or its arrow. Gate 5 untouched.

### ADDENDUM 1 (2026-08-28) — A CLAUSE WITHDRAWN (memo 120, the owner's catch)
D4 said the layer flow supplies "the FORM of a dynamics, **and the
record did not have one**." **That clause is withdrawn.** The record
already banked a flow: B721/B716's Anosov suspension of
σ = [[2,1],[1,1]] — real, hyperbolic, measure-preserving — with a
dimensionless rate (φ per tick) and an entropy (2 log φ). **This
memo's derivation and its closed form stand; the novelty claim does
not.** Correctly re-placed (memo 120): L is a **third** flow, distinct
from the Anosov suspension and from the Fricke map T, and its growth
is a different *class* — log|z| doubles per level (ratio → exactly 2)
where the pulse is φⁿ per tick, with **no banked map identifying a
level with a tick**, so the two are incommensurable as they stand.
Consequently D4's "candidate for the first half of the deficit" is
**weakened**: a super-exponential flow is a poor candidate for the
record's exponential pulse, and the λ level-crossing test should be
told so. This addendum is the only mutation.
