# MEMO 157 — GATE C ADJUDICATED: THE GENERATION GATE CLOSES ON BANKED MATERIAL IT NEVER CITED

**Banked 2026-08-30.** Seal `seals/GATE_C_ADJUDICATION_PREREG.md`, pushed before the arcs were read
in full. Gate C is `OPEN_PROBLEMS.md`'s **generation-count** gate — one of four where the record says
values could cross, and **the only one that would deliver a *feature*.**

---

## 0. OUTCOMES

| cell | outcome |
|---|---|
| **G-1** | **G1-BEARING** — two of the four uncited arcs speak directly to the gate's clauses |
| **G-2** | **G2-CLOSES** — the gate's own closing clause is met by material banked since `B324` |
| **G-3** | **G3-DISTINGUISHED** — `h¹ = 3` is a *multiplicity* statement, and the record says so itself |

**The declared prior was G2-STANDS. It was wrong, and in the direction that matters:** the gate is
answerable from the record, and has been for a long time.

---

## 1. THE GATE'S OWN CONDITION, AND WHAT ANSWERS IT

> **Opens** iff the commensurator ℤ/3 gives **three symmetric copies of the 27**.
> **Closes** iff those three images are the trinification factors **or otherwise fail to be
> symmetric matter copies**.

The gate's *Settled* list cites `B307`, `B302`, `B321`. **It does not cite `B323` or `B324` — the two
arcs that actually compute what the commensurator ℤ/3 does.**

**`B324` (PROVED) computed the three images exactly, in ℤ[ω], from explicit generators.** The
order-3 commensurator element `g = [[0,−1],[1,−1]]`, `g³ = I`, conjugates the Riley representation
into three images `genᵢ = gⁱ·{a₀,b₀}·g⁻ⁱ`. Its four findings, and the fourth is the gate's answer:

1. **Degenerate** — the two subdominant eigenvalues have *equal* magnitude (`|1| = |ω²| = 1`):
   **no mass hierarchy.**
2. **Ubiquitous ω** — the Eisenstein coefficient is the same ω running through the trace field and
   the trinification; not a new value.
3. **Tautological circulant** — g-conjugation *forces* `M[i,j]` to depend only on `(j−i) mod 3` by
   the cyclic trace identity. **The ℤ/3-circulant structure is automatic, not a discovery.**
4. **Same character** — *"The three 'generations' are g-**conjugates**, hence **share the same
   character** … whether they are distinct matter generations is **the B307-walled multiplicity
   question itself**."*

**`B323` (PROVED) settles which branch of the closing clause fires.** Its verified four-level table
finds ℤ/3 **only** at the E₆ gauge level and the commensurator, **and they are distinct** — level 1
(π₁(4₁)) has symmetry D₄ of order 8, and `3 ∤ 8`; level 2 (the cusp torus) is ℤ/2 × ℤ/2.

So: **the images are *not* the trinification factors** — the first closing branch does not fire —
**and they *do* fail to be symmetric matter copies** — the second one does.

**The closure is a conjunction, and neither arc does it alone.** `B324` hands the matter question
back to `B307`'s wall; `B307` kills symmetric C₃ three-generation trace fields. Together they close
the gate as posed. Separately, each looks like a partial result — **which is precisely why a
citation list that omits them leaves the gate looking open.**

---

## 2. G-3 — THE MULTIPLICITY TRAP, AND THE RECORD DISARMED IT AT BANKING

The seal made this binding: `B632` banks **`h¹(M; 27_ρ) = 3`** — exact, by Fox calculus, with an
independent block cross-check (`h⁰=1, h¹=3, h²=2`, Euler gate `1−3+2 = 0` PASS). Three is not three
generations, and this cell may not slide.

**It does not need to. `B632` states the caveat itself, at banking:**

> *"the three slots are **GRADED** (spins 0, 4, 8), **not three interchangeable copies** — one
> abelian + two chiral. Any generation reading must carry this structure; it is suggestive … but it
> is **a structural difference from 'three copies of one representation'**."*

**So `h¹ = 3` is a multiplicity statement, and an explicitly *heterogeneous* one.** It is not "three
symmetric copies of the 27" and cannot open the gate.

`B1036` confirms the direction: on the mirror double, **`h¹(dbl; 27) = 5 = 2+2+1`, not 3** — *"the
multiplicity mechanism is LIVE for the first time in the record, AND the number is 5, not 3 — a
candidate, not a generations theorem."*

---

## 3. WHAT CLOSING GATE C DOES *NOT* DO — the finding under the finding

**Closing the gate does not close the generation question. It redirects it**, and `B632` says where,
in its own words:

> *"This is a multiplicity mechanism **DIFFERENT from B302's commensurator ℤ/3** and **NOT blocked by
> B307** (which kills symmetric trace-field triples, **not cohomology multiplicity**)."*

So the live route is the **cohomological / heterotic-style count** (generations = matter-valued H¹),
and Gate C — the gate the record names as the generation question — **asks about the mechanism that
is dead, not the one that is alive.** Its current state on that live route:

- **solo: 3, but graded** (1 abelian + 2 chiral), so not three symmetric generations;
- **double: 5**, so the mechanism does not deliver 3 either;
- `B632`'s **cell 2 (the cup-product texture) is queued and unrun**, with a *falsifiable prediction
  already registered before it runs* — the pairing is block-diagonal, so cross-block textures vanish
  identically.

**That queued cell is the actual live generation computation on the record, and it is not any of
Q7–Q10.**

---

## 4. WHAT THIS CHANGES

1. **Q9 must not be sent as drafted.** Its ask — *does the intrinsic ℤ/3 realize as three
   generations?* — is answerable NO from `B323` + `B324` + `B307`. Sending it would ask a specialist
   to redo banked work, which is the finished-but-forgotten failure at its most expensive.
2. **Relayed to cc (main-side):** Gate C's *Settled* list should cite `B323` and `B324`, and the
   gate should be closed by its own refutation condition — or a reason given why the second clause
   does not fire. **This seat does not edit `OPEN_PROBLEMS.md`.**
3. **The four-gate count is affected.** The record's load-bearing *"four named gates where values
   could cross"* would become **three**, and the one that carried the generation question would be
   gone — with the generation question *not* gone, only relocated to `B632`'s cell 2.
4. **If Q9 is to be sent at all**, it should be rewritten around the live mechanism: *the
   cup-product texture on `H¹(M; 27)`, its block-diagonality prediction, and why the count is 3
   graded solo and 5 on the double.* That is a different question with a different specialist.

---

## 5. FENCES

- **The inference from `B324` to the 27 is mine, and is stated as such.** `B324` computes at the
  holonomy level; since `ρ = principal ∘ ρ_geo`, conjugate holonomies induce conjugate — hence
  equal-character — images on the 27. I judge that transfer sound; a reader may check it.
- **I did not close the gate.** `OPEN_PROBLEMS.md` is main-side. This memo reports that its own
  stated condition is met and relays the call.
- **No numerical coincidence of the integer 3 was used as evidence**, per the seal.
- The prior I declared was wrong. Recorded plainly, because a seal whose prior is only ever
  vindicated is not doing any work.
