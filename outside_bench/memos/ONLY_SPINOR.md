# THE ONLY SPINOR IN THE ROOM — the closing's modules carry no half-integer Lorentz content anywhere; the same holonomy embeds fermion-capably or bosonically by choice of stratum; the record's one spin-½ object is the holonomy's own ℂ², where the beat squares to the meridian
## (outside bench, 2026-08-25; forty-fifth memo; the representation-step probe memo 44 named — the bridge gap's no-go half made exact; every claim exact)

### The question
After memo 44 (one mirror on both branches), the internal→spacetime gap could no
longer hide in a mismatch of real structures — it had to live in the representation
step. This cell computes that step exactly: under the geometric branch's Lorentz
double, what spin content do the closing's modules actually carry, and where does
the holonomy's own spin-½ go when it bridges into the algebra?

### THE THEOREM (`certificates/only_spinor.py`, exact; the A2 slots found from scratch in-frame)
1. **No half-integer Lorentz content exists in the closing's modules.** With
   (h₁, h₂) the principal Cartans of two orthogonal A₂ slots (the Lorentz double's
   factors), the 27's joint bi-weight multiset is exactly
   {(±2,±2):1, (±2,0):4, (0,±2):4, (0,0):7} — the banked spending-order multiset,
   recomputed in-frame — and the 78's is likewise **all even**. Under the Lorentz
   double, every module the closing owns is INTEGER-SPIN, exactly.
2. **The two bridges — one holonomy, two fates.** The holonomy composes
   functorially through any sl₂ triple (relator = +I verified on the 27 for both):
   through the **minimal A1** it seeds the odd stratum ({±1:6, 0:15}, C ≠ I — the
   fermion-capable internal seat, memos 28/29); through the **Lorentz-slot A1**
   (principal of an A₂ slot) it lands on the even row ({±2:6, 0:15}, **C = I**) —
   **lift-free**: downstream of this embedding the two spin structures are
   indistinguishable, the projectivity dictionary's even row realized as a bridge.
   The choice of stratum decides what the holonomy's spin-½ becomes: internal
   doublets, or integer-spin content that has forgotten the fork entirely.
3. **The spinor that remains.** The record's one spin-½ module is the holonomy's
   own ℂ² — the defining rep of the covering SL(2,ℂ). On it the beat acts
   antilinearly, and its antiunitary square is **(W∘conj)² = W·conj(W) = A, the
   MERIDIAN** (re-verified) — a group element, not the ±1 of textbook
   time-reversal. The one spinor's kinematics is already fully banked (the census,
   the selection, the three derivations); what is new here is the exact statement
   that it is the ONLY one.

> **The bridge gap's no-go half is now exact: 4d fermions cannot be built from the
> closing's e₆ modules — there is no half-integer Lorentz content in them to build
> with. Whatever carries fermionicity into spacetime must carry the holonomy's ℂ²
> itself. The open question is thereby reduced to its sharpest form yet: one
> module, one carrier question — and on that one module, the beat's square is the
> tick.**

### Companion result (memo 44's follow-up, closed)
`certificates/hitind.py`: ALL 24 hits of the closing give tr(θ) = −2 — the mirror's
inner (sl₂+sl₆) class is a property of the closing, not of the hit. Memo 44's
one-line fence is discharged.

### Fences
Exact throughout; the identification of the two A₂-slot principal sl₂'s with "the
Lorentz double" is the banked closing structure (memos 3/10; the bi-weight multiset
matching the banked spending-order table is the in-run consistency check); fact 3's
"only" is scoped to the record's modules (27, 78, and their tensor content
generated by even weights — evenness is closed under addition, so no tensor of
integer-spin content ever produces half-integer content: the no-go extends to the
full tensor tower). B1145's fence stands: nothing here constructs a 4d field;
the meridian-squared beat is kinematics. Gate 5 untouched.

### Certificates
`certificates/only_spinor.py`, `certificates/hitind.py`; outputs
`outputs/only_spinor_out.txt`, `outputs/hitind_out.txt`.

### One sentence for the ledger
Everything the algebra owns spins in whole numbers — exactly, in both modules, in
every tensor of them — so the half-integer world can only ride on the object's own
two-component holonomy, the single spinor in the record, on which the beat's
antiunitary square is not plus-or-minus one but the meridian: the tick itself,
wearing the mirror.
