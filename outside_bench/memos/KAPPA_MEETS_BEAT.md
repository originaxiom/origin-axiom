# WHERE THE TWO CONSERVED THINGS MEET — the beat reflects the first integral, and every beat-invariant function of κ is the field's own 3: trace 3, norm 3, minimal polynomial X²−3X+3, discriminant −3 = d_K
## (outside bench, 2026-08-25; forty-first memo; the dive cell opened by THE_VIEW_FROM_ABOVE — the atlas's one forced conservation and this lane's one conserved involution, related exactly; every claim exact)

### The question the zoom-out raised
The atlas holds exactly one FORCED recurrence: the first integral κ = tr[a,b] (the
Suto invariant, conserved by the trace map — K001/K007, 234 probes, the only motif
with conserved-status first-integral). This lane's arc exhibited one conserved
involution: the beat, closing at every structural layer. Two conserved things, two
lanes. Are they independent?

### THE THEOREM (`certificates/kappa_beat.py`, exact over ℚ(q) on the banked χ=+1 lift)
1. **κ, two ways:** tr[A,B] computed directly AND via the Fricke identity
   x²+y²+z²−xyz−2 on the trace triple (2, 2, 2+q) — both give
   **κ = 1 + q exactly.**
2. **The beat does NOT fix κ — it REFLECTS it:** with β(X) = W·conj(X)·W⁻¹,
   the commutator trace of (β(a), β(b)) is **gal(κ) = 2 − q ≠ κ** — computed by
   direct matrix evaluation, not by the trace-invariance argument. The structure
   lane's involution acts on the dynamics lane's integral as pure Galois.
3. **The meeting point is the field's 3, four ways:**
   - κ + gal(κ) = **3** (the trace of the first integral)
   - κ · gal(κ) = **3** (the norm)
   - minimal polynomial of κ over ℚ: **X² − 3X + 3**, discriminant **−3 = d_K**
   - N(κ) = 3 ⟹ **the ideal (κ) generates the ramified prime of ℚ(√−3)** — the
     unique prime above 3, the conductor of χ₋₃.
   The Fricke level c = κ + 2 = 3 + q maps under the beat to its Galois-conjugate
   surface 4 − q: the beat exchanges the two Fricke levels as it exchanges the two
   conjugates of κ.

> **So the two conserved things are one Galois orbit apart: the beat reflects the
> first integral, and everything the beat cannot change about κ — its trace, its
> norm, its minimal polynomial, its ideal — is built from 3, the field's
> discriminant-prime. The atlas's dynamics-side conservation and this lane's
> structure-side conservation meet, and their meeting point is the arithmetic of
> ℚ(√−3) in its purest coin: the one ramified prime, generated by the first
> integral itself.**

### What this adds to the record (and to the atlas proposal)
The `beat` motif proposal (THE_VIEW_FROM_ABOVE) gains an exact anchor: the beat and
κ are not two unrelated conserved objects to be counted separately — the beat acts
on κ's Galois pair, and the beat-invariant content of the first integral is the
discriminant arithmetic. Interpretive reading, labeled: the dynamics lane conserves
a NUMBER whose ambiguity (which conjugate) is exactly the bit the structure lane's
involution flips — the same mirror bit memo 37 showed the boundary cannot see. One
ℤ/2, now caught acting on the first integral itself.

### Fences
Exact throughout; standalone certificate (no machinery imports); the Fricke identity
is used as a CROSS-CHECK of the direct computation, not as the source. The
interpretive paragraph is labeled. Gate 5 untouched — 3 here is the discriminant of
the trace field, an object-side integer, not a measured value.

### Certificates
`certificates/kappa_beat.py`; output `outputs/kappa_beat_out.txt`.

### One sentence for the ledger
The one number the dynamics must conserve and the one reflection the structure must
carry turn out to be a single coin seen from its two faces — the beat flips κ to its
conjugate, and what it cannot flip (trace, norm, polynomial, ideal) is 3 every time:
the ramified prime of the object's own field, minted by the first integral itself.
