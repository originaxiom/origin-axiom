# THE SURVIVING ℤ/2s NAMED — the unique chain's discrete remainder is ONE SM-torus shadow (P_Y = P_T, the ℤ/6-center identity measured on the 27) plus 14 genuinely extra E6 remnants, and NOT ONE of the 15 is an R-parity substitute: every survivor is odd on exactly 8 of the 15 SM states
## (outside bench, 2026-08-26; seventy-sixth memo; the memo 72 follow-up; BRANCH N fired; one preregistration error filed — and the error is itself a theorem)

### THE FACTS (`certificates/z2_census.py`; asserts GREEN)
- **FACT 0 (the roster).** Memo 72's machinery rebuilt with the found SM
  assignment now CAPTURED: the 27 = 15-plet (q 6, u^c 3, d^c 3, l 2, e^c 1)
  + 4 Higgs-candidate doublet states + 6 exotic colored states (D/D^c) + the
  2 vev states (ν^c/S). The grading space of memo 72 confirmed a dim-4 GF(2)
  vector space (16 patterns, 15 nontrivial).
- **FACT 1 (the shadow — ERROR FILED, and the error is a finding).** The
  bench preregistered TWO independent SM-torus shadows (P_Y = Y/Yq mod 2
  with hY verified in the root lattice, and P_T = 2T3 mod 2), span dim 2.
  **The machine refused: P_Y = P_T identically on all 27 states** — oddness
  of 6Y coincides with weak-doubletness state-by-state (q, l, and the Higgs
  doublets odd; every singlet and every exotic even). This is the ℤ/6-center
  identity of the SM's global structure [SU(3)×SU(2)×U(1)]/ℤ₆ — (−1)^(6Y)
  acts as the SU(2) center times color triality, and triality is invisible
  mod 2 (CITED context; the identity itself verified exactly here).
  Corrected claim: **the SM shadow subgroup has dim 1** — ONE nontrivial
  shadow — so **14 of the 15 survivors are EXTRA**: discrete remnants of E6
  beyond anything the surviving gauge group provides.
- **FACT 2 (the R-parity census — BRANCH N).** Gradings odd on ALL 15
  SM-plet states: **ZERO**. The measured census is stark: **every one of
  the 15 survivors is odd on exactly 8 of the 15 SM states** (a
  constant-weight restriction — the survivor code restricted to the 15-plet
  is a constant-weight-8 code; measured, no mechanism claimed). The full
  table: 3× (8/15 SM, 0/4 Higgs, 4/6 exotic), 6× (8, 2, 2), 2× (8, 2, 6),
  1× the shadow (8, 4, 0), 3× (8, 4, 4). **No surviving ℤ/2 separates
  matter from Higgs the way R-parity must.**
- **FACT 3 (the dark column).** Each survivor is constant on exactly ONE of
  the three ψ-classes (the trivial singlet); **none is constant-odd on the
  ψ-10 dark class** — no surviving grading protects the dark block in the
  ψ-frame either. (Frames misalign — memo 72's filed error — and here that
  misalignment is measured as total: 0 of 15.)

> **The unique chain's discrete remainder is now fully named: one ℤ/6-shadow
> the SM carries anyway, and 14 extra E6 ℤ/2s — none of which is a matter
> parity, none of which shields the dark states. Combined with memo 71 (no
> supercharge) this closes the R-parity question at the kinematic level from
> both ends: the object gives neither the SUSY that would need an R-parity
> nor the R-parity that SUSY would need. Consistent with the E6 folklore
> that 27-only breaking loses matter parity — here as an exhaustive census,
> not folklore. Necessary conditions only; Gates 2/3/5 untouched.**

### Certificates
`certificates/z2_census.py`; output `outputs/z2_census_out.txt` (in-lane
rerun byte-identical).
