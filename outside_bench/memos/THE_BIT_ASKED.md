# 194 — THE BIT, ASKED AT LAST: the object is at zero, and the sister that shares its atom is not

**Date** 2026-09-10 · **Lane** outside bench · **Branch** `<seat>/outside-bench`
**Preregistration** `seals/L192_THE_BIT_PREREG.md`, sealed `969d8bd1` **before** the certificate was
written · **Certificate** `certificates/l192_the_bit.py` · **Output**
`outputs/l192_the_bit_out.txt` · **Gate 5 untouched**

---

## 1. Why this question, and whose question it is

It is not mine. `docs/OPEN_LEADS.md` **L192**, registered 2026-08-31 by B1226 at ★★★, **never run**:

> Box D — β-odd, dimensionless: `θ_QCD`, `δ_CKM`, `δ_PMNS` — is the only box where the object has an
> output at all, and that output is `CS`, forced 2-torsion by amphichirality (B1224): **one bit**.
> Every probe ever fired into box D demanded a *continuous value* from that bit-valued channel —
> B1027 (δ₁₃, MISS 11.4σ/38.0σ), B1137 (algebraic combinations, DISJOINT), B813 (CS = θ_QCD,
> REFUTED ON TYPE). **3/3 asked for a value; 0/3 asked the bit.**

Three probes fired into a bit-valued channel and each demanded a real number. L192's diagnosis is
that those negatives measured **the type mismatch, not the object**. It then set three preconditions
before anything could be run. This certificate meets all three and asks the bit.

*Corpus exhausted first, terms stated: `already_banked.py` on "2-torsion subgroup mirror odd
invariant amphichiral", "cannot count only a bit chirality", "A[2] value group torsion chirality",
"generation number impossible amphichiral theorem", "mirror-odd quantum invariant twist family".
The 2-torsion **fact** is banked many times over (B152, B1224, B928/B938/B939, `THE_END_TO_END_CHAIN`,
L119). The bit-level **question** is banked nowhere.*

## 2. What was computed

Every one-cusped orientable manifold in SnapPy's census — **203 122 manifolds** — classified by
amphichirality and by `CS mod ½`, with the distance to the nearest element of `A[2] = {0, ¼}`
printed rather than asserted.

| | | |
|---|---|---|
| **C1** | every amphichiral manifold lands in `A[2]` | worst distance **2.6 × 10⁻¹⁵**, tol 10⁻⁹ — **PASS** |
| **C2** | the theorem is non-vacuous | **75 of 181** amphichiral manifolds have `CS ≠ 0` — **PASS** |
| **C3** | the converse fails | **47 chiral** manifolds sit at 0 (m208, s595, s956, v2037, …) — **PASS** |
| **C4** | margins printed, not asserted | **PASS** |

C3 is the one that matters most and it is the one L192 did not ask for: without it the bit would be
a chirality detector wearing another name. It is not. `CS = 0` does **not** imply amphichiral.

## 3. The four cells, all preregistered, all landing on B

**CELL 1 — is the bit non-vacuous?** Amphichiral occupancy of `A[2]`: **106 at 0, 75 at ¼**.
**B.** Amphichirality forces *membership* in `A[2]`; it does **not** force *which element*. The
channel carries information.

**CELL 2 — where is the object?** `m004`: amphichiral, `CS = 1.35 × 10⁻¹⁶`, class **0**.
**B — the object sits at the identity.**

**CELL 3 — does volume predict it?** **59 volumes carry both elements.** **B — independent data.**

**CELL 4 — does the atom predict it?** **B, and this is the finding.**

## 4. The finding

> **`m003` and `m004` are commensurable. They share the volume 2.029883, the invariant trace field
> ℚ(√−3), and V₄ — and they sit at opposite elements of the bit.**

Read from the record, not recomputed: B781's own preregistration (*"m003 share trace field ℚ(√−3),
volume 2.029883, AND V4"*), B762's findings, B993 (commensurable, index 12).

Why that is worth more than it looks. **B727/B993 is the genericity theorem**: the chain buys the
*field* ℚ(√−3) at geometrization, and by B803's rule a step depending only on the invariant trace
field, the invariant quaternion algebra, or arithmeticity is a **commensurability-class** statement —
which is exactly why the E₆ recurrence is generic and not evidence. B993 then said, in as many
words, where to look instead: *"object-specificity, if it exists anywhere, lives at the group
level."*

**The bit is below the class.** It separates `m004` from the one manifold that agrees with it on
everything the chain actually buys. Whatever else it is, it is not generic in the way B727 showed
the rest of the chain to be. And the same structure repeats across the census — `m135`/`m136`,
`m206`/`m207`, `s463`/`s464`, `s725`/`s726`, `s881`/`s882`/`t03910`/`t03911` — 59 volumes over.

## 5. What this does NOT establish — and the fence is the seal's, written before the run

No value of any CP-odd phase follows. **B813 is the governing theorem**: *"a fixed PSL(2,ℂ)
geometric invariant cannot fill the coefficient slot of `e^{iθW(A)}`, whose functional slot a
Chern–Simons object already occupies."*

Nothing here fills that slot. What is compared is `A[2]` for two different circles — `ℝ/½ℤ` on the
object side, where `A[2] = {0, ¼}`; and `ℝ/ℤ` on the phase side, where the CP-even locus is the fixed
set of `φ ↦ −φ`, i.e. `A'[2] = {0, ½}`. A homomorphism of two-element groups is not a value
dictionary, and **no dictionary carrying the object's bit to any measured phase is proposed here**,
because B813 shows the naive one does not exist. The seal says in advance: *a reader who judges that
this still re-enters B813's slot should record the cell as failed.*

Nor does it connect the bit to a Lagrangian, nor establish that any Standard-Model phase inherits
anything. **Gate 5 untouched**: the object side was computed and printed before any measured
quantity was named, and none was used as input.

## 6. What it changes

**L192 is answered and can be closed as RUN.** Its three preconditions were met; its bite control
(m003) fired; its criterion can fail in both directions and does.

**The three old negatives are re-typed, not overturned.** B1027, B1137 and B813 asked a bit-valued
channel for a real number. They are correct as stated and they measure the mismatch. The channel
itself is now characterised for the first time: **well defined, discriminating, and object-level.**

**One new lane, named and bounded, not started.** Whether any CP-odd Standard-Model phase can
inherit a bit from a carrier is a *dictionary* question, and B813 fences the naive answer. That is
where box D goes next, and it is a question about a dictionary, not about this object.

*Interpretive passages are labelled in the certificate output. Nothing here promotes to `CLAIMS.md`.*
