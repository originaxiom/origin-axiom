# ORDER WITHOUT DIRECTION — the owner's proposal ("the very initial a, ab, ba locks the arrow") lands as a theorem-shaped half: the first two letters DO lock that temporal order exists — and the lock's exact measure is κ itself (tr[a,b] = 1+ω = κ) — while the direction stays free three measured ways
## (outside bench, 2026-08-27; eighty-sixth memo; wave-4, the arrow dossier's row AR0; the owner's conjecture typed, half-confirmed, half-priced)

### THE FACTS (`certificates/order_direction.py`; asserts GREEN)
- **FACT 1 — ORDER IS FORCED, and its measure is κ.** ab ≠ ba from the
  first two letters, exactly: at the SL2 level the commutator is
  nontrivial with **tr[a,b] = 1 + ω = (3+√−3)/2 = κ** — the initial
  noncommutativity's exact size is the record's conserved quantity, the
  κ of the trace-3 quadratic X²−3X+3 the lane has tracked since memo 30.
  (Observed identity, exhibited in-run; the standard fact that tr[a,b]
  generates the invariant trace field is CITED context.) At the internal
  level A₂₇B₂₇ ≠ B₂₇A₂₇ (entry exhibited). **Time-order structure — that
  doing a then b differs from b then a — is object-paid from the record's
  first act, and the price tag reads κ.**
- **FACT 2 — but ab ~ ba.** ba = a⁻¹(ab)a: conjugate elements, equal
  traces at both levels (asserted), the same closed geodesic. The object
  cannot tell ab from ba as a *state of affairs* — only as a local
  composition.
- **FACT 3 — the DIRECTION is free, three measured ways.** (i) inversion
  w ↦ w⁻¹ is character-invisible (memo 79 re-asserted, 0 exceptions);
  (ii) NEW: the strong-inversion relabeling σ (every letter to its
  inverse, order kept) is ALSO character-invisible — 0 of 160 ball words
  change character: running every generator backwards costs nothing;
  (iii) the tick's staircase is direction-independent: N′ = A⁻¹−I =
  −A⁻¹N exactly, so ker N′ᵏ = ker Nᵏ — the depth-3 one-way filtration
  (memo 50) is the SAME staircase whichever way the meridian runs.

> **The refined statement of the owner's insight: the initial a, b lock
> the EXISTENCE of the arrow — the record is temporal in structure from
> its first act, with κ as the exact noncommutativity price — and leave
> the arrow's DIRECTION to the observer's c-leg, precisely the split the
> parity law (B1168) and the shared involution (B1174) demand. Order:
> object. Direction: observer. The proposal was right about what is
> locked; the record says what "locked" means. Dossier row AR0 filed.
> Gate 5 untouched.**

### Certificates
`certificates/order_direction.py`; output `outputs/order_direction_out.txt`
(in-lane rerun byte-identical).

### ADDENDUM 1 (2026-08-28) — THE COMPANION PRINCIPLE, at the owner's proposal (memo 113)
This memo read ORDER off the first two letters. The owner asked for
another principle "from that one beat," and it is now derived:
**COMPLETENESS** comes off the same two letters. The triple
x = tr(a), y = tr(b), z = tr(ab) determines the trace of EVERY word at
EVERY length by universal integer polynomials (4,372 words to length 7
verified, zero mismatches) — the second beat and every beat after it
adds NO new invariant. And this memo's κ is **re-derived from that
triple**: the Fricke commutator polynomial x²+y²+z²−xyz−2 evaluated at
(2, 2, 2−ω) is exactly 1+ω. So the first beat fixes order AND fixes
the entire information budget; later letters multiply classes, never
invariants. This addendum is the only mutation.
