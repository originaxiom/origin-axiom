# THE ANOMALY PAYMENT — the visible 16 alone is quantum-inconsistent under the family u(1); the dark block 10 ⊕ 1 carries exactly the opposite anomaly tensor, component for component, with the so(10) Dynkin ratio 2 as the mechanism
## (outside bench, 2026-08-26; fifty-eighth memo; serious-probe rung 3 for ladder X3 — the anomaly Diophantine level; integer identities only, value-level but not value-matching; REPRODUCED-NOT-PREDICTED per B950's rule; the corpus's registered L132 is the adjacent cell in B892's u(1)³ frame — related, not executed here)

### The question
The last rung of the dark-sector probe ladder that needs no dynamics:
anomaly cancellation. These are *integer* equations — the one place where
accepted physics derives rather than measures — so the firewall permits
them. The sharp form: is the SM-shaped 16, by itself, even a consistent
chiral spectrum if the object's family u(1) is gauged? Or are the dark
states required?

### THE FACTS (`certificates/anomaly_payment.py`; frame check det(D5 Cartan) = 4; every claim an assert, GREEN)

- **FACT 0 — every anomaly dies at once (reproduced in-stack).** The full
  symmetric cubic weight tensor of the 27 — all 56 components over the
  basis {q, m₁…m₅} of Cartan functionals — is **identically zero**. Every
  gauge anomaly of every subgroup chain vanishes. (Textbook E6 lore — this
  is *why* E6 GUTs use the 27 — here re-derived from the object's own
  crystal, per B950's reproduced-not-predicted rule.)
- **FACT 1 — the visible block alone is inconsistent.** T₁₆ ≠ 0: its 35
  pure-D5 components all vanish (the 16 is an so(10) rep and SO(10) is
  anomaly-safe), and its nonzero components are **exactly the q-touching
  channels**: [u(1)]³ = 16, linear/gravitational Σq = 16, and the mixed
  [D5]²·u(1) form B₁₆ (components 8 and −4 in the Dynkin-label basis).
- **FACT 2 — the payment is exact.** T_dark = −T₁₆ in **every** component:
  [u(1)]³: 10·(−8) + 4³ = −16; linear: −20 + 4 = −16; mixed: −2·B₁₀.
- **FACT 3 — the mechanism is the index ratio.** The mixed cancellation
  1·B₁₆ + (−2)·B₁₀ = 0 holds because **B₁₆ = 2·B₁₀ entry by entry** — the
  so(10) Dynkin-index ratio T(16)/T(10) = 2, exact in the stack — and the
  singlet contributes B₁ = 0. The charge assignment (1, −2, 4) and the
  index ratio 2 are two halves of one identity.

> **THE ANOMALY PAYMENT: if the family u(1) is gauged, the visible
> generation by itself is not a consistent quantum theory — it is anomalous
> in precisely the channels that see the u(1) — and the dark block is the
> exact, component-by-component payment that restores consistency. In this
> kinematics the dark states are not optional passengers; they are what the
> quantum consistency of the visible sector costs.**

### The dark-sector arc, complete at the kinematic level (memos 56–58)
1. **Topology (56):** the singlet's only door is the vector-like 10; a
   matter parity is conserved; the object's lock is a different ℤ/2, and
   the only mirror-stable one.
2. **The escalator (57):** the object's clock walks the family ladder
   (1 → 16 → 10, charge −3 per rung); the dark candidate is the bottom of a
   locked chain ending in the 16; the boundary's fixed space grazes it in
   one mixed dimension.
3. **Necessity (58, this memo):** with the u(1) gauged, the dark block is
   the anomaly payment for the visible one — required, not decorative.
   What remains for dynamics/values (Gates 2/3/5): whether the u(1) *is*
   gauged and broken, masses, stability lifetime, abundance.

### Fences
Integer identities only; nothing here matches a measured number. The D5
frame fence (memo 56) applies: the split 16 ⊕ 10 ⊕ 1 is frame-paid; the
tensors are then forced. FACT 0 is standard and labeled reproduced; the
added content is the split payment structure (FACTS 1–3) in the object's
coordinates. L132 (the corpus's registered hypercharge/anomaly lead in
B892's rank-3 abelian frame) is the natural sibling cell — flagged for the
seat, not claimed here. Gates 2/3/5 not crossed.

### Certificates
`certificates/anomaly_payment.py`; output `outputs/anomaly_payment_out.txt`
(vendored copy re-run in-lane, byte-identical).

### One sentence for the ledger
The dark sector's reason-for-being, at the only level the record can yet
ask it: the visible generation's quantum ledger does not balance under the
object's family charge — it is off by exactly sixteen in every channel that
counts — and the dark states are the entries that make the books close.

### ADDENDUM (2026-08-26, from B1158's harvest triage — quarantine adopted)
The seat banks the INTEGER IDENTITY (own-reproduced, and independently
matched by codex R012) and quarantines the "required, not decorative"
headline as CONDITIONAL — which this memo's own box and fences already
state ("if the family u(1) is gauged") but the headline rode ahead of.
Adopted: the banked content is the exact identity T_dark = −T₁₆ in every
channel; "required" holds conditional on (a) the u(1) being gauged and
(b) the observer-paid D5 frame. OA-C0014 stays EXTERNAL_BLOCKER.
