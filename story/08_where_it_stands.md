# 08 — Where it stands

> Narrative, not claim. Cites the mathematics one-way.

What is **proven** (the core that stands without any of the speculation): the metallic classification (**B92**);
the Dickson tower exact at `n ≤ 4`, including the from-first-principles SL(4) proof (**B80**) and the universal
class-function reframing (**B103**); degree=rank at `n = 3, 4` and the figure-eight A-polynomial family,
`M⁴ = L` symbolic-exact over `ℚ(ω)` (**B89**, **B67/B71/B83**), now shown **complete on the irreducible locus** —
B89's family is the entire component of irreducible `SL(4)` bundle reps, `M⁴=L` unconditional there (**B149**, chapter
10); the three fixed-point classes and the per-eigenvector law (**B106**). The proven core `P1–P16` is untouched by
all of it.

The **central open problem** is still one clean statement: prove **`char(ρ_n) = the Dickson catalog`** by reproducing
the opposition-involution multiplicities directly from `ρ_n` (chapter 04). It would close the `n ≥ 5` catalog *by
proof* rather than by computation, and dissolve the `n=5` wall (chapter 05) into a corollary. The representation-theory
consolidation (**B111–B124**, chapter 09) sharpened it: the **sign half** is *proved for all `n`* via the opposition
involution (**B112**), and the **magnitude half** has a canonical name — the tower is the **symmetric powers of the
external monodromy fundamental**, `ρ_n = Sym^n(W) ⊕ (Sym^{n−3}(W) ⊖ W)`, `W = V⊕1`, `det(W) = −1` (**B122**/**B121**).
So the wall is re-aimed but not lowered: *prove the tower is **functorially** `Sym^n(W)`* — the missing
`Sym(W) → trace-ring` construction. **This is now the one genuinely-open central math problem.**

Since chapter 09 was written, two further arcs ran to completion — and one of them was the physics question the whole
project had orbited. The **chirality axis closed** (**B144–B147**, chapter 10): chirality is forced and generic but
always either self-mirror (canonical singles) or mirror-paired (composites), so no handedness is ever preferred; the
arithmeticity arm was refuted outright (`RRL/RLL` are arithmetic chiral, `ℚ(√−7)`). The **unit's symmetry was pinned
and then recognized** (**B148**, **B150**, chapter 11): `κ = 4·I_FV + 2`, the metallic monodromies *are* the `SL(2,ℤ)`
mapping-class action — and that action *is* the `N=2*` class-S S-duality action (literature-confirmed, the widest reach
the one-object picture achieves). And the **physics question reached its boundary and was mapped** (**B151**, chapter
11): the firewall *holds* — the object's invariants are dimensionless and carry no physical scale. The physics arc is
**closed**: a real bridge (a symmetry identity) and a confirmed wall (no scale crosses).

The **live calculation pointers** — the only thing the physics exercise licensed (chapter 07,
`../speculations/PHYSICS_EXERCISE.md`) — all point inward at the mathematics:

1. **`θ = −w₀ → c`** — does the opposition involution predict the Dehn-filling scalars `c = {1, −1, i}`? It feeds
   the `ρ_n` proof directly and decides whether the off-locus sector is determined or independent.
   (`../speculations/S012`)
2. the genuinely **off-locus irreducible sector** — the one place independent content could live, and *not* the
   B106 Dehn-filling reps (`../speculations/S011`).
3. **amphichirality `Z_k = Z_{−k}` at SL(3)** + whether `m=1` is uniquely amphichiral — a robust topological fact
   bearing on `m=1` selection (`../speculations/S001`).

Other open threads remain logged and honest (`../docs/OPEN_LEADS.md`): genus-2 generality of `Mⁿ = L`, the
Lawvere/renormalization grounding, the `GL(2,ℤ)` gluing/interaction landscape (H5), and the one non-deflatable
"minimality" question (H4: why the *arithmetic* axis bottoms out at the figure-eight). The dead ends are kept as
first-class boundaries (`../philosophy/P003`, `../speculations/TOMBSTONES.md`), and the physics chapter stays CLOSED.

That is the state: a small self-generating object that turned out to know about knots, Lie theory, quasicrystals, and
a four-dimensional gauge theory's duality; **one sharp open theorem** (the functorial `Sym(W)` wall); a physics
question **reached and mapped to its boundary**; a mapped frontier; and an honest door on the speculation that
motivated it.

→ `09_the_representation.md` (the object crystallized as a representation), then `10_chirality_and_completeness.md`
and `11_the_bridge_and_the_wall.md` (the closing arc). ← back to `00_the_question.md`. See also `../ARCHITECTURE.md`.
