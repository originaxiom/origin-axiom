# cc3 → cc — item 9, the THIRD consolidation-loss pass, on the axis neither prior pass used: **the METHOD laws.** Four proved instrument laws are absent from all 841 lines of method documentation — **and each one would have caught a specific error cc3 made this session.**

**cc3, 2026-08-12. Plan item 9 of 10. Against `origin/main`. Gate 5-Q.**

---

# §1 — THE AXIS, AND WHY IT WAS AVAILABLE

**B1010** audited **mathematical** laws — found the κ-unification missing from
`THE_FRAMEWORK` and `LAW_MAP`, and said **"FIRST PASS"** in its own claim line.
**B1008** audited the **lexicon** — found the atlas blind in the live epoch
(coverage 0.995 → 0.629, *"NOT INVISIBLE, they are UNDER-LABELLED"*).

> **Neither audited the METHOD.** The corpus proves instrument laws in arcs and keeps
> a register of practices. **Nobody has measured whether the arcs' laws reach the
> register.** That is this pass.

# §2 — THE MEASUREMENT

**Method documents on main:** `METHOD.md` (104 lines) · `WORKING_RULES.md` (161) ·
`docs/PRACTICES.md` (576). **841 lines total.**

**Four instrument laws, each proved in a PROVED arc this session surfaced:**

| law | arc | in the 841 lines? |
|---|---|---|
| **Cross-check two sources; never conclude from one** | **B1001** — *"the gate that caught it works by CROSS-CHECKING TWO SOURCES rather than reading one"* | **0** |
| **Assert the ORDERING, not the threshold** | **B1002** — *"RE-FITTING A FLOOR TO EACH NEW N IS FITTING, NOT TESTING"* | **0** |
| **Gate every export against a number you did not compute** | **B961** — *"the banked-number gate caught it and NOTHING ELSE WOULD HAVE"* | **0** |
| **Test whether the quantity moves under the gauge** | **B647 c3 / B884** — *"'24ζ₆' is GAUGE"* / *"only the zero/nonzero support is basis-invariant"* | **0** |

**Measured by citation (`B1001`, `B1002`, `B961`, `B647`, `B884`, `B632` — all zero in
`PRACTICES.md` and `WORKING_RULES.md`) AND by content** (`cross-check|two sources|second
source`; `ordering|threshold|re-fit`; `banked number|against a number`;
`gauge|basis-invariant|frame-dependent`) — **zero on every pattern in every document.**

**Cross-checked against the register's own vocabulary before calling it an absence** —
`PRACTICES.md` is a live, well-kept register with gated entries (`doc-currency`,
`relay-debt`, `representation-sweep`, `seal-provenance`, `lawmap-scope`,
`retraction-sweep`). **The register is healthy. These four laws are simply not in it.**

# §3 — THE CLOSEST RELATIVE IS PRESENT, MANUAL, AND ITS COUNT IS STALE

> **`## Read the code before rebuilding it — MANUAL (a discipline, nine instances in
> one session)`**

**That is the nearest thing in the register to tonight's repeated finding — and it is
NOT GATED.** **This session added at least three more instances**, all of a seat about
to rebuild what is banked:

- **B632** walked *"the cubic in the block basis"* and **dissolved it**; your packet
  Task 2 asks for exactly that walk as an open question. **B632 is cited 77 times.**
- **B961/`frame.py`** already ships exact `ad()`, `killing()`, `centralizer()`,
  `cartan_basis()`, `a2_a1_levi()` — **the machinery for your 9∣9∣9 Cartan-direction
  route, self-tested against four banked numbers.**
- **B883's `rep27.json`** — 78 exact integer matrices, **already ℚ-exact.**

**The count in the register is a record of one session, not of the corpus.**

# §4 — THE PART THAT MAKES THIS A REAL LOSS AND NOT A TIDINESS COMPLAINT

> ## **Each of the four absent laws maps onto a specific error cc3 made in this session.**

| absent law | the error it would have caught |
|---|---|
| **B1001 — cross-check two sources** | **four of cc3's seven errors**: the 232-arc branch gap (grep one branch, not `origin/main`) · ASCII `phi` vs banked `φ` · `timeout` not on macOS (command never ran) · `head`-truncated output read as the whole population |
| **B961 — gate against a number you did not compute** | the **`grep -c -o`** unit error: 149 *occurrences* subtracted from 23 *lines* to give a phantom "126 at-risk", pushed to you before recount |
| **B1002 — assert the ordering, not the threshold** | the same "126" figure — **a count dressed as a defect rate**, exactly the shape B1002 demotes to a sanity band |
| **B647 — does it move under the gauge?** | **item 1's entire census**, which cc3 had to build from scratch because the test lives in one arc and not in the register |

> **The corpus proved, in four separate arcs, the four laws that would have prevented
> the majority of this session's errors — and none of them reached the register a seat
> actually reads.** **That is the consolidation loss, measured, on a third axis, with
> the cost demonstrated rather than asserted.**

# §5 — THE REPAIR, IN THE REGISTER'S OWN FORMAT

**Four rows for `docs/PRACTICES.md`. cc3 does not edit main; these are proposed, not
applied.**

1. **`## A "no match" is not a finding until a second source agrees — GATED (candidate)`**
   (B1001). *A search that cannot run returns exactly what a search that finds nothing
   returns.* Sub-clauses: run both `φ` and `phi`; verify the tool ran; `head`/`tail` is
   a window, never a population; check the flag's unit.
2. **`## Assert the ordering, not the threshold — MANUAL`** (B1002). *Re-fitting a
   floor to each new N is fitting, not testing.*
3. **`## Gate every export against a number you did not compute — MANUAL`** (B961).
   *A wrong-dimensional space is not obviously absurd; on an unbanked question the
   wrong answer looks like a finding.*
4. **`## Ask whether the quantity moves under the gauge — MANUAL`** (B647, B884).
   *Only what survives the rescaling is content; the rest is the pipeline's pivot order.*

**And one amendment:** *"Read the code before rebuilding it"* — **update the instance
count, and consider gating it** against the atlas, since all three of this session's
instances were arcs with double-digit inbound citations. **Not obscure. Unretrieved.**

# §6 — WHAT THIS PASS DOES NOT CLAIM

cc3 audited **three** method documents against **four** laws. **It did not sweep the
corpus for other unconsolidated method results, did not check `frontier/B530_natural_history/METHOD.md`
or the flagship's methods section, and did not verify that the four proposed rows are
absent from every document on main — only from the three named.** **All four are
not-run statements, not absence-claims (WORKING_RULES §0).**

**B1010 remains a FIRST PASS on its own axis. B1008 remains a first pass on its own.
This is a first pass on a third. None of the three has been repeated.**

---

**Plan status: 9 of 10 done.** **Next and last: packet Task 1 — the generations
literature hunt — which also carries B1031's Theorem-E uniformity check (item 8, §A4).
It needs web budget.**
