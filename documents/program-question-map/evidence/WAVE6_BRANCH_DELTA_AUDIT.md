# Wave-6 branch delta audit

Date: 2026-08-29.

Source locks used for the final reconciliation:

- main: `4526eb49214cdc3c038191870421f32d6819b79d` (through B1209);
- outside-bench: `287e8f75efa34141dfb00ff2c30215cd50036ee5`;
- paper: `a31456d2d5e4f08723aa9cfabb7a8573cc2c13d4`;
- Golden hostile record: `6fc86147e553773335b665d6d460e1eaa77aaaf0`;
- Codex exact cells: R020 `2331e189`, R021 `e9b76a12`, R022 `0b5429be`.

## R020--R022: exact local dispositions

### OA-C1067 — REFUTED by R020

The first R020 draft was rejected before commit. It reversed B1140's two
orthogonal A2 components, chose a noncompact arbitrary swapper and omitted the
particular solution in its GF(2) Chevalley-lift equations. That map squared to
one but failed 864 of 6084 ordered basis-bracket checks.

The committed certificate fixes all three defects in the stored compact
E6(-26) representative (`swapper#13`, `lift#0`). It verifies every Chevalley
bracket, signatures `(26,52,0)` and `(0,8,0)`, Killing orthogonality, both
five-strings and the ambient square on all 78 basis vectors. The principal
semilinear beat sends the selected 64 outside itself; both 5s and the colored 54
leak separately. Thus no restricted action exists in this frame. Covariance
over all 24 compact hits and any physical-spin claim remain outside scope.

### OA-C1069 — PROVED narrowly by R021

For `N=m000` and its orientation cover `M=m004`, R021 obtains
`H1(N;Z)=Z<t>` with `a=b=2t`, `H1(M;Z)=Z<a=b>` and `H^2(N;F2)=0`.
The Pin-minus obstruction vanishes. Both structure torsors have two elements,
while `p*:H^1(N;F2)->H^1(M;F2)` is zero; restriction is constant onto one
unnamed spin structure. OA-C1140 keeps the unpaid tangent-frame comparison with
B1141's named holonomy lift.

### OA-C1133 — REFUTED; its corrected children split

R022 proves all unlabelled free four-point V4 actions regular and equivalent,
and exactly separates the displayed `Q(zeta_12)` and
`Q(sqrt(-3),sqrt(5))` field annotations by discriminants/ramification
`144/{2,3}` versus `225/{3,5}`. B1175 then freezes named-label preservation as
the charter's intended category and author-adjudicates the original three-way
equivalence false. OA-C1139 is therefore PROVED, not conditional. The useful
rescope drops being-by-hearing; B1182 proves the unique surviving C4-prime
isomorphism `(c,r,theta)->(k11,k7,k5)`, recorded as OA-C1147.

## Main B1172--B1196: exact status changes

- B1182 closes OA-C1131 at the algebraic/program-arrow scope: `r=k7` is a
  finite-place form-class reversal, distinct from archimedean orientation `c`.
  It is not a thermodynamic or causal arrow.
- B1183 closes OA-C1141: the QP-4 sign torsor and orientation torsor are one
  nontrivial class under global `c`; physical closing acts are not thereby
  identified.
- B1184 refutes OA-C1142's conjunction. A mirror-even self-name fixed point
  exists, but an object-native setter for the mirror-odd bit cannot exist.
- B1185 closes OA-C1144 with answer GENUINELY THREE: rank/support,
  selectivity and family-index invariants separate the heterotic up zero,
  object E6 support and E8 rank-two kernel. Its missing characteristic-zero
  down evaluator is preserved separately as OA-C1148.
- B1186 reconstructs a bounded 112-member Paper-IV family. The exact witness
  `o10_150700` with `H1=Z` refutes OA-C1103. Its two one-cusped equal-shape
  candidates were checked with a float/tolerance comparison, not an exact
  peripheral certificate, so hostile grading keeps OA-C1134 OPEN.
- OA-C1074, OA-C1115, OA-C1117, OA-C1118, OA-C1132, OA-C1140 and OA-C1145
  receive no qualifying proof through B1196 and remain OPEN.
- OA-C1143 remains an EXTERNAL_BLOCKER: the phason knob exists, but no
  commissioned 5-versus-6 edge-mode count or uncertainty exists.

## Questions exposed by the grand-computation reconciliation

B1193 names six merged-core computations. They are now rows rather than prose:

1. OA-C1149 — place or delete the continuous Powers weight `lambda`;
2. OA-C1150 — adjudicate the surviving projective Higgs-line input;
3. OA-C1151 — construct L154's genuine E6 boundary character or kill the
   sigma-deletion route;
4. OA-C1159 — force or refute the hierarchy-carrying D2 twist;
5. OA-C1153 — execute the typed `(Vol,CS)` clock-coherence comparison;
6. OA-C1154 — identify or separate the outside branch Z2 and `r=k7`.

B1194 adds three mathematical residues and one previously blind domain:

- OA-C1152 — the exhibited relational c-bit's general norm classification;
- OA-C1155 — the D2 generation-three carrier adjudication;
- OA-C1156 — the Kolmogorov Selector seal, beyond merely selecting an invariant
  prior;
- OA-C1157 — one honest cosmology specification ledger.

Seam A is not promoted to a physics door. OA-C1109 already records it as an
operational wall: even a full arithmetic-Chern-Simons comparison would not
select a heterotic realization, a 4d theory or an SM value.

## Main B1195--B1196: hostile reconciliation, not headline inheritance

The five new repository tests pass from an immutable archive (`5 passed`), but
they only inspect stored JSON, strings and file presence. They rerun none of the
verdict-bearing mathematics. The cell evidence and its hostile returns therefore
control the dispositions:

- GC-21 reproduces the founding saddle polynomial, Rogers identity, Hessians and
  the CS=0 lattice controls. Its own two hostile returns mark `survives=false`:
  the universal stationary-phase obstruction and quoted precision multiplier
  exceed the tested ansatz/convergence evidence. Seam A stays sharpened and
  indeterminate, not a physics door.
- GC-22 proves a narrow BTZ/KMS tautology: a free BTZ mass can reparameterize any
  chosen Powers weight, and at `lambda=0.4` it reproduces B723's already defined
  modular period. Its hostile lens catches the claimed `phi^-4` control at about
  `1.98e-19`, not `1.5e-9`, and rejects the inference from one free-mass
  strawman to every object-native placement. OA-C1149 remains OPEN.
- GC-23 excludes the tested raw/direct cusp Gram routes. Its universal
  indecomposability claim fails because interior, linking and
  Poincare-Lefschetz pairings were not classified. OA-C1151 remains OPEN.
- GC-24 survives as PARTIAL: the partner-unit-norm conjecture is false and its
  bounded exact data support `gen_det=-kappa/g^2`; equal `kappa=121` can give
  different outcomes through the saturation index `g`. No symbolic general
  proof, exhaustive partner classification or forced partner exists, so
  OA-C1152 remains OPEN.
- GC-25's `P^3` permanence claim has `survives=false`: the rational regular-
  representation stand-in assumes rather than derives the action on the actual
  four-dimensional multiplicity space. OA-C1150 remains OPEN.
- GC-26 survives and closes one question negatively. Every B891 foreign
  eigenspace has dimension 16 and spreads over all three nine-dimensional
  trinification blocks. It cannot be one block or a union of the `9+9+9`
  partition. OA-C1155 is REFUTED; this does not create three chiral generations.
- GC-27 is explicitly PARTIAL. The exhibited relational epsilon is a
  conjugation-invariant pair-class name, not a selected torsor point; no uniform
  Kolmogorov theorem exists, and lambda has no typed `(T,G)` pair. OA-C1156
  remains OPEN.
- GC-28 lands a useful eight-row cosmology inventory. It records, rather than
  repairs, the missing 4d state/action, dynamics, scale/time calibration,
  inflation, stable dark matter, baryogenesis rate, CMB spectrum and structure
  formation. OA-C1157 remains OPEN.

Two governance mismatches are material. B1195 promotes four cells whose own
hostile returns say `survives=false`, and calls them laws while its verdict says
`creates_law:false`. B1196's aggregate test accepts GC-27 although its actual
grade is PARTIAL. Thus “fully dispositioned” is a bookkeeping headline, not a
mathematical closure.

## Outside-accounting conflict caught

The latest outside memo places `lambda` at `Q(sqrt(5))` and builds a
`(gamma5,lambda)` golden-end pair. Main B1192's correctness lens rejects that
exact step: the banked `lambda` is a free Powers weight and no source map ties
it to the cat-map field. B1193 retains placement as open. The outside memo's
quadratic-unit arithmetic may stand independently, but that placement and all
conclusions using it are not integrated into this map. The same comparison
finds four further dispositions: OA-C1158 keeps B919's genuinely open 40639
trace leg; OA-C1160 records the specialist S1 curve comparison, now classified
as blocked on a construction absent from the corpus; OA-C1161 records B729's
already-refuted object-native Born overlays; and OA-C1162 closes the supposedly
missing discriminant-6237 class-group step by B1093.

## Outside tip `bc9d381d`: three claimed cells under hostile rerun

All three scripts exit zero from their immutable branch content. That fact is
not sufficient: the hostile rerun below separates exact subresults from two
semantic/convention failures in the claims printed by those scripts.

- OA-C1163 is PROVED narrowly after splitting the proposition:
  `class_group_953.py` exhibits `alpha=-26-theta+2theta^2` with exact norm 953
  for `theta^3-12theta+5`; `theta -> -theta` aligns the sign convention with
  B1093. The factorization modulo 953 has one degree-one and one degree-two
  prime, so the norm exhibit generates the selected degree-one prime. It does
  not select a unit associate or explain the element's hierarchy role;
  OA-C1166 keeps that gap.
- The same certificate's advertised independent class-number proof is
  rejected. At p=5 its relation row records the norm valuation
  `v_P+2v_Q` instead of the ideal-class coefficient `v_P-v_Q`. Its Smith
  quotient is therefore not certified. OA-C1162 remains PROVED solely by the
  independent B1093 Minkowski calculation.
- OA-C1164 REFUTES the advertised certificate-level bridge. The script uses
  lower-left entry `-omega` for its `b` matrix, whereas the banked m004 Riley
  realization uses `+omega`. Its matrices send the banked relator
  `abABaBAbaB` to `[[-1,0],[-4,-1]]`, not the identity, and the script never
  checks the presentation. With the corrected sign, `baBAABab` is not the
  banked longitude; the established word is `bABaaBAb`. The discriminant-48
  cusp fact may stand elsewhere, but this claimed grammar derivation does not.
- OA-C1165 is PROVED locally: at the trivial fixed point, the exact rational
  reverser and nontrivial `Q(sqrt(5))` Galois automorphism induce the same swap
  of the two projective golden eigenlines. They are not literally the same
  linear/semilinear map, the short control list is not an exhaustive census,
  and the result supplies neither physical time nor OA-C1154's branch-to-`r`
  level-crossing.

The same outside tip sharpens three earlier rows without closing their parent
physics gates. The proposed conditional closure of OA-C1159 is DECLINED. B928
uniquely carries the already specified H-plus-D2 form, but explicitly says its
census does not pin D2; the other symmetric Hermitian forms were not tested for
generation resolution. The named premise therefore does not imply uniqueness.
OA-C1160 becomes EXTERNAL_BLOCKER because no E6 Seiberg--Witten/state-integral
construction exists to instantiate the proposed comparison. The matching
one-flow-datum/one-unit count does not place `lambda`, so OA-C1149 stays OPEN.

## Successor outside delta `bc9d381d..9915068b`

Two new certificates were run independently from their immutable blobs.

- `principal_witnesses.py` repairs the earlier class-number method defect by a
  disjoint elementary route. It lists exact generators for all eight prime
  ideals of norm at most the Minkowski bound and separates the same-norm
  primes with Hensel valuations. R023's standard-library certificate rechecks
  all eight norms and the discriminating valuations. The script itself assumes
  rather than proves `Z[theta]=O_K`; B1093 supplies that maximal-order gate.
  Together they reinforce OA-C1162 and cross-pin OA-C1163. They do not select a
  unit associate or rerun the B918/B931 divisor pipeline, so OA-C1166 remains
  OPEN.
- `e6_boundary.py` constructs the generic level-one E6 lattice source: it is
  free rank three over an Eisenstein order, has discriminant group F3,
  primaries with ground multiplicities `{1,27,27}`, vacuum coefficients
  `1,78,729`, and 27-sector coefficients `27,378`. The exact script reproduces.
  It gives no normalized map to a record-side q-series and no sigma
  identification. Its prose tie through memo 100 is not a bridge because
  OA-C1164 refutes memo 100's Riley realization. OA-C1151 therefore remains
  OPEN, now sharpened from “build the source” to “build the character map.”

`THE_CLOSURE_ROUTES.md` is useful ownership/trigger metadata, not proof that
the programme has only its listed rows. It also carries forward the refuted
memo-100 bridge and the declined D2 selection assembly. The canonical typed
map, not that route count, remains the exhaustion ledger.

## Successor outside delta `9915068b..f6b96658`

The new trace/fence certificates were independently rerun from immutable branch
content. `relational_kappa.py`, `fence_independence.py`, `bit_ledger.py` and
`first_beat_law.py` all reproduce their stored exact outputs. The sound scope is
narrower than several synthesis headlines:

- the founding quadratic, Coxeter-four polynomial and determinant-minus-one
  crown-pair realizer coincide exactly for the exhibited pair;
- the fixed Riley representation's trace algebra is generated by
  `(tr(a),tr(b),tr(ab))`, so every free-word trace is a polynomial in those
  generators; this does not cover word labels, eigenlines or non-trace data;
- the finite word sweep and generator intertwiner support a model-pair
  non-definability witness, but do not supply a complete semantic proof system;
- the scripts call `abaBAbabAB` a presentation relator because it maps to
  `+/-I` in one representation. They do not prove membership in the normal
  closure of the banked relator `abABaBAbaB`; this is a representation-kernel
  word, not an independently pinned presentation relator.

The constructive amphichiral word map is a useful exact typing result:
`a -> a`, `b -> bAB`, with its square inner in the declared realization. It
does not identify the dynamical branch bit with `r=k7`. The quine/intertwiner
solve leaves one coordinate bit only in its declared matrix model; “one bit
buys occupation” is an owner interpretation, not a theorem. The occupant-type
examples refute a naive field-norm rule but do not classify every observer.
The frozen tier-one menu (`W1=11720`) is a preregistered comparison instrument,
not a prediction or parameter-selection theorem. The awareness verdict is
therefore a properly firewalled synthesis, not a closure of dynamics,
consciousness or Standard-Model physics.

No canonical row changes status from this outside delta. OA-C1152, OA-C1154 and
OA-C1156 stay OPEN; the exact new facts are attached as scoped evidence.

## Main delta `40916c36..31b49156`: clock split and primary literature check

B1197's two SnapPy computations were rerun independently from an immutable
archive. They reproduce 156 oriented closings and the sign control `156/156`,
78 deduplicated closings, a monotone 29-rung `(1,n)` ladder, 15 violations of
global `|CS|` monotonicity and 36 shuffled-control violations. Every fixed-`p`
family from `p=2` through `p=8` fails. This yields a clean split:

- OA-C1167 is REFUTED: reduced orientation-blind `|CS|` is not a globally
  monotone reparameterization of volume over the full B289 census.
- OA-C1153 remains OPEN. B1197 chooses absolute mod-one CS, does not construct a
  signed orientation-fixed or saddle-clock map, and leaves the physical domain
  between one selected ladder and the whole census unselected. The ladder pass
  is therefore a conditional subtrajectory observation, not a clock theorem.

B1198 correctly grades itself OPEN, but its literature half was rechecked here
against Dong Uk Lee's primary PDF rather than the repository's automated
summary. The paper does construct a mixed Tate motive over the invariant trace
field whose regulator gives normalized complex Chern--Simons, proves a
path-torsor Hodge statement conditional on Conjecture 7.4.2 and verifies that
conjecture at all four figure-eight ideal points in Appendix A. B1201 corrects
the first repository reading: because `|a1|=1` at these points, the admissible
tangent at each selected ideal point is unique. The actual twofold datum is the
augmented-character lift `lambda` versus `lambda^-1`; matching that lift to
programme conjugation `c` is only a hypothesis. The ideal point/local
parameter, augmented lift and Bloch representative are not jointly selected;
the motivic path torsor remains hypothetical in the required generality. No
Dedekind-zeta normalization or finite Kim/Artin-Verdier order-six phase appears.
OA-C1109 remains an EXTERNAL_BLOCKER: the literature confirms the regulator
side, not the canonical finite-phase-to-volume map.

B1198's numerical half reproduces with `mpmath`. Its shell script invokes the
environment's unqualified `python3`; here that resolves to a Python without
`mpmath`, so the script fails unless the declared dependency environment is put
on `PATH`. The new pytest locks do not execute it. This is a portability debt,
not a refutation of the separately reproduced L-value identities.

## Outside delta `f6b96658..c82fff25`: pattern supply is not sufficiency

`pattern_ladder.py` reproduces byte-for-byte: 13,120 reduced words through
length eight, 180 trace values, maximum trace degeneracy 376 and candidate-pair
fractions `0.7481, 0.9031, 0.9355` through depths three, four and five. The
scope corrections are load-bearing:

- the enumerated objects are freely reduced words, not group-conjugacy classes;
- G2 uses unordered `combinations` despite calling the pairs ordered;
- “unlike” is implemented only as unequal trace, and the noncommuting/unequal-
  trace predicates are broad necessary-pattern tests;
- exponential word supply and trace degeneracy do not construct memory,
  feedback, dynamics, measurement or an occupant token.

No earlier row closes. The certificate instead exposes OA-C1168: prove or
refute that the declared type-eligible relation is sufficient for an
operational occupant. The outside memo correctly fences phenomenal awareness;
the canonical map additionally fences its necessary-to-sufficient jump.

## Paper tip `01d2d0cb`: final delta and record defects

The paper branch settles the Paper-I `m=12` count at three proper classes,
retracts Paper-IV's separator claims, corrects the bounded family to 112 and
narrows the Paper-II mod-p residue exposure to `{16,20,26,28,36}` without
closing it. Its B8142 Alexander control reproduces 9/9. These changes agree
with the map's existing dispositions and add no new closure row.

Four paper-record defects remain visible and are not silently repaired here:
`B8152_family_definition/arc_verdict.json` still says 111; its `results.json`
retains legacy 111 strings; Paper IV opens with the historical 14-member
definition while later retracting it in favor of 112; and
`docs/views/REVIEWER.md` contains trailing whitespace on two lines.

B1194 is used here as an inventory pointer, not as automatic proof of every
claim inside its six audit lenses. Its own archived hostile returns flag a
wrong Big-Bang grep count and one-sided controls in older time-gap locks. Those
defects do not create a cosmology; they strengthen the need for OA-C1157.

## Main delta `31b49156..4526eb49`: B1199--B1209

The final main refresh adds ten arcs but closes no canonical OA row.

- B1199 confirms the stale-register diagnosis and completes the finite
  selection-cochain read. R023 independently reloads the banked 745 primitive
  classes, obtains the same unique unit-prime/trace-three selector `LR`,
  reconstructs `SL(2,F5)` of order 120 with conjugacy-class sizes
  `1,1,12,12,12,12,20,20,30`, and verifies pointwise constancy on all nine
  shadow classes. This is a complete finite class readout, not a selected
  object or basepoint. B1199 also correctly rejects the proposed additive-
  period/multiplicative-ratio clock reconciliation as a type error.
- B1200's exact `Phi_3` algebra is sound at a chosen shape, but its “fourth
  face” needs both conjugate shapes; one fixed `t` supplies only its own square.
  B1201 then corrects the Lee marking claim as recorded above. Its aggregate
  “clean” language overstates one rejected lens and its kappa cross-link is a
  generic Fricke identity on unrelated pairs, not a new bridge.
- B1202's retrieval checker is not yet reliable for the motivating nested-law
  case: nested hits are tagged outside the settled set and its implemented hit
  threshold differs from the documented threshold.
- B1203's c-filter cuts none of the declared menu because every atom is real.
  The climb's kappa invariance is already the classical Fricke mechanism. Its
  `W1=11720` number is hard-coded rather than enumerated, and one advertised
  negative control uses the wrong Fricke triple.
- B1204 classifies two forcing patterns found in the current record; it does
  not prove those are the only possible families.
- B1205/B1206 show that a generic 3-by-3 matrix linear in four Higgs
  coordinates has a determinant cubic and remains one condition short. They do
  not evaluate the object's tensor. A valid skew-symmetric-slice control makes
  the determinant identically zero, so actual tensor evaluation and basis
  canonicity remain load-bearing.
- B1207 records the confirming slow-lane run and governance repairs. Its
  lightweight reproduction checks postconditions rather than rerunning the
  four-hour suite; the timing-drift issue remains explicitly unfixed.
- B1208 independently verifies several scoped external results and correctly
  repairs the lambda-rank derivation: Cartan support alone admits rank one,
  while full SU(2) invariance leaves only the epsilon form and ranks zero or
  two. It also verifies the one-27 H_d census but leaves the generation-level
  lepton character/evaluator as a three-outcome fork. Thus OA-C1148 and
  OA-C1150 remain OPEN.
- R024 executes the datum available in the committed height-308 frames. On both
  retained Wilson branches, `e^c` selects `A_0` and `l/H_d` selects `B_0`, so
  both coarse characters are zero. The tail rule has abstract admissible pairs
  `(0,8)`, `(2,6)`, `(4,4)`, but no committed map identifies a physical lepton
  generation representative with one of them. Coarse equality therefore proves
  none of B1208's same-tensor, independent-tensor or absent-operator outcomes.
- B1208's clock verification strengthens the global negative: signed CS is
  opposite at equal volume on 156/156 mirror pairs and absolute CS has a
  541-fold spread-to-window witness. The physical trajectory premise remains
  unselected, so OA-C1153 remains OPEN.
- B1208's dark census exactly excludes the tested affine Cartan characters
  modulo two through six from leaving at most two charged weights, with a
  planted positive control. It closes that narrow ledger route, not every
  abelian representation, nonabelian symmetry, kinematic stability or
  OA-C1157.
- B1209 source-checks Lee and independently reconstructs the four Newton-polygon
  edge vectors. Every edge is L-thin, hence `|a1|=1` and the admissible tangent
  torsor is trivial at every figure-eight ideal point. This definitively kills
  the proposed tangent/orientation-bit identification. The mixed-Tate motive
  over the invariant trace field and its complex-volume regulator remain the
  positive arithmetic meeting; neither a finite order-six phase map nor a
  heterotic/4d selector follows. Its exact certificate reproduces and its five
  locks pass. Two scope fences remain: the script does not independently check
  edge initial-polynomial roots or ramification before identifying every edge
  with an ideal point, and Lee's third appendix case contains a branch-dependent
  logarithmic term rather than a raw limit uniformly equal to plus-or-minus
  `2*pi^2/3`. Neither caveat revives the tangent/orientation route because Lee's
  own four `a1` values already settle it.

The new mainline verification scripts pass under the repository's pyenv
environment (`24 passed` for the B1200--B1207 locks plus the B1208 reproduction
lane). Several reproduce wrappers silently fail under the system Python because
they suppress dependency errors. String/presence locks are therefore not
substituted for the mathematics above.

## Outside delta `c82fff25..287e8f75`: scoped negatives and portability debt

Fifteen of seventeen new certificate/output pairs reproduce byte-identically
when their required external source ref and dependencies are supplied. The
important exact results narrow questions without closing their parents:

- eight observer predicates yield six distinct admissible sets; A5 has five
  competitors, and the proposed phi-stability gate is unforced and false on
  A5. OA-C1156 and OA-C1168 remain OPEN;
- the tested affine characters modulo two through six isolate no two-state
  neutral sector; this is not a theorem about every abelian, nonabelian or
  kinematic dark-matter stabilizer;
- one-27 lambda/H_d calculations fix invariant coefficient shapes but do not
  evaluate the generation-level down/lepton maps;
- signed and absolute global clock readings fail, while the chosen ladder's
  physical-history interpretation is an explicit SCOPE-1b premise. Later
  records mark LEAP-1 provisional because owner adoption is unconfirmed;
- the exact yin arithmetic does not revive OA-C1164's refuted grammar/Riley
  bridge, and the exact layer map does not supply physical dynamics.

Four contract failures matter for banking: `anatomy_reconcile.py` scans a
floating `origin/main` and its stored counts are stale; `leap1_propagation.py`
self-scans newly added documents and contradicts the later provisional status;
`d2_scope.py` expects a main-branch JSON absent from a clean outside checkout;
and `duality_index.py` requires an external Git ref. Their mathematical cores
may be rerun in a prepared environment, but these are not self-contained,
byte-stable certificates as filed.

## Paper delta `01d2d0cb..a31456d2`: exact B8154, stale family verifier

B8154's symbolic certificate passes 19/19 and its tests pass 6/6. It proves on
the chosen representation that `t^2` is the primitive cube root, `u -> u^2`
is conjugation/root swap and the tested relator survives; the prime-order
subgroup argument closes only the shrink-the-group escape. It selects no root
and independently establishes neither discreteness/faithfulness nor peripheral
or mirror geometry.

The Paper-IV 112-member correction still lacks a committed executable census.
`check_family.py` and `extended_census.py` remain hard-coded to the withdrawn
14-member set and reproduce the withdrawn separator claims. B8152 JSON mixes
111 and 112, one paper sentence still says “all fourteen,” and generated
verdict views retain stale counts. The 112 prose result may be correct, but its
current branch-local verifier is not the proof.

## Resulting registry

The canonical registry has 185 rows: 70 PROVED, 55 REFUTED, 15 CONDITIONAL,
22 EXTERNAL_BLOCKER, 2 EMPIRICAL and 21 OPEN. The expanded list is progress in
accounting, not progress by denominator inflation. The current record still
does not derive a unique 4d local QFT, net chiral index three, selected stable
vacuum, complete light spectrum, physical dynamics or normalized Standard-Model
parameters from the minimal-description seed.

The exact norm, p=5 defect, Riley-relator failure and golden-reverser identities
are independently reproduced by the branch-local standard-library certificate
`certificates/r023_wave6_outside_hostile.py`. The B299 triality computation and
B891 multiplicity computation are rerun by
`certificates/r023_b1196_generation_obstruction.py`, which independently checks
that dimension 16 is not the dimension of any union of three nine-dimensional
trinification blocks. `certificates/r023_b1197_clock_coherence.py` independently
reruns the full closing census and its monotonicity controls.
`certificates/r023_b1199_selection_recheck.py` independently reconstructs the
745-class selector table and all nine finite shadow classes.
`certificates/r024_lepton_character_datum.py` independently rebuilds the two
retained coarse lepton sectors and fences the unresolved frame-level fork.
