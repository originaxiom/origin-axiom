# Progress bank — Campaign II, Wave 3 checkpoint

**Date:** 2026-08-26

**Main source:** `9d6979db424c0b878c62541a3f21e0a2ca39f274`

**Outside-bench source:** `0fcdb66cd57edeb13c8703b7f05717fcc2609893`

**Status:** active campaign; not a parameter-free closure claim

## Outcome first

The canonical map now contains 108 typed questions:

```text
37 PROVED
36 REFUTED
13 CONDITIONAL
16 EXTERNAL_BLOCKER
 1 EMPIRICAL
 5 OPEN
```

The validator passes all integrity checks.  This wave converts six of Wave 2's proposed
computations into exact theorems, closes two preregistered routes negatively, registers the
B1146 frame correction, and hostile-audits outside memos 41-53 without promoting their
representation language into physics.

## What changed mathematically

1. **The 2T center is visible on the selected minimal A1.**  The selected minimal-A1 restrictions
   contain 12 odd states on the 27 and 40 on the adjoint.  Principal-A1 center blindness was a
   frame-specific fact, not a general obstruction.  This corrects the evidence for OA-C0006 but
   still does not select an A1.
2. **The E6 invariant census is exact.**  Symmetric invariant multiplicities on the selected 27
   in degrees 1-4 are `0,0,1,0`; the full ordered tensor cube has one E6 invariant.  Under the
   selected trinification subgroup the ordered/symmetric counts are `9/4`.
3. **Omega-one parity is redundant on integral characteristics.**  Even weighted-Dynkin labels
   force even coroot coordinates modulo two.  The integrality hypothesis is essential; the
   inherited 20-row orbit census remains conditionally complete.
4. **The finite Habiro tests are now honestly split.**  Raw consecutive p-power evaluations have
   valuation exactly two in six cells; Taylor-at-one truncations meet `v_pi>=N` in all 24 frozen
   cells.  These are exact finite tables, not a universal p-adic theorem.
5. **The preregistered Kashaev recognition failed.**  The run through `N<=4000` falls far short of
   its 60-digit gate.  OA-C1076 is refuted for that protocol.
6. **The peripheral character map is not generically degree one.**  Its unoriented data are
   generically two-to-one; oriented `L` versus `L^-1` can retain the missing bit.

## Carrier/Yukawa audit

The chosen `C^2 tensor 27` carrier, its lock/clock identities, the invariant tensor
`epsilon tensor C`, and the selected E8 family-channel factorization all reproduce exactly as
finite-dimensional representation theory.

Three advertised upgrades fail:

- the displayed conjugate pair is not the full trace-map fixed locus; `(0,0,0)` is another genuine
  fixed character;
- the 54-dimensional carrier is not unique-minimal or canonically selected;
- the semilinear beat is not an involutive antiunitary real structure on a positive Hilbert space.

No compactification, zero-mode intertwiner, BCDD cup product, Calabi-Yau trace, matter metric,
Dirac index or normalized Yukawa value occurs in those certificates.  The exact same-monad
one-`H_u` up-Yukawa rank-zero theorem therefore remains untouched.

## Record defect

B1147's immutable clean lock requires a missing `verification/reproduce.log`.  A clean archive
gives one failure and four passes.  Independent reruns support the memo results, so this is a
self-containment defect (OA-C1091), not a mathematical retraction.

## Five live computations

| row | remaining exact test |
|---|---|
| OA-C1067 | Apply the semilinear Sigma to the complete banked 64 basis and compute its square. |
| OA-C1069 | Construct the Pin-minus obstruction/torsor and affine restriction, beyond the zero H1 linear map. |
| OA-C1074 | Derive the exact zero-counting formula with an explicit argument-principle error bound. |
| OA-C1077 | Complete and audit the preregistered large-T GUE run. |
| OA-C1083 | Reduce `tr(ab^-1)-gal(kappa)` in the full selected character-component coordinate ring. |

The next campaign wave should execute these five in cost order while the flavor critical path
continues with the missing normalized cyclic/Serre chain map for the 36-entry down block.  None of
that relaxes the strict endpoint gates: a physical 4d functor, selected chiral spectrum, vacuum,
dynamics and normalized values remain required.

## Reproduction

```text
PYTHONDONTWRITEBYTECODE=1 python3 certificates/oa_c1070_omega1_parity.py

PYTHONDONTWRITEBYTECODE=1 python3 \
  certificates/r006_e6_invariants/tensor_invariant_counts.py

PYTHONDONTWRITEBYTECODE=1 python3 documents/program-question-map/validate.py
```
