# Progress bank — Campaign II, Wave 6 checkpoint

Date: 2026-08-29.

Source locks: main `4526eb49214cdc3c038191870421f32d6819b79d`;
outside-bench `287e8f75efa34141dfb00ff2c30215cd50036ee5`; paper
`a31456d2d5e4f08723aa9cfabb7a8573cc2c13d4`; Golden hostile record
`6fc86147e553773335b665d6d460e1eaa77aaaf0`.

Status: active hostile closure campaign. No parameter-free Standard Model or TOE
closure is claimed.

## Canonical state

```text
70 PROVED
55 REFUTED
15 CONDITIONAL
22 EXTERNAL_BLOCKER
 2 EMPIRICAL
21 OPEN
```

The map grows when an audit exposes a previously hidden obligation; that is not
regression. The physical endpoint remains unclosed. In particular, the record
does not yet derive a unique 4d local QFT, net chiral index three, one selected
stable vacuum, a complete light spectrum, physical dynamics or normalized
Standard-Model parameters from the declared minimal-description seed.

## Chain status, without correspondence inflation

1. Bare minimal description does not select a seed independently of coding.
   The Fibonacci rule is conditional on a declared restricted category/cost.
2. The move from the substitution to the oriented punctured-torus carrier is
   conditional on carrier, puncture and orientation data.
3. Given m004, the arithmetic entrance `m004 -> SL(2,F3)=2T -> E6` is proved.
4. E6 supplies a rich, exact representation skeleton and several selected-frame
   SM-shaped decompositions, anomaly ratios and cubic supports.
5. The jump from that skeleton to a unique compact 4d quantum field theory is
   refuted as a functor-selection claim. The heterotic construction is one
   conditional witness, not a derived realization.
6. Three physical generations, a stable rank-reducing vacuum, complete light
   content, nonzero evaluated Yukawas and observed parameter values remain
   external, conditional or open. The value searches performed so far are
   reproducible negatives, not successful predictions.

That is the honest programme state: a substantial structure theorem and a web
of exact correspondences, not yet a parameter-free Standard Model derivation.

## New exact facts in this wave

- The principal semilinear beat does not descend to B1140's selected 64; both
  five-strings and the colored 54 leak, while the ambient square identity holds.
- Gieseking has two Pin-minus structures whose restrictions to m004 coincide in
  one unnamed spin structure. Naming the image remains OA-C1140.
- The original named three-way V4 equivalence is refuted. The corrected
  branch/measurement C4-prime pair has the unique isomorphism
  `(c,r,theta)->(k11,k7,k5)`.
- The programme's algebraic arrow is the finite-place reversal `r=k7`, not the
  archimedean orientation bit; this says nothing about thermodynamic time.
- QP-4 and orientation carry one nontrivial c-torsor obstruction class. A
  mirror-even self-name can exist, but no object-native map can set the odd bit.
- The heterotic up zero, object E6 up support and E8 rank-two family kernel are
  three pairwise distinct mechanisms. The missing down evaluator is separate.
- The corrected Paper-IV family has 112 members at the stated bounds;
  `H1=Z` is not an m004 separator. Two cusp-shape witnesses make the stronger
  cusp claim nearly refuted, pending one exact peripheral certificate.
- The supposed missing discriminant-6237 class-group computation was already
  closed by B1093, and the outside tip now constructs an explicit generator
  `-26-theta+2theta^2` of norm 953. Its unit normalization and pipeline-free
  explanatory role remain open.
- The successor outside tip repairs its flawed relation-matrix class-number
  proof with a disjoint list of exact generators for every prime ideal below
  the Minkowski bound. R023 independently rechecks all eight norms and the
  discriminating valuations. This reinforces OA-C1162; it does not close the
  unit-normalization residue OA-C1166.
- The exact level-one E6 lattice construction produces an Eisenstein rank-three
  lattice, discriminant group F3, primaries `{1,27,27bar}` and character
  openings `1,78,729` / `27,378`. This is a candidate boundary object, not the
  missing map to a record-side q-series or a derivation of sigma.
- The outside grammar-to-discriminant-48 closure fails its first representation
  gate: the chosen Riley sign does not satisfy the banked m004 relator. The
  separate exact reverser survives only as a local projective golden-eigenline
  exchange, not a branch-selector theorem.
- The proposed D2-tw selection theorem does not follow from the premise that
  generations must be resolved: B928 uniquely carries an already specified
  H-plus-D2 form but does not test every symmetric census member for generation
  resolution. The gauge-selection row remains open.
- The E6 Seiberg--Witten/A-polynomial comparison is blocked on a construction
  absent from the corpus and current literature lane; it is not an unrun local
  script.
- The outside placement of the Powers weight `lambda` in `Q(sqrt(5))` is not a
  result: B1192's hostile lens rejects it. Placement remains open.
- B1195's advertised lambda placement, universal cusp obstruction and permanent
  Higgs `P^3` floor are each rejected by that batch's own hostile returns. The
  exact BTZ tautology, tested cusp negatives and representation stand-in survive
  only in their narrower scopes.
- B1196 decisively separates B891's three 16-dimensional foreign eigenspaces
  from the trinification `9+9+9` partition. This refutes that proposed
  generation carrier; it does not derive three chiral generations.
- The new cosmology ledger is a valuable inventory of what is absent, not a
  four-dimensional cosmological model. Its missing action, dynamics, scales,
  inflation, dark sector and observables remain physics obligations.
- The outside trace batch proves the fixed-representation all-word trace law,
  a constructive amphichiral word map and several finite-model fences. Its
  quine, occupant and awareness readings do not select an observer or any
  Standard-Model value.
- The full B289 closing census refutes global orientation-blind clock coherence:
  volume ordering has 15 `|CS|` rises and every `p>=2` family fails. The selected
  `(1,n)` ladder is monotone, so the broader signed/saddle and trajectory-choice
  parent remains open.
- Lee's motivic complex-volume paper is now read at the primary-source level.
  It verifies its conjectural asymptotic for all four figure-eight ideal points,
  but uses a chosen ideal point/local parameter, leaves motivic canonicity
  caveats and contains no finite order-six phase map. Seam A stays a wall.
- The latest outside pattern count is exact but calls reduced words “classes”
  and unordered combinations “ordered pairs.” More importantly, necessary
  noncommuting/unequal-trace patterns are not sufficient occupants; OA-C1168
  registers that newly exposed bridge.
- R023 independently reproduces B1199's full 745-class finite selector table,
  the unique `LR` unit-prime/trace-three selector and pointwise constancy on all
  nine SL(2,F5) shadow classes. This completes a finite readout, not a unique
  object or basepoint.
- B1201 corrects the Lee marking detail: the admissible tangent is unique at a
  selected figure-eight ideal point, while the augmented lift
  `lambda/lambda^-1`, ideal point and finite-phase comparison remain unselected.
- B1209 confirms `|a1|=1` from Lee's source and independently from the
  A-polynomial's L-thin Newton polygon. The tangent torsor is trivial and cannot
  carry the orientation bit. The mixed-Tate regulator meeting remains positive;
  the finite-phase and physical-realization bridges remain absent.
- B1205--B1208 close several named Higgs-line routes but not the object
  computation. Full SU(2) invariance makes the lambda block rank zero or two;
  the one-27 H_d census has one canonical functional. A generic determinant
  cubic is not the actual height-308 tensor, and the generation-level lepton
  character assignment is now the highest-leverage discriminator.
- R024 pays the coarse part of that discriminator: `e^c` and `l` both select
  character zero on `k=4,8`. R025 then corrects its tail interpretation. The
  quark `A7` rule has B-pair sum 8, whereas the lepton `A11` rule has sum 4;
  the physical lepton pure-tail square is the repeated one-dimensional `B2`
  tail and vanishes. The connecting and mixed entries and frame-level
  representatives remain open, so the three-way B1208 fork and the
  characteristic-zero evaluator remain open.
- R026 constructs the previously missing ordered determinant comparison in
  exact `Q(zeta12)` frames. It proves an exact nonzero six-Euler minor,
  quotient-lift and splitting independence, the equivariant determinant
  phase, and the positive-sign sparse local connecting formula. The 18-entry
  connecting problem is now reduced to the normalized hypersurface trace;
  Serre tails and all physical normalizations remain separate debts.
- R027 constructs the canonical-weight `dP6` top Cech class and a normalized
  cyclic dual cycle, then its signed 384-simplex Eilenberg--Zilber product on
  the actual 36-chart cover of `dP6 x dP6`. This pays the ambient
  `H^4(K_Z)` trace functional. The characteristic-zero refinement collapse,
  the hypersurface connecting representative `delta(ctilde)/f` for each of
  the 18 entries, and the Serre-tail realization remain separate debts.
- R028 pins every toric chart convention needed to feed that trace: the exact
  ray/monomial actions, deterministic chart orbits, common Laurent frames and
  the 18-positive/18-negative orientation census. It proves
  `raw=q_(D,sigma)*t^u` for all relevant monomials. Exact `Q(zeta12)`
  `Phi_308` and Bezout coefficients remain the next payload rather than being
  reconstructed from the good-prime unit-ideal test.
- The outside uniqueness test finds six distinct admissible sets from eight
  predicates and leaves five competitors to A5. Its proposed phi-stability
  repair is unforced and fails on A5. The Selector and observer-sufficiency
  bridge stay open.
- The tested dark affine characters modulo two through six cannot leave only
  two charged weights; the exact minimum is eleven. This closes one
  character-level stabilizer route, not all dark-matter stability or cosmology.
- Paper B8154's root-swap algebra is exact, but the committed Paper-IV family
  scripts still enumerate the withdrawn 14-member set rather than 112. Latest
  outside scripts also contain floating-ref and self-scan portability defects.

## Twenty-one locally active questions

| row | exact obligation |
|---|---|
| OA-C1074 | completed Eisenstein zero-count formula with controlled error |
| OA-C1115 | all-level, all-order normalized Habiro germ transport |
| OA-C1117 | typed heterotic/carrier intertwiner and preservation audit |
| OA-C1118 | object-selected complete spectrum and rank-three abelian arena |
| OA-C1132 | matched m004/m015 arithmetic-versus-dynamics ablation |
| OA-C1134 | exact peripheral certificate for a same-cusp family witness |
| OA-C1140 | identify the constant Gieseking Pin-minus image by name |
| OA-C1145 | one portable full `Q(zeta12)/dP6` construction certificate |
| OA-C1148 | normalized hypersurface trace for the now-framed connecting row, then exact Serre-tail completion |
| OA-C1149 | place or prove external the continuous Powers weight `lambda` |
| OA-C1150 | quotient and price the projective Higgs-line freedom |
| OA-C1151 | construct L154's boundary character or kill sigma deletion |
| OA-C1152 | classify relational-c partners and decide forcing |
| OA-C1153 | select and justify a physical trajectory, then compare the remaining signed/saddle clock with fixed normalization |
| OA-C1154 | compare the outside branch Z2 with `r=k7` |
| OA-C1156 | finish or refute the Kolmogorov Selector |
| OA-C1157 | turn the inventory into a typed 4d state/action, dynamics and observables after the narrow character-level dark route is closed |
| OA-C1158 | finish the independent-prime 40639 weak-trace reconstruction |
| OA-C1159 | force or refute the hierarchy-carrying D2 twist across all admissible Hermitian forms |
| OA-C1166 | canonically unit-normalize the norm-953 generator and derive its pipeline-free role |
| OA-C1168 | prove or refute sufficiency from a type-eligible relation to an operational occupant |

## Revised masterplan

### Phase A — make every load-bearing result portable

Pay OA-C1145 first, then OA-C1134, OA-C1158 and OA-C1166. Repair the paper's
112-family enumerator and pin the outside scripts that currently float on
`origin/main`, external Git refs or their own growing document tree. These are finite,
discriminating certificate debts. They prevent stale or single-homed premises
from entering more expensive physics cells.

### Phase B — close the discrete structural bill

Run OA-C1152, OA-C1154 and OA-C1159, followed by OA-C1118. OA-C1155 is now
closed negatively: B891's 16-dimensional sectors are not the trinification
family blocks. The D2-tw cell must test every admissible symmetric Hermitian form for generation
resolution; mirror-evenness and uniqueness of the carrier of a preselected
form are not a selector. The required output is not an SM-shaped subset: it is
a selected complete light
spectrum, a proved family index, a forced hierarchy carrier and every residual
bit/label explicitly priced. A negative is a valid closure; preloading SM15 is
not.

### Phase C — attack the physical-functor and dynamics gap

Run OA-C1132 as the in-corpus two-sided provenance test. Commission or monitor
OA-C1160 as a separate specialist-construction blocker; do not count it as a
local computation until an E6 Seiberg--Witten/state-integral object actually
exists. In parallel, type OA-C1149, OA-C1151 and OA-C1153 in one
dynamics/gravity ledger. A curve correspondence, static L-value or internal
tick cannot be promoted to a 4d action, dynamics or clock without the missing
maps. B1197 has already killed the full-census orientation-blind branch; the
remaining OA-C1153 task must type the signed/saddle clock and justify a physical
trajectory rather than selecting the one ladder that passed.

For OA-C1109, Lee's path-torsor theorem is now primary-read evidence, not the
missing finite-to-archimedean map. Its chosen tangent and motivic caveats sharpen
the bar; they do not turn the wall into a door.

For OA-C1151, treat the new level-one E6 lattice object as the source object,
not as the bridge: the closing deliverable is still an exact normalized map
from its three characters to a record-side q-series with a negative control.

### Phase D — exhaust the conditional heterotic witness

Freeze every Wilson, bundle, Higgs and carrier matrix before OA-C1117. R024/R025
now fix the coarse and raw lepton characters and close the pure-tail square
negatively. R026 fixes the determinant frame and R027 fixes the normalized
ambient top trace. R028 fixes all chart and line-bundle frame transport. Next
serialize the exact `Q(zeta12)` `Phi_308` payload, construct its
characteristic-zero Bezout refinement collapse and the
`H^3(O_Y) -> H^4(K_Z)` connecting representative for the 18 entries, then
the Serre-tail lift, execute B1208's preregistered fork, and quotient the Higgs
line in OA-C1150. This is the
highest-leverage finite value-arm computation because it can show the lepton
cut is identical, independent or absent. This lane can establish or kill one
explicit charged-spectrum witness. It cannot by itself remove the
already proved nonuniqueness of the heterotic realization functor.

### Phase E — universal arithmetic and selector questions

Attack OA-C1115 at one normalized local prime and stop on the first exact
counterexample. Derive OA-C1074 by the argument principle with the full Gamma
and conductor terms. Treat OA-C1156 as a uniqueness theorem: an invariant prior
is not a selected point. Keep OA-C1168 separate: abundance of necessary-type
relations is not sufficiency for an operational observer. It closes only with a
typed dynamics/feedback/measurement construction or a counterexample.

### Phase F — physics completion audit

Only after A--E, re-audit the six parent gates: 4d functor/action, chiral index
three, vacuum, complete spectrum, dynamics and normalized values. If any parent
no-go remains, the honest end product is a theorem of underdetermination plus a
minimal typed input list, not a claimed TOE. OA-C1157 then builds the separate
cosmology specification; it may not borrow a Big Bang, inflation or dark matter
from analogies.

## Reproduction

```text
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r023_wave6_outside_hostile.py
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r023_b1196_generation_obstruction.py
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r023_b1197_clock_coherence.py
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r023_b1199_selection_recheck.py
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r024_lepton_character_datum.py
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r025_lepton_tail_selection/lepton_tail_selection.py
PYTHONDONTWRITEBYTECODE=1 python3 certificates/r026_yukawa_determinant_frame/determinant_frame.py
PYTHONDONTWRITEBYTECODE=1 python3 documents/program-question-map/validate.py
PYTHONDONTWRITEBYTECODE=1 python3 documents/program-question-map/render.py \
  --source documents/program-question-map/inventory/backbone.json \
  --markdown documents/PROGRAM_QUESTION_ANSWER_MAP.md \
  --as-of 2026-08-29 --check
```
