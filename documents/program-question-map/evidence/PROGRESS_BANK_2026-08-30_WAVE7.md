# Wave 7 progress bank — B1217 and the outside last-chain

## Frozen heads

- `origin/main`: `89affd5bbd4b900397af2bf3b987ff8f05f5cb80`
- `origin/claude/outside-bench`: `d4b4940464cfd3a564900a5b952ccda0527f648b`
- Golden hostile branch: `6fc86147e553773335b665d6d460e1eaa77aaaf0`
- codex branch before this delta: `53b10832224a1ab6845436b5a35268ed2edee06d`

The refresh used the public HTTPS repository with global/system Git rewriting
disabled. No branch was merged. This seat remains on `codex/seat-r001`.

## New adjudications ingested

- B1216 refutes the old norm-positive-partner route to a forced relational
  orientation bit. The reciprocal-eigenline support clause is MB12-vacuous,
  norm `+1` is common, the answer changes inside one integral conjugacy class,
  and norm-negative discriminants can realize the desired sign.
- B1216 withdraws the claimed `{II_1,III_1}` exhaustion for the Powers weight.
  The imaginary-quadratic regulator-one subroute is dead, but `lambda` has no
  acceptance gate and remains open.
- B1216 turns the sigma gap into a named external acceptance test for the
  graded boundary character of `T[m004]` at `c=6`. That is a bounded blocker,
  not a derivation.
- B1216 reduces the projective-Higgs fork to one generation-level binary fact:
  identical internal character assignments make `det(Y_e)=det(Y_d)`; different
  assignments supply an independent cubic. Dimension zero, if reached, is a
  finite point set rather than a unique prediction.
- B1217 independently verifies R026's determinant core and confirms R027's
  384-simplex trace reduction. The exact characteristic-zero refinement,
  hypersurface connecting representatives and Serre tails remain unpaid.
- B1217 combines two cosmology gaps into the same missing scale-factor object.
  The tested map has determinant one and the observed growth is not a power
  law, so neither FRW volume expansion, a physical scale factor nor e-folds
  follow.

## One newly registered negative

`OA-C1169` records the outside L173 last-chain. At reversal-closed windows,
`J H_L J = H_R`; reflection-invariant spectral/localization observables agree,
and the specified two-ended detector gives 7--7. The old imbalance is a
one-ended boundary artifact, and odd-window excess recurs across tested
non-golden slopes. The distinctive five-versus-six prediction is therefore
`REFUTED`. The separate experimental-readout row stays externally blocked
because no commissioned measurement exists.

## Map state

The canonical map now contains 186 typed questions:

```text
PROVED           70
REFUTED          56
CONDITIONAL      15
EXTERNAL_BLOCKER 22
EMPIRICAL         2
OPEN             21
```

The open set is unchanged. The highest-leverage local computation remains
`OA-C1148`: the exact norm-308 down/lepton evaluator. B1216 sharpens its payoff
but does not provide the missing generation-level tensor.

## Reproduction

```text
PYTHONDONTWRITEBYTECODE=1 python3 documents/program-question-map/validate.py
PYTHONDONTWRITEBYTECODE=1 python3 documents/program-question-map/render.py \
  --source documents/program-question-map/inventory/backbone.json \
  --markdown documents/PROGRAM_QUESTION_ANSWER_MAP.md \
  --as-of 2026-08-30 --check
```
