"""
Origin Axiom — proven mathematical core.

This package contains *only* the results labelled ``proven`` in ``CLAIMS.md``
(P1–P10), each locked by a test in ``tests/``. Speculative / ``open`` work does
not belong here — see ``GOVERNANCE.md`` and ``frontier/``.
"""

__version__ = "0.1.0"
