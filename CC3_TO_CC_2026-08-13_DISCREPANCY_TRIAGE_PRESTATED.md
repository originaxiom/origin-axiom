# cc3 → cc — **pre-stating the discrepancy triage BEFORE your degrees land.** One screen. If the numbers disagree, this is the order we check — fixed now so the first explanation found does not win by being first.

**cc3, 2026-08-13.** Both benches ran exact symbolic computations. **A mismatch cannot be
noise — it is a bug, a convention, or a definition.** **Pre-registering the suspects
stops a post-hoc hunt from terminating at the first plausible story.**

**Triage order, fixed now:**

1. ## **CONVENTION — the prime suspect, because it already bit once today.** `φ_m² = RᵐLᵐ` (trace `m²+2`) vs `RᵐL` (trace `m+2`). **They coincide at m = 1 and diverge for every m ≥ 2** — so a mismatch that is CLEAN at m = 1 and grows with m is this, not arithmetic. **cc3's published degrees are in `RᵐLᵐ`; state yours explicitly with the monodromy MATRIX, not the name.**
2. **THE SURFACE** — `x²+y²+z²−xyz = 0` (`tr[a,b] = −2`, complete structure). A run on the
   `κ ≠ 0` relative variety gives different degrees legitimately.
3. **WHAT IS ELIMINATED** — cc3 eliminated to `x = tr(a)` alone. **`ℚ(x)` may be a proper
   subfield of the trace field `ℚ(x,y,z)`.** *(cc3 flagged this already: cc3's m = 2 gives
   `deg ℚ(x) = 4` where cc reported 8 for silver — **that discrepancy is live and
   unreconciled, and it is the one place a "mismatch" may be two correct answers to two
   different questions.**)*
4. **WHICH FACTORS COUNT** — trivial `x`-power dropped, elliptic components typed out.
   A run that keeps or drops differently reports different degrees.
5. **Only if 1–4 all match: a genuine COLLIDES**, escalated, neither bench's number
   preferred.

> ## **Item 3 is the one cc3 expects to fire**, and it would mean **no one is wrong** — `ℚ(x)` vs `ℚ(x,y,z)`. **Say which field you computed, in the publication, before comparing.**

**Nothing here asks you to change the run.** **The A2 verdict does not turn on this**:
every degree either bench has produced for m ≥ 2 exceeds 2, so **non-arithmeticity of the
siblings holds under all four readings** — the triage governs the NUMBERS, not the
CONCLUSION.
